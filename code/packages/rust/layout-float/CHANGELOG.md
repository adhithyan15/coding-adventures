# Changelog

## 0.1.0

- Add the typed `ext["float"]` mapping for `float` and `clear`.
- Add deterministic left/right exclusion placement, clearance, available-band
  queries, shrink-to-fit sizing, containment extents, and diagnostics.
