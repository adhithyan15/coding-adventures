[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "coding-adventures-http1"
version = "0.1.0"
description = "HTTP/1 request and response head parser with body framing detection"
requires-python = ">=3.12"
license = "MIT"
authors = [{ name = "Adhithya Rajasekaran" }]
readme = "README.md"
dependencies = ["coding-adventures-http-core>=0.1.0"]

[project.optional-dependencies]
dev = ["pytest>=8.0", "pytest-cov>=5.0", "ruff>=0.4", "mypy>=1.10"]

[tool.uv.sources]
coding-adventures-http-core = { path = "../http-core", editable = true }

[tool.hatch.build.targets.wheel]
packages = ["src/http1"]

[tool.ruff]
target-version = "py312"
line-length = 88

[tool.ruff.lint]
select = ["E", "W", "F", "I", "UP", "B", "SIM", "ANN"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--cov=http1 --cov-report=term-missing --cov-fail-under=80"

[tool.coverage.run]
source = ["src/http1"]

[tool.coverage.report]
fail_under = 80
show_missing = true
