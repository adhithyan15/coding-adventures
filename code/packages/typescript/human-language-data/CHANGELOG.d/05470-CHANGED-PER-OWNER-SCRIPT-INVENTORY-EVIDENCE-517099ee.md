### Changed — Per-owner script inventory evidence

- Replaced mutable whole-script inventory digests with exact per-glyph and
  per-mark static evidence, keeping concurrent additions on disjoint paths.
