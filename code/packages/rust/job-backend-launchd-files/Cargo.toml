[package]
name = "job-backend-launchd-files"
version = "0.1.0"
edition = "2021"
description = "launchd backend file generation for native job installation"

[dependencies]
job-core = { path = "../job-core" }
