[package]
name = "cas-fourier"
version = "0.1.0"
edition = "2021"
description = "Table-driven symbolic Fourier and inverse Fourier transforms over symbolic IR"
license = "MIT"

[dependencies]
symbolic-ir = { path = "../symbolic-ir" }
