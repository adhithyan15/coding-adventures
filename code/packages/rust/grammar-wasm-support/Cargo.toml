[package]
name = "grammar-wasm-support"
version = "0.1.0"
edition = "2021"
description = "Shared JSON serialization helpers for grammar-backed WASM wrappers"
license = "MIT"

[dependencies]
lexer = { path = "../lexer" }
parser = { path = "../parser" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
