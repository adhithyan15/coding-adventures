# Changelog

## 0.1.0

- Add authenticated, batched Home Assistant historical-state collection.
- Add deterministic state-to-device-event planning with source diagnostics.
- Add idempotent import into durable D23 replay history while preserving the
  topology export's current state.
- Add dry-run and applied artifacts, atomic CLI output, and real WebSocket
  protocol/process tests.
