# minify_tagged_subst — lexer does not yet support template substitution `${...}`

See `input/a.js` for input and `expected.stdout` for the
upstream Closure v20240317 WHITESPACE_ONLY output.

Captured by CLOC14.9. Verdict: IGNORED (gap-044)
