[package]
name = "diagram-layout-board"
version = "0.1.0"
edition = "2021"
description = "Deterministic backend-neutral layout for board diagrams"

[dependencies]
diagram-ir = { path = "../diagram-ir" }
