# Changelog

## 0.1.0

- Add fragment-preserving canonical bookmark URLs and ordered bookmark entries.
- Add duplicate-safe catalog construction, add/update/remove/toggle semantics,
  an object-safe repository contract, and persist-before-commit transactions.
- Add an in-memory repository for embedders and deterministic tests.
