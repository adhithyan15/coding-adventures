---
category: Swift
---

# `GrammarLexer` emits `KEYWORD` for all keywords

with the actual word in `value`. Swift lexer wrappers must promote: map `KEYWORD` to `token.value.uppercased()` as the type.
