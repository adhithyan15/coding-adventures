package com.codingadventures.tomlparser;

/**
 * TomlParser — TOML parser - parses TOML source text using the grammar-driven parser infrastructure
 *
 */
public final class TomlParser {
    public String ping() {
        return "toml-parser";
    }
}
