# Kotlin Directed Graph

Native Kotlin implementation of the directed graph package.

It provides:

- `Graph` for standard directed graphs
- `LabeledDirectedGraph` for labeled edges
- cycle detection and topological sorting
- transitive reachability helpers
- shared traversal behavior via the Kotlin `graph` package
