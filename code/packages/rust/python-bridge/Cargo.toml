[package]
name = "python-bridge"
version = "0.1.0"
edition = "2021"
description = "Thin safe Rust wrapper over Python's C API — no PyO3, no macros, no magic"

[dependencies]
pyo3-ffi = { version = "0.23", features = ["extension-module"] }
