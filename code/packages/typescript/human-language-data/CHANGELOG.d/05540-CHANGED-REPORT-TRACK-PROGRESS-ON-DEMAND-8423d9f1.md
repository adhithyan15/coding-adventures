### Changed

- Retired the 23 tracked per-language progress cards. The same registry-ordered
  lesson, curriculum, and book facts now render through a read-only on-demand
  report, while CI rejects any source-tree progress projection that returns.
