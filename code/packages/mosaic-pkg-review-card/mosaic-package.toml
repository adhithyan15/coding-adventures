[package]
name = "mosaic-pkg-review-card"
version = "0.1.0"
description = "A reusable spaced-repetition review card component built on the Mosaic UI pipeline"
license = "MIT OR Apache-2.0"

[components]
exports = ["ReviewCard"]

[dependencies]
# No userland component dependencies yet. The first slice uses only UI29 kernel
# primitives so every backend can compile it today.

[kernel]
version = "1"
