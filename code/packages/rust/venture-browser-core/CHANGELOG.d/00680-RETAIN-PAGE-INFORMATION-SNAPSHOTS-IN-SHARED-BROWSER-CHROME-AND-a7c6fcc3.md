- Retain Page Information snapshots in shared browser chrome and
  project a deterministic visible title, requested/final address, HTTP status,
  resource summary, and close lifecycle through every generated Mosaic host;
  preserve the existing typed host effect for richer platform presenters and
  clear stale panel metadata when the retained session synchronizes.
