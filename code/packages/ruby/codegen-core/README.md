# coding_adventures_codegen_core

Backend registry for LANG InterpreterIR. The initial Ruby implementation emits
portable textual artifacts for the pure VM, JVM, CLR, and WASM targets so every
frontend can use one lowering path.
