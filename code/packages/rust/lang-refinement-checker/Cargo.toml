[package]
name = "lang-refinement-checker"
version = "0.2.0"
edition = "2021"
description = "LANG23 PRs 23-C + 23-D — refinement obligation checker (per-binding + function-scope CFG-based path-sensitive analysis)"

[dependencies]
constraint-core = { path = "../constraint-core" }
constraint-engine = { path = "../constraint-engine" }
constraint-vm = { path = "../constraint-vm" }
lang-refined-types = { path = "../lang-refined-types" }
