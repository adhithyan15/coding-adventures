[package]
name = "smart-home-data-governance"
version = "0.4.0"
edition = "2021"
description = "Bounded host-owned privacy, consent, identifier, presence, and telemetry-egress policy for D23 smart-home integrations"
license = "MIT"

[dependencies]
smart-home-core = { path = "../smart-home-core" }
url-parser = { path = "../url-parser" }
