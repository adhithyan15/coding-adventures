[project]
name = "hello-world"
version = "0.1.0"
description = "The first program — Hello, World!"
requires-python = ">=3.12"

[tool.ruff]
target-version = "py312"
line-length = 88

[tool.ruff.lint]
select = ["E", "W", "F", "I", "UP", "B", "SIM", "ANN"]
