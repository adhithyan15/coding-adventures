{
  "$schema": "https://raw.githubusercontent.com/adhithyan15/coding-adventures/main/code/specs/schemas/required_capabilities.schema.json",
  "version": 1,
  "package": "rust/compute-runtime",
  "capabilities": [
    {
      "category": "ffi",
      "action": "call",
      "target": "internal unsafe pointer operations",
      "justification": "Uses unsafe blocks for raw pointer manipulation in GPU compute memory management and command queue execution."
    }
  ],
  "justification": "GPU compute runtime simulator. Uses unsafe for internal raw pointer operations in memory management and command queue processing."
}
