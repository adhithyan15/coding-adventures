## 0.285.0

- Let bounded `while`-element control analysis consume statically selected integer and real control updates from recursively supported loop bodies while preserving the existing dynamic-selector, extra-write, overflow, finiteness, and iteration-cap guards.
