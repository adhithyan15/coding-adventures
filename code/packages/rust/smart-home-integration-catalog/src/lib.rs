//! First-party smart-home integration catalog model and seed entries.
//!
//! The catalog is intentionally pure data. It lets D23 runtime packages and
//! D18D tools answer "what can this system support?" without starting workers,
//! opening sockets, reading secrets, or probing the local network.

#![forbid(unsafe_code)]

use smart_home_core::{
    CapabilityId, EntityKind, IntegrationId, PrivilegeTier, ProtocolFamily, RuntimeKind,
    ToolDescriptor, ToolSideEffects,
};
use std::{
    cmp::Ordering,
    collections::{BTreeMap, BTreeSet},
};

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationCategory {
    ProtocolStandard,
    LocalHub,
    LocalDevice,
    BluetoothProfile,
    CloudHub,
    CameraMedia,
    EnergyClimate,
    NotificationChannel,
    DataService,
    HelperCalculated,
    VirtualAlias,
    SystemHardware,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum ConnectivityClass {
    LocalPush,
    LocalPolling,
    CloudPush,
    CloudPolling,
    Calculated,
    AssumedState,
}

impl ConnectivityClass {
    pub fn as_home_assistant_iot_class(self) -> &'static str {
        match self {
            Self::LocalPush => "local_push",
            Self::LocalPolling => "local_polling",
            Self::CloudPush => "cloud_push",
            Self::CloudPolling => "cloud_polling",
            Self::Calculated => "calculated",
            Self::AssumedState => "assumed_state",
        }
    }

    pub fn is_local(self) -> bool {
        matches!(self, Self::LocalPush | Self::LocalPolling)
    }

    pub fn requires_cloud(self) -> bool {
        matches!(self, Self::CloudPush | Self::CloudPolling)
    }

    pub fn is_push(self) -> bool {
        matches!(self, Self::LocalPush | Self::CloudPush)
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum DiscoveryMechanism {
    Mdns,
    WsDiscovery,
    Ssdp,
    UdpMulticast,
    UdpBroadcast,
    Bluetooth,
    Usb,
    Dhcp,
    Mqtt,
    Manual,
    CloudAccount,
    Webhook,
    FileConfig,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum AuthMode {
    None,
    LocalPairing,
    LocalToken,
    UsernamePassword,
    OAuth2,
    ApiKey,
    Certificate,
    RadioNetworkKey,
    MqttCredentials,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum ImplementationStatus {
    Cataloged,
    Specified,
    Scaffolded,
    Simulated,
    FirstPartyRuntime,
    ProductionReady,
    DelegatedToStandard,
    Unsupported,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum PrimitiveFamily {
    NormalizedModel,
    DiscoveryIndex,
    Mdns,
    WsDiscovery,
    Ssdp,
    Udp,
    Dhcp,
    LocalHttp,
    WebSocket,
    ServerSentEvents,
    Mqtt,
    BluetoothLowEnergy,
    Usb,
    SerialController,
    Radio802154,
    ZWaveSerialApi,
    MatterCommissioning,
    HomeKitPairing,
    CloudApi,
    Webhook,
    OAuth2,
    LocalPairing,
    LocalToken,
    CertificatePairing,
    RadioNetworkKey,
    MqttCredentials,
    CameraMedia,
    EnergyTelemetry,
    CalculatedState,
    CommandMapping,
    CapabilityPolicy,
    VaultLease,
    Supervision,
    TestSimulator,
}

impl PrimitiveFamily {
    pub fn as_str(self) -> &'static str {
        match self {
            Self::NormalizedModel => "normalized_model",
            Self::DiscoveryIndex => "discovery_index",
            Self::Mdns => "mdns",
            Self::WsDiscovery => "ws_discovery",
            Self::Ssdp => "ssdp",
            Self::Udp => "udp",
            Self::Dhcp => "dhcp",
            Self::LocalHttp => "local_http",
            Self::WebSocket => "websocket",
            Self::ServerSentEvents => "server_sent_events",
            Self::Mqtt => "mqtt",
            Self::BluetoothLowEnergy => "bluetooth_low_energy",
            Self::Usb => "usb",
            Self::SerialController => "serial_controller",
            Self::Radio802154 => "radio_802154",
            Self::ZWaveSerialApi => "zwave_serial_api",
            Self::MatterCommissioning => "matter_commissioning",
            Self::HomeKitPairing => "homekit_pairing",
            Self::CloudApi => "cloud_api",
            Self::Webhook => "webhook",
            Self::OAuth2 => "oauth2",
            Self::LocalPairing => "local_pairing",
            Self::LocalToken => "local_token",
            Self::CertificatePairing => "certificate_pairing",
            Self::RadioNetworkKey => "radio_network_key",
            Self::MqttCredentials => "mqtt_credentials",
            Self::CameraMedia => "camera_media",
            Self::EnergyTelemetry => "energy_telemetry",
            Self::CalculatedState => "calculated_state",
            Self::CommandMapping => "command_mapping",
            Self::CapabilityPolicy => "capability_policy",
            Self::VaultLease => "vault_lease",
            Self::Supervision => "supervision",
            Self::TestSimulator => "test_simulator",
        }
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationPolicySurface {
    LocalActuation,
    EntryAccess,
    ClimateControl,
    CameraMedia,
    EnergyManagement,
    CredentialLease,
    CredentialedCloud,
    RadioNetworkManagement,
    NetworkInfrastructure,
}

impl IntegrationPolicySurface {
    pub fn as_str(self) -> &'static str {
        match self {
            Self::LocalActuation => "local_actuation",
            Self::EntryAccess => "entry_access",
            Self::ClimateControl => "climate_control",
            Self::CameraMedia => "camera_media",
            Self::EnergyManagement => "energy_management",
            Self::CredentialLease => "credential_lease",
            Self::CredentialedCloud => "credentialed_cloud",
            Self::RadioNetworkManagement => "radio_network_management",
            Self::NetworkInfrastructure => "network_infrastructure",
        }
    }

    pub fn required_tier(self) -> PrivilegeTier {
        match self {
            Self::EntryAccess => PrivilegeTier::HighRisk,
            Self::LocalActuation => PrivilegeTier::LowRisk,
            Self::ClimateControl
            | Self::CameraMedia
            | Self::EnergyManagement
            | Self::CredentialLease
            | Self::CredentialedCloud
            | Self::RadioNetworkManagement
            | Self::NetworkInfrastructure => PrivilegeTier::HumanApproval,
        }
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationCatalogTool {
    ListIntegrations,
    DescribeIntegration,
    ListPrimitives,
    DescribePrimitive,
}

impl IntegrationCatalogTool {
    pub fn tool_id(self) -> &'static str {
        match self {
            Self::ListIntegrations => "smart_home.list_integrations",
            Self::DescribeIntegration => "smart_home.describe_integration",
            Self::ListPrimitives => "smart_home.list_primitives",
            Self::DescribePrimitive => "smart_home.describe_primitive",
        }
    }

    pub fn descriptor(self) -> ToolDescriptor {
        read_catalog_tool(self.tool_id())
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub struct PrimitiveFamilyDescriptor {
    pub primitive: PrimitiveFamily,
    pub display_name: &'static str,
    pub summary: &'static str,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct SourceReference {
    pub label: String,
    pub url: String,
    pub external_id: Option<String>,
}

impl SourceReference {
    pub fn home_assistant(domain: &'static str) -> Self {
        Self {
            label: "Home Assistant".to_string(),
            url: format!("https://www.home-assistant.io/integrations/{domain}/"),
            external_id: Some(domain.to_string()),
        }
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum EcosystemSurveyPlatform {
    HomeAssistant,
    Hubitat,
    HomeyPro,
    SmartThings,
    OpenHab,
    Homebridge,
    IoBroker,
    Domoticz,
    Jeedom,
    HomeSeer,
    AppleHome,
    GoogleHome,
    AmazonAlexa,
    ZWaveAlliance,
    ThreadGroup,
}

impl EcosystemSurveyPlatform {
    pub fn as_str(self) -> &'static str {
        match self {
            Self::HomeAssistant => "home_assistant",
            Self::Hubitat => "hubitat",
            Self::HomeyPro => "homey_pro",
            Self::SmartThings => "smartthings",
            Self::OpenHab => "openhab",
            Self::Homebridge => "homebridge",
            Self::IoBroker => "iobroker",
            Self::Domoticz => "domoticz",
            Self::Jeedom => "jeedom",
            Self::HomeSeer => "homeseer",
            Self::AppleHome => "apple_home",
            Self::GoogleHome => "google_home",
            Self::AmazonAlexa => "amazon_alexa",
            Self::ZWaveAlliance => "zwave_alliance",
            Self::ThreadGroup => "thread_group",
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct EcosystemSurveySource {
    pub platform: EcosystemSurveyPlatform,
    pub display_name: &'static str,
    pub source_url: &'static str,
    pub source_surface: &'static str,
    pub contributes: &'static str,
    pub primitive_hints: Vec<PrimitiveFamily>,
}

impl EcosystemSurveySource {
    pub fn requires_primitive(&self, primitive: PrimitiveFamily) -> bool {
        self.primitive_hints.contains(&primitive)
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct EcosystemPrimitiveCoverage {
    pub primitive: PrimitiveFamily,
    pub platforms: Vec<EcosystemSurveyPlatform>,
    pub source_count: usize,
}

impl EcosystemPrimitiveCoverage {
    pub fn platform_count(&self) -> usize {
        self.platforms.len()
    }

    pub fn is_gap(&self) -> bool {
        self.source_count == 0
    }

    pub fn covers_platform(&self, platform: EcosystemSurveyPlatform) -> bool {
        self.platforms.contains(&platform)
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationMeshProtocolSubstrateStage {
    Controller,
    Radio,
    Discovery,
    NetworkSecurity,
    Supervision,
}

impl IntegrationMeshProtocolSubstrateStage {
    pub fn as_str(self) -> &'static str {
        match self {
            Self::Controller => "controller",
            Self::Radio => "radio",
            Self::Discovery => "discovery",
            Self::NetworkSecurity => "network_security",
            Self::Supervision => "supervision",
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshProtocolSubstrateStageRow {
    pub protocol: ProtocolFamily,
    pub primitive: PrimitiveFamily,
    pub stage: IntegrationMeshProtocolSubstrateStage,
    pub available: bool,
}

impl IntegrationMeshProtocolSubstrateStageRow {
    pub fn is_ready(&self) -> bool {
        self.available
    }

    pub fn is_blocked(&self) -> bool {
        !self.available
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshProtocolSubstrateStageSummary {
    pub total_rows: usize,
    pub ready_rows: usize,
    pub blocked_rows: usize,
    pub unique_required_stages: Vec<IntegrationMeshProtocolSubstrateStage>,
    pub unique_blocked_stages: Vec<IntegrationMeshProtocolSubstrateStage>,
    pub controller_blockers: usize,
    pub radio_blockers: usize,
    pub discovery_blockers: usize,
    pub network_security_blockers: usize,
    pub supervision_blockers: usize,
    pub first_blocked_protocol: Option<ProtocolFamily>,
    pub first_blocked_stage: Option<IntegrationMeshProtocolSubstrateStage>,
}

impl IntegrationMeshProtocolSubstrateStageSummary {
    pub fn from_rows<'a>(
        rows: impl IntoIterator<Item = &'a IntegrationMeshProtocolSubstrateStageRow>,
    ) -> Self {
        let rows = rows.into_iter().collect::<Vec<_>>();
        let unique_required_stages = rows
            .iter()
            .map(|row| row.stage)
            .collect::<BTreeSet<_>>()
            .into_iter()
            .collect::<Vec<_>>();
        let unique_blocked_stages = rows
            .iter()
            .filter(|row| row.is_blocked())
            .map(|row| row.stage)
            .collect::<BTreeSet<_>>()
            .into_iter()
            .collect::<Vec<_>>();
        let ready_rows = rows.iter().filter(|row| row.is_ready()).count();
        let blocked_rows = rows.len().saturating_sub(ready_rows);
        let first_blocked = rows
            .iter()
            .filter(|row| row.is_blocked())
            .min_by_key(|row| {
                (
                    mesh_protocol_sort_key(&row.protocol),
                    row.stage,
                    row.primitive,
                )
            });

        Self {
            total_rows: rows.len(),
            ready_rows,
            blocked_rows,
            unique_required_stages,
            unique_blocked_stages,
            controller_blockers: mesh_substrate_stage_blockers(
                rows.iter().copied(),
                IntegrationMeshProtocolSubstrateStage::Controller,
            ),
            radio_blockers: mesh_substrate_stage_blockers(
                rows.iter().copied(),
                IntegrationMeshProtocolSubstrateStage::Radio,
            ),
            discovery_blockers: mesh_substrate_stage_blockers(
                rows.iter().copied(),
                IntegrationMeshProtocolSubstrateStage::Discovery,
            ),
            network_security_blockers: mesh_substrate_stage_blockers(
                rows.iter().copied(),
                IntegrationMeshProtocolSubstrateStage::NetworkSecurity,
            ),
            supervision_blockers: mesh_substrate_stage_blockers(
                rows.iter().copied(),
                IntegrationMeshProtocolSubstrateStage::Supervision,
            ),
            first_blocked_protocol: first_blocked.map(|row| row.protocol.clone()),
            first_blocked_stage: first_blocked.map(|row| row.stage),
        }
    }

    pub fn all_ready(&self) -> bool {
        self.total_rows > 0 && self.blocked_rows == 0
    }

    pub fn has_blockers(&self) -> bool {
        self.blocked_rows > 0
    }

    pub fn needs_network_security(&self) -> bool {
        self.network_security_blockers > 0
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshProtocolSubstrateAction {
    pub sequence: usize,
    pub protocol: ProtocolFamily,
    pub primitive: PrimitiveFamily,
    pub stage: IntegrationMeshProtocolSubstrateStage,
}

impl IntegrationMeshProtocolSubstrateAction {
    pub fn from_stage_row(
        sequence: usize,
        row: &IntegrationMeshProtocolSubstrateStageRow,
    ) -> Option<Self> {
        row.is_blocked().then(|| Self {
            sequence,
            protocol: row.protocol.clone(),
            primitive: row.primitive,
            stage: row.stage,
        })
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshProtocolSubstrateActionSummary {
    pub total_actions: usize,
    pub unique_protocols: usize,
    pub unique_primitives: usize,
    pub controller_actions: usize,
    pub radio_actions: usize,
    pub discovery_actions: usize,
    pub network_security_actions: usize,
    pub supervision_actions: usize,
    pub first_protocol: Option<ProtocolFamily>,
    pub first_stage: Option<IntegrationMeshProtocolSubstrateStage>,
    pub first_primitive: Option<PrimitiveFamily>,
}

impl IntegrationMeshProtocolSubstrateActionSummary {
    pub fn from_actions<'a>(
        actions: impl IntoIterator<Item = &'a IntegrationMeshProtocolSubstrateAction>,
    ) -> Self {
        let actions = actions.into_iter().collect::<Vec<_>>();
        let mut protocols = BTreeSet::new();
        let mut primitives = BTreeSet::new();
        let first_action = actions.iter().min_by_key(|action| action.sequence);

        for action in &actions {
            protocols.insert(protocol_sort_token(&action.protocol));
            primitives.insert(action.primitive);
        }

        Self {
            total_actions: actions.len(),
            unique_protocols: protocols.len(),
            unique_primitives: primitives.len(),
            controller_actions: mesh_substrate_action_count(
                actions.iter().copied(),
                IntegrationMeshProtocolSubstrateStage::Controller,
            ),
            radio_actions: mesh_substrate_action_count(
                actions.iter().copied(),
                IntegrationMeshProtocolSubstrateStage::Radio,
            ),
            discovery_actions: mesh_substrate_action_count(
                actions.iter().copied(),
                IntegrationMeshProtocolSubstrateStage::Discovery,
            ),
            network_security_actions: mesh_substrate_action_count(
                actions.iter().copied(),
                IntegrationMeshProtocolSubstrateStage::NetworkSecurity,
            ),
            supervision_actions: mesh_substrate_action_count(
                actions.iter().copied(),
                IntegrationMeshProtocolSubstrateStage::Supervision,
            ),
            first_protocol: first_action.map(|action| action.protocol.clone()),
            first_stage: first_action.map(|action| action.stage),
            first_primitive: first_action.map(|action| action.primitive),
        }
    }

    pub fn is_empty(&self) -> bool {
        self.total_actions == 0
    }

    pub fn has_actions(&self) -> bool {
        self.total_actions > 0
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshProtocolSubstratePreflightCheck {
    pub sequence: usize,
    pub protocol: ProtocolFamily,
    pub primitive: PrimitiveFamily,
    pub stage: IntegrationMeshProtocolSubstrateStage,
    pub required: bool,
    pub passed: bool,
    pub blocking: bool,
    pub operator_required: bool,
}

impl IntegrationMeshProtocolSubstratePreflightCheck {
    pub fn from_stage_row(sequence: usize, row: &IntegrationMeshProtocolSubstrateStageRow) -> Self {
        let operator_required = matches!(
            row.stage,
            IntegrationMeshProtocolSubstrateStage::Radio
                | IntegrationMeshProtocolSubstrateStage::NetworkSecurity
        );

        Self {
            sequence,
            protocol: row.protocol.clone(),
            primitive: row.primitive,
            stage: row.stage,
            required: true,
            passed: row.is_ready(),
            blocking: row.is_blocked(),
            operator_required: operator_required && row.is_blocked(),
        }
    }

    pub fn passed(&self) -> bool {
        self.passed
    }

    pub fn failed(&self) -> bool {
        !self.passed
    }

    pub fn blocks_preflight(&self) -> bool {
        self.blocking
    }

    pub fn requires_operator(&self) -> bool {
        self.operator_required
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshProtocolSubstratePreflightSummary {
    pub total_checks: usize,
    pub passed_checks: usize,
    pub failed_checks: usize,
    pub blocking_checks: usize,
    pub operator_required_checks: usize,
    pub controller_blockers: usize,
    pub radio_blockers: usize,
    pub discovery_blockers: usize,
    pub network_security_blockers: usize,
    pub supervision_blockers: usize,
    pub first_failed_protocol: Option<ProtocolFamily>,
    pub first_failed_stage: Option<IntegrationMeshProtocolSubstrateStage>,
    pub first_failed_primitive: Option<PrimitiveFamily>,
    pub preflight_ready: bool,
}

impl IntegrationMeshProtocolSubstratePreflightSummary {
    pub fn from_checks<'a>(
        checks: impl IntoIterator<Item = &'a IntegrationMeshProtocolSubstratePreflightCheck>,
    ) -> Self {
        let checks = checks.into_iter().collect::<Vec<_>>();
        let passed_checks = checks.iter().filter(|check| check.passed()).count();
        let failed_checks = checks.len().saturating_sub(passed_checks);
        let first_failed = checks
            .iter()
            .filter(|check| check.failed())
            .min_by_key(|check| check.sequence);

        Self {
            total_checks: checks.len(),
            passed_checks,
            failed_checks,
            blocking_checks: checks
                .iter()
                .filter(|check| check.blocks_preflight())
                .count(),
            operator_required_checks: checks
                .iter()
                .filter(|check| check.requires_operator())
                .count(),
            controller_blockers: mesh_substrate_preflight_blockers(
                checks.iter().copied(),
                IntegrationMeshProtocolSubstrateStage::Controller,
            ),
            radio_blockers: mesh_substrate_preflight_blockers(
                checks.iter().copied(),
                IntegrationMeshProtocolSubstrateStage::Radio,
            ),
            discovery_blockers: mesh_substrate_preflight_blockers(
                checks.iter().copied(),
                IntegrationMeshProtocolSubstrateStage::Discovery,
            ),
            network_security_blockers: mesh_substrate_preflight_blockers(
                checks.iter().copied(),
                IntegrationMeshProtocolSubstrateStage::NetworkSecurity,
            ),
            supervision_blockers: mesh_substrate_preflight_blockers(
                checks.iter().copied(),
                IntegrationMeshProtocolSubstrateStage::Supervision,
            ),
            first_failed_protocol: first_failed.map(|check| check.protocol.clone()),
            first_failed_stage: first_failed.map(|check| check.stage),
            first_failed_primitive: first_failed.map(|check| check.primitive),
            preflight_ready: !checks.is_empty() && failed_checks == 0,
        }
    }

    pub fn ready(&self) -> bool {
        self.preflight_ready
    }

    pub fn has_blockers(&self) -> bool {
        self.blocking_checks > 0
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_checks > 0
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationMeshProtocolSubstratePreflightActionKind {
    ProvisionController,
    ProvisionRadio,
    EnableDiscovery,
    InstallNetworkKey,
    EnableSupervision,
}

impl IntegrationMeshProtocolSubstratePreflightActionKind {
    pub fn as_str(self) -> &'static str {
        match self {
            Self::ProvisionController => "provision_controller",
            Self::ProvisionRadio => "provision_radio",
            Self::EnableDiscovery => "enable_discovery",
            Self::InstallNetworkKey => "install_network_key",
            Self::EnableSupervision => "enable_supervision",
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshProtocolSubstratePreflightAction {
    pub sequence: usize,
    pub protocol: ProtocolFamily,
    pub primitive: PrimitiveFamily,
    pub stage: IntegrationMeshProtocolSubstrateStage,
    pub action_kind: IntegrationMeshProtocolSubstratePreflightActionKind,
    pub blocking: bool,
    pub operator_required: bool,
}

impl IntegrationMeshProtocolSubstratePreflightAction {
    pub fn from_check(
        sequence: usize,
        check: &IntegrationMeshProtocolSubstratePreflightCheck,
    ) -> Option<Self> {
        check.failed().then(|| Self {
            sequence,
            protocol: check.protocol.clone(),
            primitive: check.primitive,
            stage: check.stage,
            action_kind: match check.stage {
                IntegrationMeshProtocolSubstrateStage::Controller => {
                    IntegrationMeshProtocolSubstratePreflightActionKind::ProvisionController
                }
                IntegrationMeshProtocolSubstrateStage::Radio => {
                    IntegrationMeshProtocolSubstratePreflightActionKind::ProvisionRadio
                }
                IntegrationMeshProtocolSubstrateStage::Discovery => {
                    IntegrationMeshProtocolSubstratePreflightActionKind::EnableDiscovery
                }
                IntegrationMeshProtocolSubstrateStage::NetworkSecurity => {
                    IntegrationMeshProtocolSubstratePreflightActionKind::InstallNetworkKey
                }
                IntegrationMeshProtocolSubstrateStage::Supervision => {
                    IntegrationMeshProtocolSubstratePreflightActionKind::EnableSupervision
                }
            },
            blocking: check.blocks_preflight(),
            operator_required: check.requires_operator(),
        })
    }

    pub fn blocks_preflight(&self) -> bool {
        self.blocking
    }

    pub fn requires_operator(&self) -> bool {
        self.operator_required
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshProtocolSubstratePreflightActionSummary {
    pub total_actions: usize,
    pub blocking_actions: usize,
    pub operator_required_actions: usize,
    pub unique_protocols: usize,
    pub unique_primitives: usize,
    pub controller_actions: usize,
    pub radio_actions: usize,
    pub discovery_actions: usize,
    pub network_security_actions: usize,
    pub supervision_actions: usize,
    pub first_protocol: Option<ProtocolFamily>,
    pub first_stage: Option<IntegrationMeshProtocolSubstrateStage>,
    pub first_primitive: Option<PrimitiveFamily>,
    pub first_action_kind: Option<IntegrationMeshProtocolSubstratePreflightActionKind>,
    pub preflight_actions_ready: bool,
}

impl IntegrationMeshProtocolSubstratePreflightActionSummary {
    pub fn from_actions<'a>(
        actions: impl IntoIterator<Item = &'a IntegrationMeshProtocolSubstratePreflightAction>,
    ) -> Self {
        let actions = actions.into_iter().collect::<Vec<_>>();
        let mut protocols = BTreeSet::new();
        let mut primitives = BTreeSet::new();
        let first_action = actions.iter().min_by_key(|action| action.sequence);

        for action in &actions {
            protocols.insert(protocol_sort_token(&action.protocol));
            primitives.insert(action.primitive);
        }

        Self {
            total_actions: actions.len(),
            blocking_actions: actions
                .iter()
                .filter(|action| action.blocks_preflight())
                .count(),
            operator_required_actions: actions
                .iter()
                .filter(|action| action.requires_operator())
                .count(),
            unique_protocols: protocols.len(),
            unique_primitives: primitives.len(),
            controller_actions: mesh_substrate_preflight_action_count(
                actions.iter().copied(),
                IntegrationMeshProtocolSubstrateStage::Controller,
            ),
            radio_actions: mesh_substrate_preflight_action_count(
                actions.iter().copied(),
                IntegrationMeshProtocolSubstrateStage::Radio,
            ),
            discovery_actions: mesh_substrate_preflight_action_count(
                actions.iter().copied(),
                IntegrationMeshProtocolSubstrateStage::Discovery,
            ),
            network_security_actions: mesh_substrate_preflight_action_count(
                actions.iter().copied(),
                IntegrationMeshProtocolSubstrateStage::NetworkSecurity,
            ),
            supervision_actions: mesh_substrate_preflight_action_count(
                actions.iter().copied(),
                IntegrationMeshProtocolSubstrateStage::Supervision,
            ),
            first_protocol: first_action.map(|action| action.protocol.clone()),
            first_stage: first_action.map(|action| action.stage),
            first_primitive: first_action.map(|action| action.primitive),
            first_action_kind: first_action.map(|action| action.action_kind),
            preflight_actions_ready: actions.is_empty(),
        }
    }

    pub fn is_empty(&self) -> bool {
        self.total_actions == 0
    }

    pub fn has_actions(&self) -> bool {
        self.total_actions > 0
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_actions > 0
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshProtocolSubstratePreflightRepairBatch {
    pub sequence: usize,
    pub stage: IntegrationMeshProtocolSubstrateStage,
    pub action_kind: IntegrationMeshProtocolSubstratePreflightActionKind,
    pub action_count: usize,
    pub blocking_actions: usize,
    pub operator_required_actions: usize,
    pub protocols: Vec<ProtocolFamily>,
    pub primitives: Vec<PrimitiveFamily>,
    pub first_protocol: Option<ProtocolFamily>,
    pub first_primitive: Option<PrimitiveFamily>,
}

impl IntegrationMeshProtocolSubstratePreflightRepairBatch {
    pub fn from_actions<'a>(
        sequence: usize,
        stage: IntegrationMeshProtocolSubstrateStage,
        action_kind: IntegrationMeshProtocolSubstratePreflightActionKind,
        actions: impl IntoIterator<Item = &'a IntegrationMeshProtocolSubstratePreflightAction>,
    ) -> Self {
        let mut actions = actions.into_iter().collect::<Vec<_>>();
        actions.sort_by_key(|action| action.sequence);
        let first_action = actions.first();
        let mut protocols = Vec::new();
        let mut primitives = Vec::new();

        for action in &actions {
            if !protocols.contains(&action.protocol) {
                protocols.push(action.protocol.clone());
            }
            if !primitives.contains(&action.primitive) {
                primitives.push(action.primitive);
            }
        }

        Self {
            sequence,
            stage,
            action_kind,
            action_count: actions.len(),
            blocking_actions: actions
                .iter()
                .filter(|action| action.blocks_preflight())
                .count(),
            operator_required_actions: actions
                .iter()
                .filter(|action| action.requires_operator())
                .count(),
            protocols,
            primitives,
            first_protocol: first_action.map(|action| action.protocol.clone()),
            first_primitive: first_action.map(|action| action.primitive),
        }
    }

    pub fn blocks_preflight(&self) -> bool {
        self.blocking_actions > 0
    }

    pub fn requires_operator(&self) -> bool {
        self.operator_required_actions > 0
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshProtocolSubstratePreflightRepairBatchSummary {
    pub total_batches: usize,
    pub total_actions: usize,
    pub blocking_batches: usize,
    pub operator_required_batches: usize,
    pub controller_batches: usize,
    pub radio_batches: usize,
    pub discovery_batches: usize,
    pub network_security_batches: usize,
    pub supervision_batches: usize,
    pub first_stage: Option<IntegrationMeshProtocolSubstrateStage>,
    pub first_action_kind: Option<IntegrationMeshProtocolSubstratePreflightActionKind>,
    pub first_protocol: Option<ProtocolFamily>,
    pub first_primitive: Option<PrimitiveFamily>,
    pub repair_batches_ready: bool,
}

impl IntegrationMeshProtocolSubstratePreflightRepairBatchSummary {
    pub fn from_batches<'a>(
        batches: impl IntoIterator<Item = &'a IntegrationMeshProtocolSubstratePreflightRepairBatch>,
    ) -> Self {
        let batches = batches.into_iter().collect::<Vec<_>>();
        let first_batch = batches.iter().min_by_key(|batch| batch.sequence);

        Self {
            total_batches: batches.len(),
            total_actions: batches.iter().map(|batch| batch.action_count).sum(),
            blocking_batches: batches
                .iter()
                .filter(|batch| batch.blocks_preflight())
                .count(),
            operator_required_batches: batches
                .iter()
                .filter(|batch| batch.requires_operator())
                .count(),
            controller_batches: mesh_substrate_preflight_repair_batch_count(
                batches.iter().copied(),
                IntegrationMeshProtocolSubstrateStage::Controller,
            ),
            radio_batches: mesh_substrate_preflight_repair_batch_count(
                batches.iter().copied(),
                IntegrationMeshProtocolSubstrateStage::Radio,
            ),
            discovery_batches: mesh_substrate_preflight_repair_batch_count(
                batches.iter().copied(),
                IntegrationMeshProtocolSubstrateStage::Discovery,
            ),
            network_security_batches: mesh_substrate_preflight_repair_batch_count(
                batches.iter().copied(),
                IntegrationMeshProtocolSubstrateStage::NetworkSecurity,
            ),
            supervision_batches: mesh_substrate_preflight_repair_batch_count(
                batches.iter().copied(),
                IntegrationMeshProtocolSubstrateStage::Supervision,
            ),
            first_stage: first_batch.map(|batch| batch.stage),
            first_action_kind: first_batch.map(|batch| batch.action_kind),
            first_protocol: first_batch.and_then(|batch| batch.first_protocol.clone()),
            first_primitive: first_batch.and_then(|batch| batch.first_primitive),
            repair_batches_ready: batches.is_empty(),
        }
    }

    pub fn is_empty(&self) -> bool {
        self.total_batches == 0
    }

    pub fn has_batches(&self) -> bool {
        self.total_batches > 0
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_batches > 0
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshProtocolSubstratePreflightRepairScheduleSlot {
    pub sequence: usize,
    pub batch_sequence: usize,
    pub stage: IntegrationMeshProtocolSubstrateStage,
    pub action_kind: IntegrationMeshProtocolSubstratePreflightActionKind,
    pub action_count: usize,
    pub blocking_actions: usize,
    pub operator_required_actions: usize,
    pub protocols: Vec<ProtocolFamily>,
    pub primitives: Vec<PrimitiveFamily>,
    pub first_protocol: Option<ProtocolFamily>,
    pub first_primitive: Option<PrimitiveFamily>,
    pub release_blocking: bool,
    pub operator_required: bool,
}

impl IntegrationMeshProtocolSubstratePreflightRepairScheduleSlot {
    pub fn from_batch(
        sequence: usize,
        batch: &IntegrationMeshProtocolSubstratePreflightRepairBatch,
    ) -> Self {
        Self {
            sequence,
            batch_sequence: batch.sequence,
            stage: batch.stage,
            action_kind: batch.action_kind,
            action_count: batch.action_count,
            blocking_actions: batch.blocking_actions,
            operator_required_actions: batch.operator_required_actions,
            protocols: batch.protocols.clone(),
            primitives: batch.primitives.clone(),
            first_protocol: batch.first_protocol.clone(),
            first_primitive: batch.first_primitive,
            release_blocking: batch.blocks_preflight(),
            operator_required: batch.requires_operator(),
        }
    }

    pub fn blocks_preflight(&self) -> bool {
        self.release_blocking
    }

    pub fn requires_operator(&self) -> bool {
        self.operator_required
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshProtocolSubstratePreflightRepairScheduleSummary {
    pub total_slots: usize,
    pub total_actions: usize,
    pub blocking_slots: usize,
    pub operator_required_slots: usize,
    pub controller_slots: usize,
    pub radio_slots: usize,
    pub discovery_slots: usize,
    pub network_security_slots: usize,
    pub supervision_slots: usize,
    pub first_stage: Option<IntegrationMeshProtocolSubstrateStage>,
    pub first_action_kind: Option<IntegrationMeshProtocolSubstratePreflightActionKind>,
    pub first_protocol: Option<ProtocolFamily>,
    pub first_primitive: Option<PrimitiveFamily>,
    pub repair_schedule_ready: bool,
}

impl IntegrationMeshProtocolSubstratePreflightRepairScheduleSummary {
    pub fn from_slots<'a>(
        slots: impl IntoIterator<Item = &'a IntegrationMeshProtocolSubstratePreflightRepairScheduleSlot>,
    ) -> Self {
        let slots = slots.into_iter().collect::<Vec<_>>();
        let first_slot = slots.iter().min_by_key(|slot| slot.sequence);

        Self {
            total_slots: slots.len(),
            total_actions: slots.iter().map(|slot| slot.action_count).sum(),
            blocking_slots: slots.iter().filter(|slot| slot.blocks_preflight()).count(),
            operator_required_slots: slots.iter().filter(|slot| slot.requires_operator()).count(),
            controller_slots: mesh_substrate_preflight_repair_schedule_slot_count(
                slots.iter().copied(),
                IntegrationMeshProtocolSubstrateStage::Controller,
            ),
            radio_slots: mesh_substrate_preflight_repair_schedule_slot_count(
                slots.iter().copied(),
                IntegrationMeshProtocolSubstrateStage::Radio,
            ),
            discovery_slots: mesh_substrate_preflight_repair_schedule_slot_count(
                slots.iter().copied(),
                IntegrationMeshProtocolSubstrateStage::Discovery,
            ),
            network_security_slots: mesh_substrate_preflight_repair_schedule_slot_count(
                slots.iter().copied(),
                IntegrationMeshProtocolSubstrateStage::NetworkSecurity,
            ),
            supervision_slots: mesh_substrate_preflight_repair_schedule_slot_count(
                slots.iter().copied(),
                IntegrationMeshProtocolSubstrateStage::Supervision,
            ),
            first_stage: first_slot.map(|slot| slot.stage),
            first_action_kind: first_slot.map(|slot| slot.action_kind),
            first_protocol: first_slot.and_then(|slot| slot.first_protocol.clone()),
            first_primitive: first_slot.and_then(|slot| slot.first_primitive),
            repair_schedule_ready: slots.is_empty(),
        }
    }

    pub fn is_empty(&self) -> bool {
        self.total_slots == 0
    }

    pub fn has_slots(&self) -> bool {
        self.total_slots > 0
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_slots > 0
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshProtocolSubstratePreflightRepairSlotAuditRow {
    pub sequence: usize,
    pub slot_sequence: usize,
    pub batch_sequence: usize,
    pub stage: IntegrationMeshProtocolSubstrateStage,
    pub action_kind: IntegrationMeshProtocolSubstratePreflightActionKind,
    pub action_count: usize,
    pub protocol_count: usize,
    pub primitive_count: usize,
    pub protocols: Vec<ProtocolFamily>,
    pub primitives: Vec<PrimitiveFamily>,
    pub release_blocking: bool,
    pub operator_required: bool,
}

impl IntegrationMeshProtocolSubstratePreflightRepairSlotAuditRow {
    pub fn from_slot(
        sequence: usize,
        slot: &IntegrationMeshProtocolSubstratePreflightRepairScheduleSlot,
    ) -> Self {
        Self {
            sequence,
            slot_sequence: slot.sequence,
            batch_sequence: slot.batch_sequence,
            stage: slot.stage,
            action_kind: slot.action_kind,
            action_count: slot.action_count,
            protocol_count: slot.protocols.len(),
            primitive_count: slot.primitives.len(),
            protocols: slot.protocols.clone(),
            primitives: slot.primitives.clone(),
            release_blocking: slot.blocks_preflight(),
            operator_required: slot.requires_operator(),
        }
    }

    pub fn blocks_preflight(&self) -> bool {
        self.release_blocking
    }

    pub fn requires_operator(&self) -> bool {
        self.operator_required
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshProtocolSubstratePreflightRepairSlotAuditSummary {
    pub total_rows: usize,
    pub scheduled_actions: usize,
    pub blocking_rows: usize,
    pub operator_required_rows: usize,
    pub protocol_mentions: usize,
    pub primitive_mentions: usize,
    pub first_blocking_sequence: Option<usize>,
    pub first_operator_required_sequence: Option<usize>,
    pub first_stage: Option<IntegrationMeshProtocolSubstrateStage>,
    pub first_action_kind: Option<IntegrationMeshProtocolSubstratePreflightActionKind>,
    pub repair_slot_audit_ready: bool,
}

impl IntegrationMeshProtocolSubstratePreflightRepairSlotAuditSummary {
    pub fn from_rows<'a>(
        rows: impl IntoIterator<Item = &'a IntegrationMeshProtocolSubstratePreflightRepairSlotAuditRow>,
    ) -> Self {
        let rows = rows.into_iter().collect::<Vec<_>>();
        let first_row = rows.iter().min_by_key(|row| row.sequence);
        let first_blocking_sequence = rows
            .iter()
            .filter(|row| row.blocks_preflight())
            .map(|row| row.sequence)
            .min();
        let first_operator_required_sequence = rows
            .iter()
            .filter(|row| row.requires_operator())
            .map(|row| row.sequence)
            .min();

        Self {
            total_rows: rows.len(),
            scheduled_actions: rows.iter().map(|row| row.action_count).sum(),
            blocking_rows: rows.iter().filter(|row| row.blocks_preflight()).count(),
            operator_required_rows: rows.iter().filter(|row| row.requires_operator()).count(),
            protocol_mentions: rows.iter().map(|row| row.protocol_count).sum(),
            primitive_mentions: rows.iter().map(|row| row.primitive_count).sum(),
            first_blocking_sequence,
            first_operator_required_sequence,
            first_stage: first_row.map(|row| row.stage),
            first_action_kind: first_row.map(|row| row.action_kind),
            repair_slot_audit_ready: rows.is_empty(),
        }
    }

    pub fn is_empty(&self) -> bool {
        self.total_rows == 0
    }

    pub fn has_rows(&self) -> bool {
        self.total_rows > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.blocking_rows > 0
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_rows > 0
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionStatus {
    Blocked,
    OperatorHandoff,
}

impl IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionStatus {
    pub fn as_str(self) -> &'static str {
        match self {
            Self::Blocked => "blocked",
            Self::OperatorHandoff => "operator_handoff",
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionTicket {
    pub sequence: usize,
    pub ticket_key: String,
    pub audit_sequence: usize,
    pub slot_sequence: usize,
    pub batch_sequence: usize,
    pub stage: IntegrationMeshProtocolSubstrateStage,
    pub action_kind: IntegrationMeshProtocolSubstratePreflightActionKind,
    pub status: IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionStatus,
    pub action_count: usize,
    pub protocol_count: usize,
    pub primitive_count: usize,
    pub protocols: Vec<ProtocolFamily>,
    pub primitives: Vec<PrimitiveFamily>,
    pub release_blocking: bool,
    pub operator_required: bool,
}

impl IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionTicket {
    pub fn from_audit_row(
        sequence: usize,
        row: &IntegrationMeshProtocolSubstratePreflightRepairSlotAuditRow,
    ) -> Self {
        let status = if row.requires_operator() {
            IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionStatus::OperatorHandoff
        } else {
            IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionStatus::Blocked
        };

        Self {
            sequence,
            ticket_key: format!(
                "slot-{sequence:02}-{}-{}",
                row.stage.as_str(),
                row.action_kind.as_str()
            ),
            audit_sequence: row.sequence,
            slot_sequence: row.slot_sequence,
            batch_sequence: row.batch_sequence,
            stage: row.stage,
            action_kind: row.action_kind,
            status,
            action_count: row.action_count,
            protocol_count: row.protocol_count,
            primitive_count: row.primitive_count,
            protocols: row.protocols.clone(),
            primitives: row.primitives.clone(),
            release_blocking: row.blocks_preflight(),
            operator_required: row.requires_operator(),
        }
    }

    pub fn blocks_preflight(&self) -> bool {
        self.release_blocking
    }

    pub fn requires_operator(&self) -> bool {
        self.operator_required
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionTicketSummary {
    pub total_tickets: usize,
    pub scheduled_actions: usize,
    pub blocking_tickets: usize,
    pub operator_required_tickets: usize,
    pub protocol_mentions: usize,
    pub primitive_mentions: usize,
    pub first_ticket_key: Option<String>,
    pub first_blocking_ticket_key: Option<String>,
    pub first_operator_ticket_key: Option<String>,
    pub first_stage: Option<IntegrationMeshProtocolSubstrateStage>,
    pub first_action_kind: Option<IntegrationMeshProtocolSubstratePreflightActionKind>,
    pub execution_tickets_ready: bool,
}

impl IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionTicketSummary {
    pub fn from_tickets<'a>(
        tickets: impl IntoIterator<
            Item = &'a IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionTicket,
        >,
    ) -> Self {
        let tickets = tickets.into_iter().collect::<Vec<_>>();
        let first_ticket = tickets.iter().min_by_key(|ticket| ticket.sequence);
        let first_blocking_ticket = tickets
            .iter()
            .filter(|ticket| ticket.blocks_preflight())
            .min_by_key(|ticket| ticket.sequence);
        let first_operator_ticket = tickets
            .iter()
            .filter(|ticket| ticket.requires_operator())
            .min_by_key(|ticket| ticket.sequence);

        Self {
            total_tickets: tickets.len(),
            scheduled_actions: tickets.iter().map(|ticket| ticket.action_count).sum(),
            blocking_tickets: tickets
                .iter()
                .filter(|ticket| ticket.blocks_preflight())
                .count(),
            operator_required_tickets: tickets
                .iter()
                .filter(|ticket| ticket.requires_operator())
                .count(),
            protocol_mentions: tickets.iter().map(|ticket| ticket.protocol_count).sum(),
            primitive_mentions: tickets.iter().map(|ticket| ticket.primitive_count).sum(),
            first_ticket_key: first_ticket.map(|ticket| ticket.ticket_key.clone()),
            first_blocking_ticket_key: first_blocking_ticket
                .map(|ticket| ticket.ticket_key.clone()),
            first_operator_ticket_key: first_operator_ticket
                .map(|ticket| ticket.ticket_key.clone()),
            first_stage: first_ticket.map(|ticket| ticket.stage),
            first_action_kind: first_ticket.map(|ticket| ticket.action_kind),
            execution_tickets_ready: tickets.is_empty(),
        }
    }

    pub fn is_empty(&self) -> bool {
        self.total_tickets == 0
    }

    pub fn has_tickets(&self) -> bool {
        self.total_tickets > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.blocking_tickets > 0
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_tickets > 0
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionWorkOrder {
    pub sequence: usize,
    pub work_order_key: String,
    pub ticket_sequence: usize,
    pub ticket_key: String,
    pub slot_sequence: usize,
    pub batch_sequence: usize,
    pub stage: IntegrationMeshProtocolSubstrateStage,
    pub action_kind: IntegrationMeshProtocolSubstratePreflightActionKind,
    pub status: IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionStatus,
    pub action_count: usize,
    pub protocol_count: usize,
    pub primitive_count: usize,
    pub protocols: Vec<ProtocolFamily>,
    pub primitives: Vec<PrimitiveFamily>,
    pub release_blocking: bool,
    pub operator_required: bool,
}

impl IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionWorkOrder {
    pub fn from_ticket(
        sequence: usize,
        ticket: &IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionTicket,
    ) -> Self {
        Self {
            sequence,
            work_order_key: format!(
                "work-{sequence:02}-{}-{}",
                ticket.stage.as_str(),
                ticket.action_kind.as_str()
            ),
            ticket_sequence: ticket.sequence,
            ticket_key: ticket.ticket_key.clone(),
            slot_sequence: ticket.slot_sequence,
            batch_sequence: ticket.batch_sequence,
            stage: ticket.stage,
            action_kind: ticket.action_kind,
            status: ticket.status,
            action_count: ticket.action_count,
            protocol_count: ticket.protocol_count,
            primitive_count: ticket.primitive_count,
            protocols: ticket.protocols.clone(),
            primitives: ticket.primitives.clone(),
            release_blocking: ticket.blocks_preflight(),
            operator_required: ticket.requires_operator(),
        }
    }

    pub fn blocks_preflight(&self) -> bool {
        self.release_blocking
    }

    pub fn requires_operator(&self) -> bool {
        self.operator_required
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionWorkOrderSummary {
    pub total_work_orders: usize,
    pub scheduled_actions: usize,
    pub blocking_work_orders: usize,
    pub operator_required_work_orders: usize,
    pub protocol_mentions: usize,
    pub primitive_mentions: usize,
    pub first_work_order_key: Option<String>,
    pub first_blocking_work_order_key: Option<String>,
    pub first_operator_work_order_key: Option<String>,
    pub first_ticket_key: Option<String>,
    pub first_stage: Option<IntegrationMeshProtocolSubstrateStage>,
    pub first_action_kind: Option<IntegrationMeshProtocolSubstratePreflightActionKind>,
    pub execution_work_orders_ready: bool,
}

impl IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionWorkOrderSummary {
    pub fn from_work_orders<'a>(
        work_orders: impl IntoIterator<
            Item = &'a IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionWorkOrder,
        >,
    ) -> Self {
        let work_orders = work_orders.into_iter().collect::<Vec<_>>();
        let first_work_order = work_orders.iter().min_by_key(|order| order.sequence);
        let first_blocking_work_order = work_orders
            .iter()
            .filter(|order| order.blocks_preflight())
            .min_by_key(|order| order.sequence);
        let first_operator_work_order = work_orders
            .iter()
            .filter(|order| order.requires_operator())
            .min_by_key(|order| order.sequence);

        Self {
            total_work_orders: work_orders.len(),
            scheduled_actions: work_orders.iter().map(|order| order.action_count).sum(),
            blocking_work_orders: work_orders
                .iter()
                .filter(|order| order.blocks_preflight())
                .count(),
            operator_required_work_orders: work_orders
                .iter()
                .filter(|order| order.requires_operator())
                .count(),
            protocol_mentions: work_orders.iter().map(|order| order.protocol_count).sum(),
            primitive_mentions: work_orders.iter().map(|order| order.primitive_count).sum(),
            first_work_order_key: first_work_order.map(|order| order.work_order_key.clone()),
            first_blocking_work_order_key: first_blocking_work_order
                .map(|order| order.work_order_key.clone()),
            first_operator_work_order_key: first_operator_work_order
                .map(|order| order.work_order_key.clone()),
            first_ticket_key: first_work_order.map(|order| order.ticket_key.clone()),
            first_stage: first_work_order.map(|order| order.stage),
            first_action_kind: first_work_order.map(|order| order.action_kind),
            execution_work_orders_ready: work_orders.is_empty(),
        }
    }

    pub fn is_empty(&self) -> bool {
        self.total_work_orders == 0
    }

    pub fn has_work_orders(&self) -> bool {
        self.total_work_orders > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.blocking_work_orders > 0
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_work_orders > 0
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionWorkOrderGuardrailKind {
    ReleaseBlocker,
    OperatorHandoff,
    ReadyToExecute,
}

impl IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionWorkOrderGuardrailKind {
    pub fn as_str(self) -> &'static str {
        match self {
            Self::ReleaseBlocker => "release_blocker",
            Self::OperatorHandoff => "operator_handoff",
            Self::ReadyToExecute => "ready_to_execute",
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionWorkOrderGuardrail {
    pub sequence: usize,
    pub work_order_sequence: usize,
    pub work_order_key: String,
    pub ticket_key: String,
    pub stage: IntegrationMeshProtocolSubstrateStage,
    pub action_kind: IntegrationMeshProtocolSubstratePreflightActionKind,
    pub guardrail_kind:
        IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionWorkOrderGuardrailKind,
    pub action_count: usize,
    pub protocol_count: usize,
    pub primitive_count: usize,
    pub release_blocking: bool,
    pub operator_required: bool,
}

impl IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionWorkOrderGuardrail {
    pub fn from_work_order(
        sequence: usize,
        work_order: &IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionWorkOrder,
    ) -> Self {
        let guardrail_kind = if work_order.requires_operator() {
            IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionWorkOrderGuardrailKind::OperatorHandoff
        } else if work_order.blocks_preflight() {
            IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionWorkOrderGuardrailKind::ReleaseBlocker
        } else {
            IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionWorkOrderGuardrailKind::ReadyToExecute
        };

        Self {
            sequence,
            work_order_sequence: work_order.sequence,
            work_order_key: work_order.work_order_key.clone(),
            ticket_key: work_order.ticket_key.clone(),
            stage: work_order.stage,
            action_kind: work_order.action_kind,
            guardrail_kind,
            action_count: work_order.action_count,
            protocol_count: work_order.protocol_count,
            primitive_count: work_order.primitive_count,
            release_blocking: work_order.blocks_preflight(),
            operator_required: work_order.requires_operator(),
        }
    }

    pub fn blocks_release(&self) -> bool {
        self.release_blocking
    }

    pub fn requires_operator(&self) -> bool {
        self.operator_required
    }

    pub fn ready_to_execute(&self) -> bool {
        self.guardrail_kind
            == IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionWorkOrderGuardrailKind::ReadyToExecute
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionWorkOrderGuardrailSummary {
    pub total_guardrails: usize,
    pub release_blocker_guardrails: usize,
    pub operator_handoff_guardrails: usize,
    pub ready_to_execute_guardrails: usize,
    pub scheduled_actions: usize,
    pub protocol_mentions: usize,
    pub primitive_mentions: usize,
    pub first_work_order_key: Option<String>,
    pub first_release_blocker_work_order_key: Option<String>,
    pub first_operator_handoff_work_order_key: Option<String>,
    pub first_ready_to_execute_work_order_key: Option<String>,
    pub first_guardrail_kind:
        Option<IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionWorkOrderGuardrailKind>,
    pub first_stage: Option<IntegrationMeshProtocolSubstrateStage>,
    pub first_action_kind: Option<IntegrationMeshProtocolSubstratePreflightActionKind>,
    pub execution_work_order_guardrails_ready: bool,
}

impl IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionWorkOrderGuardrailSummary {
    pub fn from_guardrails<'a>(
        guardrails: impl IntoIterator<
            Item = &'a IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionWorkOrderGuardrail,
        >,
    ) -> Self {
        let guardrails = guardrails.into_iter().collect::<Vec<_>>();
        let first_guardrail = guardrails.iter().min_by_key(|guardrail| guardrail.sequence);
        let first_release_blocker = guardrails
            .iter()
            .filter(|guardrail| {
                guardrail.guardrail_kind
                    == IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionWorkOrderGuardrailKind::ReleaseBlocker
            })
            .min_by_key(|guardrail| guardrail.sequence);
        let first_operator_handoff = guardrails
            .iter()
            .filter(|guardrail| {
                guardrail.guardrail_kind
                    == IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionWorkOrderGuardrailKind::OperatorHandoff
            })
            .min_by_key(|guardrail| guardrail.sequence);
        let first_ready_to_execute = guardrails
            .iter()
            .filter(|guardrail| guardrail.ready_to_execute())
            .min_by_key(|guardrail| guardrail.sequence);

        Self {
            total_guardrails: guardrails.len(),
            release_blocker_guardrails: guardrails
                .iter()
                .filter(|guardrail| {
                    guardrail.guardrail_kind
                        == IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionWorkOrderGuardrailKind::ReleaseBlocker
                })
                .count(),
            operator_handoff_guardrails: guardrails
                .iter()
                .filter(|guardrail| {
                    guardrail.guardrail_kind
                        == IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionWorkOrderGuardrailKind::OperatorHandoff
                })
                .count(),
            ready_to_execute_guardrails: guardrails
                .iter()
                .filter(|guardrail| guardrail.ready_to_execute())
                .count(),
            scheduled_actions: guardrails
                .iter()
                .map(|guardrail| guardrail.action_count)
                .sum(),
            protocol_mentions: guardrails
                .iter()
                .map(|guardrail| guardrail.protocol_count)
                .sum(),
            primitive_mentions: guardrails
                .iter()
                .map(|guardrail| guardrail.primitive_count)
                .sum(),
            first_work_order_key: first_guardrail
                .map(|guardrail| guardrail.work_order_key.clone()),
            first_release_blocker_work_order_key: first_release_blocker
                .map(|guardrail| guardrail.work_order_key.clone()),
            first_operator_handoff_work_order_key: first_operator_handoff
                .map(|guardrail| guardrail.work_order_key.clone()),
            first_ready_to_execute_work_order_key: first_ready_to_execute
                .map(|guardrail| guardrail.work_order_key.clone()),
            first_guardrail_kind: first_guardrail.map(|guardrail| guardrail.guardrail_kind),
            first_stage: first_guardrail.map(|guardrail| guardrail.stage),
            first_action_kind: first_guardrail.map(|guardrail| guardrail.action_kind),
            execution_work_order_guardrails_ready: guardrails
                .iter()
                .all(|guardrail| guardrail.ready_to_execute()),
        }
    }

    pub fn is_empty(&self) -> bool {
        self.total_guardrails == 0
    }

    pub fn has_guardrails(&self) -> bool {
        self.total_guardrails > 0
    }

    pub fn has_release_blockers(&self) -> bool {
        self.release_blocker_guardrails > 0 || self.operator_handoff_guardrails > 0
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_handoff_guardrails > 0
    }

    pub fn has_ready_to_execute_work(&self) -> bool {
        self.ready_to_execute_guardrails > 0
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidencePacket {
    pub sequence: usize,
    pub evidence_key: String,
    pub work_order_sequence: usize,
    pub work_order_key: String,
    pub ticket_sequence: usize,
    pub ticket_key: String,
    pub slot_sequence: usize,
    pub batch_sequence: usize,
    pub stage: IntegrationMeshProtocolSubstrateStage,
    pub action_kind: IntegrationMeshProtocolSubstratePreflightActionKind,
    pub status: IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionStatus,
    pub action_count: usize,
    pub protocol_count: usize,
    pub primitive_count: usize,
    pub protocols: Vec<ProtocolFamily>,
    pub primitives: Vec<PrimitiveFamily>,
    pub release_blocking: bool,
    pub operator_required: bool,
    pub lineage_complete: bool,
}

impl IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidencePacket {
    pub fn from_work_order(
        sequence: usize,
        work_order: &IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionWorkOrder,
    ) -> Self {
        let lineage_complete = !work_order.work_order_key.is_empty()
            && work_order.ticket_sequence > 0
            && !work_order.ticket_key.is_empty()
            && work_order.slot_sequence > 0
            && work_order.batch_sequence > 0;

        Self {
            sequence,
            evidence_key: format!(
                "evidence-{sequence:02}-{}-{}",
                work_order.stage.as_str(),
                work_order.action_kind.as_str()
            ),
            work_order_sequence: work_order.sequence,
            work_order_key: work_order.work_order_key.clone(),
            ticket_sequence: work_order.ticket_sequence,
            ticket_key: work_order.ticket_key.clone(),
            slot_sequence: work_order.slot_sequence,
            batch_sequence: work_order.batch_sequence,
            stage: work_order.stage,
            action_kind: work_order.action_kind,
            status: work_order.status,
            action_count: work_order.action_count,
            protocol_count: work_order.protocol_count,
            primitive_count: work_order.primitive_count,
            protocols: work_order.protocols.clone(),
            primitives: work_order.primitives.clone(),
            release_blocking: work_order.blocks_preflight(),
            operator_required: work_order.requires_operator(),
            lineage_complete,
        }
    }

    pub fn blocks_preflight(&self) -> bool {
        self.release_blocking
    }

    pub fn requires_operator(&self) -> bool {
        self.operator_required
    }

    pub fn has_lineage(&self) -> bool {
        self.lineage_complete
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceSummary {
    pub total_packets: usize,
    pub scheduled_actions: usize,
    pub blocking_packets: usize,
    pub operator_required_packets: usize,
    pub lineage_complete_packets: usize,
    pub protocol_mentions: usize,
    pub primitive_mentions: usize,
    pub first_evidence_key: Option<String>,
    pub first_blocking_evidence_key: Option<String>,
    pub first_operator_evidence_key: Option<String>,
    pub first_work_order_key: Option<String>,
    pub first_ticket_key: Option<String>,
    pub first_stage: Option<IntegrationMeshProtocolSubstrateStage>,
    pub first_action_kind: Option<IntegrationMeshProtocolSubstratePreflightActionKind>,
    pub execution_evidence_ready: bool,
}

impl IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceSummary {
    pub fn from_packets<'a>(
        packets: impl IntoIterator<
            Item = &'a IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidencePacket,
        >,
    ) -> Self {
        let packets = packets.into_iter().collect::<Vec<_>>();
        let first_packet = packets.iter().min_by_key(|packet| packet.sequence);
        let first_blocking_packet = packets
            .iter()
            .filter(|packet| packet.blocks_preflight())
            .min_by_key(|packet| packet.sequence);
        let first_operator_packet = packets
            .iter()
            .filter(|packet| packet.requires_operator())
            .min_by_key(|packet| packet.sequence);

        Self {
            total_packets: packets.len(),
            scheduled_actions: packets.iter().map(|packet| packet.action_count).sum(),
            blocking_packets: packets
                .iter()
                .filter(|packet| packet.blocks_preflight())
                .count(),
            operator_required_packets: packets
                .iter()
                .filter(|packet| packet.requires_operator())
                .count(),
            lineage_complete_packets: packets.iter().filter(|packet| packet.has_lineage()).count(),
            protocol_mentions: packets.iter().map(|packet| packet.protocol_count).sum(),
            primitive_mentions: packets.iter().map(|packet| packet.primitive_count).sum(),
            first_evidence_key: first_packet.map(|packet| packet.evidence_key.clone()),
            first_blocking_evidence_key: first_blocking_packet
                .map(|packet| packet.evidence_key.clone()),
            first_operator_evidence_key: first_operator_packet
                .map(|packet| packet.evidence_key.clone()),
            first_work_order_key: first_packet.map(|packet| packet.work_order_key.clone()),
            first_ticket_key: first_packet.map(|packet| packet.ticket_key.clone()),
            first_stage: first_packet.map(|packet| packet.stage),
            first_action_kind: first_packet.map(|packet| packet.action_kind),
            execution_evidence_ready: packets.is_empty(),
        }
    }

    pub fn is_empty(&self) -> bool {
        self.total_packets == 0
    }

    pub fn has_packets(&self) -> bool {
        self.total_packets > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.blocking_packets > 0
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_packets > 0
    }

    pub fn has_complete_lineage(&self) -> bool {
        self.lineage_complete_packets == self.total_packets
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReview {
    pub sequence: usize,
    pub review_key: String,
    pub evidence_sequence: usize,
    pub evidence_key: String,
    pub work_order_sequence: usize,
    pub work_order_key: String,
    pub ticket_sequence: usize,
    pub ticket_key: String,
    pub slot_sequence: usize,
    pub batch_sequence: usize,
    pub stage: IntegrationMeshProtocolSubstrateStage,
    pub action_kind: IntegrationMeshProtocolSubstratePreflightActionKind,
    pub status: IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionStatus,
    pub action_count: usize,
    pub protocol_count: usize,
    pub primitive_count: usize,
    pub protocols: Vec<ProtocolFamily>,
    pub primitives: Vec<PrimitiveFamily>,
    pub release_blocking: bool,
    pub operator_required: bool,
    pub lineage_complete: bool,
    pub review_required: bool,
    pub ready_for_execution: bool,
}

impl IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReview {
    pub fn from_packet(
        sequence: usize,
        packet: &IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidencePacket,
    ) -> Self {
        let review_required =
            packet.blocks_preflight() || packet.requires_operator() || !packet.has_lineage();
        let ready_for_execution =
            !packet.blocks_preflight() && !packet.requires_operator() && packet.has_lineage();

        Self {
            sequence,
            review_key: format!(
                "review-{sequence:02}-{}-{}",
                packet.stage.as_str(),
                packet.action_kind.as_str()
            ),
            evidence_sequence: packet.sequence,
            evidence_key: packet.evidence_key.clone(),
            work_order_sequence: packet.work_order_sequence,
            work_order_key: packet.work_order_key.clone(),
            ticket_sequence: packet.ticket_sequence,
            ticket_key: packet.ticket_key.clone(),
            slot_sequence: packet.slot_sequence,
            batch_sequence: packet.batch_sequence,
            stage: packet.stage,
            action_kind: packet.action_kind,
            status: packet.status,
            action_count: packet.action_count,
            protocol_count: packet.protocol_count,
            primitive_count: packet.primitive_count,
            protocols: packet.protocols.clone(),
            primitives: packet.primitives.clone(),
            release_blocking: packet.blocks_preflight(),
            operator_required: packet.requires_operator(),
            lineage_complete: packet.has_lineage(),
            review_required,
            ready_for_execution,
        }
    }

    pub fn blocks_preflight(&self) -> bool {
        self.release_blocking
    }

    pub fn requires_operator(&self) -> bool {
        self.operator_required
    }

    pub fn has_lineage(&self) -> bool {
        self.lineage_complete
    }

    pub fn needs_review(&self) -> bool {
        self.review_required
    }

    pub fn is_ready_for_execution(&self) -> bool {
        self.ready_for_execution
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewSummary {
    pub total_reviews: usize,
    pub scheduled_actions: usize,
    pub blocking_reviews: usize,
    pub operator_required_reviews: usize,
    pub lineage_complete_reviews: usize,
    pub review_required_reviews: usize,
    pub execution_ready_reviews: usize,
    pub protocol_mentions: usize,
    pub primitive_mentions: usize,
    pub first_review_key: Option<String>,
    pub first_blocking_review_key: Option<String>,
    pub first_operator_review_key: Option<String>,
    pub first_evidence_key: Option<String>,
    pub first_work_order_key: Option<String>,
    pub first_ticket_key: Option<String>,
    pub first_stage: Option<IntegrationMeshProtocolSubstrateStage>,
    pub first_action_kind: Option<IntegrationMeshProtocolSubstratePreflightActionKind>,
    pub execution_evidence_reviews_ready: bool,
}

impl IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewSummary {
    pub fn from_reviews<'a>(
        reviews: impl IntoIterator<
            Item = &'a IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReview,
        >,
    ) -> Self {
        let reviews = reviews.into_iter().collect::<Vec<_>>();
        let first_review = reviews.iter().min_by_key(|review| review.sequence);
        let first_blocking_review = reviews
            .iter()
            .filter(|review| review.blocks_preflight())
            .min_by_key(|review| review.sequence);
        let first_operator_review = reviews
            .iter()
            .filter(|review| review.requires_operator())
            .min_by_key(|review| review.sequence);

        Self {
            total_reviews: reviews.len(),
            scheduled_actions: reviews.iter().map(|review| review.action_count).sum(),
            blocking_reviews: reviews
                .iter()
                .filter(|review| review.blocks_preflight())
                .count(),
            operator_required_reviews: reviews
                .iter()
                .filter(|review| review.requires_operator())
                .count(),
            lineage_complete_reviews: reviews.iter().filter(|review| review.has_lineage()).count(),
            review_required_reviews: reviews
                .iter()
                .filter(|review| review.needs_review())
                .count(),
            execution_ready_reviews: reviews
                .iter()
                .filter(|review| review.is_ready_for_execution())
                .count(),
            protocol_mentions: reviews.iter().map(|review| review.protocol_count).sum(),
            primitive_mentions: reviews.iter().map(|review| review.primitive_count).sum(),
            first_review_key: first_review.map(|review| review.review_key.clone()),
            first_blocking_review_key: first_blocking_review
                .map(|review| review.review_key.clone()),
            first_operator_review_key: first_operator_review
                .map(|review| review.review_key.clone()),
            first_evidence_key: first_review.map(|review| review.evidence_key.clone()),
            first_work_order_key: first_review.map(|review| review.work_order_key.clone()),
            first_ticket_key: first_review.map(|review| review.ticket_key.clone()),
            first_stage: first_review.map(|review| review.stage),
            first_action_kind: first_review.map(|review| review.action_kind),
            execution_evidence_reviews_ready: reviews.is_empty(),
        }
    }

    pub fn is_empty(&self) -> bool {
        self.total_reviews == 0
    }

    pub fn has_reviews(&self) -> bool {
        self.total_reviews > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.blocking_reviews > 0
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_reviews > 0
    }

    pub fn needs_review(&self) -> bool {
        self.review_required_reviews > 0
    }

    pub fn has_complete_lineage(&self) -> bool {
        self.lineage_complete_reviews == self.total_reviews
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub enum IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionKind {
    OperatorHandoff,
    RepairRequired,
    LineageGap,
    ReadyForExecution,
}

impl IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionKind {
    pub fn as_str(self) -> &'static str {
        match self {
            Self::OperatorHandoff => "operator_handoff",
            Self::RepairRequired => "repair_required",
            Self::LineageGap => "lineage_gap",
            Self::ReadyForExecution => "ready_for_execution",
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDisposition {
    pub sequence: usize,
    pub disposition_key: String,
    pub review_sequence: usize,
    pub review_key: String,
    pub evidence_sequence: usize,
    pub evidence_key: String,
    pub work_order_sequence: usize,
    pub work_order_key: String,
    pub ticket_sequence: usize,
    pub ticket_key: String,
    pub slot_sequence: usize,
    pub batch_sequence: usize,
    pub stage: IntegrationMeshProtocolSubstrateStage,
    pub action_kind: IntegrationMeshProtocolSubstratePreflightActionKind,
    pub status: IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionStatus,
    pub disposition_kind:
        IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionKind,
    pub action_count: usize,
    pub protocol_count: usize,
    pub primitive_count: usize,
    pub protocols: Vec<ProtocolFamily>,
    pub primitives: Vec<PrimitiveFamily>,
    pub release_blocking: bool,
    pub operator_required: bool,
    pub lineage_complete: bool,
    pub review_required: bool,
    pub ready_for_execution: bool,
}

impl IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDisposition {
    pub fn from_review(
        sequence: usize,
        review: &IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReview,
    ) -> Self {
        let disposition_kind = if !review.has_lineage() {
            IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionKind::LineageGap
        } else if review.requires_operator() {
            IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionKind::OperatorHandoff
        } else if review.blocks_preflight() || review.needs_review() {
            IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionKind::RepairRequired
        } else {
            IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionKind::ReadyForExecution
        };

        Self {
            sequence,
            disposition_key: format!(
                "disposition-{sequence:02}-{}-{}",
                review.stage.as_str(),
                review.action_kind.as_str()
            ),
            review_sequence: review.sequence,
            review_key: review.review_key.clone(),
            evidence_sequence: review.evidence_sequence,
            evidence_key: review.evidence_key.clone(),
            work_order_sequence: review.work_order_sequence,
            work_order_key: review.work_order_key.clone(),
            ticket_sequence: review.ticket_sequence,
            ticket_key: review.ticket_key.clone(),
            slot_sequence: review.slot_sequence,
            batch_sequence: review.batch_sequence,
            stage: review.stage,
            action_kind: review.action_kind,
            status: review.status,
            disposition_kind,
            action_count: review.action_count,
            protocol_count: review.protocol_count,
            primitive_count: review.primitive_count,
            protocols: review.protocols.clone(),
            primitives: review.primitives.clone(),
            release_blocking: review.blocks_preflight(),
            operator_required: review.requires_operator(),
            lineage_complete: review.has_lineage(),
            review_required: review.needs_review(),
            ready_for_execution: review.is_ready_for_execution(),
        }
    }

    pub fn blocks_preflight(&self) -> bool {
        self.release_blocking
    }

    pub fn requires_operator(&self) -> bool {
        self.operator_required
    }

    pub fn has_lineage(&self) -> bool {
        self.lineage_complete
    }

    pub fn needs_review(&self) -> bool {
        self.review_required
    }

    pub fn is_ready_for_execution(&self) -> bool {
        self.ready_for_execution
    }

    pub fn is_operator_handoff(&self) -> bool {
        self.disposition_kind
            == IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionKind::OperatorHandoff
    }

    pub fn is_repair_required(&self) -> bool {
        self.disposition_kind
            == IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionKind::RepairRequired
    }

    pub fn is_lineage_gap(&self) -> bool {
        self.disposition_kind
            == IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionKind::LineageGap
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionSummary
{
    pub total_dispositions: usize,
    pub scheduled_actions: usize,
    pub blocking_dispositions: usize,
    pub operator_handoff_dispositions: usize,
    pub repair_required_dispositions: usize,
    pub lineage_gap_dispositions: usize,
    pub review_required_dispositions: usize,
    pub execution_ready_dispositions: usize,
    pub lineage_complete_dispositions: usize,
    pub protocol_mentions: usize,
    pub primitive_mentions: usize,
    pub first_disposition_key: Option<String>,
    pub first_operator_handoff_disposition_key: Option<String>,
    pub first_repair_required_disposition_key: Option<String>,
    pub first_lineage_gap_disposition_key: Option<String>,
    pub first_review_key: Option<String>,
    pub first_evidence_key: Option<String>,
    pub first_work_order_key: Option<String>,
    pub first_ticket_key: Option<String>,
    pub first_stage: Option<IntegrationMeshProtocolSubstrateStage>,
    pub first_action_kind: Option<IntegrationMeshProtocolSubstratePreflightActionKind>,
    pub execution_evidence_review_dispositions_ready: bool,
}

impl IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionSummary {
    pub fn from_dispositions<'a>(
        dispositions: impl IntoIterator<
            Item = &'a IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDisposition,
        >,
    ) -> Self {
        let dispositions = dispositions.into_iter().collect::<Vec<_>>();
        let first_disposition = dispositions
            .iter()
            .min_by_key(|disposition| disposition.sequence);
        let first_operator_handoff_disposition = dispositions
            .iter()
            .filter(|disposition| disposition.is_operator_handoff())
            .min_by_key(|disposition| disposition.sequence);
        let first_repair_required_disposition = dispositions
            .iter()
            .filter(|disposition| disposition.is_repair_required())
            .min_by_key(|disposition| disposition.sequence);
        let first_lineage_gap_disposition = dispositions
            .iter()
            .filter(|disposition| disposition.is_lineage_gap())
            .min_by_key(|disposition| disposition.sequence);

        Self {
            total_dispositions: dispositions.len(),
            scheduled_actions: dispositions
                .iter()
                .map(|disposition| disposition.action_count)
                .sum(),
            blocking_dispositions: dispositions
                .iter()
                .filter(|disposition| disposition.blocks_preflight())
                .count(),
            operator_handoff_dispositions: dispositions
                .iter()
                .filter(|disposition| disposition.is_operator_handoff())
                .count(),
            repair_required_dispositions: dispositions
                .iter()
                .filter(|disposition| disposition.is_repair_required())
                .count(),
            lineage_gap_dispositions: dispositions
                .iter()
                .filter(|disposition| disposition.is_lineage_gap())
                .count(),
            review_required_dispositions: dispositions
                .iter()
                .filter(|disposition| disposition.needs_review())
                .count(),
            execution_ready_dispositions: dispositions
                .iter()
                .filter(|disposition| disposition.is_ready_for_execution())
                .count(),
            lineage_complete_dispositions: dispositions
                .iter()
                .filter(|disposition| disposition.has_lineage())
                .count(),
            protocol_mentions: dispositions
                .iter()
                .map(|disposition| disposition.protocol_count)
                .sum(),
            primitive_mentions: dispositions
                .iter()
                .map(|disposition| disposition.primitive_count)
                .sum(),
            first_disposition_key: first_disposition
                .map(|disposition| disposition.disposition_key.clone()),
            first_operator_handoff_disposition_key: first_operator_handoff_disposition
                .map(|disposition| disposition.disposition_key.clone()),
            first_repair_required_disposition_key: first_repair_required_disposition
                .map(|disposition| disposition.disposition_key.clone()),
            first_lineage_gap_disposition_key: first_lineage_gap_disposition
                .map(|disposition| disposition.disposition_key.clone()),
            first_review_key: first_disposition.map(|disposition| disposition.review_key.clone()),
            first_evidence_key: first_disposition
                .map(|disposition| disposition.evidence_key.clone()),
            first_work_order_key: first_disposition
                .map(|disposition| disposition.work_order_key.clone()),
            first_ticket_key: first_disposition.map(|disposition| disposition.ticket_key.clone()),
            first_stage: first_disposition.map(|disposition| disposition.stage),
            first_action_kind: first_disposition.map(|disposition| disposition.action_kind),
            execution_evidence_review_dispositions_ready: dispositions.is_empty(),
        }
    }

    pub fn is_empty(&self) -> bool {
        self.total_dispositions == 0
    }

    pub fn has_dispositions(&self) -> bool {
        self.total_dispositions > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.blocking_dispositions > 0
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_handoff_dispositions > 0
    }

    pub fn needs_repair(&self) -> bool {
        self.repair_required_dispositions > 0
    }

    pub fn needs_review(&self) -> bool {
        self.review_required_dispositions > 0
    }

    pub fn has_lineage_gaps(&self) -> bool {
        self.lineage_gap_dispositions > 0
    }

    pub fn has_complete_lineage(&self) -> bool {
        self.lineage_complete_dispositions == self.total_dispositions
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub enum IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionActionKind
{
    CoordinateOperatorHandoff,
    ScheduleRepairWork,
    RestoreEvidenceLineage,
    ReleaseExecution,
}

impl
    IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionActionKind
{
    pub fn as_str(self) -> &'static str {
        match self {
            Self::CoordinateOperatorHandoff => "coordinate_operator_handoff",
            Self::ScheduleRepairWork => "schedule_repair_work",
            Self::RestoreEvidenceLineage => "restore_evidence_lineage",
            Self::ReleaseExecution => "release_execution",
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionAction {
    pub sequence: usize,
    pub action_key: String,
    pub disposition_sequence: usize,
    pub disposition_key: String,
    pub review_sequence: usize,
    pub review_key: String,
    pub evidence_sequence: usize,
    pub evidence_key: String,
    pub work_order_sequence: usize,
    pub work_order_key: String,
    pub ticket_sequence: usize,
    pub ticket_key: String,
    pub slot_sequence: usize,
    pub batch_sequence: usize,
    pub stage: IntegrationMeshProtocolSubstrateStage,
    pub action_kind: IntegrationMeshProtocolSubstratePreflightActionKind,
    pub status: IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionStatus,
    pub disposition_kind:
        IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionKind,
    pub disposition_action_kind:
        IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionActionKind,
    pub action_count: usize,
    pub protocol_count: usize,
    pub primitive_count: usize,
    pub protocols: Vec<ProtocolFamily>,
    pub primitives: Vec<PrimitiveFamily>,
    pub release_blocking: bool,
    pub operator_required: bool,
    pub lineage_complete: bool,
    pub review_required: bool,
    pub ready_for_execution: bool,
    pub action_required: bool,
}

impl IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionAction {
    pub fn from_disposition(
        sequence: usize,
        disposition: &IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDisposition,
    ) -> Self {
        let disposition_action_kind = match disposition.disposition_kind {
            IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionKind::OperatorHandoff => {
                IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionActionKind::CoordinateOperatorHandoff
            }
            IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionKind::RepairRequired => {
                IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionActionKind::ScheduleRepairWork
            }
            IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionKind::LineageGap => {
                IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionActionKind::RestoreEvidenceLineage
            }
            IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionKind::ReadyForExecution => {
                IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionActionKind::ReleaseExecution
            }
        };

        Self {
            sequence,
            action_key: format!(
                "disposition-action-{sequence:02}-{}-{}",
                disposition.stage.as_str(),
                disposition.action_kind.as_str()
            ),
            disposition_sequence: disposition.sequence,
            disposition_key: disposition.disposition_key.clone(),
            review_sequence: disposition.review_sequence,
            review_key: disposition.review_key.clone(),
            evidence_sequence: disposition.evidence_sequence,
            evidence_key: disposition.evidence_key.clone(),
            work_order_sequence: disposition.work_order_sequence,
            work_order_key: disposition.work_order_key.clone(),
            ticket_sequence: disposition.ticket_sequence,
            ticket_key: disposition.ticket_key.clone(),
            slot_sequence: disposition.slot_sequence,
            batch_sequence: disposition.batch_sequence,
            stage: disposition.stage,
            action_kind: disposition.action_kind,
            status: disposition.status,
            disposition_kind: disposition.disposition_kind,
            disposition_action_kind,
            action_count: disposition.action_count,
            protocol_count: disposition.protocol_count,
            primitive_count: disposition.primitive_count,
            protocols: disposition.protocols.clone(),
            primitives: disposition.primitives.clone(),
            release_blocking: disposition.blocks_preflight(),
            operator_required: disposition.requires_operator(),
            lineage_complete: disposition.has_lineage(),
            review_required: disposition.needs_review(),
            ready_for_execution: disposition.is_ready_for_execution(),
            action_required: !disposition.is_ready_for_execution(),
        }
    }

    pub fn requires_action(&self) -> bool {
        self.action_required
    }

    pub fn blocks_preflight(&self) -> bool {
        self.release_blocking
    }

    pub fn requires_operator(&self) -> bool {
        self.operator_required
    }

    pub fn has_lineage(&self) -> bool {
        self.lineage_complete
    }

    pub fn needs_review(&self) -> bool {
        self.review_required
    }

    pub fn is_operator_handoff(&self) -> bool {
        self.disposition_action_kind
            == IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionActionKind::CoordinateOperatorHandoff
    }

    pub fn is_repair_action(&self) -> bool {
        self.disposition_action_kind
            == IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionActionKind::ScheduleRepairWork
    }

    pub fn is_lineage_action(&self) -> bool {
        self.disposition_action_kind
            == IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionActionKind::RestoreEvidenceLineage
    }

    pub fn is_release_execution_action(&self) -> bool {
        self.disposition_action_kind
            == IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionActionKind::ReleaseExecution
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionActionSummary
{
    pub total_actions: usize,
    pub required_actions: usize,
    pub blocking_actions: usize,
    pub operator_handoff_actions: usize,
    pub repair_actions: usize,
    pub lineage_gap_actions: usize,
    pub release_execution_actions: usize,
    pub review_required_actions: usize,
    pub lineage_complete_actions: usize,
    pub protocol_mentions: usize,
    pub primitive_mentions: usize,
    pub first_action_key: Option<String>,
    pub first_required_action_key: Option<String>,
    pub first_operator_handoff_action_key: Option<String>,
    pub first_repair_action_key: Option<String>,
    pub first_lineage_gap_action_key: Option<String>,
    pub first_release_execution_action_key: Option<String>,
    pub first_disposition_key: Option<String>,
    pub first_review_key: Option<String>,
    pub first_evidence_key: Option<String>,
    pub first_work_order_key: Option<String>,
    pub first_ticket_key: Option<String>,
    pub first_stage: Option<IntegrationMeshProtocolSubstrateStage>,
    pub first_action_kind: Option<IntegrationMeshProtocolSubstratePreflightActionKind>,
    pub execution_evidence_review_disposition_actions_ready: bool,
}

impl IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionActionSummary {
    pub fn from_actions<'a>(
        actions: impl IntoIterator<
            Item = &'a IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionAction,
        >,
    ) -> Self {
        let actions = actions.into_iter().collect::<Vec<_>>();
        let first_action = actions.iter().min_by_key(|action| action.sequence);
        let first_required_action = actions
            .iter()
            .filter(|action| action.requires_action())
            .min_by_key(|action| action.sequence);
        let first_operator_handoff_action = actions
            .iter()
            .filter(|action| action.is_operator_handoff())
            .min_by_key(|action| action.sequence);
        let first_repair_action = actions
            .iter()
            .filter(|action| action.is_repair_action())
            .min_by_key(|action| action.sequence);
        let first_lineage_gap_action = actions
            .iter()
            .filter(|action| action.is_lineage_action())
            .min_by_key(|action| action.sequence);
        let first_release_execution_action = actions
            .iter()
            .filter(|action| action.is_release_execution_action())
            .min_by_key(|action| action.sequence);

        Self {
            total_actions: actions.len(),
            required_actions: actions
                .iter()
                .filter(|action| action.requires_action())
                .count(),
            blocking_actions: actions
                .iter()
                .filter(|action| action.blocks_preflight())
                .count(),
            operator_handoff_actions: actions
                .iter()
                .filter(|action| action.is_operator_handoff())
                .count(),
            repair_actions: actions
                .iter()
                .filter(|action| action.is_repair_action())
                .count(),
            lineage_gap_actions: actions
                .iter()
                .filter(|action| action.is_lineage_action())
                .count(),
            release_execution_actions: actions
                .iter()
                .filter(|action| action.is_release_execution_action())
                .count(),
            review_required_actions: actions
                .iter()
                .filter(|action| action.needs_review())
                .count(),
            lineage_complete_actions: actions.iter().filter(|action| action.has_lineage()).count(),
            protocol_mentions: actions.iter().map(|action| action.protocol_count).sum(),
            primitive_mentions: actions.iter().map(|action| action.primitive_count).sum(),
            first_action_key: first_action.map(|action| action.action_key.clone()),
            first_required_action_key: first_required_action
                .map(|action| action.action_key.clone()),
            first_operator_handoff_action_key: first_operator_handoff_action
                .map(|action| action.action_key.clone()),
            first_repair_action_key: first_repair_action.map(|action| action.action_key.clone()),
            first_lineage_gap_action_key: first_lineage_gap_action
                .map(|action| action.action_key.clone()),
            first_release_execution_action_key: first_release_execution_action
                .map(|action| action.action_key.clone()),
            first_disposition_key: first_action.map(|action| action.disposition_key.clone()),
            first_review_key: first_action.map(|action| action.review_key.clone()),
            first_evidence_key: first_action.map(|action| action.evidence_key.clone()),
            first_work_order_key: first_action.map(|action| action.work_order_key.clone()),
            first_ticket_key: first_action.map(|action| action.ticket_key.clone()),
            first_stage: first_action.map(|action| action.stage),
            first_action_kind: first_action.map(|action| action.action_kind),
            execution_evidence_review_disposition_actions_ready: actions.is_empty(),
        }
    }

    pub fn is_empty(&self) -> bool {
        self.total_actions == 0
    }

    pub fn has_actions(&self) -> bool {
        self.total_actions > 0
    }

    pub fn has_required_actions(&self) -> bool {
        self.required_actions > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.blocking_actions > 0
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_handoff_actions > 0
    }

    pub fn needs_repair(&self) -> bool {
        self.repair_actions > 0
    }

    pub fn has_lineage_gaps(&self) -> bool {
        self.lineage_gap_actions > 0
    }

    pub fn has_release_execution_actions(&self) -> bool {
        self.release_execution_actions > 0
    }

    pub fn needs_review(&self) -> bool {
        self.review_required_actions > 0
    }

    pub fn has_complete_lineage(&self) -> bool {
        self.lineage_complete_actions == self.total_actions
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionActionSlot {
    pub sequence: usize,
    pub slot_key: String,
    pub action_sequence: usize,
    pub action_key: String,
    pub disposition_key: String,
    pub work_order_key: String,
    pub ticket_key: String,
    pub stage: IntegrationMeshProtocolSubstrateStage,
    pub action_kind: IntegrationMeshProtocolSubstratePreflightActionKind,
    pub status: IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionStatus,
    pub disposition_action_kind:
        IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionActionKind,
    pub release_blocking: bool,
    pub operator_required: bool,
    pub review_required: bool,
    pub lineage_complete: bool,
    pub action_required: bool,
    pub ready_for_execution: bool,
}

impl
    IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionActionSlot
{
    pub fn from_action(
        sequence: usize,
        action: &IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionAction,
    ) -> Self {
        Self {
            sequence,
            slot_key: format!(
                "disposition-action-slot-{sequence:02}-{}-{}",
                action.stage.as_str(),
                action.action_kind.as_str()
            ),
            action_sequence: action.sequence,
            action_key: action.action_key.clone(),
            disposition_key: action.disposition_key.clone(),
            work_order_key: action.work_order_key.clone(),
            ticket_key: action.ticket_key.clone(),
            stage: action.stage,
            action_kind: action.action_kind,
            status: action.status,
            disposition_action_kind: action.disposition_action_kind,
            release_blocking: action.blocks_preflight(),
            operator_required: action.requires_operator(),
            review_required: action.needs_review(),
            lineage_complete: action.has_lineage(),
            action_required: action.requires_action(),
            ready_for_execution: action.is_release_execution_action(),
        }
    }

    pub fn requires_action(&self) -> bool {
        self.action_required
    }

    pub fn blocks_preflight(&self) -> bool {
        self.release_blocking
    }

    pub fn requires_operator(&self) -> bool {
        self.operator_required
    }

    pub fn needs_review(&self) -> bool {
        self.review_required
    }

    pub fn has_lineage(&self) -> bool {
        self.lineage_complete
    }

    pub fn is_operator_handoff_slot(&self) -> bool {
        self.disposition_action_kind
            == IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionActionKind::CoordinateOperatorHandoff
    }

    pub fn is_repair_slot(&self) -> bool {
        self.disposition_action_kind
            == IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionActionKind::ScheduleRepairWork
    }

    pub fn is_lineage_slot(&self) -> bool {
        self.disposition_action_kind
            == IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionActionKind::RestoreEvidenceLineage
    }

    pub fn is_release_execution_slot(&self) -> bool {
        self.disposition_action_kind
            == IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionActionKind::ReleaseExecution
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionActionSlotSummary
{
    pub total_slots: usize,
    pub required_slots: usize,
    pub blocking_slots: usize,
    pub operator_handoff_slots: usize,
    pub repair_slots: usize,
    pub lineage_gap_slots: usize,
    pub release_execution_slots: usize,
    pub review_required_slots: usize,
    pub lineage_complete_slots: usize,
    pub first_slot_key: Option<String>,
    pub first_required_slot_key: Option<String>,
    pub first_operator_handoff_slot_key: Option<String>,
    pub first_repair_slot_key: Option<String>,
    pub first_lineage_gap_slot_key: Option<String>,
    pub first_release_execution_slot_key: Option<String>,
    pub first_action_key: Option<String>,
    pub first_disposition_key: Option<String>,
    pub first_work_order_key: Option<String>,
    pub first_ticket_key: Option<String>,
    pub first_stage: Option<IntegrationMeshProtocolSubstrateStage>,
    pub first_action_kind: Option<IntegrationMeshProtocolSubstratePreflightActionKind>,
    pub execution_evidence_review_disposition_action_slots_ready: bool,
}

impl
    IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionActionSlotSummary
{
    pub fn from_slots<'a>(
        slots: impl IntoIterator<
            Item = &'a IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionActionSlot,
        >,
    ) -> Self {
        let slots = slots.into_iter().collect::<Vec<_>>();
        let first_slot = slots.iter().min_by_key(|slot| slot.sequence);
        let first_required_slot = slots
            .iter()
            .filter(|slot| slot.requires_action())
            .min_by_key(|slot| slot.sequence);
        let first_operator_handoff_slot = slots
            .iter()
            .filter(|slot| slot.is_operator_handoff_slot())
            .min_by_key(|slot| slot.sequence);
        let first_repair_slot = slots
            .iter()
            .filter(|slot| slot.is_repair_slot())
            .min_by_key(|slot| slot.sequence);
        let first_lineage_gap_slot = slots
            .iter()
            .filter(|slot| slot.is_lineage_slot())
            .min_by_key(|slot| slot.sequence);
        let first_release_execution_slot = slots
            .iter()
            .filter(|slot| slot.is_release_execution_slot())
            .min_by_key(|slot| slot.sequence);

        Self {
            total_slots: slots.len(),
            required_slots: slots.iter().filter(|slot| slot.requires_action()).count(),
            blocking_slots: slots.iter().filter(|slot| slot.blocks_preflight()).count(),
            operator_handoff_slots: slots
                .iter()
                .filter(|slot| slot.is_operator_handoff_slot())
                .count(),
            repair_slots: slots.iter().filter(|slot| slot.is_repair_slot()).count(),
            lineage_gap_slots: slots
                .iter()
                .filter(|slot| slot.is_lineage_slot())
                .count(),
            release_execution_slots: slots
                .iter()
                .filter(|slot| slot.is_release_execution_slot())
                .count(),
            review_required_slots: slots.iter().filter(|slot| slot.needs_review()).count(),
            lineage_complete_slots: slots.iter().filter(|slot| slot.has_lineage()).count(),
            first_slot_key: first_slot.map(|slot| slot.slot_key.clone()),
            first_required_slot_key: first_required_slot.map(|slot| slot.slot_key.clone()),
            first_operator_handoff_slot_key: first_operator_handoff_slot
                .map(|slot| slot.slot_key.clone()),
            first_repair_slot_key: first_repair_slot.map(|slot| slot.slot_key.clone()),
            first_lineage_gap_slot_key: first_lineage_gap_slot.map(|slot| slot.slot_key.clone()),
            first_release_execution_slot_key: first_release_execution_slot
                .map(|slot| slot.slot_key.clone()),
            first_action_key: first_slot.map(|slot| slot.action_key.clone()),
            first_disposition_key: first_slot.map(|slot| slot.disposition_key.clone()),
            first_work_order_key: first_slot.map(|slot| slot.work_order_key.clone()),
            first_ticket_key: first_slot.map(|slot| slot.ticket_key.clone()),
            first_stage: first_slot.map(|slot| slot.stage),
            first_action_kind: first_slot.map(|slot| slot.action_kind),
            execution_evidence_review_disposition_action_slots_ready: slots.is_empty(),
        }
    }

    pub fn is_empty(&self) -> bool {
        self.total_slots == 0
    }

    pub fn has_slots(&self) -> bool {
        self.total_slots > 0
    }

    pub fn has_required_slots(&self) -> bool {
        self.required_slots > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.blocking_slots > 0
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_handoff_slots > 0
    }

    pub fn needs_repair(&self) -> bool {
        self.repair_slots > 0
    }

    pub fn has_lineage_gaps(&self) -> bool {
        self.lineage_gap_slots > 0
    }

    pub fn has_release_execution_slots(&self) -> bool {
        self.release_execution_slots > 0
    }

    pub fn needs_review(&self) -> bool {
        self.review_required_slots > 0
    }

    pub fn has_complete_lineage(&self) -> bool {
        self.lineage_complete_slots == self.total_slots
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshProtocolPrimitiveReadinessRow {
    pub protocol: ProtocolFamily,
    pub required_primitives: Vec<PrimitiveFamily>,
    pub available_primitives: Vec<PrimitiveFamily>,
    pub missing_primitives: Vec<PrimitiveFamily>,
    pub required_primitive_count: usize,
    pub available_primitive_count: usize,
    pub missing_primitive_count: usize,
    pub ready: bool,
    pub radio_network_key_ready: bool,
    pub supervision_ready: bool,
}

impl IntegrationMeshProtocolPrimitiveReadinessRow {
    pub fn from_protocol(
        protocol: ProtocolFamily,
        available_primitives: &[PrimitiveFamily],
    ) -> Option<Self> {
        if !is_low_level_mesh_protocol(&protocol) {
            return None;
        }

        let required_primitives = protocol_primitives(&protocol).to_vec();
        let available_set: BTreeSet<PrimitiveFamily> =
            available_primitives.iter().copied().collect();
        let available_primitives = required_primitives
            .iter()
            .copied()
            .filter(|primitive| available_set.contains(primitive))
            .collect::<Vec<_>>();
        let missing_primitives = required_primitives
            .iter()
            .copied()
            .filter(|primitive| !available_set.contains(primitive))
            .collect::<Vec<_>>();
        let required_primitive_count = required_primitives.len();
        let available_primitive_count = available_primitives.len();
        let missing_primitive_count = missing_primitives.len();
        let radio_network_key_ready = !required_primitives
            .contains(&PrimitiveFamily::RadioNetworkKey)
            || available_primitives.contains(&PrimitiveFamily::RadioNetworkKey);
        let supervision_ready = !required_primitives.contains(&PrimitiveFamily::Supervision)
            || available_primitives.contains(&PrimitiveFamily::Supervision);

        Some(Self {
            protocol,
            required_primitives,
            available_primitives,
            missing_primitives,
            required_primitive_count,
            available_primitive_count,
            missing_primitive_count,
            ready: missing_primitive_count == 0,
            radio_network_key_ready,
            supervision_ready,
        })
    }

    pub fn is_ready(&self) -> bool {
        self.ready
    }

    pub fn has_missing_primitives(&self) -> bool {
        self.missing_primitive_count > 0
    }

    pub fn missing_radio_network_key(&self) -> bool {
        self.missing_primitives
            .contains(&PrimitiveFamily::RadioNetworkKey)
    }

    pub fn missing_supervision(&self) -> bool {
        self.missing_primitives
            .contains(&PrimitiveFamily::Supervision)
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshProtocolPrimitiveReadinessSummary {
    pub total_protocols: usize,
    pub ready_protocols: usize,
    pub blocked_protocols: usize,
    pub unique_required_primitives: Vec<PrimitiveFamily>,
    pub unique_missing_primitives: Vec<PrimitiveFamily>,
    pub required_primitive_count: usize,
    pub missing_primitive_count: usize,
    pub radio_network_key_blockers: usize,
    pub supervision_blockers: usize,
    pub first_blocked_protocol: Option<ProtocolFamily>,
}

impl IntegrationMeshProtocolPrimitiveReadinessSummary {
    pub fn from_rows<'a>(
        rows: impl IntoIterator<Item = &'a IntegrationMeshProtocolPrimitiveReadinessRow>,
    ) -> Self {
        let rows = rows.into_iter().collect::<Vec<_>>();
        let unique_required_primitives = rows
            .iter()
            .flat_map(|row| row.required_primitives.iter().copied())
            .collect::<BTreeSet<_>>()
            .into_iter()
            .collect::<Vec<_>>();
        let unique_missing_primitives = rows
            .iter()
            .flat_map(|row| row.missing_primitives.iter().copied())
            .collect::<BTreeSet<_>>()
            .into_iter()
            .collect::<Vec<_>>();
        let ready_protocols = rows.iter().filter(|row| row.ready).count();
        let blocked_protocols = rows.len().saturating_sub(ready_protocols);
        let first_blocked_protocol = rows
            .iter()
            .filter(|row| row.has_missing_primitives())
            .min_by_key(|row| mesh_protocol_sort_key(&row.protocol))
            .map(|row| row.protocol.clone());

        Self {
            total_protocols: rows.len(),
            ready_protocols,
            blocked_protocols,
            required_primitive_count: unique_required_primitives.len(),
            missing_primitive_count: unique_missing_primitives.len(),
            unique_required_primitives,
            unique_missing_primitives,
            radio_network_key_blockers: rows
                .iter()
                .filter(|row| row.missing_radio_network_key())
                .count(),
            supervision_blockers: rows.iter().filter(|row| row.missing_supervision()).count(),
            first_blocked_protocol,
        }
    }

    pub fn all_ready(&self) -> bool {
        self.total_protocols > 0 && self.blocked_protocols == 0
    }

    pub fn has_blockers(&self) -> bool {
        self.blocked_protocols > 0
    }

    pub fn needs_radio_network_key(&self) -> bool {
        self.radio_network_key_blockers > 0
    }

    pub fn needs_supervision(&self) -> bool {
        self.supervision_blockers > 0
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReadinessPackageSummary {
    pub mesh_primitive_summary: IntegrationMeshProtocolPrimitiveReadinessSummary,
    pub remediation_summary: IntegrationActivationEvidenceRemediationSummary,
    pub total_protocols: usize,
    pub ready_protocols: usize,
    pub blocked_protocols: usize,
    pub missing_primitive_count: usize,
    pub radio_network_key_blockers: usize,
    pub supervision_blockers: usize,
    pub remediation_item_count: usize,
    pub blocked_integration_count: usize,
    pub readiness_remediation_items: usize,
    pub requires_human_review: bool,
    pub primitive_substrate_ready: bool,
    pub release_ready: bool,
    pub first_blocked_protocol: Option<ProtocolFamily>,
    pub next_remediation_kind: Option<IntegrationActivationEvidenceRemediationKind>,
}

impl IntegrationMeshReadinessPackageSummary {
    pub fn from_parts(
        mesh_primitive_summary: IntegrationMeshProtocolPrimitiveReadinessSummary,
        remediation_summary: IntegrationActivationEvidenceRemediationSummary,
    ) -> Self {
        let primitive_substrate_ready = mesh_primitive_summary.all_ready();
        let release_ready = primitive_substrate_ready && remediation_summary.is_empty();

        Self {
            total_protocols: mesh_primitive_summary.total_protocols,
            ready_protocols: mesh_primitive_summary.ready_protocols,
            blocked_protocols: mesh_primitive_summary.blocked_protocols,
            missing_primitive_count: mesh_primitive_summary.missing_primitive_count,
            radio_network_key_blockers: mesh_primitive_summary.radio_network_key_blockers,
            supervision_blockers: mesh_primitive_summary.supervision_blockers,
            remediation_item_count: remediation_summary.total_remediation_items,
            blocked_integration_count: remediation_summary.total_blocked_integrations,
            readiness_remediation_items: remediation_summary.readiness_remediation_items,
            requires_human_review: remediation_summary.requires_human_review(),
            first_blocked_protocol: mesh_primitive_summary.first_blocked_protocol.clone(),
            next_remediation_kind: remediation_summary.next_remediation_kind,
            mesh_primitive_summary,
            remediation_summary,
            primitive_substrate_ready,
            release_ready,
        }
    }

    pub fn has_blockers(&self) -> bool {
        !self.release_ready
    }

    pub fn has_remediations(&self) -> bool {
        self.remediation_summary.has_remediations()
    }

    pub fn needs_radio_network_key(&self) -> bool {
        self.radio_network_key_blockers > 0
    }

    pub fn needs_supervision(&self) -> bool {
        self.supervision_blockers > 0
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshStageReleaseSummary {
    pub package_summary: IntegrationMeshReadinessPackageSummary,
    pub substrate_stage_summary: IntegrationMeshProtocolSubstrateStageSummary,
    pub total_protocols: usize,
    pub stage_blocker_count: usize,
    pub primitive_blocker_count: usize,
    pub remediation_item_count: usize,
    pub controller_blockers: usize,
    pub radio_blockers: usize,
    pub discovery_blockers: usize,
    pub network_security_blockers: usize,
    pub supervision_blockers: usize,
    pub first_blocked_protocol: Option<ProtocolFamily>,
    pub first_blocked_stage: Option<IntegrationMeshProtocolSubstrateStage>,
    pub next_remediation_kind: Option<IntegrationActivationEvidenceRemediationKind>,
    pub substrate_ready: bool,
    pub package_ready: bool,
    pub release_ready: bool,
}

impl IntegrationMeshStageReleaseSummary {
    pub fn from_parts(
        package_summary: IntegrationMeshReadinessPackageSummary,
        substrate_stage_summary: IntegrationMeshProtocolSubstrateStageSummary,
    ) -> Self {
        let substrate_ready = substrate_stage_summary.all_ready();
        let package_ready = package_summary.release_ready;
        let release_ready = substrate_ready && package_ready;

        Self {
            total_protocols: package_summary.total_protocols,
            stage_blocker_count: substrate_stage_summary.blocked_rows,
            primitive_blocker_count: package_summary.missing_primitive_count,
            remediation_item_count: package_summary.remediation_item_count,
            controller_blockers: substrate_stage_summary.controller_blockers,
            radio_blockers: substrate_stage_summary.radio_blockers,
            discovery_blockers: substrate_stage_summary.discovery_blockers,
            network_security_blockers: substrate_stage_summary.network_security_blockers,
            supervision_blockers: substrate_stage_summary.supervision_blockers,
            first_blocked_protocol: substrate_stage_summary
                .first_blocked_protocol
                .clone()
                .or_else(|| package_summary.first_blocked_protocol.clone()),
            first_blocked_stage: substrate_stage_summary.first_blocked_stage,
            next_remediation_kind: package_summary.next_remediation_kind,
            package_summary,
            substrate_stage_summary,
            substrate_ready,
            package_ready,
            release_ready,
        }
    }

    pub fn has_blockers(&self) -> bool {
        !self.release_ready
    }

    pub fn has_stage_blockers(&self) -> bool {
        self.stage_blocker_count > 0
    }

    pub fn has_remediations(&self) -> bool {
        self.remediation_item_count > 0
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshActionReadinessSummary {
    pub release_summary: IntegrationMeshStageReleaseSummary,
    pub action_summary: IntegrationMeshProtocolSubstrateActionSummary,
    pub total_protocols: usize,
    pub queued_substrate_actions: usize,
    pub queued_stage_count: usize,
    pub queued_primitive_count: usize,
    pub remediation_item_count: usize,
    pub first_action_protocol: Option<ProtocolFamily>,
    pub first_action_stage: Option<IntegrationMeshProtocolSubstrateStage>,
    pub first_action_primitive: Option<PrimitiveFamily>,
    pub substrate_actions_ready: bool,
    pub release_ready: bool,
}

impl IntegrationMeshActionReadinessSummary {
    pub fn from_parts(
        release_summary: IntegrationMeshStageReleaseSummary,
        action_summary: IntegrationMeshProtocolSubstrateActionSummary,
    ) -> Self {
        let substrate_actions_ready = action_summary.is_empty();
        let release_ready = release_summary.release_ready && substrate_actions_ready;

        Self {
            total_protocols: release_summary.total_protocols,
            queued_substrate_actions: action_summary.total_actions,
            queued_stage_count: mesh_substrate_action_stage_count(&action_summary),
            queued_primitive_count: action_summary.unique_primitives,
            remediation_item_count: release_summary.remediation_item_count,
            first_action_protocol: action_summary.first_protocol.clone(),
            first_action_stage: action_summary.first_stage,
            first_action_primitive: action_summary.first_primitive,
            release_summary,
            action_summary,
            substrate_actions_ready,
            release_ready,
        }
    }

    pub fn has_substrate_actions(&self) -> bool {
        self.queued_substrate_actions > 0
    }

    pub fn has_remediations(&self) -> bool {
        self.remediation_item_count > 0
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseReadinessSummary {
    pub action_readiness_summary: IntegrationMeshActionReadinessSummary,
    pub total_protocols: usize,
    pub release_blocker_count: usize,
    pub stage_blocker_count: usize,
    pub primitive_blocker_count: usize,
    pub remediation_item_count: usize,
    pub queued_substrate_actions: usize,
    pub first_blocked_protocol: Option<ProtocolFamily>,
    pub first_blocked_stage: Option<IntegrationMeshProtocolSubstrateStage>,
    pub first_action_protocol: Option<ProtocolFamily>,
    pub first_action_stage: Option<IntegrationMeshProtocolSubstrateStage>,
    pub next_remediation_kind: Option<IntegrationActivationEvidenceRemediationKind>,
    pub package_ready: bool,
    pub substrate_ready: bool,
    pub substrate_actions_ready: bool,
    pub release_ready: bool,
}

impl IntegrationMeshReleaseReadinessSummary {
    pub fn from_action_readiness_summary(
        action_readiness_summary: IntegrationMeshActionReadinessSummary,
    ) -> Self {
        let stage_summary = &action_readiness_summary.release_summary;
        let release_blocker_count = stage_summary.stage_blocker_count
            + stage_summary.primitive_blocker_count
            + stage_summary.remediation_item_count
            + action_readiness_summary.queued_substrate_actions;

        Self {
            total_protocols: action_readiness_summary.total_protocols,
            release_blocker_count,
            stage_blocker_count: stage_summary.stage_blocker_count,
            primitive_blocker_count: stage_summary.primitive_blocker_count,
            remediation_item_count: stage_summary.remediation_item_count,
            queued_substrate_actions: action_readiness_summary.queued_substrate_actions,
            first_blocked_protocol: stage_summary.first_blocked_protocol.clone(),
            first_blocked_stage: stage_summary.first_blocked_stage,
            first_action_protocol: action_readiness_summary.first_action_protocol.clone(),
            first_action_stage: action_readiness_summary.first_action_stage,
            next_remediation_kind: stage_summary.next_remediation_kind,
            package_ready: stage_summary.package_ready,
            substrate_ready: stage_summary.substrate_ready,
            substrate_actions_ready: action_readiness_summary.substrate_actions_ready,
            release_ready: action_readiness_summary.release_ready,
            action_readiness_summary,
        }
    }

    pub fn has_blockers(&self) -> bool {
        !self.release_ready
    }

    pub fn has_queued_actions(&self) -> bool {
        self.queued_substrate_actions > 0
    }

    pub fn has_remediations(&self) -> bool {
        self.remediation_item_count > 0
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshPreflightReadinessSummary {
    pub release_readiness_summary: IntegrationMeshReleaseReadinessSummary,
    pub preflight_summary: IntegrationMeshProtocolSubstratePreflightSummary,
    pub total_protocols: usize,
    pub total_preflight_checks: usize,
    pub preflight_blocker_count: usize,
    pub release_blocker_count: usize,
    pub total_blocker_count: usize,
    pub operator_required_checks: usize,
    pub first_failed_protocol: Option<ProtocolFamily>,
    pub first_failed_stage: Option<IntegrationMeshProtocolSubstrateStage>,
    pub first_failed_primitive: Option<PrimitiveFamily>,
    pub next_remediation_kind: Option<IntegrationActivationEvidenceRemediationKind>,
    pub preflight_ready: bool,
    pub release_ready: bool,
    pub ready_for_release: bool,
}

impl IntegrationMeshPreflightReadinessSummary {
    pub fn from_parts(
        release_readiness_summary: IntegrationMeshReleaseReadinessSummary,
        preflight_summary: IntegrationMeshProtocolSubstratePreflightSummary,
    ) -> Self {
        let total_blocker_count =
            release_readiness_summary.release_blocker_count + preflight_summary.blocking_checks;

        Self {
            total_protocols: release_readiness_summary.total_protocols,
            total_preflight_checks: preflight_summary.total_checks,
            preflight_blocker_count: preflight_summary.blocking_checks,
            release_blocker_count: release_readiness_summary.release_blocker_count,
            total_blocker_count,
            operator_required_checks: preflight_summary.operator_required_checks,
            first_failed_protocol: preflight_summary.first_failed_protocol.clone(),
            first_failed_stage: preflight_summary.first_failed_stage,
            first_failed_primitive: preflight_summary.first_failed_primitive,
            next_remediation_kind: release_readiness_summary.next_remediation_kind,
            preflight_ready: preflight_summary.ready(),
            release_ready: release_readiness_summary.release_ready,
            ready_for_release: preflight_summary.ready() && release_readiness_summary.release_ready,
            release_readiness_summary,
            preflight_summary,
        }
    }

    pub fn has_blockers(&self) -> bool {
        self.total_blocker_count > 0
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_checks > 0
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshPreflightRepairReadinessSummary {
    pub preflight_readiness_summary: IntegrationMeshPreflightReadinessSummary,
    pub preflight_action_summary: IntegrationMeshProtocolSubstratePreflightActionSummary,
    pub total_protocols: usize,
    pub total_preflight_checks: usize,
    pub preflight_blocker_count: usize,
    pub release_blocker_count: usize,
    pub queued_preflight_actions: usize,
    pub blocking_preflight_actions: usize,
    pub operator_required_actions: usize,
    pub queued_stage_count: usize,
    pub queued_primitive_count: usize,
    pub first_repair_protocol: Option<ProtocolFamily>,
    pub first_repair_stage: Option<IntegrationMeshProtocolSubstrateStage>,
    pub first_repair_primitive: Option<PrimitiveFamily>,
    pub first_repair_action_kind: Option<IntegrationMeshProtocolSubstratePreflightActionKind>,
    pub preflight_ready: bool,
    pub preflight_actions_ready: bool,
    pub release_ready: bool,
    pub ready_for_release: bool,
}

impl IntegrationMeshPreflightRepairReadinessSummary {
    pub fn from_parts(
        preflight_readiness_summary: IntegrationMeshPreflightReadinessSummary,
        preflight_action_summary: IntegrationMeshProtocolSubstratePreflightActionSummary,
    ) -> Self {
        let preflight_actions_ready = preflight_action_summary.preflight_actions_ready;
        let ready_for_release =
            preflight_readiness_summary.ready_for_release && preflight_actions_ready;

        Self {
            total_protocols: preflight_readiness_summary.total_protocols,
            total_preflight_checks: preflight_readiness_summary.total_preflight_checks,
            preflight_blocker_count: preflight_readiness_summary.preflight_blocker_count,
            release_blocker_count: preflight_readiness_summary.release_blocker_count,
            queued_preflight_actions: preflight_action_summary.total_actions,
            blocking_preflight_actions: preflight_action_summary.blocking_actions,
            operator_required_actions: preflight_action_summary.operator_required_actions,
            queued_stage_count: mesh_substrate_preflight_action_stage_count(
                &preflight_action_summary,
            ),
            queued_primitive_count: preflight_action_summary.unique_primitives,
            first_repair_protocol: preflight_action_summary.first_protocol.clone(),
            first_repair_stage: preflight_action_summary.first_stage,
            first_repair_primitive: preflight_action_summary.first_primitive,
            first_repair_action_kind: preflight_action_summary.first_action_kind,
            preflight_ready: preflight_readiness_summary.preflight_ready,
            release_ready: preflight_readiness_summary.release_ready,
            preflight_readiness_summary,
            preflight_action_summary,
            preflight_actions_ready,
            ready_for_release,
        }
    }

    pub fn has_repair_actions(&self) -> bool {
        self.queued_preflight_actions > 0
    }

    pub fn has_blockers(&self) -> bool {
        !self.ready_for_release
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_actions > 0 || self.preflight_readiness_summary.needs_operator()
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshPreflightBatchReadinessSummary {
    pub repair_readiness_summary: IntegrationMeshPreflightRepairReadinessSummary,
    pub repair_batch_summary: IntegrationMeshProtocolSubstratePreflightRepairBatchSummary,
    pub total_protocols: usize,
    pub total_preflight_checks: usize,
    pub preflight_blocker_count: usize,
    pub release_blocker_count: usize,
    pub queued_preflight_actions: usize,
    pub repair_batch_count: usize,
    pub batched_preflight_actions: usize,
    pub blocking_repair_batches: usize,
    pub operator_required_batches: usize,
    pub first_batch_stage: Option<IntegrationMeshProtocolSubstrateStage>,
    pub first_batch_action_kind: Option<IntegrationMeshProtocolSubstratePreflightActionKind>,
    pub first_batch_protocol: Option<ProtocolFamily>,
    pub first_batch_primitive: Option<PrimitiveFamily>,
    pub preflight_ready: bool,
    pub repair_actions_ready: bool,
    pub repair_batches_ready: bool,
    pub release_ready: bool,
    pub ready_for_release: bool,
}

impl IntegrationMeshPreflightBatchReadinessSummary {
    pub fn from_parts(
        repair_readiness_summary: IntegrationMeshPreflightRepairReadinessSummary,
        repair_batch_summary: IntegrationMeshProtocolSubstratePreflightRepairBatchSummary,
    ) -> Self {
        let repair_batches_ready = repair_batch_summary.repair_batches_ready;
        let ready_for_release = repair_readiness_summary.ready_for_release && repair_batches_ready;

        Self {
            total_protocols: repair_readiness_summary.total_protocols,
            total_preflight_checks: repair_readiness_summary.total_preflight_checks,
            preflight_blocker_count: repair_readiness_summary.preflight_blocker_count,
            release_blocker_count: repair_readiness_summary.release_blocker_count,
            queued_preflight_actions: repair_readiness_summary.queued_preflight_actions,
            repair_batch_count: repair_batch_summary.total_batches,
            batched_preflight_actions: repair_batch_summary.total_actions,
            blocking_repair_batches: repair_batch_summary.blocking_batches,
            operator_required_batches: repair_batch_summary.operator_required_batches,
            first_batch_stage: repair_batch_summary.first_stage,
            first_batch_action_kind: repair_batch_summary.first_action_kind,
            first_batch_protocol: repair_batch_summary.first_protocol.clone(),
            first_batch_primitive: repair_batch_summary.first_primitive,
            preflight_ready: repair_readiness_summary.preflight_ready,
            repair_actions_ready: repair_readiness_summary.preflight_actions_ready,
            release_ready: repair_readiness_summary.release_ready,
            repair_readiness_summary,
            repair_batch_summary,
            repair_batches_ready,
            ready_for_release,
        }
    }

    pub fn has_repair_batches(&self) -> bool {
        self.repair_batch_count > 0
    }

    pub fn has_blockers(&self) -> bool {
        !self.ready_for_release
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_batches > 0 || self.repair_readiness_summary.needs_operator()
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshPreflightScheduleReadinessSummary {
    pub batch_readiness_summary: IntegrationMeshPreflightBatchReadinessSummary,
    pub repair_schedule_summary: IntegrationMeshProtocolSubstratePreflightRepairScheduleSummary,
    pub total_protocols: usize,
    pub total_preflight_checks: usize,
    pub preflight_blocker_count: usize,
    pub release_blocker_count: usize,
    pub queued_preflight_actions: usize,
    pub repair_batch_count: usize,
    pub repair_slot_count: usize,
    pub scheduled_preflight_actions: usize,
    pub blocking_repair_slots: usize,
    pub operator_required_slots: usize,
    pub first_schedule_stage: Option<IntegrationMeshProtocolSubstrateStage>,
    pub first_schedule_action_kind: Option<IntegrationMeshProtocolSubstratePreflightActionKind>,
    pub first_schedule_protocol: Option<ProtocolFamily>,
    pub first_schedule_primitive: Option<PrimitiveFamily>,
    pub preflight_ready: bool,
    pub repair_actions_ready: bool,
    pub repair_batches_ready: bool,
    pub repair_schedule_ready: bool,
    pub release_ready: bool,
    pub ready_for_release: bool,
}

impl IntegrationMeshPreflightScheduleReadinessSummary {
    pub fn from_parts(
        batch_readiness_summary: IntegrationMeshPreflightBatchReadinessSummary,
        repair_schedule_summary: IntegrationMeshProtocolSubstratePreflightRepairScheduleSummary,
    ) -> Self {
        let repair_schedule_ready = repair_schedule_summary.repair_schedule_ready;
        let ready_for_release = batch_readiness_summary.ready_for_release && repair_schedule_ready;

        Self {
            total_protocols: batch_readiness_summary.total_protocols,
            total_preflight_checks: batch_readiness_summary.total_preflight_checks,
            preflight_blocker_count: batch_readiness_summary.preflight_blocker_count,
            release_blocker_count: batch_readiness_summary.release_blocker_count,
            queued_preflight_actions: batch_readiness_summary.queued_preflight_actions,
            repair_batch_count: batch_readiness_summary.repair_batch_count,
            repair_slot_count: repair_schedule_summary.total_slots,
            scheduled_preflight_actions: repair_schedule_summary.total_actions,
            blocking_repair_slots: repair_schedule_summary.blocking_slots,
            operator_required_slots: repair_schedule_summary.operator_required_slots,
            first_schedule_stage: repair_schedule_summary.first_stage,
            first_schedule_action_kind: repair_schedule_summary.first_action_kind,
            first_schedule_protocol: repair_schedule_summary.first_protocol.clone(),
            first_schedule_primitive: repair_schedule_summary.first_primitive,
            preflight_ready: batch_readiness_summary.preflight_ready,
            repair_actions_ready: batch_readiness_summary.repair_actions_ready,
            repair_batches_ready: batch_readiness_summary.repair_batches_ready,
            release_ready: batch_readiness_summary.release_ready,
            batch_readiness_summary,
            repair_schedule_summary,
            repair_schedule_ready,
            ready_for_release,
        }
    }

    pub fn has_repair_schedule(&self) -> bool {
        self.repair_slot_count > 0
    }

    pub fn has_blockers(&self) -> bool {
        !self.ready_for_release
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_slots > 0 || self.batch_readiness_summary.needs_operator()
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshPreflightSlotReadinessSummary {
    pub schedule_readiness_summary: IntegrationMeshPreflightScheduleReadinessSummary,
    pub slot_audit_summary: IntegrationMeshProtocolSubstratePreflightRepairSlotAuditSummary,
    pub total_protocols: usize,
    pub total_preflight_checks: usize,
    pub preflight_blocker_count: usize,
    pub release_blocker_count: usize,
    pub queued_preflight_actions: usize,
    pub repair_batch_count: usize,
    pub repair_slot_count: usize,
    pub scheduled_preflight_actions: usize,
    pub slot_audit_rows: usize,
    pub audited_preflight_actions: usize,
    pub blocking_slot_audit_rows: usize,
    pub operator_required_slot_audit_rows: usize,
    pub first_blocking_slot_sequence: Option<usize>,
    pub first_operator_slot_sequence: Option<usize>,
    pub first_slot_stage: Option<IntegrationMeshProtocolSubstrateStage>,
    pub first_slot_action_kind: Option<IntegrationMeshProtocolSubstratePreflightActionKind>,
    pub preflight_ready: bool,
    pub repair_actions_ready: bool,
    pub repair_batches_ready: bool,
    pub repair_schedule_ready: bool,
    pub repair_slot_audit_ready: bool,
    pub release_ready: bool,
    pub ready_for_release: bool,
}

impl IntegrationMeshPreflightSlotReadinessSummary {
    pub fn from_parts(
        schedule_readiness_summary: IntegrationMeshPreflightScheduleReadinessSummary,
        slot_audit_summary: IntegrationMeshProtocolSubstratePreflightRepairSlotAuditSummary,
    ) -> Self {
        let repair_slot_audit_ready = slot_audit_summary.repair_slot_audit_ready;
        let ready_for_release =
            schedule_readiness_summary.ready_for_release && repair_slot_audit_ready;

        Self {
            total_protocols: schedule_readiness_summary.total_protocols,
            total_preflight_checks: schedule_readiness_summary.total_preflight_checks,
            preflight_blocker_count: schedule_readiness_summary.preflight_blocker_count,
            release_blocker_count: schedule_readiness_summary.release_blocker_count,
            queued_preflight_actions: schedule_readiness_summary.queued_preflight_actions,
            repair_batch_count: schedule_readiness_summary.repair_batch_count,
            repair_slot_count: schedule_readiness_summary.repair_slot_count,
            scheduled_preflight_actions: schedule_readiness_summary.scheduled_preflight_actions,
            slot_audit_rows: slot_audit_summary.total_rows,
            audited_preflight_actions: slot_audit_summary.scheduled_actions,
            blocking_slot_audit_rows: slot_audit_summary.blocking_rows,
            operator_required_slot_audit_rows: slot_audit_summary.operator_required_rows,
            first_blocking_slot_sequence: slot_audit_summary.first_blocking_sequence,
            first_operator_slot_sequence: slot_audit_summary.first_operator_required_sequence,
            first_slot_stage: slot_audit_summary.first_stage,
            first_slot_action_kind: slot_audit_summary.first_action_kind,
            preflight_ready: schedule_readiness_summary.preflight_ready,
            repair_actions_ready: schedule_readiness_summary.repair_actions_ready,
            repair_batches_ready: schedule_readiness_summary.repair_batches_ready,
            repair_schedule_ready: schedule_readiness_summary.repair_schedule_ready,
            release_ready: schedule_readiness_summary.release_ready,
            schedule_readiness_summary,
            slot_audit_summary,
            repair_slot_audit_ready,
            ready_for_release,
        }
    }

    pub fn has_slot_audit_rows(&self) -> bool {
        self.slot_audit_rows > 0
    }

    pub fn has_blockers(&self) -> bool {
        !self.ready_for_release
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_slot_audit_rows > 0
            || self.schedule_readiness_summary.needs_operator()
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshPreflightExecutionReadinessSummary {
    pub slot_readiness_summary: IntegrationMeshPreflightSlotReadinessSummary,
    pub execution_ticket_summary:
        IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionTicketSummary,
    pub total_protocols: usize,
    pub total_preflight_checks: usize,
    pub preflight_blocker_count: usize,
    pub release_blocker_count: usize,
    pub queued_preflight_actions: usize,
    pub repair_batch_count: usize,
    pub repair_slot_count: usize,
    pub scheduled_preflight_actions: usize,
    pub slot_audit_rows: usize,
    pub execution_ticket_count: usize,
    pub ticketed_preflight_actions: usize,
    pub blocking_execution_tickets: usize,
    pub operator_required_execution_tickets: usize,
    pub first_execution_ticket_key: Option<String>,
    pub first_blocking_execution_ticket_key: Option<String>,
    pub first_operator_execution_ticket_key: Option<String>,
    pub first_execution_ticket_stage: Option<IntegrationMeshProtocolSubstrateStage>,
    pub first_execution_ticket_action_kind:
        Option<IntegrationMeshProtocolSubstratePreflightActionKind>,
    pub preflight_ready: bool,
    pub repair_actions_ready: bool,
    pub repair_batches_ready: bool,
    pub repair_schedule_ready: bool,
    pub repair_slot_audit_ready: bool,
    pub execution_tickets_ready: bool,
    pub release_ready: bool,
    pub ready_for_release: bool,
}

impl IntegrationMeshPreflightExecutionReadinessSummary {
    pub fn from_parts(
        slot_readiness_summary: IntegrationMeshPreflightSlotReadinessSummary,
        execution_ticket_summary: IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionTicketSummary,
    ) -> Self {
        let execution_tickets_ready = execution_ticket_summary.execution_tickets_ready;
        let ready_for_release = slot_readiness_summary.ready_for_release && execution_tickets_ready;

        Self {
            total_protocols: slot_readiness_summary.total_protocols,
            total_preflight_checks: slot_readiness_summary.total_preflight_checks,
            preflight_blocker_count: slot_readiness_summary.preflight_blocker_count,
            release_blocker_count: slot_readiness_summary.release_blocker_count,
            queued_preflight_actions: slot_readiness_summary.queued_preflight_actions,
            repair_batch_count: slot_readiness_summary.repair_batch_count,
            repair_slot_count: slot_readiness_summary.repair_slot_count,
            scheduled_preflight_actions: slot_readiness_summary.scheduled_preflight_actions,
            slot_audit_rows: slot_readiness_summary.slot_audit_rows,
            execution_ticket_count: execution_ticket_summary.total_tickets,
            ticketed_preflight_actions: execution_ticket_summary.scheduled_actions,
            blocking_execution_tickets: execution_ticket_summary.blocking_tickets,
            operator_required_execution_tickets: execution_ticket_summary.operator_required_tickets,
            first_execution_ticket_key: execution_ticket_summary.first_ticket_key.clone(),
            first_blocking_execution_ticket_key: execution_ticket_summary
                .first_blocking_ticket_key
                .clone(),
            first_operator_execution_ticket_key: execution_ticket_summary
                .first_operator_ticket_key
                .clone(),
            first_execution_ticket_stage: execution_ticket_summary.first_stage,
            first_execution_ticket_action_kind: execution_ticket_summary.first_action_kind,
            preflight_ready: slot_readiness_summary.preflight_ready,
            repair_actions_ready: slot_readiness_summary.repair_actions_ready,
            repair_batches_ready: slot_readiness_summary.repair_batches_ready,
            repair_schedule_ready: slot_readiness_summary.repair_schedule_ready,
            repair_slot_audit_ready: slot_readiness_summary.repair_slot_audit_ready,
            release_ready: slot_readiness_summary.release_ready,
            slot_readiness_summary,
            execution_ticket_summary,
            execution_tickets_ready,
            ready_for_release,
        }
    }

    pub fn has_execution_tickets(&self) -> bool {
        self.execution_ticket_count > 0
    }

    pub fn has_blockers(&self) -> bool {
        !self.ready_for_release
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_execution_tickets > 0 || self.slot_readiness_summary.needs_operator()
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshPreflightWorkOrderReadinessSummary {
    pub execution_readiness_summary: IntegrationMeshPreflightExecutionReadinessSummary,
    pub execution_work_order_summary:
        IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionWorkOrderSummary,
    pub total_protocols: usize,
    pub total_preflight_checks: usize,
    pub preflight_blocker_count: usize,
    pub release_blocker_count: usize,
    pub queued_preflight_actions: usize,
    pub repair_batch_count: usize,
    pub repair_slot_count: usize,
    pub scheduled_preflight_actions: usize,
    pub execution_ticket_count: usize,
    pub execution_work_order_count: usize,
    pub ordered_preflight_actions: usize,
    pub blocking_execution_work_orders: usize,
    pub operator_required_execution_work_orders: usize,
    pub first_execution_work_order_key: Option<String>,
    pub first_blocking_execution_work_order_key: Option<String>,
    pub first_operator_execution_work_order_key: Option<String>,
    pub first_execution_ticket_key: Option<String>,
    pub first_execution_work_order_stage: Option<IntegrationMeshProtocolSubstrateStage>,
    pub first_execution_work_order_action_kind:
        Option<IntegrationMeshProtocolSubstratePreflightActionKind>,
    pub preflight_ready: bool,
    pub repair_actions_ready: bool,
    pub repair_batches_ready: bool,
    pub repair_schedule_ready: bool,
    pub repair_slot_audit_ready: bool,
    pub execution_tickets_ready: bool,
    pub execution_work_orders_ready: bool,
    pub release_ready: bool,
    pub ready_for_release: bool,
}

impl IntegrationMeshPreflightWorkOrderReadinessSummary {
    pub fn from_parts(
        execution_readiness_summary: IntegrationMeshPreflightExecutionReadinessSummary,
        execution_work_order_summary: IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionWorkOrderSummary,
    ) -> Self {
        let execution_work_orders_ready = execution_work_order_summary.execution_work_orders_ready;
        let ready_for_release =
            execution_readiness_summary.ready_for_release && execution_work_orders_ready;

        Self {
            total_protocols: execution_readiness_summary.total_protocols,
            total_preflight_checks: execution_readiness_summary.total_preflight_checks,
            preflight_blocker_count: execution_readiness_summary.preflight_blocker_count,
            release_blocker_count: execution_readiness_summary.release_blocker_count,
            queued_preflight_actions: execution_readiness_summary.queued_preflight_actions,
            repair_batch_count: execution_readiness_summary.repair_batch_count,
            repair_slot_count: execution_readiness_summary.repair_slot_count,
            scheduled_preflight_actions: execution_readiness_summary.scheduled_preflight_actions,
            execution_ticket_count: execution_readiness_summary.execution_ticket_count,
            execution_work_order_count: execution_work_order_summary.total_work_orders,
            ordered_preflight_actions: execution_work_order_summary.scheduled_actions,
            blocking_execution_work_orders: execution_work_order_summary.blocking_work_orders,
            operator_required_execution_work_orders: execution_work_order_summary
                .operator_required_work_orders,
            first_execution_work_order_key: execution_work_order_summary
                .first_work_order_key
                .clone(),
            first_blocking_execution_work_order_key: execution_work_order_summary
                .first_blocking_work_order_key
                .clone(),
            first_operator_execution_work_order_key: execution_work_order_summary
                .first_operator_work_order_key
                .clone(),
            first_execution_ticket_key: execution_work_order_summary.first_ticket_key.clone(),
            first_execution_work_order_stage: execution_work_order_summary.first_stage,
            first_execution_work_order_action_kind: execution_work_order_summary.first_action_kind,
            preflight_ready: execution_readiness_summary.preflight_ready,
            repair_actions_ready: execution_readiness_summary.repair_actions_ready,
            repair_batches_ready: execution_readiness_summary.repair_batches_ready,
            repair_schedule_ready: execution_readiness_summary.repair_schedule_ready,
            repair_slot_audit_ready: execution_readiness_summary.repair_slot_audit_ready,
            execution_tickets_ready: execution_readiness_summary.execution_tickets_ready,
            release_ready: execution_readiness_summary.release_ready,
            execution_readiness_summary,
            execution_work_order_summary,
            execution_work_orders_ready,
            ready_for_release,
        }
    }

    pub fn has_execution_work_orders(&self) -> bool {
        self.execution_work_order_count > 0
    }

    pub fn has_blockers(&self) -> bool {
        !self.ready_for_release
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_execution_work_orders > 0
            || self.execution_readiness_summary.needs_operator()
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshPreflightGuardrailReadinessSummary {
    pub work_order_readiness_summary: IntegrationMeshPreflightWorkOrderReadinessSummary,
    pub work_order_guardrail_summary:
        IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionWorkOrderGuardrailSummary,
    pub evidence_review_disposition_summary:
        IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionSummary,
    pub total_protocols: usize,
    pub total_preflight_checks: usize,
    pub preflight_blocker_count: usize,
    pub release_blocker_count: usize,
    pub queued_preflight_actions: usize,
    pub repair_batch_count: usize,
    pub repair_slot_count: usize,
    pub scheduled_preflight_actions: usize,
    pub execution_ticket_count: usize,
    pub execution_work_order_count: usize,
    pub ordered_preflight_actions: usize,
    pub work_order_guardrail_count: usize,
    pub release_blocker_guardrails: usize,
    pub operator_handoff_guardrails: usize,
    pub ready_to_execute_guardrails: usize,
    pub evidence_review_dispositions: usize,
    pub blocking_dispositions: usize,
    pub operator_handoff_dispositions: usize,
    pub repair_required_dispositions: usize,
    pub lineage_gap_dispositions: usize,
    pub review_required_dispositions: usize,
    pub first_guardrail_work_order_key: Option<String>,
    pub first_release_blocker_work_order_key: Option<String>,
    pub first_operator_handoff_work_order_key: Option<String>,
    pub first_disposition_key: Option<String>,
    pub first_operator_handoff_disposition_key: Option<String>,
    pub first_repair_required_disposition_key: Option<String>,
    pub first_lineage_gap_disposition_key: Option<String>,
    pub first_guardrail_stage: Option<IntegrationMeshProtocolSubstrateStage>,
    pub first_guardrail_action_kind: Option<IntegrationMeshProtocolSubstratePreflightActionKind>,
    pub preflight_ready: bool,
    pub repair_actions_ready: bool,
    pub repair_batches_ready: bool,
    pub repair_schedule_ready: bool,
    pub repair_slot_audit_ready: bool,
    pub execution_tickets_ready: bool,
    pub execution_work_orders_ready: bool,
    pub execution_work_order_guardrails_ready: bool,
    pub execution_evidence_review_dispositions_ready: bool,
    pub release_ready: bool,
    pub ready_for_release: bool,
}

impl IntegrationMeshPreflightGuardrailReadinessSummary {
    pub fn from_parts(
        work_order_readiness_summary: IntegrationMeshPreflightWorkOrderReadinessSummary,
        work_order_guardrail_summary: IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionWorkOrderGuardrailSummary,
        evidence_review_disposition_summary: IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionSummary,
    ) -> Self {
        let execution_work_order_guardrails_ready =
            work_order_guardrail_summary.execution_work_order_guardrails_ready;
        let execution_evidence_review_dispositions_ready =
            evidence_review_disposition_summary.execution_evidence_review_dispositions_ready;
        let ready_for_release = work_order_readiness_summary.ready_for_release
            && execution_work_order_guardrails_ready
            && execution_evidence_review_dispositions_ready;

        Self {
            total_protocols: work_order_readiness_summary.total_protocols,
            total_preflight_checks: work_order_readiness_summary.total_preflight_checks,
            preflight_blocker_count: work_order_readiness_summary.preflight_blocker_count,
            release_blocker_count: work_order_readiness_summary.release_blocker_count,
            queued_preflight_actions: work_order_readiness_summary.queued_preflight_actions,
            repair_batch_count: work_order_readiness_summary.repair_batch_count,
            repair_slot_count: work_order_readiness_summary.repair_slot_count,
            scheduled_preflight_actions: work_order_readiness_summary.scheduled_preflight_actions,
            execution_ticket_count: work_order_readiness_summary.execution_ticket_count,
            execution_work_order_count: work_order_readiness_summary.execution_work_order_count,
            ordered_preflight_actions: work_order_readiness_summary.ordered_preflight_actions,
            work_order_guardrail_count: work_order_guardrail_summary.total_guardrails,
            release_blocker_guardrails: work_order_guardrail_summary.release_blocker_guardrails,
            operator_handoff_guardrails: work_order_guardrail_summary.operator_handoff_guardrails,
            ready_to_execute_guardrails: work_order_guardrail_summary.ready_to_execute_guardrails,
            evidence_review_dispositions: evidence_review_disposition_summary.total_dispositions,
            blocking_dispositions: evidence_review_disposition_summary.blocking_dispositions,
            operator_handoff_dispositions: evidence_review_disposition_summary
                .operator_handoff_dispositions,
            repair_required_dispositions: evidence_review_disposition_summary
                .repair_required_dispositions,
            lineage_gap_dispositions: evidence_review_disposition_summary.lineage_gap_dispositions,
            review_required_dispositions: evidence_review_disposition_summary
                .review_required_dispositions,
            first_guardrail_work_order_key: work_order_guardrail_summary
                .first_work_order_key
                .clone(),
            first_release_blocker_work_order_key: work_order_guardrail_summary
                .first_release_blocker_work_order_key
                .clone(),
            first_operator_handoff_work_order_key: work_order_guardrail_summary
                .first_operator_handoff_work_order_key
                .clone(),
            first_disposition_key: evidence_review_disposition_summary
                .first_disposition_key
                .clone(),
            first_operator_handoff_disposition_key: evidence_review_disposition_summary
                .first_operator_handoff_disposition_key
                .clone(),
            first_repair_required_disposition_key: evidence_review_disposition_summary
                .first_repair_required_disposition_key
                .clone(),
            first_lineage_gap_disposition_key: evidence_review_disposition_summary
                .first_lineage_gap_disposition_key
                .clone(),
            first_guardrail_stage: work_order_guardrail_summary.first_stage,
            first_guardrail_action_kind: work_order_guardrail_summary.first_action_kind,
            preflight_ready: work_order_readiness_summary.preflight_ready,
            repair_actions_ready: work_order_readiness_summary.repair_actions_ready,
            repair_batches_ready: work_order_readiness_summary.repair_batches_ready,
            repair_schedule_ready: work_order_readiness_summary.repair_schedule_ready,
            repair_slot_audit_ready: work_order_readiness_summary.repair_slot_audit_ready,
            execution_tickets_ready: work_order_readiness_summary.execution_tickets_ready,
            execution_work_orders_ready: work_order_readiness_summary.execution_work_orders_ready,
            release_ready: work_order_readiness_summary.release_ready,
            work_order_readiness_summary,
            work_order_guardrail_summary,
            evidence_review_disposition_summary,
            execution_work_order_guardrails_ready,
            execution_evidence_review_dispositions_ready,
            ready_for_release,
        }
    }

    pub fn has_guardrails(&self) -> bool {
        self.work_order_guardrail_count > 0
    }

    pub fn has_dispositions(&self) -> bool {
        self.evidence_review_dispositions > 0
    }

    pub fn has_blockers(&self) -> bool {
        !self.ready_for_release
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_handoff_guardrails > 0
            || self.operator_handoff_dispositions > 0
            || self.work_order_readiness_summary.needs_operator()
    }

    pub fn needs_review(&self) -> bool {
        self.review_required_dispositions > 0
    }

    pub fn needs_repair(&self) -> bool {
        self.repair_required_dispositions > 0
    }

    pub fn has_lineage_gaps(&self) -> bool {
        self.lineage_gap_dispositions > 0
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshPreflightDispositionActionReadinessSummary {
    pub guardrail_readiness_summary: IntegrationMeshPreflightGuardrailReadinessSummary,
    pub disposition_action_summary:
        IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionActionSummary,
    pub total_protocols: usize,
    pub total_preflight_checks: usize,
    pub preflight_blocker_count: usize,
    pub release_blocker_count: usize,
    pub queued_preflight_actions: usize,
    pub repair_batch_count: usize,
    pub repair_slot_count: usize,
    pub scheduled_preflight_actions: usize,
    pub execution_ticket_count: usize,
    pub execution_work_order_count: usize,
    pub work_order_guardrail_count: usize,
    pub evidence_review_dispositions: usize,
    pub disposition_action_count: usize,
    pub required_disposition_actions: usize,
    pub blocking_disposition_actions: usize,
    pub operator_handoff_actions: usize,
    pub repair_actions: usize,
    pub lineage_gap_actions: usize,
    pub release_execution_actions: usize,
    pub review_required_actions: usize,
    pub lineage_complete_actions: usize,
    pub first_disposition_action_key: Option<String>,
    pub first_required_disposition_action_key: Option<String>,
    pub first_operator_handoff_action_key: Option<String>,
    pub first_repair_action_key: Option<String>,
    pub first_lineage_gap_action_key: Option<String>,
    pub first_release_execution_action_key: Option<String>,
    pub first_disposition_key: Option<String>,
    pub first_work_order_key: Option<String>,
    pub first_ticket_key: Option<String>,
    pub first_action_stage: Option<IntegrationMeshProtocolSubstrateStage>,
    pub first_action_kind: Option<IntegrationMeshProtocolSubstratePreflightActionKind>,
    pub preflight_ready: bool,
    pub repair_actions_ready: bool,
    pub repair_batches_ready: bool,
    pub repair_schedule_ready: bool,
    pub repair_slot_audit_ready: bool,
    pub execution_tickets_ready: bool,
    pub execution_work_orders_ready: bool,
    pub execution_work_order_guardrails_ready: bool,
    pub execution_evidence_review_dispositions_ready: bool,
    pub execution_evidence_review_disposition_actions_ready: bool,
    pub release_ready: bool,
    pub ready_for_release: bool,
}

impl IntegrationMeshPreflightDispositionActionReadinessSummary {
    pub fn from_parts(
        guardrail_readiness_summary: IntegrationMeshPreflightGuardrailReadinessSummary,
        disposition_action_summary: IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionActionSummary,
    ) -> Self {
        let execution_evidence_review_disposition_actions_ready =
            disposition_action_summary.execution_evidence_review_disposition_actions_ready;
        let ready_for_release = guardrail_readiness_summary.ready_for_release
            && execution_evidence_review_disposition_actions_ready;

        Self {
            total_protocols: guardrail_readiness_summary.total_protocols,
            total_preflight_checks: guardrail_readiness_summary.total_preflight_checks,
            preflight_blocker_count: guardrail_readiness_summary.preflight_blocker_count,
            release_blocker_count: guardrail_readiness_summary.release_blocker_count,
            queued_preflight_actions: guardrail_readiness_summary.queued_preflight_actions,
            repair_batch_count: guardrail_readiness_summary.repair_batch_count,
            repair_slot_count: guardrail_readiness_summary.repair_slot_count,
            scheduled_preflight_actions: guardrail_readiness_summary.scheduled_preflight_actions,
            execution_ticket_count: guardrail_readiness_summary.execution_ticket_count,
            execution_work_order_count: guardrail_readiness_summary.execution_work_order_count,
            work_order_guardrail_count: guardrail_readiness_summary.work_order_guardrail_count,
            evidence_review_dispositions: guardrail_readiness_summary.evidence_review_dispositions,
            disposition_action_count: disposition_action_summary.total_actions,
            required_disposition_actions: disposition_action_summary.required_actions,
            blocking_disposition_actions: disposition_action_summary.blocking_actions,
            operator_handoff_actions: disposition_action_summary.operator_handoff_actions,
            repair_actions: disposition_action_summary.repair_actions,
            lineage_gap_actions: disposition_action_summary.lineage_gap_actions,
            release_execution_actions: disposition_action_summary.release_execution_actions,
            review_required_actions: disposition_action_summary.review_required_actions,
            lineage_complete_actions: disposition_action_summary.lineage_complete_actions,
            first_disposition_action_key: disposition_action_summary.first_action_key.clone(),
            first_required_disposition_action_key: disposition_action_summary
                .first_required_action_key
                .clone(),
            first_operator_handoff_action_key: disposition_action_summary
                .first_operator_handoff_action_key
                .clone(),
            first_repair_action_key: disposition_action_summary.first_repair_action_key.clone(),
            first_lineage_gap_action_key: disposition_action_summary
                .first_lineage_gap_action_key
                .clone(),
            first_release_execution_action_key: disposition_action_summary
                .first_release_execution_action_key
                .clone(),
            first_disposition_key: disposition_action_summary.first_disposition_key.clone(),
            first_work_order_key: disposition_action_summary.first_work_order_key.clone(),
            first_ticket_key: disposition_action_summary.first_ticket_key.clone(),
            first_action_stage: disposition_action_summary.first_stage,
            first_action_kind: disposition_action_summary.first_action_kind,
            preflight_ready: guardrail_readiness_summary.preflight_ready,
            repair_actions_ready: guardrail_readiness_summary.repair_actions_ready,
            repair_batches_ready: guardrail_readiness_summary.repair_batches_ready,
            repair_schedule_ready: guardrail_readiness_summary.repair_schedule_ready,
            repair_slot_audit_ready: guardrail_readiness_summary.repair_slot_audit_ready,
            execution_tickets_ready: guardrail_readiness_summary.execution_tickets_ready,
            execution_work_orders_ready: guardrail_readiness_summary.execution_work_orders_ready,
            execution_work_order_guardrails_ready: guardrail_readiness_summary
                .execution_work_order_guardrails_ready,
            execution_evidence_review_dispositions_ready: guardrail_readiness_summary
                .execution_evidence_review_dispositions_ready,
            release_ready: guardrail_readiness_summary.release_ready,
            guardrail_readiness_summary,
            disposition_action_summary,
            execution_evidence_review_disposition_actions_ready,
            ready_for_release,
        }
    }

    pub fn has_disposition_actions(&self) -> bool {
        self.disposition_action_count > 0
    }

    pub fn has_required_actions(&self) -> bool {
        self.required_disposition_actions > 0
    }

    pub fn has_blockers(&self) -> bool {
        !self.ready_for_release
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_handoff_actions > 0 || self.guardrail_readiness_summary.needs_operator()
    }

    pub fn needs_repair(&self) -> bool {
        self.repair_actions > 0 || self.guardrail_readiness_summary.needs_repair()
    }

    pub fn needs_review(&self) -> bool {
        self.review_required_actions > 0 || self.guardrail_readiness_summary.needs_review()
    }

    pub fn has_lineage_gaps(&self) -> bool {
        self.lineage_gap_actions > 0 || self.guardrail_readiness_summary.has_lineage_gaps()
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationMeshReadinessHandoffKind {
    SubstrateAction,
    EvidenceRemediation,
    ReleaseReady,
}

impl IntegrationMeshReadinessHandoffKind {
    pub fn as_str(self) -> &'static str {
        match self {
            Self::SubstrateAction => "substrate_action",
            Self::EvidenceRemediation => "evidence_remediation",
            Self::ReleaseReady => "release_ready",
        }
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationMeshReadinessHandoffStatus {
    Ready,
    NeedsReview,
    Blocked,
}

impl IntegrationMeshReadinessHandoffStatus {
    pub fn as_str(self) -> &'static str {
        match self {
            Self::Ready => "ready",
            Self::NeedsReview => "needs_review",
            Self::Blocked => "blocked",
        }
    }

    pub fn requires_attention(self) -> bool {
        matches!(self, Self::NeedsReview | Self::Blocked)
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReadinessHandoffPackage {
    pub sequence: usize,
    pub package_kind: IntegrationMeshReadinessHandoffKind,
    pub handoff_status: IntegrationMeshReadinessHandoffStatus,
    pub action_sequence: Option<usize>,
    pub protocol: Option<ProtocolFamily>,
    pub stage: Option<IntegrationMeshProtocolSubstrateStage>,
    pub primitive: Option<PrimitiveFamily>,
    pub remediation_kind: Option<IntegrationActivationEvidenceRemediationKind>,
    pub queued_substrate_actions: usize,
    pub remediation_item_count: usize,
    pub stage_blocker_count: usize,
    pub primitive_blocker_count: usize,
    pub substrate_actions_ready: bool,
    pub release_ready: bool,
    pub ready_for_handoff: bool,
    pub blocked: bool,
    pub review_required: bool,
    pub operator_required: bool,
    pub requires_attention: bool,
}

impl IntegrationMeshReadinessHandoffPackage {
    fn from_substrate_action(
        action: &IntegrationMeshProtocolSubstrateAction,
        summary: &IntegrationMeshActionReadinessSummary,
    ) -> Self {
        Self {
            sequence: 0,
            package_kind: IntegrationMeshReadinessHandoffKind::SubstrateAction,
            handoff_status: IntegrationMeshReadinessHandoffStatus::Blocked,
            action_sequence: Some(action.sequence),
            protocol: Some(action.protocol.clone()),
            stage: Some(action.stage),
            primitive: Some(action.primitive),
            remediation_kind: None,
            queued_substrate_actions: summary.queued_substrate_actions,
            remediation_item_count: summary.remediation_item_count,
            stage_blocker_count: summary.release_summary.stage_blocker_count,
            primitive_blocker_count: summary.release_summary.primitive_blocker_count,
            substrate_actions_ready: false,
            release_ready: false,
            ready_for_handoff: false,
            blocked: true,
            review_required: false,
            operator_required: true,
            requires_attention: true,
        }
    }

    fn evidence_remediation(summary: &IntegrationMeshActionReadinessSummary) -> Option<Self> {
        summary.has_remediations().then(|| {
            let review_required = summary
                .release_summary
                .package_summary
                .requires_human_review;
            let handoff_status = if review_required {
                IntegrationMeshReadinessHandoffStatus::NeedsReview
            } else {
                IntegrationMeshReadinessHandoffStatus::Blocked
            };

            Self {
                sequence: 0,
                package_kind: IntegrationMeshReadinessHandoffKind::EvidenceRemediation,
                handoff_status,
                action_sequence: None,
                protocol: None,
                stage: None,
                primitive: None,
                remediation_kind: summary.release_summary.next_remediation_kind,
                queued_substrate_actions: summary.queued_substrate_actions,
                remediation_item_count: summary.remediation_item_count,
                stage_blocker_count: summary.release_summary.stage_blocker_count,
                primitive_blocker_count: summary.release_summary.primitive_blocker_count,
                substrate_actions_ready: summary.substrate_actions_ready,
                release_ready: false,
                ready_for_handoff: false,
                blocked: handoff_status == IntegrationMeshReadinessHandoffStatus::Blocked,
                review_required,
                operator_required: true,
                requires_attention: true,
            }
        })
    }

    fn release_ready(summary: &IntegrationMeshActionReadinessSummary) -> Option<Self> {
        summary.release_ready.then_some(Self {
            sequence: 0,
            package_kind: IntegrationMeshReadinessHandoffKind::ReleaseReady,
            handoff_status: IntegrationMeshReadinessHandoffStatus::Ready,
            action_sequence: None,
            protocol: None,
            stage: None,
            primitive: None,
            remediation_kind: None,
            queued_substrate_actions: 0,
            remediation_item_count: 0,
            stage_blocker_count: 0,
            primitive_blocker_count: 0,
            substrate_actions_ready: true,
            release_ready: true,
            ready_for_handoff: true,
            blocked: false,
            review_required: false,
            operator_required: false,
            requires_attention: false,
        })
    }

    pub fn ready_for_handoff(&self) -> bool {
        self.ready_for_handoff
    }

    pub fn blocked(&self) -> bool {
        self.blocked
    }

    pub fn review_required(&self) -> bool {
        self.review_required
    }

    pub fn operator_required(&self) -> bool {
        self.operator_required
    }

    pub fn requires_attention(&self) -> bool {
        self.requires_attention || self.handoff_status.requires_attention()
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReadinessHandoffSummary {
    pub action_readiness_summary: IntegrationMeshActionReadinessSummary,
    pub total_packages: usize,
    pub action_packages: usize,
    pub remediation_packages: usize,
    pub ready_packages: usize,
    pub review_required_packages: usize,
    pub blocked_packages: usize,
    pub operator_required_packages: usize,
    pub packages_requiring_attention: usize,
    pub queued_substrate_actions: usize,
    pub queued_stage_count: usize,
    pub queued_primitive_count: usize,
    pub remediation_item_count: usize,
    pub stage_blocker_count: usize,
    pub primitive_blocker_count: usize,
    pub next_handoff_status: Option<IntegrationMeshReadinessHandoffStatus>,
    pub next_package_kind: Option<IntegrationMeshReadinessHandoffKind>,
    pub next_package_sequence: Option<usize>,
    pub first_action_sequence: Option<usize>,
    pub first_remediation_sequence: Option<usize>,
    pub first_ready_sequence: Option<usize>,
    pub first_blocked_sequence: Option<usize>,
    pub first_review_sequence: Option<usize>,
    pub first_action_protocol: Option<ProtocolFamily>,
    pub first_action_stage: Option<IntegrationMeshProtocolSubstrateStage>,
    pub first_action_primitive: Option<PrimitiveFamily>,
    pub next_remediation_kind: Option<IntegrationActivationEvidenceRemediationKind>,
    pub substrate_actions_ready: bool,
    pub release_ready: bool,
}

impl IntegrationMeshReadinessHandoffSummary {
    pub fn from_parts<'a>(
        action_readiness_summary: IntegrationMeshActionReadinessSummary,
        packages: impl IntoIterator<Item = &'a IntegrationMeshReadinessHandoffPackage>,
    ) -> Self {
        let packages = packages.into_iter().collect::<Vec<_>>();
        let mut summary = Self {
            queued_substrate_actions: action_readiness_summary.queued_substrate_actions,
            queued_stage_count: action_readiness_summary.queued_stage_count,
            queued_primitive_count: action_readiness_summary.queued_primitive_count,
            remediation_item_count: action_readiness_summary.remediation_item_count,
            stage_blocker_count: action_readiness_summary.release_summary.stage_blocker_count,
            primitive_blocker_count: action_readiness_summary
                .release_summary
                .primitive_blocker_count,
            next_remediation_kind: action_readiness_summary
                .release_summary
                .next_remediation_kind,
            substrate_actions_ready: action_readiness_summary.substrate_actions_ready,
            release_ready: action_readiness_summary.release_ready,
            action_readiness_summary,
            total_packages: 0,
            action_packages: 0,
            remediation_packages: 0,
            ready_packages: 0,
            review_required_packages: 0,
            blocked_packages: 0,
            operator_required_packages: 0,
            packages_requiring_attention: 0,
            next_handoff_status: None,
            next_package_kind: None,
            next_package_sequence: None,
            first_action_sequence: None,
            first_remediation_sequence: None,
            first_ready_sequence: None,
            first_blocked_sequence: None,
            first_review_sequence: None,
            first_action_protocol: None,
            first_action_stage: None,
            first_action_primitive: None,
        };

        for package in packages {
            summary.total_packages += 1;

            if summary.next_handoff_status.is_none() {
                summary.next_handoff_status = Some(package.handoff_status);
                summary.next_package_kind = Some(package.package_kind);
                summary.next_package_sequence = Some(package.sequence);
            }

            match package.package_kind {
                IntegrationMeshReadinessHandoffKind::SubstrateAction => {
                    summary.action_packages += 1;
                    summary.first_action_sequence =
                        summary.first_action_sequence.or(Some(package.sequence));
                    summary.first_action_protocol = summary
                        .first_action_protocol
                        .clone()
                        .or_else(|| package.protocol.clone());
                    summary.first_action_stage = summary.first_action_stage.or(package.stage);
                    summary.first_action_primitive =
                        summary.first_action_primitive.or(package.primitive);
                }
                IntegrationMeshReadinessHandoffKind::EvidenceRemediation => {
                    summary.remediation_packages += 1;
                    summary.first_remediation_sequence = summary
                        .first_remediation_sequence
                        .or(Some(package.sequence));
                }
                IntegrationMeshReadinessHandoffKind::ReleaseReady => {
                    summary.ready_packages += 1;
                    summary.first_ready_sequence =
                        summary.first_ready_sequence.or(Some(package.sequence));
                }
            }

            if package.ready_for_handoff() {
                if package.package_kind != IntegrationMeshReadinessHandoffKind::ReleaseReady {
                    summary.ready_packages += 1;
                }
                summary.first_ready_sequence =
                    summary.first_ready_sequence.or(Some(package.sequence));
            }
            if package.blocked() {
                summary.blocked_packages += 1;
                summary.first_blocked_sequence =
                    summary.first_blocked_sequence.or(Some(package.sequence));
            }
            if package.review_required() {
                summary.review_required_packages += 1;
                summary.first_review_sequence =
                    summary.first_review_sequence.or(Some(package.sequence));
            }
            if package.operator_required() {
                summary.operator_required_packages += 1;
            }
            if package.requires_attention() {
                summary.packages_requiring_attention += 1;
            }
        }

        summary
    }

    pub fn ready_for_handoff(&self) -> bool {
        self.release_ready
            && self.ready_packages > 0
            && !self.has_blockers()
            && !self.has_review_work()
    }

    pub fn has_blockers(&self) -> bool {
        self.blocked_packages > 0
            || self.queued_substrate_actions > 0
            || self.stage_blocker_count > 0
            || self.primitive_blocker_count > 0
    }

    pub fn has_review_work(&self) -> bool {
        self.review_required_packages > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.packages_requiring_attention > 0 || self.has_blockers() || self.has_review_work()
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationMeshReleaseReadinessCheckKind {
    SubstrateActions,
    EvidenceRemediation,
    HumanReview,
    OperatorHandoff,
    ReleasePacket,
}

impl IntegrationMeshReleaseReadinessCheckKind {
    pub fn as_str(self) -> &'static str {
        match self {
            Self::SubstrateActions => "substrate_actions",
            Self::EvidenceRemediation => "evidence_remediation",
            Self::HumanReview => "human_review",
            Self::OperatorHandoff => "operator_handoff",
            Self::ReleasePacket => "release_packet",
        }
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationMeshReleaseReadinessStatus {
    Ready,
    NeedsReview,
    Blocked,
}

impl IntegrationMeshReleaseReadinessStatus {
    pub fn as_str(self) -> &'static str {
        match self {
            Self::Ready => "ready",
            Self::NeedsReview => "needs_review",
            Self::Blocked => "blocked",
        }
    }

    pub fn requires_attention(self) -> bool {
        matches!(self, Self::NeedsReview | Self::Blocked)
    }

    fn as_handoff_status(self) -> IntegrationMeshReadinessHandoffStatus {
        match self {
            Self::Ready => IntegrationMeshReadinessHandoffStatus::Ready,
            Self::NeedsReview => IntegrationMeshReadinessHandoffStatus::NeedsReview,
            Self::Blocked => IntegrationMeshReadinessHandoffStatus::Blocked,
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseReadinessCheck {
    pub sequence: usize,
    pub check_kind: IntegrationMeshReleaseReadinessCheckKind,
    pub status: IntegrationMeshReleaseReadinessStatus,
    pub package_kind: Option<IntegrationMeshReadinessHandoffKind>,
    pub handoff_status: Option<IntegrationMeshReadinessHandoffStatus>,
    pub queued_substrate_actions: usize,
    pub queued_stage_count: usize,
    pub queued_primitive_count: usize,
    pub remediation_item_count: usize,
    pub review_required_packages: usize,
    pub operator_required_packages: usize,
    pub blocked_packages: usize,
    pub packages_requiring_attention: usize,
    pub ready: bool,
    pub blocked: bool,
    pub review_required: bool,
    pub operator_required: bool,
    pub requires_attention: bool,
}

impl IntegrationMeshReleaseReadinessCheck {
    fn from_summary(
        sequence: usize,
        check_kind: IntegrationMeshReleaseReadinessCheckKind,
        status: IntegrationMeshReleaseReadinessStatus,
        package_kind: Option<IntegrationMeshReadinessHandoffKind>,
        handoff_status: Option<IntegrationMeshReadinessHandoffStatus>,
        handoff_summary: &IntegrationMeshReadinessHandoffSummary,
        operator_required: bool,
    ) -> Self {
        let ready = status == IntegrationMeshReleaseReadinessStatus::Ready;
        let blocked = status == IntegrationMeshReleaseReadinessStatus::Blocked;
        let review_required = status == IntegrationMeshReleaseReadinessStatus::NeedsReview;

        Self {
            sequence,
            check_kind,
            status,
            package_kind,
            handoff_status,
            queued_substrate_actions: handoff_summary.queued_substrate_actions,
            queued_stage_count: handoff_summary.queued_stage_count,
            queued_primitive_count: handoff_summary.queued_primitive_count,
            remediation_item_count: handoff_summary.remediation_item_count,
            review_required_packages: handoff_summary.review_required_packages,
            operator_required_packages: handoff_summary.operator_required_packages,
            blocked_packages: handoff_summary.blocked_packages,
            packages_requiring_attention: handoff_summary.packages_requiring_attention,
            ready,
            blocked,
            review_required,
            operator_required,
            requires_attention: status.requires_attention() || operator_required,
        }
    }

    pub fn ready(&self) -> bool {
        self.ready
    }

    pub fn blocked(&self) -> bool {
        self.blocked
    }

    pub fn review_required(&self) -> bool {
        self.review_required
    }

    pub fn operator_required(&self) -> bool {
        self.operator_required
    }

    pub fn requires_attention(&self) -> bool {
        self.requires_attention
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseReadinessCheckSummary {
    pub handoff_summary: IntegrationMeshReadinessHandoffSummary,
    pub total_checks: usize,
    pub ready_checks: usize,
    pub blocked_checks: usize,
    pub review_required_checks: usize,
    pub operator_required_checks: usize,
    pub checks_requiring_attention: usize,
    pub queued_substrate_actions: usize,
    pub queued_stage_count: usize,
    pub queued_primitive_count: usize,
    pub remediation_item_count: usize,
    pub review_required_packages: usize,
    pub operator_required_packages: usize,
    pub blocked_packages: usize,
    pub packages_requiring_attention: usize,
    pub next_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub next_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_check_sequence: Option<usize>,
    pub first_blocked_check_sequence: Option<usize>,
    pub first_review_check_sequence: Option<usize>,
    pub first_operator_check_sequence: Option<usize>,
    pub next_package_kind: Option<IntegrationMeshReadinessHandoffKind>,
    pub next_handoff_status: Option<IntegrationMeshReadinessHandoffStatus>,
    pub substrate_actions_ready: bool,
    pub release_ready: bool,
    pub ready_for_handoff: bool,
    pub release_packet_ready: bool,
}

impl IntegrationMeshReleaseReadinessCheckSummary {
    pub fn from_parts<'a>(
        handoff_summary: IntegrationMeshReadinessHandoffSummary,
        checks: impl IntoIterator<Item = &'a IntegrationMeshReleaseReadinessCheck>,
    ) -> Self {
        let checks = checks.into_iter().collect::<Vec<_>>();
        let ready_for_handoff = handoff_summary.ready_for_handoff();
        let mut summary = Self {
            queued_substrate_actions: handoff_summary.queued_substrate_actions,
            queued_stage_count: handoff_summary.queued_stage_count,
            queued_primitive_count: handoff_summary.queued_primitive_count,
            remediation_item_count: handoff_summary.remediation_item_count,
            review_required_packages: handoff_summary.review_required_packages,
            operator_required_packages: handoff_summary.operator_required_packages,
            blocked_packages: handoff_summary.blocked_packages,
            packages_requiring_attention: handoff_summary.packages_requiring_attention,
            next_package_kind: handoff_summary.next_package_kind,
            next_handoff_status: handoff_summary.next_handoff_status,
            substrate_actions_ready: handoff_summary.substrate_actions_ready,
            release_ready: handoff_summary.release_ready,
            ready_for_handoff,
            release_packet_ready: ready_for_handoff,
            handoff_summary,
            total_checks: 0,
            ready_checks: 0,
            blocked_checks: 0,
            review_required_checks: 0,
            operator_required_checks: 0,
            checks_requiring_attention: 0,
            next_check_kind: None,
            next_check_status: None,
            next_check_sequence: None,
            first_blocked_check_sequence: None,
            first_review_check_sequence: None,
            first_operator_check_sequence: None,
        };

        for check in checks {
            summary.total_checks += 1;

            if check.ready() {
                summary.ready_checks += 1;
            }
            if check.blocked() {
                summary.blocked_checks += 1;
                summary.first_blocked_check_sequence = summary
                    .first_blocked_check_sequence
                    .or(Some(check.sequence));
            }
            if check.review_required() {
                summary.review_required_checks += 1;
                summary.first_review_check_sequence =
                    summary.first_review_check_sequence.or(Some(check.sequence));
            }
            if check.operator_required() {
                summary.operator_required_checks += 1;
                summary.first_operator_check_sequence = summary
                    .first_operator_check_sequence
                    .or(Some(check.sequence));
            }
            if check.requires_attention() {
                summary.checks_requiring_attention += 1;
            }

            if summary.next_check_sequence.is_none() && check.requires_attention() {
                summary.next_check_kind = Some(check.check_kind);
                summary.next_check_status = Some(check.status);
                summary.next_check_sequence = Some(check.sequence);
            }
        }

        summary
    }

    pub fn has_blockers(&self) -> bool {
        self.blocked_checks > 0 || self.handoff_summary.has_blockers()
    }

    pub fn has_review_work(&self) -> bool {
        self.review_required_checks > 0 || self.handoff_summary.has_review_work()
    }

    pub fn requires_attention(&self) -> bool {
        self.checks_requiring_attention > 0
            || self.has_blockers()
            || self.has_review_work()
            || !self.release_packet_ready
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseReadinessCheckSlot {
    pub sequence: usize,
    pub slot_key: String,
    pub check_sequence: usize,
    pub check_kind: IntegrationMeshReleaseReadinessCheckKind,
    pub status: IntegrationMeshReleaseReadinessStatus,
    pub package_kind: Option<IntegrationMeshReadinessHandoffKind>,
    pub handoff_status: Option<IntegrationMeshReadinessHandoffStatus>,
    pub queued_substrate_actions: usize,
    pub queued_stage_count: usize,
    pub queued_primitive_count: usize,
    pub remediation_item_count: usize,
    pub review_required_packages: usize,
    pub operator_required_packages: usize,
    pub blocked_packages: usize,
    pub packages_requiring_attention: usize,
    pub ready: bool,
    pub blocked: bool,
    pub review_required: bool,
    pub operator_required: bool,
    pub requires_attention: bool,
}

impl IntegrationMeshReleaseReadinessCheckSlot {
    pub fn from_check(sequence: usize, check: &IntegrationMeshReleaseReadinessCheck) -> Self {
        Self {
            sequence,
            slot_key: format!(
                "release-check-slot-{sequence:02}-{}",
                check.check_kind.as_str()
            ),
            check_sequence: check.sequence,
            check_kind: check.check_kind,
            status: check.status,
            package_kind: check.package_kind,
            handoff_status: check.handoff_status,
            queued_substrate_actions: check.queued_substrate_actions,
            queued_stage_count: check.queued_stage_count,
            queued_primitive_count: check.queued_primitive_count,
            remediation_item_count: check.remediation_item_count,
            review_required_packages: check.review_required_packages,
            operator_required_packages: check.operator_required_packages,
            blocked_packages: check.blocked_packages,
            packages_requiring_attention: check.packages_requiring_attention,
            ready: check.ready(),
            blocked: check.blocked(),
            review_required: check.review_required(),
            operator_required: check.operator_required(),
            requires_attention: check.requires_attention(),
        }
    }

    pub fn ready(&self) -> bool {
        self.ready
    }

    pub fn blocked(&self) -> bool {
        self.blocked
    }

    pub fn review_required(&self) -> bool {
        self.review_required
    }

    pub fn operator_required(&self) -> bool {
        self.operator_required
    }

    pub fn requires_attention(&self) -> bool {
        self.requires_attention
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseReadinessCheckSlotSummary {
    pub total_slots: usize,
    pub ready_slots: usize,
    pub blocked_slots: usize,
    pub review_required_slots: usize,
    pub operator_required_slots: usize,
    pub slots_requiring_attention: usize,
    pub queued_substrate_actions: usize,
    pub queued_stage_count: usize,
    pub queued_primitive_count: usize,
    pub remediation_item_count: usize,
    pub review_required_packages: usize,
    pub operator_required_packages: usize,
    pub blocked_packages: usize,
    pub packages_requiring_attention: usize,
    pub first_slot_key: Option<String>,
    pub first_blocked_slot_key: Option<String>,
    pub first_review_slot_key: Option<String>,
    pub first_operator_slot_key: Option<String>,
    pub next_slot_key: Option<String>,
    pub next_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub next_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_package_kind: Option<IntegrationMeshReadinessHandoffKind>,
    pub next_handoff_status: Option<IntegrationMeshReadinessHandoffStatus>,
    pub release_check_slots_ready: bool,
}

impl IntegrationMeshReleaseReadinessCheckSlotSummary {
    pub fn from_slots<'a>(
        slots: impl IntoIterator<Item = &'a IntegrationMeshReleaseReadinessCheckSlot>,
    ) -> Self {
        let slots = slots.into_iter().collect::<Vec<_>>();
        let first_slot = slots.iter().min_by_key(|slot| slot.sequence);
        let first_blocked_slot = slots
            .iter()
            .filter(|slot| slot.blocked())
            .min_by_key(|slot| slot.sequence);
        let first_review_slot = slots
            .iter()
            .filter(|slot| slot.review_required())
            .min_by_key(|slot| slot.sequence);
        let first_operator_slot = slots
            .iter()
            .filter(|slot| slot.operator_required())
            .min_by_key(|slot| slot.sequence);
        let next_slot = slots
            .iter()
            .filter(|slot| slot.requires_attention())
            .min_by_key(|slot| slot.sequence);
        let release_check_slots_ready = !slots.is_empty() && slots.iter().all(|slot| slot.ready());

        Self {
            total_slots: slots.len(),
            ready_slots: slots.iter().filter(|slot| slot.ready()).count(),
            blocked_slots: slots.iter().filter(|slot| slot.blocked()).count(),
            review_required_slots: slots.iter().filter(|slot| slot.review_required()).count(),
            operator_required_slots: slots.iter().filter(|slot| slot.operator_required()).count(),
            slots_requiring_attention: slots
                .iter()
                .filter(|slot| slot.requires_attention())
                .count(),
            queued_substrate_actions: first_slot.map_or(0, |slot| slot.queued_substrate_actions),
            queued_stage_count: first_slot.map_or(0, |slot| slot.queued_stage_count),
            queued_primitive_count: first_slot.map_or(0, |slot| slot.queued_primitive_count),
            remediation_item_count: first_slot.map_or(0, |slot| slot.remediation_item_count),
            review_required_packages: first_slot.map_or(0, |slot| slot.review_required_packages),
            operator_required_packages: first_slot
                .map_or(0, |slot| slot.operator_required_packages),
            blocked_packages: first_slot.map_or(0, |slot| slot.blocked_packages),
            packages_requiring_attention: first_slot
                .map_or(0, |slot| slot.packages_requiring_attention),
            first_slot_key: first_slot.map(|slot| slot.slot_key.clone()),
            first_blocked_slot_key: first_blocked_slot.map(|slot| slot.slot_key.clone()),
            first_review_slot_key: first_review_slot.map(|slot| slot.slot_key.clone()),
            first_operator_slot_key: first_operator_slot.map(|slot| slot.slot_key.clone()),
            next_slot_key: next_slot.map(|slot| slot.slot_key.clone()),
            next_check_kind: next_slot.map(|slot| slot.check_kind),
            next_check_status: next_slot.map(|slot| slot.status),
            next_package_kind: next_slot.and_then(|slot| slot.package_kind),
            next_handoff_status: next_slot.and_then(|slot| slot.handoff_status),
            release_check_slots_ready,
        }
    }

    pub fn is_empty(&self) -> bool {
        self.total_slots == 0
    }

    pub fn has_slots(&self) -> bool {
        self.total_slots > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.blocked_slots > 0 || self.blocked_packages > 0
    }

    pub fn has_review_work(&self) -> bool {
        self.review_required_slots > 0 || self.review_required_packages > 0
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_slots > 0 || self.operator_required_packages > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.slots_requiring_attention > 0
            || self.has_blockers()
            || self.has_review_work()
            || self.needs_operator()
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleasePacketReadinessSummary {
    pub release_readiness_check_summary: IntegrationMeshReleaseReadinessCheckSummary,
    pub total_checks: usize,
    pub ready_checks: usize,
    pub blocked_checks: usize,
    pub review_required_checks: usize,
    pub operator_required_checks: usize,
    pub checks_requiring_attention: usize,
    pub queued_substrate_actions: usize,
    pub remediation_item_count: usize,
    pub review_required_packages: usize,
    pub operator_required_packages: usize,
    pub blocked_packages: usize,
    pub next_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub next_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_check_sequence: Option<usize>,
    pub next_package_kind: Option<IntegrationMeshReadinessHandoffKind>,
    pub next_handoff_status: Option<IntegrationMeshReadinessHandoffStatus>,
    pub first_blocked_check_sequence: Option<usize>,
    pub first_review_check_sequence: Option<usize>,
    pub first_operator_check_sequence: Option<usize>,
    pub substrate_actions_ready: bool,
    pub release_ready: bool,
    pub ready_for_handoff: bool,
    pub release_packet_ready: bool,
    pub packet_status: IntegrationMeshReleaseReadinessStatus,
}

impl IntegrationMeshReleasePacketReadinessSummary {
    pub fn from_summary(
        release_readiness_check_summary: IntegrationMeshReleaseReadinessCheckSummary,
    ) -> Self {
        let packet_status = if release_readiness_check_summary.release_packet_ready {
            IntegrationMeshReleaseReadinessStatus::Ready
        } else if release_readiness_check_summary.has_blockers() {
            IntegrationMeshReleaseReadinessStatus::Blocked
        } else if release_readiness_check_summary.has_review_work()
            || release_readiness_check_summary.requires_attention()
        {
            IntegrationMeshReleaseReadinessStatus::NeedsReview
        } else {
            IntegrationMeshReleaseReadinessStatus::Blocked
        };

        Self {
            total_checks: release_readiness_check_summary.total_checks,
            ready_checks: release_readiness_check_summary.ready_checks,
            blocked_checks: release_readiness_check_summary.blocked_checks,
            review_required_checks: release_readiness_check_summary.review_required_checks,
            operator_required_checks: release_readiness_check_summary.operator_required_checks,
            checks_requiring_attention: release_readiness_check_summary.checks_requiring_attention,
            queued_substrate_actions: release_readiness_check_summary.queued_substrate_actions,
            remediation_item_count: release_readiness_check_summary.remediation_item_count,
            review_required_packages: release_readiness_check_summary.review_required_packages,
            operator_required_packages: release_readiness_check_summary.operator_required_packages,
            blocked_packages: release_readiness_check_summary.blocked_packages,
            next_check_kind: release_readiness_check_summary.next_check_kind,
            next_check_status: release_readiness_check_summary.next_check_status,
            next_check_sequence: release_readiness_check_summary.next_check_sequence,
            next_package_kind: release_readiness_check_summary.next_package_kind,
            next_handoff_status: release_readiness_check_summary.next_handoff_status,
            first_blocked_check_sequence: release_readiness_check_summary
                .first_blocked_check_sequence,
            first_review_check_sequence: release_readiness_check_summary
                .first_review_check_sequence,
            first_operator_check_sequence: release_readiness_check_summary
                .first_operator_check_sequence,
            substrate_actions_ready: release_readiness_check_summary.substrate_actions_ready,
            release_ready: release_readiness_check_summary.release_ready,
            ready_for_handoff: release_readiness_check_summary.ready_for_handoff,
            release_packet_ready: release_readiness_check_summary.release_packet_ready,
            release_readiness_check_summary,
            packet_status,
        }
    }

    pub fn ready_for_release(&self) -> bool {
        self.packet_status == IntegrationMeshReleaseReadinessStatus::Ready
            && self.release_packet_ready
            && !self.has_blockers()
            && !self.needs_review()
    }

    pub fn has_blockers(&self) -> bool {
        self.packet_status == IntegrationMeshReleaseReadinessStatus::Blocked
            || self.blocked_checks > 0
            || self.blocked_packages > 0
            || self.queued_substrate_actions > 0
    }

    pub fn needs_review(&self) -> bool {
        self.packet_status == IntegrationMeshReleaseReadinessStatus::NeedsReview
            || self.review_required_checks > 0
            || self.review_required_packages > 0
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_checks > 0 || self.operator_required_packages > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.packet_status.requires_attention()
            || self.checks_requiring_attention > 0
            || self.needs_operator()
            || self.has_blockers()
            || self.needs_review()
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseExecutionReadinessSummary {
    pub release_packet_readiness_summary: IntegrationMeshReleasePacketReadinessSummary,
    pub release_check_slot_summary: IntegrationMeshReleaseReadinessCheckSlotSummary,
    pub total_checks: usize,
    pub ready_checks: usize,
    pub blocked_checks: usize,
    pub review_required_checks: usize,
    pub operator_required_checks: usize,
    pub checks_requiring_attention: usize,
    pub total_slots: usize,
    pub ready_slots: usize,
    pub blocked_slots: usize,
    pub review_required_slots: usize,
    pub operator_required_slots: usize,
    pub slots_requiring_attention: usize,
    pub queued_substrate_actions: usize,
    pub remediation_item_count: usize,
    pub review_required_packages: usize,
    pub operator_required_packages: usize,
    pub blocked_packages: usize,
    pub next_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub next_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_check_sequence: Option<usize>,
    pub next_package_kind: Option<IntegrationMeshReadinessHandoffKind>,
    pub next_handoff_status: Option<IntegrationMeshReadinessHandoffStatus>,
    pub next_slot_key: Option<String>,
    pub first_blocked_slot_key: Option<String>,
    pub first_review_slot_key: Option<String>,
    pub first_operator_slot_key: Option<String>,
    pub packet_status: IntegrationMeshReleaseReadinessStatus,
    pub execution_status: IntegrationMeshReleaseReadinessStatus,
    pub release_packet_ready: bool,
    pub release_check_slots_ready: bool,
    pub release_execution_ready: bool,
}

impl IntegrationMeshReleaseExecutionReadinessSummary {
    pub fn from_summaries(
        release_packet_readiness_summary: IntegrationMeshReleasePacketReadinessSummary,
        release_check_slot_summary: IntegrationMeshReleaseReadinessCheckSlotSummary,
    ) -> Self {
        let release_execution_ready = release_packet_readiness_summary.ready_for_release()
            && release_check_slot_summary.release_check_slots_ready;
        let execution_status = if release_execution_ready {
            IntegrationMeshReleaseReadinessStatus::Ready
        } else if release_packet_readiness_summary.has_blockers()
            || release_check_slot_summary.has_blockers()
        {
            IntegrationMeshReleaseReadinessStatus::Blocked
        } else if release_packet_readiness_summary.needs_review()
            || release_check_slot_summary.has_review_work()
            || release_packet_readiness_summary.requires_attention()
            || release_check_slot_summary.requires_attention()
        {
            IntegrationMeshReleaseReadinessStatus::NeedsReview
        } else {
            IntegrationMeshReleaseReadinessStatus::Blocked
        };

        Self {
            total_checks: release_packet_readiness_summary.total_checks,
            ready_checks: release_packet_readiness_summary.ready_checks,
            blocked_checks: release_packet_readiness_summary.blocked_checks,
            review_required_checks: release_packet_readiness_summary.review_required_checks,
            operator_required_checks: release_packet_readiness_summary.operator_required_checks,
            checks_requiring_attention: release_packet_readiness_summary.checks_requiring_attention,
            total_slots: release_check_slot_summary.total_slots,
            ready_slots: release_check_slot_summary.ready_slots,
            blocked_slots: release_check_slot_summary.blocked_slots,
            review_required_slots: release_check_slot_summary.review_required_slots,
            operator_required_slots: release_check_slot_summary.operator_required_slots,
            slots_requiring_attention: release_check_slot_summary.slots_requiring_attention,
            queued_substrate_actions: release_packet_readiness_summary.queued_substrate_actions,
            remediation_item_count: release_packet_readiness_summary.remediation_item_count,
            review_required_packages: release_packet_readiness_summary.review_required_packages,
            operator_required_packages: release_packet_readiness_summary.operator_required_packages,
            blocked_packages: release_packet_readiness_summary.blocked_packages,
            next_check_kind: release_packet_readiness_summary.next_check_kind,
            next_check_status: release_packet_readiness_summary.next_check_status,
            next_check_sequence: release_packet_readiness_summary.next_check_sequence,
            next_package_kind: release_packet_readiness_summary.next_package_kind,
            next_handoff_status: release_packet_readiness_summary.next_handoff_status,
            next_slot_key: release_check_slot_summary.next_slot_key.clone(),
            first_blocked_slot_key: release_check_slot_summary.first_blocked_slot_key.clone(),
            first_review_slot_key: release_check_slot_summary.first_review_slot_key.clone(),
            first_operator_slot_key: release_check_slot_summary.first_operator_slot_key.clone(),
            packet_status: release_packet_readiness_summary.packet_status,
            execution_status,
            release_packet_ready: release_packet_readiness_summary.release_packet_ready,
            release_check_slots_ready: release_check_slot_summary.release_check_slots_ready,
            release_execution_ready,
            release_packet_readiness_summary,
            release_check_slot_summary,
        }
    }

    pub fn ready_for_execution(&self) -> bool {
        self.execution_status == IntegrationMeshReleaseReadinessStatus::Ready
            && self.release_execution_ready
            && !self.has_blockers()
            && !self.needs_review()
    }

    pub fn has_release_slots(&self) -> bool {
        self.total_slots > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.execution_status == IntegrationMeshReleaseReadinessStatus::Blocked
            || self.blocked_checks > 0
            || self.blocked_slots > 0
            || self.blocked_packages > 0
            || self.queued_substrate_actions > 0
    }

    pub fn needs_review(&self) -> bool {
        self.execution_status == IntegrationMeshReleaseReadinessStatus::NeedsReview
            || self.review_required_checks > 0
            || self.review_required_slots > 0
            || self.review_required_packages > 0
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_checks > 0
            || self.operator_required_slots > 0
            || self.operator_required_packages > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.execution_status.requires_attention()
            || self.checks_requiring_attention > 0
            || self.slots_requiring_attention > 0
            || self.needs_operator()
            || self.has_blockers()
            || self.needs_review()
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseExecutionTask {
    pub sequence: usize,
    pub task_key: String,
    pub slot_key: String,
    pub check_sequence: usize,
    pub check_kind: IntegrationMeshReleaseReadinessCheckKind,
    pub status: IntegrationMeshReleaseReadinessStatus,
    pub package_kind: Option<IntegrationMeshReadinessHandoffKind>,
    pub handoff_status: Option<IntegrationMeshReadinessHandoffStatus>,
    pub queued_substrate_actions: usize,
    pub remediation_item_count: usize,
    pub review_required_packages: usize,
    pub operator_required_packages: usize,
    pub blocked_packages: usize,
    pub ready: bool,
    pub blocked: bool,
    pub review_required: bool,
    pub operator_required: bool,
    pub requires_attention: bool,
    pub packet_status: IntegrationMeshReleaseReadinessStatus,
    pub execution_status: IntegrationMeshReleaseReadinessStatus,
    pub release_execution_ready: bool,
}

impl IntegrationMeshReleaseExecutionTask {
    pub fn from_slot(
        sequence: usize,
        slot: &IntegrationMeshReleaseReadinessCheckSlot,
        release_execution_readiness_summary: &IntegrationMeshReleaseExecutionReadinessSummary,
    ) -> Self {
        Self {
            sequence,
            task_key: format!(
                "release-execution-task-{sequence:02}-{}",
                slot.check_kind.as_str()
            ),
            slot_key: slot.slot_key.clone(),
            check_sequence: slot.check_sequence,
            check_kind: slot.check_kind,
            status: slot.status,
            package_kind: slot.package_kind,
            handoff_status: slot.handoff_status,
            queued_substrate_actions: slot.queued_substrate_actions,
            remediation_item_count: slot.remediation_item_count,
            review_required_packages: slot.review_required_packages,
            operator_required_packages: slot.operator_required_packages,
            blocked_packages: slot.blocked_packages,
            ready: slot.ready(),
            blocked: slot.blocked(),
            review_required: slot.review_required(),
            operator_required: slot.operator_required(),
            requires_attention: slot.requires_attention(),
            packet_status: release_execution_readiness_summary.packet_status,
            execution_status: release_execution_readiness_summary.execution_status,
            release_execution_ready: release_execution_readiness_summary.release_execution_ready,
        }
    }

    pub fn ready(&self) -> bool {
        self.ready
    }

    pub fn blocked(&self) -> bool {
        self.blocked
    }

    pub fn review_required(&self) -> bool {
        self.review_required
    }

    pub fn operator_required(&self) -> bool {
        self.operator_required
    }

    pub fn requires_attention(&self) -> bool {
        self.requires_attention
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseExecutionTaskSummary {
    pub release_execution_readiness_summary: IntegrationMeshReleaseExecutionReadinessSummary,
    pub total_tasks: usize,
    pub ready_tasks: usize,
    pub blocked_tasks: usize,
    pub review_required_tasks: usize,
    pub operator_required_tasks: usize,
    pub tasks_requiring_attention: usize,
    pub queued_substrate_actions: usize,
    pub remediation_item_count: usize,
    pub review_required_packages: usize,
    pub operator_required_packages: usize,
    pub blocked_packages: usize,
    pub first_task_key: Option<String>,
    pub first_blocked_task_key: Option<String>,
    pub first_review_task_key: Option<String>,
    pub first_operator_task_key: Option<String>,
    pub next_task_key: Option<String>,
    pub next_slot_key: Option<String>,
    pub next_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub next_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_package_kind: Option<IntegrationMeshReadinessHandoffKind>,
    pub next_handoff_status: Option<IntegrationMeshReadinessHandoffStatus>,
    pub packet_status: IntegrationMeshReleaseReadinessStatus,
    pub execution_status: IntegrationMeshReleaseReadinessStatus,
    pub release_execution_ready: bool,
}

impl IntegrationMeshReleaseExecutionTaskSummary {
    pub fn from_parts<'a>(
        release_execution_readiness_summary: IntegrationMeshReleaseExecutionReadinessSummary,
        tasks: impl IntoIterator<Item = &'a IntegrationMeshReleaseExecutionTask>,
    ) -> Self {
        let tasks = tasks.into_iter().collect::<Vec<_>>();
        let first_task = tasks.iter().min_by_key(|task| task.sequence);
        let first_blocked_task = tasks
            .iter()
            .filter(|task| task.blocked())
            .min_by_key(|task| task.sequence);
        let first_review_task = tasks
            .iter()
            .filter(|task| task.review_required())
            .min_by_key(|task| task.sequence);
        let first_operator_task = tasks
            .iter()
            .filter(|task| task.operator_required())
            .min_by_key(|task| task.sequence);
        let next_task = tasks
            .iter()
            .filter(|task| task.requires_attention())
            .min_by_key(|task| task.sequence);
        let release_execution_ready = release_execution_readiness_summary.release_execution_ready
            && !tasks.is_empty()
            && tasks.iter().all(|task| task.ready());

        Self {
            total_tasks: tasks.len(),
            ready_tasks: tasks.iter().filter(|task| task.ready()).count(),
            blocked_tasks: tasks.iter().filter(|task| task.blocked()).count(),
            review_required_tasks: tasks.iter().filter(|task| task.review_required()).count(),
            operator_required_tasks: tasks.iter().filter(|task| task.operator_required()).count(),
            tasks_requiring_attention: tasks
                .iter()
                .filter(|task| task.requires_attention())
                .count(),
            queued_substrate_actions: release_execution_readiness_summary.queued_substrate_actions,
            remediation_item_count: release_execution_readiness_summary.remediation_item_count,
            review_required_packages: release_execution_readiness_summary.review_required_packages,
            operator_required_packages: release_execution_readiness_summary
                .operator_required_packages,
            blocked_packages: release_execution_readiness_summary.blocked_packages,
            first_task_key: first_task.map(|task| task.task_key.clone()),
            first_blocked_task_key: first_blocked_task.map(|task| task.task_key.clone()),
            first_review_task_key: first_review_task.map(|task| task.task_key.clone()),
            first_operator_task_key: first_operator_task.map(|task| task.task_key.clone()),
            next_task_key: next_task.map(|task| task.task_key.clone()),
            next_slot_key: next_task.map(|task| task.slot_key.clone()),
            next_check_kind: next_task.map(|task| task.check_kind),
            next_check_status: next_task.map(|task| task.status),
            next_package_kind: next_task.and_then(|task| task.package_kind),
            next_handoff_status: next_task.and_then(|task| task.handoff_status),
            packet_status: release_execution_readiness_summary.packet_status,
            execution_status: release_execution_readiness_summary.execution_status,
            release_execution_ready,
            release_execution_readiness_summary,
        }
    }

    pub fn is_empty(&self) -> bool {
        self.total_tasks == 0
    }

    pub fn has_tasks(&self) -> bool {
        self.total_tasks > 0
    }

    pub fn ready_for_execution(&self) -> bool {
        self.execution_status == IntegrationMeshReleaseReadinessStatus::Ready
            && self.release_execution_ready
            && !self.has_blockers()
            && !self.has_review_work()
    }

    pub fn has_blockers(&self) -> bool {
        self.blocked_tasks > 0 || self.release_execution_readiness_summary.has_blockers()
    }

    pub fn has_review_work(&self) -> bool {
        self.review_required_tasks > 0 || self.release_execution_readiness_summary.needs_review()
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_tasks > 0
            || self.release_execution_readiness_summary.needs_operator()
    }

    pub fn requires_attention(&self) -> bool {
        self.tasks_requiring_attention > 0
            || self.has_blockers()
            || self.has_review_work()
            || self.needs_operator()
            || !self.release_execution_ready
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTaskReadinessSummary {
    pub release_execution_readiness_summary: IntegrationMeshReleaseExecutionReadinessSummary,
    pub release_execution_task_summary: IntegrationMeshReleaseExecutionTaskSummary,
    pub total_checks: usize,
    pub ready_checks: usize,
    pub blocked_checks: usize,
    pub review_required_checks: usize,
    pub operator_required_checks: usize,
    pub checks_requiring_attention: usize,
    pub total_slots: usize,
    pub ready_slots: usize,
    pub blocked_slots: usize,
    pub review_required_slots: usize,
    pub operator_required_slots: usize,
    pub slots_requiring_attention: usize,
    pub total_tasks: usize,
    pub ready_tasks: usize,
    pub blocked_tasks: usize,
    pub review_required_tasks: usize,
    pub operator_required_tasks: usize,
    pub tasks_requiring_attention: usize,
    pub queued_substrate_actions: usize,
    pub remediation_item_count: usize,
    pub review_required_packages: usize,
    pub operator_required_packages: usize,
    pub blocked_packages: usize,
    pub first_task_key: Option<String>,
    pub first_blocked_task_key: Option<String>,
    pub first_review_task_key: Option<String>,
    pub first_operator_task_key: Option<String>,
    pub next_task_key: Option<String>,
    pub next_slot_key: Option<String>,
    pub next_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub next_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_package_kind: Option<IntegrationMeshReadinessHandoffKind>,
    pub next_handoff_status: Option<IntegrationMeshReadinessHandoffStatus>,
    pub packet_status: IntegrationMeshReleaseReadinessStatus,
    pub execution_status: IntegrationMeshReleaseReadinessStatus,
    pub release_packet_ready: bool,
    pub release_check_slots_ready: bool,
    pub release_execution_ready: bool,
    pub release_tasks_ready: bool,
}

impl IntegrationMeshReleaseTaskReadinessSummary {
    pub fn from_summaries(
        release_execution_readiness_summary: IntegrationMeshReleaseExecutionReadinessSummary,
        release_execution_task_summary: IntegrationMeshReleaseExecutionTaskSummary,
    ) -> Self {
        let next_package_kind = release_execution_task_summary
            .next_package_kind
            .or(release_execution_readiness_summary.next_package_kind);
        let next_handoff_status = release_execution_task_summary
            .next_handoff_status
            .or(release_execution_readiness_summary.next_handoff_status);
        let release_tasks_ready = release_execution_task_summary.ready_for_execution();
        let release_execution_ready =
            release_execution_readiness_summary.ready_for_execution() && release_tasks_ready;

        Self {
            total_checks: release_execution_readiness_summary.total_checks,
            ready_checks: release_execution_readiness_summary.ready_checks,
            blocked_checks: release_execution_readiness_summary.blocked_checks,
            review_required_checks: release_execution_readiness_summary.review_required_checks,
            operator_required_checks: release_execution_readiness_summary.operator_required_checks,
            checks_requiring_attention: release_execution_readiness_summary
                .checks_requiring_attention,
            total_slots: release_execution_readiness_summary.total_slots,
            ready_slots: release_execution_readiness_summary.ready_slots,
            blocked_slots: release_execution_readiness_summary.blocked_slots,
            review_required_slots: release_execution_readiness_summary.review_required_slots,
            operator_required_slots: release_execution_readiness_summary.operator_required_slots,
            slots_requiring_attention: release_execution_readiness_summary
                .slots_requiring_attention,
            total_tasks: release_execution_task_summary.total_tasks,
            ready_tasks: release_execution_task_summary.ready_tasks,
            blocked_tasks: release_execution_task_summary.blocked_tasks,
            review_required_tasks: release_execution_task_summary.review_required_tasks,
            operator_required_tasks: release_execution_task_summary.operator_required_tasks,
            tasks_requiring_attention: release_execution_task_summary.tasks_requiring_attention,
            queued_substrate_actions: release_execution_readiness_summary.queued_substrate_actions,
            remediation_item_count: release_execution_readiness_summary.remediation_item_count,
            review_required_packages: release_execution_readiness_summary.review_required_packages,
            operator_required_packages: release_execution_readiness_summary
                .operator_required_packages,
            blocked_packages: release_execution_readiness_summary.blocked_packages,
            first_task_key: release_execution_task_summary.first_task_key.clone(),
            first_blocked_task_key: release_execution_task_summary
                .first_blocked_task_key
                .clone(),
            first_review_task_key: release_execution_task_summary.first_review_task_key.clone(),
            first_operator_task_key: release_execution_task_summary
                .first_operator_task_key
                .clone(),
            next_task_key: release_execution_task_summary.next_task_key.clone(),
            next_slot_key: release_execution_task_summary.next_slot_key.clone(),
            next_check_kind: release_execution_task_summary.next_check_kind,
            next_check_status: release_execution_task_summary.next_check_status,
            next_package_kind,
            next_handoff_status,
            packet_status: release_execution_readiness_summary.packet_status,
            execution_status: release_execution_readiness_summary.execution_status,
            release_packet_ready: release_execution_readiness_summary.release_packet_ready,
            release_check_slots_ready: release_execution_readiness_summary
                .release_check_slots_ready,
            release_execution_ready,
            release_tasks_ready,
            release_execution_readiness_summary,
            release_execution_task_summary,
        }
    }

    pub fn ready_for_release_execution(&self) -> bool {
        self.execution_status == IntegrationMeshReleaseReadinessStatus::Ready
            && self.release_packet_ready
            && self.release_check_slots_ready
            && self.release_execution_ready
            && self.release_tasks_ready
            && !self.has_blockers()
            && !self.has_review_work()
    }

    pub fn has_release_tasks(&self) -> bool {
        self.total_tasks > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.blocked_checks > 0
            || self.blocked_slots > 0
            || self.blocked_tasks > 0
            || self.blocked_packages > 0
            || self.queued_substrate_actions > 0
    }

    pub fn has_review_work(&self) -> bool {
        self.review_required_checks > 0
            || self.review_required_slots > 0
            || self.review_required_tasks > 0
            || self.review_required_packages > 0
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_checks > 0
            || self.operator_required_slots > 0
            || self.operator_required_tasks > 0
            || self.operator_required_packages > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.checks_requiring_attention > 0
            || self.slots_requiring_attention > 0
            || self.tasks_requiring_attention > 0
            || self.has_blockers()
            || self.has_review_work()
            || self.needs_operator()
            || !self.release_tasks_ready
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseExecutionTaskDispatchSlot {
    pub sequence: usize,
    pub dispatch_key: String,
    pub task_key: String,
    pub slot_key: String,
    pub check_sequence: usize,
    pub check_kind: IntegrationMeshReleaseReadinessCheckKind,
    pub status: IntegrationMeshReleaseReadinessStatus,
    pub package_kind: Option<IntegrationMeshReadinessHandoffKind>,
    pub handoff_status: Option<IntegrationMeshReadinessHandoffStatus>,
    pub ready: bool,
    pub blocked: bool,
    pub review_required: bool,
    pub operator_required: bool,
    pub requires_attention: bool,
    pub dispatch_required: bool,
    pub packet_status: IntegrationMeshReleaseReadinessStatus,
    pub execution_status: IntegrationMeshReleaseReadinessStatus,
    pub release_execution_ready: bool,
    pub release_tasks_ready: bool,
}

impl IntegrationMeshReleaseExecutionTaskDispatchSlot {
    pub fn from_task(
        sequence: usize,
        task: &IntegrationMeshReleaseExecutionTask,
        release_task_readiness_summary: &IntegrationMeshReleaseTaskReadinessSummary,
    ) -> Self {
        let dispatch_required = task.requires_attention() || !task.ready();

        Self {
            sequence,
            dispatch_key: format!(
                "release-task-dispatch-{sequence:02}-{}",
                task.check_kind.as_str()
            ),
            task_key: task.task_key.clone(),
            slot_key: task.slot_key.clone(),
            check_sequence: task.check_sequence,
            check_kind: task.check_kind,
            status: task.status,
            package_kind: task.package_kind,
            handoff_status: task.handoff_status,
            ready: task.ready(),
            blocked: task.blocked(),
            review_required: task.review_required(),
            operator_required: task.operator_required(),
            requires_attention: task.requires_attention(),
            dispatch_required,
            packet_status: release_task_readiness_summary.packet_status,
            execution_status: release_task_readiness_summary.execution_status,
            release_execution_ready: release_task_readiness_summary.release_execution_ready,
            release_tasks_ready: release_task_readiness_summary.release_tasks_ready,
        }
    }

    pub fn ready(&self) -> bool {
        self.ready
    }

    pub fn blocked(&self) -> bool {
        self.blocked
    }

    pub fn review_required(&self) -> bool {
        self.review_required
    }

    pub fn operator_required(&self) -> bool {
        self.operator_required
    }

    pub fn dispatch_required(&self) -> bool {
        self.dispatch_required
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseExecutionTaskDispatchSummary {
    pub release_task_readiness_summary: IntegrationMeshReleaseTaskReadinessSummary,
    pub total_dispatch_slots: usize,
    pub ready_dispatch_slots: usize,
    pub blocked_dispatch_slots: usize,
    pub review_required_dispatch_slots: usize,
    pub operator_required_dispatch_slots: usize,
    pub dispatch_slots_requiring_attention: usize,
    pub dispatch_required_slots: usize,
    pub first_dispatch_key: Option<String>,
    pub first_blocked_dispatch_key: Option<String>,
    pub first_review_dispatch_key: Option<String>,
    pub first_operator_dispatch_key: Option<String>,
    pub next_dispatch_key: Option<String>,
    pub next_task_key: Option<String>,
    pub next_slot_key: Option<String>,
    pub next_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub next_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_package_kind: Option<IntegrationMeshReadinessHandoffKind>,
    pub next_handoff_status: Option<IntegrationMeshReadinessHandoffStatus>,
    pub packet_status: IntegrationMeshReleaseReadinessStatus,
    pub execution_status: IntegrationMeshReleaseReadinessStatus,
    pub release_execution_ready: bool,
    pub release_tasks_ready: bool,
}

impl IntegrationMeshReleaseExecutionTaskDispatchSummary {
    pub fn from_parts<'a>(
        release_task_readiness_summary: IntegrationMeshReleaseTaskReadinessSummary,
        dispatch_slots: impl IntoIterator<Item = &'a IntegrationMeshReleaseExecutionTaskDispatchSlot>,
    ) -> Self {
        let dispatch_slots = dispatch_slots.into_iter().collect::<Vec<_>>();
        let first_dispatch_slot = dispatch_slots.iter().min_by_key(|slot| slot.sequence);
        let first_blocked_dispatch_slot = dispatch_slots
            .iter()
            .filter(|slot| slot.blocked())
            .min_by_key(|slot| slot.sequence);
        let first_review_dispatch_slot = dispatch_slots
            .iter()
            .filter(|slot| slot.review_required())
            .min_by_key(|slot| slot.sequence);
        let first_operator_dispatch_slot = dispatch_slots
            .iter()
            .filter(|slot| slot.operator_required())
            .min_by_key(|slot| slot.sequence);
        let next_dispatch_slot = dispatch_slots
            .iter()
            .filter(|slot| slot.dispatch_required())
            .min_by_key(|slot| slot.sequence);

        Self {
            total_dispatch_slots: dispatch_slots.len(),
            ready_dispatch_slots: dispatch_slots.iter().filter(|slot| slot.ready()).count(),
            blocked_dispatch_slots: dispatch_slots.iter().filter(|slot| slot.blocked()).count(),
            review_required_dispatch_slots: dispatch_slots
                .iter()
                .filter(|slot| slot.review_required())
                .count(),
            operator_required_dispatch_slots: dispatch_slots
                .iter()
                .filter(|slot| slot.operator_required())
                .count(),
            dispatch_slots_requiring_attention: dispatch_slots
                .iter()
                .filter(|slot| slot.requires_attention)
                .count(),
            dispatch_required_slots: dispatch_slots
                .iter()
                .filter(|slot| slot.dispatch_required())
                .count(),
            first_dispatch_key: first_dispatch_slot.map(|slot| slot.dispatch_key.clone()),
            first_blocked_dispatch_key: first_blocked_dispatch_slot
                .map(|slot| slot.dispatch_key.clone()),
            first_review_dispatch_key: first_review_dispatch_slot
                .map(|slot| slot.dispatch_key.clone()),
            first_operator_dispatch_key: first_operator_dispatch_slot
                .map(|slot| slot.dispatch_key.clone()),
            next_dispatch_key: next_dispatch_slot.map(|slot| slot.dispatch_key.clone()),
            next_task_key: next_dispatch_slot.map(|slot| slot.task_key.clone()),
            next_slot_key: next_dispatch_slot.map(|slot| slot.slot_key.clone()),
            next_check_kind: next_dispatch_slot.map(|slot| slot.check_kind),
            next_check_status: next_dispatch_slot.map(|slot| slot.status),
            next_package_kind: next_dispatch_slot.and_then(|slot| slot.package_kind),
            next_handoff_status: next_dispatch_slot.and_then(|slot| slot.handoff_status),
            packet_status: release_task_readiness_summary.packet_status,
            execution_status: release_task_readiness_summary.execution_status,
            release_execution_ready: release_task_readiness_summary.release_execution_ready,
            release_tasks_ready: release_task_readiness_summary.release_tasks_ready,
            release_task_readiness_summary,
        }
    }

    pub fn has_dispatch_slots(&self) -> bool {
        self.total_dispatch_slots > 0
    }

    pub fn ready_for_dispatch(&self) -> bool {
        self.execution_status == IntegrationMeshReleaseReadinessStatus::Ready
            && self.release_execution_ready
            && self.release_tasks_ready
            && self.dispatch_required_slots == 0
    }

    pub fn has_blockers(&self) -> bool {
        self.blocked_dispatch_slots > 0 || self.release_task_readiness_summary.has_blockers()
    }

    pub fn has_review_work(&self) -> bool {
        self.review_required_dispatch_slots > 0
            || self.release_task_readiness_summary.has_review_work()
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_dispatch_slots > 0
            || self.release_task_readiness_summary.needs_operator()
    }

    pub fn requires_attention(&self) -> bool {
        self.dispatch_required_slots > 0
            || self.has_blockers()
            || self.has_review_work()
            || self.needs_operator()
            || !self.release_tasks_ready
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseDispatchReadinessSummary {
    pub release_task_readiness_summary: IntegrationMeshReleaseTaskReadinessSummary,
    pub release_execution_task_dispatch_summary: IntegrationMeshReleaseExecutionTaskDispatchSummary,
    pub total_checks: usize,
    pub ready_checks: usize,
    pub blocked_checks: usize,
    pub review_required_checks: usize,
    pub operator_required_checks: usize,
    pub checks_requiring_attention: usize,
    pub total_tasks: usize,
    pub ready_tasks: usize,
    pub blocked_tasks: usize,
    pub review_required_tasks: usize,
    pub operator_required_tasks: usize,
    pub tasks_requiring_attention: usize,
    pub total_dispatch_slots: usize,
    pub ready_dispatch_slots: usize,
    pub blocked_dispatch_slots: usize,
    pub review_required_dispatch_slots: usize,
    pub operator_required_dispatch_slots: usize,
    pub dispatch_slots_requiring_attention: usize,
    pub dispatch_required_slots: usize,
    pub queued_substrate_actions: usize,
    pub remediation_item_count: usize,
    pub review_required_packages: usize,
    pub operator_required_packages: usize,
    pub blocked_packages: usize,
    pub first_dispatch_key: Option<String>,
    pub first_blocked_dispatch_key: Option<String>,
    pub first_review_dispatch_key: Option<String>,
    pub first_operator_dispatch_key: Option<String>,
    pub next_dispatch_key: Option<String>,
    pub next_task_key: Option<String>,
    pub next_slot_key: Option<String>,
    pub next_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub next_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_package_kind: Option<IntegrationMeshReadinessHandoffKind>,
    pub next_handoff_status: Option<IntegrationMeshReadinessHandoffStatus>,
    pub packet_status: IntegrationMeshReleaseReadinessStatus,
    pub execution_status: IntegrationMeshReleaseReadinessStatus,
    pub release_packet_ready: bool,
    pub release_execution_ready: bool,
    pub release_tasks_ready: bool,
    pub release_dispatch_ready: bool,
}

impl IntegrationMeshReleaseDispatchReadinessSummary {
    pub fn from_summaries(
        release_task_readiness_summary: IntegrationMeshReleaseTaskReadinessSummary,
        release_execution_task_dispatch_summary: IntegrationMeshReleaseExecutionTaskDispatchSummary,
    ) -> Self {
        let next_package_kind = release_execution_task_dispatch_summary
            .next_package_kind
            .or(release_task_readiness_summary.next_package_kind);
        let next_handoff_status = release_execution_task_dispatch_summary
            .next_handoff_status
            .or(release_task_readiness_summary.next_handoff_status);
        let release_dispatch_ready = release_execution_task_dispatch_summary.ready_for_dispatch()
            && release_task_readiness_summary.ready_for_release_execution();

        Self {
            total_checks: release_task_readiness_summary.total_checks,
            ready_checks: release_task_readiness_summary.ready_checks,
            blocked_checks: release_task_readiness_summary.blocked_checks,
            review_required_checks: release_task_readiness_summary.review_required_checks,
            operator_required_checks: release_task_readiness_summary.operator_required_checks,
            checks_requiring_attention: release_task_readiness_summary.checks_requiring_attention,
            total_tasks: release_task_readiness_summary.total_tasks,
            ready_tasks: release_task_readiness_summary.ready_tasks,
            blocked_tasks: release_task_readiness_summary.blocked_tasks,
            review_required_tasks: release_task_readiness_summary.review_required_tasks,
            operator_required_tasks: release_task_readiness_summary.operator_required_tasks,
            tasks_requiring_attention: release_task_readiness_summary.tasks_requiring_attention,
            total_dispatch_slots: release_execution_task_dispatch_summary.total_dispatch_slots,
            ready_dispatch_slots: release_execution_task_dispatch_summary.ready_dispatch_slots,
            blocked_dispatch_slots: release_execution_task_dispatch_summary.blocked_dispatch_slots,
            review_required_dispatch_slots: release_execution_task_dispatch_summary
                .review_required_dispatch_slots,
            operator_required_dispatch_slots: release_execution_task_dispatch_summary
                .operator_required_dispatch_slots,
            dispatch_slots_requiring_attention: release_execution_task_dispatch_summary
                .dispatch_slots_requiring_attention,
            dispatch_required_slots: release_execution_task_dispatch_summary
                .dispatch_required_slots,
            queued_substrate_actions: release_task_readiness_summary.queued_substrate_actions,
            remediation_item_count: release_task_readiness_summary.remediation_item_count,
            review_required_packages: release_task_readiness_summary.review_required_packages,
            operator_required_packages: release_task_readiness_summary.operator_required_packages,
            blocked_packages: release_task_readiness_summary.blocked_packages,
            first_dispatch_key: release_execution_task_dispatch_summary
                .first_dispatch_key
                .clone(),
            first_blocked_dispatch_key: release_execution_task_dispatch_summary
                .first_blocked_dispatch_key
                .clone(),
            first_review_dispatch_key: release_execution_task_dispatch_summary
                .first_review_dispatch_key
                .clone(),
            first_operator_dispatch_key: release_execution_task_dispatch_summary
                .first_operator_dispatch_key
                .clone(),
            next_dispatch_key: release_execution_task_dispatch_summary
                .next_dispatch_key
                .clone(),
            next_task_key: release_execution_task_dispatch_summary
                .next_task_key
                .clone(),
            next_slot_key: release_execution_task_dispatch_summary
                .next_slot_key
                .clone(),
            next_check_kind: release_execution_task_dispatch_summary.next_check_kind,
            next_check_status: release_execution_task_dispatch_summary.next_check_status,
            next_package_kind,
            next_handoff_status,
            packet_status: release_task_readiness_summary.packet_status,
            execution_status: release_task_readiness_summary.execution_status,
            release_packet_ready: release_task_readiness_summary.release_packet_ready,
            release_execution_ready: release_task_readiness_summary.release_execution_ready,
            release_tasks_ready: release_task_readiness_summary.release_tasks_ready,
            release_dispatch_ready,
            release_task_readiness_summary,
            release_execution_task_dispatch_summary,
        }
    }

    pub fn ready_for_release_dispatch(&self) -> bool {
        self.execution_status == IntegrationMeshReleaseReadinessStatus::Ready
            && self.release_packet_ready
            && self.release_execution_ready
            && self.release_tasks_ready
            && self.release_dispatch_ready
            && !self.has_blockers()
            && !self.has_review_work()
    }

    pub fn has_dispatch_slots(&self) -> bool {
        self.total_dispatch_slots > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.blocked_checks > 0
            || self.blocked_tasks > 0
            || self.blocked_dispatch_slots > 0
            || self.blocked_packages > 0
            || self.queued_substrate_actions > 0
    }

    pub fn has_review_work(&self) -> bool {
        self.review_required_checks > 0
            || self.review_required_tasks > 0
            || self.review_required_dispatch_slots > 0
            || self.review_required_packages > 0
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_checks > 0
            || self.operator_required_tasks > 0
            || self.operator_required_dispatch_slots > 0
            || self.operator_required_packages > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.checks_requiring_attention > 0
            || self.tasks_requiring_attention > 0
            || self.dispatch_slots_requiring_attention > 0
            || self.dispatch_required_slots > 0
            || self.has_blockers()
            || self.has_review_work()
            || self.needs_operator()
            || !self.release_dispatch_ready
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseDispatchTicket {
    pub sequence: usize,
    pub ticket_key: String,
    pub dispatch_key: String,
    pub task_key: String,
    pub slot_key: String,
    pub check_sequence: usize,
    pub check_kind: IntegrationMeshReleaseReadinessCheckKind,
    pub status: IntegrationMeshReleaseReadinessStatus,
    pub package_kind: Option<IntegrationMeshReadinessHandoffKind>,
    pub handoff_status: Option<IntegrationMeshReadinessHandoffStatus>,
    pub ready: bool,
    pub blocked: bool,
    pub review_required: bool,
    pub operator_required: bool,
    pub dispatch_required: bool,
    pub packet_status: IntegrationMeshReleaseReadinessStatus,
    pub execution_status: IntegrationMeshReleaseReadinessStatus,
    pub release_dispatch_ready: bool,
}

impl IntegrationMeshReleaseDispatchTicket {
    pub fn from_dispatch_slot(
        sequence: usize,
        dispatch_slot: &IntegrationMeshReleaseExecutionTaskDispatchSlot,
        release_dispatch_readiness_summary: &IntegrationMeshReleaseDispatchReadinessSummary,
    ) -> Self {
        Self {
            sequence,
            ticket_key: format!(
                "release-dispatch-ticket-{sequence:02}-{}",
                dispatch_slot.check_kind.as_str()
            ),
            dispatch_key: dispatch_slot.dispatch_key.clone(),
            task_key: dispatch_slot.task_key.clone(),
            slot_key: dispatch_slot.slot_key.clone(),
            check_sequence: dispatch_slot.check_sequence,
            check_kind: dispatch_slot.check_kind,
            status: dispatch_slot.status,
            package_kind: dispatch_slot.package_kind,
            handoff_status: dispatch_slot.handoff_status,
            ready: dispatch_slot.ready(),
            blocked: dispatch_slot.blocked(),
            review_required: dispatch_slot.review_required(),
            operator_required: dispatch_slot.operator_required(),
            dispatch_required: dispatch_slot.dispatch_required(),
            packet_status: release_dispatch_readiness_summary.packet_status,
            execution_status: release_dispatch_readiness_summary.execution_status,
            release_dispatch_ready: release_dispatch_readiness_summary.release_dispatch_ready,
        }
    }

    pub fn ready(&self) -> bool {
        self.ready
    }

    pub fn blocked(&self) -> bool {
        self.blocked
    }

    pub fn review_required(&self) -> bool {
        self.review_required
    }

    pub fn operator_required(&self) -> bool {
        self.operator_required
    }

    pub fn dispatch_required(&self) -> bool {
        self.dispatch_required
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseDispatchTicketSummary {
    pub release_dispatch_readiness_summary: IntegrationMeshReleaseDispatchReadinessSummary,
    pub total_tickets: usize,
    pub ready_tickets: usize,
    pub blocked_tickets: usize,
    pub review_required_tickets: usize,
    pub operator_required_tickets: usize,
    pub dispatch_required_tickets: usize,
    pub first_ticket_key: Option<String>,
    pub first_blocked_ticket_key: Option<String>,
    pub first_review_ticket_key: Option<String>,
    pub first_operator_ticket_key: Option<String>,
    pub next_ticket_key: Option<String>,
    pub next_dispatch_key: Option<String>,
    pub next_task_key: Option<String>,
    pub next_slot_key: Option<String>,
    pub next_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub next_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_package_kind: Option<IntegrationMeshReadinessHandoffKind>,
    pub next_handoff_status: Option<IntegrationMeshReadinessHandoffStatus>,
    pub packet_status: IntegrationMeshReleaseReadinessStatus,
    pub execution_status: IntegrationMeshReleaseReadinessStatus,
    pub release_dispatch_ready: bool,
}

impl IntegrationMeshReleaseDispatchTicketSummary {
    pub fn from_parts<'a>(
        release_dispatch_readiness_summary: IntegrationMeshReleaseDispatchReadinessSummary,
        tickets: impl IntoIterator<Item = &'a IntegrationMeshReleaseDispatchTicket>,
    ) -> Self {
        let tickets = tickets.into_iter().collect::<Vec<_>>();
        let first_ticket = tickets.iter().min_by_key(|ticket| ticket.sequence);
        let first_blocked_ticket = tickets
            .iter()
            .filter(|ticket| ticket.blocked())
            .min_by_key(|ticket| ticket.sequence);
        let first_review_ticket = tickets
            .iter()
            .filter(|ticket| ticket.review_required())
            .min_by_key(|ticket| ticket.sequence);
        let first_operator_ticket = tickets
            .iter()
            .filter(|ticket| ticket.operator_required())
            .min_by_key(|ticket| ticket.sequence);
        let next_ticket = tickets
            .iter()
            .filter(|ticket| ticket.dispatch_required())
            .min_by_key(|ticket| ticket.sequence);

        Self {
            total_tickets: tickets.len(),
            ready_tickets: tickets.iter().filter(|ticket| ticket.ready()).count(),
            blocked_tickets: tickets.iter().filter(|ticket| ticket.blocked()).count(),
            review_required_tickets: tickets
                .iter()
                .filter(|ticket| ticket.review_required())
                .count(),
            operator_required_tickets: tickets
                .iter()
                .filter(|ticket| ticket.operator_required())
                .count(),
            dispatch_required_tickets: tickets
                .iter()
                .filter(|ticket| ticket.dispatch_required())
                .count(),
            first_ticket_key: first_ticket.map(|ticket| ticket.ticket_key.clone()),
            first_blocked_ticket_key: first_blocked_ticket.map(|ticket| ticket.ticket_key.clone()),
            first_review_ticket_key: first_review_ticket.map(|ticket| ticket.ticket_key.clone()),
            first_operator_ticket_key: first_operator_ticket
                .map(|ticket| ticket.ticket_key.clone()),
            next_ticket_key: next_ticket.map(|ticket| ticket.ticket_key.clone()),
            next_dispatch_key: next_ticket.map(|ticket| ticket.dispatch_key.clone()),
            next_task_key: next_ticket.map(|ticket| ticket.task_key.clone()),
            next_slot_key: next_ticket.map(|ticket| ticket.slot_key.clone()),
            next_check_kind: next_ticket.map(|ticket| ticket.check_kind),
            next_check_status: next_ticket.map(|ticket| ticket.status),
            next_package_kind: next_ticket.and_then(|ticket| ticket.package_kind),
            next_handoff_status: next_ticket.and_then(|ticket| ticket.handoff_status),
            packet_status: release_dispatch_readiness_summary.packet_status,
            execution_status: release_dispatch_readiness_summary.execution_status,
            release_dispatch_ready: release_dispatch_readiness_summary.release_dispatch_ready,
            release_dispatch_readiness_summary,
        }
    }

    pub fn has_tickets(&self) -> bool {
        self.total_tickets > 0
    }

    pub fn ready_for_ticket_dispatch(&self) -> bool {
        self.execution_status == IntegrationMeshReleaseReadinessStatus::Ready
            && self.release_dispatch_ready
            && self.dispatch_required_tickets == 0
    }

    pub fn has_blockers(&self) -> bool {
        self.blocked_tickets > 0 || self.release_dispatch_readiness_summary.has_blockers()
    }

    pub fn has_review_work(&self) -> bool {
        self.review_required_tickets > 0
            || self.release_dispatch_readiness_summary.has_review_work()
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_tickets > 0
            || self.release_dispatch_readiness_summary.needs_operator()
    }

    pub fn requires_attention(&self) -> bool {
        self.dispatch_required_tickets > 0
            || self.has_blockers()
            || self.has_review_work()
            || self.needs_operator()
            || !self.release_dispatch_ready
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationMeshReleaseDispatchTicketHandoffLane {
    Release,
    Operator,
    Repair,
    Review,
}

impl IntegrationMeshReleaseDispatchTicketHandoffLane {
    pub fn as_str(self) -> &'static str {
        match self {
            Self::Release => "release",
            Self::Operator => "operator",
            Self::Repair => "repair",
            Self::Review => "review",
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseDispatchTicketHandoffPacket {
    pub sequence: usize,
    pub packet_key: String,
    pub ticket_key: String,
    pub dispatch_key: String,
    pub task_key: String,
    pub slot_key: String,
    pub check_sequence: usize,
    pub check_kind: IntegrationMeshReleaseReadinessCheckKind,
    pub status: IntegrationMeshReleaseReadinessStatus,
    pub package_kind: Option<IntegrationMeshReadinessHandoffKind>,
    pub handoff_status: Option<IntegrationMeshReadinessHandoffStatus>,
    pub handoff_lane: IntegrationMeshReleaseDispatchTicketHandoffLane,
    pub ready: bool,
    pub blocked: bool,
    pub review_required: bool,
    pub operator_required: bool,
    pub dispatch_required: bool,
    pub packet_status: IntegrationMeshReleaseReadinessStatus,
    pub execution_status: IntegrationMeshReleaseReadinessStatus,
    pub release_dispatch_ready: bool,
}

impl IntegrationMeshReleaseDispatchTicketHandoffPacket {
    pub fn from_ticket(ticket: &IntegrationMeshReleaseDispatchTicket) -> Self {
        let handoff_lane = if ticket.blocked() {
            IntegrationMeshReleaseDispatchTicketHandoffLane::Repair
        } else if ticket.review_required() {
            IntegrationMeshReleaseDispatchTicketHandoffLane::Review
        } else if ticket.operator_required() || ticket.dispatch_required() {
            IntegrationMeshReleaseDispatchTicketHandoffLane::Operator
        } else {
            IntegrationMeshReleaseDispatchTicketHandoffLane::Release
        };

        Self {
            sequence: ticket.sequence,
            packet_key: format!(
                "release-dispatch-handoff-packet-{sequence:02}-{}",
                ticket.check_kind.as_str(),
                sequence = ticket.sequence
            ),
            ticket_key: ticket.ticket_key.clone(),
            dispatch_key: ticket.dispatch_key.clone(),
            task_key: ticket.task_key.clone(),
            slot_key: ticket.slot_key.clone(),
            check_sequence: ticket.check_sequence,
            check_kind: ticket.check_kind,
            status: ticket.status,
            package_kind: ticket.package_kind,
            handoff_status: ticket.handoff_status,
            handoff_lane,
            ready: ticket.ready(),
            blocked: ticket.blocked(),
            review_required: ticket.review_required(),
            operator_required: ticket.operator_required(),
            dispatch_required: ticket.dispatch_required(),
            packet_status: ticket.packet_status,
            execution_status: ticket.execution_status,
            release_dispatch_ready: ticket.release_dispatch_ready,
        }
    }

    pub fn ready(&self) -> bool {
        self.ready
    }

    pub fn blocked(&self) -> bool {
        self.blocked
    }

    pub fn review_required(&self) -> bool {
        self.review_required
    }

    pub fn operator_required(&self) -> bool {
        self.operator_required
    }

    pub fn dispatch_required(&self) -> bool {
        self.dispatch_required
    }

    pub fn is_release_lane(&self) -> bool {
        self.handoff_lane == IntegrationMeshReleaseDispatchTicketHandoffLane::Release
    }

    pub fn is_operator_lane(&self) -> bool {
        self.handoff_lane == IntegrationMeshReleaseDispatchTicketHandoffLane::Operator
    }

    pub fn is_repair_lane(&self) -> bool {
        self.handoff_lane == IntegrationMeshReleaseDispatchTicketHandoffLane::Repair
    }

    pub fn is_review_lane(&self) -> bool {
        self.handoff_lane == IntegrationMeshReleaseDispatchTicketHandoffLane::Review
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseDispatchTicketHandoffSummary {
    pub release_dispatch_ticket_summary: IntegrationMeshReleaseDispatchTicketSummary,
    pub total_packets: usize,
    pub release_lane_packets: usize,
    pub operator_lane_packets: usize,
    pub repair_lane_packets: usize,
    pub review_lane_packets: usize,
    pub dispatch_required_packets: usize,
    pub first_packet_key: Option<String>,
    pub first_release_packet_key: Option<String>,
    pub first_operator_packet_key: Option<String>,
    pub first_repair_packet_key: Option<String>,
    pub first_review_packet_key: Option<String>,
    pub next_packet_key: Option<String>,
    pub next_ticket_key: Option<String>,
    pub next_dispatch_key: Option<String>,
    pub next_task_key: Option<String>,
    pub next_slot_key: Option<String>,
    pub next_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub next_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_package_kind: Option<IntegrationMeshReadinessHandoffKind>,
    pub next_handoff_status: Option<IntegrationMeshReadinessHandoffStatus>,
    pub next_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub packet_status: IntegrationMeshReleaseReadinessStatus,
    pub execution_status: IntegrationMeshReleaseReadinessStatus,
    pub release_dispatch_ready: bool,
    pub release_ticket_ready: bool,
    pub release_handoff_ready: bool,
}

impl IntegrationMeshReleaseDispatchTicketHandoffSummary {
    pub fn from_parts<'a>(
        release_dispatch_ticket_summary: IntegrationMeshReleaseDispatchTicketSummary,
        packets: impl IntoIterator<Item = &'a IntegrationMeshReleaseDispatchTicketHandoffPacket>,
    ) -> Self {
        let packets = packets.into_iter().collect::<Vec<_>>();
        let first_packet = packets.iter().min_by_key(|packet| packet.sequence);
        let first_release_packet = packets
            .iter()
            .filter(|packet| packet.is_release_lane())
            .min_by_key(|packet| packet.sequence);
        let first_operator_packet = packets
            .iter()
            .filter(|packet| packet.is_operator_lane())
            .min_by_key(|packet| packet.sequence);
        let first_repair_packet = packets
            .iter()
            .filter(|packet| packet.is_repair_lane())
            .min_by_key(|packet| packet.sequence);
        let first_review_packet = packets
            .iter()
            .filter(|packet| packet.is_review_lane())
            .min_by_key(|packet| packet.sequence);
        let next_packet = packets
            .iter()
            .filter(|packet| packet.dispatch_required())
            .min_by_key(|packet| packet.sequence);
        let release_handoff_ready = release_dispatch_ticket_summary.ready_for_ticket_dispatch()
            && packets.iter().all(|packet| packet.is_release_lane());

        Self {
            total_packets: packets.len(),
            release_lane_packets: packets
                .iter()
                .filter(|packet| packet.is_release_lane())
                .count(),
            operator_lane_packets: packets
                .iter()
                .filter(|packet| packet.is_operator_lane())
                .count(),
            repair_lane_packets: packets
                .iter()
                .filter(|packet| packet.is_repair_lane())
                .count(),
            review_lane_packets: packets
                .iter()
                .filter(|packet| packet.is_review_lane())
                .count(),
            dispatch_required_packets: packets
                .iter()
                .filter(|packet| packet.dispatch_required())
                .count(),
            first_packet_key: first_packet.map(|packet| packet.packet_key.clone()),
            first_release_packet_key: first_release_packet.map(|packet| packet.packet_key.clone()),
            first_operator_packet_key: first_operator_packet
                .map(|packet| packet.packet_key.clone()),
            first_repair_packet_key: first_repair_packet.map(|packet| packet.packet_key.clone()),
            first_review_packet_key: first_review_packet.map(|packet| packet.packet_key.clone()),
            next_packet_key: next_packet.map(|packet| packet.packet_key.clone()),
            next_ticket_key: next_packet.map(|packet| packet.ticket_key.clone()),
            next_dispatch_key: next_packet.map(|packet| packet.dispatch_key.clone()),
            next_task_key: next_packet.map(|packet| packet.task_key.clone()),
            next_slot_key: next_packet.map(|packet| packet.slot_key.clone()),
            next_check_kind: next_packet.map(|packet| packet.check_kind),
            next_check_status: next_packet.map(|packet| packet.status),
            next_package_kind: next_packet.and_then(|packet| packet.package_kind),
            next_handoff_status: next_packet.and_then(|packet| packet.handoff_status),
            next_handoff_lane: next_packet.map(|packet| packet.handoff_lane),
            packet_status: release_dispatch_ticket_summary.packet_status,
            execution_status: release_dispatch_ticket_summary.execution_status,
            release_dispatch_ready: release_dispatch_ticket_summary.release_dispatch_ready,
            release_ticket_ready: release_dispatch_ticket_summary.ready_for_ticket_dispatch(),
            release_handoff_ready,
            release_dispatch_ticket_summary,
        }
    }

    pub fn has_packets(&self) -> bool {
        self.total_packets > 0
    }

    pub fn ready_for_release_handoff(&self) -> bool {
        self.release_handoff_ready && !self.requires_attention()
    }

    pub fn has_repair_work(&self) -> bool {
        self.repair_lane_packets > 0
    }

    pub fn has_review_work(&self) -> bool {
        self.review_lane_packets > 0 || self.release_dispatch_ticket_summary.has_review_work()
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_lane_packets > 0 || self.release_dispatch_ticket_summary.needs_operator()
    }

    pub fn requires_attention(&self) -> bool {
        self.dispatch_required_packets > 0
            || self.has_repair_work()
            || self.has_review_work()
            || self.needs_operator()
            || !self.release_dispatch_ready
            || !self.release_ticket_ready
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketReadinessSummary {
    pub release_dispatch_readiness_summary: IntegrationMeshReleaseDispatchReadinessSummary,
    pub release_dispatch_ticket_summary: IntegrationMeshReleaseDispatchTicketSummary,
    pub total_checks: usize,
    pub ready_checks: usize,
    pub blocked_checks: usize,
    pub review_required_checks: usize,
    pub operator_required_checks: usize,
    pub checks_requiring_attention: usize,
    pub total_tasks: usize,
    pub ready_tasks: usize,
    pub blocked_tasks: usize,
    pub review_required_tasks: usize,
    pub operator_required_tasks: usize,
    pub tasks_requiring_attention: usize,
    pub total_dispatch_slots: usize,
    pub ready_dispatch_slots: usize,
    pub blocked_dispatch_slots: usize,
    pub review_required_dispatch_slots: usize,
    pub operator_required_dispatch_slots: usize,
    pub dispatch_slots_requiring_attention: usize,
    pub dispatch_required_slots: usize,
    pub total_tickets: usize,
    pub ready_tickets: usize,
    pub blocked_tickets: usize,
    pub review_required_tickets: usize,
    pub operator_required_tickets: usize,
    pub dispatch_required_tickets: usize,
    pub queued_substrate_actions: usize,
    pub remediation_item_count: usize,
    pub review_required_packages: usize,
    pub operator_required_packages: usize,
    pub blocked_packages: usize,
    pub first_ticket_key: Option<String>,
    pub first_blocked_ticket_key: Option<String>,
    pub first_review_ticket_key: Option<String>,
    pub first_operator_ticket_key: Option<String>,
    pub next_ticket_key: Option<String>,
    pub next_dispatch_key: Option<String>,
    pub next_task_key: Option<String>,
    pub next_slot_key: Option<String>,
    pub next_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub next_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_package_kind: Option<IntegrationMeshReadinessHandoffKind>,
    pub next_handoff_status: Option<IntegrationMeshReadinessHandoffStatus>,
    pub packet_status: IntegrationMeshReleaseReadinessStatus,
    pub execution_status: IntegrationMeshReleaseReadinessStatus,
    pub release_packet_ready: bool,
    pub release_execution_ready: bool,
    pub release_tasks_ready: bool,
    pub release_dispatch_ready: bool,
    pub release_ticket_ready: bool,
}

impl IntegrationMeshReleaseTicketReadinessSummary {
    pub fn from_summaries(
        release_dispatch_readiness_summary: IntegrationMeshReleaseDispatchReadinessSummary,
        release_dispatch_ticket_summary: IntegrationMeshReleaseDispatchTicketSummary,
    ) -> Self {
        let next_package_kind = release_dispatch_ticket_summary
            .next_package_kind
            .or(release_dispatch_readiness_summary.next_package_kind);
        let next_handoff_status = release_dispatch_ticket_summary
            .next_handoff_status
            .or(release_dispatch_readiness_summary.next_handoff_status);
        let release_ticket_ready = release_dispatch_readiness_summary.ready_for_release_dispatch()
            && release_dispatch_ticket_summary.ready_for_ticket_dispatch();

        Self {
            total_checks: release_dispatch_readiness_summary.total_checks,
            ready_checks: release_dispatch_readiness_summary.ready_checks,
            blocked_checks: release_dispatch_readiness_summary.blocked_checks,
            review_required_checks: release_dispatch_readiness_summary.review_required_checks,
            operator_required_checks: release_dispatch_readiness_summary.operator_required_checks,
            checks_requiring_attention: release_dispatch_readiness_summary
                .checks_requiring_attention,
            total_tasks: release_dispatch_readiness_summary.total_tasks,
            ready_tasks: release_dispatch_readiness_summary.ready_tasks,
            blocked_tasks: release_dispatch_readiness_summary.blocked_tasks,
            review_required_tasks: release_dispatch_readiness_summary.review_required_tasks,
            operator_required_tasks: release_dispatch_readiness_summary.operator_required_tasks,
            tasks_requiring_attention: release_dispatch_readiness_summary.tasks_requiring_attention,
            total_dispatch_slots: release_dispatch_readiness_summary.total_dispatch_slots,
            ready_dispatch_slots: release_dispatch_readiness_summary.ready_dispatch_slots,
            blocked_dispatch_slots: release_dispatch_readiness_summary.blocked_dispatch_slots,
            review_required_dispatch_slots: release_dispatch_readiness_summary
                .review_required_dispatch_slots,
            operator_required_dispatch_slots: release_dispatch_readiness_summary
                .operator_required_dispatch_slots,
            dispatch_slots_requiring_attention: release_dispatch_readiness_summary
                .dispatch_slots_requiring_attention,
            dispatch_required_slots: release_dispatch_readiness_summary.dispatch_required_slots,
            total_tickets: release_dispatch_ticket_summary.total_tickets,
            ready_tickets: release_dispatch_ticket_summary.ready_tickets,
            blocked_tickets: release_dispatch_ticket_summary.blocked_tickets,
            review_required_tickets: release_dispatch_ticket_summary.review_required_tickets,
            operator_required_tickets: release_dispatch_ticket_summary.operator_required_tickets,
            dispatch_required_tickets: release_dispatch_ticket_summary.dispatch_required_tickets,
            queued_substrate_actions: release_dispatch_readiness_summary.queued_substrate_actions,
            remediation_item_count: release_dispatch_readiness_summary.remediation_item_count,
            review_required_packages: release_dispatch_readiness_summary.review_required_packages,
            operator_required_packages: release_dispatch_readiness_summary
                .operator_required_packages,
            blocked_packages: release_dispatch_readiness_summary.blocked_packages,
            first_ticket_key: release_dispatch_ticket_summary.first_ticket_key.clone(),
            first_blocked_ticket_key: release_dispatch_ticket_summary
                .first_blocked_ticket_key
                .clone(),
            first_review_ticket_key: release_dispatch_ticket_summary
                .first_review_ticket_key
                .clone(),
            first_operator_ticket_key: release_dispatch_ticket_summary
                .first_operator_ticket_key
                .clone(),
            next_ticket_key: release_dispatch_ticket_summary.next_ticket_key.clone(),
            next_dispatch_key: release_dispatch_ticket_summary.next_dispatch_key.clone(),
            next_task_key: release_dispatch_ticket_summary.next_task_key.clone(),
            next_slot_key: release_dispatch_ticket_summary.next_slot_key.clone(),
            next_check_kind: release_dispatch_ticket_summary.next_check_kind,
            next_check_status: release_dispatch_ticket_summary.next_check_status,
            next_package_kind,
            next_handoff_status,
            packet_status: release_dispatch_readiness_summary.packet_status,
            execution_status: release_dispatch_readiness_summary.execution_status,
            release_packet_ready: release_dispatch_readiness_summary.release_packet_ready,
            release_execution_ready: release_dispatch_readiness_summary.release_execution_ready,
            release_tasks_ready: release_dispatch_readiness_summary.release_tasks_ready,
            release_dispatch_ready: release_dispatch_readiness_summary.release_dispatch_ready,
            release_ticket_ready,
            release_dispatch_readiness_summary,
            release_dispatch_ticket_summary,
        }
    }

    pub fn ready_for_release_ticket_dispatch(&self) -> bool {
        self.execution_status == IntegrationMeshReleaseReadinessStatus::Ready
            && self.release_packet_ready
            && self.release_execution_ready
            && self.release_tasks_ready
            && self.release_dispatch_ready
            && self.release_ticket_ready
            && !self.has_blockers()
            && !self.has_review_work()
    }

    pub fn has_tickets(&self) -> bool {
        self.total_tickets > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.blocked_checks > 0
            || self.blocked_tasks > 0
            || self.blocked_dispatch_slots > 0
            || self.blocked_tickets > 0
            || self.blocked_packages > 0
            || self.queued_substrate_actions > 0
    }

    pub fn has_review_work(&self) -> bool {
        self.review_required_checks > 0
            || self.review_required_tasks > 0
            || self.review_required_dispatch_slots > 0
            || self.review_required_tickets > 0
            || self.review_required_packages > 0
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_checks > 0
            || self.operator_required_tasks > 0
            || self.operator_required_dispatch_slots > 0
            || self.operator_required_tickets > 0
            || self.operator_required_packages > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.checks_requiring_attention > 0
            || self.tasks_requiring_attention > 0
            || self.dispatch_slots_requiring_attention > 0
            || self.dispatch_required_slots > 0
            || self.dispatch_required_tickets > 0
            || self.has_blockers()
            || self.has_review_work()
            || self.needs_operator()
            || !self.release_ticket_ready
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffReadinessSummary {
    pub release_ticket_readiness_summary: IntegrationMeshReleaseTicketReadinessSummary,
    pub release_dispatch_ticket_handoff_summary: IntegrationMeshReleaseDispatchTicketHandoffSummary,
    pub total_tickets: usize,
    pub ready_tickets: usize,
    pub blocked_tickets: usize,
    pub review_required_tickets: usize,
    pub operator_required_tickets: usize,
    pub dispatch_required_tickets: usize,
    pub total_handoff_packets: usize,
    pub release_lane_packets: usize,
    pub operator_lane_packets: usize,
    pub repair_lane_packets: usize,
    pub review_lane_packets: usize,
    pub dispatch_required_packets: usize,
    pub queued_substrate_actions: usize,
    pub remediation_item_count: usize,
    pub review_required_packages: usize,
    pub operator_required_packages: usize,
    pub blocked_packages: usize,
    pub first_ticket_key: Option<String>,
    pub first_packet_key: Option<String>,
    pub first_release_packet_key: Option<String>,
    pub first_operator_packet_key: Option<String>,
    pub first_repair_packet_key: Option<String>,
    pub first_review_packet_key: Option<String>,
    pub next_packet_key: Option<String>,
    pub next_ticket_key: Option<String>,
    pub next_dispatch_key: Option<String>,
    pub next_task_key: Option<String>,
    pub next_slot_key: Option<String>,
    pub next_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub next_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_package_kind: Option<IntegrationMeshReadinessHandoffKind>,
    pub next_handoff_status: Option<IntegrationMeshReadinessHandoffStatus>,
    pub next_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub packet_status: IntegrationMeshReleaseReadinessStatus,
    pub execution_status: IntegrationMeshReleaseReadinessStatus,
    pub release_packet_ready: bool,
    pub release_execution_ready: bool,
    pub release_tasks_ready: bool,
    pub release_dispatch_ready: bool,
    pub release_ticket_ready: bool,
    pub release_handoff_ready: bool,
}

impl IntegrationMeshReleaseTicketHandoffReadinessSummary {
    pub fn from_summaries(
        release_ticket_readiness_summary: IntegrationMeshReleaseTicketReadinessSummary,
        release_dispatch_ticket_handoff_summary: IntegrationMeshReleaseDispatchTicketHandoffSummary,
    ) -> Self {
        let next_package_kind = release_dispatch_ticket_handoff_summary
            .next_package_kind
            .or(release_ticket_readiness_summary.next_package_kind);
        let next_handoff_status = release_dispatch_ticket_handoff_summary
            .next_handoff_status
            .or(release_ticket_readiness_summary.next_handoff_status);
        let next_packet_key = release_dispatch_ticket_handoff_summary
            .next_packet_key
            .clone();
        let next_ticket_key = release_dispatch_ticket_handoff_summary
            .next_ticket_key
            .clone()
            .or_else(|| release_ticket_readiness_summary.next_ticket_key.clone());
        let next_dispatch_key = release_dispatch_ticket_handoff_summary
            .next_dispatch_key
            .clone()
            .or_else(|| release_ticket_readiness_summary.next_dispatch_key.clone());
        let next_task_key = release_dispatch_ticket_handoff_summary
            .next_task_key
            .clone()
            .or_else(|| release_ticket_readiness_summary.next_task_key.clone());
        let next_slot_key = release_dispatch_ticket_handoff_summary
            .next_slot_key
            .clone()
            .or_else(|| release_ticket_readiness_summary.next_slot_key.clone());
        let next_check_kind = release_dispatch_ticket_handoff_summary
            .next_check_kind
            .or(release_ticket_readiness_summary.next_check_kind);
        let next_check_status = release_dispatch_ticket_handoff_summary
            .next_check_status
            .or(release_ticket_readiness_summary.next_check_status);

        Self {
            total_tickets: release_ticket_readiness_summary.total_tickets,
            ready_tickets: release_ticket_readiness_summary.ready_tickets,
            blocked_tickets: release_ticket_readiness_summary.blocked_tickets,
            review_required_tickets: release_ticket_readiness_summary.review_required_tickets,
            operator_required_tickets: release_ticket_readiness_summary.operator_required_tickets,
            dispatch_required_tickets: release_ticket_readiness_summary.dispatch_required_tickets,
            total_handoff_packets: release_dispatch_ticket_handoff_summary.total_packets,
            release_lane_packets: release_dispatch_ticket_handoff_summary.release_lane_packets,
            operator_lane_packets: release_dispatch_ticket_handoff_summary.operator_lane_packets,
            repair_lane_packets: release_dispatch_ticket_handoff_summary.repair_lane_packets,
            review_lane_packets: release_dispatch_ticket_handoff_summary.review_lane_packets,
            dispatch_required_packets: release_dispatch_ticket_handoff_summary
                .dispatch_required_packets,
            queued_substrate_actions: release_ticket_readiness_summary.queued_substrate_actions,
            remediation_item_count: release_ticket_readiness_summary.remediation_item_count,
            review_required_packages: release_ticket_readiness_summary.review_required_packages,
            operator_required_packages: release_ticket_readiness_summary.operator_required_packages,
            blocked_packages: release_ticket_readiness_summary.blocked_packages,
            first_ticket_key: release_ticket_readiness_summary.first_ticket_key.clone(),
            first_packet_key: release_dispatch_ticket_handoff_summary
                .first_packet_key
                .clone(),
            first_release_packet_key: release_dispatch_ticket_handoff_summary
                .first_release_packet_key
                .clone(),
            first_operator_packet_key: release_dispatch_ticket_handoff_summary
                .first_operator_packet_key
                .clone(),
            first_repair_packet_key: release_dispatch_ticket_handoff_summary
                .first_repair_packet_key
                .clone(),
            first_review_packet_key: release_dispatch_ticket_handoff_summary
                .first_review_packet_key
                .clone(),
            next_packet_key,
            next_ticket_key,
            next_dispatch_key,
            next_task_key,
            next_slot_key,
            next_check_kind,
            next_check_status,
            next_package_kind,
            next_handoff_status,
            next_handoff_lane: release_dispatch_ticket_handoff_summary.next_handoff_lane,
            packet_status: release_ticket_readiness_summary.packet_status,
            execution_status: release_ticket_readiness_summary.execution_status,
            release_packet_ready: release_ticket_readiness_summary.release_packet_ready,
            release_execution_ready: release_ticket_readiness_summary.release_execution_ready,
            release_tasks_ready: release_ticket_readiness_summary.release_tasks_ready,
            release_dispatch_ready: release_ticket_readiness_summary.release_dispatch_ready,
            release_ticket_ready: release_ticket_readiness_summary.release_ticket_ready,
            release_handoff_ready: release_dispatch_ticket_handoff_summary.release_handoff_ready,
            release_ticket_readiness_summary,
            release_dispatch_ticket_handoff_summary,
        }
    }

    pub fn ready_for_release_handoff(&self) -> bool {
        self.execution_status == IntegrationMeshReleaseReadinessStatus::Ready
            && self.release_packet_ready
            && self.release_execution_ready
            && self.release_tasks_ready
            && self.release_dispatch_ready
            && self.release_ticket_ready
            && self.release_handoff_ready
            && !self.requires_attention()
    }

    pub fn has_handoff_packets(&self) -> bool {
        self.total_handoff_packets > 0
    }

    pub fn has_repair_work(&self) -> bool {
        self.repair_lane_packets > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.has_repair_work() || self.release_ticket_readiness_summary.has_blockers()
    }

    pub fn has_review_work(&self) -> bool {
        self.review_lane_packets > 0 || self.release_ticket_readiness_summary.has_review_work()
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_lane_packets > 0 || self.release_ticket_readiness_summary.needs_operator()
    }

    pub fn requires_attention(&self) -> bool {
        self.dispatch_required_packets > 0
            || self.dispatch_required_tickets > 0
            || self.has_blockers()
            || self.has_review_work()
            || self.needs_operator()
            || !self.release_ticket_ready
            || !self.release_handoff_ready
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffExecutionSlot {
    pub sequence: usize,
    pub execution_slot_key: String,
    pub packet_key: String,
    pub ticket_key: String,
    pub dispatch_key: String,
    pub task_key: String,
    pub slot_key: String,
    pub check_sequence: usize,
    pub check_kind: IntegrationMeshReleaseReadinessCheckKind,
    pub status: IntegrationMeshReleaseReadinessStatus,
    pub package_kind: Option<IntegrationMeshReadinessHandoffKind>,
    pub handoff_status: Option<IntegrationMeshReadinessHandoffStatus>,
    pub handoff_lane: IntegrationMeshReleaseDispatchTicketHandoffLane,
    pub ready: bool,
    pub blocked: bool,
    pub review_required: bool,
    pub operator_required: bool,
    pub dispatch_required: bool,
    pub execution_required: bool,
    pub packet_status: IntegrationMeshReleaseReadinessStatus,
    pub execution_status: IntegrationMeshReleaseReadinessStatus,
    pub release_dispatch_ready: bool,
}

impl IntegrationMeshReleaseTicketHandoffExecutionSlot {
    pub fn from_packet(
        sequence: usize,
        packet: &IntegrationMeshReleaseDispatchTicketHandoffPacket,
    ) -> Self {
        let execution_required = !packet.ready() || !packet.is_release_lane();

        Self {
            sequence,
            execution_slot_key: format!(
                "release-ticket-handoff-execution-slot-{sequence:02}-{}-{}",
                packet.handoff_lane.as_str(),
                packet.check_kind.as_str()
            ),
            packet_key: packet.packet_key.clone(),
            ticket_key: packet.ticket_key.clone(),
            dispatch_key: packet.dispatch_key.clone(),
            task_key: packet.task_key.clone(),
            slot_key: packet.slot_key.clone(),
            check_sequence: packet.check_sequence,
            check_kind: packet.check_kind,
            status: packet.status,
            package_kind: packet.package_kind,
            handoff_status: packet.handoff_status,
            handoff_lane: packet.handoff_lane,
            ready: packet.ready(),
            blocked: packet.blocked(),
            review_required: packet.review_required(),
            operator_required: packet.operator_required(),
            dispatch_required: packet.dispatch_required(),
            execution_required,
            packet_status: packet.packet_status,
            execution_status: packet.execution_status,
            release_dispatch_ready: packet.release_dispatch_ready,
        }
    }

    pub fn ready(&self) -> bool {
        self.ready
    }

    pub fn blocked(&self) -> bool {
        self.blocked
    }

    pub fn review_required(&self) -> bool {
        self.review_required
    }

    pub fn operator_required(&self) -> bool {
        self.operator_required
    }

    pub fn dispatch_required(&self) -> bool {
        self.dispatch_required
    }

    pub fn execution_required(&self) -> bool {
        self.execution_required
    }

    pub fn is_release_lane(&self) -> bool {
        self.handoff_lane == IntegrationMeshReleaseDispatchTicketHandoffLane::Release
    }

    pub fn is_operator_lane(&self) -> bool {
        self.handoff_lane == IntegrationMeshReleaseDispatchTicketHandoffLane::Operator
    }

    pub fn is_repair_lane(&self) -> bool {
        self.handoff_lane == IntegrationMeshReleaseDispatchTicketHandoffLane::Repair
    }

    pub fn is_review_lane(&self) -> bool {
        self.handoff_lane == IntegrationMeshReleaseDispatchTicketHandoffLane::Review
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffExecutionSlotSummary {
    pub release_ticket_handoff_readiness_summary:
        IntegrationMeshReleaseTicketHandoffReadinessSummary,
    pub total_execution_slots: usize,
    pub ready_execution_slots: usize,
    pub blocked_execution_slots: usize,
    pub review_required_execution_slots: usize,
    pub operator_required_execution_slots: usize,
    pub dispatch_required_execution_slots: usize,
    pub release_lane_execution_slots: usize,
    pub operator_lane_execution_slots: usize,
    pub repair_lane_execution_slots: usize,
    pub review_lane_execution_slots: usize,
    pub execution_required_slots: usize,
    pub first_execution_slot_key: Option<String>,
    pub first_release_execution_slot_key: Option<String>,
    pub first_operator_execution_slot_key: Option<String>,
    pub first_repair_execution_slot_key: Option<String>,
    pub first_review_execution_slot_key: Option<String>,
    pub next_execution_slot_key: Option<String>,
    pub next_packet_key: Option<String>,
    pub next_ticket_key: Option<String>,
    pub next_dispatch_key: Option<String>,
    pub next_task_key: Option<String>,
    pub next_slot_key: Option<String>,
    pub next_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub next_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_package_kind: Option<IntegrationMeshReadinessHandoffKind>,
    pub next_handoff_status: Option<IntegrationMeshReadinessHandoffStatus>,
    pub next_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub packet_status: IntegrationMeshReleaseReadinessStatus,
    pub execution_status: IntegrationMeshReleaseReadinessStatus,
    pub release_packet_ready: bool,
    pub release_execution_ready: bool,
    pub release_tasks_ready: bool,
    pub release_dispatch_ready: bool,
    pub release_ticket_ready: bool,
    pub release_handoff_ready: bool,
    pub release_handoff_execution_ready: bool,
}

impl IntegrationMeshReleaseTicketHandoffExecutionSlotSummary {
    pub fn from_parts<'a>(
        release_ticket_handoff_readiness_summary: IntegrationMeshReleaseTicketHandoffReadinessSummary,
        execution_slots: impl IntoIterator<Item = &'a IntegrationMeshReleaseTicketHandoffExecutionSlot>,
    ) -> Self {
        let execution_slots = execution_slots.into_iter().collect::<Vec<_>>();
        let first_execution_slot = execution_slots.iter().min_by_key(|slot| slot.sequence);
        let first_release_execution_slot = execution_slots
            .iter()
            .filter(|slot| slot.is_release_lane())
            .min_by_key(|slot| slot.sequence);
        let first_operator_execution_slot = execution_slots
            .iter()
            .filter(|slot| slot.is_operator_lane())
            .min_by_key(|slot| slot.sequence);
        let first_repair_execution_slot = execution_slots
            .iter()
            .filter(|slot| slot.is_repair_lane())
            .min_by_key(|slot| slot.sequence);
        let first_review_execution_slot = execution_slots
            .iter()
            .filter(|slot| slot.is_review_lane())
            .min_by_key(|slot| slot.sequence);
        let next_execution_slot = execution_slots
            .iter()
            .filter(|slot| slot.execution_required())
            .min_by_key(|slot| slot.sequence);
        let release_handoff_execution_ready = release_ticket_handoff_readiness_summary
            .ready_for_release_handoff()
            && !execution_slots.is_empty()
            && execution_slots
                .iter()
                .all(|slot| slot.ready() && slot.is_release_lane());

        Self {
            total_execution_slots: execution_slots.len(),
            ready_execution_slots: execution_slots.iter().filter(|slot| slot.ready()).count(),
            blocked_execution_slots: execution_slots.iter().filter(|slot| slot.blocked()).count(),
            review_required_execution_slots: execution_slots
                .iter()
                .filter(|slot| slot.review_required())
                .count(),
            operator_required_execution_slots: execution_slots
                .iter()
                .filter(|slot| slot.operator_required())
                .count(),
            dispatch_required_execution_slots: execution_slots
                .iter()
                .filter(|slot| slot.dispatch_required())
                .count(),
            release_lane_execution_slots: execution_slots
                .iter()
                .filter(|slot| slot.is_release_lane())
                .count(),
            operator_lane_execution_slots: execution_slots
                .iter()
                .filter(|slot| slot.is_operator_lane())
                .count(),
            repair_lane_execution_slots: execution_slots
                .iter()
                .filter(|slot| slot.is_repair_lane())
                .count(),
            review_lane_execution_slots: execution_slots
                .iter()
                .filter(|slot| slot.is_review_lane())
                .count(),
            execution_required_slots: execution_slots
                .iter()
                .filter(|slot| slot.execution_required())
                .count(),
            first_execution_slot_key: first_execution_slot
                .map(|slot| slot.execution_slot_key.clone()),
            first_release_execution_slot_key: first_release_execution_slot
                .map(|slot| slot.execution_slot_key.clone()),
            first_operator_execution_slot_key: first_operator_execution_slot
                .map(|slot| slot.execution_slot_key.clone()),
            first_repair_execution_slot_key: first_repair_execution_slot
                .map(|slot| slot.execution_slot_key.clone()),
            first_review_execution_slot_key: first_review_execution_slot
                .map(|slot| slot.execution_slot_key.clone()),
            next_execution_slot_key: next_execution_slot
                .map(|slot| slot.execution_slot_key.clone()),
            next_packet_key: next_execution_slot.map(|slot| slot.packet_key.clone()),
            next_ticket_key: next_execution_slot.map(|slot| slot.ticket_key.clone()),
            next_dispatch_key: next_execution_slot.map(|slot| slot.dispatch_key.clone()),
            next_task_key: next_execution_slot.map(|slot| slot.task_key.clone()),
            next_slot_key: next_execution_slot
                .map(|slot| slot.slot_key.clone())
                .or_else(|| {
                    release_ticket_handoff_readiness_summary
                        .next_slot_key
                        .clone()
                }),
            next_check_kind: next_execution_slot
                .map(|slot| slot.check_kind)
                .or(release_ticket_handoff_readiness_summary.next_check_kind),
            next_check_status: next_execution_slot
                .map(|slot| slot.status)
                .or(release_ticket_handoff_readiness_summary.next_check_status),
            next_package_kind: next_execution_slot
                .and_then(|slot| slot.package_kind)
                .or(release_ticket_handoff_readiness_summary.next_package_kind),
            next_handoff_status: next_execution_slot
                .and_then(|slot| slot.handoff_status)
                .or(release_ticket_handoff_readiness_summary.next_handoff_status),
            next_handoff_lane: next_execution_slot
                .map(|slot| slot.handoff_lane)
                .or(release_ticket_handoff_readiness_summary.next_handoff_lane),
            packet_status: release_ticket_handoff_readiness_summary.packet_status,
            execution_status: release_ticket_handoff_readiness_summary.execution_status,
            release_packet_ready: release_ticket_handoff_readiness_summary.release_packet_ready,
            release_execution_ready: release_ticket_handoff_readiness_summary
                .release_execution_ready,
            release_tasks_ready: release_ticket_handoff_readiness_summary.release_tasks_ready,
            release_dispatch_ready: release_ticket_handoff_readiness_summary.release_dispatch_ready,
            release_ticket_ready: release_ticket_handoff_readiness_summary.release_ticket_ready,
            release_handoff_ready: release_ticket_handoff_readiness_summary.release_handoff_ready,
            release_handoff_execution_ready,
            release_ticket_handoff_readiness_summary,
        }
    }

    pub fn has_execution_slots(&self) -> bool {
        self.total_execution_slots > 0
    }

    pub fn ready_for_release_handoff_execution(&self) -> bool {
        self.execution_status == IntegrationMeshReleaseReadinessStatus::Ready
            && self.release_packet_ready
            && self.release_execution_ready
            && self.release_tasks_ready
            && self.release_dispatch_ready
            && self.release_ticket_ready
            && self.release_handoff_ready
            && self.release_handoff_execution_ready
            && !self.requires_attention()
    }

    pub fn has_repair_work(&self) -> bool {
        self.repair_lane_execution_slots > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.blocked_execution_slots > 0
            || self.has_repair_work()
            || self.release_ticket_handoff_readiness_summary.has_blockers()
    }

    pub fn has_review_work(&self) -> bool {
        self.review_required_execution_slots > 0
            || self.review_lane_execution_slots > 0
            || self
                .release_ticket_handoff_readiness_summary
                .has_review_work()
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_execution_slots > 0
            || self.operator_lane_execution_slots > 0
            || self
                .release_ticket_handoff_readiness_summary
                .needs_operator()
    }

    pub fn requires_attention(&self) -> bool {
        self.execution_required_slots > 0
            || self.dispatch_required_execution_slots > 0
            || self.has_blockers()
            || self.has_review_work()
            || self.needs_operator()
            || !self.release_handoff_execution_ready
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffExecutionReadinessSummary {
    pub release_ticket_handoff_readiness_summary:
        IntegrationMeshReleaseTicketHandoffReadinessSummary,
    pub release_ticket_handoff_execution_slot_summary:
        IntegrationMeshReleaseTicketHandoffExecutionSlotSummary,
    pub total_tickets: usize,
    pub ready_tickets: usize,
    pub blocked_tickets: usize,
    pub review_required_tickets: usize,
    pub operator_required_tickets: usize,
    pub dispatch_required_tickets: usize,
    pub total_handoff_packets: usize,
    pub release_lane_packets: usize,
    pub operator_lane_packets: usize,
    pub repair_lane_packets: usize,
    pub review_lane_packets: usize,
    pub dispatch_required_packets: usize,
    pub total_execution_slots: usize,
    pub ready_execution_slots: usize,
    pub blocked_execution_slots: usize,
    pub review_required_execution_slots: usize,
    pub operator_required_execution_slots: usize,
    pub dispatch_required_execution_slots: usize,
    pub release_lane_execution_slots: usize,
    pub operator_lane_execution_slots: usize,
    pub repair_lane_execution_slots: usize,
    pub review_lane_execution_slots: usize,
    pub execution_required_slots: usize,
    pub queued_substrate_actions: usize,
    pub remediation_item_count: usize,
    pub review_required_packages: usize,
    pub operator_required_packages: usize,
    pub blocked_packages: usize,
    pub first_execution_slot_key: Option<String>,
    pub next_execution_slot_key: Option<String>,
    pub next_packet_key: Option<String>,
    pub next_ticket_key: Option<String>,
    pub next_dispatch_key: Option<String>,
    pub next_task_key: Option<String>,
    pub next_slot_key: Option<String>,
    pub next_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub next_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_package_kind: Option<IntegrationMeshReadinessHandoffKind>,
    pub next_handoff_status: Option<IntegrationMeshReadinessHandoffStatus>,
    pub next_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub packet_status: IntegrationMeshReleaseReadinessStatus,
    pub execution_status: IntegrationMeshReleaseReadinessStatus,
    pub release_packet_ready: bool,
    pub release_execution_ready: bool,
    pub release_tasks_ready: bool,
    pub release_dispatch_ready: bool,
    pub release_ticket_ready: bool,
    pub release_handoff_ready: bool,
    pub release_handoff_execution_ready: bool,
    pub release_ticket_handoff_execution_ready: bool,
}

impl IntegrationMeshReleaseTicketHandoffExecutionReadinessSummary {
    pub fn from_summaries(
        release_ticket_handoff_readiness_summary: IntegrationMeshReleaseTicketHandoffReadinessSummary,
        release_ticket_handoff_execution_slot_summary: IntegrationMeshReleaseTicketHandoffExecutionSlotSummary,
    ) -> Self {
        let release_ticket_handoff_execution_ready = release_ticket_handoff_readiness_summary
            .ready_for_release_handoff()
            && release_ticket_handoff_execution_slot_summary.ready_for_release_handoff_execution();

        Self {
            total_tickets: release_ticket_handoff_readiness_summary.total_tickets,
            ready_tickets: release_ticket_handoff_readiness_summary.ready_tickets,
            blocked_tickets: release_ticket_handoff_readiness_summary.blocked_tickets,
            review_required_tickets: release_ticket_handoff_readiness_summary
                .review_required_tickets,
            operator_required_tickets: release_ticket_handoff_readiness_summary
                .operator_required_tickets,
            dispatch_required_tickets: release_ticket_handoff_readiness_summary
                .dispatch_required_tickets,
            total_handoff_packets: release_ticket_handoff_readiness_summary.total_handoff_packets,
            release_lane_packets: release_ticket_handoff_readiness_summary.release_lane_packets,
            operator_lane_packets: release_ticket_handoff_readiness_summary.operator_lane_packets,
            repair_lane_packets: release_ticket_handoff_readiness_summary.repair_lane_packets,
            review_lane_packets: release_ticket_handoff_readiness_summary.review_lane_packets,
            dispatch_required_packets: release_ticket_handoff_readiness_summary
                .dispatch_required_packets,
            total_execution_slots: release_ticket_handoff_execution_slot_summary
                .total_execution_slots,
            ready_execution_slots: release_ticket_handoff_execution_slot_summary
                .ready_execution_slots,
            blocked_execution_slots: release_ticket_handoff_execution_slot_summary
                .blocked_execution_slots,
            review_required_execution_slots: release_ticket_handoff_execution_slot_summary
                .review_required_execution_slots,
            operator_required_execution_slots: release_ticket_handoff_execution_slot_summary
                .operator_required_execution_slots,
            dispatch_required_execution_slots: release_ticket_handoff_execution_slot_summary
                .dispatch_required_execution_slots,
            release_lane_execution_slots: release_ticket_handoff_execution_slot_summary
                .release_lane_execution_slots,
            operator_lane_execution_slots: release_ticket_handoff_execution_slot_summary
                .operator_lane_execution_slots,
            repair_lane_execution_slots: release_ticket_handoff_execution_slot_summary
                .repair_lane_execution_slots,
            review_lane_execution_slots: release_ticket_handoff_execution_slot_summary
                .review_lane_execution_slots,
            execution_required_slots: release_ticket_handoff_execution_slot_summary
                .execution_required_slots,
            queued_substrate_actions: release_ticket_handoff_readiness_summary
                .queued_substrate_actions,
            remediation_item_count: release_ticket_handoff_readiness_summary.remediation_item_count,
            review_required_packages: release_ticket_handoff_readiness_summary
                .review_required_packages,
            operator_required_packages: release_ticket_handoff_readiness_summary
                .operator_required_packages,
            blocked_packages: release_ticket_handoff_readiness_summary.blocked_packages,
            first_execution_slot_key: release_ticket_handoff_execution_slot_summary
                .first_execution_slot_key
                .clone(),
            next_execution_slot_key: release_ticket_handoff_execution_slot_summary
                .next_execution_slot_key
                .clone(),
            next_packet_key: release_ticket_handoff_execution_slot_summary
                .next_packet_key
                .clone(),
            next_ticket_key: release_ticket_handoff_execution_slot_summary
                .next_ticket_key
                .clone(),
            next_dispatch_key: release_ticket_handoff_execution_slot_summary
                .next_dispatch_key
                .clone(),
            next_task_key: release_ticket_handoff_execution_slot_summary
                .next_task_key
                .clone(),
            next_slot_key: release_ticket_handoff_execution_slot_summary
                .next_slot_key
                .clone(),
            next_check_kind: release_ticket_handoff_execution_slot_summary.next_check_kind,
            next_check_status: release_ticket_handoff_execution_slot_summary.next_check_status,
            next_package_kind: release_ticket_handoff_execution_slot_summary.next_package_kind,
            next_handoff_status: release_ticket_handoff_execution_slot_summary.next_handoff_status,
            next_handoff_lane: release_ticket_handoff_execution_slot_summary.next_handoff_lane,
            packet_status: release_ticket_handoff_readiness_summary.packet_status,
            execution_status: release_ticket_handoff_readiness_summary.execution_status,
            release_packet_ready: release_ticket_handoff_readiness_summary.release_packet_ready,
            release_execution_ready: release_ticket_handoff_readiness_summary
                .release_execution_ready,
            release_tasks_ready: release_ticket_handoff_readiness_summary.release_tasks_ready,
            release_dispatch_ready: release_ticket_handoff_readiness_summary.release_dispatch_ready,
            release_ticket_ready: release_ticket_handoff_readiness_summary.release_ticket_ready,
            release_handoff_ready: release_ticket_handoff_readiness_summary.release_handoff_ready,
            release_handoff_execution_ready: release_ticket_handoff_execution_slot_summary
                .release_handoff_execution_ready,
            release_ticket_handoff_execution_ready,
            release_ticket_handoff_readiness_summary,
            release_ticket_handoff_execution_slot_summary,
        }
    }

    pub fn ready_for_release_ticket_handoff_execution(&self) -> bool {
        self.execution_status == IntegrationMeshReleaseReadinessStatus::Ready
            && self.release_packet_ready
            && self.release_execution_ready
            && self.release_tasks_ready
            && self.release_dispatch_ready
            && self.release_ticket_ready
            && self.release_handoff_ready
            && self.release_handoff_execution_ready
            && self.release_ticket_handoff_execution_ready
            && !self.requires_attention()
    }

    pub fn has_execution_slots(&self) -> bool {
        self.total_execution_slots > 0
    }

    pub fn has_repair_work(&self) -> bool {
        self.repair_lane_execution_slots > 0
            || self.repair_lane_packets > 0
            || self
                .release_ticket_handoff_execution_slot_summary
                .has_repair_work()
    }

    pub fn has_blockers(&self) -> bool {
        self.blocked_execution_slots > 0
            || self.blocked_tickets > 0
            || self.blocked_packages > 0
            || self.queued_substrate_actions > 0
            || self.has_repair_work()
            || self
                .release_ticket_handoff_execution_slot_summary
                .has_blockers()
    }

    pub fn has_review_work(&self) -> bool {
        self.review_required_execution_slots > 0
            || self.review_lane_execution_slots > 0
            || self.review_required_tickets > 0
            || self.review_lane_packets > 0
            || self.review_required_packages > 0
            || self
                .release_ticket_handoff_execution_slot_summary
                .has_review_work()
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_execution_slots > 0
            || self.operator_lane_execution_slots > 0
            || self.operator_required_tickets > 0
            || self.operator_lane_packets > 0
            || self.operator_required_packages > 0
            || self
                .release_ticket_handoff_execution_slot_summary
                .needs_operator()
    }

    pub fn requires_attention(&self) -> bool {
        self.execution_required_slots > 0
            || self.dispatch_required_execution_slots > 0
            || self.dispatch_required_packets > 0
            || self.dispatch_required_tickets > 0
            || self.has_blockers()
            || self.has_review_work()
            || self.needs_operator()
            || !self.release_ticket_handoff_execution_ready
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffExecutionWorkOrder {
    pub sequence: usize,
    pub work_order_key: String,
    pub execution_slot_key: String,
    pub packet_key: String,
    pub ticket_key: String,
    pub dispatch_key: String,
    pub task_key: String,
    pub slot_key: String,
    pub check_sequence: usize,
    pub check_kind: IntegrationMeshReleaseReadinessCheckKind,
    pub status: IntegrationMeshReleaseReadinessStatus,
    pub package_kind: Option<IntegrationMeshReadinessHandoffKind>,
    pub handoff_status: Option<IntegrationMeshReadinessHandoffStatus>,
    pub handoff_lane: IntegrationMeshReleaseDispatchTicketHandoffLane,
    pub ready: bool,
    pub blocked: bool,
    pub review_required: bool,
    pub operator_required: bool,
    pub dispatch_required: bool,
    pub execution_required: bool,
    pub packet_status: IntegrationMeshReleaseReadinessStatus,
    pub execution_status: IntegrationMeshReleaseReadinessStatus,
    pub release_dispatch_ready: bool,
}

impl IntegrationMeshReleaseTicketHandoffExecutionWorkOrder {
    pub fn from_execution_slot(
        sequence: usize,
        execution_slot: &IntegrationMeshReleaseTicketHandoffExecutionSlot,
    ) -> Self {
        Self {
            sequence,
            work_order_key: format!(
                "release-ticket-handoff-execution-work-order-{sequence:02}-{}-{}",
                execution_slot.handoff_lane.as_str(),
                execution_slot.check_kind.as_str()
            ),
            execution_slot_key: execution_slot.execution_slot_key.clone(),
            packet_key: execution_slot.packet_key.clone(),
            ticket_key: execution_slot.ticket_key.clone(),
            dispatch_key: execution_slot.dispatch_key.clone(),
            task_key: execution_slot.task_key.clone(),
            slot_key: execution_slot.slot_key.clone(),
            check_sequence: execution_slot.check_sequence,
            check_kind: execution_slot.check_kind,
            status: execution_slot.status,
            package_kind: execution_slot.package_kind,
            handoff_status: execution_slot.handoff_status,
            handoff_lane: execution_slot.handoff_lane,
            ready: execution_slot.ready(),
            blocked: execution_slot.blocked(),
            review_required: execution_slot.review_required(),
            operator_required: execution_slot.operator_required(),
            dispatch_required: execution_slot.dispatch_required(),
            execution_required: execution_slot.execution_required(),
            packet_status: execution_slot.packet_status,
            execution_status: execution_slot.execution_status,
            release_dispatch_ready: execution_slot.release_dispatch_ready,
        }
    }

    pub fn ready(&self) -> bool {
        self.ready
    }

    pub fn blocked(&self) -> bool {
        self.blocked
    }

    pub fn review_required(&self) -> bool {
        self.review_required
    }

    pub fn operator_required(&self) -> bool {
        self.operator_required
    }

    pub fn dispatch_required(&self) -> bool {
        self.dispatch_required
    }

    pub fn execution_required(&self) -> bool {
        self.execution_required
    }

    pub fn is_release_lane(&self) -> bool {
        self.handoff_lane == IntegrationMeshReleaseDispatchTicketHandoffLane::Release
    }

    pub fn is_operator_lane(&self) -> bool {
        self.handoff_lane == IntegrationMeshReleaseDispatchTicketHandoffLane::Operator
    }

    pub fn is_repair_lane(&self) -> bool {
        self.handoff_lane == IntegrationMeshReleaseDispatchTicketHandoffLane::Repair
    }

    pub fn is_review_lane(&self) -> bool {
        self.handoff_lane == IntegrationMeshReleaseDispatchTicketHandoffLane::Review
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailKind {
    ReleaseBlocker,
    OperatorHandoff,
    ReviewGate,
    ReadyToExecute,
}

impl IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailKind {
    pub fn as_str(self) -> &'static str {
        match self {
            Self::ReleaseBlocker => "release_blocker",
            Self::OperatorHandoff => "operator_handoff",
            Self::ReviewGate => "review_gate",
            Self::ReadyToExecute => "ready_to_execute",
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrail {
    pub sequence: usize,
    pub guardrail_key: String,
    pub work_order_sequence: usize,
    pub work_order_key: String,
    pub execution_slot_key: String,
    pub packet_key: String,
    pub ticket_key: String,
    pub dispatch_key: String,
    pub task_key: String,
    pub slot_key: String,
    pub check_sequence: usize,
    pub check_kind: IntegrationMeshReleaseReadinessCheckKind,
    pub status: IntegrationMeshReleaseReadinessStatus,
    pub package_kind: Option<IntegrationMeshReadinessHandoffKind>,
    pub handoff_status: Option<IntegrationMeshReadinessHandoffStatus>,
    pub handoff_lane: IntegrationMeshReleaseDispatchTicketHandoffLane,
    pub guardrail_kind: IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailKind,
    pub release_blocking: bool,
    pub operator_required: bool,
    pub review_required: bool,
    pub dispatch_required: bool,
    pub execution_required: bool,
}

impl IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrail {
    pub fn from_work_order(
        sequence: usize,
        work_order: &IntegrationMeshReleaseTicketHandoffExecutionWorkOrder,
    ) -> Self {
        let guardrail_kind = if work_order.ready() && work_order.is_release_lane() {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailKind::ReadyToExecute
        } else {
            match work_order.handoff_lane {
                IntegrationMeshReleaseDispatchTicketHandoffLane::Operator => {
                    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailKind::OperatorHandoff
                }
                IntegrationMeshReleaseDispatchTicketHandoffLane::Review => {
                    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailKind::ReviewGate
                }
                IntegrationMeshReleaseDispatchTicketHandoffLane::Repair
                | IntegrationMeshReleaseDispatchTicketHandoffLane::Release => {
                    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailKind::ReleaseBlocker
                }
            }
        };

        Self {
            sequence,
            guardrail_key: format!(
                "release-ticket-handoff-execution-work-order-guardrail-{sequence:02}-{}-{}",
                guardrail_kind.as_str(),
                work_order.check_kind.as_str()
            ),
            work_order_sequence: work_order.sequence,
            work_order_key: work_order.work_order_key.clone(),
            execution_slot_key: work_order.execution_slot_key.clone(),
            packet_key: work_order.packet_key.clone(),
            ticket_key: work_order.ticket_key.clone(),
            dispatch_key: work_order.dispatch_key.clone(),
            task_key: work_order.task_key.clone(),
            slot_key: work_order.slot_key.clone(),
            check_sequence: work_order.check_sequence,
            check_kind: work_order.check_kind,
            status: work_order.status,
            package_kind: work_order.package_kind,
            handoff_status: work_order.handoff_status,
            handoff_lane: work_order.handoff_lane,
            guardrail_kind,
            release_blocking: work_order.blocked() || work_order.is_repair_lane(),
            operator_required: work_order.operator_required(),
            review_required: work_order.review_required(),
            dispatch_required: work_order.dispatch_required(),
            execution_required: work_order.execution_required(),
        }
    }

    pub fn blocks_release(&self) -> bool {
        self.release_blocking
    }

    pub fn requires_operator(&self) -> bool {
        self.operator_required
            || self.guardrail_kind
                == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailKind::OperatorHandoff
    }

    pub fn requires_review(&self) -> bool {
        self.review_required
            || self.guardrail_kind
                == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailKind::ReviewGate
    }

    pub fn ready_to_execute(&self) -> bool {
        self.guardrail_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailKind::ReadyToExecute
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailSummary {
    pub total_guardrails: usize,
    pub release_blocker_guardrails: usize,
    pub operator_handoff_guardrails: usize,
    pub review_gate_guardrails: usize,
    pub ready_to_execute_guardrails: usize,
    pub release_blocking_guardrails: usize,
    pub operator_required_guardrails: usize,
    pub review_required_guardrails: usize,
    pub dispatch_required_guardrails: usize,
    pub execution_required_guardrails: usize,
    pub first_guardrail_key: Option<String>,
    pub first_work_order_key: Option<String>,
    pub first_release_blocker_work_order_key: Option<String>,
    pub first_operator_handoff_work_order_key: Option<String>,
    pub first_review_gate_work_order_key: Option<String>,
    pub first_ready_to_execute_work_order_key: Option<String>,
    pub first_guardrail_kind:
        Option<IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailKind>,
    pub first_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub first_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub first_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_execution_slot_key: Option<String>,
    pub next_packet_key: Option<String>,
    pub next_ticket_key: Option<String>,
    pub release_handoff_execution_work_order_guardrails_ready: bool,
}

impl IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailSummary {
    pub fn from_guardrails<'a>(
        guardrails: impl IntoIterator<
            Item = &'a IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrail,
        >,
    ) -> Self {
        let guardrails = guardrails.into_iter().collect::<Vec<_>>();
        let first_guardrail = guardrails.iter().min_by_key(|guardrail| guardrail.sequence);
        let first_release_blocker = guardrails
            .iter()
            .filter(|guardrail| {
                guardrail.guardrail_kind
                    == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailKind::ReleaseBlocker
            })
            .min_by_key(|guardrail| guardrail.sequence);
        let first_operator_handoff = guardrails
            .iter()
            .filter(|guardrail| {
                guardrail.guardrail_kind
                    == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailKind::OperatorHandoff
            })
            .min_by_key(|guardrail| guardrail.sequence);
        let first_review_gate = guardrails
            .iter()
            .filter(|guardrail| {
                guardrail.guardrail_kind
                    == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailKind::ReviewGate
            })
            .min_by_key(|guardrail| guardrail.sequence);
        let first_ready_to_execute = guardrails
            .iter()
            .filter(|guardrail| guardrail.ready_to_execute())
            .min_by_key(|guardrail| guardrail.sequence);
        let next_guardrail = guardrails
            .iter()
            .filter(|guardrail| !guardrail.ready_to_execute())
            .min_by_key(|guardrail| guardrail.sequence);

        Self {
            total_guardrails: guardrails.len(),
            release_blocker_guardrails: guardrails
                .iter()
                .filter(|guardrail| {
                    guardrail.guardrail_kind
                        == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailKind::ReleaseBlocker
                })
                .count(),
            operator_handoff_guardrails: guardrails
                .iter()
                .filter(|guardrail| {
                    guardrail.guardrail_kind
                        == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailKind::OperatorHandoff
                })
                .count(),
            review_gate_guardrails: guardrails
                .iter()
                .filter(|guardrail| {
                    guardrail.guardrail_kind
                        == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailKind::ReviewGate
                })
                .count(),
            ready_to_execute_guardrails: guardrails
                .iter()
                .filter(|guardrail| guardrail.ready_to_execute())
                .count(),
            release_blocking_guardrails: guardrails
                .iter()
                .filter(|guardrail| guardrail.blocks_release())
                .count(),
            operator_required_guardrails: guardrails
                .iter()
                .filter(|guardrail| guardrail.requires_operator())
                .count(),
            review_required_guardrails: guardrails
                .iter()
                .filter(|guardrail| guardrail.requires_review())
                .count(),
            dispatch_required_guardrails: guardrails
                .iter()
                .filter(|guardrail| guardrail.dispatch_required)
                .count(),
            execution_required_guardrails: guardrails
                .iter()
                .filter(|guardrail| guardrail.execution_required)
                .count(),
            first_guardrail_key: first_guardrail
                .map(|guardrail| guardrail.guardrail_key.clone()),
            first_work_order_key: first_guardrail
                .map(|guardrail| guardrail.work_order_key.clone()),
            first_release_blocker_work_order_key: first_release_blocker
                .map(|guardrail| guardrail.work_order_key.clone()),
            first_operator_handoff_work_order_key: first_operator_handoff
                .map(|guardrail| guardrail.work_order_key.clone()),
            first_review_gate_work_order_key: first_review_gate
                .map(|guardrail| guardrail.work_order_key.clone()),
            first_ready_to_execute_work_order_key: first_ready_to_execute
                .map(|guardrail| guardrail.work_order_key.clone()),
            first_guardrail_kind: first_guardrail.map(|guardrail| guardrail.guardrail_kind),
            first_handoff_lane: first_guardrail.map(|guardrail| guardrail.handoff_lane),
            first_check_kind: first_guardrail.map(|guardrail| guardrail.check_kind),
            first_check_status: first_guardrail.map(|guardrail| guardrail.status),
            next_execution_slot_key: next_guardrail
                .map(|guardrail| guardrail.execution_slot_key.clone()),
            next_packet_key: next_guardrail.map(|guardrail| guardrail.packet_key.clone()),
            next_ticket_key: next_guardrail.map(|guardrail| guardrail.ticket_key.clone()),
            release_handoff_execution_work_order_guardrails_ready: !guardrails.is_empty()
                && guardrails.iter().all(|guardrail| guardrail.ready_to_execute()),
        }
    }

    pub fn has_guardrails(&self) -> bool {
        self.total_guardrails > 0
    }

    pub fn has_release_blockers(&self) -> bool {
        self.release_blocker_guardrails > 0 || self.release_blocking_guardrails > 0
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_handoff_guardrails > 0 || self.operator_required_guardrails > 0
    }

    pub fn has_review_gates(&self) -> bool {
        self.review_gate_guardrails > 0 || self.review_required_guardrails > 0
    }

    pub fn has_ready_to_execute_work(&self) -> bool {
        self.ready_to_execute_guardrails > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.has_release_blockers()
            || self.needs_operator()
            || self.has_review_gates()
            || self.dispatch_required_guardrails > 0
            || self.execution_required_guardrails > 0
            || !self.release_handoff_execution_work_order_guardrails_ready
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditSeverity {
    ReleaseBlocker,
    OperatorHandoff,
    ReviewGate,
    ReadyToExecute,
}

impl IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditSeverity {
    pub fn as_str(self) -> &'static str {
        match self {
            Self::ReleaseBlocker => "release_blocker",
            Self::OperatorHandoff => "operator_handoff",
            Self::ReviewGate => "review_gate",
            Self::ReadyToExecute => "ready_to_execute",
        }
    }
}

impl From<IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailKind>
    for IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditSeverity
{
    fn from(kind: IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailKind) -> Self {
        match kind {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailKind::ReleaseBlocker => {
                Self::ReleaseBlocker
            }
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailKind::OperatorHandoff => {
                Self::OperatorHandoff
            }
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailKind::ReviewGate => {
                Self::ReviewGate
            }
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailKind::ReadyToExecute => {
                Self::ReadyToExecute
            }
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditRow {
    pub sequence: usize,
    pub audit_key: String,
    pub guardrail_key: String,
    pub work_order_key: String,
    pub execution_slot_key: String,
    pub packet_key: String,
    pub ticket_key: String,
    pub check_sequence: usize,
    pub check_kind: IntegrationMeshReleaseReadinessCheckKind,
    pub status: IntegrationMeshReleaseReadinessStatus,
    pub handoff_lane: IntegrationMeshReleaseDispatchTicketHandoffLane,
    pub guardrail_kind: IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailKind,
    pub severity: IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditSeverity,
    pub release_blocking: bool,
    pub operator_required: bool,
    pub review_required: bool,
    pub dispatch_required: bool,
    pub execution_required: bool,
}

impl IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditRow {
    pub fn from_guardrail(
        sequence: usize,
        guardrail: &IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrail,
    ) -> Self {
        let severity: IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditSeverity =
            guardrail.guardrail_kind.into();

        Self {
            sequence,
            audit_key: format!(
                "release-ticket-handoff-execution-work-order-guardrail-audit-{sequence:02}-{}-{}",
                severity.as_str(),
                guardrail.check_kind.as_str()
            ),
            guardrail_key: guardrail.guardrail_key.clone(),
            work_order_key: guardrail.work_order_key.clone(),
            execution_slot_key: guardrail.execution_slot_key.clone(),
            packet_key: guardrail.packet_key.clone(),
            ticket_key: guardrail.ticket_key.clone(),
            check_sequence: guardrail.check_sequence,
            check_kind: guardrail.check_kind,
            status: guardrail.status,
            handoff_lane: guardrail.handoff_lane,
            guardrail_kind: guardrail.guardrail_kind,
            severity,
            release_blocking: guardrail.blocks_release(),
            operator_required: guardrail.requires_operator(),
            review_required: guardrail.requires_review(),
            dispatch_required: guardrail.dispatch_required,
            execution_required: guardrail.execution_required,
        }
    }

    pub fn blocks_release(&self) -> bool {
        self.release_blocking || self.severity == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditSeverity::ReleaseBlocker
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required || self.severity == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditSeverity::OperatorHandoff
    }

    pub fn needs_review(&self) -> bool {
        self.review_required || self.severity == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditSeverity::ReviewGate
    }

    pub fn ready_to_execute(&self) -> bool {
        self.severity
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditSeverity::ReadyToExecute
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditSummary {
    pub total_audit_rows: usize,
    pub release_blocker_rows: usize,
    pub operator_handoff_rows: usize,
    pub review_gate_rows: usize,
    pub ready_to_execute_rows: usize,
    pub release_blocking_rows: usize,
    pub operator_required_rows: usize,
    pub review_required_rows: usize,
    pub dispatch_required_rows: usize,
    pub execution_required_rows: usize,
    pub first_audit_key: Option<String>,
    pub first_guardrail_key: Option<String>,
    pub first_work_order_key: Option<String>,
    pub first_release_blocker_work_order_key: Option<String>,
    pub first_operator_handoff_work_order_key: Option<String>,
    pub first_review_gate_work_order_key: Option<String>,
    pub first_ready_to_execute_work_order_key: Option<String>,
    pub first_severity:
        Option<IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditSeverity>,
    pub first_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub first_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub first_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_execution_slot_key: Option<String>,
    pub next_packet_key: Option<String>,
    pub next_ticket_key: Option<String>,
    pub release_handoff_execution_work_order_guardrail_audit_ready: bool,
}

impl IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditSummary {
    pub fn from_audit_rows<'a>(
        audit_rows: impl IntoIterator<
            Item = &'a IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditRow,
        >,
    ) -> Self {
        let audit_rows = audit_rows.into_iter().collect::<Vec<_>>();
        let first_audit_row = audit_rows.iter().min_by_key(|row| row.sequence);
        let first_release_blocker = audit_rows
            .iter()
            .filter(|row| {
                row.severity
                    == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditSeverity::ReleaseBlocker
            })
            .min_by_key(|row| row.sequence);
        let first_operator_handoff = audit_rows
            .iter()
            .filter(|row| {
                row.severity
                    == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditSeverity::OperatorHandoff
            })
            .min_by_key(|row| row.sequence);
        let first_review_gate = audit_rows
            .iter()
            .filter(|row| {
                row.severity
                    == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditSeverity::ReviewGate
            })
            .min_by_key(|row| row.sequence);
        let first_ready_to_execute = audit_rows
            .iter()
            .filter(|row| row.ready_to_execute())
            .min_by_key(|row| row.sequence);
        let next_audit_row = audit_rows
            .iter()
            .filter(|row| !row.ready_to_execute())
            .min_by_key(|row| row.sequence);

        Self {
            total_audit_rows: audit_rows.len(),
            release_blocker_rows: audit_rows
                .iter()
                .filter(|row| {
                    row.severity
                        == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditSeverity::ReleaseBlocker
                })
                .count(),
            operator_handoff_rows: audit_rows
                .iter()
                .filter(|row| {
                    row.severity
                        == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditSeverity::OperatorHandoff
                })
                .count(),
            review_gate_rows: audit_rows
                .iter()
                .filter(|row| {
                    row.severity
                        == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditSeverity::ReviewGate
                })
                .count(),
            ready_to_execute_rows: audit_rows
                .iter()
                .filter(|row| row.ready_to_execute())
                .count(),
            release_blocking_rows: audit_rows
                .iter()
                .filter(|row| row.blocks_release())
                .count(),
            operator_required_rows: audit_rows
                .iter()
                .filter(|row| row.needs_operator())
                .count(),
            review_required_rows: audit_rows.iter().filter(|row| row.needs_review()).count(),
            dispatch_required_rows: audit_rows
                .iter()
                .filter(|row| row.dispatch_required)
                .count(),
            execution_required_rows: audit_rows
                .iter()
                .filter(|row| row.execution_required)
                .count(),
            first_audit_key: first_audit_row.map(|row| row.audit_key.clone()),
            first_guardrail_key: first_audit_row.map(|row| row.guardrail_key.clone()),
            first_work_order_key: first_audit_row.map(|row| row.work_order_key.clone()),
            first_release_blocker_work_order_key: first_release_blocker
                .map(|row| row.work_order_key.clone()),
            first_operator_handoff_work_order_key: first_operator_handoff
                .map(|row| row.work_order_key.clone()),
            first_review_gate_work_order_key: first_review_gate
                .map(|row| row.work_order_key.clone()),
            first_ready_to_execute_work_order_key: first_ready_to_execute
                .map(|row| row.work_order_key.clone()),
            first_severity: first_audit_row.map(|row| row.severity),
            first_handoff_lane: first_audit_row.map(|row| row.handoff_lane),
            first_check_kind: first_audit_row.map(|row| row.check_kind),
            first_check_status: first_audit_row.map(|row| row.status),
            next_execution_slot_key: next_audit_row.map(|row| row.execution_slot_key.clone()),
            next_packet_key: next_audit_row.map(|row| row.packet_key.clone()),
            next_ticket_key: next_audit_row.map(|row| row.ticket_key.clone()),
            release_handoff_execution_work_order_guardrail_audit_ready: !audit_rows.is_empty()
                && audit_rows.iter().all(|row| row.ready_to_execute()),
        }
    }

    pub fn has_audit_rows(&self) -> bool {
        self.total_audit_rows > 0
    }

    pub fn has_release_blockers(&self) -> bool {
        self.release_blocker_rows > 0 || self.release_blocking_rows > 0
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_handoff_rows > 0 || self.operator_required_rows > 0
    }

    pub fn has_review_gates(&self) -> bool {
        self.review_gate_rows > 0 || self.review_required_rows > 0
    }

    pub fn has_ready_to_execute_work(&self) -> bool {
        self.ready_to_execute_rows > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.has_release_blockers()
            || self.needs_operator()
            || self.has_review_gates()
            || self.dispatch_required_rows > 0
            || self.execution_required_rows > 0
            || !self.release_handoff_execution_work_order_guardrail_audit_ready
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind {
    ReleaseBlocked,
    OperatorHandoff,
    ReviewRequired,
    Cleared,
}

impl IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind {
    pub fn as_str(self) -> &'static str {
        match self {
            Self::ReleaseBlocked => "release_blocked",
            Self::OperatorHandoff => "operator_handoff",
            Self::ReviewRequired => "review_required",
            Self::Cleared => "cleared",
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceRow {
    pub sequence: usize,
    pub clearance_key: String,
    pub audit_key: String,
    pub guardrail_key: String,
    pub work_order_key: String,
    pub execution_slot_key: String,
    pub packet_key: String,
    pub ticket_key: String,
    pub check_sequence: usize,
    pub check_kind: IntegrationMeshReleaseReadinessCheckKind,
    pub status: IntegrationMeshReleaseReadinessStatus,
    pub handoff_lane: IntegrationMeshReleaseDispatchTicketHandoffLane,
    pub audit_severity: IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditSeverity,
    pub clearance_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind,
    pub release_blocking: bool,
    pub operator_required: bool,
    pub review_required: bool,
    pub dispatch_required: bool,
    pub execution_required: bool,
    pub cleared_for_release: bool,
}

impl IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceRow {
    pub fn from_audit_row(
        sequence: usize,
        audit_row: &IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditRow,
    ) -> Self {
        let clearance_kind = if audit_row.blocks_release() {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind::ReleaseBlocked
        } else if audit_row.needs_review() {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind::ReviewRequired
        } else if audit_row.needs_operator() {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind::OperatorHandoff
        } else {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind::Cleared
        };
        let cleared_for_release = clearance_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind::Cleared
            && audit_row.ready_to_execute()
            && !audit_row.dispatch_required
            && !audit_row.execution_required;

        Self {
            sequence,
            clearance_key: format!(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-{sequence:02}-{}-{}",
                clearance_kind.as_str(),
                audit_row.check_kind.as_str()
            ),
            audit_key: audit_row.audit_key.clone(),
            guardrail_key: audit_row.guardrail_key.clone(),
            work_order_key: audit_row.work_order_key.clone(),
            execution_slot_key: audit_row.execution_slot_key.clone(),
            packet_key: audit_row.packet_key.clone(),
            ticket_key: audit_row.ticket_key.clone(),
            check_sequence: audit_row.check_sequence,
            check_kind: audit_row.check_kind,
            status: audit_row.status,
            handoff_lane: audit_row.handoff_lane,
            audit_severity: audit_row.severity,
            clearance_kind,
            release_blocking: audit_row.blocks_release(),
            operator_required: audit_row.needs_operator(),
            review_required: audit_row.needs_review(),
            dispatch_required: audit_row.dispatch_required,
            execution_required: audit_row.execution_required,
            cleared_for_release,
        }
    }

    pub fn blocks_release(&self) -> bool {
        self.release_blocking
            || self.clearance_kind
                == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind::ReleaseBlocked
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required
            || self.clearance_kind
                == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind::OperatorHandoff
    }

    pub fn needs_review(&self) -> bool {
        self.review_required
            || self.clearance_kind
                == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind::ReviewRequired
    }

    pub fn ready_to_clear(&self) -> bool {
        self.cleared_for_release
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceSummary {
    pub total_clearance_rows: usize,
    pub release_blocked_clearances: usize,
    pub operator_handoff_clearances: usize,
    pub review_gate_clearances: usize,
    pub cleared_clearances: usize,
    pub release_blocking_clearances: usize,
    pub operator_required_clearances: usize,
    pub review_required_clearances: usize,
    pub dispatch_required_clearances: usize,
    pub execution_required_clearances: usize,
    pub first_clearance_key: Option<String>,
    pub first_audit_key: Option<String>,
    pub first_guardrail_key: Option<String>,
    pub first_work_order_key: Option<String>,
    pub first_release_blocked_work_order_key: Option<String>,
    pub first_operator_handoff_work_order_key: Option<String>,
    pub first_review_required_work_order_key: Option<String>,
    pub first_cleared_work_order_key: Option<String>,
    pub first_clearance_kind:
        Option<IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind>,
    pub first_audit_severity:
        Option<IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditSeverity>,
    pub first_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub first_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub first_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_work_order_key: Option<String>,
    pub next_execution_slot_key: Option<String>,
    pub next_packet_key: Option<String>,
    pub next_ticket_key: Option<String>,
    pub release_handoff_execution_work_order_guardrail_audit_clearance_ready: bool,
}

impl IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceSummary {
    pub fn from_clearance_rows<'a>(
        clearance_rows: impl IntoIterator<
            Item = &'a IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceRow,
        >,
    ) -> Self {
        let clearance_rows = clearance_rows.into_iter().collect::<Vec<_>>();
        let first_clearance_row = clearance_rows.iter().min_by_key(|row| row.sequence);
        let first_release_blocked = clearance_rows
            .iter()
            .filter(|row| {
                row.clearance_kind
                    == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind::ReleaseBlocked
            })
            .min_by_key(|row| row.sequence);
        let first_operator_handoff = clearance_rows
            .iter()
            .filter(|row| {
                row.clearance_kind
                    == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind::OperatorHandoff
            })
            .min_by_key(|row| row.sequence);
        let first_review_required = clearance_rows
            .iter()
            .filter(|row| {
                row.clearance_kind
                    == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind::ReviewRequired
            })
            .min_by_key(|row| row.sequence);
        let first_cleared = clearance_rows
            .iter()
            .filter(|row| row.ready_to_clear())
            .min_by_key(|row| row.sequence);
        let next_clearance_row = clearance_rows
            .iter()
            .filter(|row| !row.ready_to_clear())
            .min_by_key(|row| row.sequence);

        Self {
            total_clearance_rows: clearance_rows.len(),
            release_blocked_clearances: clearance_rows
                .iter()
                .filter(|row| {
                    row.clearance_kind
                        == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind::ReleaseBlocked
                })
                .count(),
            operator_handoff_clearances: clearance_rows
                .iter()
                .filter(|row| {
                    row.clearance_kind
                        == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind::OperatorHandoff
                })
                .count(),
            review_gate_clearances: clearance_rows
                .iter()
                .filter(|row| {
                    row.clearance_kind
                        == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind::ReviewRequired
                })
                .count(),
            cleared_clearances: clearance_rows
                .iter()
                .filter(|row| row.ready_to_clear())
                .count(),
            release_blocking_clearances: clearance_rows
                .iter()
                .filter(|row| row.blocks_release())
                .count(),
            operator_required_clearances: clearance_rows
                .iter()
                .filter(|row| row.needs_operator())
                .count(),
            review_required_clearances: clearance_rows
                .iter()
                .filter(|row| row.needs_review())
                .count(),
            dispatch_required_clearances: clearance_rows
                .iter()
                .filter(|row| row.dispatch_required)
                .count(),
            execution_required_clearances: clearance_rows
                .iter()
                .filter(|row| row.execution_required)
                .count(),
            first_clearance_key: first_clearance_row.map(|row| row.clearance_key.clone()),
            first_audit_key: first_clearance_row.map(|row| row.audit_key.clone()),
            first_guardrail_key: first_clearance_row.map(|row| row.guardrail_key.clone()),
            first_work_order_key: first_clearance_row.map(|row| row.work_order_key.clone()),
            first_release_blocked_work_order_key: first_release_blocked
                .map(|row| row.work_order_key.clone()),
            first_operator_handoff_work_order_key: first_operator_handoff
                .map(|row| row.work_order_key.clone()),
            first_review_required_work_order_key: first_review_required
                .map(|row| row.work_order_key.clone()),
            first_cleared_work_order_key: first_cleared.map(|row| row.work_order_key.clone()),
            first_clearance_kind: first_clearance_row.map(|row| row.clearance_kind),
            first_audit_severity: first_clearance_row.map(|row| row.audit_severity),
            first_handoff_lane: first_clearance_row.map(|row| row.handoff_lane),
            first_check_kind: first_clearance_row.map(|row| row.check_kind),
            first_check_status: first_clearance_row.map(|row| row.status),
            next_work_order_key: next_clearance_row.map(|row| row.work_order_key.clone()),
            next_execution_slot_key: next_clearance_row.map(|row| row.execution_slot_key.clone()),
            next_packet_key: next_clearance_row.map(|row| row.packet_key.clone()),
            next_ticket_key: next_clearance_row.map(|row| row.ticket_key.clone()),
            release_handoff_execution_work_order_guardrail_audit_clearance_ready:
                !clearance_rows.is_empty() && clearance_rows.iter().all(|row| row.ready_to_clear()),
        }
    }

    pub fn has_clearance_rows(&self) -> bool {
        self.total_clearance_rows > 0
    }

    pub fn has_release_blockers(&self) -> bool {
        self.release_blocked_clearances > 0 || self.release_blocking_clearances > 0
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_handoff_clearances > 0 || self.operator_required_clearances > 0
    }

    pub fn needs_review(&self) -> bool {
        self.review_gate_clearances > 0 || self.review_required_clearances > 0
    }

    pub fn has_ready_clearances(&self) -> bool {
        self.cleared_clearances > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.has_release_blockers()
            || self.needs_operator()
            || self.needs_review()
            || self.dispatch_required_clearances > 0
            || self.execution_required_clearances > 0
            || !self.release_handoff_execution_work_order_guardrail_audit_clearance_ready
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionKind {
    RepairReleaseBlocker,
    CoordinateOperatorHandoff,
    RouteReviewGate,
    ReleaseClearance,
}

impl IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionKind {
    pub fn as_str(self) -> &'static str {
        match self {
            Self::RepairReleaseBlocker => "repair_release_blocker",
            Self::CoordinateOperatorHandoff => "coordinate_operator_handoff",
            Self::RouteReviewGate => "route_review_gate",
            Self::ReleaseClearance => "release_clearance",
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceAction {
    pub sequence: usize,
    pub action_key: String,
    pub clearance_sequence: usize,
    pub clearance_key: String,
    pub audit_key: String,
    pub guardrail_key: String,
    pub work_order_key: String,
    pub execution_slot_key: String,
    pub packet_key: String,
    pub ticket_key: String,
    pub check_sequence: usize,
    pub check_kind: IntegrationMeshReleaseReadinessCheckKind,
    pub status: IntegrationMeshReleaseReadinessStatus,
    pub handoff_lane: IntegrationMeshReleaseDispatchTicketHandoffLane,
    pub clearance_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind,
    pub action_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionKind,
    pub release_blocking: bool,
    pub operator_required: bool,
    pub review_required: bool,
    pub dispatch_required: bool,
    pub execution_required: bool,
    pub cleared_for_release: bool,
    pub action_required: bool,
}

impl IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceAction {
    pub fn from_clearance_row(
        sequence: usize,
        clearance_row: &IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceRow,
    ) -> Self {
        let action_kind = match clearance_row.clearance_kind {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind::ReleaseBlocked => {
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionKind::RepairReleaseBlocker
            }
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind::OperatorHandoff => {
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionKind::CoordinateOperatorHandoff
            }
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind::ReviewRequired => {
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionKind::RouteReviewGate
            }
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind::Cleared => {
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionKind::ReleaseClearance
            }
        };

        Self {
            sequence,
            action_key: format!(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-{sequence:02}-{}-{}",
                action_kind.as_str(),
                clearance_row.check_kind.as_str()
            ),
            clearance_sequence: clearance_row.sequence,
            clearance_key: clearance_row.clearance_key.clone(),
            audit_key: clearance_row.audit_key.clone(),
            guardrail_key: clearance_row.guardrail_key.clone(),
            work_order_key: clearance_row.work_order_key.clone(),
            execution_slot_key: clearance_row.execution_slot_key.clone(),
            packet_key: clearance_row.packet_key.clone(),
            ticket_key: clearance_row.ticket_key.clone(),
            check_sequence: clearance_row.check_sequence,
            check_kind: clearance_row.check_kind,
            status: clearance_row.status,
            handoff_lane: clearance_row.handoff_lane,
            clearance_kind: clearance_row.clearance_kind,
            action_kind,
            release_blocking: clearance_row.blocks_release(),
            operator_required: clearance_row.needs_operator(),
            review_required: clearance_row.needs_review(),
            dispatch_required: clearance_row.dispatch_required,
            execution_required: clearance_row.execution_required,
            cleared_for_release: clearance_row.ready_to_clear(),
            action_required: !clearance_row.ready_to_clear(),
        }
    }

    pub fn requires_action(&self) -> bool {
        self.action_required
    }

    pub fn blocks_release(&self) -> bool {
        self.release_blocking
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required
    }

    pub fn needs_review(&self) -> bool {
        self.review_required
    }

    pub fn release_clearance_ready(&self) -> bool {
        self.cleared_for_release && !self.requires_action()
    }

    pub fn is_repair_action(&self) -> bool {
        self.action_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionKind::RepairReleaseBlocker
    }

    pub fn is_operator_handoff_action(&self) -> bool {
        self.action_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionKind::CoordinateOperatorHandoff
    }

    pub fn is_review_gate_action(&self) -> bool {
        self.action_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionKind::RouteReviewGate
    }

    pub fn is_release_clearance_action(&self) -> bool {
        self.action_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionKind::ReleaseClearance
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionSummary
{
    pub total_actions: usize,
    pub required_actions: usize,
    pub repair_release_blocker_actions: usize,
    pub operator_handoff_actions: usize,
    pub review_gate_actions: usize,
    pub release_clearance_actions: usize,
    pub release_blocking_actions: usize,
    pub operator_required_actions: usize,
    pub review_required_actions: usize,
    pub dispatch_required_actions: usize,
    pub execution_required_actions: usize,
    pub first_action_key: Option<String>,
    pub first_required_action_key: Option<String>,
    pub first_repair_action_key: Option<String>,
    pub first_operator_handoff_action_key: Option<String>,
    pub first_review_gate_action_key: Option<String>,
    pub first_release_clearance_action_key: Option<String>,
    pub first_clearance_key: Option<String>,
    pub first_work_order_key: Option<String>,
    pub first_ticket_key: Option<String>,
    pub first_action_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionKind,
    >,
    pub first_clearance_kind:
        Option<IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind>,
    pub first_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub first_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub first_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub release_handoff_execution_work_order_guardrail_audit_clearance_actions_ready: bool,
}

impl IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionSummary {
    pub fn from_actions<'a>(
        actions: impl IntoIterator<
            Item = &'a IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceAction,
        >,
    ) -> Self {
        let actions = actions.into_iter().collect::<Vec<_>>();
        let first_action = actions.iter().min_by_key(|action| action.sequence);
        let first_required_action = actions
            .iter()
            .filter(|action| action.requires_action())
            .min_by_key(|action| action.sequence);
        let first_repair_action = actions
            .iter()
            .filter(|action| action.is_repair_action())
            .min_by_key(|action| action.sequence);
        let first_operator_handoff_action = actions
            .iter()
            .filter(|action| action.is_operator_handoff_action())
            .min_by_key(|action| action.sequence);
        let first_review_gate_action = actions
            .iter()
            .filter(|action| action.is_review_gate_action())
            .min_by_key(|action| action.sequence);
        let first_release_clearance_action = actions
            .iter()
            .filter(|action| action.is_release_clearance_action())
            .min_by_key(|action| action.sequence);

        Self {
            total_actions: actions.len(),
            required_actions: actions
                .iter()
                .filter(|action| action.requires_action())
                .count(),
            repair_release_blocker_actions: actions
                .iter()
                .filter(|action| action.is_repair_action())
                .count(),
            operator_handoff_actions: actions
                .iter()
                .filter(|action| action.is_operator_handoff_action())
                .count(),
            review_gate_actions: actions
                .iter()
                .filter(|action| action.is_review_gate_action())
                .count(),
            release_clearance_actions: actions
                .iter()
                .filter(|action| action.is_release_clearance_action())
                .count(),
            release_blocking_actions: actions
                .iter()
                .filter(|action| action.blocks_release())
                .count(),
            operator_required_actions: actions
                .iter()
                .filter(|action| action.needs_operator())
                .count(),
            review_required_actions: actions
                .iter()
                .filter(|action| action.needs_review())
                .count(),
            dispatch_required_actions: actions
                .iter()
                .filter(|action| action.dispatch_required)
                .count(),
            execution_required_actions: actions
                .iter()
                .filter(|action| action.execution_required)
                .count(),
            first_action_key: first_action.map(|action| action.action_key.clone()),
            first_required_action_key: first_required_action
                .map(|action| action.action_key.clone()),
            first_repair_action_key: first_repair_action.map(|action| action.action_key.clone()),
            first_operator_handoff_action_key: first_operator_handoff_action
                .map(|action| action.action_key.clone()),
            first_review_gate_action_key: first_review_gate_action
                .map(|action| action.action_key.clone()),
            first_release_clearance_action_key: first_release_clearance_action
                .map(|action| action.action_key.clone()),
            first_clearance_key: first_action.map(|action| action.clearance_key.clone()),
            first_work_order_key: first_action.map(|action| action.work_order_key.clone()),
            first_ticket_key: first_action.map(|action| action.ticket_key.clone()),
            first_action_kind: first_action.map(|action| action.action_kind),
            first_clearance_kind: first_action.map(|action| action.clearance_kind),
            first_handoff_lane: first_action.map(|action| action.handoff_lane),
            first_check_kind: first_action.map(|action| action.check_kind),
            first_check_status: first_action.map(|action| action.status),
            release_handoff_execution_work_order_guardrail_audit_clearance_actions_ready: !actions
                .is_empty()
                && actions
                    .iter()
                    .all(|action| action.release_clearance_ready()),
        }
    }

    pub fn has_actions(&self) -> bool {
        self.total_actions > 0
    }

    pub fn has_required_actions(&self) -> bool {
        self.required_actions > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.release_blocking_actions > 0
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_handoff_actions > 0 || self.operator_required_actions > 0
    }

    pub fn needs_review(&self) -> bool {
        self.review_gate_actions > 0 || self.review_required_actions > 0
    }

    pub fn has_release_clearance_actions(&self) -> bool {
        self.release_clearance_actions > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.has_required_actions()
            || self.has_blockers()
            || self.needs_operator()
            || self.needs_review()
            || self.dispatch_required_actions > 0
            || self.execution_required_actions > 0
            || !self.release_handoff_execution_work_order_guardrail_audit_clearance_actions_ready
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceKind
{
    BlockerEvidence,
    OperatorEvidence,
    ReviewEvidence,
    ReleaseEvidence,
}

impl
    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceKind
{
    pub fn as_str(self) -> &'static str {
        match self {
            Self::BlockerEvidence => "blocker_evidence",
            Self::OperatorEvidence => "operator_evidence",
            Self::ReviewEvidence => "review_evidence",
            Self::ReleaseEvidence => "release_evidence",
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidence {
    pub sequence: usize,
    pub evidence_key: String,
    pub action_sequence: usize,
    pub action_key: String,
    pub clearance_sequence: usize,
    pub clearance_key: String,
    pub audit_key: String,
    pub guardrail_key: String,
    pub work_order_key: String,
    pub execution_slot_key: String,
    pub packet_key: String,
    pub ticket_key: String,
    pub check_sequence: usize,
    pub check_kind: IntegrationMeshReleaseReadinessCheckKind,
    pub status: IntegrationMeshReleaseReadinessStatus,
    pub handoff_lane: IntegrationMeshReleaseDispatchTicketHandoffLane,
    pub clearance_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind,
    pub action_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionKind,
    pub evidence_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceKind,
    pub release_blocking: bool,
    pub operator_required: bool,
    pub review_required: bool,
    pub dispatch_required: bool,
    pub execution_required: bool,
    pub release_evidence: bool,
    pub lineage_complete: bool,
}

impl IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidence {
    pub fn from_action(
        sequence: usize,
        action: &IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceAction,
    ) -> Self {
        let evidence_kind = match action.action_kind {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionKind::RepairReleaseBlocker => {
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceKind::BlockerEvidence
            }
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionKind::CoordinateOperatorHandoff => {
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceKind::OperatorEvidence
            }
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionKind::RouteReviewGate => {
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceKind::ReviewEvidence
            }
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionKind::ReleaseClearance => {
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceKind::ReleaseEvidence
            }
        };
        let lineage_complete = action.clearance_sequence > 0
            && action.check_sequence > 0
            && !action.action_key.is_empty()
            && !action.clearance_key.is_empty()
            && !action.audit_key.is_empty()
            && !action.guardrail_key.is_empty()
            && !action.work_order_key.is_empty()
            && !action.execution_slot_key.is_empty()
            && !action.packet_key.is_empty()
            && !action.ticket_key.is_empty();

        Self {
            sequence,
            evidence_key: format!(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-evidence-{sequence:02}-{}-{}",
                evidence_kind.as_str(),
                action.check_kind.as_str()
            ),
            action_sequence: action.sequence,
            action_key: action.action_key.clone(),
            clearance_sequence: action.clearance_sequence,
            clearance_key: action.clearance_key.clone(),
            audit_key: action.audit_key.clone(),
            guardrail_key: action.guardrail_key.clone(),
            work_order_key: action.work_order_key.clone(),
            execution_slot_key: action.execution_slot_key.clone(),
            packet_key: action.packet_key.clone(),
            ticket_key: action.ticket_key.clone(),
            check_sequence: action.check_sequence,
            check_kind: action.check_kind,
            status: action.status,
            handoff_lane: action.handoff_lane,
            clearance_kind: action.clearance_kind,
            action_kind: action.action_kind,
            evidence_kind,
            release_blocking: action.blocks_release(),
            operator_required: action.needs_operator(),
            review_required: action.needs_review(),
            dispatch_required: action.dispatch_required,
            execution_required: action.execution_required,
            release_evidence: action.release_clearance_ready(),
            lineage_complete,
        }
    }

    pub fn blocks_release(&self) -> bool {
        self.release_blocking
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required
    }

    pub fn needs_review(&self) -> bool {
        self.review_required
    }

    pub fn is_release_evidence(&self) -> bool {
        self.release_evidence
    }

    pub fn has_lineage(&self) -> bool {
        self.lineage_complete
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceSummary {
    pub total_evidence: usize,
    pub blocking_evidence: usize,
    pub operator_required_evidence: usize,
    pub review_required_evidence: usize,
    pub dispatch_required_evidence: usize,
    pub execution_required_evidence: usize,
    pub release_evidence: usize,
    pub lineage_complete_evidence: usize,
    pub first_evidence_key: Option<String>,
    pub first_blocking_evidence_key: Option<String>,
    pub first_operator_evidence_key: Option<String>,
    pub first_review_evidence_key: Option<String>,
    pub first_release_evidence_key: Option<String>,
    pub first_action_key: Option<String>,
    pub first_clearance_key: Option<String>,
    pub first_work_order_key: Option<String>,
    pub first_ticket_key: Option<String>,
    pub first_evidence_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceKind,
    >,
    pub first_action_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionKind,
    >,
    pub first_clearance_kind:
        Option<IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind>,
    pub first_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub first_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub first_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_ready: bool,
}

impl IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceSummary {
    pub fn from_evidence<'a>(
        evidence: impl IntoIterator<
            Item = &'a IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidence,
        >,
    ) -> Self {
        let evidence = evidence.into_iter().collect::<Vec<_>>();
        let first_evidence = evidence.iter().min_by_key(|row| row.sequence);
        let first_blocking_evidence = evidence
            .iter()
            .filter(|row| row.blocks_release())
            .min_by_key(|row| row.sequence);
        let first_operator_evidence = evidence
            .iter()
            .filter(|row| row.needs_operator())
            .min_by_key(|row| row.sequence);
        let first_review_evidence = evidence
            .iter()
            .filter(|row| row.needs_review())
            .min_by_key(|row| row.sequence);
        let first_release_evidence = evidence
            .iter()
            .filter(|row| row.is_release_evidence())
            .min_by_key(|row| row.sequence);

        Self {
            total_evidence: evidence.len(),
            blocking_evidence: evidence
                .iter()
                .filter(|row| row.blocks_release())
                .count(),
            operator_required_evidence: evidence
                .iter()
                .filter(|row| row.needs_operator())
                .count(),
            review_required_evidence: evidence
                .iter()
                .filter(|row| row.needs_review())
                .count(),
            dispatch_required_evidence: evidence
                .iter()
                .filter(|row| row.dispatch_required)
                .count(),
            execution_required_evidence: evidence
                .iter()
                .filter(|row| row.execution_required)
                .count(),
            release_evidence: evidence
                .iter()
                .filter(|row| row.is_release_evidence())
                .count(),
            lineage_complete_evidence: evidence.iter().filter(|row| row.has_lineage()).count(),
            first_evidence_key: first_evidence.map(|row| row.evidence_key.clone()),
            first_blocking_evidence_key: first_blocking_evidence
                .map(|row| row.evidence_key.clone()),
            first_operator_evidence_key: first_operator_evidence
                .map(|row| row.evidence_key.clone()),
            first_review_evidence_key: first_review_evidence.map(|row| row.evidence_key.clone()),
            first_release_evidence_key: first_release_evidence
                .map(|row| row.evidence_key.clone()),
            first_action_key: first_evidence.map(|row| row.action_key.clone()),
            first_clearance_key: first_evidence.map(|row| row.clearance_key.clone()),
            first_work_order_key: first_evidence.map(|row| row.work_order_key.clone()),
            first_ticket_key: first_evidence.map(|row| row.ticket_key.clone()),
            first_evidence_kind: first_evidence.map(|row| row.evidence_kind),
            first_action_kind: first_evidence.map(|row| row.action_kind),
            first_clearance_kind: first_evidence.map(|row| row.clearance_kind),
            first_handoff_lane: first_evidence.map(|row| row.handoff_lane),
            first_check_kind: first_evidence.map(|row| row.check_kind),
            first_check_status: first_evidence.map(|row| row.status),
            release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_ready:
                !evidence.is_empty()
                    && evidence.iter().all(|row| {
                        row.is_release_evidence() && row.has_lineage()
                    }),
        }
    }

    pub fn has_evidence(&self) -> bool {
        self.total_evidence > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.blocking_evidence > 0
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_evidence > 0
    }

    pub fn needs_review(&self) -> bool {
        self.review_required_evidence > 0
    }

    pub fn has_release_evidence(&self) -> bool {
        self.release_evidence > 0
    }

    pub fn has_complete_lineage(&self) -> bool {
        self.lineage_complete_evidence == self.total_evidence
    }

    pub fn requires_attention(&self) -> bool {
        self.has_blockers()
            || self.needs_operator()
            || self.needs_review()
            || self.dispatch_required_evidence > 0
            || self.execution_required_evidence > 0
            || !self
                .release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_ready
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffExecutionWorkOrderSummary {
    pub release_ticket_handoff_execution_readiness_summary:
        IntegrationMeshReleaseTicketHandoffExecutionReadinessSummary,
    pub total_work_orders: usize,
    pub ready_work_orders: usize,
    pub blocked_work_orders: usize,
    pub review_required_work_orders: usize,
    pub operator_required_work_orders: usize,
    pub dispatch_required_work_orders: usize,
    pub release_lane_work_orders: usize,
    pub operator_lane_work_orders: usize,
    pub repair_lane_work_orders: usize,
    pub review_lane_work_orders: usize,
    pub execution_required_work_orders: usize,
    pub first_work_order_key: Option<String>,
    pub first_release_work_order_key: Option<String>,
    pub first_operator_work_order_key: Option<String>,
    pub first_repair_work_order_key: Option<String>,
    pub first_review_work_order_key: Option<String>,
    pub next_work_order_key: Option<String>,
    pub next_execution_slot_key: Option<String>,
    pub next_packet_key: Option<String>,
    pub next_ticket_key: Option<String>,
    pub next_dispatch_key: Option<String>,
    pub next_task_key: Option<String>,
    pub next_slot_key: Option<String>,
    pub next_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub next_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_package_kind: Option<IntegrationMeshReadinessHandoffKind>,
    pub next_handoff_status: Option<IntegrationMeshReadinessHandoffStatus>,
    pub next_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub packet_status: IntegrationMeshReleaseReadinessStatus,
    pub execution_status: IntegrationMeshReleaseReadinessStatus,
    pub release_packet_ready: bool,
    pub release_execution_ready: bool,
    pub release_tasks_ready: bool,
    pub release_dispatch_ready: bool,
    pub release_ticket_ready: bool,
    pub release_handoff_ready: bool,
    pub release_handoff_execution_ready: bool,
    pub release_ticket_handoff_execution_ready: bool,
    pub release_handoff_execution_work_orders_ready: bool,
}

impl IntegrationMeshReleaseTicketHandoffExecutionWorkOrderSummary {
    pub fn from_parts<'a>(
        release_ticket_handoff_execution_readiness_summary: IntegrationMeshReleaseTicketHandoffExecutionReadinessSummary,
        work_orders: impl IntoIterator<Item = &'a IntegrationMeshReleaseTicketHandoffExecutionWorkOrder>,
    ) -> Self {
        let work_orders = work_orders.into_iter().collect::<Vec<_>>();
        let first_work_order = work_orders
            .iter()
            .min_by_key(|work_order| work_order.sequence);
        let first_release_work_order = work_orders
            .iter()
            .filter(|work_order| work_order.is_release_lane())
            .min_by_key(|work_order| work_order.sequence);
        let first_operator_work_order = work_orders
            .iter()
            .filter(|work_order| work_order.is_operator_lane())
            .min_by_key(|work_order| work_order.sequence);
        let first_repair_work_order = work_orders
            .iter()
            .filter(|work_order| work_order.is_repair_lane())
            .min_by_key(|work_order| work_order.sequence);
        let first_review_work_order = work_orders
            .iter()
            .filter(|work_order| work_order.is_review_lane())
            .min_by_key(|work_order| work_order.sequence);
        let next_work_order = work_orders
            .iter()
            .filter(|work_order| work_order.execution_required())
            .min_by_key(|work_order| work_order.sequence);
        let release_handoff_execution_work_orders_ready =
            release_ticket_handoff_execution_readiness_summary
                .ready_for_release_ticket_handoff_execution()
                && !work_orders.is_empty()
                && work_orders
                    .iter()
                    .all(|work_order| work_order.ready() && work_order.is_release_lane());

        Self {
            total_work_orders: work_orders.len(),
            ready_work_orders: work_orders
                .iter()
                .filter(|work_order| work_order.ready())
                .count(),
            blocked_work_orders: work_orders
                .iter()
                .filter(|work_order| work_order.blocked())
                .count(),
            review_required_work_orders: work_orders
                .iter()
                .filter(|work_order| work_order.review_required())
                .count(),
            operator_required_work_orders: work_orders
                .iter()
                .filter(|work_order| work_order.operator_required())
                .count(),
            dispatch_required_work_orders: work_orders
                .iter()
                .filter(|work_order| work_order.dispatch_required())
                .count(),
            release_lane_work_orders: work_orders
                .iter()
                .filter(|work_order| work_order.is_release_lane())
                .count(),
            operator_lane_work_orders: work_orders
                .iter()
                .filter(|work_order| work_order.is_operator_lane())
                .count(),
            repair_lane_work_orders: work_orders
                .iter()
                .filter(|work_order| work_order.is_repair_lane())
                .count(),
            review_lane_work_orders: work_orders
                .iter()
                .filter(|work_order| work_order.is_review_lane())
                .count(),
            execution_required_work_orders: work_orders
                .iter()
                .filter(|work_order| work_order.execution_required())
                .count(),
            first_work_order_key: first_work_order
                .map(|work_order| work_order.work_order_key.clone()),
            first_release_work_order_key: first_release_work_order
                .map(|work_order| work_order.work_order_key.clone()),
            first_operator_work_order_key: first_operator_work_order
                .map(|work_order| work_order.work_order_key.clone()),
            first_repair_work_order_key: first_repair_work_order
                .map(|work_order| work_order.work_order_key.clone()),
            first_review_work_order_key: first_review_work_order
                .map(|work_order| work_order.work_order_key.clone()),
            next_work_order_key: next_work_order
                .map(|work_order| work_order.work_order_key.clone()),
            next_execution_slot_key: next_work_order
                .map(|work_order| work_order.execution_slot_key.clone()),
            next_packet_key: next_work_order.map(|work_order| work_order.packet_key.clone()),
            next_ticket_key: next_work_order.map(|work_order| work_order.ticket_key.clone()),
            next_dispatch_key: next_work_order.map(|work_order| work_order.dispatch_key.clone()),
            next_task_key: next_work_order.map(|work_order| work_order.task_key.clone()),
            next_slot_key: next_work_order
                .map(|work_order| work_order.slot_key.clone())
                .or_else(|| {
                    release_ticket_handoff_execution_readiness_summary
                        .next_slot_key
                        .clone()
                }),
            next_check_kind: next_work_order
                .map(|work_order| work_order.check_kind)
                .or(release_ticket_handoff_execution_readiness_summary.next_check_kind),
            next_check_status: next_work_order
                .map(|work_order| work_order.status)
                .or(release_ticket_handoff_execution_readiness_summary.next_check_status),
            next_package_kind: next_work_order
                .and_then(|work_order| work_order.package_kind)
                .or(release_ticket_handoff_execution_readiness_summary.next_package_kind),
            next_handoff_status: next_work_order
                .and_then(|work_order| work_order.handoff_status)
                .or(release_ticket_handoff_execution_readiness_summary.next_handoff_status),
            next_handoff_lane: next_work_order
                .map(|work_order| work_order.handoff_lane)
                .or(release_ticket_handoff_execution_readiness_summary.next_handoff_lane),
            packet_status: release_ticket_handoff_execution_readiness_summary.packet_status,
            execution_status: release_ticket_handoff_execution_readiness_summary.execution_status,
            release_packet_ready: release_ticket_handoff_execution_readiness_summary
                .release_packet_ready,
            release_execution_ready: release_ticket_handoff_execution_readiness_summary
                .release_execution_ready,
            release_tasks_ready: release_ticket_handoff_execution_readiness_summary
                .release_tasks_ready,
            release_dispatch_ready: release_ticket_handoff_execution_readiness_summary
                .release_dispatch_ready,
            release_ticket_ready: release_ticket_handoff_execution_readiness_summary
                .release_ticket_ready,
            release_handoff_ready: release_ticket_handoff_execution_readiness_summary
                .release_handoff_ready,
            release_handoff_execution_ready: release_ticket_handoff_execution_readiness_summary
                .release_handoff_execution_ready,
            release_ticket_handoff_execution_ready:
                release_ticket_handoff_execution_readiness_summary
                    .release_ticket_handoff_execution_ready,
            release_handoff_execution_work_orders_ready,
            release_ticket_handoff_execution_readiness_summary,
        }
    }

    pub fn has_work_orders(&self) -> bool {
        self.total_work_orders > 0
    }

    pub fn ready_for_release_handoff_execution_work_orders(&self) -> bool {
        self.execution_status == IntegrationMeshReleaseReadinessStatus::Ready
            && self.release_packet_ready
            && self.release_execution_ready
            && self.release_tasks_ready
            && self.release_dispatch_ready
            && self.release_ticket_ready
            && self.release_handoff_ready
            && self.release_handoff_execution_ready
            && self.release_ticket_handoff_execution_ready
            && self.release_handoff_execution_work_orders_ready
            && !self.requires_attention()
    }

    pub fn has_repair_work(&self) -> bool {
        self.repair_lane_work_orders > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.blocked_work_orders > 0
            || self.has_repair_work()
            || self
                .release_ticket_handoff_execution_readiness_summary
                .has_blockers()
    }

    pub fn has_review_work(&self) -> bool {
        self.review_required_work_orders > 0
            || self.review_lane_work_orders > 0
            || self
                .release_ticket_handoff_execution_readiness_summary
                .has_review_work()
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_work_orders > 0
            || self.operator_lane_work_orders > 0
            || self
                .release_ticket_handoff_execution_readiness_summary
                .needs_operator()
    }

    pub fn requires_attention(&self) -> bool {
        self.execution_required_work_orders > 0
            || self.dispatch_required_work_orders > 0
            || self.has_blockers()
            || self.has_review_work()
            || self.needs_operator()
            || !self.release_handoff_execution_work_orders_ready
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffWorkOrderReadinessSummary {
    pub release_ticket_handoff_execution_readiness_summary:
        IntegrationMeshReleaseTicketHandoffExecutionReadinessSummary,
    pub release_ticket_handoff_execution_work_order_summary:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderSummary,
    pub total_tickets: usize,
    pub ready_tickets: usize,
    pub blocked_tickets: usize,
    pub review_required_tickets: usize,
    pub operator_required_tickets: usize,
    pub dispatch_required_tickets: usize,
    pub total_handoff_packets: usize,
    pub total_execution_slots: usize,
    pub total_work_orders: usize,
    pub ready_work_orders: usize,
    pub blocked_work_orders: usize,
    pub review_required_work_orders: usize,
    pub operator_required_work_orders: usize,
    pub dispatch_required_work_orders: usize,
    pub release_lane_work_orders: usize,
    pub operator_lane_work_orders: usize,
    pub repair_lane_work_orders: usize,
    pub review_lane_work_orders: usize,
    pub execution_required_work_orders: usize,
    pub queued_substrate_actions: usize,
    pub remediation_item_count: usize,
    pub review_required_packages: usize,
    pub operator_required_packages: usize,
    pub blocked_packages: usize,
    pub first_execution_slot_key: Option<String>,
    pub first_work_order_key: Option<String>,
    pub first_release_work_order_key: Option<String>,
    pub next_work_order_key: Option<String>,
    pub next_execution_slot_key: Option<String>,
    pub next_packet_key: Option<String>,
    pub next_ticket_key: Option<String>,
    pub next_dispatch_key: Option<String>,
    pub next_task_key: Option<String>,
    pub next_slot_key: Option<String>,
    pub next_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub next_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_package_kind: Option<IntegrationMeshReadinessHandoffKind>,
    pub next_handoff_status: Option<IntegrationMeshReadinessHandoffStatus>,
    pub next_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub packet_status: IntegrationMeshReleaseReadinessStatus,
    pub execution_status: IntegrationMeshReleaseReadinessStatus,
    pub release_packet_ready: bool,
    pub release_execution_ready: bool,
    pub release_tasks_ready: bool,
    pub release_dispatch_ready: bool,
    pub release_ticket_ready: bool,
    pub release_handoff_ready: bool,
    pub release_handoff_execution_ready: bool,
    pub release_ticket_handoff_execution_ready: bool,
    pub release_handoff_execution_work_orders_ready: bool,
    pub release_ticket_handoff_work_orders_ready: bool,
}

impl IntegrationMeshReleaseTicketHandoffWorkOrderReadinessSummary {
    pub fn from_summaries(
        release_ticket_handoff_execution_readiness_summary: IntegrationMeshReleaseTicketHandoffExecutionReadinessSummary,
        release_ticket_handoff_execution_work_order_summary: IntegrationMeshReleaseTicketHandoffExecutionWorkOrderSummary,
    ) -> Self {
        let release_ticket_handoff_work_orders_ready =
            release_ticket_handoff_execution_readiness_summary
                .ready_for_release_ticket_handoff_execution()
                && release_ticket_handoff_execution_work_order_summary
                    .ready_for_release_handoff_execution_work_orders();

        Self {
            total_tickets: release_ticket_handoff_execution_readiness_summary.total_tickets,
            ready_tickets: release_ticket_handoff_execution_readiness_summary.ready_tickets,
            blocked_tickets: release_ticket_handoff_execution_readiness_summary.blocked_tickets,
            review_required_tickets: release_ticket_handoff_execution_readiness_summary
                .review_required_tickets,
            operator_required_tickets: release_ticket_handoff_execution_readiness_summary
                .operator_required_tickets,
            dispatch_required_tickets: release_ticket_handoff_execution_readiness_summary
                .dispatch_required_tickets,
            total_handoff_packets: release_ticket_handoff_execution_readiness_summary
                .total_handoff_packets,
            total_execution_slots: release_ticket_handoff_execution_readiness_summary
                .total_execution_slots,
            total_work_orders: release_ticket_handoff_execution_work_order_summary
                .total_work_orders,
            ready_work_orders: release_ticket_handoff_execution_work_order_summary
                .ready_work_orders,
            blocked_work_orders: release_ticket_handoff_execution_work_order_summary
                .blocked_work_orders,
            review_required_work_orders: release_ticket_handoff_execution_work_order_summary
                .review_required_work_orders,
            operator_required_work_orders: release_ticket_handoff_execution_work_order_summary
                .operator_required_work_orders,
            dispatch_required_work_orders: release_ticket_handoff_execution_work_order_summary
                .dispatch_required_work_orders,
            release_lane_work_orders: release_ticket_handoff_execution_work_order_summary
                .release_lane_work_orders,
            operator_lane_work_orders: release_ticket_handoff_execution_work_order_summary
                .operator_lane_work_orders,
            repair_lane_work_orders: release_ticket_handoff_execution_work_order_summary
                .repair_lane_work_orders,
            review_lane_work_orders: release_ticket_handoff_execution_work_order_summary
                .review_lane_work_orders,
            execution_required_work_orders: release_ticket_handoff_execution_work_order_summary
                .execution_required_work_orders,
            queued_substrate_actions: release_ticket_handoff_execution_readiness_summary
                .queued_substrate_actions,
            remediation_item_count: release_ticket_handoff_execution_readiness_summary
                .remediation_item_count,
            review_required_packages: release_ticket_handoff_execution_readiness_summary
                .review_required_packages,
            operator_required_packages: release_ticket_handoff_execution_readiness_summary
                .operator_required_packages,
            blocked_packages: release_ticket_handoff_execution_readiness_summary.blocked_packages,
            first_execution_slot_key: release_ticket_handoff_execution_readiness_summary
                .first_execution_slot_key
                .clone(),
            first_work_order_key: release_ticket_handoff_execution_work_order_summary
                .first_work_order_key
                .clone(),
            first_release_work_order_key: release_ticket_handoff_execution_work_order_summary
                .first_release_work_order_key
                .clone(),
            next_work_order_key: release_ticket_handoff_execution_work_order_summary
                .next_work_order_key
                .clone(),
            next_execution_slot_key: release_ticket_handoff_execution_work_order_summary
                .next_execution_slot_key
                .clone(),
            next_packet_key: release_ticket_handoff_execution_work_order_summary
                .next_packet_key
                .clone(),
            next_ticket_key: release_ticket_handoff_execution_work_order_summary
                .next_ticket_key
                .clone(),
            next_dispatch_key: release_ticket_handoff_execution_work_order_summary
                .next_dispatch_key
                .clone(),
            next_task_key: release_ticket_handoff_execution_work_order_summary
                .next_task_key
                .clone(),
            next_slot_key: release_ticket_handoff_execution_work_order_summary
                .next_slot_key
                .clone(),
            next_check_kind: release_ticket_handoff_execution_work_order_summary.next_check_kind,
            next_check_status: release_ticket_handoff_execution_work_order_summary
                .next_check_status,
            next_package_kind: release_ticket_handoff_execution_work_order_summary
                .next_package_kind,
            next_handoff_status: release_ticket_handoff_execution_work_order_summary
                .next_handoff_status,
            next_handoff_lane: release_ticket_handoff_execution_work_order_summary
                .next_handoff_lane,
            packet_status: release_ticket_handoff_execution_readiness_summary.packet_status,
            execution_status: release_ticket_handoff_execution_readiness_summary.execution_status,
            release_packet_ready: release_ticket_handoff_execution_readiness_summary
                .release_packet_ready,
            release_execution_ready: release_ticket_handoff_execution_readiness_summary
                .release_execution_ready,
            release_tasks_ready: release_ticket_handoff_execution_readiness_summary
                .release_tasks_ready,
            release_dispatch_ready: release_ticket_handoff_execution_readiness_summary
                .release_dispatch_ready,
            release_ticket_ready: release_ticket_handoff_execution_readiness_summary
                .release_ticket_ready,
            release_handoff_ready: release_ticket_handoff_execution_readiness_summary
                .release_handoff_ready,
            release_handoff_execution_ready: release_ticket_handoff_execution_readiness_summary
                .release_handoff_execution_ready,
            release_ticket_handoff_execution_ready:
                release_ticket_handoff_execution_readiness_summary
                    .release_ticket_handoff_execution_ready,
            release_handoff_execution_work_orders_ready:
                release_ticket_handoff_execution_work_order_summary
                    .release_handoff_execution_work_orders_ready,
            release_ticket_handoff_work_orders_ready,
            release_ticket_handoff_execution_readiness_summary,
            release_ticket_handoff_execution_work_order_summary,
        }
    }

    pub fn has_work_orders(&self) -> bool {
        self.total_work_orders > 0
    }

    pub fn ready_for_release_ticket_handoff_work_orders(&self) -> bool {
        self.execution_status == IntegrationMeshReleaseReadinessStatus::Ready
            && self.release_packet_ready
            && self.release_execution_ready
            && self.release_tasks_ready
            && self.release_dispatch_ready
            && self.release_ticket_ready
            && self.release_handoff_ready
            && self.release_handoff_execution_ready
            && self.release_ticket_handoff_execution_ready
            && self.release_handoff_execution_work_orders_ready
            && self.release_ticket_handoff_work_orders_ready
            && !self.requires_attention()
    }

    pub fn has_repair_work(&self) -> bool {
        self.repair_lane_work_orders > 0
            || self
                .release_ticket_handoff_execution_readiness_summary
                .has_repair_work()
            || self
                .release_ticket_handoff_execution_work_order_summary
                .has_repair_work()
    }

    pub fn has_blockers(&self) -> bool {
        self.blocked_work_orders > 0
            || self.blocked_packages > 0
            || self.has_repair_work()
            || self
                .release_ticket_handoff_execution_readiness_summary
                .has_blockers()
            || self
                .release_ticket_handoff_execution_work_order_summary
                .has_blockers()
    }

    pub fn has_review_work(&self) -> bool {
        self.review_required_work_orders > 0
            || self.review_lane_work_orders > 0
            || self.review_required_packages > 0
            || self
                .release_ticket_handoff_execution_readiness_summary
                .has_review_work()
            || self
                .release_ticket_handoff_execution_work_order_summary
                .has_review_work()
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_work_orders > 0
            || self.operator_lane_work_orders > 0
            || self.operator_required_packages > 0
            || self
                .release_ticket_handoff_execution_readiness_summary
                .needs_operator()
            || self
                .release_ticket_handoff_execution_work_order_summary
                .needs_operator()
    }

    pub fn requires_execution(&self) -> bool {
        self.execution_required_work_orders > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.requires_execution()
            || self.dispatch_required_work_orders > 0
            || self.has_blockers()
            || self.has_review_work()
            || self.needs_operator()
            || !self.release_ticket_handoff_work_orders_ready
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffWorkOrderGuardrailReadinessSummary {
    pub release_ticket_handoff_work_order_readiness_summary:
        IntegrationMeshReleaseTicketHandoffWorkOrderReadinessSummary,
    pub release_ticket_handoff_execution_work_order_guardrail_summary:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailSummary,
    pub total_tickets: usize,
    pub ready_tickets: usize,
    pub blocked_tickets: usize,
    pub review_required_tickets: usize,
    pub operator_required_tickets: usize,
    pub dispatch_required_tickets: usize,
    pub total_handoff_packets: usize,
    pub total_execution_slots: usize,
    pub total_work_orders: usize,
    pub ready_work_orders: usize,
    pub blocked_work_orders: usize,
    pub review_required_work_orders: usize,
    pub operator_required_work_orders: usize,
    pub dispatch_required_work_orders: usize,
    pub release_lane_work_orders: usize,
    pub operator_lane_work_orders: usize,
    pub repair_lane_work_orders: usize,
    pub review_lane_work_orders: usize,
    pub execution_required_work_orders: usize,
    pub total_guardrails: usize,
    pub release_blocker_guardrails: usize,
    pub operator_handoff_guardrails: usize,
    pub review_gate_guardrails: usize,
    pub ready_to_execute_guardrails: usize,
    pub release_blocking_guardrails: usize,
    pub operator_required_guardrails: usize,
    pub review_required_guardrails: usize,
    pub dispatch_required_guardrails: usize,
    pub execution_required_guardrails: usize,
    pub queued_substrate_actions: usize,
    pub remediation_item_count: usize,
    pub review_required_packages: usize,
    pub operator_required_packages: usize,
    pub blocked_packages: usize,
    pub first_guardrail_key: Option<String>,
    pub first_guardrail_work_order_key: Option<String>,
    pub first_release_blocker_work_order_key: Option<String>,
    pub first_operator_handoff_work_order_key: Option<String>,
    pub first_review_gate_work_order_key: Option<String>,
    pub first_ready_to_execute_work_order_key: Option<String>,
    pub first_guardrail_kind:
        Option<IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailKind>,
    pub first_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub first_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub first_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_work_order_key: Option<String>,
    pub next_execution_slot_key: Option<String>,
    pub next_packet_key: Option<String>,
    pub next_ticket_key: Option<String>,
    pub next_dispatch_key: Option<String>,
    pub next_task_key: Option<String>,
    pub next_slot_key: Option<String>,
    pub next_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub next_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_package_kind: Option<IntegrationMeshReadinessHandoffKind>,
    pub next_handoff_status: Option<IntegrationMeshReadinessHandoffStatus>,
    pub next_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub packet_status: IntegrationMeshReleaseReadinessStatus,
    pub execution_status: IntegrationMeshReleaseReadinessStatus,
    pub release_packet_ready: bool,
    pub release_execution_ready: bool,
    pub release_tasks_ready: bool,
    pub release_dispatch_ready: bool,
    pub release_ticket_ready: bool,
    pub release_handoff_ready: bool,
    pub release_handoff_execution_ready: bool,
    pub release_ticket_handoff_execution_ready: bool,
    pub release_handoff_execution_work_orders_ready: bool,
    pub release_ticket_handoff_work_orders_ready: bool,
    pub release_handoff_execution_work_order_guardrails_ready: bool,
    pub release_ticket_handoff_work_order_guardrails_ready: bool,
}

impl IntegrationMeshReleaseTicketHandoffWorkOrderGuardrailReadinessSummary {
    pub fn from_summaries(
        release_ticket_handoff_work_order_readiness_summary: IntegrationMeshReleaseTicketHandoffWorkOrderReadinessSummary,
        release_ticket_handoff_execution_work_order_guardrail_summary: IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailSummary,
    ) -> Self {
        let release_handoff_execution_work_order_guardrails_ready =
            release_ticket_handoff_execution_work_order_guardrail_summary
                .release_handoff_execution_work_order_guardrails_ready;
        let release_ticket_handoff_work_order_guardrails_ready =
            release_ticket_handoff_work_order_readiness_summary
                .ready_for_release_ticket_handoff_work_orders()
                && release_handoff_execution_work_order_guardrails_ready;
        let next_guardrail_work_order_key =
            if release_ticket_handoff_execution_work_order_guardrail_summary.requires_attention() {
                release_ticket_handoff_execution_work_order_guardrail_summary
                    .first_work_order_key
                    .clone()
            } else {
                None
            };
        let next_guardrail_check_kind =
            if release_ticket_handoff_execution_work_order_guardrail_summary.requires_attention() {
                release_ticket_handoff_execution_work_order_guardrail_summary.first_check_kind
            } else {
                None
            };
        let next_guardrail_check_status =
            if release_ticket_handoff_execution_work_order_guardrail_summary.requires_attention() {
                release_ticket_handoff_execution_work_order_guardrail_summary.first_check_status
            } else {
                None
            };
        let next_guardrail_handoff_lane =
            if release_ticket_handoff_execution_work_order_guardrail_summary.requires_attention() {
                release_ticket_handoff_execution_work_order_guardrail_summary.first_handoff_lane
            } else {
                None
            };

        Self {
            total_tickets: release_ticket_handoff_work_order_readiness_summary.total_tickets,
            ready_tickets: release_ticket_handoff_work_order_readiness_summary.ready_tickets,
            blocked_tickets: release_ticket_handoff_work_order_readiness_summary.blocked_tickets,
            review_required_tickets: release_ticket_handoff_work_order_readiness_summary
                .review_required_tickets,
            operator_required_tickets: release_ticket_handoff_work_order_readiness_summary
                .operator_required_tickets,
            dispatch_required_tickets: release_ticket_handoff_work_order_readiness_summary
                .dispatch_required_tickets,
            total_handoff_packets: release_ticket_handoff_work_order_readiness_summary
                .total_handoff_packets,
            total_execution_slots: release_ticket_handoff_work_order_readiness_summary
                .total_execution_slots,
            total_work_orders: release_ticket_handoff_work_order_readiness_summary
                .total_work_orders,
            ready_work_orders: release_ticket_handoff_work_order_readiness_summary
                .ready_work_orders,
            blocked_work_orders: release_ticket_handoff_work_order_readiness_summary
                .blocked_work_orders,
            review_required_work_orders: release_ticket_handoff_work_order_readiness_summary
                .review_required_work_orders,
            operator_required_work_orders: release_ticket_handoff_work_order_readiness_summary
                .operator_required_work_orders,
            dispatch_required_work_orders: release_ticket_handoff_work_order_readiness_summary
                .dispatch_required_work_orders,
            release_lane_work_orders: release_ticket_handoff_work_order_readiness_summary
                .release_lane_work_orders,
            operator_lane_work_orders: release_ticket_handoff_work_order_readiness_summary
                .operator_lane_work_orders,
            repair_lane_work_orders: release_ticket_handoff_work_order_readiness_summary
                .repair_lane_work_orders,
            review_lane_work_orders: release_ticket_handoff_work_order_readiness_summary
                .review_lane_work_orders,
            execution_required_work_orders: release_ticket_handoff_work_order_readiness_summary
                .execution_required_work_orders,
            total_guardrails: release_ticket_handoff_execution_work_order_guardrail_summary
                .total_guardrails,
            release_blocker_guardrails:
                release_ticket_handoff_execution_work_order_guardrail_summary
                    .release_blocker_guardrails,
            operator_handoff_guardrails:
                release_ticket_handoff_execution_work_order_guardrail_summary
                    .operator_handoff_guardrails,
            review_gate_guardrails: release_ticket_handoff_execution_work_order_guardrail_summary
                .review_gate_guardrails,
            ready_to_execute_guardrails:
                release_ticket_handoff_execution_work_order_guardrail_summary
                    .ready_to_execute_guardrails,
            release_blocking_guardrails:
                release_ticket_handoff_execution_work_order_guardrail_summary
                    .release_blocking_guardrails,
            operator_required_guardrails:
                release_ticket_handoff_execution_work_order_guardrail_summary
                    .operator_required_guardrails,
            review_required_guardrails:
                release_ticket_handoff_execution_work_order_guardrail_summary
                    .review_required_guardrails,
            dispatch_required_guardrails:
                release_ticket_handoff_execution_work_order_guardrail_summary
                    .dispatch_required_guardrails,
            execution_required_guardrails:
                release_ticket_handoff_execution_work_order_guardrail_summary
                    .execution_required_guardrails,
            queued_substrate_actions: release_ticket_handoff_work_order_readiness_summary
                .queued_substrate_actions,
            remediation_item_count: release_ticket_handoff_work_order_readiness_summary
                .remediation_item_count,
            review_required_packages: release_ticket_handoff_work_order_readiness_summary
                .review_required_packages,
            operator_required_packages: release_ticket_handoff_work_order_readiness_summary
                .operator_required_packages,
            blocked_packages: release_ticket_handoff_work_order_readiness_summary.blocked_packages,
            first_guardrail_key: release_ticket_handoff_execution_work_order_guardrail_summary
                .first_guardrail_key
                .clone(),
            first_guardrail_work_order_key:
                release_ticket_handoff_execution_work_order_guardrail_summary
                    .first_work_order_key
                    .clone(),
            first_release_blocker_work_order_key:
                release_ticket_handoff_execution_work_order_guardrail_summary
                    .first_release_blocker_work_order_key
                    .clone(),
            first_operator_handoff_work_order_key:
                release_ticket_handoff_execution_work_order_guardrail_summary
                    .first_operator_handoff_work_order_key
                    .clone(),
            first_review_gate_work_order_key:
                release_ticket_handoff_execution_work_order_guardrail_summary
                    .first_review_gate_work_order_key
                    .clone(),
            first_ready_to_execute_work_order_key:
                release_ticket_handoff_execution_work_order_guardrail_summary
                    .first_ready_to_execute_work_order_key
                    .clone(),
            first_guardrail_kind: release_ticket_handoff_execution_work_order_guardrail_summary
                .first_guardrail_kind,
            first_handoff_lane: release_ticket_handoff_execution_work_order_guardrail_summary
                .first_handoff_lane,
            first_check_kind: release_ticket_handoff_execution_work_order_guardrail_summary
                .first_check_kind,
            first_check_status: release_ticket_handoff_execution_work_order_guardrail_summary
                .first_check_status,
            next_work_order_key: next_guardrail_work_order_key.or_else(|| {
                release_ticket_handoff_work_order_readiness_summary
                    .next_work_order_key
                    .clone()
            }),
            next_execution_slot_key: release_ticket_handoff_execution_work_order_guardrail_summary
                .next_execution_slot_key
                .clone()
                .or_else(|| {
                    release_ticket_handoff_work_order_readiness_summary
                        .next_execution_slot_key
                        .clone()
                }),
            next_packet_key: release_ticket_handoff_execution_work_order_guardrail_summary
                .next_packet_key
                .clone()
                .or_else(|| {
                    release_ticket_handoff_work_order_readiness_summary
                        .next_packet_key
                        .clone()
                }),
            next_ticket_key: release_ticket_handoff_execution_work_order_guardrail_summary
                .next_ticket_key
                .clone()
                .or_else(|| {
                    release_ticket_handoff_work_order_readiness_summary
                        .next_ticket_key
                        .clone()
                }),
            next_dispatch_key: release_ticket_handoff_work_order_readiness_summary
                .next_dispatch_key
                .clone(),
            next_task_key: release_ticket_handoff_work_order_readiness_summary
                .next_task_key
                .clone(),
            next_slot_key: release_ticket_handoff_work_order_readiness_summary
                .next_slot_key
                .clone(),
            next_check_kind: next_guardrail_check_kind
                .or(release_ticket_handoff_work_order_readiness_summary.next_check_kind),
            next_check_status: next_guardrail_check_status
                .or(release_ticket_handoff_work_order_readiness_summary.next_check_status),
            next_package_kind: release_ticket_handoff_work_order_readiness_summary
                .next_package_kind,
            next_handoff_status: release_ticket_handoff_work_order_readiness_summary
                .next_handoff_status,
            next_handoff_lane: next_guardrail_handoff_lane
                .or(release_ticket_handoff_work_order_readiness_summary.next_handoff_lane),
            packet_status: release_ticket_handoff_work_order_readiness_summary.packet_status,
            execution_status: release_ticket_handoff_work_order_readiness_summary.execution_status,
            release_packet_ready: release_ticket_handoff_work_order_readiness_summary
                .release_packet_ready,
            release_execution_ready: release_ticket_handoff_work_order_readiness_summary
                .release_execution_ready,
            release_tasks_ready: release_ticket_handoff_work_order_readiness_summary
                .release_tasks_ready,
            release_dispatch_ready: release_ticket_handoff_work_order_readiness_summary
                .release_dispatch_ready,
            release_ticket_ready: release_ticket_handoff_work_order_readiness_summary
                .release_ticket_ready,
            release_handoff_ready: release_ticket_handoff_work_order_readiness_summary
                .release_handoff_ready,
            release_handoff_execution_ready: release_ticket_handoff_work_order_readiness_summary
                .release_handoff_execution_ready,
            release_ticket_handoff_execution_ready:
                release_ticket_handoff_work_order_readiness_summary
                    .release_ticket_handoff_execution_ready,
            release_handoff_execution_work_orders_ready:
                release_ticket_handoff_work_order_readiness_summary
                    .release_handoff_execution_work_orders_ready,
            release_ticket_handoff_work_orders_ready:
                release_ticket_handoff_work_order_readiness_summary
                    .release_ticket_handoff_work_orders_ready,
            release_handoff_execution_work_order_guardrails_ready,
            release_ticket_handoff_work_order_guardrails_ready,
            release_ticket_handoff_work_order_readiness_summary,
            release_ticket_handoff_execution_work_order_guardrail_summary,
        }
    }

    pub fn has_guardrails(&self) -> bool {
        self.total_guardrails > 0
    }

    pub fn ready_for_release_ticket_handoff_work_order_guardrails(&self) -> bool {
        self.execution_status == IntegrationMeshReleaseReadinessStatus::Ready
            && self.release_packet_ready
            && self.release_execution_ready
            && self.release_tasks_ready
            && self.release_dispatch_ready
            && self.release_ticket_ready
            && self.release_handoff_ready
            && self.release_handoff_execution_ready
            && self.release_ticket_handoff_execution_ready
            && self.release_handoff_execution_work_orders_ready
            && self.release_ticket_handoff_work_orders_ready
            && self.release_handoff_execution_work_order_guardrails_ready
            && self.release_ticket_handoff_work_order_guardrails_ready
            && !self.requires_attention()
    }

    pub fn has_blockers(&self) -> bool {
        self.release_blocker_guardrails > 0
            || self.release_blocking_guardrails > 0
            || self
                .release_ticket_handoff_work_order_readiness_summary
                .has_blockers()
            || self
                .release_ticket_handoff_execution_work_order_guardrail_summary
                .has_release_blockers()
    }

    pub fn has_review_work(&self) -> bool {
        self.review_gate_guardrails > 0
            || self.review_required_guardrails > 0
            || self
                .release_ticket_handoff_work_order_readiness_summary
                .has_review_work()
            || self
                .release_ticket_handoff_execution_work_order_guardrail_summary
                .has_review_gates()
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_handoff_guardrails > 0
            || self.operator_required_guardrails > 0
            || self
                .release_ticket_handoff_work_order_readiness_summary
                .needs_operator()
            || self
                .release_ticket_handoff_execution_work_order_guardrail_summary
                .needs_operator()
    }

    pub fn requires_execution(&self) -> bool {
        self.execution_required_work_orders > 0 || self.execution_required_guardrails > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.requires_execution()
            || self.dispatch_required_work_orders > 0
            || self.dispatch_required_guardrails > 0
            || self.has_blockers()
            || self.has_review_work()
            || self.needs_operator()
            || self
                .release_ticket_handoff_work_order_readiness_summary
                .requires_attention()
            || self
                .release_ticket_handoff_execution_work_order_guardrail_summary
                .requires_attention()
            || !self.release_ticket_handoff_work_order_guardrails_ready
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffGuardrailAuditReadinessSummary {
    pub release_ticket_handoff_work_order_guardrail_readiness_summary:
        IntegrationMeshReleaseTicketHandoffWorkOrderGuardrailReadinessSummary,
    pub release_ticket_handoff_execution_work_order_guardrail_audit_summary:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditSummary,
    pub total_tickets: usize,
    pub ready_tickets: usize,
    pub blocked_tickets: usize,
    pub total_work_orders: usize,
    pub ready_work_orders: usize,
    pub blocked_work_orders: usize,
    pub review_required_work_orders: usize,
    pub operator_required_work_orders: usize,
    pub dispatch_required_work_orders: usize,
    pub execution_required_work_orders: usize,
    pub total_guardrails: usize,
    pub release_blocker_guardrails: usize,
    pub review_gate_guardrails: usize,
    pub ready_to_execute_guardrails: usize,
    pub total_audit_rows: usize,
    pub release_blocker_audit_rows: usize,
    pub operator_handoff_audit_rows: usize,
    pub review_gate_audit_rows: usize,
    pub ready_to_execute_audit_rows: usize,
    pub release_blocking_audit_rows: usize,
    pub operator_required_audit_rows: usize,
    pub review_required_audit_rows: usize,
    pub dispatch_required_audit_rows: usize,
    pub execution_required_audit_rows: usize,
    pub queued_substrate_actions: usize,
    pub remediation_item_count: usize,
    pub review_required_packages: usize,
    pub operator_required_packages: usize,
    pub blocked_packages: usize,
    pub first_audit_key: Option<String>,
    pub first_audit_guardrail_key: Option<String>,
    pub first_audit_work_order_key: Option<String>,
    pub first_release_blocker_audit_work_order_key: Option<String>,
    pub first_operator_handoff_audit_work_order_key: Option<String>,
    pub first_review_gate_audit_work_order_key: Option<String>,
    pub first_ready_to_execute_audit_work_order_key: Option<String>,
    pub first_audit_severity:
        Option<IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditSeverity>,
    pub first_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub first_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub first_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_work_order_key: Option<String>,
    pub next_execution_slot_key: Option<String>,
    pub next_packet_key: Option<String>,
    pub next_ticket_key: Option<String>,
    pub next_dispatch_key: Option<String>,
    pub next_task_key: Option<String>,
    pub next_slot_key: Option<String>,
    pub next_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub next_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_package_kind: Option<IntegrationMeshReadinessHandoffKind>,
    pub next_handoff_status: Option<IntegrationMeshReadinessHandoffStatus>,
    pub next_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub packet_status: IntegrationMeshReleaseReadinessStatus,
    pub execution_status: IntegrationMeshReleaseReadinessStatus,
    pub release_packet_ready: bool,
    pub release_execution_ready: bool,
    pub release_tasks_ready: bool,
    pub release_dispatch_ready: bool,
    pub release_ticket_ready: bool,
    pub release_handoff_ready: bool,
    pub release_handoff_execution_ready: bool,
    pub release_ticket_handoff_execution_ready: bool,
    pub release_handoff_execution_work_orders_ready: bool,
    pub release_ticket_handoff_work_orders_ready: bool,
    pub release_handoff_execution_work_order_guardrails_ready: bool,
    pub release_ticket_handoff_work_order_guardrails_ready: bool,
    pub release_handoff_execution_work_order_guardrail_audit_ready: bool,
    pub release_ticket_handoff_guardrail_audit_ready: bool,
}

impl IntegrationMeshReleaseTicketHandoffGuardrailAuditReadinessSummary {
    pub fn from_summaries(
        release_ticket_handoff_work_order_guardrail_readiness_summary: IntegrationMeshReleaseTicketHandoffWorkOrderGuardrailReadinessSummary,
        release_ticket_handoff_execution_work_order_guardrail_audit_summary: IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditSummary,
    ) -> Self {
        let release_handoff_execution_work_order_guardrail_audit_ready =
            release_ticket_handoff_execution_work_order_guardrail_audit_summary
                .release_handoff_execution_work_order_guardrail_audit_ready;
        let release_ticket_handoff_guardrail_audit_ready =
            release_ticket_handoff_work_order_guardrail_readiness_summary
                .ready_for_release_ticket_handoff_work_order_guardrails()
                && release_handoff_execution_work_order_guardrail_audit_ready;
        let next_audit_work_order_key =
            if release_ticket_handoff_execution_work_order_guardrail_audit_summary
                .requires_attention()
            {
                release_ticket_handoff_execution_work_order_guardrail_audit_summary
                    .first_work_order_key
                    .clone()
            } else {
                None
            };
        let next_audit_check_kind =
            if release_ticket_handoff_execution_work_order_guardrail_audit_summary
                .requires_attention()
            {
                release_ticket_handoff_execution_work_order_guardrail_audit_summary.first_check_kind
            } else {
                None
            };
        let next_audit_check_status =
            if release_ticket_handoff_execution_work_order_guardrail_audit_summary
                .requires_attention()
            {
                release_ticket_handoff_execution_work_order_guardrail_audit_summary
                    .first_check_status
            } else {
                None
            };
        let next_audit_handoff_lane =
            if release_ticket_handoff_execution_work_order_guardrail_audit_summary
                .requires_attention()
            {
                release_ticket_handoff_execution_work_order_guardrail_audit_summary
                    .first_handoff_lane
            } else {
                None
            };

        Self {
            total_tickets: release_ticket_handoff_work_order_guardrail_readiness_summary
                .total_tickets,
            ready_tickets: release_ticket_handoff_work_order_guardrail_readiness_summary
                .ready_tickets,
            blocked_tickets: release_ticket_handoff_work_order_guardrail_readiness_summary
                .blocked_tickets,
            total_work_orders: release_ticket_handoff_work_order_guardrail_readiness_summary
                .total_work_orders,
            ready_work_orders: release_ticket_handoff_work_order_guardrail_readiness_summary
                .ready_work_orders,
            blocked_work_orders: release_ticket_handoff_work_order_guardrail_readiness_summary
                .blocked_work_orders,
            review_required_work_orders:
                release_ticket_handoff_work_order_guardrail_readiness_summary
                    .review_required_work_orders,
            operator_required_work_orders:
                release_ticket_handoff_work_order_guardrail_readiness_summary
                    .operator_required_work_orders,
            dispatch_required_work_orders:
                release_ticket_handoff_work_order_guardrail_readiness_summary
                    .dispatch_required_work_orders,
            execution_required_work_orders:
                release_ticket_handoff_work_order_guardrail_readiness_summary
                    .execution_required_work_orders,
            total_guardrails: release_ticket_handoff_work_order_guardrail_readiness_summary
                .total_guardrails,
            release_blocker_guardrails:
                release_ticket_handoff_work_order_guardrail_readiness_summary
                    .release_blocker_guardrails,
            review_gate_guardrails: release_ticket_handoff_work_order_guardrail_readiness_summary
                .review_gate_guardrails,
            ready_to_execute_guardrails:
                release_ticket_handoff_work_order_guardrail_readiness_summary
                    .ready_to_execute_guardrails,
            total_audit_rows: release_ticket_handoff_execution_work_order_guardrail_audit_summary
                .total_audit_rows,
            release_blocker_audit_rows:
                release_ticket_handoff_execution_work_order_guardrail_audit_summary
                    .release_blocker_rows,
            operator_handoff_audit_rows:
                release_ticket_handoff_execution_work_order_guardrail_audit_summary
                    .operator_handoff_rows,
            review_gate_audit_rows:
                release_ticket_handoff_execution_work_order_guardrail_audit_summary.review_gate_rows,
            ready_to_execute_audit_rows:
                release_ticket_handoff_execution_work_order_guardrail_audit_summary
                    .ready_to_execute_rows,
            release_blocking_audit_rows:
                release_ticket_handoff_execution_work_order_guardrail_audit_summary
                    .release_blocking_rows,
            operator_required_audit_rows:
                release_ticket_handoff_execution_work_order_guardrail_audit_summary
                    .operator_required_rows,
            review_required_audit_rows:
                release_ticket_handoff_execution_work_order_guardrail_audit_summary
                    .review_required_rows,
            dispatch_required_audit_rows:
                release_ticket_handoff_execution_work_order_guardrail_audit_summary
                    .dispatch_required_rows,
            execution_required_audit_rows:
                release_ticket_handoff_execution_work_order_guardrail_audit_summary
                    .execution_required_rows,
            queued_substrate_actions: release_ticket_handoff_work_order_guardrail_readiness_summary
                .queued_substrate_actions,
            remediation_item_count: release_ticket_handoff_work_order_guardrail_readiness_summary
                .remediation_item_count,
            review_required_packages: release_ticket_handoff_work_order_guardrail_readiness_summary
                .review_required_packages,
            operator_required_packages:
                release_ticket_handoff_work_order_guardrail_readiness_summary
                    .operator_required_packages,
            blocked_packages: release_ticket_handoff_work_order_guardrail_readiness_summary
                .blocked_packages,
            first_audit_key: release_ticket_handoff_execution_work_order_guardrail_audit_summary
                .first_audit_key
                .clone(),
            first_audit_guardrail_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_summary
                    .first_guardrail_key
                    .clone(),
            first_audit_work_order_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_summary
                    .first_work_order_key
                    .clone(),
            first_release_blocker_audit_work_order_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_summary
                    .first_release_blocker_work_order_key
                    .clone(),
            first_operator_handoff_audit_work_order_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_summary
                    .first_operator_handoff_work_order_key
                    .clone(),
            first_review_gate_audit_work_order_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_summary
                    .first_review_gate_work_order_key
                    .clone(),
            first_ready_to_execute_audit_work_order_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_summary
                    .first_ready_to_execute_work_order_key
                    .clone(),
            first_audit_severity:
                release_ticket_handoff_execution_work_order_guardrail_audit_summary.first_severity,
            first_handoff_lane: release_ticket_handoff_execution_work_order_guardrail_audit_summary
                .first_handoff_lane,
            first_check_kind: release_ticket_handoff_execution_work_order_guardrail_audit_summary
                .first_check_kind,
            first_check_status: release_ticket_handoff_execution_work_order_guardrail_audit_summary
                .first_check_status,
            next_work_order_key: next_audit_work_order_key.or_else(|| {
                release_ticket_handoff_work_order_guardrail_readiness_summary
                    .next_work_order_key
                    .clone()
            }),
            next_execution_slot_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_summary
                    .next_execution_slot_key
                    .clone()
                    .or_else(|| {
                        release_ticket_handoff_work_order_guardrail_readiness_summary
                            .next_execution_slot_key
                            .clone()
                    }),
            next_packet_key: release_ticket_handoff_execution_work_order_guardrail_audit_summary
                .next_packet_key
                .clone()
                .or_else(|| {
                    release_ticket_handoff_work_order_guardrail_readiness_summary
                        .next_packet_key
                        .clone()
                }),
            next_ticket_key: release_ticket_handoff_execution_work_order_guardrail_audit_summary
                .next_ticket_key
                .clone()
                .or_else(|| {
                    release_ticket_handoff_work_order_guardrail_readiness_summary
                        .next_ticket_key
                        .clone()
                }),
            next_dispatch_key: release_ticket_handoff_work_order_guardrail_readiness_summary
                .next_dispatch_key
                .clone(),
            next_task_key: release_ticket_handoff_work_order_guardrail_readiness_summary
                .next_task_key
                .clone(),
            next_slot_key: release_ticket_handoff_work_order_guardrail_readiness_summary
                .next_slot_key
                .clone(),
            next_check_kind: next_audit_check_kind
                .or(release_ticket_handoff_work_order_guardrail_readiness_summary.next_check_kind),
            next_check_status: next_audit_check_status.or(
                release_ticket_handoff_work_order_guardrail_readiness_summary.next_check_status,
            ),
            next_package_kind: release_ticket_handoff_work_order_guardrail_readiness_summary
                .next_package_kind,
            next_handoff_status: release_ticket_handoff_work_order_guardrail_readiness_summary
                .next_handoff_status,
            next_handoff_lane: next_audit_handoff_lane.or(
                release_ticket_handoff_work_order_guardrail_readiness_summary.next_handoff_lane,
            ),
            packet_status: release_ticket_handoff_work_order_guardrail_readiness_summary
                .packet_status,
            execution_status: release_ticket_handoff_work_order_guardrail_readiness_summary
                .execution_status,
            release_packet_ready: release_ticket_handoff_work_order_guardrail_readiness_summary
                .release_packet_ready,
            release_execution_ready: release_ticket_handoff_work_order_guardrail_readiness_summary
                .release_execution_ready,
            release_tasks_ready: release_ticket_handoff_work_order_guardrail_readiness_summary
                .release_tasks_ready,
            release_dispatch_ready: release_ticket_handoff_work_order_guardrail_readiness_summary
                .release_dispatch_ready,
            release_ticket_ready: release_ticket_handoff_work_order_guardrail_readiness_summary
                .release_ticket_ready,
            release_handoff_ready: release_ticket_handoff_work_order_guardrail_readiness_summary
                .release_handoff_ready,
            release_handoff_execution_ready:
                release_ticket_handoff_work_order_guardrail_readiness_summary
                    .release_handoff_execution_ready,
            release_ticket_handoff_execution_ready:
                release_ticket_handoff_work_order_guardrail_readiness_summary
                    .release_ticket_handoff_execution_ready,
            release_handoff_execution_work_orders_ready:
                release_ticket_handoff_work_order_guardrail_readiness_summary
                    .release_handoff_execution_work_orders_ready,
            release_ticket_handoff_work_orders_ready:
                release_ticket_handoff_work_order_guardrail_readiness_summary
                    .release_ticket_handoff_work_orders_ready,
            release_handoff_execution_work_order_guardrails_ready:
                release_ticket_handoff_work_order_guardrail_readiness_summary
                    .release_handoff_execution_work_order_guardrails_ready,
            release_ticket_handoff_work_order_guardrails_ready:
                release_ticket_handoff_work_order_guardrail_readiness_summary
                    .release_ticket_handoff_work_order_guardrails_ready,
            release_handoff_execution_work_order_guardrail_audit_ready,
            release_ticket_handoff_guardrail_audit_ready,
            release_ticket_handoff_work_order_guardrail_readiness_summary,
            release_ticket_handoff_execution_work_order_guardrail_audit_summary,
        }
    }

    pub fn has_audit_rows(&self) -> bool {
        self.total_audit_rows > 0
    }

    pub fn ready_for_release_ticket_handoff_guardrail_audit(&self) -> bool {
        self.execution_status == IntegrationMeshReleaseReadinessStatus::Ready
            && self.release_packet_ready
            && self.release_execution_ready
            && self.release_tasks_ready
            && self.release_dispatch_ready
            && self.release_ticket_ready
            && self.release_handoff_ready
            && self.release_handoff_execution_ready
            && self.release_ticket_handoff_execution_ready
            && self.release_handoff_execution_work_orders_ready
            && self.release_ticket_handoff_work_orders_ready
            && self.release_handoff_execution_work_order_guardrails_ready
            && self.release_ticket_handoff_work_order_guardrails_ready
            && self.release_handoff_execution_work_order_guardrail_audit_ready
            && self.release_ticket_handoff_guardrail_audit_ready
            && !self.requires_attention()
    }

    pub fn has_blockers(&self) -> bool {
        self.release_blocker_audit_rows > 0
            || self.release_blocking_audit_rows > 0
            || self
                .release_ticket_handoff_work_order_guardrail_readiness_summary
                .has_blockers()
            || self
                .release_ticket_handoff_execution_work_order_guardrail_audit_summary
                .has_release_blockers()
    }

    pub fn has_review_work(&self) -> bool {
        self.review_gate_audit_rows > 0
            || self.review_required_audit_rows > 0
            || self
                .release_ticket_handoff_work_order_guardrail_readiness_summary
                .has_review_work()
            || self
                .release_ticket_handoff_execution_work_order_guardrail_audit_summary
                .has_review_gates()
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_handoff_audit_rows > 0
            || self.operator_required_audit_rows > 0
            || self
                .release_ticket_handoff_work_order_guardrail_readiness_summary
                .needs_operator()
            || self
                .release_ticket_handoff_execution_work_order_guardrail_audit_summary
                .needs_operator()
    }

    pub fn requires_execution(&self) -> bool {
        self.execution_required_work_orders > 0 || self.execution_required_audit_rows > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.requires_execution()
            || self.dispatch_required_work_orders > 0
            || self.dispatch_required_audit_rows > 0
            || self.has_blockers()
            || self.has_review_work()
            || self.needs_operator()
            || self
                .release_ticket_handoff_work_order_guardrail_readiness_summary
                .requires_attention()
            || self
                .release_ticket_handoff_execution_work_order_guardrail_audit_summary
                .requires_attention()
            || !self.release_ticket_handoff_guardrail_audit_ready
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceReadinessSummary {
    pub release_ticket_handoff_guardrail_audit_readiness_summary:
        IntegrationMeshReleaseTicketHandoffGuardrailAuditReadinessSummary,
    pub release_ticket_handoff_execution_work_order_guardrail_audit_clearance_summary:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceSummary,
    pub total_tickets: usize,
    pub ready_tickets: usize,
    pub blocked_tickets: usize,
    pub total_work_orders: usize,
    pub ready_work_orders: usize,
    pub blocked_work_orders: usize,
    pub review_required_work_orders: usize,
    pub operator_required_work_orders: usize,
    pub dispatch_required_work_orders: usize,
    pub execution_required_work_orders: usize,
    pub total_guardrails: usize,
    pub release_blocker_guardrails: usize,
    pub review_gate_guardrails: usize,
    pub ready_to_execute_guardrails: usize,
    pub total_audit_rows: usize,
    pub release_blocker_audit_rows: usize,
    pub operator_handoff_audit_rows: usize,
    pub review_gate_audit_rows: usize,
    pub ready_to_execute_audit_rows: usize,
    pub total_clearance_rows: usize,
    pub release_blocked_clearances: usize,
    pub operator_handoff_clearances: usize,
    pub review_gate_clearances: usize,
    pub cleared_clearances: usize,
    pub release_blocking_clearances: usize,
    pub operator_required_clearances: usize,
    pub review_required_clearances: usize,
    pub dispatch_required_clearances: usize,
    pub execution_required_clearances: usize,
    pub queued_substrate_actions: usize,
    pub remediation_item_count: usize,
    pub review_required_packages: usize,
    pub operator_required_packages: usize,
    pub blocked_packages: usize,
    pub first_clearance_key: Option<String>,
    pub first_clearance_audit_key: Option<String>,
    pub first_clearance_guardrail_key: Option<String>,
    pub first_clearance_work_order_key: Option<String>,
    pub first_release_blocked_clearance_work_order_key: Option<String>,
    pub first_operator_handoff_clearance_work_order_key: Option<String>,
    pub first_review_required_clearance_work_order_key: Option<String>,
    pub first_cleared_work_order_key: Option<String>,
    pub first_clearance_kind:
        Option<IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind>,
    pub first_audit_severity:
        Option<IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditSeverity>,
    pub first_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub first_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub first_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_work_order_key: Option<String>,
    pub next_execution_slot_key: Option<String>,
    pub next_packet_key: Option<String>,
    pub next_ticket_key: Option<String>,
    pub next_dispatch_key: Option<String>,
    pub next_task_key: Option<String>,
    pub next_slot_key: Option<String>,
    pub next_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub next_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_package_kind: Option<IntegrationMeshReadinessHandoffKind>,
    pub next_handoff_status: Option<IntegrationMeshReadinessHandoffStatus>,
    pub next_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub packet_status: IntegrationMeshReleaseReadinessStatus,
    pub execution_status: IntegrationMeshReleaseReadinessStatus,
    pub release_packet_ready: bool,
    pub release_execution_ready: bool,
    pub release_tasks_ready: bool,
    pub release_dispatch_ready: bool,
    pub release_ticket_ready: bool,
    pub release_handoff_ready: bool,
    pub release_handoff_execution_ready: bool,
    pub release_ticket_handoff_execution_ready: bool,
    pub release_handoff_execution_work_orders_ready: bool,
    pub release_ticket_handoff_work_orders_ready: bool,
    pub release_handoff_execution_work_order_guardrails_ready: bool,
    pub release_ticket_handoff_work_order_guardrails_ready: bool,
    pub release_handoff_execution_work_order_guardrail_audit_ready: bool,
    pub release_ticket_handoff_guardrail_audit_ready: bool,
    pub release_handoff_execution_work_order_guardrail_audit_clearance_ready: bool,
    pub release_ticket_handoff_guardrail_audit_clearance_ready: bool,
}

impl IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceReadinessSummary {
    pub fn from_summaries(
        release_ticket_handoff_guardrail_audit_readiness_summary: IntegrationMeshReleaseTicketHandoffGuardrailAuditReadinessSummary,
        release_ticket_handoff_execution_work_order_guardrail_audit_clearance_summary: IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceSummary,
    ) -> Self {
        let release_handoff_execution_work_order_guardrail_audit_clearance_ready =
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_summary
                .release_handoff_execution_work_order_guardrail_audit_clearance_ready;
        let release_ticket_handoff_guardrail_audit_clearance_ready =
            release_ticket_handoff_guardrail_audit_readiness_summary
                .ready_for_release_ticket_handoff_guardrail_audit()
                && release_handoff_execution_work_order_guardrail_audit_clearance_ready;
        let next_clearance_work_order_key =
            if release_ticket_handoff_execution_work_order_guardrail_audit_clearance_summary
                .requires_attention()
            {
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_summary
                    .next_work_order_key
                    .clone()
                    .or_else(|| {
                        release_ticket_handoff_execution_work_order_guardrail_audit_clearance_summary
                            .first_work_order_key
                            .clone()
                    })
            } else {
                None
            };
        let next_clearance_check_kind =
            if release_ticket_handoff_execution_work_order_guardrail_audit_clearance_summary
                .requires_attention()
            {
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_summary
                    .first_check_kind
            } else {
                None
            };
        let next_clearance_check_status =
            if release_ticket_handoff_execution_work_order_guardrail_audit_clearance_summary
                .requires_attention()
            {
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_summary
                    .first_check_status
            } else {
                None
            };
        let next_clearance_handoff_lane =
            if release_ticket_handoff_execution_work_order_guardrail_audit_clearance_summary
                .requires_attention()
            {
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_summary
                    .first_handoff_lane
            } else {
                None
            };

        Self {
            total_tickets: release_ticket_handoff_guardrail_audit_readiness_summary.total_tickets,
            ready_tickets: release_ticket_handoff_guardrail_audit_readiness_summary.ready_tickets,
            blocked_tickets: release_ticket_handoff_guardrail_audit_readiness_summary
                .blocked_tickets,
            total_work_orders: release_ticket_handoff_guardrail_audit_readiness_summary
                .total_work_orders,
            ready_work_orders: release_ticket_handoff_guardrail_audit_readiness_summary
                .ready_work_orders,
            blocked_work_orders: release_ticket_handoff_guardrail_audit_readiness_summary
                .blocked_work_orders,
            review_required_work_orders: release_ticket_handoff_guardrail_audit_readiness_summary
                .review_required_work_orders,
            operator_required_work_orders: release_ticket_handoff_guardrail_audit_readiness_summary
                .operator_required_work_orders,
            dispatch_required_work_orders: release_ticket_handoff_guardrail_audit_readiness_summary
                .dispatch_required_work_orders,
            execution_required_work_orders:
                release_ticket_handoff_guardrail_audit_readiness_summary
                    .execution_required_work_orders,
            total_guardrails: release_ticket_handoff_guardrail_audit_readiness_summary
                .total_guardrails,
            release_blocker_guardrails: release_ticket_handoff_guardrail_audit_readiness_summary
                .release_blocker_guardrails,
            review_gate_guardrails: release_ticket_handoff_guardrail_audit_readiness_summary
                .review_gate_guardrails,
            ready_to_execute_guardrails: release_ticket_handoff_guardrail_audit_readiness_summary
                .ready_to_execute_guardrails,
            total_audit_rows: release_ticket_handoff_guardrail_audit_readiness_summary
                .total_audit_rows,
            release_blocker_audit_rows: release_ticket_handoff_guardrail_audit_readiness_summary
                .release_blocker_audit_rows,
            operator_handoff_audit_rows: release_ticket_handoff_guardrail_audit_readiness_summary
                .operator_handoff_audit_rows,
            review_gate_audit_rows: release_ticket_handoff_guardrail_audit_readiness_summary
                .review_gate_audit_rows,
            ready_to_execute_audit_rows: release_ticket_handoff_guardrail_audit_readiness_summary
                .ready_to_execute_audit_rows,
            total_clearance_rows:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_summary
                    .total_clearance_rows,
            release_blocked_clearances:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_summary
                    .release_blocked_clearances,
            operator_handoff_clearances:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_summary
                    .operator_handoff_clearances,
            review_gate_clearances:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_summary
                    .review_gate_clearances,
            cleared_clearances:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_summary
                    .cleared_clearances,
            release_blocking_clearances:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_summary
                    .release_blocking_clearances,
            operator_required_clearances:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_summary
                    .operator_required_clearances,
            review_required_clearances:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_summary
                    .review_required_clearances,
            dispatch_required_clearances:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_summary
                    .dispatch_required_clearances,
            execution_required_clearances:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_summary
                    .execution_required_clearances,
            queued_substrate_actions: release_ticket_handoff_guardrail_audit_readiness_summary
                .queued_substrate_actions,
            remediation_item_count: release_ticket_handoff_guardrail_audit_readiness_summary
                .remediation_item_count,
            review_required_packages: release_ticket_handoff_guardrail_audit_readiness_summary
                .review_required_packages,
            operator_required_packages: release_ticket_handoff_guardrail_audit_readiness_summary
                .operator_required_packages,
            blocked_packages: release_ticket_handoff_guardrail_audit_readiness_summary
                .blocked_packages,
            first_clearance_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_summary
                    .first_clearance_key
                    .clone(),
            first_clearance_audit_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_summary
                    .first_audit_key
                    .clone(),
            first_clearance_guardrail_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_summary
                    .first_guardrail_key
                    .clone(),
            first_clearance_work_order_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_summary
                    .first_work_order_key
                    .clone(),
            first_release_blocked_clearance_work_order_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_summary
                    .first_release_blocked_work_order_key
                    .clone(),
            first_operator_handoff_clearance_work_order_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_summary
                    .first_operator_handoff_work_order_key
                    .clone(),
            first_review_required_clearance_work_order_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_summary
                    .first_review_required_work_order_key
                    .clone(),
            first_cleared_work_order_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_summary
                    .first_cleared_work_order_key
                    .clone(),
            first_clearance_kind:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_summary
                    .first_clearance_kind,
            first_audit_severity:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_summary
                    .first_audit_severity,
            first_handoff_lane:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_summary
                    .first_handoff_lane,
            first_check_kind:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_summary
                    .first_check_kind,
            first_check_status:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_summary
                    .first_check_status,
            next_work_order_key: next_clearance_work_order_key.or_else(|| {
                release_ticket_handoff_guardrail_audit_readiness_summary
                    .next_work_order_key
                    .clone()
            }),
            next_execution_slot_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_summary
                    .next_execution_slot_key
                    .clone()
                    .or_else(|| {
                        release_ticket_handoff_guardrail_audit_readiness_summary
                            .next_execution_slot_key
                            .clone()
                    }),
            next_packet_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_summary
                    .next_packet_key
                    .clone()
                    .or_else(|| {
                        release_ticket_handoff_guardrail_audit_readiness_summary
                            .next_packet_key
                            .clone()
                    }),
            next_ticket_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_summary
                    .next_ticket_key
                    .clone()
                    .or_else(|| {
                        release_ticket_handoff_guardrail_audit_readiness_summary
                            .next_ticket_key
                            .clone()
                    }),
            next_dispatch_key: release_ticket_handoff_guardrail_audit_readiness_summary
                .next_dispatch_key
                .clone(),
            next_task_key: release_ticket_handoff_guardrail_audit_readiness_summary
                .next_task_key
                .clone(),
            next_slot_key: release_ticket_handoff_guardrail_audit_readiness_summary
                .next_slot_key
                .clone(),
            next_check_kind: next_clearance_check_kind
                .or(release_ticket_handoff_guardrail_audit_readiness_summary.next_check_kind),
            next_check_status: next_clearance_check_status
                .or(release_ticket_handoff_guardrail_audit_readiness_summary.next_check_status),
            next_package_kind: release_ticket_handoff_guardrail_audit_readiness_summary
                .next_package_kind,
            next_handoff_status: release_ticket_handoff_guardrail_audit_readiness_summary
                .next_handoff_status,
            next_handoff_lane: next_clearance_handoff_lane
                .or(release_ticket_handoff_guardrail_audit_readiness_summary.next_handoff_lane),
            packet_status: release_ticket_handoff_guardrail_audit_readiness_summary.packet_status,
            execution_status: release_ticket_handoff_guardrail_audit_readiness_summary
                .execution_status,
            release_packet_ready: release_ticket_handoff_guardrail_audit_readiness_summary
                .release_packet_ready,
            release_execution_ready: release_ticket_handoff_guardrail_audit_readiness_summary
                .release_execution_ready,
            release_tasks_ready: release_ticket_handoff_guardrail_audit_readiness_summary
                .release_tasks_ready,
            release_dispatch_ready: release_ticket_handoff_guardrail_audit_readiness_summary
                .release_dispatch_ready,
            release_ticket_ready: release_ticket_handoff_guardrail_audit_readiness_summary
                .release_ticket_ready,
            release_handoff_ready: release_ticket_handoff_guardrail_audit_readiness_summary
                .release_handoff_ready,
            release_handoff_execution_ready:
                release_ticket_handoff_guardrail_audit_readiness_summary
                    .release_handoff_execution_ready,
            release_ticket_handoff_execution_ready:
                release_ticket_handoff_guardrail_audit_readiness_summary
                    .release_ticket_handoff_execution_ready,
            release_handoff_execution_work_orders_ready:
                release_ticket_handoff_guardrail_audit_readiness_summary
                    .release_handoff_execution_work_orders_ready,
            release_ticket_handoff_work_orders_ready:
                release_ticket_handoff_guardrail_audit_readiness_summary
                    .release_ticket_handoff_work_orders_ready,
            release_handoff_execution_work_order_guardrails_ready:
                release_ticket_handoff_guardrail_audit_readiness_summary
                    .release_handoff_execution_work_order_guardrails_ready,
            release_ticket_handoff_work_order_guardrails_ready:
                release_ticket_handoff_guardrail_audit_readiness_summary
                    .release_ticket_handoff_work_order_guardrails_ready,
            release_handoff_execution_work_order_guardrail_audit_ready:
                release_ticket_handoff_guardrail_audit_readiness_summary
                    .release_handoff_execution_work_order_guardrail_audit_ready,
            release_ticket_handoff_guardrail_audit_ready:
                release_ticket_handoff_guardrail_audit_readiness_summary
                    .release_ticket_handoff_guardrail_audit_ready,
            release_handoff_execution_work_order_guardrail_audit_clearance_ready,
            release_ticket_handoff_guardrail_audit_clearance_ready,
            release_ticket_handoff_guardrail_audit_readiness_summary,
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_summary,
        }
    }

    pub fn has_clearance_rows(&self) -> bool {
        self.total_clearance_rows > 0
    }

    pub fn ready_for_release_ticket_handoff_guardrail_audit_clearance(&self) -> bool {
        self.execution_status == IntegrationMeshReleaseReadinessStatus::Ready
            && self.release_packet_ready
            && self.release_execution_ready
            && self.release_tasks_ready
            && self.release_dispatch_ready
            && self.release_ticket_ready
            && self.release_handoff_ready
            && self.release_handoff_execution_ready
            && self.release_ticket_handoff_execution_ready
            && self.release_handoff_execution_work_orders_ready
            && self.release_ticket_handoff_work_orders_ready
            && self.release_handoff_execution_work_order_guardrails_ready
            && self.release_ticket_handoff_work_order_guardrails_ready
            && self.release_handoff_execution_work_order_guardrail_audit_ready
            && self.release_ticket_handoff_guardrail_audit_ready
            && self.release_handoff_execution_work_order_guardrail_audit_clearance_ready
            && self.release_ticket_handoff_guardrail_audit_clearance_ready
            && !self.requires_attention()
    }

    pub fn has_blockers(&self) -> bool {
        self.release_blocked_clearances > 0
            || self.release_blocking_clearances > 0
            || self
                .release_ticket_handoff_guardrail_audit_readiness_summary
                .has_blockers()
            || self
                .release_ticket_handoff_execution_work_order_guardrail_audit_clearance_summary
                .has_release_blockers()
    }

    pub fn has_review_work(&self) -> bool {
        self.review_gate_clearances > 0
            || self.review_required_clearances > 0
            || self
                .release_ticket_handoff_guardrail_audit_readiness_summary
                .has_review_work()
            || self
                .release_ticket_handoff_execution_work_order_guardrail_audit_clearance_summary
                .needs_review()
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_handoff_clearances > 0
            || self.operator_required_clearances > 0
            || self
                .release_ticket_handoff_guardrail_audit_readiness_summary
                .needs_operator()
            || self
                .release_ticket_handoff_execution_work_order_guardrail_audit_clearance_summary
                .needs_operator()
    }

    pub fn requires_execution(&self) -> bool {
        self.execution_required_work_orders > 0 || self.execution_required_clearances > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.requires_execution()
            || self.dispatch_required_work_orders > 0
            || self.dispatch_required_clearances > 0
            || self.has_blockers()
            || self.has_review_work()
            || self.needs_operator()
            || self
                .release_ticket_handoff_guardrail_audit_readiness_summary
                .requires_attention()
            || self
                .release_ticket_handoff_execution_work_order_guardrail_audit_clearance_summary
                .requires_attention()
            || !self.release_ticket_handoff_guardrail_audit_clearance_ready
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionReadinessSummary {
    pub release_ticket_handoff_guardrail_audit_clearance_readiness_summary:
        IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceReadinessSummary,
    pub release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_summary:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionSummary,
    pub total_tickets: usize,
    pub ready_tickets: usize,
    pub blocked_tickets: usize,
    pub total_work_orders: usize,
    pub ready_work_orders: usize,
    pub blocked_work_orders: usize,
    pub review_required_work_orders: usize,
    pub operator_required_work_orders: usize,
    pub dispatch_required_work_orders: usize,
    pub execution_required_work_orders: usize,
    pub total_clearance_rows: usize,
    pub release_blocked_clearances: usize,
    pub operator_handoff_clearances: usize,
    pub review_gate_clearances: usize,
    pub cleared_clearances: usize,
    pub release_blocking_clearances: usize,
    pub operator_required_clearances: usize,
    pub review_required_clearances: usize,
    pub dispatch_required_clearances: usize,
    pub execution_required_clearances: usize,
    pub total_clearance_actions: usize,
    pub required_clearance_actions: usize,
    pub repair_release_blocker_actions: usize,
    pub operator_handoff_actions: usize,
    pub review_gate_actions: usize,
    pub release_clearance_actions: usize,
    pub release_blocking_actions: usize,
    pub operator_required_actions: usize,
    pub review_required_actions: usize,
    pub dispatch_required_actions: usize,
    pub execution_required_actions: usize,
    pub queued_substrate_actions: usize,
    pub remediation_item_count: usize,
    pub review_required_packages: usize,
    pub operator_required_packages: usize,
    pub blocked_packages: usize,
    pub first_clearance_key: Option<String>,
    pub first_clearance_action_key: Option<String>,
    pub first_required_clearance_action_key: Option<String>,
    pub first_repair_clearance_action_key: Option<String>,
    pub first_operator_handoff_clearance_action_key: Option<String>,
    pub first_review_gate_clearance_action_key: Option<String>,
    pub first_release_clearance_action_key: Option<String>,
    pub first_clearance_kind:
        Option<IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind>,
    pub first_clearance_action_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionKind,
    >,
    pub first_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub first_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub first_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_work_order_key: Option<String>,
    pub next_execution_slot_key: Option<String>,
    pub next_packet_key: Option<String>,
    pub next_ticket_key: Option<String>,
    pub next_dispatch_key: Option<String>,
    pub next_task_key: Option<String>,
    pub next_slot_key: Option<String>,
    pub next_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub next_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_package_kind: Option<IntegrationMeshReadinessHandoffKind>,
    pub next_handoff_status: Option<IntegrationMeshReadinessHandoffStatus>,
    pub next_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub packet_status: IntegrationMeshReleaseReadinessStatus,
    pub execution_status: IntegrationMeshReleaseReadinessStatus,
    pub release_packet_ready: bool,
    pub release_execution_ready: bool,
    pub release_tasks_ready: bool,
    pub release_dispatch_ready: bool,
    pub release_ticket_ready: bool,
    pub release_handoff_ready: bool,
    pub release_handoff_execution_ready: bool,
    pub release_ticket_handoff_execution_ready: bool,
    pub release_handoff_execution_work_orders_ready: bool,
    pub release_ticket_handoff_work_orders_ready: bool,
    pub release_handoff_execution_work_order_guardrails_ready: bool,
    pub release_ticket_handoff_work_order_guardrails_ready: bool,
    pub release_handoff_execution_work_order_guardrail_audit_ready: bool,
    pub release_ticket_handoff_guardrail_audit_ready: bool,
    pub release_handoff_execution_work_order_guardrail_audit_clearance_ready: bool,
    pub release_ticket_handoff_guardrail_audit_clearance_ready: bool,
    pub release_handoff_execution_work_order_guardrail_audit_clearance_actions_ready: bool,
    pub release_ticket_handoff_guardrail_audit_clearance_actions_ready: bool,
}

impl IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionReadinessSummary {
    pub fn from_summaries(
        release_ticket_handoff_guardrail_audit_clearance_readiness_summary: IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceReadinessSummary,
        release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_summary: IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionSummary,
    ) -> Self {
        let release_handoff_execution_work_order_guardrail_audit_clearance_actions_ready =
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_summary
                .release_handoff_execution_work_order_guardrail_audit_clearance_actions_ready;
        let release_ticket_handoff_guardrail_audit_clearance_actions_ready =
            release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                .ready_for_release_ticket_handoff_guardrail_audit_clearance()
                && release_handoff_execution_work_order_guardrail_audit_clearance_actions_ready;
        let next_action_requires_attention =
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_summary
                .requires_attention();
        let next_action_work_order_key = if next_action_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_summary
                .first_work_order_key
                .clone()
        } else {
            None
        };
        let next_action_ticket_key = if next_action_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_summary
                .first_ticket_key
                .clone()
        } else {
            None
        };
        let next_action_check_kind = if next_action_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_summary
                .first_check_kind
        } else {
            None
        };
        let next_action_check_status = if next_action_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_summary
                .first_check_status
        } else {
            None
        };
        let next_action_handoff_lane = if next_action_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_summary
                .first_handoff_lane
        } else {
            None
        };

        Self {
            total_tickets: release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                .total_tickets,
            ready_tickets: release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                .ready_tickets,
            blocked_tickets: release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                .blocked_tickets,
            total_work_orders: release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                .total_work_orders,
            ready_work_orders: release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                .ready_work_orders,
            blocked_work_orders: release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                .blocked_work_orders,
            review_required_work_orders:
                release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                    .review_required_work_orders,
            operator_required_work_orders:
                release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                    .operator_required_work_orders,
            dispatch_required_work_orders:
                release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                    .dispatch_required_work_orders,
            execution_required_work_orders:
                release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                    .execution_required_work_orders,
            total_clearance_rows:
                release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                    .total_clearance_rows,
            release_blocked_clearances:
                release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                    .release_blocked_clearances,
            operator_handoff_clearances:
                release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                    .operator_handoff_clearances,
            review_gate_clearances:
                release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                    .review_gate_clearances,
            cleared_clearances: release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                .cleared_clearances,
            release_blocking_clearances:
                release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                    .release_blocking_clearances,
            operator_required_clearances:
                release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                    .operator_required_clearances,
            review_required_clearances:
                release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                    .review_required_clearances,
            dispatch_required_clearances:
                release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                    .dispatch_required_clearances,
            execution_required_clearances:
                release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                    .execution_required_clearances,
            total_clearance_actions:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_summary
                    .total_actions,
            required_clearance_actions:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_summary
                    .required_actions,
            repair_release_blocker_actions:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_summary
                    .repair_release_blocker_actions,
            operator_handoff_actions:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_summary
                    .operator_handoff_actions,
            review_gate_actions:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_summary
                    .review_gate_actions,
            release_clearance_actions:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_summary
                    .release_clearance_actions,
            release_blocking_actions:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_summary
                    .release_blocking_actions,
            operator_required_actions:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_summary
                    .operator_required_actions,
            review_required_actions:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_summary
                    .review_required_actions,
            dispatch_required_actions:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_summary
                    .dispatch_required_actions,
            execution_required_actions:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_summary
                    .execution_required_actions,
            queued_substrate_actions:
                release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                    .queued_substrate_actions,
            remediation_item_count:
                release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                    .remediation_item_count,
            review_required_packages:
                release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                    .review_required_packages,
            operator_required_packages:
                release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                    .operator_required_packages,
            blocked_packages: release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                .blocked_packages,
            first_clearance_key: release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                .first_clearance_key
                .clone(),
            first_clearance_action_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_summary
                    .first_action_key
                    .clone(),
            first_required_clearance_action_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_summary
                    .first_required_action_key
                    .clone(),
            first_repair_clearance_action_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_summary
                    .first_repair_action_key
                    .clone(),
            first_operator_handoff_clearance_action_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_summary
                    .first_operator_handoff_action_key
                    .clone(),
            first_review_gate_clearance_action_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_summary
                    .first_review_gate_action_key
                    .clone(),
            first_release_clearance_action_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_summary
                    .first_release_clearance_action_key
                    .clone(),
            first_clearance_kind:
                release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                    .first_clearance_kind,
            first_clearance_action_kind:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_summary
                    .first_action_kind,
            first_handoff_lane: release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                .first_handoff_lane,
            first_check_kind: release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                .first_check_kind,
            first_check_status: release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                .first_check_status,
            next_work_order_key: next_action_work_order_key.or_else(|| {
                release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                    .next_work_order_key
                    .clone()
            }),
            next_execution_slot_key:
                release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                    .next_execution_slot_key
                    .clone(),
            next_packet_key: release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                .next_packet_key
                .clone(),
            next_ticket_key: next_action_ticket_key.or_else(|| {
                release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                    .next_ticket_key
                    .clone()
            }),
            next_dispatch_key: release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                .next_dispatch_key
                .clone(),
            next_task_key: release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                .next_task_key
                .clone(),
            next_slot_key: release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                .next_slot_key
                .clone(),
            next_check_kind: next_action_check_kind.or(
                release_ticket_handoff_guardrail_audit_clearance_readiness_summary.next_check_kind,
            ),
            next_check_status: next_action_check_status.or(
                release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                    .next_check_status,
            ),
            next_package_kind: release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                .next_package_kind,
            next_handoff_status: release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                .next_handoff_status,
            next_handoff_lane: next_action_handoff_lane.or(
                release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                    .next_handoff_lane,
            ),
            packet_status: release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                .packet_status,
            execution_status: release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                .execution_status,
            release_packet_ready:
                release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                    .release_packet_ready,
            release_execution_ready:
                release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                    .release_execution_ready,
            release_tasks_ready: release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                .release_tasks_ready,
            release_dispatch_ready:
                release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                    .release_dispatch_ready,
            release_ticket_ready:
                release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                    .release_ticket_ready,
            release_handoff_ready:
                release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                    .release_handoff_ready,
            release_handoff_execution_ready:
                release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                    .release_handoff_execution_ready,
            release_ticket_handoff_execution_ready:
                release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                    .release_ticket_handoff_execution_ready,
            release_handoff_execution_work_orders_ready:
                release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                    .release_handoff_execution_work_orders_ready,
            release_ticket_handoff_work_orders_ready:
                release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                    .release_ticket_handoff_work_orders_ready,
            release_handoff_execution_work_order_guardrails_ready:
                release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                    .release_handoff_execution_work_order_guardrails_ready,
            release_ticket_handoff_work_order_guardrails_ready:
                release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                    .release_ticket_handoff_work_order_guardrails_ready,
            release_handoff_execution_work_order_guardrail_audit_ready:
                release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                    .release_handoff_execution_work_order_guardrail_audit_ready,
            release_ticket_handoff_guardrail_audit_ready:
                release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                    .release_ticket_handoff_guardrail_audit_ready,
            release_handoff_execution_work_order_guardrail_audit_clearance_ready:
                release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                    .release_handoff_execution_work_order_guardrail_audit_clearance_ready,
            release_ticket_handoff_guardrail_audit_clearance_ready:
                release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                    .release_ticket_handoff_guardrail_audit_clearance_ready,
            release_handoff_execution_work_order_guardrail_audit_clearance_actions_ready,
            release_ticket_handoff_guardrail_audit_clearance_actions_ready,
            release_ticket_handoff_guardrail_audit_clearance_readiness_summary,
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_summary,
        }
    }

    pub fn has_clearance_actions(&self) -> bool {
        self.total_clearance_actions > 0
    }

    pub fn ready_for_release_ticket_handoff_guardrail_audit_clearance_actions(&self) -> bool {
        self.release_ticket_handoff_guardrail_audit_clearance_readiness_summary
            .ready_for_release_ticket_handoff_guardrail_audit_clearance()
            && self.release_handoff_execution_work_order_guardrail_audit_clearance_actions_ready
            && self.release_ticket_handoff_guardrail_audit_clearance_actions_ready
            && !self.requires_attention()
    }

    pub fn has_blockers(&self) -> bool {
        self.release_blocking_actions > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                .has_blockers()
            || self
                .release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_summary
                .has_blockers()
    }

    pub fn has_review_work(&self) -> bool {
        self.review_gate_actions > 0
            || self.review_required_actions > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                .has_review_work()
            || self
                .release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_summary
                .needs_review()
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_handoff_actions > 0
            || self.operator_required_actions > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                .needs_operator()
            || self
                .release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_summary
                .needs_operator()
    }

    pub fn requires_execution(&self) -> bool {
        self.execution_required_actions > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                .requires_execution()
    }

    pub fn requires_attention(&self) -> bool {
        self.requires_execution()
            || self.dispatch_required_actions > 0
            || self.has_blockers()
            || self.has_review_work()
            || self.needs_operator()
            || self
                .release_ticket_handoff_guardrail_audit_clearance_readiness_summary
                .requires_attention()
            || self
                .release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_summary
                .requires_attention()
            || !self.release_ticket_handoff_guardrail_audit_clearance_actions_ready
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind
{
    BlockerReview,
    OperatorReview,
    ReviewGate,
    LineageGap,
    ReleaseReady,
}

impl
    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind
{
    pub fn as_str(self) -> &'static str {
        match self {
            Self::BlockerReview => "blocker_review",
            Self::OperatorReview => "operator_review",
            Self::ReviewGate => "review_gate",
            Self::LineageGap => "lineage_gap",
            Self::ReleaseReady => "release_ready",
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReview {
    pub sequence: usize,
    pub review_key: String,
    pub evidence_sequence: usize,
    pub evidence_key: String,
    pub action_sequence: usize,
    pub action_key: String,
    pub clearance_sequence: usize,
    pub clearance_key: String,
    pub audit_key: String,
    pub guardrail_key: String,
    pub work_order_key: String,
    pub execution_slot_key: String,
    pub packet_key: String,
    pub ticket_key: String,
    pub check_sequence: usize,
    pub check_kind: IntegrationMeshReleaseReadinessCheckKind,
    pub status: IntegrationMeshReleaseReadinessStatus,
    pub handoff_lane: IntegrationMeshReleaseDispatchTicketHandoffLane,
    pub clearance_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind,
    pub action_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionKind,
    pub evidence_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceKind,
    pub review_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind,
    pub release_blocking: bool,
    pub operator_required: bool,
    pub review_required: bool,
    pub dispatch_required: bool,
    pub execution_required: bool,
    pub release_evidence: bool,
    pub lineage_complete: bool,
    pub release_ready: bool,
}

impl
    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReview
{
    pub fn from_evidence(
        sequence: usize,
        evidence: &IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidence,
    ) -> Self {
        let review_kind = if !evidence.has_lineage() {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::LineageGap
        } else if evidence.blocks_release() {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::BlockerReview
        } else if evidence.needs_review() {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::ReviewGate
        } else if evidence.needs_operator() {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::OperatorReview
        } else {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::ReleaseReady
        };
        let release_ready = evidence.is_release_evidence()
            && evidence.has_lineage()
            && !evidence.blocks_release()
            && !evidence.needs_operator()
            && !evidence.needs_review()
            && !evidence.dispatch_required
            && !evidence.execution_required;

        Self {
            sequence,
            review_key: format!(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-evidence-review-{sequence:02}-{}-{}",
                review_kind.as_str(),
                evidence.check_kind.as_str()
            ),
            evidence_sequence: evidence.sequence,
            evidence_key: evidence.evidence_key.clone(),
            action_sequence: evidence.action_sequence,
            action_key: evidence.action_key.clone(),
            clearance_sequence: evidence.clearance_sequence,
            clearance_key: evidence.clearance_key.clone(),
            audit_key: evidence.audit_key.clone(),
            guardrail_key: evidence.guardrail_key.clone(),
            work_order_key: evidence.work_order_key.clone(),
            execution_slot_key: evidence.execution_slot_key.clone(),
            packet_key: evidence.packet_key.clone(),
            ticket_key: evidence.ticket_key.clone(),
            check_sequence: evidence.check_sequence,
            check_kind: evidence.check_kind,
            status: evidence.status,
            handoff_lane: evidence.handoff_lane,
            clearance_kind: evidence.clearance_kind,
            action_kind: evidence.action_kind,
            evidence_kind: evidence.evidence_kind,
            review_kind,
            release_blocking: evidence.blocks_release(),
            operator_required: evidence.needs_operator(),
            review_required: evidence.needs_review(),
            dispatch_required: evidence.dispatch_required,
            execution_required: evidence.execution_required,
            release_evidence: evidence.is_release_evidence(),
            lineage_complete: evidence.has_lineage(),
            release_ready,
        }
    }

    pub fn blocks_release(&self) -> bool {
        self.release_blocking
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required
    }

    pub fn needs_review(&self) -> bool {
        self.review_required
    }

    pub fn has_lineage(&self) -> bool {
        self.lineage_complete
    }

    pub fn is_release_ready(&self) -> bool {
        self.release_ready
    }

    pub fn requires_attention(&self) -> bool {
        self.blocks_release()
            || self.needs_operator()
            || self.needs_review()
            || self.dispatch_required
            || self.execution_required
            || !self.has_lineage()
            || !self.is_release_ready()
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewSummary {
    pub total_reviews: usize,
    pub blocking_reviews: usize,
    pub operator_required_reviews: usize,
    pub review_required_reviews: usize,
    pub dispatch_required_reviews: usize,
    pub execution_required_reviews: usize,
    pub lineage_gap_reviews: usize,
    pub release_ready_reviews: usize,
    pub lineage_complete_reviews: usize,
    pub first_review_key: Option<String>,
    pub first_blocking_review_key: Option<String>,
    pub first_operator_review_key: Option<String>,
    pub first_review_gate_key: Option<String>,
    pub first_lineage_gap_review_key: Option<String>,
    pub first_release_ready_review_key: Option<String>,
    pub first_evidence_key: Option<String>,
    pub first_action_key: Option<String>,
    pub first_clearance_key: Option<String>,
    pub first_work_order_key: Option<String>,
    pub first_ticket_key: Option<String>,
    pub first_review_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind,
    >,
    pub first_evidence_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceKind,
    >,
    pub first_action_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionKind,
    >,
    pub first_clearance_kind:
        Option<IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind>,
    pub first_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub first_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub first_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_reviews_ready: bool,
}

impl IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewSummary {
    pub fn from_reviews<'a>(
        reviews: impl IntoIterator<
            Item = &'a IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReview,
        >,
    ) -> Self {
        let reviews = reviews.into_iter().collect::<Vec<_>>();
        let first_review = reviews.iter().min_by_key(|row| row.sequence);
        let first_blocking_review = reviews
            .iter()
            .filter(|row| row.blocks_release())
            .min_by_key(|row| row.sequence);
        let first_operator_review = reviews
            .iter()
            .filter(|row| row.needs_operator())
            .min_by_key(|row| row.sequence);
        let first_review_gate = reviews
            .iter()
            .filter(|row| row.needs_review())
            .min_by_key(|row| row.sequence);
        let first_lineage_gap_review = reviews
            .iter()
            .filter(|row| !row.has_lineage())
            .min_by_key(|row| row.sequence);
        let first_release_ready_review = reviews
            .iter()
            .filter(|row| row.is_release_ready())
            .min_by_key(|row| row.sequence);

        Self {
            total_reviews: reviews.len(),
            blocking_reviews: reviews
                .iter()
                .filter(|row| row.blocks_release())
                .count(),
            operator_required_reviews: reviews
                .iter()
                .filter(|row| row.needs_operator())
                .count(),
            review_required_reviews: reviews.iter().filter(|row| row.needs_review()).count(),
            dispatch_required_reviews: reviews
                .iter()
                .filter(|row| row.dispatch_required)
                .count(),
            execution_required_reviews: reviews
                .iter()
                .filter(|row| row.execution_required)
                .count(),
            lineage_gap_reviews: reviews.iter().filter(|row| !row.has_lineage()).count(),
            release_ready_reviews: reviews
                .iter()
                .filter(|row| row.is_release_ready())
                .count(),
            lineage_complete_reviews: reviews.iter().filter(|row| row.has_lineage()).count(),
            first_review_key: first_review.map(|row| row.review_key.clone()),
            first_blocking_review_key: first_blocking_review.map(|row| row.review_key.clone()),
            first_operator_review_key: first_operator_review.map(|row| row.review_key.clone()),
            first_review_gate_key: first_review_gate.map(|row| row.review_key.clone()),
            first_lineage_gap_review_key: first_lineage_gap_review
                .map(|row| row.review_key.clone()),
            first_release_ready_review_key: first_release_ready_review
                .map(|row| row.review_key.clone()),
            first_evidence_key: first_review.map(|row| row.evidence_key.clone()),
            first_action_key: first_review.map(|row| row.action_key.clone()),
            first_clearance_key: first_review.map(|row| row.clearance_key.clone()),
            first_work_order_key: first_review.map(|row| row.work_order_key.clone()),
            first_ticket_key: first_review.map(|row| row.ticket_key.clone()),
            first_review_kind: first_review.map(|row| row.review_kind),
            first_evidence_kind: first_review.map(|row| row.evidence_kind),
            first_action_kind: first_review.map(|row| row.action_kind),
            first_clearance_kind: first_review.map(|row| row.clearance_kind),
            first_handoff_lane: first_review.map(|row| row.handoff_lane),
            first_check_kind: first_review.map(|row| row.check_kind),
            first_check_status: first_review.map(|row| row.status),
            release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_reviews_ready:
                !reviews.is_empty() && reviews.iter().all(|row| row.is_release_ready()),
        }
    }

    pub fn has_reviews(&self) -> bool {
        self.total_reviews > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.blocking_reviews > 0
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_reviews > 0
    }

    pub fn needs_review(&self) -> bool {
        self.review_required_reviews > 0
    }

    pub fn has_lineage_gaps(&self) -> bool {
        self.lineage_gap_reviews > 0
    }

    pub fn has_release_ready_reviews(&self) -> bool {
        self.release_ready_reviews > 0
    }

    pub fn has_complete_lineage(&self) -> bool {
        self.lineage_complete_reviews == self.total_reviews
    }

    pub fn requires_attention(&self) -> bool {
        self.has_blockers()
            || self.needs_operator()
            || self.needs_review()
            || self.dispatch_required_reviews > 0
            || self.execution_required_reviews > 0
            || self.has_lineage_gaps()
            || !self
                .release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_reviews_ready
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind
{
    RepairReleaseBlocker,
    CoordinateOperatorHandoff,
    ReviewRequired,
    RestoreEvidenceLineage,
    ReleaseReady,
}

impl
    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind
{
    pub fn as_str(self) -> &'static str {
        match self {
            Self::RepairReleaseBlocker => "repair_release_blocker",
            Self::CoordinateOperatorHandoff => "coordinate_operator_handoff",
            Self::ReviewRequired => "review_required",
            Self::RestoreEvidenceLineage => "restore_evidence_lineage",
            Self::ReleaseReady => "release_ready",
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDisposition {
    pub sequence: usize,
    pub disposition_key: String,
    pub review_sequence: usize,
    pub review_key: String,
    pub evidence_sequence: usize,
    pub evidence_key: String,
    pub action_sequence: usize,
    pub action_key: String,
    pub clearance_sequence: usize,
    pub clearance_key: String,
    pub audit_key: String,
    pub guardrail_key: String,
    pub work_order_key: String,
    pub execution_slot_key: String,
    pub packet_key: String,
    pub ticket_key: String,
    pub check_sequence: usize,
    pub check_kind: IntegrationMeshReleaseReadinessCheckKind,
    pub status: IntegrationMeshReleaseReadinessStatus,
    pub handoff_lane: IntegrationMeshReleaseDispatchTicketHandoffLane,
    pub clearance_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind,
    pub action_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionKind,
    pub evidence_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceKind,
    pub review_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind,
    pub disposition_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind,
    pub release_blocking: bool,
    pub operator_required: bool,
    pub review_required: bool,
    pub dispatch_required: bool,
    pub execution_required: bool,
    pub lineage_complete: bool,
    pub release_ready: bool,
}

impl
    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDisposition
{
    pub fn from_review(
        sequence: usize,
        review: &IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReview,
    ) -> Self {
        let disposition_kind = if !review.has_lineage() {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind::RestoreEvidenceLineage
        } else if review.blocks_release() {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind::RepairReleaseBlocker
        } else if review.needs_review() {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind::ReviewRequired
        } else if review.needs_operator() {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind::CoordinateOperatorHandoff
        } else {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind::ReleaseReady
        };

        Self {
            sequence,
            disposition_key: format!(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-evidence-review-disposition-{sequence:02}-{}-{}",
                disposition_kind.as_str(),
                review.check_kind.as_str()
            ),
            review_sequence: review.sequence,
            review_key: review.review_key.clone(),
            evidence_sequence: review.evidence_sequence,
            evidence_key: review.evidence_key.clone(),
            action_sequence: review.action_sequence,
            action_key: review.action_key.clone(),
            clearance_sequence: review.clearance_sequence,
            clearance_key: review.clearance_key.clone(),
            audit_key: review.audit_key.clone(),
            guardrail_key: review.guardrail_key.clone(),
            work_order_key: review.work_order_key.clone(),
            execution_slot_key: review.execution_slot_key.clone(),
            packet_key: review.packet_key.clone(),
            ticket_key: review.ticket_key.clone(),
            check_sequence: review.check_sequence,
            check_kind: review.check_kind,
            status: review.status,
            handoff_lane: review.handoff_lane,
            clearance_kind: review.clearance_kind,
            action_kind: review.action_kind,
            evidence_kind: review.evidence_kind,
            review_kind: review.review_kind,
            disposition_kind,
            release_blocking: review.blocks_release(),
            operator_required: review.needs_operator(),
            review_required: review.needs_review(),
            dispatch_required: review.dispatch_required,
            execution_required: review.execution_required,
            lineage_complete: review.has_lineage(),
            release_ready: review.is_release_ready(),
        }
    }

    pub fn blocks_release(&self) -> bool {
        self.release_blocking
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required
    }

    pub fn needs_review(&self) -> bool {
        self.review_required
    }

    pub fn has_lineage(&self) -> bool {
        self.lineage_complete
    }

    pub fn is_release_ready(&self) -> bool {
        self.release_ready
    }

    pub fn is_repair_required(&self) -> bool {
        self.disposition_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind::RepairReleaseBlocker
    }

    pub fn is_operator_handoff(&self) -> bool {
        self.disposition_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind::CoordinateOperatorHandoff
    }

    pub fn is_review_required(&self) -> bool {
        self.disposition_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind::ReviewRequired
    }

    pub fn is_lineage_gap(&self) -> bool {
        self.disposition_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind::RestoreEvidenceLineage
    }

    pub fn requires_attention(&self) -> bool {
        self.blocks_release()
            || self.needs_operator()
            || self.needs_review()
            || self.dispatch_required
            || self.execution_required
            || !self.has_lineage()
            || !self.is_release_ready()
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionSummary {
    pub total_dispositions: usize,
    pub release_blocking_dispositions: usize,
    pub operator_required_dispositions: usize,
    pub review_required_dispositions: usize,
    pub dispatch_required_dispositions: usize,
    pub execution_required_dispositions: usize,
    pub repair_required_dispositions: usize,
    pub operator_handoff_dispositions: usize,
    pub review_gate_dispositions: usize,
    pub lineage_gap_dispositions: usize,
    pub release_ready_dispositions: usize,
    pub lineage_complete_dispositions: usize,
    pub first_disposition_key: Option<String>,
    pub first_repair_required_disposition_key: Option<String>,
    pub first_operator_handoff_disposition_key: Option<String>,
    pub first_review_gate_disposition_key: Option<String>,
    pub first_lineage_gap_disposition_key: Option<String>,
    pub first_release_ready_disposition_key: Option<String>,
    pub first_review_key: Option<String>,
    pub first_evidence_key: Option<String>,
    pub first_action_key: Option<String>,
    pub first_clearance_key: Option<String>,
    pub first_work_order_key: Option<String>,
    pub first_ticket_key: Option<String>,
    pub first_disposition_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind,
    >,
    pub first_review_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind,
    >,
    pub first_evidence_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceKind,
    >,
    pub first_action_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionKind,
    >,
    pub first_clearance_kind:
        Option<IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind>,
    pub first_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub first_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub first_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_dispositions_ready: bool,
}

impl IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionSummary {
    pub fn from_dispositions<'a>(
        dispositions: impl IntoIterator<
            Item = &'a IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDisposition,
        >,
    ) -> Self {
        let dispositions = dispositions.into_iter().collect::<Vec<_>>();
        let first_disposition = dispositions.iter().min_by_key(|row| row.sequence);
        let first_repair_required_disposition = dispositions
            .iter()
            .filter(|row| row.is_repair_required())
            .min_by_key(|row| row.sequence);
        let first_operator_handoff_disposition = dispositions
            .iter()
            .filter(|row| row.is_operator_handoff())
            .min_by_key(|row| row.sequence);
        let first_review_gate_disposition = dispositions
            .iter()
            .filter(|row| row.is_review_required())
            .min_by_key(|row| row.sequence);
        let first_lineage_gap_disposition = dispositions
            .iter()
            .filter(|row| row.is_lineage_gap())
            .min_by_key(|row| row.sequence);
        let first_release_ready_disposition = dispositions
            .iter()
            .filter(|row| row.is_release_ready())
            .min_by_key(|row| row.sequence);

        Self {
            total_dispositions: dispositions.len(),
            release_blocking_dispositions: dispositions
                .iter()
                .filter(|row| row.blocks_release())
                .count(),
            operator_required_dispositions: dispositions
                .iter()
                .filter(|row| row.needs_operator())
                .count(),
            review_required_dispositions: dispositions
                .iter()
                .filter(|row| row.needs_review())
                .count(),
            dispatch_required_dispositions: dispositions
                .iter()
                .filter(|row| row.dispatch_required)
                .count(),
            execution_required_dispositions: dispositions
                .iter()
                .filter(|row| row.execution_required)
                .count(),
            repair_required_dispositions: dispositions
                .iter()
                .filter(|row| row.is_repair_required())
                .count(),
            operator_handoff_dispositions: dispositions
                .iter()
                .filter(|row| row.is_operator_handoff())
                .count(),
            review_gate_dispositions: dispositions
                .iter()
                .filter(|row| row.is_review_required())
                .count(),
            lineage_gap_dispositions: dispositions
                .iter()
                .filter(|row| row.is_lineage_gap())
                .count(),
            release_ready_dispositions: dispositions
                .iter()
                .filter(|row| row.is_release_ready())
                .count(),
            lineage_complete_dispositions: dispositions
                .iter()
                .filter(|row| row.has_lineage())
                .count(),
            first_disposition_key: first_disposition.map(|row| row.disposition_key.clone()),
            first_repair_required_disposition_key: first_repair_required_disposition
                .map(|row| row.disposition_key.clone()),
            first_operator_handoff_disposition_key: first_operator_handoff_disposition
                .map(|row| row.disposition_key.clone()),
            first_review_gate_disposition_key: first_review_gate_disposition
                .map(|row| row.disposition_key.clone()),
            first_lineage_gap_disposition_key: first_lineage_gap_disposition
                .map(|row| row.disposition_key.clone()),
            first_release_ready_disposition_key: first_release_ready_disposition
                .map(|row| row.disposition_key.clone()),
            first_review_key: first_disposition.map(|row| row.review_key.clone()),
            first_evidence_key: first_disposition.map(|row| row.evidence_key.clone()),
            first_action_key: first_disposition.map(|row| row.action_key.clone()),
            first_clearance_key: first_disposition.map(|row| row.clearance_key.clone()),
            first_work_order_key: first_disposition.map(|row| row.work_order_key.clone()),
            first_ticket_key: first_disposition.map(|row| row.ticket_key.clone()),
            first_disposition_kind: first_disposition.map(|row| row.disposition_kind),
            first_review_kind: first_disposition.map(|row| row.review_kind),
            first_evidence_kind: first_disposition.map(|row| row.evidence_kind),
            first_action_kind: first_disposition.map(|row| row.action_kind),
            first_clearance_kind: first_disposition.map(|row| row.clearance_kind),
            first_handoff_lane: first_disposition.map(|row| row.handoff_lane),
            first_check_kind: first_disposition.map(|row| row.check_kind),
            first_check_status: first_disposition.map(|row| row.status),
            release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_dispositions_ready:
                !dispositions.is_empty() && dispositions.iter().all(|row| row.is_release_ready()),
        }
    }

    pub fn has_dispositions(&self) -> bool {
        self.total_dispositions > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.release_blocking_dispositions > 0 || self.repair_required_dispositions > 0
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_dispositions > 0 || self.operator_handoff_dispositions > 0
    }

    pub fn needs_review(&self) -> bool {
        self.review_required_dispositions > 0 || self.review_gate_dispositions > 0
    }

    pub fn has_lineage_gaps(&self) -> bool {
        self.lineage_gap_dispositions > 0
    }

    pub fn has_release_ready_dispositions(&self) -> bool {
        self.release_ready_dispositions > 0
    }

    pub fn has_complete_lineage(&self) -> bool {
        self.lineage_complete_dispositions == self.total_dispositions
    }

    pub fn requires_attention(&self) -> bool {
        self.has_blockers()
            || self.needs_operator()
            || self.needs_review()
            || self.dispatch_required_dispositions > 0
            || self.execution_required_dispositions > 0
            || self.has_lineage_gaps()
            || !self
                .release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_dispositions_ready
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionKind
{
    ScheduleReleaseBlockerRepair,
    CoordinateOperatorHandoff,
    CompleteEvidenceReview,
    RestoreEvidenceLineage,
    ReleaseTicketHandoff,
}

impl
    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionKind
{
    pub fn as_str(self) -> &'static str {
        match self {
            Self::ScheduleReleaseBlockerRepair => "schedule_release_blocker_repair",
            Self::CoordinateOperatorHandoff => "coordinate_operator_handoff",
            Self::CompleteEvidenceReview => "complete_evidence_review",
            Self::RestoreEvidenceLineage => "restore_evidence_lineage",
            Self::ReleaseTicketHandoff => "release_ticket_handoff",
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionAction {
    pub sequence: usize,
    pub disposition_action_key: String,
    pub disposition_sequence: usize,
    pub disposition_key: String,
    pub review_sequence: usize,
    pub review_key: String,
    pub evidence_sequence: usize,
    pub evidence_key: String,
    pub clearance_action_sequence: usize,
    pub clearance_action_key: String,
    pub clearance_sequence: usize,
    pub clearance_key: String,
    pub audit_key: String,
    pub guardrail_key: String,
    pub work_order_key: String,
    pub execution_slot_key: String,
    pub packet_key: String,
    pub ticket_key: String,
    pub check_sequence: usize,
    pub check_kind: IntegrationMeshReleaseReadinessCheckKind,
    pub status: IntegrationMeshReleaseReadinessStatus,
    pub handoff_lane: IntegrationMeshReleaseDispatchTicketHandoffLane,
    pub clearance_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind,
    pub clearance_action_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionKind,
    pub evidence_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceKind,
    pub review_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind,
    pub disposition_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind,
    pub disposition_action_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionKind,
    pub release_blocking: bool,
    pub operator_required: bool,
    pub review_required: bool,
    pub dispatch_required: bool,
    pub execution_required: bool,
    pub lineage_complete: bool,
    pub release_ready: bool,
    pub action_required: bool,
}

impl
    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionAction
{
    pub fn from_disposition(
        sequence: usize,
        disposition: &IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDisposition,
    ) -> Self {
        let disposition_action_kind = match disposition.disposition_kind {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind::RepairReleaseBlocker => {
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionKind::ScheduleReleaseBlockerRepair
            }
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind::CoordinateOperatorHandoff => {
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionKind::CoordinateOperatorHandoff
            }
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind::ReviewRequired => {
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionKind::CompleteEvidenceReview
            }
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind::RestoreEvidenceLineage => {
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionKind::RestoreEvidenceLineage
            }
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind::ReleaseReady => {
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionKind::ReleaseTicketHandoff
            }
        };

        Self {
            sequence,
            disposition_action_key: format!(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-evidence-review-disposition-action-{sequence:02}-{}-{}",
                disposition_action_kind.as_str(),
                disposition.check_kind.as_str()
            ),
            disposition_sequence: disposition.sequence,
            disposition_key: disposition.disposition_key.clone(),
            review_sequence: disposition.review_sequence,
            review_key: disposition.review_key.clone(),
            evidence_sequence: disposition.evidence_sequence,
            evidence_key: disposition.evidence_key.clone(),
            clearance_action_sequence: disposition.action_sequence,
            clearance_action_key: disposition.action_key.clone(),
            clearance_sequence: disposition.clearance_sequence,
            clearance_key: disposition.clearance_key.clone(),
            audit_key: disposition.audit_key.clone(),
            guardrail_key: disposition.guardrail_key.clone(),
            work_order_key: disposition.work_order_key.clone(),
            execution_slot_key: disposition.execution_slot_key.clone(),
            packet_key: disposition.packet_key.clone(),
            ticket_key: disposition.ticket_key.clone(),
            check_sequence: disposition.check_sequence,
            check_kind: disposition.check_kind,
            status: disposition.status,
            handoff_lane: disposition.handoff_lane,
            clearance_kind: disposition.clearance_kind,
            clearance_action_kind: disposition.action_kind,
            evidence_kind: disposition.evidence_kind,
            review_kind: disposition.review_kind,
            disposition_kind: disposition.disposition_kind,
            disposition_action_kind,
            release_blocking: disposition.blocks_release(),
            operator_required: disposition.needs_operator(),
            review_required: disposition.needs_review(),
            dispatch_required: disposition.dispatch_required,
            execution_required: disposition.execution_required,
            lineage_complete: disposition.has_lineage(),
            release_ready: disposition.is_release_ready(),
            action_required: !disposition.is_release_ready(),
        }
    }

    pub fn requires_action(&self) -> bool {
        self.action_required
    }

    pub fn blocks_release(&self) -> bool {
        self.release_blocking
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required
    }

    pub fn needs_review(&self) -> bool {
        self.review_required
    }

    pub fn has_lineage(&self) -> bool {
        self.lineage_complete
    }

    pub fn is_release_ready(&self) -> bool {
        self.release_ready
    }

    pub fn is_repair_action(&self) -> bool {
        self.disposition_action_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionKind::ScheduleReleaseBlockerRepair
    }

    pub fn is_operator_handoff_action(&self) -> bool {
        self.disposition_action_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionKind::CoordinateOperatorHandoff
    }

    pub fn is_review_gate_action(&self) -> bool {
        self.disposition_action_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionKind::CompleteEvidenceReview
    }

    pub fn is_lineage_action(&self) -> bool {
        self.disposition_action_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionKind::RestoreEvidenceLineage
    }

    pub fn is_release_handoff_action(&self) -> bool {
        self.disposition_action_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionKind::ReleaseTicketHandoff
    }

    pub fn requires_attention(&self) -> bool {
        self.requires_action()
            || self.blocks_release()
            || self.needs_operator()
            || self.needs_review()
            || self.dispatch_required
            || self.execution_required
            || !self.has_lineage()
            || !self.is_release_ready()
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSummary {
    pub total_actions: usize,
    pub required_actions: usize,
    pub release_blocking_actions: usize,
    pub operator_required_actions: usize,
    pub review_required_actions: usize,
    pub dispatch_required_actions: usize,
    pub execution_required_actions: usize,
    pub repair_actions: usize,
    pub operator_handoff_actions: usize,
    pub review_gate_actions: usize,
    pub lineage_gap_actions: usize,
    pub release_handoff_actions: usize,
    pub lineage_complete_actions: usize,
    pub first_action_key: Option<String>,
    pub first_required_action_key: Option<String>,
    pub first_repair_action_key: Option<String>,
    pub first_operator_handoff_action_key: Option<String>,
    pub first_review_gate_action_key: Option<String>,
    pub first_lineage_gap_action_key: Option<String>,
    pub first_release_handoff_action_key: Option<String>,
    pub first_disposition_key: Option<String>,
    pub first_review_key: Option<String>,
    pub first_evidence_key: Option<String>,
    pub first_clearance_action_key: Option<String>,
    pub first_clearance_key: Option<String>,
    pub first_work_order_key: Option<String>,
    pub first_ticket_key: Option<String>,
    pub first_disposition_action_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionKind,
    >,
    pub first_disposition_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind,
    >,
    pub first_review_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind,
    >,
    pub first_evidence_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceKind,
    >,
    pub first_clearance_action_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionKind,
    >,
    pub first_clearance_kind:
        Option<IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind>,
    pub first_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub first_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub first_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_actions_ready: bool,
}

impl IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSummary {
    pub fn from_actions<'a>(
        actions: impl IntoIterator<
            Item = &'a IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionAction,
        >,
    ) -> Self {
        let actions = actions.into_iter().collect::<Vec<_>>();
        let first_action = actions.iter().min_by_key(|action| action.sequence);
        let first_required_action = actions
            .iter()
            .filter(|action| action.requires_action())
            .min_by_key(|action| action.sequence);
        let first_repair_action = actions
            .iter()
            .filter(|action| action.is_repair_action())
            .min_by_key(|action| action.sequence);
        let first_operator_handoff_action = actions
            .iter()
            .filter(|action| action.is_operator_handoff_action())
            .min_by_key(|action| action.sequence);
        let first_review_gate_action = actions
            .iter()
            .filter(|action| action.is_review_gate_action())
            .min_by_key(|action| action.sequence);
        let first_lineage_gap_action = actions
            .iter()
            .filter(|action| action.is_lineage_action())
            .min_by_key(|action| action.sequence);
        let first_release_handoff_action = actions
            .iter()
            .filter(|action| action.is_release_handoff_action())
            .min_by_key(|action| action.sequence);

        Self {
            total_actions: actions.len(),
            required_actions: actions
                .iter()
                .filter(|action| action.requires_action())
                .count(),
            release_blocking_actions: actions
                .iter()
                .filter(|action| action.blocks_release())
                .count(),
            operator_required_actions: actions
                .iter()
                .filter(|action| action.needs_operator())
                .count(),
            review_required_actions: actions
                .iter()
                .filter(|action| action.needs_review())
                .count(),
            dispatch_required_actions: actions
                .iter()
                .filter(|action| action.dispatch_required)
                .count(),
            execution_required_actions: actions
                .iter()
                .filter(|action| action.execution_required)
                .count(),
            repair_actions: actions
                .iter()
                .filter(|action| action.is_repair_action())
                .count(),
            operator_handoff_actions: actions
                .iter()
                .filter(|action| action.is_operator_handoff_action())
                .count(),
            review_gate_actions: actions
                .iter()
                .filter(|action| action.is_review_gate_action())
                .count(),
            lineage_gap_actions: actions
                .iter()
                .filter(|action| action.is_lineage_action())
                .count(),
            release_handoff_actions: actions
                .iter()
                .filter(|action| action.is_release_handoff_action())
                .count(),
            lineage_complete_actions: actions.iter().filter(|action| action.has_lineage()).count(),
            first_action_key: first_action.map(|action| action.disposition_action_key.clone()),
            first_required_action_key: first_required_action
                .map(|action| action.disposition_action_key.clone()),
            first_repair_action_key: first_repair_action
                .map(|action| action.disposition_action_key.clone()),
            first_operator_handoff_action_key: first_operator_handoff_action
                .map(|action| action.disposition_action_key.clone()),
            first_review_gate_action_key: first_review_gate_action
                .map(|action| action.disposition_action_key.clone()),
            first_lineage_gap_action_key: first_lineage_gap_action
                .map(|action| action.disposition_action_key.clone()),
            first_release_handoff_action_key: first_release_handoff_action
                .map(|action| action.disposition_action_key.clone()),
            first_disposition_key: first_action.map(|action| action.disposition_key.clone()),
            first_review_key: first_action.map(|action| action.review_key.clone()),
            first_evidence_key: first_action.map(|action| action.evidence_key.clone()),
            first_clearance_action_key: first_action
                .map(|action| action.clearance_action_key.clone()),
            first_clearance_key: first_action.map(|action| action.clearance_key.clone()),
            first_work_order_key: first_action.map(|action| action.work_order_key.clone()),
            first_ticket_key: first_action.map(|action| action.ticket_key.clone()),
            first_disposition_action_kind: first_action.map(|action| action.disposition_action_kind),
            first_disposition_kind: first_action.map(|action| action.disposition_kind),
            first_review_kind: first_action.map(|action| action.review_kind),
            first_evidence_kind: first_action.map(|action| action.evidence_kind),
            first_clearance_action_kind: first_action.map(|action| action.clearance_action_kind),
            first_clearance_kind: first_action.map(|action| action.clearance_kind),
            first_handoff_lane: first_action.map(|action| action.handoff_lane),
            first_check_kind: first_action.map(|action| action.check_kind),
            first_check_status: first_action.map(|action| action.status),
            release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_actions_ready:
                !actions.is_empty() && actions.iter().all(|action| action.is_release_ready()),
        }
    }

    pub fn has_actions(&self) -> bool {
        self.total_actions > 0
    }

    pub fn has_required_actions(&self) -> bool {
        self.required_actions > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.release_blocking_actions > 0 || self.repair_actions > 0
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_actions > 0 || self.operator_handoff_actions > 0
    }

    pub fn needs_review(&self) -> bool {
        self.review_required_actions > 0 || self.review_gate_actions > 0
    }

    pub fn has_lineage_gaps(&self) -> bool {
        self.lineage_gap_actions > 0
    }

    pub fn has_release_handoff_actions(&self) -> bool {
        self.release_handoff_actions > 0
    }

    pub fn has_complete_lineage(&self) -> bool {
        self.lineage_complete_actions == self.total_actions
    }

    pub fn requires_attention(&self) -> bool {
        self.has_required_actions()
            || self.has_blockers()
            || self.needs_operator()
            || self.needs_review()
            || self.dispatch_required_actions > 0
            || self.execution_required_actions > 0
            || self.has_lineage_gaps()
            || !self
                .release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_actions_ready
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlot {
    pub sequence: usize,
    pub slot_key: String,
    pub disposition_action_sequence: usize,
    pub disposition_action_key: String,
    pub disposition_sequence: usize,
    pub disposition_key: String,
    pub review_key: String,
    pub evidence_key: String,
    pub clearance_action_key: String,
    pub clearance_key: String,
    pub audit_key: String,
    pub guardrail_key: String,
    pub work_order_key: String,
    pub execution_slot_key: String,
    pub packet_key: String,
    pub ticket_key: String,
    pub check_sequence: usize,
    pub check_kind: IntegrationMeshReleaseReadinessCheckKind,
    pub status: IntegrationMeshReleaseReadinessStatus,
    pub handoff_lane: IntegrationMeshReleaseDispatchTicketHandoffLane,
    pub disposition_action_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionKind,
    pub disposition_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind,
    pub release_blocking: bool,
    pub operator_required: bool,
    pub review_required: bool,
    pub dispatch_required: bool,
    pub execution_required: bool,
    pub lineage_complete: bool,
    pub action_required: bool,
    pub ready_for_handoff: bool,
}

impl
    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlot
{
    pub fn from_action(
        sequence: usize,
        action: &IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionAction,
    ) -> Self {
        Self {
            sequence,
            slot_key: format!(
                "release-ticket-handoff-disposition-action-slot-{sequence:02}-{}-{}",
                action.disposition_action_kind.as_str(),
                action.check_kind.as_str()
            ),
            disposition_action_sequence: action.sequence,
            disposition_action_key: action.disposition_action_key.clone(),
            disposition_sequence: action.disposition_sequence,
            disposition_key: action.disposition_key.clone(),
            review_key: action.review_key.clone(),
            evidence_key: action.evidence_key.clone(),
            clearance_action_key: action.clearance_action_key.clone(),
            clearance_key: action.clearance_key.clone(),
            audit_key: action.audit_key.clone(),
            guardrail_key: action.guardrail_key.clone(),
            work_order_key: action.work_order_key.clone(),
            execution_slot_key: action.execution_slot_key.clone(),
            packet_key: action.packet_key.clone(),
            ticket_key: action.ticket_key.clone(),
            check_sequence: action.check_sequence,
            check_kind: action.check_kind,
            status: action.status,
            handoff_lane: action.handoff_lane,
            disposition_action_kind: action.disposition_action_kind,
            disposition_kind: action.disposition_kind,
            release_blocking: action.blocks_release(),
            operator_required: action.needs_operator(),
            review_required: action.needs_review(),
            dispatch_required: action.dispatch_required,
            execution_required: action.execution_required,
            lineage_complete: action.has_lineage(),
            action_required: action.requires_action(),
            ready_for_handoff: action.is_release_handoff_action(),
        }
    }

    pub fn requires_action(&self) -> bool {
        self.action_required
    }

    pub fn blocks_release(&self) -> bool {
        self.release_blocking
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required
    }

    pub fn needs_review(&self) -> bool {
        self.review_required
    }

    pub fn has_lineage(&self) -> bool {
        self.lineage_complete
    }

    pub fn is_ready_for_handoff(&self) -> bool {
        self.ready_for_handoff
    }

    pub fn is_repair_slot(&self) -> bool {
        self.disposition_action_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionKind::ScheduleReleaseBlockerRepair
    }

    pub fn is_operator_handoff_slot(&self) -> bool {
        self.disposition_action_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionKind::CoordinateOperatorHandoff
    }

    pub fn is_review_gate_slot(&self) -> bool {
        self.disposition_action_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionKind::CompleteEvidenceReview
    }

    pub fn is_lineage_slot(&self) -> bool {
        self.disposition_action_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionKind::RestoreEvidenceLineage
    }

    pub fn is_release_handoff_slot(&self) -> bool {
        self.disposition_action_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionKind::ReleaseTicketHandoff
    }

    pub fn requires_attention(&self) -> bool {
        self.requires_action()
            || self.blocks_release()
            || self.needs_operator()
            || self.needs_review()
            || self.dispatch_required
            || self.execution_required
            || !self.has_lineage()
            || !self.is_ready_for_handoff()
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotSummary {
    pub total_slots: usize,
    pub required_slots: usize,
    pub release_blocking_slots: usize,
    pub operator_required_slots: usize,
    pub review_required_slots: usize,
    pub dispatch_required_slots: usize,
    pub execution_required_slots: usize,
    pub repair_slots: usize,
    pub operator_handoff_slots: usize,
    pub review_gate_slots: usize,
    pub lineage_gap_slots: usize,
    pub release_handoff_slots: usize,
    pub lineage_complete_slots: usize,
    pub first_slot_key: Option<String>,
    pub first_required_slot_key: Option<String>,
    pub first_repair_slot_key: Option<String>,
    pub first_operator_handoff_slot_key: Option<String>,
    pub first_review_gate_slot_key: Option<String>,
    pub first_lineage_gap_slot_key: Option<String>,
    pub first_release_handoff_slot_key: Option<String>,
    pub first_disposition_action_key: Option<String>,
    pub first_disposition_key: Option<String>,
    pub first_review_key: Option<String>,
    pub first_evidence_key: Option<String>,
    pub first_clearance_action_key: Option<String>,
    pub first_clearance_key: Option<String>,
    pub first_work_order_key: Option<String>,
    pub first_ticket_key: Option<String>,
    pub first_disposition_action_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionKind,
    >,
    pub first_disposition_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind,
    >,
    pub first_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub first_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub first_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slots_ready: bool,
}

impl
    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotSummary
{
    pub fn from_slots<'a>(
        slots: impl IntoIterator<
            Item = &'a IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlot,
        >,
    ) -> Self {
        let slots = slots.into_iter().collect::<Vec<_>>();
        let first_slot = slots.iter().min_by_key(|slot| slot.sequence);
        let first_required_slot = slots
            .iter()
            .filter(|slot| slot.requires_action())
            .min_by_key(|slot| slot.sequence);
        let first_repair_slot = slots
            .iter()
            .filter(|slot| slot.is_repair_slot())
            .min_by_key(|slot| slot.sequence);
        let first_operator_handoff_slot = slots
            .iter()
            .filter(|slot| slot.is_operator_handoff_slot())
            .min_by_key(|slot| slot.sequence);
        let first_review_gate_slot = slots
            .iter()
            .filter(|slot| slot.is_review_gate_slot())
            .min_by_key(|slot| slot.sequence);
        let first_lineage_gap_slot = slots
            .iter()
            .filter(|slot| slot.is_lineage_slot())
            .min_by_key(|slot| slot.sequence);
        let first_release_handoff_slot = slots
            .iter()
            .filter(|slot| slot.is_release_handoff_slot())
            .min_by_key(|slot| slot.sequence);

        Self {
            total_slots: slots.len(),
            required_slots: slots.iter().filter(|slot| slot.requires_action()).count(),
            release_blocking_slots: slots.iter().filter(|slot| slot.blocks_release()).count(),
            operator_required_slots: slots.iter().filter(|slot| slot.needs_operator()).count(),
            review_required_slots: slots.iter().filter(|slot| slot.needs_review()).count(),
            dispatch_required_slots: slots.iter().filter(|slot| slot.dispatch_required).count(),
            execution_required_slots: slots.iter().filter(|slot| slot.execution_required).count(),
            repair_slots: slots.iter().filter(|slot| slot.is_repair_slot()).count(),
            operator_handoff_slots: slots
                .iter()
                .filter(|slot| slot.is_operator_handoff_slot())
                .count(),
            review_gate_slots: slots
                .iter()
                .filter(|slot| slot.is_review_gate_slot())
                .count(),
            lineage_gap_slots: slots.iter().filter(|slot| slot.is_lineage_slot()).count(),
            release_handoff_slots: slots
                .iter()
                .filter(|slot| slot.is_release_handoff_slot())
                .count(),
            lineage_complete_slots: slots.iter().filter(|slot| slot.has_lineage()).count(),
            first_slot_key: first_slot.map(|slot| slot.slot_key.clone()),
            first_required_slot_key: first_required_slot.map(|slot| slot.slot_key.clone()),
            first_repair_slot_key: first_repair_slot.map(|slot| slot.slot_key.clone()),
            first_operator_handoff_slot_key: first_operator_handoff_slot
                .map(|slot| slot.slot_key.clone()),
            first_review_gate_slot_key: first_review_gate_slot.map(|slot| slot.slot_key.clone()),
            first_lineage_gap_slot_key: first_lineage_gap_slot.map(|slot| slot.slot_key.clone()),
            first_release_handoff_slot_key: first_release_handoff_slot
                .map(|slot| slot.slot_key.clone()),
            first_disposition_action_key: first_slot
                .map(|slot| slot.disposition_action_key.clone()),
            first_disposition_key: first_slot.map(|slot| slot.disposition_key.clone()),
            first_review_key: first_slot.map(|slot| slot.review_key.clone()),
            first_evidence_key: first_slot.map(|slot| slot.evidence_key.clone()),
            first_clearance_action_key: first_slot
                .map(|slot| slot.clearance_action_key.clone()),
            first_clearance_key: first_slot.map(|slot| slot.clearance_key.clone()),
            first_work_order_key: first_slot.map(|slot| slot.work_order_key.clone()),
            first_ticket_key: first_slot.map(|slot| slot.ticket_key.clone()),
            first_disposition_action_kind: first_slot.map(|slot| slot.disposition_action_kind),
            first_disposition_kind: first_slot.map(|slot| slot.disposition_kind),
            first_handoff_lane: first_slot.map(|slot| slot.handoff_lane),
            first_check_kind: first_slot.map(|slot| slot.check_kind),
            first_check_status: first_slot.map(|slot| slot.status),
            release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slots_ready:
                !slots.is_empty() && slots.iter().all(|slot| slot.is_ready_for_handoff()),
        }
    }

    pub fn has_slots(&self) -> bool {
        self.total_slots > 0
    }

    pub fn has_required_slots(&self) -> bool {
        self.required_slots > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.release_blocking_slots > 0 || self.repair_slots > 0
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_slots > 0 || self.operator_handoff_slots > 0
    }

    pub fn needs_review(&self) -> bool {
        self.review_required_slots > 0 || self.review_gate_slots > 0
    }

    pub fn needs_repair(&self) -> bool {
        self.repair_slots > 0
    }

    pub fn has_lineage_gaps(&self) -> bool {
        self.lineage_gap_slots > 0
    }

    pub fn has_release_handoff_slots(&self) -> bool {
        self.release_handoff_slots > 0
    }

    pub fn has_complete_lineage(&self) -> bool {
        self.lineage_complete_slots == self.total_slots
    }

    pub fn requires_attention(&self) -> bool {
        self.has_required_slots()
            || self.has_blockers()
            || self.needs_operator()
            || self.needs_review()
            || self.dispatch_required_slots > 0
            || self.execution_required_slots > 0
            || self.has_lineage_gaps()
            || !self
                .release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slots_ready
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceKind
{
    RepairRequired,
    OperatorHandoff,
    ReviewRequired,
    LineageGap,
    ReleaseHandoff,
}

impl
    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceKind
{
    pub fn as_str(self) -> &'static str {
        match self {
            Self::RepairRequired => "repair_required",
            Self::OperatorHandoff => "operator_handoff",
            Self::ReviewRequired => "review_required",
            Self::LineageGap => "lineage_gap",
            Self::ReleaseHandoff => "release_handoff",
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceRow {
    pub sequence: usize,
    pub slot_clearance_key: String,
    pub slot_sequence: usize,
    pub slot_key: String,
    pub disposition_action_key: String,
    pub disposition_key: String,
    pub review_key: String,
    pub evidence_key: String,
    pub clearance_action_key: String,
    pub clearance_key: String,
    pub audit_key: String,
    pub guardrail_key: String,
    pub work_order_key: String,
    pub execution_slot_key: String,
    pub packet_key: String,
    pub ticket_key: String,
    pub check_sequence: usize,
    pub check_kind: IntegrationMeshReleaseReadinessCheckKind,
    pub status: IntegrationMeshReleaseReadinessStatus,
    pub handoff_lane: IntegrationMeshReleaseDispatchTicketHandoffLane,
    pub disposition_action_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionKind,
    pub disposition_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind,
    pub slot_clearance_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceKind,
    pub release_blocking: bool,
    pub operator_required: bool,
    pub review_required: bool,
    pub dispatch_required: bool,
    pub execution_required: bool,
    pub lineage_complete: bool,
    pub action_required: bool,
    pub cleared_for_handoff: bool,
}

impl
    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceRow
{
    pub fn from_slot(
        sequence: usize,
        slot: &IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlot,
    ) -> Self {
        let slot_clearance_kind = if !slot.has_lineage() {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceKind::LineageGap
        } else if slot.blocks_release() {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceKind::RepairRequired
        } else if slot.needs_review() {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceKind::ReviewRequired
        } else if slot.needs_operator() {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceKind::OperatorHandoff
        } else {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceKind::ReleaseHandoff
        };
        let cleared_for_handoff = slot_clearance_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceKind::ReleaseHandoff
            && slot.is_ready_for_handoff();

        Self {
            sequence,
            slot_clearance_key: format!(
                "release-ticket-handoff-disposition-action-slot-clearance-{sequence:02}-{}-{}",
                slot_clearance_kind.as_str(),
                slot.check_kind.as_str()
            ),
            slot_sequence: slot.sequence,
            slot_key: slot.slot_key.clone(),
            disposition_action_key: slot.disposition_action_key.clone(),
            disposition_key: slot.disposition_key.clone(),
            review_key: slot.review_key.clone(),
            evidence_key: slot.evidence_key.clone(),
            clearance_action_key: slot.clearance_action_key.clone(),
            clearance_key: slot.clearance_key.clone(),
            audit_key: slot.audit_key.clone(),
            guardrail_key: slot.guardrail_key.clone(),
            work_order_key: slot.work_order_key.clone(),
            execution_slot_key: slot.execution_slot_key.clone(),
            packet_key: slot.packet_key.clone(),
            ticket_key: slot.ticket_key.clone(),
            check_sequence: slot.check_sequence,
            check_kind: slot.check_kind,
            status: slot.status,
            handoff_lane: slot.handoff_lane,
            disposition_action_kind: slot.disposition_action_kind,
            disposition_kind: slot.disposition_kind,
            slot_clearance_kind,
            release_blocking: slot.blocks_release(),
            operator_required: slot.needs_operator(),
            review_required: slot.needs_review(),
            dispatch_required: slot.dispatch_required,
            execution_required: slot.execution_required,
            lineage_complete: slot.has_lineage(),
            action_required: slot.requires_action(),
            cleared_for_handoff,
        }
    }

    pub fn requires_action(&self) -> bool {
        self.action_required
    }

    pub fn blocks_release(&self) -> bool {
        self.release_blocking
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required
            || self.slot_clearance_kind
                == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceKind::OperatorHandoff
    }

    pub fn needs_review(&self) -> bool {
        self.review_required
            || self.slot_clearance_kind
                == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceKind::ReviewRequired
    }

    pub fn has_lineage(&self) -> bool {
        self.lineage_complete
    }

    pub fn is_repair_clearance(&self) -> bool {
        self.slot_clearance_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceKind::RepairRequired
    }

    pub fn is_operator_handoff_clearance(&self) -> bool {
        self.slot_clearance_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceKind::OperatorHandoff
    }

    pub fn is_review_clearance(&self) -> bool {
        self.slot_clearance_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceKind::ReviewRequired
    }

    pub fn is_lineage_gap_clearance(&self) -> bool {
        self.slot_clearance_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceKind::LineageGap
    }

    pub fn is_release_handoff_clearance(&self) -> bool {
        self.slot_clearance_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceKind::ReleaseHandoff
    }

    pub fn ready_to_clear(&self) -> bool {
        self.cleared_for_handoff
    }

    pub fn requires_attention(&self) -> bool {
        self.requires_action()
            || self.blocks_release()
            || self.needs_operator()
            || self.needs_review()
            || self.dispatch_required
            || self.execution_required
            || !self.has_lineage()
            || !self.ready_to_clear()
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceSummary {
    pub total_slot_clearance_rows: usize,
    pub repair_required_clearances: usize,
    pub operator_handoff_clearances: usize,
    pub review_required_clearances: usize,
    pub lineage_gap_clearances: usize,
    pub release_handoff_clearances: usize,
    pub required_clearances: usize,
    pub release_blocking_clearances: usize,
    pub operator_required_clearances: usize,
    pub review_required_clearances_count: usize,
    pub dispatch_required_clearances: usize,
    pub execution_required_clearances: usize,
    pub lineage_complete_clearances: usize,
    pub first_slot_clearance_key: Option<String>,
    pub first_required_clearance_key: Option<String>,
    pub first_repair_clearance_key: Option<String>,
    pub first_operator_handoff_clearance_key: Option<String>,
    pub first_review_clearance_key: Option<String>,
    pub first_lineage_gap_clearance_key: Option<String>,
    pub first_release_handoff_clearance_key: Option<String>,
    pub first_slot_key: Option<String>,
    pub first_disposition_action_key: Option<String>,
    pub first_disposition_key: Option<String>,
    pub first_work_order_key: Option<String>,
    pub first_ticket_key: Option<String>,
    pub first_slot_clearance_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceKind,
    >,
    pub first_disposition_action_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionKind,
    >,
    pub first_disposition_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind,
    >,
    pub first_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub first_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub first_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearances_ready: bool,
}

impl
    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceSummary
{
    pub fn from_clearance_rows<'a>(
        clearance_rows: impl IntoIterator<
            Item = &'a IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceRow,
        >,
    ) -> Self {
        let clearance_rows = clearance_rows.into_iter().collect::<Vec<_>>();
        let first_clearance_row = clearance_rows.iter().min_by_key(|row| row.sequence);
        let first_required_clearance = clearance_rows
            .iter()
            .filter(|row| row.requires_action())
            .min_by_key(|row| row.sequence);
        let first_repair_clearance = clearance_rows
            .iter()
            .filter(|row| row.is_repair_clearance())
            .min_by_key(|row| row.sequence);
        let first_operator_handoff_clearance = clearance_rows
            .iter()
            .filter(|row| row.is_operator_handoff_clearance())
            .min_by_key(|row| row.sequence);
        let first_review_clearance = clearance_rows
            .iter()
            .filter(|row| row.is_review_clearance())
            .min_by_key(|row| row.sequence);
        let first_lineage_gap_clearance = clearance_rows
            .iter()
            .filter(|row| row.is_lineage_gap_clearance())
            .min_by_key(|row| row.sequence);
        let first_release_handoff_clearance = clearance_rows
            .iter()
            .filter(|row| row.is_release_handoff_clearance())
            .min_by_key(|row| row.sequence);

        Self {
            total_slot_clearance_rows: clearance_rows.len(),
            repair_required_clearances: clearance_rows
                .iter()
                .filter(|row| row.is_repair_clearance())
                .count(),
            operator_handoff_clearances: clearance_rows
                .iter()
                .filter(|row| row.is_operator_handoff_clearance())
                .count(),
            review_required_clearances: clearance_rows
                .iter()
                .filter(|row| row.is_review_clearance())
                .count(),
            lineage_gap_clearances: clearance_rows
                .iter()
                .filter(|row| row.is_lineage_gap_clearance())
                .count(),
            release_handoff_clearances: clearance_rows
                .iter()
                .filter(|row| row.is_release_handoff_clearance())
                .count(),
            required_clearances: clearance_rows
                .iter()
                .filter(|row| row.requires_action())
                .count(),
            release_blocking_clearances: clearance_rows
                .iter()
                .filter(|row| row.blocks_release())
                .count(),
            operator_required_clearances: clearance_rows
                .iter()
                .filter(|row| row.needs_operator())
                .count(),
            review_required_clearances_count: clearance_rows
                .iter()
                .filter(|row| row.needs_review())
                .count(),
            dispatch_required_clearances: clearance_rows
                .iter()
                .filter(|row| row.dispatch_required)
                .count(),
            execution_required_clearances: clearance_rows
                .iter()
                .filter(|row| row.execution_required)
                .count(),
            lineage_complete_clearances: clearance_rows
                .iter()
                .filter(|row| row.has_lineage())
                .count(),
            first_slot_clearance_key: first_clearance_row
                .map(|row| row.slot_clearance_key.clone()),
            first_required_clearance_key: first_required_clearance
                .map(|row| row.slot_clearance_key.clone()),
            first_repair_clearance_key: first_repair_clearance
                .map(|row| row.slot_clearance_key.clone()),
            first_operator_handoff_clearance_key: first_operator_handoff_clearance
                .map(|row| row.slot_clearance_key.clone()),
            first_review_clearance_key: first_review_clearance
                .map(|row| row.slot_clearance_key.clone()),
            first_lineage_gap_clearance_key: first_lineage_gap_clearance
                .map(|row| row.slot_clearance_key.clone()),
            first_release_handoff_clearance_key: first_release_handoff_clearance
                .map(|row| row.slot_clearance_key.clone()),
            first_slot_key: first_clearance_row.map(|row| row.slot_key.clone()),
            first_disposition_action_key: first_clearance_row
                .map(|row| row.disposition_action_key.clone()),
            first_disposition_key: first_clearance_row.map(|row| row.disposition_key.clone()),
            first_work_order_key: first_clearance_row.map(|row| row.work_order_key.clone()),
            first_ticket_key: first_clearance_row.map(|row| row.ticket_key.clone()),
            first_slot_clearance_kind: first_clearance_row.map(|row| row.slot_clearance_kind),
            first_disposition_action_kind: first_clearance_row
                .map(|row| row.disposition_action_kind),
            first_disposition_kind: first_clearance_row.map(|row| row.disposition_kind),
            first_handoff_lane: first_clearance_row.map(|row| row.handoff_lane),
            first_check_kind: first_clearance_row.map(|row| row.check_kind),
            first_check_status: first_clearance_row.map(|row| row.status),
            release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearances_ready:
                !clearance_rows.is_empty() && clearance_rows.iter().all(|row| row.ready_to_clear()),
        }
    }

    pub fn has_slot_clearance_rows(&self) -> bool {
        self.total_slot_clearance_rows > 0
    }

    pub fn has_required_clearances(&self) -> bool {
        self.required_clearances > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.release_blocking_clearances > 0 || self.repair_required_clearances > 0
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_clearances > 0 || self.operator_handoff_clearances > 0
    }

    pub fn needs_review(&self) -> bool {
        self.review_required_clearances_count > 0 || self.review_required_clearances > 0
    }

    pub fn has_lineage_gaps(&self) -> bool {
        self.lineage_gap_clearances > 0
    }

    pub fn has_release_handoff_clearances(&self) -> bool {
        self.release_handoff_clearances > 0
    }

    pub fn has_complete_lineage(&self) -> bool {
        self.lineage_complete_clearances == self.total_slot_clearance_rows
    }

    pub fn requires_attention(&self) -> bool {
        self.has_required_clearances()
            || self.has_blockers()
            || self.needs_operator()
            || self.needs_review()
            || self.dispatch_required_clearances > 0
            || self.execution_required_clearances > 0
            || self.has_lineage_gaps()
            || !self
                .release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearances_ready
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionKind
{
    ScheduleRepair,
    CoordinateOperatorHandoff,
    CompleteReview,
    RestoreLineage,
    ReleaseHandoff,
}

impl
    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionKind
{
    pub fn as_str(self) -> &'static str {
        match self {
            Self::ScheduleRepair => "schedule_repair",
            Self::CoordinateOperatorHandoff => "coordinate_operator_handoff",
            Self::CompleteReview => "complete_review",
            Self::RestoreLineage => "restore_lineage",
            Self::ReleaseHandoff => "release_handoff",
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionRow {
    pub sequence: usize,
    pub slot_clearance_action_key: String,
    pub slot_clearance_sequence: usize,
    pub slot_clearance_key: String,
    pub slot_sequence: usize,
    pub slot_key: String,
    pub disposition_action_key: String,
    pub disposition_key: String,
    pub review_key: String,
    pub evidence_key: String,
    pub clearance_action_key: String,
    pub clearance_key: String,
    pub audit_key: String,
    pub guardrail_key: String,
    pub work_order_key: String,
    pub execution_slot_key: String,
    pub packet_key: String,
    pub ticket_key: String,
    pub check_sequence: usize,
    pub check_kind: IntegrationMeshReleaseReadinessCheckKind,
    pub status: IntegrationMeshReleaseReadinessStatus,
    pub handoff_lane: IntegrationMeshReleaseDispatchTicketHandoffLane,
    pub disposition_action_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionKind,
    pub disposition_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind,
    pub slot_clearance_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceKind,
    pub slot_clearance_action_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionKind,
    pub release_blocking: bool,
    pub operator_required: bool,
    pub review_required: bool,
    pub dispatch_required: bool,
    pub execution_required: bool,
    pub lineage_complete: bool,
    pub clearance_action_required: bool,
    pub cleared_for_handoff: bool,
    pub action_ready: bool,
}

impl
    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionRow
{
    pub fn from_slot_clearance_row(
        sequence: usize,
        clearance_row: &IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceRow,
    ) -> Self {
        let slot_clearance_action_kind = if !clearance_row.has_lineage() {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionKind::RestoreLineage
        } else if clearance_row.is_repair_clearance() || clearance_row.blocks_release() {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionKind::ScheduleRepair
        } else if clearance_row.is_review_clearance() || clearance_row.needs_review() {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionKind::CompleteReview
        } else if clearance_row.is_operator_handoff_clearance() || clearance_row.needs_operator() {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionKind::CoordinateOperatorHandoff
        } else {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionKind::ReleaseHandoff
        };
        let action_ready = slot_clearance_action_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionKind::ReleaseHandoff
            && clearance_row.ready_to_clear();

        Self {
            sequence,
            slot_clearance_action_key: format!(
                "release-ticket-handoff-disposition-action-slot-clearance-action-{sequence:02}-{}-{}",
                slot_clearance_action_kind.as_str(),
                clearance_row.check_kind.as_str()
            ),
            slot_clearance_sequence: clearance_row.sequence,
            slot_clearance_key: clearance_row.slot_clearance_key.clone(),
            slot_sequence: clearance_row.slot_sequence,
            slot_key: clearance_row.slot_key.clone(),
            disposition_action_key: clearance_row.disposition_action_key.clone(),
            disposition_key: clearance_row.disposition_key.clone(),
            review_key: clearance_row.review_key.clone(),
            evidence_key: clearance_row.evidence_key.clone(),
            clearance_action_key: clearance_row.clearance_action_key.clone(),
            clearance_key: clearance_row.clearance_key.clone(),
            audit_key: clearance_row.audit_key.clone(),
            guardrail_key: clearance_row.guardrail_key.clone(),
            work_order_key: clearance_row.work_order_key.clone(),
            execution_slot_key: clearance_row.execution_slot_key.clone(),
            packet_key: clearance_row.packet_key.clone(),
            ticket_key: clearance_row.ticket_key.clone(),
            check_sequence: clearance_row.check_sequence,
            check_kind: clearance_row.check_kind,
            status: clearance_row.status,
            handoff_lane: clearance_row.handoff_lane,
            disposition_action_kind: clearance_row.disposition_action_kind,
            disposition_kind: clearance_row.disposition_kind,
            slot_clearance_kind: clearance_row.slot_clearance_kind,
            slot_clearance_action_kind,
            release_blocking: clearance_row.blocks_release(),
            operator_required: clearance_row.needs_operator(),
            review_required: clearance_row.needs_review(),
            dispatch_required: clearance_row.dispatch_required,
            execution_required: clearance_row.execution_required,
            lineage_complete: clearance_row.has_lineage(),
            clearance_action_required: clearance_row.requires_attention(),
            cleared_for_handoff: clearance_row.ready_to_clear(),
            action_ready,
        }
    }

    pub fn requires_action(&self) -> bool {
        self.clearance_action_required
    }

    pub fn blocks_release(&self) -> bool {
        self.release_blocking
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required
    }

    pub fn needs_review(&self) -> bool {
        self.review_required
    }

    pub fn has_lineage(&self) -> bool {
        self.lineage_complete
    }

    pub fn is_repair_action(&self) -> bool {
        self.slot_clearance_action_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionKind::ScheduleRepair
    }

    pub fn is_operator_handoff_action(&self) -> bool {
        self.slot_clearance_action_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionKind::CoordinateOperatorHandoff
    }

    pub fn is_review_action(&self) -> bool {
        self.slot_clearance_action_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionKind::CompleteReview
    }

    pub fn is_lineage_action(&self) -> bool {
        self.slot_clearance_action_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionKind::RestoreLineage
    }

    pub fn is_release_handoff_action(&self) -> bool {
        self.slot_clearance_action_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionKind::ReleaseHandoff
    }

    pub fn ready_to_release_handoff(&self) -> bool {
        self.action_ready
    }

    pub fn requires_attention(&self) -> bool {
        self.requires_action()
            || self.blocks_release()
            || self.needs_operator()
            || self.needs_review()
            || self.dispatch_required
            || self.execution_required
            || !self.has_lineage()
            || !self.ready_to_release_handoff()
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionSummary {
    pub total_slot_clearance_action_rows: usize,
    pub repair_actions: usize,
    pub operator_handoff_actions: usize,
    pub review_actions: usize,
    pub lineage_actions: usize,
    pub release_handoff_actions: usize,
    pub required_actions: usize,
    pub release_blocking_actions: usize,
    pub operator_required_actions: usize,
    pub review_required_actions: usize,
    pub dispatch_required_actions: usize,
    pub execution_required_actions: usize,
    pub lineage_complete_actions: usize,
    pub ready_actions: usize,
    pub first_slot_clearance_action_key: Option<String>,
    pub first_required_action_key: Option<String>,
    pub first_repair_action_key: Option<String>,
    pub first_operator_handoff_action_key: Option<String>,
    pub first_review_action_key: Option<String>,
    pub first_lineage_action_key: Option<String>,
    pub first_release_handoff_action_key: Option<String>,
    pub first_slot_clearance_key: Option<String>,
    pub first_slot_key: Option<String>,
    pub first_disposition_action_key: Option<String>,
    pub first_disposition_key: Option<String>,
    pub first_work_order_key: Option<String>,
    pub first_ticket_key: Option<String>,
    pub first_slot_clearance_action_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionKind,
    >,
    pub first_slot_clearance_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceKind,
    >,
    pub first_disposition_action_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionKind,
    >,
    pub first_disposition_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind,
    >,
    pub first_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub first_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub first_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_actions_ready: bool,
}

impl
    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionSummary
{
    pub fn from_action_rows<'a>(
        action_rows: impl IntoIterator<
            Item = &'a IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionRow,
        >,
    ) -> Self {
        let action_rows = action_rows.into_iter().collect::<Vec<_>>();
        let first_action_row = action_rows.iter().min_by_key(|row| row.sequence);
        let first_required_action = action_rows
            .iter()
            .filter(|row| row.requires_action())
            .min_by_key(|row| row.sequence);
        let first_repair_action = action_rows
            .iter()
            .filter(|row| row.is_repair_action())
            .min_by_key(|row| row.sequence);
        let first_operator_handoff_action = action_rows
            .iter()
            .filter(|row| row.is_operator_handoff_action())
            .min_by_key(|row| row.sequence);
        let first_review_action = action_rows
            .iter()
            .filter(|row| row.is_review_action())
            .min_by_key(|row| row.sequence);
        let first_lineage_action = action_rows
            .iter()
            .filter(|row| row.is_lineage_action())
            .min_by_key(|row| row.sequence);
        let first_release_handoff_action = action_rows
            .iter()
            .filter(|row| row.is_release_handoff_action())
            .min_by_key(|row| row.sequence);

        Self {
            total_slot_clearance_action_rows: action_rows.len(),
            repair_actions: action_rows
                .iter()
                .filter(|row| row.is_repair_action())
                .count(),
            operator_handoff_actions: action_rows
                .iter()
                .filter(|row| row.is_operator_handoff_action())
                .count(),
            review_actions: action_rows
                .iter()
                .filter(|row| row.is_review_action())
                .count(),
            lineage_actions: action_rows
                .iter()
                .filter(|row| row.is_lineage_action())
                .count(),
            release_handoff_actions: action_rows
                .iter()
                .filter(|row| row.is_release_handoff_action())
                .count(),
            required_actions: action_rows
                .iter()
                .filter(|row| row.requires_action())
                .count(),
            release_blocking_actions: action_rows
                .iter()
                .filter(|row| row.blocks_release())
                .count(),
            operator_required_actions: action_rows
                .iter()
                .filter(|row| row.needs_operator())
                .count(),
            review_required_actions: action_rows
                .iter()
                .filter(|row| row.needs_review())
                .count(),
            dispatch_required_actions: action_rows
                .iter()
                .filter(|row| row.dispatch_required)
                .count(),
            execution_required_actions: action_rows
                .iter()
                .filter(|row| row.execution_required)
                .count(),
            lineage_complete_actions: action_rows
                .iter()
                .filter(|row| row.has_lineage())
                .count(),
            ready_actions: action_rows
                .iter()
                .filter(|row| row.ready_to_release_handoff())
                .count(),
            first_slot_clearance_action_key: first_action_row
                .map(|row| row.slot_clearance_action_key.clone()),
            first_required_action_key: first_required_action
                .map(|row| row.slot_clearance_action_key.clone()),
            first_repair_action_key: first_repair_action
                .map(|row| row.slot_clearance_action_key.clone()),
            first_operator_handoff_action_key: first_operator_handoff_action
                .map(|row| row.slot_clearance_action_key.clone()),
            first_review_action_key: first_review_action
                .map(|row| row.slot_clearance_action_key.clone()),
            first_lineage_action_key: first_lineage_action
                .map(|row| row.slot_clearance_action_key.clone()),
            first_release_handoff_action_key: first_release_handoff_action
                .map(|row| row.slot_clearance_action_key.clone()),
            first_slot_clearance_key: first_action_row
                .map(|row| row.slot_clearance_key.clone()),
            first_slot_key: first_action_row.map(|row| row.slot_key.clone()),
            first_disposition_action_key: first_action_row
                .map(|row| row.disposition_action_key.clone()),
            first_disposition_key: first_action_row.map(|row| row.disposition_key.clone()),
            first_work_order_key: first_action_row.map(|row| row.work_order_key.clone()),
            first_ticket_key: first_action_row.map(|row| row.ticket_key.clone()),
            first_slot_clearance_action_kind: first_action_row
                .map(|row| row.slot_clearance_action_kind),
            first_slot_clearance_kind: first_action_row.map(|row| row.slot_clearance_kind),
            first_disposition_action_kind: first_action_row
                .map(|row| row.disposition_action_kind),
            first_disposition_kind: first_action_row.map(|row| row.disposition_kind),
            first_handoff_lane: first_action_row.map(|row| row.handoff_lane),
            first_check_kind: first_action_row.map(|row| row.check_kind),
            first_check_status: first_action_row.map(|row| row.status),
            release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_actions_ready:
                !action_rows.is_empty()
                    && action_rows
                        .iter()
                        .all(|row| row.ready_to_release_handoff()),
        }
    }

    pub fn has_slot_clearance_action_rows(&self) -> bool {
        self.total_slot_clearance_action_rows > 0
    }

    pub fn has_required_actions(&self) -> bool {
        self.required_actions > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.release_blocking_actions > 0 || self.repair_actions > 0
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_actions > 0 || self.operator_handoff_actions > 0
    }

    pub fn needs_review(&self) -> bool {
        self.review_required_actions > 0 || self.review_actions > 0
    }

    pub fn has_lineage_gaps(&self) -> bool {
        self.lineage_actions > 0
    }

    pub fn has_release_handoff_actions(&self) -> bool {
        self.release_handoff_actions > 0
    }

    pub fn has_complete_lineage(&self) -> bool {
        self.lineage_complete_actions == self.total_slot_clearance_action_rows
    }

    pub fn requires_attention(&self) -> bool {
        self.has_required_actions()
            || self.has_blockers()
            || self.needs_operator()
            || self.needs_review()
            || self.dispatch_required_actions > 0
            || self.execution_required_actions > 0
            || self.has_lineage_gaps()
            || !self
                .release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_actions_ready
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceKind
{
    RepairEvidence,
    OperatorHandoffEvidence,
    ReviewEvidence,
    LineageEvidence,
    ReleaseHandoffEvidence,
}

impl
    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceKind
{
    pub fn as_str(self) -> &'static str {
        match self {
            Self::RepairEvidence => "repair_evidence",
            Self::OperatorHandoffEvidence => "operator_handoff_evidence",
            Self::ReviewEvidence => "review_evidence",
            Self::LineageEvidence => "lineage_evidence",
            Self::ReleaseHandoffEvidence => "release_handoff_evidence",
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceRow {
    pub sequence: usize,
    pub slot_clearance_action_evidence_key: String,
    pub slot_clearance_action_sequence: usize,
    pub slot_clearance_action_key: String,
    pub slot_clearance_sequence: usize,
    pub slot_clearance_key: String,
    pub slot_sequence: usize,
    pub slot_key: String,
    pub disposition_action_key: String,
    pub disposition_key: String,
    pub review_key: String,
    pub evidence_key: String,
    pub clearance_action_key: String,
    pub clearance_key: String,
    pub audit_key: String,
    pub guardrail_key: String,
    pub work_order_key: String,
    pub execution_slot_key: String,
    pub packet_key: String,
    pub ticket_key: String,
    pub check_sequence: usize,
    pub check_kind: IntegrationMeshReleaseReadinessCheckKind,
    pub status: IntegrationMeshReleaseReadinessStatus,
    pub handoff_lane: IntegrationMeshReleaseDispatchTicketHandoffLane,
    pub disposition_action_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionKind,
    pub disposition_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind,
    pub slot_clearance_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceKind,
    pub slot_clearance_action_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionKind,
    pub slot_clearance_action_evidence_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceKind,
    pub release_blocking: bool,
    pub operator_required: bool,
    pub review_required: bool,
    pub dispatch_required: bool,
    pub execution_required: bool,
    pub lineage_complete: bool,
    pub action_required: bool,
    pub release_handoff_evidence: bool,
    pub action_evidence_ready: bool,
}

impl
    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceRow
{
    pub fn from_slot_clearance_action_row(
        sequence: usize,
        action_row: &IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionRow,
    ) -> Self {
        let slot_clearance_action_evidence_kind = match action_row.slot_clearance_action_kind {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionKind::ScheduleRepair => {
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceKind::RepairEvidence
            }
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionKind::CoordinateOperatorHandoff => {
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceKind::OperatorHandoffEvidence
            }
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionKind::CompleteReview => {
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceKind::ReviewEvidence
            }
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionKind::RestoreLineage => {
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceKind::LineageEvidence
            }
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionKind::ReleaseHandoff => {
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceKind::ReleaseHandoffEvidence
            }
        };
        let lineage_complete = action_row.has_lineage()
            && action_row.check_sequence > 0
            && !action_row.slot_clearance_action_key.is_empty()
            && !action_row.slot_clearance_key.is_empty()
            && !action_row.slot_key.is_empty()
            && !action_row.disposition_action_key.is_empty()
            && !action_row.disposition_key.is_empty()
            && !action_row.review_key.is_empty()
            && !action_row.evidence_key.is_empty()
            && !action_row.clearance_action_key.is_empty()
            && !action_row.clearance_key.is_empty()
            && !action_row.audit_key.is_empty()
            && !action_row.guardrail_key.is_empty()
            && !action_row.work_order_key.is_empty()
            && !action_row.execution_slot_key.is_empty()
            && !action_row.packet_key.is_empty()
            && !action_row.ticket_key.is_empty();
        let release_handoff_evidence = slot_clearance_action_evidence_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceKind::ReleaseHandoffEvidence;
        let action_evidence_ready =
            release_handoff_evidence && action_row.ready_to_release_handoff() && lineage_complete;

        Self {
            sequence,
            slot_clearance_action_evidence_key: format!(
                "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-{sequence:02}-{}-{}",
                slot_clearance_action_evidence_kind.as_str(),
                action_row.check_kind.as_str()
            ),
            slot_clearance_action_sequence: action_row.sequence,
            slot_clearance_action_key: action_row.slot_clearance_action_key.clone(),
            slot_clearance_sequence: action_row.slot_clearance_sequence,
            slot_clearance_key: action_row.slot_clearance_key.clone(),
            slot_sequence: action_row.slot_sequence,
            slot_key: action_row.slot_key.clone(),
            disposition_action_key: action_row.disposition_action_key.clone(),
            disposition_key: action_row.disposition_key.clone(),
            review_key: action_row.review_key.clone(),
            evidence_key: action_row.evidence_key.clone(),
            clearance_action_key: action_row.clearance_action_key.clone(),
            clearance_key: action_row.clearance_key.clone(),
            audit_key: action_row.audit_key.clone(),
            guardrail_key: action_row.guardrail_key.clone(),
            work_order_key: action_row.work_order_key.clone(),
            execution_slot_key: action_row.execution_slot_key.clone(),
            packet_key: action_row.packet_key.clone(),
            ticket_key: action_row.ticket_key.clone(),
            check_sequence: action_row.check_sequence,
            check_kind: action_row.check_kind,
            status: action_row.status,
            handoff_lane: action_row.handoff_lane,
            disposition_action_kind: action_row.disposition_action_kind,
            disposition_kind: action_row.disposition_kind,
            slot_clearance_kind: action_row.slot_clearance_kind,
            slot_clearance_action_kind: action_row.slot_clearance_action_kind,
            slot_clearance_action_evidence_kind,
            release_blocking: action_row.blocks_release(),
            operator_required: action_row.needs_operator(),
            review_required: action_row.needs_review(),
            dispatch_required: action_row.dispatch_required,
            execution_required: action_row.execution_required,
            lineage_complete,
            action_required: action_row.requires_action(),
            release_handoff_evidence,
            action_evidence_ready,
        }
    }

    pub fn requires_action(&self) -> bool {
        self.action_required
    }

    pub fn blocks_release(&self) -> bool {
        self.release_blocking
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required
    }

    pub fn needs_review(&self) -> bool {
        self.review_required
    }

    pub fn has_lineage(&self) -> bool {
        self.lineage_complete
    }

    pub fn is_repair_evidence(&self) -> bool {
        self.slot_clearance_action_evidence_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceKind::RepairEvidence
    }

    pub fn is_operator_handoff_evidence(&self) -> bool {
        self.slot_clearance_action_evidence_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceKind::OperatorHandoffEvidence
    }

    pub fn is_review_evidence(&self) -> bool {
        self.slot_clearance_action_evidence_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceKind::ReviewEvidence
    }

    pub fn is_lineage_evidence(&self) -> bool {
        self.slot_clearance_action_evidence_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceKind::LineageEvidence
    }

    pub fn is_release_handoff_evidence(&self) -> bool {
        self.release_handoff_evidence
    }

    pub fn ready_to_release_handoff(&self) -> bool {
        self.action_evidence_ready
    }

    pub fn requires_attention(&self) -> bool {
        self.requires_action()
            || self.blocks_release()
            || self.needs_operator()
            || self.needs_review()
            || self.dispatch_required
            || self.execution_required
            || !self.has_lineage()
            || !self.ready_to_release_handoff()
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceSummary {
    pub total_slot_clearance_action_evidence_rows: usize,
    pub repair_evidence: usize,
    pub operator_handoff_evidence: usize,
    pub review_evidence: usize,
    pub lineage_evidence: usize,
    pub release_handoff_evidence: usize,
    pub required_evidence: usize,
    pub release_blocking_evidence: usize,
    pub operator_required_evidence: usize,
    pub review_required_evidence: usize,
    pub dispatch_required_evidence: usize,
    pub execution_required_evidence: usize,
    pub lineage_complete_evidence: usize,
    pub ready_evidence: usize,
    pub first_slot_clearance_action_evidence_key: Option<String>,
    pub first_required_evidence_key: Option<String>,
    pub first_repair_evidence_key: Option<String>,
    pub first_operator_handoff_evidence_key: Option<String>,
    pub first_review_evidence_key: Option<String>,
    pub first_lineage_evidence_key: Option<String>,
    pub first_release_handoff_evidence_key: Option<String>,
    pub first_slot_clearance_action_key: Option<String>,
    pub first_slot_clearance_key: Option<String>,
    pub first_slot_key: Option<String>,
    pub first_disposition_action_key: Option<String>,
    pub first_work_order_key: Option<String>,
    pub first_ticket_key: Option<String>,
    pub first_slot_clearance_action_evidence_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceKind,
    >,
    pub first_slot_clearance_action_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionKind,
    >,
    pub first_slot_clearance_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceKind,
    >,
    pub first_disposition_action_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionKind,
    >,
    pub first_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub first_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub first_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_ready: bool,
}

impl
    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceSummary
{
    pub fn from_action_evidence_rows<'a>(
        evidence_rows: impl IntoIterator<
            Item = &'a IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceRow,
        >,
    ) -> Self {
        let evidence_rows = evidence_rows.into_iter().collect::<Vec<_>>();
        let first_evidence_row = evidence_rows.iter().min_by_key(|row| row.sequence);
        let first_required_evidence = evidence_rows
            .iter()
            .filter(|row| row.requires_action())
            .min_by_key(|row| row.sequence);
        let first_repair_evidence = evidence_rows
            .iter()
            .filter(|row| row.is_repair_evidence())
            .min_by_key(|row| row.sequence);
        let first_operator_handoff_evidence = evidence_rows
            .iter()
            .filter(|row| row.is_operator_handoff_evidence())
            .min_by_key(|row| row.sequence);
        let first_review_evidence = evidence_rows
            .iter()
            .filter(|row| row.is_review_evidence())
            .min_by_key(|row| row.sequence);
        let first_lineage_evidence = evidence_rows
            .iter()
            .filter(|row| row.is_lineage_evidence())
            .min_by_key(|row| row.sequence);
        let first_release_handoff_evidence = evidence_rows
            .iter()
            .filter(|row| row.is_release_handoff_evidence())
            .min_by_key(|row| row.sequence);

        Self {
            total_slot_clearance_action_evidence_rows: evidence_rows.len(),
            repair_evidence: evidence_rows
                .iter()
                .filter(|row| row.is_repair_evidence())
                .count(),
            operator_handoff_evidence: evidence_rows
                .iter()
                .filter(|row| row.is_operator_handoff_evidence())
                .count(),
            review_evidence: evidence_rows
                .iter()
                .filter(|row| row.is_review_evidence())
                .count(),
            lineage_evidence: evidence_rows
                .iter()
                .filter(|row| row.is_lineage_evidence())
                .count(),
            release_handoff_evidence: evidence_rows
                .iter()
                .filter(|row| row.is_release_handoff_evidence())
                .count(),
            required_evidence: evidence_rows
                .iter()
                .filter(|row| row.requires_action())
                .count(),
            release_blocking_evidence: evidence_rows
                .iter()
                .filter(|row| row.blocks_release())
                .count(),
            operator_required_evidence: evidence_rows
                .iter()
                .filter(|row| row.needs_operator())
                .count(),
            review_required_evidence: evidence_rows
                .iter()
                .filter(|row| row.needs_review())
                .count(),
            dispatch_required_evidence: evidence_rows
                .iter()
                .filter(|row| row.dispatch_required)
                .count(),
            execution_required_evidence: evidence_rows
                .iter()
                .filter(|row| row.execution_required)
                .count(),
            lineage_complete_evidence: evidence_rows
                .iter()
                .filter(|row| row.has_lineage())
                .count(),
            ready_evidence: evidence_rows
                .iter()
                .filter(|row| row.ready_to_release_handoff())
                .count(),
            first_slot_clearance_action_evidence_key: first_evidence_row
                .map(|row| row.slot_clearance_action_evidence_key.clone()),
            first_required_evidence_key: first_required_evidence
                .map(|row| row.slot_clearance_action_evidence_key.clone()),
            first_repair_evidence_key: first_repair_evidence
                .map(|row| row.slot_clearance_action_evidence_key.clone()),
            first_operator_handoff_evidence_key: first_operator_handoff_evidence
                .map(|row| row.slot_clearance_action_evidence_key.clone()),
            first_review_evidence_key: first_review_evidence
                .map(|row| row.slot_clearance_action_evidence_key.clone()),
            first_lineage_evidence_key: first_lineage_evidence
                .map(|row| row.slot_clearance_action_evidence_key.clone()),
            first_release_handoff_evidence_key: first_release_handoff_evidence
                .map(|row| row.slot_clearance_action_evidence_key.clone()),
            first_slot_clearance_action_key: first_evidence_row
                .map(|row| row.slot_clearance_action_key.clone()),
            first_slot_clearance_key: first_evidence_row
                .map(|row| row.slot_clearance_key.clone()),
            first_slot_key: first_evidence_row.map(|row| row.slot_key.clone()),
            first_disposition_action_key: first_evidence_row
                .map(|row| row.disposition_action_key.clone()),
            first_work_order_key: first_evidence_row.map(|row| row.work_order_key.clone()),
            first_ticket_key: first_evidence_row.map(|row| row.ticket_key.clone()),
            first_slot_clearance_action_evidence_kind: first_evidence_row
                .map(|row| row.slot_clearance_action_evidence_kind),
            first_slot_clearance_action_kind: first_evidence_row
                .map(|row| row.slot_clearance_action_kind),
            first_slot_clearance_kind: first_evidence_row.map(|row| row.slot_clearance_kind),
            first_disposition_action_kind: first_evidence_row
                .map(|row| row.disposition_action_kind),
            first_handoff_lane: first_evidence_row.map(|row| row.handoff_lane),
            first_check_kind: first_evidence_row.map(|row| row.check_kind),
            first_check_status: first_evidence_row.map(|row| row.status),
            release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_ready:
                !evidence_rows.is_empty()
                    && evidence_rows
                        .iter()
                        .all(|row| row.ready_to_release_handoff()),
        }
    }

    pub fn has_slot_clearance_action_evidence_rows(&self) -> bool {
        self.total_slot_clearance_action_evidence_rows > 0
    }

    pub fn has_required_evidence(&self) -> bool {
        self.required_evidence > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.release_blocking_evidence > 0 || self.repair_evidence > 0
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_evidence > 0 || self.operator_handoff_evidence > 0
    }

    pub fn needs_review(&self) -> bool {
        self.review_required_evidence > 0 || self.review_evidence > 0
    }

    pub fn has_lineage_gaps(&self) -> bool {
        self.lineage_evidence > 0
    }

    pub fn has_release_handoff_evidence(&self) -> bool {
        self.release_handoff_evidence > 0
    }

    pub fn has_complete_lineage(&self) -> bool {
        self.lineage_complete_evidence == self.total_slot_clearance_action_evidence_rows
    }

    pub fn requires_attention(&self) -> bool {
        self.has_required_evidence()
            || self.has_blockers()
            || self.needs_operator()
            || self.needs_review()
            || self.dispatch_required_evidence > 0
            || self.execution_required_evidence > 0
            || self.has_lineage_gaps()
            || !self
                .release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_ready
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReview {
    pub sequence: usize,
    pub slot_clearance_action_evidence_review_key: String,
    pub slot_clearance_action_evidence_sequence: usize,
    pub slot_clearance_action_evidence_key: String,
    pub slot_clearance_action_sequence: usize,
    pub slot_clearance_action_key: String,
    pub slot_clearance_sequence: usize,
    pub slot_clearance_key: String,
    pub slot_sequence: usize,
    pub slot_key: String,
    pub disposition_action_key: String,
    pub disposition_key: String,
    pub review_key: String,
    pub evidence_key: String,
    pub clearance_action_key: String,
    pub clearance_key: String,
    pub audit_key: String,
    pub guardrail_key: String,
    pub work_order_key: String,
    pub execution_slot_key: String,
    pub packet_key: String,
    pub ticket_key: String,
    pub check_sequence: usize,
    pub check_kind: IntegrationMeshReleaseReadinessCheckKind,
    pub status: IntegrationMeshReleaseReadinessStatus,
    pub handoff_lane: IntegrationMeshReleaseDispatchTicketHandoffLane,
    pub disposition_action_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionKind,
    pub disposition_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind,
    pub slot_clearance_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceKind,
    pub slot_clearance_action_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionKind,
    pub slot_clearance_action_evidence_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceKind,
    pub review_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind,
    pub release_blocking: bool,
    pub operator_required: bool,
    pub review_required: bool,
    pub dispatch_required: bool,
    pub execution_required: bool,
    pub lineage_complete: bool,
    pub action_required: bool,
    pub release_handoff_evidence: bool,
    pub release_ready: bool,
}

impl
    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReview
{
    pub fn from_slot_clearance_action_evidence_row(
        sequence: usize,
        evidence_row: &IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceRow,
    ) -> Self {
        let review_kind = if !evidence_row.has_lineage() {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::LineageGap
        } else if evidence_row.blocks_release() {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::BlockerReview
        } else if evidence_row.needs_review() {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::ReviewGate
        } else if evidence_row.needs_operator() {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::OperatorReview
        } else {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::ReleaseReady
        };
        let release_ready = evidence_row.ready_to_release_handoff()
            && evidence_row.has_lineage()
            && !evidence_row.blocks_release()
            && !evidence_row.needs_operator()
            && !evidence_row.needs_review()
            && !evidence_row.dispatch_required
            && !evidence_row.execution_required;

        Self {
            sequence,
            slot_clearance_action_evidence_review_key: format!(
                "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-review-{sequence:02}-{}-{}",
                review_kind.as_str(),
                evidence_row.check_kind.as_str()
            ),
            slot_clearance_action_evidence_sequence: evidence_row.sequence,
            slot_clearance_action_evidence_key: evidence_row
                .slot_clearance_action_evidence_key
                .clone(),
            slot_clearance_action_sequence: evidence_row.slot_clearance_action_sequence,
            slot_clearance_action_key: evidence_row.slot_clearance_action_key.clone(),
            slot_clearance_sequence: evidence_row.slot_clearance_sequence,
            slot_clearance_key: evidence_row.slot_clearance_key.clone(),
            slot_sequence: evidence_row.slot_sequence,
            slot_key: evidence_row.slot_key.clone(),
            disposition_action_key: evidence_row.disposition_action_key.clone(),
            disposition_key: evidence_row.disposition_key.clone(),
            review_key: evidence_row.review_key.clone(),
            evidence_key: evidence_row.evidence_key.clone(),
            clearance_action_key: evidence_row.clearance_action_key.clone(),
            clearance_key: evidence_row.clearance_key.clone(),
            audit_key: evidence_row.audit_key.clone(),
            guardrail_key: evidence_row.guardrail_key.clone(),
            work_order_key: evidence_row.work_order_key.clone(),
            execution_slot_key: evidence_row.execution_slot_key.clone(),
            packet_key: evidence_row.packet_key.clone(),
            ticket_key: evidence_row.ticket_key.clone(),
            check_sequence: evidence_row.check_sequence,
            check_kind: evidence_row.check_kind,
            status: evidence_row.status,
            handoff_lane: evidence_row.handoff_lane,
            disposition_action_kind: evidence_row.disposition_action_kind,
            disposition_kind: evidence_row.disposition_kind,
            slot_clearance_kind: evidence_row.slot_clearance_kind,
            slot_clearance_action_kind: evidence_row.slot_clearance_action_kind,
            slot_clearance_action_evidence_kind: evidence_row.slot_clearance_action_evidence_kind,
            review_kind,
            release_blocking: evidence_row.blocks_release(),
            operator_required: evidence_row.needs_operator(),
            review_required: evidence_row.needs_review(),
            dispatch_required: evidence_row.dispatch_required,
            execution_required: evidence_row.execution_required,
            lineage_complete: evidence_row.has_lineage(),
            action_required: evidence_row.requires_action(),
            release_handoff_evidence: evidence_row.is_release_handoff_evidence(),
            release_ready,
        }
    }

    pub fn requires_action(&self) -> bool {
        self.action_required
    }

    pub fn blocks_release(&self) -> bool {
        self.release_blocking
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required
    }

    pub fn needs_review(&self) -> bool {
        self.review_required
    }

    pub fn has_lineage(&self) -> bool {
        self.lineage_complete
    }

    pub fn is_release_handoff_evidence(&self) -> bool {
        self.release_handoff_evidence
    }

    pub fn is_release_ready(&self) -> bool {
        self.release_ready
    }

    pub fn requires_attention(&self) -> bool {
        self.requires_action()
            || self.blocks_release()
            || self.needs_operator()
            || self.needs_review()
            || self.dispatch_required
            || self.execution_required
            || !self.has_lineage()
            || !self.is_release_ready()
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewSummary {
    pub total_slot_clearance_action_evidence_reviews: usize,
    pub required_reviews: usize,
    pub blocking_reviews: usize,
    pub operator_required_reviews: usize,
    pub review_required_reviews: usize,
    pub dispatch_required_reviews: usize,
    pub execution_required_reviews: usize,
    pub lineage_gap_reviews: usize,
    pub release_ready_reviews: usize,
    pub lineage_complete_reviews: usize,
    pub release_handoff_evidence_reviews: usize,
    pub first_slot_clearance_action_evidence_review_key: Option<String>,
    pub first_required_review_key: Option<String>,
    pub first_blocking_review_key: Option<String>,
    pub first_operator_review_key: Option<String>,
    pub first_review_gate_key: Option<String>,
    pub first_lineage_gap_review_key: Option<String>,
    pub first_release_ready_review_key: Option<String>,
    pub first_slot_clearance_action_evidence_key: Option<String>,
    pub first_slot_clearance_action_key: Option<String>,
    pub first_slot_clearance_key: Option<String>,
    pub first_slot_key: Option<String>,
    pub first_disposition_action_key: Option<String>,
    pub first_work_order_key: Option<String>,
    pub first_ticket_key: Option<String>,
    pub first_review_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind,
    >,
    pub first_slot_clearance_action_evidence_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceKind,
    >,
    pub first_slot_clearance_action_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionKind,
    >,
    pub first_slot_clearance_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceKind,
    >,
    pub first_disposition_action_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionKind,
    >,
    pub first_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub first_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub first_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_reviews_ready: bool,
}

impl
    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewSummary
{
    pub fn from_action_evidence_reviews<'a>(
        reviews: impl IntoIterator<
            Item = &'a IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReview,
        >,
    ) -> Self {
        let reviews = reviews.into_iter().collect::<Vec<_>>();
        let first_review = reviews.iter().min_by_key(|row| row.sequence);
        let first_required_review = reviews
            .iter()
            .filter(|row| row.requires_action())
            .min_by_key(|row| row.sequence);
        let first_blocking_review = reviews
            .iter()
            .filter(|row| row.blocks_release())
            .min_by_key(|row| row.sequence);
        let first_operator_review = reviews
            .iter()
            .filter(|row| row.needs_operator())
            .min_by_key(|row| row.sequence);
        let first_review_gate = reviews
            .iter()
            .filter(|row| row.needs_review())
            .min_by_key(|row| row.sequence);
        let first_lineage_gap_review = reviews
            .iter()
            .filter(|row| !row.has_lineage())
            .min_by_key(|row| row.sequence);
        let first_release_ready_review = reviews
            .iter()
            .filter(|row| row.is_release_ready())
            .min_by_key(|row| row.sequence);

        Self {
            total_slot_clearance_action_evidence_reviews: reviews.len(),
            required_reviews: reviews.iter().filter(|row| row.requires_action()).count(),
            blocking_reviews: reviews
                .iter()
                .filter(|row| row.blocks_release())
                .count(),
            operator_required_reviews: reviews
                .iter()
                .filter(|row| row.needs_operator())
                .count(),
            review_required_reviews: reviews.iter().filter(|row| row.needs_review()).count(),
            dispatch_required_reviews: reviews
                .iter()
                .filter(|row| row.dispatch_required)
                .count(),
            execution_required_reviews: reviews
                .iter()
                .filter(|row| row.execution_required)
                .count(),
            lineage_gap_reviews: reviews.iter().filter(|row| !row.has_lineage()).count(),
            release_ready_reviews: reviews
                .iter()
                .filter(|row| row.is_release_ready())
                .count(),
            lineage_complete_reviews: reviews.iter().filter(|row| row.has_lineage()).count(),
            release_handoff_evidence_reviews: reviews
                .iter()
                .filter(|row| row.is_release_handoff_evidence())
                .count(),
            first_slot_clearance_action_evidence_review_key: first_review
                .map(|row| row.slot_clearance_action_evidence_review_key.clone()),
            first_required_review_key: first_required_review
                .map(|row| row.slot_clearance_action_evidence_review_key.clone()),
            first_blocking_review_key: first_blocking_review
                .map(|row| row.slot_clearance_action_evidence_review_key.clone()),
            first_operator_review_key: first_operator_review
                .map(|row| row.slot_clearance_action_evidence_review_key.clone()),
            first_review_gate_key: first_review_gate
                .map(|row| row.slot_clearance_action_evidence_review_key.clone()),
            first_lineage_gap_review_key: first_lineage_gap_review
                .map(|row| row.slot_clearance_action_evidence_review_key.clone()),
            first_release_ready_review_key: first_release_ready_review
                .map(|row| row.slot_clearance_action_evidence_review_key.clone()),
            first_slot_clearance_action_evidence_key: first_review
                .map(|row| row.slot_clearance_action_evidence_key.clone()),
            first_slot_clearance_action_key: first_review
                .map(|row| row.slot_clearance_action_key.clone()),
            first_slot_clearance_key: first_review.map(|row| row.slot_clearance_key.clone()),
            first_slot_key: first_review.map(|row| row.slot_key.clone()),
            first_disposition_action_key: first_review
                .map(|row| row.disposition_action_key.clone()),
            first_work_order_key: first_review.map(|row| row.work_order_key.clone()),
            first_ticket_key: first_review.map(|row| row.ticket_key.clone()),
            first_review_kind: first_review.map(|row| row.review_kind),
            first_slot_clearance_action_evidence_kind: first_review
                .map(|row| row.slot_clearance_action_evidence_kind),
            first_slot_clearance_action_kind: first_review
                .map(|row| row.slot_clearance_action_kind),
            first_slot_clearance_kind: first_review.map(|row| row.slot_clearance_kind),
            first_disposition_action_kind: first_review.map(|row| row.disposition_action_kind),
            first_handoff_lane: first_review.map(|row| row.handoff_lane),
            first_check_kind: first_review.map(|row| row.check_kind),
            first_check_status: first_review.map(|row| row.status),
            release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_reviews_ready:
                !reviews.is_empty() && reviews.iter().all(|row| row.is_release_ready()),
        }
    }

    pub fn has_slot_clearance_action_evidence_reviews(&self) -> bool {
        self.total_slot_clearance_action_evidence_reviews > 0
    }

    pub fn has_required_reviews(&self) -> bool {
        self.required_reviews > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.blocking_reviews > 0
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_reviews > 0
    }

    pub fn needs_review(&self) -> bool {
        self.review_required_reviews > 0
    }

    pub fn has_lineage_gaps(&self) -> bool {
        self.lineage_gap_reviews > 0
    }

    pub fn has_release_ready_reviews(&self) -> bool {
        self.release_ready_reviews > 0
    }

    pub fn has_complete_lineage(&self) -> bool {
        self.lineage_complete_reviews == self.total_slot_clearance_action_evidence_reviews
    }

    pub fn requires_attention(&self) -> bool {
        self.has_required_reviews()
            || self.has_blockers()
            || self.needs_operator()
            || self.needs_review()
            || self.dispatch_required_reviews > 0
            || self.execution_required_reviews > 0
            || self.has_lineage_gaps()
            || !self
                .release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_reviews_ready
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReadinessSummary {
    pub release_ticket_handoff_guardrail_audit_clearance_action_readiness_summary:
        Box<IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionReadinessSummary>,
    pub release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_summary:
        Box<IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceSummary>,
    pub total_clearance_actions: usize,
    pub total_action_evidence: usize,
    pub blocking_evidence: usize,
    pub operator_required_evidence: usize,
    pub review_required_evidence: usize,
    pub dispatch_required_evidence: usize,
    pub execution_required_evidence: usize,
    pub release_evidence: usize,
    pub lineage_complete_evidence: usize,
    pub first_clearance_action_key: Option<String>,
    pub first_action_evidence_key: Option<String>,
    pub first_blocking_evidence_key: Option<String>,
    pub first_operator_evidence_key: Option<String>,
    pub first_review_evidence_key: Option<String>,
    pub first_release_evidence_key: Option<String>,
    pub first_clearance_key: Option<String>,
    pub first_work_order_key: Option<String>,
    pub first_ticket_key: Option<String>,
    pub first_evidence_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceKind,
    >,
    pub first_clearance_action_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionKind,
    >,
    pub first_clearance_kind:
        Option<IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind>,
    pub first_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub first_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub first_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_action_evidence_key: Option<String>,
    pub next_clearance_action_key: Option<String>,
    pub next_work_order_key: Option<String>,
    pub next_ticket_key: Option<String>,
    pub next_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub next_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub release_handoff_execution_work_order_guardrail_audit_clearance_actions_ready: bool,
    pub release_ticket_handoff_guardrail_audit_clearance_actions_ready: bool,
    pub release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_ready: bool,
    pub release_ticket_handoff_guardrail_audit_clearance_action_evidence_ready: bool,
}

impl IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReadinessSummary {
    pub fn from_summaries(
        release_ticket_handoff_guardrail_audit_clearance_action_readiness_summary: IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionReadinessSummary,
        release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_summary: IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceSummary,
    ) -> Self {
        let release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_ready =
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_summary
                .release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_ready;
        let release_ticket_handoff_guardrail_audit_clearance_action_evidence_ready =
            release_ticket_handoff_guardrail_audit_clearance_action_readiness_summary
                .ready_for_release_ticket_handoff_guardrail_audit_clearance_actions()
                && release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_ready;
        let next_evidence_requires_attention =
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_summary
                .requires_attention();
        let next_action_evidence_key = if next_evidence_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_summary
                .first_evidence_key
                .clone()
        } else {
            None
        };
        let next_action_key = if next_evidence_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_summary
                .first_action_key
                .clone()
        } else {
            None
        };
        let next_work_order_key = if next_evidence_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_summary
                .first_work_order_key
                .clone()
        } else {
            None
        };
        let next_ticket_key = if next_evidence_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_summary
                .first_ticket_key
                .clone()
        } else {
            None
        };
        let next_check_kind = if next_evidence_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_summary
                .first_check_kind
        } else {
            None
        };
        let next_check_status = if next_evidence_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_summary
                .first_check_status
        } else {
            None
        };
        let next_handoff_lane = if next_evidence_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_summary
                .first_handoff_lane
        } else {
            None
        };

        Self {
            total_clearance_actions:
                release_ticket_handoff_guardrail_audit_clearance_action_readiness_summary
                    .total_clearance_actions,
            total_action_evidence:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_summary
                    .total_evidence,
            blocking_evidence:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_summary
                    .blocking_evidence,
            operator_required_evidence:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_summary
                    .operator_required_evidence,
            review_required_evidence:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_summary
                    .review_required_evidence,
            dispatch_required_evidence:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_summary
                    .dispatch_required_evidence,
            execution_required_evidence:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_summary
                    .execution_required_evidence,
            release_evidence:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_summary
                    .release_evidence,
            lineage_complete_evidence:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_summary
                    .lineage_complete_evidence,
            first_clearance_action_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_summary
                    .first_action_key
                    .clone(),
            first_action_evidence_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_summary
                    .first_evidence_key
                    .clone(),
            first_blocking_evidence_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_summary
                    .first_blocking_evidence_key
                    .clone(),
            first_operator_evidence_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_summary
                    .first_operator_evidence_key
                    .clone(),
            first_review_evidence_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_summary
                    .first_review_evidence_key
                    .clone(),
            first_release_evidence_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_summary
                    .first_release_evidence_key
                    .clone(),
            first_clearance_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_summary
                    .first_clearance_key
                    .clone(),
            first_work_order_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_summary
                    .first_work_order_key
                    .clone(),
            first_ticket_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_summary
                    .first_ticket_key
                    .clone(),
            first_evidence_kind:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_summary
                    .first_evidence_kind,
            first_clearance_action_kind:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_summary
                    .first_action_kind,
            first_clearance_kind:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_summary
                    .first_clearance_kind,
            first_handoff_lane:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_summary
                    .first_handoff_lane,
            first_check_kind:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_summary
                    .first_check_kind,
            first_check_status:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_summary
                    .first_check_status,
            next_action_evidence_key,
            next_clearance_action_key: next_action_key.or_else(|| {
                release_ticket_handoff_guardrail_audit_clearance_action_readiness_summary
                    .next_work_order_key
                    .as_ref()
                    .and_then(|_| {
                        release_ticket_handoff_guardrail_audit_clearance_action_readiness_summary
                            .first_clearance_action_key
                            .clone()
                    })
            }),
            next_work_order_key: next_work_order_key.or_else(|| {
                release_ticket_handoff_guardrail_audit_clearance_action_readiness_summary
                    .next_work_order_key
                    .clone()
            }),
            next_ticket_key: next_ticket_key.or_else(|| {
                release_ticket_handoff_guardrail_audit_clearance_action_readiness_summary
                    .next_ticket_key
                    .clone()
            }),
            next_check_kind: next_check_kind.or(
                release_ticket_handoff_guardrail_audit_clearance_action_readiness_summary
                    .next_check_kind,
            ),
            next_check_status: next_check_status.or(
                release_ticket_handoff_guardrail_audit_clearance_action_readiness_summary
                    .next_check_status,
            ),
            next_handoff_lane: next_handoff_lane.or(
                release_ticket_handoff_guardrail_audit_clearance_action_readiness_summary
                    .next_handoff_lane,
            ),
            release_handoff_execution_work_order_guardrail_audit_clearance_actions_ready:
                release_ticket_handoff_guardrail_audit_clearance_action_readiness_summary
                    .release_handoff_execution_work_order_guardrail_audit_clearance_actions_ready,
            release_ticket_handoff_guardrail_audit_clearance_actions_ready:
                release_ticket_handoff_guardrail_audit_clearance_action_readiness_summary
                    .release_ticket_handoff_guardrail_audit_clearance_actions_ready,
            release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_ready,
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_ready,
            release_ticket_handoff_guardrail_audit_clearance_action_readiness_summary: Box::new(
                release_ticket_handoff_guardrail_audit_clearance_action_readiness_summary,
            ),
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_summary:
                Box::new(
                    release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_summary,
                ),
        }
    }

    pub fn has_action_evidence(&self) -> bool {
        self.total_action_evidence > 0
    }

    pub fn ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence(
        &self,
    ) -> bool {
        self.release_ticket_handoff_guardrail_audit_clearance_action_readiness_summary
            .ready_for_release_ticket_handoff_guardrail_audit_clearance_actions()
            && self.release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_ready
            && self.release_ticket_handoff_guardrail_audit_clearance_action_evidence_ready
            && !self.requires_attention()
    }

    pub fn has_blockers(&self) -> bool {
        self.blocking_evidence > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_readiness_summary
                .has_blockers()
            || self
                .release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_summary
                .has_blockers()
    }

    pub fn has_review_work(&self) -> bool {
        self.review_required_evidence > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_readiness_summary
                .has_review_work()
            || self
                .release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_summary
                .needs_review()
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_evidence > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_readiness_summary
                .needs_operator()
            || self
                .release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_summary
                .needs_operator()
    }

    pub fn requires_execution(&self) -> bool {
        self.execution_required_evidence > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_readiness_summary
                .requires_execution()
    }

    pub fn requires_attention(&self) -> bool {
        self.requires_execution()
            || self.dispatch_required_evidence > 0
            || self.has_blockers()
            || self.has_review_work()
            || self.needs_operator()
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_readiness_summary
                .requires_attention()
            || self
                .release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_summary
                .requires_attention()
            || !self.release_ticket_handoff_guardrail_audit_clearance_action_evidence_ready
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewReadinessSummary {
    pub release_ticket_handoff_guardrail_audit_clearance_action_evidence_readiness_summary:
        Box<IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReadinessSummary>,
    pub release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_summary:
        Box<IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewSummary>,
    pub total_clearance_actions: usize,
    pub total_action_evidence: usize,
    pub total_evidence_reviews: usize,
    pub blocking_reviews: usize,
    pub operator_required_reviews: usize,
    pub review_required_reviews: usize,
    pub dispatch_required_reviews: usize,
    pub execution_required_reviews: usize,
    pub lineage_gap_reviews: usize,
    pub release_ready_reviews: usize,
    pub lineage_complete_reviews: usize,
    pub first_evidence_review_key: Option<String>,
    pub first_blocking_review_key: Option<String>,
    pub first_operator_review_key: Option<String>,
    pub first_review_gate_key: Option<String>,
    pub first_lineage_gap_review_key: Option<String>,
    pub first_release_ready_review_key: Option<String>,
    pub first_action_evidence_key: Option<String>,
    pub first_clearance_action_key: Option<String>,
    pub first_clearance_key: Option<String>,
    pub first_work_order_key: Option<String>,
    pub first_ticket_key: Option<String>,
    pub first_review_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind,
    >,
    pub first_evidence_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceKind,
    >,
    pub first_clearance_action_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionKind,
    >,
    pub first_clearance_kind:
        Option<IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind>,
    pub first_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub first_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub first_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_evidence_review_key: Option<String>,
    pub next_action_evidence_key: Option<String>,
    pub next_clearance_action_key: Option<String>,
    pub next_work_order_key: Option<String>,
    pub next_ticket_key: Option<String>,
    pub next_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub next_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub release_ticket_handoff_guardrail_audit_clearance_action_evidence_ready: bool,
    pub release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_reviews_ready: bool,
    pub release_ticket_handoff_guardrail_audit_clearance_action_evidence_reviews_ready: bool,
}

impl
    IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewReadinessSummary
{
    pub fn from_summaries(
        release_ticket_handoff_guardrail_audit_clearance_action_evidence_readiness_summary: IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReadinessSummary,
        release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_summary: IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewSummary,
    ) -> Self {
        let release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_reviews_ready =
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_summary
                .release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_reviews_ready;
        let release_ticket_handoff_guardrail_audit_clearance_action_evidence_ready =
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_readiness_summary
                .ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence();
        let release_ticket_handoff_guardrail_audit_clearance_action_evidence_reviews_ready =
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_ready
                && release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_reviews_ready;
        let next_review_requires_attention =
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_summary
                .requires_attention();
        let next_review_key = if next_review_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_summary
                .first_review_key
                .clone()
        } else {
            None
        };
        let next_action_evidence_key = if next_review_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_summary
                .first_evidence_key
                .clone()
        } else {
            None
        };
        let next_action_key = if next_review_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_summary
                .first_action_key
                .clone()
        } else {
            None
        };
        let next_work_order_key = if next_review_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_summary
                .first_work_order_key
                .clone()
        } else {
            None
        };
        let next_ticket_key = if next_review_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_summary
                .first_ticket_key
                .clone()
        } else {
            None
        };
        let next_check_kind = if next_review_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_summary
                .first_check_kind
        } else {
            None
        };
        let next_check_status = if next_review_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_summary
                .first_check_status
        } else {
            None
        };
        let next_handoff_lane = if next_review_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_summary
                .first_handoff_lane
        } else {
            None
        };

        Self {
            total_clearance_actions:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_readiness_summary
                    .total_clearance_actions,
            total_action_evidence:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_readiness_summary
                    .total_action_evidence,
            total_evidence_reviews:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_summary
                    .total_reviews,
            blocking_reviews:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_summary
                    .blocking_reviews,
            operator_required_reviews:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_summary
                    .operator_required_reviews,
            review_required_reviews:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_summary
                    .review_required_reviews,
            dispatch_required_reviews:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_summary
                    .dispatch_required_reviews,
            execution_required_reviews:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_summary
                    .execution_required_reviews,
            lineage_gap_reviews:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_summary
                    .lineage_gap_reviews,
            release_ready_reviews:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_summary
                    .release_ready_reviews,
            lineage_complete_reviews:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_summary
                    .lineage_complete_reviews,
            first_evidence_review_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_summary
                    .first_review_key
                    .clone(),
            first_blocking_review_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_summary
                    .first_blocking_review_key
                    .clone(),
            first_operator_review_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_summary
                    .first_operator_review_key
                    .clone(),
            first_review_gate_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_summary
                    .first_review_gate_key
                    .clone(),
            first_lineage_gap_review_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_summary
                    .first_lineage_gap_review_key
                    .clone(),
            first_release_ready_review_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_summary
                    .first_release_ready_review_key
                    .clone(),
            first_action_evidence_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_summary
                    .first_evidence_key
                    .clone(),
            first_clearance_action_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_summary
                    .first_action_key
                    .clone(),
            first_clearance_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_summary
                    .first_clearance_key
                    .clone(),
            first_work_order_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_summary
                    .first_work_order_key
                    .clone(),
            first_ticket_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_summary
                    .first_ticket_key
                    .clone(),
            first_review_kind:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_summary
                    .first_review_kind,
            first_evidence_kind:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_summary
                    .first_evidence_kind,
            first_clearance_action_kind:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_summary
                    .first_action_kind,
            first_clearance_kind:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_summary
                    .first_clearance_kind,
            first_handoff_lane:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_summary
                    .first_handoff_lane,
            first_check_kind:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_summary
                    .first_check_kind,
            first_check_status:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_summary
                    .first_check_status,
            next_evidence_review_key: next_review_key,
            next_action_evidence_key: next_action_evidence_key.or_else(|| {
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_readiness_summary
                    .next_action_evidence_key
                    .clone()
            }),
            next_clearance_action_key: next_action_key.or_else(|| {
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_readiness_summary
                    .next_clearance_action_key
                    .clone()
            }),
            next_work_order_key: next_work_order_key.or_else(|| {
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_readiness_summary
                    .next_work_order_key
                    .clone()
            }),
            next_ticket_key: next_ticket_key.or_else(|| {
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_readiness_summary
                    .next_ticket_key
                    .clone()
            }),
            next_check_kind: next_check_kind.or(
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_readiness_summary
                    .next_check_kind,
            ),
            next_check_status: next_check_status.or(
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_readiness_summary
                    .next_check_status,
            ),
            next_handoff_lane: next_handoff_lane.or(
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_readiness_summary
                    .next_handoff_lane,
            ),
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_ready,
            release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_reviews_ready,
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_reviews_ready,
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_readiness_summary:
                Box::new(
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_readiness_summary,
                ),
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_summary:
                Box::new(
                    release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_summary,
                ),
        }
    }

    pub fn has_evidence_reviews(&self) -> bool {
        self.total_evidence_reviews > 0
    }

    pub fn ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_reviews(
        &self,
    ) -> bool {
        self.release_ticket_handoff_guardrail_audit_clearance_action_evidence_ready
            && self.release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_reviews_ready
            && self.release_ticket_handoff_guardrail_audit_clearance_action_evidence_reviews_ready
            && !self.requires_attention()
    }

    pub fn has_blockers(&self) -> bool {
        self.blocking_reviews > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_readiness_summary
                .has_blockers()
            || self
                .release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_summary
                .has_blockers()
    }

    pub fn has_review_work(&self) -> bool {
        self.review_required_reviews > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_readiness_summary
                .has_review_work()
            || self
                .release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_summary
                .needs_review()
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_reviews > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_readiness_summary
                .needs_operator()
            || self
                .release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_summary
                .needs_operator()
    }

    pub fn has_lineage_gaps(&self) -> bool {
        self.lineage_gap_reviews > 0
            || self
                .release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_summary
                .has_lineage_gaps()
    }

    pub fn requires_execution(&self) -> bool {
        self.execution_required_reviews > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_readiness_summary
                .requires_execution()
    }

    pub fn requires_attention(&self) -> bool {
        self.requires_execution()
            || self.dispatch_required_reviews > 0
            || self.has_blockers()
            || self.has_review_work()
            || self.needs_operator()
            || self.has_lineage_gaps()
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_readiness_summary
                .requires_attention()
            || self
                .release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_summary
                .requires_attention()
            || !self.release_ticket_handoff_guardrail_audit_clearance_action_evidence_reviews_ready
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionReadinessSummary {
    pub release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_readiness_summary:
        Box<IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewReadinessSummary>,
    pub release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_summary:
        Box<IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionSummary>,
    pub total_clearance_actions: usize,
    pub total_action_evidence: usize,
    pub total_evidence_reviews: usize,
    pub total_review_dispositions: usize,
    pub release_blocking_dispositions: usize,
    pub operator_required_dispositions: usize,
    pub review_required_dispositions: usize,
    pub dispatch_required_dispositions: usize,
    pub execution_required_dispositions: usize,
    pub repair_required_dispositions: usize,
    pub operator_handoff_dispositions: usize,
    pub review_gate_dispositions: usize,
    pub lineage_gap_dispositions: usize,
    pub release_ready_dispositions: usize,
    pub lineage_complete_dispositions: usize,
    pub first_review_disposition_key: Option<String>,
    pub first_repair_required_disposition_key: Option<String>,
    pub first_operator_handoff_disposition_key: Option<String>,
    pub first_review_gate_disposition_key: Option<String>,
    pub first_lineage_gap_disposition_key: Option<String>,
    pub first_release_ready_disposition_key: Option<String>,
    pub first_evidence_review_key: Option<String>,
    pub first_action_evidence_key: Option<String>,
    pub first_clearance_action_key: Option<String>,
    pub first_clearance_key: Option<String>,
    pub first_work_order_key: Option<String>,
    pub first_ticket_key: Option<String>,
    pub first_disposition_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind,
    >,
    pub first_review_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind,
    >,
    pub first_evidence_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceKind,
    >,
    pub first_clearance_action_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionKind,
    >,
    pub first_clearance_kind:
        Option<IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind>,
    pub first_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub first_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub first_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_review_disposition_key: Option<String>,
    pub next_evidence_review_key: Option<String>,
    pub next_action_evidence_key: Option<String>,
    pub next_clearance_action_key: Option<String>,
    pub next_work_order_key: Option<String>,
    pub next_ticket_key: Option<String>,
    pub next_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub next_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub release_ticket_handoff_guardrail_audit_clearance_action_evidence_reviews_ready: bool,
    pub release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_dispositions_ready: bool,
    pub release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_dispositions_ready:
        bool,
}

impl
    IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionReadinessSummary
{
    pub fn from_summaries(
        release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_readiness_summary: IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewReadinessSummary,
        release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_summary: IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionSummary,
    ) -> Self {
        let release_ticket_handoff_guardrail_audit_clearance_action_evidence_reviews_ready =
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_readiness_summary
                .ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_reviews();
        let release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_dispositions_ready =
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_summary
                .release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_dispositions_ready;
        let release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_dispositions_ready =
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_reviews_ready
                && release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_dispositions_ready;
        let next_disposition_requires_attention =
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_summary
                .requires_attention();
        let next_review_disposition_key = if next_disposition_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_summary
                .first_disposition_key
                .clone()
        } else {
            None
        };
        let next_review_key = if next_disposition_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_summary
                .first_review_key
                .clone()
        } else {
            None
        };
        let next_action_evidence_key = if next_disposition_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_summary
                .first_evidence_key
                .clone()
        } else {
            None
        };
        let next_action_key = if next_disposition_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_summary
                .first_action_key
                .clone()
        } else {
            None
        };
        let next_work_order_key = if next_disposition_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_summary
                .first_work_order_key
                .clone()
        } else {
            None
        };
        let next_ticket_key = if next_disposition_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_summary
                .first_ticket_key
                .clone()
        } else {
            None
        };
        let next_check_kind = if next_disposition_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_summary
                .first_check_kind
        } else {
            None
        };
        let next_check_status = if next_disposition_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_summary
                .first_check_status
        } else {
            None
        };
        let next_handoff_lane = if next_disposition_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_summary
                .first_handoff_lane
        } else {
            None
        };

        Self {
            total_clearance_actions:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_readiness_summary
                    .total_clearance_actions,
            total_action_evidence:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_readiness_summary
                    .total_action_evidence,
            total_evidence_reviews:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_readiness_summary
                    .total_evidence_reviews,
            total_review_dispositions:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_summary
                    .total_dispositions,
            release_blocking_dispositions:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_summary
                    .release_blocking_dispositions,
            operator_required_dispositions:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_summary
                    .operator_required_dispositions,
            review_required_dispositions:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_summary
                    .review_required_dispositions,
            dispatch_required_dispositions:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_summary
                    .dispatch_required_dispositions,
            execution_required_dispositions:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_summary
                    .execution_required_dispositions,
            repair_required_dispositions:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_summary
                    .repair_required_dispositions,
            operator_handoff_dispositions:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_summary
                    .operator_handoff_dispositions,
            review_gate_dispositions:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_summary
                    .review_gate_dispositions,
            lineage_gap_dispositions:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_summary
                    .lineage_gap_dispositions,
            release_ready_dispositions:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_summary
                    .release_ready_dispositions,
            lineage_complete_dispositions:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_summary
                    .lineage_complete_dispositions,
            first_review_disposition_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_summary
                    .first_disposition_key
                    .clone(),
            first_repair_required_disposition_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_summary
                    .first_repair_required_disposition_key
                    .clone(),
            first_operator_handoff_disposition_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_summary
                    .first_operator_handoff_disposition_key
                    .clone(),
            first_review_gate_disposition_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_summary
                    .first_review_gate_disposition_key
                    .clone(),
            first_lineage_gap_disposition_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_summary
                    .first_lineage_gap_disposition_key
                    .clone(),
            first_release_ready_disposition_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_summary
                    .first_release_ready_disposition_key
                    .clone(),
            first_evidence_review_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_summary
                    .first_review_key
                    .clone(),
            first_action_evidence_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_summary
                    .first_evidence_key
                    .clone(),
            first_clearance_action_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_summary
                    .first_action_key
                    .clone(),
            first_clearance_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_summary
                    .first_clearance_key
                    .clone(),
            first_work_order_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_summary
                    .first_work_order_key
                    .clone(),
            first_ticket_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_summary
                    .first_ticket_key
                    .clone(),
            first_disposition_kind:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_summary
                    .first_disposition_kind,
            first_review_kind:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_summary
                    .first_review_kind,
            first_evidence_kind:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_summary
                    .first_evidence_kind,
            first_clearance_action_kind:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_summary
                    .first_action_kind,
            first_clearance_kind:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_summary
                    .first_clearance_kind,
            first_handoff_lane:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_summary
                    .first_handoff_lane,
            first_check_kind:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_summary
                    .first_check_kind,
            first_check_status:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_summary
                    .first_check_status,
            next_review_disposition_key,
            next_evidence_review_key: next_review_key.or_else(|| {
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_readiness_summary
                    .next_evidence_review_key
                    .clone()
            }),
            next_action_evidence_key: next_action_evidence_key.or_else(|| {
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_readiness_summary
                    .next_action_evidence_key
                    .clone()
            }),
            next_clearance_action_key: next_action_key.or_else(|| {
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_readiness_summary
                    .next_clearance_action_key
                    .clone()
            }),
            next_work_order_key: next_work_order_key.or_else(|| {
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_readiness_summary
                    .next_work_order_key
                    .clone()
            }),
            next_ticket_key: next_ticket_key.or_else(|| {
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_readiness_summary
                    .next_ticket_key
                    .clone()
            }),
            next_check_kind: next_check_kind.or(
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_readiness_summary
                    .next_check_kind,
            ),
            next_check_status: next_check_status.or(
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_readiness_summary
                    .next_check_status,
            ),
            next_handoff_lane: next_handoff_lane.or(
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_readiness_summary
                    .next_handoff_lane,
            ),
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_reviews_ready,
            release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_dispositions_ready,
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_dispositions_ready,
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_readiness_summary:
                Box::new(
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_readiness_summary,
                ),
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_summary:
                Box::new(
                    release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_summary,
                ),
        }
    }

    pub fn has_review_dispositions(&self) -> bool {
        self.total_review_dispositions > 0
    }

    pub fn ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_dispositions(
        &self,
    ) -> bool {
        self.release_ticket_handoff_guardrail_audit_clearance_action_evidence_reviews_ready
            && self.release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_dispositions_ready
            && self.release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_dispositions_ready
            && !self.requires_attention()
    }

    pub fn has_blockers(&self) -> bool {
        self.release_blocking_dispositions > 0
            || self.repair_required_dispositions > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_readiness_summary
                .has_blockers()
            || self
                .release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_summary
                .has_blockers()
    }

    pub fn has_review_work(&self) -> bool {
        self.review_required_dispositions > 0
            || self.review_gate_dispositions > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_readiness_summary
                .has_review_work()
            || self
                .release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_summary
                .needs_review()
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_dispositions > 0
            || self.operator_handoff_dispositions > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_readiness_summary
                .needs_operator()
            || self
                .release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_summary
                .needs_operator()
    }

    pub fn has_lineage_gaps(&self) -> bool {
        self.lineage_gap_dispositions > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_readiness_summary
                .has_lineage_gaps()
            || self
                .release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_summary
                .has_lineage_gaps()
    }

    pub fn requires_execution(&self) -> bool {
        self.execution_required_dispositions > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_readiness_summary
                .requires_execution()
    }

    pub fn requires_attention(&self) -> bool {
        self.requires_execution()
            || self.dispatch_required_dispositions > 0
            || self.has_blockers()
            || self.has_review_work()
            || self.needs_operator()
            || self.has_lineage_gaps()
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_readiness_summary
                .requires_attention()
            || self
                .release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_summary
                .requires_attention()
            || !self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_dispositions_ready
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionReadinessSummary {
    pub release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_readiness_summary:
        Box<IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionReadinessSummary>,
    pub release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary:
        Box<IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSummary>,
    pub total_clearance_actions: usize,
    pub total_action_evidence: usize,
    pub total_evidence_reviews: usize,
    pub total_review_dispositions: usize,
    pub total_review_disposition_actions: usize,
    pub required_disposition_actions: usize,
    pub release_blocking_disposition_actions: usize,
    pub operator_required_disposition_actions: usize,
    pub review_required_disposition_actions: usize,
    pub dispatch_required_disposition_actions: usize,
    pub execution_required_disposition_actions: usize,
    pub repair_disposition_actions: usize,
    pub operator_handoff_disposition_actions: usize,
    pub review_gate_disposition_actions: usize,
    pub lineage_gap_disposition_actions: usize,
    pub release_handoff_disposition_actions: usize,
    pub lineage_complete_disposition_actions: usize,
    pub first_review_disposition_action_key: Option<String>,
    pub first_required_disposition_action_key: Option<String>,
    pub first_repair_disposition_action_key: Option<String>,
    pub first_operator_handoff_disposition_action_key: Option<String>,
    pub first_review_gate_disposition_action_key: Option<String>,
    pub first_lineage_gap_disposition_action_key: Option<String>,
    pub first_release_handoff_disposition_action_key: Option<String>,
    pub first_review_disposition_key: Option<String>,
    pub first_evidence_review_key: Option<String>,
    pub first_action_evidence_key: Option<String>,
    pub first_clearance_action_key: Option<String>,
    pub first_clearance_key: Option<String>,
    pub first_work_order_key: Option<String>,
    pub first_ticket_key: Option<String>,
    pub first_disposition_action_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionKind,
    >,
    pub first_disposition_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind,
    >,
    pub first_review_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind,
    >,
    pub first_evidence_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceKind,
    >,
    pub first_clearance_action_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionKind,
    >,
    pub first_clearance_kind:
        Option<IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind>,
    pub first_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub first_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub first_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_review_disposition_action_key: Option<String>,
    pub next_review_disposition_key: Option<String>,
    pub next_evidence_review_key: Option<String>,
    pub next_action_evidence_key: Option<String>,
    pub next_clearance_action_key: Option<String>,
    pub next_work_order_key: Option<String>,
    pub next_ticket_key: Option<String>,
    pub next_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub next_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_dispositions_ready:
        bool,
    pub release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_actions_ready: bool,
    pub release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_actions_ready:
        bool,
}

impl
    IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionReadinessSummary
{
    pub fn from_summaries(
        release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_readiness_summary: IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionReadinessSummary,
        release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary: IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSummary,
    ) -> Self {
        let release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_dispositions_ready =
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_readiness_summary
                .ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_dispositions();
        let release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_actions_ready =
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary
                .release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_actions_ready;
        let release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_actions_ready =
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_dispositions_ready
                && release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_actions_ready;
        let next_action_requires_attention =
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary
                .requires_attention();
        let next_action_key = if next_action_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary
                .first_required_action_key
                .clone()
                .or_else(|| {
                    release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary
                        .first_action_key
                        .clone()
                })
        } else {
            None
        };
        let next_disposition_key = if next_action_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary
                .first_disposition_key
                .clone()
        } else {
            None
        };
        let next_review_key = if next_action_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary
                .first_review_key
                .clone()
        } else {
            None
        };
        let next_evidence_key = if next_action_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary
                .first_evidence_key
                .clone()
        } else {
            None
        };
        let next_clearance_action_key = if next_action_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary
                .first_clearance_action_key
                .clone()
        } else {
            None
        };
        let next_work_order_key = if next_action_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary
                .first_work_order_key
                .clone()
        } else {
            None
        };
        let next_ticket_key = if next_action_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary
                .first_ticket_key
                .clone()
        } else {
            None
        };
        let next_check_kind = if next_action_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary
                .first_check_kind
        } else {
            None
        };
        let next_check_status = if next_action_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary
                .first_check_status
        } else {
            None
        };
        let next_handoff_lane = if next_action_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary
                .first_handoff_lane
        } else {
            None
        };

        Self {
            total_clearance_actions:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_readiness_summary
                    .total_clearance_actions,
            total_action_evidence:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_readiness_summary
                    .total_action_evidence,
            total_evidence_reviews:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_readiness_summary
                    .total_evidence_reviews,
            total_review_dispositions:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_readiness_summary
                    .total_review_dispositions,
            total_review_disposition_actions:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary
                    .total_actions,
            required_disposition_actions:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary
                    .required_actions,
            release_blocking_disposition_actions:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary
                    .release_blocking_actions,
            operator_required_disposition_actions:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary
                    .operator_required_actions,
            review_required_disposition_actions:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary
                    .review_required_actions,
            dispatch_required_disposition_actions:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary
                    .dispatch_required_actions,
            execution_required_disposition_actions:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary
                    .execution_required_actions,
            repair_disposition_actions:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary
                    .repair_actions,
            operator_handoff_disposition_actions:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary
                    .operator_handoff_actions,
            review_gate_disposition_actions:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary
                    .review_gate_actions,
            lineage_gap_disposition_actions:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary
                    .lineage_gap_actions,
            release_handoff_disposition_actions:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary
                    .release_handoff_actions,
            lineage_complete_disposition_actions:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary
                    .lineage_complete_actions,
            first_review_disposition_action_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary
                    .first_action_key
                    .clone(),
            first_required_disposition_action_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary
                    .first_required_action_key
                    .clone(),
            first_repair_disposition_action_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary
                    .first_repair_action_key
                    .clone(),
            first_operator_handoff_disposition_action_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary
                    .first_operator_handoff_action_key
                    .clone(),
            first_review_gate_disposition_action_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary
                    .first_review_gate_action_key
                    .clone(),
            first_lineage_gap_disposition_action_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary
                    .first_lineage_gap_action_key
                    .clone(),
            first_release_handoff_disposition_action_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary
                    .first_release_handoff_action_key
                    .clone(),
            first_review_disposition_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary
                    .first_disposition_key
                    .clone(),
            first_evidence_review_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary
                    .first_review_key
                    .clone(),
            first_action_evidence_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary
                    .first_evidence_key
                    .clone(),
            first_clearance_action_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary
                    .first_clearance_action_key
                    .clone(),
            first_clearance_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary
                    .first_clearance_key
                    .clone(),
            first_work_order_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary
                    .first_work_order_key
                    .clone(),
            first_ticket_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary
                    .first_ticket_key
                    .clone(),
            first_disposition_action_kind:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary
                    .first_disposition_action_kind,
            first_disposition_kind:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary
                    .first_disposition_kind,
            first_review_kind:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary
                    .first_review_kind,
            first_evidence_kind:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary
                    .first_evidence_kind,
            first_clearance_action_kind:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary
                    .first_clearance_action_kind,
            first_clearance_kind:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary
                    .first_clearance_kind,
            first_handoff_lane:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary
                    .first_handoff_lane,
            first_check_kind:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary
                    .first_check_kind,
            first_check_status:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary
                    .first_check_status,
            next_review_disposition_action_key: next_action_key,
            next_review_disposition_key: next_disposition_key.or_else(|| {
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_readiness_summary
                    .next_review_disposition_key
                    .clone()
            }),
            next_evidence_review_key: next_review_key.or_else(|| {
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_readiness_summary
                    .next_evidence_review_key
                    .clone()
            }),
            next_action_evidence_key: next_evidence_key.or_else(|| {
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_readiness_summary
                    .next_action_evidence_key
                    .clone()
            }),
            next_clearance_action_key: next_clearance_action_key.or_else(|| {
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_readiness_summary
                    .next_clearance_action_key
                    .clone()
            }),
            next_work_order_key: next_work_order_key.or_else(|| {
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_readiness_summary
                    .next_work_order_key
                    .clone()
            }),
            next_ticket_key: next_ticket_key.or_else(|| {
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_readiness_summary
                    .next_ticket_key
                    .clone()
            }),
            next_check_kind: next_check_kind.or(
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_readiness_summary
                    .next_check_kind,
            ),
            next_check_status: next_check_status.or(
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_readiness_summary
                    .next_check_status,
            ),
            next_handoff_lane: next_handoff_lane.or(
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_readiness_summary
                    .next_handoff_lane,
            ),
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_dispositions_ready,
            release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_actions_ready,
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_actions_ready,
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_readiness_summary:
                Box::new(
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_readiness_summary,
                ),
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary:
                Box::new(
                    release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary,
                ),
        }
    }

    pub fn has_review_disposition_actions(&self) -> bool {
        self.total_review_disposition_actions > 0
    }

    pub fn ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_actions(
        &self,
    ) -> bool {
        self.release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_dispositions_ready
            && self.release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_actions_ready
            && self.release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_actions_ready
            && !self.requires_attention()
    }

    pub fn has_required_actions(&self) -> bool {
        self.required_disposition_actions > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.release_blocking_disposition_actions > 0
            || self.repair_disposition_actions > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_readiness_summary
                .has_blockers()
            || self
                .release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary
                .has_blockers()
    }

    pub fn has_review_work(&self) -> bool {
        self.review_required_disposition_actions > 0
            || self.review_gate_disposition_actions > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_readiness_summary
                .has_review_work()
            || self
                .release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary
                .needs_review()
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_disposition_actions > 0
            || self.operator_handoff_disposition_actions > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_readiness_summary
                .needs_operator()
            || self
                .release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary
                .needs_operator()
    }

    pub fn has_lineage_gaps(&self) -> bool {
        self.lineage_gap_disposition_actions > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_readiness_summary
                .has_lineage_gaps()
            || self
                .release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary
                .has_lineage_gaps()
    }

    pub fn has_release_handoff_actions(&self) -> bool {
        self.release_handoff_disposition_actions > 0
    }

    pub fn has_complete_lineage(&self) -> bool {
        self.lineage_complete_disposition_actions == self.total_review_disposition_actions
            && self
                .release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary
                .has_complete_lineage()
    }

    pub fn requires_execution(&self) -> bool {
        self.execution_required_disposition_actions > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_readiness_summary
                .requires_execution()
    }

    pub fn requires_attention(&self) -> bool {
        self.requires_execution()
            || self.dispatch_required_disposition_actions > 0
            || self.has_required_actions()
            || self.has_blockers()
            || self.has_review_work()
            || self.needs_operator()
            || self.has_lineage_gaps()
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_readiness_summary
                .requires_attention()
            || self
                .release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary
                .requires_attention()
            || !self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_actions_ready
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotReadinessSummary {
    pub release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_readiness_summary:
        Box<IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionReadinessSummary>,
    pub release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_summary:
        Box<IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotSummary>,
    pub total_review_disposition_actions: usize,
    pub total_review_disposition_action_slots: usize,
    pub required_slots: usize,
    pub release_blocking_slots: usize,
    pub operator_required_slots: usize,
    pub review_required_slots: usize,
    pub dispatch_required_slots: usize,
    pub execution_required_slots: usize,
    pub repair_slots: usize,
    pub operator_handoff_slots: usize,
    pub review_gate_slots: usize,
    pub lineage_gap_slots: usize,
    pub release_handoff_slots: usize,
    pub lineage_complete_slots: usize,
    pub first_review_disposition_action_slot_key: Option<String>,
    pub first_required_slot_key: Option<String>,
    pub first_repair_slot_key: Option<String>,
    pub first_operator_handoff_slot_key: Option<String>,
    pub first_review_gate_slot_key: Option<String>,
    pub first_lineage_gap_slot_key: Option<String>,
    pub first_release_handoff_slot_key: Option<String>,
    pub first_review_disposition_action_key: Option<String>,
    pub first_review_disposition_key: Option<String>,
    pub first_disposition_action_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionKind,
    >,
    pub first_disposition_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind,
    >,
    pub first_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub first_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub first_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_review_disposition_action_slot_key: Option<String>,
    pub next_review_disposition_action_key: Option<String>,
    pub next_review_disposition_key: Option<String>,
    pub next_work_order_key: Option<String>,
    pub next_ticket_key: Option<String>,
    pub next_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub next_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_actions_ready:
        bool,
    pub release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slots_ready: bool,
    pub release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slots_ready:
        bool,
}

impl
    IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotReadinessSummary
{
    pub fn from_summaries(
        release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_readiness_summary: IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionReadinessSummary,
        release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_summary: IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotSummary,
    ) -> Self {
        let release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_actions_ready =
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_readiness_summary
                .ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_actions();
        let release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slots_ready =
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_summary
                .release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slots_ready;
        let release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slots_ready =
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_actions_ready
                && release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slots_ready;
        let next_slot_requires_attention =
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_summary
                .requires_attention();
        let next_slot_key = if next_slot_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_summary
                .first_required_slot_key
                .clone()
                .or_else(|| {
                    release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_summary
                        .first_slot_key
                        .clone()
                })
        } else {
            None
        };
        let next_action_key = if next_slot_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_summary
                .first_disposition_action_key
                .clone()
        } else {
            None
        };
        let next_disposition_key = if next_slot_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_summary
                .first_disposition_key
                .clone()
        } else {
            None
        };
        let next_work_order_key = if next_slot_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_summary
                .first_work_order_key
                .clone()
        } else {
            None
        };
        let next_ticket_key = if next_slot_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_summary
                .first_ticket_key
                .clone()
        } else {
            None
        };
        let next_check_kind = if next_slot_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_summary
                .first_check_kind
        } else {
            None
        };
        let next_check_status = if next_slot_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_summary
                .first_check_status
        } else {
            None
        };
        let next_handoff_lane = if next_slot_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_summary
                .first_handoff_lane
        } else {
            None
        };

        Self {
            total_review_disposition_actions:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_readiness_summary
                    .total_review_disposition_actions,
            total_review_disposition_action_slots:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_summary
                    .total_slots,
            required_slots:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_summary
                    .required_slots,
            release_blocking_slots:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_summary
                    .release_blocking_slots,
            operator_required_slots:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_summary
                    .operator_required_slots,
            review_required_slots:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_summary
                    .review_required_slots,
            dispatch_required_slots:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_summary
                    .dispatch_required_slots,
            execution_required_slots:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_summary
                    .execution_required_slots,
            repair_slots:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_summary
                    .repair_slots,
            operator_handoff_slots:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_summary
                    .operator_handoff_slots,
            review_gate_slots:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_summary
                    .review_gate_slots,
            lineage_gap_slots:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_summary
                    .lineage_gap_slots,
            release_handoff_slots:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_summary
                    .release_handoff_slots,
            lineage_complete_slots:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_summary
                    .lineage_complete_slots,
            first_review_disposition_action_slot_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_summary
                    .first_slot_key
                    .clone(),
            first_required_slot_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_summary
                    .first_required_slot_key
                    .clone(),
            first_repair_slot_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_summary
                    .first_repair_slot_key
                    .clone(),
            first_operator_handoff_slot_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_summary
                    .first_operator_handoff_slot_key
                    .clone(),
            first_review_gate_slot_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_summary
                    .first_review_gate_slot_key
                    .clone(),
            first_lineage_gap_slot_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_summary
                    .first_lineage_gap_slot_key
                    .clone(),
            first_release_handoff_slot_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_summary
                    .first_release_handoff_slot_key
                    .clone(),
            first_review_disposition_action_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_summary
                    .first_disposition_action_key
                    .clone(),
            first_review_disposition_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_summary
                    .first_disposition_key
                    .clone(),
            first_disposition_action_kind:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_summary
                    .first_disposition_action_kind,
            first_disposition_kind:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_summary
                    .first_disposition_kind,
            first_handoff_lane:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_summary
                    .first_handoff_lane,
            first_check_kind:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_summary
                    .first_check_kind,
            first_check_status:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_summary
                    .first_check_status,
            next_review_disposition_action_slot_key: next_slot_key,
            next_review_disposition_action_key: next_action_key.or_else(|| {
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_readiness_summary
                    .next_review_disposition_action_key
                    .clone()
            }),
            next_review_disposition_key: next_disposition_key.or_else(|| {
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_readiness_summary
                    .next_review_disposition_key
                    .clone()
            }),
            next_work_order_key: next_work_order_key.or_else(|| {
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_readiness_summary
                    .next_work_order_key
                    .clone()
            }),
            next_ticket_key: next_ticket_key.or_else(|| {
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_readiness_summary
                    .next_ticket_key
                    .clone()
            }),
            next_check_kind: next_check_kind.or(
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_readiness_summary
                    .next_check_kind,
            ),
            next_check_status: next_check_status.or(
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_readiness_summary
                    .next_check_status,
            ),
            next_handoff_lane: next_handoff_lane.or(
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_readiness_summary
                    .next_handoff_lane,
            ),
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_actions_ready,
            release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slots_ready,
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slots_ready,
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_readiness_summary:
                Box::new(
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_readiness_summary,
                ),
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_summary:
                Box::new(
                    release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_summary,
                ),
        }
    }

    pub fn has_review_disposition_action_slots(&self) -> bool {
        self.total_review_disposition_action_slots > 0
    }

    pub fn ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slots(
        &self,
    ) -> bool {
        self.release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_actions_ready
            && self.release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slots_ready
            && self.release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slots_ready
            && !self.requires_attention()
    }

    pub fn has_required_slots(&self) -> bool {
        self.required_slots > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.release_blocking_slots > 0
            || self.repair_slots > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_readiness_summary
                .has_blockers()
            || self
                .release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_summary
                .has_blockers()
    }

    pub fn has_review_work(&self) -> bool {
        self.review_required_slots > 0
            || self.review_gate_slots > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_readiness_summary
                .has_review_work()
            || self
                .release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_summary
                .needs_review()
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_slots > 0
            || self.operator_handoff_slots > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_readiness_summary
                .needs_operator()
            || self
                .release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_summary
                .needs_operator()
    }

    pub fn has_lineage_gaps(&self) -> bool {
        self.lineage_gap_slots > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_readiness_summary
                .has_lineage_gaps()
            || self
                .release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_summary
                .has_lineage_gaps()
    }

    pub fn has_release_handoff_slots(&self) -> bool {
        self.release_handoff_slots > 0
    }

    pub fn has_complete_lineage(&self) -> bool {
        self.lineage_complete_slots == self.total_review_disposition_action_slots
            && self
                .release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_summary
                .has_complete_lineage()
    }

    pub fn requires_execution(&self) -> bool {
        self.execution_required_slots > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_readiness_summary
                .requires_execution()
    }

    pub fn requires_attention(&self) -> bool {
        self.requires_execution()
            || self.dispatch_required_slots > 0
            || self.has_required_slots()
            || self.has_blockers()
            || self.has_review_work()
            || self.needs_operator()
            || self.has_lineage_gaps()
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_readiness_summary
                .requires_attention()
            || self
                .release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_summary
                .requires_attention()
            || !self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slots_ready
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceReadinessSummary {
    pub release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_readiness_summary:
        Box<IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotReadinessSummary>,
    pub release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_summary:
        Box<IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceSummary>,
    pub total_review_disposition_action_slots: usize,
    pub total_slot_clearance_rows: usize,
    pub required_clearances: usize,
    pub release_blocking_clearances: usize,
    pub operator_required_clearances: usize,
    pub review_required_clearances: usize,
    pub dispatch_required_clearances: usize,
    pub execution_required_clearances: usize,
    pub repair_required_clearances: usize,
    pub operator_handoff_clearances: usize,
    pub review_gate_clearances: usize,
    pub lineage_gap_clearances: usize,
    pub release_handoff_clearances: usize,
    pub lineage_complete_clearances: usize,
    pub first_slot_clearance_key: Option<String>,
    pub first_required_clearance_key: Option<String>,
    pub first_repair_clearance_key: Option<String>,
    pub first_operator_handoff_clearance_key: Option<String>,
    pub first_review_clearance_key: Option<String>,
    pub first_lineage_gap_clearance_key: Option<String>,
    pub first_release_handoff_clearance_key: Option<String>,
    pub first_review_disposition_action_slot_key: Option<String>,
    pub first_review_disposition_action_key: Option<String>,
    pub first_review_disposition_key: Option<String>,
    pub first_slot_clearance_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceKind,
    >,
    pub first_disposition_action_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionKind,
    >,
    pub first_disposition_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind,
    >,
    pub first_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub first_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub first_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_slot_clearance_key: Option<String>,
    pub next_review_disposition_action_slot_key: Option<String>,
    pub next_review_disposition_action_key: Option<String>,
    pub next_review_disposition_key: Option<String>,
    pub next_work_order_key: Option<String>,
    pub next_ticket_key: Option<String>,
    pub next_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub next_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slots_ready:
        bool,
    pub release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearances_ready: bool,
    pub release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearances_ready:
        bool,
}

impl
    IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceReadinessSummary
{
    pub fn from_summaries(
        release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_readiness_summary: IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotReadinessSummary,
        release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_summary: IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceSummary,
    ) -> Self {
        let release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slots_ready =
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_readiness_summary
                .ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slots();
        let release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearances_ready =
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_summary
                .release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearances_ready;
        let release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearances_ready =
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slots_ready
                && release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearances_ready;
        let next_clearance_requires_attention =
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_summary
                .requires_attention();
        let next_clearance_key = if next_clearance_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_summary
                .first_required_clearance_key
                .clone()
                .or_else(|| {
                    release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_summary
                        .first_slot_clearance_key
                        .clone()
                })
        } else {
            None
        };
        let next_slot_key = if next_clearance_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_summary
                .first_slot_key
                .clone()
        } else {
            None
        };
        let next_action_key = if next_clearance_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_summary
                .first_disposition_action_key
                .clone()
        } else {
            None
        };
        let next_disposition_key = if next_clearance_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_summary
                .first_disposition_key
                .clone()
        } else {
            None
        };
        let next_work_order_key = if next_clearance_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_summary
                .first_work_order_key
                .clone()
        } else {
            None
        };
        let next_ticket_key = if next_clearance_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_summary
                .first_ticket_key
                .clone()
        } else {
            None
        };
        let next_check_kind = if next_clearance_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_summary
                .first_check_kind
        } else {
            None
        };
        let next_check_status = if next_clearance_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_summary
                .first_check_status
        } else {
            None
        };
        let next_handoff_lane = if next_clearance_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_summary
                .first_handoff_lane
        } else {
            None
        };

        Self {
            total_review_disposition_action_slots:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_readiness_summary
                    .total_review_disposition_action_slots,
            total_slot_clearance_rows:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_summary
                    .total_slot_clearance_rows,
            required_clearances:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_summary
                    .required_clearances,
            release_blocking_clearances:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_summary
                    .release_blocking_clearances,
            operator_required_clearances:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_summary
                    .operator_required_clearances,
            review_required_clearances:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_summary
                    .review_required_clearances_count,
            dispatch_required_clearances:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_summary
                    .dispatch_required_clearances,
            execution_required_clearances:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_summary
                    .execution_required_clearances,
            repair_required_clearances:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_summary
                    .repair_required_clearances,
            operator_handoff_clearances:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_summary
                    .operator_handoff_clearances,
            review_gate_clearances:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_summary
                    .review_required_clearances,
            lineage_gap_clearances:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_summary
                    .lineage_gap_clearances,
            release_handoff_clearances:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_summary
                    .release_handoff_clearances,
            lineage_complete_clearances:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_summary
                    .lineage_complete_clearances,
            first_slot_clearance_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_summary
                    .first_slot_clearance_key
                    .clone(),
            first_required_clearance_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_summary
                    .first_required_clearance_key
                    .clone(),
            first_repair_clearance_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_summary
                    .first_repair_clearance_key
                    .clone(),
            first_operator_handoff_clearance_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_summary
                    .first_operator_handoff_clearance_key
                    .clone(),
            first_review_clearance_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_summary
                    .first_review_clearance_key
                    .clone(),
            first_lineage_gap_clearance_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_summary
                    .first_lineage_gap_clearance_key
                    .clone(),
            first_release_handoff_clearance_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_summary
                    .first_release_handoff_clearance_key
                    .clone(),
            first_review_disposition_action_slot_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_summary
                    .first_slot_key
                    .clone(),
            first_review_disposition_action_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_summary
                    .first_disposition_action_key
                    .clone(),
            first_review_disposition_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_summary
                    .first_disposition_key
                    .clone(),
            first_slot_clearance_kind:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_summary
                    .first_slot_clearance_kind,
            first_disposition_action_kind:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_summary
                    .first_disposition_action_kind,
            first_disposition_kind:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_summary
                    .first_disposition_kind,
            first_handoff_lane:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_summary
                    .first_handoff_lane,
            first_check_kind:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_summary
                    .first_check_kind,
            first_check_status:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_summary
                    .first_check_status,
            next_slot_clearance_key: next_clearance_key,
            next_review_disposition_action_slot_key: next_slot_key.or_else(|| {
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_readiness_summary
                    .next_review_disposition_action_slot_key
                    .clone()
            }),
            next_review_disposition_action_key: next_action_key.or_else(|| {
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_readiness_summary
                    .next_review_disposition_action_key
                    .clone()
            }),
            next_review_disposition_key: next_disposition_key.or_else(|| {
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_readiness_summary
                    .next_review_disposition_key
                    .clone()
            }),
            next_work_order_key: next_work_order_key.or_else(|| {
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_readiness_summary
                    .next_work_order_key
                    .clone()
            }),
            next_ticket_key: next_ticket_key.or_else(|| {
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_readiness_summary
                    .next_ticket_key
                    .clone()
            }),
            next_check_kind: next_check_kind.or(
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_readiness_summary
                    .next_check_kind,
            ),
            next_check_status: next_check_status.or(
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_readiness_summary
                    .next_check_status,
            ),
            next_handoff_lane: next_handoff_lane.or(
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_readiness_summary
                    .next_handoff_lane,
            ),
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slots_ready,
            release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearances_ready,
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearances_ready,
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_readiness_summary:
                Box::new(
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_readiness_summary,
                ),
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_summary:
                Box::new(
                    release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_summary,
                ),
        }
    }

    pub fn has_slot_clearance_rows(&self) -> bool {
        self.total_slot_clearance_rows > 0
    }

    pub fn ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearances(
        &self,
    ) -> bool {
        self.release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slots_ready
            && self.release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearances_ready
            && self.release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearances_ready
            && !self.requires_attention()
    }

    pub fn has_required_clearances(&self) -> bool {
        self.required_clearances > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.release_blocking_clearances > 0
            || self.repair_required_clearances > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_readiness_summary
                .has_blockers()
            || self
                .release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_summary
                .has_blockers()
    }

    pub fn has_review_work(&self) -> bool {
        self.review_required_clearances > 0
            || self.review_gate_clearances > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_readiness_summary
                .has_review_work()
            || self
                .release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_summary
                .needs_review()
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_clearances > 0
            || self.operator_handoff_clearances > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_readiness_summary
                .needs_operator()
            || self
                .release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_summary
                .needs_operator()
    }

    pub fn has_lineage_gaps(&self) -> bool {
        self.lineage_gap_clearances > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_readiness_summary
                .has_lineage_gaps()
            || self
                .release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_summary
                .has_lineage_gaps()
    }

    pub fn has_release_handoff_clearances(&self) -> bool {
        self.release_handoff_clearances > 0
    }

    pub fn has_complete_lineage(&self) -> bool {
        self.lineage_complete_clearances == self.total_slot_clearance_rows
            && self
                .release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_summary
                .has_complete_lineage()
    }

    pub fn requires_execution(&self) -> bool {
        self.execution_required_clearances > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_readiness_summary
                .requires_execution()
    }

    pub fn requires_attention(&self) -> bool {
        self.requires_execution()
            || self.dispatch_required_clearances > 0
            || self.has_required_clearances()
            || self.has_blockers()
            || self.has_review_work()
            || self.needs_operator()
            || self.has_lineage_gaps()
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_readiness_summary
                .requires_attention()
            || self
                .release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_summary
                .requires_attention()
            || !self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearances_ready
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionReadinessSummary {
    pub release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_readiness_summary:
        Box<IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceReadinessSummary>,
    pub release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary:
        Box<IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionSummary>,
    pub total_slot_clearance_rows: usize,
    pub total_slot_clearance_action_rows: usize,
    pub required_actions: usize,
    pub release_blocking_actions: usize,
    pub operator_required_actions: usize,
    pub review_required_actions: usize,
    pub dispatch_required_actions: usize,
    pub execution_required_actions: usize,
    pub repair_actions: usize,
    pub operator_handoff_actions: usize,
    pub review_actions: usize,
    pub lineage_actions: usize,
    pub release_handoff_actions: usize,
    pub lineage_complete_actions: usize,
    pub ready_actions: usize,
    pub first_slot_clearance_action_key: Option<String>,
    pub first_required_action_key: Option<String>,
    pub first_repair_action_key: Option<String>,
    pub first_operator_handoff_action_key: Option<String>,
    pub first_review_action_key: Option<String>,
    pub first_lineage_action_key: Option<String>,
    pub first_release_handoff_action_key: Option<String>,
    pub first_slot_clearance_key: Option<String>,
    pub first_review_disposition_action_slot_key: Option<String>,
    pub first_review_disposition_action_key: Option<String>,
    pub first_review_disposition_key: Option<String>,
    pub first_work_order_key: Option<String>,
    pub first_ticket_key: Option<String>,
    pub first_slot_clearance_action_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionKind,
    >,
    pub first_slot_clearance_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceKind,
    >,
    pub first_disposition_action_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionKind,
    >,
    pub first_disposition_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind,
    >,
    pub first_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub first_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub first_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_slot_clearance_action_key: Option<String>,
    pub next_slot_clearance_key: Option<String>,
    pub next_review_disposition_action_slot_key: Option<String>,
    pub next_review_disposition_action_key: Option<String>,
    pub next_review_disposition_key: Option<String>,
    pub next_work_order_key: Option<String>,
    pub next_ticket_key: Option<String>,
    pub next_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub next_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearances_ready:
        bool,
    pub release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_actions_ready: bool,
    pub release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_actions_ready:
        bool,
}

impl
    IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionReadinessSummary
{
    pub fn from_summaries(
        release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_readiness_summary: IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceReadinessSummary,
        release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary: IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionSummary,
    ) -> Self {
        let release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearances_ready =
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_readiness_summary
                .ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearances();
        let release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_actions_ready =
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary
                .release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_actions_ready;
        let release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_actions_ready =
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearances_ready
                && release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_actions_ready;
        let next_action_requires_attention =
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary
                .requires_attention();
        let next_slot_clearance_action_key = if next_action_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary
                .first_required_action_key
                .clone()
                .or_else(|| {
                    release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary
                        .first_slot_clearance_action_key
                        .clone()
                })
        } else {
            None
        };
        let next_slot_clearance_key = if next_action_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary
                .first_slot_clearance_key
                .clone()
        } else {
            None
        };
        let next_slot_key = if next_action_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary
                .first_slot_key
                .clone()
        } else {
            None
        };
        let next_action_key = if next_action_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary
                .first_disposition_action_key
                .clone()
        } else {
            None
        };
        let next_disposition_key = if next_action_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary
                .first_disposition_key
                .clone()
        } else {
            None
        };
        let next_work_order_key = if next_action_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary
                .first_work_order_key
                .clone()
        } else {
            None
        };
        let next_ticket_key = if next_action_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary
                .first_ticket_key
                .clone()
        } else {
            None
        };
        let next_check_kind = if next_action_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary
                .first_check_kind
        } else {
            None
        };
        let next_check_status = if next_action_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary
                .first_check_status
        } else {
            None
        };
        let next_handoff_lane = if next_action_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary
                .first_handoff_lane
        } else {
            None
        };

        Self {
            total_slot_clearance_rows:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_readiness_summary
                    .total_slot_clearance_rows,
            total_slot_clearance_action_rows:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary
                    .total_slot_clearance_action_rows,
            required_actions:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary
                    .required_actions,
            release_blocking_actions:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary
                    .release_blocking_actions,
            operator_required_actions:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary
                    .operator_required_actions,
            review_required_actions:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary
                    .review_required_actions,
            dispatch_required_actions:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary
                    .dispatch_required_actions,
            execution_required_actions:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary
                    .execution_required_actions,
            repair_actions:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary
                    .repair_actions,
            operator_handoff_actions:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary
                    .operator_handoff_actions,
            review_actions:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary
                    .review_actions,
            lineage_actions:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary
                    .lineage_actions,
            release_handoff_actions:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary
                    .release_handoff_actions,
            lineage_complete_actions:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary
                    .lineage_complete_actions,
            ready_actions:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary
                    .ready_actions,
            first_slot_clearance_action_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary
                    .first_slot_clearance_action_key
                    .clone(),
            first_required_action_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary
                    .first_required_action_key
                    .clone(),
            first_repair_action_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary
                    .first_repair_action_key
                    .clone(),
            first_operator_handoff_action_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary
                    .first_operator_handoff_action_key
                    .clone(),
            first_review_action_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary
                    .first_review_action_key
                    .clone(),
            first_lineage_action_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary
                    .first_lineage_action_key
                    .clone(),
            first_release_handoff_action_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary
                    .first_release_handoff_action_key
                    .clone(),
            first_slot_clearance_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary
                    .first_slot_clearance_key
                    .clone(),
            first_review_disposition_action_slot_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary
                    .first_slot_key
                    .clone(),
            first_review_disposition_action_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary
                    .first_disposition_action_key
                    .clone(),
            first_review_disposition_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary
                    .first_disposition_key
                    .clone(),
            first_work_order_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary
                    .first_work_order_key
                    .clone(),
            first_ticket_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary
                    .first_ticket_key
                    .clone(),
            first_slot_clearance_action_kind:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary
                    .first_slot_clearance_action_kind,
            first_slot_clearance_kind:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary
                    .first_slot_clearance_kind,
            first_disposition_action_kind:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary
                    .first_disposition_action_kind,
            first_disposition_kind:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary
                    .first_disposition_kind,
            first_handoff_lane:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary
                    .first_handoff_lane,
            first_check_kind:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary
                    .first_check_kind,
            first_check_status:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary
                    .first_check_status,
            next_slot_clearance_action_key,
            next_slot_clearance_key: next_slot_clearance_key.or_else(|| {
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_readiness_summary
                    .next_slot_clearance_key
                    .clone()
            }),
            next_review_disposition_action_slot_key: next_slot_key.or_else(|| {
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_readiness_summary
                    .next_review_disposition_action_slot_key
                    .clone()
            }),
            next_review_disposition_action_key: next_action_key.or_else(|| {
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_readiness_summary
                    .next_review_disposition_action_key
                    .clone()
            }),
            next_review_disposition_key: next_disposition_key.or_else(|| {
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_readiness_summary
                    .next_review_disposition_key
                    .clone()
            }),
            next_work_order_key: next_work_order_key.or_else(|| {
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_readiness_summary
                    .next_work_order_key
                    .clone()
            }),
            next_ticket_key: next_ticket_key.or_else(|| {
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_readiness_summary
                    .next_ticket_key
                    .clone()
            }),
            next_check_kind: next_check_kind.or(
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_readiness_summary
                    .next_check_kind,
            ),
            next_check_status: next_check_status.or(
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_readiness_summary
                    .next_check_status,
            ),
            next_handoff_lane: next_handoff_lane.or(
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_readiness_summary
                    .next_handoff_lane,
            ),
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearances_ready,
            release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_actions_ready,
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_actions_ready,
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_readiness_summary:
                Box::new(
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_readiness_summary,
                ),
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary:
                Box::new(
                    release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary,
                ),
        }
    }

    pub fn has_slot_clearance_action_rows(&self) -> bool {
        self.total_slot_clearance_action_rows > 0
    }

    pub fn ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_actions(
        &self,
    ) -> bool {
        self.release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearances_ready
            && self.release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_actions_ready
            && self.release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_actions_ready
            && !self.requires_attention()
    }

    pub fn has_required_actions(&self) -> bool {
        self.required_actions > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.release_blocking_actions > 0
            || self.repair_actions > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_readiness_summary
                .has_blockers()
            || self
                .release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary
                .has_blockers()
    }

    pub fn has_review_work(&self) -> bool {
        self.review_required_actions > 0
            || self.review_actions > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_readiness_summary
                .has_review_work()
            || self
                .release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary
                .needs_review()
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_actions > 0
            || self.operator_handoff_actions > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_readiness_summary
                .needs_operator()
            || self
                .release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary
                .needs_operator()
    }

    pub fn has_lineage_gaps(&self) -> bool {
        self.lineage_actions > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_readiness_summary
                .has_lineage_gaps()
            || self
                .release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary
                .has_lineage_gaps()
    }

    pub fn has_release_handoff_actions(&self) -> bool {
        self.release_handoff_actions > 0
    }

    pub fn has_complete_lineage(&self) -> bool {
        self.lineage_complete_actions == self.total_slot_clearance_action_rows
            && self
                .release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary
                .has_complete_lineage()
    }

    pub fn requires_execution(&self) -> bool {
        self.execution_required_actions > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_readiness_summary
                .requires_execution()
    }

    pub fn requires_attention(&self) -> bool {
        self.requires_execution()
            || self.dispatch_required_actions > 0
            || self.has_required_actions()
            || self.has_blockers()
            || self.has_review_work()
            || self.needs_operator()
            || self.has_lineage_gaps()
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_readiness_summary
                .requires_attention()
            || self
                .release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary
                .requires_attention()
            || !self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_actions_ready
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReadinessSummary {
    pub release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_readiness_summary:
        Box<IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionReadinessSummary>,
    pub release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary:
        Box<IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceSummary>,
    pub total_slot_clearance_action_rows: usize,
    pub total_slot_clearance_action_evidence_rows: usize,
    pub required_action_evidence: usize,
    pub release_blocking_action_evidence: usize,
    pub operator_required_action_evidence: usize,
    pub review_required_action_evidence: usize,
    pub dispatch_required_action_evidence: usize,
    pub execution_required_action_evidence: usize,
    pub repair_action_evidence: usize,
    pub operator_handoff_action_evidence: usize,
    pub review_action_evidence: usize,
    pub lineage_action_evidence: usize,
    pub release_handoff_action_evidence: usize,
    pub lineage_complete_action_evidence: usize,
    pub ready_action_evidence: usize,
    pub first_slot_clearance_action_evidence_key: Option<String>,
    pub first_required_action_evidence_key: Option<String>,
    pub first_repair_action_evidence_key: Option<String>,
    pub first_operator_handoff_action_evidence_key: Option<String>,
    pub first_review_action_evidence_key: Option<String>,
    pub first_lineage_action_evidence_key: Option<String>,
    pub first_release_handoff_action_evidence_key: Option<String>,
    pub first_slot_clearance_action_key: Option<String>,
    pub first_slot_clearance_key: Option<String>,
    pub first_review_disposition_action_slot_key: Option<String>,
    pub first_review_disposition_action_key: Option<String>,
    pub first_work_order_key: Option<String>,
    pub first_ticket_key: Option<String>,
    pub first_slot_clearance_action_evidence_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceKind,
    >,
    pub first_slot_clearance_action_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionKind,
    >,
    pub first_slot_clearance_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceKind,
    >,
    pub first_disposition_action_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionKind,
    >,
    pub first_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub first_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub first_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_slot_clearance_action_evidence_key: Option<String>,
    pub next_slot_clearance_action_key: Option<String>,
    pub next_slot_clearance_key: Option<String>,
    pub next_review_disposition_action_slot_key: Option<String>,
    pub next_review_disposition_action_key: Option<String>,
    pub next_work_order_key: Option<String>,
    pub next_ticket_key: Option<String>,
    pub next_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub next_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_actions_ready:
        bool,
    pub release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_ready:
        bool,
    pub release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_ready:
        bool,
}

impl
    IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReadinessSummary
{
    pub fn from_summaries(
        release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_readiness_summary: IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionReadinessSummary,
        release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary: IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceSummary,
    ) -> Self {
        let release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_actions_ready =
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_readiness_summary
                .ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_actions();
        let release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_ready =
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary
                .release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_ready;
        let release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_ready =
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_actions_ready
                && release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_ready;
        let next_evidence_requires_attention =
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary
                .requires_attention();
        let next_slot_clearance_action_evidence_key = if next_evidence_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary
                .first_required_evidence_key
                .clone()
                .or_else(|| {
                    release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary
                        .first_slot_clearance_action_evidence_key
                        .clone()
                })
        } else {
            None
        };
        let next_slot_clearance_action_key = if next_evidence_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary
                .first_slot_clearance_action_key
                .clone()
        } else {
            None
        };
        let next_slot_clearance_key = if next_evidence_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary
                .first_slot_clearance_key
                .clone()
        } else {
            None
        };
        let next_slot_key = if next_evidence_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary
                .first_slot_key
                .clone()
        } else {
            None
        };
        let next_action_key = if next_evidence_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary
                .first_disposition_action_key
                .clone()
        } else {
            None
        };
        let next_work_order_key = if next_evidence_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary
                .first_work_order_key
                .clone()
        } else {
            None
        };
        let next_ticket_key = if next_evidence_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary
                .first_ticket_key
                .clone()
        } else {
            None
        };
        let next_check_kind = if next_evidence_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary
                .first_check_kind
        } else {
            None
        };
        let next_check_status = if next_evidence_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary
                .first_check_status
        } else {
            None
        };
        let next_handoff_lane = if next_evidence_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary
                .first_handoff_lane
        } else {
            None
        };

        Self {
            total_slot_clearance_action_rows:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_readiness_summary
                    .total_slot_clearance_action_rows,
            total_slot_clearance_action_evidence_rows:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary
                    .total_slot_clearance_action_evidence_rows,
            required_action_evidence:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary
                    .required_evidence,
            release_blocking_action_evidence:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary
                    .release_blocking_evidence,
            operator_required_action_evidence:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary
                    .operator_required_evidence,
            review_required_action_evidence:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary
                    .review_required_evidence,
            dispatch_required_action_evidence:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary
                    .dispatch_required_evidence,
            execution_required_action_evidence:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary
                    .execution_required_evidence,
            repair_action_evidence:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary
                    .repair_evidence,
            operator_handoff_action_evidence:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary
                    .operator_handoff_evidence,
            review_action_evidence:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary
                    .review_evidence,
            lineage_action_evidence:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary
                    .lineage_evidence,
            release_handoff_action_evidence:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary
                    .release_handoff_evidence,
            lineage_complete_action_evidence:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary
                    .lineage_complete_evidence,
            ready_action_evidence:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary
                    .ready_evidence,
            first_slot_clearance_action_evidence_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary
                    .first_slot_clearance_action_evidence_key
                    .clone(),
            first_required_action_evidence_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary
                    .first_required_evidence_key
                    .clone(),
            first_repair_action_evidence_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary
                    .first_repair_evidence_key
                    .clone(),
            first_operator_handoff_action_evidence_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary
                    .first_operator_handoff_evidence_key
                    .clone(),
            first_review_action_evidence_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary
                    .first_review_evidence_key
                    .clone(),
            first_lineage_action_evidence_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary
                    .first_lineage_evidence_key
                    .clone(),
            first_release_handoff_action_evidence_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary
                    .first_release_handoff_evidence_key
                    .clone(),
            first_slot_clearance_action_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary
                    .first_slot_clearance_action_key
                    .clone(),
            first_slot_clearance_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary
                    .first_slot_clearance_key
                    .clone(),
            first_review_disposition_action_slot_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary
                    .first_slot_key
                    .clone(),
            first_review_disposition_action_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary
                    .first_disposition_action_key
                    .clone(),
            first_work_order_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary
                    .first_work_order_key
                    .clone(),
            first_ticket_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary
                    .first_ticket_key
                    .clone(),
            first_slot_clearance_action_evidence_kind:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary
                    .first_slot_clearance_action_evidence_kind,
            first_slot_clearance_action_kind:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary
                    .first_slot_clearance_action_kind,
            first_slot_clearance_kind:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary
                    .first_slot_clearance_kind,
            first_disposition_action_kind:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary
                    .first_disposition_action_kind,
            first_handoff_lane:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary
                    .first_handoff_lane,
            first_check_kind:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary
                    .first_check_kind,
            first_check_status:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary
                    .first_check_status,
            next_slot_clearance_action_evidence_key,
            next_slot_clearance_action_key: next_slot_clearance_action_key.or_else(|| {
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_readiness_summary
                    .next_slot_clearance_action_key
                    .clone()
            }),
            next_slot_clearance_key: next_slot_clearance_key.or_else(|| {
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_readiness_summary
                    .next_slot_clearance_key
                    .clone()
            }),
            next_review_disposition_action_slot_key: next_slot_key.or_else(|| {
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_readiness_summary
                    .next_review_disposition_action_slot_key
                    .clone()
            }),
            next_review_disposition_action_key: next_action_key.or_else(|| {
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_readiness_summary
                    .next_review_disposition_action_key
                    .clone()
            }),
            next_work_order_key: next_work_order_key.or_else(|| {
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_readiness_summary
                    .next_work_order_key
                    .clone()
            }),
            next_ticket_key: next_ticket_key.or_else(|| {
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_readiness_summary
                    .next_ticket_key
                    .clone()
            }),
            next_check_kind: next_check_kind.or(
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_readiness_summary
                    .next_check_kind,
            ),
            next_check_status: next_check_status.or(
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_readiness_summary
                    .next_check_status,
            ),
            next_handoff_lane: next_handoff_lane.or(
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_readiness_summary
                    .next_handoff_lane,
            ),
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_actions_ready,
            release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_ready,
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_ready,
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_readiness_summary:
                Box::new(
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_readiness_summary,
                ),
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary:
                Box::new(
                    release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary,
                ),
        }
    }

    pub fn has_slot_clearance_action_evidence_rows(&self) -> bool {
        self.total_slot_clearance_action_evidence_rows > 0
    }

    pub fn ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence(
        &self,
    ) -> bool {
        self.release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_actions_ready
            && self.release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_ready
            && self.release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_ready
            && !self.requires_attention()
    }

    pub fn has_required_action_evidence(&self) -> bool {
        self.required_action_evidence > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.release_blocking_action_evidence > 0
            || self.repair_action_evidence > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_readiness_summary
                .has_blockers()
            || self
                .release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary
                .has_blockers()
    }

    pub fn has_review_work(&self) -> bool {
        self.review_required_action_evidence > 0
            || self.review_action_evidence > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_readiness_summary
                .has_review_work()
            || self
                .release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary
                .needs_review()
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_action_evidence > 0
            || self.operator_handoff_action_evidence > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_readiness_summary
                .needs_operator()
            || self
                .release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary
                .needs_operator()
    }

    pub fn has_lineage_gaps(&self) -> bool {
        self.lineage_action_evidence > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_readiness_summary
                .has_lineage_gaps()
            || self
                .release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary
                .has_lineage_gaps()
    }

    pub fn has_release_handoff_action_evidence(&self) -> bool {
        self.release_handoff_action_evidence > 0
    }

    pub fn has_complete_lineage(&self) -> bool {
        self.lineage_complete_action_evidence == self.total_slot_clearance_action_evidence_rows
            && self
                .release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary
                .has_complete_lineage()
    }

    pub fn requires_execution(&self) -> bool {
        self.execution_required_action_evidence > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_readiness_summary
                .requires_execution()
    }

    pub fn requires_attention(&self) -> bool {
        self.requires_execution()
            || self.dispatch_required_action_evidence > 0
            || self.has_required_action_evidence()
            || self.has_blockers()
            || self.has_review_work()
            || self.needs_operator()
            || self.has_lineage_gaps()
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_readiness_summary
                .requires_attention()
            || self
                .release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary
                .requires_attention()
            || !self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_ready
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewReadinessSummary {
    pub release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_readiness_summary:
        Box<IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReadinessSummary>,
    pub release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary:
        Box<IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewSummary>,
    pub total_slot_clearance_action_evidence_rows: usize,
    pub total_slot_clearance_action_evidence_reviews: usize,
    pub required_action_evidence_reviews: usize,
    pub blocking_action_evidence_reviews: usize,
    pub operator_required_action_evidence_reviews: usize,
    pub review_required_action_evidence_reviews: usize,
    pub dispatch_required_action_evidence_reviews: usize,
    pub execution_required_action_evidence_reviews: usize,
    pub lineage_gap_action_evidence_reviews: usize,
    pub release_ready_action_evidence_reviews: usize,
    pub release_handoff_action_evidence_reviews: usize,
    pub lineage_complete_action_evidence_reviews: usize,
    pub first_slot_clearance_action_evidence_review_key: Option<String>,
    pub first_required_action_evidence_review_key: Option<String>,
    pub first_blocking_action_evidence_review_key: Option<String>,
    pub first_operator_action_evidence_review_key: Option<String>,
    pub first_review_gate_action_evidence_review_key: Option<String>,
    pub first_lineage_gap_action_evidence_review_key: Option<String>,
    pub first_release_ready_action_evidence_review_key: Option<String>,
    pub first_slot_clearance_action_evidence_key: Option<String>,
    pub first_slot_clearance_action_key: Option<String>,
    pub first_slot_clearance_key: Option<String>,
    pub first_review_disposition_action_slot_key: Option<String>,
    pub first_review_disposition_action_key: Option<String>,
    pub first_work_order_key: Option<String>,
    pub first_ticket_key: Option<String>,
    pub first_action_evidence_review_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind,
    >,
    pub first_slot_clearance_action_evidence_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceKind,
    >,
    pub first_slot_clearance_action_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionKind,
    >,
    pub first_slot_clearance_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceKind,
    >,
    pub first_disposition_action_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionKind,
    >,
    pub first_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub first_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub first_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_slot_clearance_action_evidence_review_key: Option<String>,
    pub next_slot_clearance_action_evidence_key: Option<String>,
    pub next_slot_clearance_action_key: Option<String>,
    pub next_slot_clearance_key: Option<String>,
    pub next_review_disposition_action_slot_key: Option<String>,
    pub next_review_disposition_action_key: Option<String>,
    pub next_work_order_key: Option<String>,
    pub next_ticket_key: Option<String>,
    pub next_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub next_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_ready:
        bool,
    pub release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_reviews_ready:
        bool,
    pub release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_reviews_ready:
        bool,
}

impl
    IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewReadinessSummary
{
    pub fn from_summaries(
        release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_readiness_summary: IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReadinessSummary,
        release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary: IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewSummary,
    ) -> Self {
        let release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_ready =
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_readiness_summary
                .ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence();
        let release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_reviews_ready =
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary
                .release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_reviews_ready;
        let release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_reviews_ready =
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_ready
                && release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_reviews_ready;
        let next_review_requires_attention =
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary
                .requires_attention();
        let next_slot_clearance_action_evidence_review_key =
            if next_review_requires_attention {
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary
                    .first_required_review_key
                    .clone()
                    .or_else(|| {
                        release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary
                            .first_slot_clearance_action_evidence_review_key
                            .clone()
                    })
            } else {
                None
            };
        let next_slot_clearance_action_evidence_key = if next_review_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary
                .first_slot_clearance_action_evidence_key
                .clone()
        } else {
            None
        };
        let next_slot_clearance_action_key = if next_review_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary
                .first_slot_clearance_action_key
                .clone()
        } else {
            None
        };
        let next_slot_clearance_key = if next_review_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary
                .first_slot_clearance_key
                .clone()
        } else {
            None
        };
        let next_slot_key = if next_review_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary
                .first_slot_key
                .clone()
        } else {
            None
        };
        let next_action_key = if next_review_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary
                .first_disposition_action_key
                .clone()
        } else {
            None
        };
        let next_work_order_key = if next_review_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary
                .first_work_order_key
                .clone()
        } else {
            None
        };
        let next_ticket_key = if next_review_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary
                .first_ticket_key
                .clone()
        } else {
            None
        };
        let next_check_kind = if next_review_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary
                .first_check_kind
        } else {
            None
        };
        let next_check_status = if next_review_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary
                .first_check_status
        } else {
            None
        };
        let next_handoff_lane = if next_review_requires_attention {
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary
                .first_handoff_lane
        } else {
            None
        };

        Self {
            total_slot_clearance_action_evidence_rows:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_readiness_summary
                    .total_slot_clearance_action_evidence_rows,
            total_slot_clearance_action_evidence_reviews:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary
                    .total_slot_clearance_action_evidence_reviews,
            required_action_evidence_reviews:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary
                    .required_reviews,
            blocking_action_evidence_reviews:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary
                    .blocking_reviews,
            operator_required_action_evidence_reviews:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary
                    .operator_required_reviews,
            review_required_action_evidence_reviews:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary
                    .review_required_reviews,
            dispatch_required_action_evidence_reviews:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary
                    .dispatch_required_reviews,
            execution_required_action_evidence_reviews:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary
                    .execution_required_reviews,
            lineage_gap_action_evidence_reviews:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary
                    .lineage_gap_reviews,
            release_ready_action_evidence_reviews:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary
                    .release_ready_reviews,
            release_handoff_action_evidence_reviews:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary
                    .release_handoff_evidence_reviews,
            lineage_complete_action_evidence_reviews:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary
                    .lineage_complete_reviews,
            first_slot_clearance_action_evidence_review_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary
                    .first_slot_clearance_action_evidence_review_key
                    .clone(),
            first_required_action_evidence_review_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary
                    .first_required_review_key
                    .clone(),
            first_blocking_action_evidence_review_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary
                    .first_blocking_review_key
                    .clone(),
            first_operator_action_evidence_review_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary
                    .first_operator_review_key
                    .clone(),
            first_review_gate_action_evidence_review_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary
                    .first_review_gate_key
                    .clone(),
            first_lineage_gap_action_evidence_review_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary
                    .first_lineage_gap_review_key
                    .clone(),
            first_release_ready_action_evidence_review_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary
                    .first_release_ready_review_key
                    .clone(),
            first_slot_clearance_action_evidence_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary
                    .first_slot_clearance_action_evidence_key
                    .clone(),
            first_slot_clearance_action_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary
                    .first_slot_clearance_action_key
                    .clone(),
            first_slot_clearance_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary
                    .first_slot_clearance_key
                    .clone(),
            first_review_disposition_action_slot_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary
                    .first_slot_key
                    .clone(),
            first_review_disposition_action_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary
                    .first_disposition_action_key
                    .clone(),
            first_work_order_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary
                    .first_work_order_key
                    .clone(),
            first_ticket_key:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary
                    .first_ticket_key
                    .clone(),
            first_action_evidence_review_kind:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary
                    .first_review_kind,
            first_slot_clearance_action_evidence_kind:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary
                    .first_slot_clearance_action_evidence_kind,
            first_slot_clearance_action_kind:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary
                    .first_slot_clearance_action_kind,
            first_slot_clearance_kind:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary
                    .first_slot_clearance_kind,
            first_disposition_action_kind:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary
                    .first_disposition_action_kind,
            first_handoff_lane:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary
                    .first_handoff_lane,
            first_check_kind:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary
                    .first_check_kind,
            first_check_status:
                release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary
                    .first_check_status,
            next_slot_clearance_action_evidence_review_key,
            next_slot_clearance_action_evidence_key: next_slot_clearance_action_evidence_key.or_else(|| {
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_readiness_summary
                    .next_slot_clearance_action_evidence_key
                    .clone()
            }),
            next_slot_clearance_action_key: next_slot_clearance_action_key.or_else(|| {
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_readiness_summary
                    .next_slot_clearance_action_key
                    .clone()
            }),
            next_slot_clearance_key: next_slot_clearance_key.or_else(|| {
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_readiness_summary
                    .next_slot_clearance_key
                    .clone()
            }),
            next_review_disposition_action_slot_key: next_slot_key.or_else(|| {
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_readiness_summary
                    .next_review_disposition_action_slot_key
                    .clone()
            }),
            next_review_disposition_action_key: next_action_key.or_else(|| {
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_readiness_summary
                    .next_review_disposition_action_key
                    .clone()
            }),
            next_work_order_key: next_work_order_key.or_else(|| {
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_readiness_summary
                    .next_work_order_key
                    .clone()
            }),
            next_ticket_key: next_ticket_key.or_else(|| {
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_readiness_summary
                    .next_ticket_key
                    .clone()
            }),
            next_check_kind: next_check_kind.or(
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_readiness_summary
                    .next_check_kind,
            ),
            next_check_status: next_check_status.or(
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_readiness_summary
                    .next_check_status,
            ),
            next_handoff_lane: next_handoff_lane.or(
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_readiness_summary
                    .next_handoff_lane,
            ),
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_ready,
            release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_reviews_ready,
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_reviews_ready,
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_readiness_summary:
                Box::new(
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_readiness_summary,
                ),
            release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary:
                Box::new(
                    release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary,
                ),
        }
    }

    pub fn has_slot_clearance_action_evidence_reviews(&self) -> bool {
        self.total_slot_clearance_action_evidence_reviews > 0
    }

    pub fn ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_reviews(
        &self,
    ) -> bool {
        self.release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_ready
            && self.release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_reviews_ready
            && self.release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_reviews_ready
            && !self.requires_attention()
    }

    pub fn has_required_action_evidence_reviews(&self) -> bool {
        self.required_action_evidence_reviews > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.blocking_action_evidence_reviews > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_readiness_summary
                .has_blockers()
            || self
                .release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary
                .has_blockers()
    }

    pub fn has_review_work(&self) -> bool {
        self.review_required_action_evidence_reviews > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_readiness_summary
                .has_review_work()
            || self
                .release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary
                .needs_review()
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_action_evidence_reviews > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_readiness_summary
                .needs_operator()
            || self
                .release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary
                .needs_operator()
    }

    pub fn has_lineage_gaps(&self) -> bool {
        self.lineage_gap_action_evidence_reviews > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_readiness_summary
                .has_lineage_gaps()
            || self
                .release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary
                .has_lineage_gaps()
    }

    pub fn has_release_ready_action_evidence_reviews(&self) -> bool {
        self.release_ready_action_evidence_reviews > 0
    }

    pub fn has_complete_lineage(&self) -> bool {
        self.lineage_complete_action_evidence_reviews
            == self.total_slot_clearance_action_evidence_reviews
            && self
                .release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary
                .has_complete_lineage()
    }

    pub fn requires_execution(&self) -> bool {
        self.execution_required_action_evidence_reviews > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_readiness_summary
                .requires_execution()
    }

    pub fn requires_attention(&self) -> bool {
        self.requires_execution()
            || self.dispatch_required_action_evidence_reviews > 0
            || self.has_required_action_evidence_reviews()
            || self.has_blockers()
            || self.has_review_work()
            || self.needs_operator()
            || self.has_lineage_gaps()
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_readiness_summary
                .requires_attention()
            || self
                .release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary
                .requires_attention()
            || !self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_reviews_ready
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceKind
{
    RepairRequired,
    OperatorHandoff,
    ReviewRequired,
    DispatchRequired,
    ExecutionRequired,
    LineageGap,
    ReleaseHandoff,
}

impl
    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceKind
{
    pub fn as_str(self) -> &'static str {
        match self {
            Self::RepairRequired => "repair_required",
            Self::OperatorHandoff => "operator_handoff",
            Self::ReviewRequired => "review_required",
            Self::DispatchRequired => "dispatch_required",
            Self::ExecutionRequired => "execution_required",
            Self::LineageGap => "lineage_gap",
            Self::ReleaseHandoff => "release_handoff",
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceRow {
    pub sequence: usize,
    pub slot_clearance_action_evidence_review_clearance_key: String,
    pub slot_clearance_action_evidence_review_sequence: usize,
    pub slot_clearance_action_evidence_review_key: String,
    pub slot_clearance_action_evidence_key: String,
    pub slot_clearance_action_key: String,
    pub slot_clearance_key: String,
    pub slot_key: String,
    pub disposition_action_key: String,
    pub disposition_key: String,
    pub work_order_key: String,
    pub ticket_key: String,
    pub check_sequence: usize,
    pub check_kind: IntegrationMeshReleaseReadinessCheckKind,
    pub status: IntegrationMeshReleaseReadinessStatus,
    pub handoff_lane: IntegrationMeshReleaseDispatchTicketHandoffLane,
    pub review_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind,
    pub clearance_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceKind,
    pub release_blocking: bool,
    pub operator_required: bool,
    pub review_required: bool,
    pub dispatch_required: bool,
    pub execution_required: bool,
    pub lineage_complete: bool,
    pub action_required: bool,
    pub release_handoff_evidence: bool,
    pub release_ready_review: bool,
    pub evidence_review_readiness_ready: bool,
    pub cleared_for_handoff: bool,
}

impl IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceRow {
    pub fn from_action_evidence_review(
        sequence: usize,
        review: &IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReview,
        evidence_review_readiness_ready: bool,
    ) -> Self {
        let clearance_kind = if !review.has_lineage() {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceKind::LineageGap
        } else if review.blocks_release() {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceKind::RepairRequired
        } else if review.needs_review() {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceKind::ReviewRequired
        } else if review.needs_operator() {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceKind::OperatorHandoff
        } else if review.dispatch_required {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceKind::DispatchRequired
        } else if review.execution_required {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceKind::ExecutionRequired
        } else {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceKind::ReleaseHandoff
        };
        let release_ready_review = review.is_release_ready();
        let cleared_for_handoff = evidence_review_readiness_ready
            && release_ready_review
            && clearance_kind
                == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceKind::ReleaseHandoff;

        Self {
            sequence,
            slot_clearance_action_evidence_review_clearance_key: format!(
                "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-review-clearance-{sequence:02}-{}-{}",
                clearance_kind.as_str(),
                review.check_kind.as_str()
            ),
            slot_clearance_action_evidence_review_sequence: review.sequence,
            slot_clearance_action_evidence_review_key: review
                .slot_clearance_action_evidence_review_key
                .clone(),
            slot_clearance_action_evidence_key: review
                .slot_clearance_action_evidence_key
                .clone(),
            slot_clearance_action_key: review.slot_clearance_action_key.clone(),
            slot_clearance_key: review.slot_clearance_key.clone(),
            slot_key: review.slot_key.clone(),
            disposition_action_key: review.disposition_action_key.clone(),
            disposition_key: review.disposition_key.clone(),
            work_order_key: review.work_order_key.clone(),
            ticket_key: review.ticket_key.clone(),
            check_sequence: review.check_sequence,
            check_kind: review.check_kind,
            status: review.status,
            handoff_lane: review.handoff_lane,
            review_kind: review.review_kind,
            clearance_kind,
            release_blocking: review.blocks_release(),
            operator_required: review.needs_operator(),
            review_required: review.needs_review(),
            dispatch_required: review.dispatch_required,
            execution_required: review.execution_required,
            lineage_complete: review.has_lineage(),
            action_required: review.requires_action(),
            release_handoff_evidence: review.is_release_handoff_evidence(),
            release_ready_review,
            evidence_review_readiness_ready,
            cleared_for_handoff,
        }
    }

    pub fn requires_action(&self) -> bool {
        self.action_required
    }

    pub fn blocks_release(&self) -> bool {
        self.release_blocking
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required
    }

    pub fn needs_review(&self) -> bool {
        self.review_required
    }

    pub fn requires_dispatch(&self) -> bool {
        self.dispatch_required
    }

    pub fn requires_execution(&self) -> bool {
        self.execution_required
    }

    pub fn has_lineage(&self) -> bool {
        self.lineage_complete
    }

    pub fn is_release_handoff_clearance(&self) -> bool {
        self.clearance_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceKind::ReleaseHandoff
    }

    pub fn ready_to_release_handoff(&self) -> bool {
        self.cleared_for_handoff
    }

    pub fn requires_attention(&self) -> bool {
        self.requires_action()
            || self.blocks_release()
            || self.needs_operator()
            || self.needs_review()
            || self.requires_dispatch()
            || self.requires_execution()
            || !self.has_lineage()
            || !self.ready_to_release_handoff()
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceReadinessSummary {
    pub release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_readiness_summary:
        Box<IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewReadinessSummary>,
    pub total_slot_clearance_action_evidence_reviews: usize,
    pub total_slot_clearance_action_evidence_review_clearance_rows: usize,
    pub required_clearances: usize,
    pub release_blocking_clearances: usize,
    pub operator_required_clearances: usize,
    pub review_required_clearances: usize,
    pub dispatch_required_clearances: usize,
    pub execution_required_clearances: usize,
    pub repair_required_clearances: usize,
    pub operator_handoff_clearances: usize,
    pub review_gate_clearances: usize,
    pub lineage_gap_clearances: usize,
    pub release_handoff_clearances: usize,
    pub release_ready_clearances: usize,
    pub lineage_complete_clearances: usize,
    pub cleared_for_handoff_clearances: usize,
    pub first_slot_clearance_action_evidence_review_clearance_key: Option<String>,
    pub first_required_clearance_key: Option<String>,
    pub first_repair_clearance_key: Option<String>,
    pub first_operator_handoff_clearance_key: Option<String>,
    pub first_review_clearance_key: Option<String>,
    pub first_lineage_gap_clearance_key: Option<String>,
    pub first_release_handoff_clearance_key: Option<String>,
    pub first_slot_clearance_action_evidence_review_key: Option<String>,
    pub first_slot_clearance_action_evidence_key: Option<String>,
    pub first_slot_clearance_action_key: Option<String>,
    pub first_slot_clearance_key: Option<String>,
    pub first_review_disposition_action_slot_key: Option<String>,
    pub first_review_disposition_action_key: Option<String>,
    pub first_work_order_key: Option<String>,
    pub first_ticket_key: Option<String>,
    pub first_clearance_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceKind,
    >,
    pub first_action_evidence_review_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind,
    >,
    pub first_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub first_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub first_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_slot_clearance_action_evidence_review_clearance_key: Option<String>,
    pub next_slot_clearance_action_evidence_review_key: Option<String>,
    pub next_slot_clearance_action_evidence_key: Option<String>,
    pub next_slot_clearance_action_key: Option<String>,
    pub next_slot_clearance_key: Option<String>,
    pub next_review_disposition_action_slot_key: Option<String>,
    pub next_review_disposition_action_key: Option<String>,
    pub next_work_order_key: Option<String>,
    pub next_ticket_key: Option<String>,
    pub next_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub next_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_reviews_ready:
        bool,
    pub release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearances_ready:
        bool,
}

impl
    IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceReadinessSummary
{
    pub fn from_summaries<'a>(
        release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_readiness_summary: IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewReadinessSummary,
        clearance_rows: impl IntoIterator<
            Item = &'a IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceRow,
        >,
    ) -> Self {
        let clearance_rows = clearance_rows.into_iter().collect::<Vec<_>>();
        let first_clearance_row = clearance_rows.iter().min_by_key(|row| row.sequence);
        let first_required_clearance = clearance_rows
            .iter()
            .filter(|row| row.requires_attention())
            .min_by_key(|row| row.sequence);
        let first_repair_clearance = clearance_rows
            .iter()
            .filter(|row| row.clearance_kind == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceKind::RepairRequired)
            .min_by_key(|row| row.sequence);
        let first_operator_handoff_clearance = clearance_rows
            .iter()
            .filter(|row| row.clearance_kind == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceKind::OperatorHandoff)
            .min_by_key(|row| row.sequence);
        let first_review_clearance = clearance_rows
            .iter()
            .filter(|row| row.clearance_kind == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceKind::ReviewRequired)
            .min_by_key(|row| row.sequence);
        let first_lineage_gap_clearance = clearance_rows
            .iter()
            .filter(|row| row.clearance_kind == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceKind::LineageGap)
            .min_by_key(|row| row.sequence);
        let first_release_handoff_clearance = clearance_rows
            .iter()
            .filter(|row| row.clearance_kind == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceKind::ReleaseHandoff)
            .min_by_key(|row| row.sequence);
        let next_clearance = clearance_rows
            .iter()
            .filter(|row| row.requires_attention())
            .min_by_key(|row| row.sequence);
        let release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_reviews_ready =
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_readiness_summary
                .ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_reviews();
        let release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearances_ready =
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_reviews_ready
                && !clearance_rows.is_empty()
                && clearance_rows.iter().all(|row| row.ready_to_release_handoff())
                && clearance_rows.iter().all(|row| row.has_lineage());

        Self {
            total_slot_clearance_action_evidence_reviews:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_readiness_summary
                    .total_slot_clearance_action_evidence_reviews,
            total_slot_clearance_action_evidence_review_clearance_rows: clearance_rows.len(),
            required_clearances: clearance_rows
                .iter()
                .filter(|row| row.requires_attention())
                .count(),
            release_blocking_clearances: clearance_rows
                .iter()
                .filter(|row| row.blocks_release())
                .count(),
            operator_required_clearances: clearance_rows
                .iter()
                .filter(|row| row.needs_operator())
                .count(),
            review_required_clearances: clearance_rows
                .iter()
                .filter(|row| row.needs_review())
                .count(),
            dispatch_required_clearances: clearance_rows
                .iter()
                .filter(|row| row.requires_dispatch())
                .count(),
            execution_required_clearances: clearance_rows
                .iter()
                .filter(|row| row.requires_execution())
                .count(),
            repair_required_clearances: clearance_rows
                .iter()
                .filter(|row| row.clearance_kind == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceKind::RepairRequired)
                .count(),
            operator_handoff_clearances: clearance_rows
                .iter()
                .filter(|row| row.clearance_kind == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceKind::OperatorHandoff)
                .count(),
            review_gate_clearances: clearance_rows
                .iter()
                .filter(|row| row.clearance_kind == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceKind::ReviewRequired)
                .count(),
            lineage_gap_clearances: clearance_rows
                .iter()
                .filter(|row| row.clearance_kind == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceKind::LineageGap)
                .count(),
            release_handoff_clearances: clearance_rows
                .iter()
                .filter(|row| row.is_release_handoff_clearance())
                .count(),
            release_ready_clearances: clearance_rows
                .iter()
                .filter(|row| row.release_ready_review)
                .count(),
            lineage_complete_clearances: clearance_rows
                .iter()
                .filter(|row| row.has_lineage())
                .count(),
            cleared_for_handoff_clearances: clearance_rows
                .iter()
                .filter(|row| row.ready_to_release_handoff())
                .count(),
            first_slot_clearance_action_evidence_review_clearance_key: first_clearance_row
                .map(|row| row.slot_clearance_action_evidence_review_clearance_key.clone()),
            first_required_clearance_key: first_required_clearance
                .map(|row| row.slot_clearance_action_evidence_review_clearance_key.clone()),
            first_repair_clearance_key: first_repair_clearance
                .map(|row| row.slot_clearance_action_evidence_review_clearance_key.clone()),
            first_operator_handoff_clearance_key: first_operator_handoff_clearance
                .map(|row| row.slot_clearance_action_evidence_review_clearance_key.clone()),
            first_review_clearance_key: first_review_clearance
                .map(|row| row.slot_clearance_action_evidence_review_clearance_key.clone()),
            first_lineage_gap_clearance_key: first_lineage_gap_clearance
                .map(|row| row.slot_clearance_action_evidence_review_clearance_key.clone()),
            first_release_handoff_clearance_key: first_release_handoff_clearance
                .map(|row| row.slot_clearance_action_evidence_review_clearance_key.clone()),
            first_slot_clearance_action_evidence_review_key: first_clearance_row
                .map(|row| row.slot_clearance_action_evidence_review_key.clone()),
            first_slot_clearance_action_evidence_key: first_clearance_row
                .map(|row| row.slot_clearance_action_evidence_key.clone()),
            first_slot_clearance_action_key: first_clearance_row
                .map(|row| row.slot_clearance_action_key.clone()),
            first_slot_clearance_key: first_clearance_row.map(|row| row.slot_clearance_key.clone()),
            first_review_disposition_action_slot_key: first_clearance_row
                .map(|row| row.slot_key.clone()),
            first_review_disposition_action_key: first_clearance_row
                .map(|row| row.disposition_action_key.clone()),
            first_work_order_key: first_clearance_row.map(|row| row.work_order_key.clone()),
            first_ticket_key: first_clearance_row.map(|row| row.ticket_key.clone()),
            first_clearance_kind: first_clearance_row.map(|row| row.clearance_kind),
            first_action_evidence_review_kind: first_clearance_row.map(|row| row.review_kind),
            first_handoff_lane: first_clearance_row.map(|row| row.handoff_lane),
            first_check_kind: first_clearance_row.map(|row| row.check_kind),
            first_check_status: first_clearance_row.map(|row| row.status),
            next_slot_clearance_action_evidence_review_clearance_key: next_clearance
                .map(|row| row.slot_clearance_action_evidence_review_clearance_key.clone()),
            next_slot_clearance_action_evidence_review_key: next_clearance
                .map(|row| row.slot_clearance_action_evidence_review_key.clone())
                .or_else(|| {
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_readiness_summary
                        .next_slot_clearance_action_evidence_review_key
                        .clone()
                }),
            next_slot_clearance_action_evidence_key: next_clearance
                .map(|row| row.slot_clearance_action_evidence_key.clone())
                .or_else(|| {
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_readiness_summary
                        .next_slot_clearance_action_evidence_key
                        .clone()
                }),
            next_slot_clearance_action_key: next_clearance
                .map(|row| row.slot_clearance_action_key.clone())
                .or_else(|| {
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_readiness_summary
                        .next_slot_clearance_action_key
                        .clone()
                }),
            next_slot_clearance_key: next_clearance
                .map(|row| row.slot_clearance_key.clone())
                .or_else(|| {
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_readiness_summary
                        .next_slot_clearance_key
                        .clone()
                }),
            next_review_disposition_action_slot_key: next_clearance
                .map(|row| row.slot_key.clone())
                .or_else(|| {
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_readiness_summary
                        .next_review_disposition_action_slot_key
                        .clone()
                }),
            next_review_disposition_action_key: next_clearance
                .map(|row| row.disposition_action_key.clone())
                .or_else(|| {
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_readiness_summary
                        .next_review_disposition_action_key
                        .clone()
                }),
            next_work_order_key: next_clearance
                .map(|row| row.work_order_key.clone())
                .or_else(|| {
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_readiness_summary
                        .next_work_order_key
                        .clone()
                }),
            next_ticket_key: next_clearance
                .map(|row| row.ticket_key.clone())
                .or_else(|| {
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_readiness_summary
                        .next_ticket_key
                        .clone()
                }),
            next_check_kind: next_clearance.map(|row| row.check_kind).or(
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_readiness_summary
                    .next_check_kind,
            ),
            next_check_status: next_clearance.map(|row| row.status).or(
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_readiness_summary
                    .next_check_status,
            ),
            next_handoff_lane: next_clearance.map(|row| row.handoff_lane).or(
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_readiness_summary
                    .next_handoff_lane,
            ),
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_reviews_ready,
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearances_ready,
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_readiness_summary:
                Box::new(
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_readiness_summary,
                ),
        }
    }

    pub fn has_clearance_rows(&self) -> bool {
        self.total_slot_clearance_action_evidence_review_clearance_rows > 0
    }

    pub fn ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearances(
        &self,
    ) -> bool {
        self.release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearances_ready
            && !self.requires_attention()
    }

    pub fn has_required_clearances(&self) -> bool {
        self.required_clearances > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.release_blocking_clearances > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_readiness_summary
                .has_blockers()
    }

    pub fn has_review_work(&self) -> bool {
        self.review_required_clearances > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_readiness_summary
                .has_review_work()
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_clearances > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_readiness_summary
                .needs_operator()
    }

    pub fn has_lineage_gaps(&self) -> bool {
        self.lineage_gap_clearances > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_readiness_summary
                .has_lineage_gaps()
    }

    pub fn has_release_handoff_clearances(&self) -> bool {
        self.release_handoff_clearances > 0
    }

    pub fn has_complete_lineage(&self) -> bool {
        self.lineage_complete_clearances
            == self.total_slot_clearance_action_evidence_review_clearance_rows
            && self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_readiness_summary
                .has_complete_lineage()
    }

    pub fn requires_execution(&self) -> bool {
        self.execution_required_clearances > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_readiness_summary
                .requires_execution()
    }

    pub fn requires_attention(&self) -> bool {
        self.requires_execution()
            || self.dispatch_required_clearances > 0
            || self.has_required_clearances()
            || self.has_blockers()
            || self.has_review_work()
            || self.needs_operator()
            || self.has_lineage_gaps()
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_readiness_summary
                .requires_attention()
            || !self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearances_ready
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionKind
{
    ScheduleRepair,
    CoordinateOperatorHandoff,
    CompleteReview,
    DispatchClearance,
    ExecuteClearance,
    RestoreLineage,
    ReleaseHandoff,
}

impl
    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionKind
{
    pub fn as_str(self) -> &'static str {
        match self {
            Self::ScheduleRepair => "schedule_repair",
            Self::CoordinateOperatorHandoff => "coordinate_operator_handoff",
            Self::CompleteReview => "complete_review",
            Self::DispatchClearance => "dispatch_clearance",
            Self::ExecuteClearance => "execute_clearance",
            Self::RestoreLineage => "restore_lineage",
            Self::ReleaseHandoff => "release_handoff",
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionRow {
    pub sequence: usize,
    pub slot_clearance_action_evidence_review_clearance_action_key: String,
    pub slot_clearance_action_evidence_review_clearance_sequence: usize,
    pub slot_clearance_action_evidence_review_clearance_key: String,
    pub slot_clearance_action_evidence_review_key: String,
    pub slot_clearance_action_evidence_key: String,
    pub slot_clearance_action_key: String,
    pub slot_clearance_key: String,
    pub slot_key: String,
    pub disposition_action_key: String,
    pub disposition_key: String,
    pub work_order_key: String,
    pub ticket_key: String,
    pub check_sequence: usize,
    pub check_kind: IntegrationMeshReleaseReadinessCheckKind,
    pub status: IntegrationMeshReleaseReadinessStatus,
    pub handoff_lane: IntegrationMeshReleaseDispatchTicketHandoffLane,
    pub review_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind,
    pub clearance_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceKind,
    pub clearance_action_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionKind,
    pub release_blocking: bool,
    pub operator_required: bool,
    pub review_required: bool,
    pub dispatch_required: bool,
    pub execution_required: bool,
    pub lineage_complete: bool,
    pub action_required: bool,
    pub release_handoff_evidence: bool,
    pub release_ready_review: bool,
    pub evidence_review_clearance_readiness_ready: bool,
    pub ready_for_release_handoff: bool,
}

impl IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionRow {
    pub fn from_clearance_row(
        sequence: usize,
        clearance_row: &IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceRow,
        evidence_review_clearance_readiness_ready: bool,
    ) -> Self {
        let clearance_action_kind = if !clearance_row.has_lineage() {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionKind::RestoreLineage
        } else if clearance_row.blocks_release() {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionKind::ScheduleRepair
        } else if clearance_row.needs_review() {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionKind::CompleteReview
        } else if clearance_row.needs_operator() {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionKind::CoordinateOperatorHandoff
        } else if clearance_row.requires_dispatch() {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionKind::DispatchClearance
        } else if clearance_row.requires_execution() {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionKind::ExecuteClearance
        } else {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionKind::ReleaseHandoff
        };
        let ready_for_release_handoff = evidence_review_clearance_readiness_ready
            && clearance_row.ready_to_release_handoff()
            && clearance_action_kind
                == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionKind::ReleaseHandoff;

        Self {
            sequence,
            slot_clearance_action_evidence_review_clearance_action_key: format!(
                "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-review-clearance-action-{sequence:02}-{}-{}",
                clearance_action_kind.as_str(),
                clearance_row.check_kind.as_str()
            ),
            slot_clearance_action_evidence_review_clearance_sequence: clearance_row.sequence,
            slot_clearance_action_evidence_review_clearance_key: clearance_row
                .slot_clearance_action_evidence_review_clearance_key
                .clone(),
            slot_clearance_action_evidence_review_key: clearance_row
                .slot_clearance_action_evidence_review_key
                .clone(),
            slot_clearance_action_evidence_key: clearance_row
                .slot_clearance_action_evidence_key
                .clone(),
            slot_clearance_action_key: clearance_row.slot_clearance_action_key.clone(),
            slot_clearance_key: clearance_row.slot_clearance_key.clone(),
            slot_key: clearance_row.slot_key.clone(),
            disposition_action_key: clearance_row.disposition_action_key.clone(),
            disposition_key: clearance_row.disposition_key.clone(),
            work_order_key: clearance_row.work_order_key.clone(),
            ticket_key: clearance_row.ticket_key.clone(),
            check_sequence: clearance_row.check_sequence,
            check_kind: clearance_row.check_kind,
            status: clearance_row.status,
            handoff_lane: clearance_row.handoff_lane,
            review_kind: clearance_row.review_kind,
            clearance_kind: clearance_row.clearance_kind,
            clearance_action_kind,
            release_blocking: clearance_row.blocks_release(),
            operator_required: clearance_row.needs_operator(),
            review_required: clearance_row.needs_review(),
            dispatch_required: clearance_row.requires_dispatch(),
            execution_required: clearance_row.requires_execution(),
            lineage_complete: clearance_row.has_lineage(),
            action_required: clearance_row.requires_attention(),
            release_handoff_evidence: clearance_row.release_handoff_evidence,
            release_ready_review: clearance_row.release_ready_review,
            evidence_review_clearance_readiness_ready,
            ready_for_release_handoff,
        }
    }

    pub fn requires_action(&self) -> bool {
        self.action_required
    }

    pub fn blocks_release(&self) -> bool {
        self.release_blocking
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required
            || self.clearance_action_kind
                == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionKind::CoordinateOperatorHandoff
    }

    pub fn needs_review(&self) -> bool {
        self.review_required
            || self.clearance_action_kind
                == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionKind::CompleteReview
    }

    pub fn requires_dispatch(&self) -> bool {
        self.dispatch_required
    }

    pub fn requires_execution(&self) -> bool {
        self.execution_required
    }

    pub fn has_lineage(&self) -> bool {
        self.lineage_complete
    }

    pub fn is_repair_action(&self) -> bool {
        self.clearance_action_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionKind::ScheduleRepair
    }

    pub fn is_review_action(&self) -> bool {
        self.clearance_action_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionKind::CompleteReview
    }

    pub fn is_lineage_action(&self) -> bool {
        self.clearance_action_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionKind::RestoreLineage
    }

    pub fn is_release_handoff_action(&self) -> bool {
        self.clearance_action_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionKind::ReleaseHandoff
    }

    pub fn ready_to_release_handoff(&self) -> bool {
        self.ready_for_release_handoff
    }

    pub fn requires_attention(&self) -> bool {
        self.requires_action()
            || self.blocks_release()
            || self.needs_operator()
            || self.needs_review()
            || self.requires_dispatch()
            || self.requires_execution()
            || !self.has_lineage()
            || !self.ready_to_release_handoff()
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessSummary {
    pub release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_readiness_summary:
        Box<IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceReadinessSummary>,
    pub total_slot_clearance_action_evidence_review_clearance_rows: usize,
    pub total_slot_clearance_action_evidence_review_clearance_action_rows: usize,
    pub required_actions: usize,
    pub release_blocking_actions: usize,
    pub operator_required_actions: usize,
    pub review_required_actions: usize,
    pub dispatch_required_actions: usize,
    pub execution_required_actions: usize,
    pub repair_actions: usize,
    pub operator_handoff_actions: usize,
    pub review_actions: usize,
    pub dispatch_actions: usize,
    pub execution_actions: usize,
    pub lineage_actions: usize,
    pub release_handoff_actions: usize,
    pub lineage_complete_actions: usize,
    pub ready_actions: usize,
    pub first_slot_clearance_action_evidence_review_clearance_action_key: Option<String>,
    pub first_required_action_key: Option<String>,
    pub first_repair_action_key: Option<String>,
    pub first_operator_handoff_action_key: Option<String>,
    pub first_review_action_key: Option<String>,
    pub first_dispatch_action_key: Option<String>,
    pub first_execution_action_key: Option<String>,
    pub first_lineage_action_key: Option<String>,
    pub first_release_handoff_action_key: Option<String>,
    pub first_slot_clearance_action_evidence_review_clearance_key: Option<String>,
    pub first_slot_clearance_action_evidence_review_key: Option<String>,
    pub first_slot_clearance_action_evidence_key: Option<String>,
    pub first_slot_clearance_action_key: Option<String>,
    pub first_slot_clearance_key: Option<String>,
    pub first_review_disposition_action_slot_key: Option<String>,
    pub first_review_disposition_action_key: Option<String>,
    pub first_work_order_key: Option<String>,
    pub first_ticket_key: Option<String>,
    pub first_clearance_action_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionKind,
    >,
    pub first_clearance_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceKind,
    >,
    pub first_action_evidence_review_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind,
    >,
    pub first_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub first_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub first_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_slot_clearance_action_evidence_review_clearance_action_key: Option<String>,
    pub next_slot_clearance_action_evidence_review_clearance_key: Option<String>,
    pub next_slot_clearance_action_evidence_review_key: Option<String>,
    pub next_slot_clearance_action_evidence_key: Option<String>,
    pub next_slot_clearance_action_key: Option<String>,
    pub next_slot_clearance_key: Option<String>,
    pub next_review_disposition_action_slot_key: Option<String>,
    pub next_review_disposition_action_key: Option<String>,
    pub next_work_order_key: Option<String>,
    pub next_ticket_key: Option<String>,
    pub next_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub next_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearances_ready:
        bool,
    pub release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_actions_ready:
        bool,
}

impl
    IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessSummary
{
    pub fn from_summaries<'a>(
        release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_readiness_summary: IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceReadinessSummary,
        action_rows: impl IntoIterator<
            Item = &'a IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionRow,
        >,
    ) -> Self {
        let action_rows = action_rows.into_iter().collect::<Vec<_>>();
        let first_action_row = action_rows.iter().min_by_key(|row| row.sequence);
        let first_required_action = action_rows
            .iter()
            .filter(|row| row.requires_attention())
            .min_by_key(|row| row.sequence);
        let first_repair_action = action_rows
            .iter()
            .filter(|row| row.is_repair_action())
            .min_by_key(|row| row.sequence);
        let first_operator_handoff_action = action_rows
            .iter()
            .filter(|row| row.clearance_action_kind == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionKind::CoordinateOperatorHandoff)
            .min_by_key(|row| row.sequence);
        let first_review_action = action_rows
            .iter()
            .filter(|row| row.is_review_action())
            .min_by_key(|row| row.sequence);
        let first_dispatch_action = action_rows
            .iter()
            .filter(|row| row.clearance_action_kind == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionKind::DispatchClearance)
            .min_by_key(|row| row.sequence);
        let first_execution_action = action_rows
            .iter()
            .filter(|row| row.clearance_action_kind == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionKind::ExecuteClearance)
            .min_by_key(|row| row.sequence);
        let first_lineage_action = action_rows
            .iter()
            .filter(|row| row.is_lineage_action())
            .min_by_key(|row| row.sequence);
        let first_release_handoff_action = action_rows
            .iter()
            .filter(|row| row.is_release_handoff_action())
            .min_by_key(|row| row.sequence);
        let next_action = action_rows
            .iter()
            .filter(|row| row.requires_attention())
            .min_by_key(|row| row.sequence);
        let release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearances_ready =
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_readiness_summary
                .ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearances();
        let release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_actions_ready =
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearances_ready
                && !action_rows.is_empty()
                && action_rows.iter().all(|row| row.ready_to_release_handoff())
                && action_rows.iter().all(|row| row.has_lineage());

        Self {
            total_slot_clearance_action_evidence_review_clearance_rows:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_readiness_summary
                    .total_slot_clearance_action_evidence_review_clearance_rows,
            total_slot_clearance_action_evidence_review_clearance_action_rows: action_rows.len(),
            required_actions: action_rows
                .iter()
                .filter(|row| row.requires_attention())
                .count(),
            release_blocking_actions: action_rows
                .iter()
                .filter(|row| row.blocks_release())
                .count(),
            operator_required_actions: action_rows
                .iter()
                .filter(|row| row.needs_operator())
                .count(),
            review_required_actions: action_rows
                .iter()
                .filter(|row| row.needs_review())
                .count(),
            dispatch_required_actions: action_rows
                .iter()
                .filter(|row| row.requires_dispatch())
                .count(),
            execution_required_actions: action_rows
                .iter()
                .filter(|row| row.requires_execution())
                .count(),
            repair_actions: action_rows
                .iter()
                .filter(|row| row.is_repair_action())
                .count(),
            operator_handoff_actions: action_rows
                .iter()
                .filter(|row| row.clearance_action_kind == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionKind::CoordinateOperatorHandoff)
                .count(),
            review_actions: action_rows
                .iter()
                .filter(|row| row.is_review_action())
                .count(),
            dispatch_actions: action_rows
                .iter()
                .filter(|row| row.clearance_action_kind == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionKind::DispatchClearance)
                .count(),
            execution_actions: action_rows
                .iter()
                .filter(|row| row.clearance_action_kind == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionKind::ExecuteClearance)
                .count(),
            lineage_actions: action_rows
                .iter()
                .filter(|row| row.is_lineage_action())
                .count(),
            release_handoff_actions: action_rows
                .iter()
                .filter(|row| row.is_release_handoff_action())
                .count(),
            lineage_complete_actions: action_rows
                .iter()
                .filter(|row| row.has_lineage())
                .count(),
            ready_actions: action_rows
                .iter()
                .filter(|row| row.ready_to_release_handoff())
                .count(),
            first_slot_clearance_action_evidence_review_clearance_action_key: first_action_row
                .map(|row| row.slot_clearance_action_evidence_review_clearance_action_key.clone()),
            first_required_action_key: first_required_action
                .map(|row| row.slot_clearance_action_evidence_review_clearance_action_key.clone()),
            first_repair_action_key: first_repair_action
                .map(|row| row.slot_clearance_action_evidence_review_clearance_action_key.clone()),
            first_operator_handoff_action_key: first_operator_handoff_action
                .map(|row| row.slot_clearance_action_evidence_review_clearance_action_key.clone()),
            first_review_action_key: first_review_action
                .map(|row| row.slot_clearance_action_evidence_review_clearance_action_key.clone()),
            first_dispatch_action_key: first_dispatch_action
                .map(|row| row.slot_clearance_action_evidence_review_clearance_action_key.clone()),
            first_execution_action_key: first_execution_action
                .map(|row| row.slot_clearance_action_evidence_review_clearance_action_key.clone()),
            first_lineage_action_key: first_lineage_action
                .map(|row| row.slot_clearance_action_evidence_review_clearance_action_key.clone()),
            first_release_handoff_action_key: first_release_handoff_action
                .map(|row| row.slot_clearance_action_evidence_review_clearance_action_key.clone()),
            first_slot_clearance_action_evidence_review_clearance_key: first_action_row
                .map(|row| row.slot_clearance_action_evidence_review_clearance_key.clone()),
            first_slot_clearance_action_evidence_review_key: first_action_row
                .map(|row| row.slot_clearance_action_evidence_review_key.clone()),
            first_slot_clearance_action_evidence_key: first_action_row
                .map(|row| row.slot_clearance_action_evidence_key.clone()),
            first_slot_clearance_action_key: first_action_row
                .map(|row| row.slot_clearance_action_key.clone()),
            first_slot_clearance_key: first_action_row.map(|row| row.slot_clearance_key.clone()),
            first_review_disposition_action_slot_key: first_action_row
                .map(|row| row.slot_key.clone()),
            first_review_disposition_action_key: first_action_row
                .map(|row| row.disposition_action_key.clone()),
            first_work_order_key: first_action_row.map(|row| row.work_order_key.clone()),
            first_ticket_key: first_action_row.map(|row| row.ticket_key.clone()),
            first_clearance_action_kind: first_action_row.map(|row| row.clearance_action_kind),
            first_clearance_kind: first_action_row.map(|row| row.clearance_kind),
            first_action_evidence_review_kind: first_action_row.map(|row| row.review_kind),
            first_handoff_lane: first_action_row.map(|row| row.handoff_lane),
            first_check_kind: first_action_row.map(|row| row.check_kind),
            first_check_status: first_action_row.map(|row| row.status),
            next_slot_clearance_action_evidence_review_clearance_action_key: next_action
                .map(|row| row.slot_clearance_action_evidence_review_clearance_action_key.clone()),
            next_slot_clearance_action_evidence_review_clearance_key: next_action
                .map(|row| row.slot_clearance_action_evidence_review_clearance_key.clone())
                .or_else(|| {
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_readiness_summary
                        .next_slot_clearance_action_evidence_review_clearance_key
                        .clone()
                }),
            next_slot_clearance_action_evidence_review_key: next_action
                .map(|row| row.slot_clearance_action_evidence_review_key.clone())
                .or_else(|| {
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_readiness_summary
                        .next_slot_clearance_action_evidence_review_key
                        .clone()
                }),
            next_slot_clearance_action_evidence_key: next_action
                .map(|row| row.slot_clearance_action_evidence_key.clone())
                .or_else(|| {
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_readiness_summary
                        .next_slot_clearance_action_evidence_key
                        .clone()
                }),
            next_slot_clearance_action_key: next_action
                .map(|row| row.slot_clearance_action_key.clone())
                .or_else(|| {
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_readiness_summary
                        .next_slot_clearance_action_key
                        .clone()
                }),
            next_slot_clearance_key: next_action
                .map(|row| row.slot_clearance_key.clone())
                .or_else(|| {
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_readiness_summary
                        .next_slot_clearance_key
                        .clone()
                }),
            next_review_disposition_action_slot_key: next_action
                .map(|row| row.slot_key.clone())
                .or_else(|| {
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_readiness_summary
                        .next_review_disposition_action_slot_key
                        .clone()
                }),
            next_review_disposition_action_key: next_action
                .map(|row| row.disposition_action_key.clone())
                .or_else(|| {
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_readiness_summary
                        .next_review_disposition_action_key
                        .clone()
                }),
            next_work_order_key: next_action
                .map(|row| row.work_order_key.clone())
                .or_else(|| {
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_readiness_summary
                        .next_work_order_key
                        .clone()
                }),
            next_ticket_key: next_action
                .map(|row| row.ticket_key.clone())
                .or_else(|| {
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_readiness_summary
                        .next_ticket_key
                        .clone()
                }),
            next_check_kind: next_action.map(|row| row.check_kind).or(
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_readiness_summary
                    .next_check_kind,
            ),
            next_check_status: next_action.map(|row| row.status).or(
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_readiness_summary
                    .next_check_status,
            ),
            next_handoff_lane: next_action.map(|row| row.handoff_lane).or(
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_readiness_summary
                    .next_handoff_lane,
            ),
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearances_ready,
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_actions_ready,
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_readiness_summary:
                Box::new(
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_readiness_summary,
                ),
        }
    }

    pub fn has_clearance_action_rows(&self) -> bool {
        self.total_slot_clearance_action_evidence_review_clearance_action_rows > 0
    }

    pub fn ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_actions(
        &self,
    ) -> bool {
        self.release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_actions_ready
            && !self.requires_attention()
    }

    pub fn has_required_actions(&self) -> bool {
        self.required_actions > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.release_blocking_actions > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_readiness_summary
                .has_blockers()
    }

    pub fn has_review_work(&self) -> bool {
        self.review_required_actions > 0
            || self.review_actions > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_readiness_summary
                .has_review_work()
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_actions > 0
            || self.operator_handoff_actions > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_readiness_summary
                .needs_operator()
    }

    pub fn has_lineage_gaps(&self) -> bool {
        self.lineage_actions > 0
            || self.lineage_complete_actions
                < self.total_slot_clearance_action_evidence_review_clearance_action_rows
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_readiness_summary
                .has_lineage_gaps()
    }

    pub fn has_release_handoff_actions(&self) -> bool {
        self.release_handoff_actions > 0
    }

    pub fn has_complete_lineage(&self) -> bool {
        self.lineage_complete_actions
            == self.total_slot_clearance_action_evidence_review_clearance_action_rows
            && self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_readiness_summary
                .has_complete_lineage()
    }

    pub fn requires_execution(&self) -> bool {
        self.execution_required_actions > 0
            || self.execution_actions > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_readiness_summary
                .requires_execution()
    }

    pub fn requires_attention(&self) -> bool {
        self.requires_execution()
            || self.dispatch_required_actions > 0
            || self.dispatch_actions > 0
            || self.has_required_actions()
            || self.has_blockers()
            || self.has_review_work()
            || self.needs_operator()
            || self.has_lineage_gaps()
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_readiness_summary
                .requires_attention()
            || !self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_actions_ready
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceKind
{
    RepairEvidence,
    OperatorHandoffEvidence,
    ReviewEvidence,
    DispatchEvidence,
    ExecutionEvidence,
    LineageEvidence,
    ReleaseHandoffEvidence,
}

impl
    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceKind
{
    pub fn as_str(self) -> &'static str {
        match self {
            Self::RepairEvidence => "repair_evidence",
            Self::OperatorHandoffEvidence => "operator_handoff_evidence",
            Self::ReviewEvidence => "review_evidence",
            Self::DispatchEvidence => "dispatch_evidence",
            Self::ExecutionEvidence => "execution_evidence",
            Self::LineageEvidence => "lineage_evidence",
            Self::ReleaseHandoffEvidence => "release_handoff_evidence",
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceRow {
    pub sequence: usize,
    pub slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key: String,
    pub slot_clearance_action_evidence_review_clearance_action_sequence: usize,
    pub slot_clearance_action_evidence_review_clearance_action_key: String,
    pub slot_clearance_action_evidence_review_clearance_sequence: usize,
    pub slot_clearance_action_evidence_review_clearance_key: String,
    pub slot_clearance_action_evidence_review_key: String,
    pub slot_clearance_action_evidence_key: String,
    pub slot_clearance_action_key: String,
    pub slot_clearance_key: String,
    pub slot_key: String,
    pub disposition_action_key: String,
    pub disposition_key: String,
    pub work_order_key: String,
    pub ticket_key: String,
    pub check_sequence: usize,
    pub check_kind: IntegrationMeshReleaseReadinessCheckKind,
    pub status: IntegrationMeshReleaseReadinessStatus,
    pub handoff_lane: IntegrationMeshReleaseDispatchTicketHandoffLane,
    pub review_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind,
    pub clearance_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceKind,
    pub clearance_action_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionKind,
    pub clearance_action_readiness_evidence_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceKind,
    pub release_blocking: bool,
    pub operator_required: bool,
    pub review_required: bool,
    pub dispatch_required: bool,
    pub execution_required: bool,
    pub lineage_complete: bool,
    pub action_required: bool,
    pub release_handoff_evidence: bool,
    pub release_ready_review: bool,
    pub clearance_action_readiness_ready: bool,
    pub ready_for_release_handoff: bool,
}

impl
    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceRow
{
    pub fn from_clearance_action_row(
        sequence: usize,
        action_row: &IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionRow,
        clearance_action_readiness_ready: bool,
    ) -> Self {
        let clearance_action_readiness_evidence_kind = match action_row.clearance_action_kind {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionKind::ScheduleRepair => {
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceKind::RepairEvidence
            }
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionKind::CoordinateOperatorHandoff => {
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceKind::OperatorHandoffEvidence
            }
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionKind::CompleteReview => {
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceKind::ReviewEvidence
            }
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionKind::DispatchClearance => {
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceKind::DispatchEvidence
            }
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionKind::ExecuteClearance => {
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceKind::ExecutionEvidence
            }
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionKind::RestoreLineage => {
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceKind::LineageEvidence
            }
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionKind::ReleaseHandoff => {
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceKind::ReleaseHandoffEvidence
            }
        };
        let lineage_complete = action_row.has_lineage()
            && action_row.check_sequence > 0
            && !action_row.slot_clearance_action_evidence_review_clearance_action_key.is_empty()
            && !action_row
                .slot_clearance_action_evidence_review_clearance_key
                .is_empty()
            && !action_row.slot_clearance_action_evidence_review_key.is_empty()
            && !action_row.slot_clearance_action_evidence_key.is_empty()
            && !action_row.slot_clearance_action_key.is_empty()
            && !action_row.slot_clearance_key.is_empty()
            && !action_row.slot_key.is_empty()
            && !action_row.disposition_action_key.is_empty()
            && !action_row.disposition_key.is_empty()
            && !action_row.work_order_key.is_empty()
            && !action_row.ticket_key.is_empty();
        let release_handoff_evidence = clearance_action_readiness_evidence_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceKind::ReleaseHandoffEvidence;
        let ready_for_release_handoff = clearance_action_readiness_ready
            && release_handoff_evidence
            && action_row.ready_to_release_handoff()
            && lineage_complete;

        Self {
            sequence,
            slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key: format!(
                "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-review-clearance-action-readiness-evidence-{sequence:02}-{}-{}",
                clearance_action_readiness_evidence_kind.as_str(),
                action_row.check_kind.as_str()
            ),
            slot_clearance_action_evidence_review_clearance_action_sequence: action_row.sequence,
            slot_clearance_action_evidence_review_clearance_action_key: action_row
                .slot_clearance_action_evidence_review_clearance_action_key
                .clone(),
            slot_clearance_action_evidence_review_clearance_sequence: action_row
                .slot_clearance_action_evidence_review_clearance_sequence,
            slot_clearance_action_evidence_review_clearance_key: action_row
                .slot_clearance_action_evidence_review_clearance_key
                .clone(),
            slot_clearance_action_evidence_review_key: action_row
                .slot_clearance_action_evidence_review_key
                .clone(),
            slot_clearance_action_evidence_key: action_row
                .slot_clearance_action_evidence_key
                .clone(),
            slot_clearance_action_key: action_row.slot_clearance_action_key.clone(),
            slot_clearance_key: action_row.slot_clearance_key.clone(),
            slot_key: action_row.slot_key.clone(),
            disposition_action_key: action_row.disposition_action_key.clone(),
            disposition_key: action_row.disposition_key.clone(),
            work_order_key: action_row.work_order_key.clone(),
            ticket_key: action_row.ticket_key.clone(),
            check_sequence: action_row.check_sequence,
            check_kind: action_row.check_kind,
            status: action_row.status,
            handoff_lane: action_row.handoff_lane,
            review_kind: action_row.review_kind,
            clearance_kind: action_row.clearance_kind,
            clearance_action_kind: action_row.clearance_action_kind,
            clearance_action_readiness_evidence_kind,
            release_blocking: action_row.blocks_release(),
            operator_required: action_row.needs_operator(),
            review_required: action_row.needs_review(),
            dispatch_required: action_row.requires_dispatch(),
            execution_required: action_row.requires_execution(),
            lineage_complete,
            action_required: action_row.requires_action(),
            release_handoff_evidence,
            release_ready_review: action_row.release_ready_review,
            clearance_action_readiness_ready,
            ready_for_release_handoff,
        }
    }

    pub fn requires_action(&self) -> bool {
        self.action_required
    }

    pub fn blocks_release(&self) -> bool {
        self.release_blocking
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required
    }

    pub fn needs_review(&self) -> bool {
        self.review_required
    }

    pub fn requires_dispatch(&self) -> bool {
        self.dispatch_required
    }

    pub fn requires_execution(&self) -> bool {
        self.execution_required
    }

    pub fn has_lineage(&self) -> bool {
        self.lineage_complete
    }

    pub fn is_repair_evidence(&self) -> bool {
        self.clearance_action_readiness_evidence_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceKind::RepairEvidence
    }

    pub fn is_operator_handoff_evidence(&self) -> bool {
        self.clearance_action_readiness_evidence_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceKind::OperatorHandoffEvidence
    }

    pub fn is_review_evidence(&self) -> bool {
        self.clearance_action_readiness_evidence_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceKind::ReviewEvidence
    }

    pub fn is_dispatch_evidence(&self) -> bool {
        self.clearance_action_readiness_evidence_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceKind::DispatchEvidence
    }

    pub fn is_execution_evidence(&self) -> bool {
        self.clearance_action_readiness_evidence_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceKind::ExecutionEvidence
    }

    pub fn is_lineage_evidence(&self) -> bool {
        self.clearance_action_readiness_evidence_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceKind::LineageEvidence
    }

    pub fn is_release_handoff_evidence(&self) -> bool {
        self.release_handoff_evidence
    }

    pub fn ready_to_release_handoff(&self) -> bool {
        self.ready_for_release_handoff
    }

    pub fn requires_attention(&self) -> bool {
        self.requires_action()
            || self.blocks_release()
            || self.needs_operator()
            || self.needs_review()
            || self.requires_dispatch()
            || self.requires_execution()
            || !self.has_lineage()
            || !self.ready_to_release_handoff()
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceSummary {
    pub release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_summary:
        Box<IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessSummary>,
    pub total_slot_clearance_action_evidence_review_clearance_action_rows: usize,
    pub total_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_rows:
        usize,
    pub required_evidence: usize,
    pub release_blocking_evidence: usize,
    pub operator_required_evidence: usize,
    pub review_required_evidence: usize,
    pub dispatch_required_evidence: usize,
    pub execution_required_evidence: usize,
    pub repair_evidence: usize,
    pub operator_handoff_evidence: usize,
    pub review_evidence: usize,
    pub dispatch_evidence: usize,
    pub execution_evidence: usize,
    pub lineage_evidence: usize,
    pub release_handoff_evidence: usize,
    pub lineage_complete_evidence: usize,
    pub ready_evidence: usize,
    pub first_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key:
        Option<String>,
    pub first_required_evidence_key: Option<String>,
    pub first_repair_evidence_key: Option<String>,
    pub first_operator_handoff_evidence_key: Option<String>,
    pub first_review_evidence_key: Option<String>,
    pub first_dispatch_evidence_key: Option<String>,
    pub first_execution_evidence_key: Option<String>,
    pub first_lineage_evidence_key: Option<String>,
    pub first_release_handoff_evidence_key: Option<String>,
    pub first_slot_clearance_action_evidence_review_clearance_action_key: Option<String>,
    pub first_slot_clearance_action_evidence_review_clearance_key: Option<String>,
    pub first_slot_clearance_action_evidence_review_key: Option<String>,
    pub first_slot_clearance_action_evidence_key: Option<String>,
    pub first_slot_clearance_action_key: Option<String>,
    pub first_slot_clearance_key: Option<String>,
    pub first_review_disposition_action_slot_key: Option<String>,
    pub first_review_disposition_action_key: Option<String>,
    pub first_work_order_key: Option<String>,
    pub first_ticket_key: Option<String>,
    pub first_clearance_action_readiness_evidence_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceKind,
    >,
    pub first_clearance_action_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionKind,
    >,
    pub first_clearance_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceKind,
    >,
    pub first_action_evidence_review_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind,
    >,
    pub first_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub first_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub first_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key:
        Option<String>,
    pub next_slot_clearance_action_evidence_review_clearance_action_key: Option<String>,
    pub next_slot_clearance_action_evidence_review_clearance_key: Option<String>,
    pub next_slot_clearance_action_evidence_review_key: Option<String>,
    pub next_slot_clearance_action_evidence_key: Option<String>,
    pub next_slot_clearance_action_key: Option<String>,
    pub next_slot_clearance_key: Option<String>,
    pub next_review_disposition_action_slot_key: Option<String>,
    pub next_review_disposition_action_key: Option<String>,
    pub next_work_order_key: Option<String>,
    pub next_ticket_key: Option<String>,
    pub next_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub next_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_actions_ready:
        bool,
    pub release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_ready:
        bool,
}

impl
    IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceSummary
{
    pub fn from_summaries<'a>(
        release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_summary: IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessSummary,
        evidence_rows: impl IntoIterator<
            Item = &'a IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceRow,
        >,
    ) -> Self {
        let evidence_rows = evidence_rows.into_iter().collect::<Vec<_>>();
        let first_evidence_row = evidence_rows.iter().min_by_key(|row| row.sequence);
        let first_required_evidence = evidence_rows
            .iter()
            .filter(|row| row.requires_attention())
            .min_by_key(|row| row.sequence);
        let first_repair_evidence = evidence_rows
            .iter()
            .filter(|row| row.is_repair_evidence())
            .min_by_key(|row| row.sequence);
        let first_operator_handoff_evidence = evidence_rows
            .iter()
            .filter(|row| row.is_operator_handoff_evidence())
            .min_by_key(|row| row.sequence);
        let first_review_evidence = evidence_rows
            .iter()
            .filter(|row| row.is_review_evidence())
            .min_by_key(|row| row.sequence);
        let first_dispatch_evidence = evidence_rows
            .iter()
            .filter(|row| row.is_dispatch_evidence())
            .min_by_key(|row| row.sequence);
        let first_execution_evidence = evidence_rows
            .iter()
            .filter(|row| row.is_execution_evidence())
            .min_by_key(|row| row.sequence);
        let first_lineage_evidence = evidence_rows
            .iter()
            .filter(|row| row.is_lineage_evidence())
            .min_by_key(|row| row.sequence);
        let first_release_handoff_evidence = evidence_rows
            .iter()
            .filter(|row| row.is_release_handoff_evidence())
            .min_by_key(|row| row.sequence);
        let next_evidence = evidence_rows
            .iter()
            .filter(|row| row.requires_attention())
            .min_by_key(|row| row.sequence);
        let release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_actions_ready =
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_summary
                .ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_actions();
        let release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_ready =
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_actions_ready
                && !evidence_rows.is_empty()
                && evidence_rows.iter().all(|row| row.ready_to_release_handoff())
                && evidence_rows.iter().all(|row| row.has_lineage());

        Self {
            total_slot_clearance_action_evidence_review_clearance_action_rows:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_summary
                    .total_slot_clearance_action_evidence_review_clearance_action_rows,
            total_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_rows:
                evidence_rows.len(),
            required_evidence: evidence_rows
                .iter()
                .filter(|row| row.requires_attention())
                .count(),
            release_blocking_evidence: evidence_rows
                .iter()
                .filter(|row| row.blocks_release())
                .count(),
            operator_required_evidence: evidence_rows
                .iter()
                .filter(|row| row.needs_operator())
                .count(),
            review_required_evidence: evidence_rows
                .iter()
                .filter(|row| row.needs_review())
                .count(),
            dispatch_required_evidence: evidence_rows
                .iter()
                .filter(|row| row.requires_dispatch())
                .count(),
            execution_required_evidence: evidence_rows
                .iter()
                .filter(|row| row.requires_execution())
                .count(),
            repair_evidence: evidence_rows
                .iter()
                .filter(|row| row.is_repair_evidence())
                .count(),
            operator_handoff_evidence: evidence_rows
                .iter()
                .filter(|row| row.is_operator_handoff_evidence())
                .count(),
            review_evidence: evidence_rows
                .iter()
                .filter(|row| row.is_review_evidence())
                .count(),
            dispatch_evidence: evidence_rows
                .iter()
                .filter(|row| row.is_dispatch_evidence())
                .count(),
            execution_evidence: evidence_rows
                .iter()
                .filter(|row| row.is_execution_evidence())
                .count(),
            lineage_evidence: evidence_rows
                .iter()
                .filter(|row| row.is_lineage_evidence())
                .count(),
            release_handoff_evidence: evidence_rows
                .iter()
                .filter(|row| row.is_release_handoff_evidence())
                .count(),
            lineage_complete_evidence: evidence_rows
                .iter()
                .filter(|row| row.has_lineage())
                .count(),
            ready_evidence: evidence_rows
                .iter()
                .filter(|row| row.ready_to_release_handoff())
                .count(),
            first_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key:
                first_evidence_row.map(|row| {
                    row.slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key
                        .clone()
                }),
            first_required_evidence_key: first_required_evidence.map(|row| {
                row.slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key
                    .clone()
            }),
            first_repair_evidence_key: first_repair_evidence.map(|row| {
                row.slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key
                    .clone()
            }),
            first_operator_handoff_evidence_key: first_operator_handoff_evidence.map(|row| {
                row.slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key
                    .clone()
            }),
            first_review_evidence_key: first_review_evidence.map(|row| {
                row.slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key
                    .clone()
            }),
            first_dispatch_evidence_key: first_dispatch_evidence.map(|row| {
                row.slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key
                    .clone()
            }),
            first_execution_evidence_key: first_execution_evidence.map(|row| {
                row.slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key
                    .clone()
            }),
            first_lineage_evidence_key: first_lineage_evidence.map(|row| {
                row.slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key
                    .clone()
            }),
            first_release_handoff_evidence_key: first_release_handoff_evidence.map(|row| {
                row.slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key
                    .clone()
            }),
            first_slot_clearance_action_evidence_review_clearance_action_key: first_evidence_row
                .map(|row| row.slot_clearance_action_evidence_review_clearance_action_key.clone()),
            first_slot_clearance_action_evidence_review_clearance_key: first_evidence_row
                .map(|row| row.slot_clearance_action_evidence_review_clearance_key.clone()),
            first_slot_clearance_action_evidence_review_key: first_evidence_row
                .map(|row| row.slot_clearance_action_evidence_review_key.clone()),
            first_slot_clearance_action_evidence_key: first_evidence_row
                .map(|row| row.slot_clearance_action_evidence_key.clone()),
            first_slot_clearance_action_key: first_evidence_row
                .map(|row| row.slot_clearance_action_key.clone()),
            first_slot_clearance_key: first_evidence_row
                .map(|row| row.slot_clearance_key.clone()),
            first_review_disposition_action_slot_key: first_evidence_row
                .map(|row| row.slot_key.clone()),
            first_review_disposition_action_key: first_evidence_row
                .map(|row| row.disposition_action_key.clone()),
            first_work_order_key: first_evidence_row.map(|row| row.work_order_key.clone()),
            first_ticket_key: first_evidence_row.map(|row| row.ticket_key.clone()),
            first_clearance_action_readiness_evidence_kind: first_evidence_row
                .map(|row| row.clearance_action_readiness_evidence_kind),
            first_clearance_action_kind: first_evidence_row.map(|row| row.clearance_action_kind),
            first_clearance_kind: first_evidence_row.map(|row| row.clearance_kind),
            first_action_evidence_review_kind: first_evidence_row.map(|row| row.review_kind),
            first_handoff_lane: first_evidence_row.map(|row| row.handoff_lane),
            first_check_kind: first_evidence_row.map(|row| row.check_kind),
            first_check_status: first_evidence_row.map(|row| row.status),
            next_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key:
                next_evidence.map(|row| {
                    row.slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key
                        .clone()
                }),
            next_slot_clearance_action_evidence_review_clearance_action_key: next_evidence
                .map(|row| row.slot_clearance_action_evidence_review_clearance_action_key.clone())
                .or_else(|| {
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_summary
                        .next_slot_clearance_action_evidence_review_clearance_action_key
                        .clone()
                }),
            next_slot_clearance_action_evidence_review_clearance_key: next_evidence
                .map(|row| row.slot_clearance_action_evidence_review_clearance_key.clone())
                .or_else(|| {
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_summary
                        .next_slot_clearance_action_evidence_review_clearance_key
                        .clone()
                }),
            next_slot_clearance_action_evidence_review_key: next_evidence
                .map(|row| row.slot_clearance_action_evidence_review_key.clone())
                .or_else(|| {
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_summary
                        .next_slot_clearance_action_evidence_review_key
                        .clone()
                }),
            next_slot_clearance_action_evidence_key: next_evidence
                .map(|row| row.slot_clearance_action_evidence_key.clone())
                .or_else(|| {
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_summary
                        .next_slot_clearance_action_evidence_key
                        .clone()
                }),
            next_slot_clearance_action_key: next_evidence
                .map(|row| row.slot_clearance_action_key.clone())
                .or_else(|| {
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_summary
                        .next_slot_clearance_action_key
                        .clone()
                }),
            next_slot_clearance_key: next_evidence
                .map(|row| row.slot_clearance_key.clone())
                .or_else(|| {
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_summary
                        .next_slot_clearance_key
                        .clone()
                }),
            next_review_disposition_action_slot_key: next_evidence
                .map(|row| row.slot_key.clone())
                .or_else(|| {
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_summary
                        .next_review_disposition_action_slot_key
                        .clone()
                }),
            next_review_disposition_action_key: next_evidence
                .map(|row| row.disposition_action_key.clone())
                .or_else(|| {
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_summary
                        .next_review_disposition_action_key
                        .clone()
                }),
            next_work_order_key: next_evidence
                .map(|row| row.work_order_key.clone())
                .or_else(|| {
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_summary
                        .next_work_order_key
                        .clone()
                }),
            next_ticket_key: next_evidence
                .map(|row| row.ticket_key.clone())
                .or_else(|| {
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_summary
                        .next_ticket_key
                        .clone()
                }),
            next_check_kind: next_evidence.map(|row| row.check_kind).or(
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_summary
                    .next_check_kind,
            ),
            next_check_status: next_evidence.map(|row| row.status).or(
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_summary
                    .next_check_status,
            ),
            next_handoff_lane: next_evidence.map(|row| row.handoff_lane).or(
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_summary
                    .next_handoff_lane,
            ),
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_actions_ready,
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_ready,
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_summary:
                Box::new(
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_summary,
                ),
        }
    }

    pub fn has_evidence_rows(&self) -> bool {
        self.total_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_rows
            > 0
    }

    pub fn ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence(
        &self,
    ) -> bool {
        self.release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_ready
            && !self.requires_attention()
    }

    pub fn has_required_evidence(&self) -> bool {
        self.required_evidence > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.release_blocking_evidence > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_summary
                .has_blockers()
    }

    pub fn has_review_work(&self) -> bool {
        self.review_required_evidence > 0
            || self.review_evidence > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_summary
                .has_review_work()
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_evidence > 0
            || self.operator_handoff_evidence > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_summary
                .needs_operator()
    }

    pub fn requires_dispatch(&self) -> bool {
        self.dispatch_required_evidence > 0 || self.dispatch_evidence > 0
    }

    pub fn requires_execution(&self) -> bool {
        self.execution_required_evidence > 0
            || self.execution_evidence > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_summary
                .requires_execution()
    }

    pub fn has_lineage_gaps(&self) -> bool {
        self.lineage_evidence > 0
            || self.lineage_complete_evidence
                < self
                    .total_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_rows
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_summary
                .has_lineage_gaps()
    }

    pub fn has_release_handoff_evidence(&self) -> bool {
        self.release_handoff_evidence > 0
    }

    pub fn has_complete_lineage(&self) -> bool {
        self.lineage_complete_evidence
            == self
                .total_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_rows
            && self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_summary
                .has_complete_lineage()
    }

    pub fn requires_attention(&self) -> bool {
        self.requires_dispatch()
            || self.requires_execution()
            || self.has_required_evidence()
            || self.has_blockers()
            || self.has_review_work()
            || self.needs_operator()
            || self.has_lineage_gaps()
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_summary
                .requires_attention()
            || !self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_ready
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewRow {
    pub sequence: usize,
    pub slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_key:
        String,
    pub slot_clearance_action_evidence_review_clearance_action_readiness_evidence_sequence:
        usize,
    pub slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key: String,
    pub slot_clearance_action_evidence_review_clearance_action_sequence: usize,
    pub slot_clearance_action_evidence_review_clearance_action_key: String,
    pub slot_clearance_action_evidence_review_clearance_sequence: usize,
    pub slot_clearance_action_evidence_review_clearance_key: String,
    pub slot_clearance_action_evidence_review_key: String,
    pub slot_clearance_action_evidence_key: String,
    pub slot_clearance_action_key: String,
    pub slot_clearance_key: String,
    pub slot_key: String,
    pub disposition_action_key: String,
    pub disposition_key: String,
    pub work_order_key: String,
    pub ticket_key: String,
    pub check_sequence: usize,
    pub check_kind: IntegrationMeshReleaseReadinessCheckKind,
    pub status: IntegrationMeshReleaseReadinessStatus,
    pub handoff_lane: IntegrationMeshReleaseDispatchTicketHandoffLane,
    pub action_evidence_review_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind,
    pub clearance_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceKind,
    pub clearance_action_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionKind,
    pub clearance_action_readiness_evidence_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceKind,
    pub readiness_evidence_review_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind,
    pub release_blocking: bool,
    pub operator_required: bool,
    pub review_required: bool,
    pub dispatch_required: bool,
    pub execution_required: bool,
    pub lineage_complete: bool,
    pub evidence_required: bool,
    pub release_handoff_evidence: bool,
    pub release_ready_review: bool,
    pub clearance_action_readiness_ready: bool,
    pub readiness_evidence_ready: bool,
    pub ready_for_release_handoff: bool,
}

impl
    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewRow
{
    pub fn from_readiness_evidence_row(
        sequence: usize,
        evidence_row: &IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceRow,
        readiness_evidence_ready: bool,
    ) -> Self {
        let readiness_evidence_review_kind = if !evidence_row.has_lineage() {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::LineageGap
        } else if evidence_row.blocks_release() {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::BlockerReview
        } else if evidence_row.needs_review() {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::ReviewGate
        } else if evidence_row.needs_operator() {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::OperatorReview
        } else {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::ReleaseReady
        };
        let release_ready_review = readiness_evidence_ready
            && evidence_row.ready_to_release_handoff()
            && evidence_row.has_lineage()
            && !evidence_row.blocks_release()
            && !evidence_row.needs_operator()
            && !evidence_row.needs_review()
            && !evidence_row.requires_dispatch()
            && !evidence_row.requires_execution()
            && readiness_evidence_review_kind
                == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::ReleaseReady;

        Self {
            sequence,
            slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_key:
                format!(
                    "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-review-clearance-action-readiness-evidence-review-{sequence:02}-{}-{}",
                    readiness_evidence_review_kind.as_str(),
                    evidence_row.check_kind.as_str()
                ),
            slot_clearance_action_evidence_review_clearance_action_readiness_evidence_sequence:
                evidence_row.sequence,
            slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key:
                evidence_row
                    .slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key
                    .clone(),
            slot_clearance_action_evidence_review_clearance_action_sequence: evidence_row
                .slot_clearance_action_evidence_review_clearance_action_sequence,
            slot_clearance_action_evidence_review_clearance_action_key: evidence_row
                .slot_clearance_action_evidence_review_clearance_action_key
                .clone(),
            slot_clearance_action_evidence_review_clearance_sequence: evidence_row
                .slot_clearance_action_evidence_review_clearance_sequence,
            slot_clearance_action_evidence_review_clearance_key: evidence_row
                .slot_clearance_action_evidence_review_clearance_key
                .clone(),
            slot_clearance_action_evidence_review_key: evidence_row
                .slot_clearance_action_evidence_review_key
                .clone(),
            slot_clearance_action_evidence_key: evidence_row
                .slot_clearance_action_evidence_key
                .clone(),
            slot_clearance_action_key: evidence_row.slot_clearance_action_key.clone(),
            slot_clearance_key: evidence_row.slot_clearance_key.clone(),
            slot_key: evidence_row.slot_key.clone(),
            disposition_action_key: evidence_row.disposition_action_key.clone(),
            disposition_key: evidence_row.disposition_key.clone(),
            work_order_key: evidence_row.work_order_key.clone(),
            ticket_key: evidence_row.ticket_key.clone(),
            check_sequence: evidence_row.check_sequence,
            check_kind: evidence_row.check_kind,
            status: evidence_row.status,
            handoff_lane: evidence_row.handoff_lane,
            action_evidence_review_kind: evidence_row.review_kind,
            clearance_kind: evidence_row.clearance_kind,
            clearance_action_kind: evidence_row.clearance_action_kind,
            clearance_action_readiness_evidence_kind: evidence_row
                .clearance_action_readiness_evidence_kind,
            readiness_evidence_review_kind,
            release_blocking: evidence_row.blocks_release(),
            operator_required: evidence_row.needs_operator(),
            review_required: evidence_row.needs_review(),
            dispatch_required: evidence_row.requires_dispatch(),
            execution_required: evidence_row.requires_execution(),
            lineage_complete: evidence_row.has_lineage(),
            evidence_required: evidence_row.requires_attention(),
            release_handoff_evidence: evidence_row.is_release_handoff_evidence(),
            release_ready_review,
            clearance_action_readiness_ready: evidence_row.clearance_action_readiness_ready,
            readiness_evidence_ready,
            ready_for_release_handoff: release_ready_review,
        }
    }

    pub fn requires_evidence_review(&self) -> bool {
        self.evidence_required
    }

    pub fn blocks_release(&self) -> bool {
        self.release_blocking
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required
    }

    pub fn needs_review(&self) -> bool {
        self.review_required
    }

    pub fn has_lineage(&self) -> bool {
        self.lineage_complete
    }

    pub fn is_blocker_review(&self) -> bool {
        self.readiness_evidence_review_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::BlockerReview
    }

    pub fn is_operator_review(&self) -> bool {
        self.readiness_evidence_review_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::OperatorReview
    }

    pub fn is_review_gate(&self) -> bool {
        self.readiness_evidence_review_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::ReviewGate
    }

    pub fn is_lineage_gap(&self) -> bool {
        self.readiness_evidence_review_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::LineageGap
    }

    pub fn is_release_ready_review(&self) -> bool {
        self.release_ready_review
    }

    pub fn is_release_handoff_evidence(&self) -> bool {
        self.release_handoff_evidence
    }

    pub fn ready_to_release_handoff(&self) -> bool {
        self.ready_for_release_handoff
    }

    pub fn requires_attention(&self) -> bool {
        self.requires_evidence_review()
            || self.blocks_release()
            || self.needs_operator()
            || self.needs_review()
            || self.dispatch_required
            || self.execution_required
            || !self.has_lineage()
            || !self.is_release_ready_review()
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewSummary {
    pub release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_summary:
        Box<IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceSummary>,
    pub total_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_rows:
        usize,
    pub total_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_rows:
        usize,
    pub required_reviews: usize,
    pub blocker_reviews: usize,
    pub operator_reviews: usize,
    pub review_gate_reviews: usize,
    pub lineage_gap_reviews: usize,
    pub release_ready_reviews: usize,
    pub release_handoff_evidence_reviews: usize,
    pub lineage_complete_reviews: usize,
    pub ready_reviews: usize,
    pub first_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_key:
        Option<String>,
    pub first_required_review_key: Option<String>,
    pub first_blocker_review_key: Option<String>,
    pub first_operator_review_key: Option<String>,
    pub first_review_gate_key: Option<String>,
    pub first_lineage_gap_review_key: Option<String>,
    pub first_release_ready_review_key: Option<String>,
    pub first_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key:
        Option<String>,
    pub first_slot_clearance_action_evidence_review_clearance_action_key: Option<String>,
    pub first_slot_clearance_action_evidence_review_clearance_key: Option<String>,
    pub first_slot_clearance_action_evidence_review_key: Option<String>,
    pub first_slot_clearance_action_evidence_key: Option<String>,
    pub first_slot_clearance_action_key: Option<String>,
    pub first_slot_clearance_key: Option<String>,
    pub first_review_disposition_action_slot_key: Option<String>,
    pub first_review_disposition_action_key: Option<String>,
    pub first_work_order_key: Option<String>,
    pub first_ticket_key: Option<String>,
    pub first_readiness_evidence_review_kind:
        Option<IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind>,
    pub first_clearance_action_readiness_evidence_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceKind,
    >,
    pub first_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub first_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub first_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_key:
        Option<String>,
    pub next_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key:
        Option<String>,
    pub next_slot_clearance_action_evidence_review_clearance_action_key: Option<String>,
    pub next_slot_clearance_action_evidence_review_clearance_key: Option<String>,
    pub next_slot_clearance_action_evidence_review_key: Option<String>,
    pub next_slot_clearance_action_evidence_key: Option<String>,
    pub next_slot_clearance_action_key: Option<String>,
    pub next_slot_clearance_key: Option<String>,
    pub next_review_disposition_action_slot_key: Option<String>,
    pub next_review_disposition_action_key: Option<String>,
    pub next_work_order_key: Option<String>,
    pub next_ticket_key: Option<String>,
    pub next_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub next_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_ready:
        bool,
    pub release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_reviews_ready:
        bool,
}

impl
    IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewSummary
{
    pub fn from_summaries<'a>(
        release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_summary: IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceSummary,
        review_rows: impl IntoIterator<
            Item = &'a IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewRow,
        >,
    ) -> Self {
        let review_rows = review_rows.into_iter().collect::<Vec<_>>();
        let first_review_row = review_rows.iter().min_by_key(|row| row.sequence);
        let first_required_review = review_rows
            .iter()
            .filter(|row| row.requires_attention())
            .min_by_key(|row| row.sequence);
        let first_blocker_review = review_rows
            .iter()
            .filter(|row| row.is_blocker_review())
            .min_by_key(|row| row.sequence);
        let first_operator_review = review_rows
            .iter()
            .filter(|row| row.is_operator_review())
            .min_by_key(|row| row.sequence);
        let first_review_gate = review_rows
            .iter()
            .filter(|row| row.is_review_gate())
            .min_by_key(|row| row.sequence);
        let first_lineage_gap_review = review_rows
            .iter()
            .filter(|row| row.is_lineage_gap())
            .min_by_key(|row| row.sequence);
        let first_release_ready_review = review_rows
            .iter()
            .filter(|row| row.is_release_ready_review())
            .min_by_key(|row| row.sequence);
        let next_review = review_rows
            .iter()
            .filter(|row| row.requires_attention())
            .min_by_key(|row| row.sequence);
        let release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_ready =
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_summary
                .ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence();
        let release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_reviews_ready =
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_ready
                && !review_rows.is_empty()
                && review_rows.iter().all(|row| row.is_release_ready_review())
                && review_rows.iter().all(|row| row.has_lineage())
                && review_rows.iter().all(|row| row.ready_to_release_handoff());

        Self {
            total_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_rows:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_summary
                    .total_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_rows,
            total_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_rows:
                review_rows.len(),
            required_reviews: review_rows
                .iter()
                .filter(|row| row.requires_attention())
                .count(),
            blocker_reviews: review_rows
                .iter()
                .filter(|row| row.is_blocker_review())
                .count(),
            operator_reviews: review_rows
                .iter()
                .filter(|row| row.is_operator_review())
                .count(),
            review_gate_reviews: review_rows
                .iter()
                .filter(|row| row.is_review_gate())
                .count(),
            lineage_gap_reviews: review_rows
                .iter()
                .filter(|row| row.is_lineage_gap())
                .count(),
            release_ready_reviews: review_rows
                .iter()
                .filter(|row| row.is_release_ready_review())
                .count(),
            release_handoff_evidence_reviews: review_rows
                .iter()
                .filter(|row| row.is_release_handoff_evidence())
                .count(),
            lineage_complete_reviews: review_rows
                .iter()
                .filter(|row| row.has_lineage())
                .count(),
            ready_reviews: review_rows
                .iter()
                .filter(|row| row.ready_to_release_handoff())
                .count(),
            first_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_key:
                first_review_row.map(|row| {
                    row.slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_key
                        .clone()
                }),
            first_required_review_key: first_required_review.map(|row| {
                row.slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_key
                    .clone()
            }),
            first_blocker_review_key: first_blocker_review.map(|row| {
                row.slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_key
                    .clone()
            }),
            first_operator_review_key: first_operator_review.map(|row| {
                row.slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_key
                    .clone()
            }),
            first_review_gate_key: first_review_gate.map(|row| {
                row.slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_key
                    .clone()
            }),
            first_lineage_gap_review_key: first_lineage_gap_review.map(|row| {
                row.slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_key
                    .clone()
            }),
            first_release_ready_review_key: first_release_ready_review.map(|row| {
                row.slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_key
                    .clone()
            }),
            first_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key:
                first_review_row.map(|row| {
                    row.slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key
                        .clone()
                }),
            first_slot_clearance_action_evidence_review_clearance_action_key: first_review_row
                .map(|row| row.slot_clearance_action_evidence_review_clearance_action_key.clone()),
            first_slot_clearance_action_evidence_review_clearance_key: first_review_row
                .map(|row| row.slot_clearance_action_evidence_review_clearance_key.clone()),
            first_slot_clearance_action_evidence_review_key: first_review_row
                .map(|row| row.slot_clearance_action_evidence_review_key.clone()),
            first_slot_clearance_action_evidence_key: first_review_row
                .map(|row| row.slot_clearance_action_evidence_key.clone()),
            first_slot_clearance_action_key: first_review_row
                .map(|row| row.slot_clearance_action_key.clone()),
            first_slot_clearance_key: first_review_row.map(|row| row.slot_clearance_key.clone()),
            first_review_disposition_action_slot_key: first_review_row
                .map(|row| row.slot_key.clone()),
            first_review_disposition_action_key: first_review_row
                .map(|row| row.disposition_action_key.clone()),
            first_work_order_key: first_review_row.map(|row| row.work_order_key.clone()),
            first_ticket_key: first_review_row.map(|row| row.ticket_key.clone()),
            first_readiness_evidence_review_kind: first_review_row
                .map(|row| row.readiness_evidence_review_kind),
            first_clearance_action_readiness_evidence_kind: first_review_row
                .map(|row| row.clearance_action_readiness_evidence_kind),
            first_handoff_lane: first_review_row.map(|row| row.handoff_lane),
            first_check_kind: first_review_row.map(|row| row.check_kind),
            first_check_status: first_review_row.map(|row| row.status),
            next_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_key:
                next_review.map(|row| {
                    row.slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_key
                        .clone()
                }),
            next_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key:
                next_review
                    .map(|row| {
                        row.slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key
                            .clone()
                    })
                    .or_else(|| {
                        release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_summary
                            .next_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key
                            .clone()
                    }),
            next_slot_clearance_action_evidence_review_clearance_action_key: next_review
                .map(|row| row.slot_clearance_action_evidence_review_clearance_action_key.clone())
                .or_else(|| {
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_summary
                        .next_slot_clearance_action_evidence_review_clearance_action_key
                        .clone()
                }),
            next_slot_clearance_action_evidence_review_clearance_key: next_review
                .map(|row| row.slot_clearance_action_evidence_review_clearance_key.clone())
                .or_else(|| {
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_summary
                        .next_slot_clearance_action_evidence_review_clearance_key
                        .clone()
                }),
            next_slot_clearance_action_evidence_review_key: next_review
                .map(|row| row.slot_clearance_action_evidence_review_key.clone())
                .or_else(|| {
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_summary
                        .next_slot_clearance_action_evidence_review_key
                        .clone()
                }),
            next_slot_clearance_action_evidence_key: next_review
                .map(|row| row.slot_clearance_action_evidence_key.clone())
                .or_else(|| {
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_summary
                        .next_slot_clearance_action_evidence_key
                        .clone()
                }),
            next_slot_clearance_action_key: next_review
                .map(|row| row.slot_clearance_action_key.clone())
                .or_else(|| {
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_summary
                        .next_slot_clearance_action_key
                        .clone()
                }),
            next_slot_clearance_key: next_review
                .map(|row| row.slot_clearance_key.clone())
                .or_else(|| {
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_summary
                        .next_slot_clearance_key
                        .clone()
                }),
            next_review_disposition_action_slot_key: next_review
                .map(|row| row.slot_key.clone())
                .or_else(|| {
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_summary
                        .next_review_disposition_action_slot_key
                        .clone()
                }),
            next_review_disposition_action_key: next_review
                .map(|row| row.disposition_action_key.clone())
                .or_else(|| {
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_summary
                        .next_review_disposition_action_key
                        .clone()
                }),
            next_work_order_key: next_review
                .map(|row| row.work_order_key.clone())
                .or_else(|| {
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_summary
                        .next_work_order_key
                        .clone()
                }),
            next_ticket_key: next_review.map(|row| row.ticket_key.clone()).or_else(|| {
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_summary
                    .next_ticket_key
                    .clone()
            }),
            next_check_kind: next_review.map(|row| row.check_kind).or(
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_summary
                    .next_check_kind,
            ),
            next_check_status: next_review.map(|row| row.status).or(
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_summary
                    .next_check_status,
            ),
            next_handoff_lane: next_review.map(|row| row.handoff_lane).or(
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_summary
                    .next_handoff_lane,
            ),
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_ready,
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_reviews_ready,
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_summary:
                Box::new(
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_summary,
                ),
        }
    }

    pub fn has_review_rows(&self) -> bool {
        self.total_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_rows
            > 0
    }

    pub fn ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_reviews(
        &self,
    ) -> bool {
        self.release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_reviews_ready
            && !self.requires_attention()
    }

    pub fn has_required_reviews(&self) -> bool {
        self.required_reviews > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.blocker_reviews > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_summary
                .has_blockers()
    }

    pub fn has_review_work(&self) -> bool {
        self.review_gate_reviews > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_summary
                .has_review_work()
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_reviews > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_summary
                .needs_operator()
    }

    pub fn has_lineage_gaps(&self) -> bool {
        self.lineage_gap_reviews > 0
            || self.lineage_complete_reviews
                < self
                    .total_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_rows
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_summary
                .has_lineage_gaps()
    }

    pub fn has_release_ready_reviews(&self) -> bool {
        self.release_ready_reviews > 0
    }

    pub fn has_release_handoff_evidence(&self) -> bool {
        self.release_handoff_evidence_reviews > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.has_required_reviews()
            || self.has_blockers()
            || self.has_review_work()
            || self.needs_operator()
            || self.has_lineage_gaps()
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_summary
                .requires_attention()
            || !self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_reviews_ready
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionRow {
    pub sequence: usize,
    pub slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_key:
        String,
    pub slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_sequence:
        usize,
    pub slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_key:
        String,
    pub slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key: String,
    pub slot_clearance_action_evidence_review_clearance_action_key: String,
    pub slot_clearance_action_evidence_review_clearance_key: String,
    pub slot_clearance_action_evidence_review_key: String,
    pub slot_clearance_action_evidence_key: String,
    pub slot_clearance_action_key: String,
    pub slot_clearance_key: String,
    pub slot_key: String,
    pub disposition_action_key: String,
    pub disposition_key: String,
    pub work_order_key: String,
    pub ticket_key: String,
    pub check_sequence: usize,
    pub check_kind: IntegrationMeshReleaseReadinessCheckKind,
    pub status: IntegrationMeshReleaseReadinessStatus,
    pub handoff_lane: IntegrationMeshReleaseDispatchTicketHandoffLane,
    pub readiness_evidence_review_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind,
    pub readiness_evidence_review_disposition_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind,
    pub release_blocking: bool,
    pub operator_required: bool,
    pub review_required: bool,
    pub dispatch_required: bool,
    pub execution_required: bool,
    pub lineage_complete: bool,
    pub release_handoff_evidence: bool,
    pub release_ready_review: bool,
    pub disposition_ready: bool,
}

impl
    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionRow
{
    pub fn from_readiness_evidence_review_row(
        sequence: usize,
        review_row: &IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewRow,
    ) -> Self {
        let readiness_evidence_review_disposition_kind = if !review_row.has_lineage() {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind::RestoreEvidenceLineage
        } else if review_row.blocks_release() {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind::RepairReleaseBlocker
        } else if review_row.needs_review() {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind::ReviewRequired
        } else if review_row.needs_operator() {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind::CoordinateOperatorHandoff
        } else {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind::ReleaseReady
        };
        let disposition_ready = readiness_evidence_review_disposition_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind::ReleaseReady
            && review_row.ready_to_release_handoff()
            && review_row.is_release_ready_review()
            && review_row.has_lineage();

        Self {
            sequence,
            slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_key:
                format!(
                    "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-review-clearance-action-readiness-evidence-review-disposition-{sequence:02}-{}-{}",
                    readiness_evidence_review_disposition_kind.as_str(),
                    review_row.check_kind.as_str()
                ),
            slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_sequence:
                review_row.sequence,
            slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_key:
                review_row
                    .slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_key
                    .clone(),
            slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key:
                review_row
                    .slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key
                    .clone(),
            slot_clearance_action_evidence_review_clearance_action_key: review_row
                .slot_clearance_action_evidence_review_clearance_action_key
                .clone(),
            slot_clearance_action_evidence_review_clearance_key: review_row
                .slot_clearance_action_evidence_review_clearance_key
                .clone(),
            slot_clearance_action_evidence_review_key: review_row
                .slot_clearance_action_evidence_review_key
                .clone(),
            slot_clearance_action_evidence_key: review_row
                .slot_clearance_action_evidence_key
                .clone(),
            slot_clearance_action_key: review_row.slot_clearance_action_key.clone(),
            slot_clearance_key: review_row.slot_clearance_key.clone(),
            slot_key: review_row.slot_key.clone(),
            disposition_action_key: review_row.disposition_action_key.clone(),
            disposition_key: review_row.disposition_key.clone(),
            work_order_key: review_row.work_order_key.clone(),
            ticket_key: review_row.ticket_key.clone(),
            check_sequence: review_row.check_sequence,
            check_kind: review_row.check_kind,
            status: review_row.status,
            handoff_lane: review_row.handoff_lane,
            readiness_evidence_review_kind: review_row.readiness_evidence_review_kind,
            readiness_evidence_review_disposition_kind,
            release_blocking: review_row.blocks_release(),
            operator_required: review_row.needs_operator(),
            review_required: review_row.needs_review(),
            dispatch_required: review_row.dispatch_required,
            execution_required: review_row.execution_required,
            lineage_complete: review_row.has_lineage(),
            release_handoff_evidence: review_row.is_release_handoff_evidence(),
            release_ready_review: review_row.is_release_ready_review(),
            disposition_ready,
        }
    }

    pub fn blocks_release(&self) -> bool {
        self.release_blocking
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required
    }

    pub fn needs_review(&self) -> bool {
        self.review_required
    }

    pub fn requires_dispatch(&self) -> bool {
        self.dispatch_required
    }

    pub fn requires_execution(&self) -> bool {
        self.execution_required
    }

    pub fn has_lineage(&self) -> bool {
        self.lineage_complete
    }

    pub fn is_repair_required(&self) -> bool {
        self.readiness_evidence_review_disposition_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind::RepairReleaseBlocker
    }

    pub fn is_operator_handoff(&self) -> bool {
        self.readiness_evidence_review_disposition_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind::CoordinateOperatorHandoff
    }

    pub fn is_review_required(&self) -> bool {
        self.readiness_evidence_review_disposition_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind::ReviewRequired
    }

    pub fn is_lineage_gap(&self) -> bool {
        self.readiness_evidence_review_disposition_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind::RestoreEvidenceLineage
    }

    pub fn is_release_ready(&self) -> bool {
        self.disposition_ready
    }

    pub fn is_release_handoff_evidence(&self) -> bool {
        self.release_handoff_evidence
    }

    pub fn requires_attention(&self) -> bool {
        self.blocks_release()
            || self.needs_operator()
            || self.needs_review()
            || self.requires_dispatch()
            || self.requires_execution()
            || !self.has_lineage()
            || !self.is_release_ready()
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionSummary {
    pub release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_summary:
        Box<IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewSummary>,
    pub total_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_rows:
        usize,
    pub total_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_rows:
        usize,
    pub required_dispositions: usize,
    pub release_blocking_dispositions: usize,
    pub repair_required_dispositions: usize,
    pub operator_handoff_dispositions: usize,
    pub operator_required_dispositions: usize,
    pub review_required_dispositions: usize,
    pub dispatch_required_dispositions: usize,
    pub execution_required_dispositions: usize,
    pub lineage_gap_dispositions: usize,
    pub lineage_complete_dispositions: usize,
    pub release_ready_dispositions: usize,
    pub release_handoff_evidence_dispositions: usize,
    pub first_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_key:
        Option<String>,
    pub first_required_disposition_key: Option<String>,
    pub first_release_blocking_disposition_key: Option<String>,
    pub first_repair_required_disposition_key: Option<String>,
    pub first_operator_handoff_disposition_key: Option<String>,
    pub first_review_required_disposition_key: Option<String>,
    pub first_lineage_gap_disposition_key: Option<String>,
    pub first_release_ready_disposition_key: Option<String>,
    pub first_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_key:
        Option<String>,
    pub first_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key:
        Option<String>,
    pub first_slot_clearance_action_evidence_review_clearance_action_key: Option<String>,
    pub first_slot_clearance_action_evidence_review_clearance_key: Option<String>,
    pub first_slot_clearance_action_evidence_review_key: Option<String>,
    pub first_slot_clearance_action_evidence_key: Option<String>,
    pub first_slot_clearance_action_key: Option<String>,
    pub first_slot_clearance_key: Option<String>,
    pub first_review_disposition_action_slot_key: Option<String>,
    pub first_review_disposition_action_key: Option<String>,
    pub first_review_disposition_key: Option<String>,
    pub first_work_order_key: Option<String>,
    pub first_ticket_key: Option<String>,
    pub first_readiness_evidence_review_kind:
        Option<IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind>,
    pub first_readiness_evidence_review_disposition_kind:
        Option<IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind>,
    pub first_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub first_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub first_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_key:
        Option<String>,
    pub next_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_key:
        Option<String>,
    pub next_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key:
        Option<String>,
    pub next_slot_clearance_action_evidence_review_clearance_action_key: Option<String>,
    pub next_slot_clearance_action_evidence_review_clearance_key: Option<String>,
    pub next_slot_clearance_action_evidence_review_key: Option<String>,
    pub next_slot_clearance_action_evidence_key: Option<String>,
    pub next_slot_clearance_action_key: Option<String>,
    pub next_slot_clearance_key: Option<String>,
    pub next_review_disposition_action_slot_key: Option<String>,
    pub next_review_disposition_action_key: Option<String>,
    pub next_review_disposition_key: Option<String>,
    pub next_work_order_key: Option<String>,
    pub next_ticket_key: Option<String>,
    pub next_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub next_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_reviews_ready:
        bool,
    pub release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_dispositions_ready:
        bool,
}

impl
    IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionSummary
{
    pub fn from_summaries<'a>(
        release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_summary: IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewSummary,
        disposition_rows: impl IntoIterator<
            Item = &'a IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionRow,
        >,
    ) -> Self {
        let disposition_rows = disposition_rows.into_iter().collect::<Vec<_>>();
        let first_disposition_row = disposition_rows.iter().min_by_key(|row| row.sequence);
        let first_required_disposition = disposition_rows
            .iter()
            .filter(|row| row.requires_attention())
            .min_by_key(|row| row.sequence);
        let first_release_blocking_disposition = disposition_rows
            .iter()
            .filter(|row| row.blocks_release())
            .min_by_key(|row| row.sequence);
        let first_repair_required_disposition = disposition_rows
            .iter()
            .filter(|row| row.is_repair_required())
            .min_by_key(|row| row.sequence);
        let first_operator_handoff_disposition = disposition_rows
            .iter()
            .filter(|row| row.is_operator_handoff())
            .min_by_key(|row| row.sequence);
        let first_review_required_disposition = disposition_rows
            .iter()
            .filter(|row| row.is_review_required())
            .min_by_key(|row| row.sequence);
        let first_lineage_gap_disposition = disposition_rows
            .iter()
            .filter(|row| row.is_lineage_gap())
            .min_by_key(|row| row.sequence);
        let first_release_ready_disposition = disposition_rows
            .iter()
            .filter(|row| row.is_release_ready())
            .min_by_key(|row| row.sequence);
        let next_disposition = disposition_rows
            .iter()
            .filter(|row| row.requires_attention())
            .min_by_key(|row| row.sequence);
        let release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_reviews_ready =
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_summary
                .ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_reviews();
        let release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_dispositions_ready =
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_reviews_ready
                && !disposition_rows.is_empty()
                && disposition_rows.iter().all(|row| row.is_release_ready())
                && disposition_rows.iter().all(|row| row.has_lineage())
                && disposition_rows
                    .iter()
                    .all(|row| row.is_release_handoff_evidence());

        Self {
            total_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_rows:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_summary
                    .total_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_rows,
            total_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_rows:
                disposition_rows.len(),
            required_dispositions: disposition_rows
                .iter()
                .filter(|row| row.requires_attention())
                .count(),
            release_blocking_dispositions: disposition_rows
                .iter()
                .filter(|row| row.blocks_release())
                .count(),
            repair_required_dispositions: disposition_rows
                .iter()
                .filter(|row| row.is_repair_required())
                .count(),
            operator_handoff_dispositions: disposition_rows
                .iter()
                .filter(|row| row.is_operator_handoff())
                .count(),
            operator_required_dispositions: disposition_rows
                .iter()
                .filter(|row| row.needs_operator())
                .count(),
            review_required_dispositions: disposition_rows
                .iter()
                .filter(|row| row.needs_review())
                .count(),
            dispatch_required_dispositions: disposition_rows
                .iter()
                .filter(|row| row.requires_dispatch())
                .count(),
            execution_required_dispositions: disposition_rows
                .iter()
                .filter(|row| row.requires_execution())
                .count(),
            lineage_gap_dispositions: disposition_rows
                .iter()
                .filter(|row| row.is_lineage_gap())
                .count(),
            lineage_complete_dispositions: disposition_rows
                .iter()
                .filter(|row| row.has_lineage())
                .count(),
            release_ready_dispositions: disposition_rows
                .iter()
                .filter(|row| row.is_release_ready())
                .count(),
            release_handoff_evidence_dispositions: disposition_rows
                .iter()
                .filter(|row| row.is_release_handoff_evidence())
                .count(),
            first_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_key:
                first_disposition_row.map(|row| {
                    row.slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_key
                        .clone()
                }),
            first_required_disposition_key: first_required_disposition.map(|row| {
                row.slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_key
                    .clone()
            }),
            first_release_blocking_disposition_key: first_release_blocking_disposition.map(|row| {
                row.slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_key
                    .clone()
            }),
            first_repair_required_disposition_key: first_repair_required_disposition.map(|row| {
                row.slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_key
                    .clone()
            }),
            first_operator_handoff_disposition_key: first_operator_handoff_disposition.map(|row| {
                row.slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_key
                    .clone()
            }),
            first_review_required_disposition_key: first_review_required_disposition.map(|row| {
                row.slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_key
                    .clone()
            }),
            first_lineage_gap_disposition_key: first_lineage_gap_disposition.map(|row| {
                row.slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_key
                    .clone()
            }),
            first_release_ready_disposition_key: first_release_ready_disposition.map(|row| {
                row.slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_key
                    .clone()
            }),
            first_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_key:
                first_disposition_row.map(|row| {
                    row.slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_key
                        .clone()
                }),
            first_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key:
                first_disposition_row.map(|row| {
                    row.slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key
                        .clone()
                }),
            first_slot_clearance_action_evidence_review_clearance_action_key: first_disposition_row
                .map(|row| row.slot_clearance_action_evidence_review_clearance_action_key.clone()),
            first_slot_clearance_action_evidence_review_clearance_key: first_disposition_row
                .map(|row| row.slot_clearance_action_evidence_review_clearance_key.clone()),
            first_slot_clearance_action_evidence_review_key: first_disposition_row
                .map(|row| row.slot_clearance_action_evidence_review_key.clone()),
            first_slot_clearance_action_evidence_key: first_disposition_row
                .map(|row| row.slot_clearance_action_evidence_key.clone()),
            first_slot_clearance_action_key: first_disposition_row
                .map(|row| row.slot_clearance_action_key.clone()),
            first_slot_clearance_key: first_disposition_row
                .map(|row| row.slot_clearance_key.clone()),
            first_review_disposition_action_slot_key: first_disposition_row
                .map(|row| row.slot_key.clone()),
            first_review_disposition_action_key: first_disposition_row
                .map(|row| row.disposition_action_key.clone()),
            first_review_disposition_key: first_disposition_row
                .map(|row| row.disposition_key.clone()),
            first_work_order_key: first_disposition_row.map(|row| row.work_order_key.clone()),
            first_ticket_key: first_disposition_row.map(|row| row.ticket_key.clone()),
            first_readiness_evidence_review_kind: first_disposition_row
                .map(|row| row.readiness_evidence_review_kind),
            first_readiness_evidence_review_disposition_kind: first_disposition_row
                .map(|row| row.readiness_evidence_review_disposition_kind),
            first_handoff_lane: first_disposition_row.map(|row| row.handoff_lane),
            first_check_kind: first_disposition_row.map(|row| row.check_kind),
            first_check_status: first_disposition_row.map(|row| row.status),
            next_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_key:
                next_disposition.map(|row| {
                    row.slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_key
                        .clone()
                }),
            next_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_key:
                next_disposition
                    .map(|row| {
                        row.slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_key
                            .clone()
                    })
                    .or_else(|| {
                        release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_summary
                            .next_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_key
                            .clone()
                    }),
            next_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key:
                next_disposition
                    .map(|row| {
                        row.slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key
                            .clone()
                    })
                    .or_else(|| {
                        release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_summary
                            .next_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key
                            .clone()
                    }),
            next_slot_clearance_action_evidence_review_clearance_action_key: next_disposition
                .map(|row| row.slot_clearance_action_evidence_review_clearance_action_key.clone())
                .or_else(|| {
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_summary
                        .next_slot_clearance_action_evidence_review_clearance_action_key
                        .clone()
                }),
            next_slot_clearance_action_evidence_review_clearance_key: next_disposition
                .map(|row| row.slot_clearance_action_evidence_review_clearance_key.clone())
                .or_else(|| {
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_summary
                        .next_slot_clearance_action_evidence_review_clearance_key
                        .clone()
                }),
            next_slot_clearance_action_evidence_review_key: next_disposition
                .map(|row| row.slot_clearance_action_evidence_review_key.clone())
                .or_else(|| {
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_summary
                        .next_slot_clearance_action_evidence_review_key
                        .clone()
                }),
            next_slot_clearance_action_evidence_key: next_disposition
                .map(|row| row.slot_clearance_action_evidence_key.clone())
                .or_else(|| {
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_summary
                        .next_slot_clearance_action_evidence_key
                        .clone()
                }),
            next_slot_clearance_action_key: next_disposition
                .map(|row| row.slot_clearance_action_key.clone())
                .or_else(|| {
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_summary
                        .next_slot_clearance_action_key
                        .clone()
                }),
            next_slot_clearance_key: next_disposition
                .map(|row| row.slot_clearance_key.clone())
                .or_else(|| {
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_summary
                        .next_slot_clearance_key
                        .clone()
                }),
            next_review_disposition_action_slot_key: next_disposition
                .map(|row| row.slot_key.clone())
                .or_else(|| {
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_summary
                        .next_review_disposition_action_slot_key
                        .clone()
                }),
            next_review_disposition_action_key: next_disposition
                .map(|row| row.disposition_action_key.clone())
                .or_else(|| {
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_summary
                        .next_review_disposition_action_key
                        .clone()
                }),
            next_review_disposition_key: next_disposition.map(|row| row.disposition_key.clone()),
            next_work_order_key: next_disposition
                .map(|row| row.work_order_key.clone())
                .or_else(|| {
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_summary
                        .next_work_order_key
                        .clone()
                }),
            next_ticket_key: next_disposition
                .map(|row| row.ticket_key.clone())
                .or_else(|| {
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_summary
                        .next_ticket_key
                        .clone()
                }),
            next_check_kind: next_disposition.map(|row| row.check_kind).or(
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_summary
                    .next_check_kind,
            ),
            next_check_status: next_disposition.map(|row| row.status).or(
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_summary
                    .next_check_status,
            ),
            next_handoff_lane: next_disposition.map(|row| row.handoff_lane).or(
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_summary
                    .next_handoff_lane,
            ),
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_reviews_ready,
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_dispositions_ready,
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_summary:
                Box::new(
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_summary,
                ),
        }
    }

    pub fn has_disposition_rows(&self) -> bool {
        self.total_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_rows
            > 0
    }

    pub fn ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_dispositions(
        &self,
    ) -> bool {
        self.release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_dispositions_ready
            && !self.requires_attention()
    }

    pub fn has_required_dispositions(&self) -> bool {
        self.required_dispositions > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.release_blocking_dispositions > 0
            || self.repair_required_dispositions > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_summary
                .has_blockers()
    }

    pub fn has_review_work(&self) -> bool {
        self.review_required_dispositions > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_summary
                .has_review_work()
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_dispositions > 0
            || self.operator_handoff_dispositions > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_summary
                .needs_operator()
    }

    pub fn requires_dispatch(&self) -> bool {
        self.dispatch_required_dispositions > 0
    }

    pub fn requires_execution(&self) -> bool {
        self.execution_required_dispositions > 0
    }

    pub fn has_lineage_gaps(&self) -> bool {
        self.lineage_gap_dispositions > 0
            || self.lineage_complete_dispositions
                < self
                    .total_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_rows
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_summary
                .has_lineage_gaps()
    }

    pub fn has_release_ready_dispositions(&self) -> bool {
        self.release_ready_dispositions > 0
    }

    pub fn has_release_handoff_evidence(&self) -> bool {
        self.release_handoff_evidence_dispositions > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_summary
                .has_release_handoff_evidence()
    }

    pub fn requires_attention(&self) -> bool {
        self.has_required_dispositions()
            || self.has_blockers()
            || self.has_review_work()
            || self.needs_operator()
            || self.requires_dispatch()
            || self.requires_execution()
            || self.has_lineage_gaps()
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_summary
                .requires_attention()
            || !self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_dispositions_ready
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind
{
    ScheduleReleaseBlockerRepair,
    CoordinateOperatorHandoff,
    CompleteEvidenceReview,
    RestoreEvidenceLineage,
    ReleaseHandoff,
}

impl IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind {
    pub fn as_str(self) -> &'static str {
        match self {
            Self::ScheduleReleaseBlockerRepair => "schedule_release_blocker_repair",
            Self::CoordinateOperatorHandoff => "coordinate_operator_handoff",
            Self::CompleteEvidenceReview => "complete_evidence_review",
            Self::RestoreEvidenceLineage => "restore_evidence_lineage",
            Self::ReleaseHandoff => "release_handoff",
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionRow {
    pub sequence: usize,
    pub slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_key:
        String,
    pub slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_sequence:
        usize,
    pub slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_key:
        String,
    pub slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_key:
        String,
    pub slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key: String,
    pub slot_clearance_action_evidence_review_clearance_action_key: String,
    pub slot_clearance_action_evidence_review_clearance_key: String,
    pub slot_clearance_action_evidence_review_key: String,
    pub slot_clearance_action_evidence_key: String,
    pub slot_clearance_action_key: String,
    pub slot_clearance_key: String,
    pub slot_key: String,
    pub disposition_action_key: String,
    pub disposition_key: String,
    pub work_order_key: String,
    pub ticket_key: String,
    pub check_sequence: usize,
    pub check_kind: IntegrationMeshReleaseReadinessCheckKind,
    pub status: IntegrationMeshReleaseReadinessStatus,
    pub handoff_lane: IntegrationMeshReleaseDispatchTicketHandoffLane,
    pub disposition_action_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind,
    pub readiness_evidence_review_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind,
    pub readiness_evidence_review_disposition_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind,
    pub release_blocking: bool,
    pub operator_required: bool,
    pub review_required: bool,
    pub dispatch_required: bool,
    pub execution_required: bool,
    pub lineage_complete: bool,
    pub release_handoff_evidence: bool,
    pub release_ready_disposition: bool,
}

impl
    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionRow
{
    pub fn from_readiness_evidence_review_disposition_row(
        sequence: usize,
        disposition_row: &IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionRow,
    ) -> Self {
        let disposition_action_kind = match disposition_row
            .readiness_evidence_review_disposition_kind
        {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind::RepairReleaseBlocker => {
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::ScheduleReleaseBlockerRepair
            }
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind::CoordinateOperatorHandoff => {
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::CoordinateOperatorHandoff
            }
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind::ReviewRequired => {
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::CompleteEvidenceReview
            }
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind::RestoreEvidenceLineage => {
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::RestoreEvidenceLineage
            }
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind::ReleaseReady => {
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::ReleaseHandoff
            }
        };

        Self {
            sequence,
            slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_key:
                format!(
                    "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-review-clearance-action-readiness-evidence-review-disposition-action-{sequence:02}-{}-{}",
                    disposition_action_kind.as_str(),
                    disposition_row.check_kind.as_str()
                ),
            slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_sequence:
                disposition_row.sequence,
            slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_key:
                disposition_row
                    .slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_key
                    .clone(),
            slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_key:
                disposition_row
                    .slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_key
                    .clone(),
            slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key:
                disposition_row
                    .slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key
                    .clone(),
            slot_clearance_action_evidence_review_clearance_action_key: disposition_row
                .slot_clearance_action_evidence_review_clearance_action_key
                .clone(),
            slot_clearance_action_evidence_review_clearance_key: disposition_row
                .slot_clearance_action_evidence_review_clearance_key
                .clone(),
            slot_clearance_action_evidence_review_key: disposition_row
                .slot_clearance_action_evidence_review_key
                .clone(),
            slot_clearance_action_evidence_key: disposition_row
                .slot_clearance_action_evidence_key
                .clone(),
            slot_clearance_action_key: disposition_row.slot_clearance_action_key.clone(),
            slot_clearance_key: disposition_row.slot_clearance_key.clone(),
            slot_key: disposition_row.slot_key.clone(),
            disposition_action_key: disposition_row.disposition_action_key.clone(),
            disposition_key: disposition_row.disposition_key.clone(),
            work_order_key: disposition_row.work_order_key.clone(),
            ticket_key: disposition_row.ticket_key.clone(),
            check_sequence: disposition_row.check_sequence,
            check_kind: disposition_row.check_kind,
            status: disposition_row.status,
            handoff_lane: disposition_row.handoff_lane,
            disposition_action_kind,
            readiness_evidence_review_kind: disposition_row.readiness_evidence_review_kind,
            readiness_evidence_review_disposition_kind: disposition_row
                .readiness_evidence_review_disposition_kind,
            release_blocking: disposition_row.blocks_release(),
            operator_required: disposition_row.needs_operator(),
            review_required: disposition_row.needs_review(),
            dispatch_required: disposition_row.requires_dispatch(),
            execution_required: disposition_row.requires_execution(),
            lineage_complete: disposition_row.has_lineage(),
            release_handoff_evidence: disposition_row.is_release_handoff_evidence(),
            release_ready_disposition: disposition_row.is_release_ready(),
        }
    }

    pub fn blocks_release(&self) -> bool {
        self.release_blocking
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required
    }

    pub fn needs_review(&self) -> bool {
        self.review_required
    }

    pub fn requires_dispatch(&self) -> bool {
        self.dispatch_required
    }

    pub fn requires_execution(&self) -> bool {
        self.execution_required
    }

    pub fn has_lineage(&self) -> bool {
        self.lineage_complete
    }

    pub fn is_repair_action(&self) -> bool {
        self.disposition_action_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::ScheduleReleaseBlockerRepair
    }

    pub fn is_operator_handoff_action(&self) -> bool {
        self.disposition_action_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::CoordinateOperatorHandoff
    }

    pub fn is_review_action(&self) -> bool {
        self.disposition_action_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::CompleteEvidenceReview
    }

    pub fn is_lineage_action(&self) -> bool {
        self.disposition_action_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::RestoreEvidenceLineage
    }

    pub fn is_release_handoff_action(&self) -> bool {
        self.disposition_action_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::ReleaseHandoff
    }

    pub fn is_release_handoff_evidence(&self) -> bool {
        self.release_handoff_evidence
    }

    pub fn is_release_ready(&self) -> bool {
        self.release_ready_disposition && self.is_release_handoff_action()
    }

    pub fn requires_attention(&self) -> bool {
        self.blocks_release()
            || self.needs_operator()
            || self.needs_review()
            || self.requires_dispatch()
            || self.requires_execution()
            || !self.has_lineage()
            || !self.is_release_ready()
            || !self.is_release_handoff_evidence()
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionSummary {
    pub release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_summary:
        Box<IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionSummary>,
    pub total_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_rows:
        usize,
    pub total_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_rows:
        usize,
    pub required_actions: usize,
    pub release_blocking_actions: usize,
    pub repair_actions: usize,
    pub operator_handoff_actions: usize,
    pub operator_required_actions: usize,
    pub review_actions: usize,
    pub dispatch_required_actions: usize,
    pub execution_required_actions: usize,
    pub lineage_actions: usize,
    pub lineage_complete_actions: usize,
    pub release_handoff_actions: usize,
    pub release_handoff_evidence_actions: usize,
    pub first_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_key:
        Option<String>,
    pub first_required_action_key: Option<String>,
    pub first_repair_action_key: Option<String>,
    pub first_operator_handoff_action_key: Option<String>,
    pub first_review_action_key: Option<String>,
    pub first_lineage_action_key: Option<String>,
    pub first_release_handoff_action_key: Option<String>,
    pub first_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_key:
        Option<String>,
    pub first_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_key:
        Option<String>,
    pub first_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key:
        Option<String>,
    pub first_work_order_key: Option<String>,
    pub first_ticket_key: Option<String>,
    pub first_disposition_action_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind,
    >,
    pub first_readiness_evidence_review_disposition_kind:
        Option<IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind>,
    pub first_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub first_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub first_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_key:
        Option<String>,
    pub next_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_key:
        Option<String>,
    pub next_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_key:
        Option<String>,
    pub next_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key:
        Option<String>,
    pub next_work_order_key: Option<String>,
    pub next_ticket_key: Option<String>,
    pub next_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub next_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_dispositions_ready:
        bool,
    pub release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_actions_ready:
        bool,
}

impl
    IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionSummary
{
    pub fn from_summaries<'a>(
        release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_summary: IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionSummary,
        action_rows: impl IntoIterator<
            Item = &'a IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionRow,
        >,
    ) -> Self {
        let action_rows = action_rows.into_iter().collect::<Vec<_>>();
        let first_action = action_rows.iter().min_by_key(|row| row.sequence);
        let first_required_action = action_rows
            .iter()
            .filter(|row| row.requires_attention())
            .min_by_key(|row| row.sequence);
        let first_repair_action = action_rows
            .iter()
            .filter(|row| row.is_repair_action())
            .min_by_key(|row| row.sequence);
        let first_operator_handoff_action = action_rows
            .iter()
            .filter(|row| row.is_operator_handoff_action())
            .min_by_key(|row| row.sequence);
        let first_review_action = action_rows
            .iter()
            .filter(|row| row.is_review_action())
            .min_by_key(|row| row.sequence);
        let first_lineage_action = action_rows
            .iter()
            .filter(|row| row.is_lineage_action())
            .min_by_key(|row| row.sequence);
        let first_release_handoff_action = action_rows
            .iter()
            .filter(|row| row.is_release_handoff_action())
            .min_by_key(|row| row.sequence);
        let next_action = action_rows
            .iter()
            .filter(|row| row.requires_attention())
            .min_by_key(|row| row.sequence);
        let release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_dispositions_ready =
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_summary
                .ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_dispositions();
        let release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_actions_ready =
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_dispositions_ready
                && !action_rows.is_empty()
                && action_rows.iter().all(|row| row.is_release_handoff_action())
                && action_rows.iter().all(|row| row.is_release_handoff_evidence())
                && action_rows.iter().all(|row| row.has_lineage())
                && action_rows.iter().all(|row| !row.requires_attention());

        Self {
            total_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_rows:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_summary
                    .total_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_rows,
            total_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_rows:
                action_rows.len(),
            required_actions: action_rows.iter().filter(|row| row.requires_attention()).count(),
            release_blocking_actions: action_rows
                .iter()
                .filter(|row| row.blocks_release())
                .count(),
            repair_actions: action_rows
                .iter()
                .filter(|row| row.is_repair_action())
                .count(),
            operator_handoff_actions: action_rows
                .iter()
                .filter(|row| row.is_operator_handoff_action())
                .count(),
            operator_required_actions: action_rows
                .iter()
                .filter(|row| row.needs_operator())
                .count(),
            review_actions: action_rows
                .iter()
                .filter(|row| row.is_review_action())
                .count(),
            dispatch_required_actions: action_rows
                .iter()
                .filter(|row| row.requires_dispatch())
                .count(),
            execution_required_actions: action_rows
                .iter()
                .filter(|row| row.requires_execution())
                .count(),
            lineage_actions: action_rows
                .iter()
                .filter(|row| row.is_lineage_action())
                .count(),
            lineage_complete_actions: action_rows
                .iter()
                .filter(|row| row.has_lineage())
                .count(),
            release_handoff_actions: action_rows
                .iter()
                .filter(|row| row.is_release_handoff_action())
                .count(),
            release_handoff_evidence_actions: action_rows
                .iter()
                .filter(|row| row.is_release_handoff_evidence())
                .count(),
            first_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_key:
                first_action.map(|row| {
                    row.slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_key
                        .clone()
                }),
            first_required_action_key: first_required_action.map(|row| {
                row.slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_key
                    .clone()
            }),
            first_repair_action_key: first_repair_action.map(|row| {
                row.slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_key
                    .clone()
            }),
            first_operator_handoff_action_key: first_operator_handoff_action.map(|row| {
                row.slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_key
                    .clone()
            }),
            first_review_action_key: first_review_action.map(|row| {
                row.slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_key
                    .clone()
            }),
            first_lineage_action_key: first_lineage_action.map(|row| {
                row.slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_key
                    .clone()
            }),
            first_release_handoff_action_key: first_release_handoff_action.map(|row| {
                row.slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_key
                    .clone()
            }),
            first_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_key:
                first_action.map(|row| {
                    row.slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_key
                        .clone()
                }),
            first_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_key:
                first_action.map(|row| {
                    row.slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_key
                        .clone()
                }),
            first_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key:
                first_action.map(|row| {
                    row.slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key
                        .clone()
                }),
            first_work_order_key: first_action.map(|row| row.work_order_key.clone()),
            first_ticket_key: first_action.map(|row| row.ticket_key.clone()),
            first_disposition_action_kind: first_action.map(|row| row.disposition_action_kind),
            first_readiness_evidence_review_disposition_kind: first_action
                .map(|row| row.readiness_evidence_review_disposition_kind),
            first_handoff_lane: first_action.map(|row| row.handoff_lane),
            first_check_kind: first_action.map(|row| row.check_kind),
            first_check_status: first_action.map(|row| row.status),
            next_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_key:
                next_action.map(|row| {
                    row.slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_key
                        .clone()
                }),
            next_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_key:
                next_action
                    .map(|row| {
                        row.slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_key
                            .clone()
                    })
                    .or_else(|| {
                        release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_summary
                            .next_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_key
                            .clone()
                    }),
            next_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_key:
                next_action
                    .map(|row| {
                        row.slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_key
                            .clone()
                    })
                    .or_else(|| {
                        release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_summary
                            .next_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_key
                            .clone()
                    }),
            next_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key:
                next_action
                    .map(|row| {
                        row.slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key
                            .clone()
                    })
                    .or_else(|| {
                        release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_summary
                            .next_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key
                            .clone()
                    }),
            next_work_order_key: next_action
                .map(|row| row.work_order_key.clone())
                .or_else(|| {
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_summary
                        .next_work_order_key
                        .clone()
                }),
            next_ticket_key: next_action
                .map(|row| row.ticket_key.clone())
                .or_else(|| {
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_summary
                        .next_ticket_key
                        .clone()
                }),
            next_check_kind: next_action.map(|row| row.check_kind).or(
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_summary
                    .next_check_kind,
            ),
            next_check_status: next_action.map(|row| row.status).or(
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_summary
                    .next_check_status,
            ),
            next_handoff_lane: next_action.map(|row| row.handoff_lane).or(
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_summary
                    .next_handoff_lane,
            ),
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_dispositions_ready,
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_actions_ready,
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_summary:
                Box::new(
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_summary,
                ),
        }
    }

    pub fn has_action_rows(&self) -> bool {
        self.total_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_rows
            > 0
    }

    pub fn ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_actions(
        &self,
    ) -> bool {
        self.release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_actions_ready
            && !self.requires_attention()
    }

    pub fn has_required_actions(&self) -> bool {
        self.required_actions > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.release_blocking_actions > 0
            || self.repair_actions > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_summary
                .has_blockers()
    }

    pub fn has_review_work(&self) -> bool {
        self.review_actions > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_summary
                .has_review_work()
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_actions > 0
            || self.operator_handoff_actions > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_summary
                .needs_operator()
    }

    pub fn requires_dispatch(&self) -> bool {
        self.dispatch_required_actions > 0
    }

    pub fn requires_execution(&self) -> bool {
        self.execution_required_actions > 0
    }

    pub fn has_lineage_gaps(&self) -> bool {
        self.lineage_actions > 0
            || self.lineage_complete_actions
                < self
                    .total_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_rows
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_summary
                .has_lineage_gaps()
    }

    pub fn has_release_handoff_actions(&self) -> bool {
        self.release_handoff_actions > 0
    }

    pub fn has_release_handoff_evidence(&self) -> bool {
        self.release_handoff_evidence_actions > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_summary
                .has_release_handoff_evidence()
    }

    pub fn requires_attention(&self) -> bool {
        self.has_required_actions()
            || self.has_blockers()
            || self.has_review_work()
            || self.needs_operator()
            || self.requires_dispatch()
            || self.requires_execution()
            || self.has_lineage_gaps()
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_summary
                .requires_attention()
            || !self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_actions_ready
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionReadinessSummary {
    pub release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary:
        Box<IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionSummary>,
    pub total_readiness_evidence_review_disposition_rows: usize,
    pub total_readiness_evidence_review_disposition_actions: usize,
    pub required_readiness_evidence_review_disposition_actions: usize,
    pub release_blocking_readiness_evidence_review_disposition_actions: usize,
    pub repair_readiness_evidence_review_disposition_actions: usize,
    pub operator_handoff_readiness_evidence_review_disposition_actions: usize,
    pub operator_required_readiness_evidence_review_disposition_actions: usize,
    pub review_readiness_evidence_review_disposition_actions: usize,
    pub dispatch_required_readiness_evidence_review_disposition_actions: usize,
    pub execution_required_readiness_evidence_review_disposition_actions: usize,
    pub lineage_readiness_evidence_review_disposition_actions: usize,
    pub lineage_complete_readiness_evidence_review_disposition_actions: usize,
    pub release_handoff_readiness_evidence_review_disposition_actions: usize,
    pub release_handoff_evidence_readiness_evidence_review_disposition_actions: usize,
    pub first_readiness_evidence_review_disposition_action_key: Option<String>,
    pub first_required_readiness_evidence_review_disposition_action_key: Option<String>,
    pub first_repair_readiness_evidence_review_disposition_action_key: Option<String>,
    pub first_operator_handoff_readiness_evidence_review_disposition_action_key: Option<String>,
    pub first_review_readiness_evidence_review_disposition_action_key: Option<String>,
    pub first_lineage_readiness_evidence_review_disposition_action_key: Option<String>,
    pub first_release_handoff_readiness_evidence_review_disposition_action_key: Option<String>,
    pub first_readiness_evidence_review_disposition_key: Option<String>,
    pub first_readiness_evidence_review_key: Option<String>,
    pub first_readiness_evidence_key: Option<String>,
    pub first_work_order_key: Option<String>,
    pub first_ticket_key: Option<String>,
    pub first_disposition_action_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind,
    >,
    pub first_readiness_evidence_review_disposition_kind:
        Option<IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind>,
    pub first_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub first_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub first_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_readiness_evidence_review_disposition_action_key: Option<String>,
    pub next_readiness_evidence_review_disposition_key: Option<String>,
    pub next_readiness_evidence_review_key: Option<String>,
    pub next_readiness_evidence_key: Option<String>,
    pub next_work_order_key: Option<String>,
    pub next_ticket_key: Option<String>,
    pub next_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub next_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_dispositions_ready:
        bool,
    pub release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_actions_ready:
        bool,
    pub release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_readiness_ready:
        bool,
}

impl
    IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionReadinessSummary
{
    pub fn from_summary(
        release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary: IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionSummary,
    ) -> Self {
        let release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_readiness_ready =
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary
                .ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_actions();
        let next_readiness_evidence_review_disposition_action_key = if
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary
                .requires_attention()
        {
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary
                .next_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_key
                .clone()
                .or_else(|| {
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary
                        .first_required_action_key
                        .clone()
                })
                .or_else(|| {
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary
                        .first_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_key
                        .clone()
                })
        } else {
            None
        };

        Self {
            total_readiness_evidence_review_disposition_rows:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary
                    .total_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_rows,
            total_readiness_evidence_review_disposition_actions:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary
                    .total_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_rows,
            required_readiness_evidence_review_disposition_actions:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary
                    .required_actions,
            release_blocking_readiness_evidence_review_disposition_actions:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary
                    .release_blocking_actions,
            repair_readiness_evidence_review_disposition_actions:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary
                    .repair_actions,
            operator_handoff_readiness_evidence_review_disposition_actions:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary
                    .operator_handoff_actions,
            operator_required_readiness_evidence_review_disposition_actions:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary
                    .operator_required_actions,
            review_readiness_evidence_review_disposition_actions:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary
                    .review_actions,
            dispatch_required_readiness_evidence_review_disposition_actions:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary
                    .dispatch_required_actions,
            execution_required_readiness_evidence_review_disposition_actions:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary
                    .execution_required_actions,
            lineage_readiness_evidence_review_disposition_actions:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary
                    .lineage_actions,
            lineage_complete_readiness_evidence_review_disposition_actions:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary
                    .lineage_complete_actions,
            release_handoff_readiness_evidence_review_disposition_actions:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary
                    .release_handoff_actions,
            release_handoff_evidence_readiness_evidence_review_disposition_actions:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary
                    .release_handoff_evidence_actions,
            first_readiness_evidence_review_disposition_action_key:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary
                    .first_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_key
                    .clone(),
            first_required_readiness_evidence_review_disposition_action_key:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary
                    .first_required_action_key
                    .clone(),
            first_repair_readiness_evidence_review_disposition_action_key:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary
                    .first_repair_action_key
                    .clone(),
            first_operator_handoff_readiness_evidence_review_disposition_action_key:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary
                    .first_operator_handoff_action_key
                    .clone(),
            first_review_readiness_evidence_review_disposition_action_key:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary
                    .first_review_action_key
                    .clone(),
            first_lineage_readiness_evidence_review_disposition_action_key:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary
                    .first_lineage_action_key
                    .clone(),
            first_release_handoff_readiness_evidence_review_disposition_action_key:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary
                    .first_release_handoff_action_key
                    .clone(),
            first_readiness_evidence_review_disposition_key:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary
                    .first_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_key
                    .clone(),
            first_readiness_evidence_review_key:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary
                    .first_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_key
                    .clone(),
            first_readiness_evidence_key:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary
                    .first_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key
                    .clone(),
            first_work_order_key:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary
                    .first_work_order_key
                    .clone(),
            first_ticket_key:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary
                    .first_ticket_key
                    .clone(),
            first_disposition_action_kind:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary
                    .first_disposition_action_kind,
            first_readiness_evidence_review_disposition_kind:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary
                    .first_readiness_evidence_review_disposition_kind,
            first_handoff_lane:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary
                    .first_handoff_lane,
            first_check_kind:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary
                    .first_check_kind,
            first_check_status:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary
                    .first_check_status,
            next_readiness_evidence_review_disposition_action_key,
            next_readiness_evidence_review_disposition_key:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary
                    .next_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_key
                    .clone(),
            next_readiness_evidence_review_key:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary
                    .next_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_key
                    .clone(),
            next_readiness_evidence_key:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary
                    .next_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key
                    .clone(),
            next_work_order_key:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary
                    .next_work_order_key
                    .clone(),
            next_ticket_key:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary
                    .next_ticket_key
                    .clone(),
            next_check_kind:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary
                    .next_check_kind,
            next_check_status:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary
                    .next_check_status,
            next_handoff_lane:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary
                    .next_handoff_lane,
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_dispositions_ready:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary
                    .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_dispositions_ready,
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_actions_ready:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary
                    .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_actions_ready,
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_readiness_ready,
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary:
                Box::new(
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary,
                ),
        }
    }

    pub fn has_readiness_evidence_review_disposition_actions(&self) -> bool {
        self.total_readiness_evidence_review_disposition_actions > 0
    }

    pub fn ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_readiness(
        &self,
    ) -> bool {
        self.release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_readiness_ready
            && !self.requires_attention()
    }

    pub fn has_required_actions(&self) -> bool {
        self.required_readiness_evidence_review_disposition_actions > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.release_blocking_readiness_evidence_review_disposition_actions > 0
            || self.repair_readiness_evidence_review_disposition_actions > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary
                .has_blockers()
    }

    pub fn has_review_work(&self) -> bool {
        self.review_readiness_evidence_review_disposition_actions > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary
                .has_review_work()
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_readiness_evidence_review_disposition_actions > 0
            || self.operator_handoff_readiness_evidence_review_disposition_actions > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary
                .needs_operator()
    }

    pub fn requires_dispatch(&self) -> bool {
        self.dispatch_required_readiness_evidence_review_disposition_actions > 0
    }

    pub fn requires_execution(&self) -> bool {
        self.execution_required_readiness_evidence_review_disposition_actions > 0
    }

    pub fn has_lineage_gaps(&self) -> bool {
        self.lineage_readiness_evidence_review_disposition_actions > 0
            || self.lineage_complete_readiness_evidence_review_disposition_actions
                < self.total_readiness_evidence_review_disposition_actions
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary
                .has_lineage_gaps()
    }

    pub fn has_release_handoff_actions(&self) -> bool {
        self.release_handoff_readiness_evidence_review_disposition_actions > 0
    }

    pub fn has_release_handoff_evidence(&self) -> bool {
        self.release_handoff_evidence_readiness_evidence_review_disposition_actions > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary
                .has_release_handoff_evidence()
    }

    pub fn has_complete_lineage(&self) -> bool {
        self.lineage_complete_readiness_evidence_review_disposition_actions
            == self.total_readiness_evidence_review_disposition_actions
            && !self.has_lineage_gaps()
    }

    pub fn requires_attention(&self) -> bool {
        self.has_required_actions()
            || self.has_blockers()
            || self.has_review_work()
            || self.needs_operator()
            || self.requires_dispatch()
            || self.requires_execution()
            || self.has_lineage_gaps()
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary
                .requires_attention()
            || !self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_readiness_ready
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionReadinessExecutionSlot {
    pub sequence: usize,
    pub readiness_evidence_review_disposition_action_readiness_execution_slot_key: String,
    pub readiness_evidence_review_disposition_action_sequence: usize,
    pub readiness_evidence_review_disposition_action_key: String,
    pub readiness_evidence_review_disposition_key: String,
    pub readiness_evidence_review_key: String,
    pub readiness_evidence_key: String,
    pub slot_clearance_action_evidence_review_clearance_action_key: String,
    pub slot_clearance_action_evidence_review_clearance_key: String,
    pub slot_clearance_action_evidence_review_key: String,
    pub slot_clearance_action_evidence_key: String,
    pub slot_clearance_action_key: String,
    pub slot_clearance_key: String,
    pub slot_key: String,
    pub disposition_action_key: String,
    pub disposition_key: String,
    pub work_order_key: String,
    pub ticket_key: String,
    pub check_sequence: usize,
    pub check_kind: IntegrationMeshReleaseReadinessCheckKind,
    pub status: IntegrationMeshReleaseReadinessStatus,
    pub handoff_lane: IntegrationMeshReleaseDispatchTicketHandoffLane,
    pub disposition_action_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind,
    pub readiness_evidence_review_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind,
    pub readiness_evidence_review_disposition_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind,
    pub release_blocking: bool,
    pub operator_required: bool,
    pub review_required: bool,
    pub dispatch_required: bool,
    pub execution_required: bool,
    pub lineage_complete: bool,
    pub release_handoff_evidence: bool,
    pub release_ready: bool,
}

impl
    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionReadinessExecutionSlot
{
    pub fn from_readiness_evidence_review_disposition_action_row(
        sequence: usize,
        action_row: &IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionRow,
    ) -> Self {
        Self {
            sequence,
            readiness_evidence_review_disposition_action_readiness_execution_slot_key: format!(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-slot-{sequence:02}-{}-{}",
                action_row.disposition_action_kind.as_str(),
                action_row.check_kind.as_str()
            ),
            readiness_evidence_review_disposition_action_sequence: action_row.sequence,
            readiness_evidence_review_disposition_action_key: action_row
                .slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_key
                .clone(),
            readiness_evidence_review_disposition_key: action_row
                .slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_key
                .clone(),
            readiness_evidence_review_key: action_row
                .slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_key
                .clone(),
            readiness_evidence_key: action_row
                .slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key
                .clone(),
            slot_clearance_action_evidence_review_clearance_action_key: action_row
                .slot_clearance_action_evidence_review_clearance_action_key
                .clone(),
            slot_clearance_action_evidence_review_clearance_key: action_row
                .slot_clearance_action_evidence_review_clearance_key
                .clone(),
            slot_clearance_action_evidence_review_key: action_row
                .slot_clearance_action_evidence_review_key
                .clone(),
            slot_clearance_action_evidence_key: action_row
                .slot_clearance_action_evidence_key
                .clone(),
            slot_clearance_action_key: action_row.slot_clearance_action_key.clone(),
            slot_clearance_key: action_row.slot_clearance_key.clone(),
            slot_key: action_row.slot_key.clone(),
            disposition_action_key: action_row.disposition_action_key.clone(),
            disposition_key: action_row.disposition_key.clone(),
            work_order_key: action_row.work_order_key.clone(),
            ticket_key: action_row.ticket_key.clone(),
            check_sequence: action_row.check_sequence,
            check_kind: action_row.check_kind,
            status: action_row.status,
            handoff_lane: action_row.handoff_lane,
            disposition_action_kind: action_row.disposition_action_kind,
            readiness_evidence_review_kind: action_row.readiness_evidence_review_kind,
            readiness_evidence_review_disposition_kind: action_row
                .readiness_evidence_review_disposition_kind,
            release_blocking: action_row.blocks_release(),
            operator_required: action_row.needs_operator(),
            review_required: action_row.needs_review(),
            dispatch_required: action_row.requires_dispatch(),
            execution_required: action_row.requires_execution(),
            lineage_complete: action_row.has_lineage(),
            release_handoff_evidence: action_row.is_release_handoff_evidence(),
            release_ready: action_row.is_release_ready(),
        }
    }

    pub fn blocks_release(&self) -> bool {
        self.release_blocking
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required
    }

    pub fn needs_review(&self) -> bool {
        self.review_required
    }

    pub fn requires_dispatch(&self) -> bool {
        self.dispatch_required
    }

    pub fn requires_execution(&self) -> bool {
        self.execution_required
    }

    pub fn has_lineage(&self) -> bool {
        self.lineage_complete
    }

    pub fn is_repair_slot(&self) -> bool {
        self.disposition_action_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::ScheduleReleaseBlockerRepair
    }

    pub fn is_review_slot(&self) -> bool {
        self.disposition_action_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::CompleteEvidenceReview
    }

    pub fn is_release_handoff_slot(&self) -> bool {
        self.disposition_action_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::ReleaseHandoff
    }

    pub fn is_release_ready(&self) -> bool {
        self.release_ready && self.is_release_handoff_slot()
    }

    pub fn requires_attention(&self) -> bool {
        self.blocks_release()
            || self.needs_operator()
            || self.needs_review()
            || self.requires_dispatch()
            || self.requires_execution()
            || !self.has_lineage()
            || !self.release_handoff_evidence
            || !self.is_release_ready()
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionReadinessExecutionSlotSummary {
    pub release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_readiness_summary:
        Box<IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionReadinessSummary>,
    pub total_readiness_evidence_review_disposition_actions: usize,
    pub total_readiness_evidence_review_disposition_action_readiness_execution_slots: usize,
    pub required_readiness_evidence_review_disposition_action_readiness_execution_slots: usize,
    pub release_blocking_readiness_evidence_review_disposition_action_readiness_execution_slots:
        usize,
    pub repair_readiness_evidence_review_disposition_action_readiness_execution_slots: usize,
    pub operator_required_readiness_evidence_review_disposition_action_readiness_execution_slots:
        usize,
    pub review_readiness_evidence_review_disposition_action_readiness_execution_slots: usize,
    pub dispatch_required_readiness_evidence_review_disposition_action_readiness_execution_slots:
        usize,
    pub execution_required_readiness_evidence_review_disposition_action_readiness_execution_slots:
        usize,
    pub lineage_complete_readiness_evidence_review_disposition_action_readiness_execution_slots:
        usize,
    pub release_handoff_readiness_evidence_review_disposition_action_readiness_execution_slots:
        usize,
    pub release_ready_readiness_evidence_review_disposition_action_readiness_execution_slots: usize,
    pub first_readiness_evidence_review_disposition_action_readiness_execution_slot_key:
        Option<String>,
    pub first_required_readiness_evidence_review_disposition_action_readiness_execution_slot_key:
        Option<String>,
    pub first_repair_readiness_evidence_review_disposition_action_readiness_execution_slot_key:
        Option<String>,
    pub first_review_readiness_evidence_review_disposition_action_readiness_execution_slot_key:
        Option<String>,
    pub first_release_handoff_readiness_evidence_review_disposition_action_readiness_execution_slot_key:
        Option<String>,
    pub first_readiness_evidence_review_disposition_action_key: Option<String>,
    pub first_work_order_key: Option<String>,
    pub first_ticket_key: Option<String>,
    pub first_disposition_action_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind,
    >,
    pub first_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub first_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub first_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_readiness_evidence_review_disposition_action_readiness_execution_slot_key:
        Option<String>,
    pub next_readiness_evidence_review_disposition_action_key: Option<String>,
    pub next_work_order_key: Option<String>,
    pub next_ticket_key: Option<String>,
    pub next_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub next_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_readiness_ready:
        bool,
    pub release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slots_ready:
        bool,
}

impl
    IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionReadinessExecutionSlotSummary
{
    pub fn from_summaries<'a>(
        release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_readiness_summary: IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionReadinessSummary,
        slots: impl IntoIterator<
            Item = &'a IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionReadinessExecutionSlot,
        >,
    ) -> Self {
        let slots: Vec<&IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionReadinessExecutionSlot> =
            slots.into_iter().collect();
        let total_readiness_evidence_review_disposition_action_readiness_execution_slots =
            slots.len();
        let first_slot = slots.first().copied();
        let first_required_slot = slots
            .iter()
            .copied()
            .find(|slot| slot.requires_attention());
        let first_repair_slot = slots.iter().copied().find(|slot| slot.is_repair_slot());
        let first_review_slot = slots.iter().copied().find(|slot| slot.is_review_slot());
        let first_release_handoff_slot = slots
            .iter()
            .copied()
            .find(|slot| slot.is_release_handoff_slot());
        let next_slot = first_required_slot;
        let release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slots_ready =
            total_readiness_evidence_review_disposition_action_readiness_execution_slots
                == release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_readiness_summary
                    .total_readiness_evidence_review_disposition_actions
                && slots.iter().all(|slot| !slot.requires_attention());

        Self {
            total_readiness_evidence_review_disposition_actions:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_readiness_summary
                    .total_readiness_evidence_review_disposition_actions,
            total_readiness_evidence_review_disposition_action_readiness_execution_slots,
            required_readiness_evidence_review_disposition_action_readiness_execution_slots:
                slots.iter().filter(|slot| slot.requires_attention()).count(),
            release_blocking_readiness_evidence_review_disposition_action_readiness_execution_slots:
                slots.iter().filter(|slot| slot.blocks_release()).count(),
            repair_readiness_evidence_review_disposition_action_readiness_execution_slots:
                slots.iter().filter(|slot| slot.is_repair_slot()).count(),
            operator_required_readiness_evidence_review_disposition_action_readiness_execution_slots:
                slots.iter().filter(|slot| slot.needs_operator()).count(),
            review_readiness_evidence_review_disposition_action_readiness_execution_slots:
                slots.iter().filter(|slot| slot.is_review_slot()).count(),
            dispatch_required_readiness_evidence_review_disposition_action_readiness_execution_slots:
                slots.iter().filter(|slot| slot.requires_dispatch()).count(),
            execution_required_readiness_evidence_review_disposition_action_readiness_execution_slots:
                slots.iter().filter(|slot| slot.requires_execution()).count(),
            lineage_complete_readiness_evidence_review_disposition_action_readiness_execution_slots:
                slots.iter().filter(|slot| slot.has_lineage()).count(),
            release_handoff_readiness_evidence_review_disposition_action_readiness_execution_slots:
                slots
                    .iter()
                    .filter(|slot| slot.is_release_handoff_slot())
                    .count(),
            release_ready_readiness_evidence_review_disposition_action_readiness_execution_slots:
                slots.iter().filter(|slot| slot.is_release_ready()).count(),
            first_readiness_evidence_review_disposition_action_readiness_execution_slot_key:
                first_slot.map(|slot| {
                    slot.readiness_evidence_review_disposition_action_readiness_execution_slot_key
                        .clone()
                }),
            first_required_readiness_evidence_review_disposition_action_readiness_execution_slot_key:
                first_required_slot.map(|slot| {
                    slot.readiness_evidence_review_disposition_action_readiness_execution_slot_key
                        .clone()
                }),
            first_repair_readiness_evidence_review_disposition_action_readiness_execution_slot_key:
                first_repair_slot.map(|slot| {
                    slot.readiness_evidence_review_disposition_action_readiness_execution_slot_key
                        .clone()
                }),
            first_review_readiness_evidence_review_disposition_action_readiness_execution_slot_key:
                first_review_slot.map(|slot| {
                    slot.readiness_evidence_review_disposition_action_readiness_execution_slot_key
                        .clone()
                }),
            first_release_handoff_readiness_evidence_review_disposition_action_readiness_execution_slot_key:
                first_release_handoff_slot.map(|slot| {
                    slot.readiness_evidence_review_disposition_action_readiness_execution_slot_key
                        .clone()
                }),
            first_readiness_evidence_review_disposition_action_key:
                first_slot.map(|slot| slot.readiness_evidence_review_disposition_action_key.clone()),
            first_work_order_key: first_slot.map(|slot| slot.work_order_key.clone()),
            first_ticket_key: first_slot.map(|slot| slot.ticket_key.clone()),
            first_disposition_action_kind: first_slot.map(|slot| slot.disposition_action_kind),
            first_handoff_lane: first_slot.map(|slot| slot.handoff_lane),
            first_check_kind: first_slot.map(|slot| slot.check_kind),
            first_check_status: first_slot.map(|slot| slot.status),
            next_readiness_evidence_review_disposition_action_readiness_execution_slot_key:
                next_slot.map(|slot| {
                    slot.readiness_evidence_review_disposition_action_readiness_execution_slot_key
                        .clone()
                }),
            next_readiness_evidence_review_disposition_action_key:
                next_slot.map(|slot| slot.readiness_evidence_review_disposition_action_key.clone()),
            next_work_order_key: next_slot.map(|slot| slot.work_order_key.clone()),
            next_ticket_key: next_slot.map(|slot| slot.ticket_key.clone()),
            next_check_kind: next_slot.map(|slot| slot.check_kind),
            next_check_status: next_slot.map(|slot| slot.status),
            next_handoff_lane: next_slot.map(|slot| slot.handoff_lane),
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_readiness_ready:
                release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_readiness_summary
                    .ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_readiness(),
            release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slots_ready,
            release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_readiness_summary:
                Box::new(
                    release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_readiness_summary,
                ),
        }
    }

    pub fn has_execution_slots(&self) -> bool {
        self.total_readiness_evidence_review_disposition_action_readiness_execution_slots > 0
    }

    pub fn ready_for_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slots(
        &self,
    ) -> bool {
        self.release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_readiness_ready
            && self.release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slots_ready
            && !self.requires_attention()
    }

    pub fn has_required_slots(&self) -> bool {
        self.required_readiness_evidence_review_disposition_action_readiness_execution_slots > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.release_blocking_readiness_evidence_review_disposition_action_readiness_execution_slots
            > 0
            || self.repair_readiness_evidence_review_disposition_action_readiness_execution_slots
                > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_readiness_summary
                .has_blockers()
    }

    pub fn has_review_work(&self) -> bool {
        self.review_readiness_evidence_review_disposition_action_readiness_execution_slots > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_readiness_summary
                .has_review_work()
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_readiness_evidence_review_disposition_action_readiness_execution_slots
            > 0
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_readiness_summary
                .needs_operator()
    }

    pub fn requires_dispatch(&self) -> bool {
        self.dispatch_required_readiness_evidence_review_disposition_action_readiness_execution_slots
            > 0
    }

    pub fn requires_execution(&self) -> bool {
        self.execution_required_readiness_evidence_review_disposition_action_readiness_execution_slots
            > 0
    }

    pub fn has_lineage_gaps(&self) -> bool {
        self.lineage_complete_readiness_evidence_review_disposition_action_readiness_execution_slots
            < self.total_readiness_evidence_review_disposition_action_readiness_execution_slots
            || self
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_readiness_summary
                .has_lineage_gaps()
    }

    pub fn has_release_handoff_slots(&self) -> bool {
        self.release_handoff_readiness_evidence_review_disposition_action_readiness_execution_slots
            > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.has_required_slots()
            || self.has_blockers()
            || self.has_review_work()
            || self.needs_operator()
            || self.requires_dispatch()
            || self.requires_execution()
            || self.has_lineage_gaps()
            || !self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slots_ready
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffRow {
    pub sequence: usize,
    pub handoff_row_key: String,
    pub readiness_evidence_review_disposition_action_readiness_execution_slot_key: String,
    pub readiness_evidence_review_disposition_action_key: String,
    pub readiness_evidence_review_disposition_key: String,
    pub readiness_evidence_review_key: String,
    pub readiness_evidence_key: String,
    pub work_order_key: String,
    pub ticket_key: String,
    pub check_sequence: usize,
    pub check_kind: IntegrationMeshReleaseReadinessCheckKind,
    pub status: IntegrationMeshReleaseReadinessStatus,
    pub handoff_lane: IntegrationMeshReleaseDispatchTicketHandoffLane,
    pub disposition_action_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind,
    pub release_blocking: bool,
    pub operator_required: bool,
    pub review_required: bool,
    pub dispatch_required: bool,
    pub execution_required: bool,
    pub lineage_complete: bool,
    pub release_handoff_evidence: bool,
    pub release_ready: bool,
    pub handoff_ready: bool,
}

impl
    IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffRow
{
    pub fn from_readiness_execution_slot(
        sequence: usize,
        slot: &IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionReadinessExecutionSlot,
    ) -> Self {
        let handoff_ready = slot.is_release_handoff_slot()
            && slot.is_release_ready()
            && slot.has_lineage()
            && !slot.requires_attention();

        Self {
            sequence,
            handoff_row_key: format!(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-{sequence:02}-{}-{}",
                slot.handoff_lane.as_str(),
                slot.check_kind.as_str()
            ),
            readiness_evidence_review_disposition_action_readiness_execution_slot_key: slot
                .readiness_evidence_review_disposition_action_readiness_execution_slot_key
                .clone(),
            readiness_evidence_review_disposition_action_key: slot
                .readiness_evidence_review_disposition_action_key
                .clone(),
            readiness_evidence_review_disposition_key: slot
                .readiness_evidence_review_disposition_key
                .clone(),
            readiness_evidence_review_key: slot.readiness_evidence_review_key.clone(),
            readiness_evidence_key: slot.readiness_evidence_key.clone(),
            work_order_key: slot.work_order_key.clone(),
            ticket_key: slot.ticket_key.clone(),
            check_sequence: slot.check_sequence,
            check_kind: slot.check_kind,
            status: slot.status,
            handoff_lane: slot.handoff_lane,
            disposition_action_kind: slot.disposition_action_kind,
            release_blocking: slot.blocks_release(),
            operator_required: slot.needs_operator(),
            review_required: slot.needs_review(),
            dispatch_required: slot.requires_dispatch(),
            execution_required: slot.requires_execution(),
            lineage_complete: slot.has_lineage(),
            release_handoff_evidence: slot.release_handoff_evidence,
            release_ready: slot.is_release_ready(),
            handoff_ready,
        }
    }

    pub fn blocks_release(&self) -> bool {
        self.release_blocking
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required
    }

    pub fn needs_review(&self) -> bool {
        self.review_required
    }

    pub fn requires_dispatch(&self) -> bool {
        self.dispatch_required
    }

    pub fn requires_execution(&self) -> bool {
        self.execution_required
    }

    pub fn has_lineage(&self) -> bool {
        self.lineage_complete
    }

    pub fn is_repair_handoff(&self) -> bool {
        self.handoff_lane == IntegrationMeshReleaseDispatchTicketHandoffLane::Repair
    }

    pub fn is_review_handoff(&self) -> bool {
        self.handoff_lane == IntegrationMeshReleaseDispatchTicketHandoffLane::Review
    }

    pub fn is_release_handoff(&self) -> bool {
        self.handoff_lane == IntegrationMeshReleaseDispatchTicketHandoffLane::Release
    }

    pub fn ready_for_handoff(&self) -> bool {
        self.handoff_ready
    }

    pub fn requires_attention(&self) -> bool {
        self.blocks_release()
            || self.needs_operator()
            || self.needs_review()
            || self.requires_dispatch()
            || self.requires_execution()
            || !self.has_lineage()
            || !self.release_handoff_evidence
            || !self.ready_for_handoff()
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub enum IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind
{
    RepairHandoff,
    ReviewHandoff,
    OperatorHandoff,
    DispatchHandoff,
    ExecuteHandoff,
    PreserveLineage,
    ReleaseHandoff,
}

impl
    IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind
{
    pub fn as_str(&self) -> &'static str {
        match self {
            Self::RepairHandoff => "repair_handoff",
            Self::ReviewHandoff => "review_handoff",
            Self::OperatorHandoff => "operator_handoff",
            Self::DispatchHandoff => "dispatch_handoff",
            Self::ExecuteHandoff => "execute_handoff",
            Self::PreserveLineage => "preserve_lineage",
            Self::ReleaseHandoff => "release_handoff",
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionRow {
    pub sequence: usize,
    pub handoff_action_row_key: String,
    pub handoff_row_key: String,
    pub readiness_evidence_review_disposition_action_readiness_execution_slot_key: String,
    pub readiness_evidence_review_disposition_action_key: String,
    pub work_order_key: String,
    pub ticket_key: String,
    pub check_sequence: usize,
    pub check_kind: IntegrationMeshReleaseReadinessCheckKind,
    pub status: IntegrationMeshReleaseReadinessStatus,
    pub handoff_lane: IntegrationMeshReleaseDispatchTicketHandoffLane,
    pub handoff_action_kind:
        IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind,
    pub disposition_action_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind,
    pub release_blocking: bool,
    pub operator_required: bool,
    pub review_required: bool,
    pub dispatch_required: bool,
    pub execution_required: bool,
    pub lineage_complete: bool,
    pub release_handoff_evidence: bool,
    pub release_ready: bool,
    pub handoff_ready: bool,
}

impl
    IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionRow
{
    pub fn from_handoff_row(
        sequence: usize,
        row: &IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffRow,
    ) -> Self {
        let handoff_action_kind = if row.ready_for_handoff() {
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind::ReleaseHandoff
        } else if row.is_repair_handoff() || row.blocks_release() {
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind::RepairHandoff
        } else if row.is_review_handoff() || row.needs_review() {
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind::ReviewHandoff
        } else if row.needs_operator() {
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind::OperatorHandoff
        } else if row.requires_dispatch() {
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind::DispatchHandoff
        } else if row.requires_execution() {
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind::ExecuteHandoff
        } else {
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind::PreserveLineage
        };

        Self {
            sequence,
            handoff_action_row_key: format!(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-{sequence:02}-{}-{}",
                handoff_action_kind.as_str(),
                row.check_kind.as_str()
            ),
            handoff_row_key: row.handoff_row_key.clone(),
            readiness_evidence_review_disposition_action_readiness_execution_slot_key: row
                .readiness_evidence_review_disposition_action_readiness_execution_slot_key
                .clone(),
            readiness_evidence_review_disposition_action_key: row
                .readiness_evidence_review_disposition_action_key
                .clone(),
            work_order_key: row.work_order_key.clone(),
            ticket_key: row.ticket_key.clone(),
            check_sequence: row.check_sequence,
            check_kind: row.check_kind,
            status: row.status,
            handoff_lane: row.handoff_lane,
            handoff_action_kind,
            disposition_action_kind: row.disposition_action_kind,
            release_blocking: row.blocks_release(),
            operator_required: row.needs_operator(),
            review_required: row.needs_review(),
            dispatch_required: row.requires_dispatch(),
            execution_required: row.requires_execution(),
            lineage_complete: row.has_lineage(),
            release_handoff_evidence: row.release_handoff_evidence,
            release_ready: row.release_ready,
            handoff_ready: row.ready_for_handoff(),
        }
    }

    pub fn blocks_release(&self) -> bool {
        self.release_blocking
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required
    }

    pub fn needs_review(&self) -> bool {
        self.review_required
    }

    pub fn requires_dispatch(&self) -> bool {
        self.dispatch_required
    }

    pub fn requires_execution(&self) -> bool {
        self.execution_required
    }

    pub fn has_lineage(&self) -> bool {
        self.lineage_complete
    }

    pub fn is_repair_action(&self) -> bool {
        self.handoff_action_kind
            == IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind::RepairHandoff
    }

    pub fn is_review_action(&self) -> bool {
        self.handoff_action_kind
            == IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind::ReviewHandoff
    }

    pub fn is_release_handoff_action(&self) -> bool {
        self.handoff_action_kind
            == IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind::ReleaseHandoff
    }

    pub fn ready_for_release_handoff(&self) -> bool {
        self.is_release_handoff_action()
            && self.handoff_ready
            && self.release_ready
            && self.release_handoff_evidence
            && self.has_lineage()
    }

    pub fn requires_attention(&self) -> bool {
        self.blocks_release()
            || self.needs_operator()
            || self.needs_review()
            || self.requires_dispatch()
            || self.requires_execution()
            || !self.has_lineage()
            || !self.release_handoff_evidence
            || !self.ready_for_release_handoff()
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceRow {
    pub sequence: usize,
    pub handoff_action_evidence_row_key: String,
    pub handoff_action_row_key: String,
    pub handoff_row_key: String,
    pub readiness_evidence_review_disposition_action_readiness_execution_slot_key: String,
    pub readiness_evidence_review_disposition_action_key: String,
    pub work_order_key: String,
    pub ticket_key: String,
    pub check_sequence: usize,
    pub check_kind: IntegrationMeshReleaseReadinessCheckKind,
    pub status: IntegrationMeshReleaseReadinessStatus,
    pub handoff_lane: IntegrationMeshReleaseDispatchTicketHandoffLane,
    pub handoff_action_kind:
        IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind,
    pub disposition_action_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind,
    pub release_blocking: bool,
    pub operator_required: bool,
    pub review_required: bool,
    pub dispatch_required: bool,
    pub execution_required: bool,
    pub lineage_complete: bool,
    pub release_handoff_evidence: bool,
    pub release_ready: bool,
    pub handoff_ready: bool,
    pub handoff_action_ready: bool,
}

impl
    IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceRow
{
    pub fn from_action_row(
        sequence: usize,
        row: &IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionRow,
    ) -> Self {
        Self {
            sequence,
            handoff_action_evidence_row_key: format!(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-evidence-{sequence:02}-{}-{}",
                row.handoff_action_kind.as_str(),
                row.check_kind.as_str()
            ),
            handoff_action_row_key: row.handoff_action_row_key.clone(),
            handoff_row_key: row.handoff_row_key.clone(),
            readiness_evidence_review_disposition_action_readiness_execution_slot_key: row
                .readiness_evidence_review_disposition_action_readiness_execution_slot_key
                .clone(),
            readiness_evidence_review_disposition_action_key: row
                .readiness_evidence_review_disposition_action_key
                .clone(),
            work_order_key: row.work_order_key.clone(),
            ticket_key: row.ticket_key.clone(),
            check_sequence: row.check_sequence,
            check_kind: row.check_kind,
            status: row.status,
            handoff_lane: row.handoff_lane,
            handoff_action_kind: row.handoff_action_kind,
            disposition_action_kind: row.disposition_action_kind,
            release_blocking: row.blocks_release(),
            operator_required: row.needs_operator(),
            review_required: row.needs_review(),
            dispatch_required: row.requires_dispatch(),
            execution_required: row.requires_execution(),
            lineage_complete: row.has_lineage(),
            release_handoff_evidence: row.release_handoff_evidence,
            release_ready: row.release_ready,
            handoff_ready: row.handoff_ready,
            handoff_action_ready: row.ready_for_release_handoff(),
        }
    }

    pub fn blocks_release(&self) -> bool {
        self.release_blocking
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required
    }

    pub fn needs_review(&self) -> bool {
        self.review_required
    }

    pub fn requires_dispatch(&self) -> bool {
        self.dispatch_required
    }

    pub fn requires_execution(&self) -> bool {
        self.execution_required
    }

    pub fn has_lineage(&self) -> bool {
        self.lineage_complete
    }

    pub fn has_release_handoff_evidence(&self) -> bool {
        self.release_handoff_evidence
    }

    pub fn is_repair_action(&self) -> bool {
        self.handoff_action_kind
            == IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind::RepairHandoff
    }

    pub fn is_review_action(&self) -> bool {
        self.handoff_action_kind
            == IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind::ReviewHandoff
    }

    pub fn is_release_handoff_action(&self) -> bool {
        self.handoff_action_kind
            == IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind::ReleaseHandoff
    }

    pub fn ready_for_release_handoff(&self) -> bool {
        self.handoff_action_ready
    }

    pub fn requires_attention(&self) -> bool {
        self.blocks_release()
            || self.needs_operator()
            || self.needs_review()
            || self.requires_dispatch()
            || self.requires_execution()
            || !self.has_lineage()
            || !self.has_release_handoff_evidence()
            || !self.ready_for_release_handoff()
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewRow {
    pub sequence: usize,
    pub handoff_action_evidence_review_row_key: String,
    pub handoff_action_evidence_row_sequence: usize,
    pub handoff_action_evidence_row_key: String,
    pub handoff_action_row_key: String,
    pub handoff_row_key: String,
    pub readiness_evidence_review_disposition_action_readiness_execution_slot_key: String,
    pub readiness_evidence_review_disposition_action_key: String,
    pub work_order_key: String,
    pub ticket_key: String,
    pub check_sequence: usize,
    pub check_kind: IntegrationMeshReleaseReadinessCheckKind,
    pub status: IntegrationMeshReleaseReadinessStatus,
    pub handoff_lane: IntegrationMeshReleaseDispatchTicketHandoffLane,
    pub handoff_action_kind:
        IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind,
    pub disposition_action_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind,
    pub handoff_action_evidence_review_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind,
    pub release_blocking: bool,
    pub operator_required: bool,
    pub review_required: bool,
    pub dispatch_required: bool,
    pub execution_required: bool,
    pub lineage_complete: bool,
    pub evidence_required: bool,
    pub release_handoff_evidence: bool,
    pub release_ready_review: bool,
    pub handoff_actions_ready: bool,
    pub handoff_action_evidence_ready: bool,
    pub ready_for_release_handoff: bool,
}

impl
    IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewRow
{
    pub fn from_evidence_row(
        sequence: usize,
        evidence_row: &IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceRow,
        handoff_action_evidence_ready: bool,
    ) -> Self {
        let handoff_action_evidence_review_kind = if !evidence_row.has_lineage() {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::LineageGap
        } else if evidence_row.blocks_release() {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::BlockerReview
        } else if evidence_row.needs_review() {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::ReviewGate
        } else if evidence_row.needs_operator() {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::OperatorReview
        } else {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::ReleaseReady
        };
        let release_ready_review = handoff_action_evidence_ready
            && evidence_row.ready_for_release_handoff()
            && evidence_row.has_lineage()
            && !evidence_row.blocks_release()
            && !evidence_row.needs_operator()
            && !evidence_row.needs_review()
            && !evidence_row.requires_dispatch()
            && !evidence_row.requires_execution()
            && handoff_action_evidence_review_kind
                == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::ReleaseReady;

        Self {
            sequence,
            handoff_action_evidence_review_row_key: format!(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-evidence-review-{sequence:02}-{}-{}",
                handoff_action_evidence_review_kind.as_str(),
                evidence_row.check_kind.as_str()
            ),
            handoff_action_evidence_row_sequence: evidence_row.sequence,
            handoff_action_evidence_row_key: evidence_row.handoff_action_evidence_row_key.clone(),
            handoff_action_row_key: evidence_row.handoff_action_row_key.clone(),
            handoff_row_key: evidence_row.handoff_row_key.clone(),
            readiness_evidence_review_disposition_action_readiness_execution_slot_key: evidence_row
                .readiness_evidence_review_disposition_action_readiness_execution_slot_key
                .clone(),
            readiness_evidence_review_disposition_action_key: evidence_row
                .readiness_evidence_review_disposition_action_key
                .clone(),
            work_order_key: evidence_row.work_order_key.clone(),
            ticket_key: evidence_row.ticket_key.clone(),
            check_sequence: evidence_row.check_sequence,
            check_kind: evidence_row.check_kind,
            status: evidence_row.status,
            handoff_lane: evidence_row.handoff_lane,
            handoff_action_kind: evidence_row.handoff_action_kind,
            disposition_action_kind: evidence_row.disposition_action_kind,
            handoff_action_evidence_review_kind,
            release_blocking: evidence_row.blocks_release(),
            operator_required: evidence_row.needs_operator(),
            review_required: evidence_row.needs_review(),
            dispatch_required: evidence_row.requires_dispatch(),
            execution_required: evidence_row.requires_execution(),
            lineage_complete: evidence_row.has_lineage(),
            evidence_required: evidence_row.requires_attention(),
            release_handoff_evidence: evidence_row.has_release_handoff_evidence(),
            release_ready_review,
            handoff_actions_ready: evidence_row.ready_for_release_handoff(),
            handoff_action_evidence_ready,
            ready_for_release_handoff: release_ready_review,
        }
    }

    pub fn is_lineage_gap(&self) -> bool {
        self.handoff_action_evidence_review_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::LineageGap
    }

    pub fn is_blocker_review(&self) -> bool {
        self.handoff_action_evidence_review_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::BlockerReview
    }

    pub fn is_review_gate(&self) -> bool {
        self.handoff_action_evidence_review_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::ReviewGate
    }

    pub fn is_operator_review(&self) -> bool {
        self.handoff_action_evidence_review_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::OperatorReview
    }

    pub fn is_release_ready_review(&self) -> bool {
        self.release_ready_review
    }

    pub fn blocks_release(&self) -> bool {
        self.release_blocking
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required
    }

    pub fn needs_review(&self) -> bool {
        self.review_required
    }

    pub fn requires_dispatch(&self) -> bool {
        self.dispatch_required
    }

    pub fn requires_execution(&self) -> bool {
        self.execution_required
    }

    pub fn has_lineage(&self) -> bool {
        self.lineage_complete
    }

    pub fn requires_evidence_review(&self) -> bool {
        self.evidence_required
    }

    pub fn is_repair_action(&self) -> bool {
        self.handoff_action_kind
            == IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind::RepairHandoff
    }

    pub fn is_review_action(&self) -> bool {
        self.handoff_action_kind
            == IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind::ReviewHandoff
    }

    pub fn is_release_handoff_action(&self) -> bool {
        self.handoff_action_kind
            == IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind::ReleaseHandoff
    }

    pub fn is_release_handoff_evidence(&self) -> bool {
        self.release_handoff_evidence
    }

    pub fn ready_to_release_handoff(&self) -> bool {
        self.ready_for_release_handoff
    }

    pub fn requires_attention(&self) -> bool {
        self.blocks_release()
            || self.needs_operator()
            || self.needs_review()
            || self.requires_dispatch()
            || self.requires_execution()
            || self.is_lineage_gap()
            || !self.has_lineage()
            || !self.is_release_handoff_evidence()
            || !self.ready_to_release_handoff()
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionRow {
    pub sequence: usize,
    pub handoff_action_evidence_review_disposition_row_key: String,
    pub handoff_action_evidence_review_row_sequence: usize,
    pub handoff_action_evidence_review_row_key: String,
    pub handoff_action_evidence_row_key: String,
    pub handoff_action_row_key: String,
    pub handoff_row_key: String,
    pub readiness_evidence_review_disposition_action_readiness_execution_slot_key: String,
    pub readiness_evidence_review_disposition_action_key: String,
    pub work_order_key: String,
    pub ticket_key: String,
    pub check_sequence: usize,
    pub check_kind: IntegrationMeshReleaseReadinessCheckKind,
    pub status: IntegrationMeshReleaseReadinessStatus,
    pub handoff_lane: IntegrationMeshReleaseDispatchTicketHandoffLane,
    pub handoff_action_kind:
        IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind,
    pub disposition_action_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind,
    pub handoff_action_evidence_review_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind,
    pub handoff_action_evidence_review_disposition_action_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind,
    pub release_blocking: bool,
    pub operator_required: bool,
    pub review_required: bool,
    pub dispatch_required: bool,
    pub execution_required: bool,
    pub lineage_complete: bool,
    pub release_handoff_evidence: bool,
    pub release_ready_review: bool,
    pub handoff_action_evidence_reviews_ready: bool,
    pub release_ready_disposition: bool,
    pub ready_for_release_handoff: bool,
}

impl
    IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionRow
{
    pub fn from_review_row(
        sequence: usize,
        review_row: &IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewRow,
        handoff_action_evidence_reviews_ready: bool,
    ) -> Self {
        let handoff_action_evidence_review_disposition_action_kind = if !review_row.has_lineage()
            || review_row.is_lineage_gap()
        {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::RestoreEvidenceLineage
        } else if review_row.blocks_release() {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::ScheduleReleaseBlockerRepair
        } else if review_row.needs_review() || review_row.is_review_gate() {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::CompleteEvidenceReview
        } else if review_row.needs_operator() {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::CoordinateOperatorHandoff
        } else {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::ReleaseHandoff
        };
        let release_ready_disposition = handoff_action_evidence_reviews_ready
            && review_row.ready_to_release_handoff()
            && review_row.is_release_ready_review()
            && review_row.has_lineage()
            && handoff_action_evidence_review_disposition_action_kind
                == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::ReleaseHandoff;

        Self {
            sequence,
            handoff_action_evidence_review_disposition_row_key: format!(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-evidence-review-disposition-{sequence:02}-{}-{}",
                handoff_action_evidence_review_disposition_action_kind.as_str(),
                review_row.check_kind.as_str()
            ),
            handoff_action_evidence_review_row_sequence: review_row.sequence,
            handoff_action_evidence_review_row_key: review_row
                .handoff_action_evidence_review_row_key
                .clone(),
            handoff_action_evidence_row_key: review_row.handoff_action_evidence_row_key.clone(),
            handoff_action_row_key: review_row.handoff_action_row_key.clone(),
            handoff_row_key: review_row.handoff_row_key.clone(),
            readiness_evidence_review_disposition_action_readiness_execution_slot_key: review_row
                .readiness_evidence_review_disposition_action_readiness_execution_slot_key
                .clone(),
            readiness_evidence_review_disposition_action_key: review_row
                .readiness_evidence_review_disposition_action_key
                .clone(),
            work_order_key: review_row.work_order_key.clone(),
            ticket_key: review_row.ticket_key.clone(),
            check_sequence: review_row.check_sequence,
            check_kind: review_row.check_kind,
            status: review_row.status,
            handoff_lane: review_row.handoff_lane,
            handoff_action_kind: review_row.handoff_action_kind,
            disposition_action_kind: review_row.disposition_action_kind,
            handoff_action_evidence_review_kind: review_row.handoff_action_evidence_review_kind,
            handoff_action_evidence_review_disposition_action_kind,
            release_blocking: review_row.blocks_release(),
            operator_required: review_row.needs_operator(),
            review_required: review_row.needs_review(),
            dispatch_required: review_row.requires_dispatch(),
            execution_required: review_row.requires_execution(),
            lineage_complete: review_row.has_lineage(),
            release_handoff_evidence: review_row.is_release_handoff_evidence(),
            release_ready_review: review_row.is_release_ready_review(),
            handoff_action_evidence_reviews_ready,
            release_ready_disposition,
            ready_for_release_handoff: release_ready_disposition,
        }
    }

    pub fn blocks_release(&self) -> bool {
        self.release_blocking
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required
    }

    pub fn needs_review(&self) -> bool {
        self.review_required
    }

    pub fn requires_dispatch(&self) -> bool {
        self.dispatch_required
    }

    pub fn requires_execution(&self) -> bool {
        self.execution_required
    }

    pub fn has_lineage(&self) -> bool {
        self.lineage_complete
    }

    pub fn is_repair_disposition(&self) -> bool {
        self.handoff_action_evidence_review_disposition_action_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::ScheduleReleaseBlockerRepair
    }

    pub fn is_operator_disposition(&self) -> bool {
        self.handoff_action_evidence_review_disposition_action_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::CoordinateOperatorHandoff
    }

    pub fn is_review_disposition(&self) -> bool {
        self.handoff_action_evidence_review_disposition_action_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::CompleteEvidenceReview
    }

    pub fn is_lineage_repair_disposition(&self) -> bool {
        self.handoff_action_evidence_review_disposition_action_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::RestoreEvidenceLineage
    }

    pub fn is_release_handoff_disposition(&self) -> bool {
        self.handoff_action_evidence_review_disposition_action_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::ReleaseHandoff
    }

    pub fn is_release_handoff_evidence(&self) -> bool {
        self.release_handoff_evidence
    }

    pub fn is_release_ready_review(&self) -> bool {
        self.release_ready_review
    }

    pub fn is_release_ready_disposition(&self) -> bool {
        self.release_ready_disposition && self.is_release_handoff_disposition()
    }

    pub fn ready_to_release_handoff(&self) -> bool {
        self.ready_for_release_handoff
    }

    pub fn requires_attention(&self) -> bool {
        self.blocks_release()
            || self.needs_operator()
            || self.needs_review()
            || self.requires_dispatch()
            || self.requires_execution()
            || !self.has_lineage()
            || !self.is_release_handoff_evidence()
            || !self.is_release_ready_review()
            || !self.is_release_ready_disposition()
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionRow {
    pub sequence: usize,
    pub handoff_action_evidence_review_disposition_action_row_key: String,
    pub handoff_action_evidence_review_disposition_row_sequence: usize,
    pub handoff_action_evidence_review_disposition_row_key: String,
    pub handoff_action_evidence_review_row_key: String,
    pub handoff_action_evidence_row_key: String,
    pub handoff_action_row_key: String,
    pub handoff_row_key: String,
    pub readiness_evidence_review_disposition_action_readiness_execution_slot_key: String,
    pub readiness_evidence_review_disposition_action_key: String,
    pub work_order_key: String,
    pub ticket_key: String,
    pub check_sequence: usize,
    pub check_kind: IntegrationMeshReleaseReadinessCheckKind,
    pub status: IntegrationMeshReleaseReadinessStatus,
    pub handoff_lane: IntegrationMeshReleaseDispatchTicketHandoffLane,
    pub handoff_action_kind:
        IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind,
    pub disposition_action_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind,
    pub handoff_action_evidence_review_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind,
    pub handoff_action_evidence_review_disposition_action_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind,
    pub release_blocking: bool,
    pub operator_required: bool,
    pub review_required: bool,
    pub dispatch_required: bool,
    pub execution_required: bool,
    pub lineage_complete: bool,
    pub release_handoff_evidence: bool,
    pub release_ready_review: bool,
    pub handoff_action_evidence_reviews_ready: bool,
    pub release_ready_disposition: bool,
    pub release_ready_disposition_action: bool,
    pub ready_for_release_handoff: bool,
}

impl
    IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionRow
{
    pub fn from_disposition_row(
        sequence: usize,
        disposition_row: &IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionRow,
    ) -> Self {
        let release_ready_disposition_action = disposition_row.is_release_ready_disposition()
            && disposition_row.ready_to_release_handoff()
            && disposition_row.handoff_action_evidence_review_disposition_action_kind
                == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::ReleaseHandoff;

        Self {
            sequence,
            handoff_action_evidence_review_disposition_action_row_key: format!(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-evidence-review-disposition-action-{sequence:02}-{}-{}",
                disposition_row
                    .handoff_action_evidence_review_disposition_action_kind
                    .as_str(),
                disposition_row.check_kind.as_str()
            ),
            handoff_action_evidence_review_disposition_row_sequence: disposition_row.sequence,
            handoff_action_evidence_review_disposition_row_key: disposition_row
                .handoff_action_evidence_review_disposition_row_key
                .clone(),
            handoff_action_evidence_review_row_key: disposition_row
                .handoff_action_evidence_review_row_key
                .clone(),
            handoff_action_evidence_row_key: disposition_row
                .handoff_action_evidence_row_key
                .clone(),
            handoff_action_row_key: disposition_row.handoff_action_row_key.clone(),
            handoff_row_key: disposition_row.handoff_row_key.clone(),
            readiness_evidence_review_disposition_action_readiness_execution_slot_key:
                disposition_row
                    .readiness_evidence_review_disposition_action_readiness_execution_slot_key
                    .clone(),
            readiness_evidence_review_disposition_action_key: disposition_row
                .readiness_evidence_review_disposition_action_key
                .clone(),
            work_order_key: disposition_row.work_order_key.clone(),
            ticket_key: disposition_row.ticket_key.clone(),
            check_sequence: disposition_row.check_sequence,
            check_kind: disposition_row.check_kind,
            status: disposition_row.status,
            handoff_lane: disposition_row.handoff_lane,
            handoff_action_kind: disposition_row.handoff_action_kind,
            disposition_action_kind: disposition_row.disposition_action_kind,
            handoff_action_evidence_review_kind: disposition_row
                .handoff_action_evidence_review_kind,
            handoff_action_evidence_review_disposition_action_kind: disposition_row
                .handoff_action_evidence_review_disposition_action_kind,
            release_blocking: disposition_row.blocks_release(),
            operator_required: disposition_row.needs_operator(),
            review_required: disposition_row.needs_review(),
            dispatch_required: disposition_row.requires_dispatch(),
            execution_required: disposition_row.requires_execution(),
            lineage_complete: disposition_row.has_lineage(),
            release_handoff_evidence: disposition_row.is_release_handoff_evidence(),
            release_ready_review: disposition_row.is_release_ready_review(),
            handoff_action_evidence_reviews_ready: disposition_row
                .handoff_action_evidence_reviews_ready,
            release_ready_disposition: disposition_row.is_release_ready_disposition(),
            release_ready_disposition_action,
            ready_for_release_handoff: release_ready_disposition_action,
        }
    }

    pub fn blocks_release(&self) -> bool {
        self.release_blocking
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required
    }

    pub fn needs_review(&self) -> bool {
        self.review_required
    }

    pub fn requires_dispatch(&self) -> bool {
        self.dispatch_required
    }

    pub fn requires_execution(&self) -> bool {
        self.execution_required
    }

    pub fn has_lineage(&self) -> bool {
        self.lineage_complete
    }

    pub fn is_repair_action(&self) -> bool {
        self.handoff_action_evidence_review_disposition_action_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::ScheduleReleaseBlockerRepair
    }

    pub fn is_operator_handoff_action(&self) -> bool {
        self.handoff_action_evidence_review_disposition_action_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::CoordinateOperatorHandoff
    }

    pub fn is_review_action(&self) -> bool {
        self.handoff_action_evidence_review_disposition_action_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::CompleteEvidenceReview
    }

    pub fn is_lineage_action(&self) -> bool {
        self.handoff_action_evidence_review_disposition_action_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::RestoreEvidenceLineage
    }

    pub fn is_release_handoff_action(&self) -> bool {
        self.handoff_action_evidence_review_disposition_action_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::ReleaseHandoff
    }

    pub fn is_release_handoff_evidence(&self) -> bool {
        self.release_handoff_evidence
    }

    pub fn is_release_ready_review(&self) -> bool {
        self.release_ready_review
    }

    pub fn is_release_ready_disposition(&self) -> bool {
        self.release_ready_disposition && self.is_release_handoff_action()
    }

    pub fn is_release_ready_disposition_action(&self) -> bool {
        self.release_ready_disposition_action && self.is_release_handoff_action()
    }

    pub fn ready_to_release_handoff(&self) -> bool {
        self.ready_for_release_handoff
    }

    pub fn requires_attention(&self) -> bool {
        self.blocks_release()
            || self.needs_operator()
            || self.needs_review()
            || self.requires_dispatch()
            || self.requires_execution()
            || !self.has_lineage()
            || !self.is_release_handoff_evidence()
            || !self.is_release_ready_review()
            || !self.is_release_ready_disposition()
            || !self.is_release_ready_disposition_action()
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionSummary {
    pub release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_summary:
        Box<IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionSummary>,
    pub total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_rows:
        usize,
    pub total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows:
        usize,
    pub required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows:
        usize,
    pub repair_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows:
        usize,
    pub operator_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows:
        usize,
    pub operator_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows:
        usize,
    pub review_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows:
        usize,
    pub lineage_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows:
        usize,
    pub lineage_complete_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows:
        usize,
    pub release_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows:
        usize,
    pub release_handoff_evidence_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows:
        usize,
    pub release_ready_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows:
        usize,
    pub first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_row_key:
        Option<String>,
    pub first_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_row_key:
        Option<String>,
    pub first_repair_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_row_key:
        Option<String>,
    pub first_operator_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_row_key:
        Option<String>,
    pub first_review_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_row_key:
        Option<String>,
    pub first_lineage_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_row_key:
        Option<String>,
    pub first_release_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_row_key:
        Option<String>,
    pub first_release_ready_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_row_key:
        Option<String>,
    pub first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_row_key:
        Option<String>,
    pub first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_row_key:
        Option<String>,
    pub first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_row_key:
        Option<String>,
    pub first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_row_key:
        Option<String>,
    pub first_readiness_evidence_review_disposition_action_readiness_execution_handoff_row_key:
        Option<String>,
    pub first_readiness_evidence_review_disposition_action_readiness_execution_slot_key:
        Option<String>,
    pub first_readiness_evidence_review_disposition_action_key: Option<String>,
    pub first_work_order_key: Option<String>,
    pub first_ticket_key: Option<String>,
    pub first_handoff_action_kind: Option<
        IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind,
    >,
    pub first_handoff_action_evidence_review_disposition_action_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind,
    >,
    pub first_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub first_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub first_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_row_key:
        Option<String>,
    pub next_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_row_key:
        Option<String>,
    pub next_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_row_key:
        Option<String>,
    pub next_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_row_key:
        Option<String>,
    pub next_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_row_key:
        Option<String>,
    pub next_readiness_evidence_review_disposition_action_readiness_execution_handoff_row_key:
        Option<String>,
    pub next_readiness_evidence_review_disposition_action_readiness_execution_slot_key:
        Option<String>,
    pub next_readiness_evidence_review_disposition_action_key: Option<String>,
    pub next_work_order_key: Option<String>,
    pub next_ticket_key: Option<String>,
    pub next_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub next_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_dispositions_ready:
        bool,
    pub release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_actions_ready:
        bool,
}

impl
    IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionSummary
{
    pub fn from_summaries<'a>(
        release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_summary: IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionSummary,
        action_rows: impl IntoIterator<
            Item = &'a IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionRow,
        >,
    ) -> Self {
        let action_rows: Vec<&IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionRow> =
            action_rows.into_iter().collect();
        let first_row = action_rows.first().copied();
        let first_required_row = action_rows
            .iter()
            .copied()
            .find(|row| row.requires_attention());
        let first_repair_row = action_rows
            .iter()
            .copied()
            .find(|row| row.is_repair_action());
        let first_operator_row = action_rows
            .iter()
            .copied()
            .find(|row| row.is_operator_handoff_action());
        let first_review_row = action_rows
            .iter()
            .copied()
            .find(|row| row.is_review_action());
        let first_lineage_row = action_rows
            .iter()
            .copied()
            .find(|row| row.is_lineage_action());
        let first_release_handoff_row = action_rows
            .iter()
            .copied()
            .find(|row| row.is_release_handoff_action());
        let first_release_ready_row = action_rows
            .iter()
            .copied()
            .find(|row| row.is_release_ready_disposition_action());
        let next_row = first_required_row;
        let release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_dispositions_ready =
            release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_summary
                .ready_for_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_dispositions();
        let release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_actions_ready =
            release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_dispositions_ready
                && action_rows.len()
                    == release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_summary
                        .total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_rows
                && action_rows
                    .iter()
                    .all(|row| row.ready_to_release_handoff())
                && action_rows.iter().all(|row| !row.requires_attention());

        Self {
            total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_rows:
                release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_summary
                    .total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_rows,
            total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows:
                action_rows.len(),
            required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows:
                action_rows.iter().filter(|row| row.requires_attention()).count(),
            repair_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows:
                action_rows.iter().filter(|row| row.is_repair_action()).count(),
            operator_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows:
                action_rows.iter().filter(|row| row.is_operator_handoff_action()).count(),
            operator_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows:
                action_rows.iter().filter(|row| row.needs_operator()).count(),
            review_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows:
                action_rows.iter().filter(|row| row.is_review_action()).count(),
            lineage_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows:
                action_rows.iter().filter(|row| row.is_lineage_action()).count(),
            lineage_complete_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows:
                action_rows.iter().filter(|row| row.has_lineage()).count(),
            release_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows:
                action_rows.iter().filter(|row| row.is_release_handoff_action()).count(),
            release_handoff_evidence_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows:
                action_rows.iter().filter(|row| row.is_release_handoff_evidence()).count(),
            release_ready_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows:
                action_rows.iter().filter(|row| row.is_release_ready_disposition_action()).count(),
            first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_row_key:
                first_row.map(|row| row.handoff_action_evidence_review_disposition_action_row_key.clone()),
            first_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_row_key:
                first_required_row.map(|row| row.handoff_action_evidence_review_disposition_action_row_key.clone()),
            first_repair_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_row_key:
                first_repair_row.map(|row| row.handoff_action_evidence_review_disposition_action_row_key.clone()),
            first_operator_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_row_key:
                first_operator_row.map(|row| row.handoff_action_evidence_review_disposition_action_row_key.clone()),
            first_review_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_row_key:
                first_review_row.map(|row| row.handoff_action_evidence_review_disposition_action_row_key.clone()),
            first_lineage_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_row_key:
                first_lineage_row.map(|row| row.handoff_action_evidence_review_disposition_action_row_key.clone()),
            first_release_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_row_key:
                first_release_handoff_row.map(|row| row.handoff_action_evidence_review_disposition_action_row_key.clone()),
            first_release_ready_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_row_key:
                first_release_ready_row.map(|row| row.handoff_action_evidence_review_disposition_action_row_key.clone()),
            first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_row_key:
                first_row.map(|row| row.handoff_action_evidence_review_disposition_row_key.clone()),
            first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_row_key:
                first_row.map(|row| row.handoff_action_evidence_review_row_key.clone()),
            first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_row_key:
                first_row.map(|row| row.handoff_action_evidence_row_key.clone()),
            first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_row_key:
                first_row.map(|row| row.handoff_action_row_key.clone()),
            first_readiness_evidence_review_disposition_action_readiness_execution_handoff_row_key:
                first_row.map(|row| row.handoff_row_key.clone()),
            first_readiness_evidence_review_disposition_action_readiness_execution_slot_key:
                first_row.map(|row| {
                    row.readiness_evidence_review_disposition_action_readiness_execution_slot_key
                        .clone()
                }),
            first_readiness_evidence_review_disposition_action_key:
                first_row.map(|row| row.readiness_evidence_review_disposition_action_key.clone()),
            first_work_order_key: first_row.map(|row| row.work_order_key.clone()),
            first_ticket_key: first_row.map(|row| row.ticket_key.clone()),
            first_handoff_action_kind: first_row.map(|row| row.handoff_action_kind),
            first_handoff_action_evidence_review_disposition_action_kind:
                first_row.map(|row| row.handoff_action_evidence_review_disposition_action_kind),
            first_handoff_lane: first_row.map(|row| row.handoff_lane),
            first_check_kind: first_row.map(|row| row.check_kind),
            first_check_status: first_row.map(|row| row.status),
            next_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_row_key:
                next_row.map(|row| row.handoff_action_evidence_review_disposition_action_row_key.clone()),
            next_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_row_key:
                next_row.map(|row| row.handoff_action_evidence_review_disposition_row_key.clone()),
            next_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_row_key:
                next_row.map(|row| row.handoff_action_evidence_review_row_key.clone()),
            next_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_row_key:
                next_row.map(|row| row.handoff_action_evidence_row_key.clone()),
            next_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_row_key:
                next_row.map(|row| row.handoff_action_row_key.clone()),
            next_readiness_evidence_review_disposition_action_readiness_execution_handoff_row_key:
                next_row.map(|row| row.handoff_row_key.clone()),
            next_readiness_evidence_review_disposition_action_readiness_execution_slot_key:
                next_row.map(|row| {
                    row.readiness_evidence_review_disposition_action_readiness_execution_slot_key
                        .clone()
                }),
            next_readiness_evidence_review_disposition_action_key:
                next_row.map(|row| row.readiness_evidence_review_disposition_action_key.clone()),
            next_work_order_key: next_row.map(|row| row.work_order_key.clone()),
            next_ticket_key: next_row.map(|row| row.ticket_key.clone()),
            next_check_kind: next_row.map(|row| row.check_kind),
            next_check_status: next_row.map(|row| row.status),
            next_handoff_lane: next_row.map(|row| row.handoff_lane),
            release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_dispositions_ready,
            release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_actions_ready,
            release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_summary:
                Box::new(
                    release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_summary,
                ),
        }
    }

    pub fn has_handoff_action_evidence_review_disposition_action_rows(&self) -> bool {
        self.total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows > 0
    }

    pub fn ready_for_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_actions(
        &self,
    ) -> bool {
        self.release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_dispositions_ready
            && self.release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_actions_ready
            && !self.requires_attention()
    }

    pub fn has_required_actions(&self) -> bool {
        self.required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows
            > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.repair_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows
            > 0
            || self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_summary
                .has_blockers()
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows
            > 0
            || self.operator_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows
                > 0
            || self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_summary
                .needs_operator()
    }

    pub fn has_review_work(&self) -> bool {
        self.review_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows
            > 0
            || self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_summary
                .has_review_work()
    }

    pub fn has_lineage_actions(&self) -> bool {
        self.lineage_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows
            > 0
            || self.lineage_complete_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows
                < self.total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows
            || self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_summary
                .has_lineage_repairs()
    }

    pub fn has_release_handoff_actions(&self) -> bool {
        self.release_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows
            > 0
    }

    pub fn has_release_handoff_evidence(&self) -> bool {
        self.release_handoff_evidence_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows
            > 0
            || self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_summary
                .has_release_handoff_dispositions()
    }

    pub fn has_release_ready_actions(&self) -> bool {
        self.release_ready_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows
            > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.has_required_actions()
            || self.has_blockers()
            || self.needs_operator()
            || self.has_review_work()
            || self.has_lineage_actions()
            || self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_summary
                .requires_attention()
            || !self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_actions_ready
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessRow {
    pub sequence: usize,
    pub handoff_action_evidence_review_disposition_action_readiness_row_key: String,
    pub handoff_action_evidence_review_disposition_action_row_sequence: usize,
    pub handoff_action_evidence_review_disposition_action_row_key: String,
    pub handoff_action_evidence_review_disposition_row_key: String,
    pub handoff_action_evidence_review_row_key: String,
    pub handoff_action_evidence_row_key: String,
    pub handoff_action_row_key: String,
    pub handoff_row_key: String,
    pub readiness_evidence_review_disposition_action_readiness_execution_slot_key: String,
    pub readiness_evidence_review_disposition_action_key: String,
    pub work_order_key: String,
    pub ticket_key: String,
    pub check_sequence: usize,
    pub check_kind: IntegrationMeshReleaseReadinessCheckKind,
    pub status: IntegrationMeshReleaseReadinessStatus,
    pub handoff_lane: IntegrationMeshReleaseDispatchTicketHandoffLane,
    pub handoff_action_kind:
        IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind,
    pub disposition_action_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind,
    pub handoff_action_evidence_review_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind,
    pub handoff_action_evidence_review_disposition_action_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind,
    pub release_blocking: bool,
    pub operator_required: bool,
    pub review_required: bool,
    pub dispatch_required: bool,
    pub execution_required: bool,
    pub lineage_complete: bool,
    pub release_handoff_evidence: bool,
    pub release_ready_review: bool,
    pub handoff_action_evidence_reviews_ready: bool,
    pub release_ready_disposition: bool,
    pub release_ready_disposition_action: bool,
    pub handoff_action_evidence_review_disposition_actions_ready: bool,
    pub ready_for_release_handoff: bool,
}

impl
    IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessRow
{
    pub fn from_disposition_action_row(
        sequence: usize,
        action_row: &IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionRow,
        handoff_action_evidence_review_disposition_actions_ready: bool,
    ) -> Self {
        let ready_for_release_handoff = handoff_action_evidence_review_disposition_actions_ready
            && action_row.ready_to_release_handoff()
            && action_row.is_release_ready_disposition_action();

        Self {
            sequence,
            handoff_action_evidence_review_disposition_action_readiness_row_key: format!(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-evidence-review-disposition-action-readiness-{sequence:02}-{}-{}",
                action_row
                    .handoff_action_evidence_review_disposition_action_kind
                    .as_str(),
                action_row.check_kind.as_str()
            ),
            handoff_action_evidence_review_disposition_action_row_sequence: action_row.sequence,
            handoff_action_evidence_review_disposition_action_row_key: action_row
                .handoff_action_evidence_review_disposition_action_row_key
                .clone(),
            handoff_action_evidence_review_disposition_row_key: action_row
                .handoff_action_evidence_review_disposition_row_key
                .clone(),
            handoff_action_evidence_review_row_key: action_row
                .handoff_action_evidence_review_row_key
                .clone(),
            handoff_action_evidence_row_key: action_row.handoff_action_evidence_row_key.clone(),
            handoff_action_row_key: action_row.handoff_action_row_key.clone(),
            handoff_row_key: action_row.handoff_row_key.clone(),
            readiness_evidence_review_disposition_action_readiness_execution_slot_key: action_row
                .readiness_evidence_review_disposition_action_readiness_execution_slot_key
                .clone(),
            readiness_evidence_review_disposition_action_key: action_row
                .readiness_evidence_review_disposition_action_key
                .clone(),
            work_order_key: action_row.work_order_key.clone(),
            ticket_key: action_row.ticket_key.clone(),
            check_sequence: action_row.check_sequence,
            check_kind: action_row.check_kind,
            status: action_row.status,
            handoff_lane: action_row.handoff_lane,
            handoff_action_kind: action_row.handoff_action_kind,
            disposition_action_kind: action_row.disposition_action_kind,
            handoff_action_evidence_review_kind: action_row.handoff_action_evidence_review_kind,
            handoff_action_evidence_review_disposition_action_kind: action_row
                .handoff_action_evidence_review_disposition_action_kind,
            release_blocking: action_row.blocks_release(),
            operator_required: action_row.needs_operator(),
            review_required: action_row.needs_review(),
            dispatch_required: action_row.requires_dispatch(),
            execution_required: action_row.requires_execution(),
            lineage_complete: action_row.has_lineage(),
            release_handoff_evidence: action_row.is_release_handoff_evidence(),
            release_ready_review: action_row.is_release_ready_review(),
            handoff_action_evidence_reviews_ready: action_row.handoff_action_evidence_reviews_ready,
            release_ready_disposition: action_row.is_release_ready_disposition(),
            release_ready_disposition_action: action_row.is_release_ready_disposition_action(),
            handoff_action_evidence_review_disposition_actions_ready,
            ready_for_release_handoff,
        }
    }

    pub fn blocks_release(&self) -> bool {
        self.release_blocking
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required
    }

    pub fn needs_review(&self) -> bool {
        self.review_required
    }

    pub fn requires_dispatch(&self) -> bool {
        self.dispatch_required
    }

    pub fn requires_execution(&self) -> bool {
        self.execution_required
    }

    pub fn has_lineage(&self) -> bool {
        self.lineage_complete
    }

    pub fn is_repair_readiness(&self) -> bool {
        self.handoff_action_evidence_review_disposition_action_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::ScheduleReleaseBlockerRepair
    }

    pub fn is_operator_handoff_readiness(&self) -> bool {
        self.handoff_action_evidence_review_disposition_action_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::CoordinateOperatorHandoff
    }

    pub fn is_review_readiness(&self) -> bool {
        self.handoff_action_evidence_review_disposition_action_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::CompleteEvidenceReview
    }

    pub fn is_lineage_readiness(&self) -> bool {
        self.handoff_action_evidence_review_disposition_action_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::RestoreEvidenceLineage
    }

    pub fn is_release_handoff_readiness(&self) -> bool {
        self.handoff_action_evidence_review_disposition_action_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::ReleaseHandoff
    }

    pub fn is_release_handoff_evidence(&self) -> bool {
        self.release_handoff_evidence
    }

    pub fn is_release_ready_review(&self) -> bool {
        self.release_ready_review
    }

    pub fn is_release_ready_disposition(&self) -> bool {
        self.release_ready_disposition && self.is_release_handoff_readiness()
    }

    pub fn is_release_ready_disposition_action(&self) -> bool {
        self.release_ready_disposition_action && self.is_release_handoff_readiness()
    }

    pub fn ready_to_release_handoff(&self) -> bool {
        self.ready_for_release_handoff
    }

    pub fn requires_attention(&self) -> bool {
        self.blocks_release()
            || self.needs_operator()
            || self.needs_review()
            || self.requires_dispatch()
            || self.requires_execution()
            || !self.has_lineage()
            || !self.is_release_handoff_evidence()
            || !self.is_release_ready_review()
            || !self.is_release_ready_disposition()
            || !self.is_release_ready_disposition_action()
            || !self.handoff_action_evidence_review_disposition_actions_ready
            || !self.ready_to_release_handoff()
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessSummary {
    pub release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_summary:
        Box<IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionSummary>,
    pub total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows:
        usize,
    pub total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows:
        usize,
    pub required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows:
        usize,
    pub repair_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows:
        usize,
    pub operator_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows:
        usize,
    pub operator_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows:
        usize,
    pub review_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows:
        usize,
    pub dispatch_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows:
        usize,
    pub execution_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows:
        usize,
    pub lineage_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows:
        usize,
    pub lineage_complete_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows:
        usize,
    pub release_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows:
        usize,
    pub release_handoff_evidence_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows:
        usize,
    pub release_ready_review_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows:
        usize,
    pub release_ready_disposition_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows:
        usize,
    pub release_ready_disposition_action_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows:
        usize,
    pub release_ready_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows:
        usize,
    pub first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_row_key:
        Option<String>,
    pub first_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_row_key:
        Option<String>,
    pub first_repair_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_row_key:
        Option<String>,
    pub first_operator_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_row_key:
        Option<String>,
    pub first_review_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_row_key:
        Option<String>,
    pub first_lineage_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_row_key:
        Option<String>,
    pub first_release_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_row_key:
        Option<String>,
    pub first_release_ready_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_row_key:
        Option<String>,
    pub first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_row_key:
        Option<String>,
    pub first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_row_key:
        Option<String>,
    pub first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_row_key:
        Option<String>,
    pub first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_row_key:
        Option<String>,
    pub first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_row_key:
        Option<String>,
    pub first_readiness_evidence_review_disposition_action_readiness_execution_handoff_row_key:
        Option<String>,
    pub first_readiness_evidence_review_disposition_action_readiness_execution_slot_key:
        Option<String>,
    pub first_readiness_evidence_review_disposition_action_key: Option<String>,
    pub first_work_order_key: Option<String>,
    pub first_ticket_key: Option<String>,
    pub first_handoff_action_kind: Option<
        IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind,
    >,
    pub first_handoff_action_evidence_review_disposition_action_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind,
    >,
    pub first_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub first_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub first_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_row_key:
        Option<String>,
    pub next_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_row_key:
        Option<String>,
    pub next_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_row_key:
        Option<String>,
    pub next_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_row_key:
        Option<String>,
    pub next_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_row_key:
        Option<String>,
    pub next_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_row_key:
        Option<String>,
    pub next_readiness_evidence_review_disposition_action_readiness_execution_handoff_row_key:
        Option<String>,
    pub next_readiness_evidence_review_disposition_action_readiness_execution_slot_key:
        Option<String>,
    pub next_readiness_evidence_review_disposition_action_key: Option<String>,
    pub next_work_order_key: Option<String>,
    pub next_ticket_key: Option<String>,
    pub next_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub next_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_actions_ready:
        bool,
    pub release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_ready:
        bool,
}

impl
    IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessSummary
{
    pub fn from_summaries<'a>(
        release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_summary: IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionSummary,
        readiness_rows: impl IntoIterator<
            Item = &'a IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessRow,
        >,
    ) -> Self {
        let readiness_rows: Vec<&IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessRow> =
            readiness_rows.into_iter().collect();
        let first_row = readiness_rows.first().copied();
        let first_required_row = readiness_rows
            .iter()
            .copied()
            .find(|row| row.requires_attention());
        let first_repair_row = readiness_rows
            .iter()
            .copied()
            .find(|row| row.is_repair_readiness());
        let first_operator_row = readiness_rows
            .iter()
            .copied()
            .find(|row| row.is_operator_handoff_readiness());
        let first_review_row = readiness_rows
            .iter()
            .copied()
            .find(|row| row.is_review_readiness());
        let first_lineage_row = readiness_rows
            .iter()
            .copied()
            .find(|row| row.is_lineage_readiness());
        let first_release_handoff_row = readiness_rows
            .iter()
            .copied()
            .find(|row| row.is_release_handoff_readiness());
        let first_release_ready_row = readiness_rows
            .iter()
            .copied()
            .find(|row| row.ready_to_release_handoff());
        let next_row = first_required_row;
        let release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_actions_ready =
            release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_summary
                .ready_for_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_actions();
        let release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_ready =
            release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_actions_ready
                && readiness_rows.len()
                    == release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_summary
                        .total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows
                && readiness_rows
                    .iter()
                    .all(|row| row.ready_to_release_handoff())
                && readiness_rows.iter().all(|row| !row.requires_attention());

        Self {
            total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows:
                release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_summary
                    .total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows,
            total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows:
                readiness_rows.len(),
            required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows:
                readiness_rows.iter().filter(|row| row.requires_attention()).count(),
            repair_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows:
                readiness_rows.iter().filter(|row| row.is_repair_readiness()).count(),
            operator_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows:
                readiness_rows.iter().filter(|row| row.is_operator_handoff_readiness()).count(),
            operator_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows:
                readiness_rows.iter().filter(|row| row.needs_operator()).count(),
            review_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows:
                readiness_rows.iter().filter(|row| row.is_review_readiness()).count(),
            dispatch_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows:
                readiness_rows.iter().filter(|row| row.requires_dispatch()).count(),
            execution_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows:
                readiness_rows.iter().filter(|row| row.requires_execution()).count(),
            lineage_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows:
                readiness_rows.iter().filter(|row| row.is_lineage_readiness()).count(),
            lineage_complete_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows:
                readiness_rows.iter().filter(|row| row.has_lineage()).count(),
            release_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows:
                readiness_rows.iter().filter(|row| row.is_release_handoff_readiness()).count(),
            release_handoff_evidence_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows:
                readiness_rows.iter().filter(|row| row.is_release_handoff_evidence()).count(),
            release_ready_review_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows:
                readiness_rows.iter().filter(|row| row.is_release_ready_review()).count(),
            release_ready_disposition_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows:
                readiness_rows.iter().filter(|row| row.is_release_ready_disposition()).count(),
            release_ready_disposition_action_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows:
                readiness_rows.iter().filter(|row| row.is_release_ready_disposition_action()).count(),
            release_ready_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows:
                readiness_rows.iter().filter(|row| row.ready_to_release_handoff()).count(),
            first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_row_key:
                first_row.map(|row| row.handoff_action_evidence_review_disposition_action_readiness_row_key.clone()),
            first_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_row_key:
                first_required_row.map(|row| row.handoff_action_evidence_review_disposition_action_readiness_row_key.clone()),
            first_repair_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_row_key:
                first_repair_row.map(|row| row.handoff_action_evidence_review_disposition_action_readiness_row_key.clone()),
            first_operator_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_row_key:
                first_operator_row.map(|row| row.handoff_action_evidence_review_disposition_action_readiness_row_key.clone()),
            first_review_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_row_key:
                first_review_row.map(|row| row.handoff_action_evidence_review_disposition_action_readiness_row_key.clone()),
            first_lineage_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_row_key:
                first_lineage_row.map(|row| row.handoff_action_evidence_review_disposition_action_readiness_row_key.clone()),
            first_release_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_row_key:
                first_release_handoff_row.map(|row| row.handoff_action_evidence_review_disposition_action_readiness_row_key.clone()),
            first_release_ready_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_row_key:
                first_release_ready_row.map(|row| row.handoff_action_evidence_review_disposition_action_readiness_row_key.clone()),
            first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_row_key:
                first_row.map(|row| row.handoff_action_evidence_review_disposition_action_row_key.clone()),
            first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_row_key:
                first_row.map(|row| row.handoff_action_evidence_review_disposition_row_key.clone()),
            first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_row_key:
                first_row.map(|row| row.handoff_action_evidence_review_row_key.clone()),
            first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_row_key:
                first_row.map(|row| row.handoff_action_evidence_row_key.clone()),
            first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_row_key:
                first_row.map(|row| row.handoff_action_row_key.clone()),
            first_readiness_evidence_review_disposition_action_readiness_execution_handoff_row_key:
                first_row.map(|row| row.handoff_row_key.clone()),
            first_readiness_evidence_review_disposition_action_readiness_execution_slot_key:
                first_row.map(|row| row.readiness_evidence_review_disposition_action_readiness_execution_slot_key.clone()),
            first_readiness_evidence_review_disposition_action_key:
                first_row.map(|row| row.readiness_evidence_review_disposition_action_key.clone()),
            first_work_order_key: first_row.map(|row| row.work_order_key.clone()),
            first_ticket_key: first_row.map(|row| row.ticket_key.clone()),
            first_handoff_action_kind: first_row.map(|row| row.handoff_action_kind),
            first_handoff_action_evidence_review_disposition_action_kind:
                first_row.map(|row| row.handoff_action_evidence_review_disposition_action_kind),
            first_handoff_lane: first_row.map(|row| row.handoff_lane),
            first_check_kind: first_row.map(|row| row.check_kind),
            first_check_status: first_row.map(|row| row.status),
            next_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_row_key:
                next_row.map(|row| row.handoff_action_evidence_review_disposition_action_readiness_row_key.clone()),
            next_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_row_key:
                next_row.map(|row| row.handoff_action_evidence_review_disposition_action_row_key.clone()),
            next_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_row_key:
                next_row.map(|row| row.handoff_action_evidence_review_disposition_row_key.clone()),
            next_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_row_key:
                next_row.map(|row| row.handoff_action_evidence_review_row_key.clone()),
            next_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_row_key:
                next_row.map(|row| row.handoff_action_evidence_row_key.clone()),
            next_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_row_key:
                next_row.map(|row| row.handoff_action_row_key.clone()),
            next_readiness_evidence_review_disposition_action_readiness_execution_handoff_row_key:
                next_row.map(|row| row.handoff_row_key.clone()),
            next_readiness_evidence_review_disposition_action_readiness_execution_slot_key:
                next_row.map(|row| row.readiness_evidence_review_disposition_action_readiness_execution_slot_key.clone()),
            next_readiness_evidence_review_disposition_action_key:
                next_row.map(|row| row.readiness_evidence_review_disposition_action_key.clone()),
            next_work_order_key: next_row.map(|row| row.work_order_key.clone()),
            next_ticket_key: next_row.map(|row| row.ticket_key.clone()),
            next_check_kind: next_row.map(|row| row.check_kind),
            next_check_status: next_row.map(|row| row.status),
            next_handoff_lane: next_row.map(|row| row.handoff_lane),
            release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_actions_ready,
            release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_ready,
            release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_summary:
                Box::new(
                    release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_summary,
                ),
        }
    }

    pub fn has_handoff_action_evidence_review_disposition_action_readiness_rows(&self) -> bool {
        self.total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows > 0
    }

    pub fn ready_for_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness(
        &self,
    ) -> bool {
        self.release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_actions_ready
            && self.release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_ready
            && !self.requires_attention()
    }

    pub fn has_required_readiness(&self) -> bool {
        self.required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows
            > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.repair_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows
            > 0
            || self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_summary
                .has_blockers()
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows
            > 0
            || self.operator_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows
                > 0
            || self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_summary
                .needs_operator()
    }

    pub fn has_review_work(&self) -> bool {
        self.review_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows
            > 0
            || self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_summary
                .has_review_work()
    }

    pub fn has_lineage_readiness(&self) -> bool {
        self.lineage_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows
            > 0
            || self.lineage_complete_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows
                < self.total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows
            || self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_summary
                .has_lineage_actions()
    }

    pub fn has_release_handoff_readiness(&self) -> bool {
        self.release_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows
            > 0
    }

    pub fn has_release_handoff_evidence(&self) -> bool {
        self.release_handoff_evidence_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows
            > 0
    }

    pub fn has_release_ready_readiness(&self) -> bool {
        self.release_ready_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows
            > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.has_required_readiness()
            || self.has_blockers()
            || self.needs_operator()
            || self.has_review_work()
            || self.dispatch_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows > 0
            || self.execution_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows > 0
            || self.has_lineage_readiness()
            || self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_summary
                .requires_attention()
            || !self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_ready
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionSlot {
    pub sequence: usize,
    pub handoff_action_evidence_review_disposition_action_readiness_execution_slot_key: String,
    pub handoff_action_evidence_review_disposition_action_readiness_row_sequence: usize,
    pub handoff_action_evidence_review_disposition_action_readiness_row_key: String,
    pub handoff_action_evidence_review_disposition_action_row_key: String,
    pub handoff_action_evidence_review_disposition_row_key: String,
    pub handoff_action_evidence_review_row_key: String,
    pub handoff_action_evidence_row_key: String,
    pub handoff_action_row_key: String,
    pub handoff_row_key: String,
    pub readiness_evidence_review_disposition_action_readiness_execution_slot_key: String,
    pub readiness_evidence_review_disposition_action_key: String,
    pub work_order_key: String,
    pub ticket_key: String,
    pub check_sequence: usize,
    pub check_kind: IntegrationMeshReleaseReadinessCheckKind,
    pub status: IntegrationMeshReleaseReadinessStatus,
    pub handoff_lane: IntegrationMeshReleaseDispatchTicketHandoffLane,
    pub handoff_action_kind:
        IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind,
    pub disposition_action_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind,
    pub handoff_action_evidence_review_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind,
    pub handoff_action_evidence_review_disposition_action_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind,
    pub release_blocking: bool,
    pub operator_required: bool,
    pub review_required: bool,
    pub dispatch_required: bool,
    pub execution_required: bool,
    pub lineage_complete: bool,
    pub release_handoff_evidence: bool,
    pub release_ready_review: bool,
    pub release_ready_disposition: bool,
    pub release_ready_disposition_action: bool,
    pub handoff_action_evidence_review_disposition_action_readiness_ready: bool,
    pub ready_for_execution_handoff: bool,
}

impl
    IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionSlot
{
    pub fn from_readiness_row(
        sequence: usize,
        readiness_row: &IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessRow,
        handoff_action_evidence_review_disposition_action_readiness_ready: bool,
    ) -> Self {
        let ready_for_execution_handoff =
            handoff_action_evidence_review_disposition_action_readiness_ready
                && readiness_row.ready_to_release_handoff()
                && !readiness_row.requires_attention();

        Self {
            sequence,
            handoff_action_evidence_review_disposition_action_readiness_execution_slot_key:
                format!(
                    "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-execution-slot-{sequence:02}-{}-{}",
                    readiness_row
                        .handoff_action_evidence_review_disposition_action_kind
                        .as_str(),
                    readiness_row.check_kind.as_str()
                ),
            handoff_action_evidence_review_disposition_action_readiness_row_sequence:
                readiness_row.sequence,
            handoff_action_evidence_review_disposition_action_readiness_row_key: readiness_row
                .handoff_action_evidence_review_disposition_action_readiness_row_key
                .clone(),
            handoff_action_evidence_review_disposition_action_row_key: readiness_row
                .handoff_action_evidence_review_disposition_action_row_key
                .clone(),
            handoff_action_evidence_review_disposition_row_key: readiness_row
                .handoff_action_evidence_review_disposition_row_key
                .clone(),
            handoff_action_evidence_review_row_key: readiness_row
                .handoff_action_evidence_review_row_key
                .clone(),
            handoff_action_evidence_row_key: readiness_row
                .handoff_action_evidence_row_key
                .clone(),
            handoff_action_row_key: readiness_row.handoff_action_row_key.clone(),
            handoff_row_key: readiness_row.handoff_row_key.clone(),
            readiness_evidence_review_disposition_action_readiness_execution_slot_key:
                readiness_row
                    .readiness_evidence_review_disposition_action_readiness_execution_slot_key
                    .clone(),
            readiness_evidence_review_disposition_action_key: readiness_row
                .readiness_evidence_review_disposition_action_key
                .clone(),
            work_order_key: readiness_row.work_order_key.clone(),
            ticket_key: readiness_row.ticket_key.clone(),
            check_sequence: readiness_row.check_sequence,
            check_kind: readiness_row.check_kind,
            status: readiness_row.status,
            handoff_lane: readiness_row.handoff_lane,
            handoff_action_kind: readiness_row.handoff_action_kind,
            disposition_action_kind: readiness_row.disposition_action_kind,
            handoff_action_evidence_review_kind: readiness_row.handoff_action_evidence_review_kind,
            handoff_action_evidence_review_disposition_action_kind: readiness_row
                .handoff_action_evidence_review_disposition_action_kind,
            release_blocking: readiness_row.blocks_release(),
            operator_required: readiness_row.needs_operator(),
            review_required: readiness_row.needs_review(),
            dispatch_required: readiness_row.requires_dispatch(),
            execution_required: readiness_row.requires_execution(),
            lineage_complete: readiness_row.has_lineage(),
            release_handoff_evidence: readiness_row.is_release_handoff_evidence(),
            release_ready_review: readiness_row.is_release_ready_review(),
            release_ready_disposition: readiness_row.is_release_ready_disposition(),
            release_ready_disposition_action: readiness_row.is_release_ready_disposition_action(),
            handoff_action_evidence_review_disposition_action_readiness_ready,
            ready_for_execution_handoff,
        }
    }

    pub fn blocks_release(&self) -> bool {
        self.release_blocking
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required
    }

    pub fn needs_review(&self) -> bool {
        self.review_required
    }

    pub fn requires_dispatch(&self) -> bool {
        self.dispatch_required
    }

    pub fn requires_execution(&self) -> bool {
        self.execution_required
    }

    pub fn has_lineage(&self) -> bool {
        self.lineage_complete
    }

    pub fn is_repair_slot(&self) -> bool {
        self.handoff_action_evidence_review_disposition_action_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::ScheduleReleaseBlockerRepair
    }

    pub fn is_operator_handoff_slot(&self) -> bool {
        self.handoff_action_evidence_review_disposition_action_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::CoordinateOperatorHandoff
    }

    pub fn is_review_slot(&self) -> bool {
        self.handoff_action_evidence_review_disposition_action_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::CompleteEvidenceReview
    }

    pub fn is_lineage_slot(&self) -> bool {
        self.handoff_action_evidence_review_disposition_action_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::RestoreEvidenceLineage
    }

    pub fn is_release_handoff_slot(&self) -> bool {
        self.handoff_action_evidence_review_disposition_action_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::ReleaseHandoff
    }

    pub fn ready_for_execution_handoff(&self) -> bool {
        self.ready_for_execution_handoff
    }

    pub fn requires_attention(&self) -> bool {
        self.blocks_release()
            || self.needs_operator()
            || self.needs_review()
            || self.requires_dispatch()
            || self.requires_execution()
            || !self.has_lineage()
            || !self.release_handoff_evidence
            || !self.release_ready_review
            || !self.release_ready_disposition
            || !self.release_ready_disposition_action
            || !self.handoff_action_evidence_review_disposition_action_readiness_ready
            || !self.ready_for_execution_handoff()
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionSlotSummary {
    pub total_handoff_action_evidence_review_disposition_action_readiness_rows: usize,
    pub total_handoff_action_evidence_review_disposition_action_readiness_execution_slots: usize,
    pub required_handoff_action_evidence_review_disposition_action_readiness_execution_slots:
        usize,
    pub repair_handoff_action_evidence_review_disposition_action_readiness_execution_slots: usize,
    pub operator_handoff_action_evidence_review_disposition_action_readiness_execution_slots:
        usize,
    pub operator_required_handoff_action_evidence_review_disposition_action_readiness_execution_slots:
        usize,
    pub review_handoff_action_evidence_review_disposition_action_readiness_execution_slots: usize,
    pub dispatch_required_handoff_action_evidence_review_disposition_action_readiness_execution_slots:
        usize,
    pub execution_required_handoff_action_evidence_review_disposition_action_readiness_execution_slots:
        usize,
    pub lineage_handoff_action_evidence_review_disposition_action_readiness_execution_slots: usize,
    pub lineage_complete_handoff_action_evidence_review_disposition_action_readiness_execution_slots:
        usize,
    pub release_handoff_action_evidence_review_disposition_action_readiness_execution_slots: usize,
    pub release_handoff_evidence_handoff_action_evidence_review_disposition_action_readiness_execution_slots:
        usize,
    pub release_ready_review_handoff_action_evidence_review_disposition_action_readiness_execution_slots:
        usize,
    pub release_ready_disposition_handoff_action_evidence_review_disposition_action_readiness_execution_slots:
        usize,
    pub release_ready_disposition_action_handoff_action_evidence_review_disposition_action_readiness_execution_slots:
        usize,
    pub release_ready_handoff_action_evidence_review_disposition_action_readiness_execution_slots:
        usize,
    pub first_handoff_action_evidence_review_disposition_action_readiness_execution_slot_key:
        Option<String>,
    pub first_required_handoff_action_evidence_review_disposition_action_readiness_execution_slot_key:
        Option<String>,
    pub first_repair_handoff_action_evidence_review_disposition_action_readiness_execution_slot_key:
        Option<String>,
    pub first_operator_handoff_action_evidence_review_disposition_action_readiness_execution_slot_key:
        Option<String>,
    pub first_review_handoff_action_evidence_review_disposition_action_readiness_execution_slot_key:
        Option<String>,
    pub first_lineage_handoff_action_evidence_review_disposition_action_readiness_execution_slot_key:
        Option<String>,
    pub first_release_handoff_action_evidence_review_disposition_action_readiness_execution_slot_key:
        Option<String>,
    pub first_release_ready_handoff_action_evidence_review_disposition_action_readiness_execution_slot_key:
        Option<String>,
    pub first_handoff_action_evidence_review_disposition_action_readiness_row_key:
        Option<String>,
    pub first_handoff_action_evidence_review_disposition_action_row_key: Option<String>,
    pub first_handoff_action_evidence_review_disposition_row_key: Option<String>,
    pub first_handoff_action_evidence_review_row_key: Option<String>,
    pub first_handoff_action_evidence_row_key: Option<String>,
    pub first_handoff_action_row_key: Option<String>,
    pub first_handoff_row_key: Option<String>,
    pub first_readiness_evidence_review_disposition_action_key: Option<String>,
    pub first_work_order_key: Option<String>,
    pub first_ticket_key: Option<String>,
    pub first_handoff_action_kind: Option<
        IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind,
    >,
    pub first_handoff_action_evidence_review_disposition_action_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind,
    >,
    pub first_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub first_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub first_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_handoff_action_evidence_review_disposition_action_readiness_execution_slot_key:
        Option<String>,
    pub next_handoff_action_evidence_review_disposition_action_readiness_row_key:
        Option<String>,
    pub next_handoff_action_evidence_review_disposition_action_row_key: Option<String>,
    pub next_handoff_action_evidence_review_disposition_row_key: Option<String>,
    pub next_handoff_action_evidence_review_row_key: Option<String>,
    pub next_handoff_action_evidence_row_key: Option<String>,
    pub next_handoff_action_row_key: Option<String>,
    pub next_handoff_row_key: Option<String>,
    pub next_readiness_evidence_review_disposition_action_key: Option<String>,
    pub next_work_order_key: Option<String>,
    pub next_ticket_key: Option<String>,
    pub next_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub next_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_ready:
        bool,
    pub release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slots_ready:
        bool,
}

impl
    IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionSlotSummary
{
    pub fn from_slots<'a>(
        total_handoff_action_evidence_review_disposition_action_readiness_rows: usize,
        release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_ready: bool,
        slots: impl IntoIterator<
            Item = &'a IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionSlot,
        >,
    ) -> Self {
        let slots: Vec<&IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionSlot> =
            slots.into_iter().collect();
        let first_slot = slots.first().copied();
        let first_required_slot = slots
            .iter()
            .copied()
            .find(|slot| slot.requires_attention());
        let first_repair_slot = slots.iter().copied().find(|slot| slot.is_repair_slot());
        let first_operator_slot = slots
            .iter()
            .copied()
            .find(|slot| slot.is_operator_handoff_slot());
        let first_review_slot = slots.iter().copied().find(|slot| slot.is_review_slot());
        let first_lineage_slot = slots.iter().copied().find(|slot| slot.is_lineage_slot());
        let first_release_handoff_slot = slots
            .iter()
            .copied()
            .find(|slot| slot.is_release_handoff_slot());
        let first_release_ready_slot = slots
            .iter()
            .copied()
            .find(|slot| slot.ready_for_execution_handoff());
        let next_slot = first_required_slot;
        let release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slots_ready =
            release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_ready
                && slots.len()
                    == total_handoff_action_evidence_review_disposition_action_readiness_rows
                && slots
                    .iter()
                    .all(|slot| slot.ready_for_execution_handoff())
                && slots.iter().all(|slot| !slot.requires_attention());

        Self {
            total_handoff_action_evidence_review_disposition_action_readiness_rows,
            total_handoff_action_evidence_review_disposition_action_readiness_execution_slots:
                slots.len(),
            required_handoff_action_evidence_review_disposition_action_readiness_execution_slots:
                slots.iter().filter(|slot| slot.requires_attention()).count(),
            repair_handoff_action_evidence_review_disposition_action_readiness_execution_slots:
                slots.iter().filter(|slot| slot.is_repair_slot()).count(),
            operator_handoff_action_evidence_review_disposition_action_readiness_execution_slots:
                slots
                    .iter()
                    .filter(|slot| slot.is_operator_handoff_slot())
                    .count(),
            operator_required_handoff_action_evidence_review_disposition_action_readiness_execution_slots:
                slots.iter().filter(|slot| slot.needs_operator()).count(),
            review_handoff_action_evidence_review_disposition_action_readiness_execution_slots:
                slots.iter().filter(|slot| slot.is_review_slot()).count(),
            dispatch_required_handoff_action_evidence_review_disposition_action_readiness_execution_slots:
                slots.iter().filter(|slot| slot.requires_dispatch()).count(),
            execution_required_handoff_action_evidence_review_disposition_action_readiness_execution_slots:
                slots.iter().filter(|slot| slot.requires_execution()).count(),
            lineage_handoff_action_evidence_review_disposition_action_readiness_execution_slots:
                slots.iter().filter(|slot| slot.is_lineage_slot()).count(),
            lineage_complete_handoff_action_evidence_review_disposition_action_readiness_execution_slots:
                slots.iter().filter(|slot| slot.has_lineage()).count(),
            release_handoff_action_evidence_review_disposition_action_readiness_execution_slots:
                slots
                    .iter()
                    .filter(|slot| slot.is_release_handoff_slot())
                    .count(),
            release_handoff_evidence_handoff_action_evidence_review_disposition_action_readiness_execution_slots:
                slots.iter().filter(|slot| slot.release_handoff_evidence).count(),
            release_ready_review_handoff_action_evidence_review_disposition_action_readiness_execution_slots:
                slots.iter().filter(|slot| slot.release_ready_review).count(),
            release_ready_disposition_handoff_action_evidence_review_disposition_action_readiness_execution_slots:
                slots.iter().filter(|slot| slot.release_ready_disposition).count(),
            release_ready_disposition_action_handoff_action_evidence_review_disposition_action_readiness_execution_slots:
                slots.iter().filter(|slot| slot.release_ready_disposition_action).count(),
            release_ready_handoff_action_evidence_review_disposition_action_readiness_execution_slots:
                slots
                    .iter()
                    .filter(|slot| slot.ready_for_execution_handoff())
                    .count(),
            first_handoff_action_evidence_review_disposition_action_readiness_execution_slot_key:
                first_slot.map(|slot| {
                    slot.handoff_action_evidence_review_disposition_action_readiness_execution_slot_key.clone()
                }),
            first_required_handoff_action_evidence_review_disposition_action_readiness_execution_slot_key:
                first_required_slot.map(|slot| {
                    slot.handoff_action_evidence_review_disposition_action_readiness_execution_slot_key.clone()
                }),
            first_repair_handoff_action_evidence_review_disposition_action_readiness_execution_slot_key:
                first_repair_slot.map(|slot| {
                    slot.handoff_action_evidence_review_disposition_action_readiness_execution_slot_key.clone()
                }),
            first_operator_handoff_action_evidence_review_disposition_action_readiness_execution_slot_key:
                first_operator_slot.map(|slot| {
                    slot.handoff_action_evidence_review_disposition_action_readiness_execution_slot_key.clone()
                }),
            first_review_handoff_action_evidence_review_disposition_action_readiness_execution_slot_key:
                first_review_slot.map(|slot| {
                    slot.handoff_action_evidence_review_disposition_action_readiness_execution_slot_key.clone()
                }),
            first_lineage_handoff_action_evidence_review_disposition_action_readiness_execution_slot_key:
                first_lineage_slot.map(|slot| {
                    slot.handoff_action_evidence_review_disposition_action_readiness_execution_slot_key.clone()
                }),
            first_release_handoff_action_evidence_review_disposition_action_readiness_execution_slot_key:
                first_release_handoff_slot.map(|slot| {
                    slot.handoff_action_evidence_review_disposition_action_readiness_execution_slot_key.clone()
                }),
            first_release_ready_handoff_action_evidence_review_disposition_action_readiness_execution_slot_key:
                first_release_ready_slot.map(|slot| {
                    slot.handoff_action_evidence_review_disposition_action_readiness_execution_slot_key.clone()
                }),
            first_handoff_action_evidence_review_disposition_action_readiness_row_key:
                first_slot.map(|slot| {
                    slot.handoff_action_evidence_review_disposition_action_readiness_row_key
                        .clone()
                }),
            first_handoff_action_evidence_review_disposition_action_row_key:
                first_slot.map(|slot| {
                    slot.handoff_action_evidence_review_disposition_action_row_key
                        .clone()
                }),
            first_handoff_action_evidence_review_disposition_row_key: first_slot.map(|slot| {
                slot.handoff_action_evidence_review_disposition_row_key.clone()
            }),
            first_handoff_action_evidence_review_row_key: first_slot
                .map(|slot| slot.handoff_action_evidence_review_row_key.clone()),
            first_handoff_action_evidence_row_key: first_slot
                .map(|slot| slot.handoff_action_evidence_row_key.clone()),
            first_handoff_action_row_key: first_slot
                .map(|slot| slot.handoff_action_row_key.clone()),
            first_handoff_row_key: first_slot.map(|slot| slot.handoff_row_key.clone()),
            first_readiness_evidence_review_disposition_action_key: first_slot.map(|slot| {
                slot.readiness_evidence_review_disposition_action_key.clone()
            }),
            first_work_order_key: first_slot.map(|slot| slot.work_order_key.clone()),
            first_ticket_key: first_slot.map(|slot| slot.ticket_key.clone()),
            first_handoff_action_kind: first_slot.map(|slot| slot.handoff_action_kind),
            first_handoff_action_evidence_review_disposition_action_kind: first_slot
                .map(|slot| slot.handoff_action_evidence_review_disposition_action_kind),
            first_handoff_lane: first_slot.map(|slot| slot.handoff_lane),
            first_check_kind: first_slot.map(|slot| slot.check_kind),
            first_check_status: first_slot.map(|slot| slot.status),
            next_handoff_action_evidence_review_disposition_action_readiness_execution_slot_key:
                next_slot.map(|slot| {
                    slot.handoff_action_evidence_review_disposition_action_readiness_execution_slot_key.clone()
                }),
            next_handoff_action_evidence_review_disposition_action_readiness_row_key:
                next_slot.map(|slot| {
                    slot.handoff_action_evidence_review_disposition_action_readiness_row_key
                        .clone()
                }),
            next_handoff_action_evidence_review_disposition_action_row_key:
                next_slot.map(|slot| {
                    slot.handoff_action_evidence_review_disposition_action_row_key
                        .clone()
                }),
            next_handoff_action_evidence_review_disposition_row_key: next_slot.map(|slot| {
                slot.handoff_action_evidence_review_disposition_row_key.clone()
            }),
            next_handoff_action_evidence_review_row_key: next_slot
                .map(|slot| slot.handoff_action_evidence_review_row_key.clone()),
            next_handoff_action_evidence_row_key: next_slot
                .map(|slot| slot.handoff_action_evidence_row_key.clone()),
            next_handoff_action_row_key: next_slot.map(|slot| slot.handoff_action_row_key.clone()),
            next_handoff_row_key: next_slot.map(|slot| slot.handoff_row_key.clone()),
            next_readiness_evidence_review_disposition_action_key: next_slot.map(|slot| {
                slot.readiness_evidence_review_disposition_action_key.clone()
            }),
            next_work_order_key: next_slot.map(|slot| slot.work_order_key.clone()),
            next_ticket_key: next_slot.map(|slot| slot.ticket_key.clone()),
            next_check_kind: next_slot.map(|slot| slot.check_kind),
            next_check_status: next_slot.map(|slot| slot.status),
            next_handoff_lane: next_slot.map(|slot| slot.handoff_lane),
            release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_ready,
            release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slots_ready,
        }
    }

    pub fn has_execution_slots(&self) -> bool {
        self.total_handoff_action_evidence_review_disposition_action_readiness_execution_slots > 0
    }

    pub fn ready_for_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slots(
        &self,
    ) -> bool {
        self.release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_ready
            && self.release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slots_ready
            && !self.requires_attention()
    }

    pub fn has_required_slots(&self) -> bool {
        self.required_handoff_action_evidence_review_disposition_action_readiness_execution_slots
            > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.repair_handoff_action_evidence_review_disposition_action_readiness_execution_slots > 0
            || self
                .required_handoff_action_evidence_review_disposition_action_readiness_execution_slots
                > 0
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_handoff_action_evidence_review_disposition_action_readiness_execution_slots
            > 0
            || self
                .operator_required_handoff_action_evidence_review_disposition_action_readiness_execution_slots
                > 0
    }

    pub fn has_review_work(&self) -> bool {
        self.review_handoff_action_evidence_review_disposition_action_readiness_execution_slots > 0
    }

    pub fn requires_dispatch(&self) -> bool {
        self.dispatch_required_handoff_action_evidence_review_disposition_action_readiness_execution_slots
            > 0
    }

    pub fn requires_execution(&self) -> bool {
        self.execution_required_handoff_action_evidence_review_disposition_action_readiness_execution_slots
            > 0
    }

    pub fn has_lineage_gaps(&self) -> bool {
        self.lineage_handoff_action_evidence_review_disposition_action_readiness_execution_slots
            > 0
            || self
                .lineage_complete_handoff_action_evidence_review_disposition_action_readiness_execution_slots
                < self.total_handoff_action_evidence_review_disposition_action_readiness_execution_slots
    }

    pub fn has_release_handoff_slots(&self) -> bool {
        self.release_handoff_action_evidence_review_disposition_action_readiness_execution_slots
            > 0
    }

    pub fn has_release_ready_slots(&self) -> bool {
        self.release_ready_handoff_action_evidence_review_disposition_action_readiness_execution_slots
            > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.has_required_slots()
            || self.has_blockers()
            || self.needs_operator()
            || self.has_review_work()
            || self.requires_dispatch()
            || self.requires_execution()
            || self.has_lineage_gaps()
            || !self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slots_ready
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionHandoffRow {
    pub sequence: usize,
    pub handoff_action_evidence_review_disposition_action_readiness_execution_handoff_row_key:
        String,
    pub handoff_action_evidence_review_disposition_action_readiness_execution_slot_sequence:
        usize,
    pub handoff_action_evidence_review_disposition_action_readiness_execution_slot_key:
        String,
    pub handoff_action_evidence_review_disposition_action_readiness_row_key: String,
    pub handoff_action_evidence_review_disposition_action_row_key: String,
    pub handoff_action_evidence_review_disposition_row_key: String,
    pub handoff_action_evidence_review_row_key: String,
    pub handoff_action_evidence_row_key: String,
    pub handoff_action_row_key: String,
    pub handoff_row_key: String,
    pub readiness_evidence_review_disposition_action_readiness_execution_slot_key: String,
    pub readiness_evidence_review_disposition_action_key: String,
    pub work_order_key: String,
    pub ticket_key: String,
    pub check_sequence: usize,
    pub check_kind: IntegrationMeshReleaseReadinessCheckKind,
    pub status: IntegrationMeshReleaseReadinessStatus,
    pub handoff_lane: IntegrationMeshReleaseDispatchTicketHandoffLane,
    pub handoff_action_kind:
        IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind,
    pub disposition_action_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind,
    pub handoff_action_evidence_review_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind,
    pub handoff_action_evidence_review_disposition_action_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind,
    pub release_blocking: bool,
    pub operator_required: bool,
    pub review_required: bool,
    pub dispatch_required: bool,
    pub execution_required: bool,
    pub lineage_complete: bool,
    pub release_handoff_evidence: bool,
    pub release_ready_review: bool,
    pub release_ready_disposition: bool,
    pub release_ready_disposition_action: bool,
    pub release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slots_ready:
        bool,
    pub handoff_ready: bool,
}

impl
    IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionHandoffRow
{
    pub fn from_readiness_execution_slot(
        sequence: usize,
        slot: &IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionSlot,
        release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slots_ready: bool,
    ) -> Self {
        let handoff_ready =
            release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slots_ready
                && slot.ready_for_execution_handoff()
                && slot.is_release_handoff_slot()
                && slot.release_handoff_evidence
                && slot.release_ready_review
                && slot.release_ready_disposition
                && slot.release_ready_disposition_action
                && !slot.requires_attention();

        Self {
            sequence,
            handoff_action_evidence_review_disposition_action_readiness_execution_handoff_row_key:
                format!(
                    "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-execution-handoff-{sequence:02}-{}-{}",
                    slot.handoff_action_evidence_review_disposition_action_kind
                        .as_str(),
                    slot.check_kind.as_str()
                ),
            handoff_action_evidence_review_disposition_action_readiness_execution_slot_sequence:
                slot.sequence,
            handoff_action_evidence_review_disposition_action_readiness_execution_slot_key:
                slot.handoff_action_evidence_review_disposition_action_readiness_execution_slot_key
                    .clone(),
            handoff_action_evidence_review_disposition_action_readiness_row_key: slot
                .handoff_action_evidence_review_disposition_action_readiness_row_key
                .clone(),
            handoff_action_evidence_review_disposition_action_row_key: slot
                .handoff_action_evidence_review_disposition_action_row_key
                .clone(),
            handoff_action_evidence_review_disposition_row_key: slot
                .handoff_action_evidence_review_disposition_row_key
                .clone(),
            handoff_action_evidence_review_row_key: slot
                .handoff_action_evidence_review_row_key
                .clone(),
            handoff_action_evidence_row_key: slot.handoff_action_evidence_row_key.clone(),
            handoff_action_row_key: slot.handoff_action_row_key.clone(),
            handoff_row_key: slot.handoff_row_key.clone(),
            readiness_evidence_review_disposition_action_readiness_execution_slot_key: slot
                .readiness_evidence_review_disposition_action_readiness_execution_slot_key
                .clone(),
            readiness_evidence_review_disposition_action_key: slot
                .readiness_evidence_review_disposition_action_key
                .clone(),
            work_order_key: slot.work_order_key.clone(),
            ticket_key: slot.ticket_key.clone(),
            check_sequence: slot.check_sequence,
            check_kind: slot.check_kind,
            status: slot.status,
            handoff_lane: slot.handoff_lane,
            handoff_action_kind: slot.handoff_action_kind,
            disposition_action_kind: slot.disposition_action_kind,
            handoff_action_evidence_review_kind: slot.handoff_action_evidence_review_kind,
            handoff_action_evidence_review_disposition_action_kind: slot
                .handoff_action_evidence_review_disposition_action_kind,
            release_blocking: slot.blocks_release(),
            operator_required: slot.needs_operator(),
            review_required: slot.needs_review(),
            dispatch_required: slot.requires_dispatch(),
            execution_required: slot.requires_execution(),
            lineage_complete: slot.has_lineage(),
            release_handoff_evidence: slot.release_handoff_evidence,
            release_ready_review: slot.release_ready_review,
            release_ready_disposition: slot.release_ready_disposition,
            release_ready_disposition_action: slot.release_ready_disposition_action,
            release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slots_ready,
            handoff_ready,
        }
    }

    pub fn blocks_release(&self) -> bool {
        self.release_blocking
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required
    }

    pub fn needs_review(&self) -> bool {
        self.review_required
    }

    pub fn requires_dispatch(&self) -> bool {
        self.dispatch_required
    }

    pub fn requires_execution(&self) -> bool {
        self.execution_required
    }

    pub fn has_lineage(&self) -> bool {
        self.lineage_complete
    }

    pub fn is_repair_handoff(&self) -> bool {
        self.handoff_action_evidence_review_disposition_action_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::ScheduleReleaseBlockerRepair
    }

    pub fn is_review_handoff(&self) -> bool {
        self.handoff_action_evidence_review_disposition_action_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::CompleteEvidenceReview
    }

    pub fn is_release_handoff(&self) -> bool {
        self.handoff_action_evidence_review_disposition_action_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::ReleaseHandoff
    }

    pub fn ready_for_handoff(&self) -> bool {
        self.handoff_ready
    }

    pub fn requires_attention(&self) -> bool {
        self.blocks_release()
            || self.needs_operator()
            || self.needs_review()
            || self.requires_dispatch()
            || self.requires_execution()
            || !self.has_lineage()
            || !self.release_handoff_evidence
            || !self.release_ready_review
            || !self.release_ready_disposition
            || !self.release_ready_disposition_action
            || !self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slots_ready
            || !self.ready_for_handoff()
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionHandoffSummary {
    pub release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slot_summary:
        Box<IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionSlotSummary>,
    pub total_handoff_action_evidence_review_disposition_action_readiness_execution_slots: usize,
    pub total_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_rows:
        usize,
    pub required_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_rows:
        usize,
    pub release_blocking_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_rows:
        usize,
    pub repair_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_rows:
        usize,
    pub review_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_rows:
        usize,
    pub operator_required_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_rows:
        usize,
    pub dispatch_required_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_rows:
        usize,
    pub execution_required_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_rows:
        usize,
    pub lineage_complete_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_rows:
        usize,
    pub release_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_rows:
        usize,
    pub release_ready_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_rows:
        usize,
    pub first_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_row_key:
        Option<String>,
    pub first_required_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_row_key:
        Option<String>,
    pub first_repair_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_row_key:
        Option<String>,
    pub first_review_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_row_key:
        Option<String>,
    pub first_release_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_row_key:
        Option<String>,
    pub first_handoff_action_evidence_review_disposition_action_readiness_execution_slot_key:
        Option<String>,
    pub first_handoff_action_evidence_review_disposition_action_readiness_row_key:
        Option<String>,
    pub first_handoff_action_evidence_review_disposition_action_row_key: Option<String>,
    pub first_handoff_action_evidence_review_disposition_row_key: Option<String>,
    pub first_handoff_action_evidence_review_row_key: Option<String>,
    pub first_handoff_action_evidence_row_key: Option<String>,
    pub first_handoff_action_row_key: Option<String>,
    pub first_handoff_row_key: Option<String>,
    pub first_readiness_evidence_review_disposition_action_key: Option<String>,
    pub first_work_order_key: Option<String>,
    pub first_ticket_key: Option<String>,
    pub first_handoff_action_kind: Option<
        IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind,
    >,
    pub first_handoff_action_evidence_review_disposition_action_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind,
    >,
    pub first_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub first_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub first_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_row_key:
        Option<String>,
    pub next_handoff_action_evidence_review_disposition_action_readiness_execution_slot_key:
        Option<String>,
    pub next_handoff_action_evidence_review_disposition_action_readiness_row_key:
        Option<String>,
    pub next_handoff_action_evidence_review_disposition_action_row_key: Option<String>,
    pub next_handoff_action_evidence_review_disposition_row_key: Option<String>,
    pub next_handoff_action_evidence_review_row_key: Option<String>,
    pub next_handoff_action_evidence_row_key: Option<String>,
    pub next_handoff_action_row_key: Option<String>,
    pub next_handoff_row_key: Option<String>,
    pub next_readiness_evidence_review_disposition_action_key: Option<String>,
    pub next_work_order_key: Option<String>,
    pub next_ticket_key: Option<String>,
    pub next_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub next_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slots_ready:
        bool,
    pub release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows_ready:
        bool,
}

impl
    IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionHandoffSummary
{
    pub fn from_summaries<'a>(
        release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slot_summary: IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionSlotSummary,
        rows: impl IntoIterator<
            Item = &'a IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionHandoffRow,
        >,
    ) -> Self {
        let rows: Vec<&IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionHandoffRow> =
            rows.into_iter().collect();
        let first_row = rows.first().copied();
        let first_required_row = rows
            .iter()
            .copied()
            .find(|row| row.requires_attention());
        let first_repair_row = rows.iter().copied().find(|row| row.is_repair_handoff());
        let first_review_row = rows.iter().copied().find(|row| row.is_review_handoff());
        let first_release_handoff_row =
            rows.iter().copied().find(|row| row.is_release_handoff());
        let next_row = first_required_row;
        let release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows_ready =
            rows.len()
                == release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slot_summary
                    .total_handoff_action_evidence_review_disposition_action_readiness_execution_slots
                && rows.iter().all(|row| row.ready_for_handoff());

        Self {
            total_handoff_action_evidence_review_disposition_action_readiness_execution_slots:
                release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slot_summary
                    .total_handoff_action_evidence_review_disposition_action_readiness_execution_slots,
            total_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_rows:
                rows.len(),
            required_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_rows:
                rows.iter().filter(|row| row.requires_attention()).count(),
            release_blocking_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_rows:
                rows.iter().filter(|row| row.blocks_release()).count(),
            repair_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_rows:
                rows.iter().filter(|row| row.is_repair_handoff()).count(),
            review_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_rows:
                rows.iter().filter(|row| row.is_review_handoff()).count(),
            operator_required_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_rows:
                rows.iter().filter(|row| row.needs_operator()).count(),
            dispatch_required_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_rows:
                rows.iter().filter(|row| row.requires_dispatch()).count(),
            execution_required_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_rows:
                rows.iter().filter(|row| row.requires_execution()).count(),
            lineage_complete_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_rows:
                rows.iter().filter(|row| row.has_lineage()).count(),
            release_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_rows:
                rows.iter().filter(|row| row.is_release_handoff()).count(),
            release_ready_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_rows:
                rows.iter().filter(|row| row.ready_for_handoff()).count(),
            first_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_row_key:
                first_row.map(|row| {
                    row.handoff_action_evidence_review_disposition_action_readiness_execution_handoff_row_key.clone()
                }),
            first_required_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_row_key:
                first_required_row.map(|row| {
                    row.handoff_action_evidence_review_disposition_action_readiness_execution_handoff_row_key.clone()
                }),
            first_repair_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_row_key:
                first_repair_row.map(|row| {
                    row.handoff_action_evidence_review_disposition_action_readiness_execution_handoff_row_key.clone()
                }),
            first_review_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_row_key:
                first_review_row.map(|row| {
                    row.handoff_action_evidence_review_disposition_action_readiness_execution_handoff_row_key.clone()
                }),
            first_release_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_row_key:
                first_release_handoff_row.map(|row| {
                    row.handoff_action_evidence_review_disposition_action_readiness_execution_handoff_row_key.clone()
                }),
            first_handoff_action_evidence_review_disposition_action_readiness_execution_slot_key:
                first_row.map(|row| {
                    row.handoff_action_evidence_review_disposition_action_readiness_execution_slot_key.clone()
                }),
            first_handoff_action_evidence_review_disposition_action_readiness_row_key:
                first_row.map(|row| {
                    row.handoff_action_evidence_review_disposition_action_readiness_row_key
                        .clone()
                }),
            first_handoff_action_evidence_review_disposition_action_row_key:
                first_row.map(|row| {
                    row.handoff_action_evidence_review_disposition_action_row_key
                        .clone()
                }),
            first_handoff_action_evidence_review_disposition_row_key: first_row.map(|row| {
                row.handoff_action_evidence_review_disposition_row_key.clone()
            }),
            first_handoff_action_evidence_review_row_key: first_row
                .map(|row| row.handoff_action_evidence_review_row_key.clone()),
            first_handoff_action_evidence_row_key: first_row
                .map(|row| row.handoff_action_evidence_row_key.clone()),
            first_handoff_action_row_key: first_row.map(|row| row.handoff_action_row_key.clone()),
            first_handoff_row_key: first_row.map(|row| row.handoff_row_key.clone()),
            first_readiness_evidence_review_disposition_action_key: first_row.map(|row| {
                row.readiness_evidence_review_disposition_action_key.clone()
            }),
            first_work_order_key: first_row.map(|row| row.work_order_key.clone()),
            first_ticket_key: first_row.map(|row| row.ticket_key.clone()),
            first_handoff_action_kind: first_row.map(|row| row.handoff_action_kind),
            first_handoff_action_evidence_review_disposition_action_kind: first_row
                .map(|row| row.handoff_action_evidence_review_disposition_action_kind),
            first_handoff_lane: first_row.map(|row| row.handoff_lane),
            first_check_kind: first_row.map(|row| row.check_kind),
            first_check_status: first_row.map(|row| row.status),
            next_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_row_key:
                next_row.map(|row| {
                    row.handoff_action_evidence_review_disposition_action_readiness_execution_handoff_row_key.clone()
                }),
            next_handoff_action_evidence_review_disposition_action_readiness_execution_slot_key:
                next_row.map(|row| {
                    row.handoff_action_evidence_review_disposition_action_readiness_execution_slot_key.clone()
                }),
            next_handoff_action_evidence_review_disposition_action_readiness_row_key:
                next_row.map(|row| {
                    row.handoff_action_evidence_review_disposition_action_readiness_row_key
                        .clone()
                }),
            next_handoff_action_evidence_review_disposition_action_row_key:
                next_row.map(|row| {
                    row.handoff_action_evidence_review_disposition_action_row_key
                        .clone()
                }),
            next_handoff_action_evidence_review_disposition_row_key: next_row.map(|row| {
                row.handoff_action_evidence_review_disposition_row_key.clone()
            }),
            next_handoff_action_evidence_review_row_key: next_row
                .map(|row| row.handoff_action_evidence_review_row_key.clone()),
            next_handoff_action_evidence_row_key: next_row
                .map(|row| row.handoff_action_evidence_row_key.clone()),
            next_handoff_action_row_key: next_row.map(|row| row.handoff_action_row_key.clone()),
            next_handoff_row_key: next_row.map(|row| row.handoff_row_key.clone()),
            next_readiness_evidence_review_disposition_action_key: next_row.map(|row| {
                row.readiness_evidence_review_disposition_action_key.clone()
            }),
            next_work_order_key: next_row.map(|row| row.work_order_key.clone()),
            next_ticket_key: next_row.map(|row| row.ticket_key.clone()),
            next_check_kind: next_row.map(|row| row.check_kind),
            next_check_status: next_row.map(|row| row.status),
            next_handoff_lane: next_row.map(|row| row.handoff_lane),
            release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slots_ready:
                release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slot_summary
                    .ready_for_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slots(),
            release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows_ready,
            release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slot_summary:
                Box::new(
                    release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slot_summary,
                ),
        }
    }

    pub fn has_handoff_rows(&self) -> bool {
        self.total_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_rows > 0
    }

    pub fn ready_for_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoffs(
        &self,
    ) -> bool {
        self.release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slots_ready
            && self.release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows_ready
            && !self.requires_attention()
    }

    pub fn has_required_handoffs(&self) -> bool {
        self.required_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_rows
            > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.release_blocking_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_rows
            > 0
            || self.repair_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_rows
                > 0
            || self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slot_summary
                .has_blockers()
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_rows
            > 0
            || self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slot_summary
                .needs_operator()
    }

    pub fn has_review_work(&self) -> bool {
        self.review_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_rows
            > 0
            || self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slot_summary
                .has_review_work()
    }

    pub fn requires_dispatch(&self) -> bool {
        self.dispatch_required_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_rows
            > 0
    }

    pub fn requires_execution(&self) -> bool {
        self.execution_required_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_rows
            > 0
    }

    pub fn has_lineage_gaps(&self) -> bool {
        self.lineage_complete_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_rows
            < self.total_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_rows
            || self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slot_summary
                .has_lineage_gaps()
    }

    pub fn has_release_handoffs(&self) -> bool {
        self.release_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_rows
            > 0
    }

    pub fn has_release_ready_handoffs(&self) -> bool {
        self.release_ready_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_rows
            > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.has_required_handoffs()
            || self.has_blockers()
            || self.needs_operator()
            || self.has_review_work()
            || self.requires_dispatch()
            || self.requires_execution()
            || self.has_lineage_gaps()
            || !self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows_ready
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionHandoffActionRow {
    pub sequence: usize,
    pub handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_row_key:
        String,
    pub handoff_action_evidence_review_disposition_action_readiness_execution_handoff_row_sequence:
        usize,
    pub handoff_action_evidence_review_disposition_action_readiness_execution_handoff_row_key:
        String,
    pub handoff_action_evidence_review_disposition_action_readiness_execution_slot_key:
        String,
    pub handoff_action_evidence_review_disposition_action_readiness_row_key: String,
    pub handoff_action_evidence_review_disposition_action_row_key: String,
    pub handoff_action_evidence_review_disposition_row_key: String,
    pub handoff_action_evidence_review_row_key: String,
    pub handoff_action_evidence_row_key: String,
    pub handoff_action_row_key: String,
    pub handoff_row_key: String,
    pub readiness_evidence_review_disposition_action_readiness_execution_slot_key: String,
    pub readiness_evidence_review_disposition_action_key: String,
    pub work_order_key: String,
    pub ticket_key: String,
    pub check_sequence: usize,
    pub check_kind: IntegrationMeshReleaseReadinessCheckKind,
    pub status: IntegrationMeshReleaseReadinessStatus,
    pub handoff_lane: IntegrationMeshReleaseDispatchTicketHandoffLane,
    pub handoff_action_kind:
        IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind,
    pub disposition_action_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind,
    pub handoff_action_evidence_review_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind,
    pub handoff_action_evidence_review_disposition_action_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind,
    pub release_blocking: bool,
    pub operator_required: bool,
    pub review_required: bool,
    pub dispatch_required: bool,
    pub execution_required: bool,
    pub lineage_complete: bool,
    pub release_handoff_evidence: bool,
    pub release_ready_review: bool,
    pub release_ready_disposition: bool,
    pub release_ready_disposition_action: bool,
    pub release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoffs_ready:
        bool,
    pub handoff_ready: bool,
}

impl
    IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionHandoffActionRow
{
    pub fn from_handoff_row(
        sequence: usize,
        row: &IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionHandoffRow,
        release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoffs_ready: bool,
    ) -> Self {
        let handoff_action_kind =
            if row.ready_for_handoff()
                && release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoffs_ready
            {
                IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind::ReleaseHandoff
            } else if row.is_repair_handoff() || row.blocks_release() {
                IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind::RepairHandoff
            } else if row.is_review_handoff() || row.needs_review() {
                IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind::ReviewHandoff
            } else if row.needs_operator() {
                IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind::OperatorHandoff
            } else if row.requires_dispatch() {
                IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind::DispatchHandoff
            } else if row.requires_execution() {
                IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind::ExecuteHandoff
            } else {
                IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind::PreserveLineage
            };

        Self {
            sequence,
            handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_row_key:
                format!(
                    "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-execution-handoff-action-{sequence:02}-{}-{}",
                    handoff_action_kind.as_str(),
                    row.check_kind.as_str()
                ),
            handoff_action_evidence_review_disposition_action_readiness_execution_handoff_row_sequence:
                row.sequence,
            handoff_action_evidence_review_disposition_action_readiness_execution_handoff_row_key:
                row.handoff_action_evidence_review_disposition_action_readiness_execution_handoff_row_key.clone(),
            handoff_action_evidence_review_disposition_action_readiness_execution_slot_key:
                row.handoff_action_evidence_review_disposition_action_readiness_execution_slot_key
                    .clone(),
            handoff_action_evidence_review_disposition_action_readiness_row_key: row
                .handoff_action_evidence_review_disposition_action_readiness_row_key
                .clone(),
            handoff_action_evidence_review_disposition_action_row_key: row
                .handoff_action_evidence_review_disposition_action_row_key
                .clone(),
            handoff_action_evidence_review_disposition_row_key: row
                .handoff_action_evidence_review_disposition_row_key
                .clone(),
            handoff_action_evidence_review_row_key: row
                .handoff_action_evidence_review_row_key
                .clone(),
            handoff_action_evidence_row_key: row.handoff_action_evidence_row_key.clone(),
            handoff_action_row_key: row.handoff_action_row_key.clone(),
            handoff_row_key: row.handoff_row_key.clone(),
            readiness_evidence_review_disposition_action_readiness_execution_slot_key: row
                .readiness_evidence_review_disposition_action_readiness_execution_slot_key
                .clone(),
            readiness_evidence_review_disposition_action_key: row
                .readiness_evidence_review_disposition_action_key
                .clone(),
            work_order_key: row.work_order_key.clone(),
            ticket_key: row.ticket_key.clone(),
            check_sequence: row.check_sequence,
            check_kind: row.check_kind,
            status: row.status,
            handoff_lane: row.handoff_lane,
            handoff_action_kind,
            disposition_action_kind: row.disposition_action_kind,
            handoff_action_evidence_review_kind: row.handoff_action_evidence_review_kind,
            handoff_action_evidence_review_disposition_action_kind: row
                .handoff_action_evidence_review_disposition_action_kind,
            release_blocking: row.blocks_release(),
            operator_required: row.needs_operator(),
            review_required: row.needs_review(),
            dispatch_required: row.requires_dispatch(),
            execution_required: row.requires_execution(),
            lineage_complete: row.has_lineage(),
            release_handoff_evidence: row.release_handoff_evidence,
            release_ready_review: row.release_ready_review,
            release_ready_disposition: row.release_ready_disposition,
            release_ready_disposition_action: row.release_ready_disposition_action,
            release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoffs_ready,
            handoff_ready: row.ready_for_handoff(),
        }
    }

    pub fn blocks_release(&self) -> bool {
        self.release_blocking
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required
    }

    pub fn needs_review(&self) -> bool {
        self.review_required
    }

    pub fn requires_dispatch(&self) -> bool {
        self.dispatch_required
    }

    pub fn requires_execution(&self) -> bool {
        self.execution_required
    }

    pub fn has_lineage(&self) -> bool {
        self.lineage_complete
    }

    pub fn is_repair_action(&self) -> bool {
        self.handoff_action_kind
            == IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind::RepairHandoff
    }

    pub fn is_review_action(&self) -> bool {
        self.handoff_action_kind
            == IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind::ReviewHandoff
    }

    pub fn is_release_handoff_action(&self) -> bool {
        self.handoff_action_kind
            == IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind::ReleaseHandoff
    }

    pub fn ready_for_release_handoff(&self) -> bool {
        self.is_release_handoff_action()
            && self.release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoffs_ready
            && self.handoff_ready
            && self.release_handoff_evidence
            && self.release_ready_review
            && self.release_ready_disposition
            && self.release_ready_disposition_action
            && self.has_lineage()
    }

    pub fn requires_attention(&self) -> bool {
        self.blocks_release()
            || self.needs_operator()
            || self.needs_review()
            || self.requires_dispatch()
            || self.requires_execution()
            || !self.has_lineage()
            || !self.release_handoff_evidence
            || !self.release_ready_review
            || !self.release_ready_disposition
            || !self.release_ready_disposition_action
            || !self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoffs_ready
            || !self.ready_for_release_handoff()
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionHandoffActionSummary {
    pub release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_summary:
        Box<IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionHandoffSummary>,
    pub total_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_rows:
        usize,
    pub total_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_rows:
        usize,
    pub required_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_rows:
        usize,
    pub release_blocking_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_rows:
        usize,
    pub repair_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_rows:
        usize,
    pub review_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_rows:
        usize,
    pub operator_required_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_rows:
        usize,
    pub dispatch_required_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_rows:
        usize,
    pub execution_required_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_rows:
        usize,
    pub lineage_complete_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_rows:
        usize,
    pub release_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_rows:
        usize,
    pub release_handoff_evidence_action_evidence_review_disposition_action_readiness_execution_handoff_action_rows:
        usize,
    pub release_ready_review_action_evidence_review_disposition_action_readiness_execution_handoff_action_rows:
        usize,
    pub release_ready_disposition_action_evidence_review_disposition_action_readiness_execution_handoff_action_rows:
        usize,
    pub release_ready_disposition_action_action_evidence_review_disposition_action_readiness_execution_handoff_action_rows:
        usize,
    pub release_ready_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_rows:
        usize,
    pub first_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_row_key:
        Option<String>,
    pub first_required_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_row_key:
        Option<String>,
    pub first_repair_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_row_key:
        Option<String>,
    pub first_review_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_row_key:
        Option<String>,
    pub first_release_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_row_key:
        Option<String>,
    pub first_release_ready_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_row_key:
        Option<String>,
    pub first_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_row_key:
        Option<String>,
    pub first_handoff_action_evidence_review_disposition_action_readiness_execution_slot_key:
        Option<String>,
    pub first_handoff_action_evidence_review_disposition_action_readiness_row_key:
        Option<String>,
    pub first_handoff_action_evidence_review_disposition_action_row_key: Option<String>,
    pub first_handoff_action_evidence_review_disposition_row_key: Option<String>,
    pub first_handoff_action_evidence_review_row_key: Option<String>,
    pub first_handoff_action_evidence_row_key: Option<String>,
    pub first_handoff_action_row_key: Option<String>,
    pub first_handoff_row_key: Option<String>,
    pub first_readiness_evidence_review_disposition_action_key: Option<String>,
    pub first_work_order_key: Option<String>,
    pub first_ticket_key: Option<String>,
    pub first_handoff_action_kind: Option<
        IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind,
    >,
    pub first_handoff_action_evidence_review_disposition_action_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind,
    >,
    pub first_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub first_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub first_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_row_key:
        Option<String>,
    pub next_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_row_key:
        Option<String>,
    pub next_handoff_action_evidence_review_disposition_action_readiness_execution_slot_key:
        Option<String>,
    pub next_handoff_action_evidence_review_disposition_action_readiness_row_key:
        Option<String>,
    pub next_handoff_action_evidence_review_disposition_action_row_key: Option<String>,
    pub next_handoff_action_evidence_review_disposition_row_key: Option<String>,
    pub next_handoff_action_evidence_review_row_key: Option<String>,
    pub next_handoff_action_evidence_row_key: Option<String>,
    pub next_handoff_action_row_key: Option<String>,
    pub next_handoff_row_key: Option<String>,
    pub next_readiness_evidence_review_disposition_action_key: Option<String>,
    pub next_work_order_key: Option<String>,
    pub next_ticket_key: Option<String>,
    pub next_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub next_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoffs_ready:
        bool,
    pub release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_actions_ready:
        bool,
}

impl
    IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionHandoffActionSummary
{
    pub fn from_summaries<'a>(
        release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_summary: IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionHandoffSummary,
        action_rows: impl IntoIterator<
            Item = &'a IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionHandoffActionRow,
        >,
    ) -> Self {
        let action_rows: Vec<&IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionHandoffActionRow> =
            action_rows.into_iter().collect();
        let first_row = action_rows.first().copied();
        let first_required_row = action_rows
            .iter()
            .copied()
            .find(|row| row.requires_attention());
        let first_repair_row = action_rows.iter().copied().find(|row| row.is_repair_action());
        let first_review_row = action_rows.iter().copied().find(|row| row.is_review_action());
        let first_release_handoff_row = action_rows
            .iter()
            .copied()
            .find(|row| row.is_release_handoff_action());
        let first_release_ready_row = action_rows
            .iter()
            .copied()
            .find(|row| row.ready_for_release_handoff());
        let next_row = first_required_row;
        let release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_actions_ready =
            action_rows.len()
                == release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_summary
                    .total_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_rows
                && action_rows
                    .iter()
                    .all(|row| row.ready_for_release_handoff());

        Self {
            total_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_rows:
                release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_summary
                    .total_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_rows,
            total_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_rows:
                action_rows.len(),
            required_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_rows:
                action_rows.iter().filter(|row| row.requires_attention()).count(),
            release_blocking_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_rows:
                action_rows.iter().filter(|row| row.blocks_release()).count(),
            repair_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_rows:
                action_rows.iter().filter(|row| row.is_repair_action()).count(),
            review_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_rows:
                action_rows.iter().filter(|row| row.is_review_action()).count(),
            operator_required_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_rows:
                action_rows.iter().filter(|row| row.needs_operator()).count(),
            dispatch_required_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_rows:
                action_rows.iter().filter(|row| row.requires_dispatch()).count(),
            execution_required_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_rows:
                action_rows.iter().filter(|row| row.requires_execution()).count(),
            lineage_complete_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_rows:
                action_rows.iter().filter(|row| row.has_lineage()).count(),
            release_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_rows:
                action_rows
                    .iter()
                    .filter(|row| row.is_release_handoff_action())
                    .count(),
            release_handoff_evidence_action_evidence_review_disposition_action_readiness_execution_handoff_action_rows:
                action_rows.iter().filter(|row| row.release_handoff_evidence).count(),
            release_ready_review_action_evidence_review_disposition_action_readiness_execution_handoff_action_rows:
                action_rows.iter().filter(|row| row.release_ready_review).count(),
            release_ready_disposition_action_evidence_review_disposition_action_readiness_execution_handoff_action_rows:
                action_rows
                    .iter()
                    .filter(|row| row.release_ready_disposition)
                    .count(),
            release_ready_disposition_action_action_evidence_review_disposition_action_readiness_execution_handoff_action_rows:
                action_rows
                    .iter()
                    .filter(|row| row.release_ready_disposition_action)
                    .count(),
            release_ready_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_rows:
                action_rows
                    .iter()
                    .filter(|row| row.ready_for_release_handoff())
                    .count(),
            first_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_row_key:
                first_row.map(|row| {
                    row.handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_row_key.clone()
                }),
            first_required_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_row_key:
                first_required_row.map(|row| {
                    row.handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_row_key.clone()
                }),
            first_repair_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_row_key:
                first_repair_row.map(|row| {
                    row.handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_row_key.clone()
                }),
            first_review_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_row_key:
                first_review_row.map(|row| {
                    row.handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_row_key.clone()
                }),
            first_release_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_row_key:
                first_release_handoff_row.map(|row| {
                    row.handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_row_key.clone()
                }),
            first_release_ready_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_row_key:
                first_release_ready_row.map(|row| {
                    row.handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_row_key.clone()
                }),
            first_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_row_key:
                first_row.map(|row| {
                    row.handoff_action_evidence_review_disposition_action_readiness_execution_handoff_row_key.clone()
                }),
            first_handoff_action_evidence_review_disposition_action_readiness_execution_slot_key:
                first_row.map(|row| {
                    row.handoff_action_evidence_review_disposition_action_readiness_execution_slot_key.clone()
                }),
            first_handoff_action_evidence_review_disposition_action_readiness_row_key:
                first_row.map(|row| {
                    row.handoff_action_evidence_review_disposition_action_readiness_row_key
                        .clone()
                }),
            first_handoff_action_evidence_review_disposition_action_row_key:
                first_row.map(|row| {
                    row.handoff_action_evidence_review_disposition_action_row_key
                        .clone()
                }),
            first_handoff_action_evidence_review_disposition_row_key: first_row.map(|row| {
                row.handoff_action_evidence_review_disposition_row_key.clone()
            }),
            first_handoff_action_evidence_review_row_key: first_row
                .map(|row| row.handoff_action_evidence_review_row_key.clone()),
            first_handoff_action_evidence_row_key: first_row
                .map(|row| row.handoff_action_evidence_row_key.clone()),
            first_handoff_action_row_key: first_row.map(|row| row.handoff_action_row_key.clone()),
            first_handoff_row_key: first_row.map(|row| row.handoff_row_key.clone()),
            first_readiness_evidence_review_disposition_action_key: first_row.map(|row| {
                row.readiness_evidence_review_disposition_action_key.clone()
            }),
            first_work_order_key: first_row.map(|row| row.work_order_key.clone()),
            first_ticket_key: first_row.map(|row| row.ticket_key.clone()),
            first_handoff_action_kind: first_row.map(|row| row.handoff_action_kind),
            first_handoff_action_evidence_review_disposition_action_kind: first_row
                .map(|row| row.handoff_action_evidence_review_disposition_action_kind),
            first_handoff_lane: first_row.map(|row| row.handoff_lane),
            first_check_kind: first_row.map(|row| row.check_kind),
            first_check_status: first_row.map(|row| row.status),
            next_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_row_key:
                next_row.map(|row| {
                    row.handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_row_key.clone()
                }),
            next_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_row_key:
                next_row.map(|row| {
                    row.handoff_action_evidence_review_disposition_action_readiness_execution_handoff_row_key.clone()
                }),
            next_handoff_action_evidence_review_disposition_action_readiness_execution_slot_key:
                next_row.map(|row| {
                    row.handoff_action_evidence_review_disposition_action_readiness_execution_slot_key.clone()
                }),
            next_handoff_action_evidence_review_disposition_action_readiness_row_key:
                next_row.map(|row| {
                    row.handoff_action_evidence_review_disposition_action_readiness_row_key
                        .clone()
                }),
            next_handoff_action_evidence_review_disposition_action_row_key:
                next_row.map(|row| {
                    row.handoff_action_evidence_review_disposition_action_row_key
                        .clone()
                }),
            next_handoff_action_evidence_review_disposition_row_key: next_row.map(|row| {
                row.handoff_action_evidence_review_disposition_row_key.clone()
            }),
            next_handoff_action_evidence_review_row_key: next_row
                .map(|row| row.handoff_action_evidence_review_row_key.clone()),
            next_handoff_action_evidence_row_key: next_row
                .map(|row| row.handoff_action_evidence_row_key.clone()),
            next_handoff_action_row_key: next_row.map(|row| row.handoff_action_row_key.clone()),
            next_handoff_row_key: next_row.map(|row| row.handoff_row_key.clone()),
            next_readiness_evidence_review_disposition_action_key: next_row.map(|row| {
                row.readiness_evidence_review_disposition_action_key.clone()
            }),
            next_work_order_key: next_row.map(|row| row.work_order_key.clone()),
            next_ticket_key: next_row.map(|row| row.ticket_key.clone()),
            next_check_kind: next_row.map(|row| row.check_kind),
            next_check_status: next_row.map(|row| row.status),
            next_handoff_lane: next_row.map(|row| row.handoff_lane),
            release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoffs_ready:
                release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_summary
                    .ready_for_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoffs(),
            release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_actions_ready,
            release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_summary:
                Box::new(
                    release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_summary,
                ),
        }
    }

    pub fn has_handoff_action_rows(&self) -> bool {
        self.total_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_rows > 0
    }

    pub fn ready_for_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_actions(
        &self,
    ) -> bool {
        self.release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoffs_ready
            && self.release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_actions_ready
            && !self.requires_attention()
    }

    pub fn has_required_actions(&self) -> bool {
        self.required_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_rows
            > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.release_blocking_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_rows
            > 0
            || self.repair_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_rows
                > 0
            || self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_summary
                .has_blockers()
    }

    pub fn has_review_work(&self) -> bool {
        self.review_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_rows
            > 0
            || self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_summary
                .has_review_work()
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_rows
            > 0
            || self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_summary
                .needs_operator()
    }

    pub fn requires_dispatch(&self) -> bool {
        self.dispatch_required_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_rows
            > 0
    }

    pub fn requires_execution(&self) -> bool {
        self.execution_required_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_rows
            > 0
    }

    pub fn has_lineage_gaps(&self) -> bool {
        self.lineage_complete_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_rows
            < self.total_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_rows
            || self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_summary
                .has_lineage_gaps()
    }

    pub fn has_release_handoff_actions(&self) -> bool {
        self.release_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_rows
            > 0
    }

    pub fn has_release_ready_handoff_actions(&self) -> bool {
        self.release_ready_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_rows
            > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.has_required_actions()
            || self.has_blockers()
            || self.has_review_work()
            || self.needs_operator()
            || self.requires_dispatch()
            || self.requires_execution()
            || self.has_lineage_gaps()
            || !self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_actions_ready
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionSummary {
    pub release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_summary:
        Box<IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffSummary>,
    pub total_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows: usize,
    pub total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows:
        usize,
    pub required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows:
        usize,
    pub release_blocking_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows:
        usize,
    pub repair_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows:
        usize,
    pub review_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows:
        usize,
    pub operator_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows:
        usize,
    pub dispatch_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows:
        usize,
    pub execution_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows:
        usize,
    pub lineage_complete_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows:
        usize,
    pub release_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows:
        usize,
    pub release_ready_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows:
        usize,
    pub first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_row_key:
        Option<String>,
    pub first_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_row_key:
        Option<String>,
    pub first_repair_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_row_key:
        Option<String>,
    pub first_review_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_row_key:
        Option<String>,
    pub first_release_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_row_key:
        Option<String>,
    pub first_readiness_evidence_review_disposition_action_readiness_execution_handoff_row_key:
        Option<String>,
    pub first_readiness_evidence_review_disposition_action_readiness_execution_slot_key:
        Option<String>,
    pub first_readiness_evidence_review_disposition_action_key: Option<String>,
    pub first_work_order_key: Option<String>,
    pub first_ticket_key: Option<String>,
    pub first_handoff_action_kind: Option<
        IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind,
    >,
    pub first_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub first_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub first_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_row_key:
        Option<String>,
    pub next_readiness_evidence_review_disposition_action_readiness_execution_handoff_row_key:
        Option<String>,
    pub next_readiness_evidence_review_disposition_action_readiness_execution_slot_key:
        Option<String>,
    pub next_readiness_evidence_review_disposition_action_key: Option<String>,
    pub next_work_order_key: Option<String>,
    pub next_ticket_key: Option<String>,
    pub next_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub next_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoffs_ready:
        bool,
    pub release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_actions_ready:
        bool,
}

impl
    IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionSummary
{
    pub fn from_summaries<'a>(
        release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_summary: IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffSummary,
        action_rows: impl IntoIterator<
            Item = &'a IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionRow,
        >,
    ) -> Self {
        let action_rows: Vec<&IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionRow> =
            action_rows.into_iter().collect();
        let first_row = action_rows.first().copied();
        let first_required_row = action_rows
            .iter()
            .copied()
            .find(|row| row.requires_attention());
        let first_repair_row = action_rows.iter().copied().find(|row| row.is_repair_action());
        let first_review_row = action_rows.iter().copied().find(|row| row.is_review_action());
        let first_release_handoff_row = action_rows
            .iter()
            .copied()
            .find(|row| row.is_release_handoff_action());
        let next_row = first_required_row;
        let release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_actions_ready =
            action_rows.len()
                == release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_summary
                    .total_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows
                && action_rows
                    .iter()
                    .all(|row| row.ready_for_release_handoff());

        Self {
            total_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows:
                release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_summary
                    .total_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows,
            total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows:
                action_rows.len(),
            required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows:
                action_rows.iter().filter(|row| row.requires_attention()).count(),
            release_blocking_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows:
                action_rows.iter().filter(|row| row.blocks_release()).count(),
            repair_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows:
                action_rows.iter().filter(|row| row.is_repair_action()).count(),
            review_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows:
                action_rows.iter().filter(|row| row.is_review_action()).count(),
            operator_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows:
                action_rows.iter().filter(|row| row.needs_operator()).count(),
            dispatch_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows:
                action_rows.iter().filter(|row| row.requires_dispatch()).count(),
            execution_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows:
                action_rows.iter().filter(|row| row.requires_execution()).count(),
            lineage_complete_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows:
                action_rows.iter().filter(|row| row.has_lineage()).count(),
            release_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows:
                action_rows
                    .iter()
                    .filter(|row| row.is_release_handoff_action())
                    .count(),
            release_ready_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows:
                action_rows
                    .iter()
                    .filter(|row| row.ready_for_release_handoff())
                    .count(),
            first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_row_key:
                first_row.map(|row| row.handoff_action_row_key.clone()),
            first_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_row_key:
                first_required_row.map(|row| row.handoff_action_row_key.clone()),
            first_repair_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_row_key:
                first_repair_row.map(|row| row.handoff_action_row_key.clone()),
            first_review_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_row_key:
                first_review_row.map(|row| row.handoff_action_row_key.clone()),
            first_release_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_row_key:
                first_release_handoff_row.map(|row| row.handoff_action_row_key.clone()),
            first_readiness_evidence_review_disposition_action_readiness_execution_handoff_row_key:
                first_row.map(|row| row.handoff_row_key.clone()),
            first_readiness_evidence_review_disposition_action_readiness_execution_slot_key:
                first_row.map(|row| {
                    row.readiness_evidence_review_disposition_action_readiness_execution_slot_key
                        .clone()
                }),
            first_readiness_evidence_review_disposition_action_key:
                first_row.map(|row| row.readiness_evidence_review_disposition_action_key.clone()),
            first_work_order_key: first_row.map(|row| row.work_order_key.clone()),
            first_ticket_key: first_row.map(|row| row.ticket_key.clone()),
            first_handoff_action_kind: first_row.map(|row| row.handoff_action_kind),
            first_handoff_lane: first_row.map(|row| row.handoff_lane),
            first_check_kind: first_row.map(|row| row.check_kind),
            first_check_status: first_row.map(|row| row.status),
            next_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_row_key:
                next_row.map(|row| row.handoff_action_row_key.clone()),
            next_readiness_evidence_review_disposition_action_readiness_execution_handoff_row_key:
                next_row.map(|row| row.handoff_row_key.clone()),
            next_readiness_evidence_review_disposition_action_readiness_execution_slot_key:
                next_row.map(|row| {
                    row.readiness_evidence_review_disposition_action_readiness_execution_slot_key
                        .clone()
                }),
            next_readiness_evidence_review_disposition_action_key:
                next_row.map(|row| row.readiness_evidence_review_disposition_action_key.clone()),
            next_work_order_key: next_row.map(|row| row.work_order_key.clone()),
            next_ticket_key: next_row.map(|row| row.ticket_key.clone()),
            next_check_kind: next_row.map(|row| row.check_kind),
            next_check_status: next_row.map(|row| row.status),
            next_handoff_lane: next_row.map(|row| row.handoff_lane),
            release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoffs_ready:
                release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_summary
                    .ready_for_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoffs(),
            release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_actions_ready,
            release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_summary:
                Box::new(
                    release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_summary,
                ),
        }
    }

    pub fn has_handoff_action_rows(&self) -> bool {
        self.total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows > 0
    }

    pub fn ready_for_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_actions(
        &self,
    ) -> bool {
        self.release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoffs_ready
            && self.release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_actions_ready
            && !self.requires_attention()
    }

    pub fn has_required_actions(&self) -> bool {
        self.required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows
            > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.release_blocking_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows
            > 0
            || self.repair_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows
                > 0
            || self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_summary
                .has_blockers()
    }

    pub fn has_review_work(&self) -> bool {
        self.review_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows
            > 0
            || self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_summary
                .has_review_work()
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows
            > 0
            || self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_summary
                .needs_operator()
    }

    pub fn requires_dispatch(&self) -> bool {
        self.dispatch_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows
            > 0
    }

    pub fn requires_execution(&self) -> bool {
        self.execution_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows
            > 0
    }

    pub fn has_lineage_gaps(&self) -> bool {
        self.lineage_complete_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows
            < self.total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows
            || self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_summary
                .has_lineage_gaps()
    }

    pub fn has_release_handoff_actions(&self) -> bool {
        self.release_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows
            > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.has_required_actions()
            || self.has_blockers()
            || self.has_review_work()
            || self.needs_operator()
            || self.requires_dispatch()
            || self.requires_execution()
            || self.has_lineage_gaps()
            || !self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_actions_ready
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceSummary {
    pub release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_summary:
        Box<IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionSummary>,
    pub total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows:
        usize,
    pub total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows:
        usize,
    pub required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows:
        usize,
    pub release_blocking_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows:
        usize,
    pub repair_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows:
        usize,
    pub review_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows:
        usize,
    pub operator_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows:
        usize,
    pub dispatch_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows:
        usize,
    pub execution_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows:
        usize,
    pub lineage_complete_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows:
        usize,
    pub release_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows:
        usize,
    pub release_ready_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows:
        usize,
    pub first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_row_key:
        Option<String>,
    pub first_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_row_key:
        Option<String>,
    pub first_repair_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_row_key:
        Option<String>,
    pub first_review_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_row_key:
        Option<String>,
    pub first_release_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_row_key:
        Option<String>,
    pub first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_row_key:
        Option<String>,
    pub first_readiness_evidence_review_disposition_action_readiness_execution_handoff_row_key:
        Option<String>,
    pub first_readiness_evidence_review_disposition_action_readiness_execution_slot_key:
        Option<String>,
    pub first_readiness_evidence_review_disposition_action_key: Option<String>,
    pub first_work_order_key: Option<String>,
    pub first_ticket_key: Option<String>,
    pub first_handoff_action_kind: Option<
        IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind,
    >,
    pub first_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub first_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub first_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_row_key:
        Option<String>,
    pub next_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_row_key:
        Option<String>,
    pub next_readiness_evidence_review_disposition_action_readiness_execution_handoff_row_key:
        Option<String>,
    pub next_readiness_evidence_review_disposition_action_readiness_execution_slot_key:
        Option<String>,
    pub next_readiness_evidence_review_disposition_action_key: Option<String>,
    pub next_work_order_key: Option<String>,
    pub next_ticket_key: Option<String>,
    pub next_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub next_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_actions_ready:
        bool,
    pub release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_ready:
        bool,
}

impl
    IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceSummary
{
    pub fn from_summaries<'a>(
        release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_summary: IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionSummary,
        evidence_rows: impl IntoIterator<
            Item = &'a IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceRow,
        >,
    ) -> Self {
        let evidence_rows: Vec<&IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceRow> =
            evidence_rows.into_iter().collect();
        let first_row = evidence_rows.first().copied();
        let first_required_row = evidence_rows
            .iter()
            .copied()
            .find(|row| row.requires_attention());
        let first_repair_row = evidence_rows
            .iter()
            .copied()
            .find(|row| row.is_repair_action());
        let first_review_row = evidence_rows
            .iter()
            .copied()
            .find(|row| row.is_review_action());
        let first_release_handoff_row = evidence_rows
            .iter()
            .copied()
            .find(|row| row.is_release_handoff_action());
        let next_row = first_required_row;
        let release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_ready =
            evidence_rows.len()
                == release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_summary
                    .total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows
                && evidence_rows
                    .iter()
                    .all(|row| row.ready_for_release_handoff());

        Self {
            total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows:
                release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_summary
                    .total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows,
            total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows:
                evidence_rows.len(),
            required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows:
                evidence_rows.iter().filter(|row| row.requires_attention()).count(),
            release_blocking_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows:
                evidence_rows.iter().filter(|row| row.blocks_release()).count(),
            repair_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows:
                evidence_rows.iter().filter(|row| row.is_repair_action()).count(),
            review_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows:
                evidence_rows.iter().filter(|row| row.is_review_action()).count(),
            operator_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows:
                evidence_rows.iter().filter(|row| row.needs_operator()).count(),
            dispatch_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows:
                evidence_rows.iter().filter(|row| row.requires_dispatch()).count(),
            execution_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows:
                evidence_rows.iter().filter(|row| row.requires_execution()).count(),
            lineage_complete_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows:
                evidence_rows.iter().filter(|row| row.has_lineage()).count(),
            release_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows:
                evidence_rows
                    .iter()
                    .filter(|row| row.is_release_handoff_action())
                    .count(),
            release_ready_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows:
                evidence_rows
                    .iter()
                    .filter(|row| row.ready_for_release_handoff())
                    .count(),
            first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_row_key:
                first_row.map(|row| row.handoff_action_evidence_row_key.clone()),
            first_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_row_key:
                first_required_row.map(|row| row.handoff_action_evidence_row_key.clone()),
            first_repair_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_row_key:
                first_repair_row.map(|row| row.handoff_action_evidence_row_key.clone()),
            first_review_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_row_key:
                first_review_row.map(|row| row.handoff_action_evidence_row_key.clone()),
            first_release_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_row_key:
                first_release_handoff_row.map(|row| row.handoff_action_evidence_row_key.clone()),
            first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_row_key:
                first_row.map(|row| row.handoff_action_row_key.clone()),
            first_readiness_evidence_review_disposition_action_readiness_execution_handoff_row_key:
                first_row.map(|row| row.handoff_row_key.clone()),
            first_readiness_evidence_review_disposition_action_readiness_execution_slot_key:
                first_row.map(|row| {
                    row.readiness_evidence_review_disposition_action_readiness_execution_slot_key
                        .clone()
                }),
            first_readiness_evidence_review_disposition_action_key:
                first_row.map(|row| row.readiness_evidence_review_disposition_action_key.clone()),
            first_work_order_key: first_row.map(|row| row.work_order_key.clone()),
            first_ticket_key: first_row.map(|row| row.ticket_key.clone()),
            first_handoff_action_kind: first_row.map(|row| row.handoff_action_kind),
            first_handoff_lane: first_row.map(|row| row.handoff_lane),
            first_check_kind: first_row.map(|row| row.check_kind),
            first_check_status: first_row.map(|row| row.status),
            next_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_row_key:
                next_row.map(|row| row.handoff_action_evidence_row_key.clone()),
            next_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_row_key:
                next_row.map(|row| row.handoff_action_row_key.clone()),
            next_readiness_evidence_review_disposition_action_readiness_execution_handoff_row_key:
                next_row.map(|row| row.handoff_row_key.clone()),
            next_readiness_evidence_review_disposition_action_readiness_execution_slot_key:
                next_row.map(|row| {
                    row.readiness_evidence_review_disposition_action_readiness_execution_slot_key
                        .clone()
                }),
            next_readiness_evidence_review_disposition_action_key:
                next_row.map(|row| row.readiness_evidence_review_disposition_action_key.clone()),
            next_work_order_key: next_row.map(|row| row.work_order_key.clone()),
            next_ticket_key: next_row.map(|row| row.ticket_key.clone()),
            next_check_kind: next_row.map(|row| row.check_kind),
            next_check_status: next_row.map(|row| row.status),
            next_handoff_lane: next_row.map(|row| row.handoff_lane),
            release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_actions_ready:
                release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_summary
                    .ready_for_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_actions(),
            release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_ready,
            release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_summary:
                Box::new(
                    release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_summary,
                ),
        }
    }

    pub fn has_handoff_action_evidence_rows(&self) -> bool {
        self.total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows > 0
    }

    pub fn ready_for_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence(
        &self,
    ) -> bool {
        self.release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_actions_ready
            && self.release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_ready
            && !self.requires_attention()
    }

    pub fn has_required_evidence(&self) -> bool {
        self.required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows
            > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.release_blocking_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows
            > 0
            || self.repair_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows
                > 0
            || self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_summary
                .has_blockers()
    }

    pub fn has_review_work(&self) -> bool {
        self.review_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows
            > 0
            || self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_summary
                .has_review_work()
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows
            > 0
            || self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_summary
                .needs_operator()
    }

    pub fn requires_dispatch(&self) -> bool {
        self.dispatch_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows
            > 0
            || self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_summary
                .requires_dispatch()
    }

    pub fn requires_execution(&self) -> bool {
        self.execution_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows
            > 0
            || self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_summary
                .requires_execution()
    }

    pub fn has_lineage_gaps(&self) -> bool {
        self.lineage_complete_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows
            < self.total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows
            || self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_summary
                .has_lineage_gaps()
    }

    pub fn has_release_handoff_evidence(&self) -> bool {
        self.release_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows
            > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.has_required_evidence()
            || self.has_blockers()
            || self.has_review_work()
            || self.needs_operator()
            || self.requires_dispatch()
            || self.requires_execution()
            || self.has_lineage_gaps()
            || !self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_ready
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePacketRow
{
    pub sequence: usize,
    pub protocol_evidence_packet_key: String,
    pub protocol: ProtocolFamily,
    pub total_substrate_rows: usize,
    pub ready_substrate_rows: usize,
    pub blocked_substrate_rows: usize,
    pub total_handoff_action_evidence_rows: usize,
    pub required_handoff_action_evidence_rows: usize,
    pub release_blocking_handoff_action_evidence_rows: usize,
    pub operator_required_handoff_action_evidence_rows: usize,
    pub review_required_handoff_action_evidence_rows: usize,
    pub dispatch_required_handoff_action_evidence_rows: usize,
    pub execution_required_handoff_action_evidence_rows: usize,
    pub lineage_complete_handoff_action_evidence_rows: usize,
    pub release_handoff_evidence_rows: usize,
    pub release_ready_handoff_action_evidence_rows: usize,
    pub first_handoff_action_evidence_row_key: Option<String>,
    pub first_required_handoff_action_evidence_row_key: Option<String>,
    pub first_blocked_substrate_stage: Option<IntegrationMeshProtocolSubstrateStage>,
    pub handoff_action_evidence_ready: bool,
    pub protocol_substrate_ready: bool,
    pub protocol_evidence_packet_ready: bool,
}

impl
    IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePacketRow
{
    pub fn from_protocol<'a>(
        sequence: usize,
        protocol: ProtocolFamily,
        substrate_rows: impl IntoIterator<Item = &'a IntegrationMeshProtocolSubstrateStageRow>,
        evidence_summary: &IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceSummary,
        evidence_rows: impl IntoIterator<
            Item = &'a IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceRow,
        >,
    ) -> Self {
        let substrate_rows = substrate_rows.into_iter().collect::<Vec<_>>();
        let evidence_rows = evidence_rows.into_iter().collect::<Vec<_>>();
        let first_evidence_row = evidence_rows.first().copied();
        let first_required_evidence_row = evidence_rows
            .iter()
            .copied()
            .find(|row| row.requires_attention());
        let first_blocked_substrate_stage = substrate_rows
            .iter()
            .find(|row| row.is_blocked())
            .map(|row| row.stage);
        let ready_substrate_rows = substrate_rows.iter().filter(|row| row.is_ready()).count();
        let blocked_substrate_rows = substrate_rows.len().saturating_sub(ready_substrate_rows);
        let handoff_action_evidence_ready = evidence_summary
            .ready_for_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence();
        let protocol_substrate_ready = !substrate_rows.is_empty() && blocked_substrate_rows == 0;
        let protocol_evidence_packet_ready = protocol_substrate_ready
            && handoff_action_evidence_ready
            && evidence_summary
                .required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows
                == 0;

        Self {
            sequence,
            protocol_evidence_packet_key: format!(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-protocol-evidence-packet-{sequence:02}-{}",
                protocol_sort_token(&protocol)
            ),
            protocol,
            total_substrate_rows: substrate_rows.len(),
            ready_substrate_rows,
            blocked_substrate_rows,
            total_handoff_action_evidence_rows: evidence_summary
                .total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows,
            required_handoff_action_evidence_rows: evidence_summary
                .required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows,
            release_blocking_handoff_action_evidence_rows: evidence_summary
                .release_blocking_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows,
            operator_required_handoff_action_evidence_rows: evidence_summary
                .operator_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows,
            review_required_handoff_action_evidence_rows: evidence_summary
                .review_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows,
            dispatch_required_handoff_action_evidence_rows: evidence_summary
                .dispatch_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows,
            execution_required_handoff_action_evidence_rows: evidence_summary
                .execution_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows,
            lineage_complete_handoff_action_evidence_rows: evidence_summary
                .lineage_complete_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows,
            release_handoff_evidence_rows: evidence_summary
                .release_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows,
            release_ready_handoff_action_evidence_rows: evidence_summary
                .release_ready_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows,
            first_handoff_action_evidence_row_key: first_evidence_row
                .map(|row| row.handoff_action_evidence_row_key.clone()),
            first_required_handoff_action_evidence_row_key: first_required_evidence_row
                .map(|row| row.handoff_action_evidence_row_key.clone()),
            first_blocked_substrate_stage,
            handoff_action_evidence_ready,
            protocol_substrate_ready,
            protocol_evidence_packet_ready,
        }
    }

    pub fn has_blocked_substrate(&self) -> bool {
        self.blocked_substrate_rows > 0
    }

    pub fn has_required_evidence(&self) -> bool {
        self.required_handoff_action_evidence_rows > 0
    }

    pub fn ready_for_protocol_evidence_packet(&self) -> bool {
        self.protocol_evidence_packet_ready
    }

    pub fn requires_attention(&self) -> bool {
        self.has_blocked_substrate()
            || self.has_required_evidence()
            || !self.handoff_action_evidence_ready
            || !self.protocol_substrate_ready
            || !self.protocol_evidence_packet_ready
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePacketSummary
{
    pub total_protocol_evidence_packets: usize,
    pub ready_protocol_evidence_packets: usize,
    pub blocked_protocol_evidence_packets: usize,
    pub protocol_substrate_blockers: usize,
    pub required_handoff_action_evidence_rows: usize,
    pub release_blocking_handoff_action_evidence_rows: usize,
    pub operator_required_handoff_action_evidence_rows: usize,
    pub review_required_handoff_action_evidence_rows: usize,
    pub dispatch_required_handoff_action_evidence_rows: usize,
    pub execution_required_handoff_action_evidence_rows: usize,
    pub release_handoff_evidence_rows: usize,
    pub release_ready_handoff_action_evidence_rows: usize,
    pub first_protocol_evidence_packet_key: Option<String>,
    pub first_required_protocol_evidence_packet_key: Option<String>,
    pub first_protocol: Option<ProtocolFamily>,
    pub first_blocked_protocol: Option<ProtocolFamily>,
    pub first_blocked_substrate_stage: Option<IntegrationMeshProtocolSubstrateStage>,
    pub first_handoff_action_evidence_row_key: Option<String>,
    pub first_required_handoff_action_evidence_row_key: Option<String>,
    pub handoff_action_evidence_ready: bool,
    pub protocol_evidence_packets_ready: bool,
}

impl
    IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePacketSummary
{
    pub fn from_rows<'a>(
        rows: impl IntoIterator<
            Item = &'a IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePacketRow,
        >,
    ) -> Self {
        let rows = rows.into_iter().collect::<Vec<_>>();
        let first_row = rows.first().copied();
        let first_required_row = rows.iter().copied().find(|row| row.requires_attention());
        let first_blocked_row = rows.iter().copied().find(|row| row.has_blocked_substrate());
        let ready_protocol_evidence_packets = rows
            .iter()
            .filter(|row| row.ready_for_protocol_evidence_packet())
            .count();
        let handoff_action_evidence_ready = rows
            .iter()
            .all(|row| row.handoff_action_evidence_ready);
        let protocol_evidence_packets_ready = !rows.is_empty()
            && ready_protocol_evidence_packets == rows.len()
            && handoff_action_evidence_ready;

        Self {
            total_protocol_evidence_packets: rows.len(),
            ready_protocol_evidence_packets,
            blocked_protocol_evidence_packets: rows.len().saturating_sub(ready_protocol_evidence_packets),
            protocol_substrate_blockers: rows.iter().map(|row| row.blocked_substrate_rows).sum(),
            required_handoff_action_evidence_rows: first_row
                .map(|row| row.required_handoff_action_evidence_rows)
                .unwrap_or(0),
            release_blocking_handoff_action_evidence_rows: first_row
                .map(|row| row.release_blocking_handoff_action_evidence_rows)
                .unwrap_or(0),
            operator_required_handoff_action_evidence_rows: first_row
                .map(|row| row.operator_required_handoff_action_evidence_rows)
                .unwrap_or(0),
            review_required_handoff_action_evidence_rows: first_row
                .map(|row| row.review_required_handoff_action_evidence_rows)
                .unwrap_or(0),
            dispatch_required_handoff_action_evidence_rows: first_row
                .map(|row| row.dispatch_required_handoff_action_evidence_rows)
                .unwrap_or(0),
            execution_required_handoff_action_evidence_rows: first_row
                .map(|row| row.execution_required_handoff_action_evidence_rows)
                .unwrap_or(0),
            release_handoff_evidence_rows: first_row
                .map(|row| row.release_handoff_evidence_rows)
                .unwrap_or(0),
            release_ready_handoff_action_evidence_rows: first_row
                .map(|row| row.release_ready_handoff_action_evidence_rows)
                .unwrap_or(0),
            first_protocol_evidence_packet_key: first_row
                .map(|row| row.protocol_evidence_packet_key.clone()),
            first_required_protocol_evidence_packet_key: first_required_row
                .map(|row| row.protocol_evidence_packet_key.clone()),
            first_protocol: first_row.map(|row| row.protocol.clone()),
            first_blocked_protocol: first_blocked_row.map(|row| row.protocol.clone()),
            first_blocked_substrate_stage: first_blocked_row
                .and_then(|row| row.first_blocked_substrate_stage),
            first_handoff_action_evidence_row_key: first_row
                .and_then(|row| row.first_handoff_action_evidence_row_key.clone()),
            first_required_handoff_action_evidence_row_key: first_required_row
                .and_then(|row| row.first_required_handoff_action_evidence_row_key.clone()),
            handoff_action_evidence_ready,
            protocol_evidence_packets_ready,
        }
    }

    pub fn ready_for_release_ticket_handoff_protocol_evidence_packets(&self) -> bool {
        self.protocol_evidence_packets_ready && !self.requires_attention()
    }

    pub fn has_blockers(&self) -> bool {
        self.protocol_substrate_blockers > 0
            || self.release_blocking_handoff_action_evidence_rows > 0
    }

    pub fn has_required_evidence(&self) -> bool {
        self.required_handoff_action_evidence_rows > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.blocked_protocol_evidence_packets > 0
            || self.has_blockers()
            || self.has_required_evidence()
            || !self.handoff_action_evidence_ready
            || !self.protocol_evidence_packets_ready
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageReadinessSummary {
    pub release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_packet_summary:
        Box<IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePacketSummary>,
    pub protocol_evidence_package_key: String,
    pub total_protocol_evidence_packets: usize,
    pub ready_protocol_evidence_packets: usize,
    pub blocked_protocol_evidence_packets: usize,
    pub protocol_substrate_blockers: usize,
    pub required_handoff_action_evidence_rows: usize,
    pub release_blocking_handoff_action_evidence_rows: usize,
    pub operator_required_handoff_action_evidence_rows: usize,
    pub review_required_handoff_action_evidence_rows: usize,
    pub dispatch_required_handoff_action_evidence_rows: usize,
    pub execution_required_handoff_action_evidence_rows: usize,
    pub release_handoff_evidence_rows: usize,
    pub release_ready_handoff_action_evidence_rows: usize,
    pub first_protocol_evidence_packet_key: Option<String>,
    pub next_protocol_evidence_packet_key: Option<String>,
    pub next_handoff_action_evidence_row_key: Option<String>,
    pub next_protocol: Option<ProtocolFamily>,
    pub next_blocked_substrate_stage: Option<IntegrationMeshProtocolSubstrateStage>,
    pub handoff_action_evidence_ready: bool,
    pub protocol_evidence_packets_ready: bool,
    pub release_protocol_evidence_package_ready: bool,
}

impl
    IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageReadinessSummary
{
    pub fn from_packet_summary(
        packet_summary: IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePacketSummary,
    ) -> Self {
        let release_protocol_evidence_package_ready = packet_summary
            .ready_for_release_ticket_handoff_protocol_evidence_packets();

        Self {
            protocol_evidence_package_key:
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-protocol-evidence-package"
                    .to_string(),
            total_protocol_evidence_packets: packet_summary.total_protocol_evidence_packets,
            ready_protocol_evidence_packets: packet_summary.ready_protocol_evidence_packets,
            blocked_protocol_evidence_packets: packet_summary.blocked_protocol_evidence_packets,
            protocol_substrate_blockers: packet_summary.protocol_substrate_blockers,
            required_handoff_action_evidence_rows: packet_summary
                .required_handoff_action_evidence_rows,
            release_blocking_handoff_action_evidence_rows: packet_summary
                .release_blocking_handoff_action_evidence_rows,
            operator_required_handoff_action_evidence_rows: packet_summary
                .operator_required_handoff_action_evidence_rows,
            review_required_handoff_action_evidence_rows: packet_summary
                .review_required_handoff_action_evidence_rows,
            dispatch_required_handoff_action_evidence_rows: packet_summary
                .dispatch_required_handoff_action_evidence_rows,
            execution_required_handoff_action_evidence_rows: packet_summary
                .execution_required_handoff_action_evidence_rows,
            release_handoff_evidence_rows: packet_summary.release_handoff_evidence_rows,
            release_ready_handoff_action_evidence_rows: packet_summary
                .release_ready_handoff_action_evidence_rows,
            first_protocol_evidence_packet_key: packet_summary
                .first_protocol_evidence_packet_key
                .clone(),
            next_protocol_evidence_packet_key: packet_summary
                .first_required_protocol_evidence_packet_key
                .clone(),
            next_handoff_action_evidence_row_key: packet_summary
                .first_required_handoff_action_evidence_row_key
                .clone(),
            next_protocol: packet_summary.first_blocked_protocol.clone(),
            next_blocked_substrate_stage: packet_summary.first_blocked_substrate_stage,
            handoff_action_evidence_ready: packet_summary.handoff_action_evidence_ready,
            protocol_evidence_packets_ready: packet_summary.protocol_evidence_packets_ready,
            release_protocol_evidence_package_ready,
            release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_packet_summary:
                Box::new(packet_summary),
        }
    }

    pub fn ready_for_release_ticket_handoff_protocol_evidence_package(&self) -> bool {
        self.release_protocol_evidence_package_ready && !self.requires_attention()
    }

    pub fn has_blockers(&self) -> bool {
        self.protocol_substrate_blockers > 0
            || self.release_blocking_handoff_action_evidence_rows > 0
    }

    pub fn has_required_evidence(&self) -> bool {
        self.required_handoff_action_evidence_rows > 0
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_handoff_action_evidence_rows > 0
    }

    pub fn has_review_work(&self) -> bool {
        self.review_required_handoff_action_evidence_rows > 0
    }

    pub fn requires_dispatch(&self) -> bool {
        self.dispatch_required_handoff_action_evidence_rows > 0
    }

    pub fn requires_execution(&self) -> bool {
        self.execution_required_handoff_action_evidence_rows > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.blocked_protocol_evidence_packets > 0
            || self.has_blockers()
            || self.has_required_evidence()
            || self.needs_operator()
            || self.has_review_work()
            || self.requires_dispatch()
            || self.requires_execution()
            || !self.handoff_action_evidence_ready
            || !self.protocol_evidence_packets_ready
            || !self.release_protocol_evidence_package_ready
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffRow
{
    pub sequence: usize,
    pub protocol_evidence_package_handoff_row_key: String,
    pub protocol_evidence_package_key: String,
    pub protocol_evidence_packet_key: String,
    pub protocol: ProtocolFamily,
    pub total_substrate_rows: usize,
    pub ready_substrate_rows: usize,
    pub blocked_substrate_rows: usize,
    pub total_handoff_action_evidence_rows: usize,
    pub required_handoff_action_evidence_rows: usize,
    pub release_blocking_handoff_action_evidence_rows: usize,
    pub operator_required_handoff_action_evidence_rows: usize,
    pub review_required_handoff_action_evidence_rows: usize,
    pub dispatch_required_handoff_action_evidence_rows: usize,
    pub execution_required_handoff_action_evidence_rows: usize,
    pub release_handoff_evidence_rows: usize,
    pub release_ready_handoff_action_evidence_rows: usize,
    pub first_handoff_action_evidence_row_key: Option<String>,
    pub first_required_handoff_action_evidence_row_key: Option<String>,
    pub first_blocked_substrate_stage: Option<IntegrationMeshProtocolSubstrateStage>,
    pub handoff_action_evidence_ready: bool,
    pub protocol_substrate_ready: bool,
    pub protocol_evidence_packet_ready: bool,
    pub protocol_evidence_package_ready: bool,
    pub package_handoff_ready: bool,
}

impl
    IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffRow
{
    pub fn from_packet_row(
        sequence: usize,
        packet_row: &IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePacketRow,
        package_summary: &IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageReadinessSummary,
    ) -> Self {
        let protocol_evidence_package_ready =
            package_summary.ready_for_release_ticket_handoff_protocol_evidence_package();
        let package_handoff_ready =
            protocol_evidence_package_ready && packet_row.ready_for_protocol_evidence_packet();

        Self {
            sequence,
            protocol_evidence_package_handoff_row_key: format!(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-protocol-evidence-package-handoff-row-{sequence:02}-{}",
                protocol_sort_token(&packet_row.protocol)
            ),
            protocol_evidence_package_key: package_summary.protocol_evidence_package_key.clone(),
            protocol_evidence_packet_key: packet_row.protocol_evidence_packet_key.clone(),
            protocol: packet_row.protocol.clone(),
            total_substrate_rows: packet_row.total_substrate_rows,
            ready_substrate_rows: packet_row.ready_substrate_rows,
            blocked_substrate_rows: packet_row.blocked_substrate_rows,
            total_handoff_action_evidence_rows: packet_row.total_handoff_action_evidence_rows,
            required_handoff_action_evidence_rows: packet_row
                .required_handoff_action_evidence_rows,
            release_blocking_handoff_action_evidence_rows: packet_row
                .release_blocking_handoff_action_evidence_rows,
            operator_required_handoff_action_evidence_rows: packet_row
                .operator_required_handoff_action_evidence_rows,
            review_required_handoff_action_evidence_rows: packet_row
                .review_required_handoff_action_evidence_rows,
            dispatch_required_handoff_action_evidence_rows: packet_row
                .dispatch_required_handoff_action_evidence_rows,
            execution_required_handoff_action_evidence_rows: packet_row
                .execution_required_handoff_action_evidence_rows,
            release_handoff_evidence_rows: packet_row.release_handoff_evidence_rows,
            release_ready_handoff_action_evidence_rows: packet_row
                .release_ready_handoff_action_evidence_rows,
            first_handoff_action_evidence_row_key: packet_row
                .first_handoff_action_evidence_row_key
                .clone(),
            first_required_handoff_action_evidence_row_key: packet_row
                .first_required_handoff_action_evidence_row_key
                .clone(),
            first_blocked_substrate_stage: packet_row.first_blocked_substrate_stage,
            handoff_action_evidence_ready: packet_row.handoff_action_evidence_ready,
            protocol_substrate_ready: packet_row.protocol_substrate_ready,
            protocol_evidence_packet_ready: packet_row.protocol_evidence_packet_ready,
            protocol_evidence_package_ready,
            package_handoff_ready,
        }
    }

    pub fn has_blocked_substrate(&self) -> bool {
        self.blocked_substrate_rows > 0
    }

    pub fn has_required_evidence(&self) -> bool {
        self.required_handoff_action_evidence_rows > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.has_blocked_substrate() || self.release_blocking_handoff_action_evidence_rows > 0
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_handoff_action_evidence_rows > 0
    }

    pub fn has_review_work(&self) -> bool {
        self.review_required_handoff_action_evidence_rows > 0
    }

    pub fn requires_dispatch(&self) -> bool {
        self.dispatch_required_handoff_action_evidence_rows > 0
    }

    pub fn requires_execution(&self) -> bool {
        self.execution_required_handoff_action_evidence_rows > 0
    }

    pub fn ready_for_package_handoff(&self) -> bool {
        self.package_handoff_ready && !self.requires_attention()
    }

    pub fn requires_attention(&self) -> bool {
        self.has_blockers()
            || self.has_required_evidence()
            || self.needs_operator()
            || self.has_review_work()
            || self.requires_dispatch()
            || self.requires_execution()
            || !self.handoff_action_evidence_ready
            || !self.protocol_substrate_ready
            || !self.protocol_evidence_packet_ready
            || !self.protocol_evidence_package_ready
            || !self.package_handoff_ready
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffSummary
{
    pub release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_readiness_summary:
        Box<IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageReadinessSummary>,
    pub total_protocol_evidence_package_handoff_rows: usize,
    pub ready_protocol_evidence_package_handoff_rows: usize,
    pub blocked_protocol_evidence_package_handoff_rows: usize,
    pub protocol_substrate_blockers: usize,
    pub required_handoff_action_evidence_rows: usize,
    pub release_blocking_handoff_action_evidence_rows: usize,
    pub operator_required_handoff_action_evidence_rows: usize,
    pub review_required_handoff_action_evidence_rows: usize,
    pub dispatch_required_handoff_action_evidence_rows: usize,
    pub execution_required_handoff_action_evidence_rows: usize,
    pub release_handoff_evidence_rows: usize,
    pub release_ready_handoff_action_evidence_rows: usize,
    pub first_protocol_evidence_package_handoff_row_key: Option<String>,
    pub first_required_protocol_evidence_package_handoff_row_key: Option<String>,
    pub first_ready_protocol_evidence_package_handoff_row_key: Option<String>,
    pub first_protocol_evidence_packet_key: Option<String>,
    pub next_protocol_evidence_package_handoff_row_key: Option<String>,
    pub next_protocol_evidence_packet_key: Option<String>,
    pub next_handoff_action_evidence_row_key: Option<String>,
    pub next_protocol: Option<ProtocolFamily>,
    pub next_blocked_substrate_stage: Option<IntegrationMeshProtocolSubstrateStage>,
    pub handoff_action_evidence_ready: bool,
    pub protocol_evidence_package_ready: bool,
    pub protocol_evidence_package_handoff_rows_ready: bool,
}

impl
    IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffSummary
{
    pub fn from_summaries<'a>(
        package_readiness_summary: IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageReadinessSummary,
        rows: impl IntoIterator<
            Item = &'a IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffRow,
        >,
    ) -> Self {
        let rows = rows.into_iter().collect::<Vec<_>>();
        let first_row = rows.first().copied();
        let first_required_row = rows.iter().copied().find(|row| row.requires_attention());
        let first_ready_row = rows
            .iter()
            .copied()
            .find(|row| row.ready_for_package_handoff());
        let ready_protocol_evidence_package_handoff_rows = rows
            .iter()
            .filter(|row| row.ready_for_package_handoff())
            .count();
        let protocol_evidence_package_handoff_rows_ready = !rows.is_empty()
            && rows.len() == package_readiness_summary.total_protocol_evidence_packets
            && ready_protocol_evidence_package_handoff_rows == rows.len()
            && package_readiness_summary
                .ready_for_release_ticket_handoff_protocol_evidence_package();

        Self {
            total_protocol_evidence_package_handoff_rows: rows.len(),
            ready_protocol_evidence_package_handoff_rows,
            blocked_protocol_evidence_package_handoff_rows: rows
                .len()
                .saturating_sub(ready_protocol_evidence_package_handoff_rows),
            protocol_substrate_blockers: rows.iter().map(|row| row.blocked_substrate_rows).sum(),
            required_handoff_action_evidence_rows: first_row
                .map(|row| row.required_handoff_action_evidence_rows)
                .unwrap_or(0),
            release_blocking_handoff_action_evidence_rows: first_row
                .map(|row| row.release_blocking_handoff_action_evidence_rows)
                .unwrap_or(0),
            operator_required_handoff_action_evidence_rows: first_row
                .map(|row| row.operator_required_handoff_action_evidence_rows)
                .unwrap_or(0),
            review_required_handoff_action_evidence_rows: first_row
                .map(|row| row.review_required_handoff_action_evidence_rows)
                .unwrap_or(0),
            dispatch_required_handoff_action_evidence_rows: first_row
                .map(|row| row.dispatch_required_handoff_action_evidence_rows)
                .unwrap_or(0),
            execution_required_handoff_action_evidence_rows: first_row
                .map(|row| row.execution_required_handoff_action_evidence_rows)
                .unwrap_or(0),
            release_handoff_evidence_rows: first_row
                .map(|row| row.release_handoff_evidence_rows)
                .unwrap_or(0),
            release_ready_handoff_action_evidence_rows: first_row
                .map(|row| row.release_ready_handoff_action_evidence_rows)
                .unwrap_or(0),
            first_protocol_evidence_package_handoff_row_key: first_row
                .map(|row| row.protocol_evidence_package_handoff_row_key.clone()),
            first_required_protocol_evidence_package_handoff_row_key: first_required_row
                .map(|row| row.protocol_evidence_package_handoff_row_key.clone()),
            first_ready_protocol_evidence_package_handoff_row_key: first_ready_row
                .map(|row| row.protocol_evidence_package_handoff_row_key.clone()),
            first_protocol_evidence_packet_key: first_row
                .map(|row| row.protocol_evidence_packet_key.clone()),
            next_protocol_evidence_package_handoff_row_key: first_required_row
                .map(|row| row.protocol_evidence_package_handoff_row_key.clone()),
            next_protocol_evidence_packet_key: first_required_row
                .map(|row| row.protocol_evidence_packet_key.clone()),
            next_handoff_action_evidence_row_key: first_required_row
                .and_then(|row| row.first_required_handoff_action_evidence_row_key.clone()),
            next_protocol: first_required_row.map(|row| row.protocol.clone()),
            next_blocked_substrate_stage: first_required_row
                .and_then(|row| row.first_blocked_substrate_stage),
            handoff_action_evidence_ready: rows
                .iter()
                .all(|row| row.handoff_action_evidence_ready),
            protocol_evidence_package_ready: package_readiness_summary
                .ready_for_release_ticket_handoff_protocol_evidence_package(),
            protocol_evidence_package_handoff_rows_ready,
            release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_readiness_summary:
                Box::new(package_readiness_summary),
        }
    }

    pub fn ready_for_release_ticket_handoff_protocol_evidence_package_handoff(&self) -> bool {
        self.protocol_evidence_package_handoff_rows_ready && !self.requires_attention()
    }

    pub fn has_blockers(&self) -> bool {
        self.protocol_substrate_blockers > 0
            || self.release_blocking_handoff_action_evidence_rows > 0
    }

    pub fn has_required_evidence(&self) -> bool {
        self.required_handoff_action_evidence_rows > 0
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_handoff_action_evidence_rows > 0
    }

    pub fn has_review_work(&self) -> bool {
        self.review_required_handoff_action_evidence_rows > 0
    }

    pub fn requires_dispatch(&self) -> bool {
        self.dispatch_required_handoff_action_evidence_rows > 0
    }

    pub fn requires_execution(&self) -> bool {
        self.execution_required_handoff_action_evidence_rows > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.blocked_protocol_evidence_package_handoff_rows > 0
            || self.has_blockers()
            || self.has_required_evidence()
            || self.needs_operator()
            || self.has_review_work()
            || self.requires_dispatch()
            || self.requires_execution()
            || !self.handoff_action_evidence_ready
            || !self.protocol_evidence_package_ready
            || !self.protocol_evidence_package_handoff_rows_ready
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionSlot
{
    pub sequence: usize,
    pub protocol_evidence_package_handoff_execution_slot_key: String,
    pub protocol_evidence_package_handoff_row_sequence: usize,
    pub protocol_evidence_package_handoff_row_key: String,
    pub protocol_evidence_package_key: String,
    pub protocol_evidence_packet_key: String,
    pub protocol: ProtocolFamily,
    pub handoff_lane: IntegrationMeshReleaseDispatchTicketHandoffLane,
    pub total_substrate_rows: usize,
    pub ready_substrate_rows: usize,
    pub blocked_substrate_rows: usize,
    pub total_handoff_action_evidence_rows: usize,
    pub required_handoff_action_evidence_rows: usize,
    pub release_blocking_handoff_action_evidence_rows: usize,
    pub operator_required_handoff_action_evidence_rows: usize,
    pub review_required_handoff_action_evidence_rows: usize,
    pub dispatch_required_handoff_action_evidence_rows: usize,
    pub execution_required_handoff_action_evidence_rows: usize,
    pub release_handoff_evidence_rows: usize,
    pub release_ready_handoff_action_evidence_rows: usize,
    pub first_handoff_action_evidence_row_key: Option<String>,
    pub first_required_handoff_action_evidence_row_key: Option<String>,
    pub first_blocked_substrate_stage: Option<IntegrationMeshProtocolSubstrateStage>,
    pub handoff_action_evidence_ready: bool,
    pub protocol_substrate_ready: bool,
    pub protocol_evidence_packet_ready: bool,
    pub protocol_evidence_package_ready: bool,
    pub package_handoff_ready: bool,
    pub protocol_evidence_package_handoff_ready: bool,
    pub ready_for_package_handoff_execution: bool,
}

impl
    IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionSlot
{
    pub fn from_handoff_row(
        sequence: usize,
        row: &IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffRow,
        protocol_evidence_package_handoff_ready: bool,
    ) -> Self {
        let handoff_lane = if row.ready_for_package_handoff() {
            IntegrationMeshReleaseDispatchTicketHandoffLane::Release
        } else if row.has_review_work() {
            IntegrationMeshReleaseDispatchTicketHandoffLane::Review
        } else if row.needs_operator() {
            IntegrationMeshReleaseDispatchTicketHandoffLane::Operator
        } else {
            IntegrationMeshReleaseDispatchTicketHandoffLane::Repair
        };
        let ready_for_package_handoff_execution =
            protocol_evidence_package_handoff_ready && row.ready_for_package_handoff();

        Self {
            sequence,
            protocol_evidence_package_handoff_execution_slot_key: format!(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-protocol-evidence-package-handoff-execution-slot-{sequence:02}-{}",
                protocol_sort_token(&row.protocol)
            ),
            protocol_evidence_package_handoff_row_sequence: row.sequence,
            protocol_evidence_package_handoff_row_key: row
                .protocol_evidence_package_handoff_row_key
                .clone(),
            protocol_evidence_package_key: row.protocol_evidence_package_key.clone(),
            protocol_evidence_packet_key: row.protocol_evidence_packet_key.clone(),
            protocol: row.protocol.clone(),
            handoff_lane,
            total_substrate_rows: row.total_substrate_rows,
            ready_substrate_rows: row.ready_substrate_rows,
            blocked_substrate_rows: row.blocked_substrate_rows,
            total_handoff_action_evidence_rows: row.total_handoff_action_evidence_rows,
            required_handoff_action_evidence_rows: row.required_handoff_action_evidence_rows,
            release_blocking_handoff_action_evidence_rows: row
                .release_blocking_handoff_action_evidence_rows,
            operator_required_handoff_action_evidence_rows: row
                .operator_required_handoff_action_evidence_rows,
            review_required_handoff_action_evidence_rows: row
                .review_required_handoff_action_evidence_rows,
            dispatch_required_handoff_action_evidence_rows: row
                .dispatch_required_handoff_action_evidence_rows,
            execution_required_handoff_action_evidence_rows: row
                .execution_required_handoff_action_evidence_rows,
            release_handoff_evidence_rows: row.release_handoff_evidence_rows,
            release_ready_handoff_action_evidence_rows: row.release_ready_handoff_action_evidence_rows,
            first_handoff_action_evidence_row_key: row
                .first_handoff_action_evidence_row_key
                .clone(),
            first_required_handoff_action_evidence_row_key: row
                .first_required_handoff_action_evidence_row_key
                .clone(),
            first_blocked_substrate_stage: row.first_blocked_substrate_stage,
            handoff_action_evidence_ready: row.handoff_action_evidence_ready,
            protocol_substrate_ready: row.protocol_substrate_ready,
            protocol_evidence_packet_ready: row.protocol_evidence_packet_ready,
            protocol_evidence_package_ready: row.protocol_evidence_package_ready,
            package_handoff_ready: row.package_handoff_ready,
            protocol_evidence_package_handoff_ready,
            ready_for_package_handoff_execution,
        }
    }

    pub fn has_blocked_substrate(&self) -> bool {
        self.blocked_substrate_rows > 0
    }

    pub fn has_required_evidence(&self) -> bool {
        self.required_handoff_action_evidence_rows > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.has_blocked_substrate() || self.release_blocking_handoff_action_evidence_rows > 0
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_handoff_action_evidence_rows > 0
    }

    pub fn has_review_work(&self) -> bool {
        self.review_required_handoff_action_evidence_rows > 0
    }

    pub fn requires_dispatch(&self) -> bool {
        self.dispatch_required_handoff_action_evidence_rows > 0
    }

    pub fn requires_execution(&self) -> bool {
        self.execution_required_handoff_action_evidence_rows > 0
    }

    pub fn is_repair_slot(&self) -> bool {
        self.handoff_lane == IntegrationMeshReleaseDispatchTicketHandoffLane::Repair
    }

    pub fn is_operator_slot(&self) -> bool {
        self.handoff_lane == IntegrationMeshReleaseDispatchTicketHandoffLane::Operator
    }

    pub fn is_review_slot(&self) -> bool {
        self.handoff_lane == IntegrationMeshReleaseDispatchTicketHandoffLane::Review
    }

    pub fn is_release_slot(&self) -> bool {
        self.handoff_lane == IntegrationMeshReleaseDispatchTicketHandoffLane::Release
    }

    pub fn ready_for_package_handoff_execution(&self) -> bool {
        self.ready_for_package_handoff_execution && !self.requires_attention()
    }

    pub fn requires_attention(&self) -> bool {
        self.has_blockers()
            || self.has_required_evidence()
            || self.needs_operator()
            || self.has_review_work()
            || self.requires_dispatch()
            || self.requires_execution()
            || !self.handoff_action_evidence_ready
            || !self.protocol_substrate_ready
            || !self.protocol_evidence_packet_ready
            || !self.protocol_evidence_package_ready
            || !self.package_handoff_ready
            || !self.protocol_evidence_package_handoff_ready
            || !self.ready_for_package_handoff_execution
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionSlotSummary
{
    pub release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_summary:
        Box<IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffSummary>,
    pub total_protocol_evidence_package_handoff_rows: usize,
    pub total_protocol_evidence_package_handoff_execution_slots: usize,
    pub ready_protocol_evidence_package_handoff_execution_slots: usize,
    pub blocked_protocol_evidence_package_handoff_execution_slots: usize,
    pub repair_protocol_evidence_package_handoff_execution_slots: usize,
    pub operator_protocol_evidence_package_handoff_execution_slots: usize,
    pub review_protocol_evidence_package_handoff_execution_slots: usize,
    pub release_protocol_evidence_package_handoff_execution_slots: usize,
    pub protocol_substrate_blockers: usize,
    pub required_handoff_action_evidence_rows: usize,
    pub release_blocking_handoff_action_evidence_rows: usize,
    pub operator_required_handoff_action_evidence_rows: usize,
    pub review_required_handoff_action_evidence_rows: usize,
    pub dispatch_required_handoff_action_evidence_rows: usize,
    pub execution_required_handoff_action_evidence_rows: usize,
    pub release_handoff_evidence_rows: usize,
    pub release_ready_handoff_action_evidence_rows: usize,
    pub first_protocol_evidence_package_handoff_execution_slot_key: Option<String>,
    pub first_required_protocol_evidence_package_handoff_execution_slot_key: Option<String>,
    pub first_ready_protocol_evidence_package_handoff_execution_slot_key: Option<String>,
    pub first_release_protocol_evidence_package_handoff_execution_slot_key: Option<String>,
    pub first_protocol_evidence_package_handoff_row_key: Option<String>,
    pub first_protocol_evidence_packet_key: Option<String>,
    pub next_protocol_evidence_package_handoff_execution_slot_key: Option<String>,
    pub next_protocol_evidence_package_handoff_row_key: Option<String>,
    pub next_protocol_evidence_packet_key: Option<String>,
    pub next_handoff_action_evidence_row_key: Option<String>,
    pub next_protocol: Option<ProtocolFamily>,
    pub next_blocked_substrate_stage: Option<IntegrationMeshProtocolSubstrateStage>,
    pub next_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub protocol_evidence_package_handoff_ready: bool,
    pub protocol_evidence_package_handoff_execution_slots_ready: bool,
}

impl
    IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionSlotSummary
{
    pub fn from_summaries<'a>(
        handoff_summary: IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffSummary,
        slots: impl IntoIterator<
            Item = &'a IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionSlot,
        >,
    ) -> Self {
        let slots: Vec<&IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionSlot> =
            slots.into_iter().collect();
        let first_slot = slots.first().copied();
        let first_required_slot = slots.iter().copied().find(|slot| slot.requires_attention());
        let first_ready_slot = slots
            .iter()
            .copied()
            .find(|slot| slot.ready_for_package_handoff_execution());
        let first_release_slot = slots.iter().copied().find(|slot| slot.is_release_slot());
        let ready_protocol_evidence_package_handoff_execution_slots = slots
            .iter()
            .filter(|slot| slot.ready_for_package_handoff_execution())
            .count();
        let protocol_evidence_package_handoff_execution_slots_ready = !slots.is_empty()
            && slots.len() == handoff_summary.total_protocol_evidence_package_handoff_rows
            && ready_protocol_evidence_package_handoff_execution_slots == slots.len()
            && handoff_summary
                .ready_for_release_ticket_handoff_protocol_evidence_package_handoff();

        Self {
            total_protocol_evidence_package_handoff_rows: handoff_summary
                .total_protocol_evidence_package_handoff_rows,
            total_protocol_evidence_package_handoff_execution_slots: slots.len(),
            ready_protocol_evidence_package_handoff_execution_slots,
            blocked_protocol_evidence_package_handoff_execution_slots: slots
                .len()
                .saturating_sub(ready_protocol_evidence_package_handoff_execution_slots),
            repair_protocol_evidence_package_handoff_execution_slots: slots
                .iter()
                .filter(|slot| slot.is_repair_slot())
                .count(),
            operator_protocol_evidence_package_handoff_execution_slots: slots
                .iter()
                .filter(|slot| slot.is_operator_slot())
                .count(),
            review_protocol_evidence_package_handoff_execution_slots: slots
                .iter()
                .filter(|slot| slot.is_review_slot())
                .count(),
            release_protocol_evidence_package_handoff_execution_slots: slots
                .iter()
                .filter(|slot| slot.is_release_slot())
                .count(),
            protocol_substrate_blockers: slots
                .iter()
                .map(|slot| slot.blocked_substrate_rows)
                .sum(),
            required_handoff_action_evidence_rows: first_slot
                .map(|slot| slot.required_handoff_action_evidence_rows)
                .unwrap_or(0),
            release_blocking_handoff_action_evidence_rows: first_slot
                .map(|slot| slot.release_blocking_handoff_action_evidence_rows)
                .unwrap_or(0),
            operator_required_handoff_action_evidence_rows: first_slot
                .map(|slot| slot.operator_required_handoff_action_evidence_rows)
                .unwrap_or(0),
            review_required_handoff_action_evidence_rows: first_slot
                .map(|slot| slot.review_required_handoff_action_evidence_rows)
                .unwrap_or(0),
            dispatch_required_handoff_action_evidence_rows: first_slot
                .map(|slot| slot.dispatch_required_handoff_action_evidence_rows)
                .unwrap_or(0),
            execution_required_handoff_action_evidence_rows: first_slot
                .map(|slot| slot.execution_required_handoff_action_evidence_rows)
                .unwrap_or(0),
            release_handoff_evidence_rows: first_slot
                .map(|slot| slot.release_handoff_evidence_rows)
                .unwrap_or(0),
            release_ready_handoff_action_evidence_rows: first_slot
                .map(|slot| slot.release_ready_handoff_action_evidence_rows)
                .unwrap_or(0),
            first_protocol_evidence_package_handoff_execution_slot_key: first_slot
                .map(|slot| slot.protocol_evidence_package_handoff_execution_slot_key.clone()),
            first_required_protocol_evidence_package_handoff_execution_slot_key:
                first_required_slot
                    .map(|slot| slot.protocol_evidence_package_handoff_execution_slot_key.clone()),
            first_ready_protocol_evidence_package_handoff_execution_slot_key: first_ready_slot
                .map(|slot| slot.protocol_evidence_package_handoff_execution_slot_key.clone()),
            first_release_protocol_evidence_package_handoff_execution_slot_key: first_release_slot
                .map(|slot| slot.protocol_evidence_package_handoff_execution_slot_key.clone()),
            first_protocol_evidence_package_handoff_row_key: first_slot
                .map(|slot| slot.protocol_evidence_package_handoff_row_key.clone()),
            first_protocol_evidence_packet_key: first_slot
                .map(|slot| slot.protocol_evidence_packet_key.clone()),
            next_protocol_evidence_package_handoff_execution_slot_key: first_required_slot
                .map(|slot| slot.protocol_evidence_package_handoff_execution_slot_key.clone()),
            next_protocol_evidence_package_handoff_row_key: first_required_slot
                .map(|slot| slot.protocol_evidence_package_handoff_row_key.clone()),
            next_protocol_evidence_packet_key: first_required_slot
                .map(|slot| slot.protocol_evidence_packet_key.clone()),
            next_handoff_action_evidence_row_key: first_required_slot
                .and_then(|slot| slot.first_required_handoff_action_evidence_row_key.clone()),
            next_protocol: first_required_slot.map(|slot| slot.protocol.clone()),
            next_blocked_substrate_stage: first_required_slot
                .and_then(|slot| slot.first_blocked_substrate_stage),
            next_handoff_lane: first_required_slot.map(|slot| slot.handoff_lane),
            protocol_evidence_package_handoff_ready: handoff_summary
                .ready_for_release_ticket_handoff_protocol_evidence_package_handoff(),
            protocol_evidence_package_handoff_execution_slots_ready,
            release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_summary:
                Box::new(handoff_summary),
        }
    }

    pub fn ready_for_release_ticket_handoff_protocol_evidence_package_handoff_execution(
        &self,
    ) -> bool {
        self.protocol_evidence_package_handoff_ready
            && self.protocol_evidence_package_handoff_execution_slots_ready
            && !self.requires_attention()
    }

    pub fn has_blockers(&self) -> bool {
        self.protocol_substrate_blockers > 0
            || self.release_blocking_handoff_action_evidence_rows > 0
    }

    pub fn has_required_evidence(&self) -> bool {
        self.required_handoff_action_evidence_rows > 0
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_handoff_action_evidence_rows > 0
            || self.operator_protocol_evidence_package_handoff_execution_slots > 0
    }

    pub fn has_review_work(&self) -> bool {
        self.review_required_handoff_action_evidence_rows > 0
            || self.review_protocol_evidence_package_handoff_execution_slots > 0
    }

    pub fn requires_dispatch(&self) -> bool {
        self.dispatch_required_handoff_action_evidence_rows > 0
    }

    pub fn requires_execution(&self) -> bool {
        self.execution_required_handoff_action_evidence_rows > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.blocked_protocol_evidence_package_handoff_execution_slots > 0
            || self.has_blockers()
            || self.has_required_evidence()
            || self.needs_operator()
            || self.has_review_work()
            || self.requires_dispatch()
            || self.requires_execution()
            || self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_summary
                .requires_attention()
            || !self.protocol_evidence_package_handoff_ready
            || !self.protocol_evidence_package_handoff_execution_slots_ready
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionActionRow
{
    pub sequence: usize,
    pub protocol_evidence_package_handoff_execution_action_row_key: String,
    pub protocol_evidence_package_handoff_execution_slot_sequence: usize,
    pub protocol_evidence_package_handoff_execution_slot_key: String,
    pub protocol_evidence_package_handoff_row_sequence: usize,
    pub protocol_evidence_package_handoff_row_key: String,
    pub protocol_evidence_package_key: String,
    pub protocol_evidence_packet_key: String,
    pub protocol: ProtocolFamily,
    pub handoff_lane: IntegrationMeshReleaseDispatchTicketHandoffLane,
    pub handoff_action_kind:
        IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind,
    pub total_substrate_rows: usize,
    pub ready_substrate_rows: usize,
    pub blocked_substrate_rows: usize,
    pub total_handoff_action_evidence_rows: usize,
    pub required_handoff_action_evidence_rows: usize,
    pub release_blocking_handoff_action_evidence_rows: usize,
    pub operator_required_handoff_action_evidence_rows: usize,
    pub review_required_handoff_action_evidence_rows: usize,
    pub dispatch_required_handoff_action_evidence_rows: usize,
    pub execution_required_handoff_action_evidence_rows: usize,
    pub release_handoff_evidence_rows: usize,
    pub release_ready_handoff_action_evidence_rows: usize,
    pub next_handoff_action_evidence_row_key: Option<String>,
    pub next_blocked_substrate_stage: Option<IntegrationMeshProtocolSubstrateStage>,
    pub handoff_action_evidence_ready: bool,
    pub protocol_substrate_ready: bool,
    pub protocol_evidence_packet_ready: bool,
    pub protocol_evidence_package_ready: bool,
    pub package_handoff_ready: bool,
    pub protocol_evidence_package_handoff_ready: bool,
    pub protocol_evidence_package_handoff_execution_slots_ready: bool,
    pub package_handoff_execution_ready: bool,
}

impl
    IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionActionRow
{
    pub fn from_execution_slot(
        sequence: usize,
        slot: &IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionSlot,
        protocol_evidence_package_handoff_execution_slots_ready: bool,
    ) -> Self {
        let handoff_action_kind = if slot.ready_for_package_handoff_execution()
            && protocol_evidence_package_handoff_execution_slots_ready
        {
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind::ReleaseHandoff
        } else if slot.has_blockers() || slot.is_repair_slot() {
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind::RepairHandoff
        } else if slot.has_review_work() || slot.is_review_slot() {
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind::ReviewHandoff
        } else if slot.needs_operator() || slot.is_operator_slot() {
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind::OperatorHandoff
        } else if slot.requires_dispatch() {
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind::DispatchHandoff
        } else if slot.requires_execution() {
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind::ExecuteHandoff
        } else {
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind::PreserveLineage
        };

        Self {
            sequence,
            protocol_evidence_package_handoff_execution_action_row_key: format!(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-protocol-evidence-package-handoff-execution-action-{sequence:02}-{}-{}",
                handoff_action_kind.as_str(),
                protocol_sort_token(&slot.protocol)
            ),
            protocol_evidence_package_handoff_execution_slot_sequence: slot.sequence,
            protocol_evidence_package_handoff_execution_slot_key: slot
                .protocol_evidence_package_handoff_execution_slot_key
                .clone(),
            protocol_evidence_package_handoff_row_sequence: slot
                .protocol_evidence_package_handoff_row_sequence,
            protocol_evidence_package_handoff_row_key: slot
                .protocol_evidence_package_handoff_row_key
                .clone(),
            protocol_evidence_package_key: slot.protocol_evidence_package_key.clone(),
            protocol_evidence_packet_key: slot.protocol_evidence_packet_key.clone(),
            protocol: slot.protocol.clone(),
            handoff_lane: slot.handoff_lane,
            handoff_action_kind,
            total_substrate_rows: slot.total_substrate_rows,
            ready_substrate_rows: slot.ready_substrate_rows,
            blocked_substrate_rows: slot.blocked_substrate_rows,
            total_handoff_action_evidence_rows: slot.total_handoff_action_evidence_rows,
            required_handoff_action_evidence_rows: slot.required_handoff_action_evidence_rows,
            release_blocking_handoff_action_evidence_rows: slot
                .release_blocking_handoff_action_evidence_rows,
            operator_required_handoff_action_evidence_rows: slot
                .operator_required_handoff_action_evidence_rows,
            review_required_handoff_action_evidence_rows: slot
                .review_required_handoff_action_evidence_rows,
            dispatch_required_handoff_action_evidence_rows: slot
                .dispatch_required_handoff_action_evidence_rows,
            execution_required_handoff_action_evidence_rows: slot
                .execution_required_handoff_action_evidence_rows,
            release_handoff_evidence_rows: slot.release_handoff_evidence_rows,
            release_ready_handoff_action_evidence_rows: slot
                .release_ready_handoff_action_evidence_rows,
            next_handoff_action_evidence_row_key: slot
                .first_required_handoff_action_evidence_row_key
                .clone(),
            next_blocked_substrate_stage: slot.first_blocked_substrate_stage,
            handoff_action_evidence_ready: slot.handoff_action_evidence_ready,
            protocol_substrate_ready: slot.protocol_substrate_ready,
            protocol_evidence_packet_ready: slot.protocol_evidence_packet_ready,
            protocol_evidence_package_ready: slot.protocol_evidence_package_ready,
            package_handoff_ready: slot.package_handoff_ready,
            protocol_evidence_package_handoff_ready: slot.protocol_evidence_package_handoff_ready,
            protocol_evidence_package_handoff_execution_slots_ready,
            package_handoff_execution_ready: slot.ready_for_package_handoff_execution(),
        }
    }

    pub fn blocks_release(&self) -> bool {
        self.blocked_substrate_rows > 0 || self.release_blocking_handoff_action_evidence_rows > 0
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_handoff_action_evidence_rows > 0
    }

    pub fn has_review_work(&self) -> bool {
        self.review_required_handoff_action_evidence_rows > 0
    }

    pub fn requires_dispatch(&self) -> bool {
        self.dispatch_required_handoff_action_evidence_rows > 0
    }

    pub fn requires_execution(&self) -> bool {
        self.execution_required_handoff_action_evidence_rows > 0
    }

    pub fn is_repair_action(&self) -> bool {
        self.handoff_action_kind
            == IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind::RepairHandoff
    }

    pub fn is_review_action(&self) -> bool {
        self.handoff_action_kind
            == IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind::ReviewHandoff
    }

    pub fn is_release_handoff_action(&self) -> bool {
        self.handoff_action_kind
            == IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind::ReleaseHandoff
    }

    pub fn ready_for_package_handoff_execution_action(&self) -> bool {
        self.is_release_handoff_action()
            && self.protocol_evidence_package_handoff_execution_slots_ready
            && self.package_handoff_execution_ready
            && !self.requires_attention()
    }

    pub fn requires_attention(&self) -> bool {
        self.blocks_release()
            || self.needs_operator()
            || self.has_review_work()
            || self.requires_dispatch()
            || self.requires_execution()
            || !self.handoff_action_evidence_ready
            || !self.protocol_substrate_ready
            || !self.protocol_evidence_packet_ready
            || !self.protocol_evidence_package_ready
            || !self.package_handoff_ready
            || !self.protocol_evidence_package_handoff_ready
            || !self.protocol_evidence_package_handoff_execution_slots_ready
            || !self.package_handoff_execution_ready
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionActionSummary
{
    pub release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_slot_summary:
        Box<IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionSlotSummary>,
    pub total_protocol_evidence_package_handoff_execution_slots: usize,
    pub total_protocol_evidence_package_handoff_execution_action_rows: usize,
    pub required_protocol_evidence_package_handoff_execution_action_rows: usize,
    pub release_blocking_protocol_evidence_package_handoff_execution_action_rows: usize,
    pub repair_protocol_evidence_package_handoff_execution_action_rows: usize,
    pub review_protocol_evidence_package_handoff_execution_action_rows: usize,
    pub operator_protocol_evidence_package_handoff_execution_action_rows: usize,
    pub dispatch_protocol_evidence_package_handoff_execution_action_rows: usize,
    pub execution_protocol_evidence_package_handoff_execution_action_rows: usize,
    pub release_handoff_protocol_evidence_package_handoff_execution_action_rows: usize,
    pub ready_protocol_evidence_package_handoff_execution_action_rows: usize,
    pub first_protocol_evidence_package_handoff_execution_action_row_key: Option<String>,
    pub first_required_protocol_evidence_package_handoff_execution_action_row_key: Option<String>,
    pub first_release_protocol_evidence_package_handoff_execution_action_row_key: Option<String>,
    pub first_ready_protocol_evidence_package_handoff_execution_action_row_key: Option<String>,
    pub first_protocol_evidence_package_handoff_execution_slot_key: Option<String>,
    pub first_protocol_evidence_package_handoff_row_key: Option<String>,
    pub next_protocol_evidence_package_handoff_execution_action_row_key: Option<String>,
    pub next_protocol_evidence_package_handoff_execution_slot_key: Option<String>,
    pub next_protocol_evidence_package_handoff_row_key: Option<String>,
    pub next_handoff_action_evidence_row_key: Option<String>,
    pub next_protocol: Option<ProtocolFamily>,
    pub next_blocked_substrate_stage: Option<IntegrationMeshProtocolSubstrateStage>,
    pub next_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub protocol_evidence_package_handoff_execution_slots_ready: bool,
    pub protocol_evidence_package_handoff_execution_actions_ready: bool,
}

impl
    IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionActionSummary
{
    pub fn from_summaries<'a>(
        slot_summary: IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionSlotSummary,
        action_rows: impl IntoIterator<
            Item = &'a IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionActionRow,
        >,
    ) -> Self {
        let action_rows: Vec<&IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionActionRow> =
            action_rows.into_iter().collect();
        let first_action = action_rows.first().copied();
        let first_required_action = action_rows
            .iter()
            .copied()
            .find(|row| row.requires_attention());
        let first_release_action = action_rows
            .iter()
            .copied()
            .find(|row| row.is_release_handoff_action());
        let first_ready_action = action_rows
            .iter()
            .copied()
            .find(|row| row.ready_for_package_handoff_execution_action());
        let ready_protocol_evidence_package_handoff_execution_action_rows = action_rows
            .iter()
            .filter(|row| row.ready_for_package_handoff_execution_action())
            .count();
        let protocol_evidence_package_handoff_execution_actions_ready = !action_rows.is_empty()
            && action_rows.len() == slot_summary.total_protocol_evidence_package_handoff_execution_slots
            && ready_protocol_evidence_package_handoff_execution_action_rows == action_rows.len()
            && slot_summary
                .ready_for_release_ticket_handoff_protocol_evidence_package_handoff_execution();

        Self {
            total_protocol_evidence_package_handoff_execution_slots: slot_summary
                .total_protocol_evidence_package_handoff_execution_slots,
            total_protocol_evidence_package_handoff_execution_action_rows: action_rows.len(),
            required_protocol_evidence_package_handoff_execution_action_rows: action_rows
                .iter()
                .filter(|row| row.requires_attention())
                .count(),
            release_blocking_protocol_evidence_package_handoff_execution_action_rows: action_rows
                .iter()
                .filter(|row| row.blocks_release())
                .count(),
            repair_protocol_evidence_package_handoff_execution_action_rows: action_rows
                .iter()
                .filter(|row| row.is_repair_action())
                .count(),
            review_protocol_evidence_package_handoff_execution_action_rows: action_rows
                .iter()
                .filter(|row| row.is_review_action())
                .count(),
            operator_protocol_evidence_package_handoff_execution_action_rows: action_rows
                .iter()
                .filter(|row| row.needs_operator())
                .count(),
            dispatch_protocol_evidence_package_handoff_execution_action_rows: action_rows
                .iter()
                .filter(|row| row.requires_dispatch())
                .count(),
            execution_protocol_evidence_package_handoff_execution_action_rows: action_rows
                .iter()
                .filter(|row| row.requires_execution())
                .count(),
            release_handoff_protocol_evidence_package_handoff_execution_action_rows: action_rows
                .iter()
                .filter(|row| row.is_release_handoff_action())
                .count(),
            ready_protocol_evidence_package_handoff_execution_action_rows,
            first_protocol_evidence_package_handoff_execution_action_row_key: first_action
                .map(|row| row.protocol_evidence_package_handoff_execution_action_row_key.clone()),
            first_required_protocol_evidence_package_handoff_execution_action_row_key:
                first_required_action.map(|row| {
                    row.protocol_evidence_package_handoff_execution_action_row_key.clone()
                }),
            first_release_protocol_evidence_package_handoff_execution_action_row_key:
                first_release_action.map(|row| {
                    row.protocol_evidence_package_handoff_execution_action_row_key.clone()
                }),
            first_ready_protocol_evidence_package_handoff_execution_action_row_key:
                first_ready_action.map(|row| {
                    row.protocol_evidence_package_handoff_execution_action_row_key.clone()
                }),
            first_protocol_evidence_package_handoff_execution_slot_key: first_action
                .map(|row| row.protocol_evidence_package_handoff_execution_slot_key.clone()),
            first_protocol_evidence_package_handoff_row_key: first_action
                .map(|row| row.protocol_evidence_package_handoff_row_key.clone()),
            next_protocol_evidence_package_handoff_execution_action_row_key: first_required_action
                .map(|row| row.protocol_evidence_package_handoff_execution_action_row_key.clone()),
            next_protocol_evidence_package_handoff_execution_slot_key: first_required_action
                .map(|row| row.protocol_evidence_package_handoff_execution_slot_key.clone()),
            next_protocol_evidence_package_handoff_row_key: first_required_action
                .map(|row| row.protocol_evidence_package_handoff_row_key.clone()),
            next_handoff_action_evidence_row_key: first_required_action
                .and_then(|row| row.next_handoff_action_evidence_row_key.clone()),
            next_protocol: first_required_action.map(|row| row.protocol.clone()),
            next_blocked_substrate_stage: first_required_action
                .and_then(|row| row.next_blocked_substrate_stage),
            next_handoff_lane: first_required_action.map(|row| row.handoff_lane),
            protocol_evidence_package_handoff_execution_slots_ready: slot_summary
                .ready_for_release_ticket_handoff_protocol_evidence_package_handoff_execution(),
            protocol_evidence_package_handoff_execution_actions_ready,
            release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_slot_summary:
                Box::new(slot_summary),
        }
    }

    pub fn ready_for_release_ticket_handoff_protocol_evidence_package_handoff_execution_actions(
        &self,
    ) -> bool {
        self.protocol_evidence_package_handoff_execution_slots_ready
            && self.protocol_evidence_package_handoff_execution_actions_ready
            && !self.requires_attention()
    }

    pub fn has_blockers(&self) -> bool {
        self.release_blocking_protocol_evidence_package_handoff_execution_action_rows > 0
    }

    pub fn has_required_actions(&self) -> bool {
        self.required_protocol_evidence_package_handoff_execution_action_rows > 0
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_protocol_evidence_package_handoff_execution_action_rows > 0
    }

    pub fn has_review_work(&self) -> bool {
        self.review_protocol_evidence_package_handoff_execution_action_rows > 0
    }

    pub fn requires_dispatch(&self) -> bool {
        self.dispatch_protocol_evidence_package_handoff_execution_action_rows > 0
    }

    pub fn requires_execution(&self) -> bool {
        self.execution_protocol_evidence_package_handoff_execution_action_rows > 0
    }

    pub fn has_release_handoff_actions(&self) -> bool {
        self.release_handoff_protocol_evidence_package_handoff_execution_action_rows > 0
    }

    pub fn has_ready_handoff_actions(&self) -> bool {
        self.ready_protocol_evidence_package_handoff_execution_action_rows > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.has_required_actions()
            || self.has_blockers()
            || self.needs_operator()
            || self.has_review_work()
            || self.requires_dispatch()
            || self.requires_execution()
            || self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_slot_summary
                .requires_attention()
            || !self.protocol_evidence_package_handoff_execution_slots_ready
            || !self.protocol_evidence_package_handoff_execution_actions_ready
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionActionEvidenceRow
{
    pub sequence: usize,
    pub protocol_evidence_package_handoff_execution_action_evidence_row_key: String,
    pub protocol_evidence_package_handoff_execution_action_row_sequence: usize,
    pub protocol_evidence_package_handoff_execution_action_row_key: String,
    pub protocol_evidence_package_handoff_execution_slot_sequence: usize,
    pub protocol_evidence_package_handoff_execution_slot_key: String,
    pub protocol_evidence_package_handoff_row_sequence: usize,
    pub protocol_evidence_package_handoff_row_key: String,
    pub protocol_evidence_package_key: String,
    pub protocol_evidence_packet_key: String,
    pub protocol: ProtocolFamily,
    pub handoff_lane: IntegrationMeshReleaseDispatchTicketHandoffLane,
    pub handoff_action_kind:
        IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind,
    pub total_substrate_rows: usize,
    pub ready_substrate_rows: usize,
    pub blocked_substrate_rows: usize,
    pub total_handoff_action_evidence_rows: usize,
    pub required_handoff_action_evidence_rows: usize,
    pub release_blocking_handoff_action_evidence_rows: usize,
    pub operator_required_handoff_action_evidence_rows: usize,
    pub review_required_handoff_action_evidence_rows: usize,
    pub dispatch_required_handoff_action_evidence_rows: usize,
    pub execution_required_handoff_action_evidence_rows: usize,
    pub release_handoff_evidence_rows: usize,
    pub release_ready_handoff_action_evidence_rows: usize,
    pub next_handoff_action_evidence_row_key: Option<String>,
    pub next_blocked_substrate_stage: Option<IntegrationMeshProtocolSubstrateStage>,
    pub release_blocking: bool,
    pub operator_required: bool,
    pub review_required: bool,
    pub dispatch_required: bool,
    pub execution_required: bool,
    pub lineage_complete: bool,
    pub release_handoff_evidence: bool,
    pub protocol_substrate_ready: bool,
    pub protocol_evidence_packet_ready: bool,
    pub protocol_evidence_package_ready: bool,
    pub package_handoff_ready: bool,
    pub protocol_evidence_package_handoff_ready: bool,
    pub protocol_evidence_package_handoff_execution_slots_ready: bool,
    pub package_handoff_execution_ready: bool,
    pub handoff_action_evidence_ready: bool,
    pub protocol_evidence_package_handoff_execution_action_evidence_ready: bool,
}

impl
    IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionActionEvidenceRow
{
    pub fn from_action_row(
        sequence: usize,
        row: &IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionActionRow,
    ) -> Self {
        let lineage_complete = !row
            .protocol_evidence_package_handoff_execution_action_row_key
            .is_empty()
            && !row
                .protocol_evidence_package_handoff_execution_slot_key
                .is_empty()
            && !row.protocol_evidence_package_handoff_row_key.is_empty()
            && !row.protocol_evidence_package_key.is_empty()
            && !row.protocol_evidence_packet_key.is_empty();
        let release_handoff_evidence = row.release_handoff_evidence_rows > 0;
        let protocol_evidence_package_handoff_execution_action_evidence_ready =
            row.ready_for_package_handoff_execution_action()
                && row.handoff_action_evidence_ready
                && release_handoff_evidence
                && lineage_complete;

        Self {
            sequence,
            protocol_evidence_package_handoff_execution_action_evidence_row_key: format!(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-protocol-evidence-package-handoff-execution-action-evidence-{sequence:02}-{}-{}",
                row.handoff_action_kind.as_str(),
                protocol_sort_token(&row.protocol)
            ),
            protocol_evidence_package_handoff_execution_action_row_sequence: row.sequence,
            protocol_evidence_package_handoff_execution_action_row_key: row
                .protocol_evidence_package_handoff_execution_action_row_key
                .clone(),
            protocol_evidence_package_handoff_execution_slot_sequence: row
                .protocol_evidence_package_handoff_execution_slot_sequence,
            protocol_evidence_package_handoff_execution_slot_key: row
                .protocol_evidence_package_handoff_execution_slot_key
                .clone(),
            protocol_evidence_package_handoff_row_sequence: row
                .protocol_evidence_package_handoff_row_sequence,
            protocol_evidence_package_handoff_row_key: row
                .protocol_evidence_package_handoff_row_key
                .clone(),
            protocol_evidence_package_key: row.protocol_evidence_package_key.clone(),
            protocol_evidence_packet_key: row.protocol_evidence_packet_key.clone(),
            protocol: row.protocol.clone(),
            handoff_lane: row.handoff_lane,
            handoff_action_kind: row.handoff_action_kind,
            total_substrate_rows: row.total_substrate_rows,
            ready_substrate_rows: row.ready_substrate_rows,
            blocked_substrate_rows: row.blocked_substrate_rows,
            total_handoff_action_evidence_rows: row.total_handoff_action_evidence_rows,
            required_handoff_action_evidence_rows: row.required_handoff_action_evidence_rows,
            release_blocking_handoff_action_evidence_rows: row
                .release_blocking_handoff_action_evidence_rows,
            operator_required_handoff_action_evidence_rows: row
                .operator_required_handoff_action_evidence_rows,
            review_required_handoff_action_evidence_rows: row
                .review_required_handoff_action_evidence_rows,
            dispatch_required_handoff_action_evidence_rows: row
                .dispatch_required_handoff_action_evidence_rows,
            execution_required_handoff_action_evidence_rows: row
                .execution_required_handoff_action_evidence_rows,
            release_handoff_evidence_rows: row.release_handoff_evidence_rows,
            release_ready_handoff_action_evidence_rows: row
                .release_ready_handoff_action_evidence_rows,
            next_handoff_action_evidence_row_key: row.next_handoff_action_evidence_row_key.clone(),
            next_blocked_substrate_stage: row.next_blocked_substrate_stage,
            release_blocking: row.blocks_release(),
            operator_required: row.needs_operator(),
            review_required: row.has_review_work(),
            dispatch_required: row.requires_dispatch(),
            execution_required: row.requires_execution(),
            lineage_complete,
            release_handoff_evidence,
            protocol_substrate_ready: row.protocol_substrate_ready,
            protocol_evidence_packet_ready: row.protocol_evidence_packet_ready,
            protocol_evidence_package_ready: row.protocol_evidence_package_ready,
            package_handoff_ready: row.package_handoff_ready,
            protocol_evidence_package_handoff_ready: row.protocol_evidence_package_handoff_ready,
            protocol_evidence_package_handoff_execution_slots_ready: row
                .protocol_evidence_package_handoff_execution_slots_ready,
            package_handoff_execution_ready: row.package_handoff_execution_ready,
            handoff_action_evidence_ready: row.handoff_action_evidence_ready,
            protocol_evidence_package_handoff_execution_action_evidence_ready,
        }
    }

    pub fn blocks_release(&self) -> bool {
        self.release_blocking
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required
    }

    pub fn has_review_work(&self) -> bool {
        self.review_required
    }

    pub fn requires_dispatch(&self) -> bool {
        self.dispatch_required
    }

    pub fn requires_execution(&self) -> bool {
        self.execution_required
    }

    pub fn has_lineage(&self) -> bool {
        self.lineage_complete
    }

    pub fn has_release_handoff_evidence(&self) -> bool {
        self.release_handoff_evidence
    }

    pub fn is_repair_action(&self) -> bool {
        self.handoff_action_kind
            == IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind::RepairHandoff
    }

    pub fn is_review_action(&self) -> bool {
        self.handoff_action_kind
            == IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind::ReviewHandoff
    }

    pub fn is_release_handoff_action(&self) -> bool {
        self.handoff_action_kind
            == IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind::ReleaseHandoff
    }

    pub fn ready_for_package_handoff_execution_action_evidence(&self) -> bool {
        self.protocol_evidence_package_handoff_execution_action_evidence_ready
    }

    pub fn requires_attention(&self) -> bool {
        self.blocks_release()
            || self.needs_operator()
            || self.has_review_work()
            || self.requires_dispatch()
            || self.requires_execution()
            || !self.has_lineage()
            || !self.has_release_handoff_evidence()
            || !self.protocol_substrate_ready
            || !self.protocol_evidence_packet_ready
            || !self.protocol_evidence_package_ready
            || !self.package_handoff_ready
            || !self.protocol_evidence_package_handoff_ready
            || !self.protocol_evidence_package_handoff_execution_slots_ready
            || !self.package_handoff_execution_ready
            || !self.handoff_action_evidence_ready
            || !self.ready_for_package_handoff_execution_action_evidence()
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionActionEvidenceReviewRow
{
    pub sequence: usize,
    pub protocol_evidence_package_handoff_execution_action_evidence_review_row_key: String,
    pub protocol_evidence_package_handoff_execution_action_evidence_row_sequence: usize,
    pub protocol_evidence_package_handoff_execution_action_evidence_row_key: String,
    pub protocol_evidence_package_handoff_execution_action_row_sequence: usize,
    pub protocol_evidence_package_handoff_execution_action_row_key: String,
    pub protocol_evidence_package_handoff_execution_slot_sequence: usize,
    pub protocol_evidence_package_handoff_execution_slot_key: String,
    pub protocol_evidence_package_handoff_row_sequence: usize,
    pub protocol_evidence_package_handoff_row_key: String,
    pub protocol_evidence_package_key: String,
    pub protocol_evidence_packet_key: String,
    pub protocol: ProtocolFamily,
    pub handoff_lane: IntegrationMeshReleaseDispatchTicketHandoffLane,
    pub handoff_action_kind:
        IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind,
    pub protocol_evidence_package_handoff_execution_action_evidence_review_kind:
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind,
    pub release_blocking: bool,
    pub operator_required: bool,
    pub review_required: bool,
    pub dispatch_required: bool,
    pub execution_required: bool,
    pub lineage_complete: bool,
    pub evidence_required: bool,
    pub release_handoff_evidence: bool,
    pub release_ready_review: bool,
    pub protocol_substrate_ready: bool,
    pub protocol_evidence_packet_ready: bool,
    pub protocol_evidence_package_ready: bool,
    pub package_handoff_ready: bool,
    pub protocol_evidence_package_handoff_ready: bool,
    pub protocol_evidence_package_handoff_execution_slots_ready: bool,
    pub package_handoff_execution_ready: bool,
    pub handoff_action_evidence_ready: bool,
    pub protocol_evidence_package_handoff_execution_action_evidence_ready: bool,
    pub protocol_evidence_package_handoff_execution_action_evidence_reviews_ready: bool,
    pub ready_for_release_handoff: bool,
}

impl
    IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionActionEvidenceReviewRow
{
    pub fn from_action_evidence_row(
        sequence: usize,
        evidence_row: &IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionActionEvidenceRow,
        protocol_evidence_package_handoff_execution_action_evidence_reviews_ready: bool,
    ) -> Self {
        let protocol_evidence_package_handoff_execution_action_evidence_review_kind =
            if !evidence_row.has_lineage() {
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::LineageGap
            } else if evidence_row.blocks_release() {
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::BlockerReview
            } else if evidence_row.has_review_work() {
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::ReviewGate
            } else if evidence_row.needs_operator() {
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::OperatorReview
            } else {
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::ReleaseReady
            };
        let release_ready_review =
            protocol_evidence_package_handoff_execution_action_evidence_reviews_ready
                && evidence_row.ready_for_package_handoff_execution_action_evidence()
                && evidence_row.has_lineage()
                && evidence_row.has_release_handoff_evidence()
                && !evidence_row.blocks_release()
                && !evidence_row.needs_operator()
                && !evidence_row.has_review_work()
                && !evidence_row.requires_dispatch()
                && !evidence_row.requires_execution()
                && protocol_evidence_package_handoff_execution_action_evidence_review_kind
                    == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::ReleaseReady;

        Self {
            sequence,
            protocol_evidence_package_handoff_execution_action_evidence_review_row_key: format!(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-protocol-evidence-package-handoff-execution-action-evidence-review-{sequence:02}-{}-{}",
                protocol_evidence_package_handoff_execution_action_evidence_review_kind.as_str(),
                protocol_sort_token(&evidence_row.protocol)
            ),
            protocol_evidence_package_handoff_execution_action_evidence_row_sequence:
                evidence_row.sequence,
            protocol_evidence_package_handoff_execution_action_evidence_row_key: evidence_row
                .protocol_evidence_package_handoff_execution_action_evidence_row_key
                .clone(),
            protocol_evidence_package_handoff_execution_action_row_sequence: evidence_row
                .protocol_evidence_package_handoff_execution_action_row_sequence,
            protocol_evidence_package_handoff_execution_action_row_key: evidence_row
                .protocol_evidence_package_handoff_execution_action_row_key
                .clone(),
            protocol_evidence_package_handoff_execution_slot_sequence: evidence_row
                .protocol_evidence_package_handoff_execution_slot_sequence,
            protocol_evidence_package_handoff_execution_slot_key: evidence_row
                .protocol_evidence_package_handoff_execution_slot_key
                .clone(),
            protocol_evidence_package_handoff_row_sequence: evidence_row
                .protocol_evidence_package_handoff_row_sequence,
            protocol_evidence_package_handoff_row_key: evidence_row
                .protocol_evidence_package_handoff_row_key
                .clone(),
            protocol_evidence_package_key: evidence_row.protocol_evidence_package_key.clone(),
            protocol_evidence_packet_key: evidence_row.protocol_evidence_packet_key.clone(),
            protocol: evidence_row.protocol.clone(),
            handoff_lane: evidence_row.handoff_lane,
            handoff_action_kind: evidence_row.handoff_action_kind,
            protocol_evidence_package_handoff_execution_action_evidence_review_kind,
            release_blocking: evidence_row.blocks_release(),
            operator_required: evidence_row.needs_operator(),
            review_required: evidence_row.has_review_work(),
            dispatch_required: evidence_row.requires_dispatch(),
            execution_required: evidence_row.requires_execution(),
            lineage_complete: evidence_row.has_lineage(),
            evidence_required: evidence_row.requires_attention(),
            release_handoff_evidence: evidence_row.has_release_handoff_evidence(),
            release_ready_review,
            protocol_substrate_ready: evidence_row.protocol_substrate_ready,
            protocol_evidence_packet_ready: evidence_row.protocol_evidence_packet_ready,
            protocol_evidence_package_ready: evidence_row.protocol_evidence_package_ready,
            package_handoff_ready: evidence_row.package_handoff_ready,
            protocol_evidence_package_handoff_ready: evidence_row
                .protocol_evidence_package_handoff_ready,
            protocol_evidence_package_handoff_execution_slots_ready: evidence_row
                .protocol_evidence_package_handoff_execution_slots_ready,
            package_handoff_execution_ready: evidence_row.package_handoff_execution_ready,
            handoff_action_evidence_ready: evidence_row.handoff_action_evidence_ready,
            protocol_evidence_package_handoff_execution_action_evidence_ready: evidence_row
                .protocol_evidence_package_handoff_execution_action_evidence_ready,
            protocol_evidence_package_handoff_execution_action_evidence_reviews_ready,
            ready_for_release_handoff: release_ready_review,
        }
    }

    pub fn is_lineage_gap(&self) -> bool {
        self.protocol_evidence_package_handoff_execution_action_evidence_review_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::LineageGap
    }

    pub fn is_blocker_review(&self) -> bool {
        self.protocol_evidence_package_handoff_execution_action_evidence_review_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::BlockerReview
    }

    pub fn is_review_gate(&self) -> bool {
        self.protocol_evidence_package_handoff_execution_action_evidence_review_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::ReviewGate
    }

    pub fn is_operator_review(&self) -> bool {
        self.protocol_evidence_package_handoff_execution_action_evidence_review_kind
            == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::OperatorReview
    }

    pub fn is_release_ready_review(&self) -> bool {
        self.release_ready_review
    }

    pub fn blocks_release(&self) -> bool {
        self.release_blocking
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required
    }

    pub fn has_review_work(&self) -> bool {
        self.review_required
    }

    pub fn requires_dispatch(&self) -> bool {
        self.dispatch_required
    }

    pub fn requires_execution(&self) -> bool {
        self.execution_required
    }

    pub fn has_lineage(&self) -> bool {
        self.lineage_complete
    }

    pub fn requires_evidence_review(&self) -> bool {
        self.evidence_required
    }

    pub fn is_repair_action(&self) -> bool {
        self.handoff_action_kind
            == IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind::RepairHandoff
    }

    pub fn is_review_action(&self) -> bool {
        self.handoff_action_kind
            == IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind::ReviewHandoff
    }

    pub fn is_release_handoff_action(&self) -> bool {
        self.handoff_action_kind
            == IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind::ReleaseHandoff
    }

    pub fn has_release_handoff_evidence(&self) -> bool {
        self.release_handoff_evidence
    }

    pub fn ready_to_release_handoff(&self) -> bool {
        self.ready_for_release_handoff
    }

    pub fn requires_attention(&self) -> bool {
        self.blocks_release()
            || self.needs_operator()
            || self.has_review_work()
            || self.requires_dispatch()
            || self.requires_execution()
            || self.is_lineage_gap()
            || !self.has_lineage()
            || !self.has_release_handoff_evidence()
            || !self.protocol_substrate_ready
            || !self.protocol_evidence_packet_ready
            || !self.protocol_evidence_package_ready
            || !self.package_handoff_ready
            || !self.protocol_evidence_package_handoff_ready
            || !self.protocol_evidence_package_handoff_execution_slots_ready
            || !self.package_handoff_execution_ready
            || !self.handoff_action_evidence_ready
            || !self.protocol_evidence_package_handoff_execution_action_evidence_ready
            || !self.protocol_evidence_package_handoff_execution_action_evidence_reviews_ready
            || !self.ready_to_release_handoff()
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionActionEvidenceSummary
{
    pub release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_action_summary:
        Box<IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionActionSummary>,
    pub total_protocol_evidence_package_handoff_execution_action_rows: usize,
    pub total_protocol_evidence_package_handoff_execution_action_evidence_rows: usize,
    pub required_protocol_evidence_package_handoff_execution_action_evidence_rows: usize,
    pub release_blocking_protocol_evidence_package_handoff_execution_action_evidence_rows: usize,
    pub repair_protocol_evidence_package_handoff_execution_action_evidence_rows: usize,
    pub review_protocol_evidence_package_handoff_execution_action_evidence_rows: usize,
    pub operator_protocol_evidence_package_handoff_execution_action_evidence_rows: usize,
    pub dispatch_protocol_evidence_package_handoff_execution_action_evidence_rows: usize,
    pub execution_protocol_evidence_package_handoff_execution_action_evidence_rows: usize,
    pub lineage_complete_protocol_evidence_package_handoff_execution_action_evidence_rows: usize,
    pub release_handoff_protocol_evidence_package_handoff_execution_action_evidence_rows: usize,
    pub release_ready_protocol_evidence_package_handoff_execution_action_evidence_rows: usize,
    pub first_protocol_evidence_package_handoff_execution_action_evidence_row_key:
        Option<String>,
    pub first_required_protocol_evidence_package_handoff_execution_action_evidence_row_key:
        Option<String>,
    pub first_repair_protocol_evidence_package_handoff_execution_action_evidence_row_key:
        Option<String>,
    pub first_review_protocol_evidence_package_handoff_execution_action_evidence_row_key:
        Option<String>,
    pub first_release_protocol_evidence_package_handoff_execution_action_evidence_row_key:
        Option<String>,
    pub first_protocol_evidence_package_handoff_execution_action_row_key: Option<String>,
    pub first_protocol_evidence_package_handoff_execution_slot_key: Option<String>,
    pub first_protocol_evidence_package_handoff_row_key: Option<String>,
    pub first_protocol_evidence_package_key: Option<String>,
    pub first_protocol_evidence_packet_key: Option<String>,
    pub first_protocol: Option<ProtocolFamily>,
    pub first_handoff_action_kind: Option<
        IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind,
    >,
    pub first_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub next_protocol_evidence_package_handoff_execution_action_evidence_row_key:
        Option<String>,
    pub next_protocol_evidence_package_handoff_execution_action_row_key: Option<String>,
    pub next_protocol_evidence_package_handoff_execution_slot_key: Option<String>,
    pub next_protocol_evidence_package_handoff_row_key: Option<String>,
    pub next_protocol_evidence_package_key: Option<String>,
    pub next_protocol_evidence_packet_key: Option<String>,
    pub next_handoff_action_evidence_row_key: Option<String>,
    pub next_protocol: Option<ProtocolFamily>,
    pub next_blocked_substrate_stage: Option<IntegrationMeshProtocolSubstrateStage>,
    pub next_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub protocol_evidence_package_handoff_execution_actions_ready: bool,
    pub protocol_evidence_package_handoff_execution_action_evidence_ready: bool,
}

impl
    IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionActionEvidenceSummary
{
    pub fn from_summaries<'a>(
        action_summary: IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionActionSummary,
        evidence_rows: impl IntoIterator<
            Item = &'a IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionActionEvidenceRow,
        >,
    ) -> Self {
        let evidence_rows: Vec<&IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionActionEvidenceRow> =
            evidence_rows.into_iter().collect();
        let first_row = evidence_rows.first().copied();
        let first_required_row = evidence_rows
            .iter()
            .copied()
            .find(|row| row.requires_attention());
        let first_repair_row = evidence_rows
            .iter()
            .copied()
            .find(|row| row.is_repair_action());
        let first_review_row = evidence_rows
            .iter()
            .copied()
            .find(|row| row.is_review_action());
        let first_release_row = evidence_rows
            .iter()
            .copied()
            .find(|row| row.is_release_handoff_action());
        let release_ready_protocol_evidence_package_handoff_execution_action_evidence_rows =
            evidence_rows
                .iter()
                .filter(|row| row.ready_for_package_handoff_execution_action_evidence())
                .count();
        let protocol_evidence_package_handoff_execution_actions_ready = action_summary
            .ready_for_release_ticket_handoff_protocol_evidence_package_handoff_execution_actions(
            );
        let protocol_evidence_package_handoff_execution_action_evidence_ready =
            !evidence_rows.is_empty()
                && evidence_rows.len()
                    == action_summary
                        .total_protocol_evidence_package_handoff_execution_action_rows
                && release_ready_protocol_evidence_package_handoff_execution_action_evidence_rows
                    == evidence_rows.len()
                && protocol_evidence_package_handoff_execution_actions_ready;

        Self {
            total_protocol_evidence_package_handoff_execution_action_rows: action_summary
                .total_protocol_evidence_package_handoff_execution_action_rows,
            total_protocol_evidence_package_handoff_execution_action_evidence_rows:
                evidence_rows.len(),
            required_protocol_evidence_package_handoff_execution_action_evidence_rows:
                evidence_rows.iter().filter(|row| row.requires_attention()).count(),
            release_blocking_protocol_evidence_package_handoff_execution_action_evidence_rows:
                evidence_rows.iter().filter(|row| row.blocks_release()).count(),
            repair_protocol_evidence_package_handoff_execution_action_evidence_rows:
                evidence_rows.iter().filter(|row| row.is_repair_action()).count(),
            review_protocol_evidence_package_handoff_execution_action_evidence_rows:
                evidence_rows.iter().filter(|row| row.is_review_action()).count(),
            operator_protocol_evidence_package_handoff_execution_action_evidence_rows:
                evidence_rows.iter().filter(|row| row.needs_operator()).count(),
            dispatch_protocol_evidence_package_handoff_execution_action_evidence_rows:
                evidence_rows.iter().filter(|row| row.requires_dispatch()).count(),
            execution_protocol_evidence_package_handoff_execution_action_evidence_rows:
                evidence_rows.iter().filter(|row| row.requires_execution()).count(),
            lineage_complete_protocol_evidence_package_handoff_execution_action_evidence_rows:
                evidence_rows.iter().filter(|row| row.has_lineage()).count(),
            release_handoff_protocol_evidence_package_handoff_execution_action_evidence_rows:
                evidence_rows
                    .iter()
                    .filter(|row| row.is_release_handoff_action())
                    .count(),
            release_ready_protocol_evidence_package_handoff_execution_action_evidence_rows,
            first_protocol_evidence_package_handoff_execution_action_evidence_row_key: first_row
                .map(|row| {
                    row.protocol_evidence_package_handoff_execution_action_evidence_row_key
                        .clone()
                }),
            first_required_protocol_evidence_package_handoff_execution_action_evidence_row_key:
                first_required_row.map(|row| {
                    row.protocol_evidence_package_handoff_execution_action_evidence_row_key
                        .clone()
                }),
            first_repair_protocol_evidence_package_handoff_execution_action_evidence_row_key:
                first_repair_row.map(|row| {
                    row.protocol_evidence_package_handoff_execution_action_evidence_row_key
                        .clone()
                }),
            first_review_protocol_evidence_package_handoff_execution_action_evidence_row_key:
                first_review_row.map(|row| {
                    row.protocol_evidence_package_handoff_execution_action_evidence_row_key
                        .clone()
                }),
            first_release_protocol_evidence_package_handoff_execution_action_evidence_row_key:
                first_release_row.map(|row| {
                    row.protocol_evidence_package_handoff_execution_action_evidence_row_key
                        .clone()
                }),
            first_protocol_evidence_package_handoff_execution_action_row_key: first_row.map(
                |row| row.protocol_evidence_package_handoff_execution_action_row_key.clone(),
            ),
            first_protocol_evidence_package_handoff_execution_slot_key: first_row.map(|row| {
                row.protocol_evidence_package_handoff_execution_slot_key
                    .clone()
            }),
            first_protocol_evidence_package_handoff_row_key: first_row
                .map(|row| row.protocol_evidence_package_handoff_row_key.clone()),
            first_protocol_evidence_package_key: first_row
                .map(|row| row.protocol_evidence_package_key.clone()),
            first_protocol_evidence_packet_key: first_row
                .map(|row| row.protocol_evidence_packet_key.clone()),
            first_protocol: first_row.map(|row| row.protocol.clone()),
            first_handoff_action_kind: first_row.map(|row| row.handoff_action_kind),
            first_handoff_lane: first_row.map(|row| row.handoff_lane),
            next_protocol_evidence_package_handoff_execution_action_evidence_row_key:
                first_required_row.map(|row| {
                    row.protocol_evidence_package_handoff_execution_action_evidence_row_key
                        .clone()
                }),
            next_protocol_evidence_package_handoff_execution_action_row_key: first_required_row
                .map(|row| row.protocol_evidence_package_handoff_execution_action_row_key.clone()),
            next_protocol_evidence_package_handoff_execution_slot_key: first_required_row.map(
                |row| {
                    row.protocol_evidence_package_handoff_execution_slot_key
                        .clone()
                },
            ),
            next_protocol_evidence_package_handoff_row_key: first_required_row
                .map(|row| row.protocol_evidence_package_handoff_row_key.clone()),
            next_protocol_evidence_package_key: first_required_row
                .map(|row| row.protocol_evidence_package_key.clone()),
            next_protocol_evidence_packet_key: first_required_row
                .map(|row| row.protocol_evidence_packet_key.clone()),
            next_handoff_action_evidence_row_key: first_required_row
                .and_then(|row| row.next_handoff_action_evidence_row_key.clone()),
            next_protocol: first_required_row.map(|row| row.protocol.clone()),
            next_blocked_substrate_stage: first_required_row
                .and_then(|row| row.next_blocked_substrate_stage),
            next_handoff_lane: first_required_row.map(|row| row.handoff_lane),
            protocol_evidence_package_handoff_execution_actions_ready,
            protocol_evidence_package_handoff_execution_action_evidence_ready,
            release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_action_summary:
                Box::new(action_summary),
        }
    }

    pub fn ready_for_release_ticket_handoff_protocol_evidence_package_handoff_execution_action_evidence(
        &self,
    ) -> bool {
        self.protocol_evidence_package_handoff_execution_actions_ready
            && self.protocol_evidence_package_handoff_execution_action_evidence_ready
            && !self.requires_attention()
    }

    pub fn has_action_evidence_rows(&self) -> bool {
        self.total_protocol_evidence_package_handoff_execution_action_evidence_rows > 0
    }

    pub fn has_required_evidence(&self) -> bool {
        self.required_protocol_evidence_package_handoff_execution_action_evidence_rows > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.release_blocking_protocol_evidence_package_handoff_execution_action_evidence_rows > 0
            || self.repair_protocol_evidence_package_handoff_execution_action_evidence_rows > 0
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_protocol_evidence_package_handoff_execution_action_evidence_rows > 0
    }

    pub fn has_review_work(&self) -> bool {
        self.review_protocol_evidence_package_handoff_execution_action_evidence_rows > 0
    }

    pub fn requires_dispatch(&self) -> bool {
        self.dispatch_protocol_evidence_package_handoff_execution_action_evidence_rows > 0
    }

    pub fn requires_execution(&self) -> bool {
        self.execution_protocol_evidence_package_handoff_execution_action_evidence_rows > 0
    }

    pub fn has_lineage_gaps(&self) -> bool {
        self.lineage_complete_protocol_evidence_package_handoff_execution_action_evidence_rows
            < self.total_protocol_evidence_package_handoff_execution_action_evidence_rows
    }

    pub fn has_release_handoff_evidence(&self) -> bool {
        self.release_handoff_protocol_evidence_package_handoff_execution_action_evidence_rows > 0
    }

    pub fn has_ready_evidence(&self) -> bool {
        self.release_ready_protocol_evidence_package_handoff_execution_action_evidence_rows > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.has_required_evidence()
            || self.has_blockers()
            || self.needs_operator()
            || self.has_review_work()
            || self.requires_dispatch()
            || self.requires_execution()
            || self.has_lineage_gaps()
            || self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_action_summary
                .requires_attention()
            || !self.protocol_evidence_package_handoff_execution_actions_ready
            || !self.protocol_evidence_package_handoff_execution_action_evidence_ready
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionActionEvidenceReviewSummary
{
    pub release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_action_evidence_summary:
        Box<IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionActionEvidenceSummary>,
    pub total_protocol_evidence_package_handoff_execution_action_evidence_rows: usize,
    pub total_protocol_evidence_package_handoff_execution_action_evidence_review_rows: usize,
    pub required_protocol_evidence_package_handoff_execution_action_evidence_review_rows: usize,
    pub blocker_protocol_evidence_package_handoff_execution_action_evidence_review_rows: usize,
    pub operator_protocol_evidence_package_handoff_execution_action_evidence_review_rows: usize,
    pub review_gate_protocol_evidence_package_handoff_execution_action_evidence_review_rows:
        usize,
    pub lineage_gap_protocol_evidence_package_handoff_execution_action_evidence_review_rows:
        usize,
    pub release_handoff_protocol_evidence_package_handoff_execution_action_evidence_review_rows:
        usize,
    pub release_ready_protocol_evidence_package_handoff_execution_action_evidence_review_rows:
        usize,
    pub first_protocol_evidence_package_handoff_execution_action_evidence_review_row_key:
        Option<String>,
    pub first_required_protocol_evidence_package_handoff_execution_action_evidence_review_row_key:
        Option<String>,
    pub first_blocker_protocol_evidence_package_handoff_execution_action_evidence_review_row_key:
        Option<String>,
    pub first_operator_protocol_evidence_package_handoff_execution_action_evidence_review_row_key:
        Option<String>,
    pub first_review_gate_protocol_evidence_package_handoff_execution_action_evidence_review_row_key:
        Option<String>,
    pub first_lineage_gap_protocol_evidence_package_handoff_execution_action_evidence_review_row_key:
        Option<String>,
    pub first_release_ready_protocol_evidence_package_handoff_execution_action_evidence_review_row_key:
        Option<String>,
    pub first_protocol_evidence_package_handoff_execution_action_evidence_row_key:
        Option<String>,
    pub first_protocol_evidence_package_handoff_execution_action_row_key: Option<String>,
    pub first_protocol_evidence_package_handoff_execution_slot_key: Option<String>,
    pub first_protocol_evidence_package_handoff_row_key: Option<String>,
    pub first_protocol_evidence_package_key: Option<String>,
    pub first_protocol_evidence_packet_key: Option<String>,
    pub first_protocol: Option<ProtocolFamily>,
    pub first_handoff_action_kind: Option<
        IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind,
    >,
    pub first_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub next_protocol_evidence_package_handoff_execution_action_evidence_review_row_key:
        Option<String>,
    pub next_protocol_evidence_package_handoff_execution_action_evidence_row_key:
        Option<String>,
    pub next_protocol_evidence_package_handoff_execution_action_row_key: Option<String>,
    pub next_protocol_evidence_package_handoff_execution_slot_key: Option<String>,
    pub next_protocol_evidence_package_handoff_row_key: Option<String>,
    pub next_protocol_evidence_package_key: Option<String>,
    pub next_protocol_evidence_packet_key: Option<String>,
    pub next_protocol: Option<ProtocolFamily>,
    pub next_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub protocol_evidence_package_handoff_execution_action_evidence_ready: bool,
    pub protocol_evidence_package_handoff_execution_action_evidence_reviews_ready: bool,
}

impl
    IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionActionEvidenceReviewSummary
{
    pub fn from_summaries<'a>(
        evidence_summary: IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionActionEvidenceSummary,
        review_rows: impl IntoIterator<
            Item = &'a IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionActionEvidenceReviewRow,
        >,
    ) -> Self {
        let review_rows: Vec<&IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionActionEvidenceReviewRow> =
            review_rows.into_iter().collect();
        let first_row = review_rows.first().copied();
        let first_required_row = review_rows
            .iter()
            .copied()
            .find(|row| row.requires_attention());
        let first_blocker_row = review_rows
            .iter()
            .copied()
            .find(|row| row.is_blocker_review());
        let first_operator_row = review_rows
            .iter()
            .copied()
            .find(|row| row.is_operator_review());
        let first_review_gate_row = review_rows
            .iter()
            .copied()
            .find(|row| row.is_review_gate());
        let first_lineage_gap_row = review_rows
            .iter()
            .copied()
            .find(|row| row.is_lineage_gap());
        let first_release_ready_row = review_rows
            .iter()
            .copied()
            .find(|row| row.is_release_ready_review());
        let next_row = first_required_row;
        let protocol_evidence_package_handoff_execution_action_evidence_reviews_ready =
            review_rows.len()
                == evidence_summary
                    .total_protocol_evidence_package_handoff_execution_action_evidence_rows
                && review_rows.iter().all(|row| row.ready_to_release_handoff());

        Self {
            total_protocol_evidence_package_handoff_execution_action_evidence_rows:
                evidence_summary
                    .total_protocol_evidence_package_handoff_execution_action_evidence_rows,
            total_protocol_evidence_package_handoff_execution_action_evidence_review_rows:
                review_rows.len(),
            required_protocol_evidence_package_handoff_execution_action_evidence_review_rows:
                review_rows.iter().filter(|row| row.requires_attention()).count(),
            blocker_protocol_evidence_package_handoff_execution_action_evidence_review_rows:
                review_rows.iter().filter(|row| row.is_blocker_review()).count(),
            operator_protocol_evidence_package_handoff_execution_action_evidence_review_rows:
                review_rows.iter().filter(|row| row.is_operator_review()).count(),
            review_gate_protocol_evidence_package_handoff_execution_action_evidence_review_rows:
                review_rows.iter().filter(|row| row.is_review_gate()).count(),
            lineage_gap_protocol_evidence_package_handoff_execution_action_evidence_review_rows:
                review_rows.iter().filter(|row| row.is_lineage_gap()).count(),
            release_handoff_protocol_evidence_package_handoff_execution_action_evidence_review_rows:
                review_rows
                    .iter()
                    .filter(|row| row.has_release_handoff_evidence())
                    .count(),
            release_ready_protocol_evidence_package_handoff_execution_action_evidence_review_rows:
                review_rows
                    .iter()
                    .filter(|row| row.is_release_ready_review())
                    .count(),
            first_protocol_evidence_package_handoff_execution_action_evidence_review_row_key:
                first_row.map(|row| {
                    row.protocol_evidence_package_handoff_execution_action_evidence_review_row_key
                        .clone()
                }),
            first_required_protocol_evidence_package_handoff_execution_action_evidence_review_row_key:
                first_required_row.map(|row| {
                    row.protocol_evidence_package_handoff_execution_action_evidence_review_row_key
                        .clone()
                }),
            first_blocker_protocol_evidence_package_handoff_execution_action_evidence_review_row_key:
                first_blocker_row.map(|row| {
                    row.protocol_evidence_package_handoff_execution_action_evidence_review_row_key
                        .clone()
                }),
            first_operator_protocol_evidence_package_handoff_execution_action_evidence_review_row_key:
                first_operator_row.map(|row| {
                    row.protocol_evidence_package_handoff_execution_action_evidence_review_row_key
                        .clone()
                }),
            first_review_gate_protocol_evidence_package_handoff_execution_action_evidence_review_row_key:
                first_review_gate_row.map(|row| {
                    row.protocol_evidence_package_handoff_execution_action_evidence_review_row_key
                        .clone()
                }),
            first_lineage_gap_protocol_evidence_package_handoff_execution_action_evidence_review_row_key:
                first_lineage_gap_row.map(|row| {
                    row.protocol_evidence_package_handoff_execution_action_evidence_review_row_key
                        .clone()
                }),
            first_release_ready_protocol_evidence_package_handoff_execution_action_evidence_review_row_key:
                first_release_ready_row.map(|row| {
                    row.protocol_evidence_package_handoff_execution_action_evidence_review_row_key
                        .clone()
                }),
            first_protocol_evidence_package_handoff_execution_action_evidence_row_key:
                first_row.map(|row| {
                    row.protocol_evidence_package_handoff_execution_action_evidence_row_key
                        .clone()
                }),
            first_protocol_evidence_package_handoff_execution_action_row_key: first_row.map(
                |row| row.protocol_evidence_package_handoff_execution_action_row_key.clone(),
            ),
            first_protocol_evidence_package_handoff_execution_slot_key: first_row.map(|row| {
                row.protocol_evidence_package_handoff_execution_slot_key
                    .clone()
            }),
            first_protocol_evidence_package_handoff_row_key: first_row
                .map(|row| row.protocol_evidence_package_handoff_row_key.clone()),
            first_protocol_evidence_package_key: first_row
                .map(|row| row.protocol_evidence_package_key.clone()),
            first_protocol_evidence_packet_key: first_row
                .map(|row| row.protocol_evidence_packet_key.clone()),
            first_protocol: first_row.map(|row| row.protocol.clone()),
            first_handoff_action_kind: first_row.map(|row| row.handoff_action_kind),
            first_handoff_lane: first_row.map(|row| row.handoff_lane),
            next_protocol_evidence_package_handoff_execution_action_evidence_review_row_key:
                next_row.map(|row| {
                    row.protocol_evidence_package_handoff_execution_action_evidence_review_row_key
                        .clone()
                }),
            next_protocol_evidence_package_handoff_execution_action_evidence_row_key:
                next_row.map(|row| {
                    row.protocol_evidence_package_handoff_execution_action_evidence_row_key
                        .clone()
                }),
            next_protocol_evidence_package_handoff_execution_action_row_key: next_row.map(
                |row| row.protocol_evidence_package_handoff_execution_action_row_key.clone(),
            ),
            next_protocol_evidence_package_handoff_execution_slot_key: next_row.map(|row| {
                row.protocol_evidence_package_handoff_execution_slot_key
                    .clone()
            }),
            next_protocol_evidence_package_handoff_row_key: next_row
                .map(|row| row.protocol_evidence_package_handoff_row_key.clone()),
            next_protocol_evidence_package_key: next_row
                .map(|row| row.protocol_evidence_package_key.clone()),
            next_protocol_evidence_packet_key: next_row
                .map(|row| row.protocol_evidence_packet_key.clone()),
            next_protocol: next_row.map(|row| row.protocol.clone()),
            next_handoff_lane: next_row.map(|row| row.handoff_lane),
            protocol_evidence_package_handoff_execution_action_evidence_ready: evidence_summary
                .ready_for_release_ticket_handoff_protocol_evidence_package_handoff_execution_action_evidence(),
            protocol_evidence_package_handoff_execution_action_evidence_reviews_ready,
            release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_action_evidence_summary:
                Box::new(evidence_summary),
        }
    }

    pub fn has_protocol_evidence_package_handoff_execution_action_evidence_review_rows(
        &self,
    ) -> bool {
        self.total_protocol_evidence_package_handoff_execution_action_evidence_review_rows > 0
    }

    pub fn ready_for_release_ticket_handoff_protocol_evidence_package_handoff_execution_action_evidence_reviews(
        &self,
    ) -> bool {
        self.protocol_evidence_package_handoff_execution_action_evidence_ready
            && self.protocol_evidence_package_handoff_execution_action_evidence_reviews_ready
            && !self.requires_attention()
    }

    pub fn has_required_reviews(&self) -> bool {
        self.required_protocol_evidence_package_handoff_execution_action_evidence_review_rows > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.blocker_protocol_evidence_package_handoff_execution_action_evidence_review_rows > 0
            || self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_action_evidence_summary
                .has_blockers()
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_protocol_evidence_package_handoff_execution_action_evidence_review_rows > 0
            || self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_action_evidence_summary
                .needs_operator()
    }

    pub fn has_review_work(&self) -> bool {
        self.review_gate_protocol_evidence_package_handoff_execution_action_evidence_review_rows
            > 0
            || self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_action_evidence_summary
                .has_review_work()
    }

    pub fn requires_dispatch(&self) -> bool {
        self.release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_action_evidence_summary
            .requires_dispatch()
    }

    pub fn requires_execution(&self) -> bool {
        self.release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_action_evidence_summary
            .requires_execution()
    }

    pub fn has_lineage_gaps(&self) -> bool {
        self.lineage_gap_protocol_evidence_package_handoff_execution_action_evidence_review_rows
            > 0
            || self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_action_evidence_summary
                .has_lineage_gaps()
    }

    pub fn has_release_handoff_reviews(&self) -> bool {
        self.release_handoff_protocol_evidence_package_handoff_execution_action_evidence_review_rows
            > 0
    }

    pub fn has_release_ready_reviews(&self) -> bool {
        self.release_ready_protocol_evidence_package_handoff_execution_action_evidence_review_rows
            > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.has_required_reviews()
            || self.has_blockers()
            || self.needs_operator()
            || self.has_review_work()
            || self.requires_dispatch()
            || self.requires_execution()
            || self.has_lineage_gaps()
            || self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_action_evidence_summary
                .requires_attention()
            || !self.protocol_evidence_package_handoff_execution_action_evidence_ready
            || !self.protocol_evidence_package_handoff_execution_action_evidence_reviews_ready
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewSummary {
    pub release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_summary:
        Box<IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceSummary>,
    pub total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows:
        usize,
    pub total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_rows:
        usize,
    pub required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_rows:
        usize,
    pub blocker_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_rows:
        usize,
    pub operator_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_rows:
        usize,
    pub review_gate_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_rows:
        usize,
    pub lineage_gap_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_rows:
        usize,
    pub release_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_rows:
        usize,
    pub release_ready_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_rows:
        usize,
    pub first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_row_key:
        Option<String>,
    pub first_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_row_key:
        Option<String>,
    pub first_blocker_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_row_key:
        Option<String>,
    pub first_operator_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_row_key:
        Option<String>,
    pub first_review_gate_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_row_key:
        Option<String>,
    pub first_lineage_gap_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_row_key:
        Option<String>,
    pub first_release_ready_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_row_key:
        Option<String>,
    pub first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_row_key:
        Option<String>,
    pub first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_row_key:
        Option<String>,
    pub first_readiness_evidence_review_disposition_action_readiness_execution_handoff_row_key:
        Option<String>,
    pub first_readiness_evidence_review_disposition_action_readiness_execution_slot_key:
        Option<String>,
    pub first_readiness_evidence_review_disposition_action_key: Option<String>,
    pub first_work_order_key: Option<String>,
    pub first_ticket_key: Option<String>,
    pub first_handoff_action_kind: Option<
        IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind,
    >,
    pub first_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub first_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub first_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_row_key:
        Option<String>,
    pub next_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_row_key:
        Option<String>,
    pub next_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_row_key:
        Option<String>,
    pub next_readiness_evidence_review_disposition_action_readiness_execution_handoff_row_key:
        Option<String>,
    pub next_readiness_evidence_review_disposition_action_readiness_execution_slot_key:
        Option<String>,
    pub next_readiness_evidence_review_disposition_action_key: Option<String>,
    pub next_work_order_key: Option<String>,
    pub next_ticket_key: Option<String>,
    pub next_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub next_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_ready:
        bool,
    pub release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_reviews_ready:
        bool,
}

impl
    IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewSummary
{
    pub fn from_summaries<'a>(
        release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_summary: IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceSummary,
        review_rows: impl IntoIterator<
            Item = &'a IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewRow,
        >,
    ) -> Self {
        let review_rows: Vec<&IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewRow> =
            review_rows.into_iter().collect();
        let first_row = review_rows.first().copied();
        let first_required_row = review_rows
            .iter()
            .copied()
            .find(|row| row.requires_attention());
        let first_blocker_row = review_rows
            .iter()
            .copied()
            .find(|row| row.is_blocker_review());
        let first_operator_row = review_rows
            .iter()
            .copied()
            .find(|row| row.is_operator_review());
        let first_review_gate_row = review_rows
            .iter()
            .copied()
            .find(|row| row.is_review_gate());
        let first_lineage_gap_row = review_rows
            .iter()
            .copied()
            .find(|row| row.is_lineage_gap());
        let first_release_ready_row = review_rows
            .iter()
            .copied()
            .find(|row| row.is_release_ready_review());
        let next_row = first_required_row;
        let release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_reviews_ready =
            review_rows.len()
                == release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_summary
                    .total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows
                && review_rows
                    .iter()
                    .all(|row| row.ready_to_release_handoff());

        Self {
            total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows:
                release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_summary
                    .total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows,
            total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_rows:
                review_rows.len(),
            required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_rows:
                review_rows.iter().filter(|row| row.requires_attention()).count(),
            blocker_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_rows:
                review_rows.iter().filter(|row| row.is_blocker_review()).count(),
            operator_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_rows:
                review_rows.iter().filter(|row| row.is_operator_review()).count(),
            review_gate_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_rows:
                review_rows.iter().filter(|row| row.is_review_gate()).count(),
            lineage_gap_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_rows:
                review_rows.iter().filter(|row| row.is_lineage_gap()).count(),
            release_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_rows:
                review_rows
                    .iter()
                    .filter(|row| row.is_release_handoff_evidence())
                    .count(),
            release_ready_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_rows:
                review_rows
                    .iter()
                    .filter(|row| row.is_release_ready_review())
                    .count(),
            first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_row_key:
                first_row.map(|row| row.handoff_action_evidence_review_row_key.clone()),
            first_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_row_key:
                first_required_row.map(|row| row.handoff_action_evidence_review_row_key.clone()),
            first_blocker_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_row_key:
                first_blocker_row.map(|row| row.handoff_action_evidence_review_row_key.clone()),
            first_operator_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_row_key:
                first_operator_row.map(|row| row.handoff_action_evidence_review_row_key.clone()),
            first_review_gate_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_row_key:
                first_review_gate_row.map(|row| row.handoff_action_evidence_review_row_key.clone()),
            first_lineage_gap_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_row_key:
                first_lineage_gap_row.map(|row| row.handoff_action_evidence_review_row_key.clone()),
            first_release_ready_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_row_key:
                first_release_ready_row.map(|row| row.handoff_action_evidence_review_row_key.clone()),
            first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_row_key:
                first_row.map(|row| row.handoff_action_evidence_row_key.clone()),
            first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_row_key:
                first_row.map(|row| row.handoff_action_row_key.clone()),
            first_readiness_evidence_review_disposition_action_readiness_execution_handoff_row_key:
                first_row.map(|row| row.handoff_row_key.clone()),
            first_readiness_evidence_review_disposition_action_readiness_execution_slot_key:
                first_row.map(|row| {
                    row.readiness_evidence_review_disposition_action_readiness_execution_slot_key
                        .clone()
                }),
            first_readiness_evidence_review_disposition_action_key:
                first_row.map(|row| row.readiness_evidence_review_disposition_action_key.clone()),
            first_work_order_key: first_row.map(|row| row.work_order_key.clone()),
            first_ticket_key: first_row.map(|row| row.ticket_key.clone()),
            first_handoff_action_kind: first_row.map(|row| row.handoff_action_kind),
            first_handoff_lane: first_row.map(|row| row.handoff_lane),
            first_check_kind: first_row.map(|row| row.check_kind),
            first_check_status: first_row.map(|row| row.status),
            next_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_row_key:
                next_row.map(|row| row.handoff_action_evidence_review_row_key.clone()),
            next_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_row_key:
                next_row.map(|row| row.handoff_action_evidence_row_key.clone()),
            next_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_row_key:
                next_row.map(|row| row.handoff_action_row_key.clone()),
            next_readiness_evidence_review_disposition_action_readiness_execution_handoff_row_key:
                next_row.map(|row| row.handoff_row_key.clone()),
            next_readiness_evidence_review_disposition_action_readiness_execution_slot_key:
                next_row.map(|row| {
                    row.readiness_evidence_review_disposition_action_readiness_execution_slot_key
                        .clone()
                }),
            next_readiness_evidence_review_disposition_action_key:
                next_row.map(|row| row.readiness_evidence_review_disposition_action_key.clone()),
            next_work_order_key: next_row.map(|row| row.work_order_key.clone()),
            next_ticket_key: next_row.map(|row| row.ticket_key.clone()),
            next_check_kind: next_row.map(|row| row.check_kind),
            next_check_status: next_row.map(|row| row.status),
            next_handoff_lane: next_row.map(|row| row.handoff_lane),
            release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_ready:
                release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_summary
                    .ready_for_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence(),
            release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_reviews_ready,
            release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_summary:
                Box::new(
                    release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_summary,
                ),
        }
    }

    pub fn has_handoff_action_evidence_review_rows(&self) -> bool {
        self.total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_rows > 0
    }

    pub fn ready_for_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_reviews(
        &self,
    ) -> bool {
        self.release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_ready
            && self.release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_reviews_ready
            && !self.requires_attention()
    }

    pub fn has_required_reviews(&self) -> bool {
        self.required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_rows
            > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.blocker_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_rows
            > 0
            || self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_summary
                .has_blockers()
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_rows
            > 0
            || self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_summary
                .needs_operator()
    }

    pub fn has_review_work(&self) -> bool {
        self.review_gate_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_rows
            > 0
            || self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_summary
                .has_review_work()
    }

    pub fn has_lineage_gaps(&self) -> bool {
        self.lineage_gap_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_rows
            > 0
            || self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_summary
                .has_lineage_gaps()
    }

    pub fn has_release_handoff_reviews(&self) -> bool {
        self.release_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_rows
            > 0
    }

    pub fn has_release_ready_reviews(&self) -> bool {
        self.release_ready_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_rows
            > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.has_required_reviews()
            || self.has_blockers()
            || self.needs_operator()
            || self.has_review_work()
            || self.has_lineage_gaps()
            || !self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_reviews_ready
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionSummary {
    pub release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_summary:
        Box<IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewSummary>,
    pub total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_rows:
        usize,
    pub total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_rows:
        usize,
    pub required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_rows:
        usize,
    pub repair_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_rows:
        usize,
    pub operator_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_rows:
        usize,
    pub review_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_rows:
        usize,
    pub lineage_repair_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_rows:
        usize,
    pub release_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_rows:
        usize,
    pub release_ready_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_rows:
        usize,
    pub first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_row_key:
        Option<String>,
    pub first_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_row_key:
        Option<String>,
    pub first_repair_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_row_key:
        Option<String>,
    pub first_operator_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_row_key:
        Option<String>,
    pub first_review_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_row_key:
        Option<String>,
    pub first_lineage_repair_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_row_key:
        Option<String>,
    pub first_release_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_row_key:
        Option<String>,
    pub first_release_ready_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_row_key:
        Option<String>,
    pub first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_row_key:
        Option<String>,
    pub first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_row_key:
        Option<String>,
    pub first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_row_key:
        Option<String>,
    pub first_readiness_evidence_review_disposition_action_readiness_execution_handoff_row_key:
        Option<String>,
    pub first_readiness_evidence_review_disposition_action_readiness_execution_slot_key:
        Option<String>,
    pub first_readiness_evidence_review_disposition_action_key: Option<String>,
    pub first_work_order_key: Option<String>,
    pub first_ticket_key: Option<String>,
    pub first_handoff_action_kind: Option<
        IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind,
    >,
    pub first_handoff_action_evidence_review_disposition_action_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind,
    >,
    pub first_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub first_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub first_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_row_key:
        Option<String>,
    pub next_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_row_key:
        Option<String>,
    pub next_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_row_key:
        Option<String>,
    pub next_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_row_key:
        Option<String>,
    pub next_readiness_evidence_review_disposition_action_readiness_execution_handoff_row_key:
        Option<String>,
    pub next_readiness_evidence_review_disposition_action_readiness_execution_slot_key:
        Option<String>,
    pub next_readiness_evidence_review_disposition_action_key: Option<String>,
    pub next_work_order_key: Option<String>,
    pub next_ticket_key: Option<String>,
    pub next_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub next_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_reviews_ready:
        bool,
    pub release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_dispositions_ready:
        bool,
}

impl
    IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionSummary
{
    pub fn from_summaries<'a>(
        release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_summary: IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewSummary,
        disposition_rows: impl IntoIterator<
            Item = &'a IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionRow,
        >,
    ) -> Self {
        let disposition_rows: Vec<&IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionRow> =
            disposition_rows.into_iter().collect();
        let first_row = disposition_rows.first().copied();
        let first_required_row = disposition_rows
            .iter()
            .copied()
            .find(|row| row.requires_attention());
        let first_repair_row = disposition_rows
            .iter()
            .copied()
            .find(|row| row.is_repair_disposition());
        let first_operator_row = disposition_rows
            .iter()
            .copied()
            .find(|row| row.is_operator_disposition());
        let first_review_row = disposition_rows
            .iter()
            .copied()
            .find(|row| row.is_review_disposition());
        let first_lineage_repair_row = disposition_rows
            .iter()
            .copied()
            .find(|row| row.is_lineage_repair_disposition());
        let first_release_handoff_row = disposition_rows
            .iter()
            .copied()
            .find(|row| row.is_release_handoff_disposition());
        let first_release_ready_row = disposition_rows
            .iter()
            .copied()
            .find(|row| row.is_release_ready_disposition());
        let next_row = first_required_row;
        let release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_dispositions_ready =
            disposition_rows.len()
                == release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_summary
                    .total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_rows
                && disposition_rows
                    .iter()
                    .all(|row| row.ready_to_release_handoff());

        Self {
            total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_rows:
                release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_summary
                    .total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_rows,
            total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_rows:
                disposition_rows.len(),
            required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_rows:
                disposition_rows.iter().filter(|row| row.requires_attention()).count(),
            repair_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_rows:
                disposition_rows.iter().filter(|row| row.is_repair_disposition()).count(),
            operator_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_rows:
                disposition_rows.iter().filter(|row| row.is_operator_disposition()).count(),
            review_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_rows:
                disposition_rows.iter().filter(|row| row.is_review_disposition()).count(),
            lineage_repair_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_rows:
                disposition_rows.iter().filter(|row| row.is_lineage_repair_disposition()).count(),
            release_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_rows:
                disposition_rows.iter().filter(|row| row.is_release_handoff_disposition()).count(),
            release_ready_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_rows:
                disposition_rows.iter().filter(|row| row.is_release_ready_disposition()).count(),
            first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_row_key:
                first_row.map(|row| row.handoff_action_evidence_review_disposition_row_key.clone()),
            first_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_row_key:
                first_required_row.map(|row| row.handoff_action_evidence_review_disposition_row_key.clone()),
            first_repair_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_row_key:
                first_repair_row.map(|row| row.handoff_action_evidence_review_disposition_row_key.clone()),
            first_operator_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_row_key:
                first_operator_row.map(|row| row.handoff_action_evidence_review_disposition_row_key.clone()),
            first_review_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_row_key:
                first_review_row.map(|row| row.handoff_action_evidence_review_disposition_row_key.clone()),
            first_lineage_repair_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_row_key:
                first_lineage_repair_row.map(|row| row.handoff_action_evidence_review_disposition_row_key.clone()),
            first_release_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_row_key:
                first_release_handoff_row.map(|row| row.handoff_action_evidence_review_disposition_row_key.clone()),
            first_release_ready_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_row_key:
                first_release_ready_row.map(|row| row.handoff_action_evidence_review_disposition_row_key.clone()),
            first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_row_key:
                first_row.map(|row| row.handoff_action_evidence_review_row_key.clone()),
            first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_row_key:
                first_row.map(|row| row.handoff_action_evidence_row_key.clone()),
            first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_row_key:
                first_row.map(|row| row.handoff_action_row_key.clone()),
            first_readiness_evidence_review_disposition_action_readiness_execution_handoff_row_key:
                first_row.map(|row| row.handoff_row_key.clone()),
            first_readiness_evidence_review_disposition_action_readiness_execution_slot_key:
                first_row.map(|row| {
                    row.readiness_evidence_review_disposition_action_readiness_execution_slot_key
                        .clone()
                }),
            first_readiness_evidence_review_disposition_action_key:
                first_row.map(|row| row.readiness_evidence_review_disposition_action_key.clone()),
            first_work_order_key: first_row.map(|row| row.work_order_key.clone()),
            first_ticket_key: first_row.map(|row| row.ticket_key.clone()),
            first_handoff_action_kind: first_row.map(|row| row.handoff_action_kind),
            first_handoff_action_evidence_review_disposition_action_kind:
                first_row.map(|row| row.handoff_action_evidence_review_disposition_action_kind),
            first_handoff_lane: first_row.map(|row| row.handoff_lane),
            first_check_kind: first_row.map(|row| row.check_kind),
            first_check_status: first_row.map(|row| row.status),
            next_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_row_key:
                next_row.map(|row| row.handoff_action_evidence_review_disposition_row_key.clone()),
            next_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_row_key:
                next_row.map(|row| row.handoff_action_evidence_review_row_key.clone()),
            next_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_row_key:
                next_row.map(|row| row.handoff_action_evidence_row_key.clone()),
            next_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_row_key:
                next_row.map(|row| row.handoff_action_row_key.clone()),
            next_readiness_evidence_review_disposition_action_readiness_execution_handoff_row_key:
                next_row.map(|row| row.handoff_row_key.clone()),
            next_readiness_evidence_review_disposition_action_readiness_execution_slot_key:
                next_row.map(|row| {
                    row.readiness_evidence_review_disposition_action_readiness_execution_slot_key
                        .clone()
                }),
            next_readiness_evidence_review_disposition_action_key:
                next_row.map(|row| row.readiness_evidence_review_disposition_action_key.clone()),
            next_work_order_key: next_row.map(|row| row.work_order_key.clone()),
            next_ticket_key: next_row.map(|row| row.ticket_key.clone()),
            next_check_kind: next_row.map(|row| row.check_kind),
            next_check_status: next_row.map(|row| row.status),
            next_handoff_lane: next_row.map(|row| row.handoff_lane),
            release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_reviews_ready:
                release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_summary
                    .ready_for_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_reviews(),
            release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_dispositions_ready,
            release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_summary:
                Box::new(
                    release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_summary,
                ),
        }
    }

    pub fn has_handoff_action_evidence_review_disposition_rows(&self) -> bool {
        self.total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_rows > 0
    }

    pub fn ready_for_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_dispositions(
        &self,
    ) -> bool {
        self.release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_reviews_ready
            && self.release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_dispositions_ready
            && !self.requires_attention()
    }

    pub fn has_required_dispositions(&self) -> bool {
        self.required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_rows
            > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.repair_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_rows
            > 0
            || self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_summary
                .has_blockers()
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_rows
            > 0
            || self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_summary
                .needs_operator()
    }

    pub fn has_review_work(&self) -> bool {
        self.review_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_rows
            > 0
            || self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_summary
                .has_review_work()
    }

    pub fn has_lineage_repairs(&self) -> bool {
        self.lineage_repair_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_rows
            > 0
            || self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_summary
                .has_lineage_gaps()
    }

    pub fn has_release_handoff_dispositions(&self) -> bool {
        self.release_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_rows
            > 0
    }

    pub fn has_release_ready_dispositions(&self) -> bool {
        self.release_ready_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_rows
            > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.has_required_dispositions()
            || self.has_blockers()
            || self.needs_operator()
            || self.has_review_work()
            || self.has_lineage_repairs()
            || !self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_dispositions_ready
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffSummary {
    pub release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slot_summary:
        Box<IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionReadinessExecutionSlotSummary>,
    pub total_readiness_evidence_review_disposition_action_readiness_execution_slots: usize,
    pub total_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows: usize,
    pub required_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows:
        usize,
    pub release_blocking_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows:
        usize,
    pub repair_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows: usize,
    pub review_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows: usize,
    pub operator_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows:
        usize,
    pub dispatch_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows:
        usize,
    pub execution_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows:
        usize,
    pub lineage_complete_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows:
        usize,
    pub release_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows:
        usize,
    pub release_ready_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows:
        usize,
    pub first_readiness_evidence_review_disposition_action_readiness_execution_handoff_row_key:
        Option<String>,
    pub first_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_row_key:
        Option<String>,
    pub first_repair_readiness_evidence_review_disposition_action_readiness_execution_handoff_row_key:
        Option<String>,
    pub first_review_readiness_evidence_review_disposition_action_readiness_execution_handoff_row_key:
        Option<String>,
    pub first_release_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_row_key:
        Option<String>,
    pub first_readiness_evidence_review_disposition_action_readiness_execution_slot_key:
        Option<String>,
    pub first_readiness_evidence_review_disposition_action_key: Option<String>,
    pub first_work_order_key: Option<String>,
    pub first_ticket_key: Option<String>,
    pub first_disposition_action_kind: Option<
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind,
    >,
    pub first_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub first_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub first_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_readiness_evidence_review_disposition_action_readiness_execution_handoff_row_key:
        Option<String>,
    pub next_readiness_evidence_review_disposition_action_readiness_execution_slot_key:
        Option<String>,
    pub next_readiness_evidence_review_disposition_action_key: Option<String>,
    pub next_work_order_key: Option<String>,
    pub next_ticket_key: Option<String>,
    pub next_check_kind: Option<IntegrationMeshReleaseReadinessCheckKind>,
    pub next_check_status: Option<IntegrationMeshReleaseReadinessStatus>,
    pub next_handoff_lane: Option<IntegrationMeshReleaseDispatchTicketHandoffLane>,
    pub release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slots_ready:
        bool,
    pub release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows_ready:
        bool,
}

impl
    IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffSummary
{
    pub fn from_summaries<'a>(
        release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slot_summary: IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionReadinessExecutionSlotSummary,
        rows: impl IntoIterator<
            Item = &'a IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffRow,
        >,
    ) -> Self {
        let rows: Vec<&IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffRow> =
            rows.into_iter().collect();
        let first_row = rows.first().copied();
        let first_required_row = rows
            .iter()
            .copied()
            .find(|row| row.requires_attention());
        let first_repair_row = rows.iter().copied().find(|row| row.is_repair_handoff());
        let first_review_row = rows.iter().copied().find(|row| row.is_review_handoff());
        let first_release_handoff_row =
            rows.iter().copied().find(|row| row.is_release_handoff());
        let next_row = first_required_row;
        let release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows_ready =
            rows.len()
                == release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slot_summary
                    .total_readiness_evidence_review_disposition_action_readiness_execution_slots
                && rows.iter().all(|row| row.ready_for_handoff());

        Self {
            total_readiness_evidence_review_disposition_action_readiness_execution_slots:
                release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slot_summary
                    .total_readiness_evidence_review_disposition_action_readiness_execution_slots,
            total_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows:
                rows.len(),
            required_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows:
                rows.iter().filter(|row| row.requires_attention()).count(),
            release_blocking_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows:
                rows.iter().filter(|row| row.blocks_release()).count(),
            repair_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows:
                rows.iter().filter(|row| row.is_repair_handoff()).count(),
            review_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows:
                rows.iter().filter(|row| row.is_review_handoff()).count(),
            operator_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows:
                rows.iter().filter(|row| row.needs_operator()).count(),
            dispatch_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows:
                rows.iter().filter(|row| row.requires_dispatch()).count(),
            execution_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows:
                rows.iter().filter(|row| row.requires_execution()).count(),
            lineage_complete_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows:
                rows.iter().filter(|row| row.has_lineage()).count(),
            release_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows:
                rows.iter().filter(|row| row.is_release_handoff()).count(),
            release_ready_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows:
                rows.iter().filter(|row| row.ready_for_handoff()).count(),
            first_readiness_evidence_review_disposition_action_readiness_execution_handoff_row_key:
                first_row.map(|row| row.handoff_row_key.clone()),
            first_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_row_key:
                first_required_row.map(|row| row.handoff_row_key.clone()),
            first_repair_readiness_evidence_review_disposition_action_readiness_execution_handoff_row_key:
                first_repair_row.map(|row| row.handoff_row_key.clone()),
            first_review_readiness_evidence_review_disposition_action_readiness_execution_handoff_row_key:
                first_review_row.map(|row| row.handoff_row_key.clone()),
            first_release_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_row_key:
                first_release_handoff_row.map(|row| row.handoff_row_key.clone()),
            first_readiness_evidence_review_disposition_action_readiness_execution_slot_key:
                first_row.map(|row| {
                    row.readiness_evidence_review_disposition_action_readiness_execution_slot_key
                        .clone()
                }),
            first_readiness_evidence_review_disposition_action_key:
                first_row.map(|row| row.readiness_evidence_review_disposition_action_key.clone()),
            first_work_order_key: first_row.map(|row| row.work_order_key.clone()),
            first_ticket_key: first_row.map(|row| row.ticket_key.clone()),
            first_disposition_action_kind: first_row.map(|row| row.disposition_action_kind),
            first_handoff_lane: first_row.map(|row| row.handoff_lane),
            first_check_kind: first_row.map(|row| row.check_kind),
            first_check_status: first_row.map(|row| row.status),
            next_readiness_evidence_review_disposition_action_readiness_execution_handoff_row_key:
                next_row.map(|row| row.handoff_row_key.clone()),
            next_readiness_evidence_review_disposition_action_readiness_execution_slot_key:
                next_row.map(|row| {
                    row.readiness_evidence_review_disposition_action_readiness_execution_slot_key
                        .clone()
                }),
            next_readiness_evidence_review_disposition_action_key:
                next_row.map(|row| row.readiness_evidence_review_disposition_action_key.clone()),
            next_work_order_key: next_row.map(|row| row.work_order_key.clone()),
            next_ticket_key: next_row.map(|row| row.ticket_key.clone()),
            next_check_kind: next_row.map(|row| row.check_kind),
            next_check_status: next_row.map(|row| row.status),
            next_handoff_lane: next_row.map(|row| row.handoff_lane),
            release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slots_ready:
                release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slot_summary
                    .ready_for_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slots(),
            release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows_ready,
            release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slot_summary:
                Box::new(
                    release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slot_summary,
                ),
        }
    }

    pub fn has_handoff_rows(&self) -> bool {
        self.total_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows > 0
    }

    pub fn ready_for_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoffs(
        &self,
    ) -> bool {
        self.release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slots_ready
            && self.release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows_ready
            && !self.requires_attention()
    }

    pub fn has_required_handoffs(&self) -> bool {
        self.required_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows
            > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.release_blocking_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows
            > 0
            || self.repair_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows
                > 0
            || self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slot_summary
                .has_blockers()
    }

    pub fn has_review_work(&self) -> bool {
        self.review_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows
            > 0
            || self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slot_summary
                .has_review_work()
    }

    pub fn needs_operator(&self) -> bool {
        self.operator_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows
            > 0
            || self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slot_summary
                .needs_operator()
    }

    pub fn requires_dispatch(&self) -> bool {
        self.dispatch_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows
            > 0
    }

    pub fn requires_execution(&self) -> bool {
        self.execution_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows
            > 0
    }

    pub fn has_lineage_gaps(&self) -> bool {
        self.lineage_complete_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows
            < self.total_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows
            || self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slot_summary
                .has_lineage_gaps()
    }

    pub fn has_release_handoffs(&self) -> bool {
        self.release_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows
            > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.has_required_handoffs()
            || self.has_blockers()
            || self.has_review_work()
            || self.needs_operator()
            || self.requires_dispatch()
            || self.requires_execution()
            || self.has_lineage_gaps()
            || !self
                .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows_ready
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationCatalogEntry {
    pub integration_id: IntegrationId,
    pub display_name: String,
    pub summary: String,
    pub category: IntegrationCategory,
    pub connectivity: ConnectivityClass,
    pub runtime_kind: RuntimeKind,
    pub implementation_status: ImplementationStatus,
    pub priority: u8,
    pub discovery_mechanisms: Vec<DiscoveryMechanism>,
    pub auth_modes: Vec<AuthMode>,
    pub required_capabilities: Vec<CapabilityId>,
    pub target_entity_kinds: Vec<EntityKind>,
    pub supported_protocols: Vec<ProtocolFamily>,
    pub depends_on_integrations: Vec<IntegrationId>,
    pub virtual_target: Option<IntegrationId>,
    pub virtual_iot_standards: Vec<ProtocolFamily>,
    pub required_primitives: Vec<PrimitiveFamily>,
    pub source_refs: Vec<SourceReference>,
    pub notes: Vec<String>,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationCatalogEntrySummary {
    pub integration_id: IntegrationId,
    pub display_name: String,
    pub category: IntegrationCategory,
    pub connectivity: ConnectivityClass,
    pub runtime_kind: RuntimeKind,
    pub implementation_status: ImplementationStatus,
    pub priority: u8,
    pub discovery_mechanism_count: usize,
    pub auth_mode_count: usize,
    pub required_capability_count: usize,
    pub target_entity_kind_count: usize,
    pub supported_protocol_count: usize,
    pub dependency_count: usize,
    pub virtual_iot_standard_count: usize,
    pub required_primitive_count: usize,
    pub source_ref_count: usize,
    pub note_count: usize,
    pub policy_surface_count: usize,
    pub highest_policy_tier: PrivilegeTier,
    pub local_only: bool,
    pub cloud_required: bool,
    pub virtual_alias: bool,
    pub has_dependencies: bool,
    pub requires_human_review: bool,
}

impl IntegrationCatalogEntrySummary {
    pub fn from_entry(entry: &IntegrationCatalogEntry) -> Self {
        let policy_surfaces = entry.policy_surfaces();
        let highest_policy_tier = policy_surfaces
            .iter()
            .map(|surface| surface.required_tier())
            .max()
            .unwrap_or(PrivilegeTier::ReadOnly);
        let local_only = entry.is_local() && !entry.requires_cloud();
        let cloud_required = entry.requires_cloud()
            || entry
                .discovery_mechanisms
                .contains(&DiscoveryMechanism::CloudAccount);
        let has_dependencies = !entry.depends_on_integrations.is_empty();
        let virtual_alias = entry.is_virtual() || entry.virtual_target.is_some();

        Self {
            integration_id: entry.integration_id.clone(),
            display_name: entry.display_name.clone(),
            category: entry.category,
            connectivity: entry.connectivity,
            runtime_kind: entry.runtime_kind,
            implementation_status: entry.implementation_status,
            priority: entry.priority,
            discovery_mechanism_count: entry.discovery_mechanisms.len(),
            auth_mode_count: entry.auth_modes.len(),
            required_capability_count: entry.required_capabilities.len(),
            target_entity_kind_count: entry.target_entity_kinds.len(),
            supported_protocol_count: entry.supported_protocols.len(),
            dependency_count: entry.depends_on_integrations.len(),
            virtual_iot_standard_count: entry.virtual_iot_standards.len(),
            required_primitive_count: entry.required_primitives.len(),
            source_ref_count: entry.source_refs.len(),
            note_count: entry.notes.len(),
            policy_surface_count: policy_surfaces.len(),
            highest_policy_tier,
            local_only,
            cloud_required,
            virtual_alias,
            has_dependencies,
            requires_human_review: highest_policy_tier >= PrivilegeTier::HumanApproval,
        }
    }

    pub fn has_catalog_metadata(&self) -> bool {
        self.discovery_mechanism_count > 0
            && self.auth_mode_count > 0
            && self.required_capability_count > 0
            && self.required_primitive_count > 0
            && self.source_ref_count > 0
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationPackageSummary {
    pub catalog_entry: IntegrationCatalogEntrySummary,
    pub activation_target: IntegrationActivationTarget,
    pub direct_activation: bool,
    pub delegated_activation: bool,
    pub required_primitive_count: usize,
    pub required_capability_count: usize,
    pub auth_mode_count: usize,
    pub discovery_mechanism_count: usize,
    pub dependency_count: usize,
    pub policy_surface_count: usize,
    pub highest_policy_tier: PrivilegeTier,
    pub local_only: bool,
    pub cloud_required: bool,
    pub requires_human_review: bool,
}

impl IntegrationActivationPackageSummary {
    pub fn from_entry(entry: &IntegrationCatalogEntry) -> Self {
        let plan = activation_plan_for_entry(entry);
        Self::from_plan(entry.summary(), &plan)
    }

    pub fn from_plan(
        catalog_entry: IntegrationCatalogEntrySummary,
        plan: &IntegrationActivationPlan,
    ) -> Self {
        let direct_activation = plan.activation_target == IntegrationActivationTarget::Direct;
        let delegated_activation = !direct_activation;

        Self {
            catalog_entry,
            activation_target: plan.activation_target.clone(),
            direct_activation,
            delegated_activation,
            required_primitive_count: plan.required_primitives.len(),
            required_capability_count: plan.required_capabilities.len(),
            auth_mode_count: plan.auth_modes.len(),
            discovery_mechanism_count: plan.discovery_mechanisms.len(),
            dependency_count: plan.depends_on_integrations.len(),
            policy_surface_count: plan.policy_surfaces.len(),
            highest_policy_tier: plan.highest_policy_tier,
            local_only: plan.local_only,
            cloud_required: plan.cloud_required,
            requires_human_review: plan.requires_human_review(),
        }
    }

    pub fn has_prerequisites(&self) -> bool {
        self.required_primitive_count > 0
            || self.required_capability_count > 0
            || self.dependency_count > 0
    }

    pub fn has_policy_review(&self) -> bool {
        self.requires_human_review || self.policy_surface_count > 0
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationReadinessPackageSummary {
    pub activation_package: IntegrationActivationPackageSummary,
    pub activation_ready: bool,
    pub blocked: bool,
    pub missing_primitive_count: usize,
    pub missing_capability_count: usize,
    pub missing_dependency_count: usize,
    pub missing_prerequisite_count: usize,
    pub requires_human_review: bool,
    pub highest_policy_tier: PrivilegeTier,
    pub local_only: bool,
    pub cloud_required: bool,
}

impl IntegrationReadinessPackageSummary {
    pub fn from_entry(
        entry: &IntegrationCatalogEntry,
        available_primitives: &[PrimitiveFamily],
        allowed_capabilities: &[CapabilityId],
        enabled_integrations: &[IntegrationId],
    ) -> Self {
        let plan = activation_plan_for_entry(entry);
        let report = readiness_report_for_plan(
            &plan,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );
        Self::from_package_and_report(
            IntegrationActivationPackageSummary::from_plan(entry.summary(), &plan),
            &report,
        )
    }

    pub fn from_package_and_report(
        activation_package: IntegrationActivationPackageSummary,
        report: &IntegrationReadinessReport,
    ) -> Self {
        let activation_ready = report.activation_ready();
        Self {
            activation_package,
            activation_ready,
            blocked: !activation_ready,
            missing_primitive_count: report.missing_primitives.len(),
            missing_capability_count: report.missing_capabilities.len(),
            missing_dependency_count: report.missing_dependencies.len(),
            missing_prerequisite_count: report.missing_prerequisite_count(),
            requires_human_review: report.requires_human_review,
            highest_policy_tier: report.highest_policy_tier,
            local_only: report.local_only,
            cloud_required: report.cloud_required,
        }
    }

    pub fn has_missing_primitives(&self) -> bool {
        self.missing_primitive_count > 0
    }

    pub fn has_missing_capabilities(&self) -> bool {
        self.missing_capability_count > 0
    }

    pub fn has_missing_dependencies(&self) -> bool {
        self.missing_dependency_count > 0
    }

    pub fn has_policy_review(&self) -> bool {
        self.requires_human_review || self.activation_package.has_policy_review()
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationEvidenceBriefingSummary {
    pub activation_package: IntegrationActivationPackageSummary,
    pub readiness_package: IntegrationReadinessPackageSummary,
    pub required_evidence_lane_count: usize,
    pub passed_evidence_lane_count: usize,
    pub blocked_evidence_lane_count: usize,
    pub missing_prerequisite_count: usize,
    pub missing_primitive_count: usize,
    pub missing_capability_count: usize,
    pub missing_dependency_count: usize,
    pub catalog_evidence_ready: bool,
    pub activation_plan_evidence_ready: bool,
    pub readiness_evidence_ready: bool,
    pub policy_evidence_ready: bool,
    pub local_boundary_evidence_ready: bool,
    pub evidence_briefing_ready: bool,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationEvidenceScorecardSummary {
    pub total_integrations: usize,
    pub evidence_ready_integrations: usize,
    pub blocked_integrations: usize,
    pub total_required_evidence_lanes: usize,
    pub total_passed_evidence_lanes: usize,
    pub total_blocked_evidence_lanes: usize,
    pub catalog_evidence_ready_integrations: usize,
    pub activation_plan_evidence_ready_integrations: usize,
    pub readiness_evidence_ready_integrations: usize,
    pub policy_evidence_ready_integrations: usize,
    pub local_boundary_evidence_ready_integrations: usize,
    pub catalog_evidence_blockers: usize,
    pub activation_plan_evidence_blockers: usize,
    pub readiness_evidence_blockers: usize,
    pub policy_evidence_blockers: usize,
    pub local_boundary_evidence_blockers: usize,
    pub missing_prerequisite_count: usize,
    pub missing_primitive_count: usize,
    pub missing_capability_count: usize,
    pub missing_dependency_count: usize,
    pub local_only_integrations: usize,
    pub cloud_required_integrations: usize,
    pub human_review_integrations: usize,
    pub first_blocked_priority: Option<u8>,
    pub first_ready_priority: Option<u8>,
    pub highest_policy_tier: PrivilegeTier,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationActivationEvidenceLane {
    Catalog,
    ActivationPlan,
    Readiness,
    Policy,
    LocalBoundary,
}

impl IntegrationActivationEvidenceLane {
    pub fn as_str(self) -> &'static str {
        match self {
            Self::Catalog => "catalog",
            Self::ActivationPlan => "activation_plan",
            Self::Readiness => "readiness",
            Self::Policy => "policy",
            Self::LocalBoundary => "local_boundary",
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationEvidenceRow {
    pub integration_id: IntegrationId,
    pub display_name: String,
    pub priority: u8,
    pub evidence_briefing_ready: bool,
    pub required_evidence_lane_count: usize,
    pub passed_evidence_lane_count: usize,
    pub blocked_evidence_lane_count: usize,
    pub next_blocked_lane: Option<IntegrationActivationEvidenceLane>,
    pub missing_prerequisite_count: usize,
    pub missing_primitive_count: usize,
    pub missing_capability_count: usize,
    pub missing_dependency_count: usize,
    pub local_only: bool,
    pub cloud_required: bool,
    pub requires_human_review: bool,
    pub highest_policy_tier: PrivilegeTier,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationEvidenceLaneInventoryItem {
    pub lane: IntegrationActivationEvidenceLane,
    pub first_blocked_priority: u8,
    pub blocked_integration_count: usize,
    pub missing_prerequisite_count: usize,
    pub missing_primitive_count: usize,
    pub missing_capability_count: usize,
    pub missing_dependency_count: usize,
    pub local_only_integrations: usize,
    pub cloud_required_integrations: usize,
    pub human_review_integrations: usize,
    pub highest_policy_tier: PrivilegeTier,
    pub integration_ids: Vec<IntegrationId>,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationEvidenceLaneInventorySummary {
    pub total_lanes: usize,
    pub total_blocked_integrations: usize,
    pub catalog_lane_blocked_integrations: usize,
    pub activation_plan_lane_blocked_integrations: usize,
    pub readiness_lane_blocked_integrations: usize,
    pub policy_lane_blocked_integrations: usize,
    pub local_boundary_lane_blocked_integrations: usize,
    pub missing_prerequisite_count: usize,
    pub missing_primitive_count: usize,
    pub missing_capability_count: usize,
    pub missing_dependency_count: usize,
    pub local_only_integrations: usize,
    pub cloud_required_integrations: usize,
    pub human_review_integrations: usize,
    pub first_blocked_priority: Option<u8>,
    pub highest_policy_tier: PrivilegeTier,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationActivationEvidenceRemediationKind {
    CompleteCatalogEvidence,
    CompleteActivationPlan,
    SupplyReadinessInputs,
    CompletePolicyReview,
    ResolveLocalBoundary,
}

impl IntegrationActivationEvidenceRemediationKind {
    pub fn as_str(self) -> &'static str {
        match self {
            Self::CompleteCatalogEvidence => "complete_catalog_evidence",
            Self::CompleteActivationPlan => "complete_activation_plan",
            Self::SupplyReadinessInputs => "supply_readiness_inputs",
            Self::CompletePolicyReview => "complete_policy_review",
            Self::ResolveLocalBoundary => "resolve_local_boundary",
        }
    }

    pub fn from_lane(lane: IntegrationActivationEvidenceLane) -> Self {
        match lane {
            IntegrationActivationEvidenceLane::Catalog => Self::CompleteCatalogEvidence,
            IntegrationActivationEvidenceLane::ActivationPlan => Self::CompleteActivationPlan,
            IntegrationActivationEvidenceLane::Readiness => Self::SupplyReadinessInputs,
            IntegrationActivationEvidenceLane::Policy => Self::CompletePolicyReview,
            IntegrationActivationEvidenceLane::LocalBoundary => Self::ResolveLocalBoundary,
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationEvidenceRemediationItem {
    pub sequence: usize,
    pub remediation_kind: IntegrationActivationEvidenceRemediationKind,
    pub lane: IntegrationActivationEvidenceLane,
    pub priority: u8,
    pub blocked_integration_count: usize,
    pub missing_prerequisite_count: usize,
    pub missing_primitive_count: usize,
    pub missing_capability_count: usize,
    pub missing_dependency_count: usize,
    pub local_only_integrations: usize,
    pub cloud_required_integrations: usize,
    pub human_review_integrations: usize,
    pub highest_policy_tier: PrivilegeTier,
    pub integration_ids: Vec<IntegrationId>,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationEvidenceRemediationSummary {
    pub total_remediation_items: usize,
    pub unique_integrations: usize,
    pub total_blocked_integrations: usize,
    pub catalog_remediation_items: usize,
    pub activation_plan_remediation_items: usize,
    pub readiness_remediation_items: usize,
    pub policy_remediation_items: usize,
    pub local_boundary_remediation_items: usize,
    pub missing_prerequisite_count: usize,
    pub missing_primitive_count: usize,
    pub missing_capability_count: usize,
    pub missing_dependency_count: usize,
    pub local_only_integrations: usize,
    pub cloud_required_integrations: usize,
    pub human_review_integrations: usize,
    pub first_remediation_priority: Option<u8>,
    pub next_remediation_kind: Option<IntegrationActivationEvidenceRemediationKind>,
    pub highest_policy_tier: PrivilegeTier,
}

impl IntegrationActivationEvidenceBriefingSummary {
    pub fn from_entry(
        entry: &IntegrationCatalogEntry,
        available_primitives: &[PrimitiveFamily],
        allowed_capabilities: &[CapabilityId],
        enabled_integrations: &[IntegrationId],
    ) -> Self {
        let activation_package = IntegrationActivationPackageSummary::from_entry(entry);
        let readiness_package = IntegrationReadinessPackageSummary::from_entry(
            entry,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );
        Self::from_packages(activation_package, readiness_package)
    }

    pub fn from_packages(
        activation_package: IntegrationActivationPackageSummary,
        readiness_package: IntegrationReadinessPackageSummary,
    ) -> Self {
        let catalog_evidence_ready = activation_package.catalog_entry.has_catalog_metadata();
        let activation_plan_evidence_ready =
            activation_package.direct_activation && activation_package.has_prerequisites();
        let readiness_evidence_ready = readiness_package.activation_ready;
        let policy_evidence_ready = readiness_package.has_policy_review();
        let local_boundary_evidence_ready =
            readiness_package.local_only && !readiness_package.cloud_required;
        let lanes = [
            catalog_evidence_ready,
            activation_plan_evidence_ready,
            readiness_evidence_ready,
            policy_evidence_ready,
            local_boundary_evidence_ready,
        ];
        let passed_evidence_lane_count = lanes.iter().filter(|ready| **ready).count();
        let required_evidence_lane_count = lanes.len();
        let blocked_evidence_lane_count = required_evidence_lane_count - passed_evidence_lane_count;
        let evidence_briefing_ready = blocked_evidence_lane_count == 0;
        let missing_prerequisite_count = readiness_package.missing_prerequisite_count;
        let missing_primitive_count = readiness_package.missing_primitive_count;
        let missing_capability_count = readiness_package.missing_capability_count;
        let missing_dependency_count = readiness_package.missing_dependency_count;

        Self {
            activation_package,
            readiness_package,
            required_evidence_lane_count,
            passed_evidence_lane_count,
            blocked_evidence_lane_count,
            missing_prerequisite_count,
            missing_primitive_count,
            missing_capability_count,
            missing_dependency_count,
            catalog_evidence_ready,
            activation_plan_evidence_ready,
            readiness_evidence_ready,
            policy_evidence_ready,
            local_boundary_evidence_ready,
            evidence_briefing_ready,
        }
    }

    pub fn is_evidence_briefing_ready(&self) -> bool {
        self.evidence_briefing_ready
    }

    pub fn has_blocked_evidence_lanes(&self) -> bool {
        self.blocked_evidence_lane_count > 0
    }

    pub fn needs_catalog_evidence(&self) -> bool {
        !self.catalog_evidence_ready
    }

    pub fn needs_activation_plan_evidence(&self) -> bool {
        !self.activation_plan_evidence_ready
    }

    pub fn needs_readiness_evidence(&self) -> bool {
        !self.readiness_evidence_ready
    }

    pub fn needs_policy_evidence(&self) -> bool {
        !self.policy_evidence_ready
    }

    pub fn needs_local_boundary_evidence(&self) -> bool {
        !self.local_boundary_evidence_ready
    }
}

impl IntegrationActivationEvidenceRow {
    pub fn from_briefing(briefing: &IntegrationActivationEvidenceBriefingSummary) -> Self {
        let catalog_entry = &briefing.activation_package.catalog_entry;
        Self {
            integration_id: catalog_entry.integration_id.clone(),
            display_name: catalog_entry.display_name.clone(),
            priority: catalog_entry.priority,
            evidence_briefing_ready: briefing.evidence_briefing_ready,
            required_evidence_lane_count: briefing.required_evidence_lane_count,
            passed_evidence_lane_count: briefing.passed_evidence_lane_count,
            blocked_evidence_lane_count: briefing.blocked_evidence_lane_count,
            next_blocked_lane: next_blocked_evidence_lane(briefing),
            missing_prerequisite_count: briefing.missing_prerequisite_count,
            missing_primitive_count: briefing.missing_primitive_count,
            missing_capability_count: briefing.missing_capability_count,
            missing_dependency_count: briefing.missing_dependency_count,
            local_only: briefing.readiness_package.local_only,
            cloud_required: briefing.readiness_package.cloud_required,
            requires_human_review: briefing.readiness_package.has_policy_review(),
            highest_policy_tier: briefing.readiness_package.highest_policy_tier,
        }
    }

    pub fn is_ready(&self) -> bool {
        self.evidence_briefing_ready
    }

    pub fn has_blockers(&self) -> bool {
        self.blocked_evidence_lane_count > 0
    }

    pub fn blocks_on(&self, lane: IntegrationActivationEvidenceLane) -> bool {
        self.next_blocked_lane == Some(lane)
    }
}

impl IntegrationActivationEvidenceLaneInventoryItem {
    fn new(
        lane: IntegrationActivationEvidenceLane,
        row: &IntegrationActivationEvidenceRow,
    ) -> Self {
        Self {
            lane,
            first_blocked_priority: row.priority,
            blocked_integration_count: 0,
            missing_prerequisite_count: 0,
            missing_primitive_count: 0,
            missing_capability_count: 0,
            missing_dependency_count: 0,
            local_only_integrations: 0,
            cloud_required_integrations: 0,
            human_review_integrations: 0,
            highest_policy_tier: PrivilegeTier::ReadOnly,
            integration_ids: Vec::new(),
        }
    }

    fn add_row(&mut self, row: &IntegrationActivationEvidenceRow) {
        self.first_blocked_priority = self.first_blocked_priority.min(row.priority);
        self.blocked_integration_count += 1;
        self.missing_prerequisite_count += row.missing_prerequisite_count;
        self.missing_primitive_count += row.missing_primitive_count;
        self.missing_capability_count += row.missing_capability_count;
        self.missing_dependency_count += row.missing_dependency_count;
        self.highest_policy_tier = self.highest_policy_tier.max(row.highest_policy_tier);
        if row.local_only {
            self.local_only_integrations += 1;
        }
        if row.cloud_required {
            self.cloud_required_integrations += 1;
        }
        if row.requires_human_review {
            self.human_review_integrations += 1;
        }
        self.integration_ids.push(row.integration_id.clone());
    }

    pub fn has_blockers(&self) -> bool {
        self.blocked_integration_count > 0
    }

    pub fn includes_integration(&self, integration_id: &IntegrationId) -> bool {
        self.integration_ids.contains(integration_id)
    }

    pub fn requires_human_review(&self) -> bool {
        self.human_review_integrations > 0
    }
}

impl IntegrationActivationEvidenceLaneInventorySummary {
    pub fn from_items<'a>(
        items: impl IntoIterator<Item = &'a IntegrationActivationEvidenceLaneInventoryItem>,
    ) -> Self {
        let mut summary = Self {
            total_lanes: 0,
            total_blocked_integrations: 0,
            catalog_lane_blocked_integrations: 0,
            activation_plan_lane_blocked_integrations: 0,
            readiness_lane_blocked_integrations: 0,
            policy_lane_blocked_integrations: 0,
            local_boundary_lane_blocked_integrations: 0,
            missing_prerequisite_count: 0,
            missing_primitive_count: 0,
            missing_capability_count: 0,
            missing_dependency_count: 0,
            local_only_integrations: 0,
            cloud_required_integrations: 0,
            human_review_integrations: 0,
            first_blocked_priority: None,
            highest_policy_tier: PrivilegeTier::ReadOnly,
        };

        for item in items {
            summary.total_lanes += 1;
            summary.total_blocked_integrations += item.blocked_integration_count;
            summary.missing_prerequisite_count += item.missing_prerequisite_count;
            summary.missing_primitive_count += item.missing_primitive_count;
            summary.missing_capability_count += item.missing_capability_count;
            summary.missing_dependency_count += item.missing_dependency_count;
            summary.local_only_integrations += item.local_only_integrations;
            summary.cloud_required_integrations += item.cloud_required_integrations;
            summary.human_review_integrations += item.human_review_integrations;
            summary.highest_policy_tier = summary.highest_policy_tier.max(item.highest_policy_tier);
            summary.first_blocked_priority = Some(
                summary
                    .first_blocked_priority
                    .map_or(item.first_blocked_priority, |existing| {
                        existing.min(item.first_blocked_priority)
                    }),
            );

            match item.lane {
                IntegrationActivationEvidenceLane::Catalog => {
                    summary.catalog_lane_blocked_integrations += item.blocked_integration_count;
                }
                IntegrationActivationEvidenceLane::ActivationPlan => {
                    summary.activation_plan_lane_blocked_integrations +=
                        item.blocked_integration_count;
                }
                IntegrationActivationEvidenceLane::Readiness => {
                    summary.readiness_lane_blocked_integrations += item.blocked_integration_count;
                }
                IntegrationActivationEvidenceLane::Policy => {
                    summary.policy_lane_blocked_integrations += item.blocked_integration_count;
                }
                IntegrationActivationEvidenceLane::LocalBoundary => {
                    summary.local_boundary_lane_blocked_integrations +=
                        item.blocked_integration_count;
                }
            }
        }

        summary
    }

    pub fn is_empty(&self) -> bool {
        self.total_lanes == 0
    }

    pub fn has_blockers(&self) -> bool {
        self.total_blocked_integrations > 0
    }

    pub fn has_readiness_lane(&self) -> bool {
        self.readiness_lane_blocked_integrations > 0
    }
}

impl IntegrationActivationEvidenceRemediationItem {
    pub fn from_lane_inventory_item(
        sequence: usize,
        item: &IntegrationActivationEvidenceLaneInventoryItem,
    ) -> Self {
        Self {
            sequence,
            remediation_kind: IntegrationActivationEvidenceRemediationKind::from_lane(item.lane),
            lane: item.lane,
            priority: item.first_blocked_priority,
            blocked_integration_count: item.blocked_integration_count,
            missing_prerequisite_count: item.missing_prerequisite_count,
            missing_primitive_count: item.missing_primitive_count,
            missing_capability_count: item.missing_capability_count,
            missing_dependency_count: item.missing_dependency_count,
            local_only_integrations: item.local_only_integrations,
            cloud_required_integrations: item.cloud_required_integrations,
            human_review_integrations: item.human_review_integrations,
            highest_policy_tier: item.highest_policy_tier,
            integration_ids: item.integration_ids.clone(),
        }
    }

    pub fn has_blockers(&self) -> bool {
        self.blocked_integration_count > 0
    }

    pub fn includes_integration(&self, integration_id: &IntegrationId) -> bool {
        self.integration_ids.contains(integration_id)
    }

    pub fn requires_human_review(&self) -> bool {
        self.human_review_integrations > 0
    }

    pub fn has_missing_inputs(&self) -> bool {
        self.missing_prerequisite_count > 0
            || self.missing_primitive_count > 0
            || self.missing_capability_count > 0
            || self.missing_dependency_count > 0
    }
}

impl IntegrationActivationEvidenceRemediationSummary {
    pub fn from_items<'a>(
        items: impl IntoIterator<Item = &'a IntegrationActivationEvidenceRemediationItem>,
    ) -> Self {
        let mut summary = Self {
            total_remediation_items: 0,
            unique_integrations: 0,
            total_blocked_integrations: 0,
            catalog_remediation_items: 0,
            activation_plan_remediation_items: 0,
            readiness_remediation_items: 0,
            policy_remediation_items: 0,
            local_boundary_remediation_items: 0,
            missing_prerequisite_count: 0,
            missing_primitive_count: 0,
            missing_capability_count: 0,
            missing_dependency_count: 0,
            local_only_integrations: 0,
            cloud_required_integrations: 0,
            human_review_integrations: 0,
            first_remediation_priority: None,
            next_remediation_kind: None,
            highest_policy_tier: PrivilegeTier::ReadOnly,
        };
        let mut integration_ids = BTreeSet::new();

        for item in items {
            summary.total_remediation_items += 1;
            summary.total_blocked_integrations += item.blocked_integration_count;
            summary.missing_prerequisite_count += item.missing_prerequisite_count;
            summary.missing_primitive_count += item.missing_primitive_count;
            summary.missing_capability_count += item.missing_capability_count;
            summary.missing_dependency_count += item.missing_dependency_count;
            summary.local_only_integrations += item.local_only_integrations;
            summary.cloud_required_integrations += item.cloud_required_integrations;
            summary.human_review_integrations += item.human_review_integrations;
            summary.highest_policy_tier = summary.highest_policy_tier.max(item.highest_policy_tier);

            if summary.next_remediation_kind.is_none() {
                summary.next_remediation_kind = Some(item.remediation_kind);
            }
            summary.first_remediation_priority = Some(
                summary
                    .first_remediation_priority
                    .map_or(item.priority, |existing| existing.min(item.priority)),
            );

            match item.remediation_kind {
                IntegrationActivationEvidenceRemediationKind::CompleteCatalogEvidence => {
                    summary.catalog_remediation_items += 1;
                }
                IntegrationActivationEvidenceRemediationKind::CompleteActivationPlan => {
                    summary.activation_plan_remediation_items += 1;
                }
                IntegrationActivationEvidenceRemediationKind::SupplyReadinessInputs => {
                    summary.readiness_remediation_items += 1;
                }
                IntegrationActivationEvidenceRemediationKind::CompletePolicyReview => {
                    summary.policy_remediation_items += 1;
                }
                IntegrationActivationEvidenceRemediationKind::ResolveLocalBoundary => {
                    summary.local_boundary_remediation_items += 1;
                }
            }

            integration_ids.extend(item.integration_ids.iter().cloned());
        }

        summary.unique_integrations = integration_ids.len();
        summary
    }

    pub fn is_empty(&self) -> bool {
        self.total_remediation_items == 0
    }

    pub fn has_remediations(&self) -> bool {
        self.total_remediation_items > 0
    }

    pub fn has_readiness_remediation(&self) -> bool {
        self.readiness_remediation_items > 0
    }

    pub fn requires_human_review(&self) -> bool {
        self.human_review_integrations > 0
    }
}

impl IntegrationActivationEvidenceScorecardSummary {
    pub fn from_briefings<'a>(
        briefings: impl IntoIterator<Item = &'a IntegrationActivationEvidenceBriefingSummary>,
    ) -> Self {
        let mut summary = Self {
            total_integrations: 0,
            evidence_ready_integrations: 0,
            blocked_integrations: 0,
            total_required_evidence_lanes: 0,
            total_passed_evidence_lanes: 0,
            total_blocked_evidence_lanes: 0,
            catalog_evidence_ready_integrations: 0,
            activation_plan_evidence_ready_integrations: 0,
            readiness_evidence_ready_integrations: 0,
            policy_evidence_ready_integrations: 0,
            local_boundary_evidence_ready_integrations: 0,
            catalog_evidence_blockers: 0,
            activation_plan_evidence_blockers: 0,
            readiness_evidence_blockers: 0,
            policy_evidence_blockers: 0,
            local_boundary_evidence_blockers: 0,
            missing_prerequisite_count: 0,
            missing_primitive_count: 0,
            missing_capability_count: 0,
            missing_dependency_count: 0,
            local_only_integrations: 0,
            cloud_required_integrations: 0,
            human_review_integrations: 0,
            first_blocked_priority: None,
            first_ready_priority: None,
            highest_policy_tier: PrivilegeTier::ReadOnly,
        };

        for briefing in briefings {
            let priority = briefing.activation_package.catalog_entry.priority;
            summary.total_integrations += 1;
            summary.total_required_evidence_lanes += briefing.required_evidence_lane_count;
            summary.total_passed_evidence_lanes += briefing.passed_evidence_lane_count;
            summary.total_blocked_evidence_lanes += briefing.blocked_evidence_lane_count;
            summary.missing_prerequisite_count += briefing.missing_prerequisite_count;
            summary.missing_primitive_count += briefing.missing_primitive_count;
            summary.missing_capability_count += briefing.missing_capability_count;
            summary.missing_dependency_count += briefing.missing_dependency_count;
            summary.highest_policy_tier = summary
                .highest_policy_tier
                .max(briefing.readiness_package.highest_policy_tier);

            if briefing.readiness_package.local_only {
                summary.local_only_integrations += 1;
            }
            if briefing.readiness_package.cloud_required {
                summary.cloud_required_integrations += 1;
            }
            if briefing.readiness_package.has_policy_review() {
                summary.human_review_integrations += 1;
            }

            if briefing.evidence_briefing_ready {
                summary.evidence_ready_integrations += 1;
                summary.first_ready_priority = Some(
                    summary
                        .first_ready_priority
                        .map_or(priority, |existing| existing.min(priority)),
                );
            } else {
                summary.blocked_integrations += 1;
                summary.first_blocked_priority = Some(
                    summary
                        .first_blocked_priority
                        .map_or(priority, |existing| existing.min(priority)),
                );
            }

            if briefing.catalog_evidence_ready {
                summary.catalog_evidence_ready_integrations += 1;
            } else {
                summary.catalog_evidence_blockers += 1;
            }
            if briefing.activation_plan_evidence_ready {
                summary.activation_plan_evidence_ready_integrations += 1;
            } else {
                summary.activation_plan_evidence_blockers += 1;
            }
            if briefing.readiness_evidence_ready {
                summary.readiness_evidence_ready_integrations += 1;
            } else {
                summary.readiness_evidence_blockers += 1;
            }
            if briefing.policy_evidence_ready {
                summary.policy_evidence_ready_integrations += 1;
            } else {
                summary.policy_evidence_blockers += 1;
            }
            if briefing.local_boundary_evidence_ready {
                summary.local_boundary_evidence_ready_integrations += 1;
            } else {
                summary.local_boundary_evidence_blockers += 1;
            }
        }

        summary
    }

    pub fn is_empty(&self) -> bool {
        self.total_integrations == 0
    }

    pub fn all_ready(&self) -> bool {
        self.total_integrations > 0 && self.blocked_integrations == 0
    }

    pub fn has_blockers(&self) -> bool {
        self.blocked_integrations > 0 || self.total_blocked_evidence_lanes > 0
    }

    pub fn has_missing_prerequisites(&self) -> bool {
        self.missing_prerequisite_count > 0
    }

    pub fn has_readiness_gaps(&self) -> bool {
        self.readiness_evidence_blockers > 0
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum IntegrationCatalogSort {
    PriorityThenName,
    Name,
    CategoryThenPriority,
    StatusThenPriority,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationCatalogQuery {
    pub categories: Vec<IntegrationCategory>,
    pub connectivity: Vec<ConnectivityClass>,
    pub implementation_statuses: Vec<ImplementationStatus>,
    pub required_primitives: Vec<PrimitiveFamily>,
    pub required_capabilities: Vec<CapabilityId>,
    pub policy_surfaces: Vec<IntegrationPolicySurface>,
    pub discovery_mechanisms: Vec<DiscoveryMechanism>,
    pub auth_modes: Vec<AuthMode>,
    pub protocol_families: Vec<ProtocolFamily>,
    pub priority_at_or_before: Option<u8>,
    pub include_virtual_aliases: bool,
    pub local_only: Option<bool>,
    pub cloud_required: Option<bool>,
    pub sort: IntegrationCatalogSort,
    pub limit: Option<usize>,
}

impl Default for IntegrationCatalogQuery {
    fn default() -> Self {
        Self {
            categories: Vec::new(),
            connectivity: Vec::new(),
            implementation_statuses: Vec::new(),
            required_primitives: Vec::new(),
            required_capabilities: Vec::new(),
            policy_surfaces: Vec::new(),
            discovery_mechanisms: Vec::new(),
            auth_modes: Vec::new(),
            protocol_families: Vec::new(),
            priority_at_or_before: None,
            include_virtual_aliases: true,
            local_only: None,
            cloud_required: None,
            sort: IntegrationCatalogSort::PriorityThenName,
            limit: None,
        }
    }
}

impl IntegrationCatalogQuery {
    pub fn new() -> Self {
        Self::default()
    }

    pub fn with_category(mut self, category: IntegrationCategory) -> Self {
        self.categories.push(category);
        self
    }

    pub fn with_connectivity(mut self, connectivity: ConnectivityClass) -> Self {
        self.connectivity.push(connectivity);
        self
    }

    pub fn with_status(mut self, status: ImplementationStatus) -> Self {
        self.implementation_statuses.push(status);
        self
    }

    pub fn requiring_primitive(mut self, primitive: PrimitiveFamily) -> Self {
        self.required_primitives.push(primitive);
        self
    }

    pub fn requiring_capability(mut self, capability_id: CapabilityId) -> Self {
        self.required_capabilities.push(capability_id);
        self
    }

    pub fn with_policy_surface(mut self, surface: IntegrationPolicySurface) -> Self {
        self.policy_surfaces.push(surface);
        self
    }

    pub fn with_discovery_mechanism(mut self, mechanism: DiscoveryMechanism) -> Self {
        self.discovery_mechanisms.push(mechanism);
        self
    }

    pub fn with_auth_mode(mut self, mode: AuthMode) -> Self {
        self.auth_modes.push(mode);
        self
    }

    pub fn with_protocol_family(mut self, protocol: ProtocolFamily) -> Self {
        self.protocol_families.push(protocol);
        self
    }

    pub fn at_or_before_priority(mut self, priority: u8) -> Self {
        self.priority_at_or_before = Some(priority);
        self
    }

    pub fn include_virtual_aliases(mut self, include: bool) -> Self {
        self.include_virtual_aliases = include;
        self
    }

    pub fn local_only(mut self, local_only: bool) -> Self {
        self.local_only = Some(local_only);
        self
    }

    pub fn cloud_required(mut self, cloud_required: bool) -> Self {
        self.cloud_required = Some(cloud_required);
        self
    }

    pub fn sorted_by(mut self, sort: IntegrationCatalogSort) -> Self {
        self.sort = sort;
        self
    }

    pub fn limited_to(mut self, limit: usize) -> Self {
        self.limit = Some(limit);
        self
    }

    pub fn matches_entry(&self, entry: &IntegrationCatalogEntry) -> bool {
        if !self.include_virtual_aliases && entry.is_virtual() {
            return false;
        }
        if let Some(priority) = self.priority_at_or_before {
            if entry.priority > priority {
                return false;
            }
        }
        if let Some(local_only) = self.local_only {
            if entry_local_only(entry) != local_only {
                return false;
            }
        }
        if let Some(cloud_required) = self.cloud_required {
            if entry_cloud_required(entry) != cloud_required {
                return false;
            }
        }
        if !matches_any(&self.categories, &entry.category) {
            return false;
        }
        if !matches_any(&self.connectivity, &entry.connectivity) {
            return false;
        }
        if !matches_any(&self.implementation_statuses, &entry.implementation_status) {
            return false;
        }
        if !self
            .required_primitives
            .iter()
            .all(|primitive| entry.requires_primitive(*primitive))
        {
            return false;
        }
        if !self
            .required_capabilities
            .iter()
            .all(|capability_id| entry.supports_capability(capability_id))
        {
            return false;
        }
        if !self
            .policy_surfaces
            .iter()
            .all(|surface| entry.has_policy_surface(*surface))
        {
            return false;
        }
        if !self
            .discovery_mechanisms
            .iter()
            .all(|mechanism| entry.uses_discovery(*mechanism))
        {
            return false;
        }
        if !self
            .auth_modes
            .iter()
            .all(|mode| entry.auth_modes.contains(mode))
        {
            return false;
        }
        if !self.protocol_families.is_empty()
            && !self.protocol_families.iter().any(|protocol| {
                entry
                    .supported_protocols
                    .iter()
                    .any(|candidate| candidate == protocol)
                    || entry
                        .virtual_iot_standards
                        .iter()
                        .any(|candidate| candidate == protocol)
            })
        {
            return false;
        }

        true
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct PrimitiveBacklogItem {
    pub primitive: PrimitiveFamily,
    pub highest_priority: u8,
    pub entry_count: usize,
    pub integration_ids: Vec<IntegrationId>,
}

impl PrimitiveBacklogItem {
    pub fn includes_integration(&self, integration_id: &IntegrationId) -> bool {
        self.integration_ids
            .iter()
            .any(|candidate| candidate == integration_id)
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct PrimitiveBacklogCoverageItem {
    pub primitive: PrimitiveFamily,
    pub highest_priority: u8,
    pub entry_count: usize,
    pub integration_ids: Vec<IntegrationId>,
    pub source_count: usize,
    pub platforms: Vec<EcosystemSurveyPlatform>,
}

impl PrimitiveBacklogCoverageItem {
    pub fn platform_count(&self) -> usize {
        self.platforms.len()
    }

    pub fn covers_platform(&self, platform: EcosystemSurveyPlatform) -> bool {
        self.platforms.contains(&platform)
    }

    pub fn includes_integration(&self, integration_id: &IntegrationId) -> bool {
        self.integration_ids
            .iter()
            .any(|candidate| candidate == integration_id)
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct PrimitiveBacklogCoverageSummary {
    pub total_primitives: usize,
    pub total_entries: usize,
    pub unique_integrations: usize,
    pub covered_primitives: usize,
    pub uncovered_primitives: usize,
    pub single_source_primitives: usize,
    pub multi_platform_primitives: usize,
    pub total_source_references: usize,
    pub total_platform_references: usize,
    pub first_uncovered_priority: Option<u8>,
    pub first_single_source_priority: Option<u8>,
    pub broadest_platform_count: usize,
}

impl PrimitiveBacklogCoverageSummary {
    pub fn from_items<'a>(
        items: impl IntoIterator<Item = &'a PrimitiveBacklogCoverageItem>,
    ) -> Self {
        let mut summary = Self {
            total_primitives: 0,
            total_entries: 0,
            unique_integrations: 0,
            covered_primitives: 0,
            uncovered_primitives: 0,
            single_source_primitives: 0,
            multi_platform_primitives: 0,
            total_source_references: 0,
            total_platform_references: 0,
            first_uncovered_priority: None,
            first_single_source_priority: None,
            broadest_platform_count: 0,
        };
        let mut integration_ids = BTreeSet::new();

        for item in items {
            summary.total_primitives += 1;
            summary.total_entries += item.entry_count;
            summary.total_source_references += item.source_count;
            summary.total_platform_references += item.platform_count();
            summary.broadest_platform_count =
                summary.broadest_platform_count.max(item.platform_count());

            if item.source_count == 0 {
                summary.uncovered_primitives += 1;
                summary.first_uncovered_priority = Some(
                    summary
                        .first_uncovered_priority
                        .map_or(item.highest_priority, |priority| {
                            priority.min(item.highest_priority)
                        }),
                );
            } else {
                summary.covered_primitives += 1;
            }

            if item.source_count == 1 {
                summary.single_source_primitives += 1;
                summary.first_single_source_priority = Some(
                    summary
                        .first_single_source_priority
                        .map_or(item.highest_priority, |priority| {
                            priority.min(item.highest_priority)
                        }),
                );
            }
            if item.platform_count() >= 2 {
                summary.multi_platform_primitives += 1;
            }

            for integration_id in &item.integration_ids {
                integration_ids.insert(integration_id.clone());
            }
        }

        summary.unique_integrations = integration_ids.len();
        summary
    }

    pub fn has_uncovered_primitives(&self) -> bool {
        self.uncovered_primitives > 0
    }

    pub fn has_single_source_primitives(&self) -> bool {
        self.single_source_primitives > 0
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct EcosystemPlatformCoverageItem {
    pub platform: EcosystemSurveyPlatform,
    pub display_name: &'static str,
    pub source_url: &'static str,
    pub source_surface: &'static str,
    pub contributes: &'static str,
    pub primitive_hints: Vec<PrimitiveFamily>,
    pub backlog_primitives: Vec<PrimitiveFamily>,
    pub covered_backlog_primitives: Vec<PrimitiveFamily>,
    pub uncovered_backlog_primitives: Vec<PrimitiveFamily>,
    pub highest_backlog_priority: Option<u8>,
    pub backlog_entry_count: usize,
}

impl EcosystemPlatformCoverageItem {
    pub fn primitive_hint_count(&self) -> usize {
        self.primitive_hints.len()
    }

    pub fn backlog_primitive_count(&self) -> usize {
        self.backlog_primitives.len()
    }

    pub fn covered_backlog_primitive_count(&self) -> usize {
        self.covered_backlog_primitives.len()
    }

    pub fn uncovered_backlog_primitive_count(&self) -> usize {
        self.uncovered_backlog_primitives.len()
    }

    pub fn has_backlog_overlap(&self) -> bool {
        !self.covered_backlog_primitives.is_empty()
    }

    pub fn covers_primitive(&self, primitive: PrimitiveFamily) -> bool {
        self.primitive_hints.contains(&primitive)
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct EcosystemPlatformCoverageSummary {
    pub total_platforms: usize,
    pub total_primitive_hints: usize,
    pub unique_primitive_hints: usize,
    pub backlog_primitive_count: usize,
    pub covered_backlog_primitives: usize,
    pub uncovered_backlog_primitives: usize,
    pub platforms_with_backlog_overlap: usize,
    pub platforms_covering_all_backlog_primitives: usize,
    pub first_covered_backlog_priority: Option<u8>,
}

impl EcosystemPlatformCoverageSummary {
    pub fn from_items<'a>(
        items: impl IntoIterator<Item = &'a EcosystemPlatformCoverageItem>,
    ) -> Self {
        let mut summary = Self {
            total_platforms: 0,
            total_primitive_hints: 0,
            unique_primitive_hints: 0,
            backlog_primitive_count: 0,
            covered_backlog_primitives: 0,
            uncovered_backlog_primitives: 0,
            platforms_with_backlog_overlap: 0,
            platforms_covering_all_backlog_primitives: 0,
            first_covered_backlog_priority: None,
        };
        let mut primitive_hints = BTreeSet::new();
        let mut backlog_primitives = BTreeSet::new();
        let mut covered_backlog_primitives = BTreeSet::new();

        for item in items {
            summary.total_platforms += 1;
            summary.total_primitive_hints += item.primitive_hint_count();
            if item.has_backlog_overlap() {
                summary.platforms_with_backlog_overlap += 1;
            }
            if item.backlog_primitive_count() > 0 && item.uncovered_backlog_primitive_count() == 0 {
                summary.platforms_covering_all_backlog_primitives += 1;
            }
            summary.first_covered_backlog_priority = match (
                summary.first_covered_backlog_priority,
                item.highest_backlog_priority,
            ) {
                (Some(left), Some(right)) => Some(left.min(right)),
                (None, Some(priority)) => Some(priority),
                (priority, None) => priority,
            };

            for primitive in &item.primitive_hints {
                primitive_hints.insert(*primitive);
            }
            for primitive in &item.backlog_primitives {
                backlog_primitives.insert(*primitive);
            }
            for primitive in &item.covered_backlog_primitives {
                covered_backlog_primitives.insert(*primitive);
            }
        }

        summary.unique_primitive_hints = primitive_hints.len();
        summary.backlog_primitive_count = backlog_primitives.len();
        summary.covered_backlog_primitives = covered_backlog_primitives.len();
        summary.uncovered_backlog_primitives = backlog_primitives
            .difference(&covered_backlog_primitives)
            .count();
        summary
    }

    pub fn has_uncovered_backlog_primitives(&self) -> bool {
        self.uncovered_backlog_primitives > 0
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationPolicySurfaceInventoryItem {
    pub surface: IntegrationPolicySurface,
    pub required_tier: PrivilegeTier,
    pub highest_priority: u8,
    pub entry_count: usize,
    pub local_entry_count: usize,
    pub cloud_entry_count: usize,
    pub human_review_entry_count: usize,
    pub integration_ids: Vec<IntegrationId>,
}

impl IntegrationPolicySurfaceInventoryItem {
    pub fn includes_integration(&self, integration_id: &IntegrationId) -> bool {
        self.integration_ids
            .iter()
            .any(|candidate| candidate == integration_id)
    }

    pub fn requires_human_review(&self) -> bool {
        self.required_tier >= PrivilegeTier::HumanApproval
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationPolicySurfaceSummary {
    pub total_surfaces: usize,
    pub total_surface_entries: usize,
    pub unique_integrations: usize,
    pub local_surface_entries: usize,
    pub cloud_surface_entries: usize,
    pub human_review_surface_entries: usize,
    pub read_only_surfaces: usize,
    pub low_risk_surfaces: usize,
    pub human_approval_surfaces: usize,
    pub high_risk_surfaces: usize,
    pub first_review_priority: Option<u8>,
    pub highest_policy_tier: PrivilegeTier,
}

impl IntegrationPolicySurfaceSummary {
    pub fn from_inventory<'a>(
        items: impl IntoIterator<Item = &'a IntegrationPolicySurfaceInventoryItem>,
    ) -> Self {
        let mut summary = Self {
            total_surfaces: 0,
            total_surface_entries: 0,
            unique_integrations: 0,
            local_surface_entries: 0,
            cloud_surface_entries: 0,
            human_review_surface_entries: 0,
            read_only_surfaces: 0,
            low_risk_surfaces: 0,
            human_approval_surfaces: 0,
            high_risk_surfaces: 0,
            first_review_priority: None,
            highest_policy_tier: PrivilegeTier::ReadOnly,
        };
        let mut integration_ids = BTreeSet::new();

        for item in items {
            summary.total_surfaces += 1;
            summary.total_surface_entries += item.entry_count;
            summary.local_surface_entries += item.local_entry_count;
            summary.cloud_surface_entries += item.cloud_entry_count;
            summary.human_review_surface_entries += item.human_review_entry_count;
            match item.required_tier {
                PrivilegeTier::ReadOnly => summary.read_only_surfaces += 1,
                PrivilegeTier::LowRisk => summary.low_risk_surfaces += 1,
                PrivilegeTier::HumanApproval => summary.human_approval_surfaces += 1,
                PrivilegeTier::HighRisk => summary.high_risk_surfaces += 1,
            }
            if item.requires_human_review() {
                summary.first_review_priority = Some(
                    summary
                        .first_review_priority
                        .map_or(item.highest_priority, |priority| {
                            priority.min(item.highest_priority)
                        }),
                );
            }
            summary.highest_policy_tier = summary.highest_policy_tier.max(item.required_tier);
            for integration_id in &item.integration_ids {
                integration_ids.insert(integration_id.clone());
            }
        }

        summary.unique_integrations = integration_ids.len();
        summary
    }

    pub fn is_empty(&self) -> bool {
        self.total_surfaces == 0
    }

    pub fn has_review_work(&self) -> bool {
        self.human_approval_surfaces > 0
            || self.high_risk_surfaces > 0
            || self.human_review_surface_entries > 0
    }

    pub fn has_high_risk_surface(&self) -> bool {
        self.high_risk_surfaces > 0
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub enum IntegrationActivationTarget {
    Direct,
    DelegatedIntegration(IntegrationId),
    DelegatedStandards(Vec<ProtocolFamily>),
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationPlan {
    pub requested_integration_id: IntegrationId,
    pub display_name: String,
    pub activation_target: IntegrationActivationTarget,
    pub implementation_status: ImplementationStatus,
    pub priority: u8,
    pub runtime_kind: RuntimeKind,
    pub required_primitives: Vec<PrimitiveFamily>,
    pub required_capabilities: Vec<CapabilityId>,
    pub auth_modes: Vec<AuthMode>,
    pub discovery_mechanisms: Vec<DiscoveryMechanism>,
    pub depends_on_integrations: Vec<IntegrationId>,
    pub policy_surfaces: Vec<IntegrationPolicySurface>,
    pub highest_policy_tier: PrivilegeTier,
    pub local_only: bool,
    pub cloud_required: bool,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationPlanSummary {
    pub total_plans: usize,
    pub direct_targets: usize,
    pub delegated_integration_targets: usize,
    pub delegated_standard_targets: usize,
    pub plans_requiring_human_review: usize,
    pub local_only_plans: usize,
    pub cloud_required_plans: usize,
    pub plans_with_dependencies: usize,
    pub plans_with_required_primitives: usize,
    pub plans_with_required_capabilities: usize,
    pub unique_required_primitives: usize,
    pub unique_required_capabilities: usize,
    pub unique_dependencies: usize,
    pub highest_policy_tier: PrivilegeTier,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationReadinessReport {
    pub requested_integration_id: IntegrationId,
    pub display_name: String,
    pub activation_target: IntegrationActivationTarget,
    pub priority: u8,
    pub missing_primitives: Vec<PrimitiveFamily>,
    pub missing_capabilities: Vec<CapabilityId>,
    pub missing_dependencies: Vec<IntegrationId>,
    pub requires_human_review: bool,
    pub highest_policy_tier: PrivilegeTier,
    pub local_only: bool,
    pub cloud_required: bool,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationActivationCandidateRecommendation {
    ReadyToActivate,
    NeedsHumanReview,
    BlockedOnPrerequisites,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationActivationHealthStatus {
    Ready,
    NeedsReview,
    Blocked,
    Empty,
}

impl IntegrationActivationHealthStatus {
    pub fn as_str(self) -> &'static str {
        match self {
            Self::Ready => "ready",
            Self::NeedsReview => "needs_review",
            Self::Blocked => "blocked",
            Self::Empty => "empty",
        }
    }

    pub fn requires_attention(self) -> bool {
        matches!(self, Self::NeedsReview | Self::Blocked)
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationActivationBriefingItemKind {
    Blocker,
    Review,
    Approval,
    Activation,
    Risk,
    Dependency,
}

impl IntegrationActivationBriefingItemKind {
    pub fn as_str(self) -> &'static str {
        match self {
            Self::Blocker => "blocker",
            Self::Review => "review",
            Self::Approval => "approval",
            Self::Activation => "activation",
            Self::Risk => "risk",
            Self::Dependency => "dependency",
        }
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationActivationForecastAction {
    ResolveBlockers,
    EnableDependencies,
    PrepareApproval,
    QueueReview,
    ReviewRisk,
    ActivateWave,
    MonitorWave,
}

impl IntegrationActivationForecastAction {
    pub fn as_str(self) -> &'static str {
        match self {
            Self::ResolveBlockers => "resolve_blockers",
            Self::EnableDependencies => "enable_dependencies",
            Self::PrepareApproval => "prepare_approval",
            Self::QueueReview => "queue_review",
            Self::ReviewRisk => "review_risk",
            Self::ActivateWave => "activate_wave",
            Self::MonitorWave => "monitor_wave",
        }
    }

    pub fn requires_attention(self) -> bool {
        !matches!(self, Self::ActivateWave | Self::MonitorWave)
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationActivationPlaybookView {
    ConstraintQueue,
    DependencyGraph,
    ApprovalPackets,
    ReviewQueue,
    RiskRegister,
    ActivationActions,
    StatusDashboard,
}

impl IntegrationActivationPlaybookView {
    pub fn from_forecast_action(action: IntegrationActivationForecastAction) -> Self {
        match action {
            IntegrationActivationForecastAction::ResolveBlockers => Self::ConstraintQueue,
            IntegrationActivationForecastAction::EnableDependencies => Self::DependencyGraph,
            IntegrationActivationForecastAction::PrepareApproval => Self::ApprovalPackets,
            IntegrationActivationForecastAction::QueueReview => Self::ReviewQueue,
            IntegrationActivationForecastAction::ReviewRisk => Self::RiskRegister,
            IntegrationActivationForecastAction::ActivateWave => Self::ActivationActions,
            IntegrationActivationForecastAction::MonitorWave => Self::StatusDashboard,
        }
    }

    pub fn as_str(self) -> &'static str {
        match self {
            Self::ConstraintQueue => "constraints",
            Self::DependencyGraph => "dependencies",
            Self::ApprovalPackets => "approvals",
            Self::ReviewQueue => "reviews",
            Self::RiskRegister => "risk",
            Self::ActivationActions => "actions",
            Self::StatusDashboard => "dashboard",
        }
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationActivationRunbookPhase {
    ClearBlockers,
    ResolveDependencies,
    PrepareApprovals,
    CompleteReviews,
    ReviewRisks,
    Activate,
    Monitor,
}

impl IntegrationActivationRunbookPhase {
    pub fn from_playbook_action(action: IntegrationActivationForecastAction) -> Self {
        match action {
            IntegrationActivationForecastAction::ResolveBlockers => Self::ClearBlockers,
            IntegrationActivationForecastAction::EnableDependencies => Self::ResolveDependencies,
            IntegrationActivationForecastAction::PrepareApproval => Self::PrepareApprovals,
            IntegrationActivationForecastAction::QueueReview => Self::CompleteReviews,
            IntegrationActivationForecastAction::ReviewRisk => Self::ReviewRisks,
            IntegrationActivationForecastAction::ActivateWave => Self::Activate,
            IntegrationActivationForecastAction::MonitorWave => Self::Monitor,
        }
    }

    pub fn as_str(self) -> &'static str {
        match self {
            Self::ClearBlockers => "clear_blockers",
            Self::ResolveDependencies => "resolve_dependencies",
            Self::PrepareApprovals => "prepare_approvals",
            Self::CompleteReviews => "complete_reviews",
            Self::ReviewRisks => "review_risks",
            Self::Activate => "activate",
            Self::Monitor => "monitor",
        }
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationActivationHandoffStatus {
    Ready,
    NeedsReview,
    Blocked,
    Monitoring,
}

impl IntegrationActivationHandoffStatus {
    pub fn as_str(self) -> &'static str {
        match self {
            Self::Ready => "ready",
            Self::NeedsReview => "needs_review",
            Self::Blocked => "blocked",
            Self::Monitoring => "monitoring",
        }
    }

    pub fn requires_attention(self) -> bool {
        matches!(self, Self::NeedsReview | Self::Blocked)
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationActivationExecutionStatus {
    Executable,
    OperatorRequired,
    NeedsApproval,
    Blocked,
    Monitoring,
}

impl IntegrationActivationExecutionStatus {
    pub fn as_str(self) -> &'static str {
        match self {
            Self::Executable => "executable",
            Self::OperatorRequired => "operator_required",
            Self::NeedsApproval => "needs_approval",
            Self::Blocked => "blocked",
            Self::Monitoring => "monitoring",
        }
    }

    pub fn requires_attention(self) -> bool {
        matches!(
            self,
            Self::OperatorRequired | Self::NeedsApproval | Self::Blocked
        )
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationActivationVerificationStatus {
    ReadyToVerify,
    PendingOperator,
    PendingApproval,
    Blocked,
    Monitoring,
}

impl IntegrationActivationVerificationStatus {
    pub fn as_str(self) -> &'static str {
        match self {
            Self::ReadyToVerify => "ready_to_verify",
            Self::PendingOperator => "pending_operator",
            Self::PendingApproval => "pending_approval",
            Self::Blocked => "blocked",
            Self::Monitoring => "monitoring",
        }
    }

    pub fn requires_attention(self) -> bool {
        matches!(
            self,
            Self::PendingOperator | Self::PendingApproval | Self::Blocked
        )
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationActivationOperatorTaskKind {
    ResolveConstraints,
    EnableDependencies,
    PrepareApproval,
    CompleteReview,
    ReviewRisk,
    ActivateWave,
    MonitorWave,
}

impl IntegrationActivationOperatorTaskKind {
    pub fn from_playbook_action(action: IntegrationActivationForecastAction) -> Self {
        match action {
            IntegrationActivationForecastAction::ResolveBlockers => Self::ResolveConstraints,
            IntegrationActivationForecastAction::EnableDependencies => Self::EnableDependencies,
            IntegrationActivationForecastAction::PrepareApproval => Self::PrepareApproval,
            IntegrationActivationForecastAction::QueueReview => Self::CompleteReview,
            IntegrationActivationForecastAction::ReviewRisk => Self::ReviewRisk,
            IntegrationActivationForecastAction::ActivateWave => Self::ActivateWave,
            IntegrationActivationForecastAction::MonitorWave => Self::MonitorWave,
        }
    }

    pub fn as_str(self) -> &'static str {
        match self {
            Self::ResolveConstraints => "resolve_constraints",
            Self::EnableDependencies => "enable_dependencies",
            Self::PrepareApproval => "prepare_approval",
            Self::CompleteReview => "complete_review",
            Self::ReviewRisk => "review_risk",
            Self::ActivateWave => "activate_wave",
            Self::MonitorWave => "monitor_wave",
        }
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationActivationCommandCenterSectionKind {
    Blockers,
    Review,
    Activation,
    Actionable,
    Monitoring,
}

impl IntegrationActivationCommandCenterSectionKind {
    pub fn from_panel(panel: &IntegrationActivationControlRoomPanel) -> Self {
        if panel.has_blockers() {
            Self::Blockers
        } else if panel.has_activation_work() {
            Self::Activation
        } else if panel.has_review_work() {
            Self::Review
        } else if panel.has_actionable_work() {
            Self::Actionable
        } else {
            Self::Monitoring
        }
    }

    pub fn as_str(self) -> &'static str {
        match self {
            Self::Blockers => "blockers",
            Self::Review => "review",
            Self::Activation => "activation",
            Self::Actionable => "actionable",
            Self::Monitoring => "monitoring",
        }
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationActivationWatchtowerSignalKind {
    Escalation,
    Review,
    Ready,
    Action,
    Observation,
}

impl IntegrationActivationWatchtowerSignalKind {
    pub fn from_section(section: &IntegrationActivationCommandCenterSection) -> Self {
        if section.has_blockers() {
            Self::Escalation
        } else if section.has_activation_work() {
            Self::Ready
        } else if section.has_review_work() {
            Self::Review
        } else if section.has_actionable_work() {
            Self::Action
        } else {
            Self::Observation
        }
    }

    pub fn as_str(self) -> &'static str {
        match self {
            Self::Escalation => "escalation",
            Self::Review => "review",
            Self::Ready => "ready",
            Self::Action => "action",
            Self::Observation => "observation",
        }
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationActivationSentinelAlertKind {
    Blocker,
    Dependency,
    PolicyRisk,
    Review,
    Ready,
    Observation,
}

impl IntegrationActivationSentinelAlertKind {
    pub fn as_str(self) -> &'static str {
        match self {
            Self::Blocker => "blocker",
            Self::Dependency => "dependency",
            Self::PolicyRisk => "policy_risk",
            Self::Review => "review",
            Self::Ready => "ready",
            Self::Observation => "observation",
        }
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationActivationEscalationCaseKind {
    Blocker,
    Dependency,
    PolicyRisk,
    Review,
    Verification,
    Audit,
}

impl IntegrationActivationEscalationCaseKind {
    pub fn as_str(self) -> &'static str {
        match self {
            Self::Blocker => "blocker",
            Self::Dependency => "dependency",
            Self::PolicyRisk => "policy_risk",
            Self::Review => "review",
            Self::Verification => "verification",
            Self::Audit => "audit",
        }
    }

    pub fn recommended_view(self) -> IntegrationActivationPlaybookView {
        match self {
            Self::Blocker => IntegrationActivationPlaybookView::ConstraintQueue,
            Self::Dependency => IntegrationActivationPlaybookView::DependencyGraph,
            Self::PolicyRisk => IntegrationActivationPlaybookView::RiskRegister,
            Self::Review => IntegrationActivationPlaybookView::ReviewQueue,
            Self::Verification | Self::Audit => IntegrationActivationPlaybookView::StatusDashboard,
        }
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationActivationResponseKind {
    ResolveBlocker,
    EnableDependency,
    ReviewPolicy,
    QueueReview,
    VerifyActivation,
    AuditFollowUp,
}

impl IntegrationActivationResponseKind {
    pub fn as_str(self) -> &'static str {
        match self {
            Self::ResolveBlocker => "resolve_blocker",
            Self::EnableDependency => "enable_dependency",
            Self::ReviewPolicy => "review_policy",
            Self::QueueReview => "queue_review",
            Self::VerifyActivation => "verify_activation",
            Self::AuditFollowUp => "audit_follow_up",
        }
    }

    pub fn from_case_kind(case_kind: IntegrationActivationEscalationCaseKind) -> Self {
        match case_kind {
            IntegrationActivationEscalationCaseKind::Blocker => Self::ResolveBlocker,
            IntegrationActivationEscalationCaseKind::Dependency => Self::EnableDependency,
            IntegrationActivationEscalationCaseKind::PolicyRisk => Self::ReviewPolicy,
            IntegrationActivationEscalationCaseKind::Review => Self::QueueReview,
            IntegrationActivationEscalationCaseKind::Verification => Self::VerifyActivation,
            IntegrationActivationEscalationCaseKind::Audit => Self::AuditFollowUp,
        }
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationActivationResponseOwnerLane {
    Platform,
    Integration,
    Security,
    Reviewer,
    Verification,
    Audit,
}

impl IntegrationActivationResponseOwnerLane {
    pub fn as_str(self) -> &'static str {
        match self {
            Self::Platform => "platform",
            Self::Integration => "integration",
            Self::Security => "security",
            Self::Reviewer => "reviewer",
            Self::Verification => "verification",
            Self::Audit => "audit",
        }
    }

    pub fn from_response_kind(response_kind: IntegrationActivationResponseKind) -> Self {
        match response_kind {
            IntegrationActivationResponseKind::ResolveBlocker => Self::Platform,
            IntegrationActivationResponseKind::EnableDependency => Self::Integration,
            IntegrationActivationResponseKind::ReviewPolicy => Self::Security,
            IntegrationActivationResponseKind::QueueReview => Self::Reviewer,
            IntegrationActivationResponseKind::VerifyActivation => Self::Verification,
            IntegrationActivationResponseKind::AuditFollowUp => Self::Audit,
        }
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationActivationRemediationKind {
    UnblockPlatform,
    EnableDependency,
    ReviewPolicy,
    CompleteReview,
    RunVerification,
    RecordAudit,
}

impl IntegrationActivationRemediationKind {
    pub fn as_str(self) -> &'static str {
        match self {
            Self::UnblockPlatform => "unblock_platform",
            Self::EnableDependency => "enable_dependency",
            Self::ReviewPolicy => "review_policy",
            Self::CompleteReview => "complete_review",
            Self::RunVerification => "run_verification",
            Self::RecordAudit => "record_audit",
        }
    }

    pub fn from_response_kind(response_kind: IntegrationActivationResponseKind) -> Self {
        match response_kind {
            IntegrationActivationResponseKind::ResolveBlocker => Self::UnblockPlatform,
            IntegrationActivationResponseKind::EnableDependency => Self::EnableDependency,
            IntegrationActivationResponseKind::ReviewPolicy => Self::ReviewPolicy,
            IntegrationActivationResponseKind::QueueReview => Self::CompleteReview,
            IntegrationActivationResponseKind::VerifyActivation => Self::RunVerification,
            IntegrationActivationResponseKind::AuditFollowUp => Self::RecordAudit,
        }
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationActivationRemediationStatus {
    Blocked,
    NeedsOwnerAction,
    ReadyToExecute,
    Tracking,
}

impl IntegrationActivationRemediationStatus {
    pub fn as_str(self) -> &'static str {
        match self {
            Self::Blocked => "blocked",
            Self::NeedsOwnerAction => "needs_owner_action",
            Self::ReadyToExecute => "ready_to_execute",
            Self::Tracking => "tracking",
        }
    }

    pub fn from_response(response: &IntegrationActivationResponseItem) -> Self {
        if response.blocked() {
            Self::Blocked
        } else if response.ready_to_verify()
            || response.response_kind == IntegrationActivationResponseKind::VerifyActivation
        {
            Self::ReadyToExecute
        } else if response.requires_attention() {
            Self::NeedsOwnerAction
        } else {
            Self::Tracking
        }
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationActivationClosureStatus {
    Blocked,
    OwnerActionRequired,
    ReadyForVerification,
    ReadyForClosure,
    Tracking,
}

impl IntegrationActivationClosureStatus {
    pub fn as_str(self) -> &'static str {
        match self {
            Self::Blocked => "blocked",
            Self::OwnerActionRequired => "owner_action_required",
            Self::ReadyForVerification => "ready_for_verification",
            Self::ReadyForClosure => "ready_for_closure",
            Self::Tracking => "tracking",
        }
    }

    pub fn from_remediation(remediation: &IntegrationActivationRemediationItem) -> Self {
        if remediation.blocked() {
            Self::Blocked
        } else if remediation.ready_to_execute() || remediation.ready_to_verify() {
            Self::ReadyForVerification
        } else if remediation.requires_attention() {
            Self::OwnerActionRequired
        } else if remediation.status == IntegrationActivationRemediationStatus::Tracking {
            Self::ReadyForClosure
        } else {
            Self::Tracking
        }
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationActivationReleaseStatus {
    Blocked,
    OwnerActionRequired,
    VerificationRequired,
    ReadyForRelease,
    Monitoring,
}

impl IntegrationActivationReleaseStatus {
    pub fn as_str(self) -> &'static str {
        match self {
            Self::Blocked => "blocked",
            Self::OwnerActionRequired => "owner_action_required",
            Self::VerificationRequired => "verification_required",
            Self::ReadyForRelease => "ready_for_release",
            Self::Monitoring => "monitoring",
        }
    }

    pub fn from_closure_gate(gate: &IntegrationActivationClosureGate) -> Self {
        if gate.blocked() {
            Self::Blocked
        } else if gate.ready_to_verify()
            || gate.closure_status == IntegrationActivationClosureStatus::ReadyForVerification
        {
            Self::VerificationRequired
        } else if gate.closure_status == IntegrationActivationClosureStatus::OwnerActionRequired
            || gate.requires_attention()
        {
            Self::OwnerActionRequired
        } else if gate.closure_ready()
            || gate.closure_status == IntegrationActivationClosureStatus::ReadyForClosure
        {
            Self::ReadyForRelease
        } else {
            Self::Monitoring
        }
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationActivationDeliveryStatus {
    Blocked,
    AwaitingVerification,
    AwaitingOwner,
    ReadyToDeliver,
    Monitoring,
}

impl IntegrationActivationDeliveryStatus {
    pub fn as_str(self) -> &'static str {
        match self {
            Self::Blocked => "blocked",
            Self::AwaitingVerification => "awaiting_verification",
            Self::AwaitingOwner => "awaiting_owner",
            Self::ReadyToDeliver => "ready_to_deliver",
            Self::Monitoring => "monitoring",
        }
    }

    pub fn from_release_packet(packet: &IntegrationActivationReleasePacket) -> Self {
        if packet.release_blocked() {
            if packet.needs_verification() {
                Self::AwaitingVerification
            } else if packet.release_status
                == IntegrationActivationReleaseStatus::OwnerActionRequired
            {
                Self::AwaitingOwner
            } else {
                Self::Blocked
            }
        } else if packet.release_ready() {
            Self::ReadyToDeliver
        } else {
            Self::Monitoring
        }
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationActivationDeliveryChannel {
    Platform,
    Integration,
    Security,
    Verification,
    Audit,
    Monitoring,
}

impl IntegrationActivationDeliveryChannel {
    pub fn as_str(self) -> &'static str {
        match self {
            Self::Platform => "platform",
            Self::Integration => "integration",
            Self::Security => "security",
            Self::Verification => "verification",
            Self::Audit => "audit",
            Self::Monitoring => "monitoring",
        }
    }

    pub fn from_release_packet(packet: &IntegrationActivationReleasePacket) -> Self {
        if packet.release_status == IntegrationActivationReleaseStatus::Monitoring {
            return Self::Monitoring;
        }
        match packet.owner_lane {
            IntegrationActivationResponseOwnerLane::Platform => Self::Platform,
            IntegrationActivationResponseOwnerLane::Integration => Self::Integration,
            IntegrationActivationResponseOwnerLane::Security => Self::Security,
            IntegrationActivationResponseOwnerLane::Reviewer
            | IntegrationActivationResponseOwnerLane::Verification => Self::Verification,
            IntegrationActivationResponseOwnerLane::Audit => Self::Audit,
        }
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationActivationDeploymentStatus {
    Blocked,
    AwaitingVerification,
    AwaitingOwner,
    ReadyToDeploy,
    Monitoring,
}

impl IntegrationActivationDeploymentStatus {
    pub fn as_str(self) -> &'static str {
        match self {
            Self::Blocked => "blocked",
            Self::AwaitingVerification => "awaiting_verification",
            Self::AwaitingOwner => "awaiting_owner",
            Self::ReadyToDeploy => "ready_to_deploy",
            Self::Monitoring => "monitoring",
        }
    }

    pub fn from_delivery_manifest(manifest: &IntegrationActivationDeliveryManifest) -> Self {
        if manifest.delivery_blocked() {
            if manifest.needs_verification() {
                Self::AwaitingVerification
            } else if manifest.delivery_status == IntegrationActivationDeliveryStatus::AwaitingOwner
            {
                Self::AwaitingOwner
            } else {
                Self::Blocked
            }
        } else if manifest.delivery_ready() {
            Self::ReadyToDeploy
        } else {
            Self::Monitoring
        }
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationActivationDeploymentRing {
    Platform,
    Integration,
    Security,
    Verification,
    Audit,
    Monitoring,
}

impl IntegrationActivationDeploymentRing {
    pub fn as_str(self) -> &'static str {
        match self {
            Self::Platform => "platform",
            Self::Integration => "integration",
            Self::Security => "security",
            Self::Verification => "verification",
            Self::Audit => "audit",
            Self::Monitoring => "monitoring",
        }
    }

    pub fn from_delivery_manifest(manifest: &IntegrationActivationDeliveryManifest) -> Self {
        if manifest.delivery_status == IntegrationActivationDeliveryStatus::Monitoring {
            return Self::Monitoring;
        }
        match manifest.delivery_channel {
            IntegrationActivationDeliveryChannel::Platform => Self::Platform,
            IntegrationActivationDeliveryChannel::Integration => Self::Integration,
            IntegrationActivationDeliveryChannel::Security => Self::Security,
            IntegrationActivationDeliveryChannel::Verification => Self::Verification,
            IntegrationActivationDeliveryChannel::Audit => Self::Audit,
            IntegrationActivationDeliveryChannel::Monitoring => Self::Monitoring,
        }
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationActivationAuditRecordKind {
    Sentinel,
    Watchtower,
    Decision,
    Evidence,
    Risk,
    Dependency,
    ReadinessGap,
}

impl IntegrationActivationAuditRecordKind {
    pub fn as_str(self) -> &'static str {
        match self {
            Self::Sentinel => "sentinel",
            Self::Watchtower => "watchtower",
            Self::Decision => "decision",
            Self::Evidence => "evidence",
            Self::Risk => "risk",
            Self::Dependency => "dependency",
            Self::ReadinessGap => "readiness_gap",
        }
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationActivationDecisionStatus {
    ReadyToApprove,
    BlockedOnPrerequisites,
}

impl IntegrationActivationDecisionStatus {
    pub fn as_str(self) -> &'static str {
        match self {
            Self::ReadyToApprove => "ready_to_approve",
            Self::BlockedOnPrerequisites => "blocked_on_prerequisites",
        }
    }

    pub fn requires_attention(self) -> bool {
        true
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationActivationEvidenceKind {
    ApprovalDecision,
    PolicyReview,
    PrimitiveBlocker,
    CapabilityBlocker,
    DependencyBlocker,
    PolicyRisk,
    DependencyEdge,
}

impl IntegrationActivationEvidenceKind {
    pub fn as_str(self) -> &'static str {
        match self {
            Self::ApprovalDecision => "approval_decision",
            Self::PolicyReview => "policy_review",
            Self::PrimitiveBlocker => "primitive_blocker",
            Self::CapabilityBlocker => "capability_blocker",
            Self::DependencyBlocker => "dependency_blocker",
            Self::PolicyRisk => "policy_risk",
            Self::DependencyEdge => "dependency_edge",
        }
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationActivationEvidenceStatus {
    SupportsApproval,
    RequiresReview,
    BlocksApproval,
}

impl IntegrationActivationEvidenceStatus {
    pub fn as_str(self) -> &'static str {
        match self {
            Self::SupportsApproval => "supports_approval",
            Self::RequiresReview => "requires_review",
            Self::BlocksApproval => "blocks_approval",
        }
    }

    pub fn requires_attention(self) -> bool {
        !matches!(self, Self::SupportsApproval)
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationActivationActionKind {
    ActivateIntegration,
    ReviewPolicy,
    ProvidePrimitive,
    GrantCapability,
    EnableDependency,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationActivationConstraintKind {
    Primitive,
    Capability,
    Dependency,
    PolicyReview,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationActivationRiskKind {
    PolicyTier,
    PolicySurface,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationCandidate {
    pub readiness_report: IntegrationReadinessReport,
    pub recommendation: IntegrationActivationCandidateRecommendation,
    pub blocker_count: usize,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationCandidateSummary {
    pub total_candidates: usize,
    pub ready_to_activate_candidates: usize,
    pub needs_human_review_candidates: usize,
    pub blocked_candidates: usize,
    pub activation_ready_candidates: usize,
    pub candidates_requiring_human_review: usize,
    pub candidates_missing_primitives: usize,
    pub candidates_missing_capabilities: usize,
    pub candidates_missing_dependencies: usize,
    pub direct_targets: usize,
    pub delegated_integration_targets: usize,
    pub delegated_standard_targets: usize,
    pub local_only_candidates: usize,
    pub cloud_required_candidates: usize,
    pub highest_policy_tier: PrivilegeTier,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationAction {
    pub kind: IntegrationActivationActionKind,
    pub requested_integration_id: IntegrationId,
    pub display_name: String,
    pub priority: u8,
    pub recommendation: IntegrationActivationCandidateRecommendation,
    pub primitive: Option<PrimitiveFamily>,
    pub capability_id: Option<CapabilityId>,
    pub dependency_integration_id: Option<IntegrationId>,
    pub highest_policy_tier: PrivilegeTier,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationActionSummary {
    pub total_actions: usize,
    pub activate_integration_actions: usize,
    pub review_policy_actions: usize,
    pub provide_primitive_actions: usize,
    pub grant_capability_actions: usize,
    pub enable_dependency_actions: usize,
    pub actionable_integration_count: usize,
    pub blocked_integration_count: usize,
    pub unique_integrations: usize,
    pub first_action_priority: Option<u8>,
    pub first_activation_priority: Option<u8>,
    pub first_blocker_priority: Option<u8>,
    pub highest_policy_tier: PrivilegeTier,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationConstraint {
    pub kind: IntegrationActivationConstraintKind,
    pub constraint_id: String,
    pub display_name: String,
    pub highest_priority: u8,
    pub affected_integration_ids: Vec<IntegrationId>,
    pub blocks_activation: bool,
    pub requires_human_review: bool,
    pub highest_policy_tier: PrivilegeTier,
    pub policy_surfaces: Vec<IntegrationPolicySurface>,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationConstraintSummary {
    pub total_constraints: usize,
    pub blocking_constraints: usize,
    pub review_constraints: usize,
    pub primitive_constraints: usize,
    pub capability_constraints: usize,
    pub dependency_constraints: usize,
    pub policy_review_constraints: usize,
    pub affected_integrations: usize,
    pub first_blocking_priority: Option<u8>,
    pub first_review_priority: Option<u8>,
    pub highest_policy_tier: PrivilegeTier,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationRiskItem {
    pub kind: IntegrationActivationRiskKind,
    pub risk_id: String,
    pub display_name: String,
    pub required_tier: PrivilegeTier,
    pub policy_surface: Option<IntegrationPolicySurface>,
    pub highest_priority: u8,
    pub integration_ids: Vec<IntegrationId>,
    pub activation_ready_integration_ids: Vec<IntegrationId>,
    pub ready_to_activate_integration_ids: Vec<IntegrationId>,
    pub review_integration_ids: Vec<IntegrationId>,
    pub blocked_integration_ids: Vec<IntegrationId>,
    pub local_only_integration_ids: Vec<IntegrationId>,
    pub cloud_required_integration_ids: Vec<IntegrationId>,
    pub candidate_summary: IntegrationActivationCandidateSummary,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationRiskSummary {
    pub total_risks: usize,
    pub policy_tier_risks: usize,
    pub policy_surface_risks: usize,
    pub total_risk_entries: usize,
    pub unique_integrations: usize,
    pub activation_ready_integrations: usize,
    pub ready_to_activate_integrations: usize,
    pub review_integrations: usize,
    pub blocked_integrations: usize,
    pub local_only_integrations: usize,
    pub cloud_required_integrations: usize,
    pub read_only_risks: usize,
    pub low_risk_risks: usize,
    pub human_approval_risks: usize,
    pub high_risk_risks: usize,
    pub first_ready_priority: Option<u8>,
    pub first_review_priority: Option<u8>,
    pub first_blocked_priority: Option<u8>,
    pub highest_policy_tier: PrivilegeTier,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationReviewItem {
    pub requested_integration_id: IntegrationId,
    pub display_name: String,
    pub priority: u8,
    pub activation_target: IntegrationActivationTarget,
    pub recommendation: IntegrationActivationCandidateRecommendation,
    pub blocker_count: usize,
    pub missing_primitives: Vec<PrimitiveFamily>,
    pub missing_capabilities: Vec<CapabilityId>,
    pub missing_dependencies: Vec<IntegrationId>,
    pub policy_surfaces: Vec<IntegrationPolicySurface>,
    pub required_tier: PrivilegeTier,
    pub local_only: bool,
    pub cloud_required: bool,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationReviewSummary {
    pub total_reviews: usize,
    pub review_ready_integrations: usize,
    pub blocked_review_integrations: usize,
    pub reviews_missing_primitives: usize,
    pub reviews_missing_capabilities: usize,
    pub reviews_missing_dependencies: usize,
    pub direct_targets: usize,
    pub delegated_integration_targets: usize,
    pub delegated_standard_targets: usize,
    pub local_only_reviews: usize,
    pub cloud_required_reviews: usize,
    pub reviews_with_policy_surfaces: usize,
    pub reviews_without_policy_surfaces: usize,
    pub unique_policy_surfaces: usize,
    pub total_blockers: usize,
    pub read_only_reviews: usize,
    pub low_risk_reviews: usize,
    pub human_approval_reviews: usize,
    pub high_risk_reviews: usize,
    pub first_review_priority: Option<u8>,
    pub first_blocked_priority: Option<u8>,
    pub highest_policy_tier: PrivilegeTier,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationApprovalPacket {
    pub review: IntegrationActivationReviewItem,
    pub actions: Vec<IntegrationActivationAction>,
    pub action_summary: IntegrationActivationActionSummary,
    pub constraints: Vec<IntegrationActivationConstraint>,
    pub constraint_summary: IntegrationActivationConstraintSummary,
    pub risks: Vec<IntegrationActivationRiskItem>,
    pub risk_summary: IntegrationActivationRiskSummary,
    pub dependency_graph: IntegrationActivationDependencyGraph,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationApprovalSummary {
    pub total_packets: usize,
    pub approval_ready_packets: usize,
    pub blocked_packets: usize,
    pub local_only_packets: usize,
    pub cloud_required_packets: usize,
    pub packets_with_policy_surfaces: usize,
    pub packets_without_policy_surfaces: usize,
    pub unique_policy_surfaces: usize,
    pub total_actions: usize,
    pub activate_integration_actions: usize,
    pub review_policy_actions: usize,
    pub provide_primitive_actions: usize,
    pub grant_capability_actions: usize,
    pub enable_dependency_actions: usize,
    pub total_constraints: usize,
    pub blocking_constraints: usize,
    pub review_constraints: usize,
    pub total_risks: usize,
    pub policy_tier_risks: usize,
    pub policy_surface_risks: usize,
    pub total_dependency_edges: usize,
    pub blocking_dependency_edges: usize,
    pub read_only_packets: usize,
    pub low_risk_packets: usize,
    pub human_approval_packets: usize,
    pub high_risk_packets: usize,
    pub first_approval_priority: Option<u8>,
    pub first_blocked_priority: Option<u8>,
    pub highest_policy_tier: PrivilegeTier,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationDecisionItem {
    pub packet: IntegrationActivationApprovalPacket,
    pub decision_status: IntegrationActivationDecisionStatus,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationDecisionSummary {
    pub total_decisions: usize,
    pub ready_to_approve_decisions: usize,
    pub blocked_decisions: usize,
    pub local_only_decisions: usize,
    pub cloud_required_decisions: usize,
    pub decisions_with_policy_surfaces: usize,
    pub decisions_without_policy_surfaces: usize,
    pub unique_policy_surfaces: usize,
    pub total_actions: usize,
    pub activate_integration_actions: usize,
    pub review_policy_actions: usize,
    pub provide_primitive_actions: usize,
    pub grant_capability_actions: usize,
    pub enable_dependency_actions: usize,
    pub total_constraints: usize,
    pub blocking_constraints: usize,
    pub review_constraints: usize,
    pub total_risks: usize,
    pub policy_tier_risks: usize,
    pub policy_surface_risks: usize,
    pub total_dependency_edges: usize,
    pub blocking_dependency_edges: usize,
    pub read_only_decisions: usize,
    pub low_risk_decisions: usize,
    pub human_approval_decisions: usize,
    pub high_risk_decisions: usize,
    pub first_approval_priority: Option<u8>,
    pub first_blocked_priority: Option<u8>,
    pub highest_policy_tier: PrivilegeTier,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationEvidenceItem {
    pub kind: IntegrationActivationEvidenceKind,
    pub status: IntegrationActivationEvidenceStatus,
    pub decision_status: IntegrationActivationDecisionStatus,
    pub requested_integration_id: IntegrationId,
    pub display_name: String,
    pub priority: u8,
    pub detail_id: String,
    pub primitive: Option<PrimitiveFamily>,
    pub capability_id: Option<CapabilityId>,
    pub dependency_integration_id: Option<IntegrationId>,
    pub policy_surface: Option<IntegrationPolicySurface>,
    pub required_tier: PrivilegeTier,
    pub local_only: bool,
    pub cloud_required: bool,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationEvidenceSummary {
    pub total_evidence: usize,
    pub approval_decision_evidence: usize,
    pub policy_review_evidence: usize,
    pub primitive_blocker_evidence: usize,
    pub capability_blocker_evidence: usize,
    pub dependency_blocker_evidence: usize,
    pub policy_risk_evidence: usize,
    pub dependency_edge_evidence: usize,
    pub supporting_evidence: usize,
    pub review_evidence: usize,
    pub blocking_evidence: usize,
    pub unique_integrations: usize,
    pub ready_to_approve_integrations: usize,
    pub blocked_integrations: usize,
    pub local_only_integrations: usize,
    pub cloud_required_integrations: usize,
    pub unique_policy_surfaces: usize,
    pub read_only_evidence: usize,
    pub low_risk_evidence: usize,
    pub human_approval_evidence: usize,
    pub high_risk_evidence: usize,
    pub first_supporting_priority: Option<u8>,
    pub first_review_priority: Option<u8>,
    pub first_blocking_priority: Option<u8>,
    pub highest_policy_tier: PrivilegeTier,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationDossierItem {
    pub decision: IntegrationActivationDecisionItem,
    pub evidence: Vec<IntegrationActivationEvidenceItem>,
    pub evidence_summary: IntegrationActivationEvidenceSummary,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationDossierSummary {
    pub total_dossiers: usize,
    pub ready_to_approve_dossiers: usize,
    pub blocked_dossiers: usize,
    pub local_only_dossiers: usize,
    pub cloud_required_dossiers: usize,
    pub dossiers_with_policy_surfaces: usize,
    pub dossiers_without_policy_surfaces: usize,
    pub unique_policy_surfaces: usize,
    pub total_actions: usize,
    pub total_constraints: usize,
    pub total_risks: usize,
    pub total_dependency_edges: usize,
    pub blocking_dependency_edges: usize,
    pub total_evidence: usize,
    pub supporting_evidence: usize,
    pub review_evidence: usize,
    pub blocking_evidence: usize,
    pub read_only_dossiers: usize,
    pub low_risk_dossiers: usize,
    pub human_approval_dossiers: usize,
    pub high_risk_dossiers: usize,
    pub first_approval_priority: Option<u8>,
    pub first_blocked_priority: Option<u8>,
    pub highest_policy_tier: PrivilegeTier,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationAgendaStage {
    pub priority: u8,
    pub candidates: Vec<IntegrationActivationCandidate>,
    pub candidate_summary: IntegrationActivationCandidateSummary,
    pub actions: Vec<IntegrationActivationAction>,
    pub action_summary: IntegrationActivationActionSummary,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationAgendaSummary {
    pub total_stages: usize,
    pub total_candidates: usize,
    pub total_actions: usize,
    pub stages_with_activation_work: usize,
    pub stages_with_blockers: usize,
    pub stages_with_review_work: usize,
    pub first_action_priority: Option<u8>,
    pub first_activation_priority: Option<u8>,
    pub first_blocker_priority: Option<u8>,
    pub highest_policy_tier: PrivilegeTier,
    pub candidate_summary: IntegrationActivationCandidateSummary,
    pub action_summary: IntegrationActivationActionSummary,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationRunwayStage {
    pub priority: u8,
    pub candidates: Vec<IntegrationActivationCandidate>,
    pub summary: IntegrationActivationCandidateSummary,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationRunwaySummary {
    pub total_stages: usize,
    pub total_candidates: usize,
    pub actionable_stages: usize,
    pub ready_stages: usize,
    pub review_stages: usize,
    pub blocked_stages: usize,
    pub first_actionable_priority: Option<u8>,
    pub first_blocked_priority: Option<u8>,
    pub next_ready_priority: Option<u8>,
    pub highest_policy_tier: PrivilegeTier,
    pub candidate_summary: IntegrationActivationCandidateSummary,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationReadinessSummary {
    pub total_reports: usize,
    pub activation_ready_reports: usize,
    pub blocked_reports: usize,
    pub reports_requiring_human_review: usize,
    pub cloud_required_reports: usize,
    pub local_only_reports: usize,
    pub direct_targets: usize,
    pub delegated_integration_targets: usize,
    pub delegated_standard_targets: usize,
    pub reports_missing_primitives: usize,
    pub reports_missing_capabilities: usize,
    pub reports_missing_dependencies: usize,
    pub unique_missing_primitives: usize,
    pub unique_missing_capabilities: usize,
    pub unique_missing_dependencies: usize,
    pub highest_policy_tier: PrivilegeTier,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationReadinessPrimitiveGap {
    pub primitive: PrimitiveFamily,
    pub highest_priority: u8,
    pub blocked_report_count: usize,
    pub integration_ids: Vec<IntegrationId>,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationReadinessCapabilityGap {
    pub capability_id: CapabilityId,
    pub highest_priority: u8,
    pub blocked_report_count: usize,
    pub integration_ids: Vec<IntegrationId>,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationReadinessDependencyGap {
    pub integration_id: IntegrationId,
    pub highest_priority: u8,
    pub blocked_report_count: usize,
    pub requested_integration_ids: Vec<IntegrationId>,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationReadinessGapInventory {
    pub total_reports: usize,
    pub activation_ready_reports: usize,
    pub blocked_reports: usize,
    pub primitive_gaps: Vec<IntegrationReadinessPrimitiveGap>,
    pub capability_gaps: Vec<IntegrationReadinessCapabilityGap>,
    pub dependency_gaps: Vec<IntegrationReadinessDependencyGap>,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationDependencyNode {
    pub integration_id: IntegrationId,
    pub display_name: String,
    pub priority: u8,
    pub activation_target: IntegrationActivationTarget,
    pub depends_on_integrations: Vec<IntegrationId>,
    pub dependent_integration_ids: Vec<IntegrationId>,
    pub missing_dependencies: Vec<IntegrationId>,
    pub enabled: bool,
    pub activation_ready: bool,
    pub requires_human_review: bool,
    pub highest_policy_tier: PrivilegeTier,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationDependencyEdge {
    pub dependency_integration_id: IntegrationId,
    pub dependent_integration_id: IntegrationId,
    pub dependency_display_name: Option<String>,
    pub dependent_display_name: String,
    pub dependency_priority: Option<u8>,
    pub dependent_priority: u8,
    pub satisfied: bool,
    pub blocks_activation: bool,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationDependencyGraph {
    pub nodes: Vec<IntegrationActivationDependencyNode>,
    pub edges: Vec<IntegrationActivationDependencyEdge>,
    pub summary: IntegrationActivationDependencySummary,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationDependencySummary {
    pub total_nodes: usize,
    pub enabled_nodes: usize,
    pub activation_ready_nodes: usize,
    pub blocked_nodes: usize,
    pub nodes_with_dependencies: usize,
    pub nodes_with_dependents: usize,
    pub nodes_with_missing_dependencies: usize,
    pub total_edges: usize,
    pub satisfied_edges: usize,
    pub blocking_edges: usize,
    pub unknown_dependency_edges: usize,
    pub first_blocked_priority: Option<u8>,
    pub highest_policy_tier: PrivilegeTier,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationHealthStage {
    pub priority: u8,
    pub health_status: IntegrationActivationHealthStatus,
    pub integration_ids: Vec<IntegrationId>,
    pub ready_to_activate_integration_ids: Vec<IntegrationId>,
    pub review_integration_ids: Vec<IntegrationId>,
    pub blocked_integration_ids: Vec<IntegrationId>,
    pub candidate_summary: IntegrationActivationCandidateSummary,
    pub gap_inventory: IntegrationReadinessGapInventory,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationHealthSummary {
    pub total_stages: usize,
    pub total_integrations: usize,
    pub ready_stages: usize,
    pub review_stages: usize,
    pub blocked_stages: usize,
    pub empty_stages: usize,
    pub activation_ready_integrations: usize,
    pub ready_to_activate_integrations: usize,
    pub review_integrations: usize,
    pub blocked_integrations: usize,
    pub primitive_gap_count: usize,
    pub capability_gap_count: usize,
    pub dependency_gap_count: usize,
    pub total_unique_gaps: usize,
    pub first_ready_priority: Option<u8>,
    pub first_review_priority: Option<u8>,
    pub first_blocked_priority: Option<u8>,
    pub highest_policy_tier: PrivilegeTier,
    pub overall_status: IntegrationActivationHealthStatus,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationMaintenanceWindow {
    pub priority: u8,
    pub health_status: IntegrationActivationHealthStatus,
    pub integration_ids: Vec<IntegrationId>,
    pub ready_to_activate_integration_ids: Vec<IntegrationId>,
    pub review_integration_ids: Vec<IntegrationId>,
    pub blocked_integration_ids: Vec<IntegrationId>,
    pub candidate_summary: IntegrationActivationCandidateSummary,
    pub action_summary: IntegrationActivationActionSummary,
    pub constraint_summary: IntegrationActivationConstraintSummary,
    pub risk_summary: IntegrationActivationRiskSummary,
    pub dependency_summary: IntegrationActivationDependencySummary,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationMaintenanceSummary {
    pub total_windows: usize,
    pub total_integrations: usize,
    pub ready_windows: usize,
    pub review_windows: usize,
    pub blocked_windows: usize,
    pub empty_windows: usize,
    pub activation_ready_integrations: usize,
    pub ready_to_activate_integrations: usize,
    pub review_integrations: usize,
    pub blocked_integrations: usize,
    pub windows_with_actions: usize,
    pub windows_with_activation_work: usize,
    pub windows_with_review_work: usize,
    pub windows_with_blockers: usize,
    pub windows_with_risks: usize,
    pub windows_with_dependency_blockers: usize,
    pub total_actions: usize,
    pub activate_integration_actions: usize,
    pub review_policy_actions: usize,
    pub blocking_constraints: usize,
    pub review_constraints: usize,
    pub total_risks: usize,
    pub total_dependency_edges: usize,
    pub blocking_dependency_edges: usize,
    pub first_ready_priority: Option<u8>,
    pub first_review_priority: Option<u8>,
    pub first_blocked_priority: Option<u8>,
    pub first_activation_priority: Option<u8>,
    pub highest_policy_tier: PrivilegeTier,
    pub overall_status: IntegrationActivationHealthStatus,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationReadoutStage {
    pub priority: u8,
    pub health_status: IntegrationActivationHealthStatus,
    pub integration_ids: Vec<IntegrationId>,
    pub maintenance_window: IntegrationActivationMaintenanceWindow,
    pub dossiers: Vec<IntegrationActivationDossierItem>,
    pub dossier_summary: IntegrationActivationDossierSummary,
    pub evidence_summary: IntegrationActivationEvidenceSummary,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationReadoutSummary {
    pub total_readouts: usize,
    pub total_integrations: usize,
    pub ready_readouts: usize,
    pub review_readouts: usize,
    pub blocked_readouts: usize,
    pub empty_readouts: usize,
    pub activation_ready_integrations: usize,
    pub ready_to_activate_integrations: usize,
    pub review_integrations: usize,
    pub blocked_integrations: usize,
    pub readouts_with_activation_work: usize,
    pub readouts_with_approval_work: usize,
    pub readouts_with_review_work: usize,
    pub readouts_with_blockers: usize,
    pub readouts_with_risks: usize,
    pub readouts_with_dependency_blockers: usize,
    pub total_actions: usize,
    pub activate_integration_actions: usize,
    pub review_policy_actions: usize,
    pub blocking_constraints: usize,
    pub review_constraints: usize,
    pub total_risks: usize,
    pub total_dependency_edges: usize,
    pub blocking_dependency_edges: usize,
    pub total_dossiers: usize,
    pub ready_to_approve_dossiers: usize,
    pub blocked_dossiers: usize,
    pub total_evidence: usize,
    pub supporting_evidence: usize,
    pub review_evidence: usize,
    pub blocking_evidence: usize,
    pub first_ready_priority: Option<u8>,
    pub first_review_priority: Option<u8>,
    pub first_blocked_priority: Option<u8>,
    pub first_activation_priority: Option<u8>,
    pub first_approval_priority: Option<u8>,
    pub highest_policy_tier: PrivilegeTier,
    pub overall_status: IntegrationActivationHealthStatus,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationBriefingItem {
    pub kind: IntegrationActivationBriefingItemKind,
    pub priority: u8,
    pub health_status: IntegrationActivationHealthStatus,
    pub integration_ids: Vec<IntegrationId>,
    pub action_count: usize,
    pub dossier_count: usize,
    pub evidence_count: usize,
    pub risk_count: usize,
    pub dependency_edge_count: usize,
    pub blocking_dependency_edge_count: usize,
    pub highest_policy_tier: PrivilegeTier,
    pub has_activation_work: bool,
    pub has_approval_ready_work: bool,
    pub has_review_work: bool,
    pub has_blockers: bool,
    pub has_risks: bool,
    pub has_dependency_blockers: bool,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationBriefingSummary {
    pub total_items: usize,
    pub unique_integrations: usize,
    pub activation_items: usize,
    pub approval_items: usize,
    pub review_items: usize,
    pub blocker_items: usize,
    pub risk_items: usize,
    pub dependency_items: usize,
    pub items_requiring_attention: usize,
    pub items_with_activation_work: usize,
    pub items_with_approval_work: usize,
    pub items_with_review_work: usize,
    pub items_with_blockers: usize,
    pub items_with_risks: usize,
    pub items_with_dependency_blockers: usize,
    pub total_actions: usize,
    pub total_dossiers: usize,
    pub total_evidence: usize,
    pub total_risks: usize,
    pub total_dependency_edges: usize,
    pub blocking_dependency_edges: usize,
    pub first_activation_priority: Option<u8>,
    pub first_approval_priority: Option<u8>,
    pub first_review_priority: Option<u8>,
    pub first_blocked_priority: Option<u8>,
    pub first_risk_priority: Option<u8>,
    pub first_dependency_priority: Option<u8>,
    pub highest_policy_tier: PrivilegeTier,
    pub overall_status: IntegrationActivationHealthStatus,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationDashboardCard {
    pub priority: u8,
    pub health_status: IntegrationActivationHealthStatus,
    pub integration_ids: Vec<IntegrationId>,
    pub briefing_item_count: usize,
    pub next_briefing_kind: Option<IntegrationActivationBriefingItemKind>,
    pub briefing_summary: IntegrationActivationBriefingSummary,
    pub action_count: usize,
    pub dossier_count: usize,
    pub evidence_count: usize,
    pub risk_count: usize,
    pub dependency_edge_count: usize,
    pub blocking_dependency_edge_count: usize,
    pub highest_policy_tier: PrivilegeTier,
    pub has_activation_work: bool,
    pub has_approval_ready_work: bool,
    pub has_review_work: bool,
    pub has_blockers: bool,
    pub has_risks: bool,
    pub has_dependency_blockers: bool,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationDashboardSummary {
    pub total_cards: usize,
    pub unique_integrations: usize,
    pub ready_cards: usize,
    pub review_cards: usize,
    pub blocked_cards: usize,
    pub empty_cards: usize,
    pub cards_requiring_attention: usize,
    pub cards_with_activation_work: usize,
    pub cards_with_approval_work: usize,
    pub cards_with_review_work: usize,
    pub cards_with_blockers: usize,
    pub cards_with_risks: usize,
    pub cards_with_dependency_blockers: usize,
    pub total_briefing_items: usize,
    pub activation_items: usize,
    pub approval_items: usize,
    pub review_items: usize,
    pub blocker_items: usize,
    pub risk_items: usize,
    pub dependency_items: usize,
    pub total_actions: usize,
    pub total_dossiers: usize,
    pub total_evidence: usize,
    pub total_risks: usize,
    pub total_dependency_edges: usize,
    pub blocking_dependency_edges: usize,
    pub first_activation_priority: Option<u8>,
    pub first_approval_priority: Option<u8>,
    pub first_review_priority: Option<u8>,
    pub first_blocked_priority: Option<u8>,
    pub first_risk_priority: Option<u8>,
    pub first_dependency_priority: Option<u8>,
    pub first_attention_priority: Option<u8>,
    pub highest_policy_tier: PrivilegeTier,
    pub overall_status: IntegrationActivationHealthStatus,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationTimelineMilestone {
    pub sequence: usize,
    pub priority: u8,
    pub milestone_kind: Option<IntegrationActivationBriefingItemKind>,
    pub dashboard_card: IntegrationActivationDashboardCard,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationTimelineSummary {
    pub total_milestones: usize,
    pub unique_integrations: usize,
    pub ready_milestones: usize,
    pub review_milestones: usize,
    pub blocked_milestones: usize,
    pub empty_milestones: usize,
    pub blocker_milestones: usize,
    pub review_queue_milestones: usize,
    pub approval_milestones: usize,
    pub activation_milestones: usize,
    pub risk_milestones: usize,
    pub dependency_milestones: usize,
    pub milestones_requiring_attention: usize,
    pub milestones_with_activation_work: usize,
    pub milestones_with_approval_work: usize,
    pub milestones_with_review_work: usize,
    pub milestones_with_blockers: usize,
    pub milestones_with_risks: usize,
    pub milestones_with_dependency_blockers: usize,
    pub total_briefing_items: usize,
    pub total_actions: usize,
    pub total_dossiers: usize,
    pub total_evidence: usize,
    pub total_risks: usize,
    pub total_dependency_edges: usize,
    pub blocking_dependency_edges: usize,
    pub first_activation_sequence: Option<usize>,
    pub first_approval_sequence: Option<usize>,
    pub first_review_sequence: Option<usize>,
    pub first_blocked_sequence: Option<usize>,
    pub first_risk_sequence: Option<usize>,
    pub first_dependency_sequence: Option<usize>,
    pub first_attention_sequence: Option<usize>,
    pub first_activation_priority: Option<u8>,
    pub first_approval_priority: Option<u8>,
    pub first_review_priority: Option<u8>,
    pub first_blocked_priority: Option<u8>,
    pub first_risk_priority: Option<u8>,
    pub first_dependency_priority: Option<u8>,
    pub first_attention_priority: Option<u8>,
    pub highest_policy_tier: PrivilegeTier,
    pub overall_status: IntegrationActivationHealthStatus,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationForecastItem {
    pub sequence: usize,
    pub priority: u8,
    pub forecast_action: IntegrationActivationForecastAction,
    pub milestone_kind: Option<IntegrationActivationBriefingItemKind>,
    pub health_status: IntegrationActivationHealthStatus,
    pub integration_ids: Vec<IntegrationId>,
    pub integration_count: usize,
    pub briefing_item_count: usize,
    pub action_count: usize,
    pub dossier_count: usize,
    pub evidence_count: usize,
    pub risk_count: usize,
    pub dependency_edge_count: usize,
    pub blocking_dependency_edge_count: usize,
    pub highest_policy_tier: PrivilegeTier,
    pub has_activation_work: bool,
    pub has_approval_ready_work: bool,
    pub has_review_work: bool,
    pub has_blockers: bool,
    pub has_risks: bool,
    pub has_dependency_blockers: bool,
    pub requires_attention: bool,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationForecastSummary {
    pub total_forecasts: usize,
    pub unique_integrations: usize,
    pub ready_forecasts: usize,
    pub review_forecasts: usize,
    pub blocked_forecasts: usize,
    pub empty_forecasts: usize,
    pub activate_wave_forecasts: usize,
    pub prepare_approval_forecasts: usize,
    pub queue_review_forecasts: usize,
    pub resolve_blocker_forecasts: usize,
    pub enable_dependency_forecasts: usize,
    pub review_risk_forecasts: usize,
    pub monitor_wave_forecasts: usize,
    pub forecasts_requiring_attention: usize,
    pub forecasts_with_activation_work: usize,
    pub forecasts_with_approval_work: usize,
    pub forecasts_with_review_work: usize,
    pub forecasts_with_blockers: usize,
    pub forecasts_with_risks: usize,
    pub forecasts_with_dependency_blockers: usize,
    pub total_briefing_items: usize,
    pub total_actions: usize,
    pub total_dossiers: usize,
    pub total_evidence: usize,
    pub total_risks: usize,
    pub total_dependency_edges: usize,
    pub blocking_dependency_edges: usize,
    pub next_action: Option<IntegrationActivationForecastAction>,
    pub next_action_sequence: Option<usize>,
    pub next_action_priority: Option<u8>,
    pub first_activation_sequence: Option<usize>,
    pub first_approval_sequence: Option<usize>,
    pub first_review_sequence: Option<usize>,
    pub first_blocked_sequence: Option<usize>,
    pub first_risk_sequence: Option<usize>,
    pub first_dependency_sequence: Option<usize>,
    pub first_attention_sequence: Option<usize>,
    pub first_activation_priority: Option<u8>,
    pub first_approval_priority: Option<u8>,
    pub first_review_priority: Option<u8>,
    pub first_blocked_priority: Option<u8>,
    pub first_risk_priority: Option<u8>,
    pub first_dependency_priority: Option<u8>,
    pub first_attention_priority: Option<u8>,
    pub highest_policy_tier: PrivilegeTier,
    pub overall_status: IntegrationActivationHealthStatus,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationPlaybookStep {
    pub sequence: usize,
    pub priority: u8,
    pub playbook_action: IntegrationActivationForecastAction,
    pub recommended_view: IntegrationActivationPlaybookView,
    pub milestone_kind: Option<IntegrationActivationBriefingItemKind>,
    pub health_status: IntegrationActivationHealthStatus,
    pub integration_ids: Vec<IntegrationId>,
    pub integration_count: usize,
    pub action_count: usize,
    pub dossier_count: usize,
    pub evidence_count: usize,
    pub risk_count: usize,
    pub dependency_edge_count: usize,
    pub blocking_dependency_edge_count: usize,
    pub highest_policy_tier: PrivilegeTier,
    pub operator_required: bool,
    pub activation_ready: bool,
    pub blocked: bool,
    pub review_required: bool,
    pub monitor_only: bool,
    pub requires_attention: bool,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationPlaybookSummary {
    pub total_steps: usize,
    pub unique_integrations: usize,
    pub operator_required_steps: usize,
    pub activation_ready_steps: usize,
    pub blocked_steps: usize,
    pub review_required_steps: usize,
    pub monitor_steps: usize,
    pub constraint_view_steps: usize,
    pub dependency_view_steps: usize,
    pub approval_view_steps: usize,
    pub review_view_steps: usize,
    pub risk_view_steps: usize,
    pub action_view_steps: usize,
    pub dashboard_view_steps: usize,
    pub total_actions: usize,
    pub total_dossiers: usize,
    pub total_evidence: usize,
    pub total_risks: usize,
    pub total_dependency_edges: usize,
    pub blocking_dependency_edges: usize,
    pub next_playbook_action: Option<IntegrationActivationForecastAction>,
    pub next_recommended_view: Option<IntegrationActivationPlaybookView>,
    pub next_step_sequence: Option<usize>,
    pub next_step_priority: Option<u8>,
    pub first_operator_sequence: Option<usize>,
    pub first_activation_sequence: Option<usize>,
    pub first_blocked_sequence: Option<usize>,
    pub first_review_sequence: Option<usize>,
    pub first_monitor_sequence: Option<usize>,
    pub first_operator_priority: Option<u8>,
    pub first_activation_priority: Option<u8>,
    pub first_blocked_priority: Option<u8>,
    pub first_review_priority: Option<u8>,
    pub first_monitor_priority: Option<u8>,
    pub highest_policy_tier: PrivilegeTier,
    pub overall_status: IntegrationActivationHealthStatus,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationRunbookEntry {
    pub sequence: usize,
    pub playbook_sequence: usize,
    pub priority: u8,
    pub phase: IntegrationActivationRunbookPhase,
    pub playbook_action: IntegrationActivationForecastAction,
    pub recommended_view: IntegrationActivationPlaybookView,
    pub milestone_kind: Option<IntegrationActivationBriefingItemKind>,
    pub health_status: IntegrationActivationHealthStatus,
    pub integration_ids: Vec<IntegrationId>,
    pub integration_count: usize,
    pub action_count: usize,
    pub dossier_count: usize,
    pub evidence_count: usize,
    pub risk_count: usize,
    pub dependency_edge_count: usize,
    pub blocking_dependency_edge_count: usize,
    pub audit_record_count: usize,
    pub attention_audit_record_count: usize,
    pub risk_audit_record_count: usize,
    pub dependency_audit_record_count: usize,
    pub readiness_gap_record_count: usize,
    pub highest_policy_tier: PrivilegeTier,
    pub operator_required: bool,
    pub activation_ready: bool,
    pub blocked: bool,
    pub review_required: bool,
    pub monitor_only: bool,
    pub requires_attention: bool,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationRunbookSummary {
    pub total_entries: usize,
    pub unique_integrations: usize,
    pub operator_required_entries: usize,
    pub activation_ready_entries: usize,
    pub blocked_entries: usize,
    pub review_required_entries: usize,
    pub monitor_entries: usize,
    pub clear_blocker_entries: usize,
    pub dependency_entries: usize,
    pub approval_entries: usize,
    pub review_entries: usize,
    pub risk_entries: usize,
    pub activation_entries: usize,
    pub monitor_phase_entries: usize,
    pub total_actions: usize,
    pub total_dossiers: usize,
    pub total_evidence: usize,
    pub total_risks: usize,
    pub total_dependency_edges: usize,
    pub blocking_dependency_edges: usize,
    pub total_audit_records: usize,
    pub attention_audit_records: usize,
    pub risk_audit_records: usize,
    pub dependency_audit_records: usize,
    pub readiness_gap_records: usize,
    pub next_phase: Option<IntegrationActivationRunbookPhase>,
    pub next_playbook_action: Option<IntegrationActivationForecastAction>,
    pub next_recommended_view: Option<IntegrationActivationPlaybookView>,
    pub next_entry_sequence: Option<usize>,
    pub next_entry_priority: Option<u8>,
    pub first_operator_sequence: Option<usize>,
    pub first_activation_sequence: Option<usize>,
    pub first_blocked_sequence: Option<usize>,
    pub first_review_sequence: Option<usize>,
    pub first_monitor_sequence: Option<usize>,
    pub first_attention_sequence: Option<usize>,
    pub first_operator_priority: Option<u8>,
    pub first_activation_priority: Option<u8>,
    pub first_blocked_priority: Option<u8>,
    pub first_review_priority: Option<u8>,
    pub first_monitor_priority: Option<u8>,
    pub first_attention_priority: Option<u8>,
    pub highest_policy_tier: PrivilegeTier,
    pub overall_status: IntegrationActivationHealthStatus,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationHandoffPackage {
    pub sequence: usize,
    pub runbook_sequence: usize,
    pub priority: u8,
    pub handoff_status: IntegrationActivationHandoffStatus,
    pub phase: IntegrationActivationRunbookPhase,
    pub playbook_action: IntegrationActivationForecastAction,
    pub recommended_view: IntegrationActivationPlaybookView,
    pub integration_ids: Vec<IntegrationId>,
    pub integration_count: usize,
    pub risk_ids: Vec<String>,
    pub dependency_integration_ids: Vec<IntegrationId>,
    pub blocking_dependency_integration_ids: Vec<IntegrationId>,
    pub risk_count: usize,
    pub dependency_edge_count: usize,
    pub blocking_dependency_edge_count: usize,
    pub primitive_gap_count: usize,
    pub capability_gap_count: usize,
    pub dependency_gap_count: usize,
    pub readiness_gap_count: usize,
    pub audit_record_count: usize,
    pub attention_audit_record_count: usize,
    pub highest_policy_tier: PrivilegeTier,
    pub operator_required: bool,
    pub activation_ready: bool,
    pub ready_for_handoff: bool,
    pub blocked: bool,
    pub review_required: bool,
    pub monitor_only: bool,
    pub requires_attention: bool,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationHandoffSummary {
    pub total_packages: usize,
    pub unique_integrations: usize,
    pub ready_packages: usize,
    pub review_required_packages: usize,
    pub blocked_packages: usize,
    pub monitor_packages: usize,
    pub operator_required_packages: usize,
    pub activation_ready_packages: usize,
    pub packages_requiring_attention: usize,
    pub total_risks: usize,
    pub total_dependency_edges: usize,
    pub blocking_dependency_edges: usize,
    pub total_primitive_gaps: usize,
    pub total_capability_gaps: usize,
    pub total_dependency_gaps: usize,
    pub total_readiness_gaps: usize,
    pub total_audit_records: usize,
    pub attention_audit_records: usize,
    pub next_handoff_status: Option<IntegrationActivationHandoffStatus>,
    pub next_phase: Option<IntegrationActivationRunbookPhase>,
    pub next_playbook_action: Option<IntegrationActivationForecastAction>,
    pub next_recommended_view: Option<IntegrationActivationPlaybookView>,
    pub next_package_sequence: Option<usize>,
    pub next_package_priority: Option<u8>,
    pub first_ready_sequence: Option<usize>,
    pub first_blocked_sequence: Option<usize>,
    pub first_review_sequence: Option<usize>,
    pub first_attention_sequence: Option<usize>,
    pub first_ready_priority: Option<u8>,
    pub first_blocked_priority: Option<u8>,
    pub first_review_priority: Option<u8>,
    pub first_attention_priority: Option<u8>,
    pub highest_policy_tier: PrivilegeTier,
    pub overall_status: IntegrationActivationHealthStatus,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationExecutionPacket {
    pub sequence: usize,
    pub handoff_sequence: usize,
    pub runbook_sequence: usize,
    pub operator_task_sequence: Option<usize>,
    pub priority: u8,
    pub execution_status: IntegrationActivationExecutionStatus,
    pub handoff_status: IntegrationActivationHandoffStatus,
    pub phase: IntegrationActivationRunbookPhase,
    pub task_kind: Option<IntegrationActivationOperatorTaskKind>,
    pub playbook_action: IntegrationActivationForecastAction,
    pub recommended_view: IntegrationActivationPlaybookView,
    pub integration_ids: Vec<IntegrationId>,
    pub integration_count: usize,
    pub risk_ids: Vec<String>,
    pub dependency_integration_ids: Vec<IntegrationId>,
    pub blocking_dependency_integration_ids: Vec<IntegrationId>,
    pub risk_count: usize,
    pub dependency_edge_count: usize,
    pub blocking_dependency_edge_count: usize,
    pub readiness_gap_count: usize,
    pub audit_record_count: usize,
    pub attention_audit_record_count: usize,
    pub highest_policy_tier: PrivilegeTier,
    pub operator_required: bool,
    pub activation_ready: bool,
    pub ready_for_handoff: bool,
    pub approval_required: bool,
    pub dependency_ready: bool,
    pub executable: bool,
    pub blocked: bool,
    pub review_required: bool,
    pub monitor_only: bool,
    pub requires_attention: bool,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationExecutionSummary {
    pub total_packets: usize,
    pub unique_integrations: usize,
    pub executable_packets: usize,
    pub operator_required_packets: usize,
    pub approval_required_packets: usize,
    pub blocked_packets: usize,
    pub dependency_ready_packets: usize,
    pub dependency_blocked_packets: usize,
    pub monitor_packets: usize,
    pub activation_ready_packets: usize,
    pub packets_requiring_attention: usize,
    pub total_risks: usize,
    pub total_dependency_edges: usize,
    pub blocking_dependency_edges: usize,
    pub total_readiness_gaps: usize,
    pub total_audit_records: usize,
    pub attention_audit_records: usize,
    pub next_execution_status: Option<IntegrationActivationExecutionStatus>,
    pub next_task_kind: Option<IntegrationActivationOperatorTaskKind>,
    pub next_phase: Option<IntegrationActivationRunbookPhase>,
    pub next_playbook_action: Option<IntegrationActivationForecastAction>,
    pub next_recommended_view: Option<IntegrationActivationPlaybookView>,
    pub next_packet_sequence: Option<usize>,
    pub next_packet_priority: Option<u8>,
    pub first_executable_sequence: Option<usize>,
    pub first_operator_sequence: Option<usize>,
    pub first_approval_sequence: Option<usize>,
    pub first_blocked_sequence: Option<usize>,
    pub first_dependency_blocked_sequence: Option<usize>,
    pub first_attention_sequence: Option<usize>,
    pub first_executable_priority: Option<u8>,
    pub first_operator_priority: Option<u8>,
    pub first_approval_priority: Option<u8>,
    pub first_blocked_priority: Option<u8>,
    pub first_dependency_blocked_priority: Option<u8>,
    pub first_attention_priority: Option<u8>,
    pub highest_policy_tier: PrivilegeTier,
    pub overall_status: IntegrationActivationHealthStatus,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationVerificationCheckpoint {
    pub sequence: usize,
    pub execution_sequence: usize,
    pub handoff_sequence: usize,
    pub runbook_sequence: usize,
    pub operator_task_sequence: Option<usize>,
    pub priority: u8,
    pub verification_status: IntegrationActivationVerificationStatus,
    pub execution_status: IntegrationActivationExecutionStatus,
    pub handoff_status: IntegrationActivationHandoffStatus,
    pub phase: IntegrationActivationRunbookPhase,
    pub task_kind: Option<IntegrationActivationOperatorTaskKind>,
    pub playbook_action: IntegrationActivationForecastAction,
    pub recommended_view: IntegrationActivationPlaybookView,
    pub integration_ids: Vec<IntegrationId>,
    pub integration_count: usize,
    pub risk_ids: Vec<String>,
    pub dependency_integration_ids: Vec<IntegrationId>,
    pub blocking_dependency_integration_ids: Vec<IntegrationId>,
    pub risk_count: usize,
    pub dependency_edge_count: usize,
    pub blocking_dependency_edge_count: usize,
    pub readiness_gap_count: usize,
    pub audit_record_count: usize,
    pub attention_audit_record_count: usize,
    pub highest_policy_tier: PrivilegeTier,
    pub operator_pending: bool,
    pub approval_pending: bool,
    pub dependency_ready: bool,
    pub can_verify: bool,
    pub verification_ready: bool,
    pub blocked: bool,
    pub monitor_only: bool,
    pub evidence_review_required: bool,
    pub requires_attention: bool,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationVerificationSummary {
    pub total_checkpoints: usize,
    pub unique_integrations: usize,
    pub ready_to_verify_checkpoints: usize,
    pub verification_ready_checkpoints: usize,
    pub pending_operator_checkpoints: usize,
    pub pending_approval_checkpoints: usize,
    pub blocked_checkpoints: usize,
    pub dependency_ready_checkpoints: usize,
    pub dependency_blocked_checkpoints: usize,
    pub monitor_checkpoints: usize,
    pub evidence_review_checkpoints: usize,
    pub checkpoints_requiring_attention: usize,
    pub total_risks: usize,
    pub total_dependency_edges: usize,
    pub blocking_dependency_edges: usize,
    pub total_readiness_gaps: usize,
    pub total_audit_records: usize,
    pub attention_audit_records: usize,
    pub next_verification_status: Option<IntegrationActivationVerificationStatus>,
    pub next_execution_status: Option<IntegrationActivationExecutionStatus>,
    pub next_task_kind: Option<IntegrationActivationOperatorTaskKind>,
    pub next_phase: Option<IntegrationActivationRunbookPhase>,
    pub next_playbook_action: Option<IntegrationActivationForecastAction>,
    pub next_recommended_view: Option<IntegrationActivationPlaybookView>,
    pub next_checkpoint_sequence: Option<usize>,
    pub next_checkpoint_priority: Option<u8>,
    pub first_ready_to_verify_sequence: Option<usize>,
    pub first_pending_operator_sequence: Option<usize>,
    pub first_pending_approval_sequence: Option<usize>,
    pub first_blocked_sequence: Option<usize>,
    pub first_attention_sequence: Option<usize>,
    pub first_ready_to_verify_priority: Option<u8>,
    pub first_pending_operator_priority: Option<u8>,
    pub first_pending_approval_priority: Option<u8>,
    pub first_blocked_priority: Option<u8>,
    pub first_attention_priority: Option<u8>,
    pub highest_policy_tier: PrivilegeTier,
    pub overall_status: IntegrationActivationHealthStatus,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationOperatorTask {
    pub sequence: usize,
    pub playbook_sequence: usize,
    pub priority: u8,
    pub task_kind: IntegrationActivationOperatorTaskKind,
    pub playbook_action: IntegrationActivationForecastAction,
    pub recommended_view: IntegrationActivationPlaybookView,
    pub milestone_kind: Option<IntegrationActivationBriefingItemKind>,
    pub health_status: IntegrationActivationHealthStatus,
    pub integration_ids: Vec<IntegrationId>,
    pub integration_count: usize,
    pub action_count: usize,
    pub dossier_count: usize,
    pub evidence_count: usize,
    pub risk_count: usize,
    pub dependency_edge_count: usize,
    pub blocking_dependency_edge_count: usize,
    pub highest_policy_tier: PrivilegeTier,
    pub operator_required: bool,
    pub actionable: bool,
    pub activation_ready: bool,
    pub blocked: bool,
    pub review_required: bool,
    pub monitor_only: bool,
    pub requires_attention: bool,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationOperatorTaskSummary {
    pub total_tasks: usize,
    pub unique_integrations: usize,
    pub operator_required_tasks: usize,
    pub actionable_tasks: usize,
    pub activation_ready_tasks: usize,
    pub blocked_tasks: usize,
    pub review_required_tasks: usize,
    pub monitor_tasks: usize,
    pub resolve_constraint_tasks: usize,
    pub enable_dependency_tasks: usize,
    pub prepare_approval_tasks: usize,
    pub complete_review_tasks: usize,
    pub review_risk_tasks: usize,
    pub activate_wave_tasks: usize,
    pub monitor_wave_tasks: usize,
    pub total_actions: usize,
    pub total_dossiers: usize,
    pub total_evidence: usize,
    pub total_risks: usize,
    pub total_dependency_edges: usize,
    pub blocking_dependency_edges: usize,
    pub next_task_kind: Option<IntegrationActivationOperatorTaskKind>,
    pub next_playbook_action: Option<IntegrationActivationForecastAction>,
    pub next_recommended_view: Option<IntegrationActivationPlaybookView>,
    pub next_task_sequence: Option<usize>,
    pub next_task_priority: Option<u8>,
    pub first_operator_sequence: Option<usize>,
    pub first_actionable_sequence: Option<usize>,
    pub first_activation_sequence: Option<usize>,
    pub first_blocked_sequence: Option<usize>,
    pub first_review_sequence: Option<usize>,
    pub first_operator_priority: Option<u8>,
    pub first_actionable_priority: Option<u8>,
    pub first_activation_priority: Option<u8>,
    pub first_blocked_priority: Option<u8>,
    pub first_review_priority: Option<u8>,
    pub highest_policy_tier: PrivilegeTier,
    pub overall_status: IntegrationActivationHealthStatus,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationControlRoomPanel {
    pub sequence: usize,
    pub recommended_view: IntegrationActivationPlaybookView,
    pub priority: u8,
    pub task_sequences: Vec<usize>,
    pub integration_ids: Vec<IntegrationId>,
    pub task_count: usize,
    pub operator_summary: IntegrationActivationOperatorTaskSummary,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationControlRoomSummary {
    pub total_panels: usize,
    pub unique_integrations: usize,
    pub panels_requiring_attention: usize,
    pub panels_with_operator_work: usize,
    pub panels_with_actionable_work: usize,
    pub panels_with_activation_work: usize,
    pub panels_with_blockers: usize,
    pub panels_with_review_work: usize,
    pub constraint_panels: usize,
    pub dependency_panels: usize,
    pub approval_panels: usize,
    pub review_panels: usize,
    pub risk_panels: usize,
    pub action_panels: usize,
    pub dashboard_panels: usize,
    pub total_tasks: usize,
    pub operator_required_tasks: usize,
    pub actionable_tasks: usize,
    pub activation_ready_tasks: usize,
    pub blocked_tasks: usize,
    pub review_required_tasks: usize,
    pub monitor_tasks: usize,
    pub next_recommended_view: Option<IntegrationActivationPlaybookView>,
    pub next_task_kind: Option<IntegrationActivationOperatorTaskKind>,
    pub next_panel_sequence: Option<usize>,
    pub next_panel_priority: Option<u8>,
    pub first_blocked_panel_sequence: Option<usize>,
    pub first_review_panel_sequence: Option<usize>,
    pub first_activation_panel_sequence: Option<usize>,
    pub first_actionable_panel_sequence: Option<usize>,
    pub first_blocked_panel_priority: Option<u8>,
    pub first_review_panel_priority: Option<u8>,
    pub first_activation_panel_priority: Option<u8>,
    pub first_actionable_panel_priority: Option<u8>,
    pub highest_policy_tier: PrivilegeTier,
    pub overall_status: IntegrationActivationHealthStatus,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationCommandCenterSection {
    pub sequence: usize,
    pub section_kind: IntegrationActivationCommandCenterSectionKind,
    pub priority: u8,
    pub panel_sequences: Vec<usize>,
    pub recommended_views: Vec<IntegrationActivationPlaybookView>,
    pub integration_ids: Vec<IntegrationId>,
    pub panel_count: usize,
    pub control_room_summary: IntegrationActivationControlRoomSummary,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationCommandCenterSummary {
    pub total_sections: usize,
    pub unique_integrations: usize,
    pub sections_requiring_attention: usize,
    pub sections_with_operator_work: usize,
    pub sections_with_actionable_work: usize,
    pub sections_with_activation_work: usize,
    pub sections_with_blockers: usize,
    pub sections_with_review_work: usize,
    pub blocker_sections: usize,
    pub review_sections: usize,
    pub activation_sections: usize,
    pub actionable_sections: usize,
    pub monitoring_sections: usize,
    pub total_panels: usize,
    pub total_tasks: usize,
    pub operator_required_tasks: usize,
    pub actionable_tasks: usize,
    pub activation_ready_tasks: usize,
    pub blocked_tasks: usize,
    pub review_required_tasks: usize,
    pub monitor_tasks: usize,
    pub next_section_kind: Option<IntegrationActivationCommandCenterSectionKind>,
    pub next_recommended_view: Option<IntegrationActivationPlaybookView>,
    pub next_task_kind: Option<IntegrationActivationOperatorTaskKind>,
    pub next_section_sequence: Option<usize>,
    pub next_section_priority: Option<u8>,
    pub first_blocker_section_sequence: Option<usize>,
    pub first_review_section_sequence: Option<usize>,
    pub first_activation_section_sequence: Option<usize>,
    pub first_actionable_section_sequence: Option<usize>,
    pub first_blocker_section_priority: Option<u8>,
    pub first_review_section_priority: Option<u8>,
    pub first_activation_section_priority: Option<u8>,
    pub first_actionable_section_priority: Option<u8>,
    pub highest_policy_tier: PrivilegeTier,
    pub overall_status: IntegrationActivationHealthStatus,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationWatchtowerSignal {
    pub sequence: usize,
    pub signal_kind: IntegrationActivationWatchtowerSignalKind,
    pub priority: u8,
    pub section_sequences: Vec<usize>,
    pub section_kinds: Vec<IntegrationActivationCommandCenterSectionKind>,
    pub recommended_views: Vec<IntegrationActivationPlaybookView>,
    pub integration_ids: Vec<IntegrationId>,
    pub section_count: usize,
    pub command_center_summary: IntegrationActivationCommandCenterSummary,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationWatchtowerSummary {
    pub total_signals: usize,
    pub unique_integrations: usize,
    pub signals_requiring_attention: usize,
    pub signals_with_operator_work: usize,
    pub signals_with_actionable_work: usize,
    pub signals_with_activation_work: usize,
    pub signals_with_blockers: usize,
    pub signals_with_review_work: usize,
    pub escalation_signals: usize,
    pub review_signals: usize,
    pub ready_signals: usize,
    pub action_signals: usize,
    pub observation_signals: usize,
    pub total_sections: usize,
    pub total_panels: usize,
    pub total_tasks: usize,
    pub operator_required_tasks: usize,
    pub actionable_tasks: usize,
    pub activation_ready_tasks: usize,
    pub blocked_tasks: usize,
    pub review_required_tasks: usize,
    pub monitor_tasks: usize,
    pub next_signal_kind: Option<IntegrationActivationWatchtowerSignalKind>,
    pub next_section_kind: Option<IntegrationActivationCommandCenterSectionKind>,
    pub next_recommended_view: Option<IntegrationActivationPlaybookView>,
    pub next_task_kind: Option<IntegrationActivationOperatorTaskKind>,
    pub next_signal_sequence: Option<usize>,
    pub next_signal_priority: Option<u8>,
    pub first_escalation_signal_sequence: Option<usize>,
    pub first_review_signal_sequence: Option<usize>,
    pub first_ready_signal_sequence: Option<usize>,
    pub first_action_signal_sequence: Option<usize>,
    pub first_escalation_signal_priority: Option<u8>,
    pub first_review_signal_priority: Option<u8>,
    pub first_ready_signal_priority: Option<u8>,
    pub first_action_signal_priority: Option<u8>,
    pub highest_policy_tier: PrivilegeTier,
    pub overall_status: IntegrationActivationHealthStatus,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationSentinelAlert {
    pub sequence: usize,
    pub alert_kind: IntegrationActivationSentinelAlertKind,
    pub priority: u8,
    pub integration_ids: Vec<IntegrationId>,
    pub watchtower_summary: IntegrationActivationWatchtowerSummary,
    pub risk_summary: IntegrationActivationRiskSummary,
    pub dependency_summary: IntegrationActivationDependencySummary,
    pub gap_inventory: IntegrationReadinessGapInventory,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationSentinelSummary {
    pub total_alerts: usize,
    pub unique_integrations: usize,
    pub alerts_requiring_attention: usize,
    pub blocker_alerts: usize,
    pub dependency_alerts: usize,
    pub policy_risk_alerts: usize,
    pub review_alerts: usize,
    pub ready_alerts: usize,
    pub observation_alerts: usize,
    pub alerts_with_gaps: usize,
    pub alerts_with_blocking_dependencies: usize,
    pub alerts_with_policy_risk: usize,
    pub alerts_with_review_work: usize,
    pub alerts_with_activation_work: usize,
    pub total_watchtower_signals: usize,
    pub total_risks: usize,
    pub total_dependency_edges: usize,
    pub blocking_dependency_edges: usize,
    pub total_unique_gaps: usize,
    pub first_attention_priority: Option<u8>,
    pub first_blocker_priority: Option<u8>,
    pub first_dependency_priority: Option<u8>,
    pub first_policy_risk_priority: Option<u8>,
    pub first_review_priority: Option<u8>,
    pub first_ready_priority: Option<u8>,
    pub highest_policy_tier: PrivilegeTier,
    pub overall_status: IntegrationActivationHealthStatus,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationAuditRecord {
    pub sequence: usize,
    pub record_kind: IntegrationActivationAuditRecordKind,
    pub record_id: String,
    pub title: String,
    pub summary: String,
    pub priority: u8,
    pub integration_ids: Vec<IntegrationId>,
    pub sentinel_alert_kind: Option<IntegrationActivationSentinelAlertKind>,
    pub watchtower_signal_kind: Option<IntegrationActivationWatchtowerSignalKind>,
    pub decision_status: Option<IntegrationActivationDecisionStatus>,
    pub evidence_kind: Option<IntegrationActivationEvidenceKind>,
    pub evidence_status: Option<IntegrationActivationEvidenceStatus>,
    pub risk_kind: Option<IntegrationActivationRiskKind>,
    pub dependency_integration_id: Option<IntegrationId>,
    pub dependent_integration_id: Option<IntegrationId>,
    pub readiness_gap_kind: Option<String>,
    pub required_tier: PrivilegeTier,
    pub policy_surface: Option<IntegrationPolicySurface>,
    pub requires_attention: bool,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationAuditSummary {
    pub total_records: usize,
    pub unique_integrations: usize,
    pub records_requiring_attention: usize,
    pub sentinel_records: usize,
    pub watchtower_records: usize,
    pub decision_records: usize,
    pub evidence_records: usize,
    pub risk_records: usize,
    pub dependency_records: usize,
    pub readiness_gap_records: usize,
    pub records_with_policy_surface: usize,
    pub records_with_dependency_link: usize,
    pub first_attention_priority: Option<u8>,
    pub first_dependency_priority: Option<u8>,
    pub first_readiness_gap_priority: Option<u8>,
    pub highest_policy_tier: PrivilegeTier,
    pub overall_status: IntegrationActivationHealthStatus,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationEscalationCase {
    pub sequence: usize,
    pub case_kind: IntegrationActivationEscalationCaseKind,
    pub source_id: String,
    pub title: String,
    pub summary: String,
    pub priority: u8,
    pub integration_ids: Vec<IntegrationId>,
    pub sentinel_alert_kind: Option<IntegrationActivationSentinelAlertKind>,
    pub verification_status: Option<IntegrationActivationVerificationStatus>,
    pub audit_record_kind: Option<IntegrationActivationAuditRecordKind>,
    pub recommended_view: IntegrationActivationPlaybookView,
    pub required_tier: PrivilegeTier,
    pub policy_surface: Option<IntegrationPolicySurface>,
    pub dependency_work: bool,
    pub policy_risk: bool,
    pub verification_ready: bool,
    pub blocked: bool,
    pub requires_attention: bool,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationEscalationSummary {
    pub total_cases: usize,
    pub unique_integrations: usize,
    pub cases_requiring_attention: usize,
    pub blocker_cases: usize,
    pub dependency_cases: usize,
    pub policy_risk_cases: usize,
    pub review_cases: usize,
    pub verification_cases: usize,
    pub audit_cases: usize,
    pub cases_with_dependency_work: usize,
    pub cases_with_policy_risk: usize,
    pub cases_ready_to_verify: usize,
    pub blocked_cases: usize,
    pub cases_with_policy_surface: usize,
    pub next_case_kind: Option<IntegrationActivationEscalationCaseKind>,
    pub next_recommended_view: Option<IntegrationActivationPlaybookView>,
    pub next_case_sequence: Option<usize>,
    pub next_case_priority: Option<u8>,
    pub first_blocker_priority: Option<u8>,
    pub first_dependency_priority: Option<u8>,
    pub first_policy_risk_priority: Option<u8>,
    pub first_review_priority: Option<u8>,
    pub first_verification_priority: Option<u8>,
    pub first_attention_priority: Option<u8>,
    pub highest_policy_tier: PrivilegeTier,
    pub overall_status: IntegrationActivationHealthStatus,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationResponseItem {
    pub sequence: usize,
    pub response_kind: IntegrationActivationResponseKind,
    pub owner_lane: IntegrationActivationResponseOwnerLane,
    pub source_case_sequence: usize,
    pub source_case_kind: IntegrationActivationEscalationCaseKind,
    pub source_id: String,
    pub title: String,
    pub summary: String,
    pub priority: u8,
    pub integration_ids: Vec<IntegrationId>,
    pub recommended_view: IntegrationActivationPlaybookView,
    pub required_tier: PrivilegeTier,
    pub policy_surface: Option<IntegrationPolicySurface>,
    pub dependency_work: bool,
    pub policy_risk: bool,
    pub verification_ready: bool,
    pub blocked: bool,
    pub requires_attention: bool,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationResponseSummary {
    pub total_responses: usize,
    pub unique_integrations: usize,
    pub responses_requiring_attention: usize,
    pub resolve_blocker_responses: usize,
    pub enable_dependency_responses: usize,
    pub review_policy_responses: usize,
    pub queue_review_responses: usize,
    pub verify_activation_responses: usize,
    pub audit_follow_up_responses: usize,
    pub platform_owner_responses: usize,
    pub integration_owner_responses: usize,
    pub security_owner_responses: usize,
    pub reviewer_owner_responses: usize,
    pub verification_owner_responses: usize,
    pub audit_owner_responses: usize,
    pub responses_with_dependency_work: usize,
    pub responses_with_policy_risk: usize,
    pub responses_ready_to_verify: usize,
    pub blocked_responses: usize,
    pub responses_with_policy_surface: usize,
    pub next_response_kind: Option<IntegrationActivationResponseKind>,
    pub next_owner_lane: Option<IntegrationActivationResponseOwnerLane>,
    pub next_recommended_view: Option<IntegrationActivationPlaybookView>,
    pub next_response_sequence: Option<usize>,
    pub next_response_priority: Option<u8>,
    pub first_attention_priority: Option<u8>,
    pub highest_policy_tier: PrivilegeTier,
    pub overall_status: IntegrationActivationHealthStatus,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationRemediationItem {
    pub sequence: usize,
    pub remediation_kind: IntegrationActivationRemediationKind,
    pub status: IntegrationActivationRemediationStatus,
    pub owner_lane: IntegrationActivationResponseOwnerLane,
    pub source_response_sequence: usize,
    pub source_response_kind: IntegrationActivationResponseKind,
    pub source_id: String,
    pub title: String,
    pub summary: String,
    pub priority: u8,
    pub integration_ids: Vec<IntegrationId>,
    pub recommended_view: IntegrationActivationPlaybookView,
    pub required_tier: PrivilegeTier,
    pub policy_surface: Option<IntegrationPolicySurface>,
    pub dependency_work: bool,
    pub policy_risk: bool,
    pub verification_ready: bool,
    pub blocked: bool,
    pub requires_attention: bool,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationRemediationSummary {
    pub total_remediations: usize,
    pub unique_integrations: usize,
    pub remediations_requiring_attention: usize,
    pub unblock_platform_remediations: usize,
    pub enable_dependency_remediations: usize,
    pub review_policy_remediations: usize,
    pub complete_review_remediations: usize,
    pub run_verification_remediations: usize,
    pub record_audit_remediations: usize,
    pub platform_owner_remediations: usize,
    pub integration_owner_remediations: usize,
    pub security_owner_remediations: usize,
    pub reviewer_owner_remediations: usize,
    pub verification_owner_remediations: usize,
    pub audit_owner_remediations: usize,
    pub blocked_remediations: usize,
    pub owner_action_remediations: usize,
    pub ready_to_execute_remediations: usize,
    pub tracking_remediations: usize,
    pub remediations_with_dependency_work: usize,
    pub remediations_with_policy_risk: usize,
    pub remediations_ready_to_verify: usize,
    pub remediations_with_policy_surface: usize,
    pub next_remediation_kind: Option<IntegrationActivationRemediationKind>,
    pub next_remediation_status: Option<IntegrationActivationRemediationStatus>,
    pub next_owner_lane: Option<IntegrationActivationResponseOwnerLane>,
    pub next_recommended_view: Option<IntegrationActivationPlaybookView>,
    pub next_remediation_sequence: Option<usize>,
    pub next_remediation_priority: Option<u8>,
    pub first_attention_priority: Option<u8>,
    pub highest_policy_tier: PrivilegeTier,
    pub overall_status: IntegrationActivationHealthStatus,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationClosureGate {
    pub sequence: usize,
    pub closure_status: IntegrationActivationClosureStatus,
    pub owner_lane: IntegrationActivationResponseOwnerLane,
    pub source_remediation_sequence: usize,
    pub source_remediation_kind: IntegrationActivationRemediationKind,
    pub source_remediation_status: IntegrationActivationRemediationStatus,
    pub source_id: String,
    pub title: String,
    pub summary: String,
    pub priority: u8,
    pub integration_ids: Vec<IntegrationId>,
    pub recommended_view: IntegrationActivationPlaybookView,
    pub required_tier: PrivilegeTier,
    pub policy_surface: Option<IntegrationPolicySurface>,
    pub dependency_work: bool,
    pub policy_risk: bool,
    pub verification_ready: bool,
    pub blocked: bool,
    pub requires_attention: bool,
    pub closure_ready: bool,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationClosureSummary {
    pub total_gates: usize,
    pub unique_integrations: usize,
    pub gates_requiring_attention: usize,
    pub blocked_gates: usize,
    pub owner_action_gates: usize,
    pub ready_for_verification_gates: usize,
    pub ready_for_closure_gates: usize,
    pub tracking_gates: usize,
    pub platform_owner_gates: usize,
    pub integration_owner_gates: usize,
    pub security_owner_gates: usize,
    pub reviewer_owner_gates: usize,
    pub verification_owner_gates: usize,
    pub audit_owner_gates: usize,
    pub gates_with_dependency_work: usize,
    pub gates_with_policy_risk: usize,
    pub gates_ready_to_verify: usize,
    pub gates_with_policy_surface: usize,
    pub next_closure_status: Option<IntegrationActivationClosureStatus>,
    pub next_remediation_kind: Option<IntegrationActivationRemediationKind>,
    pub next_owner_lane: Option<IntegrationActivationResponseOwnerLane>,
    pub next_recommended_view: Option<IntegrationActivationPlaybookView>,
    pub next_gate_sequence: Option<usize>,
    pub next_gate_priority: Option<u8>,
    pub first_attention_priority: Option<u8>,
    pub highest_policy_tier: PrivilegeTier,
    pub overall_status: IntegrationActivationHealthStatus,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationReleasePacket {
    pub sequence: usize,
    pub release_status: IntegrationActivationReleaseStatus,
    pub owner_lane: IntegrationActivationResponseOwnerLane,
    pub source_closure_sequence: usize,
    pub source_closure_status: IntegrationActivationClosureStatus,
    pub source_remediation_kind: IntegrationActivationRemediationKind,
    pub source_remediation_status: IntegrationActivationRemediationStatus,
    pub source_id: String,
    pub title: String,
    pub summary: String,
    pub priority: u8,
    pub integration_ids: Vec<IntegrationId>,
    pub recommended_view: IntegrationActivationPlaybookView,
    pub required_tier: PrivilegeTier,
    pub policy_surface: Option<IntegrationPolicySurface>,
    pub dependency_work: bool,
    pub policy_risk: bool,
    pub verification_required: bool,
    pub release_blocked: bool,
    pub requires_attention: bool,
    pub release_ready: bool,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationReleaseSummary {
    pub total_packets: usize,
    pub unique_integrations: usize,
    pub packets_requiring_attention: usize,
    pub blocked_packets: usize,
    pub owner_action_packets: usize,
    pub verification_required_packets: usize,
    pub ready_for_release_packets: usize,
    pub monitoring_packets: usize,
    pub platform_owner_packets: usize,
    pub integration_owner_packets: usize,
    pub security_owner_packets: usize,
    pub reviewer_owner_packets: usize,
    pub verification_owner_packets: usize,
    pub audit_owner_packets: usize,
    pub packets_with_dependency_work: usize,
    pub packets_with_policy_risk: usize,
    pub packets_requiring_verification: usize,
    pub packets_ready_for_release: usize,
    pub packets_with_policy_surface: usize,
    pub next_release_status: Option<IntegrationActivationReleaseStatus>,
    pub next_closure_status: Option<IntegrationActivationClosureStatus>,
    pub next_owner_lane: Option<IntegrationActivationResponseOwnerLane>,
    pub next_recommended_view: Option<IntegrationActivationPlaybookView>,
    pub next_packet_sequence: Option<usize>,
    pub next_packet_priority: Option<u8>,
    pub first_attention_priority: Option<u8>,
    pub highest_policy_tier: PrivilegeTier,
    pub overall_status: IntegrationActivationHealthStatus,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationDeliveryManifest {
    pub sequence: usize,
    pub delivery_status: IntegrationActivationDeliveryStatus,
    pub delivery_channel: IntegrationActivationDeliveryChannel,
    pub owner_lane: IntegrationActivationResponseOwnerLane,
    pub source_release_sequence: usize,
    pub source_release_status: IntegrationActivationReleaseStatus,
    pub source_closure_status: IntegrationActivationClosureStatus,
    pub source_remediation_kind: IntegrationActivationRemediationKind,
    pub source_remediation_status: IntegrationActivationRemediationStatus,
    pub source_id: String,
    pub title: String,
    pub summary: String,
    pub priority: u8,
    pub integration_ids: Vec<IntegrationId>,
    pub recommended_view: IntegrationActivationPlaybookView,
    pub required_tier: PrivilegeTier,
    pub policy_surface: Option<IntegrationPolicySurface>,
    pub dependency_work: bool,
    pub policy_risk: bool,
    pub verification_required: bool,
    pub release_ready: bool,
    pub delivery_blocked: bool,
    pub delivery_ready: bool,
    pub requires_attention: bool,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationDeliverySummary {
    pub total_manifests: usize,
    pub unique_integrations: usize,
    pub manifests_requiring_attention: usize,
    pub blocked_manifests: usize,
    pub awaiting_verification_manifests: usize,
    pub awaiting_owner_manifests: usize,
    pub ready_to_deliver_manifests: usize,
    pub monitoring_manifests: usize,
    pub platform_channel_manifests: usize,
    pub integration_channel_manifests: usize,
    pub security_channel_manifests: usize,
    pub verification_channel_manifests: usize,
    pub audit_channel_manifests: usize,
    pub monitoring_channel_manifests: usize,
    pub platform_owner_manifests: usize,
    pub integration_owner_manifests: usize,
    pub security_owner_manifests: usize,
    pub reviewer_owner_manifests: usize,
    pub verification_owner_manifests: usize,
    pub audit_owner_manifests: usize,
    pub manifests_with_dependency_work: usize,
    pub manifests_with_policy_risk: usize,
    pub manifests_requiring_verification: usize,
    pub manifests_ready_to_deliver: usize,
    pub manifests_with_policy_surface: usize,
    pub next_delivery_status: Option<IntegrationActivationDeliveryStatus>,
    pub next_delivery_channel: Option<IntegrationActivationDeliveryChannel>,
    pub next_release_status: Option<IntegrationActivationReleaseStatus>,
    pub next_owner_lane: Option<IntegrationActivationResponseOwnerLane>,
    pub next_recommended_view: Option<IntegrationActivationPlaybookView>,
    pub next_manifest_sequence: Option<usize>,
    pub next_manifest_priority: Option<u8>,
    pub first_attention_priority: Option<u8>,
    pub highest_policy_tier: PrivilegeTier,
    pub overall_status: IntegrationActivationHealthStatus,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationDeploymentRecord {
    pub sequence: usize,
    pub deployment_status: IntegrationActivationDeploymentStatus,
    pub deployment_ring: IntegrationActivationDeploymentRing,
    pub delivery_channel: IntegrationActivationDeliveryChannel,
    pub owner_lane: IntegrationActivationResponseOwnerLane,
    pub source_delivery_sequence: usize,
    pub source_delivery_status: IntegrationActivationDeliveryStatus,
    pub source_release_status: IntegrationActivationReleaseStatus,
    pub source_closure_status: IntegrationActivationClosureStatus,
    pub source_remediation_kind: IntegrationActivationRemediationKind,
    pub source_remediation_status: IntegrationActivationRemediationStatus,
    pub source_id: String,
    pub title: String,
    pub summary: String,
    pub priority: u8,
    pub integration_ids: Vec<IntegrationId>,
    pub recommended_view: IntegrationActivationPlaybookView,
    pub required_tier: PrivilegeTier,
    pub policy_surface: Option<IntegrationPolicySurface>,
    pub dependency_work: bool,
    pub policy_risk: bool,
    pub verification_required: bool,
    pub delivery_ready: bool,
    pub deployment_blocked: bool,
    pub deployment_ready: bool,
    pub requires_attention: bool,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationDeploymentSummary {
    pub total_records: usize,
    pub unique_integrations: usize,
    pub records_requiring_attention: usize,
    pub blocked_records: usize,
    pub awaiting_verification_records: usize,
    pub awaiting_owner_records: usize,
    pub ready_to_deploy_records: usize,
    pub monitoring_records: usize,
    pub platform_ring_records: usize,
    pub integration_ring_records: usize,
    pub security_ring_records: usize,
    pub verification_ring_records: usize,
    pub audit_ring_records: usize,
    pub monitoring_ring_records: usize,
    pub platform_owner_records: usize,
    pub integration_owner_records: usize,
    pub security_owner_records: usize,
    pub reviewer_owner_records: usize,
    pub verification_owner_records: usize,
    pub audit_owner_records: usize,
    pub records_with_dependency_work: usize,
    pub records_with_policy_risk: usize,
    pub records_requiring_verification: usize,
    pub records_ready_to_deploy: usize,
    pub records_with_policy_surface: usize,
    pub next_deployment_status: Option<IntegrationActivationDeploymentStatus>,
    pub next_deployment_ring: Option<IntegrationActivationDeploymentRing>,
    pub next_delivery_status: Option<IntegrationActivationDeliveryStatus>,
    pub next_owner_lane: Option<IntegrationActivationResponseOwnerLane>,
    pub next_recommended_view: Option<IntegrationActivationPlaybookView>,
    pub next_record_sequence: Option<usize>,
    pub next_record_priority: Option<u8>,
    pub first_attention_priority: Option<u8>,
    pub highest_policy_tier: PrivilegeTier,
    pub overall_status: IntegrationActivationHealthStatus,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationActivationSafetyGateStatus {
    Blocked,
    NeedsVerification,
    NeedsOwnerApproval,
    ReadyToDeploy,
    Monitoring,
}

impl IntegrationActivationSafetyGateStatus {
    pub fn as_str(self) -> &'static str {
        match self {
            Self::Blocked => "blocked",
            Self::NeedsVerification => "needs_verification",
            Self::NeedsOwnerApproval => "needs_owner_approval",
            Self::ReadyToDeploy => "ready_to_deploy",
            Self::Monitoring => "monitoring",
        }
    }

    pub fn from_deployment_status(status: IntegrationActivationDeploymentStatus) -> Self {
        match status {
            IntegrationActivationDeploymentStatus::Blocked => Self::Blocked,
            IntegrationActivationDeploymentStatus::AwaitingVerification => Self::NeedsVerification,
            IntegrationActivationDeploymentStatus::AwaitingOwner => Self::NeedsOwnerApproval,
            IntegrationActivationDeploymentStatus::ReadyToDeploy => Self::ReadyToDeploy,
            IntegrationActivationDeploymentStatus::Monitoring => Self::Monitoring,
        }
    }

    pub fn requires_attention(self) -> bool {
        matches!(
            self,
            Self::Blocked | Self::NeedsVerification | Self::NeedsOwnerApproval
        )
    }

    pub fn blocks_activation(self) -> bool {
        matches!(self, Self::Blocked)
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationSafetyGate {
    pub sequence: usize,
    pub gate_status: IntegrationActivationSafetyGateStatus,
    pub deployment_status: IntegrationActivationDeploymentStatus,
    pub deployment_ring: IntegrationActivationDeploymentRing,
    pub delivery_channel: IntegrationActivationDeliveryChannel,
    pub owner_lane: IntegrationActivationResponseOwnerLane,
    pub source_deployment_sequence: usize,
    pub source_delivery_sequence: usize,
    pub source_id: String,
    pub title: String,
    pub summary: String,
    pub priority: u8,
    pub integration_ids: Vec<IntegrationId>,
    pub recommended_view: IntegrationActivationPlaybookView,
    pub required_tier: PrivilegeTier,
    pub policy_surface: Option<IntegrationPolicySurface>,
    pub dependency_work: bool,
    pub policy_risk: bool,
    pub verification_required: bool,
    pub deployment_ready: bool,
    pub blocks_activation: bool,
    pub requires_attention: bool,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationSafetySummary {
    pub total_gates: usize,
    pub unique_integrations: usize,
    pub gates_requiring_attention: usize,
    pub blocked_gates: usize,
    pub verification_gates: usize,
    pub owner_approval_gates: usize,
    pub ready_to_deploy_gates: usize,
    pub monitoring_gates: usize,
    pub platform_ring_gates: usize,
    pub integration_ring_gates: usize,
    pub security_ring_gates: usize,
    pub verification_ring_gates: usize,
    pub audit_ring_gates: usize,
    pub monitoring_ring_gates: usize,
    pub gates_with_dependency_work: usize,
    pub gates_with_policy_risk: usize,
    pub gates_requiring_verification: usize,
    pub gates_ready_to_deploy: usize,
    pub gates_with_policy_surface: usize,
    pub next_gate_status: Option<IntegrationActivationSafetyGateStatus>,
    pub next_deployment_ring: Option<IntegrationActivationDeploymentRing>,
    pub next_owner_lane: Option<IntegrationActivationResponseOwnerLane>,
    pub next_recommended_view: Option<IntegrationActivationPlaybookView>,
    pub next_gate_sequence: Option<usize>,
    pub next_gate_priority: Option<u8>,
    pub first_attention_priority: Option<u8>,
    pub highest_policy_tier: PrivilegeTier,
    pub overall_status: IntegrationActivationHealthStatus,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationActivationRollbackAction {
    HoldDeployment,
    VerifyBeforeRollback,
    RequestOwnerApproval,
    StageRollbackPlan,
    MonitorRollbackWindow,
}

impl IntegrationActivationRollbackAction {
    pub fn as_str(self) -> &'static str {
        match self {
            Self::HoldDeployment => "hold_deployment",
            Self::VerifyBeforeRollback => "verify_before_rollback",
            Self::RequestOwnerApproval => "request_owner_approval",
            Self::StageRollbackPlan => "stage_rollback_plan",
            Self::MonitorRollbackWindow => "monitor_rollback_window",
        }
    }

    pub fn from_gate_status(status: IntegrationActivationSafetyGateStatus) -> Self {
        match status {
            IntegrationActivationSafetyGateStatus::Blocked => Self::HoldDeployment,
            IntegrationActivationSafetyGateStatus::NeedsVerification => Self::VerifyBeforeRollback,
            IntegrationActivationSafetyGateStatus::NeedsOwnerApproval => Self::RequestOwnerApproval,
            IntegrationActivationSafetyGateStatus::ReadyToDeploy => Self::StageRollbackPlan,
            IntegrationActivationSafetyGateStatus::Monitoring => Self::MonitorRollbackWindow,
        }
    }

    pub fn requires_attention(self) -> bool {
        matches!(
            self,
            Self::HoldDeployment | Self::VerifyBeforeRollback | Self::RequestOwnerApproval
        )
    }

    pub fn rollback_ready(self) -> bool {
        matches!(self, Self::StageRollbackPlan | Self::MonitorRollbackWindow)
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationRollbackPlan {
    pub sequence: usize,
    pub rollback_action: IntegrationActivationRollbackAction,
    pub gate_status: IntegrationActivationSafetyGateStatus,
    pub deployment_status: IntegrationActivationDeploymentStatus,
    pub deployment_ring: IntegrationActivationDeploymentRing,
    pub delivery_channel: IntegrationActivationDeliveryChannel,
    pub owner_lane: IntegrationActivationResponseOwnerLane,
    pub source_safety_sequence: usize,
    pub source_deployment_sequence: usize,
    pub source_delivery_sequence: usize,
    pub source_id: String,
    pub title: String,
    pub summary: String,
    pub priority: u8,
    pub integration_ids: Vec<IntegrationId>,
    pub recommended_view: IntegrationActivationPlaybookView,
    pub required_tier: PrivilegeTier,
    pub policy_surface: Option<IntegrationPolicySurface>,
    pub dependency_work: bool,
    pub policy_risk: bool,
    pub verification_required: bool,
    pub deployment_ready: bool,
    pub rollback_ready: bool,
    pub blocks_activation: bool,
    pub requires_attention: bool,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationRollbackSummary {
    pub total_plans: usize,
    pub unique_integrations: usize,
    pub plans_requiring_attention: usize,
    pub hold_deployment_plans: usize,
    pub verification_plans: usize,
    pub owner_approval_plans: usize,
    pub staged_rollback_plans: usize,
    pub monitoring_plans: usize,
    pub platform_ring_plans: usize,
    pub integration_ring_plans: usize,
    pub security_ring_plans: usize,
    pub verification_ring_plans: usize,
    pub audit_ring_plans: usize,
    pub monitoring_ring_plans: usize,
    pub plans_with_dependency_work: usize,
    pub plans_with_policy_risk: usize,
    pub plans_requiring_verification: usize,
    pub plans_ready_for_deployment: usize,
    pub plans_ready_for_rollback: usize,
    pub plans_blocking_activation: usize,
    pub plans_with_policy_surface: usize,
    pub next_rollback_action: Option<IntegrationActivationRollbackAction>,
    pub next_gate_status: Option<IntegrationActivationSafetyGateStatus>,
    pub next_deployment_ring: Option<IntegrationActivationDeploymentRing>,
    pub next_owner_lane: Option<IntegrationActivationResponseOwnerLane>,
    pub next_recommended_view: Option<IntegrationActivationPlaybookView>,
    pub next_plan_sequence: Option<usize>,
    pub next_plan_priority: Option<u8>,
    pub first_attention_priority: Option<u8>,
    pub highest_policy_tier: PrivilegeTier,
    pub overall_status: IntegrationActivationHealthStatus,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationActivationObservabilityStatus {
    Blocked,
    NeedsTelemetryReview,
    NeedsRollbackCoverage,
    ReadyToObserve,
    Monitoring,
}

impl IntegrationActivationObservabilityStatus {
    pub fn as_str(self) -> &'static str {
        match self {
            Self::Blocked => "blocked",
            Self::NeedsTelemetryReview => "needs_telemetry_review",
            Self::NeedsRollbackCoverage => "needs_rollback_coverage",
            Self::ReadyToObserve => "ready_to_observe",
            Self::Monitoring => "monitoring",
        }
    }

    pub fn requires_attention(self) -> bool {
        matches!(
            self,
            Self::Blocked | Self::NeedsTelemetryReview | Self::NeedsRollbackCoverage
        )
    }

    pub fn observability_ready(self) -> bool {
        matches!(self, Self::ReadyToObserve | Self::Monitoring)
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationObservabilityProbe {
    pub sequence: usize,
    pub observability_status: IntegrationActivationObservabilityStatus,
    pub rollback_action: IntegrationActivationRollbackAction,
    pub gate_status: IntegrationActivationSafetyGateStatus,
    pub deployment_ring: IntegrationActivationDeploymentRing,
    pub owner_lane: IntegrationActivationResponseOwnerLane,
    pub source_rollback_sequence: usize,
    pub source_safety_sequence: usize,
    pub source_deployment_sequence: usize,
    pub source_id: String,
    pub title: String,
    pub summary: String,
    pub priority: u8,
    pub integration_ids: Vec<IntegrationId>,
    pub recommended_view: IntegrationActivationPlaybookView,
    pub required_tier: PrivilegeTier,
    pub policy_surface: Option<IntegrationPolicySurface>,
    pub watchtower_signal_count: usize,
    pub has_observation_signal: bool,
    pub watchtower_requires_attention: bool,
    pub rollback_ready: bool,
    pub deployment_ready: bool,
    pub blocks_activation: bool,
    pub needs_verification: bool,
    pub requires_attention: bool,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationObservabilitySummary {
    pub total_probes: usize,
    pub unique_integrations: usize,
    pub probes_requiring_attention: usize,
    pub blocked_probes: usize,
    pub telemetry_review_probes: usize,
    pub rollback_coverage_probes: usize,
    pub ready_to_observe_probes: usize,
    pub monitoring_probes: usize,
    pub probes_with_watchtower_signals: usize,
    pub probes_with_observation_signals: usize,
    pub probes_with_watchtower_attention: usize,
    pub probes_ready_for_rollback: usize,
    pub probes_ready_for_deployment: usize,
    pub probes_blocking_activation: usize,
    pub probes_needing_verification: usize,
    pub probes_with_policy_surface: usize,
    pub next_observability_status: Option<IntegrationActivationObservabilityStatus>,
    pub next_rollback_action: Option<IntegrationActivationRollbackAction>,
    pub next_gate_status: Option<IntegrationActivationSafetyGateStatus>,
    pub next_deployment_ring: Option<IntegrationActivationDeploymentRing>,
    pub next_owner_lane: Option<IntegrationActivationResponseOwnerLane>,
    pub next_recommended_view: Option<IntegrationActivationPlaybookView>,
    pub next_probe_sequence: Option<usize>,
    pub next_probe_priority: Option<u8>,
    pub first_attention_priority: Option<u8>,
    pub highest_policy_tier: PrivilegeTier,
    pub overall_status: IntegrationActivationHealthStatus,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationActivationIncidentSeverity {
    Critical,
    High,
    Medium,
    Watch,
}

impl IntegrationActivationIncidentSeverity {
    pub fn as_str(self) -> &'static str {
        match self {
            Self::Critical => "critical",
            Self::High => "high",
            Self::Medium => "medium",
            Self::Watch => "watch",
        }
    }

    pub fn requires_attention(self) -> bool {
        !matches!(self, Self::Watch)
    }

    pub fn blocks_activation(self) -> bool {
        matches!(self, Self::Critical)
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationActivationIncidentAction {
    HoldActivation,
    TriageTelemetry,
    CompleteRollbackCoverage,
    ArmObservation,
    ContinueMonitoring,
}

impl IntegrationActivationIncidentAction {
    pub fn as_str(self) -> &'static str {
        match self {
            Self::HoldActivation => "hold_activation",
            Self::TriageTelemetry => "triage_telemetry",
            Self::CompleteRollbackCoverage => "complete_rollback_coverage",
            Self::ArmObservation => "arm_observation",
            Self::ContinueMonitoring => "continue_monitoring",
        }
    }

    pub fn requires_attention(self) -> bool {
        matches!(
            self,
            Self::HoldActivation | Self::TriageTelemetry | Self::CompleteRollbackCoverage
        )
    }

    pub fn incident_ready(self) -> bool {
        matches!(self, Self::ArmObservation | Self::ContinueMonitoring)
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationActivationGuardrailKind {
    Incident,
    PolicyRisk,
    Dependency,
    ReadinessGap,
}

impl IntegrationActivationGuardrailKind {
    pub fn as_str(self) -> &'static str {
        match self {
            Self::Incident => "incident",
            Self::PolicyRisk => "policy_risk",
            Self::Dependency => "dependency",
            Self::ReadinessGap => "readiness_gap",
        }
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub enum IntegrationActivationGuardrailVerdict {
    Pass,
    Monitor,
    NeedsReview,
    Blocked,
}

impl IntegrationActivationGuardrailVerdict {
    pub fn as_str(self) -> &'static str {
        match self {
            Self::Pass => "pass",
            Self::Monitor => "monitor",
            Self::NeedsReview => "needs_review",
            Self::Blocked => "blocked",
        }
    }

    pub fn blocks_activation(self) -> bool {
        matches!(self, Self::Blocked)
    }

    pub fn requires_attention(self) -> bool {
        matches!(self, Self::NeedsReview | Self::Blocked)
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationIncidentBrief {
    pub sequence: usize,
    pub severity: IntegrationActivationIncidentSeverity,
    pub action: IntegrationActivationIncidentAction,
    pub observability_status: IntegrationActivationObservabilityStatus,
    pub rollback_action: IntegrationActivationRollbackAction,
    pub gate_status: IntegrationActivationSafetyGateStatus,
    pub deployment_ring: IntegrationActivationDeploymentRing,
    pub owner_lane: IntegrationActivationResponseOwnerLane,
    pub source_observability_sequence: usize,
    pub source_rollback_sequence: usize,
    pub source_safety_sequence: usize,
    pub source_deployment_sequence: usize,
    pub source_id: String,
    pub title: String,
    pub summary: String,
    pub priority: u8,
    pub integration_ids: Vec<IntegrationId>,
    pub recommended_view: IntegrationActivationPlaybookView,
    pub required_tier: PrivilegeTier,
    pub policy_surface: Option<IntegrationPolicySurface>,
    pub watchtower_signal_count: usize,
    pub has_observation_signal: bool,
    pub watchtower_requires_attention: bool,
    pub rollback_ready: bool,
    pub deployment_ready: bool,
    pub blocks_activation: bool,
    pub needs_verification: bool,
    pub requires_attention: bool,
    pub incident_ready: bool,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationIncidentSummary {
    pub total_briefs: usize,
    pub unique_integrations: usize,
    pub briefs_requiring_attention: usize,
    pub critical_briefs: usize,
    pub high_briefs: usize,
    pub medium_briefs: usize,
    pub watch_briefs: usize,
    pub hold_activation_briefs: usize,
    pub telemetry_triage_briefs: usize,
    pub rollback_coverage_briefs: usize,
    pub arm_observation_briefs: usize,
    pub monitoring_briefs: usize,
    pub briefs_with_watchtower_signals: usize,
    pub briefs_with_observation_signals: usize,
    pub briefs_with_watchtower_attention: usize,
    pub briefs_ready_for_rollback: usize,
    pub briefs_ready_for_deployment: usize,
    pub briefs_blocking_activation: usize,
    pub briefs_needing_verification: usize,
    pub briefs_with_policy_surface: usize,
    pub briefs_incident_ready: usize,
    pub next_severity: Option<IntegrationActivationIncidentSeverity>,
    pub next_action: Option<IntegrationActivationIncidentAction>,
    pub next_observability_status: Option<IntegrationActivationObservabilityStatus>,
    pub next_rollback_action: Option<IntegrationActivationRollbackAction>,
    pub next_owner_lane: Option<IntegrationActivationResponseOwnerLane>,
    pub next_recommended_view: Option<IntegrationActivationPlaybookView>,
    pub next_brief_sequence: Option<usize>,
    pub next_brief_priority: Option<u8>,
    pub first_attention_priority: Option<u8>,
    pub highest_policy_tier: PrivilegeTier,
    pub overall_status: IntegrationActivationHealthStatus,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationGuardrailCheck {
    pub sequence: usize,
    pub guardrail_id: String,
    pub kind: IntegrationActivationGuardrailKind,
    pub verdict: IntegrationActivationGuardrailVerdict,
    pub title: String,
    pub summary: String,
    pub priority: u8,
    pub integration_ids: Vec<IntegrationId>,
    pub required_tier: PrivilegeTier,
    pub policy_surface: Option<IntegrationPolicySurface>,
    pub incident_severity: Option<IntegrationActivationIncidentSeverity>,
    pub incident_action: Option<IntegrationActivationIncidentAction>,
    pub risk_kind: Option<IntegrationActivationRiskKind>,
    pub dependency_integration_id: Option<IntegrationId>,
    pub dependent_integration_id: Option<IntegrationId>,
    pub readiness_gap_kind: Option<String>,
    pub blocks_activation: bool,
    pub requires_attention: bool,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct IntegrationActivationGuardrailSummary {
    pub total_checks: usize,
    pub unique_integrations: usize,
    pub checks_requiring_attention: usize,
    pub blocked_checks: usize,
    pub needs_review_checks: usize,
    pub monitor_checks: usize,
    pub pass_checks: usize,
    pub incident_checks: usize,
    pub policy_risk_checks: usize,
    pub dependency_checks: usize,
    pub readiness_gap_checks: usize,
    pub checks_with_policy_surface: usize,
    pub first_attention_priority: Option<u8>,
    pub first_blocked_priority: Option<u8>,
    pub first_review_priority: Option<u8>,
    pub highest_policy_tier: PrivilegeTier,
    pub overall_status: IntegrationActivationHealthStatus,
}

impl IntegrationActivationPlanSummary {
    pub fn from_plans<'a>(plans: impl IntoIterator<Item = &'a IntegrationActivationPlan>) -> Self {
        let mut summary = Self {
            total_plans: 0,
            direct_targets: 0,
            delegated_integration_targets: 0,
            delegated_standard_targets: 0,
            plans_requiring_human_review: 0,
            local_only_plans: 0,
            cloud_required_plans: 0,
            plans_with_dependencies: 0,
            plans_with_required_primitives: 0,
            plans_with_required_capabilities: 0,
            unique_required_primitives: 0,
            unique_required_capabilities: 0,
            unique_dependencies: 0,
            highest_policy_tier: PrivilegeTier::ReadOnly,
        };
        let mut required_primitives = BTreeSet::new();
        let mut required_capabilities = BTreeSet::new();
        let mut dependencies = BTreeSet::new();

        for plan in plans {
            summary.total_plans += 1;
            match &plan.activation_target {
                IntegrationActivationTarget::Direct => summary.direct_targets += 1,
                IntegrationActivationTarget::DelegatedIntegration(_) => {
                    summary.delegated_integration_targets += 1
                }
                IntegrationActivationTarget::DelegatedStandards(_) => {
                    summary.delegated_standard_targets += 1
                }
            }
            if plan.requires_human_review() {
                summary.plans_requiring_human_review += 1;
            }
            if plan.local_only {
                summary.local_only_plans += 1;
            }
            if plan.cloud_required {
                summary.cloud_required_plans += 1;
            }
            if !plan.depends_on_integrations.is_empty() {
                summary.plans_with_dependencies += 1;
            }
            if !plan.required_primitives.is_empty() {
                summary.plans_with_required_primitives += 1;
            }
            if !plan.required_capabilities.is_empty() {
                summary.plans_with_required_capabilities += 1;
            }
            for primitive in &plan.required_primitives {
                required_primitives.insert(*primitive);
            }
            for capability_id in &plan.required_capabilities {
                required_capabilities.insert(capability_id.clone());
            }
            for integration_id in &plan.depends_on_integrations {
                dependencies.insert(integration_id.clone());
            }
            summary.highest_policy_tier = summary.highest_policy_tier.max(plan.highest_policy_tier);
        }

        summary.unique_required_primitives = required_primitives.len();
        summary.unique_required_capabilities = required_capabilities.len();
        summary.unique_dependencies = dependencies.len();
        summary
    }

    pub fn is_empty(&self) -> bool {
        self.total_plans == 0
    }

    pub fn has_delegated_targets(&self) -> bool {
        self.delegated_integration_targets > 0 || self.delegated_standard_targets > 0
    }

    pub fn has_review_work(&self) -> bool {
        self.plans_requiring_human_review > 0
    }
}

impl IntegrationReadinessSummary {
    pub fn from_reports<'a>(
        reports: impl IntoIterator<Item = &'a IntegrationReadinessReport>,
    ) -> Self {
        let mut summary = Self {
            total_reports: 0,
            activation_ready_reports: 0,
            blocked_reports: 0,
            reports_requiring_human_review: 0,
            cloud_required_reports: 0,
            local_only_reports: 0,
            direct_targets: 0,
            delegated_integration_targets: 0,
            delegated_standard_targets: 0,
            reports_missing_primitives: 0,
            reports_missing_capabilities: 0,
            reports_missing_dependencies: 0,
            unique_missing_primitives: 0,
            unique_missing_capabilities: 0,
            unique_missing_dependencies: 0,
            highest_policy_tier: PrivilegeTier::ReadOnly,
        };
        let mut missing_primitives = BTreeSet::new();
        let mut missing_capabilities = BTreeSet::new();
        let mut missing_dependencies = BTreeSet::new();

        for report in reports {
            summary.total_reports += 1;
            if report.activation_ready() {
                summary.activation_ready_reports += 1;
            } else {
                summary.blocked_reports += 1;
            }
            if report.requires_human_review {
                summary.reports_requiring_human_review += 1;
            }
            if report.cloud_required {
                summary.cloud_required_reports += 1;
            }
            if report.local_only {
                summary.local_only_reports += 1;
            }
            match &report.activation_target {
                IntegrationActivationTarget::Direct => summary.direct_targets += 1,
                IntegrationActivationTarget::DelegatedIntegration(_) => {
                    summary.delegated_integration_targets += 1
                }
                IntegrationActivationTarget::DelegatedStandards(_) => {
                    summary.delegated_standard_targets += 1
                }
            }
            if !report.missing_primitives.is_empty() {
                summary.reports_missing_primitives += 1;
            }
            if !report.missing_capabilities.is_empty() {
                summary.reports_missing_capabilities += 1;
            }
            if !report.missing_dependencies.is_empty() {
                summary.reports_missing_dependencies += 1;
            }
            for primitive in &report.missing_primitives {
                missing_primitives.insert(*primitive);
            }
            for capability_id in &report.missing_capabilities {
                missing_capabilities.insert(capability_id.clone());
            }
            for integration_id in &report.missing_dependencies {
                missing_dependencies.insert(integration_id.clone());
            }
            summary.highest_policy_tier =
                summary.highest_policy_tier.max(report.highest_policy_tier);
        }

        summary.unique_missing_primitives = missing_primitives.len();
        summary.unique_missing_capabilities = missing_capabilities.len();
        summary.unique_missing_dependencies = missing_dependencies.len();
        summary
    }

    pub fn is_empty(&self) -> bool {
        self.total_reports == 0
    }

    pub fn all_ready(&self) -> bool {
        self.total_reports > 0 && self.blocked_reports == 0
    }

    pub fn has_blockers(&self) -> bool {
        self.blocked_reports > 0
    }
}

impl IntegrationReadinessGapInventory {
    pub fn is_empty(&self) -> bool {
        self.total_reports == 0
    }

    pub fn has_gaps(&self) -> bool {
        self.total_unique_gaps() > 0
    }

    pub fn all_ready(&self) -> bool {
        self.total_reports > 0 && !self.has_gaps()
    }

    pub fn primitive_gap_count(&self) -> usize {
        self.primitive_gaps.len()
    }

    pub fn capability_gap_count(&self) -> usize {
        self.capability_gaps.len()
    }

    pub fn dependency_gap_count(&self) -> usize {
        self.dependency_gaps.len()
    }

    pub fn total_unique_gaps(&self) -> usize {
        self.primitive_gap_count() + self.capability_gap_count() + self.dependency_gap_count()
    }
}

impl IntegrationActivationDependencySummary {
    pub fn from_graph(
        nodes: &[IntegrationActivationDependencyNode],
        edges: &[IntegrationActivationDependencyEdge],
    ) -> Self {
        let mut summary = Self {
            total_nodes: nodes.len(),
            enabled_nodes: 0,
            activation_ready_nodes: 0,
            blocked_nodes: 0,
            nodes_with_dependencies: 0,
            nodes_with_dependents: 0,
            nodes_with_missing_dependencies: 0,
            total_edges: edges.len(),
            satisfied_edges: 0,
            blocking_edges: 0,
            unknown_dependency_edges: 0,
            first_blocked_priority: None,
            highest_policy_tier: PrivilegeTier::ReadOnly,
        };

        for node in nodes {
            if node.enabled {
                summary.enabled_nodes += 1;
            }
            if node.activation_ready {
                summary.activation_ready_nodes += 1;
            } else {
                summary.blocked_nodes += 1;
                summary.first_blocked_priority = Some(
                    summary
                        .first_blocked_priority
                        .map_or(node.priority, |priority| priority.min(node.priority)),
                );
            }
            if !node.depends_on_integrations.is_empty() {
                summary.nodes_with_dependencies += 1;
            }
            if !node.dependent_integration_ids.is_empty() {
                summary.nodes_with_dependents += 1;
            }
            if !node.missing_dependencies.is_empty() {
                summary.nodes_with_missing_dependencies += 1;
            }
            summary.highest_policy_tier = summary.highest_policy_tier.max(node.highest_policy_tier);
        }

        for edge in edges {
            if edge.satisfied {
                summary.satisfied_edges += 1;
            }
            if edge.blocks_activation {
                summary.blocking_edges += 1;
            }
            if edge.dependency_display_name.is_none() {
                summary.unknown_dependency_edges += 1;
            }
        }

        summary
    }

    pub fn is_empty(&self) -> bool {
        self.total_nodes == 0
    }

    pub fn has_dependency_edges(&self) -> bool {
        self.total_edges > 0
    }

    pub fn has_blocking_dependencies(&self) -> bool {
        self.blocking_edges > 0
    }
}

impl IntegrationActivationDependencyGraph {
    pub fn is_empty(&self) -> bool {
        self.summary.is_empty()
    }

    pub fn has_blocking_dependencies(&self) -> bool {
        self.summary.has_blocking_dependencies()
    }
}

impl IntegrationActivationHealthStage {
    pub fn from_candidates(
        priority: u8,
        mut candidates: Vec<IntegrationActivationCandidate>,
    ) -> Self {
        candidates.sort_by(compare_activation_candidates);
        let candidate_summary =
            IntegrationActivationCandidateSummary::from_candidates(candidates.iter());
        let reports = candidates
            .iter()
            .map(|candidate| candidate.readiness_report.clone())
            .collect::<Vec<_>>();
        let gap_inventory = readiness_gap_inventory_from_reports(reports.iter());
        let integration_ids = candidates
            .iter()
            .map(|candidate| candidate.readiness_report.requested_integration_id.clone())
            .collect::<Vec<_>>();
        let ready_to_activate_integration_ids = candidates
            .iter()
            .filter(|candidate| {
                candidate.recommendation
                    == IntegrationActivationCandidateRecommendation::ReadyToActivate
            })
            .map(|candidate| candidate.readiness_report.requested_integration_id.clone())
            .collect::<Vec<_>>();
        let review_integration_ids = candidates
            .iter()
            .filter(|candidate| {
                candidate.recommendation
                    == IntegrationActivationCandidateRecommendation::NeedsHumanReview
            })
            .map(|candidate| candidate.readiness_report.requested_integration_id.clone())
            .collect::<Vec<_>>();
        let blocked_integration_ids = candidates
            .iter()
            .filter(|candidate| candidate.is_blocked())
            .map(|candidate| candidate.readiness_report.requested_integration_id.clone())
            .collect::<Vec<_>>();
        let health_status = activation_health_status_for_summary(&candidate_summary);

        Self {
            priority,
            health_status,
            integration_ids,
            ready_to_activate_integration_ids,
            review_integration_ids,
            blocked_integration_ids,
            candidate_summary,
            gap_inventory,
        }
    }

    pub fn has_ready_work(&self) -> bool {
        self.candidate_summary.ready_to_activate_candidates > 0
    }

    pub fn has_review_work(&self) -> bool {
        self.candidate_summary.needs_human_review_candidates > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.candidate_summary.blocked_candidates > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.health_status.requires_attention()
    }
}

impl IntegrationActivationHealthSummary {
    pub fn from_stages<'a>(
        stages: impl IntoIterator<Item = &'a IntegrationActivationHealthStage>,
    ) -> Self {
        let mut primitive_gaps = BTreeSet::new();
        let mut capability_gaps = BTreeSet::new();
        let mut dependency_gaps = BTreeSet::new();
        let mut summary = Self {
            total_stages: 0,
            total_integrations: 0,
            ready_stages: 0,
            review_stages: 0,
            blocked_stages: 0,
            empty_stages: 0,
            activation_ready_integrations: 0,
            ready_to_activate_integrations: 0,
            review_integrations: 0,
            blocked_integrations: 0,
            primitive_gap_count: 0,
            capability_gap_count: 0,
            dependency_gap_count: 0,
            total_unique_gaps: 0,
            first_ready_priority: None,
            first_review_priority: None,
            first_blocked_priority: None,
            highest_policy_tier: PrivilegeTier::ReadOnly,
            overall_status: IntegrationActivationHealthStatus::Empty,
        };

        for stage in stages {
            summary.total_stages += 1;
            summary.total_integrations += stage.candidate_summary.total_candidates;
            summary.activation_ready_integrations +=
                stage.candidate_summary.activation_ready_candidates;
            summary.ready_to_activate_integrations +=
                stage.candidate_summary.ready_to_activate_candidates;
            summary.review_integrations += stage.candidate_summary.needs_human_review_candidates;
            summary.blocked_integrations += stage.candidate_summary.blocked_candidates;
            summary.highest_policy_tier = summary
                .highest_policy_tier
                .max(stage.candidate_summary.highest_policy_tier);

            if stage.candidate_summary.is_empty() {
                summary.empty_stages += 1;
            }
            if stage.has_ready_work() {
                summary.ready_stages += 1;
                summary.first_ready_priority =
                    min_optional_priority(summary.first_ready_priority, Some(stage.priority));
            }
            if stage.has_review_work() {
                summary.review_stages += 1;
                summary.first_review_priority =
                    min_optional_priority(summary.first_review_priority, Some(stage.priority));
            }
            if stage.has_blockers() {
                summary.blocked_stages += 1;
                summary.first_blocked_priority =
                    min_optional_priority(summary.first_blocked_priority, Some(stage.priority));
            }

            for gap in &stage.gap_inventory.primitive_gaps {
                primitive_gaps.insert(gap.primitive);
            }
            for gap in &stage.gap_inventory.capability_gaps {
                capability_gaps.insert(gap.capability_id.clone());
            }
            for gap in &stage.gap_inventory.dependency_gaps {
                dependency_gaps.insert(gap.integration_id.clone());
            }
        }

        summary.primitive_gap_count = primitive_gaps.len();
        summary.capability_gap_count = capability_gaps.len();
        summary.dependency_gap_count = dependency_gaps.len();
        summary.total_unique_gaps = summary.primitive_gap_count
            + summary.capability_gap_count
            + summary.dependency_gap_count;
        summary.overall_status = activation_health_status_from_counts(
            summary.ready_to_activate_integrations,
            summary.review_integrations,
            summary.blocked_integrations,
        );
        summary
    }

    pub fn is_empty(&self) -> bool {
        self.total_stages == 0
    }

    pub fn has_ready_work(&self) -> bool {
        self.ready_to_activate_integrations > 0
    }

    pub fn has_review_work(&self) -> bool {
        self.review_integrations > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.blocked_integrations > 0 || self.total_unique_gaps > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.overall_status.requires_attention()
    }
}

impl IntegrationActivationMaintenanceWindow {
    pub fn from_candidates(
        catalog: &[IntegrationCatalogEntry],
        priority: u8,
        mut candidates: Vec<IntegrationActivationCandidate>,
        enabled_integrations: &[IntegrationId],
    ) -> Self {
        candidates.sort_by(compare_activation_candidates);
        let reports = candidates
            .iter()
            .map(|candidate| candidate.readiness_report.clone())
            .collect::<Vec<_>>();
        let integration_ids = candidates
            .iter()
            .map(|candidate| candidate.readiness_report.requested_integration_id.clone())
            .collect::<Vec<_>>();
        let ready_to_activate_integration_ids = candidates
            .iter()
            .filter(|candidate| {
                candidate.recommendation
                    == IntegrationActivationCandidateRecommendation::ReadyToActivate
            })
            .map(|candidate| candidate.readiness_report.requested_integration_id.clone())
            .collect::<Vec<_>>();
        let review_integration_ids = candidates
            .iter()
            .filter(|candidate| {
                candidate.recommendation
                    == IntegrationActivationCandidateRecommendation::NeedsHumanReview
            })
            .map(|candidate| candidate.readiness_report.requested_integration_id.clone())
            .collect::<Vec<_>>();
        let blocked_integration_ids = candidates
            .iter()
            .filter(|candidate| candidate.is_blocked())
            .map(|candidate| candidate.readiness_report.requested_integration_id.clone())
            .collect::<Vec<_>>();
        let candidate_summary =
            IntegrationActivationCandidateSummary::from_candidates(candidates.iter());
        let action_summary = IntegrationActivationActionSummary::from_actions(
            activation_actions_from_candidates(candidates.iter()).iter(),
        );
        let constraint_summary = IntegrationActivationConstraintSummary::from_constraints(
            activation_constraints_from_candidates(catalog, candidates.iter()).iter(),
        );
        let risk_summary = IntegrationActivationRiskSummary::from_risks(
            activation_risk_from_candidates(catalog, candidates.iter()).iter(),
        );
        let dependency_summary =
            activation_dependency_graph_from_reports(catalog, reports.iter(), enabled_integrations)
                .summary;
        let health_status = activation_health_status_for_summary(&candidate_summary);

        Self {
            priority,
            health_status,
            integration_ids,
            ready_to_activate_integration_ids,
            review_integration_ids,
            blocked_integration_ids,
            candidate_summary,
            action_summary,
            constraint_summary,
            risk_summary,
            dependency_summary,
        }
    }

    pub fn has_ready_work(&self) -> bool {
        self.candidate_summary.ready_to_activate_candidates > 0
    }

    pub fn has_activation_work(&self) -> bool {
        self.action_summary.has_activation_work()
    }

    pub fn has_review_work(&self) -> bool {
        self.candidate_summary.has_review_work()
            || self.action_summary.has_review_work()
            || self.constraint_summary.has_review_work()
            || self.risk_summary.has_review_work()
    }

    pub fn has_blockers(&self) -> bool {
        self.candidate_summary.has_blockers()
            || self.action_summary.has_blockers()
            || self.constraint_summary.has_blockers()
            || self.risk_summary.has_blockers()
            || self.dependency_summary.has_blocking_dependencies()
    }

    pub fn has_risks(&self) -> bool {
        !self.risk_summary.is_empty()
    }

    pub fn has_dependency_blockers(&self) -> bool {
        self.dependency_summary.has_blocking_dependencies()
    }

    pub fn requires_attention(&self) -> bool {
        self.health_status.requires_attention()
            || self.has_review_work()
            || self.has_blockers()
            || self.risk_summary.requires_attention()
    }
}

impl IntegrationActivationMaintenanceSummary {
    pub fn from_windows<'a>(
        windows: impl IntoIterator<Item = &'a IntegrationActivationMaintenanceWindow>,
    ) -> Self {
        let mut summary = Self {
            total_windows: 0,
            total_integrations: 0,
            ready_windows: 0,
            review_windows: 0,
            blocked_windows: 0,
            empty_windows: 0,
            activation_ready_integrations: 0,
            ready_to_activate_integrations: 0,
            review_integrations: 0,
            blocked_integrations: 0,
            windows_with_actions: 0,
            windows_with_activation_work: 0,
            windows_with_review_work: 0,
            windows_with_blockers: 0,
            windows_with_risks: 0,
            windows_with_dependency_blockers: 0,
            total_actions: 0,
            activate_integration_actions: 0,
            review_policy_actions: 0,
            blocking_constraints: 0,
            review_constraints: 0,
            total_risks: 0,
            total_dependency_edges: 0,
            blocking_dependency_edges: 0,
            first_ready_priority: None,
            first_review_priority: None,
            first_blocked_priority: None,
            first_activation_priority: None,
            highest_policy_tier: PrivilegeTier::ReadOnly,
            overall_status: IntegrationActivationHealthStatus::Empty,
        };

        for window in windows {
            summary.total_windows += 1;
            summary.total_integrations += window.candidate_summary.total_candidates;
            summary.activation_ready_integrations +=
                window.candidate_summary.activation_ready_candidates;
            summary.ready_to_activate_integrations +=
                window.candidate_summary.ready_to_activate_candidates;
            summary.review_integrations += window.candidate_summary.needs_human_review_candidates;
            summary.blocked_integrations += window.candidate_summary.blocked_candidates;
            summary.total_actions += window.action_summary.total_actions;
            summary.activate_integration_actions +=
                window.action_summary.activate_integration_actions;
            summary.review_policy_actions += window.action_summary.review_policy_actions;
            summary.blocking_constraints += window.constraint_summary.blocking_constraints;
            summary.review_constraints += window.constraint_summary.review_constraints;
            summary.total_risks += window.risk_summary.total_risks;
            summary.total_dependency_edges += window.dependency_summary.total_edges;
            summary.blocking_dependency_edges += window.dependency_summary.blocking_edges;
            summary.highest_policy_tier = summary
                .highest_policy_tier
                .max(window.candidate_summary.highest_policy_tier)
                .max(window.action_summary.highest_policy_tier)
                .max(window.constraint_summary.highest_policy_tier)
                .max(window.risk_summary.highest_policy_tier)
                .max(window.dependency_summary.highest_policy_tier);

            match window.health_status {
                IntegrationActivationHealthStatus::Ready => summary.ready_windows += 1,
                IntegrationActivationHealthStatus::NeedsReview => summary.review_windows += 1,
                IntegrationActivationHealthStatus::Blocked => summary.blocked_windows += 1,
                IntegrationActivationHealthStatus::Empty => summary.empty_windows += 1,
            }

            if !window.action_summary.is_empty() {
                summary.windows_with_actions += 1;
            }
            if window.has_activation_work() {
                summary.windows_with_activation_work += 1;
                summary.first_activation_priority =
                    min_optional_priority(summary.first_activation_priority, Some(window.priority));
            }
            if window.has_ready_work() {
                summary.first_ready_priority =
                    min_optional_priority(summary.first_ready_priority, Some(window.priority));
            }
            if window.has_review_work() {
                summary.windows_with_review_work += 1;
                summary.first_review_priority =
                    min_optional_priority(summary.first_review_priority, Some(window.priority));
            }
            if window.has_blockers() {
                summary.windows_with_blockers += 1;
                summary.first_blocked_priority =
                    min_optional_priority(summary.first_blocked_priority, Some(window.priority));
            }
            if window.has_risks() {
                summary.windows_with_risks += 1;
            }
            if window.has_dependency_blockers() {
                summary.windows_with_dependency_blockers += 1;
            }
        }

        summary.overall_status = activation_health_status_from_counts(
            summary.ready_to_activate_integrations,
            summary.review_integrations,
            summary.blocked_integrations,
        );
        summary
    }

    pub fn is_empty(&self) -> bool {
        self.total_windows == 0
    }

    pub fn has_activation_work(&self) -> bool {
        self.activate_integration_actions > 0
    }

    pub fn has_review_work(&self) -> bool {
        self.review_integrations > 0
            || self.review_policy_actions > 0
            || self.review_constraints > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.blocked_integrations > 0
            || self.blocking_constraints > 0
            || self.blocking_dependency_edges > 0
    }

    pub fn has_risks(&self) -> bool {
        self.total_risks > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.overall_status.requires_attention() || self.has_review_work() || self.has_blockers()
    }
}

impl IntegrationActivationReadoutStage {
    pub fn from_candidates(
        catalog: &[IntegrationCatalogEntry],
        priority: u8,
        mut candidates: Vec<IntegrationActivationCandidate>,
        enabled_integrations: &[IntegrationId],
    ) -> Self {
        candidates.sort_by(compare_activation_candidates);
        let integration_ids = candidates
            .iter()
            .map(|candidate| candidate.readiness_report.requested_integration_id.clone())
            .collect::<Vec<_>>();
        let maintenance_window = IntegrationActivationMaintenanceWindow::from_candidates(
            catalog,
            priority,
            candidates.clone(),
            enabled_integrations,
        );
        let dossiers =
            activation_dossiers_from_candidates(catalog, candidates.iter(), enabled_integrations);
        let dossier_summary = IntegrationActivationDossierSummary::from_dossiers(dossiers.iter());
        let evidence_summary = IntegrationActivationEvidenceSummary::from_evidence(
            dossiers.iter().flat_map(|dossier| dossier.evidence.iter()),
        );
        let health_status = maintenance_window.health_status;

        Self {
            priority,
            health_status,
            integration_ids,
            maintenance_window,
            dossiers,
            dossier_summary,
            evidence_summary,
        }
    }

    pub fn candidate_summary(&self) -> &IntegrationActivationCandidateSummary {
        &self.maintenance_window.candidate_summary
    }

    pub fn action_summary(&self) -> &IntegrationActivationActionSummary {
        &self.maintenance_window.action_summary
    }

    pub fn constraint_summary(&self) -> &IntegrationActivationConstraintSummary {
        &self.maintenance_window.constraint_summary
    }

    pub fn risk_summary(&self) -> &IntegrationActivationRiskSummary {
        &self.maintenance_window.risk_summary
    }

    pub fn dependency_summary(&self) -> &IntegrationActivationDependencySummary {
        &self.maintenance_window.dependency_summary
    }

    pub fn has_ready_work(&self) -> bool {
        self.maintenance_window.has_ready_work()
    }

    pub fn has_activation_work(&self) -> bool {
        self.maintenance_window.has_activation_work()
    }

    pub fn has_approval_ready_work(&self) -> bool {
        self.dossier_summary.has_approval_ready_work()
    }

    pub fn has_review_work(&self) -> bool {
        self.maintenance_window.has_review_work()
            || self.dossier_summary.has_review_work()
            || self.evidence_summary.has_review_work()
    }

    pub fn has_blockers(&self) -> bool {
        self.maintenance_window.has_blockers()
            || self.dossier_summary.has_blockers()
            || self.evidence_summary.has_blockers()
    }

    pub fn has_risks(&self) -> bool {
        self.maintenance_window.has_risks()
    }

    pub fn has_dependency_blockers(&self) -> bool {
        self.maintenance_window.has_dependency_blockers()
    }

    pub fn requires_attention(&self) -> bool {
        self.maintenance_window.requires_attention()
            || self.dossier_summary.requires_attention()
            || self.evidence_summary.requires_attention()
    }
}

impl IntegrationActivationReadoutSummary {
    pub fn from_readouts<'a>(
        readouts: impl IntoIterator<Item = &'a IntegrationActivationReadoutStage>,
    ) -> Self {
        let mut summary = Self {
            total_readouts: 0,
            total_integrations: 0,
            ready_readouts: 0,
            review_readouts: 0,
            blocked_readouts: 0,
            empty_readouts: 0,
            activation_ready_integrations: 0,
            ready_to_activate_integrations: 0,
            review_integrations: 0,
            blocked_integrations: 0,
            readouts_with_activation_work: 0,
            readouts_with_approval_work: 0,
            readouts_with_review_work: 0,
            readouts_with_blockers: 0,
            readouts_with_risks: 0,
            readouts_with_dependency_blockers: 0,
            total_actions: 0,
            activate_integration_actions: 0,
            review_policy_actions: 0,
            blocking_constraints: 0,
            review_constraints: 0,
            total_risks: 0,
            total_dependency_edges: 0,
            blocking_dependency_edges: 0,
            total_dossiers: 0,
            ready_to_approve_dossiers: 0,
            blocked_dossiers: 0,
            total_evidence: 0,
            supporting_evidence: 0,
            review_evidence: 0,
            blocking_evidence: 0,
            first_ready_priority: None,
            first_review_priority: None,
            first_blocked_priority: None,
            first_activation_priority: None,
            first_approval_priority: None,
            highest_policy_tier: PrivilegeTier::ReadOnly,
            overall_status: IntegrationActivationHealthStatus::Empty,
        };

        for readout in readouts {
            summary.total_readouts += 1;
            summary.total_integrations += readout.candidate_summary().total_candidates;
            summary.activation_ready_integrations +=
                readout.candidate_summary().activation_ready_candidates;
            summary.ready_to_activate_integrations +=
                readout.candidate_summary().ready_to_activate_candidates;
            summary.review_integrations +=
                readout.candidate_summary().needs_human_review_candidates;
            summary.blocked_integrations += readout.candidate_summary().blocked_candidates;

            summary.total_actions += readout.action_summary().total_actions;
            summary.activate_integration_actions +=
                readout.action_summary().activate_integration_actions;
            summary.review_policy_actions += readout.action_summary().review_policy_actions;
            summary.blocking_constraints += readout.constraint_summary().blocking_constraints;
            summary.review_constraints += readout.constraint_summary().review_constraints;
            summary.total_risks += readout.risk_summary().total_risks;
            summary.total_dependency_edges += readout.dependency_summary().total_edges;
            summary.blocking_dependency_edges += readout.dependency_summary().blocking_edges;

            summary.total_dossiers += readout.dossier_summary.total_dossiers;
            summary.ready_to_approve_dossiers += readout.dossier_summary.ready_to_approve_dossiers;
            summary.blocked_dossiers += readout.dossier_summary.blocked_dossiers;
            summary.total_evidence += readout.evidence_summary.total_evidence;
            summary.supporting_evidence += readout.evidence_summary.supporting_evidence;
            summary.review_evidence += readout.evidence_summary.review_evidence;
            summary.blocking_evidence += readout.evidence_summary.blocking_evidence;

            summary.highest_policy_tier = summary
                .highest_policy_tier
                .max(readout.candidate_summary().highest_policy_tier)
                .max(readout.action_summary().highest_policy_tier)
                .max(readout.constraint_summary().highest_policy_tier)
                .max(readout.risk_summary().highest_policy_tier)
                .max(readout.dependency_summary().highest_policy_tier)
                .max(readout.dossier_summary.highest_policy_tier)
                .max(readout.evidence_summary.highest_policy_tier);

            match readout.health_status {
                IntegrationActivationHealthStatus::Ready => summary.ready_readouts += 1,
                IntegrationActivationHealthStatus::NeedsReview => summary.review_readouts += 1,
                IntegrationActivationHealthStatus::Blocked => summary.blocked_readouts += 1,
                IntegrationActivationHealthStatus::Empty => summary.empty_readouts += 1,
            }

            if readout.has_activation_work() {
                summary.readouts_with_activation_work += 1;
                summary.first_activation_priority = min_optional_priority(
                    summary.first_activation_priority,
                    Some(readout.priority),
                );
            }
            if readout.has_approval_ready_work() {
                summary.readouts_with_approval_work += 1;
                summary.first_approval_priority =
                    min_optional_priority(summary.first_approval_priority, Some(readout.priority));
            }
            if readout.has_ready_work() {
                summary.first_ready_priority =
                    min_optional_priority(summary.first_ready_priority, Some(readout.priority));
            }
            if readout.has_review_work() {
                summary.readouts_with_review_work += 1;
                summary.first_review_priority =
                    min_optional_priority(summary.first_review_priority, Some(readout.priority));
            }
            if readout.has_blockers() {
                summary.readouts_with_blockers += 1;
                summary.first_blocked_priority =
                    min_optional_priority(summary.first_blocked_priority, Some(readout.priority));
            }
            if readout.has_risks() {
                summary.readouts_with_risks += 1;
            }
            if readout.has_dependency_blockers() {
                summary.readouts_with_dependency_blockers += 1;
            }
        }

        summary.overall_status = activation_health_status_from_counts(
            summary.ready_to_activate_integrations,
            summary.review_integrations,
            summary.blocked_integrations,
        );
        summary
    }

    pub fn is_empty(&self) -> bool {
        self.total_readouts == 0
    }

    pub fn has_activation_work(&self) -> bool {
        self.activate_integration_actions > 0
    }

    pub fn has_approval_ready_work(&self) -> bool {
        self.ready_to_approve_dossiers > 0
    }

    pub fn has_review_work(&self) -> bool {
        self.review_integrations > 0
            || self.review_policy_actions > 0
            || self.review_constraints > 0
            || self.review_evidence > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.blocked_integrations > 0
            || self.blocking_constraints > 0
            || self.blocking_dependency_edges > 0
            || self.blocking_evidence > 0
    }

    pub fn has_risks(&self) -> bool {
        self.total_risks > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.overall_status.requires_attention()
            || self.has_approval_ready_work()
            || self.has_review_work()
            || self.has_blockers()
    }
}

impl IntegrationActivationBriefingItem {
    fn from_readout(
        kind: IntegrationActivationBriefingItemKind,
        readout: &IntegrationActivationReadoutStage,
    ) -> Self {
        Self {
            kind,
            priority: readout.priority,
            health_status: readout.health_status,
            integration_ids: readout.integration_ids.clone(),
            action_count: readout.action_summary().total_actions,
            dossier_count: readout.dossier_summary.total_dossiers,
            evidence_count: readout.evidence_summary.total_evidence,
            risk_count: readout.risk_summary().total_risks,
            dependency_edge_count: readout.dependency_summary().total_edges,
            blocking_dependency_edge_count: readout.dependency_summary().blocking_edges,
            highest_policy_tier: readout
                .candidate_summary()
                .highest_policy_tier
                .max(readout.action_summary().highest_policy_tier)
                .max(readout.constraint_summary().highest_policy_tier)
                .max(readout.risk_summary().highest_policy_tier)
                .max(readout.dependency_summary().highest_policy_tier)
                .max(readout.dossier_summary.highest_policy_tier)
                .max(readout.evidence_summary.highest_policy_tier),
            has_activation_work: readout.has_activation_work(),
            has_approval_ready_work: readout.has_approval_ready_work(),
            has_review_work: readout.has_review_work(),
            has_blockers: readout.has_blockers(),
            has_risks: readout.has_risks(),
            has_dependency_blockers: readout.has_dependency_blockers(),
        }
    }

    pub fn integration_count(&self) -> usize {
        self.integration_ids.len()
    }

    pub fn requires_attention(&self) -> bool {
        self.health_status.requires_attention()
            || self.has_approval_ready_work
            || self.has_review_work
            || self.has_blockers
            || self.has_risks
            || self.has_dependency_blockers
    }
}

impl IntegrationActivationBriefingSummary {
    pub fn from_items<'a>(
        items: impl IntoIterator<Item = &'a IntegrationActivationBriefingItem>,
    ) -> Self {
        let mut integration_ids = BTreeSet::new();
        let mut ready_items = 0;
        let mut review_status_items = 0;
        let mut blocked_status_items = 0;
        let mut summary = Self {
            total_items: 0,
            unique_integrations: 0,
            activation_items: 0,
            approval_items: 0,
            review_items: 0,
            blocker_items: 0,
            risk_items: 0,
            dependency_items: 0,
            items_requiring_attention: 0,
            items_with_activation_work: 0,
            items_with_approval_work: 0,
            items_with_review_work: 0,
            items_with_blockers: 0,
            items_with_risks: 0,
            items_with_dependency_blockers: 0,
            total_actions: 0,
            total_dossiers: 0,
            total_evidence: 0,
            total_risks: 0,
            total_dependency_edges: 0,
            blocking_dependency_edges: 0,
            first_activation_priority: None,
            first_approval_priority: None,
            first_review_priority: None,
            first_blocked_priority: None,
            first_risk_priority: None,
            first_dependency_priority: None,
            highest_policy_tier: PrivilegeTier::ReadOnly,
            overall_status: IntegrationActivationHealthStatus::Empty,
        };

        for item in items {
            summary.total_items += 1;
            for integration_id in &item.integration_ids {
                integration_ids.insert(integration_id.clone());
            }
            summary.total_actions += item.action_count;
            summary.total_dossiers += item.dossier_count;
            summary.total_evidence += item.evidence_count;
            summary.total_risks += item.risk_count;
            summary.total_dependency_edges += item.dependency_edge_count;
            summary.blocking_dependency_edges += item.blocking_dependency_edge_count;
            summary.highest_policy_tier = summary.highest_policy_tier.max(item.highest_policy_tier);

            match item.health_status {
                IntegrationActivationHealthStatus::Ready => ready_items += 1,
                IntegrationActivationHealthStatus::NeedsReview => review_status_items += 1,
                IntegrationActivationHealthStatus::Blocked => blocked_status_items += 1,
                // `Empty` items need no separate tally: the overall status falls
                // through to `Empty` whenever no other category is present.
                IntegrationActivationHealthStatus::Empty => {}
            }

            match item.kind {
                IntegrationActivationBriefingItemKind::Activation => {
                    summary.activation_items += 1;
                    summary.first_activation_priority = min_optional_priority(
                        summary.first_activation_priority,
                        Some(item.priority),
                    );
                }
                IntegrationActivationBriefingItemKind::Approval => {
                    summary.approval_items += 1;
                    summary.first_approval_priority =
                        min_optional_priority(summary.first_approval_priority, Some(item.priority));
                }
                IntegrationActivationBriefingItemKind::Review => {
                    summary.review_items += 1;
                    summary.first_review_priority =
                        min_optional_priority(summary.first_review_priority, Some(item.priority));
                }
                IntegrationActivationBriefingItemKind::Blocker => {
                    summary.blocker_items += 1;
                    summary.first_blocked_priority =
                        min_optional_priority(summary.first_blocked_priority, Some(item.priority));
                }
                IntegrationActivationBriefingItemKind::Risk => {
                    summary.risk_items += 1;
                    summary.first_risk_priority =
                        min_optional_priority(summary.first_risk_priority, Some(item.priority));
                }
                IntegrationActivationBriefingItemKind::Dependency => {
                    summary.dependency_items += 1;
                    summary.first_dependency_priority = min_optional_priority(
                        summary.first_dependency_priority,
                        Some(item.priority),
                    );
                }
            }

            if item.requires_attention() {
                summary.items_requiring_attention += 1;
            }
            if item.has_activation_work {
                summary.items_with_activation_work += 1;
            }
            if item.has_approval_ready_work {
                summary.items_with_approval_work += 1;
            }
            if item.has_review_work {
                summary.items_with_review_work += 1;
            }
            if item.has_blockers {
                summary.items_with_blockers += 1;
            }
            if item.has_risks {
                summary.items_with_risks += 1;
            }
            if item.has_dependency_blockers {
                summary.items_with_dependency_blockers += 1;
            }
        }

        summary.unique_integrations = integration_ids.len();
        summary.overall_status = if blocked_status_items > 0 || summary.blocker_items > 0 {
            IntegrationActivationHealthStatus::Blocked
        } else if review_status_items > 0 || summary.review_items > 0 || summary.approval_items > 0
        {
            IntegrationActivationHealthStatus::NeedsReview
        } else if ready_items > 0 {
            IntegrationActivationHealthStatus::Ready
        } else {
            // Both the `empty_items > 0` case and the fall-through default map to
            // `Empty`, so they are collapsed into a single arm (behavior-identical).
            IntegrationActivationHealthStatus::Empty
        };
        summary
    }

    pub fn is_empty(&self) -> bool {
        self.total_items == 0
    }

    pub fn has_activation_work(&self) -> bool {
        self.activation_items > 0 || self.items_with_activation_work > 0
    }

    pub fn has_approval_ready_work(&self) -> bool {
        self.approval_items > 0 || self.items_with_approval_work > 0
    }

    pub fn has_review_work(&self) -> bool {
        self.review_items > 0 || self.items_with_review_work > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.blocker_items > 0 || self.items_with_blockers > 0
    }

    pub fn has_risks(&self) -> bool {
        self.risk_items > 0 || self.items_with_risks > 0
    }

    pub fn has_dependency_blockers(&self) -> bool {
        self.dependency_items > 0 || self.items_with_dependency_blockers > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.overall_status.requires_attention()
            || self.has_approval_ready_work()
            || self.has_review_work()
            || self.has_blockers()
            || self.has_risks()
            || self.has_dependency_blockers()
    }
}

impl IntegrationActivationDashboardCard {
    fn from_readout(readout: &IntegrationActivationReadoutStage) -> Self {
        let briefing_items = activation_briefing_items_from_readouts(std::iter::once(readout));
        let briefing_summary =
            IntegrationActivationBriefingSummary::from_items(briefing_items.iter());
        let next_briefing_kind = briefing_items.first().map(|item| item.kind);

        Self {
            priority: readout.priority,
            health_status: readout.health_status,
            integration_ids: readout.integration_ids.clone(),
            briefing_item_count: briefing_items.len(),
            next_briefing_kind,
            briefing_summary,
            action_count: readout.action_summary().total_actions,
            dossier_count: readout.dossier_summary.total_dossiers,
            evidence_count: readout.evidence_summary.total_evidence,
            risk_count: readout.risk_summary().total_risks,
            dependency_edge_count: readout.dependency_summary().total_edges,
            blocking_dependency_edge_count: readout.dependency_summary().blocking_edges,
            highest_policy_tier: readout
                .candidate_summary()
                .highest_policy_tier
                .max(readout.action_summary().highest_policy_tier)
                .max(readout.constraint_summary().highest_policy_tier)
                .max(readout.risk_summary().highest_policy_tier)
                .max(readout.dependency_summary().highest_policy_tier)
                .max(readout.dossier_summary.highest_policy_tier)
                .max(readout.evidence_summary.highest_policy_tier),
            has_activation_work: readout.has_activation_work(),
            has_approval_ready_work: readout.has_approval_ready_work(),
            has_review_work: readout.has_review_work(),
            has_blockers: readout.has_blockers(),
            has_risks: readout.has_risks(),
            has_dependency_blockers: readout.has_dependency_blockers(),
        }
    }

    pub fn integration_count(&self) -> usize {
        self.integration_ids.len()
    }

    pub fn requires_attention(&self) -> bool {
        self.health_status.requires_attention()
            || self.has_approval_ready_work
            || self.has_review_work
            || self.has_blockers
            || self.has_risks
            || self.has_dependency_blockers
    }
}

impl IntegrationActivationDashboardSummary {
    pub fn from_cards<'a>(
        cards: impl IntoIterator<Item = &'a IntegrationActivationDashboardCard>,
    ) -> Self {
        let mut integration_ids = BTreeSet::new();
        let mut summary = Self {
            total_cards: 0,
            unique_integrations: 0,
            ready_cards: 0,
            review_cards: 0,
            blocked_cards: 0,
            empty_cards: 0,
            cards_requiring_attention: 0,
            cards_with_activation_work: 0,
            cards_with_approval_work: 0,
            cards_with_review_work: 0,
            cards_with_blockers: 0,
            cards_with_risks: 0,
            cards_with_dependency_blockers: 0,
            total_briefing_items: 0,
            activation_items: 0,
            approval_items: 0,
            review_items: 0,
            blocker_items: 0,
            risk_items: 0,
            dependency_items: 0,
            total_actions: 0,
            total_dossiers: 0,
            total_evidence: 0,
            total_risks: 0,
            total_dependency_edges: 0,
            blocking_dependency_edges: 0,
            first_activation_priority: None,
            first_approval_priority: None,
            first_review_priority: None,
            first_blocked_priority: None,
            first_risk_priority: None,
            first_dependency_priority: None,
            first_attention_priority: None,
            highest_policy_tier: PrivilegeTier::ReadOnly,
            overall_status: IntegrationActivationHealthStatus::Empty,
        };

        for card in cards {
            summary.total_cards += 1;
            for integration_id in &card.integration_ids {
                integration_ids.insert(integration_id.clone());
            }

            match card.health_status {
                IntegrationActivationHealthStatus::Ready => summary.ready_cards += 1,
                IntegrationActivationHealthStatus::NeedsReview => summary.review_cards += 1,
                IntegrationActivationHealthStatus::Blocked => summary.blocked_cards += 1,
                IntegrationActivationHealthStatus::Empty => summary.empty_cards += 1,
            }

            summary.total_briefing_items += card.briefing_item_count;
            summary.activation_items += card.briefing_summary.activation_items;
            summary.approval_items += card.briefing_summary.approval_items;
            summary.review_items += card.briefing_summary.review_items;
            summary.blocker_items += card.briefing_summary.blocker_items;
            summary.risk_items += card.briefing_summary.risk_items;
            summary.dependency_items += card.briefing_summary.dependency_items;
            summary.total_actions += card.action_count;
            summary.total_dossiers += card.dossier_count;
            summary.total_evidence += card.evidence_count;
            summary.total_risks += card.risk_count;
            summary.total_dependency_edges += card.dependency_edge_count;
            summary.blocking_dependency_edges += card.blocking_dependency_edge_count;
            summary.highest_policy_tier = summary.highest_policy_tier.max(card.highest_policy_tier);

            if card.requires_attention() {
                summary.cards_requiring_attention += 1;
                summary.first_attention_priority =
                    min_optional_priority(summary.first_attention_priority, Some(card.priority));
            }
            if card.has_activation_work {
                summary.cards_with_activation_work += 1;
                summary.first_activation_priority =
                    min_optional_priority(summary.first_activation_priority, Some(card.priority));
            }
            if card.has_approval_ready_work {
                summary.cards_with_approval_work += 1;
                summary.first_approval_priority =
                    min_optional_priority(summary.first_approval_priority, Some(card.priority));
            }
            if card.has_review_work {
                summary.cards_with_review_work += 1;
                summary.first_review_priority =
                    min_optional_priority(summary.first_review_priority, Some(card.priority));
            }
            if card.has_blockers {
                summary.cards_with_blockers += 1;
                summary.first_blocked_priority =
                    min_optional_priority(summary.first_blocked_priority, Some(card.priority));
            }
            if card.has_risks {
                summary.cards_with_risks += 1;
                summary.first_risk_priority =
                    min_optional_priority(summary.first_risk_priority, Some(card.priority));
            }
            if card.has_dependency_blockers {
                summary.cards_with_dependency_blockers += 1;
                summary.first_dependency_priority =
                    min_optional_priority(summary.first_dependency_priority, Some(card.priority));
            }
        }

        summary.unique_integrations = integration_ids.len();
        summary.overall_status = if summary.blocked_cards > 0 || summary.cards_with_blockers > 0 {
            IntegrationActivationHealthStatus::Blocked
        } else if summary.review_cards > 0
            || summary.cards_with_review_work > 0
            || summary.cards_with_approval_work > 0
        {
            IntegrationActivationHealthStatus::NeedsReview
        } else if summary.ready_cards > 0 || summary.cards_with_activation_work > 0 {
            IntegrationActivationHealthStatus::Ready
        } else {
            IntegrationActivationHealthStatus::Empty
        };
        summary
    }

    pub fn is_empty(&self) -> bool {
        self.total_cards == 0
    }

    pub fn has_activation_work(&self) -> bool {
        self.cards_with_activation_work > 0 || self.activation_items > 0
    }

    pub fn has_approval_ready_work(&self) -> bool {
        self.cards_with_approval_work > 0 || self.approval_items > 0
    }

    pub fn has_review_work(&self) -> bool {
        self.cards_with_review_work > 0 || self.review_items > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.cards_with_blockers > 0 || self.blocker_items > 0
    }

    pub fn has_risks(&self) -> bool {
        self.cards_with_risks > 0 || self.risk_items > 0
    }

    pub fn has_dependency_blockers(&self) -> bool {
        self.cards_with_dependency_blockers > 0 || self.dependency_items > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.overall_status.requires_attention()
            || self.has_approval_ready_work()
            || self.has_review_work()
            || self.has_blockers()
            || self.has_risks()
            || self.has_dependency_blockers()
    }
}

impl IntegrationActivationTimelineMilestone {
    fn from_dashboard_card(
        sequence: usize,
        dashboard_card: IntegrationActivationDashboardCard,
    ) -> Self {
        Self {
            sequence,
            priority: dashboard_card.priority,
            milestone_kind: dashboard_card.next_briefing_kind,
            dashboard_card,
        }
    }

    pub fn integration_count(&self) -> usize {
        self.dashboard_card.integration_count()
    }

    pub fn requires_attention(&self) -> bool {
        self.dashboard_card.requires_attention()
    }

    pub fn has_activation_work(&self) -> bool {
        self.dashboard_card.has_activation_work
    }

    pub fn has_approval_ready_work(&self) -> bool {
        self.dashboard_card.has_approval_ready_work
    }

    pub fn has_review_work(&self) -> bool {
        self.dashboard_card.has_review_work
    }

    pub fn has_blockers(&self) -> bool {
        self.dashboard_card.has_blockers
    }

    pub fn has_risks(&self) -> bool {
        self.dashboard_card.has_risks
    }

    pub fn has_dependency_blockers(&self) -> bool {
        self.dashboard_card.has_dependency_blockers
    }
}

impl IntegrationActivationTimelineSummary {
    pub fn from_milestones<'a>(
        milestones: impl IntoIterator<Item = &'a IntegrationActivationTimelineMilestone>,
    ) -> Self {
        let mut integration_ids = BTreeSet::new();
        let mut summary = Self {
            total_milestones: 0,
            unique_integrations: 0,
            ready_milestones: 0,
            review_milestones: 0,
            blocked_milestones: 0,
            empty_milestones: 0,
            blocker_milestones: 0,
            review_queue_milestones: 0,
            approval_milestones: 0,
            activation_milestones: 0,
            risk_milestones: 0,
            dependency_milestones: 0,
            milestones_requiring_attention: 0,
            milestones_with_activation_work: 0,
            milestones_with_approval_work: 0,
            milestones_with_review_work: 0,
            milestones_with_blockers: 0,
            milestones_with_risks: 0,
            milestones_with_dependency_blockers: 0,
            total_briefing_items: 0,
            total_actions: 0,
            total_dossiers: 0,
            total_evidence: 0,
            total_risks: 0,
            total_dependency_edges: 0,
            blocking_dependency_edges: 0,
            first_activation_sequence: None,
            first_approval_sequence: None,
            first_review_sequence: None,
            first_blocked_sequence: None,
            first_risk_sequence: None,
            first_dependency_sequence: None,
            first_attention_sequence: None,
            first_activation_priority: None,
            first_approval_priority: None,
            first_review_priority: None,
            first_blocked_priority: None,
            first_risk_priority: None,
            first_dependency_priority: None,
            first_attention_priority: None,
            highest_policy_tier: PrivilegeTier::ReadOnly,
            overall_status: IntegrationActivationHealthStatus::Empty,
        };

        for milestone in milestones {
            summary.total_milestones += 1;
            for integration_id in &milestone.dashboard_card.integration_ids {
                integration_ids.insert(integration_id.clone());
            }

            match milestone.dashboard_card.health_status {
                IntegrationActivationHealthStatus::Ready => summary.ready_milestones += 1,
                IntegrationActivationHealthStatus::NeedsReview => summary.review_milestones += 1,
                IntegrationActivationHealthStatus::Blocked => summary.blocked_milestones += 1,
                IntegrationActivationHealthStatus::Empty => summary.empty_milestones += 1,
            }
            match milestone.milestone_kind {
                Some(IntegrationActivationBriefingItemKind::Blocker) => {
                    summary.blocker_milestones += 1
                }
                Some(IntegrationActivationBriefingItemKind::Review) => {
                    summary.review_queue_milestones += 1
                }
                Some(IntegrationActivationBriefingItemKind::Approval) => {
                    summary.approval_milestones += 1
                }
                Some(IntegrationActivationBriefingItemKind::Activation) => {
                    summary.activation_milestones += 1
                }
                Some(IntegrationActivationBriefingItemKind::Risk) => summary.risk_milestones += 1,
                Some(IntegrationActivationBriefingItemKind::Dependency) => {
                    summary.dependency_milestones += 1
                }
                None => {}
            }

            let card = &milestone.dashboard_card;
            summary.total_briefing_items += card.briefing_item_count;
            summary.total_actions += card.action_count;
            summary.total_dossiers += card.dossier_count;
            summary.total_evidence += card.evidence_count;
            summary.total_risks += card.risk_count;
            summary.total_dependency_edges += card.dependency_edge_count;
            summary.blocking_dependency_edges += card.blocking_dependency_edge_count;
            summary.highest_policy_tier = summary.highest_policy_tier.max(card.highest_policy_tier);

            if milestone.requires_attention() {
                summary.milestones_requiring_attention += 1;
                summary.first_attention_sequence = summary
                    .first_attention_sequence
                    .or(Some(milestone.sequence));
                summary.first_attention_priority = min_optional_priority(
                    summary.first_attention_priority,
                    Some(milestone.priority),
                );
            }
            if milestone.has_activation_work() {
                summary.milestones_with_activation_work += 1;
                summary.first_activation_sequence = summary
                    .first_activation_sequence
                    .or(Some(milestone.sequence));
                summary.first_activation_priority = min_optional_priority(
                    summary.first_activation_priority,
                    Some(milestone.priority),
                );
            }
            if milestone.has_approval_ready_work() {
                summary.milestones_with_approval_work += 1;
                summary.first_approval_sequence =
                    summary.first_approval_sequence.or(Some(milestone.sequence));
                summary.first_approval_priority = min_optional_priority(
                    summary.first_approval_priority,
                    Some(milestone.priority),
                );
            }
            if milestone.has_review_work() {
                summary.milestones_with_review_work += 1;
                summary.first_review_sequence =
                    summary.first_review_sequence.or(Some(milestone.sequence));
                summary.first_review_priority =
                    min_optional_priority(summary.first_review_priority, Some(milestone.priority));
            }
            if milestone.has_blockers() {
                summary.milestones_with_blockers += 1;
                summary.first_blocked_sequence =
                    summary.first_blocked_sequence.or(Some(milestone.sequence));
                summary.first_blocked_priority =
                    min_optional_priority(summary.first_blocked_priority, Some(milestone.priority));
            }
            if milestone.has_risks() {
                summary.milestones_with_risks += 1;
                summary.first_risk_sequence =
                    summary.first_risk_sequence.or(Some(milestone.sequence));
                summary.first_risk_priority =
                    min_optional_priority(summary.first_risk_priority, Some(milestone.priority));
            }
            if milestone.has_dependency_blockers() {
                summary.milestones_with_dependency_blockers += 1;
                summary.first_dependency_sequence = summary
                    .first_dependency_sequence
                    .or(Some(milestone.sequence));
                summary.first_dependency_priority = min_optional_priority(
                    summary.first_dependency_priority,
                    Some(milestone.priority),
                );
            }
        }

        summary.unique_integrations = integration_ids.len();
        summary.overall_status =
            if summary.blocked_milestones > 0 || summary.milestones_with_blockers > 0 {
                IntegrationActivationHealthStatus::Blocked
            } else if summary.review_milestones > 0
                || summary.milestones_with_review_work > 0
                || summary.milestones_with_approval_work > 0
            {
                IntegrationActivationHealthStatus::NeedsReview
            } else if summary.ready_milestones > 0 || summary.milestones_with_activation_work > 0 {
                IntegrationActivationHealthStatus::Ready
            } else {
                IntegrationActivationHealthStatus::Empty
            };
        summary
    }

    pub fn is_empty(&self) -> bool {
        self.total_milestones == 0
    }

    pub fn has_activation_work(&self) -> bool {
        self.milestones_with_activation_work > 0 || self.activation_milestones > 0
    }

    pub fn has_approval_ready_work(&self) -> bool {
        self.milestones_with_approval_work > 0 || self.approval_milestones > 0
    }

    pub fn has_review_work(&self) -> bool {
        self.milestones_with_review_work > 0 || self.review_queue_milestones > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.milestones_with_blockers > 0 || self.blocker_milestones > 0
    }

    pub fn has_risks(&self) -> bool {
        self.milestones_with_risks > 0 || self.risk_milestones > 0
    }

    pub fn has_dependency_blockers(&self) -> bool {
        self.milestones_with_dependency_blockers > 0 || self.dependency_milestones > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.overall_status.requires_attention()
            || self.has_approval_ready_work()
            || self.has_review_work()
            || self.has_blockers()
            || self.has_risks()
            || self.has_dependency_blockers()
    }
}

impl IntegrationActivationForecastItem {
    fn from_milestone(milestone: IntegrationActivationTimelineMilestone) -> Self {
        let card = &milestone.dashboard_card;
        let forecast_action = if milestone.has_dependency_blockers() {
            IntegrationActivationForecastAction::EnableDependencies
        } else if milestone.has_blockers() {
            IntegrationActivationForecastAction::ResolveBlockers
        } else if milestone.has_approval_ready_work() {
            IntegrationActivationForecastAction::PrepareApproval
        } else if milestone.has_review_work() {
            IntegrationActivationForecastAction::QueueReview
        } else if milestone.has_activation_work() {
            IntegrationActivationForecastAction::ActivateWave
        } else if milestone.has_risks() {
            IntegrationActivationForecastAction::ReviewRisk
        } else {
            IntegrationActivationForecastAction::MonitorWave
        };
        let requires_attention =
            forecast_action.requires_attention() || milestone.requires_attention();

        Self {
            sequence: milestone.sequence,
            priority: milestone.priority,
            forecast_action,
            milestone_kind: milestone.milestone_kind,
            health_status: card.health_status,
            integration_ids: card.integration_ids.clone(),
            integration_count: card.integration_count(),
            briefing_item_count: card.briefing_item_count,
            action_count: card.action_count,
            dossier_count: card.dossier_count,
            evidence_count: card.evidence_count,
            risk_count: card.risk_count,
            dependency_edge_count: card.dependency_edge_count,
            blocking_dependency_edge_count: card.blocking_dependency_edge_count,
            highest_policy_tier: card.highest_policy_tier,
            has_activation_work: card.has_activation_work,
            has_approval_ready_work: card.has_approval_ready_work,
            has_review_work: card.has_review_work,
            has_blockers: card.has_blockers,
            has_risks: card.has_risks,
            has_dependency_blockers: card.has_dependency_blockers,
            requires_attention,
        }
    }

    pub fn integration_count(&self) -> usize {
        self.integration_count
    }

    pub fn requires_attention(&self) -> bool {
        self.requires_attention
    }

    pub fn has_activation_work(&self) -> bool {
        self.has_activation_work
    }

    pub fn has_approval_ready_work(&self) -> bool {
        self.has_approval_ready_work
    }

    pub fn has_review_work(&self) -> bool {
        self.has_review_work
    }

    pub fn has_blockers(&self) -> bool {
        self.has_blockers
    }

    pub fn has_risks(&self) -> bool {
        self.has_risks
    }

    pub fn has_dependency_blockers(&self) -> bool {
        self.has_dependency_blockers
    }
}

impl IntegrationActivationForecastSummary {
    pub fn from_forecasts<'a>(
        forecasts: impl IntoIterator<Item = &'a IntegrationActivationForecastItem>,
    ) -> Self {
        let mut integration_ids = BTreeSet::new();
        let mut summary = Self {
            total_forecasts: 0,
            unique_integrations: 0,
            ready_forecasts: 0,
            review_forecasts: 0,
            blocked_forecasts: 0,
            empty_forecasts: 0,
            activate_wave_forecasts: 0,
            prepare_approval_forecasts: 0,
            queue_review_forecasts: 0,
            resolve_blocker_forecasts: 0,
            enable_dependency_forecasts: 0,
            review_risk_forecasts: 0,
            monitor_wave_forecasts: 0,
            forecasts_requiring_attention: 0,
            forecasts_with_activation_work: 0,
            forecasts_with_approval_work: 0,
            forecasts_with_review_work: 0,
            forecasts_with_blockers: 0,
            forecasts_with_risks: 0,
            forecasts_with_dependency_blockers: 0,
            total_briefing_items: 0,
            total_actions: 0,
            total_dossiers: 0,
            total_evidence: 0,
            total_risks: 0,
            total_dependency_edges: 0,
            blocking_dependency_edges: 0,
            next_action: None,
            next_action_sequence: None,
            next_action_priority: None,
            first_activation_sequence: None,
            first_approval_sequence: None,
            first_review_sequence: None,
            first_blocked_sequence: None,
            first_risk_sequence: None,
            first_dependency_sequence: None,
            first_attention_sequence: None,
            first_activation_priority: None,
            first_approval_priority: None,
            first_review_priority: None,
            first_blocked_priority: None,
            first_risk_priority: None,
            first_dependency_priority: None,
            first_attention_priority: None,
            highest_policy_tier: PrivilegeTier::ReadOnly,
            overall_status: IntegrationActivationHealthStatus::Empty,
        };

        for forecast in forecasts {
            summary.total_forecasts += 1;
            for integration_id in &forecast.integration_ids {
                integration_ids.insert(integration_id.clone());
            }

            match forecast.health_status {
                IntegrationActivationHealthStatus::Ready => summary.ready_forecasts += 1,
                IntegrationActivationHealthStatus::NeedsReview => summary.review_forecasts += 1,
                IntegrationActivationHealthStatus::Blocked => summary.blocked_forecasts += 1,
                IntegrationActivationHealthStatus::Empty => summary.empty_forecasts += 1,
            }

            match forecast.forecast_action {
                IntegrationActivationForecastAction::ActivateWave => {
                    summary.activate_wave_forecasts += 1;
                    summary.first_activation_sequence = summary
                        .first_activation_sequence
                        .or(Some(forecast.sequence));
                    summary.first_activation_priority = min_optional_priority(
                        summary.first_activation_priority,
                        Some(forecast.priority),
                    );
                }
                IntegrationActivationForecastAction::PrepareApproval => {
                    summary.prepare_approval_forecasts += 1;
                    summary.first_approval_sequence =
                        summary.first_approval_sequence.or(Some(forecast.sequence));
                    summary.first_approval_priority = min_optional_priority(
                        summary.first_approval_priority,
                        Some(forecast.priority),
                    );
                }
                IntegrationActivationForecastAction::QueueReview => {
                    summary.queue_review_forecasts += 1;
                    summary.first_review_sequence =
                        summary.first_review_sequence.or(Some(forecast.sequence));
                    summary.first_review_priority = min_optional_priority(
                        summary.first_review_priority,
                        Some(forecast.priority),
                    );
                }
                IntegrationActivationForecastAction::ResolveBlockers => {
                    summary.resolve_blocker_forecasts += 1;
                    summary.first_blocked_sequence =
                        summary.first_blocked_sequence.or(Some(forecast.sequence));
                    summary.first_blocked_priority = min_optional_priority(
                        summary.first_blocked_priority,
                        Some(forecast.priority),
                    );
                }
                IntegrationActivationForecastAction::EnableDependencies => {
                    summary.enable_dependency_forecasts += 1;
                    summary.first_dependency_sequence = summary
                        .first_dependency_sequence
                        .or(Some(forecast.sequence));
                    summary.first_dependency_priority = min_optional_priority(
                        summary.first_dependency_priority,
                        Some(forecast.priority),
                    );
                }
                IntegrationActivationForecastAction::ReviewRisk => {
                    summary.review_risk_forecasts += 1;
                    summary.first_risk_sequence =
                        summary.first_risk_sequence.or(Some(forecast.sequence));
                    summary.first_risk_priority =
                        min_optional_priority(summary.first_risk_priority, Some(forecast.priority));
                }
                IntegrationActivationForecastAction::MonitorWave => {
                    summary.monitor_wave_forecasts += 1;
                }
            }

            if summary.next_action.is_none()
                && forecast.forecast_action != IntegrationActivationForecastAction::MonitorWave
            {
                summary.next_action = Some(forecast.forecast_action);
                summary.next_action_sequence = Some(forecast.sequence);
                summary.next_action_priority = Some(forecast.priority);
            }

            summary.total_briefing_items += forecast.briefing_item_count;
            summary.total_actions += forecast.action_count;
            summary.total_dossiers += forecast.dossier_count;
            summary.total_evidence += forecast.evidence_count;
            summary.total_risks += forecast.risk_count;
            summary.total_dependency_edges += forecast.dependency_edge_count;
            summary.blocking_dependency_edges += forecast.blocking_dependency_edge_count;
            summary.highest_policy_tier = summary
                .highest_policy_tier
                .max(forecast.highest_policy_tier);

            if forecast.requires_attention() {
                summary.forecasts_requiring_attention += 1;
                summary.first_attention_sequence =
                    summary.first_attention_sequence.or(Some(forecast.sequence));
                summary.first_attention_priority = min_optional_priority(
                    summary.first_attention_priority,
                    Some(forecast.priority),
                );
            }
            if forecast.has_activation_work() {
                summary.forecasts_with_activation_work += 1;
            }
            if forecast.has_approval_ready_work() {
                summary.forecasts_with_approval_work += 1;
            }
            if forecast.has_review_work() {
                summary.forecasts_with_review_work += 1;
            }
            if forecast.has_blockers() {
                summary.forecasts_with_blockers += 1;
            }
            if forecast.has_risks() {
                summary.forecasts_with_risks += 1;
            }
            if forecast.has_dependency_blockers() {
                summary.forecasts_with_dependency_blockers += 1;
            }
        }

        summary.unique_integrations = integration_ids.len();
        summary.overall_status =
            if summary.blocked_forecasts > 0 || summary.forecasts_with_blockers > 0 {
                IntegrationActivationHealthStatus::Blocked
            } else if summary.review_forecasts > 0
                || summary.forecasts_with_review_work > 0
                || summary.forecasts_with_approval_work > 0
            {
                IntegrationActivationHealthStatus::NeedsReview
            } else if summary.ready_forecasts > 0 || summary.forecasts_with_activation_work > 0 {
                IntegrationActivationHealthStatus::Ready
            } else {
                IntegrationActivationHealthStatus::Empty
            };
        summary
    }

    pub fn is_empty(&self) -> bool {
        self.total_forecasts == 0
    }

    pub fn has_activation_work(&self) -> bool {
        self.forecasts_with_activation_work > 0 || self.activate_wave_forecasts > 0
    }

    pub fn has_approval_ready_work(&self) -> bool {
        self.forecasts_with_approval_work > 0 || self.prepare_approval_forecasts > 0
    }

    pub fn has_review_work(&self) -> bool {
        self.forecasts_with_review_work > 0 || self.queue_review_forecasts > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.forecasts_with_blockers > 0 || self.resolve_blocker_forecasts > 0
    }

    pub fn has_risks(&self) -> bool {
        self.forecasts_with_risks > 0 || self.review_risk_forecasts > 0
    }

    pub fn has_dependency_blockers(&self) -> bool {
        self.forecasts_with_dependency_blockers > 0 || self.enable_dependency_forecasts > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.forecasts_requiring_attention > 0
            || self.overall_status.requires_attention()
            || self.has_approval_ready_work()
            || self.has_review_work()
            || self.has_blockers()
            || self.has_risks()
            || self.has_dependency_blockers()
    }
}

impl IntegrationActivationPlaybookStep {
    fn from_forecast(forecast: IntegrationActivationForecastItem) -> Self {
        let recommended_view =
            IntegrationActivationPlaybookView::from_forecast_action(forecast.forecast_action);
        let activation_ready =
            forecast.forecast_action == IntegrationActivationForecastAction::ActivateWave;
        let monitor_only =
            forecast.forecast_action == IntegrationActivationForecastAction::MonitorWave;
        let blocked = matches!(
            forecast.forecast_action,
            IntegrationActivationForecastAction::ResolveBlockers
                | IntegrationActivationForecastAction::EnableDependencies
        ) || forecast.has_blockers()
            || forecast.has_dependency_blockers();
        let review_required = matches!(
            forecast.forecast_action,
            IntegrationActivationForecastAction::PrepareApproval
                | IntegrationActivationForecastAction::QueueReview
                | IntegrationActivationForecastAction::ReviewRisk
        ) || forecast.has_approval_ready_work()
            || forecast.has_review_work()
            || forecast.has_risks();
        let operator_required = forecast.forecast_action.requires_attention()
            || blocked
            || review_required
            || forecast.requires_attention();
        let requires_attention = operator_required || forecast.requires_attention();

        Self {
            sequence: forecast.sequence,
            priority: forecast.priority,
            playbook_action: forecast.forecast_action,
            recommended_view,
            milestone_kind: forecast.milestone_kind,
            health_status: forecast.health_status,
            integration_ids: forecast.integration_ids,
            integration_count: forecast.integration_count,
            action_count: forecast.action_count,
            dossier_count: forecast.dossier_count,
            evidence_count: forecast.evidence_count,
            risk_count: forecast.risk_count,
            dependency_edge_count: forecast.dependency_edge_count,
            blocking_dependency_edge_count: forecast.blocking_dependency_edge_count,
            highest_policy_tier: forecast.highest_policy_tier,
            operator_required,
            activation_ready,
            blocked,
            review_required,
            monitor_only,
            requires_attention,
        }
    }

    pub fn integration_count(&self) -> usize {
        self.integration_count
    }

    pub fn requires_attention(&self) -> bool {
        self.requires_attention
    }

    pub fn operator_required(&self) -> bool {
        self.operator_required
    }

    pub fn activation_ready(&self) -> bool {
        self.activation_ready
    }

    pub fn blocked(&self) -> bool {
        self.blocked
    }

    pub fn review_required(&self) -> bool {
        self.review_required
    }

    pub fn monitor_only(&self) -> bool {
        self.monitor_only
    }
}

impl IntegrationActivationPlaybookSummary {
    pub fn from_steps<'a>(
        steps: impl IntoIterator<Item = &'a IntegrationActivationPlaybookStep>,
    ) -> Self {
        let mut integration_ids = BTreeSet::new();
        let mut summary = Self {
            total_steps: 0,
            unique_integrations: 0,
            operator_required_steps: 0,
            activation_ready_steps: 0,
            blocked_steps: 0,
            review_required_steps: 0,
            monitor_steps: 0,
            constraint_view_steps: 0,
            dependency_view_steps: 0,
            approval_view_steps: 0,
            review_view_steps: 0,
            risk_view_steps: 0,
            action_view_steps: 0,
            dashboard_view_steps: 0,
            total_actions: 0,
            total_dossiers: 0,
            total_evidence: 0,
            total_risks: 0,
            total_dependency_edges: 0,
            blocking_dependency_edges: 0,
            next_playbook_action: None,
            next_recommended_view: None,
            next_step_sequence: None,
            next_step_priority: None,
            first_operator_sequence: None,
            first_activation_sequence: None,
            first_blocked_sequence: None,
            first_review_sequence: None,
            first_monitor_sequence: None,
            first_operator_priority: None,
            first_activation_priority: None,
            first_blocked_priority: None,
            first_review_priority: None,
            first_monitor_priority: None,
            highest_policy_tier: PrivilegeTier::ReadOnly,
            overall_status: IntegrationActivationHealthStatus::Empty,
        };

        for step in steps {
            summary.total_steps += 1;
            for integration_id in &step.integration_ids {
                integration_ids.insert(integration_id.clone());
            }

            match step.recommended_view {
                IntegrationActivationPlaybookView::ConstraintQueue => {
                    summary.constraint_view_steps += 1
                }
                IntegrationActivationPlaybookView::DependencyGraph => {
                    summary.dependency_view_steps += 1
                }
                IntegrationActivationPlaybookView::ApprovalPackets => {
                    summary.approval_view_steps += 1
                }
                IntegrationActivationPlaybookView::ReviewQueue => summary.review_view_steps += 1,
                IntegrationActivationPlaybookView::RiskRegister => summary.risk_view_steps += 1,
                IntegrationActivationPlaybookView::ActivationActions => {
                    summary.action_view_steps += 1
                }
                IntegrationActivationPlaybookView::StatusDashboard => {
                    summary.dashboard_view_steps += 1
                }
            }

            if summary.next_playbook_action.is_none() && !step.monitor_only() {
                summary.next_playbook_action = Some(step.playbook_action);
                summary.next_recommended_view = Some(step.recommended_view);
                summary.next_step_sequence = Some(step.sequence);
                summary.next_step_priority = Some(step.priority);
            }

            if step.operator_required() {
                summary.operator_required_steps += 1;
                summary.first_operator_sequence =
                    summary.first_operator_sequence.or(Some(step.sequence));
                summary.first_operator_priority =
                    min_optional_priority(summary.first_operator_priority, Some(step.priority));
            }
            if step.activation_ready() {
                summary.activation_ready_steps += 1;
                summary.first_activation_sequence =
                    summary.first_activation_sequence.or(Some(step.sequence));
                summary.first_activation_priority =
                    min_optional_priority(summary.first_activation_priority, Some(step.priority));
            }
            if step.blocked() {
                summary.blocked_steps += 1;
                summary.first_blocked_sequence =
                    summary.first_blocked_sequence.or(Some(step.sequence));
                summary.first_blocked_priority =
                    min_optional_priority(summary.first_blocked_priority, Some(step.priority));
            }
            if step.review_required() {
                summary.review_required_steps += 1;
                summary.first_review_sequence =
                    summary.first_review_sequence.or(Some(step.sequence));
                summary.first_review_priority =
                    min_optional_priority(summary.first_review_priority, Some(step.priority));
            }
            if step.monitor_only() {
                summary.monitor_steps += 1;
                summary.first_monitor_sequence =
                    summary.first_monitor_sequence.or(Some(step.sequence));
                summary.first_monitor_priority =
                    min_optional_priority(summary.first_monitor_priority, Some(step.priority));
            }

            summary.total_actions += step.action_count;
            summary.total_dossiers += step.dossier_count;
            summary.total_evidence += step.evidence_count;
            summary.total_risks += step.risk_count;
            summary.total_dependency_edges += step.dependency_edge_count;
            summary.blocking_dependency_edges += step.blocking_dependency_edge_count;
            summary.highest_policy_tier = summary.highest_policy_tier.max(step.highest_policy_tier);
        }

        summary.unique_integrations = integration_ids.len();
        summary.overall_status = if summary.blocked_steps > 0 {
            IntegrationActivationHealthStatus::Blocked
        } else if summary.review_required_steps > 0 || summary.operator_required_steps > 0 {
            IntegrationActivationHealthStatus::NeedsReview
        } else if summary.activation_ready_steps > 0 {
            IntegrationActivationHealthStatus::Ready
        } else {
            IntegrationActivationHealthStatus::Empty
        };
        summary
    }

    pub fn is_empty(&self) -> bool {
        self.total_steps == 0
    }

    pub fn has_operator_work(&self) -> bool {
        self.operator_required_steps > 0
    }

    pub fn has_activation_work(&self) -> bool {
        self.activation_ready_steps > 0 || self.action_view_steps > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.blocked_steps > 0 || self.constraint_view_steps > 0 || self.dependency_view_steps > 0
    }

    pub fn has_review_work(&self) -> bool {
        self.review_required_steps > 0
            || self.approval_view_steps > 0
            || self.review_view_steps > 0
            || self.risk_view_steps > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.has_operator_work()
            || self.has_blockers()
            || self.has_review_work()
            || self.overall_status.requires_attention()
    }
}

impl IntegrationActivationRunbookEntry {
    fn from_playbook_step(
        step: IntegrationActivationPlaybookStep,
        audit_records: &[&IntegrationActivationAuditRecord],
    ) -> Self {
        let audit_record_count = audit_records.len();
        let attention_audit_record_count = audit_records
            .iter()
            .filter(|record| record.requires_attention())
            .count();
        let risk_audit_record_count = audit_records
            .iter()
            .filter(|record| record.record_kind == IntegrationActivationAuditRecordKind::Risk)
            .count();
        let dependency_audit_record_count = audit_records
            .iter()
            .filter(|record| record.record_kind == IntegrationActivationAuditRecordKind::Dependency)
            .count();
        let readiness_gap_record_count = audit_records
            .iter()
            .filter(|record| {
                record.record_kind == IntegrationActivationAuditRecordKind::ReadinessGap
            })
            .count();
        let highest_policy_tier = audit_records
            .iter()
            .fold(step.highest_policy_tier, |tier, record| {
                tier.max(record.required_tier)
            });
        let dependency_blocking = audit_records.iter().any(|record| {
            record.record_kind == IntegrationActivationAuditRecordKind::Dependency
                && record.requires_attention()
        });
        let blocked = step.blocked() || dependency_blocking || readiness_gap_record_count > 0;
        let review_required = step.review_required()
            || risk_audit_record_count > 0
            || audit_records.iter().any(|record| {
                matches!(
                    record.record_kind,
                    IntegrationActivationAuditRecordKind::Decision
                        | IntegrationActivationAuditRecordKind::Evidence
                ) && record.requires_attention()
            });
        let operator_required = step.operator_required()
            || blocked
            || review_required
            || attention_audit_record_count > 0;
        let requires_attention =
            step.requires_attention() || operator_required || attention_audit_record_count > 0;
        let activation_ready = step.activation_ready();
        let monitor_only = step.monitor_only();

        Self {
            sequence: step.sequence,
            playbook_sequence: step.sequence,
            priority: step.priority,
            phase: IntegrationActivationRunbookPhase::from_playbook_action(step.playbook_action),
            playbook_action: step.playbook_action,
            recommended_view: step.recommended_view,
            milestone_kind: step.milestone_kind,
            health_status: step.health_status,
            integration_ids: step.integration_ids,
            integration_count: step.integration_count,
            action_count: step.action_count,
            dossier_count: step.dossier_count,
            evidence_count: step.evidence_count,
            risk_count: step.risk_count,
            dependency_edge_count: step.dependency_edge_count,
            blocking_dependency_edge_count: step.blocking_dependency_edge_count,
            audit_record_count,
            attention_audit_record_count,
            risk_audit_record_count,
            dependency_audit_record_count,
            readiness_gap_record_count,
            highest_policy_tier,
            operator_required,
            activation_ready,
            blocked,
            review_required,
            monitor_only,
            requires_attention,
        }
    }

    pub fn integration_count(&self) -> usize {
        self.integration_count
    }

    pub fn requires_attention(&self) -> bool {
        self.requires_attention
    }

    pub fn operator_required(&self) -> bool {
        self.operator_required
    }

    pub fn activation_ready(&self) -> bool {
        self.activation_ready
    }

    pub fn blocked(&self) -> bool {
        self.blocked
    }

    pub fn review_required(&self) -> bool {
        self.review_required
    }

    pub fn monitor_only(&self) -> bool {
        self.monitor_only
    }

    pub fn has_audit_context(&self) -> bool {
        self.audit_record_count > 0
    }
}

impl IntegrationActivationRunbookSummary {
    pub fn from_entries<'a>(
        entries: impl IntoIterator<Item = &'a IntegrationActivationRunbookEntry>,
    ) -> Self {
        let mut integration_ids = BTreeSet::new();
        let mut summary = Self {
            total_entries: 0,
            unique_integrations: 0,
            operator_required_entries: 0,
            activation_ready_entries: 0,
            blocked_entries: 0,
            review_required_entries: 0,
            monitor_entries: 0,
            clear_blocker_entries: 0,
            dependency_entries: 0,
            approval_entries: 0,
            review_entries: 0,
            risk_entries: 0,
            activation_entries: 0,
            monitor_phase_entries: 0,
            total_actions: 0,
            total_dossiers: 0,
            total_evidence: 0,
            total_risks: 0,
            total_dependency_edges: 0,
            blocking_dependency_edges: 0,
            total_audit_records: 0,
            attention_audit_records: 0,
            risk_audit_records: 0,
            dependency_audit_records: 0,
            readiness_gap_records: 0,
            next_phase: None,
            next_playbook_action: None,
            next_recommended_view: None,
            next_entry_sequence: None,
            next_entry_priority: None,
            first_operator_sequence: None,
            first_activation_sequence: None,
            first_blocked_sequence: None,
            first_review_sequence: None,
            first_monitor_sequence: None,
            first_attention_sequence: None,
            first_operator_priority: None,
            first_activation_priority: None,
            first_blocked_priority: None,
            first_review_priority: None,
            first_monitor_priority: None,
            first_attention_priority: None,
            highest_policy_tier: PrivilegeTier::ReadOnly,
            overall_status: IntegrationActivationHealthStatus::Empty,
        };

        for entry in entries {
            summary.total_entries += 1;
            for integration_id in &entry.integration_ids {
                integration_ids.insert(integration_id.clone());
            }

            match entry.phase {
                IntegrationActivationRunbookPhase::ClearBlockers => {
                    summary.clear_blocker_entries += 1
                }
                IntegrationActivationRunbookPhase::ResolveDependencies => {
                    summary.dependency_entries += 1
                }
                IntegrationActivationRunbookPhase::PrepareApprovals => {
                    summary.approval_entries += 1
                }
                IntegrationActivationRunbookPhase::CompleteReviews => summary.review_entries += 1,
                IntegrationActivationRunbookPhase::ReviewRisks => summary.risk_entries += 1,
                IntegrationActivationRunbookPhase::Activate => summary.activation_entries += 1,
                IntegrationActivationRunbookPhase::Monitor => summary.monitor_phase_entries += 1,
            }

            if summary.next_phase.is_none() && !entry.monitor_only() {
                summary.next_phase = Some(entry.phase);
                summary.next_playbook_action = Some(entry.playbook_action);
                summary.next_recommended_view = Some(entry.recommended_view);
                summary.next_entry_sequence = Some(entry.sequence);
                summary.next_entry_priority = Some(entry.priority);
            }

            if entry.operator_required() {
                summary.operator_required_entries += 1;
                summary.first_operator_sequence =
                    summary.first_operator_sequence.or(Some(entry.sequence));
                summary.first_operator_priority =
                    min_optional_priority(summary.first_operator_priority, Some(entry.priority));
            }
            if entry.activation_ready() {
                summary.activation_ready_entries += 1;
                summary.first_activation_sequence =
                    summary.first_activation_sequence.or(Some(entry.sequence));
                summary.first_activation_priority =
                    min_optional_priority(summary.first_activation_priority, Some(entry.priority));
            }
            if entry.blocked() {
                summary.blocked_entries += 1;
                summary.first_blocked_sequence =
                    summary.first_blocked_sequence.or(Some(entry.sequence));
                summary.first_blocked_priority =
                    min_optional_priority(summary.first_blocked_priority, Some(entry.priority));
            }
            if entry.review_required() {
                summary.review_required_entries += 1;
                summary.first_review_sequence =
                    summary.first_review_sequence.or(Some(entry.sequence));
                summary.first_review_priority =
                    min_optional_priority(summary.first_review_priority, Some(entry.priority));
            }
            if entry.monitor_only() {
                summary.monitor_entries += 1;
                summary.first_monitor_sequence =
                    summary.first_monitor_sequence.or(Some(entry.sequence));
                summary.first_monitor_priority =
                    min_optional_priority(summary.first_monitor_priority, Some(entry.priority));
            }
            if entry.requires_attention() {
                summary.first_attention_sequence =
                    summary.first_attention_sequence.or(Some(entry.sequence));
                summary.first_attention_priority =
                    min_optional_priority(summary.first_attention_priority, Some(entry.priority));
            }

            summary.total_actions += entry.action_count;
            summary.total_dossiers += entry.dossier_count;
            summary.total_evidence += entry.evidence_count;
            summary.total_risks += entry.risk_count;
            summary.total_dependency_edges += entry.dependency_edge_count;
            summary.blocking_dependency_edges += entry.blocking_dependency_edge_count;
            summary.total_audit_records += entry.audit_record_count;
            summary.attention_audit_records += entry.attention_audit_record_count;
            summary.risk_audit_records += entry.risk_audit_record_count;
            summary.dependency_audit_records += entry.dependency_audit_record_count;
            summary.readiness_gap_records += entry.readiness_gap_record_count;
            summary.highest_policy_tier =
                summary.highest_policy_tier.max(entry.highest_policy_tier);
        }

        summary.unique_integrations = integration_ids.len();
        summary.overall_status = if summary.blocked_entries > 0
            || summary.readiness_gap_records > 0
            || summary.blocking_dependency_edges > 0
        {
            IntegrationActivationHealthStatus::Blocked
        } else if summary.review_required_entries > 0
            || summary.operator_required_entries > 0
            || summary.attention_audit_records > 0
            || summary.risk_audit_records > 0
        {
            IntegrationActivationHealthStatus::NeedsReview
        } else if summary.activation_ready_entries > 0 {
            IntegrationActivationHealthStatus::Ready
        } else {
            IntegrationActivationHealthStatus::Empty
        };
        summary
    }

    pub fn is_empty(&self) -> bool {
        self.total_entries == 0
    }

    pub fn has_operator_work(&self) -> bool {
        self.operator_required_entries > 0
    }

    pub fn has_activation_work(&self) -> bool {
        self.activation_ready_entries > 0 || self.activation_entries > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.blocked_entries > 0
            || self.clear_blocker_entries > 0
            || self.dependency_entries > 0
            || self.readiness_gap_records > 0
    }

    pub fn has_review_work(&self) -> bool {
        self.review_required_entries > 0
            || self.approval_entries > 0
            || self.review_entries > 0
            || self.risk_entries > 0
            || self.risk_audit_records > 0
    }

    pub fn has_audit_context(&self) -> bool {
        self.total_audit_records > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.has_operator_work()
            || self.has_blockers()
            || self.has_review_work()
            || self.attention_audit_records > 0
            || self.overall_status.requires_attention()
    }
}

impl IntegrationActivationHandoffPackage {
    fn from_runbook_entry(
        entry: &IntegrationActivationRunbookEntry,
        risks: &[IntegrationActivationRiskItem],
        graph: &IntegrationActivationDependencyGraph,
        gap_inventory: &IntegrationReadinessGapInventory,
    ) -> Self {
        let mut risk_ids = BTreeSet::new();
        let mut highest_policy_tier = entry.highest_policy_tier;
        for risk in risks {
            if integration_ids_overlap(&risk.integration_ids, &entry.integration_ids) {
                risk_ids.insert(risk.risk_id.clone());
                highest_policy_tier = highest_policy_tier.max(risk.required_tier);
            }
        }

        let mut dependency_integration_ids = BTreeSet::new();
        let mut blocking_dependency_integration_ids = BTreeSet::new();
        let mut dependency_edge_count = 0;
        let mut blocking_dependency_edge_count = 0;
        for edge in &graph.edges {
            let relates_to_entry =
                integration_id_in_slice(&entry.integration_ids, &edge.dependency_integration_id)
                    || integration_id_in_slice(
                        &entry.integration_ids,
                        &edge.dependent_integration_id,
                    );
            if relates_to_entry {
                dependency_edge_count += 1;
                dependency_integration_ids.insert(edge.dependency_integration_id.clone());
                dependency_integration_ids.insert(edge.dependent_integration_id.clone());
                if edge.blocks_activation {
                    blocking_dependency_edge_count += 1;
                    blocking_dependency_integration_ids
                        .insert(edge.dependency_integration_id.clone());
                    blocking_dependency_integration_ids
                        .insert(edge.dependent_integration_id.clone());
                }
            }
        }

        let primitive_gap_count = gap_inventory
            .primitive_gaps
            .iter()
            .filter(|gap| integration_ids_overlap(&gap.integration_ids, &entry.integration_ids))
            .count();
        let capability_gap_count = gap_inventory
            .capability_gaps
            .iter()
            .filter(|gap| integration_ids_overlap(&gap.integration_ids, &entry.integration_ids))
            .count();
        let dependency_gap_count = gap_inventory
            .dependency_gaps
            .iter()
            .filter(|gap| {
                integration_id_in_slice(&entry.integration_ids, &gap.integration_id)
                    || integration_ids_overlap(
                        &gap.requested_integration_ids,
                        &entry.integration_ids,
                    )
            })
            .count();
        let readiness_gap_count = primitive_gap_count + capability_gap_count + dependency_gap_count;

        let handoff_status = if entry.monitor_only() {
            IntegrationActivationHandoffStatus::Monitoring
        } else if entry.blocked() || blocking_dependency_edge_count > 0 || readiness_gap_count > 0 {
            IntegrationActivationHandoffStatus::Blocked
        } else if entry.review_required()
            || !risk_ids.is_empty()
            || entry.attention_audit_record_count > 0
        {
            IntegrationActivationHandoffStatus::NeedsReview
        } else if entry.activation_ready() {
            IntegrationActivationHandoffStatus::Ready
        } else {
            IntegrationActivationHandoffStatus::NeedsReview
        };

        let blocked = handoff_status == IntegrationActivationHandoffStatus::Blocked;
        let review_required = handoff_status == IntegrationActivationHandoffStatus::NeedsReview
            || entry.review_required();
        let monitor_only = handoff_status == IntegrationActivationHandoffStatus::Monitoring;
        let ready_for_handoff = handoff_status == IntegrationActivationHandoffStatus::Ready;
        let requires_attention = handoff_status.requires_attention() || entry.requires_attention();
        let operator_required =
            entry.operator_required() || blocked || review_required || requires_attention;
        let risk_ids = risk_ids.into_iter().collect::<Vec<_>>();
        let risk_count = risk_ids.len();
        let dependency_integration_ids = dependency_integration_ids.into_iter().collect();
        let blocking_dependency_integration_ids =
            blocking_dependency_integration_ids.into_iter().collect();

        Self {
            sequence: entry.sequence,
            runbook_sequence: entry.sequence,
            priority: entry.priority,
            handoff_status,
            phase: entry.phase,
            playbook_action: entry.playbook_action,
            recommended_view: entry.recommended_view,
            integration_ids: entry.integration_ids.clone(),
            integration_count: entry.integration_count,
            risk_ids,
            dependency_integration_ids,
            blocking_dependency_integration_ids,
            risk_count,
            dependency_edge_count,
            blocking_dependency_edge_count,
            primitive_gap_count,
            capability_gap_count,
            dependency_gap_count,
            readiness_gap_count,
            audit_record_count: entry.audit_record_count,
            attention_audit_record_count: entry.attention_audit_record_count,
            highest_policy_tier,
            operator_required,
            activation_ready: entry.activation_ready(),
            ready_for_handoff,
            blocked,
            review_required,
            monitor_only,
            requires_attention,
        }
    }

    pub fn integration_count(&self) -> usize {
        self.integration_count
    }

    pub fn ready_for_handoff(&self) -> bool {
        self.ready_for_handoff
    }

    pub fn blocked(&self) -> bool {
        self.blocked
    }

    pub fn review_required(&self) -> bool {
        self.review_required
    }

    pub fn monitor_only(&self) -> bool {
        self.monitor_only
    }

    pub fn operator_required(&self) -> bool {
        self.operator_required
    }

    pub fn activation_ready(&self) -> bool {
        self.activation_ready
    }

    pub fn requires_attention(&self) -> bool {
        self.requires_attention
    }

    pub fn has_dependency_blockers(&self) -> bool {
        self.blocking_dependency_edge_count > 0
            || !self.blocking_dependency_integration_ids.is_empty()
    }

    pub fn has_readiness_gaps(&self) -> bool {
        self.readiness_gap_count > 0
    }
}

impl IntegrationActivationHandoffSummary {
    pub fn from_packages<'a>(
        packages: impl IntoIterator<Item = &'a IntegrationActivationHandoffPackage>,
    ) -> Self {
        let mut integration_ids = BTreeSet::new();
        let mut summary = Self {
            total_packages: 0,
            unique_integrations: 0,
            ready_packages: 0,
            review_required_packages: 0,
            blocked_packages: 0,
            monitor_packages: 0,
            operator_required_packages: 0,
            activation_ready_packages: 0,
            packages_requiring_attention: 0,
            total_risks: 0,
            total_dependency_edges: 0,
            blocking_dependency_edges: 0,
            total_primitive_gaps: 0,
            total_capability_gaps: 0,
            total_dependency_gaps: 0,
            total_readiness_gaps: 0,
            total_audit_records: 0,
            attention_audit_records: 0,
            next_handoff_status: None,
            next_phase: None,
            next_playbook_action: None,
            next_recommended_view: None,
            next_package_sequence: None,
            next_package_priority: None,
            first_ready_sequence: None,
            first_blocked_sequence: None,
            first_review_sequence: None,
            first_attention_sequence: None,
            first_ready_priority: None,
            first_blocked_priority: None,
            first_review_priority: None,
            first_attention_priority: None,
            highest_policy_tier: PrivilegeTier::ReadOnly,
            overall_status: IntegrationActivationHealthStatus::Empty,
        };

        for package in packages {
            summary.total_packages += 1;
            for integration_id in &package.integration_ids {
                integration_ids.insert(integration_id.clone());
            }

            if summary.next_handoff_status.is_none() && !package.monitor_only() {
                summary.next_handoff_status = Some(package.handoff_status);
                summary.next_phase = Some(package.phase);
                summary.next_playbook_action = Some(package.playbook_action);
                summary.next_recommended_view = Some(package.recommended_view);
                summary.next_package_sequence = Some(package.sequence);
                summary.next_package_priority = Some(package.priority);
            }

            match package.handoff_status {
                IntegrationActivationHandoffStatus::Ready => {
                    summary.ready_packages += 1;
                    summary.first_ready_sequence =
                        summary.first_ready_sequence.or(Some(package.sequence));
                    summary.first_ready_priority =
                        min_optional_priority(summary.first_ready_priority, Some(package.priority));
                }
                IntegrationActivationHandoffStatus::NeedsReview => {
                    summary.review_required_packages += 1;
                    summary.first_review_sequence =
                        summary.first_review_sequence.or(Some(package.sequence));
                    summary.first_review_priority = min_optional_priority(
                        summary.first_review_priority,
                        Some(package.priority),
                    );
                }
                IntegrationActivationHandoffStatus::Blocked => {
                    summary.blocked_packages += 1;
                    summary.first_blocked_sequence =
                        summary.first_blocked_sequence.or(Some(package.sequence));
                    summary.first_blocked_priority = min_optional_priority(
                        summary.first_blocked_priority,
                        Some(package.priority),
                    );
                }
                IntegrationActivationHandoffStatus::Monitoring => {
                    summary.monitor_packages += 1;
                }
            }

            if package.operator_required() {
                summary.operator_required_packages += 1;
            }
            if package.activation_ready() {
                summary.activation_ready_packages += 1;
            }
            if package.requires_attention() {
                summary.packages_requiring_attention += 1;
                summary.first_attention_sequence =
                    summary.first_attention_sequence.or(Some(package.sequence));
                summary.first_attention_priority =
                    min_optional_priority(summary.first_attention_priority, Some(package.priority));
            }

            summary.total_risks += package.risk_count;
            summary.total_dependency_edges += package.dependency_edge_count;
            summary.blocking_dependency_edges += package.blocking_dependency_edge_count;
            summary.total_primitive_gaps += package.primitive_gap_count;
            summary.total_capability_gaps += package.capability_gap_count;
            summary.total_dependency_gaps += package.dependency_gap_count;
            summary.total_readiness_gaps += package.readiness_gap_count;
            summary.total_audit_records += package.audit_record_count;
            summary.attention_audit_records += package.attention_audit_record_count;
            summary.highest_policy_tier =
                summary.highest_policy_tier.max(package.highest_policy_tier);
        }

        summary.unique_integrations = integration_ids.len();
        summary.overall_status = if summary.blocked_packages > 0
            || summary.blocking_dependency_edges > 0
            || summary.total_readiness_gaps > 0
        {
            IntegrationActivationHealthStatus::Blocked
        } else if summary.review_required_packages > 0
            || summary.operator_required_packages > 0
            || summary.packages_requiring_attention > 0
            || summary.total_risks > 0
        {
            IntegrationActivationHealthStatus::NeedsReview
        } else if summary.ready_packages > 0 {
            IntegrationActivationHealthStatus::Ready
        } else {
            IntegrationActivationHealthStatus::Empty
        };
        summary
    }

    pub fn is_empty(&self) -> bool {
        self.total_packages == 0
    }

    pub fn ready_for_handoff(&self) -> bool {
        self.ready_packages > 0 && !self.has_blockers() && !self.has_review_work()
    }

    pub fn has_blockers(&self) -> bool {
        self.blocked_packages > 0
            || self.blocking_dependency_edges > 0
            || self.total_readiness_gaps > 0
    }

    pub fn has_review_work(&self) -> bool {
        self.review_required_packages > 0
            || self.total_risks > 0
            || self.attention_audit_records > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.packages_requiring_attention > 0
            || self.operator_required_packages > 0
            || self.has_blockers()
            || self.has_review_work()
            || self.overall_status.requires_attention()
    }
}

impl IntegrationActivationExecutionPacket {
    fn from_handoff_package(
        package: IntegrationActivationHandoffPackage,
        task: Option<&IntegrationActivationOperatorTask>,
    ) -> Self {
        let task_blocked = task
            .map(IntegrationActivationOperatorTask::blocked)
            .unwrap_or(false);
        let task_review_required = task
            .map(IntegrationActivationOperatorTask::review_required)
            .unwrap_or(false);
        let task_operator_required = task
            .map(IntegrationActivationOperatorTask::operator_required)
            .unwrap_or(false);
        let task_activation_ready = task
            .map(IntegrationActivationOperatorTask::activation_ready)
            .unwrap_or(true);
        let task_monitor_only = task
            .map(IntegrationActivationOperatorTask::monitor_only)
            .unwrap_or(false);

        let dependency_ready = !package.has_dependency_blockers() && !package.has_readiness_gaps();
        let highest_policy_tier = task
            .map(|task| package.highest_policy_tier.max(task.highest_policy_tier))
            .unwrap_or(package.highest_policy_tier);
        let activation_ready = package.activation_ready() && task_activation_ready;
        let ready_for_handoff = package.ready_for_handoff();
        let review_required = package.review_required() || task_review_required;
        let approval_required =
            review_required || highest_policy_tier >= PrivilegeTier::HumanApproval;
        let blocked = package.blocked() || task_blocked || !dependency_ready;
        let monitor_only = package.monitor_only() || task_monitor_only;
        let operator_required = package.operator_required()
            || task_operator_required
            || (review_required && !monitor_only)
            || (blocked && !monitor_only);
        let executable = ready_for_handoff
            && activation_ready
            && dependency_ready
            && !approval_required
            && !blocked
            && !monitor_only;
        let execution_status = if monitor_only {
            IntegrationActivationExecutionStatus::Monitoring
        } else if blocked {
            IntegrationActivationExecutionStatus::Blocked
        } else if approval_required {
            IntegrationActivationExecutionStatus::NeedsApproval
        } else if operator_required && !executable {
            IntegrationActivationExecutionStatus::OperatorRequired
        } else {
            IntegrationActivationExecutionStatus::Executable
        };
        let requires_attention = execution_status.requires_attention()
            || package.requires_attention()
            || task
                .map(IntegrationActivationOperatorTask::requires_attention)
                .unwrap_or(false);

        Self {
            sequence: package.sequence,
            handoff_sequence: package.sequence,
            runbook_sequence: package.runbook_sequence,
            operator_task_sequence: task.map(|task| task.sequence),
            priority: package.priority,
            execution_status,
            handoff_status: package.handoff_status,
            phase: package.phase,
            task_kind: task.map(|task| task.task_kind),
            playbook_action: package.playbook_action,
            recommended_view: package.recommended_view,
            integration_ids: package.integration_ids,
            integration_count: package.integration_count,
            risk_ids: package.risk_ids,
            dependency_integration_ids: package.dependency_integration_ids,
            blocking_dependency_integration_ids: package.blocking_dependency_integration_ids,
            risk_count: package.risk_count,
            dependency_edge_count: package.dependency_edge_count,
            blocking_dependency_edge_count: package.blocking_dependency_edge_count,
            readiness_gap_count: package.readiness_gap_count,
            audit_record_count: package.audit_record_count,
            attention_audit_record_count: package.attention_audit_record_count,
            highest_policy_tier,
            operator_required,
            activation_ready,
            ready_for_handoff,
            approval_required,
            dependency_ready,
            executable,
            blocked,
            review_required,
            monitor_only,
            requires_attention,
        }
    }

    pub fn integration_count(&self) -> usize {
        self.integration_count
    }

    pub fn executable(&self) -> bool {
        self.executable
    }

    pub fn operator_required(&self) -> bool {
        self.operator_required
    }

    pub fn approval_required(&self) -> bool {
        self.approval_required
    }

    pub fn dependency_ready(&self) -> bool {
        self.dependency_ready
    }

    pub fn blocked(&self) -> bool {
        self.blocked
    }

    pub fn review_required(&self) -> bool {
        self.review_required
    }

    pub fn monitor_only(&self) -> bool {
        self.monitor_only
    }

    pub fn requires_attention(&self) -> bool {
        self.requires_attention
    }
}

impl IntegrationActivationExecutionSummary {
    pub fn from_packets<'a>(
        packets: impl IntoIterator<Item = &'a IntegrationActivationExecutionPacket>,
    ) -> Self {
        let mut integration_ids = BTreeSet::new();
        let mut summary = Self {
            total_packets: 0,
            unique_integrations: 0,
            executable_packets: 0,
            operator_required_packets: 0,
            approval_required_packets: 0,
            blocked_packets: 0,
            dependency_ready_packets: 0,
            dependency_blocked_packets: 0,
            monitor_packets: 0,
            activation_ready_packets: 0,
            packets_requiring_attention: 0,
            total_risks: 0,
            total_dependency_edges: 0,
            blocking_dependency_edges: 0,
            total_readiness_gaps: 0,
            total_audit_records: 0,
            attention_audit_records: 0,
            next_execution_status: None,
            next_task_kind: None,
            next_phase: None,
            next_playbook_action: None,
            next_recommended_view: None,
            next_packet_sequence: None,
            next_packet_priority: None,
            first_executable_sequence: None,
            first_operator_sequence: None,
            first_approval_sequence: None,
            first_blocked_sequence: None,
            first_dependency_blocked_sequence: None,
            first_attention_sequence: None,
            first_executable_priority: None,
            first_operator_priority: None,
            first_approval_priority: None,
            first_blocked_priority: None,
            first_dependency_blocked_priority: None,
            first_attention_priority: None,
            highest_policy_tier: PrivilegeTier::ReadOnly,
            overall_status: IntegrationActivationHealthStatus::Empty,
        };

        for packet in packets {
            summary.total_packets += 1;
            for integration_id in &packet.integration_ids {
                integration_ids.insert(integration_id.clone());
            }

            if summary.next_execution_status.is_none() && !packet.monitor_only() {
                summary.next_execution_status = Some(packet.execution_status);
                summary.next_task_kind = packet.task_kind;
                summary.next_phase = Some(packet.phase);
                summary.next_playbook_action = Some(packet.playbook_action);
                summary.next_recommended_view = Some(packet.recommended_view);
                summary.next_packet_sequence = Some(packet.sequence);
                summary.next_packet_priority = Some(packet.priority);
            }

            if packet.executable() {
                summary.executable_packets += 1;
                summary.first_executable_sequence =
                    summary.first_executable_sequence.or(Some(packet.sequence));
                summary.first_executable_priority =
                    min_optional_priority(summary.first_executable_priority, Some(packet.priority));
            }
            if packet.operator_required() {
                summary.operator_required_packets += 1;
                summary.first_operator_sequence =
                    summary.first_operator_sequence.or(Some(packet.sequence));
                summary.first_operator_priority =
                    min_optional_priority(summary.first_operator_priority, Some(packet.priority));
            }
            if packet.approval_required() {
                summary.approval_required_packets += 1;
                summary.first_approval_sequence =
                    summary.first_approval_sequence.or(Some(packet.sequence));
                summary.first_approval_priority =
                    min_optional_priority(summary.first_approval_priority, Some(packet.priority));
            }
            if packet.blocked() {
                summary.blocked_packets += 1;
                summary.first_blocked_sequence =
                    summary.first_blocked_sequence.or(Some(packet.sequence));
                summary.first_blocked_priority =
                    min_optional_priority(summary.first_blocked_priority, Some(packet.priority));
            }
            if packet.dependency_ready() {
                summary.dependency_ready_packets += 1;
            } else {
                summary.dependency_blocked_packets += 1;
                summary.first_dependency_blocked_sequence = summary
                    .first_dependency_blocked_sequence
                    .or(Some(packet.sequence));
                summary.first_dependency_blocked_priority = min_optional_priority(
                    summary.first_dependency_blocked_priority,
                    Some(packet.priority),
                );
            }
            if packet.monitor_only() {
                summary.monitor_packets += 1;
            }
            if packet.activation_ready {
                summary.activation_ready_packets += 1;
            }
            if packet.requires_attention() {
                summary.packets_requiring_attention += 1;
                summary.first_attention_sequence =
                    summary.first_attention_sequence.or(Some(packet.sequence));
                summary.first_attention_priority =
                    min_optional_priority(summary.first_attention_priority, Some(packet.priority));
            }

            summary.total_risks += packet.risk_count;
            summary.total_dependency_edges += packet.dependency_edge_count;
            summary.blocking_dependency_edges += packet.blocking_dependency_edge_count;
            summary.total_readiness_gaps += packet.readiness_gap_count;
            summary.total_audit_records += packet.audit_record_count;
            summary.attention_audit_records += packet.attention_audit_record_count;
            summary.highest_policy_tier =
                summary.highest_policy_tier.max(packet.highest_policy_tier);
        }

        summary.unique_integrations = integration_ids.len();
        summary.overall_status = if summary.blocked_packets > 0
            || summary.dependency_blocked_packets > 0
            || summary.blocking_dependency_edges > 0
            || summary.total_readiness_gaps > 0
        {
            IntegrationActivationHealthStatus::Blocked
        } else if summary.approval_required_packets > 0
            || summary.operator_required_packets > 0
            || summary.packets_requiring_attention > 0
            || summary.total_risks > 0
        {
            IntegrationActivationHealthStatus::NeedsReview
        } else if summary.executable_packets > 0 || summary.activation_ready_packets > 0 {
            IntegrationActivationHealthStatus::Ready
        } else {
            IntegrationActivationHealthStatus::Empty
        };
        summary
    }

    pub fn is_empty(&self) -> bool {
        self.total_packets == 0
    }

    pub fn has_blockers(&self) -> bool {
        self.blocked_packets > 0
            || self.dependency_blocked_packets > 0
            || self.blocking_dependency_edges > 0
            || self.total_readiness_gaps > 0
    }

    pub fn has_approval_work(&self) -> bool {
        self.approval_required_packets > 0
            || self.operator_required_packets > 0
            || self.attention_audit_records > 0
    }

    pub fn ready_to_execute(&self) -> bool {
        self.executable_packets > 0 && !self.has_blockers() && !self.has_approval_work()
    }

    pub fn requires_attention(&self) -> bool {
        self.packets_requiring_attention > 0
            || self.has_blockers()
            || self.has_approval_work()
            || self.overall_status.requires_attention()
    }
}

impl IntegrationActivationVerificationCheckpoint {
    fn from_execution_packet(packet: IntegrationActivationExecutionPacket) -> Self {
        let verification_status = if packet.monitor_only() {
            IntegrationActivationVerificationStatus::Monitoring
        } else if packet.blocked() || !packet.dependency_ready() {
            IntegrationActivationVerificationStatus::Blocked
        } else if packet.approval_required() {
            IntegrationActivationVerificationStatus::PendingApproval
        } else if packet.operator_required() && !packet.executable() {
            IntegrationActivationVerificationStatus::PendingOperator
        } else {
            IntegrationActivationVerificationStatus::ReadyToVerify
        };
        let evidence_review_required =
            packet.review_required() || packet.attention_audit_record_count > 0;
        let can_verify =
            packet.executable() && packet.dependency_ready() && !evidence_review_required;
        let verification_ready = can_verify && packet.audit_record_count > 0;
        let requires_attention = verification_status.requires_attention()
            || evidence_review_required
            || packet.requires_attention();
        let operator_pending = packet.operator_required() && !packet.executable();
        let approval_pending = packet.approval_required();
        let dependency_ready = packet.dependency_ready();
        let blocked = packet.blocked();
        let monitor_only = packet.monitor_only();

        Self {
            sequence: packet.sequence,
            execution_sequence: packet.sequence,
            handoff_sequence: packet.handoff_sequence,
            runbook_sequence: packet.runbook_sequence,
            operator_task_sequence: packet.operator_task_sequence,
            priority: packet.priority,
            verification_status,
            execution_status: packet.execution_status,
            handoff_status: packet.handoff_status,
            phase: packet.phase,
            task_kind: packet.task_kind,
            playbook_action: packet.playbook_action,
            recommended_view: packet.recommended_view,
            integration_ids: packet.integration_ids,
            integration_count: packet.integration_count,
            risk_ids: packet.risk_ids,
            dependency_integration_ids: packet.dependency_integration_ids,
            blocking_dependency_integration_ids: packet.blocking_dependency_integration_ids,
            risk_count: packet.risk_count,
            dependency_edge_count: packet.dependency_edge_count,
            blocking_dependency_edge_count: packet.blocking_dependency_edge_count,
            readiness_gap_count: packet.readiness_gap_count,
            audit_record_count: packet.audit_record_count,
            attention_audit_record_count: packet.attention_audit_record_count,
            highest_policy_tier: packet.highest_policy_tier,
            operator_pending,
            approval_pending,
            dependency_ready,
            can_verify,
            verification_ready,
            blocked,
            monitor_only,
            evidence_review_required,
            requires_attention,
        }
    }

    pub fn integration_count(&self) -> usize {
        self.integration_count
    }

    pub fn can_verify(&self) -> bool {
        self.can_verify
    }

    pub fn verification_ready(&self) -> bool {
        self.verification_ready
    }

    pub fn operator_pending(&self) -> bool {
        self.operator_pending
    }

    pub fn approval_pending(&self) -> bool {
        self.approval_pending
    }

    pub fn dependency_ready(&self) -> bool {
        self.dependency_ready
    }

    pub fn blocked(&self) -> bool {
        self.blocked
    }

    pub fn monitor_only(&self) -> bool {
        self.monitor_only
    }

    pub fn evidence_review_required(&self) -> bool {
        self.evidence_review_required
    }

    pub fn requires_attention(&self) -> bool {
        self.requires_attention
    }
}

impl IntegrationActivationVerificationSummary {
    pub fn from_checkpoints<'a>(
        checkpoints: impl IntoIterator<Item = &'a IntegrationActivationVerificationCheckpoint>,
    ) -> Self {
        let mut integration_ids = BTreeSet::new();
        let mut summary = Self {
            total_checkpoints: 0,
            unique_integrations: 0,
            ready_to_verify_checkpoints: 0,
            verification_ready_checkpoints: 0,
            pending_operator_checkpoints: 0,
            pending_approval_checkpoints: 0,
            blocked_checkpoints: 0,
            dependency_ready_checkpoints: 0,
            dependency_blocked_checkpoints: 0,
            monitor_checkpoints: 0,
            evidence_review_checkpoints: 0,
            checkpoints_requiring_attention: 0,
            total_risks: 0,
            total_dependency_edges: 0,
            blocking_dependency_edges: 0,
            total_readiness_gaps: 0,
            total_audit_records: 0,
            attention_audit_records: 0,
            next_verification_status: None,
            next_execution_status: None,
            next_task_kind: None,
            next_phase: None,
            next_playbook_action: None,
            next_recommended_view: None,
            next_checkpoint_sequence: None,
            next_checkpoint_priority: None,
            first_ready_to_verify_sequence: None,
            first_pending_operator_sequence: None,
            first_pending_approval_sequence: None,
            first_blocked_sequence: None,
            first_attention_sequence: None,
            first_ready_to_verify_priority: None,
            first_pending_operator_priority: None,
            first_pending_approval_priority: None,
            first_blocked_priority: None,
            first_attention_priority: None,
            highest_policy_tier: PrivilegeTier::ReadOnly,
            overall_status: IntegrationActivationHealthStatus::Empty,
        };

        for checkpoint in checkpoints {
            summary.total_checkpoints += 1;
            for integration_id in &checkpoint.integration_ids {
                integration_ids.insert(integration_id.clone());
            }

            if summary.next_verification_status.is_none() && !checkpoint.monitor_only() {
                summary.next_verification_status = Some(checkpoint.verification_status);
                summary.next_execution_status = Some(checkpoint.execution_status);
                summary.next_task_kind = checkpoint.task_kind;
                summary.next_phase = Some(checkpoint.phase);
                summary.next_playbook_action = Some(checkpoint.playbook_action);
                summary.next_recommended_view = Some(checkpoint.recommended_view);
                summary.next_checkpoint_sequence = Some(checkpoint.sequence);
                summary.next_checkpoint_priority = Some(checkpoint.priority);
            }

            match checkpoint.verification_status {
                IntegrationActivationVerificationStatus::ReadyToVerify => {
                    summary.ready_to_verify_checkpoints += 1;
                    summary.first_ready_to_verify_sequence = summary
                        .first_ready_to_verify_sequence
                        .or(Some(checkpoint.sequence));
                    summary.first_ready_to_verify_priority = min_optional_priority(
                        summary.first_ready_to_verify_priority,
                        Some(checkpoint.priority),
                    );
                }
                IntegrationActivationVerificationStatus::PendingOperator => {
                    summary.pending_operator_checkpoints += 1;
                    summary.first_pending_operator_sequence = summary
                        .first_pending_operator_sequence
                        .or(Some(checkpoint.sequence));
                    summary.first_pending_operator_priority = min_optional_priority(
                        summary.first_pending_operator_priority,
                        Some(checkpoint.priority),
                    );
                }
                IntegrationActivationVerificationStatus::PendingApproval => {
                    summary.pending_approval_checkpoints += 1;
                    summary.first_pending_approval_sequence = summary
                        .first_pending_approval_sequence
                        .or(Some(checkpoint.sequence));
                    summary.first_pending_approval_priority = min_optional_priority(
                        summary.first_pending_approval_priority,
                        Some(checkpoint.priority),
                    );
                }
                IntegrationActivationVerificationStatus::Blocked => {
                    summary.blocked_checkpoints += 1;
                    summary.first_blocked_sequence =
                        summary.first_blocked_sequence.or(Some(checkpoint.sequence));
                    summary.first_blocked_priority = min_optional_priority(
                        summary.first_blocked_priority,
                        Some(checkpoint.priority),
                    );
                }
                IntegrationActivationVerificationStatus::Monitoring => {
                    summary.monitor_checkpoints += 1;
                }
            }

            if checkpoint.verification_ready() {
                summary.verification_ready_checkpoints += 1;
            }
            if checkpoint.dependency_ready() {
                summary.dependency_ready_checkpoints += 1;
            } else {
                summary.dependency_blocked_checkpoints += 1;
            }
            if checkpoint.evidence_review_required() {
                summary.evidence_review_checkpoints += 1;
            }
            if checkpoint.requires_attention() {
                summary.checkpoints_requiring_attention += 1;
                summary.first_attention_sequence = summary
                    .first_attention_sequence
                    .or(Some(checkpoint.sequence));
                summary.first_attention_priority = min_optional_priority(
                    summary.first_attention_priority,
                    Some(checkpoint.priority),
                );
            }

            summary.total_risks += checkpoint.risk_count;
            summary.total_dependency_edges += checkpoint.dependency_edge_count;
            summary.blocking_dependency_edges += checkpoint.blocking_dependency_edge_count;
            summary.total_readiness_gaps += checkpoint.readiness_gap_count;
            summary.total_audit_records += checkpoint.audit_record_count;
            summary.attention_audit_records += checkpoint.attention_audit_record_count;
            summary.highest_policy_tier = summary
                .highest_policy_tier
                .max(checkpoint.highest_policy_tier);
        }

        summary.unique_integrations = integration_ids.len();
        summary.overall_status = if summary.blocked_checkpoints > 0
            || summary.dependency_blocked_checkpoints > 0
            || summary.blocking_dependency_edges > 0
            || summary.total_readiness_gaps > 0
        {
            IntegrationActivationHealthStatus::Blocked
        } else if summary.pending_approval_checkpoints > 0
            || summary.pending_operator_checkpoints > 0
            || summary.evidence_review_checkpoints > 0
            || summary.checkpoints_requiring_attention > 0
            || summary.total_risks > 0
        {
            IntegrationActivationHealthStatus::NeedsReview
        } else if summary.ready_to_verify_checkpoints > 0
            || summary.verification_ready_checkpoints > 0
        {
            IntegrationActivationHealthStatus::Ready
        } else {
            IntegrationActivationHealthStatus::Empty
        };
        summary
    }

    pub fn is_empty(&self) -> bool {
        self.total_checkpoints == 0
    }

    pub fn has_blockers(&self) -> bool {
        self.blocked_checkpoints > 0
            || self.dependency_blocked_checkpoints > 0
            || self.blocking_dependency_edges > 0
            || self.total_readiness_gaps > 0
    }

    pub fn has_pending_work(&self) -> bool {
        self.pending_operator_checkpoints > 0
            || self.pending_approval_checkpoints > 0
            || self.evidence_review_checkpoints > 0
    }

    pub fn ready_to_verify(&self) -> bool {
        self.ready_to_verify_checkpoints > 0 && !self.has_blockers() && !self.has_pending_work()
    }

    pub fn requires_attention(&self) -> bool {
        self.checkpoints_requiring_attention > 0
            || self.has_blockers()
            || self.has_pending_work()
            || self.overall_status.requires_attention()
    }
}

impl IntegrationActivationOperatorTask {
    fn from_playbook_step(step: IntegrationActivationPlaybookStep) -> Self {
        let task_kind =
            IntegrationActivationOperatorTaskKind::from_playbook_action(step.playbook_action);
        let operator_required = step.operator_required();
        let activation_ready = step.activation_ready();
        let blocked = step.blocked();
        let review_required = step.review_required();
        let monitor_only = step.monitor_only();
        let actionable = !monitor_only && (operator_required || activation_ready);
        let requires_attention = operator_required || step.requires_attention();

        Self {
            sequence: step.sequence,
            playbook_sequence: step.sequence,
            priority: step.priority,
            task_kind,
            playbook_action: step.playbook_action,
            recommended_view: step.recommended_view,
            milestone_kind: step.milestone_kind,
            health_status: step.health_status,
            integration_ids: step.integration_ids,
            integration_count: step.integration_count,
            action_count: step.action_count,
            dossier_count: step.dossier_count,
            evidence_count: step.evidence_count,
            risk_count: step.risk_count,
            dependency_edge_count: step.dependency_edge_count,
            blocking_dependency_edge_count: step.blocking_dependency_edge_count,
            highest_policy_tier: step.highest_policy_tier,
            operator_required,
            actionable,
            activation_ready,
            blocked,
            review_required,
            monitor_only,
            requires_attention,
        }
    }

    pub fn integration_count(&self) -> usize {
        self.integration_count
    }

    pub fn operator_required(&self) -> bool {
        self.operator_required
    }

    pub fn actionable(&self) -> bool {
        self.actionable
    }

    pub fn activation_ready(&self) -> bool {
        self.activation_ready
    }

    pub fn blocked(&self) -> bool {
        self.blocked
    }

    pub fn review_required(&self) -> bool {
        self.review_required
    }

    pub fn monitor_only(&self) -> bool {
        self.monitor_only
    }

    pub fn requires_attention(&self) -> bool {
        self.requires_attention
    }
}

impl IntegrationActivationOperatorTaskSummary {
    pub fn from_tasks<'a>(
        tasks: impl IntoIterator<Item = &'a IntegrationActivationOperatorTask>,
    ) -> Self {
        let mut integration_ids = BTreeSet::new();
        let mut summary = Self {
            total_tasks: 0,
            unique_integrations: 0,
            operator_required_tasks: 0,
            actionable_tasks: 0,
            activation_ready_tasks: 0,
            blocked_tasks: 0,
            review_required_tasks: 0,
            monitor_tasks: 0,
            resolve_constraint_tasks: 0,
            enable_dependency_tasks: 0,
            prepare_approval_tasks: 0,
            complete_review_tasks: 0,
            review_risk_tasks: 0,
            activate_wave_tasks: 0,
            monitor_wave_tasks: 0,
            total_actions: 0,
            total_dossiers: 0,
            total_evidence: 0,
            total_risks: 0,
            total_dependency_edges: 0,
            blocking_dependency_edges: 0,
            next_task_kind: None,
            next_playbook_action: None,
            next_recommended_view: None,
            next_task_sequence: None,
            next_task_priority: None,
            first_operator_sequence: None,
            first_actionable_sequence: None,
            first_activation_sequence: None,
            first_blocked_sequence: None,
            first_review_sequence: None,
            first_operator_priority: None,
            first_actionable_priority: None,
            first_activation_priority: None,
            first_blocked_priority: None,
            first_review_priority: None,
            highest_policy_tier: PrivilegeTier::ReadOnly,
            overall_status: IntegrationActivationHealthStatus::Empty,
        };

        for task in tasks {
            summary.total_tasks += 1;
            for integration_id in &task.integration_ids {
                integration_ids.insert(integration_id.clone());
            }

            match task.task_kind {
                IntegrationActivationOperatorTaskKind::ResolveConstraints => {
                    summary.resolve_constraint_tasks += 1
                }
                IntegrationActivationOperatorTaskKind::EnableDependencies => {
                    summary.enable_dependency_tasks += 1
                }
                IntegrationActivationOperatorTaskKind::PrepareApproval => {
                    summary.prepare_approval_tasks += 1
                }
                IntegrationActivationOperatorTaskKind::CompleteReview => {
                    summary.complete_review_tasks += 1
                }
                IntegrationActivationOperatorTaskKind::ReviewRisk => summary.review_risk_tasks += 1,
                IntegrationActivationOperatorTaskKind::ActivateWave => {
                    summary.activate_wave_tasks += 1
                }
                IntegrationActivationOperatorTaskKind::MonitorWave => {
                    summary.monitor_wave_tasks += 1
                }
            }

            if summary.next_task_kind.is_none() && !task.monitor_only() {
                summary.next_task_kind = Some(task.task_kind);
                summary.next_playbook_action = Some(task.playbook_action);
                summary.next_recommended_view = Some(task.recommended_view);
                summary.next_task_sequence = Some(task.sequence);
                summary.next_task_priority = Some(task.priority);
            }

            if task.operator_required() {
                summary.operator_required_tasks += 1;
                summary.first_operator_sequence =
                    summary.first_operator_sequence.or(Some(task.sequence));
                summary.first_operator_priority =
                    min_optional_priority(summary.first_operator_priority, Some(task.priority));
            }
            if task.actionable() {
                summary.actionable_tasks += 1;
                summary.first_actionable_sequence =
                    summary.first_actionable_sequence.or(Some(task.sequence));
                summary.first_actionable_priority =
                    min_optional_priority(summary.first_actionable_priority, Some(task.priority));
            }
            if task.activation_ready() {
                summary.activation_ready_tasks += 1;
                summary.first_activation_sequence =
                    summary.first_activation_sequence.or(Some(task.sequence));
                summary.first_activation_priority =
                    min_optional_priority(summary.first_activation_priority, Some(task.priority));
            }
            if task.blocked() {
                summary.blocked_tasks += 1;
                summary.first_blocked_sequence =
                    summary.first_blocked_sequence.or(Some(task.sequence));
                summary.first_blocked_priority =
                    min_optional_priority(summary.first_blocked_priority, Some(task.priority));
            }
            if task.review_required() {
                summary.review_required_tasks += 1;
                summary.first_review_sequence =
                    summary.first_review_sequence.or(Some(task.sequence));
                summary.first_review_priority =
                    min_optional_priority(summary.first_review_priority, Some(task.priority));
            }
            if task.monitor_only() {
                summary.monitor_tasks += 1;
            }

            summary.total_actions += task.action_count;
            summary.total_dossiers += task.dossier_count;
            summary.total_evidence += task.evidence_count;
            summary.total_risks += task.risk_count;
            summary.total_dependency_edges += task.dependency_edge_count;
            summary.blocking_dependency_edges += task.blocking_dependency_edge_count;
            summary.highest_policy_tier = summary.highest_policy_tier.max(task.highest_policy_tier);
        }

        summary.unique_integrations = integration_ids.len();
        summary.overall_status = if summary.blocked_tasks > 0 {
            IntegrationActivationHealthStatus::Blocked
        } else if summary.review_required_tasks > 0 || summary.operator_required_tasks > 0 {
            IntegrationActivationHealthStatus::NeedsReview
        } else if summary.activation_ready_tasks > 0 || summary.actionable_tasks > 0 {
            IntegrationActivationHealthStatus::Ready
        } else {
            IntegrationActivationHealthStatus::Empty
        };
        summary
    }

    pub fn is_empty(&self) -> bool {
        self.total_tasks == 0
    }

    pub fn has_operator_work(&self) -> bool {
        self.operator_required_tasks > 0
    }

    pub fn has_actionable_work(&self) -> bool {
        self.actionable_tasks > 0
    }

    pub fn has_activation_work(&self) -> bool {
        self.activation_ready_tasks > 0 || self.activate_wave_tasks > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.blocked_tasks > 0
            || self.resolve_constraint_tasks > 0
            || self.enable_dependency_tasks > 0
    }

    pub fn has_review_work(&self) -> bool {
        self.review_required_tasks > 0
            || self.prepare_approval_tasks > 0
            || self.complete_review_tasks > 0
            || self.review_risk_tasks > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.has_operator_work()
            || self.has_blockers()
            || self.has_review_work()
            || self.overall_status.requires_attention()
    }
}

impl IntegrationActivationControlRoomPanel {
    fn from_tasks(
        sequence: usize,
        recommended_view: IntegrationActivationPlaybookView,
        tasks: Vec<IntegrationActivationOperatorTask>,
    ) -> Self {
        let operator_summary = IntegrationActivationOperatorTaskSummary::from_tasks(tasks.iter());
        let mut task_sequences = tasks.iter().map(|task| task.sequence).collect::<Vec<_>>();
        task_sequences.sort_unstable();

        let mut integration_ids = tasks
            .iter()
            .flat_map(|task| task.integration_ids.iter().cloned())
            .collect::<BTreeSet<_>>()
            .into_iter()
            .collect::<Vec<_>>();
        integration_ids.sort();

        let priority = tasks
            .iter()
            .map(|task| task.priority)
            .min()
            .unwrap_or(u8::MAX);

        Self {
            sequence,
            recommended_view,
            priority,
            task_sequences,
            integration_ids,
            task_count: operator_summary.total_tasks,
            operator_summary,
        }
    }

    pub fn integration_count(&self) -> usize {
        self.integration_ids.len()
    }

    pub fn has_operator_work(&self) -> bool {
        self.operator_summary.has_operator_work()
    }

    pub fn has_actionable_work(&self) -> bool {
        self.operator_summary.has_actionable_work()
    }

    pub fn has_activation_work(&self) -> bool {
        self.operator_summary.has_activation_work()
    }

    pub fn has_blockers(&self) -> bool {
        self.operator_summary.has_blockers()
    }

    pub fn has_review_work(&self) -> bool {
        self.operator_summary.has_review_work()
    }

    pub fn requires_attention(&self) -> bool {
        self.operator_summary.requires_attention()
    }
}

impl IntegrationActivationControlRoomSummary {
    pub fn from_panels<'a>(
        panels: impl IntoIterator<Item = &'a IntegrationActivationControlRoomPanel>,
    ) -> Self {
        let mut integration_ids = BTreeSet::new();
        let mut summary = Self {
            total_panels: 0,
            unique_integrations: 0,
            panels_requiring_attention: 0,
            panels_with_operator_work: 0,
            panels_with_actionable_work: 0,
            panels_with_activation_work: 0,
            panels_with_blockers: 0,
            panels_with_review_work: 0,
            constraint_panels: 0,
            dependency_panels: 0,
            approval_panels: 0,
            review_panels: 0,
            risk_panels: 0,
            action_panels: 0,
            dashboard_panels: 0,
            total_tasks: 0,
            operator_required_tasks: 0,
            actionable_tasks: 0,
            activation_ready_tasks: 0,
            blocked_tasks: 0,
            review_required_tasks: 0,
            monitor_tasks: 0,
            next_recommended_view: None,
            next_task_kind: None,
            next_panel_sequence: None,
            next_panel_priority: None,
            first_blocked_panel_sequence: None,
            first_review_panel_sequence: None,
            first_activation_panel_sequence: None,
            first_actionable_panel_sequence: None,
            first_blocked_panel_priority: None,
            first_review_panel_priority: None,
            first_activation_panel_priority: None,
            first_actionable_panel_priority: None,
            highest_policy_tier: PrivilegeTier::ReadOnly,
            overall_status: IntegrationActivationHealthStatus::Empty,
        };

        for panel in panels {
            summary.total_panels += 1;
            for integration_id in &panel.integration_ids {
                integration_ids.insert(integration_id.clone());
            }

            match panel.recommended_view {
                IntegrationActivationPlaybookView::ConstraintQueue => {
                    summary.constraint_panels += 1
                }
                IntegrationActivationPlaybookView::DependencyGraph => {
                    summary.dependency_panels += 1
                }
                IntegrationActivationPlaybookView::ApprovalPackets => summary.approval_panels += 1,
                IntegrationActivationPlaybookView::ReviewQueue => summary.review_panels += 1,
                IntegrationActivationPlaybookView::RiskRegister => summary.risk_panels += 1,
                IntegrationActivationPlaybookView::ActivationActions => summary.action_panels += 1,
                IntegrationActivationPlaybookView::StatusDashboard => summary.dashboard_panels += 1,
            }

            if summary.next_recommended_view.is_none()
                && panel.operator_summary.next_task_kind.is_some()
            {
                summary.next_recommended_view = Some(panel.recommended_view);
                summary.next_task_kind = panel.operator_summary.next_task_kind;
                summary.next_panel_sequence = Some(panel.sequence);
                summary.next_panel_priority = Some(panel.priority);
            }

            if panel.requires_attention() {
                summary.panels_requiring_attention += 1;
            }
            if panel.has_operator_work() {
                summary.panels_with_operator_work += 1;
            }
            if panel.has_actionable_work() {
                summary.panels_with_actionable_work += 1;
                summary.first_actionable_panel_sequence = summary
                    .first_actionable_panel_sequence
                    .or(Some(panel.sequence));
                summary.first_actionable_panel_priority = min_optional_priority(
                    summary.first_actionable_panel_priority,
                    Some(panel.priority),
                );
            }
            if panel.has_activation_work() {
                summary.panels_with_activation_work += 1;
                summary.first_activation_panel_sequence = summary
                    .first_activation_panel_sequence
                    .or(Some(panel.sequence));
                summary.first_activation_panel_priority = min_optional_priority(
                    summary.first_activation_panel_priority,
                    Some(panel.priority),
                );
            }
            if panel.has_blockers() {
                summary.panels_with_blockers += 1;
                summary.first_blocked_panel_sequence = summary
                    .first_blocked_panel_sequence
                    .or(Some(panel.sequence));
                summary.first_blocked_panel_priority = min_optional_priority(
                    summary.first_blocked_panel_priority,
                    Some(panel.priority),
                );
            }
            if panel.has_review_work() {
                summary.panels_with_review_work += 1;
                summary.first_review_panel_sequence =
                    summary.first_review_panel_sequence.or(Some(panel.sequence));
                summary.first_review_panel_priority = min_optional_priority(
                    summary.first_review_panel_priority,
                    Some(panel.priority),
                );
            }

            summary.total_tasks += panel.operator_summary.total_tasks;
            summary.operator_required_tasks += panel.operator_summary.operator_required_tasks;
            summary.actionable_tasks += panel.operator_summary.actionable_tasks;
            summary.activation_ready_tasks += panel.operator_summary.activation_ready_tasks;
            summary.blocked_tasks += panel.operator_summary.blocked_tasks;
            summary.review_required_tasks += panel.operator_summary.review_required_tasks;
            summary.monitor_tasks += panel.operator_summary.monitor_tasks;
            summary.highest_policy_tier = summary
                .highest_policy_tier
                .max(panel.operator_summary.highest_policy_tier);
        }

        summary.unique_integrations = integration_ids.len();
        summary.overall_status = if summary.panels_with_blockers > 0 {
            IntegrationActivationHealthStatus::Blocked
        } else if summary.panels_with_review_work > 0 || summary.panels_with_operator_work > 0 {
            IntegrationActivationHealthStatus::NeedsReview
        } else if summary.panels_with_activation_work > 0 || summary.panels_with_actionable_work > 0
        {
            IntegrationActivationHealthStatus::Ready
        } else {
            IntegrationActivationHealthStatus::Empty
        };
        summary
    }

    pub fn is_empty(&self) -> bool {
        self.total_panels == 0
    }

    pub fn has_operator_work(&self) -> bool {
        self.panels_with_operator_work > 0
    }

    pub fn has_actionable_work(&self) -> bool {
        self.panels_with_actionable_work > 0
    }

    pub fn has_activation_work(&self) -> bool {
        self.panels_with_activation_work > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.panels_with_blockers > 0
    }

    pub fn has_review_work(&self) -> bool {
        self.panels_with_review_work > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.panels_requiring_attention > 0 || self.overall_status.requires_attention()
    }
}

impl IntegrationActivationCommandCenterSection {
    fn from_panels(
        sequence: usize,
        section_kind: IntegrationActivationCommandCenterSectionKind,
        panels: Vec<IntegrationActivationControlRoomPanel>,
    ) -> Self {
        let control_room_summary =
            IntegrationActivationControlRoomSummary::from_panels(panels.iter());
        let mut panel_sequences = panels
            .iter()
            .map(|panel| panel.sequence)
            .collect::<Vec<_>>();
        panel_sequences.sort_unstable();

        let mut recommended_views = panels
            .iter()
            .map(|panel| panel.recommended_view)
            .collect::<BTreeSet<_>>()
            .into_iter()
            .collect::<Vec<_>>();
        recommended_views.sort();

        let mut integration_ids = panels
            .iter()
            .flat_map(|panel| panel.integration_ids.iter().cloned())
            .collect::<BTreeSet<_>>()
            .into_iter()
            .collect::<Vec<_>>();
        integration_ids.sort();

        let priority = panels
            .iter()
            .map(|panel| panel.priority)
            .min()
            .unwrap_or(u8::MAX);

        Self {
            sequence,
            section_kind,
            priority,
            panel_sequences,
            recommended_views,
            integration_ids,
            panel_count: control_room_summary.total_panels,
            control_room_summary,
        }
    }

    pub fn integration_count(&self) -> usize {
        self.integration_ids.len()
    }

    pub fn has_operator_work(&self) -> bool {
        self.control_room_summary.has_operator_work()
    }

    pub fn has_actionable_work(&self) -> bool {
        self.control_room_summary.has_actionable_work()
    }

    pub fn has_activation_work(&self) -> bool {
        self.control_room_summary.has_activation_work()
    }

    pub fn has_blockers(&self) -> bool {
        self.control_room_summary.has_blockers()
    }

    pub fn has_review_work(&self) -> bool {
        self.control_room_summary.has_review_work()
    }

    pub fn requires_attention(&self) -> bool {
        self.control_room_summary.requires_attention()
    }
}

impl IntegrationActivationCommandCenterSummary {
    pub fn from_sections<'a>(
        sections: impl IntoIterator<Item = &'a IntegrationActivationCommandCenterSection>,
    ) -> Self {
        let mut integration_ids = BTreeSet::new();
        let mut summary = Self {
            total_sections: 0,
            unique_integrations: 0,
            sections_requiring_attention: 0,
            sections_with_operator_work: 0,
            sections_with_actionable_work: 0,
            sections_with_activation_work: 0,
            sections_with_blockers: 0,
            sections_with_review_work: 0,
            blocker_sections: 0,
            review_sections: 0,
            activation_sections: 0,
            actionable_sections: 0,
            monitoring_sections: 0,
            total_panels: 0,
            total_tasks: 0,
            operator_required_tasks: 0,
            actionable_tasks: 0,
            activation_ready_tasks: 0,
            blocked_tasks: 0,
            review_required_tasks: 0,
            monitor_tasks: 0,
            next_section_kind: None,
            next_recommended_view: None,
            next_task_kind: None,
            next_section_sequence: None,
            next_section_priority: None,
            first_blocker_section_sequence: None,
            first_review_section_sequence: None,
            first_activation_section_sequence: None,
            first_actionable_section_sequence: None,
            first_blocker_section_priority: None,
            first_review_section_priority: None,
            first_activation_section_priority: None,
            first_actionable_section_priority: None,
            highest_policy_tier: PrivilegeTier::ReadOnly,
            overall_status: IntegrationActivationHealthStatus::Empty,
        };

        for section in sections {
            summary.total_sections += 1;
            for integration_id in &section.integration_ids {
                integration_ids.insert(integration_id.clone());
            }

            match section.section_kind {
                IntegrationActivationCommandCenterSectionKind::Blockers => {
                    summary.blocker_sections += 1
                }
                IntegrationActivationCommandCenterSectionKind::Review => {
                    summary.review_sections += 1
                }
                IntegrationActivationCommandCenterSectionKind::Activation => {
                    summary.activation_sections += 1
                }
                IntegrationActivationCommandCenterSectionKind::Actionable => {
                    summary.actionable_sections += 1
                }
                IntegrationActivationCommandCenterSectionKind::Monitoring => {
                    summary.monitoring_sections += 1
                }
            }

            if summary.next_section_kind.is_none()
                && section.control_room_summary.next_task_kind.is_some()
            {
                summary.next_section_kind = Some(section.section_kind);
                summary.next_recommended_view = section.control_room_summary.next_recommended_view;
                summary.next_task_kind = section.control_room_summary.next_task_kind;
                summary.next_section_sequence = Some(section.sequence);
                summary.next_section_priority = Some(section.priority);
            }

            if section.requires_attention() {
                summary.sections_requiring_attention += 1;
            }
            if section.has_operator_work() {
                summary.sections_with_operator_work += 1;
            }
            if section.has_actionable_work() {
                summary.sections_with_actionable_work += 1;
                summary.first_actionable_section_sequence = summary
                    .first_actionable_section_sequence
                    .or(Some(section.sequence));
                summary.first_actionable_section_priority = min_optional_priority(
                    summary.first_actionable_section_priority,
                    Some(section.priority),
                );
            }
            if section.has_activation_work() {
                summary.sections_with_activation_work += 1;
                summary.first_activation_section_sequence = summary
                    .first_activation_section_sequence
                    .or(Some(section.sequence));
                summary.first_activation_section_priority = min_optional_priority(
                    summary.first_activation_section_priority,
                    Some(section.priority),
                );
            }
            if section.has_blockers() {
                summary.sections_with_blockers += 1;
                summary.first_blocker_section_sequence = summary
                    .first_blocker_section_sequence
                    .or(Some(section.sequence));
                summary.first_blocker_section_priority = min_optional_priority(
                    summary.first_blocker_section_priority,
                    Some(section.priority),
                );
            }
            if section.has_review_work() {
                summary.sections_with_review_work += 1;
                summary.first_review_section_sequence = summary
                    .first_review_section_sequence
                    .or(Some(section.sequence));
                summary.first_review_section_priority = min_optional_priority(
                    summary.first_review_section_priority,
                    Some(section.priority),
                );
            }

            summary.total_panels += section.control_room_summary.total_panels;
            summary.total_tasks += section.control_room_summary.total_tasks;
            summary.operator_required_tasks += section.control_room_summary.operator_required_tasks;
            summary.actionable_tasks += section.control_room_summary.actionable_tasks;
            summary.activation_ready_tasks += section.control_room_summary.activation_ready_tasks;
            summary.blocked_tasks += section.control_room_summary.blocked_tasks;
            summary.review_required_tasks += section.control_room_summary.review_required_tasks;
            summary.monitor_tasks += section.control_room_summary.monitor_tasks;
            summary.highest_policy_tier = summary
                .highest_policy_tier
                .max(section.control_room_summary.highest_policy_tier);
        }

        summary.unique_integrations = integration_ids.len();
        summary.overall_status = if summary.sections_with_blockers > 0 {
            IntegrationActivationHealthStatus::Blocked
        } else if summary.sections_with_review_work > 0 || summary.sections_with_operator_work > 0 {
            IntegrationActivationHealthStatus::NeedsReview
        } else if summary.sections_with_activation_work > 0
            || summary.sections_with_actionable_work > 0
        {
            IntegrationActivationHealthStatus::Ready
        } else {
            IntegrationActivationHealthStatus::Empty
        };
        summary
    }

    pub fn is_empty(&self) -> bool {
        self.total_sections == 0
    }

    pub fn has_operator_work(&self) -> bool {
        self.sections_with_operator_work > 0
    }

    pub fn has_actionable_work(&self) -> bool {
        self.sections_with_actionable_work > 0
    }

    pub fn has_activation_work(&self) -> bool {
        self.sections_with_activation_work > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.sections_with_blockers > 0
    }

    pub fn has_review_work(&self) -> bool {
        self.sections_with_review_work > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.sections_requiring_attention > 0 || self.overall_status.requires_attention()
    }
}

impl IntegrationActivationWatchtowerSignal {
    fn from_sections(
        sequence: usize,
        signal_kind: IntegrationActivationWatchtowerSignalKind,
        sections: Vec<IntegrationActivationCommandCenterSection>,
    ) -> Self {
        let command_center_summary =
            IntegrationActivationCommandCenterSummary::from_sections(sections.iter());
        let mut section_sequences = sections
            .iter()
            .map(|section| section.sequence)
            .collect::<Vec<_>>();
        section_sequences.sort_unstable();

        let mut section_kinds = sections
            .iter()
            .map(|section| section.section_kind)
            .collect::<BTreeSet<_>>()
            .into_iter()
            .collect::<Vec<_>>();
        section_kinds.sort();

        let mut recommended_views = sections
            .iter()
            .flat_map(|section| section.recommended_views.iter().copied())
            .collect::<BTreeSet<_>>()
            .into_iter()
            .collect::<Vec<_>>();
        recommended_views.sort();

        let mut integration_ids = sections
            .iter()
            .flat_map(|section| section.integration_ids.iter().cloned())
            .collect::<BTreeSet<_>>()
            .into_iter()
            .collect::<Vec<_>>();
        integration_ids.sort();

        let priority = sections
            .iter()
            .map(|section| section.priority)
            .min()
            .unwrap_or(u8::MAX);

        Self {
            sequence,
            signal_kind,
            priority,
            section_sequences,
            section_kinds,
            recommended_views,
            integration_ids,
            section_count: command_center_summary.total_sections,
            command_center_summary,
        }
    }

    pub fn integration_count(&self) -> usize {
        self.integration_ids.len()
    }

    pub fn has_operator_work(&self) -> bool {
        self.command_center_summary.has_operator_work()
    }

    pub fn has_actionable_work(&self) -> bool {
        self.command_center_summary.has_actionable_work()
    }

    pub fn has_activation_work(&self) -> bool {
        self.command_center_summary.has_activation_work()
    }

    pub fn has_blockers(&self) -> bool {
        self.command_center_summary.has_blockers()
    }

    pub fn has_review_work(&self) -> bool {
        self.command_center_summary.has_review_work()
    }

    pub fn requires_attention(&self) -> bool {
        self.command_center_summary.requires_attention()
    }

    pub fn needs_escalation(&self) -> bool {
        self.signal_kind == IntegrationActivationWatchtowerSignalKind::Escalation
            || self.has_blockers()
    }
}

impl IntegrationActivationWatchtowerSummary {
    pub fn from_signals<'a>(
        signals: impl IntoIterator<Item = &'a IntegrationActivationWatchtowerSignal>,
    ) -> Self {
        let mut integration_ids = BTreeSet::new();
        let mut summary = Self {
            total_signals: 0,
            unique_integrations: 0,
            signals_requiring_attention: 0,
            signals_with_operator_work: 0,
            signals_with_actionable_work: 0,
            signals_with_activation_work: 0,
            signals_with_blockers: 0,
            signals_with_review_work: 0,
            escalation_signals: 0,
            review_signals: 0,
            ready_signals: 0,
            action_signals: 0,
            observation_signals: 0,
            total_sections: 0,
            total_panels: 0,
            total_tasks: 0,
            operator_required_tasks: 0,
            actionable_tasks: 0,
            activation_ready_tasks: 0,
            blocked_tasks: 0,
            review_required_tasks: 0,
            monitor_tasks: 0,
            next_signal_kind: None,
            next_section_kind: None,
            next_recommended_view: None,
            next_task_kind: None,
            next_signal_sequence: None,
            next_signal_priority: None,
            first_escalation_signal_sequence: None,
            first_review_signal_sequence: None,
            first_ready_signal_sequence: None,
            first_action_signal_sequence: None,
            first_escalation_signal_priority: None,
            first_review_signal_priority: None,
            first_ready_signal_priority: None,
            first_action_signal_priority: None,
            highest_policy_tier: PrivilegeTier::ReadOnly,
            overall_status: IntegrationActivationHealthStatus::Empty,
        };

        for signal in signals {
            summary.total_signals += 1;
            for integration_id in &signal.integration_ids {
                integration_ids.insert(integration_id.clone());
            }

            match signal.signal_kind {
                IntegrationActivationWatchtowerSignalKind::Escalation => {
                    summary.escalation_signals += 1;
                    summary.first_escalation_signal_sequence = summary
                        .first_escalation_signal_sequence
                        .or(Some(signal.sequence));
                    summary.first_escalation_signal_priority = min_optional_priority(
                        summary.first_escalation_signal_priority,
                        Some(signal.priority),
                    );
                }
                IntegrationActivationWatchtowerSignalKind::Review => {
                    summary.review_signals += 1;
                    summary.first_review_signal_sequence = summary
                        .first_review_signal_sequence
                        .or(Some(signal.sequence));
                    summary.first_review_signal_priority = min_optional_priority(
                        summary.first_review_signal_priority,
                        Some(signal.priority),
                    );
                }
                IntegrationActivationWatchtowerSignalKind::Ready => {
                    summary.ready_signals += 1;
                    summary.first_ready_signal_sequence = summary
                        .first_ready_signal_sequence
                        .or(Some(signal.sequence));
                    summary.first_ready_signal_priority = min_optional_priority(
                        summary.first_ready_signal_priority,
                        Some(signal.priority),
                    );
                }
                IntegrationActivationWatchtowerSignalKind::Action => {
                    summary.action_signals += 1;
                    summary.first_action_signal_sequence = summary
                        .first_action_signal_sequence
                        .or(Some(signal.sequence));
                    summary.first_action_signal_priority = min_optional_priority(
                        summary.first_action_signal_priority,
                        Some(signal.priority),
                    );
                }
                IntegrationActivationWatchtowerSignalKind::Observation => {
                    summary.observation_signals += 1;
                }
            }

            if summary.next_signal_kind.is_none()
                && signal.command_center_summary.next_task_kind.is_some()
            {
                summary.next_signal_kind = Some(signal.signal_kind);
                summary.next_section_kind = signal.command_center_summary.next_section_kind;
                summary.next_recommended_view = signal.command_center_summary.next_recommended_view;
                summary.next_task_kind = signal.command_center_summary.next_task_kind;
                summary.next_signal_sequence = Some(signal.sequence);
                summary.next_signal_priority = Some(signal.priority);
            }

            if signal.requires_attention() {
                summary.signals_requiring_attention += 1;
            }
            if signal.has_operator_work() {
                summary.signals_with_operator_work += 1;
            }
            if signal.has_actionable_work() {
                summary.signals_with_actionable_work += 1;
            }
            if signal.has_activation_work() {
                summary.signals_with_activation_work += 1;
            }
            if signal.has_blockers() {
                summary.signals_with_blockers += 1;
            }
            if signal.has_review_work() {
                summary.signals_with_review_work += 1;
            }

            summary.total_sections += signal.command_center_summary.total_sections;
            summary.total_panels += signal.command_center_summary.total_panels;
            summary.total_tasks += signal.command_center_summary.total_tasks;
            summary.operator_required_tasks +=
                signal.command_center_summary.operator_required_tasks;
            summary.actionable_tasks += signal.command_center_summary.actionable_tasks;
            summary.activation_ready_tasks += signal.command_center_summary.activation_ready_tasks;
            summary.blocked_tasks += signal.command_center_summary.blocked_tasks;
            summary.review_required_tasks += signal.command_center_summary.review_required_tasks;
            summary.monitor_tasks += signal.command_center_summary.monitor_tasks;
            summary.highest_policy_tier = summary
                .highest_policy_tier
                .max(signal.command_center_summary.highest_policy_tier);
        }

        summary.unique_integrations = integration_ids.len();
        summary.overall_status = if summary.signals_with_blockers > 0 {
            IntegrationActivationHealthStatus::Blocked
        } else if summary.signals_with_review_work > 0 || summary.signals_with_operator_work > 0 {
            IntegrationActivationHealthStatus::NeedsReview
        } else if summary.signals_with_activation_work > 0
            || summary.signals_with_actionable_work > 0
        {
            IntegrationActivationHealthStatus::Ready
        } else {
            IntegrationActivationHealthStatus::Empty
        };
        summary
    }

    pub fn is_empty(&self) -> bool {
        self.total_signals == 0
    }

    pub fn has_operator_work(&self) -> bool {
        self.signals_with_operator_work > 0
    }

    pub fn has_actionable_work(&self) -> bool {
        self.signals_with_actionable_work > 0
    }

    pub fn has_activation_work(&self) -> bool {
        self.signals_with_activation_work > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.signals_with_blockers > 0
    }

    pub fn has_review_work(&self) -> bool {
        self.signals_with_review_work > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.signals_requiring_attention > 0 || self.overall_status.requires_attention()
    }

    pub fn needs_escalation(&self) -> bool {
        self.escalation_signals > 0 || self.has_blockers()
    }
}

impl IntegrationActivationSentinelAlert {
    // Constructor gathers many independent rollup fields into the alert record;
    // bundling them into a params struct would add churn without clarity.
    #[allow(clippy::too_many_arguments)]
    fn from_rollups(
        sequence: usize,
        alert_kind: IntegrationActivationSentinelAlertKind,
        priority: u8,
        mut integration_ids: Vec<IntegrationId>,
        watchtower_summary: IntegrationActivationWatchtowerSummary,
        risk_summary: IntegrationActivationRiskSummary,
        dependency_summary: IntegrationActivationDependencySummary,
        gap_inventory: IntegrationReadinessGapInventory,
    ) -> Self {
        integration_ids.sort();
        integration_ids.dedup();

        Self {
            sequence,
            alert_kind,
            priority,
            integration_ids,
            watchtower_summary,
            risk_summary,
            dependency_summary,
            gap_inventory,
        }
    }

    pub fn integration_count(&self) -> usize {
        self.integration_ids.len()
    }

    pub fn has_gaps(&self) -> bool {
        matches!(
            self.alert_kind,
            IntegrationActivationSentinelAlertKind::Blocker
                | IntegrationActivationSentinelAlertKind::Dependency
        ) && self.gap_inventory.has_gaps()
    }

    pub fn has_blocking_dependencies(&self) -> bool {
        matches!(
            self.alert_kind,
            IntegrationActivationSentinelAlertKind::Blocker
                | IntegrationActivationSentinelAlertKind::Dependency
        ) && self.dependency_summary.has_blocking_dependencies()
    }

    pub fn has_blockers(&self) -> bool {
        self.alert_kind == IntegrationActivationSentinelAlertKind::Blocker
    }

    pub fn has_dependency_work(&self) -> bool {
        matches!(
            self.alert_kind,
            IntegrationActivationSentinelAlertKind::Blocker
                | IntegrationActivationSentinelAlertKind::Dependency
        ) && (self.dependency_summary.has_dependency_edges()
            || self.gap_inventory.dependency_gap_count() > 0)
    }

    pub fn has_policy_risk(&self) -> bool {
        self.alert_kind == IntegrationActivationSentinelAlertKind::PolicyRisk
    }

    pub fn has_review_work(&self) -> bool {
        self.alert_kind == IntegrationActivationSentinelAlertKind::Review
            || (self.alert_kind == IntegrationActivationSentinelAlertKind::PolicyRisk
                && self.risk_summary.has_review_work())
    }

    pub fn has_activation_work(&self) -> bool {
        self.alert_kind == IntegrationActivationSentinelAlertKind::Ready
    }

    pub fn requires_attention(&self) -> bool {
        match self.alert_kind {
            IntegrationActivationSentinelAlertKind::Blocker => true,
            IntegrationActivationSentinelAlertKind::Dependency => {
                self.dependency_summary.has_blocking_dependencies() || self.gap_inventory.has_gaps()
            }
            IntegrationActivationSentinelAlertKind::PolicyRisk => {
                self.risk_summary.requires_attention()
                    || self.risk_summary.highest_policy_tier >= PrivilegeTier::HumanApproval
            }
            IntegrationActivationSentinelAlertKind::Review => true,
            IntegrationActivationSentinelAlertKind::Ready
            | IntegrationActivationSentinelAlertKind::Observation => false,
        }
    }
}

impl IntegrationActivationSentinelSummary {
    pub fn from_alerts<'a>(
        alerts: impl IntoIterator<Item = &'a IntegrationActivationSentinelAlert>,
    ) -> Self {
        let mut integration_ids = BTreeSet::new();
        let mut summary = Self {
            total_alerts: 0,
            unique_integrations: 0,
            alerts_requiring_attention: 0,
            blocker_alerts: 0,
            dependency_alerts: 0,
            policy_risk_alerts: 0,
            review_alerts: 0,
            ready_alerts: 0,
            observation_alerts: 0,
            alerts_with_gaps: 0,
            alerts_with_blocking_dependencies: 0,
            alerts_with_policy_risk: 0,
            alerts_with_review_work: 0,
            alerts_with_activation_work: 0,
            total_watchtower_signals: 0,
            total_risks: 0,
            total_dependency_edges: 0,
            blocking_dependency_edges: 0,
            total_unique_gaps: 0,
            first_attention_priority: None,
            first_blocker_priority: None,
            first_dependency_priority: None,
            first_policy_risk_priority: None,
            first_review_priority: None,
            first_ready_priority: None,
            highest_policy_tier: PrivilegeTier::ReadOnly,
            overall_status: IntegrationActivationHealthStatus::Empty,
        };

        for alert in alerts {
            summary.total_alerts += 1;
            for integration_id in &alert.integration_ids {
                integration_ids.insert(integration_id.clone());
            }

            match alert.alert_kind {
                IntegrationActivationSentinelAlertKind::Blocker => {
                    summary.blocker_alerts += 1;
                    summary.first_blocker_priority =
                        min_optional_priority(summary.first_blocker_priority, Some(alert.priority));
                }
                IntegrationActivationSentinelAlertKind::Dependency => {
                    summary.dependency_alerts += 1;
                    summary.first_dependency_priority = min_optional_priority(
                        summary.first_dependency_priority,
                        Some(alert.priority),
                    );
                }
                IntegrationActivationSentinelAlertKind::PolicyRisk => {
                    summary.policy_risk_alerts += 1;
                    summary.first_policy_risk_priority = min_optional_priority(
                        summary.first_policy_risk_priority,
                        Some(alert.priority),
                    );
                }
                IntegrationActivationSentinelAlertKind::Review => {
                    summary.review_alerts += 1;
                    summary.first_review_priority =
                        min_optional_priority(summary.first_review_priority, Some(alert.priority));
                }
                IntegrationActivationSentinelAlertKind::Ready => {
                    summary.ready_alerts += 1;
                    summary.first_ready_priority =
                        min_optional_priority(summary.first_ready_priority, Some(alert.priority));
                }
                IntegrationActivationSentinelAlertKind::Observation => {
                    summary.observation_alerts += 1;
                }
            }

            if alert.requires_attention() {
                summary.alerts_requiring_attention += 1;
                summary.first_attention_priority =
                    min_optional_priority(summary.first_attention_priority, Some(alert.priority));
            }
            if alert.has_gaps() {
                summary.alerts_with_gaps += 1;
            }
            if alert.has_blocking_dependencies() {
                summary.alerts_with_blocking_dependencies += 1;
            }
            if alert.has_policy_risk() {
                summary.alerts_with_policy_risk += 1;
            }
            if alert.has_review_work() {
                summary.alerts_with_review_work += 1;
            }
            if alert.has_activation_work() {
                summary.alerts_with_activation_work += 1;
            }

            summary.total_watchtower_signals = summary
                .total_watchtower_signals
                .max(alert.watchtower_summary.total_signals);
            summary.total_risks = summary.total_risks.max(alert.risk_summary.total_risks);
            summary.total_dependency_edges = summary
                .total_dependency_edges
                .max(alert.dependency_summary.total_edges);
            summary.blocking_dependency_edges = summary
                .blocking_dependency_edges
                .max(alert.dependency_summary.blocking_edges);
            summary.total_unique_gaps = summary
                .total_unique_gaps
                .max(alert.gap_inventory.total_unique_gaps());
            summary.highest_policy_tier = summary
                .highest_policy_tier
                .max(alert.watchtower_summary.highest_policy_tier)
                .max(alert.risk_summary.highest_policy_tier)
                .max(alert.dependency_summary.highest_policy_tier);
        }

        summary.unique_integrations = integration_ids.len();
        summary.overall_status = if summary.blocker_alerts > 0
            || summary.alerts_with_blocking_dependencies > 0
            || summary.alerts_with_gaps > 0
        {
            IntegrationActivationHealthStatus::Blocked
        } else if summary.alerts_requiring_attention > 0 {
            IntegrationActivationHealthStatus::NeedsReview
        } else if summary.ready_alerts > 0 || summary.alerts_with_activation_work > 0 {
            IntegrationActivationHealthStatus::Ready
        } else {
            IntegrationActivationHealthStatus::Empty
        };
        summary
    }

    pub fn is_empty(&self) -> bool {
        self.total_alerts == 0
    }

    pub fn has_blockers(&self) -> bool {
        self.blocker_alerts > 0
            || self.alerts_with_blocking_dependencies > 0
            || self.alerts_with_gaps > 0
    }

    pub fn has_dependency_work(&self) -> bool {
        self.dependency_alerts > 0
    }

    pub fn has_policy_risk(&self) -> bool {
        self.policy_risk_alerts > 0
    }

    pub fn has_review_work(&self) -> bool {
        self.review_alerts > 0 || self.alerts_with_review_work > 0
    }

    pub fn has_activation_work(&self) -> bool {
        self.ready_alerts > 0 || self.alerts_with_activation_work > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.alerts_requiring_attention > 0 || self.overall_status.requires_attention()
    }
}

impl IntegrationActivationAuditRecord {
    // Record constructor takes each audit field explicitly; a params struct would
    // add churn without improving clarity.
    #[allow(clippy::too_many_arguments)]
    fn new(
        record_kind: IntegrationActivationAuditRecordKind,
        record_id: String,
        title: String,
        summary: String,
        priority: u8,
        mut integration_ids: Vec<IntegrationId>,
        required_tier: PrivilegeTier,
        policy_surface: Option<IntegrationPolicySurface>,
        requires_attention: bool,
    ) -> Self {
        integration_ids.sort();
        integration_ids.dedup();

        Self {
            sequence: 0,
            record_kind,
            record_id,
            title,
            summary,
            priority,
            integration_ids,
            sentinel_alert_kind: None,
            watchtower_signal_kind: None,
            decision_status: None,
            evidence_kind: None,
            evidence_status: None,
            risk_kind: None,
            dependency_integration_id: None,
            dependent_integration_id: None,
            readiness_gap_kind: None,
            required_tier,
            policy_surface,
            requires_attention,
        }
    }

    fn from_sentinel_alert(alert: &IntegrationActivationSentinelAlert) -> Self {
        let required_tier = alert
            .watchtower_summary
            .highest_policy_tier
            .max(alert.risk_summary.highest_policy_tier)
            .max(alert.dependency_summary.highest_policy_tier);
        let mut record = Self::new(
            IntegrationActivationAuditRecordKind::Sentinel,
            format!("sentinel:{}", alert.alert_kind.as_str()),
            format!("Activation sentinel {} alert", alert.alert_kind.as_str()),
            format!(
                "{} integrations, {} dependency edges, {} readiness gaps",
                alert.integration_count(),
                alert.dependency_summary.total_edges,
                alert.gap_inventory.total_unique_gaps()
            ),
            alert.priority,
            alert.integration_ids.clone(),
            required_tier,
            None,
            alert.requires_attention(),
        );
        record.sentinel_alert_kind = Some(alert.alert_kind);
        record
    }

    fn from_watchtower_signal(signal: &IntegrationActivationWatchtowerSignal) -> Self {
        let mut record = Self::new(
            IntegrationActivationAuditRecordKind::Watchtower,
            format!("watchtower:{}", signal.signal_kind.as_str()),
            format!(
                "Activation watchtower {} signal",
                signal.signal_kind.as_str()
            ),
            format!(
                "{} command-center sections across {} integrations",
                signal.section_count,
                signal.integration_count()
            ),
            signal.priority,
            signal.integration_ids.clone(),
            signal.command_center_summary.highest_policy_tier,
            None,
            signal.requires_attention(),
        );
        record.watchtower_signal_kind = Some(signal.signal_kind);
        record
    }

    fn from_decision(decision: &IntegrationActivationDecisionItem) -> Self {
        let mut record = Self::new(
            IntegrationActivationAuditRecordKind::Decision,
            format!("decision:{}", decision.requested_integration_id().as_str()),
            format!("Activation decision for {}", decision.display_name()),
            format!(
                "{} with {} actions and {} constraints",
                decision.decision_status.as_str(),
                decision.packet.action_summary.total_actions,
                decision.packet.constraint_summary.total_constraints
            ),
            decision.priority(),
            vec![decision.requested_integration_id().clone()],
            decision.required_tier(),
            decision.packet.review.policy_surfaces.first().copied(),
            decision.requires_attention(),
        );
        record.decision_status = Some(decision.decision_status);
        record
    }

    fn from_evidence(evidence: &IntegrationActivationEvidenceItem) -> Self {
        let mut record = Self::new(
            IntegrationActivationAuditRecordKind::Evidence,
            format!(
                "evidence:{}:{}",
                evidence.requested_integration_id.as_str(),
                evidence.detail_id
            ),
            format!("Activation evidence for {}", evidence.display_name),
            format!(
                "{} evidence {}",
                evidence.kind.as_str(),
                evidence.status.as_str()
            ),
            evidence.priority,
            vec![evidence.requested_integration_id.clone()],
            evidence.required_tier,
            evidence.policy_surface,
            evidence.requires_attention(),
        );
        record.decision_status = Some(evidence.decision_status);
        record.evidence_kind = Some(evidence.kind);
        record.evidence_status = Some(evidence.status);
        record.dependency_integration_id = evidence.dependency_integration_id.clone();
        record
    }

    fn from_risk(risk: &IntegrationActivationRiskItem) -> Self {
        let mut record = Self::new(
            IntegrationActivationAuditRecordKind::Risk,
            format!("risk:{}", risk.risk_id),
            risk.display_name.clone(),
            format!(
                "{} integrations require {} review",
                risk.integration_count(),
                privilege_tier_label_for_catalog(risk.required_tier)
            ),
            risk.highest_priority,
            risk.integration_ids.clone(),
            risk.required_tier,
            risk.policy_surface,
            risk.requires_attention(),
        );
        record.risk_kind = Some(risk.kind);
        record
    }

    fn from_dependency_edge(edge: &IntegrationActivationDependencyEdge) -> Self {
        let dependency_name = edge
            .dependency_display_name
            .as_deref()
            .unwrap_or(edge.dependency_integration_id.as_str());
        let mut record = Self::new(
            IntegrationActivationAuditRecordKind::Dependency,
            format!(
                "dependency:{}:{}",
                edge.dependency_integration_id.as_str(),
                edge.dependent_integration_id.as_str()
            ),
            format!("Activation dependency for {}", edge.dependent_display_name),
            format!(
                "{} depends on {} ({})",
                edge.dependent_display_name,
                dependency_name,
                if edge.satisfied {
                    "satisfied"
                } else {
                    "blocked"
                }
            ),
            edge.dependent_priority,
            vec![
                edge.dependency_integration_id.clone(),
                edge.dependent_integration_id.clone(),
            ],
            PrivilegeTier::ReadOnly,
            None,
            edge.blocks_activation,
        );
        record.dependency_integration_id = Some(edge.dependency_integration_id.clone());
        record.dependent_integration_id = Some(edge.dependent_integration_id.clone());
        record
    }

    fn from_primitive_gap(gap: &IntegrationReadinessPrimitiveGap) -> Self {
        let mut record = Self::new(
            IntegrationActivationAuditRecordKind::ReadinessGap,
            format!("readiness_gap:primitive:{}", gap.primitive.as_str()),
            format!("Missing {} primitive", gap.primitive.as_str()),
            format!(
                "{} integrations are blocked by this primitive gap",
                gap.blocked_report_count
            ),
            gap.highest_priority,
            gap.integration_ids.clone(),
            PrivilegeTier::ReadOnly,
            None,
            true,
        );
        record.readiness_gap_kind = Some("primitive".to_string());
        record
    }

    fn from_capability_gap(gap: &IntegrationReadinessCapabilityGap) -> Self {
        let mut record = Self::new(
            IntegrationActivationAuditRecordKind::ReadinessGap,
            format!("readiness_gap:capability:{}", gap.capability_id.as_str()),
            format!("Missing {} capability", gap.capability_id.as_str()),
            format!(
                "{} integrations are blocked by this capability gap",
                gap.blocked_report_count
            ),
            gap.highest_priority,
            gap.integration_ids.clone(),
            PrivilegeTier::ReadOnly,
            None,
            true,
        );
        record.readiness_gap_kind = Some("capability".to_string());
        record
    }

    fn from_dependency_gap(gap: &IntegrationReadinessDependencyGap) -> Self {
        let mut integration_ids = gap.requested_integration_ids.clone();
        integration_ids.push(gap.integration_id.clone());
        let mut record = Self::new(
            IntegrationActivationAuditRecordKind::ReadinessGap,
            format!("readiness_gap:dependency:{}", gap.integration_id.as_str()),
            format!("Missing {} dependency", gap.integration_id.as_str()),
            format!(
                "{} integrations are blocked by this dependency gap",
                gap.blocked_report_count
            ),
            gap.highest_priority,
            integration_ids,
            PrivilegeTier::ReadOnly,
            None,
            true,
        );
        record.dependency_integration_id = Some(gap.integration_id.clone());
        record.readiness_gap_kind = Some("dependency".to_string());
        record
    }

    pub fn integration_count(&self) -> usize {
        self.integration_ids.len()
    }

    pub fn has_dependency_link(&self) -> bool {
        self.dependency_integration_id.is_some() || self.dependent_integration_id.is_some()
    }

    pub fn has_policy_surface(&self) -> bool {
        self.policy_surface.is_some()
    }

    pub fn requires_attention(&self) -> bool {
        self.requires_attention
    }
}

impl IntegrationActivationAuditSummary {
    pub fn from_records<'a>(
        records: impl IntoIterator<Item = &'a IntegrationActivationAuditRecord>,
    ) -> Self {
        let mut integration_ids = BTreeSet::new();
        let mut summary = Self {
            total_records: 0,
            unique_integrations: 0,
            records_requiring_attention: 0,
            sentinel_records: 0,
            watchtower_records: 0,
            decision_records: 0,
            evidence_records: 0,
            risk_records: 0,
            dependency_records: 0,
            readiness_gap_records: 0,
            records_with_policy_surface: 0,
            records_with_dependency_link: 0,
            first_attention_priority: None,
            first_dependency_priority: None,
            first_readiness_gap_priority: None,
            highest_policy_tier: PrivilegeTier::ReadOnly,
            overall_status: IntegrationActivationHealthStatus::Empty,
        };

        for record in records {
            summary.total_records += 1;
            for integration_id in &record.integration_ids {
                integration_ids.insert(integration_id.clone());
            }
            match record.record_kind {
                IntegrationActivationAuditRecordKind::Sentinel => summary.sentinel_records += 1,
                IntegrationActivationAuditRecordKind::Watchtower => summary.watchtower_records += 1,
                IntegrationActivationAuditRecordKind::Decision => summary.decision_records += 1,
                IntegrationActivationAuditRecordKind::Evidence => summary.evidence_records += 1,
                IntegrationActivationAuditRecordKind::Risk => summary.risk_records += 1,
                IntegrationActivationAuditRecordKind::Dependency => {
                    summary.dependency_records += 1;
                    if record.requires_attention() {
                        summary.first_dependency_priority = min_optional_priority(
                            summary.first_dependency_priority,
                            Some(record.priority),
                        );
                    }
                }
                IntegrationActivationAuditRecordKind::ReadinessGap => {
                    summary.readiness_gap_records += 1;
                    summary.first_readiness_gap_priority = min_optional_priority(
                        summary.first_readiness_gap_priority,
                        Some(record.priority),
                    );
                }
            }
            if record.requires_attention() {
                summary.records_requiring_attention += 1;
                summary.first_attention_priority =
                    min_optional_priority(summary.first_attention_priority, Some(record.priority));
            }
            if record.has_policy_surface() {
                summary.records_with_policy_surface += 1;
            }
            if record.has_dependency_link() {
                summary.records_with_dependency_link += 1;
            }
            summary.highest_policy_tier = summary.highest_policy_tier.max(record.required_tier);
        }

        summary.unique_integrations = integration_ids.len();
        summary.overall_status =
            if summary.readiness_gap_records > 0 || summary.first_dependency_priority.is_some() {
                IntegrationActivationHealthStatus::Blocked
            } else if summary.records_requiring_attention > 0 {
                IntegrationActivationHealthStatus::NeedsReview
            } else if summary.total_records > 0 {
                IntegrationActivationHealthStatus::Ready
            } else {
                IntegrationActivationHealthStatus::Empty
            };
        summary
    }

    pub fn is_empty(&self) -> bool {
        self.total_records == 0
    }

    pub fn has_dependency_work(&self) -> bool {
        self.dependency_records > 0 || self.readiness_gap_records > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.records_requiring_attention > 0 || self.overall_status.requires_attention()
    }
}

impl IntegrationActivationEscalationCase {
    // Case constructor takes each escalation field explicitly; a params struct
    // would add churn without improving clarity.
    #[allow(clippy::too_many_arguments)]
    fn new(
        case_kind: IntegrationActivationEscalationCaseKind,
        source_id: String,
        title: String,
        summary: String,
        priority: u8,
        mut integration_ids: Vec<IntegrationId>,
        required_tier: PrivilegeTier,
        policy_surface: Option<IntegrationPolicySurface>,
        requires_attention: bool,
    ) -> Self {
        integration_ids.sort();
        integration_ids.dedup();

        Self {
            sequence: 0,
            case_kind,
            source_id,
            title,
            summary,
            priority,
            integration_ids,
            sentinel_alert_kind: None,
            verification_status: None,
            audit_record_kind: None,
            recommended_view: case_kind.recommended_view(),
            required_tier,
            policy_surface,
            dependency_work: false,
            policy_risk: false,
            verification_ready: false,
            blocked: false,
            requires_attention,
        }
    }

    fn from_sentinel_alert(alert: &IntegrationActivationSentinelAlert) -> Self {
        let case_kind = match alert.alert_kind {
            IntegrationActivationSentinelAlertKind::Blocker => {
                IntegrationActivationEscalationCaseKind::Blocker
            }
            IntegrationActivationSentinelAlertKind::Dependency => {
                IntegrationActivationEscalationCaseKind::Dependency
            }
            IntegrationActivationSentinelAlertKind::PolicyRisk => {
                IntegrationActivationEscalationCaseKind::PolicyRisk
            }
            IntegrationActivationSentinelAlertKind::Review => {
                IntegrationActivationEscalationCaseKind::Review
            }
            IntegrationActivationSentinelAlertKind::Ready
            | IntegrationActivationSentinelAlertKind::Observation => {
                IntegrationActivationEscalationCaseKind::Audit
            }
        };
        let required_tier = alert
            .watchtower_summary
            .highest_policy_tier
            .max(alert.risk_summary.highest_policy_tier)
            .max(alert.dependency_summary.highest_policy_tier);
        let mut case = Self::new(
            case_kind,
            format!("sentinel:{}", alert.alert_kind.as_str()),
            format!("Escalate {} activation alert", alert.alert_kind.as_str()),
            format!(
                "{} integrations, {} blocking dependencies, {} readiness gaps",
                alert.integration_count(),
                alert.dependency_summary.blocking_edges,
                alert.gap_inventory.total_unique_gaps()
            ),
            alert.priority,
            alert.integration_ids.clone(),
            required_tier,
            None,
            alert.requires_attention(),
        );
        case.sentinel_alert_kind = Some(alert.alert_kind);
        case.dependency_work = alert.has_dependency_work();
        case.policy_risk = alert.has_policy_risk();
        case.blocked =
            alert.has_blockers() || alert.has_blocking_dependencies() || alert.has_gaps();
        case
    }

    fn from_verification_checkpoint(
        checkpoint: &IntegrationActivationVerificationCheckpoint,
    ) -> Self {
        let mut case = Self::new(
            IntegrationActivationEscalationCaseKind::Verification,
            format!("verification:{}", checkpoint.sequence),
            format!(
                "Verify activation checkpoint {}",
                checkpoint.verification_status.as_str()
            ),
            format!(
                "{} integrations, {} audit records, {} readiness gaps",
                checkpoint.integration_count(),
                checkpoint.audit_record_count,
                checkpoint.readiness_gap_count
            ),
            checkpoint.priority,
            checkpoint.integration_ids.clone(),
            checkpoint.highest_policy_tier,
            None,
            checkpoint.requires_attention() || checkpoint.can_verify(),
        );
        case.verification_status = Some(checkpoint.verification_status);
        case.recommended_view = checkpoint.recommended_view;
        case.dependency_work =
            !checkpoint.dependency_ready() || checkpoint.blocking_dependency_edge_count > 0;
        case.policy_risk = checkpoint.risk_count > 0;
        case.verification_ready = checkpoint.verification_ready() || checkpoint.can_verify();
        case.blocked = checkpoint.blocked() || !checkpoint.dependency_ready();
        case
    }

    fn from_audit_record(record: &IntegrationActivationAuditRecord) -> Self {
        let case_kind = match record.record_kind {
            IntegrationActivationAuditRecordKind::Risk => {
                IntegrationActivationEscalationCaseKind::PolicyRisk
            }
            IntegrationActivationAuditRecordKind::Dependency
            | IntegrationActivationAuditRecordKind::ReadinessGap => {
                IntegrationActivationEscalationCaseKind::Dependency
            }
            IntegrationActivationAuditRecordKind::Decision
            | IntegrationActivationAuditRecordKind::Evidence => {
                IntegrationActivationEscalationCaseKind::Review
            }
            IntegrationActivationAuditRecordKind::Sentinel
            | IntegrationActivationAuditRecordKind::Watchtower => {
                IntegrationActivationEscalationCaseKind::Audit
            }
        };
        let mut case = Self::new(
            case_kind,
            format!("audit:{}", record.record_id),
            record.title.clone(),
            record.summary.clone(),
            record.priority,
            record.integration_ids.clone(),
            record.required_tier,
            record.policy_surface,
            record.requires_attention(),
        );
        case.audit_record_kind = Some(record.record_kind);
        case.sentinel_alert_kind = record.sentinel_alert_kind;
        case.dependency_work = record.has_dependency_link()
            || record.record_kind == IntegrationActivationAuditRecordKind::Dependency
            || record.record_kind == IntegrationActivationAuditRecordKind::ReadinessGap;
        case.policy_risk = record.risk_kind.is_some() || record.policy_surface.is_some();
        case.blocked = record.record_kind == IntegrationActivationAuditRecordKind::ReadinessGap
            || (record.record_kind == IntegrationActivationAuditRecordKind::Dependency
                && record.requires_attention());
        case
    }

    pub fn integration_count(&self) -> usize {
        self.integration_ids.len()
    }

    pub fn has_dependency_work(&self) -> bool {
        self.dependency_work
    }

    pub fn has_policy_risk(&self) -> bool {
        self.policy_risk
    }

    pub fn ready_to_verify(&self) -> bool {
        self.verification_ready
    }

    pub fn blocked(&self) -> bool {
        self.blocked
    }

    pub fn has_policy_surface(&self) -> bool {
        self.policy_surface.is_some()
    }

    pub fn requires_attention(&self) -> bool {
        self.requires_attention
    }
}

impl IntegrationActivationEscalationSummary {
    pub fn from_cases<'a>(
        cases: impl IntoIterator<Item = &'a IntegrationActivationEscalationCase>,
    ) -> Self {
        let mut integration_ids = BTreeSet::new();
        let mut summary = Self {
            total_cases: 0,
            unique_integrations: 0,
            cases_requiring_attention: 0,
            blocker_cases: 0,
            dependency_cases: 0,
            policy_risk_cases: 0,
            review_cases: 0,
            verification_cases: 0,
            audit_cases: 0,
            cases_with_dependency_work: 0,
            cases_with_policy_risk: 0,
            cases_ready_to_verify: 0,
            blocked_cases: 0,
            cases_with_policy_surface: 0,
            next_case_kind: None,
            next_recommended_view: None,
            next_case_sequence: None,
            next_case_priority: None,
            first_blocker_priority: None,
            first_dependency_priority: None,
            first_policy_risk_priority: None,
            first_review_priority: None,
            first_verification_priority: None,
            first_attention_priority: None,
            highest_policy_tier: PrivilegeTier::ReadOnly,
            overall_status: IntegrationActivationHealthStatus::Empty,
        };

        for case in cases {
            summary.total_cases += 1;
            for integration_id in &case.integration_ids {
                integration_ids.insert(integration_id.clone());
            }

            if summary.next_case_kind.is_none()
                && (case.requires_attention() || case.ready_to_verify())
            {
                summary.next_case_kind = Some(case.case_kind);
                summary.next_recommended_view = Some(case.recommended_view);
                summary.next_case_sequence = Some(case.sequence);
                summary.next_case_priority = Some(case.priority);
            }

            match case.case_kind {
                IntegrationActivationEscalationCaseKind::Blocker => {
                    summary.blocker_cases += 1;
                    summary.first_blocker_priority =
                        min_optional_priority(summary.first_blocker_priority, Some(case.priority));
                }
                IntegrationActivationEscalationCaseKind::Dependency => {
                    summary.dependency_cases += 1;
                    summary.first_dependency_priority = min_optional_priority(
                        summary.first_dependency_priority,
                        Some(case.priority),
                    );
                }
                IntegrationActivationEscalationCaseKind::PolicyRisk => {
                    summary.policy_risk_cases += 1;
                    summary.first_policy_risk_priority = min_optional_priority(
                        summary.first_policy_risk_priority,
                        Some(case.priority),
                    );
                }
                IntegrationActivationEscalationCaseKind::Review => {
                    summary.review_cases += 1;
                    summary.first_review_priority =
                        min_optional_priority(summary.first_review_priority, Some(case.priority));
                }
                IntegrationActivationEscalationCaseKind::Verification => {
                    summary.verification_cases += 1;
                    summary.first_verification_priority = min_optional_priority(
                        summary.first_verification_priority,
                        Some(case.priority),
                    );
                }
                IntegrationActivationEscalationCaseKind::Audit => {
                    summary.audit_cases += 1;
                }
            }

            if case.requires_attention() {
                summary.cases_requiring_attention += 1;
                summary.first_attention_priority =
                    min_optional_priority(summary.first_attention_priority, Some(case.priority));
            }
            if case.has_dependency_work() {
                summary.cases_with_dependency_work += 1;
            }
            if case.has_policy_risk() {
                summary.cases_with_policy_risk += 1;
            }
            if case.ready_to_verify() {
                summary.cases_ready_to_verify += 1;
            }
            if case.blocked() {
                summary.blocked_cases += 1;
            }
            if case.has_policy_surface() {
                summary.cases_with_policy_surface += 1;
            }
            summary.highest_policy_tier = summary.highest_policy_tier.max(case.required_tier);
        }

        summary.unique_integrations = integration_ids.len();
        summary.overall_status = if summary.blocked_cases > 0 || summary.blocker_cases > 0 {
            IntegrationActivationHealthStatus::Blocked
        } else if summary.cases_requiring_attention > 0
            || summary.review_cases > 0
            || summary.policy_risk_cases > 0
        {
            IntegrationActivationHealthStatus::NeedsReview
        } else if summary.cases_ready_to_verify > 0 || summary.verification_cases > 0 {
            IntegrationActivationHealthStatus::Ready
        } else {
            IntegrationActivationHealthStatus::Empty
        };
        summary
    }

    pub fn is_empty(&self) -> bool {
        self.total_cases == 0
    }

    pub fn has_blockers(&self) -> bool {
        self.blocker_cases > 0 || self.blocked_cases > 0
    }

    pub fn has_dependency_work(&self) -> bool {
        self.dependency_cases > 0 || self.cases_with_dependency_work > 0
    }

    pub fn has_policy_risk(&self) -> bool {
        self.policy_risk_cases > 0 || self.cases_with_policy_risk > 0
    }

    pub fn ready_to_verify(&self) -> bool {
        self.cases_ready_to_verify > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.cases_requiring_attention > 0 || self.overall_status.requires_attention()
    }
}

impl IntegrationActivationResponseItem {
    fn from_escalation_case(case: &IntegrationActivationEscalationCase) -> Self {
        let response_kind = IntegrationActivationResponseKind::from_case_kind(case.case_kind);
        let owner_lane = IntegrationActivationResponseOwnerLane::from_response_kind(response_kind);

        Self {
            sequence: 0,
            response_kind,
            owner_lane,
            source_case_sequence: case.sequence,
            source_case_kind: case.case_kind,
            source_id: case.source_id.clone(),
            title: format!("{}: {}", response_kind.as_str(), case.title),
            summary: case.summary.clone(),
            priority: case.priority,
            integration_ids: case.integration_ids.clone(),
            recommended_view: case.recommended_view,
            required_tier: case.required_tier,
            policy_surface: case.policy_surface,
            dependency_work: case.has_dependency_work(),
            policy_risk: case.has_policy_risk(),
            verification_ready: case.ready_to_verify(),
            blocked: case.blocked(),
            requires_attention: case.requires_attention() || case.ready_to_verify(),
        }
    }

    pub fn integration_count(&self) -> usize {
        self.integration_ids.len()
    }

    pub fn has_dependency_work(&self) -> bool {
        self.dependency_work
    }

    pub fn has_policy_risk(&self) -> bool {
        self.policy_risk
    }

    pub fn ready_to_verify(&self) -> bool {
        self.verification_ready
    }

    pub fn blocked(&self) -> bool {
        self.blocked
    }

    pub fn has_policy_surface(&self) -> bool {
        self.policy_surface.is_some()
    }

    pub fn requires_attention(&self) -> bool {
        self.requires_attention
    }
}

impl IntegrationActivationResponseSummary {
    pub fn from_responses<'a>(
        responses: impl IntoIterator<Item = &'a IntegrationActivationResponseItem>,
    ) -> Self {
        let mut integration_ids = BTreeSet::new();
        let mut summary = Self {
            total_responses: 0,
            unique_integrations: 0,
            responses_requiring_attention: 0,
            resolve_blocker_responses: 0,
            enable_dependency_responses: 0,
            review_policy_responses: 0,
            queue_review_responses: 0,
            verify_activation_responses: 0,
            audit_follow_up_responses: 0,
            platform_owner_responses: 0,
            integration_owner_responses: 0,
            security_owner_responses: 0,
            reviewer_owner_responses: 0,
            verification_owner_responses: 0,
            audit_owner_responses: 0,
            responses_with_dependency_work: 0,
            responses_with_policy_risk: 0,
            responses_ready_to_verify: 0,
            blocked_responses: 0,
            responses_with_policy_surface: 0,
            next_response_kind: None,
            next_owner_lane: None,
            next_recommended_view: None,
            next_response_sequence: None,
            next_response_priority: None,
            first_attention_priority: None,
            highest_policy_tier: PrivilegeTier::ReadOnly,
            overall_status: IntegrationActivationHealthStatus::Empty,
        };

        for response in responses {
            summary.total_responses += 1;
            for integration_id in &response.integration_ids {
                integration_ids.insert(integration_id.clone());
            }

            if summary.next_response_kind.is_none()
                && (response.requires_attention() || response.ready_to_verify())
            {
                summary.next_response_kind = Some(response.response_kind);
                summary.next_owner_lane = Some(response.owner_lane);
                summary.next_recommended_view = Some(response.recommended_view);
                summary.next_response_sequence = Some(response.sequence);
                summary.next_response_priority = Some(response.priority);
            }

            match response.response_kind {
                IntegrationActivationResponseKind::ResolveBlocker => {
                    summary.resolve_blocker_responses += 1;
                }
                IntegrationActivationResponseKind::EnableDependency => {
                    summary.enable_dependency_responses += 1;
                }
                IntegrationActivationResponseKind::ReviewPolicy => {
                    summary.review_policy_responses += 1;
                }
                IntegrationActivationResponseKind::QueueReview => {
                    summary.queue_review_responses += 1;
                }
                IntegrationActivationResponseKind::VerifyActivation => {
                    summary.verify_activation_responses += 1;
                }
                IntegrationActivationResponseKind::AuditFollowUp => {
                    summary.audit_follow_up_responses += 1;
                }
            }

            match response.owner_lane {
                IntegrationActivationResponseOwnerLane::Platform => {
                    summary.platform_owner_responses += 1;
                }
                IntegrationActivationResponseOwnerLane::Integration => {
                    summary.integration_owner_responses += 1;
                }
                IntegrationActivationResponseOwnerLane::Security => {
                    summary.security_owner_responses += 1;
                }
                IntegrationActivationResponseOwnerLane::Reviewer => {
                    summary.reviewer_owner_responses += 1;
                }
                IntegrationActivationResponseOwnerLane::Verification => {
                    summary.verification_owner_responses += 1;
                }
                IntegrationActivationResponseOwnerLane::Audit => {
                    summary.audit_owner_responses += 1;
                }
            }

            if response.requires_attention() {
                summary.responses_requiring_attention += 1;
                summary.first_attention_priority = min_optional_priority(
                    summary.first_attention_priority,
                    Some(response.priority),
                );
            }
            if response.has_dependency_work() {
                summary.responses_with_dependency_work += 1;
            }
            if response.has_policy_risk() {
                summary.responses_with_policy_risk += 1;
            }
            if response.ready_to_verify() {
                summary.responses_ready_to_verify += 1;
            }
            if response.blocked() {
                summary.blocked_responses += 1;
            }
            if response.has_policy_surface() {
                summary.responses_with_policy_surface += 1;
            }
            summary.highest_policy_tier = summary.highest_policy_tier.max(response.required_tier);
        }

        summary.unique_integrations = integration_ids.len();
        summary.overall_status = if summary.blocked_responses > 0
            || summary.resolve_blocker_responses > 0
            || summary.enable_dependency_responses > 0
        {
            IntegrationActivationHealthStatus::Blocked
        } else if summary.responses_requiring_attention > 0
            || summary.review_policy_responses > 0
            || summary.queue_review_responses > 0
        {
            IntegrationActivationHealthStatus::NeedsReview
        } else if summary.responses_ready_to_verify > 0 || summary.verify_activation_responses > 0 {
            IntegrationActivationHealthStatus::Ready
        } else {
            IntegrationActivationHealthStatus::Empty
        };
        summary
    }

    pub fn is_empty(&self) -> bool {
        self.total_responses == 0
    }

    pub fn has_blockers(&self) -> bool {
        self.resolve_blocker_responses > 0 || self.blocked_responses > 0
    }

    pub fn has_dependency_work(&self) -> bool {
        self.enable_dependency_responses > 0 || self.responses_with_dependency_work > 0
    }

    pub fn has_policy_risk(&self) -> bool {
        self.review_policy_responses > 0 || self.responses_with_policy_risk > 0
    }

    pub fn ready_to_verify(&self) -> bool {
        self.responses_ready_to_verify > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.responses_requiring_attention > 0 || self.overall_status.requires_attention()
    }
}

impl IntegrationActivationRemediationItem {
    fn from_response(response: &IntegrationActivationResponseItem) -> Self {
        let remediation_kind =
            IntegrationActivationRemediationKind::from_response_kind(response.response_kind);
        let status = IntegrationActivationRemediationStatus::from_response(response);

        Self {
            sequence: 0,
            remediation_kind,
            status,
            owner_lane: response.owner_lane,
            source_response_sequence: response.sequence,
            source_response_kind: response.response_kind,
            source_id: response.source_id.clone(),
            title: format!("{}: {}", remediation_kind.as_str(), response.title),
            summary: response.summary.clone(),
            priority: response.priority,
            integration_ids: response.integration_ids.clone(),
            recommended_view: response.recommended_view,
            required_tier: response.required_tier,
            policy_surface: response.policy_surface,
            dependency_work: response.has_dependency_work(),
            policy_risk: response.has_policy_risk(),
            verification_ready: response.ready_to_verify(),
            blocked: response.blocked(),
            requires_attention: response.requires_attention()
                || status != IntegrationActivationRemediationStatus::Tracking,
        }
    }

    pub fn integration_count(&self) -> usize {
        self.integration_ids.len()
    }

    pub fn has_dependency_work(&self) -> bool {
        self.dependency_work
    }

    pub fn has_policy_risk(&self) -> bool {
        self.policy_risk
    }

    pub fn ready_to_verify(&self) -> bool {
        self.verification_ready
    }

    pub fn blocked(&self) -> bool {
        self.blocked || self.status == IntegrationActivationRemediationStatus::Blocked
    }

    pub fn ready_to_execute(&self) -> bool {
        self.status == IntegrationActivationRemediationStatus::ReadyToExecute
    }

    pub fn has_policy_surface(&self) -> bool {
        self.policy_surface.is_some()
    }

    pub fn requires_attention(&self) -> bool {
        self.requires_attention
    }
}

impl IntegrationActivationRemediationSummary {
    pub fn from_remediations<'a>(
        remediations: impl IntoIterator<Item = &'a IntegrationActivationRemediationItem>,
    ) -> Self {
        let mut integration_ids = BTreeSet::new();
        let mut summary = Self {
            total_remediations: 0,
            unique_integrations: 0,
            remediations_requiring_attention: 0,
            unblock_platform_remediations: 0,
            enable_dependency_remediations: 0,
            review_policy_remediations: 0,
            complete_review_remediations: 0,
            run_verification_remediations: 0,
            record_audit_remediations: 0,
            platform_owner_remediations: 0,
            integration_owner_remediations: 0,
            security_owner_remediations: 0,
            reviewer_owner_remediations: 0,
            verification_owner_remediations: 0,
            audit_owner_remediations: 0,
            blocked_remediations: 0,
            owner_action_remediations: 0,
            ready_to_execute_remediations: 0,
            tracking_remediations: 0,
            remediations_with_dependency_work: 0,
            remediations_with_policy_risk: 0,
            remediations_ready_to_verify: 0,
            remediations_with_policy_surface: 0,
            next_remediation_kind: None,
            next_remediation_status: None,
            next_owner_lane: None,
            next_recommended_view: None,
            next_remediation_sequence: None,
            next_remediation_priority: None,
            first_attention_priority: None,
            highest_policy_tier: PrivilegeTier::ReadOnly,
            overall_status: IntegrationActivationHealthStatus::Empty,
        };

        for remediation in remediations {
            summary.total_remediations += 1;
            for integration_id in &remediation.integration_ids {
                integration_ids.insert(integration_id.clone());
            }

            if summary.next_remediation_kind.is_none()
                && (remediation.requires_attention() || remediation.ready_to_execute())
            {
                summary.next_remediation_kind = Some(remediation.remediation_kind);
                summary.next_remediation_status = Some(remediation.status);
                summary.next_owner_lane = Some(remediation.owner_lane);
                summary.next_recommended_view = Some(remediation.recommended_view);
                summary.next_remediation_sequence = Some(remediation.sequence);
                summary.next_remediation_priority = Some(remediation.priority);
            }

            match remediation.remediation_kind {
                IntegrationActivationRemediationKind::UnblockPlatform => {
                    summary.unblock_platform_remediations += 1;
                }
                IntegrationActivationRemediationKind::EnableDependency => {
                    summary.enable_dependency_remediations += 1;
                }
                IntegrationActivationRemediationKind::ReviewPolicy => {
                    summary.review_policy_remediations += 1;
                }
                IntegrationActivationRemediationKind::CompleteReview => {
                    summary.complete_review_remediations += 1;
                }
                IntegrationActivationRemediationKind::RunVerification => {
                    summary.run_verification_remediations += 1;
                }
                IntegrationActivationRemediationKind::RecordAudit => {
                    summary.record_audit_remediations += 1;
                }
            }

            match remediation.owner_lane {
                IntegrationActivationResponseOwnerLane::Platform => {
                    summary.platform_owner_remediations += 1;
                }
                IntegrationActivationResponseOwnerLane::Integration => {
                    summary.integration_owner_remediations += 1;
                }
                IntegrationActivationResponseOwnerLane::Security => {
                    summary.security_owner_remediations += 1;
                }
                IntegrationActivationResponseOwnerLane::Reviewer => {
                    summary.reviewer_owner_remediations += 1;
                }
                IntegrationActivationResponseOwnerLane::Verification => {
                    summary.verification_owner_remediations += 1;
                }
                IntegrationActivationResponseOwnerLane::Audit => {
                    summary.audit_owner_remediations += 1;
                }
            }

            match remediation.status {
                IntegrationActivationRemediationStatus::Blocked => {
                    summary.blocked_remediations += 1;
                }
                IntegrationActivationRemediationStatus::NeedsOwnerAction => {
                    summary.owner_action_remediations += 1;
                }
                IntegrationActivationRemediationStatus::ReadyToExecute => {
                    summary.ready_to_execute_remediations += 1;
                }
                IntegrationActivationRemediationStatus::Tracking => {
                    summary.tracking_remediations += 1;
                }
            }

            if remediation.requires_attention() {
                summary.remediations_requiring_attention += 1;
                summary.first_attention_priority = min_optional_priority(
                    summary.first_attention_priority,
                    Some(remediation.priority),
                );
            }
            if remediation.has_dependency_work() {
                summary.remediations_with_dependency_work += 1;
            }
            if remediation.has_policy_risk() {
                summary.remediations_with_policy_risk += 1;
            }
            if remediation.ready_to_verify() {
                summary.remediations_ready_to_verify += 1;
            }
            if remediation.has_policy_surface() {
                summary.remediations_with_policy_surface += 1;
            }
            summary.highest_policy_tier =
                summary.highest_policy_tier.max(remediation.required_tier);
        }

        summary.unique_integrations = integration_ids.len();
        summary.overall_status = if summary.blocked_remediations > 0
            || summary.unblock_platform_remediations > 0
            || summary.enable_dependency_remediations > 0
        {
            IntegrationActivationHealthStatus::Blocked
        } else if summary.remediations_requiring_attention > 0
            || summary.review_policy_remediations > 0
            || summary.complete_review_remediations > 0
        {
            IntegrationActivationHealthStatus::NeedsReview
        } else if summary.ready_to_execute_remediations > 0
            || summary.run_verification_remediations > 0
        {
            IntegrationActivationHealthStatus::Ready
        } else {
            IntegrationActivationHealthStatus::Empty
        };
        summary
    }

    pub fn is_empty(&self) -> bool {
        self.total_remediations == 0
    }

    pub fn has_blockers(&self) -> bool {
        self.unblock_platform_remediations > 0 || self.blocked_remediations > 0
    }

    pub fn has_dependency_work(&self) -> bool {
        self.enable_dependency_remediations > 0 || self.remediations_with_dependency_work > 0
    }

    pub fn has_policy_risk(&self) -> bool {
        self.review_policy_remediations > 0 || self.remediations_with_policy_risk > 0
    }

    pub fn has_owner_action(&self) -> bool {
        self.owner_action_remediations > 0
    }

    pub fn ready_to_execute(&self) -> bool {
        self.ready_to_execute_remediations > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.remediations_requiring_attention > 0 || self.overall_status.requires_attention()
    }
}

impl IntegrationActivationClosureGate {
    fn from_remediation(remediation: &IntegrationActivationRemediationItem) -> Self {
        let closure_status = IntegrationActivationClosureStatus::from_remediation(remediation);
        let closure_ready = closure_status == IntegrationActivationClosureStatus::ReadyForClosure;

        Self {
            sequence: 0,
            closure_status,
            owner_lane: remediation.owner_lane,
            source_remediation_sequence: remediation.sequence,
            source_remediation_kind: remediation.remediation_kind,
            source_remediation_status: remediation.status,
            source_id: remediation.source_id.clone(),
            title: format!("{}: {}", closure_status.as_str(), remediation.title),
            summary: remediation.summary.clone(),
            priority: remediation.priority,
            integration_ids: remediation.integration_ids.clone(),
            recommended_view: remediation.recommended_view,
            required_tier: remediation.required_tier,
            policy_surface: remediation.policy_surface,
            dependency_work: remediation.has_dependency_work(),
            policy_risk: remediation.has_policy_risk(),
            verification_ready: remediation.ready_to_verify() || remediation.ready_to_execute(),
            blocked: remediation.blocked(),
            requires_attention: remediation.requires_attention()
                || closure_status.requires_attention(),
            closure_ready,
        }
    }

    pub fn integration_count(&self) -> usize {
        self.integration_ids.len()
    }

    pub fn has_dependency_work(&self) -> bool {
        self.dependency_work
    }

    pub fn has_policy_risk(&self) -> bool {
        self.policy_risk
    }

    pub fn ready_to_verify(&self) -> bool {
        self.verification_ready
    }

    pub fn blocked(&self) -> bool {
        self.blocked || self.closure_status == IntegrationActivationClosureStatus::Blocked
    }

    pub fn closure_ready(&self) -> bool {
        self.closure_ready
    }

    pub fn has_policy_surface(&self) -> bool {
        self.policy_surface.is_some()
    }

    pub fn requires_attention(&self) -> bool {
        self.requires_attention
    }
}

impl IntegrationActivationClosureStatus {
    pub fn requires_attention(self) -> bool {
        matches!(self, Self::Blocked | Self::OwnerActionRequired)
    }
}

impl IntegrationActivationClosureSummary {
    pub fn from_gates<'a>(
        gates: impl IntoIterator<Item = &'a IntegrationActivationClosureGate>,
    ) -> Self {
        let mut integration_ids = BTreeSet::new();
        let mut summary = Self {
            total_gates: 0,
            unique_integrations: 0,
            gates_requiring_attention: 0,
            blocked_gates: 0,
            owner_action_gates: 0,
            ready_for_verification_gates: 0,
            ready_for_closure_gates: 0,
            tracking_gates: 0,
            platform_owner_gates: 0,
            integration_owner_gates: 0,
            security_owner_gates: 0,
            reviewer_owner_gates: 0,
            verification_owner_gates: 0,
            audit_owner_gates: 0,
            gates_with_dependency_work: 0,
            gates_with_policy_risk: 0,
            gates_ready_to_verify: 0,
            gates_with_policy_surface: 0,
            next_closure_status: None,
            next_remediation_kind: None,
            next_owner_lane: None,
            next_recommended_view: None,
            next_gate_sequence: None,
            next_gate_priority: None,
            first_attention_priority: None,
            highest_policy_tier: PrivilegeTier::ReadOnly,
            overall_status: IntegrationActivationHealthStatus::Empty,
        };

        for gate in gates {
            summary.total_gates += 1;
            for integration_id in &gate.integration_ids {
                integration_ids.insert(integration_id.clone());
            }

            if summary.next_closure_status.is_none()
                && (gate.requires_attention() || gate.ready_to_verify() || gate.closure_ready())
            {
                summary.next_closure_status = Some(gate.closure_status);
                summary.next_remediation_kind = Some(gate.source_remediation_kind);
                summary.next_owner_lane = Some(gate.owner_lane);
                summary.next_recommended_view = Some(gate.recommended_view);
                summary.next_gate_sequence = Some(gate.sequence);
                summary.next_gate_priority = Some(gate.priority);
            }

            match gate.closure_status {
                IntegrationActivationClosureStatus::Blocked => summary.blocked_gates += 1,
                IntegrationActivationClosureStatus::OwnerActionRequired => {
                    summary.owner_action_gates += 1;
                }
                IntegrationActivationClosureStatus::ReadyForVerification => {
                    summary.ready_for_verification_gates += 1;
                }
                IntegrationActivationClosureStatus::ReadyForClosure => {
                    summary.ready_for_closure_gates += 1;
                }
                IntegrationActivationClosureStatus::Tracking => summary.tracking_gates += 1,
            }

            match gate.owner_lane {
                IntegrationActivationResponseOwnerLane::Platform => {
                    summary.platform_owner_gates += 1;
                }
                IntegrationActivationResponseOwnerLane::Integration => {
                    summary.integration_owner_gates += 1;
                }
                IntegrationActivationResponseOwnerLane::Security => {
                    summary.security_owner_gates += 1;
                }
                IntegrationActivationResponseOwnerLane::Reviewer => {
                    summary.reviewer_owner_gates += 1;
                }
                IntegrationActivationResponseOwnerLane::Verification => {
                    summary.verification_owner_gates += 1;
                }
                IntegrationActivationResponseOwnerLane::Audit => {
                    summary.audit_owner_gates += 1;
                }
            }

            if gate.requires_attention() {
                summary.gates_requiring_attention += 1;
                summary.first_attention_priority =
                    min_optional_priority(summary.first_attention_priority, Some(gate.priority));
            }
            if gate.has_dependency_work() {
                summary.gates_with_dependency_work += 1;
            }
            if gate.has_policy_risk() {
                summary.gates_with_policy_risk += 1;
            }
            if gate.ready_to_verify() {
                summary.gates_ready_to_verify += 1;
            }
            if gate.has_policy_surface() {
                summary.gates_with_policy_surface += 1;
            }
            summary.highest_policy_tier = summary.highest_policy_tier.max(gate.required_tier);
        }

        summary.unique_integrations = integration_ids.len();
        summary.overall_status = if summary.blocked_gates > 0 {
            IntegrationActivationHealthStatus::Blocked
        } else if summary.gates_requiring_attention > 0 || summary.owner_action_gates > 0 {
            IntegrationActivationHealthStatus::NeedsReview
        } else if summary.ready_for_verification_gates > 0 || summary.ready_for_closure_gates > 0 {
            IntegrationActivationHealthStatus::Ready
        } else {
            IntegrationActivationHealthStatus::Empty
        };
        summary
    }

    pub fn is_empty(&self) -> bool {
        self.total_gates == 0
    }

    pub fn has_blockers(&self) -> bool {
        self.blocked_gates > 0
    }

    pub fn has_owner_action(&self) -> bool {
        self.owner_action_gates > 0
    }

    pub fn ready_for_verification(&self) -> bool {
        self.ready_for_verification_gates > 0
    }

    pub fn closure_ready(&self) -> bool {
        self.ready_for_closure_gates > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.gates_requiring_attention > 0 || self.overall_status.requires_attention()
    }
}

impl IntegrationActivationReleasePacket {
    fn from_closure_gate(gate: &IntegrationActivationClosureGate) -> Self {
        let release_status = IntegrationActivationReleaseStatus::from_closure_gate(gate);
        let verification_required =
            release_status == IntegrationActivationReleaseStatus::VerificationRequired;
        let release_ready = release_status == IntegrationActivationReleaseStatus::ReadyForRelease;
        let release_blocked = matches!(
            release_status,
            IntegrationActivationReleaseStatus::Blocked
                | IntegrationActivationReleaseStatus::OwnerActionRequired
                | IntegrationActivationReleaseStatus::VerificationRequired
        );

        Self {
            sequence: 0,
            release_status,
            owner_lane: gate.owner_lane,
            source_closure_sequence: gate.sequence,
            source_closure_status: gate.closure_status,
            source_remediation_kind: gate.source_remediation_kind,
            source_remediation_status: gate.source_remediation_status,
            source_id: gate.source_id.clone(),
            title: format!("{}: {}", release_status.as_str(), gate.title),
            summary: gate.summary.clone(),
            priority: gate.priority,
            integration_ids: gate.integration_ids.clone(),
            recommended_view: gate.recommended_view,
            required_tier: gate.required_tier,
            policy_surface: gate.policy_surface,
            dependency_work: gate.has_dependency_work(),
            policy_risk: gate.has_policy_risk(),
            verification_required,
            release_blocked,
            requires_attention: gate.requires_attention() || release_status.requires_attention(),
            release_ready,
        }
    }

    pub fn integration_count(&self) -> usize {
        self.integration_ids.len()
    }

    pub fn has_dependency_work(&self) -> bool {
        self.dependency_work
    }

    pub fn has_policy_risk(&self) -> bool {
        self.policy_risk
    }

    pub fn needs_verification(&self) -> bool {
        self.verification_required
    }

    pub fn release_blocked(&self) -> bool {
        self.release_blocked
    }

    pub fn release_ready(&self) -> bool {
        self.release_ready
    }

    pub fn has_policy_surface(&self) -> bool {
        self.policy_surface.is_some()
    }

    pub fn requires_attention(&self) -> bool {
        self.requires_attention
    }
}

impl IntegrationActivationReleaseStatus {
    pub fn requires_attention(self) -> bool {
        matches!(
            self,
            Self::Blocked | Self::OwnerActionRequired | Self::VerificationRequired
        )
    }
}

impl IntegrationActivationReleaseSummary {
    pub fn from_packets<'a>(
        packets: impl IntoIterator<Item = &'a IntegrationActivationReleasePacket>,
    ) -> Self {
        let mut integration_ids = BTreeSet::new();
        let mut summary = Self {
            total_packets: 0,
            unique_integrations: 0,
            packets_requiring_attention: 0,
            blocked_packets: 0,
            owner_action_packets: 0,
            verification_required_packets: 0,
            ready_for_release_packets: 0,
            monitoring_packets: 0,
            platform_owner_packets: 0,
            integration_owner_packets: 0,
            security_owner_packets: 0,
            reviewer_owner_packets: 0,
            verification_owner_packets: 0,
            audit_owner_packets: 0,
            packets_with_dependency_work: 0,
            packets_with_policy_risk: 0,
            packets_requiring_verification: 0,
            packets_ready_for_release: 0,
            packets_with_policy_surface: 0,
            next_release_status: None,
            next_closure_status: None,
            next_owner_lane: None,
            next_recommended_view: None,
            next_packet_sequence: None,
            next_packet_priority: None,
            first_attention_priority: None,
            highest_policy_tier: PrivilegeTier::ReadOnly,
            overall_status: IntegrationActivationHealthStatus::Empty,
        };

        for packet in packets {
            summary.total_packets += 1;
            for integration_id in &packet.integration_ids {
                integration_ids.insert(integration_id.clone());
            }

            if summary.next_release_status.is_none()
                && (packet.requires_attention()
                    || packet.needs_verification()
                    || packet.release_ready())
            {
                summary.next_release_status = Some(packet.release_status);
                summary.next_closure_status = Some(packet.source_closure_status);
                summary.next_owner_lane = Some(packet.owner_lane);
                summary.next_recommended_view = Some(packet.recommended_view);
                summary.next_packet_sequence = Some(packet.sequence);
                summary.next_packet_priority = Some(packet.priority);
            }

            match packet.release_status {
                IntegrationActivationReleaseStatus::Blocked => summary.blocked_packets += 1,
                IntegrationActivationReleaseStatus::OwnerActionRequired => {
                    summary.owner_action_packets += 1;
                }
                IntegrationActivationReleaseStatus::VerificationRequired => {
                    summary.verification_required_packets += 1;
                }
                IntegrationActivationReleaseStatus::ReadyForRelease => {
                    summary.ready_for_release_packets += 1;
                }
                IntegrationActivationReleaseStatus::Monitoring => summary.monitoring_packets += 1,
            }

            match packet.owner_lane {
                IntegrationActivationResponseOwnerLane::Platform => {
                    summary.platform_owner_packets += 1;
                }
                IntegrationActivationResponseOwnerLane::Integration => {
                    summary.integration_owner_packets += 1;
                }
                IntegrationActivationResponseOwnerLane::Security => {
                    summary.security_owner_packets += 1;
                }
                IntegrationActivationResponseOwnerLane::Reviewer => {
                    summary.reviewer_owner_packets += 1;
                }
                IntegrationActivationResponseOwnerLane::Verification => {
                    summary.verification_owner_packets += 1;
                }
                IntegrationActivationResponseOwnerLane::Audit => {
                    summary.audit_owner_packets += 1;
                }
            }

            if packet.requires_attention() {
                summary.packets_requiring_attention += 1;
                summary.first_attention_priority =
                    min_optional_priority(summary.first_attention_priority, Some(packet.priority));
            }
            if packet.has_dependency_work() {
                summary.packets_with_dependency_work += 1;
            }
            if packet.has_policy_risk() {
                summary.packets_with_policy_risk += 1;
            }
            if packet.needs_verification() {
                summary.packets_requiring_verification += 1;
            }
            if packet.release_ready() {
                summary.packets_ready_for_release += 1;
            }
            if packet.has_policy_surface() {
                summary.packets_with_policy_surface += 1;
            }
            summary.highest_policy_tier = summary.highest_policy_tier.max(packet.required_tier);
        }

        summary.unique_integrations = integration_ids.len();
        summary.overall_status = if summary.blocked_packets > 0 {
            IntegrationActivationHealthStatus::Blocked
        } else if summary.packets_requiring_attention > 0
            || summary.owner_action_packets > 0
            || summary.verification_required_packets > 0
        {
            IntegrationActivationHealthStatus::NeedsReview
        } else if summary.ready_for_release_packets > 0 {
            IntegrationActivationHealthStatus::Ready
        } else {
            IntegrationActivationHealthStatus::Empty
        };
        summary
    }

    pub fn is_empty(&self) -> bool {
        self.total_packets == 0
    }

    pub fn has_blockers(&self) -> bool {
        self.blocked_packets > 0
    }

    pub fn has_owner_action(&self) -> bool {
        self.owner_action_packets > 0
    }

    pub fn needs_verification(&self) -> bool {
        self.verification_required_packets > 0 || self.packets_requiring_verification > 0
    }

    pub fn release_ready(&self) -> bool {
        self.ready_for_release_packets > 0 || self.packets_ready_for_release > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.packets_requiring_attention > 0 || self.overall_status.requires_attention()
    }
}

impl IntegrationActivationDeliveryManifest {
    fn from_release_packet(packet: &IntegrationActivationReleasePacket) -> Self {
        let delivery_status = IntegrationActivationDeliveryStatus::from_release_packet(packet);
        let delivery_channel = IntegrationActivationDeliveryChannel::from_release_packet(packet);
        let delivery_ready = delivery_status == IntegrationActivationDeliveryStatus::ReadyToDeliver;
        let delivery_blocked = matches!(
            delivery_status,
            IntegrationActivationDeliveryStatus::Blocked
                | IntegrationActivationDeliveryStatus::AwaitingVerification
                | IntegrationActivationDeliveryStatus::AwaitingOwner
        );

        Self {
            sequence: 0,
            delivery_status,
            delivery_channel,
            owner_lane: packet.owner_lane,
            source_release_sequence: packet.sequence,
            source_release_status: packet.release_status,
            source_closure_status: packet.source_closure_status,
            source_remediation_kind: packet.source_remediation_kind,
            source_remediation_status: packet.source_remediation_status,
            source_id: packet.source_id.clone(),
            title: format!("{}: {}", delivery_status.as_str(), packet.title),
            summary: packet.summary.clone(),
            priority: packet.priority,
            integration_ids: packet.integration_ids.clone(),
            recommended_view: packet.recommended_view,
            required_tier: packet.required_tier,
            policy_surface: packet.policy_surface,
            dependency_work: packet.has_dependency_work(),
            policy_risk: packet.has_policy_risk(),
            verification_required: packet.needs_verification(),
            release_ready: packet.release_ready(),
            delivery_blocked,
            delivery_ready,
            requires_attention: packet.requires_attention() || delivery_status.requires_attention(),
        }
    }

    pub fn integration_count(&self) -> usize {
        self.integration_ids.len()
    }

    pub fn has_dependency_work(&self) -> bool {
        self.dependency_work
    }

    pub fn has_policy_risk(&self) -> bool {
        self.policy_risk
    }

    pub fn needs_verification(&self) -> bool {
        self.verification_required
    }

    pub fn delivery_blocked(&self) -> bool {
        self.delivery_blocked
    }

    pub fn delivery_ready(&self) -> bool {
        self.delivery_ready
    }

    pub fn release_ready(&self) -> bool {
        self.release_ready
    }

    pub fn has_policy_surface(&self) -> bool {
        self.policy_surface.is_some()
    }

    pub fn requires_attention(&self) -> bool {
        self.requires_attention
    }
}

impl IntegrationActivationDeliveryStatus {
    pub fn requires_attention(self) -> bool {
        matches!(
            self,
            Self::Blocked | Self::AwaitingVerification | Self::AwaitingOwner
        )
    }
}

impl IntegrationActivationDeliverySummary {
    pub fn from_manifests<'a>(
        manifests: impl IntoIterator<Item = &'a IntegrationActivationDeliveryManifest>,
    ) -> Self {
        let mut integration_ids = BTreeSet::new();
        let mut summary = Self {
            total_manifests: 0,
            unique_integrations: 0,
            manifests_requiring_attention: 0,
            blocked_manifests: 0,
            awaiting_verification_manifests: 0,
            awaiting_owner_manifests: 0,
            ready_to_deliver_manifests: 0,
            monitoring_manifests: 0,
            platform_channel_manifests: 0,
            integration_channel_manifests: 0,
            security_channel_manifests: 0,
            verification_channel_manifests: 0,
            audit_channel_manifests: 0,
            monitoring_channel_manifests: 0,
            platform_owner_manifests: 0,
            integration_owner_manifests: 0,
            security_owner_manifests: 0,
            reviewer_owner_manifests: 0,
            verification_owner_manifests: 0,
            audit_owner_manifests: 0,
            manifests_with_dependency_work: 0,
            manifests_with_policy_risk: 0,
            manifests_requiring_verification: 0,
            manifests_ready_to_deliver: 0,
            manifests_with_policy_surface: 0,
            next_delivery_status: None,
            next_delivery_channel: None,
            next_release_status: None,
            next_owner_lane: None,
            next_recommended_view: None,
            next_manifest_sequence: None,
            next_manifest_priority: None,
            first_attention_priority: None,
            highest_policy_tier: PrivilegeTier::ReadOnly,
            overall_status: IntegrationActivationHealthStatus::Empty,
        };

        for manifest in manifests {
            summary.total_manifests += 1;
            for integration_id in &manifest.integration_ids {
                integration_ids.insert(integration_id.clone());
            }

            if summary.next_delivery_status.is_none()
                && (manifest.requires_attention()
                    || manifest.needs_verification()
                    || manifest.delivery_ready())
            {
                summary.next_delivery_status = Some(manifest.delivery_status);
                summary.next_delivery_channel = Some(manifest.delivery_channel);
                summary.next_release_status = Some(manifest.source_release_status);
                summary.next_owner_lane = Some(manifest.owner_lane);
                summary.next_recommended_view = Some(manifest.recommended_view);
                summary.next_manifest_sequence = Some(manifest.sequence);
                summary.next_manifest_priority = Some(manifest.priority);
            }

            match manifest.delivery_status {
                IntegrationActivationDeliveryStatus::Blocked => summary.blocked_manifests += 1,
                IntegrationActivationDeliveryStatus::AwaitingVerification => {
                    summary.awaiting_verification_manifests += 1;
                }
                IntegrationActivationDeliveryStatus::AwaitingOwner => {
                    summary.awaiting_owner_manifests += 1;
                }
                IntegrationActivationDeliveryStatus::ReadyToDeliver => {
                    summary.ready_to_deliver_manifests += 1;
                }
                IntegrationActivationDeliveryStatus::Monitoring => {
                    summary.monitoring_manifests += 1;
                }
            }

            match manifest.delivery_channel {
                IntegrationActivationDeliveryChannel::Platform => {
                    summary.platform_channel_manifests += 1;
                }
                IntegrationActivationDeliveryChannel::Integration => {
                    summary.integration_channel_manifests += 1;
                }
                IntegrationActivationDeliveryChannel::Security => {
                    summary.security_channel_manifests += 1;
                }
                IntegrationActivationDeliveryChannel::Verification => {
                    summary.verification_channel_manifests += 1;
                }
                IntegrationActivationDeliveryChannel::Audit => {
                    summary.audit_channel_manifests += 1;
                }
                IntegrationActivationDeliveryChannel::Monitoring => {
                    summary.monitoring_channel_manifests += 1;
                }
            }

            match manifest.owner_lane {
                IntegrationActivationResponseOwnerLane::Platform => {
                    summary.platform_owner_manifests += 1;
                }
                IntegrationActivationResponseOwnerLane::Integration => {
                    summary.integration_owner_manifests += 1;
                }
                IntegrationActivationResponseOwnerLane::Security => {
                    summary.security_owner_manifests += 1;
                }
                IntegrationActivationResponseOwnerLane::Reviewer => {
                    summary.reviewer_owner_manifests += 1;
                }
                IntegrationActivationResponseOwnerLane::Verification => {
                    summary.verification_owner_manifests += 1;
                }
                IntegrationActivationResponseOwnerLane::Audit => {
                    summary.audit_owner_manifests += 1;
                }
            }

            if manifest.requires_attention() {
                summary.manifests_requiring_attention += 1;
                summary.first_attention_priority = min_optional_priority(
                    summary.first_attention_priority,
                    Some(manifest.priority),
                );
            }
            if manifest.has_dependency_work() {
                summary.manifests_with_dependency_work += 1;
            }
            if manifest.has_policy_risk() {
                summary.manifests_with_policy_risk += 1;
            }
            if manifest.needs_verification() {
                summary.manifests_requiring_verification += 1;
            }
            if manifest.delivery_ready() {
                summary.manifests_ready_to_deliver += 1;
            }
            if manifest.has_policy_surface() {
                summary.manifests_with_policy_surface += 1;
            }
            summary.highest_policy_tier = summary.highest_policy_tier.max(manifest.required_tier);
        }

        summary.unique_integrations = integration_ids.len();
        summary.overall_status = if summary.blocked_manifests > 0 {
            IntegrationActivationHealthStatus::Blocked
        } else if summary.manifests_requiring_attention > 0
            || summary.awaiting_verification_manifests > 0
            || summary.awaiting_owner_manifests > 0
        {
            IntegrationActivationHealthStatus::NeedsReview
        } else if summary.ready_to_deliver_manifests > 0 {
            IntegrationActivationHealthStatus::Ready
        } else {
            IntegrationActivationHealthStatus::Empty
        };
        summary
    }

    pub fn is_empty(&self) -> bool {
        self.total_manifests == 0
    }

    pub fn has_blockers(&self) -> bool {
        self.blocked_manifests > 0
    }

    pub fn has_owner_action(&self) -> bool {
        self.awaiting_owner_manifests > 0
    }

    pub fn needs_verification(&self) -> bool {
        self.awaiting_verification_manifests > 0 || self.manifests_requiring_verification > 0
    }

    pub fn delivery_ready(&self) -> bool {
        self.ready_to_deliver_manifests > 0 || self.manifests_ready_to_deliver > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.manifests_requiring_attention > 0 || self.overall_status.requires_attention()
    }
}

impl IntegrationActivationDeploymentRecord {
    fn from_delivery_manifest(manifest: &IntegrationActivationDeliveryManifest) -> Self {
        let deployment_status =
            IntegrationActivationDeploymentStatus::from_delivery_manifest(manifest);
        let deployment_ring = IntegrationActivationDeploymentRing::from_delivery_manifest(manifest);
        let deployment_ready =
            deployment_status == IntegrationActivationDeploymentStatus::ReadyToDeploy;
        let deployment_blocked = matches!(
            deployment_status,
            IntegrationActivationDeploymentStatus::Blocked
                | IntegrationActivationDeploymentStatus::AwaitingVerification
                | IntegrationActivationDeploymentStatus::AwaitingOwner
        );

        Self {
            sequence: 0,
            deployment_status,
            deployment_ring,
            delivery_channel: manifest.delivery_channel,
            owner_lane: manifest.owner_lane,
            source_delivery_sequence: manifest.sequence,
            source_delivery_status: manifest.delivery_status,
            source_release_status: manifest.source_release_status,
            source_closure_status: manifest.source_closure_status,
            source_remediation_kind: manifest.source_remediation_kind,
            source_remediation_status: manifest.source_remediation_status,
            source_id: manifest.source_id.clone(),
            title: format!("{}: {}", deployment_status.as_str(), manifest.title),
            summary: manifest.summary.clone(),
            priority: manifest.priority,
            integration_ids: manifest.integration_ids.clone(),
            recommended_view: manifest.recommended_view,
            required_tier: manifest.required_tier,
            policy_surface: manifest.policy_surface,
            dependency_work: manifest.has_dependency_work(),
            policy_risk: manifest.has_policy_risk(),
            verification_required: manifest.needs_verification(),
            delivery_ready: manifest.delivery_ready(),
            deployment_blocked,
            deployment_ready,
            requires_attention: manifest.requires_attention()
                || deployment_status.requires_attention(),
        }
    }

    pub fn integration_count(&self) -> usize {
        self.integration_ids.len()
    }

    pub fn has_dependency_work(&self) -> bool {
        self.dependency_work
    }

    pub fn has_policy_risk(&self) -> bool {
        self.policy_risk
    }

    pub fn needs_verification(&self) -> bool {
        self.verification_required
    }

    pub fn deployment_blocked(&self) -> bool {
        self.deployment_blocked
    }

    pub fn deployment_ready(&self) -> bool {
        self.deployment_ready
    }

    pub fn delivery_ready(&self) -> bool {
        self.delivery_ready
    }

    pub fn has_policy_surface(&self) -> bool {
        self.policy_surface.is_some()
    }

    pub fn requires_attention(&self) -> bool {
        self.requires_attention
    }
}

impl IntegrationActivationDeploymentStatus {
    pub fn requires_attention(self) -> bool {
        matches!(
            self,
            Self::Blocked | Self::AwaitingVerification | Self::AwaitingOwner
        )
    }
}

impl IntegrationActivationDeploymentSummary {
    pub fn from_records<'a>(
        records: impl IntoIterator<Item = &'a IntegrationActivationDeploymentRecord>,
    ) -> Self {
        let mut integration_ids = BTreeSet::new();
        let mut summary = Self {
            total_records: 0,
            unique_integrations: 0,
            records_requiring_attention: 0,
            blocked_records: 0,
            awaiting_verification_records: 0,
            awaiting_owner_records: 0,
            ready_to_deploy_records: 0,
            monitoring_records: 0,
            platform_ring_records: 0,
            integration_ring_records: 0,
            security_ring_records: 0,
            verification_ring_records: 0,
            audit_ring_records: 0,
            monitoring_ring_records: 0,
            platform_owner_records: 0,
            integration_owner_records: 0,
            security_owner_records: 0,
            reviewer_owner_records: 0,
            verification_owner_records: 0,
            audit_owner_records: 0,
            records_with_dependency_work: 0,
            records_with_policy_risk: 0,
            records_requiring_verification: 0,
            records_ready_to_deploy: 0,
            records_with_policy_surface: 0,
            next_deployment_status: None,
            next_deployment_ring: None,
            next_delivery_status: None,
            next_owner_lane: None,
            next_recommended_view: None,
            next_record_sequence: None,
            next_record_priority: None,
            first_attention_priority: None,
            highest_policy_tier: PrivilegeTier::ReadOnly,
            overall_status: IntegrationActivationHealthStatus::Empty,
        };

        for record in records {
            summary.total_records += 1;
            for integration_id in &record.integration_ids {
                integration_ids.insert(integration_id.clone());
            }

            if summary.next_deployment_status.is_none()
                && (record.requires_attention()
                    || record.needs_verification()
                    || record.deployment_ready())
            {
                summary.next_deployment_status = Some(record.deployment_status);
                summary.next_deployment_ring = Some(record.deployment_ring);
                summary.next_delivery_status = Some(record.source_delivery_status);
                summary.next_owner_lane = Some(record.owner_lane);
                summary.next_recommended_view = Some(record.recommended_view);
                summary.next_record_sequence = Some(record.sequence);
                summary.next_record_priority = Some(record.priority);
            }

            match record.deployment_status {
                IntegrationActivationDeploymentStatus::Blocked => summary.blocked_records += 1,
                IntegrationActivationDeploymentStatus::AwaitingVerification => {
                    summary.awaiting_verification_records += 1;
                }
                IntegrationActivationDeploymentStatus::AwaitingOwner => {
                    summary.awaiting_owner_records += 1;
                }
                IntegrationActivationDeploymentStatus::ReadyToDeploy => {
                    summary.ready_to_deploy_records += 1;
                }
                IntegrationActivationDeploymentStatus::Monitoring => {
                    summary.monitoring_records += 1;
                }
            }

            match record.deployment_ring {
                IntegrationActivationDeploymentRing::Platform => {
                    summary.platform_ring_records += 1;
                }
                IntegrationActivationDeploymentRing::Integration => {
                    summary.integration_ring_records += 1;
                }
                IntegrationActivationDeploymentRing::Security => {
                    summary.security_ring_records += 1;
                }
                IntegrationActivationDeploymentRing::Verification => {
                    summary.verification_ring_records += 1;
                }
                IntegrationActivationDeploymentRing::Audit => {
                    summary.audit_ring_records += 1;
                }
                IntegrationActivationDeploymentRing::Monitoring => {
                    summary.monitoring_ring_records += 1;
                }
            }

            match record.owner_lane {
                IntegrationActivationResponseOwnerLane::Platform => {
                    summary.platform_owner_records += 1;
                }
                IntegrationActivationResponseOwnerLane::Integration => {
                    summary.integration_owner_records += 1;
                }
                IntegrationActivationResponseOwnerLane::Security => {
                    summary.security_owner_records += 1;
                }
                IntegrationActivationResponseOwnerLane::Reviewer => {
                    summary.reviewer_owner_records += 1;
                }
                IntegrationActivationResponseOwnerLane::Verification => {
                    summary.verification_owner_records += 1;
                }
                IntegrationActivationResponseOwnerLane::Audit => {
                    summary.audit_owner_records += 1;
                }
            }

            if record.requires_attention() {
                summary.records_requiring_attention += 1;
                summary.first_attention_priority =
                    min_optional_priority(summary.first_attention_priority, Some(record.priority));
            }
            if record.has_dependency_work() {
                summary.records_with_dependency_work += 1;
            }
            if record.has_policy_risk() {
                summary.records_with_policy_risk += 1;
            }
            if record.needs_verification() {
                summary.records_requiring_verification += 1;
            }
            if record.deployment_ready() {
                summary.records_ready_to_deploy += 1;
            }
            if record.has_policy_surface() {
                summary.records_with_policy_surface += 1;
            }
            summary.highest_policy_tier = summary.highest_policy_tier.max(record.required_tier);
        }

        summary.unique_integrations = integration_ids.len();
        summary.overall_status = if summary.blocked_records > 0 {
            IntegrationActivationHealthStatus::Blocked
        } else if summary.records_requiring_attention > 0
            || summary.awaiting_verification_records > 0
            || summary.awaiting_owner_records > 0
        {
            IntegrationActivationHealthStatus::NeedsReview
        } else if summary.ready_to_deploy_records > 0 {
            IntegrationActivationHealthStatus::Ready
        } else {
            IntegrationActivationHealthStatus::Empty
        };
        summary
    }

    pub fn is_empty(&self) -> bool {
        self.total_records == 0
    }

    pub fn has_blockers(&self) -> bool {
        self.blocked_records > 0
    }

    pub fn has_owner_action(&self) -> bool {
        self.awaiting_owner_records > 0
    }

    pub fn needs_verification(&self) -> bool {
        self.awaiting_verification_records > 0 || self.records_requiring_verification > 0
    }

    pub fn deployment_ready(&self) -> bool {
        self.ready_to_deploy_records > 0 || self.records_ready_to_deploy > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.records_requiring_attention > 0 || self.overall_status.requires_attention()
    }
}

impl IntegrationActivationSafetyGate {
    fn from_deployment_record(record: &IntegrationActivationDeploymentRecord) -> Self {
        let gate_status =
            IntegrationActivationSafetyGateStatus::from_deployment_status(record.deployment_status);
        let blocks_activation = gate_status.blocks_activation() || record.deployment_blocked();
        let requires_attention =
            gate_status.requires_attention() || record.requires_attention() || blocks_activation;

        Self {
            sequence: 0,
            gate_status,
            deployment_status: record.deployment_status,
            deployment_ring: record.deployment_ring,
            delivery_channel: record.delivery_channel,
            owner_lane: record.owner_lane,
            source_deployment_sequence: record.sequence,
            source_delivery_sequence: record.source_delivery_sequence,
            source_id: record.source_id.clone(),
            title: format!("{} safety gate: {}", gate_status.as_str(), record.title),
            summary: record.summary.clone(),
            priority: record.priority,
            integration_ids: record.integration_ids.clone(),
            recommended_view: record.recommended_view,
            required_tier: record.required_tier,
            policy_surface: record.policy_surface,
            dependency_work: record.dependency_work,
            policy_risk: record.policy_risk,
            verification_required: record.verification_required
                || gate_status == IntegrationActivationSafetyGateStatus::NeedsVerification,
            deployment_ready: record.deployment_ready(),
            blocks_activation,
            requires_attention,
        }
    }

    pub fn integration_count(&self) -> usize {
        self.integration_ids.len()
    }

    pub fn has_dependency_work(&self) -> bool {
        self.dependency_work
    }

    pub fn has_policy_risk(&self) -> bool {
        self.policy_risk
    }

    pub fn needs_verification(&self) -> bool {
        self.verification_required
    }

    pub fn deployment_ready(&self) -> bool {
        self.deployment_ready
    }

    pub fn blocks_activation(&self) -> bool {
        self.blocks_activation
    }

    pub fn requires_attention(&self) -> bool {
        self.requires_attention
    }

    pub fn has_policy_surface(&self) -> bool {
        self.policy_surface.is_some()
    }
}

impl IntegrationActivationSafetySummary {
    pub fn from_gates<'a>(
        gates: impl IntoIterator<Item = &'a IntegrationActivationSafetyGate>,
    ) -> Self {
        let mut integration_ids = BTreeSet::new();
        let mut summary = Self {
            total_gates: 0,
            unique_integrations: 0,
            gates_requiring_attention: 0,
            blocked_gates: 0,
            verification_gates: 0,
            owner_approval_gates: 0,
            ready_to_deploy_gates: 0,
            monitoring_gates: 0,
            platform_ring_gates: 0,
            integration_ring_gates: 0,
            security_ring_gates: 0,
            verification_ring_gates: 0,
            audit_ring_gates: 0,
            monitoring_ring_gates: 0,
            gates_with_dependency_work: 0,
            gates_with_policy_risk: 0,
            gates_requiring_verification: 0,
            gates_ready_to_deploy: 0,
            gates_with_policy_surface: 0,
            next_gate_status: None,
            next_deployment_ring: None,
            next_owner_lane: None,
            next_recommended_view: None,
            next_gate_sequence: None,
            next_gate_priority: None,
            first_attention_priority: None,
            highest_policy_tier: PrivilegeTier::ReadOnly,
            overall_status: IntegrationActivationHealthStatus::Empty,
        };

        for gate in gates {
            summary.total_gates += 1;
            for integration_id in &gate.integration_ids {
                integration_ids.insert(integration_id.clone());
            }

            if summary.next_gate_status.is_none()
                && (gate.requires_attention() || gate.deployment_ready())
            {
                summary.next_gate_status = Some(gate.gate_status);
                summary.next_deployment_ring = Some(gate.deployment_ring);
                summary.next_owner_lane = Some(gate.owner_lane);
                summary.next_recommended_view = Some(gate.recommended_view);
                summary.next_gate_sequence = Some(gate.sequence);
                summary.next_gate_priority = Some(gate.priority);
            }

            match gate.gate_status {
                IntegrationActivationSafetyGateStatus::Blocked => summary.blocked_gates += 1,
                IntegrationActivationSafetyGateStatus::NeedsVerification => {
                    summary.verification_gates += 1;
                }
                IntegrationActivationSafetyGateStatus::NeedsOwnerApproval => {
                    summary.owner_approval_gates += 1;
                }
                IntegrationActivationSafetyGateStatus::ReadyToDeploy => {
                    summary.ready_to_deploy_gates += 1;
                }
                IntegrationActivationSafetyGateStatus::Monitoring => summary.monitoring_gates += 1,
            }

            match gate.deployment_ring {
                IntegrationActivationDeploymentRing::Platform => summary.platform_ring_gates += 1,
                IntegrationActivationDeploymentRing::Integration => {
                    summary.integration_ring_gates += 1;
                }
                IntegrationActivationDeploymentRing::Security => summary.security_ring_gates += 1,
                IntegrationActivationDeploymentRing::Verification => {
                    summary.verification_ring_gates += 1;
                }
                IntegrationActivationDeploymentRing::Audit => summary.audit_ring_gates += 1,
                IntegrationActivationDeploymentRing::Monitoring => {
                    summary.monitoring_ring_gates += 1;
                }
            }

            if gate.requires_attention() {
                summary.gates_requiring_attention += 1;
                summary.first_attention_priority =
                    min_optional_priority(summary.first_attention_priority, Some(gate.priority));
            }
            if gate.has_dependency_work() {
                summary.gates_with_dependency_work += 1;
            }
            if gate.has_policy_risk() {
                summary.gates_with_policy_risk += 1;
            }
            if gate.needs_verification() {
                summary.gates_requiring_verification += 1;
            }
            if gate.deployment_ready() {
                summary.gates_ready_to_deploy += 1;
            }
            if gate.has_policy_surface() {
                summary.gates_with_policy_surface += 1;
            }
            summary.highest_policy_tier = summary.highest_policy_tier.max(gate.required_tier);
        }

        summary.unique_integrations = integration_ids.len();
        summary.overall_status = if summary.blocked_gates > 0 {
            IntegrationActivationHealthStatus::Blocked
        } else if summary.gates_requiring_attention > 0
            || summary.verification_gates > 0
            || summary.owner_approval_gates > 0
        {
            IntegrationActivationHealthStatus::NeedsReview
        } else if summary.ready_to_deploy_gates > 0 {
            IntegrationActivationHealthStatus::Ready
        } else {
            IntegrationActivationHealthStatus::Empty
        };
        summary
    }

    pub fn is_empty(&self) -> bool {
        self.total_gates == 0
    }

    pub fn has_blockers(&self) -> bool {
        self.blocked_gates > 0
    }

    pub fn has_owner_action(&self) -> bool {
        self.owner_approval_gates > 0
    }

    pub fn needs_verification(&self) -> bool {
        self.verification_gates > 0 || self.gates_requiring_verification > 0
    }

    pub fn deployment_ready(&self) -> bool {
        self.ready_to_deploy_gates > 0 || self.gates_ready_to_deploy > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.gates_requiring_attention > 0 || self.overall_status.requires_attention()
    }
}

impl IntegrationActivationRollbackPlan {
    fn from_safety_gate(gate: &IntegrationActivationSafetyGate) -> Self {
        let rollback_action =
            IntegrationActivationRollbackAction::from_gate_status(gate.gate_status);
        let blocks_activation = gate.blocks_activation();
        let requires_attention = gate.requires_attention() || rollback_action.requires_attention();
        let rollback_ready = rollback_action.rollback_ready() && !requires_attention;

        Self {
            sequence: 0,
            rollback_action,
            gate_status: gate.gate_status,
            deployment_status: gate.deployment_status,
            deployment_ring: gate.deployment_ring,
            delivery_channel: gate.delivery_channel,
            owner_lane: gate.owner_lane,
            source_safety_sequence: gate.sequence,
            source_deployment_sequence: gate.source_deployment_sequence,
            source_delivery_sequence: gate.source_delivery_sequence,
            source_id: gate.source_id.clone(),
            title: format!("{} rollback: {}", rollback_action.as_str(), gate.title),
            summary: gate.summary.clone(),
            priority: gate.priority,
            integration_ids: gate.integration_ids.clone(),
            recommended_view: gate.recommended_view,
            required_tier: gate.required_tier,
            policy_surface: gate.policy_surface,
            dependency_work: gate.dependency_work,
            policy_risk: gate.policy_risk,
            verification_required: gate.verification_required
                || rollback_action == IntegrationActivationRollbackAction::VerifyBeforeRollback,
            deployment_ready: gate.deployment_ready(),
            rollback_ready,
            blocks_activation,
            requires_attention,
        }
    }

    pub fn integration_count(&self) -> usize {
        self.integration_ids.len()
    }

    pub fn has_dependency_work(&self) -> bool {
        self.dependency_work
    }

    pub fn has_policy_risk(&self) -> bool {
        self.policy_risk
    }

    pub fn needs_verification(&self) -> bool {
        self.verification_required
    }

    pub fn deployment_ready(&self) -> bool {
        self.deployment_ready
    }

    pub fn rollback_ready(&self) -> bool {
        self.rollback_ready
    }

    pub fn blocks_activation(&self) -> bool {
        self.blocks_activation
    }

    pub fn requires_attention(&self) -> bool {
        self.requires_attention
    }

    pub fn has_policy_surface(&self) -> bool {
        self.policy_surface.is_some()
    }
}

impl IntegrationActivationRollbackSummary {
    pub fn from_plans<'a>(
        plans: impl IntoIterator<Item = &'a IntegrationActivationRollbackPlan>,
    ) -> Self {
        let mut integration_ids = BTreeSet::new();
        let mut summary = Self {
            total_plans: 0,
            unique_integrations: 0,
            plans_requiring_attention: 0,
            hold_deployment_plans: 0,
            verification_plans: 0,
            owner_approval_plans: 0,
            staged_rollback_plans: 0,
            monitoring_plans: 0,
            platform_ring_plans: 0,
            integration_ring_plans: 0,
            security_ring_plans: 0,
            verification_ring_plans: 0,
            audit_ring_plans: 0,
            monitoring_ring_plans: 0,
            plans_with_dependency_work: 0,
            plans_with_policy_risk: 0,
            plans_requiring_verification: 0,
            plans_ready_for_deployment: 0,
            plans_ready_for_rollback: 0,
            plans_blocking_activation: 0,
            plans_with_policy_surface: 0,
            next_rollback_action: None,
            next_gate_status: None,
            next_deployment_ring: None,
            next_owner_lane: None,
            next_recommended_view: None,
            next_plan_sequence: None,
            next_plan_priority: None,
            first_attention_priority: None,
            highest_policy_tier: PrivilegeTier::ReadOnly,
            overall_status: IntegrationActivationHealthStatus::Empty,
        };

        for plan in plans {
            summary.total_plans += 1;
            for integration_id in &plan.integration_ids {
                integration_ids.insert(integration_id.clone());
            }

            if summary.next_rollback_action.is_none()
                && (plan.requires_attention() || plan.rollback_ready())
            {
                summary.next_rollback_action = Some(plan.rollback_action);
                summary.next_gate_status = Some(plan.gate_status);
                summary.next_deployment_ring = Some(plan.deployment_ring);
                summary.next_owner_lane = Some(plan.owner_lane);
                summary.next_recommended_view = Some(plan.recommended_view);
                summary.next_plan_sequence = Some(plan.sequence);
                summary.next_plan_priority = Some(plan.priority);
            }

            match plan.rollback_action {
                IntegrationActivationRollbackAction::HoldDeployment => {
                    summary.hold_deployment_plans += 1;
                }
                IntegrationActivationRollbackAction::VerifyBeforeRollback => {
                    summary.verification_plans += 1;
                }
                IntegrationActivationRollbackAction::RequestOwnerApproval => {
                    summary.owner_approval_plans += 1;
                }
                IntegrationActivationRollbackAction::StageRollbackPlan => {
                    summary.staged_rollback_plans += 1;
                }
                IntegrationActivationRollbackAction::MonitorRollbackWindow => {
                    summary.monitoring_plans += 1;
                }
            }

            match plan.deployment_ring {
                IntegrationActivationDeploymentRing::Platform => summary.platform_ring_plans += 1,
                IntegrationActivationDeploymentRing::Integration => {
                    summary.integration_ring_plans += 1;
                }
                IntegrationActivationDeploymentRing::Security => summary.security_ring_plans += 1,
                IntegrationActivationDeploymentRing::Verification => {
                    summary.verification_ring_plans += 1;
                }
                IntegrationActivationDeploymentRing::Audit => summary.audit_ring_plans += 1,
                IntegrationActivationDeploymentRing::Monitoring => {
                    summary.monitoring_ring_plans += 1;
                }
            }

            if plan.requires_attention() {
                summary.plans_requiring_attention += 1;
                summary.first_attention_priority =
                    min_optional_priority(summary.first_attention_priority, Some(plan.priority));
            }
            if plan.has_dependency_work() {
                summary.plans_with_dependency_work += 1;
            }
            if plan.has_policy_risk() {
                summary.plans_with_policy_risk += 1;
            }
            if plan.needs_verification() {
                summary.plans_requiring_verification += 1;
            }
            if plan.deployment_ready() {
                summary.plans_ready_for_deployment += 1;
            }
            if plan.rollback_ready() {
                summary.plans_ready_for_rollback += 1;
            }
            if plan.blocks_activation() {
                summary.plans_blocking_activation += 1;
            }
            if plan.has_policy_surface() {
                summary.plans_with_policy_surface += 1;
            }
            summary.highest_policy_tier = summary.highest_policy_tier.max(plan.required_tier);
        }

        summary.unique_integrations = integration_ids.len();
        summary.overall_status = if summary.plans_blocking_activation > 0 {
            IntegrationActivationHealthStatus::Blocked
        } else if summary.plans_requiring_attention > 0
            || summary.verification_plans > 0
            || summary.owner_approval_plans > 0
        {
            IntegrationActivationHealthStatus::NeedsReview
        } else if summary.plans_ready_for_rollback > 0 {
            IntegrationActivationHealthStatus::Ready
        } else {
            IntegrationActivationHealthStatus::Empty
        };
        summary
    }

    pub fn is_empty(&self) -> bool {
        self.total_plans == 0
    }

    pub fn has_blockers(&self) -> bool {
        self.plans_blocking_activation > 0 || self.hold_deployment_plans > 0
    }

    pub fn has_owner_action(&self) -> bool {
        self.owner_approval_plans > 0
    }

    pub fn needs_verification(&self) -> bool {
        self.verification_plans > 0 || self.plans_requiring_verification > 0
    }

    pub fn rollback_ready(&self) -> bool {
        self.plans_ready_for_rollback > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.plans_requiring_attention > 0 || self.overall_status.requires_attention()
    }
}

impl IntegrationActivationObservabilityProbe {
    fn from_rollups(
        plan: &IntegrationActivationRollbackPlan,
        matching_signals: &[&IntegrationActivationWatchtowerSignal],
    ) -> Self {
        let has_observation_signal = matching_signals.iter().any(|signal| {
            signal.signal_kind == IntegrationActivationWatchtowerSignalKind::Observation
        });
        let watchtower_requires_attention = matching_signals
            .iter()
            .any(|signal| signal.requires_attention());
        let has_watchtower_blockers = matching_signals.iter().any(|signal| signal.has_blockers());
        let watchtower_signal_count = matching_signals.len();

        let observability_status = if plan.blocks_activation() || has_watchtower_blockers {
            IntegrationActivationObservabilityStatus::Blocked
        } else if watchtower_requires_attention {
            IntegrationActivationObservabilityStatus::NeedsTelemetryReview
        } else if !plan.rollback_ready() {
            IntegrationActivationObservabilityStatus::NeedsRollbackCoverage
        } else if has_observation_signal {
            IntegrationActivationObservabilityStatus::Monitoring
        } else {
            IntegrationActivationObservabilityStatus::ReadyToObserve
        };
        let requires_attention =
            observability_status.requires_attention() || plan.requires_attention();

        Self {
            sequence: 0,
            observability_status,
            rollback_action: plan.rollback_action,
            gate_status: plan.gate_status,
            deployment_ring: plan.deployment_ring,
            owner_lane: plan.owner_lane,
            source_rollback_sequence: plan.sequence,
            source_safety_sequence: plan.source_safety_sequence,
            source_deployment_sequence: plan.source_deployment_sequence,
            source_id: plan.source_id.clone(),
            title: format!(
                "{} observability: {}",
                observability_status.as_str(),
                plan.title
            ),
            summary: plan.summary.clone(),
            priority: plan.priority,
            integration_ids: plan.integration_ids.clone(),
            recommended_view: plan.recommended_view,
            required_tier: plan.required_tier,
            policy_surface: plan.policy_surface,
            watchtower_signal_count,
            has_observation_signal,
            watchtower_requires_attention,
            rollback_ready: plan.rollback_ready(),
            deployment_ready: plan.deployment_ready(),
            blocks_activation: plan.blocks_activation() || has_watchtower_blockers,
            needs_verification: plan.needs_verification(),
            requires_attention,
        }
    }

    pub fn integration_count(&self) -> usize {
        self.integration_ids.len()
    }

    pub fn has_watchtower_signals(&self) -> bool {
        self.watchtower_signal_count > 0
    }

    pub fn has_policy_surface(&self) -> bool {
        self.policy_surface.is_some()
    }

    pub fn observability_ready(&self) -> bool {
        self.observability_status.observability_ready()
    }

    pub fn rollback_ready(&self) -> bool {
        self.rollback_ready
    }

    pub fn deployment_ready(&self) -> bool {
        self.deployment_ready
    }

    pub fn blocks_activation(&self) -> bool {
        self.blocks_activation
    }

    pub fn needs_verification(&self) -> bool {
        self.needs_verification
    }

    pub fn requires_attention(&self) -> bool {
        self.requires_attention
    }
}

impl IntegrationActivationObservabilitySummary {
    pub fn from_probes<'a>(
        probes: impl IntoIterator<Item = &'a IntegrationActivationObservabilityProbe>,
    ) -> Self {
        let mut integration_ids = BTreeSet::new();
        let mut summary = Self {
            total_probes: 0,
            unique_integrations: 0,
            probes_requiring_attention: 0,
            blocked_probes: 0,
            telemetry_review_probes: 0,
            rollback_coverage_probes: 0,
            ready_to_observe_probes: 0,
            monitoring_probes: 0,
            probes_with_watchtower_signals: 0,
            probes_with_observation_signals: 0,
            probes_with_watchtower_attention: 0,
            probes_ready_for_rollback: 0,
            probes_ready_for_deployment: 0,
            probes_blocking_activation: 0,
            probes_needing_verification: 0,
            probes_with_policy_surface: 0,
            next_observability_status: None,
            next_rollback_action: None,
            next_gate_status: None,
            next_deployment_ring: None,
            next_owner_lane: None,
            next_recommended_view: None,
            next_probe_sequence: None,
            next_probe_priority: None,
            first_attention_priority: None,
            highest_policy_tier: PrivilegeTier::ReadOnly,
            overall_status: IntegrationActivationHealthStatus::Empty,
        };

        for probe in probes {
            summary.total_probes += 1;
            for integration_id in &probe.integration_ids {
                integration_ids.insert(integration_id.clone());
            }

            if summary.next_observability_status.is_none()
                && (probe.requires_attention() || probe.observability_ready())
            {
                summary.next_observability_status = Some(probe.observability_status);
                summary.next_rollback_action = Some(probe.rollback_action);
                summary.next_gate_status = Some(probe.gate_status);
                summary.next_deployment_ring = Some(probe.deployment_ring);
                summary.next_owner_lane = Some(probe.owner_lane);
                summary.next_recommended_view = Some(probe.recommended_view);
                summary.next_probe_sequence = Some(probe.sequence);
                summary.next_probe_priority = Some(probe.priority);
            }

            match probe.observability_status {
                IntegrationActivationObservabilityStatus::Blocked => {
                    summary.blocked_probes += 1;
                }
                IntegrationActivationObservabilityStatus::NeedsTelemetryReview => {
                    summary.telemetry_review_probes += 1;
                }
                IntegrationActivationObservabilityStatus::NeedsRollbackCoverage => {
                    summary.rollback_coverage_probes += 1;
                }
                IntegrationActivationObservabilityStatus::ReadyToObserve => {
                    summary.ready_to_observe_probes += 1;
                }
                IntegrationActivationObservabilityStatus::Monitoring => {
                    summary.monitoring_probes += 1;
                }
            }

            if probe.requires_attention() {
                summary.probes_requiring_attention += 1;
                summary.first_attention_priority =
                    min_optional_priority(summary.first_attention_priority, Some(probe.priority));
            }
            if probe.has_watchtower_signals() {
                summary.probes_with_watchtower_signals += 1;
            }
            if probe.has_observation_signal {
                summary.probes_with_observation_signals += 1;
            }
            if probe.watchtower_requires_attention {
                summary.probes_with_watchtower_attention += 1;
            }
            if probe.rollback_ready() {
                summary.probes_ready_for_rollback += 1;
            }
            if probe.deployment_ready() {
                summary.probes_ready_for_deployment += 1;
            }
            if probe.blocks_activation() {
                summary.probes_blocking_activation += 1;
            }
            if probe.needs_verification() {
                summary.probes_needing_verification += 1;
            }
            if probe.has_policy_surface() {
                summary.probes_with_policy_surface += 1;
            }
            summary.highest_policy_tier = summary.highest_policy_tier.max(probe.required_tier);
        }

        summary.unique_integrations = integration_ids.len();
        summary.overall_status = if summary.blocked_probes > 0 {
            IntegrationActivationHealthStatus::Blocked
        } else if summary.probes_requiring_attention > 0
            || summary.telemetry_review_probes > 0
            || summary.rollback_coverage_probes > 0
        {
            IntegrationActivationHealthStatus::NeedsReview
        } else if summary.ready_to_observe_probes > 0 || summary.monitoring_probes > 0 {
            IntegrationActivationHealthStatus::Ready
        } else {
            IntegrationActivationHealthStatus::Empty
        };
        summary
    }

    pub fn is_empty(&self) -> bool {
        self.total_probes == 0
    }

    pub fn has_blockers(&self) -> bool {
        self.blocked_probes > 0 || self.probes_blocking_activation > 0
    }

    pub fn has_telemetry_review(&self) -> bool {
        self.telemetry_review_probes > 0 || self.probes_with_watchtower_attention > 0
    }

    pub fn needs_rollback_coverage(&self) -> bool {
        self.rollback_coverage_probes > 0
    }

    pub fn observability_ready(&self) -> bool {
        self.ready_to_observe_probes > 0 || self.monitoring_probes > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.probes_requiring_attention > 0 || self.overall_status.requires_attention()
    }
}

impl IntegrationActivationIncidentBrief {
    fn from_observability_probe(probe: &IntegrationActivationObservabilityProbe) -> Self {
        let (severity, action) = match probe.observability_status {
            IntegrationActivationObservabilityStatus::Blocked => (
                IntegrationActivationIncidentSeverity::Critical,
                IntegrationActivationIncidentAction::HoldActivation,
            ),
            IntegrationActivationObservabilityStatus::NeedsTelemetryReview => (
                IntegrationActivationIncidentSeverity::High,
                IntegrationActivationIncidentAction::TriageTelemetry,
            ),
            IntegrationActivationObservabilityStatus::NeedsRollbackCoverage => (
                IntegrationActivationIncidentSeverity::High,
                IntegrationActivationIncidentAction::CompleteRollbackCoverage,
            ),
            IntegrationActivationObservabilityStatus::ReadyToObserve => (
                IntegrationActivationIncidentSeverity::Medium,
                IntegrationActivationIncidentAction::ArmObservation,
            ),
            IntegrationActivationObservabilityStatus::Monitoring => (
                IntegrationActivationIncidentSeverity::Watch,
                IntegrationActivationIncidentAction::ContinueMonitoring,
            ),
        };
        let requires_attention = severity.requires_attention()
            || action.requires_attention()
            || probe.requires_attention();
        let blocks_activation = severity.blocks_activation() || probe.blocks_activation();
        let incident_ready = action.incident_ready() && !requires_attention && !blocks_activation;

        Self {
            sequence: 0,
            severity,
            action,
            observability_status: probe.observability_status,
            rollback_action: probe.rollback_action,
            gate_status: probe.gate_status,
            deployment_ring: probe.deployment_ring,
            owner_lane: probe.owner_lane,
            source_observability_sequence: probe.sequence,
            source_rollback_sequence: probe.source_rollback_sequence,
            source_safety_sequence: probe.source_safety_sequence,
            source_deployment_sequence: probe.source_deployment_sequence,
            source_id: probe.source_id.clone(),
            title: format!("{} incident: {}", severity.as_str(), probe.title),
            summary: probe.summary.clone(),
            priority: probe.priority,
            integration_ids: probe.integration_ids.clone(),
            recommended_view: probe.recommended_view,
            required_tier: probe.required_tier,
            policy_surface: probe.policy_surface,
            watchtower_signal_count: probe.watchtower_signal_count,
            has_observation_signal: probe.has_observation_signal,
            watchtower_requires_attention: probe.watchtower_requires_attention,
            rollback_ready: probe.rollback_ready(),
            deployment_ready: probe.deployment_ready(),
            blocks_activation,
            needs_verification: probe.needs_verification(),
            requires_attention,
            incident_ready,
        }
    }

    pub fn integration_count(&self) -> usize {
        self.integration_ids.len()
    }

    pub fn has_watchtower_signals(&self) -> bool {
        self.watchtower_signal_count > 0
    }

    pub fn has_policy_surface(&self) -> bool {
        self.policy_surface.is_some()
    }

    pub fn rollback_ready(&self) -> bool {
        self.rollback_ready
    }

    pub fn deployment_ready(&self) -> bool {
        self.deployment_ready
    }

    pub fn blocks_activation(&self) -> bool {
        self.blocks_activation
    }

    pub fn needs_verification(&self) -> bool {
        self.needs_verification
    }

    pub fn requires_attention(&self) -> bool {
        self.requires_attention
    }

    pub fn incident_ready(&self) -> bool {
        self.incident_ready
    }
}

impl IntegrationActivationIncidentSummary {
    pub fn from_briefs<'a>(
        briefs: impl IntoIterator<Item = &'a IntegrationActivationIncidentBrief>,
    ) -> Self {
        let mut integration_ids = BTreeSet::new();
        let mut summary = Self {
            total_briefs: 0,
            unique_integrations: 0,
            briefs_requiring_attention: 0,
            critical_briefs: 0,
            high_briefs: 0,
            medium_briefs: 0,
            watch_briefs: 0,
            hold_activation_briefs: 0,
            telemetry_triage_briefs: 0,
            rollback_coverage_briefs: 0,
            arm_observation_briefs: 0,
            monitoring_briefs: 0,
            briefs_with_watchtower_signals: 0,
            briefs_with_observation_signals: 0,
            briefs_with_watchtower_attention: 0,
            briefs_ready_for_rollback: 0,
            briefs_ready_for_deployment: 0,
            briefs_blocking_activation: 0,
            briefs_needing_verification: 0,
            briefs_with_policy_surface: 0,
            briefs_incident_ready: 0,
            next_severity: None,
            next_action: None,
            next_observability_status: None,
            next_rollback_action: None,
            next_owner_lane: None,
            next_recommended_view: None,
            next_brief_sequence: None,
            next_brief_priority: None,
            first_attention_priority: None,
            highest_policy_tier: PrivilegeTier::ReadOnly,
            overall_status: IntegrationActivationHealthStatus::Empty,
        };

        for brief in briefs {
            summary.total_briefs += 1;
            for integration_id in &brief.integration_ids {
                integration_ids.insert(integration_id.clone());
            }

            if summary.next_severity.is_none()
                && (brief.requires_attention() || brief.incident_ready())
            {
                summary.next_severity = Some(brief.severity);
                summary.next_action = Some(brief.action);
                summary.next_observability_status = Some(brief.observability_status);
                summary.next_rollback_action = Some(brief.rollback_action);
                summary.next_owner_lane = Some(brief.owner_lane);
                summary.next_recommended_view = Some(brief.recommended_view);
                summary.next_brief_sequence = Some(brief.sequence);
                summary.next_brief_priority = Some(brief.priority);
            }

            match brief.severity {
                IntegrationActivationIncidentSeverity::Critical => {
                    summary.critical_briefs += 1;
                }
                IntegrationActivationIncidentSeverity::High => {
                    summary.high_briefs += 1;
                }
                IntegrationActivationIncidentSeverity::Medium => {
                    summary.medium_briefs += 1;
                }
                IntegrationActivationIncidentSeverity::Watch => {
                    summary.watch_briefs += 1;
                }
            }

            match brief.action {
                IntegrationActivationIncidentAction::HoldActivation => {
                    summary.hold_activation_briefs += 1;
                }
                IntegrationActivationIncidentAction::TriageTelemetry => {
                    summary.telemetry_triage_briefs += 1;
                }
                IntegrationActivationIncidentAction::CompleteRollbackCoverage => {
                    summary.rollback_coverage_briefs += 1;
                }
                IntegrationActivationIncidentAction::ArmObservation => {
                    summary.arm_observation_briefs += 1;
                }
                IntegrationActivationIncidentAction::ContinueMonitoring => {
                    summary.monitoring_briefs += 1;
                }
            }

            if brief.requires_attention() {
                summary.briefs_requiring_attention += 1;
                summary.first_attention_priority =
                    min_optional_priority(summary.first_attention_priority, Some(brief.priority));
            }
            if brief.has_watchtower_signals() {
                summary.briefs_with_watchtower_signals += 1;
            }
            if brief.has_observation_signal {
                summary.briefs_with_observation_signals += 1;
            }
            if brief.watchtower_requires_attention {
                summary.briefs_with_watchtower_attention += 1;
            }
            if brief.rollback_ready() {
                summary.briefs_ready_for_rollback += 1;
            }
            if brief.deployment_ready() {
                summary.briefs_ready_for_deployment += 1;
            }
            if brief.blocks_activation() {
                summary.briefs_blocking_activation += 1;
            }
            if brief.needs_verification() {
                summary.briefs_needing_verification += 1;
            }
            if brief.has_policy_surface() {
                summary.briefs_with_policy_surface += 1;
            }
            if brief.incident_ready() {
                summary.briefs_incident_ready += 1;
            }
            summary.highest_policy_tier = summary.highest_policy_tier.max(brief.required_tier);
        }

        summary.unique_integrations = integration_ids.len();
        summary.overall_status =
            if summary.critical_briefs > 0 || summary.briefs_blocking_activation > 0 {
                IntegrationActivationHealthStatus::Blocked
            } else if summary.briefs_requiring_attention > 0 || summary.high_briefs > 0 {
                IntegrationActivationHealthStatus::NeedsReview
            } else if summary.medium_briefs > 0
                || summary.watch_briefs > 0
                || summary.briefs_incident_ready > 0
            {
                IntegrationActivationHealthStatus::Ready
            } else {
                IntegrationActivationHealthStatus::Empty
            };
        summary
    }

    pub fn is_empty(&self) -> bool {
        self.total_briefs == 0
    }

    pub fn has_blockers(&self) -> bool {
        self.critical_briefs > 0
            || self.hold_activation_briefs > 0
            || self.briefs_blocking_activation > 0
    }

    pub fn has_telemetry_triage(&self) -> bool {
        self.telemetry_triage_briefs > 0 || self.briefs_with_watchtower_attention > 0
    }

    pub fn needs_rollback_coverage(&self) -> bool {
        self.rollback_coverage_briefs > 0
    }

    pub fn incident_ready(&self) -> bool {
        self.briefs_incident_ready > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.briefs_requiring_attention > 0 || self.overall_status.requires_attention()
    }
}

impl IntegrationActivationGuardrailCheck {
    fn from_incident(brief: &IntegrationActivationIncidentBrief) -> Self {
        let verdict = if brief.blocks_activation() {
            IntegrationActivationGuardrailVerdict::Blocked
        } else if brief.requires_attention() {
            IntegrationActivationGuardrailVerdict::NeedsReview
        } else if brief.incident_ready() {
            IntegrationActivationGuardrailVerdict::Monitor
        } else {
            IntegrationActivationGuardrailVerdict::Pass
        };

        Self {
            sequence: 0,
            guardrail_id: format!("incident:{}", brief.source_id.as_str()),
            kind: IntegrationActivationGuardrailKind::Incident,
            verdict,
            title: brief.title.clone(),
            summary: brief.summary.clone(),
            priority: brief.priority,
            integration_ids: brief.integration_ids.clone(),
            required_tier: brief.required_tier,
            policy_surface: brief.policy_surface,
            incident_severity: Some(brief.severity),
            incident_action: Some(brief.action),
            risk_kind: None,
            dependency_integration_id: None,
            dependent_integration_id: None,
            readiness_gap_kind: None,
            blocks_activation: verdict.blocks_activation() || brief.blocks_activation(),
            requires_attention: verdict.requires_attention() || brief.requires_attention(),
        }
    }

    fn from_risk(risk: &IntegrationActivationRiskItem) -> Self {
        let verdict = if risk.has_blockers() {
            IntegrationActivationGuardrailVerdict::Blocked
        } else if risk.requires_attention()
            || matches!(
                risk.required_tier,
                PrivilegeTier::HumanApproval | PrivilegeTier::HighRisk
            )
        {
            IntegrationActivationGuardrailVerdict::NeedsReview
        } else if risk.has_ready_work() {
            IntegrationActivationGuardrailVerdict::Monitor
        } else {
            IntegrationActivationGuardrailVerdict::Pass
        };

        Self {
            sequence: 0,
            guardrail_id: format!("risk:{}", risk.risk_id.as_str()),
            kind: IntegrationActivationGuardrailKind::PolicyRisk,
            verdict,
            title: format!("Policy risk: {}", risk.display_name),
            summary: format!(
                "{} integration(s) share this activation policy risk",
                risk.integration_count()
            ),
            priority: risk.highest_priority,
            integration_ids: risk.integration_ids.clone(),
            required_tier: risk.required_tier,
            policy_surface: risk.policy_surface,
            incident_severity: None,
            incident_action: None,
            risk_kind: Some(risk.kind),
            dependency_integration_id: None,
            dependent_integration_id: None,
            readiness_gap_kind: None,
            blocks_activation: verdict.blocks_activation(),
            requires_attention: verdict.requires_attention() || risk.requires_attention(),
        }
    }

    fn from_dependency_edge(edge: &IntegrationActivationDependencyEdge) -> Self {
        let verdict = if edge.blocks_activation {
            IntegrationActivationGuardrailVerdict::Blocked
        } else {
            IntegrationActivationGuardrailVerdict::Pass
        };
        let dependency_name = edge
            .dependency_display_name
            .as_deref()
            .unwrap_or(edge.dependency_integration_id.as_str());
        let integration_ids = if edge.dependency_integration_id == edge.dependent_integration_id {
            vec![edge.dependent_integration_id.clone()]
        } else {
            vec![
                edge.dependency_integration_id.clone(),
                edge.dependent_integration_id.clone(),
            ]
        };

        Self {
            sequence: 0,
            guardrail_id: format!(
                "dependency:{}->{}",
                edge.dependency_integration_id.as_str(),
                edge.dependent_integration_id.as_str()
            ),
            kind: IntegrationActivationGuardrailKind::Dependency,
            verdict,
            title: format!(
                "Dependency guardrail: {} requires {}",
                edge.dependent_display_name, dependency_name
            ),
            summary: if edge.satisfied {
                "Required integration dependency is enabled".to_string()
            } else {
                "Required integration dependency is not enabled".to_string()
            },
            priority: edge.dependent_priority,
            integration_ids,
            required_tier: PrivilegeTier::ReadOnly,
            policy_surface: None,
            incident_severity: None,
            incident_action: None,
            risk_kind: None,
            dependency_integration_id: Some(edge.dependency_integration_id.clone()),
            dependent_integration_id: Some(edge.dependent_integration_id.clone()),
            readiness_gap_kind: None,
            blocks_activation: edge.blocks_activation,
            requires_attention: edge.blocks_activation,
        }
    }

    fn from_primitive_gap(gap: &IntegrationReadinessPrimitiveGap) -> Self {
        Self {
            sequence: 0,
            guardrail_id: format!("readiness_gap:primitive:{}", gap.primitive.as_str()),
            kind: IntegrationActivationGuardrailKind::ReadinessGap,
            verdict: IntegrationActivationGuardrailVerdict::Blocked,
            title: format!("Readiness gap: missing {}", gap.primitive.as_str()),
            summary: format!(
                "{} integration report(s) are blocked by this primitive gap",
                gap.blocked_report_count
            ),
            priority: gap.highest_priority,
            integration_ids: gap.integration_ids.clone(),
            required_tier: PrivilegeTier::ReadOnly,
            policy_surface: None,
            incident_severity: None,
            incident_action: None,
            risk_kind: None,
            dependency_integration_id: None,
            dependent_integration_id: None,
            readiness_gap_kind: Some("primitive".to_string()),
            blocks_activation: true,
            requires_attention: true,
        }
    }

    fn from_capability_gap(gap: &IntegrationReadinessCapabilityGap) -> Self {
        Self {
            sequence: 0,
            guardrail_id: format!("readiness_gap:capability:{}", gap.capability_id.as_str()),
            kind: IntegrationActivationGuardrailKind::ReadinessGap,
            verdict: IntegrationActivationGuardrailVerdict::Blocked,
            title: format!("Readiness gap: missing {}", gap.capability_id.as_str()),
            summary: format!(
                "{} integration report(s) are blocked by this capability gap",
                gap.blocked_report_count
            ),
            priority: gap.highest_priority,
            integration_ids: gap.integration_ids.clone(),
            required_tier: PrivilegeTier::ReadOnly,
            policy_surface: None,
            incident_severity: None,
            incident_action: None,
            risk_kind: None,
            dependency_integration_id: None,
            dependent_integration_id: None,
            readiness_gap_kind: Some("capability".to_string()),
            blocks_activation: true,
            requires_attention: true,
        }
    }

    fn from_dependency_gap(gap: &IntegrationReadinessDependencyGap) -> Self {
        let mut integration_ids = gap.requested_integration_ids.clone();
        if !integration_ids
            .iter()
            .any(|integration_id| integration_id == &gap.integration_id)
        {
            integration_ids.push(gap.integration_id.clone());
        }

        Self {
            sequence: 0,
            guardrail_id: format!("readiness_gap:dependency:{}", gap.integration_id.as_str()),
            kind: IntegrationActivationGuardrailKind::ReadinessGap,
            verdict: IntegrationActivationGuardrailVerdict::Blocked,
            title: format!("Readiness gap: missing {}", gap.integration_id.as_str()),
            summary: format!(
                "{} integration report(s) are blocked by this dependency gap",
                gap.blocked_report_count
            ),
            priority: gap.highest_priority,
            integration_ids,
            required_tier: PrivilegeTier::ReadOnly,
            policy_surface: None,
            incident_severity: None,
            incident_action: None,
            risk_kind: None,
            dependency_integration_id: Some(gap.integration_id.clone()),
            dependent_integration_id: None,
            readiness_gap_kind: Some("dependency".to_string()),
            blocks_activation: true,
            requires_attention: true,
        }
    }

    pub fn integration_count(&self) -> usize {
        self.integration_ids.len()
    }

    pub fn blocks_activation(&self) -> bool {
        self.blocks_activation || self.verdict.blocks_activation()
    }

    pub fn requires_attention(&self) -> bool {
        self.requires_attention || self.verdict.requires_attention()
    }
}

impl IntegrationActivationGuardrailSummary {
    pub fn from_checks<'a>(
        checks: impl IntoIterator<Item = &'a IntegrationActivationGuardrailCheck>,
    ) -> Self {
        let mut integration_ids = BTreeSet::new();
        let mut summary = Self {
            total_checks: 0,
            unique_integrations: 0,
            checks_requiring_attention: 0,
            blocked_checks: 0,
            needs_review_checks: 0,
            monitor_checks: 0,
            pass_checks: 0,
            incident_checks: 0,
            policy_risk_checks: 0,
            dependency_checks: 0,
            readiness_gap_checks: 0,
            checks_with_policy_surface: 0,
            first_attention_priority: None,
            first_blocked_priority: None,
            first_review_priority: None,
            highest_policy_tier: PrivilegeTier::ReadOnly,
            overall_status: IntegrationActivationHealthStatus::Empty,
        };

        for check in checks {
            summary.total_checks += 1;
            for integration_id in &check.integration_ids {
                integration_ids.insert(integration_id.clone());
            }
            match check.kind {
                IntegrationActivationGuardrailKind::Incident => summary.incident_checks += 1,
                IntegrationActivationGuardrailKind::PolicyRisk => summary.policy_risk_checks += 1,
                IntegrationActivationGuardrailKind::Dependency => summary.dependency_checks += 1,
                IntegrationActivationGuardrailKind::ReadinessGap => {
                    summary.readiness_gap_checks += 1;
                }
            }
            match check.verdict {
                IntegrationActivationGuardrailVerdict::Pass => summary.pass_checks += 1,
                IntegrationActivationGuardrailVerdict::Monitor => summary.monitor_checks += 1,
                IntegrationActivationGuardrailVerdict::NeedsReview => {
                    summary.needs_review_checks += 1;
                    summary.first_review_priority =
                        min_optional_priority(summary.first_review_priority, Some(check.priority));
                }
                IntegrationActivationGuardrailVerdict::Blocked => {
                    summary.blocked_checks += 1;
                    summary.first_blocked_priority =
                        min_optional_priority(summary.first_blocked_priority, Some(check.priority));
                }
            }
            if check.requires_attention() {
                summary.checks_requiring_attention += 1;
                summary.first_attention_priority =
                    min_optional_priority(summary.first_attention_priority, Some(check.priority));
            }
            if check.policy_surface.is_some() {
                summary.checks_with_policy_surface += 1;
            }
            summary.highest_policy_tier = summary.highest_policy_tier.max(check.required_tier);
        }

        summary.unique_integrations = integration_ids.len();
        summary.overall_status = if summary.blocked_checks > 0 {
            IntegrationActivationHealthStatus::Blocked
        } else if summary.needs_review_checks > 0 || summary.checks_requiring_attention > 0 {
            IntegrationActivationHealthStatus::NeedsReview
        } else if summary.monitor_checks > 0 || summary.pass_checks > 0 {
            IntegrationActivationHealthStatus::Ready
        } else {
            IntegrationActivationHealthStatus::Empty
        };
        summary
    }

    pub fn is_empty(&self) -> bool {
        self.total_checks == 0
    }

    pub fn has_blockers(&self) -> bool {
        self.blocked_checks > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.checks_requiring_attention > 0 || self.overall_status.requires_attention()
    }
}

impl IntegrationActivationConstraintKind {
    pub fn as_str(self) -> &'static str {
        match self {
            Self::Primitive => "primitive",
            Self::Capability => "capability",
            Self::Dependency => "dependency",
            Self::PolicyReview => "policy_review",
        }
    }
}

impl IntegrationActivationConstraint {
    pub fn affected_integration_count(&self) -> usize {
        self.affected_integration_ids.len()
    }
}

impl IntegrationActivationRiskKind {
    pub fn as_str(self) -> &'static str {
        match self {
            Self::PolicyTier => "policy_tier",
            Self::PolicySurface => "policy_surface",
        }
    }
}

impl IntegrationActivationRiskItem {
    fn from_candidates(
        kind: IntegrationActivationRiskKind,
        risk_id: String,
        display_name: String,
        required_tier: PrivilegeTier,
        policy_surface: Option<IntegrationPolicySurface>,
        candidates: &[&IntegrationActivationCandidate],
    ) -> Self {
        let mut candidates = candidates.to_vec();
        candidates.sort_by(|left, right| compare_activation_candidates(left, right));

        let highest_priority = candidates
            .iter()
            .map(|candidate| candidate.readiness_report.priority)
            .min()
            .unwrap_or(u8::MAX);
        let integration_ids = candidates
            .iter()
            .map(|candidate| candidate.readiness_report.requested_integration_id.clone())
            .collect::<Vec<_>>();
        let activation_ready_integration_ids = candidates
            .iter()
            .filter(|candidate| candidate.activation_ready())
            .map(|candidate| candidate.readiness_report.requested_integration_id.clone())
            .collect::<Vec<_>>();
        let ready_to_activate_integration_ids = candidates
            .iter()
            .filter(|candidate| {
                candidate.recommendation
                    == IntegrationActivationCandidateRecommendation::ReadyToActivate
            })
            .map(|candidate| candidate.readiness_report.requested_integration_id.clone())
            .collect::<Vec<_>>();
        let review_integration_ids = candidates
            .iter()
            .filter(|candidate| {
                candidate.recommendation
                    == IntegrationActivationCandidateRecommendation::NeedsHumanReview
            })
            .map(|candidate| candidate.readiness_report.requested_integration_id.clone())
            .collect::<Vec<_>>();
        let blocked_integration_ids = candidates
            .iter()
            .filter(|candidate| candidate.is_blocked())
            .map(|candidate| candidate.readiness_report.requested_integration_id.clone())
            .collect::<Vec<_>>();
        let local_only_integration_ids = candidates
            .iter()
            .filter(|candidate| candidate.readiness_report.local_only)
            .map(|candidate| candidate.readiness_report.requested_integration_id.clone())
            .collect::<Vec<_>>();
        let cloud_required_integration_ids = candidates
            .iter()
            .filter(|candidate| candidate.readiness_report.cloud_required)
            .map(|candidate| candidate.readiness_report.requested_integration_id.clone())
            .collect::<Vec<_>>();
        let candidate_summary =
            IntegrationActivationCandidateSummary::from_candidates(candidates.iter().copied());

        Self {
            kind,
            risk_id,
            display_name,
            required_tier,
            policy_surface,
            highest_priority,
            integration_ids,
            activation_ready_integration_ids,
            ready_to_activate_integration_ids,
            review_integration_ids,
            blocked_integration_ids,
            local_only_integration_ids,
            cloud_required_integration_ids,
            candidate_summary,
        }
    }

    pub fn integration_count(&self) -> usize {
        self.integration_ids.len()
    }

    pub fn has_ready_work(&self) -> bool {
        !self.ready_to_activate_integration_ids.is_empty()
    }

    pub fn has_review_work(&self) -> bool {
        self.candidate_summary.has_review_work()
    }

    pub fn has_blockers(&self) -> bool {
        self.candidate_summary.has_blockers()
    }

    pub fn requires_attention(&self) -> bool {
        self.has_review_work() || self.has_blockers()
    }
}

impl IntegrationActivationRiskSummary {
    pub fn from_risks<'a>(
        risks: impl IntoIterator<Item = &'a IntegrationActivationRiskItem>,
    ) -> Self {
        let mut integrations = BTreeSet::new();
        let mut activation_ready_integrations = BTreeSet::new();
        let mut ready_to_activate_integrations = BTreeSet::new();
        let mut review_integrations = BTreeSet::new();
        let mut blocked_integrations = BTreeSet::new();
        let mut local_only_integrations = BTreeSet::new();
        let mut cloud_required_integrations = BTreeSet::new();
        let mut summary = Self {
            total_risks: 0,
            policy_tier_risks: 0,
            policy_surface_risks: 0,
            total_risk_entries: 0,
            unique_integrations: 0,
            activation_ready_integrations: 0,
            ready_to_activate_integrations: 0,
            review_integrations: 0,
            blocked_integrations: 0,
            local_only_integrations: 0,
            cloud_required_integrations: 0,
            read_only_risks: 0,
            low_risk_risks: 0,
            human_approval_risks: 0,
            high_risk_risks: 0,
            first_ready_priority: None,
            first_review_priority: None,
            first_blocked_priority: None,
            highest_policy_tier: PrivilegeTier::ReadOnly,
        };

        for risk in risks {
            summary.total_risks += 1;
            summary.total_risk_entries += risk.integration_count();
            match risk.kind {
                IntegrationActivationRiskKind::PolicyTier => summary.policy_tier_risks += 1,
                IntegrationActivationRiskKind::PolicySurface => summary.policy_surface_risks += 1,
            }
            match risk.required_tier {
                PrivilegeTier::ReadOnly => summary.read_only_risks += 1,
                PrivilegeTier::LowRisk => summary.low_risk_risks += 1,
                PrivilegeTier::HumanApproval => summary.human_approval_risks += 1,
                PrivilegeTier::HighRisk => summary.high_risk_risks += 1,
            }
            for integration_id in &risk.integration_ids {
                integrations.insert(integration_id.clone());
            }
            for integration_id in &risk.activation_ready_integration_ids {
                activation_ready_integrations.insert(integration_id.clone());
            }
            for integration_id in &risk.ready_to_activate_integration_ids {
                ready_to_activate_integrations.insert(integration_id.clone());
            }
            for integration_id in &risk.review_integration_ids {
                review_integrations.insert(integration_id.clone());
            }
            for integration_id in &risk.blocked_integration_ids {
                blocked_integrations.insert(integration_id.clone());
            }
            for integration_id in &risk.local_only_integration_ids {
                local_only_integrations.insert(integration_id.clone());
            }
            for integration_id in &risk.cloud_required_integration_ids {
                cloud_required_integrations.insert(integration_id.clone());
            }
            if risk.has_ready_work() {
                summary.first_ready_priority = min_optional_priority(
                    summary.first_ready_priority,
                    Some(risk.highest_priority),
                );
            }
            if risk.has_review_work() {
                summary.first_review_priority = min_optional_priority(
                    summary.first_review_priority,
                    Some(risk.highest_priority),
                );
            }
            if risk.has_blockers() {
                summary.first_blocked_priority = min_optional_priority(
                    summary.first_blocked_priority,
                    Some(risk.highest_priority),
                );
            }
            summary.highest_policy_tier = summary.highest_policy_tier.max(risk.required_tier);
        }

        summary.unique_integrations = integrations.len();
        summary.activation_ready_integrations = activation_ready_integrations.len();
        summary.ready_to_activate_integrations = ready_to_activate_integrations.len();
        summary.review_integrations = review_integrations.len();
        summary.blocked_integrations = blocked_integrations.len();
        summary.local_only_integrations = local_only_integrations.len();
        summary.cloud_required_integrations = cloud_required_integrations.len();
        summary
    }

    pub fn is_empty(&self) -> bool {
        self.total_risks == 0
    }

    pub fn has_ready_work(&self) -> bool {
        self.ready_to_activate_integrations > 0
    }

    pub fn has_review_work(&self) -> bool {
        self.review_integrations > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.blocked_integrations > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.has_review_work() || self.has_blockers()
    }
}

impl IntegrationActivationReviewItem {
    fn from_candidate(
        catalog: &[IntegrationCatalogEntry],
        candidate: &IntegrationActivationCandidate,
    ) -> Option<Self> {
        if !candidate.requires_human_review() {
            return None;
        }

        let report = &candidate.readiness_report;
        let mut policy_surfaces = find_entry(catalog, &report.requested_integration_id)
            .map(IntegrationCatalogEntry::policy_surfaces)
            .unwrap_or_default();
        policy_surfaces.sort();
        policy_surfaces.dedup();
        let required_tier = policy_surfaces
            .iter()
            .fold(report.highest_policy_tier, |tier, surface| {
                tier.max(surface.required_tier())
            });

        Some(Self {
            requested_integration_id: report.requested_integration_id.clone(),
            display_name: report.display_name.clone(),
            priority: report.priority,
            activation_target: report.activation_target.clone(),
            recommendation: candidate.recommendation,
            blocker_count: candidate.blocker_count,
            missing_primitives: report.missing_primitives.clone(),
            missing_capabilities: report.missing_capabilities.clone(),
            missing_dependencies: report.missing_dependencies.clone(),
            policy_surfaces,
            required_tier,
            local_only: report.local_only,
            cloud_required: report.cloud_required,
        })
    }

    pub fn activation_ready(&self) -> bool {
        self.missing_primitives.is_empty()
            && self.missing_capabilities.is_empty()
            && self.missing_dependencies.is_empty()
    }

    pub fn review_ready(&self) -> bool {
        self.activation_ready()
            && self.recommendation == IntegrationActivationCandidateRecommendation::NeedsHumanReview
    }

    pub fn is_blocked(&self) -> bool {
        self.recommendation == IntegrationActivationCandidateRecommendation::BlockedOnPrerequisites
            || !self.activation_ready()
    }

    pub fn has_policy_surfaces(&self) -> bool {
        !self.policy_surfaces.is_empty()
    }

    pub fn has_blockers(&self) -> bool {
        self.is_blocked()
    }

    pub fn requires_attention(&self) -> bool {
        true
    }
}

impl IntegrationActivationReviewSummary {
    pub fn from_reviews<'a>(
        reviews: impl IntoIterator<Item = &'a IntegrationActivationReviewItem>,
    ) -> Self {
        let mut policy_surfaces = BTreeSet::new();
        let mut summary = Self {
            total_reviews: 0,
            review_ready_integrations: 0,
            blocked_review_integrations: 0,
            reviews_missing_primitives: 0,
            reviews_missing_capabilities: 0,
            reviews_missing_dependencies: 0,
            direct_targets: 0,
            delegated_integration_targets: 0,
            delegated_standard_targets: 0,
            local_only_reviews: 0,
            cloud_required_reviews: 0,
            reviews_with_policy_surfaces: 0,
            reviews_without_policy_surfaces: 0,
            unique_policy_surfaces: 0,
            total_blockers: 0,
            read_only_reviews: 0,
            low_risk_reviews: 0,
            human_approval_reviews: 0,
            high_risk_reviews: 0,
            first_review_priority: None,
            first_blocked_priority: None,
            highest_policy_tier: PrivilegeTier::ReadOnly,
        };

        for review in reviews {
            summary.total_reviews += 1;
            if review.review_ready() {
                summary.review_ready_integrations += 1;
                summary.first_review_priority =
                    min_optional_priority(summary.first_review_priority, Some(review.priority));
            }
            if review.is_blocked() {
                summary.blocked_review_integrations += 1;
                summary.first_blocked_priority =
                    min_optional_priority(summary.first_blocked_priority, Some(review.priority));
            }
            if !review.missing_primitives.is_empty() {
                summary.reviews_missing_primitives += 1;
            }
            if !review.missing_capabilities.is_empty() {
                summary.reviews_missing_capabilities += 1;
            }
            if !review.missing_dependencies.is_empty() {
                summary.reviews_missing_dependencies += 1;
            }
            match &review.activation_target {
                IntegrationActivationTarget::Direct => summary.direct_targets += 1,
                IntegrationActivationTarget::DelegatedIntegration(_) => {
                    summary.delegated_integration_targets += 1
                }
                IntegrationActivationTarget::DelegatedStandards(_) => {
                    summary.delegated_standard_targets += 1
                }
            }
            if review.local_only {
                summary.local_only_reviews += 1;
            }
            if review.cloud_required {
                summary.cloud_required_reviews += 1;
            }
            if review.has_policy_surfaces() {
                summary.reviews_with_policy_surfaces += 1;
            } else {
                summary.reviews_without_policy_surfaces += 1;
            }
            for surface in &review.policy_surfaces {
                policy_surfaces.insert(*surface);
            }
            summary.total_blockers += review.blocker_count;
            match review.required_tier {
                PrivilegeTier::ReadOnly => summary.read_only_reviews += 1,
                PrivilegeTier::LowRisk => summary.low_risk_reviews += 1,
                PrivilegeTier::HumanApproval => summary.human_approval_reviews += 1,
                PrivilegeTier::HighRisk => summary.high_risk_reviews += 1,
            }
            summary.highest_policy_tier = summary.highest_policy_tier.max(review.required_tier);
        }

        summary.unique_policy_surfaces = policy_surfaces.len();
        summary
    }

    pub fn is_empty(&self) -> bool {
        self.total_reviews == 0
    }

    pub fn has_review_ready_work(&self) -> bool {
        self.review_ready_integrations > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.blocked_review_integrations > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.total_reviews > 0
    }
}

impl IntegrationActivationApprovalPacket {
    fn from_candidate(
        catalog: &[IntegrationCatalogEntry],
        candidate: &IntegrationActivationCandidate,
        enabled_integrations: &[IntegrationId],
    ) -> Option<Self> {
        let review = IntegrationActivationReviewItem::from_candidate(catalog, candidate)?;
        let actions = activation_actions_from_candidates(std::iter::once(candidate));
        let action_summary = IntegrationActivationActionSummary::from_actions(actions.iter());
        let constraints =
            activation_constraints_from_candidates(catalog, std::iter::once(candidate));
        let constraint_summary =
            IntegrationActivationConstraintSummary::from_constraints(constraints.iter());
        let risks = activation_risk_from_candidates(catalog, std::iter::once(candidate));
        let risk_summary = IntegrationActivationRiskSummary::from_risks(risks.iter());
        let dependency_graph = activation_dependency_graph_from_reports(
            catalog,
            std::iter::once(&candidate.readiness_report),
            enabled_integrations,
        );

        Some(Self {
            review,
            actions,
            action_summary,
            constraints,
            constraint_summary,
            risks,
            risk_summary,
            dependency_graph,
        })
    }

    pub fn requested_integration_id(&self) -> &IntegrationId {
        &self.review.requested_integration_id
    }

    pub fn display_name(&self) -> &str {
        &self.review.display_name
    }

    pub fn priority(&self) -> u8 {
        self.review.priority
    }

    pub fn required_tier(&self) -> PrivilegeTier {
        self.review.required_tier
    }

    pub fn approval_ready(&self) -> bool {
        self.review.review_ready()
    }

    pub fn has_blockers(&self) -> bool {
        self.review.has_blockers()
            || self.constraint_summary.has_blockers()
            || self.dependency_graph.has_blocking_dependencies()
    }

    pub fn has_policy_surfaces(&self) -> bool {
        self.review.has_policy_surfaces()
    }

    pub fn requires_attention(&self) -> bool {
        true
    }
}

impl IntegrationActivationApprovalSummary {
    pub fn from_packets<'a>(
        packets: impl IntoIterator<Item = &'a IntegrationActivationApprovalPacket>,
    ) -> Self {
        let mut policy_surfaces = BTreeSet::new();
        let mut summary = Self {
            total_packets: 0,
            approval_ready_packets: 0,
            blocked_packets: 0,
            local_only_packets: 0,
            cloud_required_packets: 0,
            packets_with_policy_surfaces: 0,
            packets_without_policy_surfaces: 0,
            unique_policy_surfaces: 0,
            total_actions: 0,
            activate_integration_actions: 0,
            review_policy_actions: 0,
            provide_primitive_actions: 0,
            grant_capability_actions: 0,
            enable_dependency_actions: 0,
            total_constraints: 0,
            blocking_constraints: 0,
            review_constraints: 0,
            total_risks: 0,
            policy_tier_risks: 0,
            policy_surface_risks: 0,
            total_dependency_edges: 0,
            blocking_dependency_edges: 0,
            read_only_packets: 0,
            low_risk_packets: 0,
            human_approval_packets: 0,
            high_risk_packets: 0,
            first_approval_priority: None,
            first_blocked_priority: None,
            highest_policy_tier: PrivilegeTier::ReadOnly,
        };

        for packet in packets {
            summary.total_packets += 1;
            if packet.approval_ready() {
                summary.approval_ready_packets += 1;
                summary.first_approval_priority =
                    min_optional_priority(summary.first_approval_priority, Some(packet.priority()));
            }
            if packet.has_blockers() {
                summary.blocked_packets += 1;
                summary.first_blocked_priority =
                    min_optional_priority(summary.first_blocked_priority, Some(packet.priority()));
            }
            if packet.review.local_only {
                summary.local_only_packets += 1;
            }
            if packet.review.cloud_required {
                summary.cloud_required_packets += 1;
            }
            if packet.has_policy_surfaces() {
                summary.packets_with_policy_surfaces += 1;
            } else {
                summary.packets_without_policy_surfaces += 1;
            }
            for surface in &packet.review.policy_surfaces {
                policy_surfaces.insert(*surface);
            }

            summary.total_actions += packet.action_summary.total_actions;
            summary.activate_integration_actions +=
                packet.action_summary.activate_integration_actions;
            summary.review_policy_actions += packet.action_summary.review_policy_actions;
            summary.provide_primitive_actions += packet.action_summary.provide_primitive_actions;
            summary.grant_capability_actions += packet.action_summary.grant_capability_actions;
            summary.enable_dependency_actions += packet.action_summary.enable_dependency_actions;

            summary.total_constraints += packet.constraint_summary.total_constraints;
            summary.blocking_constraints += packet.constraint_summary.blocking_constraints;
            summary.review_constraints += packet.constraint_summary.review_constraints;

            summary.total_risks += packet.risk_summary.total_risks;
            summary.policy_tier_risks += packet.risk_summary.policy_tier_risks;
            summary.policy_surface_risks += packet.risk_summary.policy_surface_risks;

            summary.total_dependency_edges += packet.dependency_graph.summary.total_edges;
            summary.blocking_dependency_edges += packet.dependency_graph.summary.blocking_edges;

            match packet.required_tier() {
                PrivilegeTier::ReadOnly => summary.read_only_packets += 1,
                PrivilegeTier::LowRisk => summary.low_risk_packets += 1,
                PrivilegeTier::HumanApproval => summary.human_approval_packets += 1,
                PrivilegeTier::HighRisk => summary.high_risk_packets += 1,
            }
            summary.highest_policy_tier = summary.highest_policy_tier.max(packet.required_tier());
        }

        summary.unique_policy_surfaces = policy_surfaces.len();
        summary
    }

    pub fn is_empty(&self) -> bool {
        self.total_packets == 0
    }

    pub fn has_approval_ready_work(&self) -> bool {
        self.approval_ready_packets > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.blocked_packets > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.total_packets > 0
    }
}

impl IntegrationActivationDecisionItem {
    fn from_packet(packet: IntegrationActivationApprovalPacket) -> Self {
        let decision_status = if packet.approval_ready() && !packet.has_blockers() {
            IntegrationActivationDecisionStatus::ReadyToApprove
        } else {
            IntegrationActivationDecisionStatus::BlockedOnPrerequisites
        };

        Self {
            packet,
            decision_status,
        }
    }

    pub fn requested_integration_id(&self) -> &IntegrationId {
        self.packet.requested_integration_id()
    }

    pub fn display_name(&self) -> &str {
        self.packet.display_name()
    }

    pub fn priority(&self) -> u8 {
        self.packet.priority()
    }

    pub fn required_tier(&self) -> PrivilegeTier {
        self.packet.required_tier()
    }

    pub fn approval_ready(&self) -> bool {
        self.packet.approval_ready()
    }

    pub fn has_blockers(&self) -> bool {
        self.packet.has_blockers()
    }

    pub fn has_policy_surfaces(&self) -> bool {
        self.packet.has_policy_surfaces()
    }

    pub fn requires_attention(&self) -> bool {
        self.decision_status.requires_attention()
    }
}

impl IntegrationActivationDecisionSummary {
    pub fn from_decisions<'a>(
        decisions: impl IntoIterator<Item = &'a IntegrationActivationDecisionItem>,
    ) -> Self {
        let mut policy_surfaces = BTreeSet::new();
        let mut summary = Self {
            total_decisions: 0,
            ready_to_approve_decisions: 0,
            blocked_decisions: 0,
            local_only_decisions: 0,
            cloud_required_decisions: 0,
            decisions_with_policy_surfaces: 0,
            decisions_without_policy_surfaces: 0,
            unique_policy_surfaces: 0,
            total_actions: 0,
            activate_integration_actions: 0,
            review_policy_actions: 0,
            provide_primitive_actions: 0,
            grant_capability_actions: 0,
            enable_dependency_actions: 0,
            total_constraints: 0,
            blocking_constraints: 0,
            review_constraints: 0,
            total_risks: 0,
            policy_tier_risks: 0,
            policy_surface_risks: 0,
            total_dependency_edges: 0,
            blocking_dependency_edges: 0,
            read_only_decisions: 0,
            low_risk_decisions: 0,
            human_approval_decisions: 0,
            high_risk_decisions: 0,
            first_approval_priority: None,
            first_blocked_priority: None,
            highest_policy_tier: PrivilegeTier::ReadOnly,
        };

        for decision in decisions {
            summary.total_decisions += 1;
            match decision.decision_status {
                IntegrationActivationDecisionStatus::ReadyToApprove => {
                    summary.ready_to_approve_decisions += 1;
                    summary.first_approval_priority = min_optional_priority(
                        summary.first_approval_priority,
                        Some(decision.priority()),
                    );
                }
                IntegrationActivationDecisionStatus::BlockedOnPrerequisites => {
                    summary.blocked_decisions += 1;
                    summary.first_blocked_priority = min_optional_priority(
                        summary.first_blocked_priority,
                        Some(decision.priority()),
                    );
                }
            }

            if decision.packet.review.local_only {
                summary.local_only_decisions += 1;
            }
            if decision.packet.review.cloud_required {
                summary.cloud_required_decisions += 1;
            }
            if decision.has_policy_surfaces() {
                summary.decisions_with_policy_surfaces += 1;
            } else {
                summary.decisions_without_policy_surfaces += 1;
            }
            for surface in &decision.packet.review.policy_surfaces {
                policy_surfaces.insert(*surface);
            }

            summary.total_actions += decision.packet.action_summary.total_actions;
            summary.activate_integration_actions +=
                decision.packet.action_summary.activate_integration_actions;
            summary.review_policy_actions += decision.packet.action_summary.review_policy_actions;
            summary.provide_primitive_actions +=
                decision.packet.action_summary.provide_primitive_actions;
            summary.grant_capability_actions +=
                decision.packet.action_summary.grant_capability_actions;
            summary.enable_dependency_actions +=
                decision.packet.action_summary.enable_dependency_actions;

            summary.total_constraints += decision.packet.constraint_summary.total_constraints;
            summary.blocking_constraints += decision.packet.constraint_summary.blocking_constraints;
            summary.review_constraints += decision.packet.constraint_summary.review_constraints;

            summary.total_risks += decision.packet.risk_summary.total_risks;
            summary.policy_tier_risks += decision.packet.risk_summary.policy_tier_risks;
            summary.policy_surface_risks += decision.packet.risk_summary.policy_surface_risks;

            summary.total_dependency_edges += decision.packet.dependency_graph.summary.total_edges;
            summary.blocking_dependency_edges +=
                decision.packet.dependency_graph.summary.blocking_edges;

            match decision.required_tier() {
                PrivilegeTier::ReadOnly => summary.read_only_decisions += 1,
                PrivilegeTier::LowRisk => summary.low_risk_decisions += 1,
                PrivilegeTier::HumanApproval => summary.human_approval_decisions += 1,
                PrivilegeTier::HighRisk => summary.high_risk_decisions += 1,
            }
            summary.highest_policy_tier = summary.highest_policy_tier.max(decision.required_tier());
        }

        summary.unique_policy_surfaces = policy_surfaces.len();
        summary
    }

    pub fn is_empty(&self) -> bool {
        self.total_decisions == 0
    }

    pub fn has_approval_ready_work(&self) -> bool {
        self.ready_to_approve_decisions > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.blocked_decisions > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.total_decisions > 0
    }
}

impl IntegrationActivationEvidenceItem {
    fn from_decision(
        decision: &IntegrationActivationDecisionItem,
    ) -> Vec<IntegrationActivationEvidenceItem> {
        let packet = &decision.packet;
        let mut evidence = Vec::new();
        let approval_status =
            if decision.decision_status == IntegrationActivationDecisionStatus::ReadyToApprove {
                IntegrationActivationEvidenceStatus::SupportsApproval
            } else {
                IntegrationActivationEvidenceStatus::BlocksApproval
            };

        evidence.push(Self::for_decision(
            decision,
            IntegrationActivationEvidenceKind::ApprovalDecision,
            approval_status,
            decision.decision_status.as_str().to_string(),
        ));

        if packet.action_summary.review_policy_actions > 0 || packet.has_policy_surfaces() {
            if packet.review.policy_surfaces.is_empty() {
                evidence.push(Self::for_decision(
                    decision,
                    IntegrationActivationEvidenceKind::PolicyReview,
                    IntegrationActivationEvidenceStatus::RequiresReview,
                    "policy_review".to_string(),
                ));
            } else {
                for surface in &packet.review.policy_surfaces {
                    let mut row = Self::for_decision(
                        decision,
                        IntegrationActivationEvidenceKind::PolicyReview,
                        IntegrationActivationEvidenceStatus::RequiresReview,
                        surface.as_str().to_string(),
                    );
                    row.policy_surface = Some(*surface);
                    row.required_tier = row.required_tier.max(surface.required_tier());
                    evidence.push(row);
                }
            }
        }

        for primitive in &packet.review.missing_primitives {
            let mut row = Self::for_decision(
                decision,
                IntegrationActivationEvidenceKind::PrimitiveBlocker,
                IntegrationActivationEvidenceStatus::BlocksApproval,
                primitive.as_str().to_string(),
            );
            row.primitive = Some(*primitive);
            evidence.push(row);
        }

        for capability_id in &packet.review.missing_capabilities {
            let mut row = Self::for_decision(
                decision,
                IntegrationActivationEvidenceKind::CapabilityBlocker,
                IntegrationActivationEvidenceStatus::BlocksApproval,
                capability_id.as_str().to_string(),
            );
            row.capability_id = Some(capability_id.clone());
            evidence.push(row);
        }

        for dependency_id in &packet.review.missing_dependencies {
            let mut row = Self::for_decision(
                decision,
                IntegrationActivationEvidenceKind::DependencyBlocker,
                IntegrationActivationEvidenceStatus::BlocksApproval,
                dependency_id.as_str().to_string(),
            );
            row.dependency_integration_id = Some(dependency_id.clone());
            evidence.push(row);
        }

        for risk in &packet.risks {
            let mut row = Self::for_decision(
                decision,
                IntegrationActivationEvidenceKind::PolicyRisk,
                IntegrationActivationEvidenceStatus::RequiresReview,
                risk.risk_id.clone(),
            );
            row.policy_surface = risk.policy_surface;
            row.required_tier = row.required_tier.max(risk.required_tier);
            evidence.push(row);
        }

        for edge in &packet.dependency_graph.edges {
            let mut row = Self::for_decision(
                decision,
                IntegrationActivationEvidenceKind::DependencyEdge,
                if edge.blocks_activation {
                    IntegrationActivationEvidenceStatus::BlocksApproval
                } else {
                    IntegrationActivationEvidenceStatus::SupportsApproval
                },
                format!(
                    "{}->{}",
                    edge.dependency_integration_id.as_str(),
                    edge.dependent_integration_id.as_str()
                ),
            );
            row.dependency_integration_id = Some(edge.dependency_integration_id.clone());
            evidence.push(row);
        }

        evidence
    }

    fn for_decision(
        decision: &IntegrationActivationDecisionItem,
        kind: IntegrationActivationEvidenceKind,
        status: IntegrationActivationEvidenceStatus,
        detail_id: String,
    ) -> Self {
        Self {
            kind,
            status,
            decision_status: decision.decision_status,
            requested_integration_id: decision.requested_integration_id().clone(),
            display_name: decision.display_name().to_string(),
            priority: decision.priority(),
            detail_id,
            primitive: None,
            capability_id: None,
            dependency_integration_id: None,
            policy_surface: None,
            required_tier: decision.required_tier(),
            local_only: decision.packet.review.local_only,
            cloud_required: decision.packet.review.cloud_required,
        }
    }

    pub fn blocks_approval(&self) -> bool {
        self.status == IntegrationActivationEvidenceStatus::BlocksApproval
    }

    pub fn requires_attention(&self) -> bool {
        self.status.requires_attention()
    }
}

impl IntegrationActivationEvidenceSummary {
    pub fn from_evidence<'a>(
        evidence: impl IntoIterator<Item = &'a IntegrationActivationEvidenceItem>,
    ) -> Self {
        let mut integration_ids = BTreeSet::new();
        let mut ready_integration_ids = BTreeSet::new();
        let mut blocked_integration_ids = BTreeSet::new();
        let mut local_only_ids = BTreeSet::new();
        let mut cloud_required_ids = BTreeSet::new();
        let mut policy_surfaces = BTreeSet::new();
        let mut summary = Self {
            total_evidence: 0,
            approval_decision_evidence: 0,
            policy_review_evidence: 0,
            primitive_blocker_evidence: 0,
            capability_blocker_evidence: 0,
            dependency_blocker_evidence: 0,
            policy_risk_evidence: 0,
            dependency_edge_evidence: 0,
            supporting_evidence: 0,
            review_evidence: 0,
            blocking_evidence: 0,
            unique_integrations: 0,
            ready_to_approve_integrations: 0,
            blocked_integrations: 0,
            local_only_integrations: 0,
            cloud_required_integrations: 0,
            unique_policy_surfaces: 0,
            read_only_evidence: 0,
            low_risk_evidence: 0,
            human_approval_evidence: 0,
            high_risk_evidence: 0,
            first_supporting_priority: None,
            first_review_priority: None,
            first_blocking_priority: None,
            highest_policy_tier: PrivilegeTier::ReadOnly,
        };

        for item in evidence {
            summary.total_evidence += 1;
            integration_ids.insert(item.requested_integration_id.clone());
            match item.decision_status {
                IntegrationActivationDecisionStatus::ReadyToApprove => {
                    ready_integration_ids.insert(item.requested_integration_id.clone());
                }
                IntegrationActivationDecisionStatus::BlockedOnPrerequisites => {
                    blocked_integration_ids.insert(item.requested_integration_id.clone());
                }
            }
            if item.local_only {
                local_only_ids.insert(item.requested_integration_id.clone());
            }
            if item.cloud_required {
                cloud_required_ids.insert(item.requested_integration_id.clone());
            }
            if let Some(surface) = item.policy_surface {
                policy_surfaces.insert(surface);
            }

            match item.kind {
                IntegrationActivationEvidenceKind::ApprovalDecision => {
                    summary.approval_decision_evidence += 1;
                }
                IntegrationActivationEvidenceKind::PolicyReview => {
                    summary.policy_review_evidence += 1;
                }
                IntegrationActivationEvidenceKind::PrimitiveBlocker => {
                    summary.primitive_blocker_evidence += 1;
                }
                IntegrationActivationEvidenceKind::CapabilityBlocker => {
                    summary.capability_blocker_evidence += 1;
                }
                IntegrationActivationEvidenceKind::DependencyBlocker => {
                    summary.dependency_blocker_evidence += 1;
                }
                IntegrationActivationEvidenceKind::PolicyRisk => {
                    summary.policy_risk_evidence += 1;
                }
                IntegrationActivationEvidenceKind::DependencyEdge => {
                    summary.dependency_edge_evidence += 1;
                }
            }

            match item.status {
                IntegrationActivationEvidenceStatus::SupportsApproval => {
                    summary.supporting_evidence += 1;
                    summary.first_supporting_priority = min_optional_priority(
                        summary.first_supporting_priority,
                        Some(item.priority),
                    );
                }
                IntegrationActivationEvidenceStatus::RequiresReview => {
                    summary.review_evidence += 1;
                    summary.first_review_priority =
                        min_optional_priority(summary.first_review_priority, Some(item.priority));
                }
                IntegrationActivationEvidenceStatus::BlocksApproval => {
                    summary.blocking_evidence += 1;
                    summary.first_blocking_priority =
                        min_optional_priority(summary.first_blocking_priority, Some(item.priority));
                }
            }

            match item.required_tier {
                PrivilegeTier::ReadOnly => summary.read_only_evidence += 1,
                PrivilegeTier::LowRisk => summary.low_risk_evidence += 1,
                PrivilegeTier::HumanApproval => summary.human_approval_evidence += 1,
                PrivilegeTier::HighRisk => summary.high_risk_evidence += 1,
            }
            summary.highest_policy_tier = summary.highest_policy_tier.max(item.required_tier);
        }

        summary.unique_integrations = integration_ids.len();
        summary.ready_to_approve_integrations = ready_integration_ids.len();
        summary.blocked_integrations = blocked_integration_ids.len();
        summary.local_only_integrations = local_only_ids.len();
        summary.cloud_required_integrations = cloud_required_ids.len();
        summary.unique_policy_surfaces = policy_surfaces.len();
        summary
    }

    pub fn is_empty(&self) -> bool {
        self.total_evidence == 0
    }

    pub fn has_supporting_evidence(&self) -> bool {
        self.supporting_evidence > 0
    }

    pub fn has_review_work(&self) -> bool {
        self.review_evidence > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.blocking_evidence > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.has_review_work() || self.has_blockers()
    }
}

impl IntegrationActivationDossierItem {
    fn from_decision(decision: IntegrationActivationDecisionItem) -> Self {
        let mut evidence = IntegrationActivationEvidenceItem::from_decision(&decision);
        evidence.sort_by(compare_activation_evidence);
        let evidence_summary = IntegrationActivationEvidenceSummary::from_evidence(evidence.iter());

        Self {
            decision,
            evidence,
            evidence_summary,
        }
    }

    pub fn requested_integration_id(&self) -> &IntegrationId {
        self.decision.requested_integration_id()
    }

    pub fn display_name(&self) -> &str {
        self.decision.display_name()
    }

    pub fn priority(&self) -> u8 {
        self.decision.priority()
    }

    pub fn required_tier(&self) -> PrivilegeTier {
        self.decision.required_tier()
    }

    pub fn approval_ready(&self) -> bool {
        self.decision.approval_ready()
    }

    pub fn has_blockers(&self) -> bool {
        self.decision.has_blockers() || self.evidence_summary.has_blockers()
    }

    pub fn has_review_work(&self) -> bool {
        self.evidence_summary.has_review_work()
    }

    pub fn has_policy_surfaces(&self) -> bool {
        self.decision.has_policy_surfaces()
    }

    pub fn requires_attention(&self) -> bool {
        self.has_review_work() || self.has_blockers()
    }
}

impl IntegrationActivationDossierSummary {
    pub fn from_dossiers<'a>(
        dossiers: impl IntoIterator<Item = &'a IntegrationActivationDossierItem>,
    ) -> Self {
        let mut policy_surfaces = BTreeSet::new();
        let mut summary = Self {
            total_dossiers: 0,
            ready_to_approve_dossiers: 0,
            blocked_dossiers: 0,
            local_only_dossiers: 0,
            cloud_required_dossiers: 0,
            dossiers_with_policy_surfaces: 0,
            dossiers_without_policy_surfaces: 0,
            unique_policy_surfaces: 0,
            total_actions: 0,
            total_constraints: 0,
            total_risks: 0,
            total_dependency_edges: 0,
            blocking_dependency_edges: 0,
            total_evidence: 0,
            supporting_evidence: 0,
            review_evidence: 0,
            blocking_evidence: 0,
            read_only_dossiers: 0,
            low_risk_dossiers: 0,
            human_approval_dossiers: 0,
            high_risk_dossiers: 0,
            first_approval_priority: None,
            first_blocked_priority: None,
            highest_policy_tier: PrivilegeTier::ReadOnly,
        };

        for dossier in dossiers {
            summary.total_dossiers += 1;
            match dossier.decision.decision_status {
                IntegrationActivationDecisionStatus::ReadyToApprove => {
                    summary.ready_to_approve_dossiers += 1;
                    summary.first_approval_priority = min_optional_priority(
                        summary.first_approval_priority,
                        Some(dossier.priority()),
                    );
                }
                IntegrationActivationDecisionStatus::BlockedOnPrerequisites => {
                    summary.blocked_dossiers += 1;
                    summary.first_blocked_priority = min_optional_priority(
                        summary.first_blocked_priority,
                        Some(dossier.priority()),
                    );
                }
            }

            if dossier.decision.packet.review.local_only {
                summary.local_only_dossiers += 1;
            }
            if dossier.decision.packet.review.cloud_required {
                summary.cloud_required_dossiers += 1;
            }
            if dossier.has_policy_surfaces() {
                summary.dossiers_with_policy_surfaces += 1;
            } else {
                summary.dossiers_without_policy_surfaces += 1;
            }
            for surface in &dossier.decision.packet.review.policy_surfaces {
                policy_surfaces.insert(*surface);
            }

            summary.total_actions += dossier.decision.packet.action_summary.total_actions;
            summary.total_constraints +=
                dossier.decision.packet.constraint_summary.total_constraints;
            summary.total_risks += dossier.decision.packet.risk_summary.total_risks;
            summary.total_dependency_edges +=
                dossier.decision.packet.dependency_graph.summary.total_edges;
            summary.blocking_dependency_edges += dossier
                .decision
                .packet
                .dependency_graph
                .summary
                .blocking_edges;

            summary.total_evidence += dossier.evidence_summary.total_evidence;
            summary.supporting_evidence += dossier.evidence_summary.supporting_evidence;
            summary.review_evidence += dossier.evidence_summary.review_evidence;
            summary.blocking_evidence += dossier.evidence_summary.blocking_evidence;

            match dossier.required_tier() {
                PrivilegeTier::ReadOnly => summary.read_only_dossiers += 1,
                PrivilegeTier::LowRisk => summary.low_risk_dossiers += 1,
                PrivilegeTier::HumanApproval => summary.human_approval_dossiers += 1,
                PrivilegeTier::HighRisk => summary.high_risk_dossiers += 1,
            }
            summary.highest_policy_tier = summary.highest_policy_tier.max(dossier.required_tier());
        }

        summary.unique_policy_surfaces = policy_surfaces.len();
        summary
    }

    pub fn is_empty(&self) -> bool {
        self.total_dossiers == 0
    }

    pub fn has_approval_ready_work(&self) -> bool {
        self.ready_to_approve_dossiers > 0
    }

    pub fn has_review_work(&self) -> bool {
        self.review_evidence > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.blocked_dossiers > 0 || self.blocking_evidence > 0
    }

    pub fn requires_attention(&self) -> bool {
        self.has_review_work() || self.has_blockers()
    }
}

impl IntegrationActivationConstraintSummary {
    pub fn from_constraints<'a>(
        constraints: impl IntoIterator<Item = &'a IntegrationActivationConstraint>,
    ) -> Self {
        let mut affected_integrations = BTreeSet::new();
        let mut summary = Self {
            total_constraints: 0,
            blocking_constraints: 0,
            review_constraints: 0,
            primitive_constraints: 0,
            capability_constraints: 0,
            dependency_constraints: 0,
            policy_review_constraints: 0,
            affected_integrations: 0,
            first_blocking_priority: None,
            first_review_priority: None,
            highest_policy_tier: PrivilegeTier::ReadOnly,
        };

        for constraint in constraints {
            summary.total_constraints += 1;
            if constraint.blocks_activation {
                summary.blocking_constraints += 1;
                summary.first_blocking_priority = min_optional_priority(
                    summary.first_blocking_priority,
                    Some(constraint.highest_priority),
                );
            }
            if constraint.requires_human_review {
                summary.review_constraints += 1;
                summary.first_review_priority = min_optional_priority(
                    summary.first_review_priority,
                    Some(constraint.highest_priority),
                );
            }
            match constraint.kind {
                IntegrationActivationConstraintKind::Primitive => {
                    summary.primitive_constraints += 1
                }
                IntegrationActivationConstraintKind::Capability => {
                    summary.capability_constraints += 1
                }
                IntegrationActivationConstraintKind::Dependency => {
                    summary.dependency_constraints += 1
                }
                IntegrationActivationConstraintKind::PolicyReview => {
                    summary.policy_review_constraints += 1
                }
            }
            for integration_id in &constraint.affected_integration_ids {
                affected_integrations.insert(integration_id.clone());
            }
            summary.highest_policy_tier = summary
                .highest_policy_tier
                .max(constraint.highest_policy_tier);
        }

        summary.affected_integrations = affected_integrations.len();
        summary
    }

    pub fn is_empty(&self) -> bool {
        self.total_constraints == 0
    }

    pub fn has_blockers(&self) -> bool {
        self.blocking_constraints > 0
    }

    pub fn has_review_work(&self) -> bool {
        self.review_constraints > 0
    }
}

impl IntegrationReadinessReport {
    pub fn activation_ready(&self) -> bool {
        self.missing_primitives.is_empty()
            && self.missing_capabilities.is_empty()
            && self.missing_dependencies.is_empty()
    }

    pub fn is_blocked(&self) -> bool {
        !self.activation_ready()
    }

    pub fn missing_prerequisite_count(&self) -> usize {
        self.missing_primitives.len()
            + self.missing_capabilities.len()
            + self.missing_dependencies.len()
    }

    pub fn missing_primitive(&self, primitive: PrimitiveFamily) -> bool {
        self.missing_primitives.contains(&primitive)
    }

    pub fn missing_capability(&self, capability_id: &CapabilityId) -> bool {
        self.missing_capabilities
            .iter()
            .any(|candidate| candidate == capability_id)
    }

    pub fn missing_dependency(&self, integration_id: &IntegrationId) -> bool {
        self.missing_dependencies
            .iter()
            .any(|candidate| candidate == integration_id)
    }

    pub fn delegates_to_integration(&self, integration_id: &IntegrationId) -> bool {
        matches!(
            &self.activation_target,
            IntegrationActivationTarget::DelegatedIntegration(target) if target == integration_id
        )
    }
}

impl IntegrationActivationCandidateRecommendation {
    fn from_report(report: &IntegrationReadinessReport) -> Self {
        if report.is_blocked() {
            Self::BlockedOnPrerequisites
        } else if report.requires_human_review {
            Self::NeedsHumanReview
        } else {
            Self::ReadyToActivate
        }
    }

    pub fn is_actionable(self) -> bool {
        matches!(self, Self::ReadyToActivate | Self::NeedsHumanReview)
    }
}

impl IntegrationActivationCandidate {
    pub fn from_report(report: IntegrationReadinessReport) -> Self {
        let blocker_count = report.missing_prerequisite_count();
        let recommendation = IntegrationActivationCandidateRecommendation::from_report(&report);
        Self {
            readiness_report: report,
            recommendation,
            blocker_count,
        }
    }

    pub fn activation_ready(&self) -> bool {
        self.readiness_report.activation_ready()
    }

    pub fn is_actionable(&self) -> bool {
        self.recommendation.is_actionable()
    }

    pub fn is_blocked(&self) -> bool {
        self.recommendation == IntegrationActivationCandidateRecommendation::BlockedOnPrerequisites
    }

    pub fn requires_human_review(&self) -> bool {
        self.readiness_report.requires_human_review
    }
}

impl IntegrationActivationCandidateSummary {
    pub fn from_candidates<'a>(
        candidates: impl IntoIterator<Item = &'a IntegrationActivationCandidate>,
    ) -> Self {
        let mut summary = Self {
            total_candidates: 0,
            ready_to_activate_candidates: 0,
            needs_human_review_candidates: 0,
            blocked_candidates: 0,
            activation_ready_candidates: 0,
            candidates_requiring_human_review: 0,
            candidates_missing_primitives: 0,
            candidates_missing_capabilities: 0,
            candidates_missing_dependencies: 0,
            direct_targets: 0,
            delegated_integration_targets: 0,
            delegated_standard_targets: 0,
            local_only_candidates: 0,
            cloud_required_candidates: 0,
            highest_policy_tier: PrivilegeTier::ReadOnly,
        };

        for candidate in candidates {
            summary.total_candidates += 1;
            match candidate.recommendation {
                IntegrationActivationCandidateRecommendation::ReadyToActivate => {
                    summary.ready_to_activate_candidates += 1
                }
                IntegrationActivationCandidateRecommendation::NeedsHumanReview => {
                    summary.needs_human_review_candidates += 1
                }
                IntegrationActivationCandidateRecommendation::BlockedOnPrerequisites => {
                    summary.blocked_candidates += 1
                }
            }
            if candidate.activation_ready() {
                summary.activation_ready_candidates += 1;
            }
            if candidate.requires_human_review() {
                summary.candidates_requiring_human_review += 1;
            }
            if !candidate.readiness_report.missing_primitives.is_empty() {
                summary.candidates_missing_primitives += 1;
            }
            if !candidate.readiness_report.missing_capabilities.is_empty() {
                summary.candidates_missing_capabilities += 1;
            }
            if !candidate.readiness_report.missing_dependencies.is_empty() {
                summary.candidates_missing_dependencies += 1;
            }
            match &candidate.readiness_report.activation_target {
                IntegrationActivationTarget::Direct => summary.direct_targets += 1,
                IntegrationActivationTarget::DelegatedIntegration(_) => {
                    summary.delegated_integration_targets += 1
                }
                IntegrationActivationTarget::DelegatedStandards(_) => {
                    summary.delegated_standard_targets += 1
                }
            }
            if candidate.readiness_report.local_only {
                summary.local_only_candidates += 1;
            }
            if candidate.readiness_report.cloud_required {
                summary.cloud_required_candidates += 1;
            }
            summary.highest_policy_tier = summary
                .highest_policy_tier
                .max(candidate.readiness_report.highest_policy_tier);
        }

        summary
    }

    pub fn is_empty(&self) -> bool {
        self.total_candidates == 0
    }

    pub fn has_actionable_candidates(&self) -> bool {
        self.ready_to_activate_candidates > 0 || self.needs_human_review_candidates > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.blocked_candidates > 0
    }

    pub fn has_review_work(&self) -> bool {
        self.needs_human_review_candidates > 0
    }
}

impl IntegrationActivationActionKind {
    fn sort_rank(self) -> u8 {
        match self {
            Self::ActivateIntegration => 0,
            Self::ReviewPolicy => 1,
            Self::ProvidePrimitive => 2,
            Self::GrantCapability => 3,
            Self::EnableDependency => 4,
        }
    }
}

impl IntegrationActivationAction {
    pub fn activate(candidate: &IntegrationActivationCandidate) -> Self {
        Self::from_candidate(
            candidate,
            IntegrationActivationActionKind::ActivateIntegration,
        )
    }

    pub fn review_policy(candidate: &IntegrationActivationCandidate) -> Self {
        Self::from_candidate(candidate, IntegrationActivationActionKind::ReviewPolicy)
    }

    pub fn provide_primitive(
        candidate: &IntegrationActivationCandidate,
        primitive: PrimitiveFamily,
    ) -> Self {
        Self {
            primitive: Some(primitive),
            ..Self::from_candidate(candidate, IntegrationActivationActionKind::ProvidePrimitive)
        }
    }

    pub fn grant_capability(
        candidate: &IntegrationActivationCandidate,
        capability_id: CapabilityId,
    ) -> Self {
        Self {
            capability_id: Some(capability_id),
            ..Self::from_candidate(candidate, IntegrationActivationActionKind::GrantCapability)
        }
    }

    pub fn enable_dependency(
        candidate: &IntegrationActivationCandidate,
        dependency_integration_id: IntegrationId,
    ) -> Self {
        Self {
            dependency_integration_id: Some(dependency_integration_id),
            ..Self::from_candidate(candidate, IntegrationActivationActionKind::EnableDependency)
        }
    }

    pub fn is_activation(&self) -> bool {
        self.kind == IntegrationActivationActionKind::ActivateIntegration
    }

    pub fn blocks_activation(&self) -> bool {
        !self.is_activation()
    }

    fn from_candidate(
        candidate: &IntegrationActivationCandidate,
        kind: IntegrationActivationActionKind,
    ) -> Self {
        Self {
            kind,
            requested_integration_id: candidate.readiness_report.requested_integration_id.clone(),
            display_name: candidate.readiness_report.display_name.clone(),
            priority: candidate.readiness_report.priority,
            recommendation: candidate.recommendation,
            primitive: None,
            capability_id: None,
            dependency_integration_id: None,
            highest_policy_tier: candidate.readiness_report.highest_policy_tier,
        }
    }
}

impl IntegrationActivationActionSummary {
    pub fn from_actions<'a>(
        actions: impl IntoIterator<Item = &'a IntegrationActivationAction>,
    ) -> Self {
        let mut unique_integrations = BTreeSet::new();
        let mut actionable_integrations = BTreeSet::new();
        let mut blocked_integrations = BTreeSet::new();
        let mut summary = Self {
            total_actions: 0,
            activate_integration_actions: 0,
            review_policy_actions: 0,
            provide_primitive_actions: 0,
            grant_capability_actions: 0,
            enable_dependency_actions: 0,
            actionable_integration_count: 0,
            blocked_integration_count: 0,
            unique_integrations: 0,
            first_action_priority: None,
            first_activation_priority: None,
            first_blocker_priority: None,
            highest_policy_tier: PrivilegeTier::ReadOnly,
        };

        for action in actions {
            summary.total_actions += 1;
            unique_integrations.insert(action.requested_integration_id.clone());
            summary.first_action_priority = Some(
                summary
                    .first_action_priority
                    .map_or(action.priority, |priority| priority.min(action.priority)),
            );
            summary.highest_policy_tier =
                summary.highest_policy_tier.max(action.highest_policy_tier);
            match action.kind {
                IntegrationActivationActionKind::ActivateIntegration => {
                    summary.activate_integration_actions += 1;
                    actionable_integrations.insert(action.requested_integration_id.clone());
                    summary.first_activation_priority = Some(
                        summary
                            .first_activation_priority
                            .map_or(action.priority, |priority| priority.min(action.priority)),
                    );
                }
                IntegrationActivationActionKind::ReviewPolicy => {
                    summary.review_policy_actions += 1;
                    blocked_integrations.insert(action.requested_integration_id.clone());
                    summary.first_blocker_priority = Some(
                        summary
                            .first_blocker_priority
                            .map_or(action.priority, |priority| priority.min(action.priority)),
                    );
                }
                IntegrationActivationActionKind::ProvidePrimitive => {
                    summary.provide_primitive_actions += 1;
                    blocked_integrations.insert(action.requested_integration_id.clone());
                    summary.first_blocker_priority = Some(
                        summary
                            .first_blocker_priority
                            .map_or(action.priority, |priority| priority.min(action.priority)),
                    );
                }
                IntegrationActivationActionKind::GrantCapability => {
                    summary.grant_capability_actions += 1;
                    blocked_integrations.insert(action.requested_integration_id.clone());
                    summary.first_blocker_priority = Some(
                        summary
                            .first_blocker_priority
                            .map_or(action.priority, |priority| priority.min(action.priority)),
                    );
                }
                IntegrationActivationActionKind::EnableDependency => {
                    summary.enable_dependency_actions += 1;
                    blocked_integrations.insert(action.requested_integration_id.clone());
                    summary.first_blocker_priority = Some(
                        summary
                            .first_blocker_priority
                            .map_or(action.priority, |priority| priority.min(action.priority)),
                    );
                }
            }
        }

        summary.unique_integrations = unique_integrations.len();
        summary.actionable_integration_count = actionable_integrations.len();
        summary.blocked_integration_count = blocked_integrations.len();
        summary
    }

    pub fn is_empty(&self) -> bool {
        self.total_actions == 0
    }

    pub fn has_activation_work(&self) -> bool {
        self.activate_integration_actions > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.review_policy_actions > 0
            || self.provide_primitive_actions > 0
            || self.grant_capability_actions > 0
            || self.enable_dependency_actions > 0
    }

    pub fn has_review_work(&self) -> bool {
        self.review_policy_actions > 0
    }
}

impl IntegrationActivationAgendaStage {
    pub fn from_candidates(
        priority: u8,
        mut candidates: Vec<IntegrationActivationCandidate>,
    ) -> Self {
        candidates.sort_by(compare_activation_candidates);
        let candidate_summary =
            IntegrationActivationCandidateSummary::from_candidates(candidates.iter());
        let actions = activation_actions_from_candidates(candidates.iter());
        let action_summary = IntegrationActivationActionSummary::from_actions(actions.iter());

        Self {
            priority,
            candidates,
            candidate_summary,
            actions,
            action_summary,
        }
    }

    pub fn has_activation_work(&self) -> bool {
        self.action_summary.has_activation_work()
    }

    pub fn has_blockers(&self) -> bool {
        self.action_summary.has_blockers()
    }

    pub fn has_review_work(&self) -> bool {
        self.action_summary.has_review_work()
    }

    pub fn has_actionable_candidates(&self) -> bool {
        self.candidate_summary.has_actionable_candidates()
    }
}

impl IntegrationActivationAgendaSummary {
    pub fn from_stages<'a>(
        stages: impl IntoIterator<Item = &'a IntegrationActivationAgendaStage>,
    ) -> Self {
        let mut candidates = Vec::new();
        let mut actions = Vec::new();
        let empty_candidates = Vec::<&IntegrationActivationCandidate>::new();
        let empty_actions = Vec::<&IntegrationActivationAction>::new();
        let mut summary = Self {
            total_stages: 0,
            total_candidates: 0,
            total_actions: 0,
            stages_with_activation_work: 0,
            stages_with_blockers: 0,
            stages_with_review_work: 0,
            first_action_priority: None,
            first_activation_priority: None,
            first_blocker_priority: None,
            highest_policy_tier: PrivilegeTier::ReadOnly,
            candidate_summary: IntegrationActivationCandidateSummary::from_candidates(
                empty_candidates,
            ),
            action_summary: IntegrationActivationActionSummary::from_actions(empty_actions),
        };

        for stage in stages {
            summary.total_stages += 1;
            summary.total_candidates += stage.candidates.len();
            summary.total_actions += stage.actions.len();
            if stage.has_activation_work() {
                summary.stages_with_activation_work += 1;
            }
            if stage.has_blockers() {
                summary.stages_with_blockers += 1;
            }
            if stage.has_review_work() {
                summary.stages_with_review_work += 1;
            }
            summary.highest_policy_tier = summary
                .highest_policy_tier
                .max(stage.action_summary.highest_policy_tier)
                .max(stage.candidate_summary.highest_policy_tier);
            summary.first_action_priority = min_optional_priority(
                summary.first_action_priority,
                stage.action_summary.first_action_priority,
            );
            summary.first_activation_priority = min_optional_priority(
                summary.first_activation_priority,
                stage.action_summary.first_activation_priority,
            );
            summary.first_blocker_priority = min_optional_priority(
                summary.first_blocker_priority,
                stage.action_summary.first_blocker_priority,
            );
            candidates.extend(stage.candidates.iter());
            actions.extend(stage.actions.iter());
        }

        summary.candidate_summary =
            IntegrationActivationCandidateSummary::from_candidates(candidates);
        summary.action_summary = IntegrationActivationActionSummary::from_actions(actions);
        summary
    }

    pub fn is_empty(&self) -> bool {
        self.total_stages == 0
    }

    pub fn has_activation_work(&self) -> bool {
        self.stages_with_activation_work > 0
    }

    pub fn has_blockers(&self) -> bool {
        self.stages_with_blockers > 0
    }

    pub fn has_review_work(&self) -> bool {
        self.stages_with_review_work > 0
    }
}

impl IntegrationActivationRunwayStage {
    pub fn from_candidates(
        priority: u8,
        mut candidates: Vec<IntegrationActivationCandidate>,
    ) -> Self {
        candidates.sort_by(compare_activation_candidates);
        let summary = IntegrationActivationCandidateSummary::from_candidates(candidates.iter());
        Self {
            priority,
            candidates,
            summary,
        }
    }

    pub fn has_actionable_candidates(&self) -> bool {
        self.summary.has_actionable_candidates()
    }

    pub fn has_blockers(&self) -> bool {
        self.summary.has_blockers()
    }

    pub fn has_review_work(&self) -> bool {
        self.summary.has_review_work()
    }
}

impl IntegrationActivationRunwaySummary {
    pub fn from_stages<'a>(
        stages: impl IntoIterator<Item = &'a IntegrationActivationRunwayStage>,
    ) -> Self {
        let stages = stages.into_iter().collect::<Vec<_>>();
        let candidate_summary = IntegrationActivationCandidateSummary::from_candidates(
            stages.iter().flat_map(|stage| stage.candidates.iter()),
        );
        let mut summary = Self {
            total_stages: 0,
            total_candidates: candidate_summary.total_candidates,
            actionable_stages: 0,
            ready_stages: 0,
            review_stages: 0,
            blocked_stages: 0,
            first_actionable_priority: None,
            first_blocked_priority: None,
            next_ready_priority: None,
            highest_policy_tier: candidate_summary.highest_policy_tier,
            candidate_summary,
        };

        for stage in stages {
            summary.total_stages += 1;
            if stage.has_actionable_candidates() {
                summary.actionable_stages += 1;
                if summary.first_actionable_priority.is_none() {
                    summary.first_actionable_priority = Some(stage.priority);
                }
            }
            if stage.summary.ready_to_activate_candidates > 0 {
                summary.ready_stages += 1;
                if summary.next_ready_priority.is_none() {
                    summary.next_ready_priority = Some(stage.priority);
                }
            }
            if stage.has_review_work() {
                summary.review_stages += 1;
            }
            if stage.has_blockers() {
                summary.blocked_stages += 1;
                if summary.first_blocked_priority.is_none() {
                    summary.first_blocked_priority = Some(stage.priority);
                }
            }
        }

        summary
    }

    pub fn is_empty(&self) -> bool {
        self.total_stages == 0
    }

    pub fn has_actionable_stage(&self) -> bool {
        self.actionable_stages > 0
    }

    pub fn has_blocked_stage(&self) -> bool {
        self.blocked_stages > 0
    }

    pub fn has_review_stage(&self) -> bool {
        self.review_stages > 0
    }
}

impl IntegrationActivationPlan {
    pub fn requires_human_review(&self) -> bool {
        self.highest_policy_tier >= PrivilegeTier::HumanApproval
    }

    pub fn requires_primitive(&self, primitive: PrimitiveFamily) -> bool {
        self.required_primitives.contains(&primitive)
    }

    pub fn requires_capability(&self, capability_id: &CapabilityId) -> bool {
        self.required_capabilities
            .iter()
            .any(|candidate| candidate == capability_id)
    }

    pub fn delegates_to_integration(&self, integration_id: &IntegrationId) -> bool {
        matches!(
            &self.activation_target,
            IntegrationActivationTarget::DelegatedIntegration(target) if target == integration_id
        )
    }

    pub fn delegates_to_standard(&self, protocol: &ProtocolFamily) -> bool {
        matches!(
            &self.activation_target,
            IntegrationActivationTarget::DelegatedStandards(standards)
                if standards.iter().any(|candidate| candidate == protocol)
        )
    }
}

impl IntegrationCatalogEntry {
    pub fn summary(&self) -> IntegrationCatalogEntrySummary {
        IntegrationCatalogEntrySummary::from_entry(self)
    }

    pub fn is_virtual(&self) -> bool {
        self.category == IntegrationCategory::VirtualAlias
    }

    pub fn is_local(&self) -> bool {
        self.connectivity.is_local()
    }

    pub fn requires_cloud(&self) -> bool {
        self.connectivity.requires_cloud()
    }

    pub fn supports_capability(&self, capability_id: &CapabilityId) -> bool {
        self.required_capabilities
            .iter()
            .any(|candidate| candidate == capability_id)
    }

    pub fn uses_discovery(&self, mechanism: DiscoveryMechanism) -> bool {
        self.discovery_mechanisms.contains(&mechanism)
    }

    pub fn requires_primitive(&self, primitive: PrimitiveFamily) -> bool {
        self.required_primitives.contains(&primitive)
    }

    pub fn policy_surfaces(&self) -> Vec<IntegrationPolicySurface> {
        policy_surfaces_for_entry(self)
    }

    pub fn has_policy_surface(&self, surface: IntegrationPolicySurface) -> bool {
        self.policy_surfaces().contains(&surface)
    }

    pub fn highest_policy_tier(&self) -> PrivilegeTier {
        self.policy_surfaces()
            .into_iter()
            .map(IntegrationPolicySurface::required_tier)
            .max()
            .unwrap_or(PrivilegeTier::ReadOnly)
    }
}

pub fn integration_catalog_tool_descriptors() -> Vec<ToolDescriptor> {
    [
        IntegrationCatalogTool::ListIntegrations,
        IntegrationCatalogTool::DescribeIntegration,
        IntegrationCatalogTool::ListPrimitives,
        IntegrationCatalogTool::DescribePrimitive,
    ]
    .into_iter()
    .map(IntegrationCatalogTool::descriptor)
    .collect()
}

pub fn primitive_family_descriptors() -> Vec<PrimitiveFamilyDescriptor> {
    all_primitive_families()
        .iter()
        .copied()
        .map(describe_primitive_family)
        .collect()
}

pub fn ecosystem_survey_sources() -> Vec<EcosystemSurveySource> {
    vec![
        ecosystem_source(
            EcosystemSurveyPlatform::HomeAssistant,
            "Home Assistant",
            "https://www.home-assistant.io/integrations/",
            "public integration index and Core manifests",
            "Broad integration taxonomy, IoT classes, integration types, virtual aliases, and source references.",
            &[
                PrimitiveFamily::DiscoveryIndex,
                PrimitiveFamily::LocalHttp,
                PrimitiveFamily::WebSocket,
                PrimitiveFamily::Mqtt,
                PrimitiveFamily::CloudApi,
                PrimitiveFamily::Webhook,
                PrimitiveFamily::CalculatedState,
            ],
        ),
        ecosystem_source(
            EcosystemSurveyPlatform::Hubitat,
            "Hubitat",
            "https://docs2.hubitat.com/en/devices/list-of-compatible-devices",
            "compatible-device and driver documentation",
            "Local hub, Zigbee, Z-Wave, LAN, cloud app, Groovy driver, and Matter-over-Thread lessons.",
            &[
                PrimitiveFamily::Radio802154,
                PrimitiveFamily::ZWaveSerialApi,
                PrimitiveFamily::MatterCommissioning,
                PrimitiveFamily::LocalHttp,
                PrimitiveFamily::CloudApi,
                PrimitiveFamily::Supervision,
            ],
        ),
        ecosystem_source(
            EcosystemSurveyPlatform::HomeyPro,
            "Homey Pro",
            "https://homey.app/en-us/apps/homey-pro/",
            "app store and protocol-rich hub model",
            "App-style integration packaging, guided pairing, local radios, cloud apps, flows, energy, media, and security categories.",
            &[
                PrimitiveFamily::BluetoothLowEnergy,
                PrimitiveFamily::Radio802154,
                PrimitiveFamily::ZWaveSerialApi,
                PrimitiveFamily::CloudApi,
                PrimitiveFamily::LocalPairing,
                PrimitiveFamily::Supervision,
            ],
        ),
        ecosystem_source(
            EcosystemSurveyPlatform::SmartThings,
            "SmartThings",
            "https://support.smartthings.com/hc/en-us/articles/360052390111-Devices-in-SmartThings",
            "device, hub, Edge driver, and Matter documentation",
            "Hub-mediated devices, local Edge drivers, partner devices, Matter, Zigbee, Z-Wave, LAN, and cloud linked services.",
            &[
                PrimitiveFamily::MatterCommissioning,
                PrimitiveFamily::Radio802154,
                PrimitiveFamily::ZWaveSerialApi,
                PrimitiveFamily::LocalHttp,
                PrimitiveFamily::CloudApi,
                PrimitiveFamily::CapabilityPolicy,
            ],
        ),
        ecosystem_source(
            EcosystemSurveyPlatform::OpenHab,
            "openHAB",
            "https://www.openhab.org/addons/",
            "add-ons and bindings reference",
            "Protocol-first bindings, automation add-ons, persistence, transformations, voice, UI, and service adapters.",
            &[
                PrimitiveFamily::LocalHttp,
                PrimitiveFamily::Mqtt,
                PrimitiveFamily::SerialController,
                PrimitiveFamily::CloudApi,
                PrimitiveFamily::CalculatedState,
                PrimitiveFamily::Supervision,
            ],
        ),
        ecosystem_source(
            EcosystemSurveyPlatform::Homebridge,
            "Homebridge",
            "https://homebridge.io/plugins",
            "plugin directory and verified plugin program",
            "HomeKit bridge semantics, plugin quality gates, and Node sidecar packaging lessons.",
            &[
                PrimitiveFamily::HomeKitPairing,
                PrimitiveFamily::CloudApi,
                PrimitiveFamily::LocalHttp,
                PrimitiveFamily::CapabilityPolicy,
                PrimitiveFamily::Supervision,
            ],
        ),
        ecosystem_source(
            EcosystemSurveyPlatform::IoBroker,
            "ioBroker",
            "https://download.iobroker.net/sources-dist.json",
            "adapter catalog JSON",
            "Large admin-installable adapter ecosystem and source-catalog metadata shape.",
            &[
                PrimitiveFamily::DiscoveryIndex,
                PrimitiveFamily::CloudApi,
                PrimitiveFamily::Mqtt,
                PrimitiveFamily::LocalHttp,
                PrimitiveFamily::CalculatedState,
            ],
        ),
        ecosystem_source(
            EcosystemSurveyPlatform::Domoticz,
            "Domoticz",
            "https://www.domoticz.com/wiki/Hardware",
            "hardware and protocol wiki",
            "Hardware-gateway framing across 433/868/915 MHz, Z-Wave, Zigbee, cameras, Modbus, MQTT, and serial devices.",
            &[
                PrimitiveFamily::SerialController,
                PrimitiveFamily::Radio802154,
                PrimitiveFamily::ZWaveSerialApi,
                PrimitiveFamily::Mqtt,
                PrimitiveFamily::CameraMedia,
            ],
        ),
        ecosystem_source(
            EcosystemSurveyPlatform::Jeedom,
            "Jeedom",
            "https://market.jeedom.com/",
            "plugin market and smart-home solution pages",
            "Local-first plugin marketplace, multi-protocol setup, and commercial/community plugin separation.",
            &[
                PrimitiveFamily::DiscoveryIndex,
                PrimitiveFamily::LocalHttp,
                PrimitiveFamily::SerialController,
                PrimitiveFamily::CloudApi,
                PrimitiveFamily::Supervision,
            ],
        ),
        ecosystem_source(
            EcosystemSurveyPlatform::HomeSeer,
            "HomeSeer",
            "https://shop.homeseer.com/pages/software-plugins",
            "plugin documentation and hub/software pages",
            "Commercial local hub/software plugin ecosystem with Zigbee, Z-Wave, Matter, Hue, ONVIF, and cloud examples.",
            &[
                PrimitiveFamily::ZWaveSerialApi,
                PrimitiveFamily::Radio802154,
                PrimitiveFamily::MatterCommissioning,
                PrimitiveFamily::LocalHttp,
                PrimitiveFamily::CameraMedia,
            ],
        ),
        ecosystem_source(
            EcosystemSurveyPlatform::AppleHome,
            "Apple Home",
            "https://developer.apple.com/apple-home/",
            "Apple Home developer page",
            "HomeKit, Matter, ThreadNetwork, EnergyKit, MFi, Works with Apple Home, and certification boundaries.",
            &[
                PrimitiveFamily::HomeKitPairing,
                PrimitiveFamily::MatterCommissioning,
                PrimitiveFamily::Radio802154,
                PrimitiveFamily::CertificatePairing,
                PrimitiveFamily::EnergyTelemetry,
            ],
        ),
        ecosystem_source(
            EcosystemSurveyPlatform::GoogleHome,
            "Google Home",
            "https://developers.home.google.com/matter/supported-devices",
            "Matter supported device types",
            "Matter controller surface and device-type-specific support that must map through capability policy.",
            &[
                PrimitiveFamily::MatterCommissioning,
                PrimitiveFamily::CertificatePairing,
                PrimitiveFamily::CapabilityPolicy,
                PrimitiveFamily::CloudApi,
            ],
        ),
        ecosystem_source(
            EcosystemSurveyPlatform::AmazonAlexa,
            "Amazon Alexa",
            "https://developer.amazon.com/en-US/docs/alexa/smarthome/supported-matter-device-categories.html",
            "Matter device categories and smart-home API docs",
            "Voice/cloud ecosystem plus Matter controller surface with explicit category and security restrictions.",
            &[
                PrimitiveFamily::MatterCommissioning,
                PrimitiveFamily::CloudApi,
                PrimitiveFamily::CapabilityPolicy,
                PrimitiveFamily::Webhook,
            ],
        ),
        ecosystem_source(
            EcosystemSurveyPlatform::ZWaveAlliance,
            "Z-Wave Alliance",
            "https://z-wavealliance.org/development-resources-overview/z-wave-command-classes/",
            "command-class development resources",
            "Command classes are the application primitive for Z-Wave capability mapping, reports, interviews, and routing.",
            &[
                PrimitiveFamily::ZWaveSerialApi,
                PrimitiveFamily::SerialController,
                PrimitiveFamily::RadioNetworkKey,
                PrimitiveFamily::CommandMapping,
            ],
        ),
        ecosystem_source(
            EcosystemSurveyPlatform::ThreadGroup,
            "Thread Group",
            "https://threadgroup.org/Newsroom/Blog/thread-with-matter-better-connections-smarter-homes",
            "Thread with Matter technical framing",
            "Thread is the IP mesh/network primitive while Matter is the application layer; border-router health is runtime state.",
            &[
                PrimitiveFamily::Radio802154,
                PrimitiveFamily::MatterCommissioning,
                PrimitiveFamily::RadioNetworkKey,
                PrimitiveFamily::Supervision,
            ],
        ),
    ]
}

pub fn survey_source_for_platform(
    sources: &[EcosystemSurveySource],
    platform: EcosystemSurveyPlatform,
) -> Option<&EcosystemSurveySource> {
    sources.iter().find(|source| source.platform == platform)
}

pub fn survey_sources_requiring_primitive(
    sources: &[EcosystemSurveySource],
    primitive: PrimitiveFamily,
) -> Vec<&EcosystemSurveySource> {
    sources
        .iter()
        .filter(|source| source.requires_primitive(primitive))
        .collect()
}

pub fn ecosystem_platforms_requiring_primitive(
    sources: &[EcosystemSurveySource],
    primitive: PrimitiveFamily,
) -> Vec<EcosystemSurveyPlatform> {
    sources
        .iter()
        .filter(|source| source.requires_primitive(primitive))
        .map(|source| source.platform)
        .collect::<BTreeSet<_>>()
        .into_iter()
        .collect()
}

pub fn ecosystem_primitive_coverage(
    sources: &[EcosystemSurveySource],
) -> Vec<EcosystemPrimitiveCoverage> {
    all_primitive_families()
        .iter()
        .copied()
        .map(|primitive| {
            let platforms = ecosystem_platforms_requiring_primitive(sources, primitive);
            EcosystemPrimitiveCoverage {
                primitive,
                source_count: survey_sources_requiring_primitive(sources, primitive).len(),
                platforms,
            }
        })
        .collect()
}

pub fn describe_primitive_family(primitive: PrimitiveFamily) -> PrimitiveFamilyDescriptor {
    let (display_name, summary) = match primitive {
        PrimitiveFamily::NormalizedModel => (
            "Normalized Model",
            "Bridge, device, entity, capability, event, command, health, and audit records.",
        ),
        PrimitiveFamily::DiscoveryIndex => (
            "Discovery Index",
            "Reusable observations that connect discovery sources to catalog entries.",
        ),
        PrimitiveFamily::Mdns => (
            "mDNS",
            "Local DNS-SD discovery for LAN devices and bridges.",
        ),
        PrimitiveFamily::WsDiscovery => (
            "WS-Discovery",
            "SOAP-over-UDP discovery for ONVIF cameras and other LAN devices.",
        ),
        PrimitiveFamily::Ssdp => (
            "SSDP",
            "UPnP-style discovery for media and legacy LAN devices.",
        ),
        PrimitiveFamily::Udp => (
            "UDP",
            "Bounded UDP datagrams for vendor discovery, state, and command transports.",
        ),
        PrimitiveFamily::Dhcp => (
            "DHCP",
            "Network-observed address hints for LAN device candidates.",
        ),
        PrimitiveFamily::LocalHttp => (
            "Local HTTP",
            "HTTP/HTTPS request primitives for local APIs.",
        ),
        PrimitiveFamily::WebSocket => ("WebSocket", "Bidirectional local or cloud event streams."),
        PrimitiveFamily::ServerSentEvents => (
            "Server-Sent Events",
            "One-way event streams such as Hue CLIP v2 SSE.",
        ),
        PrimitiveFamily::Mqtt => (
            "MQTT",
            "Broker topics, retained state, and command publications.",
        ),
        PrimitiveFamily::BluetoothLowEnergy => (
            "Bluetooth Low Energy",
            "BLE advertisements, GATT reads, and host adapter health.",
        ),
        PrimitiveFamily::Usb => ("USB", "USB device enumeration for radios and controllers."),
        PrimitiveFamily::SerialController => (
            "Serial Controller",
            "Serial transport leases for radio and fieldbus controllers.",
        ),
        PrimitiveFamily::Radio802154 => (
            "802.15.4 Radio",
            "Low-power radio substrate used by Zigbee and Thread-class stacks.",
        ),
        PrimitiveFamily::ZWaveSerialApi => (
            "Z-Wave Serial API",
            "Z-Wave controller serial API framing and lifecycle.",
        ),
        PrimitiveFamily::MatterCommissioning => (
            "Matter Commissioning",
            "Matter onboarding, fabrics, and commissioning metadata.",
        ),
        PrimitiveFamily::HomeKitPairing => (
            "HomeKit Pairing",
            "HAP pairing and accessory model projection.",
        ),
        PrimitiveFamily::CloudApi => ("Cloud API", "OAuth/API-key cloud service calls and quotas."),
        PrimitiveFamily::Webhook => ("Webhook", "Inbound callback registration and delivery."),
        PrimitiveFamily::OAuth2 => ("OAuth2", "Cloud account authorization and token refresh."),
        PrimitiveFamily::LocalPairing => {
            ("Local Pairing", "Physical-presence or local-code setup.")
        }
        PrimitiveFamily::LocalToken => ("Local Token", "Local API token storage and leasing."),
        PrimitiveFamily::CertificatePairing => (
            "Certificate Pairing",
            "Certificate or mTLS-style local trust setup.",
        ),
        PrimitiveFamily::RadioNetworkKey => (
            "Radio Network Key",
            "Mesh/radio network secrets and rotation.",
        ),
        PrimitiveFamily::MqttCredentials => (
            "MQTT Credentials",
            "Broker credentials and client identity leases.",
        ),
        PrimitiveFamily::CameraMedia => (
            "Camera Media",
            "Privacy-sensitive snapshots, streams, and camera events.",
        ),
        PrimitiveFamily::EnergyTelemetry => (
            "Energy Telemetry",
            "Energy, climate, utility, and production measurements.",
        ),
        PrimitiveFamily::CalculatedState => (
            "Calculated State",
            "Internal derived entities and dependency-driven state.",
        ),
        PrimitiveFamily::CommandMapping => (
            "Command Mapping",
            "Idempotent mapping from canonical commands to native effects.",
        ),
        PrimitiveFamily::CapabilityPolicy => (
            "Capability Policy",
            "Capability, privilege, and approval rules for tool execution.",
        ),
        PrimitiveFamily::VaultLease => ("Vault Lease", "Time-bounded secret access for workers."),
        PrimitiveFamily::Supervision => (
            "Supervision",
            "Worker health, restart, backoff, heartbeat, and stale-state policy.",
        ),
        PrimitiveFamily::TestSimulator => (
            "Test Simulator",
            "Fake bridges, brokers, radios, streams, and cloud APIs.",
        ),
    };

    PrimitiveFamilyDescriptor {
        primitive,
        display_name,
        summary,
    }
}

pub fn all_primitive_families() -> &'static [PrimitiveFamily] {
    &[
        PrimitiveFamily::NormalizedModel,
        PrimitiveFamily::DiscoveryIndex,
        PrimitiveFamily::Mdns,
        PrimitiveFamily::WsDiscovery,
        PrimitiveFamily::Ssdp,
        PrimitiveFamily::Udp,
        PrimitiveFamily::Dhcp,
        PrimitiveFamily::LocalHttp,
        PrimitiveFamily::WebSocket,
        PrimitiveFamily::ServerSentEvents,
        PrimitiveFamily::Mqtt,
        PrimitiveFamily::BluetoothLowEnergy,
        PrimitiveFamily::Usb,
        PrimitiveFamily::SerialController,
        PrimitiveFamily::Radio802154,
        PrimitiveFamily::ZWaveSerialApi,
        PrimitiveFamily::MatterCommissioning,
        PrimitiveFamily::HomeKitPairing,
        PrimitiveFamily::CloudApi,
        PrimitiveFamily::Webhook,
        PrimitiveFamily::OAuth2,
        PrimitiveFamily::LocalPairing,
        PrimitiveFamily::LocalToken,
        PrimitiveFamily::CertificatePairing,
        PrimitiveFamily::RadioNetworkKey,
        PrimitiveFamily::MqttCredentials,
        PrimitiveFamily::CameraMedia,
        PrimitiveFamily::EnergyTelemetry,
        PrimitiveFamily::CalculatedState,
        PrimitiveFamily::CommandMapping,
        PrimitiveFamily::CapabilityPolicy,
        PrimitiveFamily::VaultLease,
        PrimitiveFamily::Supervision,
        PrimitiveFamily::TestSimulator,
    ]
}

pub fn first_party_catalog() -> Vec<IntegrationCatalogEntry> {
    vec![
        hue_entry(),
        protocol_entry(
            "zigbee",
            "Zigbee",
            "Repository-owned Zigbee stack and coordinator integration.",
            ConnectivityClass::LocalPolling,
            ImplementationStatus::Scaffolded,
            0,
            ProtocolFamily::Zigbee,
            &["smart_home.read", "smart_home.command.light", "smart_home.manage_network"],
            &[EntityKind::Light, EntityKind::Switch, EntityKind::Sensor],
            &[DiscoveryMechanism::Usb, DiscoveryMechanism::Manual],
            &[AuthMode::RadioNetworkKey],
            "zha",
        ),
        protocol_entry(
            "zwave",
            "Z-Wave",
            "Repository-owned Z-Wave controller, serial API, and command-class integration.",
            ConnectivityClass::LocalPush,
            ImplementationStatus::Scaffolded,
            0,
            ProtocolFamily::ZWave,
            &[
                "smart_home.read",
                "smart_home.command.light",
                "smart_home.command.lock",
                "smart_home.manage_network",
            ],
            &[EntityKind::Light, EntityKind::Switch, EntityKind::Sensor, EntityKind::Lock],
            &[DiscoveryMechanism::Usb, DiscoveryMechanism::Manual],
            &[AuthMode::RadioNetworkKey],
            "zwave_js",
        ),
        protocol_entry(
            "thread",
            "Thread",
            "Repository-owned Thread networking and diagnostics integration.",
            ConnectivityClass::LocalPush,
            ImplementationStatus::Scaffolded,
            0,
            ProtocolFamily::Thread,
            &["smart_home.read", "smart_home.manage_network"],
            &[EntityKind::NetworkDiagnostic],
            &[DiscoveryMechanism::Usb, DiscoveryMechanism::Mdns, DiscoveryMechanism::Manual],
            &[AuthMode::RadioNetworkKey],
            "thread",
        ),
        protocol_entry(
            "mqtt",
            "MQTT",
            "MQTT broker integration for power-user devices, Tasmota, sensors, and bridge-style ecosystems.",
            ConnectivityClass::LocalPush,
            ImplementationStatus::FirstPartyRuntime,
            1,
            ProtocolFamily::Mqtt,
            &[
                "smart_home.read",
                "smart_home.command.light",
                "smart_home.command.switch",
                "smart_home.command.climate",
            ],
            &[
                EntityKind::Light,
                EntityKind::Switch,
                EntityKind::Sensor,
                EntityKind::Thermostat,
            ],
            &[DiscoveryMechanism::Mqtt, DiscoveryMechanism::Manual],
            &[AuthMode::MqttCredentials],
            "mqtt",
        ),
        protocol_entry(
            "matter",
            "Matter",
            "Matter controller integration over Thread, Wi-Fi, and Ethernet.",
            ConnectivityClass::LocalPush,
            ImplementationStatus::Specified,
            1,
            ProtocolFamily::Matter,
            &["smart_home.read", "smart_home.command.light", "smart_home.command.lock"],
            &[EntityKind::Light, EntityKind::Switch, EntityKind::Sensor, EntityKind::Lock],
            &[DiscoveryMechanism::Mdns, DiscoveryMechanism::Manual],
            &[AuthMode::LocalPairing, AuthMode::Certificate],
            "matter",
        ),
        local_device_entry(
            "matter_bridge",
            "Matter Bridge",
            "Catalog route for bridged Matter devices exposed by other ecosystems.",
            ConnectivityClass::LocalPush,
            ImplementationStatus::Cataloged,
            1,
            &["smart_home.read", "smart_home.command.light", "smart_home.command.switch"],
            &[EntityKind::Light, EntityKind::Switch, EntityKind::Sensor],
            &[DiscoveryMechanism::Mdns, DiscoveryMechanism::Manual],
            &[AuthMode::LocalPairing, AuthMode::Certificate],
            "matter",
        )
        .with_primitives(&[
            PrimitiveFamily::MatterCommissioning,
            PrimitiveFamily::Mdns,
            PrimitiveFamily::LocalPairing,
            PrimitiveFamily::CertificatePairing,
        ]),
        local_device_entry(
            "homekit_controller",
            "HomeKit Controller",
            "Local controller for devices that expose the HomeKit Accessory Protocol.",
            ConnectivityClass::LocalPush,
            ImplementationStatus::Specified,
            1,
            &["smart_home.read", "smart_home.command.light", "smart_home.command.lock"],
            &[EntityKind::Light, EntityKind::Switch, EntityKind::Sensor, EntityKind::Lock],
            &[DiscoveryMechanism::Mdns, DiscoveryMechanism::Bluetooth, DiscoveryMechanism::Manual],
            &[AuthMode::LocalPairing],
            "homekit_controller",
        ),
        local_device_entry(
            "esphome",
            "ESPHome",
            "Local ESPHome device integration for DIY sensors, lights, switches, and voice devices.",
            ConnectivityClass::LocalPush,
            ImplementationStatus::Specified,
            1,
            &["smart_home.read", "smart_home.command.light", "smart_home.command.switch"],
            &[EntityKind::Light, EntityKind::Switch, EntityKind::Sensor, EntityKind::Input],
            &[DiscoveryMechanism::Mdns, DiscoveryMechanism::Usb, DiscoveryMechanism::Manual],
            &[AuthMode::LocalToken],
            "esphome",
        ),
        tasmota_entry(),
        local_device_entry(
            "shelly",
            "Shelly",
            "Local Shelly Gen2 and Gen3 relay, light, input, sensor, and energy integration.",
            ConnectivityClass::LocalPolling,
            ImplementationStatus::FirstPartyRuntime,
            2,
            &[
                "smart_home.read",
                "smart_home.command.light",
                "smart_home.command.switch",
            ],
            &[EntityKind::Light, EntityKind::Switch, EntityKind::Sensor, EntityKind::Input],
            &[DiscoveryMechanism::Mdns, DiscoveryMechanism::Dhcp, DiscoveryMechanism::Manual],
            &[AuthMode::None, AuthMode::UsernamePassword],
            "shelly",
        )
        .with_protocols(vec![ProtocolFamily::Vendor("shelly".to_string())]),
        local_device_entry(
            "tplink",
            "TP-Link Smart Home",
            "Credential-free legacy Kasa LAN integration for plugs, switches, and lights.",
            ConnectivityClass::LocalPolling,
            ImplementationStatus::FirstPartyRuntime,
            2,
            &["smart_home.read", "smart_home.command.light", "smart_home.command.switch"],
            &[EntityKind::Light, EntityKind::Switch],
            &[
                DiscoveryMechanism::UdpBroadcast,
                DiscoveryMechanism::Manual,
                DiscoveryMechanism::Dhcp,
            ],
            &[AuthMode::None],
            "tplink",
        )
        .with_protocols(vec![ProtocolFamily::Vendor(
            "kasa_legacy_lan".to_string(),
        )]),
        virtual_alias(
            "tplink_tapo",
            "Tapo",
            "Tapo products route through the TP-Link integration catalog entry.",
            "tplink",
            2,
            "tplink_tapo",
        ),
        {
            let mut entry = local_device_entry(
                "nanoleaf",
                "Nanoleaf",
                "Authenticated Nanoleaf local API integration for lights and panels.",
                ConnectivityClass::LocalPolling,
                ImplementationStatus::FirstPartyRuntime,
                2,
                &["smart_home.read", "smart_home.command.light"],
                &[EntityKind::Light],
                &[DiscoveryMechanism::Mdns, DiscoveryMechanism::Manual],
                &[AuthMode::LocalPairing, AuthMode::LocalToken],
                "nanoleaf",
            )
            .with_protocols(vec![ProtocolFamily::Vendor(
                "nanoleaf_local".to_string(),
            )]);
            entry.required_primitives.push(PrimitiveFamily::VaultLease);
            entry
        },
        local_device_entry(
            "wled",
            "WLED",
            "Local WLED JSON API integration for master and segment lights.",
            ConnectivityClass::LocalPolling,
            ImplementationStatus::FirstPartyRuntime,
            2,
            &["smart_home.read", "smart_home.command.light"],
            &[EntityKind::Light],
            &[DiscoveryMechanism::Mdns, DiscoveryMechanism::Manual],
            &[AuthMode::None],
            "wled",
        )
        .with_protocols(vec![ProtocolFamily::Vendor("wled".to_string())]),
        local_device_entry(
            "lifx",
            "LIFX",
            "Local LIFX LAN UDP discovery, state, and light control.",
            ConnectivityClass::LocalPolling,
            ImplementationStatus::FirstPartyRuntime,
            2,
            &["smart_home.read", "smart_home.command.light"],
            &[EntityKind::Light],
            &[DiscoveryMechanism::UdpBroadcast, DiscoveryMechanism::Manual],
            &[AuthMode::None],
            "lifx",
        )
        .with_protocols(vec![ProtocolFamily::Vendor("lifx_lan".to_string())]),
        {
            let mut entry = base_entry(
                "govee_light_local",
                "Govee Lights Local",
                "Local Govee LAN UDP discovery, state, and light control.",
                IntegrationCategory::LocalDevice,
                ConnectivityClass::LocalPolling,
                ImplementationStatus::FirstPartyRuntime,
                2,
                "govee_light_local",
            )
            .with_capabilities(&["smart_home.read", "smart_home.command.light"])
            .with_entities(&[EntityKind::Light])
            .with_discovery(&[DiscoveryMechanism::UdpMulticast, DiscoveryMechanism::Manual])
            .with_auth(&[AuthMode::None])
            .with_protocols(vec![ProtocolFamily::Vendor("govee_lan".to_string())]);
            entry.required_primitives = vec![
                PrimitiveFamily::NormalizedModel,
                PrimitiveFamily::DiscoveryIndex,
                PrimitiveFamily::Udp,
                PrimitiveFamily::CommandMapping,
                PrimitiveFamily::CapabilityPolicy,
                PrimitiveFamily::Supervision,
            ];
            entry
        },
        bluetooth_entry(
            "switchbot",
            "SwitchBot Bluetooth",
            "Bluetooth integration for SwitchBot buttons, meters, curtains, and locks.",
            ImplementationStatus::Cataloged,
            2,
            &["smart_home.read", "smart_home.command.switch", "smart_home.command.lock"],
            &[EntityKind::Switch, EntityKind::Sensor, EntityKind::Lock, EntityKind::Input],
            "switchbot",
        ),
        local_hub_entry(
            "unifi",
            "UniFi Network",
            "Local UniFi controller integration for presence, network, and device telemetry.",
            ConnectivityClass::LocalPush,
            ImplementationStatus::Cataloged,
            2,
            &["smart_home.read", "smart_home.diagnostics"],
            &[EntityKind::Sensor, EntityKind::NetworkDiagnostic],
            &[DiscoveryMechanism::Manual],
            &[AuthMode::ApiKey, AuthMode::UsernamePassword],
            "unifi",
        ),
        base_entry(
            "sonos",
            "Sonos",
            "Local Sonos ZonePlayer discovery and read-only player-state inspection.",
            IntegrationCategory::CameraMedia,
            ConnectivityClass::LocalPolling,
            ImplementationStatus::FirstPartyRuntime,
            2,
            "sonos",
        )
        .with_capabilities(&["smart_home.read"])
        .with_entities(&[EntityKind::Unknown])
        .with_discovery(&[DiscoveryMechanism::Ssdp, DiscoveryMechanism::Manual])
        .with_auth(&[AuthMode::None])
        .with_protocols(vec![ProtocolFamily::Vendor("sonos_upnp".to_string())])
        .with_primitives(&[
            PrimitiveFamily::NormalizedModel,
            PrimitiveFamily::DiscoveryIndex,
            PrimitiveFamily::Ssdp,
            PrimitiveFamily::LocalHttp,
            PrimitiveFamily::CapabilityPolicy,
            PrimitiveFamily::Supervision,
        ])
        .with_notes(&[
            "Current runtime support is polling-only; GENA subscriptions and media commands remain future work.",
        ]),
        media_entry(
            "cast",
            "Google Cast",
            "Local Cast media-player integration.",
            ConnectivityClass::LocalPolling,
            ImplementationStatus::Cataloged,
            2,
            "cast",
        ),
        camera_entry(
            "onvif",
            "ONVIF",
            "Local ONVIF camera discovery, authenticated inspection, and privacy-gated media access.",
            ConnectivityClass::LocalPolling,
            ImplementationStatus::FirstPartyRuntime,
            3,
            "onvif",
        )
        .with_capabilities(&["smart_home.read", "camera.snapshot", "camera.stream"])
        .with_entities(&[EntityKind::Camera, EntityKind::Sensor])
        .with_discovery(&[DiscoveryMechanism::WsDiscovery, DiscoveryMechanism::Manual])
        .with_protocols(vec![ProtocolFamily::Onvif])
        .with_primitives(&[
            PrimitiveFamily::NormalizedModel,
            PrimitiveFamily::DiscoveryIndex,
            PrimitiveFamily::WsDiscovery,
            PrimitiveFamily::LocalHttp,
            PrimitiveFamily::CameraMedia,
            PrimitiveFamily::CapabilityPolicy,
            PrimitiveFamily::VaultLease,
            PrimitiveFamily::Supervision,
        ]),
        base_entry(
            "reolink",
            "Reolink",
            "Authenticated local Reolink camera and NVR device, channel, and motion inspection.",
            IntegrationCategory::CameraMedia,
            ConnectivityClass::LocalPolling,
            ImplementationStatus::FirstPartyRuntime,
            3,
            "reolink",
        )
        .with_capabilities(&["smart_home.read"])
        .with_entities(&[EntityKind::Camera, EntityKind::Sensor])
        .with_discovery(&[DiscoveryMechanism::Manual])
        .with_auth(&[AuthMode::UsernamePassword])
        .with_protocols(vec![ProtocolFamily::Vendor("reolink_cgi".to_string())])
        .with_primitives(&[
            PrimitiveFamily::NormalizedModel,
            PrimitiveFamily::DiscoveryIndex,
            PrimitiveFamily::LocalHttp,
            PrimitiveFamily::CapabilityPolicy,
            PrimitiveFamily::VaultLease,
            PrimitiveFamily::Supervision,
        ])
        .with_notes(&[
            "This runtime slice covers authenticated CGI status and motion inspection; media transfer, recording, PTZ, and push events remain separate work.",
        ]),
        base_entry(
            "roku",
            "Roku ECP",
            "Local Roku player and TV discovery plus read-only device and application inspection.",
            IntegrationCategory::CameraMedia,
            ConnectivityClass::LocalPolling,
            ImplementationStatus::FirstPartyRuntime,
            2,
            "roku",
        )
        .with_capabilities(&["smart_home.read"])
        .with_entities(&[EntityKind::Unknown])
        .with_discovery(&[DiscoveryMechanism::Ssdp, DiscoveryMechanism::Manual])
        .with_auth(&[AuthMode::None])
        .with_protocols(vec![ProtocolFamily::Vendor("roku_ecp".to_string())])
        .with_primitives(&[
            PrimitiveFamily::NormalizedModel,
            PrimitiveFamily::DiscoveryIndex,
            PrimitiveFamily::Ssdp,
            PrimitiveFamily::Udp,
            PrimitiveFamily::LocalHttp,
            PrimitiveFamily::CapabilityPolicy,
            PrimitiveFamily::Supervision,
        ])
        .with_notes(&[
            "This runtime slice is read-only until D23 has a protocol-neutral media command contract.",
        ]),
        base_entry(
            "wemo",
            "Wemo UPnP",
            "Local Wemo discovery, binary-state inspection, and light-switch control.",
            IntegrationCategory::LocalDevice,
            ConnectivityClass::LocalPolling,
            ImplementationStatus::FirstPartyRuntime,
            2,
            "wemo",
        )
        .with_capabilities(&["smart_home.read", "smart_home.command.light"])
        .with_entities(&[EntityKind::Light, EntityKind::Switch])
        .with_discovery(&[DiscoveryMechanism::Ssdp, DiscoveryMechanism::Manual])
        .with_auth(&[AuthMode::None])
        .with_protocols(vec![ProtocolFamily::Vendor("wemo_upnp".to_string())])
        .with_primitives(&[
            PrimitiveFamily::NormalizedModel,
            PrimitiveFamily::DiscoveryIndex,
            PrimitiveFamily::Ssdp,
            PrimitiveFamily::Udp,
            PrimitiveFamily::LocalHttp,
            PrimitiveFamily::CommandMapping,
            PrimitiveFamily::CapabilityPolicy,
            PrimitiveFamily::Supervision,
        ])
        .with_notes(&[
            "Wemo light switches use the D23 light command contract; generic outlets remain read-only until D23 has a switch command contract.",
        ]),
        camera_entry(
            "ring",
            "Ring",
            "Cloud Ring camera and doorbell integration.",
            ConnectivityClass::CloudPolling,
            ImplementationStatus::Cataloged,
            3,
            "ring",
        ),
        cloud_hub_entry(
            "tuya",
            "Tuya",
            "Cloud Tuya hub for broad long-tail product coverage.",
            ConnectivityClass::CloudPush,
            ImplementationStatus::Cataloged,
            5,
            &["smart_home.read", "smart_home.command.light", "smart_home.command.switch"],
            &[EntityKind::Light, EntityKind::Switch, EntityKind::Sensor],
            "tuya",
        ),
        energy_entry(
            "enphase_envoy",
            "Enphase Envoy",
            "Local solar and energy telemetry integration.",
            ConnectivityClass::LocalPolling,
            ImplementationStatus::Cataloged,
            4,
            "enphase_envoy",
        ),
        base_entry(
            "fronius",
            "Fronius",
            "Local Fronius Solar API v1 site and inverter telemetry integration.",
            IntegrationCategory::EnergyClimate,
            ConnectivityClass::LocalPolling,
            ImplementationStatus::FirstPartyRuntime,
            4,
            "fronius",
        )
        .with_capabilities(&["smart_home.read"])
        .with_entities(&[EntityKind::Sensor])
        .with_discovery(&[DiscoveryMechanism::Mdns, DiscoveryMechanism::Manual])
        .with_auth(&[AuthMode::None])
        .with_protocols(vec![ProtocolFamily::Vendor(
            "fronius_solar_api_v1".to_string(),
        )])
        .with_primitives(&[
            PrimitiveFamily::NormalizedModel,
            PrimitiveFamily::DiscoveryIndex,
            PrimitiveFamily::Mdns,
            PrimitiveFamily::LocalHttp,
            PrimitiveFamily::EnergyTelemetry,
            PrimitiveFamily::CapabilityPolicy,
            PrimitiveFamily::Supervision,
            PrimitiveFamily::TestSimulator,
        ]),
        base_entry(
            "homewizard_energy",
            "HomeWizard Energy",
            "Local HomeWizard Energy API v1 device and utility telemetry.",
            IntegrationCategory::EnergyClimate,
            ConnectivityClass::LocalPolling,
            ImplementationStatus::FirstPartyRuntime,
            4,
            "homewizard",
        )
        .with_capabilities(&["smart_home.read"])
        .with_entities(&[EntityKind::Sensor])
        .with_discovery(&[DiscoveryMechanism::Mdns, DiscoveryMechanism::Manual])
        .with_auth(&[AuthMode::None])
        .with_protocols(vec![ProtocolFamily::Vendor(
            "homewizard_energy_api_v1".to_string(),
        )])
        .with_primitives(&[
            PrimitiveFamily::NormalizedModel,
            PrimitiveFamily::DiscoveryIndex,
            PrimitiveFamily::Mdns,
            PrimitiveFamily::LocalHttp,
            PrimitiveFamily::EnergyTelemetry,
            PrimitiveFamily::CapabilityPolicy,
            PrimitiveFamily::Supervision,
            PrimitiveFamily::TestSimulator,
        ]),
        energy_entry(
            "tesla_powerwall",
            "Tesla Powerwall",
            "Local battery and energy telemetry integration.",
            ConnectivityClass::LocalPolling,
            ImplementationStatus::Cataloged,
            4,
            "tesla_powerwall",
        ),
        cloud_hub_entry(
            "ecobee",
            "ecobee",
            "Cloud thermostat and occupancy integration.",
            ConnectivityClass::CloudPolling,
            ImplementationStatus::Cataloged,
            4,
            &["smart_home.read", "smart_home.command.climate"],
            &[EntityKind::Thermostat, EntityKind::Sensor],
            "ecobee",
        ),
        cloud_hub_entry(
            "nest",
            "Google Nest",
            "Cloud thermostat, camera, and sensor integration.",
            ConnectivityClass::CloudPush,
            ImplementationStatus::Cataloged,
            4,
            &[
                "smart_home.read",
                "smart_home.command.climate",
                "smart_home.command.camera",
            ],
            &[EntityKind::Thermostat, EntityKind::Sensor, EntityKind::Unknown],
            "nest",
        ),
        cloud_hub_entry(
            "home_connect",
            "Home Connect",
            "Cloud appliance integration used by several appliance-brand aliases.",
            ConnectivityClass::CloudPush,
            ImplementationStatus::Cataloged,
            4,
            &["smart_home.read", "smart_home.command.switch"],
            &[EntityKind::Switch, EntityKind::Sensor],
            "home_connect",
        ),
        virtual_alias(
            "symfonisk",
            "IKEA SYMFONISK",
            "SYMFONISK speakers are supported through the Sonos integration.",
            "sonos",
            2,
            "symfonisk",
        ),
        virtual_standard(
            "ultraloq",
            "Ultraloq",
            "Ultraloq products route through the Z-Wave standard.",
            ProtocolFamily::ZWave,
            2,
            "ultraloq",
        ),
    ]
}

pub fn hue_catalog_entry_summary() -> IntegrationCatalogEntrySummary {
    hue_entry().summary()
}

pub fn hue_activation_package_summary() -> IntegrationActivationPackageSummary {
    IntegrationActivationPackageSummary::from_entry(&hue_entry())
}

pub fn hue_readiness_package_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationReadinessPackageSummary {
    IntegrationReadinessPackageSummary::from_entry(
        &hue_entry(),
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn hue_activation_evidence_briefing_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationActivationEvidenceBriefingSummary {
    IntegrationActivationEvidenceBriefingSummary::from_entry(
        &hue_entry(),
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn activation_evidence_briefings_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationEvidenceBriefingSummary> {
    catalog
        .iter()
        .map(|entry| {
            IntegrationActivationEvidenceBriefingSummary::from_entry(
                entry,
                available_primitives,
                allowed_capabilities,
                enabled_integrations,
            )
        })
        .collect()
}

pub fn activation_evidence_briefings_at_or_before_priority(
    catalog: &[IntegrationCatalogEntry],
    priority: u8,
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationEvidenceBriefingSummary> {
    entries_at_or_before_priority(catalog, priority)
        .into_iter()
        .map(|entry| {
            IntegrationActivationEvidenceBriefingSummary::from_entry(
                entry,
                available_primitives,
                allowed_capabilities,
                enabled_integrations,
            )
        })
        .collect()
}

pub fn activation_evidence_rows_from_briefings<'a>(
    briefings: impl IntoIterator<Item = &'a IntegrationActivationEvidenceBriefingSummary>,
) -> Vec<IntegrationActivationEvidenceRow> {
    let mut rows = briefings
        .into_iter()
        .map(IntegrationActivationEvidenceRow::from_briefing)
        .collect::<Vec<_>>();
    rows.sort_by(compare_activation_evidence_rows);
    rows
}

pub fn activation_evidence_rows_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationEvidenceRow> {
    let briefings = activation_evidence_briefings_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    activation_evidence_rows_from_briefings(briefings.iter())
}

pub fn activation_evidence_rows_at_or_before_priority(
    catalog: &[IntegrationCatalogEntry],
    priority: u8,
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationEvidenceRow> {
    let briefings = activation_evidence_briefings_at_or_before_priority(
        catalog,
        priority,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    activation_evidence_rows_from_briefings(briefings.iter())
}

pub fn activation_evidence_lane_inventory_from_rows<'a>(
    rows: impl IntoIterator<Item = &'a IntegrationActivationEvidenceRow>,
) -> Vec<IntegrationActivationEvidenceLaneInventoryItem> {
    let mut items_by_lane = BTreeMap::new();

    for row in rows {
        let Some(lane) = row.next_blocked_lane else {
            continue;
        };
        items_by_lane
            .entry(lane)
            .or_insert_with(|| IntegrationActivationEvidenceLaneInventoryItem::new(lane, row))
            .add_row(row);
    }

    let mut items = items_by_lane.into_values().collect::<Vec<_>>();
    for item in &mut items {
        item.integration_ids.sort();
    }
    items.sort_by(compare_activation_evidence_lane_inventory_items);
    items
}

pub fn activation_evidence_lane_inventory_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationEvidenceLaneInventoryItem> {
    let rows = activation_evidence_rows_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    activation_evidence_lane_inventory_from_rows(rows.iter())
}

pub fn activation_evidence_lane_inventory_at_or_before_priority(
    catalog: &[IntegrationCatalogEntry],
    priority: u8,
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationEvidenceLaneInventoryItem> {
    let rows = activation_evidence_rows_at_or_before_priority(
        catalog,
        priority,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    activation_evidence_lane_inventory_from_rows(rows.iter())
}

pub fn activation_evidence_remediation_items_from_inventory<'a>(
    inventory: impl IntoIterator<Item = &'a IntegrationActivationEvidenceLaneInventoryItem>,
) -> Vec<IntegrationActivationEvidenceRemediationItem> {
    let mut items = inventory
        .into_iter()
        .map(|item| IntegrationActivationEvidenceRemediationItem::from_lane_inventory_item(0, item))
        .collect::<Vec<_>>();
    items.sort_by(compare_activation_evidence_remediation_items);
    for (index, item) in items.iter_mut().enumerate() {
        item.sequence = index + 1;
    }
    items
}

pub fn activation_evidence_remediation_items_from_rows<'a>(
    rows: impl IntoIterator<Item = &'a IntegrationActivationEvidenceRow>,
) -> Vec<IntegrationActivationEvidenceRemediationItem> {
    let inventory = activation_evidence_lane_inventory_from_rows(rows);
    activation_evidence_remediation_items_from_inventory(inventory.iter())
}

pub fn activation_evidence_remediation_items_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationEvidenceRemediationItem> {
    let inventory = activation_evidence_lane_inventory_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    activation_evidence_remediation_items_from_inventory(inventory.iter())
}

pub fn activation_evidence_remediation_items_at_or_before_priority(
    catalog: &[IntegrationCatalogEntry],
    priority: u8,
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationEvidenceRemediationItem> {
    let inventory = activation_evidence_lane_inventory_at_or_before_priority(
        catalog,
        priority,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    activation_evidence_remediation_items_from_inventory(inventory.iter())
}

pub fn activation_evidence_scorecard_summary(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationActivationEvidenceScorecardSummary {
    let briefings = activation_evidence_briefings_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    IntegrationActivationEvidenceScorecardSummary::from_briefings(briefings.iter())
}

pub fn activation_evidence_scorecard_summary_at_or_before_priority(
    catalog: &[IntegrationCatalogEntry],
    priority: u8,
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationActivationEvidenceScorecardSummary {
    let briefings = activation_evidence_briefings_at_or_before_priority(
        catalog,
        priority,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    IntegrationActivationEvidenceScorecardSummary::from_briefings(briefings.iter())
}

fn next_blocked_evidence_lane(
    briefing: &IntegrationActivationEvidenceBriefingSummary,
) -> Option<IntegrationActivationEvidenceLane> {
    [
        (
            briefing.catalog_evidence_ready,
            IntegrationActivationEvidenceLane::Catalog,
        ),
        (
            briefing.activation_plan_evidence_ready,
            IntegrationActivationEvidenceLane::ActivationPlan,
        ),
        (
            briefing.readiness_evidence_ready,
            IntegrationActivationEvidenceLane::Readiness,
        ),
        (
            briefing.policy_evidence_ready,
            IntegrationActivationEvidenceLane::Policy,
        ),
        (
            briefing.local_boundary_evidence_ready,
            IntegrationActivationEvidenceLane::LocalBoundary,
        ),
    ]
    .into_iter()
    .find_map(|(ready, lane)| (!ready).then_some(lane))
}

fn compare_activation_evidence_rows(
    left: &IntegrationActivationEvidenceRow,
    right: &IntegrationActivationEvidenceRow,
) -> Ordering {
    left.priority
        .cmp(&right.priority)
        .then_with(|| {
            right
                .blocked_evidence_lane_count
                .cmp(&left.blocked_evidence_lane_count)
        })
        .then_with(|| left.integration_id.cmp(&right.integration_id))
}

fn compare_activation_evidence_lane_inventory_items(
    left: &IntegrationActivationEvidenceLaneInventoryItem,
    right: &IntegrationActivationEvidenceLaneInventoryItem,
) -> Ordering {
    left.first_blocked_priority
        .cmp(&right.first_blocked_priority)
        .then_with(|| {
            right
                .blocked_integration_count
                .cmp(&left.blocked_integration_count)
        })
        .then_with(|| left.lane.cmp(&right.lane))
}

fn compare_activation_evidence_remediation_items(
    left: &IntegrationActivationEvidenceRemediationItem,
    right: &IntegrationActivationEvidenceRemediationItem,
) -> Ordering {
    left.priority
        .cmp(&right.priority)
        .then_with(|| {
            right
                .blocked_integration_count
                .cmp(&left.blocked_integration_count)
        })
        .then_with(|| left.remediation_kind.cmp(&right.remediation_kind))
        .then_with(|| left.lane.cmp(&right.lane))
}

pub fn find_entry<'a>(
    catalog: &'a [IntegrationCatalogEntry],
    integration_id: &IntegrationId,
) -> Option<&'a IntegrationCatalogEntry> {
    catalog
        .iter()
        .find(|entry| &entry.integration_id == integration_id)
}

pub fn entries_by_category(
    catalog: &[IntegrationCatalogEntry],
    category: IntegrationCategory,
) -> Vec<&IntegrationCatalogEntry> {
    catalog
        .iter()
        .filter(|entry| entry.category == category)
        .collect()
}

pub fn entries_by_connectivity(
    catalog: &[IntegrationCatalogEntry],
    connectivity: ConnectivityClass,
) -> Vec<&IntegrationCatalogEntry> {
    catalog
        .iter()
        .filter(|entry| entry.connectivity == connectivity)
        .collect()
}

pub fn entries_by_status(
    catalog: &[IntegrationCatalogEntry],
    status: ImplementationStatus,
) -> Vec<&IntegrationCatalogEntry> {
    catalog
        .iter()
        .filter(|entry| entry.implementation_status == status)
        .collect()
}

pub fn entries_requiring_capability<'a>(
    catalog: &'a [IntegrationCatalogEntry],
    capability_id: &CapabilityId,
) -> Vec<&'a IntegrationCatalogEntry> {
    catalog
        .iter()
        .filter(|entry| entry.supports_capability(capability_id))
        .collect()
}

pub fn entries_requiring_primitive(
    catalog: &[IntegrationCatalogEntry],
    primitive: PrimitiveFamily,
) -> Vec<&IntegrationCatalogEntry> {
    catalog
        .iter()
        .filter(|entry| entry.requires_primitive(primitive))
        .collect()
}

pub fn entries_with_policy_surface(
    catalog: &[IntegrationCatalogEntry],
    surface: IntegrationPolicySurface,
) -> Vec<&IntegrationCatalogEntry> {
    catalog
        .iter()
        .filter(|entry| entry.has_policy_surface(surface))
        .collect()
}

pub fn entries_at_or_before_priority(
    catalog: &[IntegrationCatalogEntry],
    priority: u8,
) -> Vec<&IntegrationCatalogEntry> {
    catalog
        .iter()
        .filter(|entry| entry.priority <= priority)
        .collect()
}

pub fn query_integrations<'a>(
    catalog: &'a [IntegrationCatalogEntry],
    query: &IntegrationCatalogQuery,
) -> Vec<&'a IntegrationCatalogEntry> {
    let mut entries = catalog
        .iter()
        .filter(|entry| query.matches_entry(entry))
        .collect::<Vec<_>>();

    sort_query_results(&mut entries, query.sort);
    if let Some(limit) = query.limit {
        entries.truncate(limit);
    }

    entries
}

pub fn primitive_backlog(catalog: &[IntegrationCatalogEntry]) -> Vec<PrimitiveBacklogItem> {
    primitive_backlog_at_or_before_priority(catalog, u8::MAX)
}

pub fn primitive_backlog_at_or_before_priority(
    catalog: &[IntegrationCatalogEntry],
    priority: u8,
) -> Vec<PrimitiveBacklogItem> {
    let mut by_primitive: BTreeMap<PrimitiveFamily, (u8, Vec<IntegrationId>)> = BTreeMap::new();

    for entry in catalog.iter().filter(|entry| entry.priority <= priority) {
        for primitive in &entry.required_primitives {
            let (highest_priority, integration_ids) = by_primitive
                .entry(*primitive)
                .or_insert((entry.priority, Vec::new()));
            *highest_priority = (*highest_priority).min(entry.priority);
            if !integration_ids.contains(&entry.integration_id) {
                integration_ids.push(entry.integration_id.clone());
            }
        }
    }

    let mut backlog = by_primitive
        .into_iter()
        .map(
            |(primitive, (highest_priority, integration_ids))| PrimitiveBacklogItem {
                primitive,
                highest_priority,
                entry_count: integration_ids.len(),
                integration_ids,
            },
        )
        .collect::<Vec<_>>();
    backlog.sort_by(|left, right| {
        left.highest_priority
            .cmp(&right.highest_priority)
            .then_with(|| right.entry_count.cmp(&left.entry_count))
            .then_with(|| left.primitive.cmp(&right.primitive))
    });
    backlog
}

pub fn primitive_backlog_with_ecosystem_coverage(
    catalog: &[IntegrationCatalogEntry],
    sources: &[EcosystemSurveySource],
    priority: u8,
) -> Vec<PrimitiveBacklogCoverageItem> {
    primitive_backlog_at_or_before_priority(catalog, priority)
        .into_iter()
        .map(|item| {
            let platforms = ecosystem_platforms_requiring_primitive(sources, item.primitive);
            PrimitiveBacklogCoverageItem {
                primitive: item.primitive,
                highest_priority: item.highest_priority,
                entry_count: item.entry_count,
                integration_ids: item.integration_ids,
                source_count: survey_sources_requiring_primitive(sources, item.primitive).len(),
                platforms,
            }
        })
        .collect()
}

pub fn ecosystem_platform_coverage(
    catalog: &[IntegrationCatalogEntry],
    sources: &[EcosystemSurveySource],
    priority: u8,
) -> Vec<EcosystemPlatformCoverageItem> {
    let backlog = primitive_backlog_at_or_before_priority(catalog, priority);
    let backlog_primitives = backlog
        .iter()
        .map(|item| item.primitive)
        .collect::<Vec<_>>();
    let mut items = sources
        .iter()
        .map(|source| {
            let covered_backlog_primitives = backlog
                .iter()
                .filter(|item| source.requires_primitive(item.primitive))
                .map(|item| item.primitive)
                .collect::<Vec<_>>();
            let uncovered_backlog_primitives = backlog
                .iter()
                .filter(|item| !source.requires_primitive(item.primitive))
                .map(|item| item.primitive)
                .collect::<Vec<_>>();
            let highest_backlog_priority = backlog
                .iter()
                .filter(|item| source.requires_primitive(item.primitive))
                .map(|item| item.highest_priority)
                .min();
            let backlog_entry_count = backlog
                .iter()
                .filter(|item| source.requires_primitive(item.primitive))
                .map(|item| item.entry_count)
                .sum();

            EcosystemPlatformCoverageItem {
                platform: source.platform,
                display_name: source.display_name,
                source_url: source.source_url,
                source_surface: source.source_surface,
                contributes: source.contributes,
                primitive_hints: source.primitive_hints.clone(),
                backlog_primitives: backlog_primitives.clone(),
                covered_backlog_primitives,
                uncovered_backlog_primitives,
                highest_backlog_priority,
                backlog_entry_count,
            }
        })
        .collect::<Vec<_>>();

    items.sort_by(|left, right| {
        right
            .covered_backlog_primitive_count()
            .cmp(&left.covered_backlog_primitive_count())
            .then_with(|| left.platform.cmp(&right.platform))
    });
    items
}

pub fn policy_surface_inventory(
    catalog: &[IntegrationCatalogEntry],
) -> Vec<IntegrationPolicySurfaceInventoryItem> {
    policy_surface_inventory_at_or_before_priority(catalog, u8::MAX)
}

pub fn policy_surface_inventory_at_or_before_priority(
    catalog: &[IntegrationCatalogEntry],
    priority: u8,
) -> Vec<IntegrationPolicySurfaceInventoryItem> {
    #[derive(Default)]
    struct SurfaceAccumulator {
        highest_priority: u8,
        local_entry_count: usize,
        cloud_entry_count: usize,
        human_review_entry_count: usize,
        integration_ids: Vec<IntegrationId>,
    }

    let mut by_surface: BTreeMap<IntegrationPolicySurface, SurfaceAccumulator> = BTreeMap::new();

    for entry in catalog.iter().filter(|entry| entry.priority <= priority) {
        let surfaces = entry.policy_surfaces();
        let local_only = entry_local_only(entry);
        let cloud_required = entry_cloud_required(entry);
        let requires_human_review = entry.highest_policy_tier() >= PrivilegeTier::HumanApproval;

        for surface in surfaces {
            let accumulator = by_surface.entry(surface).or_insert(SurfaceAccumulator {
                highest_priority: entry.priority,
                ..SurfaceAccumulator::default()
            });
            accumulator.highest_priority = accumulator.highest_priority.min(entry.priority);
            if local_only {
                accumulator.local_entry_count += 1;
            }
            if cloud_required {
                accumulator.cloud_entry_count += 1;
            }
            if requires_human_review {
                accumulator.human_review_entry_count += 1;
            }
            if !accumulator.integration_ids.contains(&entry.integration_id) {
                accumulator
                    .integration_ids
                    .push(entry.integration_id.clone());
            }
        }
    }

    let mut inventory = by_surface
        .into_iter()
        .map(
            |(surface, accumulator)| IntegrationPolicySurfaceInventoryItem {
                surface,
                required_tier: surface.required_tier(),
                highest_priority: accumulator.highest_priority,
                entry_count: accumulator.integration_ids.len(),
                local_entry_count: accumulator.local_entry_count,
                cloud_entry_count: accumulator.cloud_entry_count,
                human_review_entry_count: accumulator.human_review_entry_count,
                integration_ids: accumulator.integration_ids,
            },
        )
        .collect::<Vec<_>>();
    inventory.sort_by(|left, right| {
        left.highest_priority
            .cmp(&right.highest_priority)
            .then_with(|| right.required_tier.cmp(&left.required_tier))
            .then_with(|| right.entry_count.cmp(&left.entry_count))
            .then_with(|| left.surface.cmp(&right.surface))
    });
    inventory
}

pub fn activation_plan_for_integration(
    catalog: &[IntegrationCatalogEntry],
    integration_id: &IntegrationId,
) -> Option<IntegrationActivationPlan> {
    find_entry(catalog, integration_id).map(activation_plan_for_entry)
}

pub fn activation_plans_at_or_before_priority(
    catalog: &[IntegrationCatalogEntry],
    priority: u8,
) -> Vec<IntegrationActivationPlan> {
    entries_at_or_before_priority(catalog, priority)
        .into_iter()
        .map(activation_plan_for_entry)
        .collect()
}

pub fn readiness_report_for_integration(
    catalog: &[IntegrationCatalogEntry],
    integration_id: &IntegrationId,
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Option<IntegrationReadinessReport> {
    activation_plan_for_integration(catalog, integration_id).map(|plan| {
        readiness_report_for_plan(
            &plan,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        )
    })
}

pub fn readiness_reports_at_or_before_priority(
    catalog: &[IntegrationCatalogEntry],
    priority: u8,
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationReadinessReport> {
    activation_plans_at_or_before_priority(catalog, priority)
        .into_iter()
        .map(|plan| {
            readiness_report_for_plan(
                &plan,
                available_primitives,
                allowed_capabilities,
                enabled_integrations,
            )
        })
        .collect()
}

pub fn activation_candidates_from_reports<'a>(
    reports: impl IntoIterator<Item = &'a IntegrationReadinessReport>,
) -> Vec<IntegrationActivationCandidate> {
    let mut candidates = reports
        .into_iter()
        .cloned()
        .map(IntegrationActivationCandidate::from_report)
        .collect::<Vec<_>>();

    candidates.sort_by(compare_activation_candidates);
    candidates
}

pub fn activation_candidates_at_or_before_priority(
    catalog: &[IntegrationCatalogEntry],
    priority: u8,
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationCandidate> {
    let reports = readiness_reports_at_or_before_priority(
        catalog,
        priority,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    activation_candidates_from_reports(reports.iter())
}

pub fn activation_actions_from_candidates<'a>(
    candidates: impl IntoIterator<Item = &'a IntegrationActivationCandidate>,
) -> Vec<IntegrationActivationAction> {
    let mut actions = Vec::new();

    for candidate in candidates {
        if candidate.activation_ready() {
            if candidate.requires_human_review() {
                actions.push(IntegrationActivationAction::review_policy(candidate));
            } else {
                actions.push(IntegrationActivationAction::activate(candidate));
            }
        }

        for primitive in &candidate.readiness_report.missing_primitives {
            actions.push(IntegrationActivationAction::provide_primitive(
                candidate, *primitive,
            ));
        }
        for capability_id in &candidate.readiness_report.missing_capabilities {
            actions.push(IntegrationActivationAction::grant_capability(
                candidate,
                capability_id.clone(),
            ));
        }
        for integration_id in &candidate.readiness_report.missing_dependencies {
            actions.push(IntegrationActivationAction::enable_dependency(
                candidate,
                integration_id.clone(),
            ));
        }
        if candidate.is_blocked() && candidate.requires_human_review() {
            actions.push(IntegrationActivationAction::review_policy(candidate));
        }
    }

    actions.sort_by(compare_activation_actions);
    actions
}

pub fn activation_actions_at_or_before_priority(
    catalog: &[IntegrationCatalogEntry],
    priority: u8,
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationAction> {
    let candidates = activation_candidates_at_or_before_priority(
        catalog,
        priority,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    activation_actions_from_candidates(candidates.iter())
}

pub fn activation_agenda_from_candidates(
    candidates: Vec<IntegrationActivationCandidate>,
) -> Vec<IntegrationActivationAgendaStage> {
    let mut stages_by_priority: BTreeMap<u8, Vec<IntegrationActivationCandidate>> = BTreeMap::new();
    for candidate in candidates {
        stages_by_priority
            .entry(candidate.readiness_report.priority)
            .or_default()
            .push(candidate);
    }

    stages_by_priority
        .into_iter()
        .map(|(priority, candidates)| {
            IntegrationActivationAgendaStage::from_candidates(priority, candidates)
        })
        .collect()
}

pub fn activation_agenda_at_or_before_priority(
    catalog: &[IntegrationCatalogEntry],
    priority: u8,
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationAgendaStage> {
    activation_agenda_from_candidates(activation_candidates_at_or_before_priority(
        catalog,
        priority,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    ))
}

pub fn activation_runway_from_candidates(
    candidates: Vec<IntegrationActivationCandidate>,
) -> Vec<IntegrationActivationRunwayStage> {
    let mut stages_by_priority: BTreeMap<u8, Vec<IntegrationActivationCandidate>> = BTreeMap::new();
    for candidate in candidates {
        stages_by_priority
            .entry(candidate.readiness_report.priority)
            .or_default()
            .push(candidate);
    }

    stages_by_priority
        .into_iter()
        .map(|(priority, candidates)| {
            IntegrationActivationRunwayStage::from_candidates(priority, candidates)
        })
        .collect()
}

pub fn activation_runway_at_or_before_priority(
    catalog: &[IntegrationCatalogEntry],
    priority: u8,
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationRunwayStage> {
    activation_runway_from_candidates(activation_candidates_at_or_before_priority(
        catalog,
        priority,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    ))
}

pub fn activation_health_from_candidates(
    candidates: Vec<IntegrationActivationCandidate>,
) -> Vec<IntegrationActivationHealthStage> {
    let mut stages_by_priority: BTreeMap<u8, Vec<IntegrationActivationCandidate>> = BTreeMap::new();
    for candidate in candidates {
        stages_by_priority
            .entry(candidate.readiness_report.priority)
            .or_default()
            .push(candidate);
    }

    stages_by_priority
        .into_iter()
        .map(|(priority, candidates)| {
            IntegrationActivationHealthStage::from_candidates(priority, candidates)
        })
        .collect()
}

pub fn activation_health_at_or_before_priority(
    catalog: &[IntegrationCatalogEntry],
    priority: u8,
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationHealthStage> {
    activation_health_from_candidates(activation_candidates_at_or_before_priority(
        catalog,
        priority,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    ))
}

pub fn activation_maintenance_from_candidates(
    catalog: &[IntegrationCatalogEntry],
    candidates: Vec<IntegrationActivationCandidate>,
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationMaintenanceWindow> {
    let mut windows_by_priority: BTreeMap<u8, Vec<IntegrationActivationCandidate>> =
        BTreeMap::new();
    for candidate in candidates {
        windows_by_priority
            .entry(candidate.readiness_report.priority)
            .or_default()
            .push(candidate);
    }

    windows_by_priority
        .into_iter()
        .map(|(priority, candidates)| {
            IntegrationActivationMaintenanceWindow::from_candidates(
                catalog,
                priority,
                candidates,
                enabled_integrations,
            )
        })
        .collect()
}

pub fn activation_maintenance_at_or_before_priority(
    catalog: &[IntegrationCatalogEntry],
    priority: u8,
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationMaintenanceWindow> {
    activation_maintenance_from_candidates(
        catalog,
        activation_candidates_at_or_before_priority(
            catalog,
            priority,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        ),
        enabled_integrations,
    )
}

pub fn activation_constraints_from_candidates<'a>(
    catalog: &[IntegrationCatalogEntry],
    candidates: impl IntoIterator<Item = &'a IntegrationActivationCandidate>,
) -> Vec<IntegrationActivationConstraint> {
    let candidates = candidates.into_iter().collect::<Vec<_>>();
    let policy_tier_by_integration = candidates
        .iter()
        .map(|candidate| {
            (
                candidate.readiness_report.requested_integration_id.clone(),
                candidate.readiness_report.highest_policy_tier,
            )
        })
        .collect::<BTreeMap<_, _>>();
    let gap_inventory = readiness_gap_inventory_from_reports(
        candidates
            .iter()
            .map(|candidate| &candidate.readiness_report),
    );
    let mut constraints = Vec::new();

    for gap in gap_inventory.primitive_gaps {
        let primitive = describe_primitive_family(gap.primitive);
        constraints.push(IntegrationActivationConstraint {
            kind: IntegrationActivationConstraintKind::Primitive,
            constraint_id: format!("primitive:{}", gap.primitive.as_str()),
            display_name: primitive.display_name.to_string(),
            highest_priority: gap.highest_priority,
            highest_policy_tier: highest_policy_tier_for_integrations(
                &gap.integration_ids,
                &policy_tier_by_integration,
            ),
            affected_integration_ids: gap.integration_ids,
            blocks_activation: true,
            requires_human_review: false,
            policy_surfaces: Vec::new(),
        });
    }

    for gap in gap_inventory.capability_gaps {
        constraints.push(IntegrationActivationConstraint {
            kind: IntegrationActivationConstraintKind::Capability,
            constraint_id: format!("capability:{}", gap.capability_id.as_str()),
            display_name: gap.capability_id.as_str().to_string(),
            highest_priority: gap.highest_priority,
            highest_policy_tier: highest_policy_tier_for_integrations(
                &gap.integration_ids,
                &policy_tier_by_integration,
            ),
            affected_integration_ids: gap.integration_ids,
            blocks_activation: true,
            requires_human_review: false,
            policy_surfaces: Vec::new(),
        });
    }

    for gap in gap_inventory.dependency_gaps {
        constraints.push(IntegrationActivationConstraint {
            kind: IntegrationActivationConstraintKind::Dependency,
            constraint_id: format!("dependency:{}", gap.integration_id.as_str()),
            display_name: gap.integration_id.as_str().to_string(),
            highest_priority: gap.highest_priority,
            highest_policy_tier: highest_policy_tier_for_integrations(
                &gap.requested_integration_ids,
                &policy_tier_by_integration,
            ),
            affected_integration_ids: gap.requested_integration_ids,
            blocks_activation: true,
            requires_human_review: false,
            policy_surfaces: Vec::new(),
        });
    }

    let mut policy_review_constraints: BTreeMap<
        Option<IntegrationPolicySurface>,
        (u8, BTreeSet<IntegrationId>, PrivilegeTier),
    > = BTreeMap::new();

    for candidate in candidates {
        if !candidate.requires_human_review() {
            continue;
        }

        let policy_surfaces = find_entry(
            catalog,
            &candidate.readiness_report.requested_integration_id,
        )
        .map(IntegrationCatalogEntry::policy_surfaces)
        .unwrap_or_default();

        if policy_surfaces.is_empty() {
            let (highest_priority, integration_ids, highest_policy_tier) =
                policy_review_constraints.entry(None).or_insert((
                    candidate.readiness_report.priority,
                    BTreeSet::new(),
                    candidate.readiness_report.highest_policy_tier,
                ));
            *highest_priority = (*highest_priority).min(candidate.readiness_report.priority);
            integration_ids.insert(candidate.readiness_report.requested_integration_id.clone());
            *highest_policy_tier =
                (*highest_policy_tier).max(candidate.readiness_report.highest_policy_tier);
            continue;
        }

        for surface in policy_surfaces {
            let (highest_priority, integration_ids, highest_policy_tier) =
                policy_review_constraints.entry(Some(surface)).or_insert((
                    candidate.readiness_report.priority,
                    BTreeSet::new(),
                    surface.required_tier(),
                ));
            *highest_priority = (*highest_priority).min(candidate.readiness_report.priority);
            integration_ids.insert(candidate.readiness_report.requested_integration_id.clone());
            *highest_policy_tier = (*highest_policy_tier)
                .max(surface.required_tier())
                .max(candidate.readiness_report.highest_policy_tier);
        }
    }

    for (surface, (highest_priority, integration_ids, highest_policy_tier)) in
        policy_review_constraints
    {
        let (constraint_id, display_name, policy_surfaces) = match surface {
            Some(surface) => (
                format!("policy_review:{}", surface.as_str()),
                surface.as_str().to_string(),
                vec![surface],
            ),
            None => (
                "policy_review:human_approval".to_string(),
                "human_approval".to_string(),
                Vec::new(),
            ),
        };
        constraints.push(IntegrationActivationConstraint {
            kind: IntegrationActivationConstraintKind::PolicyReview,
            constraint_id,
            display_name,
            highest_priority,
            affected_integration_ids: integration_ids.into_iter().collect(),
            blocks_activation: false,
            requires_human_review: true,
            highest_policy_tier,
            policy_surfaces,
        });
    }

    constraints.sort_by(compare_activation_constraints);
    constraints
}

pub fn activation_constraints_at_or_before_priority(
    catalog: &[IntegrationCatalogEntry],
    priority: u8,
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationConstraint> {
    let candidates = activation_candidates_at_or_before_priority(
        catalog,
        priority,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    activation_constraints_from_candidates(catalog, candidates.iter())
}

pub fn activation_risk_from_candidates<'a>(
    catalog: &[IntegrationCatalogEntry],
    candidates: impl IntoIterator<Item = &'a IntegrationActivationCandidate>,
) -> Vec<IntegrationActivationRiskItem> {
    let mut candidates = candidates.into_iter().collect::<Vec<_>>();
    candidates.sort_by(|left, right| compare_activation_candidates(left, right));

    let mut by_tier: BTreeMap<PrivilegeTier, Vec<&IntegrationActivationCandidate>> =
        BTreeMap::new();
    let mut by_surface: BTreeMap<IntegrationPolicySurface, Vec<&IntegrationActivationCandidate>> =
        BTreeMap::new();

    for candidate in candidates {
        by_tier
            .entry(candidate.readiness_report.highest_policy_tier)
            .or_default()
            .push(candidate);

        if let Some(entry) = find_entry(
            catalog,
            &candidate.readiness_report.requested_integration_id,
        ) {
            for surface in entry.policy_surfaces() {
                by_surface.entry(surface).or_default().push(candidate);
            }
        }
    }

    let mut risks = Vec::new();
    for (tier, candidates) in by_tier {
        let tier_label = privilege_tier_label_for_catalog(tier);
        risks.push(IntegrationActivationRiskItem::from_candidates(
            IntegrationActivationRiskKind::PolicyTier,
            format!("policy_tier:{tier_label}"),
            tier_label.to_string(),
            tier,
            None,
            &candidates,
        ));
    }

    for (surface, candidates) in by_surface {
        risks.push(IntegrationActivationRiskItem::from_candidates(
            IntegrationActivationRiskKind::PolicySurface,
            format!("policy_surface:{}", surface.as_str()),
            surface.as_str().to_string(),
            surface.required_tier(),
            Some(surface),
            &candidates,
        ));
    }

    risks.sort_by(compare_activation_risks);
    risks
}

pub fn activation_risk_at_or_before_priority(
    catalog: &[IntegrationCatalogEntry],
    priority: u8,
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationRiskItem> {
    let candidates = activation_candidates_at_or_before_priority(
        catalog,
        priority,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    activation_risk_from_candidates(catalog, candidates.iter())
}

pub fn activation_reviews_from_candidates<'a>(
    catalog: &[IntegrationCatalogEntry],
    candidates: impl IntoIterator<Item = &'a IntegrationActivationCandidate>,
) -> Vec<IntegrationActivationReviewItem> {
    let mut reviews = candidates
        .into_iter()
        .filter_map(|candidate| IntegrationActivationReviewItem::from_candidate(catalog, candidate))
        .collect::<Vec<_>>();

    reviews.sort_by(compare_activation_reviews);
    reviews
}

pub fn activation_reviews_at_or_before_priority(
    catalog: &[IntegrationCatalogEntry],
    priority: u8,
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationReviewItem> {
    let candidates = activation_candidates_at_or_before_priority(
        catalog,
        priority,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    activation_reviews_from_candidates(catalog, candidates.iter())
}

pub fn activation_approval_packets_from_candidates<'a>(
    catalog: &[IntegrationCatalogEntry],
    candidates: impl IntoIterator<Item = &'a IntegrationActivationCandidate>,
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationApprovalPacket> {
    let mut packets = candidates
        .into_iter()
        .filter_map(|candidate| {
            IntegrationActivationApprovalPacket::from_candidate(
                catalog,
                candidate,
                enabled_integrations,
            )
        })
        .collect::<Vec<_>>();

    packets.sort_by(compare_activation_approval_packets);
    packets
}

pub fn activation_approval_packets_at_or_before_priority(
    catalog: &[IntegrationCatalogEntry],
    priority: u8,
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationApprovalPacket> {
    let candidates = activation_candidates_at_or_before_priority(
        catalog,
        priority,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    activation_approval_packets_from_candidates(catalog, candidates.iter(), enabled_integrations)
}

pub fn activation_decisions_from_candidates<'a>(
    catalog: &[IntegrationCatalogEntry],
    candidates: impl IntoIterator<Item = &'a IntegrationActivationCandidate>,
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationDecisionItem> {
    let mut decisions =
        activation_approval_packets_from_candidates(catalog, candidates, enabled_integrations)
            .into_iter()
            .map(IntegrationActivationDecisionItem::from_packet)
            .collect::<Vec<_>>();

    decisions.sort_by(compare_activation_decisions);
    decisions
}

pub fn activation_decisions_at_or_before_priority(
    catalog: &[IntegrationCatalogEntry],
    priority: u8,
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationDecisionItem> {
    let candidates = activation_candidates_at_or_before_priority(
        catalog,
        priority,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    activation_decisions_from_candidates(catalog, candidates.iter(), enabled_integrations)
}

pub fn activation_evidence_from_decisions<'a>(
    decisions: impl IntoIterator<Item = &'a IntegrationActivationDecisionItem>,
) -> Vec<IntegrationActivationEvidenceItem> {
    let mut evidence = decisions
        .into_iter()
        .flat_map(IntegrationActivationEvidenceItem::from_decision)
        .collect::<Vec<_>>();

    evidence.sort_by(compare_activation_evidence);
    evidence
}

pub fn activation_evidence_from_candidates<'a>(
    catalog: &[IntegrationCatalogEntry],
    candidates: impl IntoIterator<Item = &'a IntegrationActivationCandidate>,
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationEvidenceItem> {
    let decisions = activation_decisions_from_candidates(catalog, candidates, enabled_integrations);
    activation_evidence_from_decisions(decisions.iter())
}

pub fn activation_evidence_at_or_before_priority(
    catalog: &[IntegrationCatalogEntry],
    priority: u8,
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationEvidenceItem> {
    let candidates = activation_candidates_at_or_before_priority(
        catalog,
        priority,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    activation_evidence_from_candidates(catalog, candidates.iter(), enabled_integrations)
}

pub fn activation_dossiers_from_decisions(
    decisions: impl IntoIterator<Item = IntegrationActivationDecisionItem>,
) -> Vec<IntegrationActivationDossierItem> {
    let mut dossiers = decisions
        .into_iter()
        .map(IntegrationActivationDossierItem::from_decision)
        .collect::<Vec<_>>();

    dossiers.sort_by(compare_activation_dossiers);
    dossiers
}

pub fn activation_dossiers_from_candidates<'a>(
    catalog: &[IntegrationCatalogEntry],
    candidates: impl IntoIterator<Item = &'a IntegrationActivationCandidate>,
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationDossierItem> {
    let decisions = activation_decisions_from_candidates(catalog, candidates, enabled_integrations);
    activation_dossiers_from_decisions(decisions)
}

pub fn activation_dossiers_at_or_before_priority(
    catalog: &[IntegrationCatalogEntry],
    priority: u8,
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationDossierItem> {
    let candidates = activation_candidates_at_or_before_priority(
        catalog,
        priority,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    activation_dossiers_from_candidates(catalog, candidates.iter(), enabled_integrations)
}

pub fn activation_readouts_from_candidates(
    catalog: &[IntegrationCatalogEntry],
    candidates: Vec<IntegrationActivationCandidate>,
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationReadoutStage> {
    let mut readouts_by_priority: BTreeMap<u8, Vec<IntegrationActivationCandidate>> =
        BTreeMap::new();
    for candidate in candidates {
        readouts_by_priority
            .entry(candidate.readiness_report.priority)
            .or_default()
            .push(candidate);
    }

    let mut readouts = readouts_by_priority
        .into_iter()
        .map(|(priority, candidates)| {
            IntegrationActivationReadoutStage::from_candidates(
                catalog,
                priority,
                candidates,
                enabled_integrations,
            )
        })
        .collect::<Vec<_>>();
    readouts.sort_by(compare_activation_readouts);
    readouts
}

pub fn activation_readouts_at_or_before_priority(
    catalog: &[IntegrationCatalogEntry],
    priority: u8,
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationReadoutStage> {
    activation_readouts_from_candidates(
        catalog,
        activation_candidates_at_or_before_priority(
            catalog,
            priority,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        ),
        enabled_integrations,
    )
}

pub fn activation_briefing_items_from_readouts<'a>(
    readouts: impl IntoIterator<Item = &'a IntegrationActivationReadoutStage>,
) -> Vec<IntegrationActivationBriefingItem> {
    let mut items = Vec::new();
    for readout in readouts {
        if readout.has_blockers() {
            items.push(IntegrationActivationBriefingItem::from_readout(
                IntegrationActivationBriefingItemKind::Blocker,
                readout,
            ));
        }
        if readout.has_review_work() {
            items.push(IntegrationActivationBriefingItem::from_readout(
                IntegrationActivationBriefingItemKind::Review,
                readout,
            ));
        }
        if readout.has_approval_ready_work() {
            items.push(IntegrationActivationBriefingItem::from_readout(
                IntegrationActivationBriefingItemKind::Approval,
                readout,
            ));
        }
        if readout.has_activation_work() {
            items.push(IntegrationActivationBriefingItem::from_readout(
                IntegrationActivationBriefingItemKind::Activation,
                readout,
            ));
        }
        if readout.has_risks() {
            items.push(IntegrationActivationBriefingItem::from_readout(
                IntegrationActivationBriefingItemKind::Risk,
                readout,
            ));
        }
        if readout.has_dependency_blockers() {
            items.push(IntegrationActivationBriefingItem::from_readout(
                IntegrationActivationBriefingItemKind::Dependency,
                readout,
            ));
        }
    }
    items.sort_by(compare_activation_briefing_items);
    items
}

pub fn activation_briefing_items_from_candidates(
    catalog: &[IntegrationCatalogEntry],
    candidates: Vec<IntegrationActivationCandidate>,
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationBriefingItem> {
    let readouts = activation_readouts_from_candidates(catalog, candidates, enabled_integrations);
    activation_briefing_items_from_readouts(readouts.iter())
}

pub fn activation_briefing_items_at_or_before_priority(
    catalog: &[IntegrationCatalogEntry],
    priority: u8,
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationBriefingItem> {
    let readouts = activation_readouts_at_or_before_priority(
        catalog,
        priority,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    activation_briefing_items_from_readouts(readouts.iter())
}

pub fn activation_dashboard_cards_from_readouts<'a>(
    readouts: impl IntoIterator<Item = &'a IntegrationActivationReadoutStage>,
) -> Vec<IntegrationActivationDashboardCard> {
    let mut cards = readouts
        .into_iter()
        .map(IntegrationActivationDashboardCard::from_readout)
        .collect::<Vec<_>>();
    cards.sort_by(compare_activation_dashboard_cards);
    cards
}

pub fn activation_dashboard_cards_from_candidates(
    catalog: &[IntegrationCatalogEntry],
    candidates: Vec<IntegrationActivationCandidate>,
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationDashboardCard> {
    let readouts = activation_readouts_from_candidates(catalog, candidates, enabled_integrations);
    activation_dashboard_cards_from_readouts(readouts.iter())
}

pub fn activation_dashboard_cards_at_or_before_priority(
    catalog: &[IntegrationCatalogEntry],
    priority: u8,
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationDashboardCard> {
    let readouts = activation_readouts_at_or_before_priority(
        catalog,
        priority,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    activation_dashboard_cards_from_readouts(readouts.iter())
}

pub fn activation_timeline_milestones_from_dashboard_cards(
    mut cards: Vec<IntegrationActivationDashboardCard>,
) -> Vec<IntegrationActivationTimelineMilestone> {
    cards.sort_by(compare_activation_dashboard_cards);
    let mut milestones = cards
        .into_iter()
        .enumerate()
        .map(|(index, card)| {
            IntegrationActivationTimelineMilestone::from_dashboard_card(index + 1, card)
        })
        .collect::<Vec<_>>();
    milestones.sort_by(compare_activation_timeline_milestones);
    for (index, milestone) in milestones.iter_mut().enumerate() {
        milestone.sequence = index + 1;
    }
    milestones
}

pub fn activation_timeline_milestones_from_readouts<'a>(
    readouts: impl IntoIterator<Item = &'a IntegrationActivationReadoutStage>,
) -> Vec<IntegrationActivationTimelineMilestone> {
    activation_timeline_milestones_from_dashboard_cards(activation_dashboard_cards_from_readouts(
        readouts,
    ))
}

pub fn activation_timeline_milestones_from_candidates(
    catalog: &[IntegrationCatalogEntry],
    candidates: Vec<IntegrationActivationCandidate>,
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationTimelineMilestone> {
    activation_timeline_milestones_from_dashboard_cards(activation_dashboard_cards_from_candidates(
        catalog,
        candidates,
        enabled_integrations,
    ))
}

pub fn activation_timeline_milestones_at_or_before_priority(
    catalog: &[IntegrationCatalogEntry],
    priority: u8,
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationTimelineMilestone> {
    activation_timeline_milestones_from_dashboard_cards(
        activation_dashboard_cards_at_or_before_priority(
            catalog,
            priority,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        ),
    )
}

pub fn activation_forecasts_from_timeline_milestones(
    mut milestones: Vec<IntegrationActivationTimelineMilestone>,
) -> Vec<IntegrationActivationForecastItem> {
    milestones.sort_by(compare_activation_timeline_milestones);
    let mut forecasts = milestones
        .into_iter()
        .map(IntegrationActivationForecastItem::from_milestone)
        .collect::<Vec<_>>();
    forecasts.sort_by(compare_activation_forecasts);
    for (index, forecast) in forecasts.iter_mut().enumerate() {
        forecast.sequence = index + 1;
    }
    forecasts
}

pub fn activation_forecasts_from_dashboard_cards(
    cards: Vec<IntegrationActivationDashboardCard>,
) -> Vec<IntegrationActivationForecastItem> {
    activation_forecasts_from_timeline_milestones(
        activation_timeline_milestones_from_dashboard_cards(cards),
    )
}

pub fn activation_forecasts_from_readouts<'a>(
    readouts: impl IntoIterator<Item = &'a IntegrationActivationReadoutStage>,
) -> Vec<IntegrationActivationForecastItem> {
    activation_forecasts_from_dashboard_cards(activation_dashboard_cards_from_readouts(readouts))
}

pub fn activation_forecasts_from_candidates(
    catalog: &[IntegrationCatalogEntry],
    candidates: Vec<IntegrationActivationCandidate>,
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationForecastItem> {
    activation_forecasts_from_dashboard_cards(activation_dashboard_cards_from_candidates(
        catalog,
        candidates,
        enabled_integrations,
    ))
}

pub fn activation_forecasts_at_or_before_priority(
    catalog: &[IntegrationCatalogEntry],
    priority: u8,
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationForecastItem> {
    activation_forecasts_from_dashboard_cards(activation_dashboard_cards_at_or_before_priority(
        catalog,
        priority,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    ))
}

pub fn activation_playbook_steps_from_forecasts(
    mut forecasts: Vec<IntegrationActivationForecastItem>,
) -> Vec<IntegrationActivationPlaybookStep> {
    forecasts.sort_by(compare_activation_forecasts);
    let mut steps = forecasts
        .into_iter()
        .map(IntegrationActivationPlaybookStep::from_forecast)
        .collect::<Vec<_>>();
    steps.sort_by(compare_activation_playbook_steps);
    for (index, step) in steps.iter_mut().enumerate() {
        step.sequence = index + 1;
    }
    steps
}

pub fn activation_playbook_steps_from_timeline_milestones(
    milestones: Vec<IntegrationActivationTimelineMilestone>,
) -> Vec<IntegrationActivationPlaybookStep> {
    activation_playbook_steps_from_forecasts(activation_forecasts_from_timeline_milestones(
        milestones,
    ))
}

pub fn activation_playbook_steps_from_dashboard_cards(
    cards: Vec<IntegrationActivationDashboardCard>,
) -> Vec<IntegrationActivationPlaybookStep> {
    activation_playbook_steps_from_forecasts(activation_forecasts_from_dashboard_cards(cards))
}

pub fn activation_playbook_steps_from_readouts<'a>(
    readouts: impl IntoIterator<Item = &'a IntegrationActivationReadoutStage>,
) -> Vec<IntegrationActivationPlaybookStep> {
    activation_playbook_steps_from_dashboard_cards(activation_dashboard_cards_from_readouts(
        readouts,
    ))
}

pub fn activation_playbook_steps_from_candidates(
    catalog: &[IntegrationCatalogEntry],
    candidates: Vec<IntegrationActivationCandidate>,
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationPlaybookStep> {
    activation_playbook_steps_from_forecasts(activation_forecasts_from_candidates(
        catalog,
        candidates,
        enabled_integrations,
    ))
}

pub fn activation_playbook_steps_at_or_before_priority(
    catalog: &[IntegrationCatalogEntry],
    priority: u8,
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationPlaybookStep> {
    activation_playbook_steps_from_forecasts(activation_forecasts_at_or_before_priority(
        catalog,
        priority,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    ))
}

fn activation_audit_record_matches_integration_ids(
    record: &IntegrationActivationAuditRecord,
    integration_ids: &[IntegrationId],
) -> bool {
    integration_ids_overlap(&record.integration_ids, integration_ids)
}

fn integration_ids_overlap(left: &[IntegrationId], right: &[IntegrationId]) -> bool {
    left.iter()
        .any(|left_id| right.iter().any(|right_id| left_id == right_id))
}

fn integration_id_in_slice(integration_ids: &[IntegrationId], needle: &IntegrationId) -> bool {
    integration_ids
        .iter()
        .any(|integration_id| integration_id == needle)
}

pub fn activation_runbook_entries_from_playbook_steps(
    mut steps: Vec<IntegrationActivationPlaybookStep>,
    audit_records: &[IntegrationActivationAuditRecord],
) -> Vec<IntegrationActivationRunbookEntry> {
    steps.sort_by(compare_activation_playbook_steps);
    let mut entries = steps
        .into_iter()
        .map(|step| {
            let matching_audit_records = audit_records
                .iter()
                .filter(|record| {
                    activation_audit_record_matches_integration_ids(record, &step.integration_ids)
                })
                .collect::<Vec<_>>();
            IntegrationActivationRunbookEntry::from_playbook_step(step, &matching_audit_records)
        })
        .collect::<Vec<_>>();
    entries.sort_by(compare_activation_runbook_entries);
    for (index, entry) in entries.iter_mut().enumerate() {
        entry.sequence = index + 1;
    }
    entries
}

pub fn activation_runbook_entries_at_or_before_priority(
    catalog: &[IntegrationCatalogEntry],
    priority: u8,
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationRunbookEntry> {
    let steps = activation_playbook_steps_at_or_before_priority(
        catalog,
        priority,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    let audit_records = activation_audit_records_at_or_before_priority(
        catalog,
        priority,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    activation_runbook_entries_from_playbook_steps(steps, &audit_records)
}

pub fn activation_handoff_packages_from_runbook_entries<'a>(
    entries: impl IntoIterator<Item = &'a IntegrationActivationRunbookEntry>,
    risks: &[IntegrationActivationRiskItem],
    graph: &IntegrationActivationDependencyGraph,
    gap_inventory: &IntegrationReadinessGapInventory,
) -> Vec<IntegrationActivationHandoffPackage> {
    let mut packages = entries
        .into_iter()
        .map(|entry| {
            IntegrationActivationHandoffPackage::from_runbook_entry(
                entry,
                risks,
                graph,
                gap_inventory,
            )
        })
        .collect::<Vec<_>>();
    packages.sort_by(compare_activation_handoff_packages);
    for (index, package) in packages.iter_mut().enumerate() {
        package.sequence = index + 1;
    }
    packages
}

pub fn activation_handoff_packages_at_or_before_priority(
    catalog: &[IntegrationCatalogEntry],
    priority: u8,
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationHandoffPackage> {
    let reports = readiness_reports_at_or_before_priority(
        catalog,
        priority,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    let candidates = activation_candidates_from_reports(reports.iter());
    let risks = activation_risk_from_candidates(catalog, candidates.iter());
    let graph =
        activation_dependency_graph_from_reports(catalog, reports.iter(), enabled_integrations);
    let gap_inventory = readiness_gap_inventory_from_reports(reports.iter());
    let entries = activation_runbook_entries_at_or_before_priority(
        catalog,
        priority,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    activation_handoff_packages_from_runbook_entries(entries.iter(), &risks, &graph, &gap_inventory)
}

pub fn activation_operator_tasks_from_playbook_steps(
    mut steps: Vec<IntegrationActivationPlaybookStep>,
) -> Vec<IntegrationActivationOperatorTask> {
    steps.sort_by(compare_activation_playbook_steps);
    let mut tasks = steps
        .into_iter()
        .map(IntegrationActivationOperatorTask::from_playbook_step)
        .collect::<Vec<_>>();
    tasks.sort_by(compare_activation_operator_tasks);
    for (index, task) in tasks.iter_mut().enumerate() {
        task.sequence = index + 1;
    }
    tasks
}

pub fn activation_operator_tasks_from_forecasts(
    forecasts: Vec<IntegrationActivationForecastItem>,
) -> Vec<IntegrationActivationOperatorTask> {
    activation_operator_tasks_from_playbook_steps(activation_playbook_steps_from_forecasts(
        forecasts,
    ))
}

pub fn activation_operator_tasks_from_timeline_milestones(
    milestones: Vec<IntegrationActivationTimelineMilestone>,
) -> Vec<IntegrationActivationOperatorTask> {
    activation_operator_tasks_from_playbook_steps(
        activation_playbook_steps_from_timeline_milestones(milestones),
    )
}

pub fn activation_operator_tasks_from_dashboard_cards(
    cards: Vec<IntegrationActivationDashboardCard>,
) -> Vec<IntegrationActivationOperatorTask> {
    activation_operator_tasks_from_playbook_steps(activation_playbook_steps_from_dashboard_cards(
        cards,
    ))
}

pub fn activation_operator_tasks_from_readouts<'a>(
    readouts: impl IntoIterator<Item = &'a IntegrationActivationReadoutStage>,
) -> Vec<IntegrationActivationOperatorTask> {
    activation_operator_tasks_from_playbook_steps(activation_playbook_steps_from_readouts(readouts))
}

pub fn activation_operator_tasks_from_candidates(
    catalog: &[IntegrationCatalogEntry],
    candidates: Vec<IntegrationActivationCandidate>,
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationOperatorTask> {
    activation_operator_tasks_from_playbook_steps(activation_playbook_steps_from_candidates(
        catalog,
        candidates,
        enabled_integrations,
    ))
}

pub fn activation_operator_tasks_at_or_before_priority(
    catalog: &[IntegrationCatalogEntry],
    priority: u8,
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationOperatorTask> {
    activation_operator_tasks_from_playbook_steps(activation_playbook_steps_at_or_before_priority(
        catalog,
        priority,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    ))
}

pub fn activation_execution_packets_from_handoff_packages(
    packages: Vec<IntegrationActivationHandoffPackage>,
    tasks: Vec<IntegrationActivationOperatorTask>,
) -> Vec<IntegrationActivationExecutionPacket> {
    let tasks_by_playbook_sequence = tasks
        .iter()
        .map(|task| (task.playbook_sequence, task))
        .collect::<BTreeMap<_, _>>();
    let mut packets = packages
        .into_iter()
        .map(|package| {
            let task = tasks_by_playbook_sequence
                .get(&package.runbook_sequence)
                .copied();
            IntegrationActivationExecutionPacket::from_handoff_package(package, task)
        })
        .collect::<Vec<_>>();
    packets.sort_by(compare_activation_execution_packets);
    for (index, packet) in packets.iter_mut().enumerate() {
        packet.sequence = index + 1;
    }
    packets
}

pub fn activation_execution_packets_at_or_before_priority(
    catalog: &[IntegrationCatalogEntry],
    priority: u8,
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationExecutionPacket> {
    let packages = activation_handoff_packages_at_or_before_priority(
        catalog,
        priority,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    let tasks = activation_operator_tasks_at_or_before_priority(
        catalog,
        priority,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    activation_execution_packets_from_handoff_packages(packages, tasks)
}

pub fn activation_verification_checkpoints_from_execution_packets(
    packets: Vec<IntegrationActivationExecutionPacket>,
) -> Vec<IntegrationActivationVerificationCheckpoint> {
    let mut checkpoints = packets
        .into_iter()
        .map(IntegrationActivationVerificationCheckpoint::from_execution_packet)
        .collect::<Vec<_>>();
    checkpoints.sort_by(compare_activation_verification_checkpoints);
    for (index, checkpoint) in checkpoints.iter_mut().enumerate() {
        checkpoint.sequence = index + 1;
    }
    checkpoints
}

pub fn activation_verification_checkpoints_at_or_before_priority(
    catalog: &[IntegrationCatalogEntry],
    priority: u8,
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationVerificationCheckpoint> {
    activation_verification_checkpoints_from_execution_packets(
        activation_execution_packets_at_or_before_priority(
            catalog,
            priority,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        ),
    )
}

pub fn activation_control_room_panels_from_operator_tasks(
    mut tasks: Vec<IntegrationActivationOperatorTask>,
) -> Vec<IntegrationActivationControlRoomPanel> {
    tasks.sort_by(compare_activation_operator_tasks);
    let mut tasks_by_view: BTreeMap<
        IntegrationActivationPlaybookView,
        Vec<IntegrationActivationOperatorTask>,
    > = BTreeMap::new();
    for task in tasks {
        tasks_by_view
            .entry(task.recommended_view)
            .or_default()
            .push(task);
    }

    let mut panels = tasks_by_view
        .into_iter()
        .map(|(view, tasks)| IntegrationActivationControlRoomPanel::from_tasks(0, view, tasks))
        .collect::<Vec<_>>();
    panels.sort_by(compare_activation_control_room_panels);
    for (index, panel) in panels.iter_mut().enumerate() {
        panel.sequence = index + 1;
    }
    panels
}

pub fn activation_control_room_panels_from_playbook_steps(
    steps: Vec<IntegrationActivationPlaybookStep>,
) -> Vec<IntegrationActivationControlRoomPanel> {
    activation_control_room_panels_from_operator_tasks(
        activation_operator_tasks_from_playbook_steps(steps),
    )
}

pub fn activation_control_room_panels_from_forecasts(
    forecasts: Vec<IntegrationActivationForecastItem>,
) -> Vec<IntegrationActivationControlRoomPanel> {
    activation_control_room_panels_from_operator_tasks(activation_operator_tasks_from_forecasts(
        forecasts,
    ))
}

pub fn activation_control_room_panels_from_timeline_milestones(
    milestones: Vec<IntegrationActivationTimelineMilestone>,
) -> Vec<IntegrationActivationControlRoomPanel> {
    activation_control_room_panels_from_operator_tasks(
        activation_operator_tasks_from_timeline_milestones(milestones),
    )
}

pub fn activation_control_room_panels_from_dashboard_cards(
    cards: Vec<IntegrationActivationDashboardCard>,
) -> Vec<IntegrationActivationControlRoomPanel> {
    activation_control_room_panels_from_operator_tasks(
        activation_operator_tasks_from_dashboard_cards(cards),
    )
}

pub fn activation_control_room_panels_from_readouts<'a>(
    readouts: impl IntoIterator<Item = &'a IntegrationActivationReadoutStage>,
) -> Vec<IntegrationActivationControlRoomPanel> {
    activation_control_room_panels_from_operator_tasks(activation_operator_tasks_from_readouts(
        readouts,
    ))
}

pub fn activation_control_room_panels_from_candidates(
    catalog: &[IntegrationCatalogEntry],
    candidates: Vec<IntegrationActivationCandidate>,
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationControlRoomPanel> {
    activation_control_room_panels_from_operator_tasks(activation_operator_tasks_from_candidates(
        catalog,
        candidates,
        enabled_integrations,
    ))
}

pub fn activation_control_room_panels_at_or_before_priority(
    catalog: &[IntegrationCatalogEntry],
    priority: u8,
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationControlRoomPanel> {
    activation_control_room_panels_from_operator_tasks(
        activation_operator_tasks_at_or_before_priority(
            catalog,
            priority,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        ),
    )
}

pub fn activation_command_center_sections_from_control_room_panels(
    mut panels: Vec<IntegrationActivationControlRoomPanel>,
) -> Vec<IntegrationActivationCommandCenterSection> {
    panels.sort_by(compare_activation_control_room_panels);
    let mut panels_by_section: BTreeMap<
        IntegrationActivationCommandCenterSectionKind,
        Vec<IntegrationActivationControlRoomPanel>,
    > = BTreeMap::new();
    for panel in panels {
        panels_by_section
            .entry(IntegrationActivationCommandCenterSectionKind::from_panel(
                &panel,
            ))
            .or_default()
            .push(panel);
    }

    let mut sections = panels_by_section
        .into_iter()
        .map(|(section_kind, panels)| {
            IntegrationActivationCommandCenterSection::from_panels(0, section_kind, panels)
        })
        .collect::<Vec<_>>();
    sections.sort_by(compare_activation_command_center_sections);
    for (index, section) in sections.iter_mut().enumerate() {
        section.sequence = index + 1;
    }
    sections
}

pub fn activation_command_center_sections_from_operator_tasks(
    tasks: Vec<IntegrationActivationOperatorTask>,
) -> Vec<IntegrationActivationCommandCenterSection> {
    activation_command_center_sections_from_control_room_panels(
        activation_control_room_panels_from_operator_tasks(tasks),
    )
}

pub fn activation_command_center_sections_from_playbook_steps(
    steps: Vec<IntegrationActivationPlaybookStep>,
) -> Vec<IntegrationActivationCommandCenterSection> {
    activation_command_center_sections_from_control_room_panels(
        activation_control_room_panels_from_playbook_steps(steps),
    )
}

pub fn activation_command_center_sections_from_forecasts(
    forecasts: Vec<IntegrationActivationForecastItem>,
) -> Vec<IntegrationActivationCommandCenterSection> {
    activation_command_center_sections_from_control_room_panels(
        activation_control_room_panels_from_forecasts(forecasts),
    )
}

pub fn activation_command_center_sections_from_timeline_milestones(
    milestones: Vec<IntegrationActivationTimelineMilestone>,
) -> Vec<IntegrationActivationCommandCenterSection> {
    activation_command_center_sections_from_control_room_panels(
        activation_control_room_panels_from_timeline_milestones(milestones),
    )
}

pub fn activation_command_center_sections_from_dashboard_cards(
    cards: Vec<IntegrationActivationDashboardCard>,
) -> Vec<IntegrationActivationCommandCenterSection> {
    activation_command_center_sections_from_control_room_panels(
        activation_control_room_panels_from_dashboard_cards(cards),
    )
}

pub fn activation_command_center_sections_from_readouts<'a>(
    readouts: impl IntoIterator<Item = &'a IntegrationActivationReadoutStage>,
) -> Vec<IntegrationActivationCommandCenterSection> {
    activation_command_center_sections_from_control_room_panels(
        activation_control_room_panels_from_readouts(readouts),
    )
}

pub fn activation_command_center_sections_from_candidates(
    catalog: &[IntegrationCatalogEntry],
    candidates: Vec<IntegrationActivationCandidate>,
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationCommandCenterSection> {
    activation_command_center_sections_from_control_room_panels(
        activation_control_room_panels_from_candidates(catalog, candidates, enabled_integrations),
    )
}

pub fn activation_command_center_sections_at_or_before_priority(
    catalog: &[IntegrationCatalogEntry],
    priority: u8,
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationCommandCenterSection> {
    activation_command_center_sections_from_control_room_panels(
        activation_control_room_panels_at_or_before_priority(
            catalog,
            priority,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        ),
    )
}

pub fn activation_watchtower_signals_from_command_center_sections(
    mut sections: Vec<IntegrationActivationCommandCenterSection>,
) -> Vec<IntegrationActivationWatchtowerSignal> {
    sections.sort_by(compare_activation_command_center_sections);
    let mut sections_by_signal: BTreeMap<
        IntegrationActivationWatchtowerSignalKind,
        Vec<IntegrationActivationCommandCenterSection>,
    > = BTreeMap::new();
    for section in sections {
        sections_by_signal
            .entry(IntegrationActivationWatchtowerSignalKind::from_section(
                &section,
            ))
            .or_default()
            .push(section);
    }

    let mut signals = sections_by_signal
        .into_iter()
        .map(|(signal_kind, sections)| {
            IntegrationActivationWatchtowerSignal::from_sections(0, signal_kind, sections)
        })
        .collect::<Vec<_>>();
    signals.sort_by(compare_activation_watchtower_signals);
    for (index, signal) in signals.iter_mut().enumerate() {
        signal.sequence = index + 1;
    }
    signals
}

pub fn activation_watchtower_signals_from_control_room_panels(
    panels: Vec<IntegrationActivationControlRoomPanel>,
) -> Vec<IntegrationActivationWatchtowerSignal> {
    activation_watchtower_signals_from_command_center_sections(
        activation_command_center_sections_from_control_room_panels(panels),
    )
}

pub fn activation_watchtower_signals_from_operator_tasks(
    tasks: Vec<IntegrationActivationOperatorTask>,
) -> Vec<IntegrationActivationWatchtowerSignal> {
    activation_watchtower_signals_from_command_center_sections(
        activation_command_center_sections_from_operator_tasks(tasks),
    )
}

pub fn activation_watchtower_signals_from_playbook_steps(
    steps: Vec<IntegrationActivationPlaybookStep>,
) -> Vec<IntegrationActivationWatchtowerSignal> {
    activation_watchtower_signals_from_command_center_sections(
        activation_command_center_sections_from_playbook_steps(steps),
    )
}

pub fn activation_watchtower_signals_from_forecasts(
    forecasts: Vec<IntegrationActivationForecastItem>,
) -> Vec<IntegrationActivationWatchtowerSignal> {
    activation_watchtower_signals_from_command_center_sections(
        activation_command_center_sections_from_forecasts(forecasts),
    )
}

pub fn activation_watchtower_signals_from_timeline_milestones(
    milestones: Vec<IntegrationActivationTimelineMilestone>,
) -> Vec<IntegrationActivationWatchtowerSignal> {
    activation_watchtower_signals_from_command_center_sections(
        activation_command_center_sections_from_timeline_milestones(milestones),
    )
}

pub fn activation_watchtower_signals_from_dashboard_cards(
    cards: Vec<IntegrationActivationDashboardCard>,
) -> Vec<IntegrationActivationWatchtowerSignal> {
    activation_watchtower_signals_from_command_center_sections(
        activation_command_center_sections_from_dashboard_cards(cards),
    )
}

pub fn activation_watchtower_signals_from_readouts<'a>(
    readouts: impl IntoIterator<Item = &'a IntegrationActivationReadoutStage>,
) -> Vec<IntegrationActivationWatchtowerSignal> {
    activation_watchtower_signals_from_command_center_sections(
        activation_command_center_sections_from_readouts(readouts),
    )
}

pub fn activation_watchtower_signals_from_candidates(
    catalog: &[IntegrationCatalogEntry],
    candidates: Vec<IntegrationActivationCandidate>,
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationWatchtowerSignal> {
    activation_watchtower_signals_from_command_center_sections(
        activation_command_center_sections_from_candidates(
            catalog,
            candidates,
            enabled_integrations,
        ),
    )
}

pub fn activation_watchtower_signals_at_or_before_priority(
    catalog: &[IntegrationCatalogEntry],
    priority: u8,
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationWatchtowerSignal> {
    activation_watchtower_signals_from_command_center_sections(
        activation_command_center_sections_at_or_before_priority(
            catalog,
            priority,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        ),
    )
}

pub fn activation_sentinel_alerts_from_rollups(
    signals: &[IntegrationActivationWatchtowerSignal],
    risks: &[IntegrationActivationRiskItem],
    graph: &IntegrationActivationDependencyGraph,
    gap_inventory: &IntegrationReadinessGapInventory,
) -> Vec<IntegrationActivationSentinelAlert> {
    let watchtower_summary = IntegrationActivationWatchtowerSummary::from_signals(signals.iter());
    let risk_summary = IntegrationActivationRiskSummary::from_risks(risks.iter());
    let dependency_summary = graph.summary.clone();
    let integration_ids =
        integration_ids_for_activation_sentinel_rollups(signals, risks, graph, gap_inventory);

    if watchtower_summary.is_empty()
        && risk_summary.is_empty()
        && graph.is_empty()
        && gap_inventory.is_empty()
    {
        return Vec::new();
    }

    let mut alerts = Vec::new();
    if watchtower_summary.has_blockers()
        || risk_summary.has_blockers()
        || dependency_summary.has_blocking_dependencies()
        || gap_inventory.has_gaps()
    {
        push_activation_sentinel_alert(
            &mut alerts,
            IntegrationActivationSentinelAlertKind::Blocker,
            priority_for_activation_sentinel_alert(
                IntegrationActivationSentinelAlertKind::Blocker,
                &watchtower_summary,
                &risk_summary,
                &dependency_summary,
                graph,
                gap_inventory,
                risks,
            ),
            &integration_ids,
            &watchtower_summary,
            &risk_summary,
            &dependency_summary,
            gap_inventory,
        );
    }

    if dependency_summary.has_dependency_edges() || gap_inventory.dependency_gap_count() > 0 {
        push_activation_sentinel_alert(
            &mut alerts,
            IntegrationActivationSentinelAlertKind::Dependency,
            priority_for_activation_sentinel_alert(
                IntegrationActivationSentinelAlertKind::Dependency,
                &watchtower_summary,
                &risk_summary,
                &dependency_summary,
                graph,
                gap_inventory,
                risks,
            ),
            &integration_ids,
            &watchtower_summary,
            &risk_summary,
            &dependency_summary,
            gap_inventory,
        );
    }

    if risk_summary.total_risks > 0 {
        push_activation_sentinel_alert(
            &mut alerts,
            IntegrationActivationSentinelAlertKind::PolicyRisk,
            priority_for_activation_sentinel_alert(
                IntegrationActivationSentinelAlertKind::PolicyRisk,
                &watchtower_summary,
                &risk_summary,
                &dependency_summary,
                graph,
                gap_inventory,
                risks,
            ),
            &integration_ids,
            &watchtower_summary,
            &risk_summary,
            &dependency_summary,
            gap_inventory,
        );
    }

    if watchtower_summary.has_review_work() || risk_summary.has_review_work() {
        push_activation_sentinel_alert(
            &mut alerts,
            IntegrationActivationSentinelAlertKind::Review,
            priority_for_activation_sentinel_alert(
                IntegrationActivationSentinelAlertKind::Review,
                &watchtower_summary,
                &risk_summary,
                &dependency_summary,
                graph,
                gap_inventory,
                risks,
            ),
            &integration_ids,
            &watchtower_summary,
            &risk_summary,
            &dependency_summary,
            gap_inventory,
        );
    }

    if watchtower_summary.has_activation_work()
        || risk_summary.has_ready_work()
        || gap_inventory.all_ready()
    {
        push_activation_sentinel_alert(
            &mut alerts,
            IntegrationActivationSentinelAlertKind::Ready,
            priority_for_activation_sentinel_alert(
                IntegrationActivationSentinelAlertKind::Ready,
                &watchtower_summary,
                &risk_summary,
                &dependency_summary,
                graph,
                gap_inventory,
                risks,
            ),
            &integration_ids,
            &watchtower_summary,
            &risk_summary,
            &dependency_summary,
            gap_inventory,
        );
    }

    if alerts.is_empty() {
        push_activation_sentinel_alert(
            &mut alerts,
            IntegrationActivationSentinelAlertKind::Observation,
            priority_for_activation_sentinel_alert(
                IntegrationActivationSentinelAlertKind::Observation,
                &watchtower_summary,
                &risk_summary,
                &dependency_summary,
                graph,
                gap_inventory,
                risks,
            ),
            &integration_ids,
            &watchtower_summary,
            &risk_summary,
            &dependency_summary,
            gap_inventory,
        );
    }

    alerts.sort_by(compare_activation_sentinel_alerts);
    for (index, alert) in alerts.iter_mut().enumerate() {
        alert.sequence = index + 1;
    }
    alerts
}

pub fn activation_sentinel_alerts_at_or_before_priority(
    catalog: &[IntegrationCatalogEntry],
    priority: u8,
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationSentinelAlert> {
    let reports = readiness_reports_at_or_before_priority(
        catalog,
        priority,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    let candidates = activation_candidates_from_reports(reports.iter());
    let risks = activation_risk_from_candidates(catalog, candidates.iter());
    let signals =
        activation_watchtower_signals_from_candidates(catalog, candidates, enabled_integrations);
    let graph =
        activation_dependency_graph_from_reports(catalog, reports.iter(), enabled_integrations);
    let gap_inventory = readiness_gap_inventory_from_reports(reports.iter());

    activation_sentinel_alerts_from_rollups(&signals, &risks, &graph, &gap_inventory)
}

pub fn activation_audit_records_from_rollups(
    alerts: &[IntegrationActivationSentinelAlert],
    signals: &[IntegrationActivationWatchtowerSignal],
    decisions: &[IntegrationActivationDecisionItem],
    evidence: &[IntegrationActivationEvidenceItem],
    risks: &[IntegrationActivationRiskItem],
    graph: &IntegrationActivationDependencyGraph,
    gap_inventory: &IntegrationReadinessGapInventory,
) -> Vec<IntegrationActivationAuditRecord> {
    let mut records = Vec::new();
    records.extend(
        alerts
            .iter()
            .map(IntegrationActivationAuditRecord::from_sentinel_alert),
    );
    records.extend(
        signals
            .iter()
            .map(IntegrationActivationAuditRecord::from_watchtower_signal),
    );
    records.extend(
        decisions
            .iter()
            .map(IntegrationActivationAuditRecord::from_decision),
    );
    records.extend(
        evidence
            .iter()
            .map(IntegrationActivationAuditRecord::from_evidence),
    );
    records.extend(
        risks
            .iter()
            .map(IntegrationActivationAuditRecord::from_risk),
    );
    records.extend(
        graph
            .edges
            .iter()
            .map(IntegrationActivationAuditRecord::from_dependency_edge),
    );
    records.extend(
        gap_inventory
            .primitive_gaps
            .iter()
            .map(IntegrationActivationAuditRecord::from_primitive_gap),
    );
    records.extend(
        gap_inventory
            .capability_gaps
            .iter()
            .map(IntegrationActivationAuditRecord::from_capability_gap),
    );
    records.extend(
        gap_inventory
            .dependency_gaps
            .iter()
            .map(IntegrationActivationAuditRecord::from_dependency_gap),
    );

    records.sort_by(compare_activation_audit_records);
    for (index, record) in records.iter_mut().enumerate() {
        record.sequence = index + 1;
    }
    records
}

pub fn activation_audit_records_at_or_before_priority(
    catalog: &[IntegrationCatalogEntry],
    priority: u8,
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationAuditRecord> {
    let reports = readiness_reports_at_or_before_priority(
        catalog,
        priority,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    let candidates = activation_candidates_from_reports(reports.iter());
    let risks = activation_risk_from_candidates(catalog, candidates.iter());
    let signals = activation_watchtower_signals_from_candidates(
        catalog,
        candidates.clone(),
        enabled_integrations,
    );
    let graph =
        activation_dependency_graph_from_reports(catalog, reports.iter(), enabled_integrations);
    let gap_inventory = readiness_gap_inventory_from_reports(reports.iter());
    let alerts = activation_sentinel_alerts_from_rollups(&signals, &risks, &graph, &gap_inventory);
    let decisions =
        activation_decisions_from_candidates(catalog, candidates.iter(), enabled_integrations);
    let evidence = activation_evidence_from_decisions(decisions.iter());

    activation_audit_records_from_rollups(
        &alerts,
        &signals,
        &decisions,
        &evidence,
        &risks,
        &graph,
        &gap_inventory,
    )
}

pub fn activation_escalation_cases_from_rollups(
    alerts: &[IntegrationActivationSentinelAlert],
    checkpoints: &[IntegrationActivationVerificationCheckpoint],
    audit_records: &[IntegrationActivationAuditRecord],
) -> Vec<IntegrationActivationEscalationCase> {
    let mut cases = Vec::new();
    cases.extend(
        alerts
            .iter()
            .filter(|alert| alert.requires_attention())
            .map(IntegrationActivationEscalationCase::from_sentinel_alert),
    );
    cases.extend(
        checkpoints
            .iter()
            .filter(|checkpoint| checkpoint.requires_attention() || checkpoint.can_verify())
            .map(IntegrationActivationEscalationCase::from_verification_checkpoint),
    );
    cases.extend(
        audit_records
            .iter()
            .filter(|record| record.requires_attention())
            .map(IntegrationActivationEscalationCase::from_audit_record),
    );

    cases.sort_by(compare_activation_escalation_cases);
    for (index, case) in cases.iter_mut().enumerate() {
        case.sequence = index + 1;
    }
    cases
}

pub fn activation_escalation_cases_at_or_before_priority(
    catalog: &[IntegrationCatalogEntry],
    priority: u8,
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationEscalationCase> {
    let alerts = activation_sentinel_alerts_at_or_before_priority(
        catalog,
        priority,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    let checkpoints = activation_verification_checkpoints_at_or_before_priority(
        catalog,
        priority,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    let audit_records = activation_audit_records_at_or_before_priority(
        catalog,
        priority,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    activation_escalation_cases_from_rollups(&alerts, &checkpoints, &audit_records)
}

pub fn activation_response_items_from_escalation_cases(
    cases: &[IntegrationActivationEscalationCase],
) -> Vec<IntegrationActivationResponseItem> {
    let mut responses: Vec<_> = cases
        .iter()
        .filter(|case| case.requires_attention() || case.ready_to_verify())
        .map(IntegrationActivationResponseItem::from_escalation_case)
        .collect();

    responses.sort_by(compare_activation_response_items);
    for (index, response) in responses.iter_mut().enumerate() {
        response.sequence = index + 1;
    }
    responses
}

pub fn activation_response_items_at_or_before_priority(
    catalog: &[IntegrationCatalogEntry],
    priority: u8,
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationResponseItem> {
    let cases = activation_escalation_cases_at_or_before_priority(
        catalog,
        priority,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    activation_response_items_from_escalation_cases(&cases)
}

pub fn activation_remediation_items_from_responses(
    responses: &[IntegrationActivationResponseItem],
) -> Vec<IntegrationActivationRemediationItem> {
    let mut remediations: Vec<_> = responses
        .iter()
        .map(IntegrationActivationRemediationItem::from_response)
        .collect();

    remediations.sort_by(compare_activation_remediation_items);
    for (index, remediation) in remediations.iter_mut().enumerate() {
        remediation.sequence = index + 1;
    }
    remediations
}

pub fn activation_remediation_items_at_or_before_priority(
    catalog: &[IntegrationCatalogEntry],
    priority: u8,
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationRemediationItem> {
    let responses = activation_response_items_at_or_before_priority(
        catalog,
        priority,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    activation_remediation_items_from_responses(&responses)
}

pub fn activation_closure_gates_from_remediations(
    remediations: &[IntegrationActivationRemediationItem],
) -> Vec<IntegrationActivationClosureGate> {
    let mut gates: Vec<_> = remediations
        .iter()
        .map(IntegrationActivationClosureGate::from_remediation)
        .collect();

    gates.sort_by(compare_activation_closure_gates);
    for (index, gate) in gates.iter_mut().enumerate() {
        gate.sequence = index + 1;
    }
    gates
}

pub fn activation_closure_gates_at_or_before_priority(
    catalog: &[IntegrationCatalogEntry],
    priority: u8,
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationClosureGate> {
    let remediations = activation_remediation_items_at_or_before_priority(
        catalog,
        priority,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    activation_closure_gates_from_remediations(&remediations)
}

pub fn activation_release_packets_from_closure_gates(
    gates: &[IntegrationActivationClosureGate],
) -> Vec<IntegrationActivationReleasePacket> {
    let mut packets: Vec<_> = gates
        .iter()
        .map(IntegrationActivationReleasePacket::from_closure_gate)
        .collect();

    packets.sort_by(compare_activation_release_packets);
    for (index, packet) in packets.iter_mut().enumerate() {
        packet.sequence = index + 1;
    }
    packets
}

pub fn activation_release_packets_at_or_before_priority(
    catalog: &[IntegrationCatalogEntry],
    priority: u8,
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationReleasePacket> {
    let gates = activation_closure_gates_at_or_before_priority(
        catalog,
        priority,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    activation_release_packets_from_closure_gates(&gates)
}

pub fn activation_delivery_manifests_from_release_packets(
    packets: &[IntegrationActivationReleasePacket],
) -> Vec<IntegrationActivationDeliveryManifest> {
    let mut manifests: Vec<_> = packets
        .iter()
        .map(IntegrationActivationDeliveryManifest::from_release_packet)
        .collect();

    manifests.sort_by(compare_activation_delivery_manifests);
    for (index, manifest) in manifests.iter_mut().enumerate() {
        manifest.sequence = index + 1;
    }
    manifests
}

pub fn activation_delivery_manifests_at_or_before_priority(
    catalog: &[IntegrationCatalogEntry],
    priority: u8,
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationDeliveryManifest> {
    let packets = activation_release_packets_at_or_before_priority(
        catalog,
        priority,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    activation_delivery_manifests_from_release_packets(&packets)
}

pub fn activation_deployment_records_from_delivery_manifests(
    manifests: &[IntegrationActivationDeliveryManifest],
) -> Vec<IntegrationActivationDeploymentRecord> {
    let mut records: Vec<_> = manifests
        .iter()
        .map(IntegrationActivationDeploymentRecord::from_delivery_manifest)
        .collect();

    records.sort_by(compare_activation_deployment_records);
    for (index, record) in records.iter_mut().enumerate() {
        record.sequence = index + 1;
    }
    records
}

pub fn activation_deployment_records_at_or_before_priority(
    catalog: &[IntegrationCatalogEntry],
    priority: u8,
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationDeploymentRecord> {
    let manifests = activation_delivery_manifests_at_or_before_priority(
        catalog,
        priority,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    activation_deployment_records_from_delivery_manifests(&manifests)
}

pub fn activation_safety_gates_from_deployment_records(
    records: &[IntegrationActivationDeploymentRecord],
) -> Vec<IntegrationActivationSafetyGate> {
    let mut gates: Vec<_> = records
        .iter()
        .map(IntegrationActivationSafetyGate::from_deployment_record)
        .collect();

    gates.sort_by(compare_activation_safety_gates);
    for (index, gate) in gates.iter_mut().enumerate() {
        gate.sequence = index + 1;
    }
    gates
}

pub fn activation_safety_gates_at_or_before_priority(
    catalog: &[IntegrationCatalogEntry],
    priority: u8,
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationSafetyGate> {
    let records = activation_deployment_records_at_or_before_priority(
        catalog,
        priority,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    activation_safety_gates_from_deployment_records(&records)
}

pub fn activation_rollback_plans_from_safety_gates(
    gates: &[IntegrationActivationSafetyGate],
) -> Vec<IntegrationActivationRollbackPlan> {
    let mut plans: Vec<_> = gates
        .iter()
        .map(IntegrationActivationRollbackPlan::from_safety_gate)
        .collect();

    plans.sort_by(compare_activation_rollback_plans);
    for (index, plan) in plans.iter_mut().enumerate() {
        plan.sequence = index + 1;
    }
    plans
}

pub fn activation_rollback_plans_at_or_before_priority(
    catalog: &[IntegrationCatalogEntry],
    priority: u8,
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationRollbackPlan> {
    let gates = activation_safety_gates_at_or_before_priority(
        catalog,
        priority,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    activation_rollback_plans_from_safety_gates(&gates)
}

pub fn activation_observability_probes_from_rollups(
    signals: &[IntegrationActivationWatchtowerSignal],
    rollback_plans: &[IntegrationActivationRollbackPlan],
) -> Vec<IntegrationActivationObservabilityProbe> {
    let mut probes = rollback_plans
        .iter()
        .map(|plan| {
            let matching_signals = signals
                .iter()
                .filter(|signal| {
                    integration_ids_overlap(&signal.integration_ids, &plan.integration_ids)
                })
                .collect::<Vec<_>>();
            IntegrationActivationObservabilityProbe::from_rollups(plan, &matching_signals)
        })
        .collect::<Vec<_>>();

    probes.sort_by(compare_activation_observability_probes);
    for (index, probe) in probes.iter_mut().enumerate() {
        probe.sequence = index + 1;
    }
    probes
}

pub fn activation_observability_probes_at_or_before_priority(
    catalog: &[IntegrationCatalogEntry],
    priority: u8,
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationObservabilityProbe> {
    let signals = activation_watchtower_signals_at_or_before_priority(
        catalog,
        priority,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    let rollback_plans = activation_rollback_plans_at_or_before_priority(
        catalog,
        priority,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    activation_observability_probes_from_rollups(&signals, &rollback_plans)
}

pub fn activation_incident_briefs_from_observability_probes(
    probes: &[IntegrationActivationObservabilityProbe],
) -> Vec<IntegrationActivationIncidentBrief> {
    let mut briefs = probes
        .iter()
        .map(IntegrationActivationIncidentBrief::from_observability_probe)
        .collect::<Vec<_>>();

    briefs.sort_by(compare_activation_incident_briefs);
    for (index, brief) in briefs.iter_mut().enumerate() {
        brief.sequence = index + 1;
    }
    briefs
}

pub fn activation_incident_briefs_at_or_before_priority(
    catalog: &[IntegrationCatalogEntry],
    priority: u8,
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationIncidentBrief> {
    let probes = activation_observability_probes_at_or_before_priority(
        catalog,
        priority,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    activation_incident_briefs_from_observability_probes(&probes)
}

pub fn activation_guardrail_checks_from_rollups(
    incidents: &[IntegrationActivationIncidentBrief],
    risks: &[IntegrationActivationRiskItem],
    graph: &IntegrationActivationDependencyGraph,
    gap_inventory: &IntegrationReadinessGapInventory,
) -> Vec<IntegrationActivationGuardrailCheck> {
    let mut checks = Vec::new();
    checks.extend(
        incidents
            .iter()
            .map(IntegrationActivationGuardrailCheck::from_incident),
    );
    checks.extend(
        risks
            .iter()
            .map(IntegrationActivationGuardrailCheck::from_risk),
    );
    checks.extend(
        graph
            .edges
            .iter()
            .map(IntegrationActivationGuardrailCheck::from_dependency_edge),
    );
    checks.extend(
        gap_inventory
            .primitive_gaps
            .iter()
            .map(IntegrationActivationGuardrailCheck::from_primitive_gap),
    );
    checks.extend(
        gap_inventory
            .capability_gaps
            .iter()
            .map(IntegrationActivationGuardrailCheck::from_capability_gap),
    );
    checks.extend(
        gap_inventory
            .dependency_gaps
            .iter()
            .map(IntegrationActivationGuardrailCheck::from_dependency_gap),
    );

    checks.sort_by(compare_activation_guardrail_checks);
    for (index, check) in checks.iter_mut().enumerate() {
        check.sequence = index + 1;
    }
    checks
}

pub fn activation_guardrail_checks_at_or_before_priority(
    catalog: &[IntegrationCatalogEntry],
    priority: u8,
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationActivationGuardrailCheck> {
    let reports = readiness_reports_at_or_before_priority(
        catalog,
        priority,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    let candidates = activation_candidates_from_reports(reports.iter());
    let risks = activation_risk_from_candidates(catalog, candidates.iter());
    let graph =
        activation_dependency_graph_from_reports(catalog, reports.iter(), enabled_integrations);
    let gap_inventory = readiness_gap_inventory_from_reports(reports.iter());
    let incidents = activation_incident_briefs_at_or_before_priority(
        catalog,
        priority,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );

    activation_guardrail_checks_from_rollups(&incidents, &risks, &graph, &gap_inventory)
}

pub fn activation_dependency_graph_from_reports<'a>(
    catalog: &[IntegrationCatalogEntry],
    reports: impl IntoIterator<Item = &'a IntegrationReadinessReport>,
    enabled_integrations: &[IntegrationId],
) -> IntegrationActivationDependencyGraph {
    let reports = reports.into_iter().cloned().collect::<Vec<_>>();
    let mut dependent_ids_by_dependency: BTreeMap<IntegrationId, BTreeSet<IntegrationId>> =
        BTreeMap::new();
    let mut edges = Vec::new();

    for report in &reports {
        for dependency_id in activation_dependency_ids_for_report(catalog, report) {
            dependent_ids_by_dependency
                .entry(dependency_id.clone())
                .or_default()
                .insert(report.requested_integration_id.clone());
            let dependency = find_entry(catalog, &dependency_id);
            let satisfied = enabled_integrations
                .iter()
                .any(|enabled| enabled == &dependency_id);
            edges.push(IntegrationActivationDependencyEdge {
                dependency_integration_id: dependency_id,
                dependent_integration_id: report.requested_integration_id.clone(),
                dependency_display_name: dependency.map(|entry| entry.display_name.clone()),
                dependent_display_name: report.display_name.clone(),
                dependency_priority: dependency.map(|entry| entry.priority),
                dependent_priority: report.priority,
                satisfied,
                blocks_activation: !satisfied,
            });
        }
    }

    let mut nodes = reports
        .iter()
        .map(|report| {
            let integration_id = report.requested_integration_id.clone();
            let mut depends_on_integrations = activation_dependency_ids_for_report(catalog, report);
            depends_on_integrations.sort();
            depends_on_integrations.dedup();
            let dependent_integration_ids = dependent_ids_by_dependency
                .remove(&integration_id)
                .map(|ids| ids.into_iter().collect())
                .unwrap_or_default();
            let enabled = enabled_integrations
                .iter()
                .any(|enabled| enabled == &integration_id);

            IntegrationActivationDependencyNode {
                integration_id,
                display_name: report.display_name.clone(),
                priority: report.priority,
                activation_target: report.activation_target.clone(),
                depends_on_integrations,
                dependent_integration_ids,
                missing_dependencies: report.missing_dependencies.clone(),
                enabled,
                activation_ready: report.activation_ready(),
                requires_human_review: report.requires_human_review,
                highest_policy_tier: report.highest_policy_tier,
            }
        })
        .collect::<Vec<_>>();

    nodes.sort_by(compare_activation_dependency_nodes);
    edges.sort_by(compare_activation_dependency_edges);
    let summary = IntegrationActivationDependencySummary::from_graph(&nodes, &edges);

    IntegrationActivationDependencyGraph {
        nodes,
        edges,
        summary,
    }
}

pub fn activation_dependency_graph_at_or_before_priority(
    catalog: &[IntegrationCatalogEntry],
    priority: u8,
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationActivationDependencyGraph {
    let reports = readiness_reports_at_or_before_priority(
        catalog,
        priority,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    activation_dependency_graph_from_reports(catalog, reports.iter(), enabled_integrations)
}

pub fn readiness_gap_inventory_from_reports<'a>(
    reports: impl IntoIterator<Item = &'a IntegrationReadinessReport>,
) -> IntegrationReadinessGapInventory {
    let mut total_reports = 0;
    let mut activation_ready_reports = 0;
    let mut blocked_reports = 0;
    let mut primitive_gaps: BTreeMap<PrimitiveFamily, (u8, BTreeSet<IntegrationId>)> =
        BTreeMap::new();
    let mut capability_gaps: BTreeMap<CapabilityId, (u8, BTreeSet<IntegrationId>)> =
        BTreeMap::new();
    let mut dependency_gaps: BTreeMap<IntegrationId, (u8, BTreeSet<IntegrationId>)> =
        BTreeMap::new();

    for report in reports {
        total_reports += 1;
        if report.activation_ready() {
            activation_ready_reports += 1;
        } else {
            blocked_reports += 1;
        }

        for primitive in &report.missing_primitives {
            let (highest_priority, integration_ids) = primitive_gaps
                .entry(*primitive)
                .or_insert((report.priority, BTreeSet::new()));
            *highest_priority = (*highest_priority).min(report.priority);
            integration_ids.insert(report.requested_integration_id.clone());
        }
        for capability_id in &report.missing_capabilities {
            let (highest_priority, integration_ids) = capability_gaps
                .entry(capability_id.clone())
                .or_insert((report.priority, BTreeSet::new()));
            *highest_priority = (*highest_priority).min(report.priority);
            integration_ids.insert(report.requested_integration_id.clone());
        }
        for integration_id in &report.missing_dependencies {
            let (highest_priority, requested_integration_ids) = dependency_gaps
                .entry(integration_id.clone())
                .or_insert((report.priority, BTreeSet::new()));
            *highest_priority = (*highest_priority).min(report.priority);
            requested_integration_ids.insert(report.requested_integration_id.clone());
        }
    }

    let mut primitive_gaps = primitive_gaps
        .into_iter()
        .map(
            |(primitive, (highest_priority, integration_ids))| IntegrationReadinessPrimitiveGap {
                primitive,
                highest_priority,
                blocked_report_count: integration_ids.len(),
                integration_ids: integration_ids.into_iter().collect(),
            },
        )
        .collect::<Vec<_>>();
    primitive_gaps.sort_by(|left, right| {
        left.highest_priority
            .cmp(&right.highest_priority)
            .then_with(|| right.blocked_report_count.cmp(&left.blocked_report_count))
            .then_with(|| left.primitive.cmp(&right.primitive))
    });

    let mut capability_gaps = capability_gaps
        .into_iter()
        .map(|(capability_id, (highest_priority, integration_ids))| {
            IntegrationReadinessCapabilityGap {
                capability_id,
                highest_priority,
                blocked_report_count: integration_ids.len(),
                integration_ids: integration_ids.into_iter().collect(),
            }
        })
        .collect::<Vec<_>>();
    capability_gaps.sort_by(|left, right| {
        left.highest_priority
            .cmp(&right.highest_priority)
            .then_with(|| right.blocked_report_count.cmp(&left.blocked_report_count))
            .then_with(|| left.capability_id.cmp(&right.capability_id))
    });

    let mut dependency_gaps = dependency_gaps
        .into_iter()
        .map(
            |(integration_id, (highest_priority, requested_integration_ids))| {
                IntegrationReadinessDependencyGap {
                    integration_id,
                    highest_priority,
                    blocked_report_count: requested_integration_ids.len(),
                    requested_integration_ids: requested_integration_ids.into_iter().collect(),
                }
            },
        )
        .collect::<Vec<_>>();
    dependency_gaps.sort_by(|left, right| {
        left.highest_priority
            .cmp(&right.highest_priority)
            .then_with(|| right.blocked_report_count.cmp(&left.blocked_report_count))
            .then_with(|| left.integration_id.cmp(&right.integration_id))
    });

    IntegrationReadinessGapInventory {
        total_reports,
        activation_ready_reports,
        blocked_reports,
        primitive_gaps,
        capability_gaps,
        dependency_gaps,
    }
}

pub fn readiness_report_for_plan(
    plan: &IntegrationActivationPlan,
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationReadinessReport {
    IntegrationReadinessReport {
        requested_integration_id: plan.requested_integration_id.clone(),
        display_name: plan.display_name.clone(),
        activation_target: plan.activation_target.clone(),
        priority: plan.priority,
        missing_primitives: missing_primitives(&plan.required_primitives, available_primitives),
        missing_capabilities: missing_capabilities(
            &plan.required_capabilities,
            allowed_capabilities,
        ),
        missing_dependencies: missing_dependencies_for_plan(plan, enabled_integrations),
        requires_human_review: plan.requires_human_review(),
        highest_policy_tier: plan.highest_policy_tier,
        local_only: plan.local_only,
        cloud_required: plan.cloud_required,
    }
}

pub fn activation_plan_for_entry(entry: &IntegrationCatalogEntry) -> IntegrationActivationPlan {
    let activation_target = if let Some(target) = &entry.virtual_target {
        IntegrationActivationTarget::DelegatedIntegration(target.clone())
    } else if !entry.virtual_iot_standards.is_empty() {
        IntegrationActivationTarget::DelegatedStandards(entry.virtual_iot_standards.clone())
    } else {
        IntegrationActivationTarget::Direct
    };
    let policy_surfaces = entry.policy_surfaces();
    let highest_policy_tier = policy_surfaces
        .iter()
        .map(|surface| surface.required_tier())
        .max()
        .unwrap_or(PrivilegeTier::ReadOnly);

    IntegrationActivationPlan {
        requested_integration_id: entry.integration_id.clone(),
        display_name: entry.display_name.clone(),
        activation_target,
        implementation_status: entry.implementation_status,
        priority: entry.priority,
        runtime_kind: entry.runtime_kind,
        required_primitives: entry.required_primitives.clone(),
        required_capabilities: entry.required_capabilities.clone(),
        auth_modes: entry.auth_modes.clone(),
        discovery_mechanisms: entry.discovery_mechanisms.clone(),
        depends_on_integrations: entry.depends_on_integrations.clone(),
        policy_surfaces,
        highest_policy_tier,
        local_only: entry.is_local() && !entry.requires_cloud(),
        cloud_required: entry.requires_cloud()
            || entry
                .discovery_mechanisms
                .contains(&DiscoveryMechanism::CloudAccount),
    }
}

pub fn policy_surfaces_for_entry(entry: &IntegrationCatalogEntry) -> Vec<IntegrationPolicySurface> {
    let mut surfaces = Vec::new();

    if entry.required_capabilities.iter().any(is_local_actuator) {
        surfaces.push(IntegrationPolicySurface::LocalActuation);
    }
    if entry
        .required_capabilities
        .contains(&CapabilityId::trusted("smart_home.command.lock"))
        || entry.target_entity_kinds.contains(&EntityKind::Lock)
    {
        surfaces.push(IntegrationPolicySurface::EntryAccess);
    }
    if entry
        .required_capabilities
        .contains(&CapabilityId::trusted("smart_home.command.climate"))
        || entry.target_entity_kinds.contains(&EntityKind::Thermostat)
    {
        surfaces.push(IntegrationPolicySurface::ClimateControl);
    }
    if entry.category == IntegrationCategory::CameraMedia
        || entry
            .required_primitives
            .contains(&PrimitiveFamily::CameraMedia)
        || entry
            .required_capabilities
            .contains(&CapabilityId::trusted("smart_home.command.camera"))
    {
        surfaces.push(IntegrationPolicySurface::CameraMedia);
    }
    if entry.category == IntegrationCategory::EnergyClimate
        || entry
            .required_primitives
            .contains(&PrimitiveFamily::EnergyTelemetry)
        || entry
            .required_capabilities
            .contains(&CapabilityId::trusted("smart_home.command.energy"))
    {
        surfaces.push(IntegrationPolicySurface::EnergyManagement);
    }
    if entry.auth_modes.iter().any(requires_secret_lease) {
        surfaces.push(IntegrationPolicySurface::CredentialLease);
    }
    if entry.connectivity.requires_cloud()
        || entry
            .discovery_mechanisms
            .contains(&DiscoveryMechanism::CloudAccount)
    {
        surfaces.push(IntegrationPolicySurface::CredentialedCloud);
    }
    if entry
        .required_capabilities
        .contains(&CapabilityId::trusted("smart_home.manage_network"))
        || entry.auth_modes.contains(&AuthMode::RadioNetworkKey)
    {
        surfaces.push(IntegrationPolicySurface::RadioNetworkManagement);
    }
    if entry
        .required_capabilities
        .contains(&CapabilityId::trusted("smart_home.diagnostics"))
        || entry
            .target_entity_kinds
            .contains(&EntityKind::NetworkDiagnostic)
    {
        surfaces.push(IntegrationPolicySurface::NetworkInfrastructure);
    }

    dedupe_policy_surfaces(surfaces)
}

fn hue_entry() -> IntegrationCatalogEntry {
    base_entry(
        "hue",
        "Philips Hue",
        "Hue bridge integration with CLIP v2 resources, pairing, event stream, and command mapping.",
        IntegrationCategory::LocalHub,
        ConnectivityClass::LocalPush,
        ImplementationStatus::Scaffolded,
        0,
        "hue",
    )
    .with_capabilities(&[
        "smart_home.read",
        "smart_home.command.light",
        "smart_home.pair",
    ])
    .with_entities(&[
        EntityKind::Light,
        EntityKind::LightGroup,
        EntityKind::Scene,
        EntityKind::Sensor,
        EntityKind::Input,
    ])
    .with_protocols(vec![ProtocolFamily::Hue, ProtocolFamily::Zigbee])
    .with_discovery(&[DiscoveryMechanism::Mdns, DiscoveryMechanism::Manual])
    .with_auth(&[AuthMode::LocalPairing, AuthMode::LocalToken])
    .with_primitives(&[
        PrimitiveFamily::Mdns,
        PrimitiveFamily::LocalHttp,
        PrimitiveFamily::ServerSentEvents,
        PrimitiveFamily::LocalPairing,
        PrimitiveFamily::LocalToken,
        PrimitiveFamily::CommandMapping,
        PrimitiveFamily::VaultLease,
        PrimitiveFamily::Supervision,
        PrimitiveFamily::TestSimulator,
    ])
}

#[allow(clippy::too_many_arguments)]
fn protocol_entry(
    id: &'static str,
    name: &'static str,
    summary: &'static str,
    connectivity: ConnectivityClass,
    status: ImplementationStatus,
    priority: u8,
    protocol: ProtocolFamily,
    capabilities: &[&'static str],
    entities: &[EntityKind],
    discovery: &[DiscoveryMechanism],
    auth: &[AuthMode],
    ha_domain: &'static str,
) -> IntegrationCatalogEntry {
    base_entry(
        id,
        name,
        summary,
        IntegrationCategory::ProtocolStandard,
        connectivity,
        status,
        priority,
        ha_domain,
    )
    .with_protocols(vec![protocol.clone()])
    .with_capabilities(capabilities)
    .with_entities(entities)
    .with_discovery(discovery)
    .with_auth(auth)
    .with_primitives(protocol_primitives(&protocol))
}

#[allow(clippy::too_many_arguments)]
fn local_hub_entry(
    id: &'static str,
    name: &'static str,
    summary: &'static str,
    connectivity: ConnectivityClass,
    status: ImplementationStatus,
    priority: u8,
    capabilities: &[&'static str],
    entities: &[EntityKind],
    discovery: &[DiscoveryMechanism],
    auth: &[AuthMode],
    ha_domain: &'static str,
) -> IntegrationCatalogEntry {
    base_entry(
        id,
        name,
        summary,
        IntegrationCategory::LocalHub,
        connectivity,
        status,
        priority,
        ha_domain,
    )
    .with_capabilities(capabilities)
    .with_entities(entities)
    .with_discovery(discovery)
    .with_auth(auth)
    .with_primitives(&local_transport_primitives(connectivity, discovery, auth))
}

#[allow(clippy::too_many_arguments)]
fn local_device_entry(
    id: &'static str,
    name: &'static str,
    summary: &'static str,
    connectivity: ConnectivityClass,
    status: ImplementationStatus,
    priority: u8,
    capabilities: &[&'static str],
    entities: &[EntityKind],
    discovery: &[DiscoveryMechanism],
    auth: &[AuthMode],
    ha_domain: &'static str,
) -> IntegrationCatalogEntry {
    base_entry(
        id,
        name,
        summary,
        IntegrationCategory::LocalDevice,
        connectivity,
        status,
        priority,
        ha_domain,
    )
    .with_capabilities(capabilities)
    .with_entities(entities)
    .with_discovery(discovery)
    .with_auth(auth)
    .with_primitives(&local_transport_primitives(connectivity, discovery, auth))
}

#[allow(clippy::too_many_arguments)]
fn bluetooth_entry(
    id: &'static str,
    name: &'static str,
    summary: &'static str,
    status: ImplementationStatus,
    priority: u8,
    capabilities: &[&'static str],
    entities: &[EntityKind],
    ha_domain: &'static str,
) -> IntegrationCatalogEntry {
    base_entry(
        id,
        name,
        summary,
        IntegrationCategory::BluetoothProfile,
        ConnectivityClass::LocalPush,
        status,
        priority,
        ha_domain,
    )
    .with_capabilities(capabilities)
    .with_entities(entities)
    .with_discovery(&[DiscoveryMechanism::Bluetooth])
    .with_auth(&[AuthMode::None, AuthMode::LocalPairing])
    .with_primitives(&[
        PrimitiveFamily::BluetoothLowEnergy,
        PrimitiveFamily::LocalPairing,
        PrimitiveFamily::Supervision,
    ])
}

fn media_entry(
    id: &'static str,
    name: &'static str,
    summary: &'static str,
    connectivity: ConnectivityClass,
    status: ImplementationStatus,
    priority: u8,
    ha_domain: &'static str,
) -> IntegrationCatalogEntry {
    base_entry(
        id,
        name,
        summary,
        IntegrationCategory::CameraMedia,
        connectivity,
        status,
        priority,
        ha_domain,
    )
    .with_capabilities(&["smart_home.read", "smart_home.command.media"])
    .with_entities(&[EntityKind::Unknown])
    .with_discovery(&[
        DiscoveryMechanism::Mdns,
        DiscoveryMechanism::Ssdp,
        DiscoveryMechanism::Manual,
    ])
    .with_auth(&[AuthMode::None, AuthMode::LocalToken])
    .with_primitives(&[
        PrimitiveFamily::Mdns,
        PrimitiveFamily::Ssdp,
        PrimitiveFamily::LocalHttp,
        PrimitiveFamily::LocalToken,
        PrimitiveFamily::CommandMapping,
        PrimitiveFamily::Supervision,
    ])
}

fn camera_entry(
    id: &'static str,
    name: &'static str,
    summary: &'static str,
    connectivity: ConnectivityClass,
    status: ImplementationStatus,
    priority: u8,
    ha_domain: &'static str,
) -> IntegrationCatalogEntry {
    base_entry(
        id,
        name,
        summary,
        IntegrationCategory::CameraMedia,
        connectivity,
        status,
        priority,
        ha_domain,
    )
    .with_capabilities(&["smart_home.read", "smart_home.command.camera"])
    .with_entities(&[EntityKind::Unknown, EntityKind::Sensor])
    .with_discovery(&[DiscoveryMechanism::Mdns, DiscoveryMechanism::Manual])
    .with_auth(&[AuthMode::UsernamePassword, AuthMode::ApiKey])
    .with_primitives(&[
        PrimitiveFamily::Mdns,
        PrimitiveFamily::LocalHttp,
        PrimitiveFamily::CameraMedia,
        PrimitiveFamily::CapabilityPolicy,
        PrimitiveFamily::VaultLease,
        PrimitiveFamily::Supervision,
    ])
    .with_notes(&["Camera/media integrations require privacy-sensitive D21 policy."])
}

fn energy_entry(
    id: &'static str,
    name: &'static str,
    summary: &'static str,
    connectivity: ConnectivityClass,
    status: ImplementationStatus,
    priority: u8,
    ha_domain: &'static str,
) -> IntegrationCatalogEntry {
    base_entry(
        id,
        name,
        summary,
        IntegrationCategory::EnergyClimate,
        connectivity,
        status,
        priority,
        ha_domain,
    )
    .with_capabilities(&["smart_home.read", "smart_home.command.energy"])
    .with_entities(&[EntityKind::Sensor])
    .with_discovery(&[
        DiscoveryMechanism::Mdns,
        DiscoveryMechanism::Dhcp,
        DiscoveryMechanism::Manual,
    ])
    .with_auth(&[
        AuthMode::None,
        AuthMode::LocalToken,
        AuthMode::UsernamePassword,
    ])
    .with_primitives(&[
        PrimitiveFamily::Mdns,
        PrimitiveFamily::Dhcp,
        PrimitiveFamily::LocalHttp,
        PrimitiveFamily::EnergyTelemetry,
        PrimitiveFamily::VaultLease,
        PrimitiveFamily::Supervision,
    ])
}

#[allow(clippy::too_many_arguments)]
fn cloud_hub_entry(
    id: &'static str,
    name: &'static str,
    summary: &'static str,
    connectivity: ConnectivityClass,
    status: ImplementationStatus,
    priority: u8,
    capabilities: &[&'static str],
    entities: &[EntityKind],
    ha_domain: &'static str,
) -> IntegrationCatalogEntry {
    base_entry(
        id,
        name,
        summary,
        IntegrationCategory::CloudHub,
        connectivity,
        status,
        priority,
        ha_domain,
    )
    .with_capabilities(capabilities)
    .with_entities(entities)
    .with_discovery(&[
        DiscoveryMechanism::CloudAccount,
        DiscoveryMechanism::Webhook,
    ])
    .with_auth(&[AuthMode::OAuth2, AuthMode::ApiKey])
    .with_primitives(&[
        PrimitiveFamily::CloudApi,
        PrimitiveFamily::Webhook,
        PrimitiveFamily::OAuth2,
        PrimitiveFamily::CommandMapping,
        PrimitiveFamily::CapabilityPolicy,
        PrimitiveFamily::VaultLease,
        PrimitiveFamily::Supervision,
    ])
}

fn tasmota_entry() -> IntegrationCatalogEntry {
    base_entry(
        "tasmota",
        "Tasmota",
        "Tasmota MQTT push and native local HTTP device integration.",
        IntegrationCategory::LocalDevice,
        ConnectivityClass::LocalPush,
        ImplementationStatus::FirstPartyRuntime,
        1,
        "tasmota",
    )
    .with_capabilities(&[
        "smart_home.read",
        "smart_home.command.light",
        "smart_home.command.switch",
    ])
    .with_entities(&[EntityKind::Light, EntityKind::Switch, EntityKind::Sensor])
    .with_discovery(&[
        DiscoveryMechanism::Mqtt,
        DiscoveryMechanism::Mdns,
        DiscoveryMechanism::Manual,
    ])
    .with_auth(&[
        AuthMode::None,
        AuthMode::UsernamePassword,
        AuthMode::MqttCredentials,
    ])
    .with_dependencies(&["mqtt"])
    .with_protocols(vec![
        ProtocolFamily::Mqtt,
        ProtocolFamily::Vendor("tasmota_http".to_string()),
    ])
    .with_primitives(&[
        PrimitiveFamily::Mqtt,
        PrimitiveFamily::MqttCredentials,
        PrimitiveFamily::Mdns,
        PrimitiveFamily::LocalHttp,
        PrimitiveFamily::VaultLease,
        PrimitiveFamily::CommandMapping,
        PrimitiveFamily::CapabilityPolicy,
        PrimitiveFamily::Supervision,
    ])
}

fn virtual_alias(
    id: &'static str,
    name: &'static str,
    summary: &'static str,
    target: &'static str,
    priority: u8,
    ha_domain: &'static str,
) -> IntegrationCatalogEntry {
    base_entry(
        id,
        name,
        summary,
        IntegrationCategory::VirtualAlias,
        ConnectivityClass::Calculated,
        ImplementationStatus::DelegatedToStandard,
        priority,
        ha_domain,
    )
    .with_virtual_target(target)
    .with_primitives(&[
        PrimitiveFamily::NormalizedModel,
        PrimitiveFamily::DiscoveryIndex,
        PrimitiveFamily::CalculatedState,
    ])
    .with_notes(&["Virtual aliases route users and agents to an implementation entry."])
}

fn virtual_standard(
    id: &'static str,
    name: &'static str,
    summary: &'static str,
    standard: ProtocolFamily,
    priority: u8,
    ha_domain: &'static str,
) -> IntegrationCatalogEntry {
    base_entry(
        id,
        name,
        summary,
        IntegrationCategory::VirtualAlias,
        ConnectivityClass::Calculated,
        ImplementationStatus::DelegatedToStandard,
        priority,
        ha_domain,
    )
    .with_virtual_iot_standards(vec![standard])
    .with_primitives(&[
        PrimitiveFamily::NormalizedModel,
        PrimitiveFamily::DiscoveryIndex,
        PrimitiveFamily::CalculatedState,
    ])
    .with_notes(&["Virtual standard aliases route pairing through the protocol stack."])
}

#[allow(clippy::too_many_arguments)]
fn base_entry(
    id: &'static str,
    name: &'static str,
    summary: &'static str,
    category: IntegrationCategory,
    connectivity: ConnectivityClass,
    implementation_status: ImplementationStatus,
    priority: u8,
    ha_domain: &'static str,
) -> IntegrationCatalogEntry {
    IntegrationCatalogEntry {
        integration_id: IntegrationId::trusted(id),
        display_name: name.to_string(),
        summary: summary.to_string(),
        category,
        connectivity,
        runtime_kind: if category == IntegrationCategory::VirtualAlias {
            RuntimeKind::InProcessRust
        } else {
            RuntimeKind::RustWorkerProcess
        },
        implementation_status,
        priority,
        discovery_mechanisms: Vec::new(),
        auth_modes: Vec::new(),
        required_capabilities: vec![capability("smart_home.read")],
        target_entity_kinds: Vec::new(),
        supported_protocols: Vec::new(),
        depends_on_integrations: Vec::new(),
        virtual_target: None,
        virtual_iot_standards: Vec::new(),
        required_primitives: vec![
            PrimitiveFamily::NormalizedModel,
            PrimitiveFamily::DiscoveryIndex,
            PrimitiveFamily::CommandMapping,
            PrimitiveFamily::CapabilityPolicy,
            PrimitiveFamily::VaultLease,
            PrimitiveFamily::Supervision,
        ],
        source_refs: vec![SourceReference::home_assistant(ha_domain)],
        notes: Vec::new(),
    }
}

trait EntryBuilder {
    fn with_capabilities(self, capabilities: &[&'static str]) -> Self;
    fn with_entities(self, entities: &[EntityKind]) -> Self;
    fn with_discovery(self, discovery: &[DiscoveryMechanism]) -> Self;
    fn with_auth(self, auth_modes: &[AuthMode]) -> Self;
    fn with_protocols(self, protocols: Vec<ProtocolFamily>) -> Self;
    fn with_dependencies(self, integration_ids: &[&'static str]) -> Self;
    fn with_virtual_target(self, integration_id: &'static str) -> Self;
    fn with_virtual_iot_standards(self, standards: Vec<ProtocolFamily>) -> Self;
    fn with_primitives(self, primitives: &[PrimitiveFamily]) -> Self;
    fn with_notes(self, notes: &[&'static str]) -> Self;
}

impl EntryBuilder for IntegrationCatalogEntry {
    fn with_capabilities(mut self, capabilities: &[&'static str]) -> Self {
        self.required_capabilities = dedupe_capabilities(capabilities);
        self
    }

    fn with_entities(mut self, entities: &[EntityKind]) -> Self {
        self.target_entity_kinds = entities.to_vec();
        self
    }

    fn with_discovery(mut self, discovery: &[DiscoveryMechanism]) -> Self {
        self.discovery_mechanisms = discovery.to_vec();
        self
    }

    fn with_auth(mut self, auth_modes: &[AuthMode]) -> Self {
        self.auth_modes = auth_modes.to_vec();
        self
    }

    fn with_protocols(mut self, protocols: Vec<ProtocolFamily>) -> Self {
        self.supported_protocols = protocols;
        self
    }

    fn with_dependencies(mut self, integration_ids: &[&'static str]) -> Self {
        self.depends_on_integrations = integration_ids
            .iter()
            .map(|integration_id| IntegrationId::trusted(*integration_id))
            .collect();
        self
    }

    fn with_virtual_target(mut self, integration_id: &'static str) -> Self {
        self.virtual_target = Some(IntegrationId::trusted(integration_id));
        self
    }

    fn with_virtual_iot_standards(mut self, standards: Vec<ProtocolFamily>) -> Self {
        self.virtual_iot_standards = standards;
        self
    }

    fn with_primitives(mut self, primitives: &[PrimitiveFamily]) -> Self {
        for primitive in primitives {
            if !self.required_primitives.contains(primitive) {
                self.required_primitives.push(*primitive);
            }
        }
        self
    }

    fn with_notes(mut self, notes: &[&'static str]) -> Self {
        self.notes = notes.iter().map(|note| (*note).to_string()).collect();
        self
    }
}

fn protocol_primitives(protocol: &ProtocolFamily) -> &'static [PrimitiveFamily] {
    match protocol {
        ProtocolFamily::Hue => &[
            PrimitiveFamily::Mdns,
            PrimitiveFamily::LocalHttp,
            PrimitiveFamily::ServerSentEvents,
            PrimitiveFamily::LocalPairing,
        ],
        ProtocolFamily::Onvif => &[
            PrimitiveFamily::WsDiscovery,
            PrimitiveFamily::LocalHttp,
            PrimitiveFamily::CameraMedia,
            PrimitiveFamily::CapabilityPolicy,
            PrimitiveFamily::VaultLease,
            PrimitiveFamily::Supervision,
        ],
        ProtocolFamily::Zigbee => &[
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::RadioNetworkKey,
            PrimitiveFamily::Supervision,
        ],
        ProtocolFamily::ZWave => &[
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::ZWaveSerialApi,
            PrimitiveFamily::RadioNetworkKey,
            PrimitiveFamily::Supervision,
        ],
        ProtocolFamily::Thread => &[
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Mdns,
            PrimitiveFamily::RadioNetworkKey,
            PrimitiveFamily::Supervision,
        ],
        ProtocolFamily::Matter => &[
            PrimitiveFamily::Mdns,
            PrimitiveFamily::MatterCommissioning,
            PrimitiveFamily::CertificatePairing,
            PrimitiveFamily::LocalPairing,
            PrimitiveFamily::Supervision,
        ],
        ProtocolFamily::Mqtt => &[
            PrimitiveFamily::Mqtt,
            PrimitiveFamily::MqttCredentials,
            PrimitiveFamily::CommandMapping,
            PrimitiveFamily::Supervision,
        ],
        ProtocolFamily::Vendor(_) => &[PrimitiveFamily::LocalHttp, PrimitiveFamily::CommandMapping],
    }
}

pub fn low_level_mesh_protocols() -> Vec<ProtocolFamily> {
    vec![
        ProtocolFamily::Zigbee,
        ProtocolFamily::ZWave,
        ProtocolFamily::Thread,
    ]
}

pub fn mesh_protocol_primitive_readiness_rows(
    available_primitives: &[PrimitiveFamily],
) -> Vec<IntegrationMeshProtocolPrimitiveReadinessRow> {
    low_level_mesh_protocols()
        .into_iter()
        .filter_map(|protocol| {
            IntegrationMeshProtocolPrimitiveReadinessRow::from_protocol(
                protocol,
                available_primitives,
            )
        })
        .collect()
}

pub fn mesh_protocol_primitive_readiness_summary(
    available_primitives: &[PrimitiveFamily],
) -> IntegrationMeshProtocolPrimitiveReadinessSummary {
    let rows = mesh_protocol_primitive_readiness_rows(available_primitives);
    IntegrationMeshProtocolPrimitiveReadinessSummary::from_rows(rows.iter())
}

pub fn mesh_protocol_substrate_stage_rows(
    available_primitives: &[PrimitiveFamily],
) -> Vec<IntegrationMeshProtocolSubstrateStageRow> {
    let available_set = available_primitives
        .iter()
        .copied()
        .collect::<BTreeSet<_>>();
    let mut rows = Vec::new();

    for protocol in low_level_mesh_protocols() {
        for primitive in protocol_primitives(&protocol).iter().copied() {
            if let Some(stage) = mesh_protocol_substrate_stage(primitive) {
                rows.push(IntegrationMeshProtocolSubstrateStageRow {
                    protocol: protocol.clone(),
                    primitive,
                    stage,
                    available: available_set.contains(&primitive),
                });
            }
        }
    }

    rows
}

pub fn mesh_protocol_substrate_stage_summary(
    available_primitives: &[PrimitiveFamily],
) -> IntegrationMeshProtocolSubstrateStageSummary {
    let rows = mesh_protocol_substrate_stage_rows(available_primitives);
    IntegrationMeshProtocolSubstrateStageSummary::from_rows(rows.iter())
}

pub fn mesh_protocol_substrate_actions(
    available_primitives: &[PrimitiveFamily],
) -> Vec<IntegrationMeshProtocolSubstrateAction> {
    let rows = mesh_protocol_substrate_stage_rows(available_primitives);
    let mut actions = rows
        .iter()
        .filter_map(|row| IntegrationMeshProtocolSubstrateAction::from_stage_row(0, row))
        .collect::<Vec<_>>();
    actions.sort_by(compare_mesh_substrate_actions);

    for (index, action) in actions.iter_mut().enumerate() {
        action.sequence = index + 1;
    }

    actions
}

pub fn mesh_protocol_substrate_action_summary(
    available_primitives: &[PrimitiveFamily],
) -> IntegrationMeshProtocolSubstrateActionSummary {
    let actions = mesh_protocol_substrate_actions(available_primitives);
    IntegrationMeshProtocolSubstrateActionSummary::from_actions(actions.iter())
}

pub fn mesh_protocol_substrate_preflight_checks(
    available_primitives: &[PrimitiveFamily],
) -> Vec<IntegrationMeshProtocolSubstratePreflightCheck> {
    let rows = mesh_protocol_substrate_stage_rows(available_primitives);
    let mut checks = rows
        .iter()
        .map(|row| IntegrationMeshProtocolSubstratePreflightCheck::from_stage_row(0, row))
        .collect::<Vec<_>>();
    checks.sort_by(compare_mesh_substrate_preflight_checks);

    for (index, check) in checks.iter_mut().enumerate() {
        check.sequence = index + 1;
    }

    checks
}

pub fn mesh_protocol_substrate_preflight_summary(
    available_primitives: &[PrimitiveFamily],
) -> IntegrationMeshProtocolSubstratePreflightSummary {
    let checks = mesh_protocol_substrate_preflight_checks(available_primitives);
    IntegrationMeshProtocolSubstratePreflightSummary::from_checks(checks.iter())
}

pub fn mesh_protocol_substrate_preflight_actions(
    available_primitives: &[PrimitiveFamily],
) -> Vec<IntegrationMeshProtocolSubstratePreflightAction> {
    let checks = mesh_protocol_substrate_preflight_checks(available_primitives);
    let mut actions = checks
        .iter()
        .filter_map(|check| IntegrationMeshProtocolSubstratePreflightAction::from_check(0, check))
        .collect::<Vec<_>>();
    actions.sort_by(compare_mesh_substrate_preflight_actions);

    for (index, action) in actions.iter_mut().enumerate() {
        action.sequence = index + 1;
    }

    actions
}

pub fn mesh_protocol_substrate_preflight_action_summary(
    available_primitives: &[PrimitiveFamily],
) -> IntegrationMeshProtocolSubstratePreflightActionSummary {
    let actions = mesh_protocol_substrate_preflight_actions(available_primitives);
    IntegrationMeshProtocolSubstratePreflightActionSummary::from_actions(actions.iter())
}

pub fn mesh_protocol_substrate_preflight_repair_batches(
    available_primitives: &[PrimitiveFamily],
) -> Vec<IntegrationMeshProtocolSubstratePreflightRepairBatch> {
    let actions = mesh_protocol_substrate_preflight_actions(available_primitives);
    let mut grouped = BTreeMap::<
        (
            IntegrationMeshProtocolSubstrateStage,
            IntegrationMeshProtocolSubstratePreflightActionKind,
        ),
        Vec<&IntegrationMeshProtocolSubstratePreflightAction>,
    >::new();

    for action in &actions {
        grouped
            .entry((action.stage, action.action_kind))
            .or_default()
            .push(action);
    }

    let mut batches = grouped
        .into_iter()
        .map(|((stage, action_kind), actions)| {
            IntegrationMeshProtocolSubstratePreflightRepairBatch::from_actions(
                0,
                stage,
                action_kind,
                actions,
            )
        })
        .collect::<Vec<_>>();
    batches.sort_by(compare_mesh_substrate_preflight_repair_batches);

    for (index, batch) in batches.iter_mut().enumerate() {
        batch.sequence = index + 1;
    }

    batches
}

pub fn mesh_protocol_substrate_preflight_repair_batch_summary(
    available_primitives: &[PrimitiveFamily],
) -> IntegrationMeshProtocolSubstratePreflightRepairBatchSummary {
    let batches = mesh_protocol_substrate_preflight_repair_batches(available_primitives);
    IntegrationMeshProtocolSubstratePreflightRepairBatchSummary::from_batches(batches.iter())
}

pub fn mesh_protocol_substrate_preflight_repair_schedule(
    available_primitives: &[PrimitiveFamily],
) -> Vec<IntegrationMeshProtocolSubstratePreflightRepairScheduleSlot> {
    mesh_protocol_substrate_preflight_repair_batches(available_primitives)
        .iter()
        .enumerate()
        .map(|(index, batch)| {
            IntegrationMeshProtocolSubstratePreflightRepairScheduleSlot::from_batch(
                index + 1,
                batch,
            )
        })
        .collect()
}

pub fn mesh_protocol_substrate_preflight_repair_schedule_summary(
    available_primitives: &[PrimitiveFamily],
) -> IntegrationMeshProtocolSubstratePreflightRepairScheduleSummary {
    let slots = mesh_protocol_substrate_preflight_repair_schedule(available_primitives);
    IntegrationMeshProtocolSubstratePreflightRepairScheduleSummary::from_slots(slots.iter())
}

pub fn mesh_protocol_substrate_preflight_repair_slot_audit_rows(
    available_primitives: &[PrimitiveFamily],
) -> Vec<IntegrationMeshProtocolSubstratePreflightRepairSlotAuditRow> {
    mesh_protocol_substrate_preflight_repair_schedule(available_primitives)
        .iter()
        .enumerate()
        .map(|(index, slot)| {
            IntegrationMeshProtocolSubstratePreflightRepairSlotAuditRow::from_slot(index + 1, slot)
        })
        .collect()
}

pub fn mesh_protocol_substrate_preflight_repair_slot_audit_summary(
    available_primitives: &[PrimitiveFamily],
) -> IntegrationMeshProtocolSubstratePreflightRepairSlotAuditSummary {
    let rows = mesh_protocol_substrate_preflight_repair_slot_audit_rows(available_primitives);
    IntegrationMeshProtocolSubstratePreflightRepairSlotAuditSummary::from_rows(rows.iter())
}

pub fn mesh_protocol_substrate_preflight_repair_slot_execution_tickets(
    available_primitives: &[PrimitiveFamily],
) -> Vec<IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionTicket> {
    mesh_protocol_substrate_preflight_repair_slot_audit_rows(available_primitives)
        .iter()
        .enumerate()
        .map(|(index, row)| {
            IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionTicket::from_audit_row(
                index + 1,
                row,
            )
        })
        .collect()
}

pub fn mesh_protocol_substrate_preflight_repair_slot_execution_ticket_summary(
    available_primitives: &[PrimitiveFamily],
) -> IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionTicketSummary {
    let tickets =
        mesh_protocol_substrate_preflight_repair_slot_execution_tickets(available_primitives);
    IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionTicketSummary::from_tickets(
        tickets.iter(),
    )
}

pub fn mesh_protocol_substrate_preflight_repair_slot_execution_work_orders(
    available_primitives: &[PrimitiveFamily],
) -> Vec<IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionWorkOrder> {
    mesh_protocol_substrate_preflight_repair_slot_execution_tickets(available_primitives)
        .iter()
        .enumerate()
        .map(|(index, ticket)| {
            IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionWorkOrder::from_ticket(
                index + 1,
                ticket,
            )
        })
        .collect()
}

pub fn mesh_protocol_substrate_preflight_repair_slot_execution_work_order_summary(
    available_primitives: &[PrimitiveFamily],
) -> IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionWorkOrderSummary {
    let work_orders =
        mesh_protocol_substrate_preflight_repair_slot_execution_work_orders(available_primitives);
    IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionWorkOrderSummary::from_work_orders(
        work_orders.iter(),
    )
}

pub fn mesh_protocol_substrate_preflight_repair_slot_execution_evidence_packets(
    available_primitives: &[PrimitiveFamily],
) -> Vec<IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidencePacket> {
    mesh_protocol_substrate_preflight_repair_slot_execution_work_orders(available_primitives)
        .iter()
        .enumerate()
        .map(|(index, work_order)| {
            IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidencePacket::from_work_order(
                index + 1,
                work_order,
            )
        })
        .collect()
}

pub fn mesh_protocol_substrate_preflight_repair_slot_execution_evidence_summary(
    available_primitives: &[PrimitiveFamily],
) -> IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceSummary {
    let packets = mesh_protocol_substrate_preflight_repair_slot_execution_evidence_packets(
        available_primitives,
    );
    IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceSummary::from_packets(
        packets.iter(),
    )
}

pub fn mesh_protocol_substrate_preflight_repair_slot_execution_evidence_reviews(
    available_primitives: &[PrimitiveFamily],
) -> Vec<IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReview> {
    mesh_protocol_substrate_preflight_repair_slot_execution_evidence_packets(available_primitives)
        .iter()
        .enumerate()
        .map(|(index, packet)| {
            IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReview::from_packet(
                index + 1,
                packet,
            )
        })
        .collect()
}

pub fn mesh_protocol_substrate_preflight_repair_slot_execution_evidence_review_summary(
    available_primitives: &[PrimitiveFamily],
) -> IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewSummary {
    let reviews = mesh_protocol_substrate_preflight_repair_slot_execution_evidence_reviews(
        available_primitives,
    );
    IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewSummary::from_reviews(
        reviews.iter(),
    )
}

pub fn mesh_protocol_substrate_preflight_repair_slot_execution_work_order_guardrails(
    available_primitives: &[PrimitiveFamily],
) -> Vec<IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionWorkOrderGuardrail> {
    mesh_protocol_substrate_preflight_repair_slot_execution_work_orders(available_primitives)
        .iter()
        .enumerate()
        .map(|(index, work_order)| {
            IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionWorkOrderGuardrail::from_work_order(
                index + 1,
                work_order,
            )
        })
        .collect()
}

pub fn mesh_protocol_substrate_preflight_repair_slot_execution_evidence_review_dispositions(
    available_primitives: &[PrimitiveFamily],
) -> Vec<IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDisposition> {
    mesh_protocol_substrate_preflight_repair_slot_execution_evidence_reviews(available_primitives)
        .iter()
        .enumerate()
        .map(|(index, review)| {
            IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDisposition::from_review(
                index + 1,
                review,
            )
        })
        .collect()
}

pub fn mesh_protocol_substrate_preflight_repair_slot_execution_work_order_guardrail_summary(
    available_primitives: &[PrimitiveFamily],
) -> IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionWorkOrderGuardrailSummary {
    let guardrails = mesh_protocol_substrate_preflight_repair_slot_execution_work_order_guardrails(
        available_primitives,
    );
    IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionWorkOrderGuardrailSummary::from_guardrails(
        guardrails.iter(),
    )
}

pub fn mesh_protocol_substrate_preflight_repair_slot_execution_evidence_review_disposition_summary(
    available_primitives: &[PrimitiveFamily],
) -> IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionSummary {
    let dispositions =
        mesh_protocol_substrate_preflight_repair_slot_execution_evidence_review_dispositions(
            available_primitives,
        );
    IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionSummary::from_dispositions(
        dispositions.iter(),
    )
}

pub fn mesh_protocol_substrate_preflight_repair_slot_execution_evidence_review_disposition_actions(
    available_primitives: &[PrimitiveFamily],
) -> Vec<IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionAction>
{
    mesh_protocol_substrate_preflight_repair_slot_execution_evidence_review_dispositions(
        available_primitives,
    )
    .iter()
    .enumerate()
    .map(|(index, disposition)| {
        IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionAction::from_disposition(
            index + 1,
            disposition,
        )
    })
    .collect()
}

pub fn mesh_protocol_substrate_preflight_repair_slot_execution_evidence_review_disposition_action_summary(
    available_primitives: &[PrimitiveFamily],
) -> IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionActionSummary
{
    let actions =
        mesh_protocol_substrate_preflight_repair_slot_execution_evidence_review_disposition_actions(
            available_primitives,
        );
    IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionActionSummary::from_actions(
        actions.iter(),
    )
}

pub fn mesh_protocol_substrate_preflight_repair_slot_execution_evidence_review_disposition_action_slots(
    available_primitives: &[PrimitiveFamily],
) -> Vec<
    IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionActionSlot,
> {
    mesh_protocol_substrate_preflight_repair_slot_execution_evidence_review_disposition_actions(
        available_primitives,
    )
    .iter()
    .enumerate()
    .map(|(index, action)| {
        IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionActionSlot::from_action(
            index + 1,
            action,
        )
    })
    .collect()
}

pub fn mesh_protocol_substrate_preflight_repair_slot_execution_evidence_review_disposition_action_slot_summary(
    available_primitives: &[PrimitiveFamily],
) -> IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionActionSlotSummary
{
    let slots =
        mesh_protocol_substrate_preflight_repair_slot_execution_evidence_review_disposition_action_slots(
            available_primitives,
        );
    IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionActionSlotSummary::from_slots(
        slots.iter(),
    )
}

pub fn mesh_readiness_package_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReadinessPackageSummary {
    let mesh_catalog = mesh_protocol_catalog_entries(catalog);
    let mesh_primitive_summary = mesh_protocol_primitive_readiness_summary(available_primitives);
    let remediation_items = activation_evidence_remediation_items_for_catalog(
        &mesh_catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    let remediation_summary =
        IntegrationActivationEvidenceRemediationSummary::from_items(remediation_items.iter());

    IntegrationMeshReadinessPackageSummary::from_parts(mesh_primitive_summary, remediation_summary)
}

pub fn mesh_readiness_package_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReadinessPackageSummary {
    let catalog = first_party_catalog();
    mesh_readiness_package_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_stage_release_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshStageReleaseSummary {
    let package_summary = mesh_readiness_package_summary_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    let substrate_stage_summary = mesh_protocol_substrate_stage_summary(available_primitives);

    IntegrationMeshStageReleaseSummary::from_parts(package_summary, substrate_stage_summary)
}

pub fn mesh_stage_release_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshStageReleaseSummary {
    let catalog = first_party_catalog();
    mesh_stage_release_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_action_readiness_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshActionReadinessSummary {
    let release_summary = mesh_stage_release_summary_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    let action_summary = mesh_protocol_substrate_action_summary(available_primitives);

    IntegrationMeshActionReadinessSummary::from_parts(release_summary, action_summary)
}

pub fn mesh_action_readiness_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshActionReadinessSummary {
    let catalog = first_party_catalog();
    mesh_action_readiness_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_readiness_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseReadinessSummary {
    let action_readiness_summary = mesh_action_readiness_summary_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );

    IntegrationMeshReleaseReadinessSummary::from_action_readiness_summary(action_readiness_summary)
}

pub fn mesh_release_readiness_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseReadinessSummary {
    let catalog = first_party_catalog();
    mesh_release_readiness_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_preflight_readiness_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshPreflightReadinessSummary {
    let release_readiness_summary = mesh_release_readiness_summary_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    let preflight_summary = mesh_protocol_substrate_preflight_summary(available_primitives);

    IntegrationMeshPreflightReadinessSummary::from_parts(
        release_readiness_summary,
        preflight_summary,
    )
}

pub fn mesh_preflight_readiness_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshPreflightReadinessSummary {
    let catalog = first_party_catalog();
    mesh_preflight_readiness_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_preflight_repair_readiness_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshPreflightRepairReadinessSummary {
    let preflight_readiness_summary = mesh_preflight_readiness_summary_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    let preflight_action_summary =
        mesh_protocol_substrate_preflight_action_summary(available_primitives);

    IntegrationMeshPreflightRepairReadinessSummary::from_parts(
        preflight_readiness_summary,
        preflight_action_summary,
    )
}

pub fn mesh_preflight_repair_readiness_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshPreflightRepairReadinessSummary {
    let catalog = first_party_catalog();
    mesh_preflight_repair_readiness_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_preflight_batch_readiness_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshPreflightBatchReadinessSummary {
    let repair_readiness_summary = mesh_preflight_repair_readiness_summary_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    let repair_batch_summary =
        mesh_protocol_substrate_preflight_repair_batch_summary(available_primitives);

    IntegrationMeshPreflightBatchReadinessSummary::from_parts(
        repair_readiness_summary,
        repair_batch_summary,
    )
}

pub fn mesh_preflight_batch_readiness_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshPreflightBatchReadinessSummary {
    let catalog = first_party_catalog();
    mesh_preflight_batch_readiness_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_preflight_schedule_readiness_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshPreflightScheduleReadinessSummary {
    let batch_readiness_summary = mesh_preflight_batch_readiness_summary_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    let repair_schedule_summary =
        mesh_protocol_substrate_preflight_repair_schedule_summary(available_primitives);

    IntegrationMeshPreflightScheduleReadinessSummary::from_parts(
        batch_readiness_summary,
        repair_schedule_summary,
    )
}

pub fn mesh_preflight_schedule_readiness_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshPreflightScheduleReadinessSummary {
    let catalog = first_party_catalog();
    mesh_preflight_schedule_readiness_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_preflight_slot_readiness_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshPreflightSlotReadinessSummary {
    let schedule_readiness_summary = mesh_preflight_schedule_readiness_summary_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    let slot_audit_summary =
        mesh_protocol_substrate_preflight_repair_slot_audit_summary(available_primitives);

    IntegrationMeshPreflightSlotReadinessSummary::from_parts(
        schedule_readiness_summary,
        slot_audit_summary,
    )
}

pub fn mesh_preflight_slot_readiness_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshPreflightSlotReadinessSummary {
    let catalog = first_party_catalog();
    mesh_preflight_slot_readiness_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_preflight_execution_readiness_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshPreflightExecutionReadinessSummary {
    let slot_readiness_summary = mesh_preflight_slot_readiness_summary_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    let execution_ticket_summary =
        mesh_protocol_substrate_preflight_repair_slot_execution_ticket_summary(
            available_primitives,
        );

    IntegrationMeshPreflightExecutionReadinessSummary::from_parts(
        slot_readiness_summary,
        execution_ticket_summary,
    )
}

pub fn mesh_preflight_execution_readiness_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshPreflightExecutionReadinessSummary {
    let catalog = first_party_catalog();
    mesh_preflight_execution_readiness_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_preflight_work_order_readiness_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshPreflightWorkOrderReadinessSummary {
    let execution_readiness_summary = mesh_preflight_execution_readiness_summary_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    let execution_work_order_summary =
        mesh_protocol_substrate_preflight_repair_slot_execution_work_order_summary(
            available_primitives,
        );

    IntegrationMeshPreflightWorkOrderReadinessSummary::from_parts(
        execution_readiness_summary,
        execution_work_order_summary,
    )
}

pub fn mesh_preflight_work_order_readiness_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshPreflightWorkOrderReadinessSummary {
    let catalog = first_party_catalog();
    mesh_preflight_work_order_readiness_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_preflight_guardrail_readiness_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshPreflightGuardrailReadinessSummary {
    let work_order_readiness_summary = mesh_preflight_work_order_readiness_summary_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    let work_order_guardrail_summary =
        mesh_protocol_substrate_preflight_repair_slot_execution_work_order_guardrail_summary(
            available_primitives,
        );
    let evidence_review_disposition_summary =
        mesh_protocol_substrate_preflight_repair_slot_execution_evidence_review_disposition_summary(
            available_primitives,
        );

    IntegrationMeshPreflightGuardrailReadinessSummary::from_parts(
        work_order_readiness_summary,
        work_order_guardrail_summary,
        evidence_review_disposition_summary,
    )
}

pub fn mesh_preflight_guardrail_readiness_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshPreflightGuardrailReadinessSummary {
    let catalog = first_party_catalog();
    mesh_preflight_guardrail_readiness_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_preflight_disposition_action_readiness_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshPreflightDispositionActionReadinessSummary {
    let guardrail_readiness_summary = mesh_preflight_guardrail_readiness_summary_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    let disposition_action_summary =
        mesh_protocol_substrate_preflight_repair_slot_execution_evidence_review_disposition_action_summary(
            available_primitives,
        );

    IntegrationMeshPreflightDispositionActionReadinessSummary::from_parts(
        guardrail_readiness_summary,
        disposition_action_summary,
    )
}

pub fn mesh_preflight_disposition_action_readiness_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshPreflightDispositionActionReadinessSummary {
    let catalog = first_party_catalog();
    mesh_preflight_disposition_action_readiness_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

fn mesh_readiness_handoff_packages_from_summary(
    summary: &IntegrationMeshActionReadinessSummary,
    actions: &[IntegrationMeshProtocolSubstrateAction],
) -> Vec<IntegrationMeshReadinessHandoffPackage> {
    let mut packages = actions
        .iter()
        .map(|action| {
            IntegrationMeshReadinessHandoffPackage::from_substrate_action(action, summary)
        })
        .collect::<Vec<_>>();

    if let Some(package) = IntegrationMeshReadinessHandoffPackage::evidence_remediation(summary) {
        packages.push(package);
    }
    if let Some(package) = IntegrationMeshReadinessHandoffPackage::release_ready(summary) {
        packages.push(package);
    }

    for (index, package) in packages.iter_mut().enumerate() {
        package.sequence = index + 1;
    }

    packages
}

pub fn mesh_readiness_handoff_packages_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReadinessHandoffPackage> {
    let summary = mesh_action_readiness_summary_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    let actions = mesh_protocol_substrate_actions(available_primitives);

    mesh_readiness_handoff_packages_from_summary(&summary, &actions)
}

pub fn mesh_readiness_handoff_packages(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReadinessHandoffPackage> {
    let catalog = first_party_catalog();
    mesh_readiness_handoff_packages_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_readiness_handoff_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReadinessHandoffSummary {
    let summary = mesh_action_readiness_summary_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    let actions = mesh_protocol_substrate_actions(available_primitives);
    let packages = mesh_readiness_handoff_packages_from_summary(&summary, &actions);

    IntegrationMeshReadinessHandoffSummary::from_parts(summary, packages.iter())
}

pub fn mesh_readiness_handoff_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReadinessHandoffSummary {
    let catalog = first_party_catalog();
    mesh_readiness_handoff_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

fn mesh_release_packet_status(
    summary: &IntegrationMeshReadinessHandoffSummary,
) -> IntegrationMeshReleaseReadinessStatus {
    if summary.ready_for_handoff() {
        IntegrationMeshReleaseReadinessStatus::Ready
    } else if summary.has_blockers() {
        IntegrationMeshReleaseReadinessStatus::Blocked
    } else if summary.has_review_work() || summary.requires_attention() {
        IntegrationMeshReleaseReadinessStatus::NeedsReview
    } else {
        IntegrationMeshReleaseReadinessStatus::Blocked
    }
}

fn mesh_release_readiness_checks_from_summary(
    summary: &IntegrationMeshReadinessHandoffSummary,
) -> Vec<IntegrationMeshReleaseReadinessCheck> {
    let substrate_status = if summary.substrate_actions_ready {
        IntegrationMeshReleaseReadinessStatus::Ready
    } else {
        IntegrationMeshReleaseReadinessStatus::Blocked
    };
    let evidence_status = if summary.remediation_item_count == 0 {
        IntegrationMeshReleaseReadinessStatus::Ready
    } else if summary.review_required_packages > 0 {
        IntegrationMeshReleaseReadinessStatus::NeedsReview
    } else {
        IntegrationMeshReleaseReadinessStatus::Blocked
    };
    let human_review_status = if summary.review_required_packages == 0 {
        IntegrationMeshReleaseReadinessStatus::Ready
    } else {
        IntegrationMeshReleaseReadinessStatus::NeedsReview
    };
    let operator_status = if summary.operator_required_packages == 0 {
        IntegrationMeshReleaseReadinessStatus::Ready
    } else if summary.blocked_packages > 0 {
        IntegrationMeshReleaseReadinessStatus::Blocked
    } else {
        IntegrationMeshReleaseReadinessStatus::NeedsReview
    };
    let release_packet_status = mesh_release_packet_status(summary);

    vec![
        IntegrationMeshReleaseReadinessCheck::from_summary(
            1,
            IntegrationMeshReleaseReadinessCheckKind::SubstrateActions,
            substrate_status,
            Some(IntegrationMeshReadinessHandoffKind::SubstrateAction),
            Some(substrate_status.as_handoff_status()),
            summary,
            !summary.substrate_actions_ready,
        ),
        IntegrationMeshReleaseReadinessCheck::from_summary(
            2,
            IntegrationMeshReleaseReadinessCheckKind::EvidenceRemediation,
            evidence_status,
            Some(IntegrationMeshReadinessHandoffKind::EvidenceRemediation),
            Some(evidence_status.as_handoff_status()),
            summary,
            summary.remediation_item_count > 0,
        ),
        IntegrationMeshReleaseReadinessCheck::from_summary(
            3,
            IntegrationMeshReleaseReadinessCheckKind::HumanReview,
            human_review_status,
            Some(IntegrationMeshReadinessHandoffKind::EvidenceRemediation),
            Some(human_review_status.as_handoff_status()),
            summary,
            false,
        ),
        IntegrationMeshReleaseReadinessCheck::from_summary(
            4,
            IntegrationMeshReleaseReadinessCheckKind::OperatorHandoff,
            operator_status,
            summary.next_package_kind,
            summary
                .next_handoff_status
                .or(Some(operator_status.as_handoff_status())),
            summary,
            summary.operator_required_packages > 0,
        ),
        IntegrationMeshReleaseReadinessCheck::from_summary(
            5,
            IntegrationMeshReleaseReadinessCheckKind::ReleasePacket,
            release_packet_status,
            Some(IntegrationMeshReadinessHandoffKind::ReleaseReady),
            Some(release_packet_status.as_handoff_status()),
            summary,
            !summary.ready_for_handoff() && summary.operator_required_packages > 0,
        ),
    ]
}

pub fn mesh_release_readiness_checks_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseReadinessCheck> {
    let summary = mesh_readiness_handoff_summary_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );

    mesh_release_readiness_checks_from_summary(&summary)
}

pub fn mesh_release_readiness_checks(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseReadinessCheck> {
    let catalog = first_party_catalog();
    mesh_release_readiness_checks_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_readiness_check_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseReadinessCheckSummary {
    let handoff_summary = mesh_readiness_handoff_summary_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    let checks = mesh_release_readiness_checks_from_summary(&handoff_summary);

    IntegrationMeshReleaseReadinessCheckSummary::from_parts(handoff_summary, checks.iter())
}

pub fn mesh_release_readiness_check_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseReadinessCheckSummary {
    let catalog = first_party_catalog();
    mesh_release_readiness_check_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_readiness_check_slots_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseReadinessCheckSlot> {
    mesh_release_readiness_checks_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
    .iter()
    .enumerate()
    .map(|(index, check)| IntegrationMeshReleaseReadinessCheckSlot::from_check(index + 1, check))
    .collect()
}

pub fn mesh_release_readiness_check_slots(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseReadinessCheckSlot> {
    let catalog = first_party_catalog();
    mesh_release_readiness_check_slots_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_readiness_check_slot_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseReadinessCheckSlotSummary {
    let slots = mesh_release_readiness_check_slots_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );

    IntegrationMeshReleaseReadinessCheckSlotSummary::from_slots(slots.iter())
}

pub fn mesh_release_readiness_check_slot_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseReadinessCheckSlotSummary {
    let catalog = first_party_catalog();
    mesh_release_readiness_check_slot_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_packet_readiness_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleasePacketReadinessSummary {
    let release_readiness_check_summary = mesh_release_readiness_check_summary_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );

    IntegrationMeshReleasePacketReadinessSummary::from_summary(release_readiness_check_summary)
}

pub fn mesh_release_packet_readiness_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleasePacketReadinessSummary {
    let catalog = first_party_catalog();
    mesh_release_packet_readiness_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_execution_readiness_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseExecutionReadinessSummary {
    let release_packet_readiness_summary = mesh_release_packet_readiness_summary_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    let release_check_slot_summary = mesh_release_readiness_check_slot_summary_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );

    IntegrationMeshReleaseExecutionReadinessSummary::from_summaries(
        release_packet_readiness_summary,
        release_check_slot_summary,
    )
}

pub fn mesh_release_execution_readiness_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseExecutionReadinessSummary {
    let catalog = first_party_catalog();
    mesh_release_execution_readiness_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_execution_tasks_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseExecutionTask> {
    let release_execution_readiness_summary = mesh_release_execution_readiness_summary_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    let slots = mesh_release_readiness_check_slots_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );

    slots
        .iter()
        .enumerate()
        .map(|(index, slot)| {
            IntegrationMeshReleaseExecutionTask::from_slot(
                index + 1,
                slot,
                &release_execution_readiness_summary,
            )
        })
        .collect()
}

pub fn mesh_release_execution_tasks(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseExecutionTask> {
    let catalog = first_party_catalog();
    mesh_release_execution_tasks_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_execution_task_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseExecutionTaskSummary {
    let release_execution_readiness_summary = mesh_release_execution_readiness_summary_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    let slots = mesh_release_readiness_check_slots_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    let tasks = slots
        .iter()
        .enumerate()
        .map(|(index, slot)| {
            IntegrationMeshReleaseExecutionTask::from_slot(
                index + 1,
                slot,
                &release_execution_readiness_summary,
            )
        })
        .collect::<Vec<_>>();

    IntegrationMeshReleaseExecutionTaskSummary::from_parts(
        release_execution_readiness_summary,
        tasks.iter(),
    )
}

pub fn mesh_release_execution_task_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseExecutionTaskSummary {
    let catalog = first_party_catalog();
    mesh_release_execution_task_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_task_readiness_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTaskReadinessSummary {
    let release_execution_readiness_summary = mesh_release_execution_readiness_summary_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    let release_execution_task_summary = mesh_release_execution_task_summary_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );

    IntegrationMeshReleaseTaskReadinessSummary::from_summaries(
        release_execution_readiness_summary,
        release_execution_task_summary,
    )
}

pub fn mesh_release_task_readiness_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTaskReadinessSummary {
    let catalog = first_party_catalog();
    mesh_release_task_readiness_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_execution_task_dispatch_slots_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseExecutionTaskDispatchSlot> {
    let release_task_readiness_summary = mesh_release_task_readiness_summary_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    let tasks = mesh_release_execution_tasks_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );

    tasks
        .iter()
        .enumerate()
        .map(|(index, task)| {
            IntegrationMeshReleaseExecutionTaskDispatchSlot::from_task(
                index + 1,
                task,
                &release_task_readiness_summary,
            )
        })
        .collect()
}

pub fn mesh_release_execution_task_dispatch_slots(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseExecutionTaskDispatchSlot> {
    let catalog = first_party_catalog();
    mesh_release_execution_task_dispatch_slots_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_execution_task_dispatch_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseExecutionTaskDispatchSummary {
    let release_task_readiness_summary = mesh_release_task_readiness_summary_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    let tasks = mesh_release_execution_tasks_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    let dispatch_slots = tasks
        .iter()
        .enumerate()
        .map(|(index, task)| {
            IntegrationMeshReleaseExecutionTaskDispatchSlot::from_task(
                index + 1,
                task,
                &release_task_readiness_summary,
            )
        })
        .collect::<Vec<_>>();

    IntegrationMeshReleaseExecutionTaskDispatchSummary::from_parts(
        release_task_readiness_summary,
        dispatch_slots.iter(),
    )
}

pub fn mesh_release_execution_task_dispatch_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseExecutionTaskDispatchSummary {
    let catalog = first_party_catalog();
    mesh_release_execution_task_dispatch_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_dispatch_readiness_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseDispatchReadinessSummary {
    let release_task_readiness_summary = mesh_release_task_readiness_summary_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    let release_execution_task_dispatch_summary =
        mesh_release_execution_task_dispatch_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    IntegrationMeshReleaseDispatchReadinessSummary::from_summaries(
        release_task_readiness_summary,
        release_execution_task_dispatch_summary,
    )
}

pub fn mesh_release_dispatch_readiness_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseDispatchReadinessSummary {
    let catalog = first_party_catalog();
    mesh_release_dispatch_readiness_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_dispatch_tickets_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseDispatchTicket> {
    let release_dispatch_readiness_summary = mesh_release_dispatch_readiness_summary_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    let dispatch_slots = mesh_release_execution_task_dispatch_slots_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );

    dispatch_slots
        .iter()
        .enumerate()
        .map(|(index, dispatch_slot)| {
            IntegrationMeshReleaseDispatchTicket::from_dispatch_slot(
                index + 1,
                dispatch_slot,
                &release_dispatch_readiness_summary,
            )
        })
        .collect()
}

pub fn mesh_release_dispatch_tickets(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseDispatchTicket> {
    let catalog = first_party_catalog();
    mesh_release_dispatch_tickets_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_dispatch_ticket_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseDispatchTicketSummary {
    let release_dispatch_readiness_summary = mesh_release_dispatch_readiness_summary_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    let dispatch_slots = mesh_release_execution_task_dispatch_slots_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    let tickets = dispatch_slots
        .iter()
        .enumerate()
        .map(|(index, dispatch_slot)| {
            IntegrationMeshReleaseDispatchTicket::from_dispatch_slot(
                index + 1,
                dispatch_slot,
                &release_dispatch_readiness_summary,
            )
        })
        .collect::<Vec<_>>();

    IntegrationMeshReleaseDispatchTicketSummary::from_parts(
        release_dispatch_readiness_summary,
        tickets.iter(),
    )
}

pub fn mesh_release_dispatch_ticket_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseDispatchTicketSummary {
    let catalog = first_party_catalog();
    mesh_release_dispatch_ticket_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_dispatch_ticket_handoff_packets_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseDispatchTicketHandoffPacket> {
    mesh_release_dispatch_tickets_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
    .iter()
    .map(IntegrationMeshReleaseDispatchTicketHandoffPacket::from_ticket)
    .collect()
}

pub fn mesh_release_dispatch_ticket_handoff_packets(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseDispatchTicketHandoffPacket> {
    let catalog = first_party_catalog();
    mesh_release_dispatch_ticket_handoff_packets_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_dispatch_ticket_handoff_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseDispatchTicketHandoffSummary {
    let release_dispatch_ticket_summary = mesh_release_dispatch_ticket_summary_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    let packets = mesh_release_dispatch_ticket_handoff_packets_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );

    IntegrationMeshReleaseDispatchTicketHandoffSummary::from_parts(
        release_dispatch_ticket_summary,
        packets.iter(),
    )
}

pub fn mesh_release_dispatch_ticket_handoff_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseDispatchTicketHandoffSummary {
    let catalog = first_party_catalog();
    mesh_release_dispatch_ticket_handoff_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_readiness_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketReadinessSummary {
    let release_dispatch_readiness_summary = mesh_release_dispatch_readiness_summary_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    let release_dispatch_ticket_summary = mesh_release_dispatch_ticket_summary_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );

    IntegrationMeshReleaseTicketReadinessSummary::from_summaries(
        release_dispatch_readiness_summary,
        release_dispatch_ticket_summary,
    )
}

pub fn mesh_release_ticket_readiness_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketReadinessSummary {
    let catalog = first_party_catalog();
    mesh_release_ticket_readiness_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_readiness_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffReadinessSummary {
    let release_ticket_readiness_summary = mesh_release_ticket_readiness_summary_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );
    let release_dispatch_ticket_handoff_summary =
        mesh_release_dispatch_ticket_handoff_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    IntegrationMeshReleaseTicketHandoffReadinessSummary::from_summaries(
        release_ticket_readiness_summary,
        release_dispatch_ticket_handoff_summary,
    )
}

pub fn mesh_release_ticket_handoff_readiness_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffReadinessSummary {
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_readiness_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_execution_slots_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseTicketHandoffExecutionSlot> {
    mesh_release_dispatch_ticket_handoff_packets_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
    .iter()
    .enumerate()
    .map(|(index, packet)| {
        IntegrationMeshReleaseTicketHandoffExecutionSlot::from_packet(index + 1, packet)
    })
    .collect()
}

pub fn mesh_release_ticket_handoff_execution_slots(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseTicketHandoffExecutionSlot> {
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_execution_slots_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_execution_slot_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffExecutionSlotSummary {
    let release_ticket_handoff_readiness_summary =
        mesh_release_ticket_handoff_readiness_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );
    let execution_slots = mesh_release_ticket_handoff_execution_slots_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );

    IntegrationMeshReleaseTicketHandoffExecutionSlotSummary::from_parts(
        release_ticket_handoff_readiness_summary,
        execution_slots.iter(),
    )
}

pub fn mesh_release_ticket_handoff_execution_slot_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffExecutionSlotSummary {
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_execution_slot_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_execution_readiness_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffExecutionReadinessSummary {
    let release_ticket_handoff_readiness_summary =
        mesh_release_ticket_handoff_readiness_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );
    let release_ticket_handoff_execution_slot_summary =
        mesh_release_ticket_handoff_execution_slot_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    IntegrationMeshReleaseTicketHandoffExecutionReadinessSummary::from_summaries(
        release_ticket_handoff_readiness_summary,
        release_ticket_handoff_execution_slot_summary,
    )
}

pub fn mesh_release_ticket_handoff_execution_readiness_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffExecutionReadinessSummary {
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_execution_readiness_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_execution_work_orders_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseTicketHandoffExecutionWorkOrder> {
    mesh_release_ticket_handoff_execution_slots_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
    .iter()
    .enumerate()
    .map(|(index, execution_slot)| {
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrder::from_execution_slot(
            index + 1,
            execution_slot,
        )
    })
    .collect()
}

pub fn mesh_release_ticket_handoff_execution_work_orders(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseTicketHandoffExecutionWorkOrder> {
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_execution_work_orders_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrails_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrail> {
    mesh_release_ticket_handoff_execution_work_orders_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
    .iter()
    .enumerate()
    .map(|(index, work_order)| {
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrail::from_work_order(
            index + 1,
            work_order,
        )
    })
    .collect()
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrails(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrail> {
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_execution_work_order_guardrails_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrail_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailSummary {
    let guardrails = mesh_release_ticket_handoff_execution_work_order_guardrails_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );

    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailSummary::from_guardrails(
        guardrails.iter(),
    )
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrail_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailSummary {
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_execution_work_order_guardrail_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_rows_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditRow> {
    mesh_release_ticket_handoff_execution_work_order_guardrails_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
    .iter()
    .enumerate()
    .map(|(index, guardrail)| {
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditRow::from_guardrail(
            index + 1,
            guardrail,
        )
    })
    .collect()
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_rows(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditRow> {
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_execution_work_order_guardrail_audit_rows_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditSummary {
    let audit_rows =
        mesh_release_ticket_handoff_execution_work_order_guardrail_audit_rows_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditSummary::from_audit_rows(
        audit_rows.iter(),
    )
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditSummary {
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_execution_work_order_guardrail_audit_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_rows_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceRow> {
    mesh_release_ticket_handoff_execution_work_order_guardrail_audit_rows_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
    .iter()
    .enumerate()
    .map(|(index, audit_row)| {
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceRow::from_audit_row(
            index + 1,
            audit_row,
        )
    })
    .collect()
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_rows(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceRow> {
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_rows_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceSummary {
    let clearance_rows =
        mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_rows_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceSummary::from_clearance_rows(
        clearance_rows.iter(),
    )
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceSummary {
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_actions_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceAction> {
    mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_rows_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
    .iter()
    .enumerate()
    .map(|(index, clearance_row)| {
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceAction::from_clearance_row(
            index + 1,
            clearance_row,
        )
    })
    .collect()
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_actions(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceAction> {
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_actions_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionSummary {
    let actions =
        mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_actions_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionSummary::from_actions(
        actions.iter(),
    )
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionSummary {
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidence>
{
    mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_actions_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
    .iter()
    .enumerate()
    .map(|(index, action)| {
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidence::from_action(
            index + 1,
            action,
        )
    })
    .collect()
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidence>
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceSummary
{
    let evidence =
        mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceSummary::from_evidence(
        evidence.iter(),
    )
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceSummary
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_reviews_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReview>
{
    mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
    .iter()
    .enumerate()
    .map(|(index, evidence)| {
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReview::from_evidence(
            index + 1,
            evidence,
        )
    })
    .collect()
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_reviews(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReview>
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_reviews_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewSummary
{
    let reviews =
        mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_reviews_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewSummary::from_reviews(
        reviews.iter(),
    )
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewSummary
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_dispositions_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDisposition>
{
    mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_reviews_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
    .iter()
    .enumerate()
    .map(|(index, review)| {
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDisposition::from_review(
            index + 1,
            review,
        )
    })
    .collect()
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_dispositions(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDisposition>
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_dispositions_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionSummary
{
    let dispositions =
        mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_dispositions_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionSummary::from_dispositions(
        dispositions.iter(),
    )
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionSummary
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_actions_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionAction>
{
    mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_dispositions_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
    .iter()
    .enumerate()
    .map(|(index, disposition)| {
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionAction::from_disposition(
            index + 1,
            disposition,
        )
    })
    .collect()
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_actions(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionAction>
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_actions_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSummary
{
    let actions =
        mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_actions_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSummary::from_actions(
        actions.iter(),
    )
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSummary
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slots_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlot>
{
    mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_actions_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
    .iter()
    .enumerate()
    .map(|(index, action)| {
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlot::from_action(
            index + 1,
            action,
        )
    })
    .collect()
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slots(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlot>
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slots_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotSummary
{
    let slots =
        mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slots_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotSummary::from_slots(
        slots.iter(),
    )
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotSummary
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_rows_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<
    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceRow,
>{
    mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slots_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
    .iter()
    .enumerate()
    .map(|(index, slot)| {
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceRow::from_slot(
            index + 1,
            slot,
        )
    })
    .collect()
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_rows(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<
    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceRow,
>{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_rows_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceSummary
{
    let clearance_rows =
        mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_rows_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceSummary::from_clearance_rows(
        clearance_rows.iter(),
    )
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceSummary
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_rows_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<
    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionRow,
>{
    mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_rows_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
    .iter()
    .enumerate()
    .map(|(index, clearance_row)| {
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionRow::from_slot_clearance_row(
            index + 1,
            clearance_row,
        )
    })
    .collect()
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_rows(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<
    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionRow,
>{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_rows_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionSummary
{
    let action_rows =
        mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_rows_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionSummary::from_action_rows(
        action_rows.iter(),
    )
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionSummary
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_rows_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<
    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceRow,
>{
    mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_rows_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
    .iter()
    .enumerate()
    .map(|(index, action_row)| {
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceRow::from_slot_clearance_action_row(
            index + 1,
            action_row,
        )
    })
    .collect()
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_rows(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<
    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceRow,
>{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_rows_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceSummary
{
    let evidence_rows =
        mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_rows_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceSummary::from_action_evidence_rows(
        evidence_rows.iter(),
    )
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceSummary
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_rows_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<
    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReview,
>{
    mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_rows_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
    .iter()
    .enumerate()
    .map(|(index, evidence_row)| {
        IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReview::from_slot_clearance_action_evidence_row(
            index + 1,
            evidence_row,
        )
    })
    .collect()
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_rows(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<
    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReview,
>{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_rows_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewSummary
{
    let review_rows =
        mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_rows_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewSummary::from_action_evidence_reviews(
        review_rows.iter(),
    )
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewSummary
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_execution_work_order_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffExecutionWorkOrderSummary {
    let release_ticket_handoff_execution_readiness_summary =
        mesh_release_ticket_handoff_execution_readiness_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );
    let work_orders = mesh_release_ticket_handoff_execution_work_orders_for_catalog(
        catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    );

    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderSummary::from_parts(
        release_ticket_handoff_execution_readiness_summary,
        work_orders.iter(),
    )
}

pub fn mesh_release_ticket_handoff_execution_work_order_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffExecutionWorkOrderSummary {
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_execution_work_order_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_work_order_readiness_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffWorkOrderReadinessSummary {
    let release_ticket_handoff_execution_readiness_summary =
        mesh_release_ticket_handoff_execution_readiness_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );
    let release_ticket_handoff_execution_work_order_summary =
        mesh_release_ticket_handoff_execution_work_order_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    IntegrationMeshReleaseTicketHandoffWorkOrderReadinessSummary::from_summaries(
        release_ticket_handoff_execution_readiness_summary,
        release_ticket_handoff_execution_work_order_summary,
    )
}

pub fn mesh_release_ticket_handoff_work_order_readiness_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffWorkOrderReadinessSummary {
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_work_order_readiness_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_work_order_guardrail_readiness_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffWorkOrderGuardrailReadinessSummary {
    let release_ticket_handoff_work_order_readiness_summary =
        mesh_release_ticket_handoff_work_order_readiness_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );
    let release_ticket_handoff_execution_work_order_guardrail_summary =
        mesh_release_ticket_handoff_execution_work_order_guardrail_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    IntegrationMeshReleaseTicketHandoffWorkOrderGuardrailReadinessSummary::from_summaries(
        release_ticket_handoff_work_order_readiness_summary,
        release_ticket_handoff_execution_work_order_guardrail_summary,
    )
}

pub fn mesh_release_ticket_handoff_work_order_guardrail_readiness_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffWorkOrderGuardrailReadinessSummary {
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_work_order_guardrail_readiness_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_guardrail_audit_readiness_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffGuardrailAuditReadinessSummary {
    let release_ticket_handoff_work_order_guardrail_readiness_summary =
        mesh_release_ticket_handoff_work_order_guardrail_readiness_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );
    let release_ticket_handoff_execution_work_order_guardrail_audit_summary =
        mesh_release_ticket_handoff_execution_work_order_guardrail_audit_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    IntegrationMeshReleaseTicketHandoffGuardrailAuditReadinessSummary::from_summaries(
        release_ticket_handoff_work_order_guardrail_readiness_summary,
        release_ticket_handoff_execution_work_order_guardrail_audit_summary,
    )
}

pub fn mesh_release_ticket_handoff_guardrail_audit_readiness_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffGuardrailAuditReadinessSummary {
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_guardrail_audit_readiness_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_guardrail_audit_clearance_readiness_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceReadinessSummary {
    let release_ticket_handoff_guardrail_audit_readiness_summary =
        mesh_release_ticket_handoff_guardrail_audit_readiness_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );
    let release_ticket_handoff_execution_work_order_guardrail_audit_clearance_summary =
        mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceReadinessSummary::from_summaries(
        release_ticket_handoff_guardrail_audit_readiness_summary,
        release_ticket_handoff_execution_work_order_guardrail_audit_clearance_summary,
    )
}

pub fn mesh_release_ticket_handoff_guardrail_audit_clearance_readiness_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceReadinessSummary {
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_guardrail_audit_clearance_readiness_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_readiness_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionReadinessSummary {
    let release_ticket_handoff_guardrail_audit_clearance_readiness_summary =
        mesh_release_ticket_handoff_guardrail_audit_clearance_readiness_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );
    let release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_summary =
        mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionReadinessSummary::from_summaries(
        release_ticket_handoff_guardrail_audit_clearance_readiness_summary,
        release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_summary,
    )
}

pub fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_readiness_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionReadinessSummary {
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_guardrail_audit_clearance_action_readiness_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_readiness_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReadinessSummary {
    let release_ticket_handoff_guardrail_audit_clearance_action_readiness_summary =
        mesh_release_ticket_handoff_guardrail_audit_clearance_action_readiness_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );
    let release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_summary =
        mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReadinessSummary::from_summaries(
        release_ticket_handoff_guardrail_audit_clearance_action_readiness_summary,
        release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_summary,
    )
}

pub fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_readiness_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReadinessSummary {
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_readiness_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_readiness_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewReadinessSummary
{
    let release_ticket_handoff_guardrail_audit_clearance_action_evidence_readiness_summary =
        mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_readiness_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );
    let release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_summary =
        mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewReadinessSummary::from_summaries(
        release_ticket_handoff_guardrail_audit_clearance_action_evidence_readiness_summary,
        release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_summary,
    )
}

pub fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_readiness_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewReadinessSummary
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_readiness_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_readiness_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionReadinessSummary
{
    let release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_readiness_summary =
        mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_readiness_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );
    let release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_summary =
        mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionReadinessSummary::from_summaries(
        release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_readiness_summary,
        release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_summary,
    )
}

pub fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_readiness_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionReadinessSummary
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_readiness_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_readiness_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionReadinessSummary
{
    let release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_readiness_summary =
        mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_readiness_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );
    let release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary =
        mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionReadinessSummary::from_summaries(
        release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_readiness_summary,
        release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary,
    )
}

pub fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_readiness_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionReadinessSummary
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_readiness_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_readiness_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotReadinessSummary
{
    let release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_readiness_summary =
        mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_readiness_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );
    let release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_summary =
        mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotReadinessSummary::from_summaries(
        release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_readiness_summary,
        release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_summary,
    )
}

pub fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_readiness_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotReadinessSummary
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_readiness_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_readiness_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceReadinessSummary
{
    let release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_readiness_summary =
        mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_readiness_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );
    let release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_summary =
        mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceReadinessSummary::from_summaries(
        release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_readiness_summary,
        release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_summary,
    )
}

pub fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_readiness_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceReadinessSummary
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_readiness_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_readiness_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionReadinessSummary
{
    let release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_readiness_summary =
        mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_readiness_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );
    let release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary =
        mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionReadinessSummary::from_summaries(
        release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_readiness_summary,
        release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary,
    )
}

pub fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_readiness_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionReadinessSummary
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_readiness_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_readiness_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReadinessSummary
{
    let release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_readiness_summary =
        mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_readiness_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );
    let release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary =
        mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReadinessSummary::from_summaries(
        release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_readiness_summary,
        release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary,
    )
}

pub fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_readiness_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReadinessSummary
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_readiness_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_readiness_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewReadinessSummary
{
    let release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_readiness_summary =
        mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_readiness_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );
    let release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary =
        mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewReadinessSummary::from_summaries(
        release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_readiness_summary,
        release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary,
    )
}

pub fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_readiness_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewReadinessSummary
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_readiness_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_rows_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<
    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceRow,
>{
    let readiness_summary =
        mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_readiness_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );
    let evidence_review_readiness_ready = readiness_summary
        .ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_reviews();
    let review_rows =
        mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_rows_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    review_rows
        .iter()
        .enumerate()
        .map(|(index, review)| {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceRow::from_action_evidence_review(
                index + 1,
                review,
                evidence_review_readiness_ready,
            )
        })
        .collect()
}

pub fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_rows(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<
    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceRow,
>{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_rows_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_readiness_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceReadinessSummary
{
    let release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_readiness_summary =
        mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_readiness_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );
    let clearance_rows =
        mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_rows_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceReadinessSummary::from_summaries(
        release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_readiness_summary,
        clearance_rows.iter(),
    )
}

pub fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_readiness_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceReadinessSummary
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_readiness_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_rows_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<
    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionRow,
>{
    let readiness_summary =
        mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_readiness_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );
    let evidence_review_clearance_readiness_ready = readiness_summary
        .ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearances();
    let clearance_rows =
        mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_rows_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    clearance_rows
        .iter()
        .enumerate()
        .map(|(index, clearance_row)| {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionRow::from_clearance_row(
                index + 1,
                clearance_row,
                evidence_review_clearance_readiness_ready,
            )
        })
        .collect()
}

pub fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_rows(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<
    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionRow,
>{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_rows_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessSummary
{
    let clearance_readiness_summary =
        mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_readiness_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );
    let action_rows =
        mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_rows_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessSummary::from_summaries(
        clearance_readiness_summary,
        action_rows.iter(),
    )
}

pub fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessSummary
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_rows_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<
    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceRow,
>{
    let readiness_summary =
        mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );
    let clearance_action_readiness_ready = readiness_summary
        .ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_actions();
    let action_rows =
        mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_rows_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    action_rows
        .iter()
        .enumerate()
        .map(|(index, action_row)| {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceRow::from_clearance_action_row(
                index + 1,
                action_row,
                clearance_action_readiness_ready,
            )
        })
        .collect()
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_rows(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<
    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceRow,
>{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_rows_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceSummary
{
    let clearance_action_readiness_summary =
        mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );
    let evidence_rows =
        mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_rows_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceSummary::from_summaries(
        clearance_action_readiness_summary,
        evidence_rows.iter(),
    )
}

pub fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceSummary
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_rows_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<
    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewRow,
>{
    let readiness_evidence_summary =
        mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );
    let readiness_evidence_ready = readiness_evidence_summary
        .ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence();
    let evidence_rows =
        mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_rows_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    evidence_rows
        .iter()
        .enumerate()
        .map(|(index, evidence_row)| {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewRow::from_readiness_evidence_row(
                index + 1,
                evidence_row,
                readiness_evidence_ready,
            )
        })
        .collect()
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_rows(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<
    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewRow,
>{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_rows_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewSummary
{
    let readiness_evidence_summary =
        mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );
    let review_rows =
        mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_rows_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewSummary::from_summaries(
        readiness_evidence_summary,
        review_rows.iter(),
    )
}

pub fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewSummary
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_rows_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<
    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionRow,
>{
    let review_rows =
        mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_rows_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    review_rows
        .iter()
        .enumerate()
        .map(|(index, review_row)| {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionRow::from_readiness_evidence_review_row(
                index + 1,
                review_row,
            )
        })
        .collect()
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_rows(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<
    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionRow,
>{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_rows_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionSummary
{
    let readiness_evidence_review_summary =
        mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );
    let disposition_rows =
        mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_rows_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionSummary::from_summaries(
        readiness_evidence_review_summary,
        disposition_rows.iter(),
    )
}

pub fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionSummary
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_rows_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<
    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionRow,
>{
    let disposition_rows =
        mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_rows_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    disposition_rows
        .iter()
        .enumerate()
        .map(|(index, disposition_row)| {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionRow::from_readiness_evidence_review_disposition_row(
                index + 1,
                disposition_row,
            )
        })
        .collect()
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_rows(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<
    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionRow,
>{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_rows_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionSummary
{
    let disposition_summary =
        mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );
    let action_rows =
        mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_rows_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionSummary::from_summaries(
        disposition_summary,
        action_rows.iter(),
    )
}

pub fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionSummary
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_readiness_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionReadinessSummary
{
    let action_summary =
        mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionReadinessSummary::from_summary(
        action_summary,
    )
}

pub fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_readiness_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionReadinessSummary
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_readiness_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_readiness_execution_slots_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<
    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionReadinessExecutionSlot,
>{
    let action_rows =
        mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_rows_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    action_rows
        .iter()
        .enumerate()
        .map(|(index, action_row)| {
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionReadinessExecutionSlot::from_readiness_evidence_review_disposition_action_row(
                index + 1,
                action_row,
            )
        })
        .collect()
}

pub fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_readiness_execution_slots(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<
    IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionReadinessExecutionSlot,
>{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_readiness_execution_slots_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_readiness_execution_slot_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionReadinessExecutionSlotSummary
{
    let readiness_summary =
        mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_readiness_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );
    let slots =
        mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_readiness_execution_slots_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionReadinessExecutionSlotSummary::from_summaries(
        readiness_summary,
        slots.iter(),
    )
}

pub fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_readiness_execution_slot_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionReadinessExecutionSlotSummary
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_readiness_execution_slot_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffRow>
{
    let slots =
        mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_readiness_execution_slots_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    slots
        .iter()
        .enumerate()
        .map(|(index, slot)| {
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffRow::from_readiness_execution_slot(
                index + 1,
                slot,
            )
        })
        .collect()
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffRow>
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffSummary
{
    let slot_summary =
        mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_readiness_execution_slot_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );
    let handoff_rows =
        mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffSummary::from_summaries(
        slot_summary,
        handoff_rows.iter(),
    )
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffSummary
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionRow>
{
    let handoff_rows =
        mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    handoff_rows
        .iter()
        .enumerate()
        .map(|(index, row)| {
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionRow::from_handoff_row(
                index + 1,
                row,
            )
        })
        .collect()
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionRow>
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceRow>
{
    let action_rows =
        mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    action_rows
        .iter()
        .enumerate()
        .map(|(index, row)| {
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceRow::from_action_row(
                index + 1,
                row,
            )
        })
        .collect()
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceRow>
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_rows_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewRow>
{
    let evidence_summary =
        mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );
    let handoff_action_evidence_ready = evidence_summary
        .ready_for_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence();
    let evidence_rows =
        mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    evidence_rows
        .iter()
        .enumerate()
        .map(|(index, evidence_row)| {
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewRow::from_evidence_row(
                index + 1,
                evidence_row,
                handoff_action_evidence_ready,
            )
        })
        .collect()
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_rows(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewRow>
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_rows_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_rows_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionRow>
{
    let review_summary =
        mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );
    let handoff_action_evidence_reviews_ready = review_summary
        .ready_for_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_reviews();
    let review_rows =
        mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_rows_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    review_rows
        .iter()
        .enumerate()
        .map(|(index, review_row)| {
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionRow::from_review_row(
                index + 1,
                review_row,
                handoff_action_evidence_reviews_ready,
            )
        })
        .collect()
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_rows(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionRow>
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_rows_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionRow>
{
    let disposition_rows =
        mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_rows_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    disposition_rows
        .iter()
        .enumerate()
        .map(|(index, disposition_row)| {
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionRow::from_disposition_row(
                index + 1,
                disposition_row,
            )
        })
        .collect()
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionRow>
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionSummary
{
    let disposition_summary =
        mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );
    let action_rows =
        mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionSummary::from_summaries(
        disposition_summary,
        action_rows.iter(),
    )
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionSummary
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessRow>
{
    let action_summary =
        mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );
    let handoff_action_evidence_review_disposition_actions_ready = action_summary
        .ready_for_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_actions();
    let action_rows =
        mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    action_rows
        .iter()
        .enumerate()
        .map(|(index, action_row)| {
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessRow::from_disposition_action_row(
                index + 1,
                action_row,
                handoff_action_evidence_review_disposition_actions_ready,
            )
        })
        .collect()
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessRow>
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessSummary
{
    let action_summary =
        mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );
    let readiness_rows =
        mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessSummary::from_summaries(
        action_summary,
        readiness_rows.iter(),
    )
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessSummary
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_execution_slots_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionSlot>
{
    let readiness_summary =
        mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );
    let readiness_ready = readiness_summary
        .ready_for_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness();
    let readiness_rows =
        mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    readiness_rows
        .iter()
        .enumerate()
        .map(|(index, readiness_row)| {
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionSlot::from_readiness_row(
                index + 1,
                readiness_row,
                readiness_ready,
            )
        })
        .collect()
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_execution_slots(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionSlot>
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_execution_slots_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_execution_slot_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionSlotSummary
{
    let readiness_summary =
        mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );
    let slots =
        mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_execution_slots_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionSlotSummary::from_slots(
        readiness_summary
            .total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows,
        readiness_summary
            .ready_for_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness(),
        slots.iter(),
    )
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_execution_slot_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionSlotSummary
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_execution_slot_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_rows_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionHandoffRow>
{
    let slot_summary =
        mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_execution_slot_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );
    let slots =
        mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_execution_slots_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );
    let release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slots_ready =
        slot_summary
            .ready_for_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slots();

    slots
        .iter()
        .enumerate()
        .map(|(index, slot)| {
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionHandoffRow::from_readiness_execution_slot(
                index + 1,
                slot,
                release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slots_ready,
            )
        })
        .collect()
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_rows(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionHandoffRow>
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_rows_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionHandoffSummary
{
    let slot_summary =
        mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_execution_slot_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );
    let rows =
        mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_rows_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionHandoffSummary::from_summaries(
        slot_summary,
        rows.iter(),
    )
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionHandoffSummary
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_rows_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionHandoffActionRow>
{
    let handoff_summary =
        mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );
    let handoffs_ready = handoff_summary
        .ready_for_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoffs();
    let rows =
        mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_rows_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    rows.iter()
        .enumerate()
        .map(|(index, row)| {
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionHandoffActionRow::from_handoff_row(
                index + 1,
                row,
                handoffs_ready,
            )
        })
        .collect()
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_rows(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionHandoffActionRow>
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_rows_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionHandoffActionSummary
{
    let handoff_summary =
        mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );
    let action_rows =
        mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_rows_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionHandoffActionSummary::from_summaries(
        handoff_summary,
        action_rows.iter(),
    )
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionHandoffActionSummary
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceSummary
{
    let action_summary =
        mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );
    let evidence_rows =
        mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceSummary::from_summaries(
        action_summary,
        evidence_rows.iter(),
    )
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceSummary
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_packet_rows_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePacketRow>
{
    let substrate_rows = mesh_protocol_substrate_stage_rows(available_primitives);
    let evidence_summary =
        mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );
    let evidence_rows =
        mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    low_level_mesh_protocols()
        .into_iter()
        .enumerate()
        .map(|(index, protocol)| {
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePacketRow::from_protocol(
                index + 1,
                protocol.clone(),
                substrate_rows
                    .iter()
                    .filter(|row| row.protocol == protocol),
                &evidence_summary,
                evidence_rows.iter(),
            )
        })
        .collect()
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_packet_rows(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePacketRow>
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_packet_rows_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_packet_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePacketSummary
{
    let rows =
        mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_packet_rows_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );
    IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePacketSummary::from_rows(
        rows.iter(),
    )
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_packet_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePacketSummary
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_packet_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_readiness_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageReadinessSummary
{
    let packet_summary =
        mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_packet_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );
    IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageReadinessSummary::from_packet_summary(
        packet_summary,
    )
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_readiness_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageReadinessSummary
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_readiness_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_rows_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffRow>
{
    let packet_rows =
        mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_packet_rows_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );
    let packet_summary =
        IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePacketSummary::from_rows(
            packet_rows.iter(),
        );
    let package_summary =
        IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageReadinessSummary::from_packet_summary(
            packet_summary,
        );

    packet_rows
        .iter()
        .enumerate()
        .map(|(index, packet_row)| {
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffRow::from_packet_row(
                index + 1,
                packet_row,
                &package_summary,
            )
        })
        .collect()
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_rows(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffRow>
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_rows_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffSummary
{
    let package_readiness_summary =
        mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_readiness_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );
    let rows =
        mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_rows_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffSummary::from_summaries(
        package_readiness_summary,
        rows.iter(),
    )
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffSummary
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_slots_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionSlot>
{
    let package_readiness_summary =
        mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_readiness_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );
    let rows =
        mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_rows_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );
    let handoff_summary =
        IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffSummary::from_summaries(
            package_readiness_summary,
            rows.iter(),
        );
    let protocol_evidence_package_handoff_ready =
        handoff_summary.ready_for_release_ticket_handoff_protocol_evidence_package_handoff();

    rows.iter()
        .enumerate()
        .map(|(index, row)| {
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionSlot::from_handoff_row(
                index + 1,
                row,
                protocol_evidence_package_handoff_ready,
            )
        })
        .collect()
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_slots(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionSlot>
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_slots_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_slot_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionSlotSummary
{
    let handoff_summary =
        mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );
    let slots =
        mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_slots_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionSlotSummary::from_summaries(
        handoff_summary,
        slots.iter(),
    )
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_slot_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionSlotSummary
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_slot_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_action_rows_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionActionRow>
{
    let slot_summary =
        mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_slot_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );
    let slots_ready =
        slot_summary.ready_for_release_ticket_handoff_protocol_evidence_package_handoff_execution();
    let slots =
        mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_slots_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    slots
        .iter()
        .enumerate()
        .map(|(index, slot)| {
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionActionRow::from_execution_slot(
                index + 1,
                slot,
                slots_ready,
            )
        })
        .collect()
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_action_rows(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionActionRow>
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_action_rows_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_action_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionActionSummary
{
    let slot_summary =
        mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_slot_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );
    let action_rows =
        mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_action_rows_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionActionSummary::from_summaries(
        slot_summary,
        action_rows.iter(),
    )
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_action_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionActionSummary
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_action_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_action_evidence_rows_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionActionEvidenceRow>
{
    let action_rows =
        mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_action_rows_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    action_rows
        .iter()
        .enumerate()
        .map(|(index, row)| {
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionActionEvidenceRow::from_action_row(
                index + 1,
                row,
            )
        })
        .collect()
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_action_evidence_rows(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionActionEvidenceRow>
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_action_evidence_rows_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_action_evidence_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionActionEvidenceSummary
{
    let action_summary =
        mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_action_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );
    let evidence_rows =
        mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_action_evidence_rows_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionActionEvidenceSummary::from_summaries(
        action_summary,
        evidence_rows.iter(),
    )
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_action_evidence_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionActionEvidenceSummary
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_action_evidence_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_action_evidence_review_rows_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionActionEvidenceReviewRow>
{
    let evidence_summary =
        mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_action_evidence_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );
    let protocol_evidence_package_handoff_execution_action_evidence_reviews_ready =
        evidence_summary
            .ready_for_release_ticket_handoff_protocol_evidence_package_handoff_execution_action_evidence();
    let evidence_rows =
        mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_action_evidence_rows_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    evidence_rows
        .iter()
        .enumerate()
        .map(|(index, evidence_row)| {
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionActionEvidenceReviewRow::from_action_evidence_row(
                index + 1,
                evidence_row,
                protocol_evidence_package_handoff_execution_action_evidence_reviews_ready,
            )
        })
        .collect()
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_action_evidence_review_rows(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionActionEvidenceReviewRow>
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_action_evidence_review_rows_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_action_evidence_review_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionActionEvidenceReviewSummary
{
    let evidence_summary =
        mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_action_evidence_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );
    let review_rows =
        mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_action_evidence_review_rows_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionActionEvidenceReviewSummary::from_summaries(
        evidence_summary,
        review_rows.iter(),
    )
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_action_evidence_review_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionActionEvidenceReviewSummary
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_action_evidence_review_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewSummary
{
    let evidence_summary =
        mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );
    let review_rows =
        mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_rows_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewSummary::from_summaries(
        evidence_summary,
        review_rows.iter(),
    )
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewSummary
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionSummary
{
    let review_summary =
        mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );
    let disposition_rows =
        mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_rows_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionSummary::from_summaries(
        review_summary,
        disposition_rows.iter(),
    )
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionSummary
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_summary_for_catalog(
    catalog: &[IntegrationCatalogEntry],
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionSummary
{
    let handoff_summary =
        mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_summary_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );
    let action_rows =
        mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows_for_catalog(
            catalog,
            available_primitives,
            allowed_capabilities,
            enabled_integrations,
        );

    IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionSummary::from_summaries(
        handoff_summary,
        action_rows.iter(),
    )
}

pub fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_summary(
    available_primitives: &[PrimitiveFamily],
    allowed_capabilities: &[CapabilityId],
    enabled_integrations: &[IntegrationId],
) -> IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionSummary
{
    let catalog = first_party_catalog();
    mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_summary_for_catalog(
        &catalog,
        available_primitives,
        allowed_capabilities,
        enabled_integrations,
    )
}

fn mesh_protocol_catalog_entries(
    catalog: &[IntegrationCatalogEntry],
) -> Vec<IntegrationCatalogEntry> {
    catalog
        .iter()
        .filter(|entry| {
            entry
                .supported_protocols
                .iter()
                .any(is_low_level_mesh_protocol)
                || entry
                    .virtual_iot_standards
                    .iter()
                    .any(is_low_level_mesh_protocol)
        })
        .cloned()
        .collect()
}

fn is_low_level_mesh_protocol(protocol: &ProtocolFamily) -> bool {
    matches!(
        protocol,
        ProtocolFamily::Zigbee | ProtocolFamily::ZWave | ProtocolFamily::Thread
    )
}

fn mesh_protocol_sort_key(protocol: &ProtocolFamily) -> u8 {
    match protocol {
        ProtocolFamily::Zigbee => 0,
        ProtocolFamily::ZWave => 1,
        ProtocolFamily::Thread => 2,
        _ => u8::MAX,
    }
}

fn mesh_protocol_substrate_stage(
    primitive: PrimitiveFamily,
) -> Option<IntegrationMeshProtocolSubstrateStage> {
    match primitive {
        PrimitiveFamily::Usb | PrimitiveFamily::SerialController => {
            Some(IntegrationMeshProtocolSubstrateStage::Controller)
        }
        PrimitiveFamily::Radio802154 | PrimitiveFamily::ZWaveSerialApi => {
            Some(IntegrationMeshProtocolSubstrateStage::Radio)
        }
        PrimitiveFamily::Mdns => Some(IntegrationMeshProtocolSubstrateStage::Discovery),
        PrimitiveFamily::RadioNetworkKey => {
            Some(IntegrationMeshProtocolSubstrateStage::NetworkSecurity)
        }
        PrimitiveFamily::Supervision => Some(IntegrationMeshProtocolSubstrateStage::Supervision),
        _ => None,
    }
}

fn mesh_substrate_stage_blockers<'a>(
    rows: impl IntoIterator<Item = &'a IntegrationMeshProtocolSubstrateStageRow>,
    stage: IntegrationMeshProtocolSubstrateStage,
) -> usize {
    rows.into_iter()
        .filter(|row| row.stage == stage && row.is_blocked())
        .count()
}

fn compare_mesh_substrate_actions(
    left: &IntegrationMeshProtocolSubstrateAction,
    right: &IntegrationMeshProtocolSubstrateAction,
) -> Ordering {
    (
        left.stage,
        mesh_protocol_sort_key(&left.protocol),
        left.primitive,
    )
        .cmp(&(
            right.stage,
            mesh_protocol_sort_key(&right.protocol),
            right.primitive,
        ))
}

fn mesh_substrate_action_count<'a>(
    actions: impl IntoIterator<Item = &'a IntegrationMeshProtocolSubstrateAction>,
    stage: IntegrationMeshProtocolSubstrateStage,
) -> usize {
    actions
        .into_iter()
        .filter(|action| action.stage == stage)
        .count()
}

fn compare_mesh_substrate_preflight_checks(
    left: &IntegrationMeshProtocolSubstratePreflightCheck,
    right: &IntegrationMeshProtocolSubstratePreflightCheck,
) -> Ordering {
    (
        left.stage,
        mesh_protocol_sort_key(&left.protocol),
        left.primitive,
    )
        .cmp(&(
            right.stage,
            mesh_protocol_sort_key(&right.protocol),
            right.primitive,
        ))
}

fn mesh_substrate_preflight_blockers<'a>(
    checks: impl IntoIterator<Item = &'a IntegrationMeshProtocolSubstratePreflightCheck>,
    stage: IntegrationMeshProtocolSubstrateStage,
) -> usize {
    checks
        .into_iter()
        .filter(|check| check.stage == stage && check.blocks_preflight())
        .count()
}

fn compare_mesh_substrate_preflight_actions(
    left: &IntegrationMeshProtocolSubstratePreflightAction,
    right: &IntegrationMeshProtocolSubstratePreflightAction,
) -> Ordering {
    (
        left.stage,
        mesh_protocol_sort_key(&left.protocol),
        left.primitive,
    )
        .cmp(&(
            right.stage,
            mesh_protocol_sort_key(&right.protocol),
            right.primitive,
        ))
}

fn mesh_substrate_preflight_action_count<'a>(
    actions: impl IntoIterator<Item = &'a IntegrationMeshProtocolSubstratePreflightAction>,
    stage: IntegrationMeshProtocolSubstrateStage,
) -> usize {
    actions
        .into_iter()
        .filter(|action| action.stage == stage)
        .count()
}

fn compare_mesh_substrate_preflight_repair_batches(
    left: &IntegrationMeshProtocolSubstratePreflightRepairBatch,
    right: &IntegrationMeshProtocolSubstratePreflightRepairBatch,
) -> Ordering {
    (left.stage, left.action_kind).cmp(&(right.stage, right.action_kind))
}

fn mesh_substrate_preflight_repair_batch_count<'a>(
    batches: impl IntoIterator<Item = &'a IntegrationMeshProtocolSubstratePreflightRepairBatch>,
    stage: IntegrationMeshProtocolSubstrateStage,
) -> usize {
    batches
        .into_iter()
        .filter(|batch| batch.stage == stage)
        .count()
}

fn mesh_substrate_preflight_repair_schedule_slot_count<'a>(
    slots: impl IntoIterator<Item = &'a IntegrationMeshProtocolSubstratePreflightRepairScheduleSlot>,
    stage: IntegrationMeshProtocolSubstrateStage,
) -> usize {
    slots.into_iter().filter(|slot| slot.stage == stage).count()
}

fn protocol_sort_token(protocol: &ProtocolFamily) -> String {
    match protocol {
        ProtocolFamily::Vendor(value) => format!("vendor:{value}"),
        _ => format!("{protocol:?}"),
    }
}

fn mesh_substrate_action_stage_count(
    summary: &IntegrationMeshProtocolSubstrateActionSummary,
) -> usize {
    [
        summary.controller_actions,
        summary.radio_actions,
        summary.discovery_actions,
        summary.network_security_actions,
        summary.supervision_actions,
    ]
    .into_iter()
    .filter(|count| *count > 0)
    .count()
}

fn mesh_substrate_preflight_action_stage_count(
    summary: &IntegrationMeshProtocolSubstratePreflightActionSummary,
) -> usize {
    [
        summary.controller_actions,
        summary.radio_actions,
        summary.discovery_actions,
        summary.network_security_actions,
        summary.supervision_actions,
    ]
    .into_iter()
    .filter(|count| *count > 0)
    .count()
}

fn local_transport_primitives(
    connectivity: ConnectivityClass,
    discovery: &[DiscoveryMechanism],
    auth: &[AuthMode],
) -> Vec<PrimitiveFamily> {
    let mut primitives = vec![
        PrimitiveFamily::LocalHttp,
        PrimitiveFamily::CommandMapping,
        PrimitiveFamily::Supervision,
    ];

    if connectivity.is_push() {
        primitives.push(PrimitiveFamily::WebSocket);
    }

    for mechanism in discovery {
        let primitive = match mechanism {
            DiscoveryMechanism::Mdns => Some(PrimitiveFamily::Mdns),
            DiscoveryMechanism::WsDiscovery => Some(PrimitiveFamily::WsDiscovery),
            DiscoveryMechanism::Ssdp => Some(PrimitiveFamily::Ssdp),
            DiscoveryMechanism::UdpMulticast | DiscoveryMechanism::UdpBroadcast => {
                Some(PrimitiveFamily::Udp)
            }
            DiscoveryMechanism::Bluetooth => Some(PrimitiveFamily::BluetoothLowEnergy),
            DiscoveryMechanism::Usb => Some(PrimitiveFamily::Usb),
            DiscoveryMechanism::Dhcp => Some(PrimitiveFamily::Dhcp),
            DiscoveryMechanism::Mqtt => Some(PrimitiveFamily::Mqtt),
            DiscoveryMechanism::Manual
            | DiscoveryMechanism::CloudAccount
            | DiscoveryMechanism::Webhook
            | DiscoveryMechanism::FileConfig => None,
        };
        if let Some(primitive) = primitive {
            primitives.push(primitive);
        }
    }

    for mode in auth {
        let primitive = match mode {
            AuthMode::None => None,
            AuthMode::LocalPairing => Some(PrimitiveFamily::LocalPairing),
            AuthMode::LocalToken => Some(PrimitiveFamily::LocalToken),
            AuthMode::UsernamePassword | AuthMode::ApiKey => Some(PrimitiveFamily::VaultLease),
            AuthMode::OAuth2 => Some(PrimitiveFamily::OAuth2),
            AuthMode::Certificate => Some(PrimitiveFamily::CertificatePairing),
            AuthMode::RadioNetworkKey => Some(PrimitiveFamily::RadioNetworkKey),
            AuthMode::MqttCredentials => Some(PrimitiveFamily::MqttCredentials),
        };
        if let Some(primitive) = primitive {
            primitives.push(primitive);
        }
    }

    dedupe_primitives(primitives)
}

fn is_local_actuator(capability_id: &CapabilityId) -> bool {
    matches!(
        capability_id.as_str(),
        "smart_home.command.light" | "smart_home.command.switch" | "smart_home.command.media"
    )
}

fn requires_secret_lease(auth_mode: &AuthMode) -> bool {
    !matches!(auth_mode, AuthMode::None)
}

fn dedupe_primitives(primitives: Vec<PrimitiveFamily>) -> Vec<PrimitiveFamily> {
    let mut result = Vec::new();
    for primitive in primitives {
        if !result.contains(&primitive) {
            result.push(primitive);
        }
    }
    result
}

fn dedupe_capabilities(capabilities: &[&'static str]) -> Vec<CapabilityId> {
    let mut result = Vec::new();
    for capability_id in capabilities {
        let capability_id = capability(capability_id);
        if !result.contains(&capability_id) {
            result.push(capability_id);
        }
    }
    result
}

fn missing_primitives(
    required: &[PrimitiveFamily],
    available: &[PrimitiveFamily],
) -> Vec<PrimitiveFamily> {
    required
        .iter()
        .copied()
        .filter(|primitive| !available.contains(primitive))
        .collect()
}

fn missing_capabilities(required: &[CapabilityId], allowed: &[CapabilityId]) -> Vec<CapabilityId> {
    required
        .iter()
        .filter(|capability_id| !allowed.iter().any(|allowed| allowed == *capability_id))
        .cloned()
        .collect()
}

fn missing_dependencies_for_plan(
    plan: &IntegrationActivationPlan,
    enabled_integrations: &[IntegrationId],
) -> Vec<IntegrationId> {
    let mut dependencies = plan.depends_on_integrations.clone();
    if let IntegrationActivationTarget::DelegatedIntegration(target) = &plan.activation_target {
        if !dependencies.contains(target) {
            dependencies.push(target.clone());
        }
    }

    dependencies
        .into_iter()
        .filter(|integration_id| {
            !enabled_integrations
                .iter()
                .any(|enabled| enabled == integration_id)
        })
        .collect()
}

fn activation_dependency_ids_for_report(
    catalog: &[IntegrationCatalogEntry],
    report: &IntegrationReadinessReport,
) -> Vec<IntegrationId> {
    let mut dependencies =
        activation_plan_for_integration(catalog, &report.requested_integration_id)
            .map(|plan| plan.depends_on_integrations)
            .unwrap_or_default();
    if let IntegrationActivationTarget::DelegatedIntegration(target) = &report.activation_target {
        if !dependencies.contains(target) {
            dependencies.push(target.clone());
        }
    }
    dependencies.sort();
    dependencies.dedup();
    dependencies
}

fn dedupe_policy_surfaces(
    surfaces: Vec<IntegrationPolicySurface>,
) -> Vec<IntegrationPolicySurface> {
    let mut result = Vec::new();
    for surface in surfaces {
        if !result.contains(&surface) {
            result.push(surface);
        }
    }
    result
}

fn matches_any<T: PartialEq>(filters: &[T], value: &T) -> bool {
    filters.is_empty() || filters.iter().any(|filter| filter == value)
}

fn entry_local_only(entry: &IntegrationCatalogEntry) -> bool {
    entry.is_local() && !entry.requires_cloud()
}

fn entry_cloud_required(entry: &IntegrationCatalogEntry) -> bool {
    entry.requires_cloud()
        || entry
            .discovery_mechanisms
            .contains(&DiscoveryMechanism::CloudAccount)
}

fn sort_query_results(entries: &mut Vec<&IntegrationCatalogEntry>, sort: IntegrationCatalogSort) {
    match sort {
        IntegrationCatalogSort::PriorityThenName => {
            entries.sort_by(|left, right| compare_by_priority_then_name(left, right))
        }
        IntegrationCatalogSort::Name => entries.sort_by(|left, right| compare_by_name(left, right)),
        IntegrationCatalogSort::CategoryThenPriority => entries.sort_by(|left, right| {
            left.category
                .cmp(&right.category)
                .then_with(|| compare_by_priority_then_name(left, right))
        }),
        IntegrationCatalogSort::StatusThenPriority => entries.sort_by(|left, right| {
            left.implementation_status
                .cmp(&right.implementation_status)
                .then_with(|| compare_by_priority_then_name(left, right))
        }),
    }
}

fn activation_health_status_for_summary(
    summary: &IntegrationActivationCandidateSummary,
) -> IntegrationActivationHealthStatus {
    activation_health_status_from_counts(
        summary.ready_to_activate_candidates,
        summary.needs_human_review_candidates,
        summary.blocked_candidates,
    )
}

fn activation_health_status_from_counts(
    ready_to_activate_count: usize,
    review_count: usize,
    blocked_count: usize,
) -> IntegrationActivationHealthStatus {
    if blocked_count > 0 {
        IntegrationActivationHealthStatus::Blocked
    } else if review_count > 0 {
        IntegrationActivationHealthStatus::NeedsReview
    } else if ready_to_activate_count > 0 {
        IntegrationActivationHealthStatus::Ready
    } else {
        IntegrationActivationHealthStatus::Empty
    }
}

fn highest_policy_tier_for_integrations(
    integration_ids: &[IntegrationId],
    policy_tier_by_integration: &BTreeMap<IntegrationId, PrivilegeTier>,
) -> PrivilegeTier {
    integration_ids
        .iter()
        .filter_map(|integration_id| policy_tier_by_integration.get(integration_id).copied())
        .max()
        .unwrap_or(PrivilegeTier::ReadOnly)
}

fn compare_activation_candidates(
    left: &IntegrationActivationCandidate,
    right: &IntegrationActivationCandidate,
) -> Ordering {
    left.recommendation
        .cmp(&right.recommendation)
        .then_with(|| {
            left.readiness_report
                .priority
                .cmp(&right.readiness_report.priority)
        })
        .then_with(|| left.blocker_count.cmp(&right.blocker_count))
        .then_with(|| {
            left.readiness_report
                .display_name
                .cmp(&right.readiness_report.display_name)
        })
        .then_with(|| {
            left.readiness_report
                .requested_integration_id
                .cmp(&right.readiness_report.requested_integration_id)
        })
}

fn compare_activation_actions(
    left: &IntegrationActivationAction,
    right: &IntegrationActivationAction,
) -> Ordering {
    left.priority
        .cmp(&right.priority)
        .then_with(|| left.kind.sort_rank().cmp(&right.kind.sort_rank()))
        .then_with(|| left.display_name.cmp(&right.display_name))
        .then_with(|| {
            left.requested_integration_id
                .cmp(&right.requested_integration_id)
        })
        .then_with(|| left.primitive.cmp(&right.primitive))
        .then_with(|| left.capability_id.cmp(&right.capability_id))
        .then_with(|| {
            left.dependency_integration_id
                .cmp(&right.dependency_integration_id)
        })
}

fn compare_activation_constraints(
    left: &IntegrationActivationConstraint,
    right: &IntegrationActivationConstraint,
) -> Ordering {
    left.highest_priority
        .cmp(&right.highest_priority)
        .then_with(|| left.kind.cmp(&right.kind))
        .then_with(|| {
            right
                .affected_integration_ids
                .len()
                .cmp(&left.affected_integration_ids.len())
        })
        .then_with(|| left.constraint_id.cmp(&right.constraint_id))
}

fn compare_activation_risks(
    left: &IntegrationActivationRiskItem,
    right: &IntegrationActivationRiskItem,
) -> Ordering {
    left.highest_priority
        .cmp(&right.highest_priority)
        .then_with(|| right.required_tier.cmp(&left.required_tier))
        .then_with(|| left.kind.cmp(&right.kind))
        .then_with(|| right.integration_count().cmp(&left.integration_count()))
        .then_with(|| left.risk_id.cmp(&right.risk_id))
}

fn compare_activation_reviews(
    left: &IntegrationActivationReviewItem,
    right: &IntegrationActivationReviewItem,
) -> Ordering {
    left.priority
        .cmp(&right.priority)
        .then_with(|| right.required_tier.cmp(&left.required_tier))
        .then_with(|| left.display_name.cmp(&right.display_name))
        .then_with(|| {
            left.requested_integration_id
                .cmp(&right.requested_integration_id)
        })
}

fn compare_activation_approval_packets(
    left: &IntegrationActivationApprovalPacket,
    right: &IntegrationActivationApprovalPacket,
) -> Ordering {
    left.priority()
        .cmp(&right.priority())
        .then_with(|| right.required_tier().cmp(&left.required_tier()))
        .then_with(|| left.display_name().cmp(right.display_name()))
        .then_with(|| {
            left.requested_integration_id()
                .cmp(right.requested_integration_id())
        })
}

fn compare_activation_decisions(
    left: &IntegrationActivationDecisionItem,
    right: &IntegrationActivationDecisionItem,
) -> Ordering {
    left.priority()
        .cmp(&right.priority())
        .then_with(|| left.decision_status.cmp(&right.decision_status))
        .then_with(|| right.required_tier().cmp(&left.required_tier()))
        .then_with(|| left.display_name().cmp(right.display_name()))
        .then_with(|| {
            left.requested_integration_id()
                .cmp(right.requested_integration_id())
        })
}

fn compare_activation_evidence(
    left: &IntegrationActivationEvidenceItem,
    right: &IntegrationActivationEvidenceItem,
) -> Ordering {
    left.priority
        .cmp(&right.priority)
        .then_with(|| left.status.cmp(&right.status))
        .then_with(|| left.kind.cmp(&right.kind))
        .then_with(|| right.required_tier.cmp(&left.required_tier))
        .then_with(|| left.display_name.cmp(&right.display_name))
        .then_with(|| {
            left.requested_integration_id
                .cmp(&right.requested_integration_id)
        })
        .then_with(|| left.detail_id.cmp(&right.detail_id))
}

fn compare_activation_dossiers(
    left: &IntegrationActivationDossierItem,
    right: &IntegrationActivationDossierItem,
) -> Ordering {
    compare_activation_decisions(&left.decision, &right.decision)
        .then_with(|| {
            right
                .evidence_summary
                .blocking_evidence
                .cmp(&left.evidence_summary.blocking_evidence)
        })
        .then_with(|| {
            right
                .evidence_summary
                .review_evidence
                .cmp(&left.evidence_summary.review_evidence)
        })
        .then_with(|| {
            right
                .evidence_summary
                .total_evidence
                .cmp(&left.evidence_summary.total_evidence)
        })
}

fn compare_activation_readouts(
    left: &IntegrationActivationReadoutStage,
    right: &IntegrationActivationReadoutStage,
) -> Ordering {
    left.priority
        .cmp(&right.priority)
        .then_with(|| right.has_blockers().cmp(&left.has_blockers()))
        .then_with(|| right.has_review_work().cmp(&left.has_review_work()))
        .then_with(|| {
            right
                .has_approval_ready_work()
                .cmp(&left.has_approval_ready_work())
        })
        .then_with(|| right.has_activation_work().cmp(&left.has_activation_work()))
        .then_with(|| right.has_risks().cmp(&left.has_risks()))
}

fn compare_activation_briefing_items(
    left: &IntegrationActivationBriefingItem,
    right: &IntegrationActivationBriefingItem,
) -> Ordering {
    left.priority
        .cmp(&right.priority)
        .then_with(|| left.kind.cmp(&right.kind))
        .then_with(|| right.requires_attention().cmp(&left.requires_attention()))
        .then_with(|| right.integration_count().cmp(&left.integration_count()))
}

fn compare_activation_dashboard_cards(
    left: &IntegrationActivationDashboardCard,
    right: &IntegrationActivationDashboardCard,
) -> Ordering {
    left.priority
        .cmp(&right.priority)
        .then_with(|| right.requires_attention().cmp(&left.requires_attention()))
        .then_with(|| right.has_blockers.cmp(&left.has_blockers))
        .then_with(|| right.has_review_work.cmp(&left.has_review_work))
        .then_with(|| {
            right
                .has_approval_ready_work
                .cmp(&left.has_approval_ready_work)
        })
        .then_with(|| right.has_activation_work.cmp(&left.has_activation_work))
        .then_with(|| right.integration_count().cmp(&left.integration_count()))
}

fn compare_activation_timeline_milestones(
    left: &IntegrationActivationTimelineMilestone,
    right: &IntegrationActivationTimelineMilestone,
) -> Ordering {
    left.priority
        .cmp(&right.priority)
        .then_with(|| right.requires_attention().cmp(&left.requires_attention()))
        .then_with(|| left.milestone_kind.cmp(&right.milestone_kind))
        .then_with(|| right.has_blockers().cmp(&left.has_blockers()))
        .then_with(|| right.has_review_work().cmp(&left.has_review_work()))
        .then_with(|| {
            right
                .has_approval_ready_work()
                .cmp(&left.has_approval_ready_work())
        })
        .then_with(|| right.has_activation_work().cmp(&left.has_activation_work()))
        .then_with(|| right.integration_count().cmp(&left.integration_count()))
}

fn compare_activation_forecasts(
    left: &IntegrationActivationForecastItem,
    right: &IntegrationActivationForecastItem,
) -> Ordering {
    left.priority
        .cmp(&right.priority)
        .then_with(|| right.requires_attention().cmp(&left.requires_attention()))
        .then_with(|| left.forecast_action.cmp(&right.forecast_action))
        .then_with(|| left.milestone_kind.cmp(&right.milestone_kind))
        .then_with(|| right.has_blockers().cmp(&left.has_blockers()))
        .then_with(|| right.has_review_work().cmp(&left.has_review_work()))
        .then_with(|| {
            right
                .has_approval_ready_work()
                .cmp(&left.has_approval_ready_work())
        })
        .then_with(|| right.has_activation_work().cmp(&left.has_activation_work()))
        .then_with(|| right.integration_count().cmp(&left.integration_count()))
}

fn compare_activation_playbook_steps(
    left: &IntegrationActivationPlaybookStep,
    right: &IntegrationActivationPlaybookStep,
) -> Ordering {
    left.priority
        .cmp(&right.priority)
        .then_with(|| right.operator_required().cmp(&left.operator_required()))
        .then_with(|| left.playbook_action.cmp(&right.playbook_action))
        .then_with(|| left.recommended_view.cmp(&right.recommended_view))
        .then_with(|| right.blocked().cmp(&left.blocked()))
        .then_with(|| right.review_required().cmp(&left.review_required()))
        .then_with(|| right.activation_ready().cmp(&left.activation_ready()))
        .then_with(|| right.integration_count().cmp(&left.integration_count()))
}

fn compare_activation_runbook_entries(
    left: &IntegrationActivationRunbookEntry,
    right: &IntegrationActivationRunbookEntry,
) -> Ordering {
    left.priority
        .cmp(&right.priority)
        .then_with(|| right.requires_attention().cmp(&left.requires_attention()))
        .then_with(|| right.blocked().cmp(&left.blocked()))
        .then_with(|| right.review_required().cmp(&left.review_required()))
        .then_with(|| right.activation_ready().cmp(&left.activation_ready()))
        .then_with(|| left.phase.cmp(&right.phase))
        .then_with(|| left.playbook_action.cmp(&right.playbook_action))
        .then_with(|| right.audit_record_count.cmp(&left.audit_record_count))
        .then_with(|| right.integration_count().cmp(&left.integration_count()))
}

fn activation_handoff_status_sort_key(status: IntegrationActivationHandoffStatus) -> u8 {
    match status {
        IntegrationActivationHandoffStatus::Blocked => 0,
        IntegrationActivationHandoffStatus::NeedsReview => 1,
        IntegrationActivationHandoffStatus::Ready => 2,
        IntegrationActivationHandoffStatus::Monitoring => 3,
    }
}

fn activation_execution_status_sort_key(status: IntegrationActivationExecutionStatus) -> u8 {
    match status {
        IntegrationActivationExecutionStatus::Blocked => 0,
        IntegrationActivationExecutionStatus::NeedsApproval => 1,
        IntegrationActivationExecutionStatus::OperatorRequired => 2,
        IntegrationActivationExecutionStatus::Executable => 3,
        IntegrationActivationExecutionStatus::Monitoring => 4,
    }
}

fn activation_verification_status_sort_key(status: IntegrationActivationVerificationStatus) -> u8 {
    match status {
        IntegrationActivationVerificationStatus::Blocked => 0,
        IntegrationActivationVerificationStatus::PendingApproval => 1,
        IntegrationActivationVerificationStatus::PendingOperator => 2,
        IntegrationActivationVerificationStatus::ReadyToVerify => 3,
        IntegrationActivationVerificationStatus::Monitoring => 4,
    }
}

fn compare_activation_handoff_packages(
    left: &IntegrationActivationHandoffPackage,
    right: &IntegrationActivationHandoffPackage,
) -> Ordering {
    left.priority
        .cmp(&right.priority)
        .then_with(|| {
            activation_handoff_status_sort_key(left.handoff_status)
                .cmp(&activation_handoff_status_sort_key(right.handoff_status))
        })
        .then_with(|| right.requires_attention().cmp(&left.requires_attention()))
        .then_with(|| right.blocked().cmp(&left.blocked()))
        .then_with(|| right.review_required().cmp(&left.review_required()))
        .then_with(|| right.ready_for_handoff().cmp(&left.ready_for_handoff()))
        .then_with(|| {
            right
                .blocking_dependency_edge_count
                .cmp(&left.blocking_dependency_edge_count)
        })
        .then_with(|| right.readiness_gap_count.cmp(&left.readiness_gap_count))
        .then_with(|| right.risk_count.cmp(&left.risk_count))
        .then_with(|| left.phase.cmp(&right.phase))
        .then_with(|| left.runbook_sequence.cmp(&right.runbook_sequence))
}

fn compare_activation_execution_packets(
    left: &IntegrationActivationExecutionPacket,
    right: &IntegrationActivationExecutionPacket,
) -> Ordering {
    left.priority
        .cmp(&right.priority)
        .then_with(|| {
            activation_execution_status_sort_key(left.execution_status).cmp(
                &activation_execution_status_sort_key(right.execution_status),
            )
        })
        .then_with(|| right.requires_attention().cmp(&left.requires_attention()))
        .then_with(|| right.blocked().cmp(&left.blocked()))
        .then_with(|| right.approval_required().cmp(&left.approval_required()))
        .then_with(|| right.operator_required().cmp(&left.operator_required()))
        .then_with(|| right.executable().cmp(&left.executable()))
        .then_with(|| right.dependency_ready().cmp(&left.dependency_ready()))
        .then_with(|| right.risk_count.cmp(&left.risk_count))
        .then_with(|| left.phase.cmp(&right.phase))
        .then_with(|| left.runbook_sequence.cmp(&right.runbook_sequence))
}

fn compare_activation_verification_checkpoints(
    left: &IntegrationActivationVerificationCheckpoint,
    right: &IntegrationActivationVerificationCheckpoint,
) -> Ordering {
    left.priority
        .cmp(&right.priority)
        .then_with(|| {
            activation_verification_status_sort_key(left.verification_status).cmp(
                &activation_verification_status_sort_key(right.verification_status),
            )
        })
        .then_with(|| right.requires_attention().cmp(&left.requires_attention()))
        .then_with(|| right.blocked().cmp(&left.blocked()))
        .then_with(|| right.approval_pending().cmp(&left.approval_pending()))
        .then_with(|| right.operator_pending().cmp(&left.operator_pending()))
        .then_with(|| right.can_verify().cmp(&left.can_verify()))
        .then_with(|| right.verification_ready().cmp(&left.verification_ready()))
        .then_with(|| right.dependency_ready().cmp(&left.dependency_ready()))
        .then_with(|| right.risk_count.cmp(&left.risk_count))
        .then_with(|| left.phase.cmp(&right.phase))
        .then_with(|| left.runbook_sequence.cmp(&right.runbook_sequence))
}

fn compare_activation_operator_tasks(
    left: &IntegrationActivationOperatorTask,
    right: &IntegrationActivationOperatorTask,
) -> Ordering {
    left.priority
        .cmp(&right.priority)
        .then_with(|| right.operator_required().cmp(&left.operator_required()))
        .then_with(|| right.actionable().cmp(&left.actionable()))
        .then_with(|| right.blocked().cmp(&left.blocked()))
        .then_with(|| right.review_required().cmp(&left.review_required()))
        .then_with(|| right.activation_ready().cmp(&left.activation_ready()))
        .then_with(|| left.task_kind.cmp(&right.task_kind))
        .then_with(|| left.recommended_view.cmp(&right.recommended_view))
        .then_with(|| right.integration_count().cmp(&left.integration_count()))
}

fn compare_activation_control_room_panels(
    left: &IntegrationActivationControlRoomPanel,
    right: &IntegrationActivationControlRoomPanel,
) -> Ordering {
    left.priority
        .cmp(&right.priority)
        .then_with(|| right.requires_attention().cmp(&left.requires_attention()))
        .then_with(|| right.has_blockers().cmp(&left.has_blockers()))
        .then_with(|| right.has_review_work().cmp(&left.has_review_work()))
        .then_with(|| right.has_activation_work().cmp(&left.has_activation_work()))
        .then_with(|| right.has_actionable_work().cmp(&left.has_actionable_work()))
        .then_with(|| left.recommended_view.cmp(&right.recommended_view))
        .then_with(|| right.task_count.cmp(&left.task_count))
        .then_with(|| right.integration_count().cmp(&left.integration_count()))
}

fn compare_activation_command_center_sections(
    left: &IntegrationActivationCommandCenterSection,
    right: &IntegrationActivationCommandCenterSection,
) -> Ordering {
    left.priority
        .cmp(&right.priority)
        .then_with(|| right.requires_attention().cmp(&left.requires_attention()))
        .then_with(|| right.has_blockers().cmp(&left.has_blockers()))
        .then_with(|| right.has_review_work().cmp(&left.has_review_work()))
        .then_with(|| right.has_activation_work().cmp(&left.has_activation_work()))
        .then_with(|| right.has_actionable_work().cmp(&left.has_actionable_work()))
        .then_with(|| left.section_kind.cmp(&right.section_kind))
        .then_with(|| right.panel_count.cmp(&left.panel_count))
        .then_with(|| right.integration_count().cmp(&left.integration_count()))
}

fn compare_activation_watchtower_signals(
    left: &IntegrationActivationWatchtowerSignal,
    right: &IntegrationActivationWatchtowerSignal,
) -> Ordering {
    left.priority
        .cmp(&right.priority)
        .then_with(|| right.requires_attention().cmp(&left.requires_attention()))
        .then_with(|| right.has_blockers().cmp(&left.has_blockers()))
        .then_with(|| right.has_review_work().cmp(&left.has_review_work()))
        .then_with(|| right.has_activation_work().cmp(&left.has_activation_work()))
        .then_with(|| right.has_actionable_work().cmp(&left.has_actionable_work()))
        .then_with(|| left.signal_kind.cmp(&right.signal_kind))
        .then_with(|| right.section_count.cmp(&left.section_count))
        .then_with(|| right.integration_count().cmp(&left.integration_count()))
}

fn compare_activation_sentinel_alerts(
    left: &IntegrationActivationSentinelAlert,
    right: &IntegrationActivationSentinelAlert,
) -> Ordering {
    left.priority
        .cmp(&right.priority)
        .then_with(|| right.requires_attention().cmp(&left.requires_attention()))
        .then_with(|| right.has_blockers().cmp(&left.has_blockers()))
        .then_with(|| right.has_dependency_work().cmp(&left.has_dependency_work()))
        .then_with(|| right.has_policy_risk().cmp(&left.has_policy_risk()))
        .then_with(|| right.has_review_work().cmp(&left.has_review_work()))
        .then_with(|| right.has_activation_work().cmp(&left.has_activation_work()))
        .then_with(|| left.alert_kind.cmp(&right.alert_kind))
        .then_with(|| right.integration_count().cmp(&left.integration_count()))
}

fn compare_activation_audit_records(
    left: &IntegrationActivationAuditRecord,
    right: &IntegrationActivationAuditRecord,
) -> Ordering {
    left.priority
        .cmp(&right.priority)
        .then_with(|| right.requires_attention().cmp(&left.requires_attention()))
        .then_with(|| left.record_kind.cmp(&right.record_kind))
        .then_with(|| left.record_id.cmp(&right.record_id))
        .then_with(|| right.integration_count().cmp(&left.integration_count()))
}

fn compare_activation_escalation_cases(
    left: &IntegrationActivationEscalationCase,
    right: &IntegrationActivationEscalationCase,
) -> Ordering {
    left.priority
        .cmp(&right.priority)
        .then_with(|| right.requires_attention().cmp(&left.requires_attention()))
        .then_with(|| right.blocked().cmp(&left.blocked()))
        .then_with(|| right.has_dependency_work().cmp(&left.has_dependency_work()))
        .then_with(|| right.has_policy_risk().cmp(&left.has_policy_risk()))
        .then_with(|| right.ready_to_verify().cmp(&left.ready_to_verify()))
        .then_with(|| left.case_kind.cmp(&right.case_kind))
        .then_with(|| left.source_id.cmp(&right.source_id))
        .then_with(|| right.integration_count().cmp(&left.integration_count()))
}

fn compare_activation_response_items(
    left: &IntegrationActivationResponseItem,
    right: &IntegrationActivationResponseItem,
) -> Ordering {
    left.priority
        .cmp(&right.priority)
        .then_with(|| right.requires_attention().cmp(&left.requires_attention()))
        .then_with(|| right.blocked().cmp(&left.blocked()))
        .then_with(|| right.has_dependency_work().cmp(&left.has_dependency_work()))
        .then_with(|| right.has_policy_risk().cmp(&left.has_policy_risk()))
        .then_with(|| right.ready_to_verify().cmp(&left.ready_to_verify()))
        .then_with(|| left.response_kind.cmp(&right.response_kind))
        .then_with(|| left.owner_lane.cmp(&right.owner_lane))
        .then_with(|| left.source_id.cmp(&right.source_id))
        .then_with(|| right.integration_count().cmp(&left.integration_count()))
}

fn compare_activation_remediation_items(
    left: &IntegrationActivationRemediationItem,
    right: &IntegrationActivationRemediationItem,
) -> Ordering {
    left.priority
        .cmp(&right.priority)
        .then_with(|| left.status.cmp(&right.status))
        .then_with(|| right.requires_attention().cmp(&left.requires_attention()))
        .then_with(|| right.blocked().cmp(&left.blocked()))
        .then_with(|| right.has_dependency_work().cmp(&left.has_dependency_work()))
        .then_with(|| right.has_policy_risk().cmp(&left.has_policy_risk()))
        .then_with(|| right.ready_to_execute().cmp(&left.ready_to_execute()))
        .then_with(|| left.remediation_kind.cmp(&right.remediation_kind))
        .then_with(|| left.owner_lane.cmp(&right.owner_lane))
        .then_with(|| left.source_id.cmp(&right.source_id))
        .then_with(|| right.integration_count().cmp(&left.integration_count()))
}

fn compare_activation_closure_gates(
    left: &IntegrationActivationClosureGate,
    right: &IntegrationActivationClosureGate,
) -> Ordering {
    left.priority
        .cmp(&right.priority)
        .then_with(|| left.closure_status.cmp(&right.closure_status))
        .then_with(|| right.requires_attention().cmp(&left.requires_attention()))
        .then_with(|| right.blocked().cmp(&left.blocked()))
        .then_with(|| right.has_dependency_work().cmp(&left.has_dependency_work()))
        .then_with(|| right.has_policy_risk().cmp(&left.has_policy_risk()))
        .then_with(|| right.ready_to_verify().cmp(&left.ready_to_verify()))
        .then_with(|| right.closure_ready().cmp(&left.closure_ready()))
        .then_with(|| {
            left.source_remediation_kind
                .cmp(&right.source_remediation_kind)
        })
        .then_with(|| left.owner_lane.cmp(&right.owner_lane))
        .then_with(|| left.source_id.cmp(&right.source_id))
        .then_with(|| right.integration_count().cmp(&left.integration_count()))
}

fn compare_activation_release_packets(
    left: &IntegrationActivationReleasePacket,
    right: &IntegrationActivationReleasePacket,
) -> Ordering {
    left.priority
        .cmp(&right.priority)
        .then_with(|| left.release_status.cmp(&right.release_status))
        .then_with(|| right.requires_attention().cmp(&left.requires_attention()))
        .then_with(|| right.release_blocked().cmp(&left.release_blocked()))
        .then_with(|| right.needs_verification().cmp(&left.needs_verification()))
        .then_with(|| right.release_ready().cmp(&left.release_ready()))
        .then_with(|| right.has_dependency_work().cmp(&left.has_dependency_work()))
        .then_with(|| right.has_policy_risk().cmp(&left.has_policy_risk()))
        .then_with(|| left.source_closure_status.cmp(&right.source_closure_status))
        .then_with(|| {
            left.source_remediation_kind
                .cmp(&right.source_remediation_kind)
        })
        .then_with(|| left.owner_lane.cmp(&right.owner_lane))
        .then_with(|| left.source_id.cmp(&right.source_id))
        .then_with(|| right.integration_count().cmp(&left.integration_count()))
}

fn compare_activation_delivery_manifests(
    left: &IntegrationActivationDeliveryManifest,
    right: &IntegrationActivationDeliveryManifest,
) -> Ordering {
    left.priority
        .cmp(&right.priority)
        .then_with(|| left.delivery_status.cmp(&right.delivery_status))
        .then_with(|| right.requires_attention().cmp(&left.requires_attention()))
        .then_with(|| right.delivery_blocked().cmp(&left.delivery_blocked()))
        .then_with(|| right.needs_verification().cmp(&left.needs_verification()))
        .then_with(|| right.delivery_ready().cmp(&left.delivery_ready()))
        .then_with(|| right.has_dependency_work().cmp(&left.has_dependency_work()))
        .then_with(|| right.has_policy_risk().cmp(&left.has_policy_risk()))
        .then_with(|| left.delivery_channel.cmp(&right.delivery_channel))
        .then_with(|| left.source_release_status.cmp(&right.source_release_status))
        .then_with(|| {
            left.source_remediation_kind
                .cmp(&right.source_remediation_kind)
        })
        .then_with(|| left.owner_lane.cmp(&right.owner_lane))
        .then_with(|| left.source_id.cmp(&right.source_id))
        .then_with(|| right.integration_count().cmp(&left.integration_count()))
}

fn compare_activation_deployment_records(
    left: &IntegrationActivationDeploymentRecord,
    right: &IntegrationActivationDeploymentRecord,
) -> Ordering {
    left.priority
        .cmp(&right.priority)
        .then_with(|| left.deployment_status.cmp(&right.deployment_status))
        .then_with(|| right.requires_attention().cmp(&left.requires_attention()))
        .then_with(|| right.deployment_blocked().cmp(&left.deployment_blocked()))
        .then_with(|| right.needs_verification().cmp(&left.needs_verification()))
        .then_with(|| right.deployment_ready().cmp(&left.deployment_ready()))
        .then_with(|| right.has_dependency_work().cmp(&left.has_dependency_work()))
        .then_with(|| right.has_policy_risk().cmp(&left.has_policy_risk()))
        .then_with(|| left.deployment_ring.cmp(&right.deployment_ring))
        .then_with(|| {
            left.source_delivery_status
                .cmp(&right.source_delivery_status)
        })
        .then_with(|| left.source_release_status.cmp(&right.source_release_status))
        .then_with(|| {
            left.source_remediation_kind
                .cmp(&right.source_remediation_kind)
        })
        .then_with(|| left.owner_lane.cmp(&right.owner_lane))
        .then_with(|| left.source_id.cmp(&right.source_id))
        .then_with(|| right.integration_count().cmp(&left.integration_count()))
}

fn compare_activation_safety_gates(
    left: &IntegrationActivationSafetyGate,
    right: &IntegrationActivationSafetyGate,
) -> Ordering {
    left.priority
        .cmp(&right.priority)
        .then_with(|| left.gate_status.cmp(&right.gate_status))
        .then_with(|| right.requires_attention().cmp(&left.requires_attention()))
        .then_with(|| right.blocks_activation().cmp(&left.blocks_activation()))
        .then_with(|| right.needs_verification().cmp(&left.needs_verification()))
        .then_with(|| right.deployment_ready().cmp(&left.deployment_ready()))
        .then_with(|| right.has_dependency_work().cmp(&left.has_dependency_work()))
        .then_with(|| right.has_policy_risk().cmp(&left.has_policy_risk()))
        .then_with(|| left.deployment_ring.cmp(&right.deployment_ring))
        .then_with(|| left.owner_lane.cmp(&right.owner_lane))
        .then_with(|| left.source_id.cmp(&right.source_id))
        .then_with(|| right.integration_count().cmp(&left.integration_count()))
}

fn compare_activation_rollback_plans(
    left: &IntegrationActivationRollbackPlan,
    right: &IntegrationActivationRollbackPlan,
) -> Ordering {
    left.priority
        .cmp(&right.priority)
        .then_with(|| left.rollback_action.cmp(&right.rollback_action))
        .then_with(|| left.gate_status.cmp(&right.gate_status))
        .then_with(|| right.requires_attention().cmp(&left.requires_attention()))
        .then_with(|| right.blocks_activation().cmp(&left.blocks_activation()))
        .then_with(|| right.needs_verification().cmp(&left.needs_verification()))
        .then_with(|| right.rollback_ready().cmp(&left.rollback_ready()))
        .then_with(|| right.deployment_ready().cmp(&left.deployment_ready()))
        .then_with(|| right.has_dependency_work().cmp(&left.has_dependency_work()))
        .then_with(|| right.has_policy_risk().cmp(&left.has_policy_risk()))
        .then_with(|| left.deployment_ring.cmp(&right.deployment_ring))
        .then_with(|| left.owner_lane.cmp(&right.owner_lane))
        .then_with(|| left.source_id.cmp(&right.source_id))
        .then_with(|| right.integration_count().cmp(&left.integration_count()))
}

fn compare_activation_observability_probes(
    left: &IntegrationActivationObservabilityProbe,
    right: &IntegrationActivationObservabilityProbe,
) -> Ordering {
    left.priority
        .cmp(&right.priority)
        .then_with(|| left.observability_status.cmp(&right.observability_status))
        .then_with(|| left.rollback_action.cmp(&right.rollback_action))
        .then_with(|| left.gate_status.cmp(&right.gate_status))
        .then_with(|| right.requires_attention().cmp(&left.requires_attention()))
        .then_with(|| right.blocks_activation().cmp(&left.blocks_activation()))
        .then_with(|| right.needs_verification().cmp(&left.needs_verification()))
        .then_with(|| right.observability_ready().cmp(&left.observability_ready()))
        .then_with(|| right.rollback_ready().cmp(&left.rollback_ready()))
        .then_with(|| right.deployment_ready().cmp(&left.deployment_ready()))
        .then_with(|| left.deployment_ring.cmp(&right.deployment_ring))
        .then_with(|| left.owner_lane.cmp(&right.owner_lane))
        .then_with(|| left.source_id.cmp(&right.source_id))
        .then_with(|| right.integration_count().cmp(&left.integration_count()))
}

fn compare_activation_incident_briefs(
    left: &IntegrationActivationIncidentBrief,
    right: &IntegrationActivationIncidentBrief,
) -> Ordering {
    left.priority
        .cmp(&right.priority)
        .then_with(|| left.severity.cmp(&right.severity))
        .then_with(|| left.action.cmp(&right.action))
        .then_with(|| left.observability_status.cmp(&right.observability_status))
        .then_with(|| right.requires_attention().cmp(&left.requires_attention()))
        .then_with(|| right.blocks_activation().cmp(&left.blocks_activation()))
        .then_with(|| right.needs_verification().cmp(&left.needs_verification()))
        .then_with(|| right.incident_ready().cmp(&left.incident_ready()))
        .then_with(|| right.rollback_ready().cmp(&left.rollback_ready()))
        .then_with(|| right.deployment_ready().cmp(&left.deployment_ready()))
        .then_with(|| left.deployment_ring.cmp(&right.deployment_ring))
        .then_with(|| left.owner_lane.cmp(&right.owner_lane))
        .then_with(|| left.source_id.cmp(&right.source_id))
        .then_with(|| right.integration_count().cmp(&left.integration_count()))
}

fn compare_activation_guardrail_checks(
    left: &IntegrationActivationGuardrailCheck,
    right: &IntegrationActivationGuardrailCheck,
) -> Ordering {
    left.priority
        .cmp(&right.priority)
        .then_with(|| right.blocks_activation().cmp(&left.blocks_activation()))
        .then_with(|| right.requires_attention().cmp(&left.requires_attention()))
        .then_with(|| {
            guardrail_verdict_rank(right.verdict).cmp(&guardrail_verdict_rank(left.verdict))
        })
        .then_with(|| left.kind.cmp(&right.kind))
        .then_with(|| left.guardrail_id.cmp(&right.guardrail_id))
        .then_with(|| right.integration_count().cmp(&left.integration_count()))
}

fn guardrail_verdict_rank(verdict: IntegrationActivationGuardrailVerdict) -> u8 {
    match verdict {
        IntegrationActivationGuardrailVerdict::Pass => 0,
        IntegrationActivationGuardrailVerdict::Monitor => 1,
        IntegrationActivationGuardrailVerdict::NeedsReview => 2,
        IntegrationActivationGuardrailVerdict::Blocked => 3,
    }
}

// Helper forwards each alert field straight into `from_rollups`; a params struct
// here would just duplicate that constructor's signature.
#[allow(clippy::too_many_arguments)]
fn push_activation_sentinel_alert(
    alerts: &mut Vec<IntegrationActivationSentinelAlert>,
    alert_kind: IntegrationActivationSentinelAlertKind,
    priority: u8,
    integration_ids: &[IntegrationId],
    watchtower_summary: &IntegrationActivationWatchtowerSummary,
    risk_summary: &IntegrationActivationRiskSummary,
    dependency_summary: &IntegrationActivationDependencySummary,
    gap_inventory: &IntegrationReadinessGapInventory,
) {
    alerts.push(IntegrationActivationSentinelAlert::from_rollups(
        0,
        alert_kind,
        priority,
        integration_ids.to_vec(),
        watchtower_summary.clone(),
        risk_summary.clone(),
        dependency_summary.clone(),
        gap_inventory.clone(),
    ));
}

fn integration_ids_for_activation_sentinel_rollups(
    signals: &[IntegrationActivationWatchtowerSignal],
    risks: &[IntegrationActivationRiskItem],
    graph: &IntegrationActivationDependencyGraph,
    gap_inventory: &IntegrationReadinessGapInventory,
) -> Vec<IntegrationId> {
    let mut integration_ids = BTreeSet::new();
    for signal in signals {
        for integration_id in &signal.integration_ids {
            integration_ids.insert(integration_id.clone());
        }
    }
    for risk in risks {
        for integration_id in &risk.integration_ids {
            integration_ids.insert(integration_id.clone());
        }
    }
    for node in &graph.nodes {
        integration_ids.insert(node.integration_id.clone());
        for integration_id in &node.depends_on_integrations {
            integration_ids.insert(integration_id.clone());
        }
        for integration_id in &node.dependent_integration_ids {
            integration_ids.insert(integration_id.clone());
        }
        for integration_id in &node.missing_dependencies {
            integration_ids.insert(integration_id.clone());
        }
    }
    for edge in &graph.edges {
        integration_ids.insert(edge.dependency_integration_id.clone());
        integration_ids.insert(edge.dependent_integration_id.clone());
    }
    for gap in &gap_inventory.primitive_gaps {
        for integration_id in &gap.integration_ids {
            integration_ids.insert(integration_id.clone());
        }
    }
    for gap in &gap_inventory.capability_gaps {
        for integration_id in &gap.integration_ids {
            integration_ids.insert(integration_id.clone());
        }
    }
    for gap in &gap_inventory.dependency_gaps {
        integration_ids.insert(gap.integration_id.clone());
        for integration_id in &gap.requested_integration_ids {
            integration_ids.insert(integration_id.clone());
        }
    }
    integration_ids.into_iter().collect()
}

fn priority_for_activation_sentinel_alert(
    alert_kind: IntegrationActivationSentinelAlertKind,
    watchtower_summary: &IntegrationActivationWatchtowerSummary,
    risk_summary: &IntegrationActivationRiskSummary,
    dependency_summary: &IntegrationActivationDependencySummary,
    graph: &IntegrationActivationDependencyGraph,
    gap_inventory: &IntegrationReadinessGapInventory,
    risks: &[IntegrationActivationRiskItem],
) -> u8 {
    let priority = match alert_kind {
        IntegrationActivationSentinelAlertKind::Blocker => min_optional_priority(
            min_optional_priority(
                watchtower_summary.first_escalation_signal_priority,
                risk_summary.first_blocked_priority,
            ),
            min_optional_priority(
                dependency_summary.first_blocked_priority,
                first_gap_priority(gap_inventory),
            ),
        ),
        IntegrationActivationSentinelAlertKind::Dependency => min_optional_priority(
            min_optional_priority(
                dependency_summary.first_blocked_priority,
                first_dependency_edge_priority(graph),
            ),
            first_dependency_gap_priority(gap_inventory),
        ),
        IntegrationActivationSentinelAlertKind::PolicyRisk => {
            first_risk_priority(risks).or(risk_summary.first_review_priority)
        }
        IntegrationActivationSentinelAlertKind::Review => min_optional_priority(
            watchtower_summary.first_review_signal_priority,
            risk_summary.first_review_priority,
        ),
        IntegrationActivationSentinelAlertKind::Ready => min_optional_priority(
            watchtower_summary.first_ready_signal_priority,
            risk_summary.first_ready_priority,
        ),
        IntegrationActivationSentinelAlertKind::Observation => min_optional_priority(
            watchtower_summary.next_signal_priority,
            min_optional_priority(
                first_risk_priority(risks),
                min_optional_priority(
                    dependency_summary.first_blocked_priority,
                    first_gap_priority(gap_inventory),
                ),
            ),
        ),
    };

    priority.unwrap_or(u8::MAX)
}

fn first_risk_priority(risks: &[IntegrationActivationRiskItem]) -> Option<u8> {
    risks
        .iter()
        .map(|risk| risk.highest_priority)
        .min()
        .filter(|priority| *priority < u8::MAX)
}

fn first_dependency_edge_priority(graph: &IntegrationActivationDependencyGraph) -> Option<u8> {
    graph.edges.iter().map(|edge| edge.dependent_priority).min()
}

fn first_gap_priority(gap_inventory: &IntegrationReadinessGapInventory) -> Option<u8> {
    min_optional_priority(
        first_primitive_gap_priority(gap_inventory),
        min_optional_priority(
            first_capability_gap_priority(gap_inventory),
            first_dependency_gap_priority(gap_inventory),
        ),
    )
}

fn first_primitive_gap_priority(gap_inventory: &IntegrationReadinessGapInventory) -> Option<u8> {
    gap_inventory
        .primitive_gaps
        .iter()
        .map(|gap| gap.highest_priority)
        .min()
}

fn first_capability_gap_priority(gap_inventory: &IntegrationReadinessGapInventory) -> Option<u8> {
    gap_inventory
        .capability_gaps
        .iter()
        .map(|gap| gap.highest_priority)
        .min()
}

fn first_dependency_gap_priority(gap_inventory: &IntegrationReadinessGapInventory) -> Option<u8> {
    gap_inventory
        .dependency_gaps
        .iter()
        .map(|gap| gap.highest_priority)
        .min()
}

fn compare_activation_dependency_nodes(
    left: &IntegrationActivationDependencyNode,
    right: &IntegrationActivationDependencyNode,
) -> Ordering {
    left.priority
        .cmp(&right.priority)
        .then_with(|| left.display_name.cmp(&right.display_name))
        .then_with(|| left.integration_id.cmp(&right.integration_id))
}

fn compare_activation_dependency_edges(
    left: &IntegrationActivationDependencyEdge,
    right: &IntegrationActivationDependencyEdge,
) -> Ordering {
    left.dependent_priority
        .cmp(&right.dependent_priority)
        .then_with(|| {
            left.dependent_display_name
                .cmp(&right.dependent_display_name)
        })
        .then_with(|| {
            left.dependent_integration_id
                .cmp(&right.dependent_integration_id)
        })
        .then_with(|| {
            left.dependency_integration_id
                .cmp(&right.dependency_integration_id)
        })
}

fn min_optional_priority(left: Option<u8>, right: Option<u8>) -> Option<u8> {
    match (left, right) {
        (Some(left), Some(right)) => Some(left.min(right)),
        (Some(priority), None) | (None, Some(priority)) => Some(priority),
        (None, None) => None,
    }
}

fn privilege_tier_label_for_catalog(tier: PrivilegeTier) -> &'static str {
    match tier {
        PrivilegeTier::ReadOnly => "read_only",
        PrivilegeTier::LowRisk => "low_risk",
        PrivilegeTier::HumanApproval => "human_approval",
        PrivilegeTier::HighRisk => "high_risk",
    }
}

fn compare_by_priority_then_name(
    left: &IntegrationCatalogEntry,
    right: &IntegrationCatalogEntry,
) -> Ordering {
    left.priority
        .cmp(&right.priority)
        .then_with(|| compare_by_name(left, right))
}

fn compare_by_name(left: &IntegrationCatalogEntry, right: &IntegrationCatalogEntry) -> Ordering {
    left.display_name
        .cmp(&right.display_name)
        .then_with(|| left.integration_id.cmp(&right.integration_id))
}

fn capability(value: &'static str) -> CapabilityId {
    CapabilityId::trusted(value)
}

fn ecosystem_source(
    platform: EcosystemSurveyPlatform,
    display_name: &'static str,
    source_url: &'static str,
    source_surface: &'static str,
    contributes: &'static str,
    primitive_hints: &[PrimitiveFamily],
) -> EcosystemSurveySource {
    EcosystemSurveySource {
        platform,
        display_name,
        source_url,
        source_surface,
        contributes,
        primitive_hints: primitive_hints.to_vec(),
    }
}

fn read_catalog_tool(tool_id: &'static str) -> ToolDescriptor {
    ToolDescriptor {
        tool_id,
        side_effects: ToolSideEffects::Read,
        required_capabilities: vec![CapabilityId::trusted("smart_home.read")],
        required_tier: PrivilegeTier::ReadOnly,
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn first_party_catalog_includes_current_and_multiplier_integrations() {
        let catalog = first_party_catalog();

        assert!(find_entry(&catalog, &IntegrationId::trusted("hue")).is_some());
        assert!(find_entry(&catalog, &IntegrationId::trusted("mqtt")).is_some());
        assert!(find_entry(&catalog, &IntegrationId::trusted("matter")).is_some());
        assert!(find_entry(&catalog, &IntegrationId::trusted("esphome")).is_some());
        assert!(catalog.len() >= 30);
    }

    #[test]
    fn catalog_tools_are_read_only_d18d_descriptors() {
        let descriptors = integration_catalog_tool_descriptors();

        assert_eq!(descriptors.len(), 4);
        assert!(descriptors
            .iter()
            .any(|descriptor| descriptor.tool_id == "smart_home.list_integrations"));
        assert!(descriptors
            .iter()
            .all(|descriptor| descriptor.side_effects == ToolSideEffects::Read));
        assert!(descriptors
            .iter()
            .all(|descriptor| descriptor.required_tier == PrivilegeTier::ReadOnly));
        assert!(descriptors.iter().all(|descriptor| descriptor
            .required_capabilities
            .contains(&CapabilityId::trusted("smart_home.read"))));
    }

    #[test]
    fn primitive_family_descriptors_cover_every_primitive() {
        let descriptors = primitive_family_descriptors();

        assert_eq!(descriptors.len(), all_primitive_families().len());
        assert!(descriptors
            .iter()
            .any(|descriptor| descriptor.primitive == PrimitiveFamily::Mqtt
                && descriptor.display_name == "MQTT"));
        assert!(descriptors.iter().any(|descriptor| descriptor.primitive
            == PrimitiveFamily::Supervision
            && descriptor.summary.contains("restart")));
    }

    #[test]
    fn ecosystem_survey_sources_cover_reference_platforms() {
        let sources = ecosystem_survey_sources();

        assert_eq!(sources.len(), 15);
        assert_eq!(
            EcosystemSurveyPlatform::HomeAssistant.as_str(),
            "home_assistant"
        );
        assert!(
            survey_source_for_platform(&sources, EcosystemSurveyPlatform::HomeAssistant)
                .unwrap()
                .source_url
                .contains("home-assistant.io/integrations")
        );
        assert!(survey_source_for_platform(&sources, EcosystemSurveyPlatform::Hubitat).is_some());
        assert!(survey_source_for_platform(&sources, EcosystemSurveyPlatform::HomeyPro).is_some());
        assert!(survey_source_for_platform(&sources, EcosystemSurveyPlatform::OpenHab).is_some());
        assert!(sources
            .iter()
            .all(|source| !source.primitive_hints.is_empty()));
    }

    #[test]
    fn ecosystem_survey_sources_group_protocol_primitives() {
        let sources = ecosystem_survey_sources();
        let matter_sources =
            survey_sources_requiring_primitive(&sources, PrimitiveFamily::MatterCommissioning);
        let zwave_sources =
            survey_sources_requiring_primitive(&sources, PrimitiveFamily::ZWaveSerialApi);
        let mqtt_sources = survey_sources_requiring_primitive(&sources, PrimitiveFamily::Mqtt);

        assert!(matter_sources
            .iter()
            .any(|source| source.platform == EcosystemSurveyPlatform::AppleHome));
        assert!(matter_sources
            .iter()
            .any(|source| source.platform == EcosystemSurveyPlatform::GoogleHome));
        assert!(zwave_sources
            .iter()
            .any(|source| source.platform == EcosystemSurveyPlatform::ZWaveAlliance));
        assert!(mqtt_sources
            .iter()
            .any(|source| source.platform == EcosystemSurveyPlatform::HomeAssistant));
    }

    #[test]
    fn ecosystem_coverage_reports_platforms_for_primitives() {
        let sources = ecosystem_survey_sources();
        let coverage = ecosystem_primitive_coverage(&sources);
        let matter = coverage
            .iter()
            .find(|item| item.primitive == PrimitiveFamily::MatterCommissioning)
            .unwrap();
        let vault = coverage
            .iter()
            .find(|item| item.primitive == PrimitiveFamily::VaultLease)
            .unwrap();

        assert_eq!(coverage.len(), all_primitive_families().len());
        assert!(matter.covers_platform(EcosystemSurveyPlatform::AppleHome));
        assert!(matter.covers_platform(EcosystemSurveyPlatform::GoogleHome));
        assert!(matter.platform_count() >= 5);
        assert!(vault.is_gap());
    }

    #[test]
    fn primitive_backlog_coverage_connects_rollout_primitives_to_survey_sources() {
        let catalog = first_party_catalog();
        let sources = ecosystem_survey_sources();
        let coverage = primitive_backlog_with_ecosystem_coverage(&catalog, &sources, 1);
        let mqtt = coverage
            .iter()
            .find(|item| item.primitive == PrimitiveFamily::Mqtt)
            .unwrap();
        let matter = coverage
            .iter()
            .find(|item| item.primitive == PrimitiveFamily::MatterCommissioning)
            .unwrap();

        assert!(mqtt.includes_integration(&IntegrationId::trusted("mqtt")));
        assert!(mqtt.includes_integration(&IntegrationId::trusted("tasmota")));
        assert!(mqtt.covers_platform(EcosystemSurveyPlatform::HomeAssistant));
        assert!(matter.includes_integration(&IntegrationId::trusted("matter")));
        assert!(matter.covers_platform(EcosystemSurveyPlatform::ThreadGroup));
        assert!(coverage
            .iter()
            .all(|item| item.source_count == item.platform_count()));
    }

    #[test]
    fn primitive_backlog_coverage_summary_highlights_rollout_gaps() {
        let catalog = first_party_catalog();
        let sources = ecosystem_survey_sources();
        let coverage = primitive_backlog_with_ecosystem_coverage(&catalog, &sources, 1);
        let summary = PrimitiveBacklogCoverageSummary::from_items(coverage.iter());

        assert_eq!(summary.total_primitives, coverage.len());
        assert!(summary.total_entries >= coverage.len());
        assert!(summary.unique_integrations >= 5);
        assert!(summary.covered_primitives > 0);
        assert!(summary.uncovered_primitives > 0);
        assert!(summary.single_source_primitives > 0);
        assert!(summary.multi_platform_primitives > 0);
        assert!(summary.total_source_references >= summary.covered_primitives);
        assert!(summary.total_platform_references >= summary.total_source_references);
        assert!(summary.first_uncovered_priority.is_some());
        assert!(summary.first_single_source_priority.is_some());
        assert!(summary.broadest_platform_count >= 2);
        assert!(summary.has_uncovered_primitives());
        assert!(summary.has_single_source_primitives());
    }

    #[test]
    fn ecosystem_platform_coverage_rolls_sources_against_backlog() {
        let catalog = first_party_catalog();
        let sources = ecosystem_survey_sources();
        let coverage = ecosystem_platform_coverage(&catalog, &sources, 1);
        let summary = EcosystemPlatformCoverageSummary::from_items(coverage.iter());
        let home_assistant = coverage
            .iter()
            .find(|item| item.platform == EcosystemSurveyPlatform::HomeAssistant)
            .unwrap();
        let thread_group = coverage
            .iter()
            .find(|item| item.platform == EcosystemSurveyPlatform::ThreadGroup)
            .unwrap();

        assert_eq!(coverage.len(), sources.len());
        assert!(home_assistant.covers_primitive(PrimitiveFamily::Mqtt));
        assert!(home_assistant.has_backlog_overlap());
        assert!(thread_group.covers_primitive(PrimitiveFamily::MatterCommissioning));
        assert!(thread_group.has_backlog_overlap());
        assert_eq!(summary.total_platforms, sources.len());
        assert!(summary.covered_backlog_primitives > 0);
        assert!(summary.platforms_with_backlog_overlap >= 5);
        assert!(summary.has_uncovered_backlog_primitives());
    }

    #[test]
    fn connectivity_class_preserves_home_assistant_iot_class_names() {
        assert_eq!(
            ConnectivityClass::LocalPush.as_home_assistant_iot_class(),
            "local_push"
        );
        assert!(ConnectivityClass::CloudPolling.requires_cloud());
        assert!(ConnectivityClass::LocalPolling.is_local());
        assert!(ConnectivityClass::CloudPush.is_push());
    }

    #[test]
    fn virtual_aliases_route_to_real_targets_or_standards() {
        let catalog = first_party_catalog();
        let tapo = find_entry(&catalog, &IntegrationId::trusted("tplink_tapo")).unwrap();
        let ultraloq = find_entry(&catalog, &IntegrationId::trusted("ultraloq")).unwrap();

        assert!(tapo.is_virtual());
        assert_eq!(tapo.virtual_target, Some(IntegrationId::trusted("tplink")));
        assert_eq!(ultraloq.virtual_iot_standards, vec![ProtocolFamily::ZWave]);
        assert_eq!(tapo.runtime_kind, RuntimeKind::InProcessRust);
    }

    #[test]
    fn capability_queries_find_high_risk_surfaces() {
        let catalog = first_party_catalog();
        let lock_entries = entries_requiring_capability(
            &catalog,
            &CapabilityId::trusted("smart_home.command.lock"),
        );

        assert!(lock_entries
            .iter()
            .any(|entry| entry.integration_id == IntegrationId::trusted("zwave")));
        assert!(lock_entries
            .iter()
            .any(|entry| entry.integration_id == IntegrationId::trusted("matter")));
    }

    #[test]
    fn priority_queries_include_wave_zero_and_one() {
        let catalog = first_party_catalog();
        let early = entries_at_or_before_priority(&catalog, 1);

        assert!(early
            .iter()
            .any(|entry| entry.integration_id == IntegrationId::trusted("hue")));
        assert!(early
            .iter()
            .any(|entry| entry.integration_id == IntegrationId::trusted("mqtt")));
        assert!(!early
            .iter()
            .any(|entry| entry.integration_id == IntegrationId::trusted("tuya")));
    }

    #[test]
    fn primitive_backlog_ranks_rollout_wave_foundations() {
        let catalog = first_party_catalog();
        let backlog = primitive_backlog_at_or_before_priority(&catalog, 1);
        let supervision = backlog
            .iter()
            .find(|item| item.primitive == PrimitiveFamily::Supervision)
            .unwrap();
        let radio = backlog
            .iter()
            .find(|item| item.primitive == PrimitiveFamily::Radio802154)
            .unwrap();
        let mqtt = backlog
            .iter()
            .find(|item| item.primitive == PrimitiveFamily::Mqtt)
            .unwrap();

        assert_eq!(backlog[0].highest_priority, 0);
        assert_eq!(supervision.highest_priority, 0);
        assert!(supervision.entry_count >= 8);
        assert!(supervision.includes_integration(&IntegrationId::trusted("hue")));
        assert!(supervision.includes_integration(&IntegrationId::trusted("zigbee")));
        assert!(supervision.includes_integration(&IntegrationId::trusted("zwave")));
        assert!(supervision.includes_integration(&IntegrationId::trusted("thread")));
        assert!(radio.includes_integration(&IntegrationId::trusted("zigbee")));
        assert!(radio.includes_integration(&IntegrationId::trusted("thread")));
        assert!(mqtt.includes_integration(&IntegrationId::trusted("mqtt")));
        assert!(mqtt.includes_integration(&IntegrationId::trusted("tasmota")));
    }

    #[test]
    fn activation_plans_resolve_direct_virtual_and_standard_targets() {
        let catalog = first_party_catalog();
        let hue =
            activation_plan_for_integration(&catalog, &IntegrationId::trusted("hue")).unwrap();
        let tapo =
            activation_plan_for_integration(&catalog, &IntegrationId::trusted("tplink_tapo"))
                .unwrap();
        let ultraloq =
            activation_plan_for_integration(&catalog, &IntegrationId::trusted("ultraloq")).unwrap();

        assert_eq!(hue.activation_target, IntegrationActivationTarget::Direct);
        assert!(hue.local_only);
        assert!(hue.requires_primitive(PrimitiveFamily::LocalPairing));

        assert!(tapo.delegates_to_integration(&IntegrationId::trusted("tplink")));
        assert_eq!(tapo.runtime_kind, RuntimeKind::InProcessRust);

        assert!(ultraloq.delegates_to_standard(&ProtocolFamily::ZWave));
        assert!(ultraloq.requires_primitive(PrimitiveFamily::CalculatedState));
    }

    #[test]
    fn activation_plans_capture_review_and_cloud_boundaries() {
        let catalog = first_party_catalog();
        let zwave =
            activation_plan_for_integration(&catalog, &IntegrationId::trusted("zwave")).unwrap();
        let ring =
            activation_plan_for_integration(&catalog, &IntegrationId::trusted("ring")).unwrap();

        assert!(zwave.requires_human_review());
        assert_eq!(zwave.highest_policy_tier, PrivilegeTier::HighRisk);
        assert!(zwave
            .policy_surfaces
            .contains(&IntegrationPolicySurface::EntryAccess));
        assert!(zwave.requires_primitive(PrimitiveFamily::RadioNetworkKey));
        assert!(zwave.requires_capability(&CapabilityId::trusted("smart_home.command.lock")));

        assert!(ring.cloud_required);
        assert!(ring.requires_human_review());
        assert!(ring
            .policy_surfaces
            .contains(&IntegrationPolicySurface::CredentialedCloud));
    }

    #[test]
    fn activation_plans_follow_rollout_priority_waves() {
        let catalog = first_party_catalog();
        let early = activation_plans_at_or_before_priority(&catalog, 2);

        assert!(early
            .iter()
            .any(|plan| plan.requested_integration_id == IntegrationId::trusted("hue")));
        assert!(early
            .iter()
            .any(|plan| plan.requested_integration_id == IntegrationId::trusted("mqtt")));
        assert!(!early
            .iter()
            .any(|plan| plan.requested_integration_id == IntegrationId::trusted("tuya")));
        assert!(early.iter().any(|plan| plan
            .depends_on_integrations
            .contains(&IntegrationId::trusted("mqtt"))));

        let summary = IntegrationActivationPlanSummary::from_plans(early.iter());
        assert_eq!(summary.total_plans, early.len());
        assert!(summary.direct_targets > 0);
        assert!(summary.has_delegated_targets());
        assert!(summary.local_only_plans > 0);
        assert!(summary.plans_with_dependencies > 0);
        assert!(summary.plans_with_required_primitives > 0);
        assert!(summary.plans_with_required_capabilities > 0);
        assert!(summary.unique_required_primitives > 0);
        assert!(summary.unique_required_capabilities > 0);
        assert!(summary.unique_dependencies > 0);
        assert!(!summary.is_empty());
    }

    #[test]
    fn readiness_reports_identify_missing_primitives_and_capabilities() {
        let catalog = first_party_catalog();
        let report = readiness_report_for_integration(
            &catalog,
            &IntegrationId::trusted("hue"),
            &[PrimitiveFamily::Mdns],
            &[CapabilityId::trusted("smart_home.read")],
            &[],
        )
        .unwrap();

        assert!(report.is_blocked());
        assert!(report.missing_primitive(PrimitiveFamily::LocalHttp));
        assert!(report.missing_primitive(PrimitiveFamily::ServerSentEvents));
        assert!(report.missing_primitive(PrimitiveFamily::LocalPairing));
        assert!(report.missing_capability(&CapabilityId::trusted("smart_home.command.light")));
        assert!(report.missing_capability(&CapabilityId::trusted("smart_home.pair")));
        assert!(!report.missing_dependency(&IntegrationId::trusted("mqtt")));
        assert!(report.requires_human_review);
    }

    #[test]
    fn readiness_reports_mark_complete_direct_integrations_ready() {
        let catalog = first_party_catalog();
        let hue = find_entry(&catalog, &IntegrationId::trusted("hue")).unwrap();
        let report = readiness_report_for_integration(
            &catalog,
            &hue.integration_id,
            &hue.required_primitives,
            &hue.required_capabilities,
            &[],
        )
        .unwrap();

        assert!(report.activation_ready());
        assert_eq!(
            report.requested_integration_id,
            IntegrationId::trusted("hue")
        );
        assert_eq!(
            report.activation_target,
            IntegrationActivationTarget::Direct
        );
        assert!(report.local_only);
        assert!(!report.cloud_required);
        assert_eq!(report.highest_policy_tier, PrivilegeTier::HumanApproval);
    }

    #[test]
    fn activation_candidates_rank_ready_review_and_blocked_work() {
        let catalog = first_party_catalog();
        let hue = find_entry(&catalog, &IntegrationId::trusted("hue")).unwrap();
        let tasmota = find_entry(&catalog, &IntegrationId::trusted("tasmota")).unwrap();
        let ready_report = IntegrationReadinessReport {
            requested_integration_id: IntegrationId::trusted("read_only_probe"),
            display_name: "Read-only Probe".to_string(),
            activation_target: IntegrationActivationTarget::Direct,
            priority: 3,
            missing_primitives: Vec::new(),
            missing_capabilities: Vec::new(),
            missing_dependencies: Vec::new(),
            requires_human_review: false,
            highest_policy_tier: PrivilegeTier::ReadOnly,
            local_only: true,
            cloud_required: false,
        };
        let review_report = readiness_report_for_integration(
            &catalog,
            &hue.integration_id,
            &hue.required_primitives,
            &hue.required_capabilities,
            &[],
        )
        .unwrap();
        let blocked_report = readiness_report_for_integration(
            &catalog,
            &tasmota.integration_id,
            &[],
            &[CapabilityId::trusted("smart_home.read")],
            &[],
        )
        .unwrap();

        assert!(review_report.activation_ready());
        assert!(review_report.requires_human_review);
        assert!(blocked_report.is_blocked());

        let candidates = activation_candidates_from_reports(
            [blocked_report, review_report, ready_report].iter(),
        );

        assert_eq!(
            candidates[0].recommendation,
            IntegrationActivationCandidateRecommendation::ReadyToActivate
        );
        assert_eq!(
            candidates[1].recommendation,
            IntegrationActivationCandidateRecommendation::NeedsHumanReview
        );
        assert_eq!(
            candidates[2].recommendation,
            IntegrationActivationCandidateRecommendation::BlockedOnPrerequisites
        );
        assert!(candidates[0].is_actionable());
        assert!(candidates[2].is_blocked());
        assert!(candidates[2].blocker_count > 0);

        let summary = IntegrationActivationCandidateSummary::from_candidates(candidates.iter());
        assert_eq!(summary.total_candidates, 3);
        assert_eq!(summary.ready_to_activate_candidates, 1);
        assert_eq!(summary.needs_human_review_candidates, 1);
        assert_eq!(summary.blocked_candidates, 1);
        assert_eq!(summary.activation_ready_candidates, 2);
        assert_eq!(summary.candidates_requiring_human_review, 2);
        assert!(summary.candidates_missing_primitives > 0);
        assert!(summary.candidates_missing_capabilities > 0);
        assert!(summary.candidates_missing_dependencies > 0);
        assert!(summary.has_actionable_candidates());
        assert!(summary.has_blockers());
        assert!(summary.has_review_work());
        assert!(!summary.is_empty());
    }

    #[test]
    fn activation_runway_groups_candidates_by_priority_wave() {
        let ready_report = IntegrationReadinessReport {
            requested_integration_id: IntegrationId::trusted("read_only_probe"),
            display_name: "Read-only Probe".to_string(),
            activation_target: IntegrationActivationTarget::Direct,
            priority: 2,
            missing_primitives: Vec::new(),
            missing_capabilities: Vec::new(),
            missing_dependencies: Vec::new(),
            requires_human_review: false,
            highest_policy_tier: PrivilegeTier::ReadOnly,
            local_only: true,
            cloud_required: false,
        };
        let review_report = IntegrationReadinessReport {
            requested_integration_id: IntegrationId::trusted("review_bridge"),
            display_name: "Review Bridge".to_string(),
            activation_target: IntegrationActivationTarget::Direct,
            priority: 1,
            missing_primitives: Vec::new(),
            missing_capabilities: Vec::new(),
            missing_dependencies: Vec::new(),
            requires_human_review: true,
            highest_policy_tier: PrivilegeTier::HumanApproval,
            local_only: true,
            cloud_required: false,
        };
        let blocked_report = IntegrationReadinessReport {
            requested_integration_id: IntegrationId::trusted("blocked_sensor"),
            display_name: "Blocked Sensor".to_string(),
            activation_target: IntegrationActivationTarget::Direct,
            priority: 1,
            missing_primitives: vec![PrimitiveFamily::Mqtt],
            missing_capabilities: vec![CapabilityId::trusted("smart_home.command.low_risk")],
            missing_dependencies: Vec::new(),
            requires_human_review: false,
            highest_policy_tier: PrivilegeTier::LowRisk,
            local_only: true,
            cloud_required: false,
        };
        let candidates = activation_candidates_from_reports(
            [ready_report, review_report, blocked_report].iter(),
        );

        let stages = activation_runway_from_candidates(candidates);

        assert_eq!(stages.len(), 2);
        assert_eq!(stages[0].priority, 1);
        assert_eq!(stages[0].summary.total_candidates, 2);
        assert_eq!(stages[0].summary.needs_human_review_candidates, 1);
        assert_eq!(stages[0].summary.blocked_candidates, 1);
        assert!(stages[0].has_actionable_candidates());
        assert!(stages[0].has_blockers());
        assert!(stages[0].has_review_work());
        assert_eq!(stages[1].priority, 2);
        assert_eq!(stages[1].summary.ready_to_activate_candidates, 1);

        let summary = IntegrationActivationRunwaySummary::from_stages(stages.iter());
        assert_eq!(summary.total_stages, 2);
        assert_eq!(summary.total_candidates, 3);
        assert_eq!(summary.actionable_stages, 2);
        assert_eq!(summary.ready_stages, 1);
        assert_eq!(summary.review_stages, 1);
        assert_eq!(summary.blocked_stages, 1);
        assert_eq!(summary.first_actionable_priority, Some(1));
        assert_eq!(summary.first_blocked_priority, Some(1));
        assert_eq!(summary.next_ready_priority, Some(2));
        assert_eq!(summary.candidate_summary.total_candidates, 3);
        assert!(summary.has_actionable_stage());
        assert!(summary.has_blocked_stage());
        assert!(summary.has_review_stage());
        assert!(!summary.is_empty());
    }

    #[test]
    fn activation_health_rolls_up_priority_stage_attention() {
        let ready_report = IntegrationReadinessReport {
            requested_integration_id: IntegrationId::trusted("read_only_probe"),
            display_name: "Read-only Probe".to_string(),
            activation_target: IntegrationActivationTarget::Direct,
            priority: 2,
            missing_primitives: Vec::new(),
            missing_capabilities: Vec::new(),
            missing_dependencies: Vec::new(),
            requires_human_review: false,
            highest_policy_tier: PrivilegeTier::ReadOnly,
            local_only: true,
            cloud_required: false,
        };
        let review_report = IntegrationReadinessReport {
            requested_integration_id: IntegrationId::trusted("review_bridge"),
            display_name: "Review Bridge".to_string(),
            activation_target: IntegrationActivationTarget::Direct,
            priority: 1,
            missing_primitives: Vec::new(),
            missing_capabilities: Vec::new(),
            missing_dependencies: Vec::new(),
            requires_human_review: true,
            highest_policy_tier: PrivilegeTier::HumanApproval,
            local_only: true,
            cloud_required: false,
        };
        let blocked_report = IntegrationReadinessReport {
            requested_integration_id: IntegrationId::trusted("blocked_sensor"),
            display_name: "Blocked Sensor".to_string(),
            activation_target: IntegrationActivationTarget::DelegatedIntegration(
                IntegrationId::trusted("mqtt"),
            ),
            priority: 1,
            missing_primitives: vec![PrimitiveFamily::Mqtt],
            missing_capabilities: vec![CapabilityId::trusted("smart_home.command.low_risk")],
            missing_dependencies: vec![IntegrationId::trusted("mqtt")],
            requires_human_review: false,
            highest_policy_tier: PrivilegeTier::LowRisk,
            local_only: true,
            cloud_required: false,
        };
        let candidates = activation_candidates_from_reports(
            [ready_report, review_report, blocked_report].iter(),
        );

        let health = activation_health_from_candidates(candidates);

        assert_eq!(health.len(), 2);
        assert_eq!(health[0].priority, 1);
        assert_eq!(
            health[0].health_status,
            IntegrationActivationHealthStatus::Blocked
        );
        assert!(health[0].requires_attention());
        assert!(health[0].has_review_work());
        assert!(health[0].has_blockers());
        assert_eq!(health[0].candidate_summary.total_candidates, 2);
        assert_eq!(health[0].gap_inventory.primitive_gap_count(), 1);
        assert_eq!(health[0].gap_inventory.capability_gap_count(), 1);
        assert_eq!(health[0].gap_inventory.dependency_gap_count(), 1);
        assert!(health[0]
            .blocked_integration_ids
            .contains(&IntegrationId::trusted("blocked_sensor")));
        assert!(health[0]
            .review_integration_ids
            .contains(&IntegrationId::trusted("review_bridge")));
        assert_eq!(health[1].priority, 2);
        assert_eq!(
            health[1].health_status,
            IntegrationActivationHealthStatus::Ready
        );
        assert!(health[1]
            .ready_to_activate_integration_ids
            .contains(&IntegrationId::trusted("read_only_probe")));

        let summary = IntegrationActivationHealthSummary::from_stages(health.iter());
        assert_eq!(summary.total_stages, 2);
        assert_eq!(summary.total_integrations, 3);
        assert_eq!(summary.ready_stages, 1);
        assert_eq!(summary.review_stages, 1);
        assert_eq!(summary.blocked_stages, 1);
        assert_eq!(summary.ready_to_activate_integrations, 1);
        assert_eq!(summary.review_integrations, 1);
        assert_eq!(summary.blocked_integrations, 1);
        assert_eq!(summary.primitive_gap_count, 1);
        assert_eq!(summary.capability_gap_count, 1);
        assert_eq!(summary.dependency_gap_count, 1);
        assert_eq!(summary.total_unique_gaps, 3);
        assert_eq!(summary.first_ready_priority, Some(2));
        assert_eq!(summary.first_review_priority, Some(1));
        assert_eq!(summary.first_blocked_priority, Some(1));
        assert_eq!(
            summary.overall_status,
            IntegrationActivationHealthStatus::Blocked
        );
        assert!(summary.requires_attention());
        assert!(summary.has_ready_work());
        assert!(summary.has_review_work());
        assert!(summary.has_blockers());
        assert!(!summary.is_empty());
    }

    #[test]
    fn activation_maintenance_windows_roll_up_activation_work() {
        let catalog = first_party_catalog();
        let available_primitives = vec![
            PrimitiveFamily::NormalizedModel,
            PrimitiveFamily::DiscoveryIndex,
            PrimitiveFamily::CommandMapping,
            PrimitiveFamily::CapabilityPolicy,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let enabled_integrations = vec![IntegrationId::trusted("mqtt")];
        let candidates = activation_candidates_at_or_before_priority(
            &catalog,
            2,
            &available_primitives,
            &allowed_capabilities,
            &enabled_integrations,
        );

        let windows = activation_maintenance_from_candidates(
            &catalog,
            candidates.clone(),
            &enabled_integrations,
        );

        assert!(!windows.is_empty());
        assert!(windows.iter().all(|window| window.priority <= 2));
        assert!(windows
            .iter()
            .any(IntegrationActivationMaintenanceWindow::has_blockers));
        assert!(windows
            .iter()
            .any(IntegrationActivationMaintenanceWindow::has_review_work));
        let first_blocked = windows.iter().find(|window| window.has_blockers()).unwrap();
        assert!(!first_blocked.integration_ids.is_empty());
        assert!(first_blocked.action_summary.total_actions > 0);
        assert!(first_blocked.constraint_summary.total_constraints > 0);
        assert!(first_blocked.risk_summary.total_risks > 0);

        let summary = IntegrationActivationMaintenanceSummary::from_windows(windows.iter());
        assert_eq!(summary.total_windows, windows.len());
        assert_eq!(summary.total_integrations, candidates.len());
        assert!(summary.total_actions > 0);
        assert!(summary.blocking_constraints > 0);
        assert!(summary.total_risks > 0);
        assert!(summary.total_dependency_edges > 0);
        assert!(summary.windows_with_actions > 0);
        assert!(summary.windows_with_blockers > 0);
        assert!(summary.windows_with_review_work > 0);
        assert!(summary.windows_with_risks > 0);
        assert!(summary.requires_attention());
        assert!(summary.has_blockers());
        assert!(summary.has_review_work());
        assert!(summary.has_risks());
        assert!(!summary.is_empty());

        let windows_from_catalog = activation_maintenance_at_or_before_priority(
            &catalog,
            2,
            &available_primitives,
            &allowed_capabilities,
            &enabled_integrations,
        );
        assert_eq!(windows, windows_from_catalog);
    }

    #[test]
    fn activation_constraints_group_readiness_and_policy_review_work() {
        let catalog = first_party_catalog();
        let available_primitives = vec![
            PrimitiveFamily::NormalizedModel,
            PrimitiveFamily::DiscoveryIndex,
            PrimitiveFamily::CommandMapping,
            PrimitiveFamily::CapabilityPolicy,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let candidates = activation_candidates_at_or_before_priority(
            &catalog,
            2,
            &available_primitives,
            &allowed_capabilities,
            &[],
        );

        let constraints = activation_constraints_from_candidates(&catalog, candidates.iter());

        assert!(constraints.iter().any(|constraint| constraint.kind
            == IntegrationActivationConstraintKind::Primitive
            && constraint.blocks_activation));
        assert!(constraints.iter().any(|constraint| constraint.kind
            == IntegrationActivationConstraintKind::Capability
            && constraint.blocks_activation));
        assert!(constraints.iter().any(|constraint| constraint.kind
            == IntegrationActivationConstraintKind::Dependency
            && constraint.blocks_activation));
        let policy_review = constraints
            .iter()
            .find(|constraint| constraint.kind == IntegrationActivationConstraintKind::PolicyReview)
            .unwrap();
        assert!(policy_review.requires_human_review);
        assert!(!policy_review.blocks_activation);
        assert!(!policy_review.policy_surfaces.is_empty());
        assert_eq!(
            policy_review.kind.as_str(),
            IntegrationActivationConstraintKind::PolicyReview.as_str()
        );

        let summary = IntegrationActivationConstraintSummary::from_constraints(constraints.iter());
        assert_eq!(summary.total_constraints, constraints.len());
        assert!(summary.blocking_constraints >= 3);
        assert!(summary.review_constraints > 0);
        assert!(summary.primitive_constraints > 0);
        assert!(summary.capability_constraints > 0);
        assert!(summary.dependency_constraints > 0);
        assert!(summary.policy_review_constraints > 0);
        assert!(summary.affected_integrations > 0);
        assert!(summary.first_blocking_priority <= Some(2));
        assert!(summary.first_review_priority <= Some(2));
        assert!(summary.has_blockers());
        assert!(summary.has_review_work());
        assert!(!summary.is_empty());

        let constraints_from_catalog = activation_constraints_at_or_before_priority(
            &catalog,
            2,
            &available_primitives,
            &allowed_capabilities,
            &[],
        );
        assert_eq!(constraints, constraints_from_catalog);
    }

    #[test]
    fn activation_risk_groups_candidates_by_policy_tier_and_surface() {
        let catalog = first_party_catalog();
        let available_primitives = vec![
            PrimitiveFamily::NormalizedModel,
            PrimitiveFamily::DiscoveryIndex,
            PrimitiveFamily::CommandMapping,
            PrimitiveFamily::CapabilityPolicy,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let candidates = activation_candidates_at_or_before_priority(
            &catalog,
            2,
            &available_primitives,
            &allowed_capabilities,
            &[],
        );

        let risks = activation_risk_from_candidates(&catalog, candidates.iter());

        assert!(risks
            .iter()
            .any(|risk| risk.kind == IntegrationActivationRiskKind::PolicyTier));
        assert!(risks
            .iter()
            .any(|risk| risk.kind == IntegrationActivationRiskKind::PolicySurface));

        let human_approval = risks
            .iter()
            .find(|risk| {
                risk.kind == IntegrationActivationRiskKind::PolicyTier
                    && risk.required_tier == PrivilegeTier::HumanApproval
            })
            .unwrap();
        assert!(human_approval.integration_count() >= 1);
        assert!(human_approval.requires_attention());

        let review_surface = risks
            .iter()
            .find(|risk| {
                risk.kind == IntegrationActivationRiskKind::PolicySurface
                    && risk.required_tier >= PrivilegeTier::HumanApproval
            })
            .unwrap();
        assert!(review_surface.policy_surface.is_some());
        assert!(review_surface.integration_count() >= 1);
        assert!(review_surface.requires_attention());

        let summary = IntegrationActivationRiskSummary::from_risks(risks.iter());
        assert_eq!(summary.total_risks, risks.len());
        assert!(summary.policy_tier_risks > 0);
        assert!(summary.policy_surface_risks > 0);
        assert!(summary.unique_integrations <= summary.total_risk_entries);
        assert!(summary.review_integrations > 0 || summary.blocked_integrations > 0);
        assert!(summary.requires_attention());
        assert!(!summary.is_empty());

        let risks_from_catalog = activation_risk_at_or_before_priority(
            &catalog,
            2,
            &available_primitives,
            &allowed_capabilities,
            &[],
        );
        assert_eq!(risks, risks_from_catalog);
    }

    #[test]
    fn activation_reviews_queue_ready_and_blocked_human_review_work() {
        let review_ready_report = IntegrationReadinessReport {
            requested_integration_id: IntegrationId::trusted("review_ready_bridge"),
            display_name: "Review Ready Bridge".to_string(),
            activation_target: IntegrationActivationTarget::Direct,
            priority: 1,
            missing_primitives: Vec::new(),
            missing_capabilities: Vec::new(),
            missing_dependencies: Vec::new(),
            requires_human_review: true,
            highest_policy_tier: PrivilegeTier::HumanApproval,
            local_only: true,
            cloud_required: false,
        };
        let blocked_review_report = IntegrationReadinessReport {
            requested_integration_id: IntegrationId::trusted("blocked_review_camera"),
            display_name: "Blocked Review Camera".to_string(),
            activation_target: IntegrationActivationTarget::DelegatedIntegration(
                IntegrationId::trusted("mqtt"),
            ),
            priority: 2,
            missing_primitives: vec![PrimitiveFamily::CameraMedia],
            missing_capabilities: vec![CapabilityId::trusted("smart_home.command.low_risk")],
            missing_dependencies: vec![IntegrationId::trusted("mqtt")],
            requires_human_review: true,
            highest_policy_tier: PrivilegeTier::HighRisk,
            local_only: false,
            cloud_required: true,
        };
        let ready_report = IntegrationReadinessReport {
            requested_integration_id: IntegrationId::trusted("read_only_probe"),
            display_name: "Read-only Probe".to_string(),
            activation_target: IntegrationActivationTarget::Direct,
            priority: 3,
            missing_primitives: Vec::new(),
            missing_capabilities: Vec::new(),
            missing_dependencies: Vec::new(),
            requires_human_review: false,
            highest_policy_tier: PrivilegeTier::ReadOnly,
            local_only: true,
            cloud_required: false,
        };
        let candidates = activation_candidates_from_reports(
            [review_ready_report, blocked_review_report, ready_report].iter(),
        );

        let reviews = activation_reviews_from_candidates(&[], candidates.iter());

        assert_eq!(reviews.len(), 2);
        let review_ready = reviews
            .iter()
            .find(|review| review.requested_integration_id.as_str() == "review_ready_bridge")
            .unwrap();
        assert!(review_ready.activation_ready());
        assert!(review_ready.review_ready());
        assert!(!review_ready.has_blockers());
        assert!(review_ready.requires_attention());
        assert_eq!(review_ready.required_tier, PrivilegeTier::HumanApproval);
        let blocked_review = reviews
            .iter()
            .find(|review| review.requested_integration_id.as_str() == "blocked_review_camera")
            .unwrap();
        assert!(!blocked_review.activation_ready());
        assert!(!blocked_review.review_ready());
        assert!(blocked_review.has_blockers());
        assert_eq!(blocked_review.blocker_count, 3);
        assert_eq!(blocked_review.required_tier, PrivilegeTier::HighRisk);

        let summary = IntegrationActivationReviewSummary::from_reviews(reviews.iter());
        assert_eq!(summary.total_reviews, 2);
        assert_eq!(summary.review_ready_integrations, 1);
        assert_eq!(summary.blocked_review_integrations, 1);
        assert_eq!(summary.reviews_missing_primitives, 1);
        assert_eq!(summary.reviews_missing_capabilities, 1);
        assert_eq!(summary.reviews_missing_dependencies, 1);
        assert_eq!(summary.total_blockers, 3);
        assert_eq!(summary.first_review_priority, Some(1));
        assert_eq!(summary.first_blocked_priority, Some(2));
        assert_eq!(summary.local_only_reviews, 1);
        assert_eq!(summary.cloud_required_reviews, 1);
        assert_eq!(summary.human_approval_reviews, 1);
        assert_eq!(summary.high_risk_reviews, 1);
        assert_eq!(summary.highest_policy_tier, PrivilegeTier::HighRisk);
        assert!(summary.has_review_ready_work());
        assert!(summary.has_blockers());
        assert!(summary.requires_attention());
        assert!(!summary.is_empty());

        let catalog = first_party_catalog();
        let available_primitives = vec![
            PrimitiveFamily::NormalizedModel,
            PrimitiveFamily::DiscoveryIndex,
            PrimitiveFamily::CommandMapping,
            PrimitiveFamily::CapabilityPolicy,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let catalog_reviews = activation_reviews_at_or_before_priority(
            &catalog,
            2,
            &available_primitives,
            &allowed_capabilities,
            &[],
        );
        assert!(catalog_reviews
            .iter()
            .any(IntegrationActivationReviewItem::has_policy_surfaces));
    }

    #[test]
    fn activation_approval_packets_bundle_review_work_for_human_decisions() {
        let review_ready_report = IntegrationReadinessReport {
            requested_integration_id: IntegrationId::trusted("review_ready_bridge"),
            display_name: "Review Ready Bridge".to_string(),
            activation_target: IntegrationActivationTarget::Direct,
            priority: 1,
            missing_primitives: Vec::new(),
            missing_capabilities: Vec::new(),
            missing_dependencies: Vec::new(),
            requires_human_review: true,
            highest_policy_tier: PrivilegeTier::HumanApproval,
            local_only: true,
            cloud_required: false,
        };
        let blocked_review_report = IntegrationReadinessReport {
            requested_integration_id: IntegrationId::trusted("blocked_review_camera"),
            display_name: "Blocked Review Camera".to_string(),
            activation_target: IntegrationActivationTarget::DelegatedIntegration(
                IntegrationId::trusted("mqtt"),
            ),
            priority: 2,
            missing_primitives: vec![PrimitiveFamily::CameraMedia],
            missing_capabilities: vec![CapabilityId::trusted("smart_home.command.low_risk")],
            missing_dependencies: vec![IntegrationId::trusted("mqtt")],
            requires_human_review: true,
            highest_policy_tier: PrivilegeTier::HighRisk,
            local_only: false,
            cloud_required: true,
        };
        let ready_report = IntegrationReadinessReport {
            requested_integration_id: IntegrationId::trusted("read_only_probe"),
            display_name: "Read-only Probe".to_string(),
            activation_target: IntegrationActivationTarget::Direct,
            priority: 3,
            missing_primitives: Vec::new(),
            missing_capabilities: Vec::new(),
            missing_dependencies: Vec::new(),
            requires_human_review: false,
            highest_policy_tier: PrivilegeTier::ReadOnly,
            local_only: true,
            cloud_required: false,
        };
        let candidates = activation_candidates_from_reports(
            [review_ready_report, blocked_review_report, ready_report].iter(),
        );

        let packets = activation_approval_packets_from_candidates(&[], candidates.iter(), &[]);

        assert_eq!(packets.len(), 2);
        let approval_ready = packets
            .iter()
            .find(|packet| packet.requested_integration_id().as_str() == "review_ready_bridge")
            .unwrap();
        assert!(approval_ready.approval_ready());
        assert!(!approval_ready.has_blockers());
        assert!(approval_ready.requires_attention());
        assert!(approval_ready.action_summary.review_policy_actions >= 1);
        assert!(approval_ready.constraint_summary.review_constraints >= 1);

        let blocked = packets
            .iter()
            .find(|packet| packet.requested_integration_id().as_str() == "blocked_review_camera")
            .unwrap();
        assert!(!blocked.approval_ready());
        assert!(blocked.has_blockers());
        assert_eq!(blocked.required_tier(), PrivilegeTier::HighRisk);
        assert!(blocked.action_summary.provide_primitive_actions >= 1);
        assert!(blocked.constraint_summary.blocking_constraints >= 1);
        assert!(blocked.risk_summary.total_risks >= 1);
        assert!(blocked.dependency_graph.summary.total_edges >= 1);
        assert!(blocked.dependency_graph.summary.blocking_edges >= 1);

        let summary = IntegrationActivationApprovalSummary::from_packets(packets.iter());
        assert_eq!(summary.total_packets, 2);
        assert_eq!(summary.approval_ready_packets, 1);
        assert_eq!(summary.blocked_packets, 1);
        assert!(summary.total_actions > 0);
        assert!(summary.review_policy_actions > 0);
        assert!(summary.blocking_constraints > 0);
        assert!(summary.total_risks > 0);
        assert!(summary.total_dependency_edges > 0);
        assert!(summary.blocking_dependency_edges > 0);
        assert_eq!(summary.human_approval_packets, 1);
        assert_eq!(summary.high_risk_packets, 1);
        assert_eq!(summary.first_approval_priority, Some(1));
        assert_eq!(summary.first_blocked_priority, Some(2));
        assert!(summary.has_approval_ready_work());
        assert!(summary.has_blockers());
        assert!(summary.requires_attention());
        assert!(!summary.is_empty());

        let catalog = first_party_catalog();
        let available_primitives = vec![
            PrimitiveFamily::NormalizedModel,
            PrimitiveFamily::DiscoveryIndex,
            PrimitiveFamily::CommandMapping,
            PrimitiveFamily::CapabilityPolicy,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let catalog_packets = activation_approval_packets_at_or_before_priority(
            &catalog,
            2,
            &available_primitives,
            &allowed_capabilities,
            &[],
        );
        assert!(catalog_packets
            .iter()
            .any(IntegrationActivationApprovalPacket::has_policy_surfaces));
    }

    #[test]
    fn activation_decisions_project_approval_packets_into_decision_queue() {
        let review_ready_report = IntegrationReadinessReport {
            requested_integration_id: IntegrationId::trusted("review_ready_bridge"),
            display_name: "Review Ready Bridge".to_string(),
            activation_target: IntegrationActivationTarget::Direct,
            priority: 1,
            missing_primitives: Vec::new(),
            missing_capabilities: Vec::new(),
            missing_dependencies: Vec::new(),
            requires_human_review: true,
            highest_policy_tier: PrivilegeTier::HumanApproval,
            local_only: true,
            cloud_required: false,
        };
        let blocked_review_report = IntegrationReadinessReport {
            requested_integration_id: IntegrationId::trusted("blocked_review_camera"),
            display_name: "Blocked Review Camera".to_string(),
            activation_target: IntegrationActivationTarget::DelegatedIntegration(
                IntegrationId::trusted("mqtt"),
            ),
            priority: 2,
            missing_primitives: vec![PrimitiveFamily::CameraMedia],
            missing_capabilities: vec![CapabilityId::trusted("smart_home.command.low_risk")],
            missing_dependencies: vec![IntegrationId::trusted("mqtt")],
            requires_human_review: true,
            highest_policy_tier: PrivilegeTier::HighRisk,
            local_only: false,
            cloud_required: true,
        };
        let ready_report = IntegrationReadinessReport {
            requested_integration_id: IntegrationId::trusted("read_only_probe"),
            display_name: "Read-only Probe".to_string(),
            activation_target: IntegrationActivationTarget::Direct,
            priority: 3,
            missing_primitives: Vec::new(),
            missing_capabilities: Vec::new(),
            missing_dependencies: Vec::new(),
            requires_human_review: false,
            highest_policy_tier: PrivilegeTier::ReadOnly,
            local_only: true,
            cloud_required: false,
        };
        let candidates = activation_candidates_from_reports(
            [review_ready_report, blocked_review_report, ready_report].iter(),
        );

        let decisions = activation_decisions_from_candidates(&[], candidates.iter(), &[]);

        assert_eq!(decisions.len(), 2);
        let ready_to_approve = decisions
            .iter()
            .find(|decision| decision.requested_integration_id().as_str() == "review_ready_bridge")
            .unwrap();
        assert_eq!(
            ready_to_approve.decision_status,
            IntegrationActivationDecisionStatus::ReadyToApprove
        );
        assert!(ready_to_approve.approval_ready());
        assert!(!ready_to_approve.has_blockers());
        assert!(ready_to_approve.requires_attention());

        let blocked = decisions
            .iter()
            .find(|decision| {
                decision.requested_integration_id().as_str() == "blocked_review_camera"
            })
            .unwrap();
        assert_eq!(
            blocked.decision_status,
            IntegrationActivationDecisionStatus::BlockedOnPrerequisites
        );
        assert!(!blocked.approval_ready());
        assert!(blocked.has_blockers());
        assert_eq!(blocked.required_tier(), PrivilegeTier::HighRisk);

        let summary = IntegrationActivationDecisionSummary::from_decisions(decisions.iter());
        assert_eq!(summary.total_decisions, 2);
        assert_eq!(summary.ready_to_approve_decisions, 1);
        assert_eq!(summary.blocked_decisions, 1);
        assert!(summary.total_actions > 0);
        assert!(summary.review_policy_actions > 0);
        assert!(summary.blocking_constraints > 0);
        assert!(summary.total_risks > 0);
        assert!(summary.total_dependency_edges > 0);
        assert!(summary.blocking_dependency_edges > 0);
        assert_eq!(summary.human_approval_decisions, 1);
        assert_eq!(summary.high_risk_decisions, 1);
        assert_eq!(summary.first_approval_priority, Some(1));
        assert_eq!(summary.first_blocked_priority, Some(2));
        assert!(summary.has_approval_ready_work());
        assert!(summary.has_blockers());
        assert!(summary.requires_attention());
        assert!(!summary.is_empty());

        let catalog = first_party_catalog();
        let available_primitives = vec![
            PrimitiveFamily::NormalizedModel,
            PrimitiveFamily::DiscoveryIndex,
            PrimitiveFamily::CommandMapping,
            PrimitiveFamily::CapabilityPolicy,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let catalog_decisions = activation_decisions_at_or_before_priority(
            &catalog,
            2,
            &available_primitives,
            &allowed_capabilities,
            &[],
        );
        assert!(catalog_decisions
            .iter()
            .any(IntegrationActivationDecisionItem::has_policy_surfaces));
    }

    #[test]
    fn activation_evidence_explains_decision_support_and_blockers() {
        let review_ready_report = IntegrationReadinessReport {
            requested_integration_id: IntegrationId::trusted("review_ready_bridge"),
            display_name: "Review Ready Bridge".to_string(),
            activation_target: IntegrationActivationTarget::Direct,
            priority: 1,
            missing_primitives: Vec::new(),
            missing_capabilities: Vec::new(),
            missing_dependencies: Vec::new(),
            requires_human_review: true,
            highest_policy_tier: PrivilegeTier::HumanApproval,
            local_only: true,
            cloud_required: false,
        };
        let blocked_review_report = IntegrationReadinessReport {
            requested_integration_id: IntegrationId::trusted("blocked_review_camera"),
            display_name: "Blocked Review Camera".to_string(),
            activation_target: IntegrationActivationTarget::DelegatedIntegration(
                IntegrationId::trusted("mqtt"),
            ),
            priority: 2,
            missing_primitives: vec![PrimitiveFamily::CameraMedia],
            missing_capabilities: vec![CapabilityId::trusted("smart_home.command.low_risk")],
            missing_dependencies: vec![IntegrationId::trusted("mqtt")],
            requires_human_review: true,
            highest_policy_tier: PrivilegeTier::HighRisk,
            local_only: false,
            cloud_required: true,
        };
        let ready_report = IntegrationReadinessReport {
            requested_integration_id: IntegrationId::trusted("read_only_probe"),
            display_name: "Read-only Probe".to_string(),
            activation_target: IntegrationActivationTarget::Direct,
            priority: 3,
            missing_primitives: Vec::new(),
            missing_capabilities: Vec::new(),
            missing_dependencies: Vec::new(),
            requires_human_review: false,
            highest_policy_tier: PrivilegeTier::ReadOnly,
            local_only: true,
            cloud_required: false,
        };
        let candidates = activation_candidates_from_reports(
            [review_ready_report, blocked_review_report, ready_report].iter(),
        );

        let evidence = activation_evidence_from_candidates(&[], candidates.iter(), &[]);

        assert!(evidence.iter().any(|item| {
            item.kind == IntegrationActivationEvidenceKind::ApprovalDecision
                && item.status == IntegrationActivationEvidenceStatus::SupportsApproval
                && item.requested_integration_id.as_str() == "review_ready_bridge"
        }));
        assert!(evidence.iter().any(|item| {
            item.kind == IntegrationActivationEvidenceKind::ApprovalDecision
                && item.status == IntegrationActivationEvidenceStatus::BlocksApproval
                && item.requested_integration_id.as_str() == "blocked_review_camera"
        }));
        assert!(evidence.iter().any(|item| {
            item.kind == IntegrationActivationEvidenceKind::PrimitiveBlocker
                && item.primitive == Some(PrimitiveFamily::CameraMedia)
                && item.blocks_approval()
        }));
        assert!(evidence.iter().any(|item| {
            item.kind == IntegrationActivationEvidenceKind::CapabilityBlocker
                && item.capability_id.as_ref().is_some_and(|capability_id| {
                    capability_id.as_str() == "smart_home.command.low_risk"
                })
                && item.blocks_approval()
        }));
        assert!(evidence.iter().any(|item| {
            item.kind == IntegrationActivationEvidenceKind::DependencyBlocker
                && item
                    .dependency_integration_id
                    .as_ref()
                    .is_some_and(|integration_id| integration_id.as_str() == "mqtt")
                && item.blocks_approval()
        }));
        assert!(evidence
            .iter()
            .any(|item| item.kind == IntegrationActivationEvidenceKind::PolicyRisk));

        let summary = IntegrationActivationEvidenceSummary::from_evidence(evidence.iter());
        assert_eq!(summary.approval_decision_evidence, 2);
        assert_eq!(summary.ready_to_approve_integrations, 1);
        assert_eq!(summary.blocked_integrations, 1);
        assert!(summary.total_evidence > summary.approval_decision_evidence);
        assert!(summary.supporting_evidence > 0);
        assert!(summary.review_evidence > 0);
        assert!(summary.blocking_evidence > 0);
        assert!(summary.policy_risk_evidence > 0);
        assert!(summary.primitive_blocker_evidence > 0);
        assert!(summary.capability_blocker_evidence > 0);
        assert!(summary.dependency_blocker_evidence > 0);
        assert_eq!(summary.first_supporting_priority, Some(1));
        assert_eq!(summary.first_blocking_priority, Some(2));
        assert!(summary.has_supporting_evidence());
        assert!(summary.has_review_work());
        assert!(summary.has_blockers());
        assert!(summary.requires_attention());
        assert!(!summary.is_empty());

        let catalog = first_party_catalog();
        let available_primitives = vec![
            PrimitiveFamily::NormalizedModel,
            PrimitiveFamily::DiscoveryIndex,
            PrimitiveFamily::CommandMapping,
            PrimitiveFamily::CapabilityPolicy,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let catalog_evidence = activation_evidence_at_or_before_priority(
            &catalog,
            2,
            &available_primitives,
            &allowed_capabilities,
            &[],
        );
        assert!(catalog_evidence
            .iter()
            .any(|item| item.policy_surface.is_some()));
    }

    #[test]
    fn activation_dossiers_bundle_decisions_with_evidence_rollups() {
        let review_ready_report = IntegrationReadinessReport {
            requested_integration_id: IntegrationId::trusted("review_ready_bridge"),
            display_name: "Review Ready Bridge".to_string(),
            activation_target: IntegrationActivationTarget::Direct,
            priority: 1,
            missing_primitives: Vec::new(),
            missing_capabilities: Vec::new(),
            missing_dependencies: Vec::new(),
            requires_human_review: true,
            highest_policy_tier: PrivilegeTier::HumanApproval,
            local_only: true,
            cloud_required: false,
        };
        let blocked_review_report = IntegrationReadinessReport {
            requested_integration_id: IntegrationId::trusted("blocked_review_camera"),
            display_name: "Blocked Review Camera".to_string(),
            activation_target: IntegrationActivationTarget::DelegatedIntegration(
                IntegrationId::trusted("mqtt"),
            ),
            priority: 2,
            missing_primitives: vec![PrimitiveFamily::CameraMedia],
            missing_capabilities: vec![CapabilityId::trusted("smart_home.command.low_risk")],
            missing_dependencies: vec![IntegrationId::trusted("mqtt")],
            requires_human_review: true,
            highest_policy_tier: PrivilegeTier::HighRisk,
            local_only: false,
            cloud_required: true,
        };
        let ready_report = IntegrationReadinessReport {
            requested_integration_id: IntegrationId::trusted("read_only_probe"),
            display_name: "Read-only Probe".to_string(),
            activation_target: IntegrationActivationTarget::Direct,
            priority: 3,
            missing_primitives: Vec::new(),
            missing_capabilities: Vec::new(),
            missing_dependencies: Vec::new(),
            requires_human_review: false,
            highest_policy_tier: PrivilegeTier::ReadOnly,
            local_only: true,
            cloud_required: false,
        };
        let candidates = activation_candidates_from_reports(
            [review_ready_report, blocked_review_report, ready_report].iter(),
        );

        let dossiers = activation_dossiers_from_candidates(&[], candidates.iter(), &[]);

        assert_eq!(dossiers.len(), 2);
        let ready = dossiers
            .iter()
            .find(|dossier| dossier.requested_integration_id().as_str() == "review_ready_bridge")
            .unwrap();
        assert!(ready.approval_ready());
        assert!(!ready.has_blockers());
        assert!(ready.evidence_summary.has_supporting_evidence());
        assert!(ready.evidence_summary.has_review_work());
        assert!(!ready.evidence.is_empty());

        let blocked = dossiers
            .iter()
            .find(|dossier| dossier.requested_integration_id().as_str() == "blocked_review_camera")
            .unwrap();
        assert!(blocked.has_blockers());
        assert!(blocked.requires_attention());
        assert!(blocked.evidence_summary.has_blockers());
        assert_eq!(blocked.required_tier(), PrivilegeTier::HighRisk);

        let summary = IntegrationActivationDossierSummary::from_dossiers(dossiers.iter());
        assert_eq!(summary.total_dossiers, 2);
        assert_eq!(summary.ready_to_approve_dossiers, 1);
        assert_eq!(summary.blocked_dossiers, 1);
        assert!(summary.total_evidence >= ready.evidence.len() + blocked.evidence.len());
        assert!(summary.supporting_evidence > 0);
        assert!(summary.review_evidence > 0);
        assert!(summary.blocking_evidence > 0);
        assert_eq!(summary.first_approval_priority, Some(1));
        assert_eq!(summary.first_blocked_priority, Some(2));
        assert_eq!(summary.highest_policy_tier, PrivilegeTier::HighRisk);
        assert!(summary.has_approval_ready_work());
        assert!(summary.has_review_work());
        assert!(summary.has_blockers());
        assert!(summary.requires_attention());
        assert!(!summary.is_empty());

        let catalog = first_party_catalog();
        let available_primitives = vec![
            PrimitiveFamily::NormalizedModel,
            PrimitiveFamily::DiscoveryIndex,
            PrimitiveFamily::CommandMapping,
            PrimitiveFamily::CapabilityPolicy,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let catalog_dossiers = activation_dossiers_at_or_before_priority(
            &catalog,
            2,
            &available_primitives,
            &allowed_capabilities,
            &[],
        );
        assert!(catalog_dossiers
            .iter()
            .any(IntegrationActivationDossierItem::has_policy_surfaces));
    }

    #[test]
    fn activation_readouts_roll_up_wave_health_dossiers_and_evidence() {
        let review_ready_report = IntegrationReadinessReport {
            requested_integration_id: IntegrationId::trusted("review_ready_bridge"),
            display_name: "Review Ready Bridge".to_string(),
            activation_target: IntegrationActivationTarget::Direct,
            priority: 1,
            missing_primitives: Vec::new(),
            missing_capabilities: Vec::new(),
            missing_dependencies: Vec::new(),
            requires_human_review: true,
            highest_policy_tier: PrivilegeTier::HumanApproval,
            local_only: true,
            cloud_required: false,
        };
        let blocked_review_report = IntegrationReadinessReport {
            requested_integration_id: IntegrationId::trusted("blocked_review_camera"),
            display_name: "Blocked Review Camera".to_string(),
            activation_target: IntegrationActivationTarget::DelegatedIntegration(
                IntegrationId::trusted("mqtt"),
            ),
            priority: 2,
            missing_primitives: vec![PrimitiveFamily::CameraMedia],
            missing_capabilities: vec![CapabilityId::trusted("smart_home.command.low_risk")],
            missing_dependencies: vec![IntegrationId::trusted("mqtt")],
            requires_human_review: true,
            highest_policy_tier: PrivilegeTier::HighRisk,
            local_only: false,
            cloud_required: true,
        };
        let ready_report = IntegrationReadinessReport {
            requested_integration_id: IntegrationId::trusted("read_only_probe"),
            display_name: "Read-only Probe".to_string(),
            activation_target: IntegrationActivationTarget::Direct,
            priority: 3,
            missing_primitives: Vec::new(),
            missing_capabilities: Vec::new(),
            missing_dependencies: Vec::new(),
            requires_human_review: false,
            highest_policy_tier: PrivilegeTier::ReadOnly,
            local_only: true,
            cloud_required: false,
        };
        let candidates = activation_candidates_from_reports(
            [review_ready_report, blocked_review_report, ready_report].iter(),
        );

        let readouts = activation_readouts_from_candidates(&[], candidates, &[]);

        assert_eq!(readouts.len(), 3);
        let approval_ready = readouts
            .iter()
            .find(|readout| readout.priority == 1)
            .unwrap();
        assert_eq!(
            approval_ready.health_status,
            IntegrationActivationHealthStatus::NeedsReview
        );
        assert!(approval_ready.has_approval_ready_work());
        assert!(approval_ready.has_review_work());
        assert_eq!(approval_ready.dossier_summary.ready_to_approve_dossiers, 1);
        assert!(approval_ready.evidence_summary.has_supporting_evidence());

        let blocked = readouts
            .iter()
            .find(|readout| readout.priority == 2)
            .unwrap();
        assert_eq!(
            blocked.health_status,
            IntegrationActivationHealthStatus::Blocked
        );
        assert!(blocked.has_blockers());
        assert!(blocked.has_review_work());
        assert_eq!(blocked.dossier_summary.blocked_dossiers, 1);
        assert!(blocked.evidence_summary.has_blockers());
        assert!(blocked.dependency_summary().has_blocking_dependencies());

        let ready = readouts
            .iter()
            .find(|readout| readout.priority == 3)
            .unwrap();
        assert_eq!(
            ready.health_status,
            IntegrationActivationHealthStatus::Ready
        );
        assert!(ready.has_activation_work());
        assert!(!ready.has_approval_ready_work());

        let summary = IntegrationActivationReadoutSummary::from_readouts(readouts.iter());
        assert_eq!(summary.total_readouts, 3);
        assert_eq!(summary.ready_readouts, 1);
        assert_eq!(summary.review_readouts, 1);
        assert_eq!(summary.blocked_readouts, 1);
        assert_eq!(summary.total_dossiers, 2);
        assert_eq!(summary.ready_to_approve_dossiers, 1);
        assert_eq!(summary.blocked_dossiers, 1);
        assert_eq!(summary.readouts_with_activation_work, 1);
        assert_eq!(summary.readouts_with_approval_work, 1);
        assert_eq!(summary.readouts_with_blockers, 2);
        assert!(summary.total_evidence > 0);
        assert!(summary.supporting_evidence > 0);
        assert!(summary.review_evidence > 0);
        assert!(summary.blocking_evidence > 0);
        assert_eq!(summary.first_approval_priority, Some(1));
        assert_eq!(summary.first_blocked_priority, Some(1));
        assert_eq!(summary.first_activation_priority, Some(3));
        assert_eq!(summary.highest_policy_tier, PrivilegeTier::HighRisk);
        assert_eq!(
            summary.overall_status,
            IntegrationActivationHealthStatus::Blocked
        );
        assert!(summary.has_activation_work());
        assert!(summary.has_approval_ready_work());
        assert!(summary.has_review_work());
        assert!(summary.has_blockers());
        assert!(summary.requires_attention());
        assert!(!summary.is_empty());

        let catalog = first_party_catalog();
        let available_primitives = vec![
            PrimitiveFamily::NormalizedModel,
            PrimitiveFamily::DiscoveryIndex,
            PrimitiveFamily::CommandMapping,
            PrimitiveFamily::CapabilityPolicy,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let catalog_readouts = activation_readouts_at_or_before_priority(
            &catalog,
            2,
            &available_primitives,
            &allowed_capabilities,
            &[],
        );
        assert!(catalog_readouts
            .iter()
            .any(|readout| readout.dossier_summary.total_dossiers > 0));
    }

    #[test]
    fn activation_briefing_items_summarize_readout_attention_sections() {
        let review_ready_report = IntegrationReadinessReport {
            requested_integration_id: IntegrationId::trusted("review_ready_bridge"),
            display_name: "Review Ready Bridge".to_string(),
            activation_target: IntegrationActivationTarget::Direct,
            priority: 1,
            missing_primitives: Vec::new(),
            missing_capabilities: Vec::new(),
            missing_dependencies: Vec::new(),
            requires_human_review: true,
            highest_policy_tier: PrivilegeTier::HumanApproval,
            local_only: true,
            cloud_required: false,
        };
        let blocked_review_report = IntegrationReadinessReport {
            requested_integration_id: IntegrationId::trusted("blocked_review_camera"),
            display_name: "Blocked Review Camera".to_string(),
            activation_target: IntegrationActivationTarget::DelegatedIntegration(
                IntegrationId::trusted("mqtt"),
            ),
            priority: 2,
            missing_primitives: vec![PrimitiveFamily::CameraMedia],
            missing_capabilities: vec![CapabilityId::trusted("smart_home.command.low_risk")],
            missing_dependencies: vec![IntegrationId::trusted("mqtt")],
            requires_human_review: true,
            highest_policy_tier: PrivilegeTier::HighRisk,
            local_only: false,
            cloud_required: true,
        };
        let ready_report = IntegrationReadinessReport {
            requested_integration_id: IntegrationId::trusted("read_only_probe"),
            display_name: "Read-only Probe".to_string(),
            activation_target: IntegrationActivationTarget::Direct,
            priority: 3,
            missing_primitives: Vec::new(),
            missing_capabilities: Vec::new(),
            missing_dependencies: Vec::new(),
            requires_human_review: false,
            highest_policy_tier: PrivilegeTier::ReadOnly,
            local_only: true,
            cloud_required: false,
        };
        let candidates = activation_candidates_from_reports(
            [review_ready_report, blocked_review_report, ready_report].iter(),
        );
        let items = activation_briefing_items_from_candidates(&[], candidates, &[]);

        assert!(items.iter().any(|item| {
            item.priority == 1 && item.kind == IntegrationActivationBriefingItemKind::Approval
        }));
        assert!(items.iter().any(|item| {
            item.priority == 2 && item.kind == IntegrationActivationBriefingItemKind::Blocker
        }));
        assert!(items.iter().any(|item| {
            item.priority == 2 && item.kind == IntegrationActivationBriefingItemKind::Dependency
        }));
        assert!(items.iter().any(|item| {
            item.priority == 3 && item.kind == IntegrationActivationBriefingItemKind::Activation
        }));
        assert!(items.iter().all(|item| item.integration_count() >= 1));

        let summary = IntegrationActivationBriefingSummary::from_items(items.iter());
        assert!(summary.total_items >= 4);
        assert_eq!(summary.unique_integrations, 3);
        assert!(summary.activation_items >= 1);
        assert!(summary.approval_items >= 1);
        assert!(summary.review_items >= 1);
        assert!(summary.blocker_items >= 1);
        assert!(summary.risk_items >= 1);
        assert!(summary.dependency_items >= 1);
        assert!(summary.items_requiring_attention >= 1);
        assert!(summary.total_actions > 0);
        assert!(summary.total_dossiers > 0);
        assert!(summary.total_evidence > 0);
        assert!(summary.total_risks > 0);
        assert!(summary.blocking_dependency_edges > 0);
        assert_eq!(summary.first_approval_priority, Some(1));
        assert_eq!(summary.first_blocked_priority, Some(1));
        assert_eq!(summary.first_activation_priority, Some(3));
        assert_eq!(summary.highest_policy_tier, PrivilegeTier::HighRisk);
        assert_eq!(
            summary.overall_status,
            IntegrationActivationHealthStatus::Blocked
        );
        assert!(summary.has_activation_work());
        assert!(summary.has_approval_ready_work());
        assert!(summary.has_review_work());
        assert!(summary.has_blockers());
        assert!(summary.has_risks());
        assert!(summary.has_dependency_blockers());
        assert!(summary.requires_attention());
        assert!(!summary.is_empty());

        let catalog = first_party_catalog();
        let available_primitives = vec![
            PrimitiveFamily::NormalizedModel,
            PrimitiveFamily::DiscoveryIndex,
            PrimitiveFamily::CommandMapping,
            PrimitiveFamily::CapabilityPolicy,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let catalog_items = activation_briefing_items_at_or_before_priority(
            &catalog,
            2,
            &available_primitives,
            &allowed_capabilities,
            &[],
        );
        assert!(catalog_items
            .iter()
            .any(|item| item.kind == IntegrationActivationBriefingItemKind::Blocker));
    }

    #[test]
    fn activation_dashboard_cards_condense_readouts_and_briefing_sections() {
        let review_ready_report = IntegrationReadinessReport {
            requested_integration_id: IntegrationId::trusted("review_ready_bridge"),
            display_name: "Review Ready Bridge".to_string(),
            activation_target: IntegrationActivationTarget::Direct,
            priority: 1,
            missing_primitives: Vec::new(),
            missing_capabilities: Vec::new(),
            missing_dependencies: Vec::new(),
            requires_human_review: true,
            highest_policy_tier: PrivilegeTier::HumanApproval,
            local_only: true,
            cloud_required: false,
        };
        let blocked_review_report = IntegrationReadinessReport {
            requested_integration_id: IntegrationId::trusted("blocked_review_camera"),
            display_name: "Blocked Review Camera".to_string(),
            activation_target: IntegrationActivationTarget::DelegatedIntegration(
                IntegrationId::trusted("mqtt"),
            ),
            priority: 2,
            missing_primitives: vec![PrimitiveFamily::CameraMedia],
            missing_capabilities: vec![CapabilityId::trusted("smart_home.command.low_risk")],
            missing_dependencies: vec![IntegrationId::trusted("mqtt")],
            requires_human_review: true,
            highest_policy_tier: PrivilegeTier::HighRisk,
            local_only: false,
            cloud_required: true,
        };
        let ready_report = IntegrationReadinessReport {
            requested_integration_id: IntegrationId::trusted("read_only_probe"),
            display_name: "Read-only Probe".to_string(),
            activation_target: IntegrationActivationTarget::Direct,
            priority: 3,
            missing_primitives: Vec::new(),
            missing_capabilities: Vec::new(),
            missing_dependencies: Vec::new(),
            requires_human_review: false,
            highest_policy_tier: PrivilegeTier::ReadOnly,
            local_only: true,
            cloud_required: false,
        };
        let candidates = activation_candidates_from_reports(
            [review_ready_report, blocked_review_report, ready_report].iter(),
        );
        let cards = activation_dashboard_cards_from_candidates(&[], candidates, &[]);

        assert_eq!(cards.len(), 3);
        let approval_ready = cards.iter().find(|card| card.priority == 1).unwrap();
        assert!(approval_ready.has_approval_ready_work);
        assert!(approval_ready.has_review_work);
        assert!(approval_ready.requires_attention());
        assert!(approval_ready.briefing_item_count >= 2);
        assert_eq!(
            approval_ready.next_briefing_kind,
            Some(IntegrationActivationBriefingItemKind::Blocker)
        );

        let blocked = cards.iter().find(|card| card.priority == 2).unwrap();
        assert_eq!(
            blocked.health_status,
            IntegrationActivationHealthStatus::Blocked
        );
        assert!(blocked.has_blockers);
        assert!(blocked.has_risks);
        assert!(blocked.has_dependency_blockers);
        assert!(blocked.blocking_dependency_edge_count > 0);

        let ready = cards.iter().find(|card| card.priority == 3).unwrap();
        assert_eq!(
            ready.health_status,
            IntegrationActivationHealthStatus::Ready
        );
        assert!(ready.has_activation_work);
        assert!(!ready.has_approval_ready_work);
        assert!(cards.iter().all(|card| card.integration_count() >= 1));

        let summary = IntegrationActivationDashboardSummary::from_cards(cards.iter());
        assert_eq!(summary.total_cards, 3);
        assert_eq!(summary.unique_integrations, 3);
        assert_eq!(summary.ready_cards, 1);
        assert_eq!(summary.review_cards, 1);
        assert_eq!(summary.blocked_cards, 1);
        assert!(summary.total_briefing_items >= 4);
        assert!(summary.activation_items >= 1);
        assert!(summary.approval_items >= 1);
        assert!(summary.review_items >= 1);
        assert!(summary.blocker_items >= 1);
        assert!(summary.risk_items >= 1);
        assert!(summary.dependency_items >= 1);
        assert!(summary.cards_requiring_attention >= 1);
        assert!(summary.total_actions > 0);
        assert!(summary.total_dossiers > 0);
        assert!(summary.total_evidence > 0);
        assert!(summary.total_risks > 0);
        assert!(summary.blocking_dependency_edges > 0);
        assert_eq!(summary.first_approval_priority, Some(1));
        assert_eq!(summary.first_blocked_priority, Some(1));
        assert_eq!(summary.first_activation_priority, Some(3));
        assert_eq!(summary.first_attention_priority, Some(1));
        assert_eq!(summary.highest_policy_tier, PrivilegeTier::HighRisk);
        assert_eq!(
            summary.overall_status,
            IntegrationActivationHealthStatus::Blocked
        );
        assert!(summary.has_activation_work());
        assert!(summary.has_approval_ready_work());
        assert!(summary.has_review_work());
        assert!(summary.has_blockers());
        assert!(summary.has_risks());
        assert!(summary.has_dependency_blockers());
        assert!(summary.requires_attention());
        assert!(!summary.is_empty());

        let catalog = first_party_catalog();
        let available_primitives = vec![
            PrimitiveFamily::NormalizedModel,
            PrimitiveFamily::DiscoveryIndex,
            PrimitiveFamily::CommandMapping,
            PrimitiveFamily::CapabilityPolicy,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let catalog_cards = activation_dashboard_cards_at_or_before_priority(
            &catalog,
            2,
            &available_primitives,
            &allowed_capabilities,
            &[],
        );
        assert!(catalog_cards
            .iter()
            .any(IntegrationActivationDashboardCard::requires_attention));
    }

    #[test]
    fn activation_timeline_milestones_order_dashboard_cards_by_wave() {
        let review_ready_report = IntegrationReadinessReport {
            requested_integration_id: IntegrationId::trusted("review_ready_bridge"),
            display_name: "Review Ready Bridge".to_string(),
            activation_target: IntegrationActivationTarget::Direct,
            priority: 1,
            missing_primitives: Vec::new(),
            missing_capabilities: Vec::new(),
            missing_dependencies: Vec::new(),
            requires_human_review: true,
            highest_policy_tier: PrivilegeTier::HumanApproval,
            local_only: true,
            cloud_required: false,
        };
        let blocked_review_report = IntegrationReadinessReport {
            requested_integration_id: IntegrationId::trusted("blocked_review_camera"),
            display_name: "Blocked Review Camera".to_string(),
            activation_target: IntegrationActivationTarget::DelegatedIntegration(
                IntegrationId::trusted("mqtt"),
            ),
            priority: 2,
            missing_primitives: vec![PrimitiveFamily::CameraMedia],
            missing_capabilities: vec![CapabilityId::trusted("smart_home.command.low_risk")],
            missing_dependencies: vec![IntegrationId::trusted("mqtt")],
            requires_human_review: true,
            highest_policy_tier: PrivilegeTier::HighRisk,
            local_only: false,
            cloud_required: true,
        };
        let ready_report = IntegrationReadinessReport {
            requested_integration_id: IntegrationId::trusted("read_only_probe"),
            display_name: "Read-only Probe".to_string(),
            activation_target: IntegrationActivationTarget::Direct,
            priority: 3,
            missing_primitives: Vec::new(),
            missing_capabilities: Vec::new(),
            missing_dependencies: Vec::new(),
            requires_human_review: false,
            highest_policy_tier: PrivilegeTier::ReadOnly,
            local_only: true,
            cloud_required: false,
        };
        let candidates = activation_candidates_from_reports(
            [review_ready_report, blocked_review_report, ready_report].iter(),
        );
        let milestones = activation_timeline_milestones_from_candidates(&[], candidates, &[]);

        assert_eq!(milestones.len(), 3);
        assert_eq!(milestones[0].sequence, 1);
        assert_eq!(milestones[0].priority, 1);
        assert_eq!(
            milestones[0].milestone_kind,
            Some(IntegrationActivationBriefingItemKind::Blocker)
        );
        assert!(milestones[0].requires_attention());
        assert!(milestones[0].has_approval_ready_work());
        assert_eq!(milestones[1].sequence, 2);
        assert_eq!(milestones[1].priority, 2);
        assert!(milestones[1].has_dependency_blockers());
        assert_eq!(milestones[2].sequence, 3);
        assert_eq!(milestones[2].priority, 3);
        assert!(milestones[2].has_activation_work());

        let summary = IntegrationActivationTimelineSummary::from_milestones(milestones.iter());
        assert_eq!(summary.total_milestones, 3);
        assert_eq!(summary.unique_integrations, 3);
        assert_eq!(summary.ready_milestones, 1);
        assert_eq!(summary.review_milestones, 1);
        assert_eq!(summary.blocked_milestones, 1);
        assert!(summary.blocker_milestones >= 1);
        assert!(summary.activation_milestones >= 1);
        assert!(summary.milestones_requiring_attention >= 1);
        assert!(summary.total_briefing_items >= 4);
        assert!(summary.total_actions > 0);
        assert!(summary.total_dossiers > 0);
        assert!(summary.total_evidence > 0);
        assert!(summary.total_risks > 0);
        assert!(summary.blocking_dependency_edges > 0);
        assert_eq!(summary.first_attention_sequence, Some(1));
        assert_eq!(summary.first_attention_priority, Some(1));
        assert_eq!(summary.first_approval_sequence, Some(1));
        assert_eq!(summary.first_blocked_sequence, Some(1));
        assert_eq!(summary.first_activation_sequence, Some(3));
        assert_eq!(summary.highest_policy_tier, PrivilegeTier::HighRisk);
        assert_eq!(
            summary.overall_status,
            IntegrationActivationHealthStatus::Blocked
        );
        assert!(summary.has_activation_work());
        assert!(summary.has_approval_ready_work());
        assert!(summary.has_review_work());
        assert!(summary.has_blockers());
        assert!(summary.has_risks());
        assert!(summary.has_dependency_blockers());
        assert!(summary.requires_attention());
        assert!(!summary.is_empty());

        let catalog = first_party_catalog();
        let available_primitives = vec![
            PrimitiveFamily::NormalizedModel,
            PrimitiveFamily::DiscoveryIndex,
            PrimitiveFamily::CommandMapping,
            PrimitiveFamily::CapabilityPolicy,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let catalog_milestones = activation_timeline_milestones_at_or_before_priority(
            &catalog,
            2,
            &available_primitives,
            &allowed_capabilities,
            &[],
        );
        assert!(catalog_milestones
            .iter()
            .any(IntegrationActivationTimelineMilestone::requires_attention));
    }

    #[test]
    fn activation_forecasts_classify_timeline_milestones_into_next_actions() {
        let review_ready_report = IntegrationReadinessReport {
            requested_integration_id: IntegrationId::trusted("review_ready_bridge"),
            display_name: "Review Ready Bridge".to_string(),
            activation_target: IntegrationActivationTarget::Direct,
            priority: 1,
            missing_primitives: Vec::new(),
            missing_capabilities: Vec::new(),
            missing_dependencies: Vec::new(),
            requires_human_review: true,
            highest_policy_tier: PrivilegeTier::HumanApproval,
            local_only: true,
            cloud_required: false,
        };
        let blocked_review_report = IntegrationReadinessReport {
            requested_integration_id: IntegrationId::trusted("blocked_review_camera"),
            display_name: "Blocked Review Camera".to_string(),
            activation_target: IntegrationActivationTarget::DelegatedIntegration(
                IntegrationId::trusted("mqtt"),
            ),
            priority: 2,
            missing_primitives: vec![PrimitiveFamily::CameraMedia],
            missing_capabilities: vec![CapabilityId::trusted("smart_home.command.low_risk")],
            missing_dependencies: vec![IntegrationId::trusted("mqtt")],
            requires_human_review: true,
            highest_policy_tier: PrivilegeTier::HighRisk,
            local_only: false,
            cloud_required: true,
        };
        let ready_report = IntegrationReadinessReport {
            requested_integration_id: IntegrationId::trusted("read_only_probe"),
            display_name: "Read-only Probe".to_string(),
            activation_target: IntegrationActivationTarget::Direct,
            priority: 3,
            missing_primitives: Vec::new(),
            missing_capabilities: Vec::new(),
            missing_dependencies: Vec::new(),
            requires_human_review: false,
            highest_policy_tier: PrivilegeTier::ReadOnly,
            local_only: true,
            cloud_required: false,
        };
        let candidates = activation_candidates_from_reports(
            [review_ready_report, blocked_review_report, ready_report].iter(),
        );
        let forecasts = activation_forecasts_from_candidates(&[], candidates, &[]);

        assert_eq!(forecasts.len(), 3);
        assert_eq!(forecasts[0].sequence, 1);
        assert_eq!(forecasts[0].priority, 1);
        assert!(forecasts[0].requires_attention());
        assert_eq!(forecasts[2].sequence, 3);
        assert_eq!(forecasts[2].priority, 3);
        assert_eq!(
            forecasts[2].forecast_action,
            IntegrationActivationForecastAction::ActivateWave
        );
        assert!(forecasts.iter().any(|forecast| forecast.forecast_action
            == IntegrationActivationForecastAction::EnableDependencies
            || forecast.forecast_action == IntegrationActivationForecastAction::ResolveBlockers));

        let summary = IntegrationActivationForecastSummary::from_forecasts(forecasts.iter());
        assert_eq!(summary.total_forecasts, 3);
        assert_eq!(summary.unique_integrations, 3);
        assert_eq!(summary.activate_wave_forecasts, 1);
        assert!(summary.resolve_blocker_forecasts + summary.enable_dependency_forecasts >= 1);
        assert!(summary.forecasts_requiring_attention >= 1);
        assert!(summary.forecasts_with_activation_work >= 1);
        assert!(summary.forecasts_with_approval_work >= 1);
        assert!(summary.forecasts_with_blockers >= 1);
        assert!(summary.total_briefing_items >= 4);
        assert!(summary.total_actions > 0);
        assert!(summary.total_dossiers > 0);
        assert!(summary.total_evidence > 0);
        assert!(summary.total_risks > 0);
        assert!(summary.blocking_dependency_edges > 0);
        assert!(summary.next_action.is_some());
        assert_eq!(summary.next_action_sequence, Some(1));
        assert_eq!(summary.next_action_priority, Some(1));
        assert_eq!(summary.first_attention_sequence, Some(1));
        assert_eq!(summary.first_activation_sequence, Some(3));
        assert_eq!(summary.first_activation_priority, Some(3));
        assert_eq!(summary.highest_policy_tier, PrivilegeTier::HighRisk);
        assert_eq!(
            summary.overall_status,
            IntegrationActivationHealthStatus::Blocked
        );
        assert!(summary.has_activation_work());
        assert!(summary.has_approval_ready_work());
        assert!(summary.has_blockers());
        assert!(summary.requires_attention());
        assert!(!summary.is_empty());

        let catalog = first_party_catalog();
        let available_primitives = vec![
            PrimitiveFamily::NormalizedModel,
            PrimitiveFamily::DiscoveryIndex,
            PrimitiveFamily::CommandMapping,
            PrimitiveFamily::CapabilityPolicy,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let catalog_forecasts = activation_forecasts_at_or_before_priority(
            &catalog,
            2,
            &available_primitives,
            &allowed_capabilities,
            &[],
        );
        assert!(catalog_forecasts
            .iter()
            .any(IntegrationActivationForecastItem::requires_attention));
    }

    #[test]
    fn activation_playbook_steps_recommend_next_planning_views() {
        let review_ready_report = IntegrationReadinessReport {
            requested_integration_id: IntegrationId::trusted("review_ready_bridge"),
            display_name: "Review Ready Bridge".to_string(),
            activation_target: IntegrationActivationTarget::Direct,
            priority: 1,
            missing_primitives: Vec::new(),
            missing_capabilities: Vec::new(),
            missing_dependencies: Vec::new(),
            requires_human_review: true,
            highest_policy_tier: PrivilegeTier::HumanApproval,
            local_only: true,
            cloud_required: false,
        };
        let blocked_review_report = IntegrationReadinessReport {
            requested_integration_id: IntegrationId::trusted("blocked_review_camera"),
            display_name: "Blocked Review Camera".to_string(),
            activation_target: IntegrationActivationTarget::DelegatedIntegration(
                IntegrationId::trusted("mqtt"),
            ),
            priority: 2,
            missing_primitives: vec![PrimitiveFamily::CameraMedia],
            missing_capabilities: vec![CapabilityId::trusted("smart_home.command.low_risk")],
            missing_dependencies: vec![IntegrationId::trusted("mqtt")],
            requires_human_review: true,
            highest_policy_tier: PrivilegeTier::HighRisk,
            local_only: false,
            cloud_required: true,
        };
        let ready_report = IntegrationReadinessReport {
            requested_integration_id: IntegrationId::trusted("read_only_probe"),
            display_name: "Read-only Probe".to_string(),
            activation_target: IntegrationActivationTarget::Direct,
            priority: 3,
            missing_primitives: Vec::new(),
            missing_capabilities: Vec::new(),
            missing_dependencies: Vec::new(),
            requires_human_review: false,
            highest_policy_tier: PrivilegeTier::ReadOnly,
            local_only: true,
            cloud_required: false,
        };
        let candidates = activation_candidates_from_reports(
            [review_ready_report, blocked_review_report, ready_report].iter(),
        );
        let steps = activation_playbook_steps_from_candidates(&[], candidates, &[]);

        assert_eq!(steps.len(), 3);
        assert_eq!(steps[0].sequence, 1);
        assert_eq!(steps[0].priority, 1);
        assert_eq!(
            steps[0].playbook_action,
            IntegrationActivationForecastAction::ResolveBlockers
        );
        assert_eq!(
            steps[0].recommended_view,
            IntegrationActivationPlaybookView::ConstraintQueue
        );
        assert!(steps[0].operator_required());
        assert!(steps[0].blocked());
        assert!(steps[1].blocked());
        assert_eq!(
            steps[1].recommended_view,
            IntegrationActivationPlaybookView::DependencyGraph
        );
        assert!(steps[2].activation_ready());
        assert_eq!(
            steps[2].recommended_view,
            IntegrationActivationPlaybookView::ActivationActions
        );

        let summary = IntegrationActivationPlaybookSummary::from_steps(steps.iter());
        assert_eq!(summary.total_steps, 3);
        assert_eq!(summary.unique_integrations, 3);
        assert!(summary.operator_required_steps >= 2);
        assert_eq!(summary.activation_ready_steps, 1);
        assert!(summary.blocked_steps >= 1);
        assert!(summary.review_required_steps >= 1);
        assert!(summary.constraint_view_steps >= 1);
        assert!(summary.dependency_view_steps >= 1);
        assert!(summary.action_view_steps >= 1);
        assert!(summary.total_actions > 0);
        assert!(summary.total_dossiers > 0);
        assert!(summary.total_evidence > 0);
        assert!(summary.blocking_dependency_edges > 0);
        assert_eq!(
            summary.next_playbook_action,
            Some(IntegrationActivationForecastAction::ResolveBlockers)
        );
        assert_eq!(
            summary.next_recommended_view,
            Some(IntegrationActivationPlaybookView::ConstraintQueue)
        );
        assert_eq!(summary.next_step_sequence, Some(1));
        assert_eq!(summary.next_step_priority, Some(1));
        assert_eq!(summary.first_operator_sequence, Some(1));
        assert_eq!(summary.first_activation_sequence, Some(3));
        assert_eq!(summary.highest_policy_tier, PrivilegeTier::HighRisk);
        assert_eq!(
            summary.overall_status,
            IntegrationActivationHealthStatus::Blocked
        );
        assert!(summary.has_operator_work());
        assert!(summary.has_activation_work());
        assert!(summary.has_blockers());
        assert!(summary.has_review_work());
        assert!(summary.requires_attention());
        assert!(!summary.is_empty());

        let catalog = first_party_catalog();
        let available_primitives = vec![
            PrimitiveFamily::NormalizedModel,
            PrimitiveFamily::DiscoveryIndex,
            PrimitiveFamily::CommandMapping,
            PrimitiveFamily::CapabilityPolicy,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let catalog_steps = activation_playbook_steps_at_or_before_priority(
            &catalog,
            2,
            &available_primitives,
            &allowed_capabilities,
            &[],
        );
        assert!(catalog_steps
            .iter()
            .any(IntegrationActivationPlaybookStep::requires_attention));
    }

    #[test]
    fn activation_operator_tasks_prioritize_human_action_queue() {
        let review_ready_report = IntegrationReadinessReport {
            requested_integration_id: IntegrationId::trusted("review_ready_bridge"),
            display_name: "Review Ready Bridge".to_string(),
            activation_target: IntegrationActivationTarget::Direct,
            priority: 1,
            missing_primitives: Vec::new(),
            missing_capabilities: Vec::new(),
            missing_dependencies: Vec::new(),
            requires_human_review: true,
            highest_policy_tier: PrivilegeTier::HumanApproval,
            local_only: true,
            cloud_required: false,
        };
        let blocked_review_report = IntegrationReadinessReport {
            requested_integration_id: IntegrationId::trusted("blocked_review_camera"),
            display_name: "Blocked Review Camera".to_string(),
            activation_target: IntegrationActivationTarget::DelegatedIntegration(
                IntegrationId::trusted("mqtt"),
            ),
            priority: 2,
            missing_primitives: vec![PrimitiveFamily::CameraMedia],
            missing_capabilities: vec![CapabilityId::trusted("smart_home.command.low_risk")],
            missing_dependencies: vec![IntegrationId::trusted("mqtt")],
            requires_human_review: true,
            highest_policy_tier: PrivilegeTier::HighRisk,
            local_only: false,
            cloud_required: true,
        };
        let ready_report = IntegrationReadinessReport {
            requested_integration_id: IntegrationId::trusted("read_only_probe"),
            display_name: "Read-only Probe".to_string(),
            activation_target: IntegrationActivationTarget::Direct,
            priority: 3,
            missing_primitives: Vec::new(),
            missing_capabilities: Vec::new(),
            missing_dependencies: Vec::new(),
            requires_human_review: false,
            highest_policy_tier: PrivilegeTier::ReadOnly,
            local_only: true,
            cloud_required: false,
        };
        let candidates = activation_candidates_from_reports(
            [review_ready_report, blocked_review_report, ready_report].iter(),
        );
        let tasks = activation_operator_tasks_from_candidates(&[], candidates, &[]);

        assert_eq!(tasks.len(), 3);
        assert_eq!(tasks[0].sequence, 1);
        assert_eq!(tasks[0].playbook_sequence, 1);
        assert_eq!(tasks[0].priority, 1);
        assert_eq!(
            tasks[0].task_kind,
            IntegrationActivationOperatorTaskKind::ResolveConstraints
        );
        assert_eq!(
            tasks[0].recommended_view,
            IntegrationActivationPlaybookView::ConstraintQueue
        );
        assert!(tasks[0].operator_required());
        assert!(tasks[0].actionable());
        assert!(tasks[0].blocked());
        assert!(tasks[1].blocked());
        assert_eq!(
            tasks[1].task_kind,
            IntegrationActivationOperatorTaskKind::EnableDependencies
        );
        assert!(tasks[2].activation_ready());
        assert!(tasks[2].actionable());
        assert_eq!(
            tasks[2].task_kind,
            IntegrationActivationOperatorTaskKind::ActivateWave
        );

        let summary = IntegrationActivationOperatorTaskSummary::from_tasks(tasks.iter());
        assert_eq!(summary.total_tasks, 3);
        assert_eq!(summary.unique_integrations, 3);
        assert!(summary.operator_required_tasks >= 2);
        assert_eq!(summary.actionable_tasks, 3);
        assert_eq!(summary.activation_ready_tasks, 1);
        assert!(summary.blocked_tasks >= 1);
        assert!(summary.review_required_tasks >= 1);
        assert_eq!(summary.resolve_constraint_tasks, 1);
        assert_eq!(summary.enable_dependency_tasks, 1);
        assert_eq!(summary.activate_wave_tasks, 1);
        assert_eq!(
            summary.next_task_kind,
            Some(IntegrationActivationOperatorTaskKind::ResolveConstraints)
        );
        assert_eq!(
            summary.next_playbook_action,
            Some(IntegrationActivationForecastAction::ResolveBlockers)
        );
        assert_eq!(
            summary.next_recommended_view,
            Some(IntegrationActivationPlaybookView::ConstraintQueue)
        );
        assert_eq!(summary.next_task_sequence, Some(1));
        assert_eq!(summary.first_actionable_sequence, Some(1));
        assert_eq!(summary.first_activation_sequence, Some(3));
        assert_eq!(summary.highest_policy_tier, PrivilegeTier::HighRisk);
        assert_eq!(
            summary.overall_status,
            IntegrationActivationHealthStatus::Blocked
        );
        assert!(summary.has_operator_work());
        assert!(summary.has_actionable_work());
        assert!(summary.has_activation_work());
        assert!(summary.has_blockers());
        assert!(summary.has_review_work());
        assert!(summary.requires_attention());
        assert!(!summary.is_empty());

        let catalog = first_party_catalog();
        let available_primitives = vec![
            PrimitiveFamily::NormalizedModel,
            PrimitiveFamily::DiscoveryIndex,
            PrimitiveFamily::CommandMapping,
            PrimitiveFamily::CapabilityPolicy,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let catalog_tasks = activation_operator_tasks_at_or_before_priority(
            &catalog,
            2,
            &available_primitives,
            &allowed_capabilities,
            &[],
        );
        assert!(catalog_tasks
            .iter()
            .any(IntegrationActivationOperatorTask::requires_attention));
    }

    #[test]
    fn activation_control_room_panels_group_operator_work_by_view() {
        let review_ready_report = IntegrationReadinessReport {
            requested_integration_id: IntegrationId::trusted("review_ready_bridge"),
            display_name: "Review Ready Bridge".to_string(),
            activation_target: IntegrationActivationTarget::Direct,
            priority: 1,
            missing_primitives: Vec::new(),
            missing_capabilities: Vec::new(),
            missing_dependencies: Vec::new(),
            requires_human_review: true,
            highest_policy_tier: PrivilegeTier::HumanApproval,
            local_only: true,
            cloud_required: false,
        };
        let blocked_review_report = IntegrationReadinessReport {
            requested_integration_id: IntegrationId::trusted("blocked_review_camera"),
            display_name: "Blocked Review Camera".to_string(),
            activation_target: IntegrationActivationTarget::DelegatedIntegration(
                IntegrationId::trusted("mqtt"),
            ),
            priority: 2,
            missing_primitives: vec![PrimitiveFamily::CameraMedia],
            missing_capabilities: vec![CapabilityId::trusted("smart_home.command.low_risk")],
            missing_dependencies: vec![IntegrationId::trusted("mqtt")],
            requires_human_review: true,
            highest_policy_tier: PrivilegeTier::HighRisk,
            local_only: false,
            cloud_required: true,
        };
        let ready_report = IntegrationReadinessReport {
            requested_integration_id: IntegrationId::trusted("read_only_probe"),
            display_name: "Read-only Probe".to_string(),
            activation_target: IntegrationActivationTarget::Direct,
            priority: 3,
            missing_primitives: Vec::new(),
            missing_capabilities: Vec::new(),
            missing_dependencies: Vec::new(),
            requires_human_review: false,
            highest_policy_tier: PrivilegeTier::ReadOnly,
            local_only: true,
            cloud_required: false,
        };
        let candidates = activation_candidates_from_reports(
            [review_ready_report, blocked_review_report, ready_report].iter(),
        );
        let panels = activation_control_room_panels_from_candidates(&[], candidates, &[]);

        assert_eq!(panels.len(), 3);
        assert_eq!(panels[0].sequence, 1);
        assert_eq!(
            panels[0].recommended_view,
            IntegrationActivationPlaybookView::ConstraintQueue
        );
        assert_eq!(panels[0].priority, 1);
        assert_eq!(panels[0].task_count, 1);
        assert_eq!(panels[0].operator_summary.blocked_tasks, 1);
        assert!(panels[0].has_blockers());
        assert!(panels[0].requires_attention());
        assert_eq!(
            panels[0].operator_summary.next_task_kind,
            Some(IntegrationActivationOperatorTaskKind::ResolveConstraints)
        );
        assert!(panels.iter().any(|panel| {
            panel.recommended_view == IntegrationActivationPlaybookView::DependencyGraph
                && panel.has_blockers()
        }));
        assert!(panels.iter().any(|panel| {
            panel.recommended_view == IntegrationActivationPlaybookView::ActivationActions
                && panel.has_activation_work()
        }));

        let summary = IntegrationActivationControlRoomSummary::from_panels(panels.iter());
        assert_eq!(summary.total_panels, 3);
        assert_eq!(summary.unique_integrations, 3);
        assert_eq!(summary.total_tasks, 3);
        assert_eq!(summary.constraint_panels, 1);
        assert_eq!(summary.dependency_panels, 1);
        assert_eq!(summary.action_panels, 1);
        assert!(summary.panels_requiring_attention >= 2);
        assert!(summary.panels_with_blockers >= 2);
        assert_eq!(summary.panels_with_activation_work, 1);
        assert_eq!(
            summary.next_recommended_view,
            Some(IntegrationActivationPlaybookView::ConstraintQueue)
        );
        assert_eq!(
            summary.next_task_kind,
            Some(IntegrationActivationOperatorTaskKind::ResolveConstraints)
        );
        assert_eq!(summary.next_panel_sequence, Some(1));
        assert_eq!(summary.first_actionable_panel_sequence, Some(1));
        assert_eq!(summary.highest_policy_tier, PrivilegeTier::HighRisk);
        assert_eq!(
            summary.overall_status,
            IntegrationActivationHealthStatus::Blocked
        );
        assert!(summary.has_operator_work());
        assert!(summary.has_actionable_work());
        assert!(summary.has_activation_work());
        assert!(summary.has_blockers());
        assert!(summary.has_review_work());
        assert!(summary.requires_attention());
        assert!(!summary.is_empty());

        let catalog = first_party_catalog();
        let available_primitives = vec![
            PrimitiveFamily::NormalizedModel,
            PrimitiveFamily::DiscoveryIndex,
            PrimitiveFamily::CommandMapping,
            PrimitiveFamily::CapabilityPolicy,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let catalog_panels = activation_control_room_panels_at_or_before_priority(
            &catalog,
            2,
            &available_primitives,
            &allowed_capabilities,
            &[],
        );
        assert!(catalog_panels
            .iter()
            .any(IntegrationActivationControlRoomPanel::requires_attention));
    }

    #[test]
    fn activation_command_center_sections_group_control_room_attention() {
        let review_ready_report = IntegrationReadinessReport {
            requested_integration_id: IntegrationId::trusted("review_ready_bridge"),
            display_name: "Review Ready Bridge".to_string(),
            activation_target: IntegrationActivationTarget::Direct,
            priority: 1,
            missing_primitives: Vec::new(),
            missing_capabilities: Vec::new(),
            missing_dependencies: Vec::new(),
            requires_human_review: true,
            highest_policy_tier: PrivilegeTier::HumanApproval,
            local_only: true,
            cloud_required: false,
        };
        let blocked_review_report = IntegrationReadinessReport {
            requested_integration_id: IntegrationId::trusted("blocked_review_camera"),
            display_name: "Blocked Review Camera".to_string(),
            activation_target: IntegrationActivationTarget::DelegatedIntegration(
                IntegrationId::trusted("mqtt"),
            ),
            priority: 2,
            missing_primitives: vec![PrimitiveFamily::CameraMedia],
            missing_capabilities: vec![CapabilityId::trusted("smart_home.command.low_risk")],
            missing_dependencies: vec![IntegrationId::trusted("mqtt")],
            requires_human_review: true,
            highest_policy_tier: PrivilegeTier::HighRisk,
            local_only: false,
            cloud_required: true,
        };
        let ready_report = IntegrationReadinessReport {
            requested_integration_id: IntegrationId::trusted("read_only_probe"),
            display_name: "Read-only Probe".to_string(),
            activation_target: IntegrationActivationTarget::Direct,
            priority: 3,
            missing_primitives: Vec::new(),
            missing_capabilities: Vec::new(),
            missing_dependencies: Vec::new(),
            requires_human_review: false,
            highest_policy_tier: PrivilegeTier::ReadOnly,
            local_only: true,
            cloud_required: false,
        };
        let candidates = activation_candidates_from_reports(
            [review_ready_report, blocked_review_report, ready_report].iter(),
        );
        let sections = activation_command_center_sections_from_candidates(&[], candidates, &[]);

        assert_eq!(sections.len(), 2);
        assert_eq!(sections[0].sequence, 1);
        assert_eq!(
            sections[0].section_kind,
            IntegrationActivationCommandCenterSectionKind::Blockers
        );
        assert_eq!(sections[0].priority, 1);
        assert_eq!(sections[0].panel_count, 2);
        assert!(sections[0].has_blockers());
        assert!(sections[0].has_review_work());
        assert!(sections[0].requires_attention());
        assert!(sections[0]
            .recommended_views
            .contains(&IntegrationActivationPlaybookView::ConstraintQueue));
        assert!(sections[0]
            .recommended_views
            .contains(&IntegrationActivationPlaybookView::DependencyGraph));
        assert!(sections.iter().any(|section| {
            section.section_kind == IntegrationActivationCommandCenterSectionKind::Activation
                && section.has_activation_work()
        }));

        let summary = IntegrationActivationCommandCenterSummary::from_sections(sections.iter());
        assert_eq!(summary.total_sections, 2);
        assert_eq!(summary.unique_integrations, 3);
        assert_eq!(summary.total_panels, 3);
        assert_eq!(summary.total_tasks, 3);
        assert_eq!(summary.blocker_sections, 1);
        assert_eq!(summary.activation_sections, 1);
        assert_eq!(summary.sections_with_blockers, 1);
        assert_eq!(summary.sections_with_activation_work, 1);
        assert_eq!(
            summary.next_section_kind,
            Some(IntegrationActivationCommandCenterSectionKind::Blockers)
        );
        assert_eq!(
            summary.next_recommended_view,
            Some(IntegrationActivationPlaybookView::ConstraintQueue)
        );
        assert_eq!(
            summary.next_task_kind,
            Some(IntegrationActivationOperatorTaskKind::ResolveConstraints)
        );
        assert_eq!(summary.next_section_sequence, Some(1));
        assert_eq!(summary.first_blocker_section_sequence, Some(1));
        assert_eq!(summary.first_activation_section_sequence, Some(2));
        assert_eq!(summary.highest_policy_tier, PrivilegeTier::HighRisk);
        assert_eq!(
            summary.overall_status,
            IntegrationActivationHealthStatus::Blocked
        );
        assert!(summary.has_operator_work());
        assert!(summary.has_actionable_work());
        assert!(summary.has_activation_work());
        assert!(summary.has_blockers());
        assert!(summary.has_review_work());
        assert!(summary.requires_attention());
        assert!(!summary.is_empty());

        let catalog = first_party_catalog();
        let available_primitives = vec![
            PrimitiveFamily::NormalizedModel,
            PrimitiveFamily::DiscoveryIndex,
            PrimitiveFamily::CommandMapping,
            PrimitiveFamily::CapabilityPolicy,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let catalog_sections = activation_command_center_sections_at_or_before_priority(
            &catalog,
            2,
            &available_primitives,
            &allowed_capabilities,
            &[],
        );
        assert!(catalog_sections
            .iter()
            .any(IntegrationActivationCommandCenterSection::requires_attention));
    }

    #[test]
    fn activation_watchtower_signals_roll_up_command_center_attention() {
        let review_ready_report = IntegrationReadinessReport {
            requested_integration_id: IntegrationId::trusted("review_ready_bridge"),
            display_name: "Review Ready Bridge".to_string(),
            activation_target: IntegrationActivationTarget::Direct,
            priority: 1,
            missing_primitives: Vec::new(),
            missing_capabilities: Vec::new(),
            missing_dependencies: Vec::new(),
            requires_human_review: true,
            highest_policy_tier: PrivilegeTier::HumanApproval,
            local_only: true,
            cloud_required: false,
        };
        let blocked_review_report = IntegrationReadinessReport {
            requested_integration_id: IntegrationId::trusted("blocked_review_camera"),
            display_name: "Blocked Review Camera".to_string(),
            activation_target: IntegrationActivationTarget::DelegatedIntegration(
                IntegrationId::trusted("mqtt"),
            ),
            priority: 2,
            missing_primitives: vec![PrimitiveFamily::CameraMedia],
            missing_capabilities: vec![CapabilityId::trusted("smart_home.command.low_risk")],
            missing_dependencies: vec![IntegrationId::trusted("mqtt")],
            requires_human_review: true,
            highest_policy_tier: PrivilegeTier::HighRisk,
            local_only: false,
            cloud_required: true,
        };
        let ready_report = IntegrationReadinessReport {
            requested_integration_id: IntegrationId::trusted("read_only_probe"),
            display_name: "Read-only Probe".to_string(),
            activation_target: IntegrationActivationTarget::Direct,
            priority: 3,
            missing_primitives: Vec::new(),
            missing_capabilities: Vec::new(),
            missing_dependencies: Vec::new(),
            requires_human_review: false,
            highest_policy_tier: PrivilegeTier::ReadOnly,
            local_only: true,
            cloud_required: false,
        };
        let candidates = activation_candidates_from_reports(
            [review_ready_report, blocked_review_report, ready_report].iter(),
        );
        let sections = activation_command_center_sections_from_candidates(&[], candidates, &[]);
        let signals = activation_watchtower_signals_from_command_center_sections(sections);

        assert_eq!(signals.len(), 2);
        assert_eq!(signals[0].sequence, 1);
        assert_eq!(
            signals[0].signal_kind,
            IntegrationActivationWatchtowerSignalKind::Escalation
        );
        assert!(signals[0].has_blockers());
        assert!(signals[0].requires_attention());
        assert!(signals[0].needs_escalation());
        assert!(signals[0]
            .section_kinds
            .contains(&IntegrationActivationCommandCenterSectionKind::Blockers));
        assert!(!signals[0].section_sequences.is_empty());
        assert!(!signals[0].recommended_views.is_empty());
        assert!(signals.iter().any(|signal| {
            signal.signal_kind == IntegrationActivationWatchtowerSignalKind::Ready
                && signal.has_activation_work()
        }));

        let summary = IntegrationActivationWatchtowerSummary::from_signals(signals.iter());
        assert_eq!(summary.total_signals, 2);
        assert_eq!(summary.unique_integrations, 3);
        assert_eq!(summary.total_sections, 2);
        assert_eq!(summary.total_panels, 3);
        assert_eq!(summary.escalation_signals, 1);
        assert_eq!(summary.ready_signals, 1);
        assert_eq!(summary.signals_with_blockers, 1);
        assert_eq!(summary.signals_with_activation_work, 1);
        assert_eq!(
            summary.next_signal_kind,
            Some(IntegrationActivationWatchtowerSignalKind::Escalation)
        );
        assert_eq!(
            summary.next_section_kind,
            Some(IntegrationActivationCommandCenterSectionKind::Blockers)
        );
        assert_eq!(
            summary.next_task_kind,
            Some(IntegrationActivationOperatorTaskKind::ResolveConstraints)
        );
        assert_eq!(summary.next_signal_sequence, Some(1));
        assert_eq!(summary.first_escalation_signal_sequence, Some(1));
        assert_eq!(summary.first_ready_signal_sequence, Some(2));
        assert_eq!(summary.highest_policy_tier, PrivilegeTier::HighRisk);
        assert_eq!(
            summary.overall_status,
            IntegrationActivationHealthStatus::Blocked
        );
        assert!(summary.has_operator_work());
        assert!(summary.has_actionable_work());
        assert!(summary.has_activation_work());
        assert!(summary.has_blockers());
        assert!(summary.has_review_work());
        assert!(summary.requires_attention());
        assert!(summary.needs_escalation());
        assert!(!summary.is_empty());

        let catalog = first_party_catalog();
        let available_primitives = vec![
            PrimitiveFamily::NormalizedModel,
            PrimitiveFamily::DiscoveryIndex,
            PrimitiveFamily::CommandMapping,
            PrimitiveFamily::CapabilityPolicy,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let catalog_signals = activation_watchtower_signals_at_or_before_priority(
            &catalog,
            2,
            &available_primitives,
            &allowed_capabilities,
            &[],
        );
        assert!(catalog_signals
            .iter()
            .any(IntegrationActivationWatchtowerSignal::requires_attention));
    }

    #[test]
    fn activation_sentinel_alerts_roll_up_activation_attention() {
        let reports = [IntegrationReadinessReport {
                requested_integration_id: IntegrationId::trusted("review_ready_bridge"),
                display_name: "Review Ready Bridge".to_string(),
                activation_target: IntegrationActivationTarget::Direct,
                priority: 1,
                missing_primitives: Vec::new(),
                missing_capabilities: Vec::new(),
                missing_dependencies: Vec::new(),
                requires_human_review: true,
                highest_policy_tier: PrivilegeTier::HumanApproval,
                local_only: true,
                cloud_required: false,
            },
            IntegrationReadinessReport {
                requested_integration_id: IntegrationId::trusted("blocked_review_camera"),
                display_name: "Blocked Review Camera".to_string(),
                activation_target: IntegrationActivationTarget::DelegatedIntegration(
                    IntegrationId::trusted("mqtt"),
                ),
                priority: 2,
                missing_primitives: vec![PrimitiveFamily::CameraMedia],
                missing_capabilities: vec![CapabilityId::trusted("smart_home.command.low_risk")],
                missing_dependencies: vec![IntegrationId::trusted("mqtt")],
                requires_human_review: true,
                highest_policy_tier: PrivilegeTier::HighRisk,
                local_only: false,
                cloud_required: true,
            },
            IntegrationReadinessReport {
                requested_integration_id: IntegrationId::trusted("read_only_probe"),
                display_name: "Read-only Probe".to_string(),
                activation_target: IntegrationActivationTarget::Direct,
                priority: 3,
                missing_primitives: Vec::new(),
                missing_capabilities: Vec::new(),
                missing_dependencies: Vec::new(),
                requires_human_review: false,
                highest_policy_tier: PrivilegeTier::ReadOnly,
                local_only: true,
                cloud_required: false,
        }];
        let candidates = activation_candidates_from_reports(reports.iter());
        let risks = activation_risk_from_candidates(&[], candidates.iter());
        let sections = activation_command_center_sections_from_candidates(&[], candidates, &[]);
        let signals = activation_watchtower_signals_from_command_center_sections(sections);
        let graph = activation_dependency_graph_from_reports(&[], reports.iter(), &[]);
        let gap_inventory = readiness_gap_inventory_from_reports(reports.iter());
        let alerts =
            activation_sentinel_alerts_from_rollups(&signals, &risks, &graph, &gap_inventory);

        assert!(alerts
            .iter()
            .any(|alert| alert.alert_kind == IntegrationActivationSentinelAlertKind::Blocker));
        assert!(alerts
            .iter()
            .any(|alert| alert.alert_kind == IntegrationActivationSentinelAlertKind::Dependency));
        assert!(alerts
            .iter()
            .any(|alert| alert.alert_kind == IntegrationActivationSentinelAlertKind::PolicyRisk));
        assert!(alerts
            .iter()
            .any(|alert| alert.alert_kind == IntegrationActivationSentinelAlertKind::Review));
        assert!(alerts
            .iter()
            .any(|alert| alert.alert_kind == IntegrationActivationSentinelAlertKind::Ready));
        assert!(alerts.iter().all(|alert| alert.sequence > 0));
        assert!(alerts
            .iter()
            .any(IntegrationActivationSentinelAlert::has_gaps));
        assert!(alerts
            .iter()
            .any(IntegrationActivationSentinelAlert::has_policy_risk));

        let summary = IntegrationActivationSentinelSummary::from_alerts(alerts.iter());
        assert_eq!(summary.total_alerts, alerts.len());
        assert_eq!(summary.unique_integrations, 4);
        assert_eq!(summary.blocker_alerts, 1);
        assert_eq!(summary.dependency_alerts, 1);
        assert_eq!(summary.policy_risk_alerts, 1);
        assert_eq!(summary.review_alerts, 1);
        assert_eq!(summary.ready_alerts, 1);
        assert_eq!(summary.total_watchtower_signals, signals.len());
        assert_eq!(summary.total_risks, risks.len());
        assert_eq!(summary.total_unique_gaps, gap_inventory.total_unique_gaps());
        assert!(summary.requires_attention());
        assert!(summary.has_blockers());
        assert!(summary.has_dependency_work());
        assert!(summary.has_policy_risk());
        assert!(summary.has_review_work());
        assert!(summary.has_activation_work());
        assert_eq!(
            summary.overall_status,
            IntegrationActivationHealthStatus::Blocked
        );

        let catalog = first_party_catalog();
        let available_primitives = vec![
            PrimitiveFamily::NormalizedModel,
            PrimitiveFamily::DiscoveryIndex,
            PrimitiveFamily::CommandMapping,
            PrimitiveFamily::CapabilityPolicy,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let catalog_alerts = activation_sentinel_alerts_at_or_before_priority(
            &catalog,
            2,
            &available_primitives,
            &allowed_capabilities,
            &[],
        );
        assert!(catalog_alerts
            .iter()
            .any(IntegrationActivationSentinelAlert::requires_attention));
    }

    #[test]
    fn activation_audit_records_join_sentinel_attention_to_evidence() {
        let reports = [IntegrationReadinessReport {
                requested_integration_id: IntegrationId::trusted("review_ready_bridge"),
                display_name: "Review Ready Bridge".to_string(),
                activation_target: IntegrationActivationTarget::Direct,
                priority: 1,
                missing_primitives: Vec::new(),
                missing_capabilities: Vec::new(),
                missing_dependencies: Vec::new(),
                requires_human_review: true,
                highest_policy_tier: PrivilegeTier::HumanApproval,
                local_only: true,
                cloud_required: false,
            },
            IntegrationReadinessReport {
                requested_integration_id: IntegrationId::trusted("blocked_review_camera"),
                display_name: "Blocked Review Camera".to_string(),
                activation_target: IntegrationActivationTarget::DelegatedIntegration(
                    IntegrationId::trusted("mqtt"),
                ),
                priority: 2,
                missing_primitives: vec![PrimitiveFamily::CameraMedia],
                missing_capabilities: vec![CapabilityId::trusted("smart_home.command.low_risk")],
                missing_dependencies: vec![IntegrationId::trusted("mqtt")],
                requires_human_review: true,
                highest_policy_tier: PrivilegeTier::HighRisk,
                local_only: false,
                cloud_required: true,
            },
            IntegrationReadinessReport {
                requested_integration_id: IntegrationId::trusted("read_only_probe"),
                display_name: "Read-only Probe".to_string(),
                activation_target: IntegrationActivationTarget::Direct,
                priority: 3,
                missing_primitives: Vec::new(),
                missing_capabilities: Vec::new(),
                missing_dependencies: Vec::new(),
                requires_human_review: false,
                highest_policy_tier: PrivilegeTier::ReadOnly,
                local_only: true,
                cloud_required: false,
        }];
        let candidates = activation_candidates_from_reports(reports.iter());
        let risks = activation_risk_from_candidates(&[], candidates.iter());
        let sections =
            activation_command_center_sections_from_candidates(&[], candidates.clone(), &[]);
        let signals = activation_watchtower_signals_from_command_center_sections(sections);
        let graph = activation_dependency_graph_from_reports(&[], reports.iter(), &[]);
        let gap_inventory = readiness_gap_inventory_from_reports(reports.iter());
        let alerts =
            activation_sentinel_alerts_from_rollups(&signals, &risks, &graph, &gap_inventory);
        let decisions = activation_decisions_from_candidates(&[], candidates.iter(), &[]);
        let evidence = activation_evidence_from_decisions(decisions.iter());
        let records = activation_audit_records_from_rollups(
            &alerts,
            &signals,
            &decisions,
            &evidence,
            &risks,
            &graph,
            &gap_inventory,
        );

        assert!(records.iter().all(|record| record.sequence > 0));
        assert!(records
            .iter()
            .any(|record| record.record_kind == IntegrationActivationAuditRecordKind::Sentinel));
        assert!(records
            .iter()
            .any(|record| record.record_kind == IntegrationActivationAuditRecordKind::Evidence));
        assert!(records
            .iter()
            .any(|record| record.record_kind == IntegrationActivationAuditRecordKind::Risk));
        assert!(records
            .iter()
            .any(|record| record.record_kind == IntegrationActivationAuditRecordKind::Dependency));
        assert!(records.iter().any(|record| {
            record.record_kind == IntegrationActivationAuditRecordKind::ReadinessGap
                && record.readiness_gap_kind.as_deref() == Some("primitive")
        }));
        assert!(records
            .iter()
            .any(IntegrationActivationAuditRecord::requires_attention));

        let summary = IntegrationActivationAuditSummary::from_records(records.iter());
        assert_eq!(summary.total_records, records.len());
        assert!(summary.sentinel_records >= alerts.len());
        assert!(summary.evidence_records >= evidence.len());
        assert!(summary.records_requiring_attention > 0);
        assert!(summary.has_dependency_work());
        assert!(summary.requires_attention());
        assert_eq!(
            summary.overall_status,
            IntegrationActivationHealthStatus::Blocked
        );

        let catalog = first_party_catalog();
        let available_primitives = vec![
            PrimitiveFamily::NormalizedModel,
            PrimitiveFamily::DiscoveryIndex,
            PrimitiveFamily::CommandMapping,
            PrimitiveFamily::CapabilityPolicy,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let catalog_records = activation_audit_records_at_or_before_priority(
            &catalog,
            2,
            &available_primitives,
            &allowed_capabilities,
            &[],
        );
        assert!(catalog_records
            .iter()
            .any(IntegrationActivationAuditRecord::requires_attention));
    }

    #[test]
    fn activation_runbook_entries_join_playbook_steps_to_audit_context() {
        let reports = [IntegrationReadinessReport {
                requested_integration_id: IntegrationId::trusted("review_ready_bridge"),
                display_name: "Review Ready Bridge".to_string(),
                activation_target: IntegrationActivationTarget::Direct,
                priority: 1,
                missing_primitives: Vec::new(),
                missing_capabilities: Vec::new(),
                missing_dependencies: Vec::new(),
                requires_human_review: true,
                highest_policy_tier: PrivilegeTier::HumanApproval,
                local_only: true,
                cloud_required: false,
            },
            IntegrationReadinessReport {
                requested_integration_id: IntegrationId::trusted("blocked_review_camera"),
                display_name: "Blocked Review Camera".to_string(),
                activation_target: IntegrationActivationTarget::DelegatedIntegration(
                    IntegrationId::trusted("mqtt"),
                ),
                priority: 2,
                missing_primitives: vec![PrimitiveFamily::CameraMedia],
                missing_capabilities: vec![CapabilityId::trusted("smart_home.command.low_risk")],
                missing_dependencies: vec![IntegrationId::trusted("mqtt")],
                requires_human_review: true,
                highest_policy_tier: PrivilegeTier::HighRisk,
                local_only: false,
                cloud_required: true,
            },
            IntegrationReadinessReport {
                requested_integration_id: IntegrationId::trusted("read_only_probe"),
                display_name: "Read-only Probe".to_string(),
                activation_target: IntegrationActivationTarget::Direct,
                priority: 3,
                missing_primitives: Vec::new(),
                missing_capabilities: Vec::new(),
                missing_dependencies: Vec::new(),
                requires_human_review: false,
                highest_policy_tier: PrivilegeTier::ReadOnly,
                local_only: true,
                cloud_required: false,
        }];
        let candidates = activation_candidates_from_reports(reports.iter());
        let steps = activation_playbook_steps_from_candidates(&[], candidates.clone(), &[]);
        let risks = activation_risk_from_candidates(&[], candidates.iter());
        let signals = activation_watchtower_signals_from_candidates(&[], candidates.clone(), &[]);
        let graph = activation_dependency_graph_from_reports(&[], reports.iter(), &[]);
        let gap_inventory = readiness_gap_inventory_from_reports(reports.iter());
        let alerts =
            activation_sentinel_alerts_from_rollups(&signals, &risks, &graph, &gap_inventory);
        let decisions = activation_decisions_from_candidates(&[], candidates.iter(), &[]);
        let evidence = activation_evidence_from_decisions(decisions.iter());
        let audit_records = activation_audit_records_from_rollups(
            &alerts,
            &signals,
            &decisions,
            &evidence,
            &risks,
            &graph,
            &gap_inventory,
        );

        let tasks = activation_operator_tasks_from_playbook_steps(steps.clone());
        let entries = activation_runbook_entries_from_playbook_steps(steps, &audit_records);

        assert!(entries.iter().all(|entry| entry.sequence > 0));
        assert!(entries
            .iter()
            .any(|entry| entry.phase == IntegrationActivationRunbookPhase::ClearBlockers));
        assert!(entries
            .iter()
            .any(IntegrationActivationRunbookEntry::review_required));
        assert!(entries
            .iter()
            .any(IntegrationActivationRunbookEntry::has_audit_context));

        let blocked = entries
            .iter()
            .find(|entry| {
                entry
                    .integration_ids
                    .contains(&IntegrationId::trusted("blocked_review_camera"))
            })
            .unwrap();
        assert!(blocked.blocked());
        assert!(blocked.requires_attention());
        assert!(blocked.audit_record_count > 0);
        assert!(blocked.dependency_audit_record_count > 0);
        assert!(blocked.readiness_gap_record_count > 0);

        let summary = IntegrationActivationRunbookSummary::from_entries(entries.iter());
        assert_eq!(summary.total_entries, entries.len());
        assert!(summary.has_audit_context());
        assert!(summary.has_blockers());
        assert!(summary.has_review_work());
        assert!(summary.requires_attention());
        assert!(summary.total_audit_records >= audit_records.len());
        assert_eq!(
            summary.overall_status,
            IntegrationActivationHealthStatus::Blocked
        );

        let handoff = activation_handoff_packages_from_runbook_entries(
            entries.iter(),
            &risks,
            &graph,
            &gap_inventory,
        );
        assert_eq!(handoff.len(), entries.len());
        assert!(handoff
            .iter()
            .any(IntegrationActivationHandoffPackage::requires_attention));

        let blocked_handoff = handoff
            .iter()
            .find(|package| {
                package
                    .integration_ids
                    .contains(&IntegrationId::trusted("blocked_review_camera"))
            })
            .unwrap();
        assert_eq!(
            blocked_handoff.handoff_status,
            IntegrationActivationHandoffStatus::Blocked
        );
        assert!(blocked_handoff.blocked());
        assert!(blocked_handoff.has_dependency_blockers());
        assert!(blocked_handoff.has_readiness_gaps());
        assert!(blocked_handoff.risk_count > 0);
        assert!(blocked_handoff.readiness_gap_count > 0);

        let handoff_summary = IntegrationActivationHandoffSummary::from_packages(handoff.iter());
        assert_eq!(handoff_summary.total_packages, handoff.len());
        assert!(handoff_summary.has_blockers());
        assert!(handoff_summary.has_review_work());
        assert!(handoff_summary.requires_attention());
        assert_eq!(
            handoff_summary.overall_status,
            IntegrationActivationHealthStatus::Blocked
        );

        let execution =
            activation_execution_packets_from_handoff_packages(handoff.clone(), tasks.clone());
        assert_eq!(execution.len(), handoff.len());
        assert!(execution
            .iter()
            .any(IntegrationActivationExecutionPacket::requires_attention));
        assert!(
            execution
                .iter()
                .any(|packet| packet.execution_status
                    == IntegrationActivationExecutionStatus::Blocked)
        );
        assert!(execution
            .iter()
            .any(|packet| packet.execution_status
                == IntegrationActivationExecutionStatus::NeedsApproval));

        let blocked_execution = execution
            .iter()
            .find(|packet| {
                packet
                    .integration_ids
                    .contains(&IntegrationId::trusted("blocked_review_camera"))
            })
            .unwrap();
        assert_eq!(
            blocked_execution.execution_status,
            IntegrationActivationExecutionStatus::Blocked
        );
        assert!(blocked_execution.blocked());
        assert!(!blocked_execution.dependency_ready());
        assert!(!blocked_execution.executable());

        let execution_summary =
            IntegrationActivationExecutionSummary::from_packets(execution.iter());
        assert_eq!(execution_summary.total_packets, execution.len());
        assert!(execution_summary.has_blockers());
        assert!(execution_summary.has_approval_work());
        assert!(execution_summary.requires_attention());
        assert_eq!(
            execution_summary.overall_status,
            IntegrationActivationHealthStatus::Blocked
        );

        let verification =
            activation_verification_checkpoints_from_execution_packets(execution.clone());
        assert_eq!(verification.len(), execution.len());
        assert!(verification
            .iter()
            .any(IntegrationActivationVerificationCheckpoint::requires_attention));
        assert!(verification
            .iter()
            .any(|checkpoint| checkpoint.verification_status
                == IntegrationActivationVerificationStatus::Blocked));
        assert!(verification
            .iter()
            .any(|checkpoint| checkpoint.verification_status
                == IntegrationActivationVerificationStatus::PendingApproval));

        let blocked_verification = verification
            .iter()
            .find(|checkpoint| {
                checkpoint
                    .integration_ids
                    .contains(&IntegrationId::trusted("blocked_review_camera"))
            })
            .unwrap();
        assert_eq!(
            blocked_verification.verification_status,
            IntegrationActivationVerificationStatus::Blocked
        );
        assert!(blocked_verification.blocked());
        assert!(!blocked_verification.dependency_ready());
        assert!(!blocked_verification.can_verify());

        let verification_summary =
            IntegrationActivationVerificationSummary::from_checkpoints(verification.iter());
        assert_eq!(verification_summary.total_checkpoints, verification.len());
        assert!(verification_summary.has_blockers());
        assert!(verification_summary.has_pending_work());
        assert!(verification_summary.requires_attention());
        assert_eq!(
            verification_summary.overall_status,
            IntegrationActivationHealthStatus::Blocked
        );

        let escalation_cases =
            activation_escalation_cases_from_rollups(&alerts, &verification, &audit_records);
        assert!(!escalation_cases.is_empty());
        assert!(escalation_cases
            .iter()
            .any(|case| case.case_kind == IntegrationActivationEscalationCaseKind::Blocker));
        assert!(escalation_cases
            .iter()
            .any(|case| case.case_kind == IntegrationActivationEscalationCaseKind::Dependency));
        assert!(escalation_cases
            .iter()
            .any(|case| case.case_kind == IntegrationActivationEscalationCaseKind::Verification));
        assert!(escalation_cases
            .iter()
            .any(IntegrationActivationEscalationCase::requires_attention));
        assert!(escalation_cases
            .iter()
            .any(IntegrationActivationEscalationCase::has_dependency_work));
        assert!(escalation_cases
            .iter()
            .any(IntegrationActivationEscalationCase::blocked));

        let escalation_summary =
            IntegrationActivationEscalationSummary::from_cases(escalation_cases.iter());
        assert_eq!(escalation_summary.total_cases, escalation_cases.len());
        assert!(escalation_summary.has_blockers());
        assert!(escalation_summary.has_dependency_work());
        assert!(escalation_summary.requires_attention());
        assert_eq!(
            escalation_summary.next_case_kind,
            Some(IntegrationActivationEscalationCaseKind::Blocker)
        );
        assert_eq!(
            escalation_summary.next_recommended_view,
            Some(IntegrationActivationPlaybookView::ConstraintQueue)
        );
        assert_eq!(
            escalation_summary.overall_status,
            IntegrationActivationHealthStatus::Blocked
        );

        let response_items = activation_response_items_from_escalation_cases(&escalation_cases);
        assert!(!response_items.is_empty());
        assert!(response_items
            .iter()
            .any(|response| response.response_kind
                == IntegrationActivationResponseKind::ResolveBlocker));
        assert!(response_items.iter().any(|response| response.response_kind
            == IntegrationActivationResponseKind::EnableDependency));
        assert!(response_items.iter().any(|response| response.response_kind
            == IntegrationActivationResponseKind::VerifyActivation));
        assert!(response_items
            .iter()
            .any(IntegrationActivationResponseItem::requires_attention));
        assert!(response_items
            .iter()
            .any(IntegrationActivationResponseItem::blocked));

        let response_summary =
            IntegrationActivationResponseSummary::from_responses(response_items.iter());
        assert_eq!(response_summary.total_responses, response_items.len());
        assert!(response_summary.has_blockers());
        assert!(response_summary.has_dependency_work());
        assert!(response_summary.requires_attention());
        assert_eq!(
            response_summary.next_response_kind,
            Some(IntegrationActivationResponseKind::ResolveBlocker)
        );
        assert_eq!(
            response_summary.next_owner_lane,
            Some(IntegrationActivationResponseOwnerLane::Platform)
        );
        assert_eq!(
            response_summary.overall_status,
            IntegrationActivationHealthStatus::Blocked
        );

        let remediation_items = activation_remediation_items_from_responses(&response_items);
        assert!(!remediation_items.is_empty());
        assert!(remediation_items
            .iter()
            .any(|remediation| remediation.remediation_kind
                == IntegrationActivationRemediationKind::UnblockPlatform));
        assert!(remediation_items
            .iter()
            .any(|remediation| remediation.remediation_kind
                == IntegrationActivationRemediationKind::EnableDependency));
        assert!(remediation_items
            .iter()
            .any(|remediation| remediation.remediation_kind
                == IntegrationActivationRemediationKind::RunVerification));
        assert!(remediation_items
            .iter()
            .any(IntegrationActivationRemediationItem::requires_attention));
        assert!(remediation_items
            .iter()
            .any(IntegrationActivationRemediationItem::blocked));

        let remediation_summary =
            IntegrationActivationRemediationSummary::from_remediations(remediation_items.iter());
        assert_eq!(
            remediation_summary.total_remediations,
            remediation_items.len()
        );
        assert!(remediation_summary.has_blockers());
        assert!(remediation_summary.has_dependency_work());
        assert!(remediation_summary.requires_attention());
        assert_eq!(
            remediation_summary.next_remediation_kind,
            Some(IntegrationActivationRemediationKind::UnblockPlatform)
        );
        assert_eq!(
            remediation_summary.next_remediation_status,
            Some(IntegrationActivationRemediationStatus::Blocked)
        );
        assert_eq!(
            remediation_summary.next_owner_lane,
            Some(IntegrationActivationResponseOwnerLane::Platform)
        );
        assert_eq!(
            remediation_summary.overall_status,
            IntegrationActivationHealthStatus::Blocked
        );

        let closure_gates = activation_closure_gates_from_remediations(&remediation_items);
        assert_eq!(closure_gates.len(), remediation_items.len());
        assert!(closure_gates
            .iter()
            .any(|gate| gate.closure_status == IntegrationActivationClosureStatus::Blocked));
        assert!(closure_gates
            .iter()
            .any(|gate| gate.closure_status
                == IntegrationActivationClosureStatus::ReadyForVerification));
        assert!(closure_gates
            .iter()
            .any(IntegrationActivationClosureGate::requires_attention));
        assert!(closure_gates
            .iter()
            .any(IntegrationActivationClosureGate::blocked));

        let closure_summary = IntegrationActivationClosureSummary::from_gates(closure_gates.iter());
        assert_eq!(closure_summary.total_gates, closure_gates.len());
        assert!(closure_summary.has_blockers());
        assert!(closure_summary.ready_for_verification());
        assert!(closure_summary.requires_attention());
        assert_eq!(
            closure_summary.next_closure_status,
            Some(IntegrationActivationClosureStatus::Blocked)
        );
        assert_eq!(
            closure_summary.next_owner_lane,
            Some(IntegrationActivationResponseOwnerLane::Platform)
        );
        assert_eq!(
            closure_summary.overall_status,
            IntegrationActivationHealthStatus::Blocked
        );

        let release_packets = activation_release_packets_from_closure_gates(&closure_gates);
        assert_eq!(release_packets.len(), closure_gates.len());
        assert!(release_packets
            .iter()
            .any(|packet| packet.release_status == IntegrationActivationReleaseStatus::Blocked));
        assert!(release_packets.iter().any(|packet| packet.release_status
            == IntegrationActivationReleaseStatus::VerificationRequired));
        assert!(release_packets
            .iter()
            .any(IntegrationActivationReleasePacket::requires_attention));
        assert!(release_packets
            .iter()
            .any(IntegrationActivationReleasePacket::release_blocked));

        let release_summary =
            IntegrationActivationReleaseSummary::from_packets(release_packets.iter());
        assert_eq!(release_summary.total_packets, release_packets.len());
        assert!(release_summary.has_blockers());
        assert!(release_summary.needs_verification());
        assert!(release_summary.requires_attention());
        assert_eq!(
            release_summary.next_release_status,
            Some(IntegrationActivationReleaseStatus::Blocked)
        );
        assert_eq!(
            release_summary.next_owner_lane,
            Some(IntegrationActivationResponseOwnerLane::Platform)
        );
        assert_eq!(
            release_summary.overall_status,
            IntegrationActivationHealthStatus::Blocked
        );

        let delivery_manifests =
            activation_delivery_manifests_from_release_packets(&release_packets);
        assert_eq!(delivery_manifests.len(), release_packets.len());
        assert!(delivery_manifests.iter().any(
            |manifest| manifest.delivery_status == IntegrationActivationDeliveryStatus::Blocked
        ));
        assert!(delivery_manifests
            .iter()
            .any(|manifest| manifest.delivery_status
                == IntegrationActivationDeliveryStatus::AwaitingVerification));
        assert!(delivery_manifests
            .iter()
            .any(IntegrationActivationDeliveryManifest::requires_attention));
        assert!(delivery_manifests
            .iter()
            .any(IntegrationActivationDeliveryManifest::delivery_blocked));

        let delivery_summary =
            IntegrationActivationDeliverySummary::from_manifests(delivery_manifests.iter());
        assert_eq!(delivery_summary.total_manifests, delivery_manifests.len());
        assert!(delivery_summary.has_blockers());
        assert!(delivery_summary.needs_verification());
        assert!(delivery_summary.requires_attention());
        assert_eq!(
            delivery_summary.next_delivery_status,
            Some(IntegrationActivationDeliveryStatus::Blocked)
        );
        assert_eq!(
            delivery_summary.next_owner_lane,
            Some(IntegrationActivationResponseOwnerLane::Platform)
        );
        assert_eq!(
            delivery_summary.overall_status,
            IntegrationActivationHealthStatus::Blocked
        );

        let deployment_records =
            activation_deployment_records_from_delivery_manifests(&delivery_manifests);
        assert_eq!(deployment_records.len(), delivery_manifests.len());
        assert!(deployment_records.iter().any(
            |record| record.deployment_status == IntegrationActivationDeploymentStatus::Blocked
        ));
        assert!(deployment_records
            .iter()
            .any(|record| record.deployment_status
                == IntegrationActivationDeploymentStatus::AwaitingVerification));
        assert!(deployment_records
            .iter()
            .any(IntegrationActivationDeploymentRecord::requires_attention));
        assert!(deployment_records
            .iter()
            .any(IntegrationActivationDeploymentRecord::deployment_blocked));

        let deployment_summary =
            IntegrationActivationDeploymentSummary::from_records(deployment_records.iter());
        assert_eq!(deployment_summary.total_records, deployment_records.len());
        assert!(deployment_summary.has_blockers());
        assert!(deployment_summary.needs_verification());
        assert!(deployment_summary.requires_attention());
        assert_eq!(
            deployment_summary.next_deployment_status,
            Some(IntegrationActivationDeploymentStatus::Blocked)
        );
        assert_eq!(
            deployment_summary.next_owner_lane,
            Some(IntegrationActivationResponseOwnerLane::Platform)
        );
        assert_eq!(
            deployment_summary.overall_status,
            IntegrationActivationHealthStatus::Blocked
        );

        let safety_gates = activation_safety_gates_from_deployment_records(&deployment_records);
        assert_eq!(safety_gates.len(), deployment_records.len());
        assert!(safety_gates
            .iter()
            .any(|gate| gate.gate_status == IntegrationActivationSafetyGateStatus::Blocked));
        assert!(safety_gates.iter().any(
            |gate| gate.gate_status == IntegrationActivationSafetyGateStatus::NeedsVerification
        ));
        assert!(safety_gates
            .iter()
            .any(IntegrationActivationSafetyGate::requires_attention));
        assert!(safety_gates
            .iter()
            .any(IntegrationActivationSafetyGate::blocks_activation));

        let safety_summary = IntegrationActivationSafetySummary::from_gates(safety_gates.iter());
        assert_eq!(safety_summary.total_gates, safety_gates.len());
        assert!(safety_summary.has_blockers());
        assert!(safety_summary.needs_verification());
        assert!(safety_summary.requires_attention());
        assert_eq!(
            safety_summary.next_gate_status,
            Some(IntegrationActivationSafetyGateStatus::Blocked)
        );
        assert_eq!(
            safety_summary.next_owner_lane,
            Some(IntegrationActivationResponseOwnerLane::Platform)
        );
        assert_eq!(
            safety_summary.overall_status,
            IntegrationActivationHealthStatus::Blocked
        );

        let rollback_plans = activation_rollback_plans_from_safety_gates(&safety_gates);
        assert_eq!(rollback_plans.len(), safety_gates.len());
        assert!(rollback_plans.iter().any(|plan| {
            plan.rollback_action == IntegrationActivationRollbackAction::HoldDeployment
        }));
        assert!(rollback_plans
            .iter()
            .any(IntegrationActivationRollbackPlan::requires_attention));
        assert!(rollback_plans
            .iter()
            .any(IntegrationActivationRollbackPlan::blocks_activation));

        let rollback_summary =
            IntegrationActivationRollbackSummary::from_plans(rollback_plans.iter());
        assert_eq!(rollback_summary.total_plans, rollback_plans.len());
        assert!(rollback_summary.has_blockers());
        assert!(rollback_summary.needs_verification());
        assert!(rollback_summary.requires_attention());
        assert_eq!(
            rollback_summary.next_rollback_action,
            Some(IntegrationActivationRollbackAction::HoldDeployment)
        );
        assert_eq!(
            rollback_summary.next_gate_status,
            Some(IntegrationActivationSafetyGateStatus::Blocked)
        );
        assert_eq!(
            rollback_summary.next_owner_lane,
            Some(IntegrationActivationResponseOwnerLane::Platform)
        );
        assert_eq!(
            rollback_summary.overall_status,
            IntegrationActivationHealthStatus::Blocked
        );

        let observability_probes =
            activation_observability_probes_from_rollups(&signals, &rollback_plans);
        assert_eq!(observability_probes.len(), rollback_plans.len());
        assert!(observability_probes.iter().any(|probe| {
            probe.observability_status == IntegrationActivationObservabilityStatus::Blocked
        }));
        assert!(observability_probes
            .iter()
            .any(IntegrationActivationObservabilityProbe::requires_attention));
        assert!(observability_probes
            .iter()
            .any(IntegrationActivationObservabilityProbe::blocks_activation));

        let observability_summary =
            IntegrationActivationObservabilitySummary::from_probes(observability_probes.iter());
        assert_eq!(
            observability_summary.total_probes,
            observability_probes.len()
        );
        assert!(observability_summary.has_blockers());
        assert!(observability_summary.requires_attention());
        assert_eq!(
            observability_summary.next_observability_status,
            Some(IntegrationActivationObservabilityStatus::Blocked)
        );
        assert_eq!(
            observability_summary.next_rollback_action,
            Some(IntegrationActivationRollbackAction::HoldDeployment)
        );
        assert_eq!(
            observability_summary.next_owner_lane,
            Some(IntegrationActivationResponseOwnerLane::Platform)
        );
        assert_eq!(
            observability_summary.overall_status,
            IntegrationActivationHealthStatus::Blocked
        );

        let incident_briefs =
            activation_incident_briefs_from_observability_probes(&observability_probes);
        assert_eq!(incident_briefs.len(), observability_probes.len());
        assert!(incident_briefs
            .iter()
            .any(|brief| { brief.severity == IntegrationActivationIncidentSeverity::Critical }));
        assert!(incident_briefs
            .iter()
            .any(IntegrationActivationIncidentBrief::requires_attention));
        assert!(incident_briefs
            .iter()
            .any(IntegrationActivationIncidentBrief::blocks_activation));

        let incident_summary =
            IntegrationActivationIncidentSummary::from_briefs(incident_briefs.iter());
        assert_eq!(incident_summary.total_briefs, incident_briefs.len());
        assert!(incident_summary.has_blockers());
        assert!(incident_summary.requires_attention());
        assert_eq!(
            incident_summary.next_severity,
            Some(IntegrationActivationIncidentSeverity::Critical)
        );
        assert_eq!(
            incident_summary.next_action,
            Some(IntegrationActivationIncidentAction::HoldActivation)
        );
        assert_eq!(
            incident_summary.next_observability_status,
            Some(IntegrationActivationObservabilityStatus::Blocked)
        );
        assert_eq!(
            incident_summary.overall_status,
            IntegrationActivationHealthStatus::Blocked
        );

        let catalog = first_party_catalog();
        let available_primitives = vec![
            PrimitiveFamily::NormalizedModel,
            PrimitiveFamily::DiscoveryIndex,
            PrimitiveFamily::CommandMapping,
            PrimitiveFamily::CapabilityPolicy,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let catalog_entries = activation_runbook_entries_at_or_before_priority(
            &catalog,
            2,
            &available_primitives,
            &allowed_capabilities,
            &[],
        );
        assert!(catalog_entries
            .iter()
            .any(IntegrationActivationRunbookEntry::requires_attention));
        let catalog_handoff_entries = activation_handoff_packages_at_or_before_priority(
            &catalog,
            2,
            &available_primitives,
            &allowed_capabilities,
            &[],
        );
        assert!(catalog_handoff_entries
            .iter()
            .any(IntegrationActivationHandoffPackage::requires_attention));
        let catalog_execution_packets = activation_execution_packets_at_or_before_priority(
            &catalog,
            2,
            &available_primitives,
            &allowed_capabilities,
            &[],
        );
        assert!(catalog_execution_packets
            .iter()
            .any(IntegrationActivationExecutionPacket::requires_attention));
        let catalog_verification_checkpoints =
            activation_verification_checkpoints_at_or_before_priority(
                &catalog,
                2,
                &available_primitives,
                &allowed_capabilities,
                &[],
            );
        assert!(catalog_verification_checkpoints
            .iter()
            .any(IntegrationActivationVerificationCheckpoint::requires_attention));
        let catalog_escalation_cases = activation_escalation_cases_at_or_before_priority(
            &catalog,
            2,
            &available_primitives,
            &allowed_capabilities,
            &[],
        );
        assert!(catalog_escalation_cases
            .iter()
            .any(IntegrationActivationEscalationCase::requires_attention));
        let catalog_response_items = activation_response_items_at_or_before_priority(
            &catalog,
            2,
            &available_primitives,
            &allowed_capabilities,
            &[],
        );
        assert!(catalog_response_items
            .iter()
            .any(IntegrationActivationResponseItem::requires_attention));
        let catalog_remediation_items = activation_remediation_items_at_or_before_priority(
            &catalog,
            2,
            &available_primitives,
            &allowed_capabilities,
            &[],
        );
        assert!(catalog_remediation_items
            .iter()
            .any(IntegrationActivationRemediationItem::requires_attention));
        let catalog_observability_probes = activation_observability_probes_at_or_before_priority(
            &catalog,
            2,
            &available_primitives,
            &allowed_capabilities,
            &[],
        );
        assert!(catalog_observability_probes
            .iter()
            .any(IntegrationActivationObservabilityProbe::requires_attention));
        assert!(catalog_observability_probes
            .iter()
            .any(IntegrationActivationObservabilityProbe::blocks_activation));
        let catalog_incident_briefs = activation_incident_briefs_at_or_before_priority(
            &catalog,
            2,
            &available_primitives,
            &allowed_capabilities,
            &[],
        );
        assert!(catalog_incident_briefs
            .iter()
            .any(IntegrationActivationIncidentBrief::requires_attention));
        assert!(catalog_incident_briefs
            .iter()
            .any(IntegrationActivationIncidentBrief::blocks_activation));
        let catalog_guardrail_checks = activation_guardrail_checks_at_or_before_priority(
            &catalog,
            2,
            &available_primitives,
            &allowed_capabilities,
            &[],
        );
        assert!(catalog_guardrail_checks
            .iter()
            .any(|check| check.kind == IntegrationActivationGuardrailKind::Incident));
        assert!(catalog_guardrail_checks
            .iter()
            .any(|check| check.kind == IntegrationActivationGuardrailKind::PolicyRisk));
        assert!(catalog_guardrail_checks
            .iter()
            .any(|check| check.kind == IntegrationActivationGuardrailKind::Dependency));
        assert!(catalog_guardrail_checks
            .iter()
            .any(|check| check.kind == IntegrationActivationGuardrailKind::ReadinessGap));
        assert!(catalog_guardrail_checks
            .iter()
            .any(IntegrationActivationGuardrailCheck::requires_attention));
        assert!(catalog_guardrail_checks
            .iter()
            .any(IntegrationActivationGuardrailCheck::blocks_activation));
        let catalog_guardrail_summary =
            IntegrationActivationGuardrailSummary::from_checks(catalog_guardrail_checks.iter());
        assert_eq!(
            catalog_guardrail_summary.total_checks,
            catalog_guardrail_checks.len()
        );
        assert!(catalog_guardrail_summary.has_blockers());
        assert!(catalog_guardrail_summary.requires_attention());
        assert!(catalog_guardrail_summary.incident_checks >= 1);
        assert!(catalog_guardrail_summary.policy_risk_checks >= 1);
        assert!(catalog_guardrail_summary.dependency_checks >= 1);
        assert!(catalog_guardrail_summary.readiness_gap_checks >= 1);
        assert_eq!(
            catalog_guardrail_summary.overall_status,
            IntegrationActivationHealthStatus::Blocked
        );
    }

    #[test]
    fn activation_actions_explain_ready_review_and_blocker_work() {
        let ready_report = IntegrationReadinessReport {
            requested_integration_id: IntegrationId::trusted("read_only_probe"),
            display_name: "Read-only Probe".to_string(),
            activation_target: IntegrationActivationTarget::Direct,
            priority: 2,
            missing_primitives: Vec::new(),
            missing_capabilities: Vec::new(),
            missing_dependencies: Vec::new(),
            requires_human_review: false,
            highest_policy_tier: PrivilegeTier::ReadOnly,
            local_only: true,
            cloud_required: false,
        };
        let review_report = IntegrationReadinessReport {
            requested_integration_id: IntegrationId::trusted("review_bridge"),
            display_name: "Review Bridge".to_string(),
            activation_target: IntegrationActivationTarget::Direct,
            priority: 1,
            missing_primitives: Vec::new(),
            missing_capabilities: Vec::new(),
            missing_dependencies: Vec::new(),
            requires_human_review: true,
            highest_policy_tier: PrivilegeTier::HumanApproval,
            local_only: true,
            cloud_required: false,
        };
        let blocked_report = IntegrationReadinessReport {
            requested_integration_id: IntegrationId::trusted("blocked_sensor"),
            display_name: "Blocked Sensor".to_string(),
            activation_target: IntegrationActivationTarget::DelegatedIntegration(
                IntegrationId::trusted("mqtt"),
            ),
            priority: 1,
            missing_primitives: vec![PrimitiveFamily::Mqtt],
            missing_capabilities: vec![CapabilityId::trusted("smart_home.command.low_risk")],
            missing_dependencies: vec![IntegrationId::trusted("mqtt")],
            requires_human_review: false,
            highest_policy_tier: PrivilegeTier::LowRisk,
            local_only: true,
            cloud_required: false,
        };
        let candidates = activation_candidates_from_reports(
            [ready_report, review_report, blocked_report].iter(),
        );

        let actions = activation_actions_from_candidates(candidates.iter());

        assert_eq!(actions.len(), 5);
        assert_eq!(
            actions[0].kind,
            IntegrationActivationActionKind::ReviewPolicy
        );
        assert_eq!(
            actions[1].kind,
            IntegrationActivationActionKind::ProvidePrimitive
        );
        assert_eq!(
            actions[2].kind,
            IntegrationActivationActionKind::GrantCapability
        );
        assert_eq!(
            actions[3].kind,
            IntegrationActivationActionKind::EnableDependency
        );
        assert_eq!(
            actions[4].kind,
            IntegrationActivationActionKind::ActivateIntegration
        );
        assert!(actions[0].blocks_activation());
        assert!(actions[4].is_activation());

        let summary = IntegrationActivationActionSummary::from_actions(actions.iter());
        assert_eq!(summary.total_actions, 5);
        assert_eq!(summary.activate_integration_actions, 1);
        assert_eq!(summary.review_policy_actions, 1);
        assert_eq!(summary.provide_primitive_actions, 1);
        assert_eq!(summary.grant_capability_actions, 1);
        assert_eq!(summary.enable_dependency_actions, 1);
        assert_eq!(summary.unique_integrations, 3);
        assert_eq!(summary.actionable_integration_count, 1);
        assert_eq!(summary.blocked_integration_count, 2);
        assert_eq!(summary.first_action_priority, Some(1));
        assert_eq!(summary.first_activation_priority, Some(2));
        assert_eq!(summary.first_blocker_priority, Some(1));
        assert!(summary.has_activation_work());
        assert!(summary.has_blockers());
        assert!(summary.has_review_work());
        assert!(!summary.is_empty());
    }

    #[test]
    fn activation_agenda_groups_actions_by_rollout_wave() {
        let ready_report = IntegrationReadinessReport {
            requested_integration_id: IntegrationId::trusted("read_only_probe"),
            display_name: "Read-only Probe".to_string(),
            activation_target: IntegrationActivationTarget::Direct,
            priority: 2,
            missing_primitives: Vec::new(),
            missing_capabilities: Vec::new(),
            missing_dependencies: Vec::new(),
            requires_human_review: false,
            highest_policy_tier: PrivilegeTier::ReadOnly,
            local_only: true,
            cloud_required: false,
        };
        let review_report = IntegrationReadinessReport {
            requested_integration_id: IntegrationId::trusted("review_bridge"),
            display_name: "Review Bridge".to_string(),
            activation_target: IntegrationActivationTarget::Direct,
            priority: 1,
            missing_primitives: Vec::new(),
            missing_capabilities: Vec::new(),
            missing_dependencies: Vec::new(),
            requires_human_review: true,
            highest_policy_tier: PrivilegeTier::HumanApproval,
            local_only: true,
            cloud_required: false,
        };
        let blocked_report = IntegrationReadinessReport {
            requested_integration_id: IntegrationId::trusted("blocked_sensor"),
            display_name: "Blocked Sensor".to_string(),
            activation_target: IntegrationActivationTarget::DelegatedIntegration(
                IntegrationId::trusted("mqtt"),
            ),
            priority: 1,
            missing_primitives: vec![PrimitiveFamily::Mqtt],
            missing_capabilities: vec![CapabilityId::trusted("smart_home.command.low_risk")],
            missing_dependencies: vec![IntegrationId::trusted("mqtt")],
            requires_human_review: false,
            highest_policy_tier: PrivilegeTier::LowRisk,
            local_only: true,
            cloud_required: false,
        };
        let candidates = activation_candidates_from_reports(
            [ready_report, review_report, blocked_report].iter(),
        );

        let agenda = activation_agenda_from_candidates(candidates);

        assert_eq!(agenda.len(), 2);
        assert_eq!(agenda[0].priority, 1);
        assert_eq!(agenda[0].candidate_summary.total_candidates, 2);
        assert_eq!(agenda[0].action_summary.total_actions, 4);
        assert_eq!(agenda[0].action_summary.review_policy_actions, 1);
        assert_eq!(agenda[0].action_summary.provide_primitive_actions, 1);
        assert!(agenda[0].has_blockers());
        assert!(agenda[0].has_review_work());
        assert!(!agenda[0].has_activation_work());
        assert_eq!(agenda[1].priority, 2);
        assert_eq!(agenda[1].candidate_summary.ready_to_activate_candidates, 1);
        assert_eq!(agenda[1].action_summary.activate_integration_actions, 1);
        assert!(agenda[1].has_activation_work());

        let summary = IntegrationActivationAgendaSummary::from_stages(agenda.iter());
        assert_eq!(summary.total_stages, 2);
        assert_eq!(summary.total_candidates, 3);
        assert_eq!(summary.total_actions, 5);
        assert_eq!(summary.stages_with_activation_work, 1);
        assert_eq!(summary.stages_with_blockers, 1);
        assert_eq!(summary.stages_with_review_work, 1);
        assert_eq!(summary.first_action_priority, Some(1));
        assert_eq!(summary.first_activation_priority, Some(2));
        assert_eq!(summary.first_blocker_priority, Some(1));
        assert_eq!(summary.candidate_summary.total_candidates, 3);
        assert_eq!(summary.action_summary.total_actions, 5);
        assert!(summary.has_activation_work());
        assert!(summary.has_blockers());
        assert!(summary.has_review_work());
        assert!(!summary.is_empty());
    }

    #[test]
    fn readiness_reports_include_delegated_integration_dependencies() {
        let catalog = first_party_catalog();
        let all_primitives = all_primitive_families().to_vec();
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let blocked = readiness_report_for_integration(
            &catalog,
            &IntegrationId::trusted("tplink_tapo"),
            &all_primitives,
            &allowed_capabilities,
            &[],
        )
        .unwrap();

        assert!(blocked.is_blocked());
        assert!(blocked.missing_dependency(&IntegrationId::trusted("tplink")));
        assert!(blocked.delegates_to_integration(&IntegrationId::trusted("tplink")));

        let ready = readiness_report_for_integration(
            &catalog,
            &IntegrationId::trusted("tplink_tapo"),
            &all_primitives,
            &allowed_capabilities,
            &[IntegrationId::trusted("tplink")],
        )
        .unwrap();

        assert!(ready.activation_ready());
        assert!(ready.missing_dependencies.is_empty());
    }

    #[test]
    fn priority_readiness_reports_track_rollout_wave_blockers() {
        let catalog = first_party_catalog();
        let available_primitives = vec![
            PrimitiveFamily::NormalizedModel,
            PrimitiveFamily::DiscoveryIndex,
            PrimitiveFamily::CommandMapping,
            PrimitiveFamily::CapabilityPolicy,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let reports = readiness_reports_at_or_before_priority(
            &catalog,
            1,
            &available_primitives,
            &allowed_capabilities,
            &[],
        );
        let hue = reports
            .iter()
            .find(|report| report.requested_integration_id == IntegrationId::trusted("hue"))
            .unwrap();
        let tasmota = reports
            .iter()
            .find(|report| report.requested_integration_id == IntegrationId::trusted("tasmota"))
            .unwrap();

        assert!(hue.missing_primitive(PrimitiveFamily::LocalPairing));
        assert!(hue.missing_capability(&CapabilityId::trusted("smart_home.command.light")));
        assert!(tasmota.missing_primitive(PrimitiveFamily::Mqtt));
        assert!(tasmota.missing_dependency(&IntegrationId::trusted("mqtt")));

        let summary = IntegrationReadinessSummary::from_reports(reports.iter());
        assert_eq!(summary.total_reports, reports.len());
        assert!(summary.has_blockers());
        assert!(summary.blocked_reports > 0);
        assert!(summary.reports_missing_primitives > 0);
        assert!(summary.reports_missing_capabilities > 0);
        assert!(summary.reports_missing_dependencies > 0);
        assert!(summary.unique_missing_primitives > 0);
        assert!(summary.unique_missing_capabilities > 0);
        assert!(summary.unique_missing_dependencies > 0);
        assert!(!summary.all_ready());

        let gaps = readiness_gap_inventory_from_reports(reports.iter());
        assert_eq!(gaps.total_reports, reports.len());
        assert_eq!(gaps.blocked_reports, summary.blocked_reports);
        assert!(gaps.has_gaps());
        assert!(gaps.primitive_gap_count() > 0);
        assert!(gaps.capability_gap_count() > 0);
        assert!(gaps.dependency_gap_count() > 0);
        assert!(gaps.primitive_gaps.first().unwrap().highest_priority <= 1);
        assert!(gaps
            .primitive_gaps
            .iter()
            .any(|gap| gap.primitive == PrimitiveFamily::LocalPairing
                && gap.integration_ids.contains(&IntegrationId::trusted("hue"))));
        assert!(gaps
            .capability_gaps
            .iter()
            .any(|gap| gap.capability_id == CapabilityId::trusted("smart_home.command.light")));
        assert!(gaps.dependency_gaps.iter().any(|gap| gap.integration_id
            == IntegrationId::trusted("mqtt")
            && gap
                .requested_integration_ids
                .contains(&IntegrationId::trusted("tasmota"))));
    }

    #[test]
    fn activation_dependency_graph_tracks_satisfied_and_blocking_edges() {
        let catalog = first_party_catalog();
        let available_primitives = all_primitive_families().to_vec();
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
        ];
        let graph = activation_dependency_graph_at_or_before_priority(
            &catalog,
            2,
            &available_primitives,
            &allowed_capabilities,
            &[IntegrationId::trusted("mqtt")],
        );

        assert!(!graph.is_empty());
        assert!(graph.summary.has_dependency_edges());
        assert!(graph.summary.satisfied_edges > 0);
        assert!(graph.summary.blocking_edges > 0);
        assert!(graph.summary.nodes_with_dependencies > 0);
        assert!(graph.summary.nodes_with_dependents > 0);
        assert!(graph.summary.nodes_with_missing_dependencies > 0);

        let tasmota = graph
            .nodes
            .iter()
            .find(|node| node.integration_id == IntegrationId::trusted("tasmota"))
            .unwrap();
        assert!(tasmota
            .depends_on_integrations
            .contains(&IntegrationId::trusted("mqtt")));
        assert!(tasmota.missing_dependencies.is_empty());

        let tapo = graph
            .nodes
            .iter()
            .find(|node| node.integration_id == IntegrationId::trusted("tplink_tapo"))
            .unwrap();
        assert!(tapo
            .missing_dependencies
            .contains(&IntegrationId::trusted("tplink")));

        let satisfied = graph
            .edges
            .iter()
            .find(|edge| {
                edge.dependent_integration_id == IntegrationId::trusted("tasmota")
                    && edge.dependency_integration_id == IntegrationId::trusted("mqtt")
            })
            .unwrap();
        assert!(satisfied.satisfied);
        assert!(!satisfied.blocks_activation);

        let blocked = graph
            .edges
            .iter()
            .find(|edge| {
                edge.dependent_integration_id == IntegrationId::trusted("tplink_tapo")
                    && edge.dependency_integration_id == IntegrationId::trusted("tplink")
            })
            .unwrap();
        assert!(!blocked.satisfied);
        assert!(blocked.blocks_activation);
        assert_eq!(
            blocked.dependency_display_name.as_deref(),
            Some("TP-Link Smart Home")
        );
    }

    #[test]
    fn local_push_entries_can_be_filtered_for_supervision_shape() {
        let catalog = first_party_catalog();
        let local_push = entries_by_connectivity(&catalog, ConnectivityClass::LocalPush);

        assert!(local_push
            .iter()
            .any(|entry| entry.integration_id == IntegrationId::trusted("hue")));
        assert!(local_push
            .iter()
            .all(|entry| entry.connectivity == ConnectivityClass::LocalPush));
    }

    #[test]
    fn hue_records_the_trial_run_primitive_shape() {
        let catalog = first_party_catalog();
        let hue = find_entry(&catalog, &IntegrationId::trusted("hue")).unwrap();

        assert!(hue.requires_primitive(PrimitiveFamily::Mdns));
        assert!(hue.requires_primitive(PrimitiveFamily::LocalHttp));
        assert!(hue.requires_primitive(PrimitiveFamily::ServerSentEvents));
        assert!(hue.requires_primitive(PrimitiveFamily::LocalPairing));
        assert!(hue.requires_primitive(PrimitiveFamily::Supervision));
    }

    #[test]
    fn catalog_entry_summary_compacts_hue_package_shape() {
        let catalog = first_party_catalog();
        let hue = find_entry(&catalog, &IntegrationId::trusted("hue")).unwrap();
        let summary = hue_catalog_entry_summary();

        assert_eq!(summary, hue.summary());
        assert_eq!(summary.integration_id, IntegrationId::trusted("hue"));
        assert_eq!(summary.category, IntegrationCategory::LocalHub);
        assert_eq!(summary.connectivity, ConnectivityClass::LocalPush);
        assert_eq!(
            summary.implementation_status,
            ImplementationStatus::Scaffolded
        );
        assert_eq!(summary.discovery_mechanism_count, 2);
        assert_eq!(summary.auth_mode_count, 2);
        assert_eq!(summary.required_capability_count, 3);
        assert_eq!(summary.supported_protocol_count, 2);
        assert_eq!(summary.dependency_count, 0);
        assert!(summary.local_only);
        assert!(!summary.cloud_required);
        assert!(!summary.virtual_alias);
        assert!(!summary.has_dependencies);
        assert!(summary.has_catalog_metadata());
        assert!(summary.policy_surface_count >= 2);
        assert_eq!(summary.highest_policy_tier, PrivilegeTier::HumanApproval);
        assert!(summary.requires_human_review);
    }

    #[test]
    fn hue_activation_package_summary_joins_catalog_and_plan_shape() {
        let catalog = first_party_catalog();
        let hue = find_entry(&catalog, &IntegrationId::trusted("hue")).unwrap();
        let plan = activation_plan_for_entry(hue);
        let summary = hue_activation_package_summary();

        assert_eq!(
            summary,
            IntegrationActivationPackageSummary::from_plan(hue.summary(), &plan)
        );
        assert_eq!(
            summary.catalog_entry.integration_id,
            IntegrationId::trusted("hue")
        );
        assert_eq!(
            summary.activation_target,
            IntegrationActivationTarget::Direct
        );
        assert!(summary.direct_activation);
        assert!(!summary.delegated_activation);
        assert_eq!(
            summary.required_primitive_count,
            hue.required_primitives.len()
        );
        assert_eq!(
            summary.required_capability_count,
            hue.required_capabilities.len()
        );
        assert_eq!(summary.auth_mode_count, 2);
        assert_eq!(summary.discovery_mechanism_count, 2);
        assert_eq!(summary.dependency_count, 0);
        assert!(summary.policy_surface_count >= 2);
        assert_eq!(summary.highest_policy_tier, PrivilegeTier::HumanApproval);
        assert!(summary.local_only);
        assert!(!summary.cloud_required);
        assert!(summary.requires_human_review);
        assert!(summary.has_prerequisites());
        assert!(summary.has_policy_review());
    }

    #[test]
    fn hue_readiness_package_summary_tracks_missing_rollout_inputs() {
        let catalog = first_party_catalog();
        let hue = find_entry(&catalog, &IntegrationId::trusted("hue")).unwrap();
        let available_primitives = vec![
            PrimitiveFamily::Mdns,
            PrimitiveFamily::CommandMapping,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let report = readiness_report_for_integration(
            &catalog,
            &IntegrationId::trusted("hue"),
            &available_primitives,
            &allowed_capabilities,
            &[],
        )
        .unwrap();
        let summary =
            hue_readiness_package_summary(&available_primitives, &allowed_capabilities, &[]);

        assert_eq!(
            summary,
            IntegrationReadinessPackageSummary::from_package_and_report(
                IntegrationActivationPackageSummary::from_entry(hue),
                &report,
            )
        );
        assert_eq!(
            summary.activation_package.catalog_entry.integration_id,
            IntegrationId::trusted("hue")
        );
        assert!(!summary.activation_ready);
        assert!(summary.blocked);
        assert_eq!(summary.activation_package.required_primitive_count, 12);
        assert_eq!(summary.missing_primitive_count, 9);
        assert_eq!(summary.missing_capability_count, 2);
        assert_eq!(summary.missing_dependency_count, 0);
        assert_eq!(summary.missing_prerequisite_count, 11);
        assert!(summary.has_missing_primitives());
        assert!(summary.has_missing_capabilities());
        assert!(!summary.has_missing_dependencies());
        assert!(summary.has_policy_review());
        assert_eq!(summary.highest_policy_tier, PrivilegeTier::HumanApproval);
        assert!(summary.local_only);
        assert!(!summary.cloud_required);
    }

    #[test]
    fn hue_readiness_package_summary_marks_ready_when_rollout_inputs_exist() {
        let available_primitives = all_primitive_families().to_vec();
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.command.lock"),
            CapabilityId::trusted("smart_home.manage_network"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let summary =
            hue_readiness_package_summary(&available_primitives, &allowed_capabilities, &[]);

        assert!(summary.activation_ready);
        assert!(!summary.blocked);
        assert_eq!(summary.missing_primitive_count, 0);
        assert_eq!(summary.missing_capability_count, 0);
        assert_eq!(summary.missing_dependency_count, 0);
        assert_eq!(summary.missing_prerequisite_count, 0);
        assert!(!summary.has_missing_primitives());
        assert!(!summary.has_missing_capabilities());
        assert!(!summary.has_missing_dependencies());
        assert!(summary.requires_human_review);
        assert!(summary.has_policy_review());
        assert_eq!(summary.highest_policy_tier, PrivilegeTier::HumanApproval);
        assert!(summary.local_only);
        assert!(!summary.cloud_required);
    }

    #[test]
    fn hue_activation_evidence_briefing_summary_routes_missing_rollout_inputs() {
        let catalog = first_party_catalog();
        let hue = find_entry(&catalog, &IntegrationId::trusted("hue")).unwrap();
        let available_primitives = vec![
            PrimitiveFamily::Mdns,
            PrimitiveFamily::CommandMapping,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let summary = hue_activation_evidence_briefing_summary(
            &available_primitives,
            &allowed_capabilities,
            &[],
        );

        assert_eq!(
            summary.activation_package,
            IntegrationActivationPackageSummary::from_entry(hue)
        );
        assert_eq!(
            summary.readiness_package,
            hue_readiness_package_summary(&available_primitives, &allowed_capabilities, &[])
        );
        assert_eq!(summary.required_evidence_lane_count, 5);
        assert_eq!(summary.passed_evidence_lane_count, 4);
        assert_eq!(summary.blocked_evidence_lane_count, 1);
        assert_eq!(summary.missing_prerequisite_count, 11);
        assert_eq!(summary.missing_primitive_count, 9);
        assert_eq!(summary.missing_capability_count, 2);
        assert_eq!(summary.missing_dependency_count, 0);
        assert!(summary.catalog_evidence_ready);
        assert!(summary.activation_plan_evidence_ready);
        assert!(!summary.readiness_evidence_ready);
        assert!(summary.policy_evidence_ready);
        assert!(summary.local_boundary_evidence_ready);
        assert!(!summary.evidence_briefing_ready);
        assert!(!summary.is_evidence_briefing_ready());
        assert!(summary.has_blocked_evidence_lanes());
        assert!(!summary.needs_catalog_evidence());
        assert!(!summary.needs_activation_plan_evidence());
        assert!(summary.needs_readiness_evidence());
        assert!(!summary.needs_policy_evidence());
        assert!(!summary.needs_local_boundary_evidence());
    }

    #[test]
    fn hue_activation_evidence_briefing_summary_marks_ready_rollout_evidence() {
        let available_primitives = all_primitive_families().to_vec();
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.command.lock"),
            CapabilityId::trusted("smart_home.manage_network"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let summary = hue_activation_evidence_briefing_summary(
            &available_primitives,
            &allowed_capabilities,
            &[],
        );

        assert_eq!(summary.required_evidence_lane_count, 5);
        assert_eq!(summary.passed_evidence_lane_count, 5);
        assert_eq!(summary.blocked_evidence_lane_count, 0);
        assert_eq!(summary.missing_prerequisite_count, 0);
        assert_eq!(summary.missing_primitive_count, 0);
        assert_eq!(summary.missing_capability_count, 0);
        assert_eq!(summary.missing_dependency_count, 0);
        assert!(summary.catalog_evidence_ready);
        assert!(summary.activation_plan_evidence_ready);
        assert!(summary.readiness_evidence_ready);
        assert!(summary.policy_evidence_ready);
        assert!(summary.local_boundary_evidence_ready);
        assert!(summary.evidence_briefing_ready);
        assert!(summary.is_evidence_briefing_ready());
        assert!(!summary.has_blocked_evidence_lanes());
        assert!(!summary.needs_catalog_evidence());
        assert!(!summary.needs_activation_plan_evidence());
        assert!(!summary.needs_readiness_evidence());
        assert!(!summary.needs_policy_evidence());
        assert!(!summary.needs_local_boundary_evidence());
    }

    #[test]
    fn activation_evidence_scorecard_rolls_up_priority_wave_briefings() {
        let catalog = first_party_catalog();
        let available_primitives = vec![
            PrimitiveFamily::NormalizedModel,
            PrimitiveFamily::DiscoveryIndex,
            PrimitiveFamily::CommandMapping,
            PrimitiveFamily::CapabilityPolicy,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let briefings = activation_evidence_briefings_at_or_before_priority(
            &catalog,
            1,
            &available_primitives,
            &allowed_capabilities,
            &[],
        );
        let summary =
            IntegrationActivationEvidenceScorecardSummary::from_briefings(briefings.iter());
        let helper_summary = activation_evidence_scorecard_summary_at_or_before_priority(
            &catalog,
            1,
            &available_primitives,
            &allowed_capabilities,
            &[],
        );

        assert_eq!(summary, helper_summary);
        assert_eq!(summary.total_integrations, briefings.len());
        assert_eq!(
            summary.total_integrations,
            entries_at_or_before_priority(&catalog, 1).len()
        );
        assert_eq!(
            summary.total_required_evidence_lanes,
            briefings
                .iter()
                .map(|briefing| briefing.required_evidence_lane_count)
                .sum::<usize>()
        );
        assert_eq!(
            summary.total_passed_evidence_lanes + summary.total_blocked_evidence_lanes,
            summary.total_required_evidence_lanes
        );
        assert_eq!(
            summary.blocked_integrations,
            briefings
                .iter()
                .filter(|briefing| briefing.has_blocked_evidence_lanes())
                .count()
        );
        assert_eq!(
            summary.readiness_evidence_blockers,
            briefings
                .iter()
                .filter(|briefing| briefing.needs_readiness_evidence())
                .count()
        );
        assert_eq!(
            summary.missing_prerequisite_count,
            briefings
                .iter()
                .map(|briefing| briefing.missing_prerequisite_count)
                .sum::<usize>()
        );
        assert!(summary.has_blockers());
        assert!(summary.has_missing_prerequisites());
        assert!(summary.has_readiness_gaps());
        assert!(summary.first_blocked_priority.unwrap() <= 1);
        assert_eq!(summary.first_ready_priority, None);
        assert!(!summary.all_ready());
        assert!(!summary.is_empty());
    }

    #[test]
    fn activation_evidence_scorecard_marks_ready_catalog_context() {
        let catalog = vec![hue_entry()];
        let available_primitives = all_primitive_families().to_vec();
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let summary = activation_evidence_scorecard_summary(
            &catalog,
            &available_primitives,
            &allowed_capabilities,
            &[],
        );

        assert_eq!(summary.total_integrations, 1);
        assert_eq!(summary.evidence_ready_integrations, 1);
        assert_eq!(summary.blocked_integrations, 0);
        assert_eq!(summary.total_required_evidence_lanes, 5);
        assert_eq!(summary.total_passed_evidence_lanes, 5);
        assert_eq!(summary.total_blocked_evidence_lanes, 0);
        assert_eq!(summary.catalog_evidence_ready_integrations, 1);
        assert_eq!(summary.activation_plan_evidence_ready_integrations, 1);
        assert_eq!(summary.readiness_evidence_ready_integrations, 1);
        assert_eq!(summary.policy_evidence_ready_integrations, 1);
        assert_eq!(summary.local_boundary_evidence_ready_integrations, 1);
        assert_eq!(summary.missing_prerequisite_count, 0);
        assert_eq!(summary.missing_primitive_count, 0);
        assert_eq!(summary.missing_capability_count, 0);
        assert_eq!(summary.missing_dependency_count, 0);
        assert_eq!(summary.local_only_integrations, 1);
        assert_eq!(summary.cloud_required_integrations, 0);
        assert_eq!(summary.human_review_integrations, 1);
        assert_eq!(summary.first_blocked_priority, None);
        assert_eq!(summary.first_ready_priority, Some(0));
        assert_eq!(summary.highest_policy_tier, PrivilegeTier::HumanApproval);
        assert!(summary.all_ready());
        assert!(!summary.has_blockers());
        assert!(!summary.has_missing_prerequisites());
        assert!(!summary.has_readiness_gaps());
    }

    #[test]
    fn activation_evidence_rows_rank_priority_wave_blockers() {
        let catalog = first_party_catalog();
        let available_primitives = vec![
            PrimitiveFamily::NormalizedModel,
            PrimitiveFamily::DiscoveryIndex,
            PrimitiveFamily::CommandMapping,
            PrimitiveFamily::CapabilityPolicy,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let rows = activation_evidence_rows_at_or_before_priority(
            &catalog,
            1,
            &available_primitives,
            &allowed_capabilities,
            &[],
        );
        let briefings = activation_evidence_briefings_at_or_before_priority(
            &catalog,
            1,
            &available_primitives,
            &allowed_capabilities,
            &[],
        );

        assert_eq!(rows.len(), briefings.len());
        assert!(rows.windows(2).all(|window| {
            window[0].priority < window[1].priority
                || (window[0].priority == window[1].priority
                    && window[0].blocked_evidence_lane_count
                        >= window[1].blocked_evidence_lane_count)
        }));
        assert!(rows
            .iter()
            .any(IntegrationActivationEvidenceRow::has_blockers));
        assert!(rows
            .iter()
            .any(|row| row.blocks_on(IntegrationActivationEvidenceLane::Readiness)));
        assert!(rows.iter().any(|row| row.missing_prerequisite_count > 0));

        let hue = rows
            .iter()
            .find(|row| row.integration_id == IntegrationId::trusted("hue"))
            .unwrap();
        assert_eq!(hue.priority, 0);
        assert_eq!(
            hue.next_blocked_lane,
            Some(IntegrationActivationEvidenceLane::Readiness)
        );
        assert_eq!(hue.next_blocked_lane.unwrap().as_str(), "readiness");
        assert!(!hue.is_ready());
        assert!(hue.local_only);
        assert!(!hue.cloud_required);
        assert!(hue.requires_human_review);
    }

    #[test]
    fn activation_evidence_rows_mark_ready_catalog_context() {
        let catalog = vec![hue_entry()];
        let available_primitives = all_primitive_families().to_vec();
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let rows = activation_evidence_rows_for_catalog(
            &catalog,
            &available_primitives,
            &allowed_capabilities,
            &[],
        );

        assert_eq!(rows.len(), 1);
        let hue = rows.first().unwrap();
        assert_eq!(hue.integration_id, IntegrationId::trusted("hue"));
        assert!(hue.is_ready());
        assert!(!hue.has_blockers());
        assert_eq!(hue.next_blocked_lane, None);
        assert_eq!(hue.required_evidence_lane_count, 5);
        assert_eq!(hue.passed_evidence_lane_count, 5);
        assert_eq!(hue.blocked_evidence_lane_count, 0);
        assert_eq!(hue.missing_prerequisite_count, 0);
        assert_eq!(hue.missing_primitive_count, 0);
        assert_eq!(hue.missing_capability_count, 0);
        assert_eq!(hue.missing_dependency_count, 0);
        assert!(hue.local_only);
        assert!(!hue.cloud_required);
        assert!(hue.requires_human_review);
        assert_eq!(hue.highest_policy_tier, PrivilegeTier::HumanApproval);
    }

    #[test]
    fn activation_evidence_lane_inventory_groups_priority_wave_blockers() {
        let catalog = first_party_catalog();
        let available_primitives = vec![
            PrimitiveFamily::NormalizedModel,
            PrimitiveFamily::DiscoveryIndex,
            PrimitiveFamily::CommandMapping,
            PrimitiveFamily::CapabilityPolicy,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let rows = activation_evidence_rows_at_or_before_priority(
            &catalog,
            1,
            &available_primitives,
            &allowed_capabilities,
            &[],
        );
        let inventory = activation_evidence_lane_inventory_from_rows(rows.iter());
        let summary =
            IntegrationActivationEvidenceLaneInventorySummary::from_items(inventory.iter());
        let blocked_row_count = rows.iter().filter(|row| row.has_blockers()).count();

        assert!(!inventory.is_empty());
        assert!(inventory.windows(2).all(|window| {
            window[0].first_blocked_priority < window[1].first_blocked_priority
                || (window[0].first_blocked_priority == window[1].first_blocked_priority
                    && window[0].blocked_integration_count >= window[1].blocked_integration_count)
        }));
        assert!(inventory
            .iter()
            .all(IntegrationActivationEvidenceLaneInventoryItem::has_blockers));

        let readiness = inventory
            .iter()
            .find(|item| item.lane == IntegrationActivationEvidenceLane::Readiness)
            .unwrap();
        assert!(readiness.includes_integration(&IntegrationId::trusted("hue")));
        assert!(readiness.requires_human_review());
        assert!(readiness.missing_prerequisite_count > 0);
        assert!(readiness.missing_capability_count > 0);
        assert_eq!(readiness.first_blocked_priority, 0);
        assert_eq!(readiness.highest_policy_tier, PrivilegeTier::HighRisk);

        assert_eq!(summary.total_lanes, inventory.len());
        assert_eq!(summary.total_blocked_integrations, blocked_row_count);
        assert_eq!(
            summary.total_blocked_integrations,
            inventory
                .iter()
                .map(|item| item.blocked_integration_count)
                .sum::<usize>()
        );
        assert!(summary.has_blockers());
        assert!(summary.has_readiness_lane());
        assert_eq!(
            summary.readiness_lane_blocked_integrations,
            readiness.blocked_integration_count
        );
        assert_eq!(summary.first_blocked_priority, Some(0));
        assert_eq!(summary.highest_policy_tier, PrivilegeTier::HighRisk);
    }

    #[test]
    fn activation_evidence_lane_inventory_marks_ready_catalog_context() {
        let catalog = vec![hue_entry()];
        let available_primitives = all_primitive_families().to_vec();
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let inventory = activation_evidence_lane_inventory_for_catalog(
            &catalog,
            &available_primitives,
            &allowed_capabilities,
            &[],
        );
        let summary =
            IntegrationActivationEvidenceLaneInventorySummary::from_items(inventory.iter());

        assert!(inventory.is_empty());
        assert!(summary.is_empty());
        assert!(!summary.has_blockers());
        assert!(!summary.has_readiness_lane());
        assert_eq!(summary.total_blocked_integrations, 0);
        assert_eq!(summary.first_blocked_priority, None);
        assert_eq!(summary.highest_policy_tier, PrivilegeTier::ReadOnly);
    }

    #[test]
    fn activation_evidence_remediation_items_plan_priority_wave_work() {
        let catalog = first_party_catalog();
        let available_primitives = vec![
            PrimitiveFamily::NormalizedModel,
            PrimitiveFamily::DiscoveryIndex,
            PrimitiveFamily::CommandMapping,
            PrimitiveFamily::CapabilityPolicy,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let rows = activation_evidence_rows_at_or_before_priority(
            &catalog,
            1,
            &available_primitives,
            &allowed_capabilities,
            &[],
        );
        let inventory = activation_evidence_lane_inventory_from_rows(rows.iter());
        let from_inventory = activation_evidence_remediation_items_from_inventory(inventory.iter());
        let from_rows = activation_evidence_remediation_items_from_rows(rows.iter());
        let summary = IntegrationActivationEvidenceRemediationSummary::from_items(from_rows.iter());

        assert_eq!(from_rows, from_inventory);
        assert!(!from_rows.is_empty());
        assert!(from_rows
            .iter()
            .enumerate()
            .all(|(index, item)| { item.sequence == index + 1 && item.has_blockers() }));
        assert!(from_rows.windows(2).all(|window| {
            window[0].priority < window[1].priority
                || (window[0].priority == window[1].priority
                    && window[0].blocked_integration_count >= window[1].blocked_integration_count)
        }));

        let readiness = from_rows
            .iter()
            .find(|item| {
                item.remediation_kind
                    == IntegrationActivationEvidenceRemediationKind::SupplyReadinessInputs
            })
            .unwrap();
        assert_eq!(readiness.lane, IntegrationActivationEvidenceLane::Readiness);
        assert_eq!(
            readiness.remediation_kind.as_str(),
            "supply_readiness_inputs"
        );
        assert!(readiness.includes_integration(&IntegrationId::trusted("hue")));
        assert!(readiness.requires_human_review());
        assert!(readiness.has_missing_inputs());
        assert!(readiness.missing_prerequisite_count > 0);
        assert!(readiness.missing_capability_count > 0);
        assert_eq!(readiness.priority, 0);
        assert_eq!(readiness.highest_policy_tier, PrivilegeTier::HighRisk);

        assert_eq!(summary.total_remediation_items, from_rows.len());
        assert_eq!(
            summary.unique_integrations,
            rows.iter().filter(|row| row.has_blockers()).count()
        );
        assert_eq!(
            summary.total_blocked_integrations,
            from_rows
                .iter()
                .map(|item| item.blocked_integration_count)
                .sum::<usize>()
        );
        assert!(summary.has_remediations());
        assert!(summary.has_readiness_remediation());
        assert!(summary.requires_human_review());
        assert_eq!(summary.first_remediation_priority, Some(0));
        assert_eq!(
            summary.next_remediation_kind,
            Some(IntegrationActivationEvidenceRemediationKind::SupplyReadinessInputs)
        );
        assert_eq!(summary.highest_policy_tier, PrivilegeTier::HighRisk);
    }

    #[test]
    fn activation_evidence_remediation_items_mark_ready_catalog_context() {
        let catalog = vec![hue_entry()];
        let available_primitives = all_primitive_families().to_vec();
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let items = activation_evidence_remediation_items_for_catalog(
            &catalog,
            &available_primitives,
            &allowed_capabilities,
            &[],
        );
        let summary = IntegrationActivationEvidenceRemediationSummary::from_items(items.iter());

        assert!(items.is_empty());
        assert!(summary.is_empty());
        assert!(!summary.has_remediations());
        assert!(!summary.has_readiness_remediation());
        assert!(!summary.requires_human_review());
        assert_eq!(summary.total_blocked_integrations, 0);
        assert_eq!(summary.first_remediation_priority, None);
        assert_eq!(summary.next_remediation_kind, None);
        assert_eq!(summary.highest_policy_tier, PrivilegeTier::ReadOnly);
    }

    #[test]
    fn mesh_protocol_primitive_readiness_groups_radio_substrate_blockers() {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let rows = mesh_protocol_primitive_readiness_rows(&available_primitives);
        let summary = IntegrationMeshProtocolPrimitiveReadinessSummary::from_rows(rows.iter());

        assert_eq!(rows.len(), 3);
        assert_eq!(summary.total_protocols, 3);
        assert_eq!(summary.ready_protocols, 0);
        assert_eq!(summary.blocked_protocols, 3);
        assert_eq!(summary.required_primitive_count, 7);
        assert_eq!(summary.missing_primitive_count, 3);
        assert_eq!(summary.radio_network_key_blockers, 3);
        assert_eq!(summary.supervision_blockers, 0);
        assert_eq!(summary.first_blocked_protocol, Some(ProtocolFamily::Zigbee));
        assert!(summary.has_blockers());
        assert!(summary.needs_radio_network_key());
        assert!(!summary.needs_supervision());
        assert!(!summary.all_ready());
        assert!(summary
            .unique_missing_primitives
            .contains(&PrimitiveFamily::RadioNetworkKey));
        assert!(summary
            .unique_missing_primitives
            .contains(&PrimitiveFamily::ZWaveSerialApi));
        assert!(summary
            .unique_missing_primitives
            .contains(&PrimitiveFamily::Mdns));

        let zigbee = rows
            .iter()
            .find(|row| row.protocol == ProtocolFamily::Zigbee)
            .unwrap();
        assert_eq!(zigbee.required_primitive_count, 5);
        assert_eq!(zigbee.available_primitive_count, 4);
        assert_eq!(zigbee.missing_primitive_count, 1);
        assert!(zigbee.missing_radio_network_key());
        assert!(!zigbee.missing_supervision());
    }

    #[test]
    fn mesh_protocol_primitive_readiness_marks_full_substrate_ready() {
        let rows = mesh_protocol_primitive_readiness_rows(all_primitive_families());
        let summary = mesh_protocol_primitive_readiness_summary(all_primitive_families());

        assert_eq!(rows.len(), 3);
        assert!(rows
            .iter()
            .all(IntegrationMeshProtocolPrimitiveReadinessRow::is_ready));
        assert_eq!(summary.total_protocols, 3);
        assert_eq!(summary.ready_protocols, 3);
        assert_eq!(summary.blocked_protocols, 0);
        assert_eq!(summary.missing_primitive_count, 0);
        assert_eq!(summary.radio_network_key_blockers, 0);
        assert_eq!(summary.supervision_blockers, 0);
        assert_eq!(summary.first_blocked_protocol, None);
        assert!(summary.all_ready());
        assert!(!summary.has_blockers());
    }

    #[test]
    fn mesh_protocol_substrate_stage_readiness_groups_low_level_blockers() {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let rows = mesh_protocol_substrate_stage_rows(&available_primitives);
        let summary = IntegrationMeshProtocolSubstrateStageSummary::from_rows(rows.iter());

        assert_eq!(rows.len(), 16);
        assert_eq!(summary.total_rows, 16);
        assert_eq!(summary.ready_rows, 11);
        assert_eq!(summary.blocked_rows, 5);
        assert_eq!(summary.controller_blockers, 0);
        assert_eq!(summary.radio_blockers, 1);
        assert_eq!(summary.discovery_blockers, 1);
        assert_eq!(summary.network_security_blockers, 3);
        assert_eq!(summary.supervision_blockers, 0);
        assert_eq!(summary.first_blocked_protocol, Some(ProtocolFamily::Zigbee));
        assert_eq!(
            summary.first_blocked_stage,
            Some(IntegrationMeshProtocolSubstrateStage::NetworkSecurity)
        );
        assert!(summary.has_blockers());
        assert!(summary.needs_network_security());
        assert!(!summary.all_ready());
        assert!(summary
            .unique_blocked_stages
            .contains(&IntegrationMeshProtocolSubstrateStage::Radio));
        assert!(summary
            .unique_blocked_stages
            .contains(&IntegrationMeshProtocolSubstrateStage::Discovery));
        assert!(summary
            .unique_blocked_stages
            .contains(&IntegrationMeshProtocolSubstrateStage::NetworkSecurity));

        let zwave_radio = rows
            .iter()
            .find(|row| {
                row.protocol == ProtocolFamily::ZWave
                    && row.primitive == PrimitiveFamily::ZWaveSerialApi
            })
            .unwrap();
        assert_eq!(
            zwave_radio.stage,
            IntegrationMeshProtocolSubstrateStage::Radio
        );
        assert!(zwave_radio.is_blocked());
    }

    #[test]
    fn mesh_protocol_substrate_stage_readiness_marks_full_substrate_ready() {
        let rows = mesh_protocol_substrate_stage_rows(all_primitive_families());
        let summary = mesh_protocol_substrate_stage_summary(all_primitive_families());

        assert_eq!(rows.len(), 16);
        assert!(rows
            .iter()
            .all(IntegrationMeshProtocolSubstrateStageRow::is_ready));
        assert_eq!(summary.total_rows, 16);
        assert_eq!(summary.ready_rows, 16);
        assert_eq!(summary.blocked_rows, 0);
        assert_eq!(summary.unique_required_stages.len(), 5);
        assert_eq!(summary.unique_blocked_stages.len(), 0);
        assert_eq!(summary.controller_blockers, 0);
        assert_eq!(summary.radio_blockers, 0);
        assert_eq!(summary.discovery_blockers, 0);
        assert_eq!(summary.network_security_blockers, 0);
        assert_eq!(summary.supervision_blockers, 0);
        assert_eq!(summary.first_blocked_protocol, None);
        assert_eq!(summary.first_blocked_stage, None);
        assert!(summary.all_ready());
        assert!(!summary.has_blockers());
        assert!(!summary.needs_network_security());
    }

    #[test]
    fn mesh_protocol_substrate_actions_order_stage_blockers() {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let actions = mesh_protocol_substrate_actions(&available_primitives);
        let summary = IntegrationMeshProtocolSubstrateActionSummary::from_actions(actions.iter());

        assert_eq!(actions.len(), 5);
        assert_eq!(summary.total_actions, 5);
        assert_eq!(summary.unique_protocols, 3);
        assert_eq!(summary.unique_primitives, 3);
        assert_eq!(summary.controller_actions, 0);
        assert_eq!(summary.radio_actions, 1);
        assert_eq!(summary.discovery_actions, 1);
        assert_eq!(summary.network_security_actions, 3);
        assert_eq!(summary.supervision_actions, 0);
        assert_eq!(summary.first_protocol, Some(ProtocolFamily::ZWave));
        assert_eq!(
            summary.first_stage,
            Some(IntegrationMeshProtocolSubstrateStage::Radio)
        );
        assert_eq!(
            summary.first_primitive,
            Some(PrimitiveFamily::ZWaveSerialApi)
        );
        assert!(summary.has_actions());
        assert!(!summary.is_empty());

        assert_eq!(actions[0].sequence, 1);
        assert_eq!(actions[0].protocol, ProtocolFamily::ZWave);
        assert_eq!(actions[0].primitive, PrimitiveFamily::ZWaveSerialApi);
        assert_eq!(
            actions[0].stage,
            IntegrationMeshProtocolSubstrateStage::Radio
        );
        assert_eq!(
            actions[1].stage,
            IntegrationMeshProtocolSubstrateStage::Discovery
        );
        assert!(actions[2..]
            .iter()
            .all(|action| action.stage == IntegrationMeshProtocolSubstrateStage::NetworkSecurity));
    }

    #[test]
    fn mesh_protocol_substrate_actions_empty_when_substrate_ready() {
        let actions = mesh_protocol_substrate_actions(all_primitive_families());
        let summary = mesh_protocol_substrate_action_summary(all_primitive_families());

        assert!(actions.is_empty());
        assert_eq!(summary.total_actions, 0);
        assert_eq!(summary.unique_protocols, 0);
        assert_eq!(summary.unique_primitives, 0);
        assert_eq!(summary.first_protocol, None);
        assert_eq!(summary.first_stage, None);
        assert_eq!(summary.first_primitive, None);
        assert!(summary.is_empty());
        assert!(!summary.has_actions());
    }

    #[test]
    fn mesh_protocol_substrate_preflight_checks_surface_operator_blockers() {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let checks = mesh_protocol_substrate_preflight_checks(&available_primitives);
        let summary = IntegrationMeshProtocolSubstratePreflightSummary::from_checks(checks.iter());

        assert_eq!(checks.len(), 16);
        assert_eq!(summary.total_checks, 16);
        assert_eq!(summary.passed_checks, 11);
        assert_eq!(summary.failed_checks, 5);
        assert_eq!(summary.blocking_checks, 5);
        assert_eq!(summary.operator_required_checks, 4);
        assert_eq!(summary.controller_blockers, 0);
        assert_eq!(summary.radio_blockers, 1);
        assert_eq!(summary.discovery_blockers, 1);
        assert_eq!(summary.network_security_blockers, 3);
        assert_eq!(summary.supervision_blockers, 0);
        assert_eq!(summary.first_failed_protocol, Some(ProtocolFamily::ZWave));
        assert_eq!(
            summary.first_failed_stage,
            Some(IntegrationMeshProtocolSubstrateStage::Radio)
        );
        assert_eq!(
            summary.first_failed_primitive,
            Some(PrimitiveFamily::ZWaveSerialApi)
        );
        assert!(!summary.ready());
        assert!(summary.has_blockers());
        assert!(summary.needs_operator());

        let first = &checks[0];
        assert_eq!(first.sequence, 1);
        assert_eq!(first.protocol, ProtocolFamily::Zigbee);
        assert_eq!(
            first.stage,
            IntegrationMeshProtocolSubstrateStage::Controller
        );
        assert!(first.passed());
        assert!(!first.requires_operator());

        let first_failed = checks.iter().find(|check| check.failed()).unwrap();
        assert_eq!(first_failed.protocol, ProtocolFamily::ZWave);
        assert_eq!(
            first_failed.stage,
            IntegrationMeshProtocolSubstrateStage::Radio
        );
        assert!(first_failed.blocks_preflight());
        assert!(first_failed.requires_operator());
    }

    #[test]
    fn mesh_protocol_substrate_preflight_checks_mark_ready_substrate() {
        let checks = mesh_protocol_substrate_preflight_checks(all_primitive_families());
        let summary = mesh_protocol_substrate_preflight_summary(all_primitive_families());

        assert_eq!(checks.len(), 16);
        assert!(checks
            .iter()
            .all(IntegrationMeshProtocolSubstratePreflightCheck::passed));
        assert_eq!(summary.total_checks, 16);
        assert_eq!(summary.passed_checks, 16);
        assert_eq!(summary.failed_checks, 0);
        assert_eq!(summary.blocking_checks, 0);
        assert_eq!(summary.operator_required_checks, 0);
        assert_eq!(summary.first_failed_protocol, None);
        assert_eq!(summary.first_failed_stage, None);
        assert_eq!(summary.first_failed_primitive, None);
        assert!(summary.ready());
        assert!(!summary.has_blockers());
        assert!(!summary.needs_operator());
    }

    #[test]
    fn mesh_protocol_substrate_preflight_actions_order_failed_gates() {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let actions = mesh_protocol_substrate_preflight_actions(&available_primitives);
        let summary =
            IntegrationMeshProtocolSubstratePreflightActionSummary::from_actions(actions.iter());

        assert_eq!(actions.len(), 5);
        assert_eq!(summary.total_actions, 5);
        assert_eq!(summary.blocking_actions, 5);
        assert_eq!(summary.operator_required_actions, 4);
        assert_eq!(summary.unique_protocols, 3);
        assert_eq!(summary.unique_primitives, 3);
        assert_eq!(summary.controller_actions, 0);
        assert_eq!(summary.radio_actions, 1);
        assert_eq!(summary.discovery_actions, 1);
        assert_eq!(summary.network_security_actions, 3);
        assert_eq!(summary.supervision_actions, 0);
        assert_eq!(summary.first_protocol, Some(ProtocolFamily::ZWave));
        assert_eq!(
            summary.first_stage,
            Some(IntegrationMeshProtocolSubstrateStage::Radio)
        );
        assert_eq!(
            summary.first_primitive,
            Some(PrimitiveFamily::ZWaveSerialApi)
        );
        assert_eq!(
            summary.first_action_kind,
            Some(IntegrationMeshProtocolSubstratePreflightActionKind::ProvisionRadio)
        );
        assert!(!summary.preflight_actions_ready);
        assert!(summary.has_actions());
        assert!(summary.needs_operator());

        assert_eq!(actions[0].sequence, 1);
        assert_eq!(actions[0].protocol, ProtocolFamily::ZWave);
        assert_eq!(
            actions[0].action_kind,
            IntegrationMeshProtocolSubstratePreflightActionKind::ProvisionRadio
        );
        assert!(actions[0].blocks_preflight());
        assert!(actions[0].requires_operator());
        assert_eq!(
            actions[1].action_kind,
            IntegrationMeshProtocolSubstratePreflightActionKind::EnableDiscovery
        );
        assert!(actions[2..].iter().all(|action| {
            action.action_kind
                == IntegrationMeshProtocolSubstratePreflightActionKind::InstallNetworkKey
        }));
    }

    #[test]
    fn mesh_protocol_substrate_preflight_actions_empty_when_ready() {
        let actions = mesh_protocol_substrate_preflight_actions(all_primitive_families());
        let summary = mesh_protocol_substrate_preflight_action_summary(all_primitive_families());

        assert!(actions.is_empty());
        assert_eq!(summary.total_actions, 0);
        assert_eq!(summary.blocking_actions, 0);
        assert_eq!(summary.operator_required_actions, 0);
        assert_eq!(summary.unique_protocols, 0);
        assert_eq!(summary.unique_primitives, 0);
        assert_eq!(summary.first_protocol, None);
        assert_eq!(summary.first_stage, None);
        assert_eq!(summary.first_primitive, None);
        assert_eq!(summary.first_action_kind, None);
        assert!(summary.preflight_actions_ready);
        assert!(summary.is_empty());
        assert!(!summary.has_actions());
        assert!(!summary.needs_operator());
    }

    #[test]
    fn mesh_protocol_substrate_preflight_repair_batches_group_actions_by_stage() {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let batches = mesh_protocol_substrate_preflight_repair_batches(&available_primitives);
        let summary = IntegrationMeshProtocolSubstratePreflightRepairBatchSummary::from_batches(
            batches.iter(),
        );

        assert_eq!(batches.len(), 3);
        assert_eq!(summary.total_batches, 3);
        assert_eq!(summary.total_actions, 5);
        assert_eq!(summary.blocking_batches, 3);
        assert_eq!(summary.operator_required_batches, 2);
        assert_eq!(summary.radio_batches, 1);
        assert_eq!(summary.discovery_batches, 1);
        assert_eq!(summary.network_security_batches, 1);
        assert_eq!(
            summary.first_stage,
            Some(IntegrationMeshProtocolSubstrateStage::Radio)
        );
        assert_eq!(
            summary.first_action_kind,
            Some(IntegrationMeshProtocolSubstratePreflightActionKind::ProvisionRadio)
        );
        assert_eq!(summary.first_protocol, Some(ProtocolFamily::ZWave));
        assert_eq!(
            summary.first_primitive,
            Some(PrimitiveFamily::ZWaveSerialApi)
        );
        assert!(!summary.repair_batches_ready);
        assert!(summary.has_batches());
        assert!(summary.needs_operator());

        let network_security = batches
            .iter()
            .find(|batch| batch.stage == IntegrationMeshProtocolSubstrateStage::NetworkSecurity)
            .unwrap();
        assert_eq!(network_security.action_count, 3);
        assert_eq!(network_security.operator_required_actions, 3);
        assert_eq!(
            network_security.action_kind,
            IntegrationMeshProtocolSubstratePreflightActionKind::InstallNetworkKey
        );
        assert_eq!(
            network_security.primitives,
            vec![PrimitiveFamily::RadioNetworkKey]
        );
        assert_eq!(
            network_security.protocols,
            vec![
                ProtocolFamily::Zigbee,
                ProtocolFamily::ZWave,
                ProtocolFamily::Thread
            ]
        );
        assert!(network_security.blocks_preflight());
        assert!(network_security.requires_operator());
    }

    #[test]
    fn mesh_protocol_substrate_preflight_repair_batches_empty_when_ready() {
        let batches = mesh_protocol_substrate_preflight_repair_batches(all_primitive_families());
        let summary =
            mesh_protocol_substrate_preflight_repair_batch_summary(all_primitive_families());

        assert!(batches.is_empty());
        assert_eq!(summary.total_batches, 0);
        assert_eq!(summary.total_actions, 0);
        assert_eq!(summary.blocking_batches, 0);
        assert_eq!(summary.operator_required_batches, 0);
        assert_eq!(summary.first_stage, None);
        assert_eq!(summary.first_action_kind, None);
        assert_eq!(summary.first_protocol, None);
        assert_eq!(summary.first_primitive, None);
        assert!(summary.repair_batches_ready);
        assert!(summary.is_empty());
        assert!(!summary.has_batches());
        assert!(!summary.needs_operator());
    }

    #[test]
    fn mesh_protocol_substrate_preflight_repair_schedule_orders_repair_slots() {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let slots = mesh_protocol_substrate_preflight_repair_schedule(&available_primitives);
        let summary = IntegrationMeshProtocolSubstratePreflightRepairScheduleSummary::from_slots(
            slots.iter(),
        );

        assert_eq!(slots.len(), 3);
        assert_eq!(summary.total_slots, 3);
        assert_eq!(summary.total_actions, 5);
        assert_eq!(summary.blocking_slots, 3);
        assert_eq!(summary.operator_required_slots, 2);
        assert_eq!(summary.radio_slots, 1);
        assert_eq!(summary.discovery_slots, 1);
        assert_eq!(summary.network_security_slots, 1);
        assert_eq!(
            summary.first_stage,
            Some(IntegrationMeshProtocolSubstrateStage::Radio)
        );
        assert_eq!(
            summary.first_action_kind,
            Some(IntegrationMeshProtocolSubstratePreflightActionKind::ProvisionRadio)
        );
        assert_eq!(summary.first_protocol, Some(ProtocolFamily::ZWave));
        assert_eq!(
            summary.first_primitive,
            Some(PrimitiveFamily::ZWaveSerialApi)
        );
        assert!(!summary.repair_schedule_ready);
        assert!(summary.has_slots());
        assert!(summary.needs_operator());

        let first = &slots[0];
        assert_eq!(first.sequence, 1);
        assert_eq!(first.batch_sequence, 1);
        assert_eq!(first.action_count, 1);
        assert_eq!(first.blocking_actions, 1);
        assert_eq!(first.operator_required_actions, 1);
        assert_eq!(first.protocols, vec![ProtocolFamily::ZWave]);
        assert_eq!(first.primitives, vec![PrimitiveFamily::ZWaveSerialApi]);
        assert!(first.blocks_preflight());
        assert!(first.requires_operator());

        let network_security = slots
            .iter()
            .find(|slot| slot.stage == IntegrationMeshProtocolSubstrateStage::NetworkSecurity)
            .unwrap();
        assert_eq!(network_security.sequence, 3);
        assert_eq!(network_security.action_count, 3);
        assert_eq!(network_security.operator_required_actions, 3);
        assert_eq!(
            network_security.action_kind,
            IntegrationMeshProtocolSubstratePreflightActionKind::InstallNetworkKey
        );
    }

    #[test]
    fn mesh_protocol_substrate_preflight_repair_schedule_empty_when_ready() {
        let slots = mesh_protocol_substrate_preflight_repair_schedule(all_primitive_families());
        let summary =
            mesh_protocol_substrate_preflight_repair_schedule_summary(all_primitive_families());

        assert!(slots.is_empty());
        assert_eq!(summary.total_slots, 0);
        assert_eq!(summary.total_actions, 0);
        assert_eq!(summary.blocking_slots, 0);
        assert_eq!(summary.operator_required_slots, 0);
        assert_eq!(summary.first_stage, None);
        assert_eq!(summary.first_action_kind, None);
        assert_eq!(summary.first_protocol, None);
        assert_eq!(summary.first_primitive, None);
        assert!(summary.repair_schedule_ready);
        assert!(summary.is_empty());
        assert!(!summary.has_slots());
        assert!(!summary.needs_operator());
    }

    #[test]
    fn mesh_protocol_substrate_preflight_repair_slot_audit_rows_surface_schedule_risk() {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let rows = mesh_protocol_substrate_preflight_repair_slot_audit_rows(&available_primitives);
        let summary =
            IntegrationMeshProtocolSubstratePreflightRepairSlotAuditSummary::from_rows(rows.iter());

        assert_eq!(rows.len(), 3);
        assert_eq!(summary.total_rows, 3);
        assert_eq!(summary.scheduled_actions, 5);
        assert_eq!(summary.blocking_rows, 3);
        assert_eq!(summary.operator_required_rows, 2);
        assert_eq!(summary.protocol_mentions, 5);
        assert_eq!(summary.primitive_mentions, 3);
        assert_eq!(summary.first_blocking_sequence, Some(1));
        assert_eq!(summary.first_operator_required_sequence, Some(1));
        assert_eq!(
            summary.first_stage,
            Some(IntegrationMeshProtocolSubstrateStage::Radio)
        );
        assert_eq!(
            summary.first_action_kind,
            Some(IntegrationMeshProtocolSubstratePreflightActionKind::ProvisionRadio)
        );
        assert!(!summary.repair_slot_audit_ready);
        assert!(summary.has_rows());
        assert!(summary.has_blockers());
        assert!(summary.needs_operator());

        let first = &rows[0];
        assert_eq!(first.sequence, 1);
        assert_eq!(first.slot_sequence, 1);
        assert_eq!(first.batch_sequence, 1);
        assert_eq!(first.action_count, 1);
        assert_eq!(first.protocol_count, 1);
        assert_eq!(first.primitive_count, 1);
        assert_eq!(first.protocols, vec![ProtocolFamily::ZWave]);
        assert_eq!(first.primitives, vec![PrimitiveFamily::ZWaveSerialApi]);
        assert!(first.blocks_preflight());
        assert!(first.requires_operator());

        let network_security = rows
            .iter()
            .find(|row| row.stage == IntegrationMeshProtocolSubstrateStage::NetworkSecurity)
            .unwrap();
        assert_eq!(network_security.sequence, 3);
        assert_eq!(network_security.action_count, 3);
        assert_eq!(network_security.protocol_count, 3);
        assert_eq!(network_security.primitive_count, 1);
        assert_eq!(
            network_security.action_kind,
            IntegrationMeshProtocolSubstratePreflightActionKind::InstallNetworkKey
        );
    }

    #[test]
    fn mesh_protocol_substrate_preflight_repair_slot_audit_empty_when_ready() {
        let rows =
            mesh_protocol_substrate_preflight_repair_slot_audit_rows(all_primitive_families());
        let summary =
            mesh_protocol_substrate_preflight_repair_slot_audit_summary(all_primitive_families());

        assert!(rows.is_empty());
        assert_eq!(summary.total_rows, 0);
        assert_eq!(summary.scheduled_actions, 0);
        assert_eq!(summary.blocking_rows, 0);
        assert_eq!(summary.operator_required_rows, 0);
        assert_eq!(summary.protocol_mentions, 0);
        assert_eq!(summary.primitive_mentions, 0);
        assert_eq!(summary.first_blocking_sequence, None);
        assert_eq!(summary.first_operator_required_sequence, None);
        assert_eq!(summary.first_stage, None);
        assert_eq!(summary.first_action_kind, None);
        assert!(summary.repair_slot_audit_ready);
        assert!(summary.is_empty());
        assert!(!summary.has_rows());
        assert!(!summary.has_blockers());
        assert!(!summary.needs_operator());
    }

    #[test]
    fn mesh_protocol_substrate_preflight_repair_slot_execution_tickets_surface_slot_work() {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let tickets =
            mesh_protocol_substrate_preflight_repair_slot_execution_tickets(&available_primitives);
        let summary =
            IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionTicketSummary::from_tickets(
                tickets.iter(),
            );

        assert_eq!(tickets.len(), 3);
        assert_eq!(summary.total_tickets, 3);
        assert_eq!(summary.scheduled_actions, 5);
        assert_eq!(summary.blocking_tickets, 3);
        assert_eq!(summary.operator_required_tickets, 2);
        assert_eq!(summary.protocol_mentions, 5);
        assert_eq!(summary.primitive_mentions, 3);
        assert_eq!(
            summary.first_ticket_key,
            Some("slot-01-radio-provision_radio".to_string())
        );
        assert_eq!(
            summary.first_blocking_ticket_key,
            Some("slot-01-radio-provision_radio".to_string())
        );
        assert_eq!(
            summary.first_operator_ticket_key,
            Some("slot-01-radio-provision_radio".to_string())
        );
        assert_eq!(
            summary.first_stage,
            Some(IntegrationMeshProtocolSubstrateStage::Radio)
        );
        assert_eq!(
            summary.first_action_kind,
            Some(IntegrationMeshProtocolSubstratePreflightActionKind::ProvisionRadio)
        );
        assert!(!summary.execution_tickets_ready);
        assert!(summary.has_tickets());
        assert!(summary.has_blockers());
        assert!(summary.needs_operator());

        let first = &tickets[0];
        assert_eq!(first.sequence, 1);
        assert_eq!(first.audit_sequence, 1);
        assert_eq!(first.slot_sequence, 1);
        assert_eq!(first.batch_sequence, 1);
        assert_eq!(first.ticket_key, "slot-01-radio-provision_radio");
        assert_eq!(
            first.status,
            IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionStatus::OperatorHandoff
        );
        assert_eq!(first.status.as_str(), "operator_handoff");
        assert_eq!(first.action_count, 1);
        assert_eq!(first.protocols, vec![ProtocolFamily::ZWave]);
        assert_eq!(first.primitives, vec![PrimitiveFamily::ZWaveSerialApi]);
        assert!(first.blocks_preflight());
        assert!(first.requires_operator());

        let discovery = tickets
            .iter()
            .find(|ticket| ticket.stage == IntegrationMeshProtocolSubstrateStage::Discovery)
            .unwrap();
        assert_eq!(
            discovery.status,
            IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionStatus::Blocked
        );
        assert_eq!(discovery.status.as_str(), "blocked");
        assert!(discovery.blocks_preflight());
        assert!(!discovery.requires_operator());
    }

    #[test]
    fn mesh_protocol_substrate_preflight_repair_slot_execution_tickets_empty_when_ready() {
        let tickets = mesh_protocol_substrate_preflight_repair_slot_execution_tickets(
            all_primitive_families(),
        );
        let summary = mesh_protocol_substrate_preflight_repair_slot_execution_ticket_summary(
            all_primitive_families(),
        );

        assert!(tickets.is_empty());
        assert_eq!(summary.total_tickets, 0);
        assert_eq!(summary.scheduled_actions, 0);
        assert_eq!(summary.blocking_tickets, 0);
        assert_eq!(summary.operator_required_tickets, 0);
        assert_eq!(summary.protocol_mentions, 0);
        assert_eq!(summary.primitive_mentions, 0);
        assert_eq!(summary.first_ticket_key, None);
        assert_eq!(summary.first_blocking_ticket_key, None);
        assert_eq!(summary.first_operator_ticket_key, None);
        assert_eq!(summary.first_stage, None);
        assert_eq!(summary.first_action_kind, None);
        assert!(summary.execution_tickets_ready);
        assert!(summary.is_empty());
        assert!(!summary.has_tickets());
        assert!(!summary.has_blockers());
        assert!(!summary.needs_operator());
    }

    #[test]
    fn mesh_protocol_substrate_preflight_repair_slot_execution_work_orders_surface_ticket_work() {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let work_orders = mesh_protocol_substrate_preflight_repair_slot_execution_work_orders(
            &available_primitives,
        );
        let summary =
            IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionWorkOrderSummary::from_work_orders(
                work_orders.iter(),
            );

        assert_eq!(work_orders.len(), 3);
        assert_eq!(summary.total_work_orders, 3);
        assert_eq!(summary.scheduled_actions, 5);
        assert_eq!(summary.blocking_work_orders, 3);
        assert_eq!(summary.operator_required_work_orders, 2);
        assert_eq!(summary.protocol_mentions, 5);
        assert_eq!(summary.primitive_mentions, 3);
        assert_eq!(
            summary.first_work_order_key,
            Some("work-01-radio-provision_radio".to_string())
        );
        assert_eq!(
            summary.first_blocking_work_order_key,
            Some("work-01-radio-provision_radio".to_string())
        );
        assert_eq!(
            summary.first_operator_work_order_key,
            Some("work-01-radio-provision_radio".to_string())
        );
        assert_eq!(
            summary.first_ticket_key,
            Some("slot-01-radio-provision_radio".to_string())
        );
        assert_eq!(
            summary.first_stage,
            Some(IntegrationMeshProtocolSubstrateStage::Radio)
        );
        assert_eq!(
            summary.first_action_kind,
            Some(IntegrationMeshProtocolSubstratePreflightActionKind::ProvisionRadio)
        );
        assert!(!summary.execution_work_orders_ready);
        assert!(summary.has_work_orders());
        assert!(summary.has_blockers());
        assert!(summary.needs_operator());

        let first = &work_orders[0];
        assert_eq!(first.sequence, 1);
        assert_eq!(first.ticket_sequence, 1);
        assert_eq!(first.slot_sequence, 1);
        assert_eq!(first.batch_sequence, 1);
        assert_eq!(first.work_order_key, "work-01-radio-provision_radio");
        assert_eq!(first.ticket_key, "slot-01-radio-provision_radio");
        assert_eq!(
            first.status,
            IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionStatus::OperatorHandoff
        );
        assert_eq!(first.action_count, 1);
        assert_eq!(first.protocols, vec![ProtocolFamily::ZWave]);
        assert_eq!(first.primitives, vec![PrimitiveFamily::ZWaveSerialApi]);
        assert!(first.blocks_preflight());
        assert!(first.requires_operator());

        let discovery = work_orders
            .iter()
            .find(|order| order.stage == IntegrationMeshProtocolSubstrateStage::Discovery)
            .unwrap();
        assert_eq!(
            discovery.status,
            IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionStatus::Blocked
        );
        assert!(discovery.blocks_preflight());
        assert!(!discovery.requires_operator());
    }

    #[test]
    fn mesh_protocol_substrate_preflight_repair_slot_execution_work_orders_empty_when_ready() {
        let work_orders = mesh_protocol_substrate_preflight_repair_slot_execution_work_orders(
            all_primitive_families(),
        );
        let summary = mesh_protocol_substrate_preflight_repair_slot_execution_work_order_summary(
            all_primitive_families(),
        );

        assert!(work_orders.is_empty());
        assert_eq!(summary.total_work_orders, 0);
        assert_eq!(summary.scheduled_actions, 0);
        assert_eq!(summary.blocking_work_orders, 0);
        assert_eq!(summary.operator_required_work_orders, 0);
        assert_eq!(summary.protocol_mentions, 0);
        assert_eq!(summary.primitive_mentions, 0);
        assert_eq!(summary.first_work_order_key, None);
        assert_eq!(summary.first_blocking_work_order_key, None);
        assert_eq!(summary.first_operator_work_order_key, None);
        assert_eq!(summary.first_ticket_key, None);
        assert_eq!(summary.first_stage, None);
        assert_eq!(summary.first_action_kind, None);
        assert!(summary.execution_work_orders_ready);
        assert!(summary.is_empty());
        assert!(!summary.has_work_orders());
        assert!(!summary.has_blockers());
        assert!(!summary.needs_operator());
    }

    #[test]
    fn mesh_protocol_substrate_preflight_repair_slot_execution_evidence_surfaces_lineage() {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let packets = mesh_protocol_substrate_preflight_repair_slot_execution_evidence_packets(
            &available_primitives,
        );
        let summary =
            IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceSummary::from_packets(
                packets.iter(),
            );

        assert_eq!(packets.len(), 3);
        assert_eq!(summary.total_packets, 3);
        assert_eq!(summary.scheduled_actions, 5);
        assert_eq!(summary.blocking_packets, 3);
        assert_eq!(summary.operator_required_packets, 2);
        assert_eq!(summary.lineage_complete_packets, 3);
        assert_eq!(summary.protocol_mentions, 5);
        assert_eq!(summary.primitive_mentions, 3);
        assert_eq!(
            summary.first_evidence_key,
            Some("evidence-01-radio-provision_radio".to_string())
        );
        assert_eq!(
            summary.first_blocking_evidence_key,
            Some("evidence-01-radio-provision_radio".to_string())
        );
        assert_eq!(
            summary.first_operator_evidence_key,
            Some("evidence-01-radio-provision_radio".to_string())
        );
        assert_eq!(
            summary.first_work_order_key,
            Some("work-01-radio-provision_radio".to_string())
        );
        assert_eq!(
            summary.first_ticket_key,
            Some("slot-01-radio-provision_radio".to_string())
        );
        assert_eq!(
            summary.first_stage,
            Some(IntegrationMeshProtocolSubstrateStage::Radio)
        );
        assert_eq!(
            summary.first_action_kind,
            Some(IntegrationMeshProtocolSubstratePreflightActionKind::ProvisionRadio)
        );
        assert!(!summary.execution_evidence_ready);
        assert!(summary.has_packets());
        assert!(summary.has_blockers());
        assert!(summary.needs_operator());
        assert!(summary.has_complete_lineage());

        let first = &packets[0];
        assert_eq!(first.sequence, 1);
        assert_eq!(first.evidence_key, "evidence-01-radio-provision_radio");
        assert_eq!(first.work_order_sequence, 1);
        assert_eq!(first.work_order_key, "work-01-radio-provision_radio");
        assert_eq!(first.ticket_sequence, 1);
        assert_eq!(first.ticket_key, "slot-01-radio-provision_radio");
        assert_eq!(first.slot_sequence, 1);
        assert_eq!(first.batch_sequence, 1);
        assert_eq!(
            first.status,
            IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionStatus::OperatorHandoff
        );
        assert_eq!(first.action_count, 1);
        assert_eq!(first.protocols, vec![ProtocolFamily::ZWave]);
        assert_eq!(first.primitives, vec![PrimitiveFamily::ZWaveSerialApi]);
        assert!(first.blocks_preflight());
        assert!(first.requires_operator());
        assert!(first.has_lineage());
    }

    #[test]
    fn mesh_protocol_substrate_preflight_repair_slot_execution_evidence_empty_when_ready() {
        let packets = mesh_protocol_substrate_preflight_repair_slot_execution_evidence_packets(
            all_primitive_families(),
        );
        let summary = mesh_protocol_substrate_preflight_repair_slot_execution_evidence_summary(
            all_primitive_families(),
        );

        assert!(packets.is_empty());
        assert_eq!(summary.total_packets, 0);
        assert_eq!(summary.scheduled_actions, 0);
        assert_eq!(summary.blocking_packets, 0);
        assert_eq!(summary.operator_required_packets, 0);
        assert_eq!(summary.lineage_complete_packets, 0);
        assert_eq!(summary.protocol_mentions, 0);
        assert_eq!(summary.primitive_mentions, 0);
        assert_eq!(summary.first_evidence_key, None);
        assert_eq!(summary.first_blocking_evidence_key, None);
        assert_eq!(summary.first_operator_evidence_key, None);
        assert_eq!(summary.first_work_order_key, None);
        assert_eq!(summary.first_ticket_key, None);
        assert_eq!(summary.first_stage, None);
        assert_eq!(summary.first_action_kind, None);
        assert!(summary.execution_evidence_ready);
        assert!(summary.is_empty());
        assert!(!summary.has_packets());
        assert!(!summary.has_blockers());
        assert!(!summary.needs_operator());
        assert!(summary.has_complete_lineage());
    }

    #[test]
    fn mesh_protocol_substrate_preflight_repair_slot_execution_evidence_reviews_track_readiness() {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let reviews = mesh_protocol_substrate_preflight_repair_slot_execution_evidence_reviews(
            &available_primitives,
        );
        let summary =
            IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewSummary::from_reviews(
                reviews.iter(),
            );

        assert_eq!(reviews.len(), 3);
        assert_eq!(summary.total_reviews, 3);
        assert_eq!(summary.scheduled_actions, 5);
        assert_eq!(summary.blocking_reviews, 3);
        assert_eq!(summary.operator_required_reviews, 2);
        assert_eq!(summary.lineage_complete_reviews, 3);
        assert_eq!(summary.review_required_reviews, 3);
        assert_eq!(summary.execution_ready_reviews, 0);
        assert_eq!(summary.protocol_mentions, 5);
        assert_eq!(summary.primitive_mentions, 3);
        assert_eq!(
            summary.first_review_key,
            Some("review-01-radio-provision_radio".to_string())
        );
        assert_eq!(
            summary.first_blocking_review_key,
            Some("review-01-radio-provision_radio".to_string())
        );
        assert_eq!(
            summary.first_operator_review_key,
            Some("review-01-radio-provision_radio".to_string())
        );
        assert_eq!(
            summary.first_evidence_key,
            Some("evidence-01-radio-provision_radio".to_string())
        );
        assert_eq!(
            summary.first_work_order_key,
            Some("work-01-radio-provision_radio".to_string())
        );
        assert_eq!(
            summary.first_ticket_key,
            Some("slot-01-radio-provision_radio".to_string())
        );
        assert_eq!(
            summary.first_stage,
            Some(IntegrationMeshProtocolSubstrateStage::Radio)
        );
        assert_eq!(
            summary.first_action_kind,
            Some(IntegrationMeshProtocolSubstratePreflightActionKind::ProvisionRadio)
        );
        assert!(!summary.execution_evidence_reviews_ready);
        assert!(summary.has_reviews());
        assert!(summary.has_blockers());
        assert!(summary.needs_operator());
        assert!(summary.needs_review());
        assert!(summary.has_complete_lineage());

        let first = &reviews[0];
        assert_eq!(first.sequence, 1);
        assert_eq!(first.review_key, "review-01-radio-provision_radio");
        assert_eq!(first.evidence_sequence, 1);
        assert_eq!(first.evidence_key, "evidence-01-radio-provision_radio");
        assert_eq!(first.work_order_sequence, 1);
        assert_eq!(first.work_order_key, "work-01-radio-provision_radio");
        assert_eq!(first.ticket_sequence, 1);
        assert_eq!(first.ticket_key, "slot-01-radio-provision_radio");
        assert_eq!(first.slot_sequence, 1);
        assert_eq!(first.batch_sequence, 1);
        assert_eq!(
            first.status,
            IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionStatus::OperatorHandoff
        );
        assert_eq!(first.action_count, 1);
        assert_eq!(first.protocols, vec![ProtocolFamily::ZWave]);
        assert_eq!(first.primitives, vec![PrimitiveFamily::ZWaveSerialApi]);
        assert!(first.blocks_preflight());
        assert!(first.requires_operator());
        assert!(first.has_lineage());
        assert!(first.needs_review());
        assert!(!first.is_ready_for_execution());
    }

    #[test]
    fn mesh_protocol_substrate_preflight_repair_slot_execution_evidence_reviews_empty_when_ready() {
        let reviews = mesh_protocol_substrate_preflight_repair_slot_execution_evidence_reviews(
            all_primitive_families(),
        );
        let summary =
            mesh_protocol_substrate_preflight_repair_slot_execution_evidence_review_summary(
                all_primitive_families(),
            );

        assert!(reviews.is_empty());
        assert_eq!(summary.total_reviews, 0);
        assert_eq!(summary.scheduled_actions, 0);
        assert_eq!(summary.blocking_reviews, 0);
        assert_eq!(summary.operator_required_reviews, 0);
        assert_eq!(summary.lineage_complete_reviews, 0);
        assert_eq!(summary.review_required_reviews, 0);
        assert_eq!(summary.execution_ready_reviews, 0);
        assert_eq!(summary.protocol_mentions, 0);
        assert_eq!(summary.primitive_mentions, 0);
        assert_eq!(summary.first_review_key, None);
        assert_eq!(summary.first_blocking_review_key, None);
        assert_eq!(summary.first_operator_review_key, None);
        assert_eq!(summary.first_evidence_key, None);
        assert_eq!(summary.first_work_order_key, None);
        assert_eq!(summary.first_ticket_key, None);
        assert_eq!(summary.first_stage, None);
        assert_eq!(summary.first_action_kind, None);
        assert!(summary.execution_evidence_reviews_ready);
        assert!(summary.is_empty());
        assert!(!summary.has_reviews());
        assert!(!summary.has_blockers());
        assert!(!summary.needs_operator());
        assert!(!summary.needs_review());
        assert!(summary.has_complete_lineage());
    }

    #[test]
    fn mesh_protocol_substrate_preflight_repair_slot_execution_evidence_review_dispositions_classify_work(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let dispositions =
            mesh_protocol_substrate_preflight_repair_slot_execution_evidence_review_dispositions(
                &available_primitives,
            );
        let summary =
            IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionSummary::from_dispositions(
                dispositions.iter(),
            );

        assert_eq!(dispositions.len(), 3);
        assert_eq!(summary.total_dispositions, 3);
        assert_eq!(summary.scheduled_actions, 5);
        assert_eq!(summary.blocking_dispositions, 3);
        assert_eq!(summary.operator_handoff_dispositions, 2);
        assert_eq!(summary.repair_required_dispositions, 1);
        assert_eq!(summary.lineage_gap_dispositions, 0);
        assert_eq!(summary.review_required_dispositions, 3);
        assert_eq!(summary.execution_ready_dispositions, 0);
        assert_eq!(summary.lineage_complete_dispositions, 3);
        assert_eq!(summary.protocol_mentions, 5);
        assert_eq!(summary.primitive_mentions, 3);
        assert_eq!(
            summary.first_disposition_key,
            Some("disposition-01-radio-provision_radio".to_string())
        );
        assert_eq!(
            summary.first_operator_handoff_disposition_key,
            Some("disposition-01-radio-provision_radio".to_string())
        );
        assert_eq!(summary.first_lineage_gap_disposition_key, None);
        assert_eq!(
            summary.first_review_key,
            Some("review-01-radio-provision_radio".to_string())
        );
        assert_eq!(
            summary.first_evidence_key,
            Some("evidence-01-radio-provision_radio".to_string())
        );
        assert_eq!(
            summary.first_work_order_key,
            Some("work-01-radio-provision_radio".to_string())
        );
        assert_eq!(
            summary.first_ticket_key,
            Some("slot-01-radio-provision_radio".to_string())
        );
        assert_eq!(
            summary.first_stage,
            Some(IntegrationMeshProtocolSubstrateStage::Radio)
        );
        assert_eq!(
            summary.first_action_kind,
            Some(IntegrationMeshProtocolSubstratePreflightActionKind::ProvisionRadio)
        );
        assert!(!summary.execution_evidence_review_dispositions_ready);
        assert!(summary.has_dispositions());
        assert!(summary.has_blockers());
        assert!(summary.needs_operator());
        assert!(summary.needs_repair());
        assert!(summary.needs_review());
        assert!(!summary.has_lineage_gaps());
        assert!(summary.has_complete_lineage());

        let first = &dispositions[0];
        assert_eq!(first.sequence, 1);
        assert_eq!(
            first.disposition_key,
            "disposition-01-radio-provision_radio"
        );
        assert_eq!(first.review_sequence, 1);
        assert_eq!(first.review_key, "review-01-radio-provision_radio");
        assert_eq!(first.evidence_sequence, 1);
        assert_eq!(first.evidence_key, "evidence-01-radio-provision_radio");
        assert_eq!(first.work_order_sequence, 1);
        assert_eq!(first.work_order_key, "work-01-radio-provision_radio");
        assert_eq!(first.ticket_sequence, 1);
        assert_eq!(first.ticket_key, "slot-01-radio-provision_radio");
        assert_eq!(
            first.disposition_kind,
            IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionKind::OperatorHandoff
        );
        assert_eq!(first.disposition_kind.as_str(), "operator_handoff");
        assert!(first.blocks_preflight());
        assert!(first.requires_operator());
        assert!(first.has_lineage());
        assert!(first.needs_review());
        assert!(first.is_operator_handoff());
        assert!(!first.is_repair_required());
        assert!(!first.is_lineage_gap());
        assert!(!first.is_ready_for_execution());
    }

    #[test]
    fn mesh_protocol_substrate_preflight_repair_slot_execution_evidence_review_dispositions_empty_when_ready(
    ) {
        let dispositions =
            mesh_protocol_substrate_preflight_repair_slot_execution_evidence_review_dispositions(
                all_primitive_families(),
            );
        let summary =
            mesh_protocol_substrate_preflight_repair_slot_execution_evidence_review_disposition_summary(
                all_primitive_families(),
            );

        assert!(dispositions.is_empty());
        assert_eq!(summary.total_dispositions, 0);
        assert_eq!(summary.scheduled_actions, 0);
        assert_eq!(summary.blocking_dispositions, 0);
        assert_eq!(summary.operator_handoff_dispositions, 0);
        assert_eq!(summary.repair_required_dispositions, 0);
        assert_eq!(summary.lineage_gap_dispositions, 0);
        assert_eq!(summary.review_required_dispositions, 0);
        assert_eq!(summary.execution_ready_dispositions, 0);
        assert_eq!(summary.lineage_complete_dispositions, 0);
        assert_eq!(summary.protocol_mentions, 0);
        assert_eq!(summary.primitive_mentions, 0);
        assert_eq!(summary.first_disposition_key, None);
        assert_eq!(summary.first_operator_handoff_disposition_key, None);
        assert_eq!(summary.first_repair_required_disposition_key, None);
        assert_eq!(summary.first_lineage_gap_disposition_key, None);
        assert_eq!(summary.first_review_key, None);
        assert_eq!(summary.first_evidence_key, None);
        assert_eq!(summary.first_work_order_key, None);
        assert_eq!(summary.first_ticket_key, None);
        assert_eq!(summary.first_stage, None);
        assert_eq!(summary.first_action_kind, None);
        assert!(summary.execution_evidence_review_dispositions_ready);
        assert!(summary.is_empty());
        assert!(!summary.has_dispositions());
        assert!(!summary.has_blockers());
        assert!(!summary.needs_operator());
        assert!(!summary.needs_repair());
        assert!(!summary.needs_review());
        assert!(!summary.has_lineage_gaps());
        assert!(summary.has_complete_lineage());
    }

    #[test]
    fn mesh_protocol_substrate_preflight_repair_slot_execution_evidence_review_disposition_actions_route_work(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let actions =
            mesh_protocol_substrate_preflight_repair_slot_execution_evidence_review_disposition_actions(
                &available_primitives,
            );
        let summary =
            IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionActionSummary::from_actions(
                actions.iter(),
            );

        assert_eq!(actions.len(), 3);
        assert_eq!(summary.total_actions, 3);
        assert_eq!(summary.required_actions, 3);
        assert_eq!(summary.blocking_actions, 3);
        assert_eq!(summary.operator_handoff_actions, 2);
        assert_eq!(summary.repair_actions, 1);
        assert_eq!(summary.lineage_gap_actions, 0);
        assert_eq!(summary.release_execution_actions, 0);
        assert_eq!(summary.review_required_actions, 3);
        assert_eq!(summary.lineage_complete_actions, 3);
        assert_eq!(summary.protocol_mentions, 5);
        assert_eq!(summary.primitive_mentions, 3);
        assert_eq!(
            summary.first_action_key,
            Some("disposition-action-01-radio-provision_radio".to_string())
        );
        assert_eq!(
            summary.first_required_action_key,
            Some("disposition-action-01-radio-provision_radio".to_string())
        );
        assert_eq!(
            summary.first_operator_handoff_action_key,
            Some("disposition-action-01-radio-provision_radio".to_string())
        );
        assert_eq!(summary.first_lineage_gap_action_key, None);
        assert_eq!(summary.first_release_execution_action_key, None);
        assert_eq!(
            summary.first_disposition_key,
            Some("disposition-01-radio-provision_radio".to_string())
        );
        assert_eq!(
            summary.first_review_key,
            Some("review-01-radio-provision_radio".to_string())
        );
        assert_eq!(
            summary.first_evidence_key,
            Some("evidence-01-radio-provision_radio".to_string())
        );
        assert_eq!(
            summary.first_work_order_key,
            Some("work-01-radio-provision_radio".to_string())
        );
        assert_eq!(
            summary.first_ticket_key,
            Some("slot-01-radio-provision_radio".to_string())
        );
        assert_eq!(
            summary.first_stage,
            Some(IntegrationMeshProtocolSubstrateStage::Radio)
        );
        assert_eq!(
            summary.first_action_kind,
            Some(IntegrationMeshProtocolSubstratePreflightActionKind::ProvisionRadio)
        );
        assert!(!summary.execution_evidence_review_disposition_actions_ready);
        assert!(summary.has_actions());
        assert!(summary.has_required_actions());
        assert!(summary.has_blockers());
        assert!(summary.needs_operator());
        assert!(summary.needs_repair());
        assert!(!summary.has_lineage_gaps());
        assert!(!summary.has_release_execution_actions());
        assert!(summary.needs_review());
        assert!(summary.has_complete_lineage());

        let first = &actions[0];
        assert_eq!(first.sequence, 1);
        assert_eq!(
            first.action_key,
            "disposition-action-01-radio-provision_radio"
        );
        assert_eq!(first.disposition_sequence, 1);
        assert_eq!(
            first.disposition_key,
            "disposition-01-radio-provision_radio"
        );
        assert_eq!(first.review_sequence, 1);
        assert_eq!(first.review_key, "review-01-radio-provision_radio");
        assert_eq!(first.evidence_sequence, 1);
        assert_eq!(first.evidence_key, "evidence-01-radio-provision_radio");
        assert_eq!(first.work_order_sequence, 1);
        assert_eq!(first.work_order_key, "work-01-radio-provision_radio");
        assert_eq!(first.ticket_sequence, 1);
        assert_eq!(first.ticket_key, "slot-01-radio-provision_radio");
        assert_eq!(
            first.disposition_action_kind,
            IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionActionKind::CoordinateOperatorHandoff
        );
        assert_eq!(
            first.disposition_action_kind.as_str(),
            "coordinate_operator_handoff"
        );
        assert!(first.requires_action());
        assert!(first.blocks_preflight());
        assert!(first.requires_operator());
        assert!(first.has_lineage());
        assert!(first.needs_review());
        assert!(first.is_operator_handoff());
        assert!(!first.is_repair_action());
        assert!(!first.is_lineage_action());
        assert!(!first.is_release_execution_action());
    }

    #[test]
    fn mesh_protocol_substrate_preflight_repair_slot_execution_evidence_review_disposition_actions_empty_when_ready(
    ) {
        let actions =
            mesh_protocol_substrate_preflight_repair_slot_execution_evidence_review_disposition_actions(
                all_primitive_families(),
            );
        let summary =
            mesh_protocol_substrate_preflight_repair_slot_execution_evidence_review_disposition_action_summary(
                all_primitive_families(),
            );

        assert!(actions.is_empty());
        assert_eq!(summary.total_actions, 0);
        assert_eq!(summary.required_actions, 0);
        assert_eq!(summary.blocking_actions, 0);
        assert_eq!(summary.operator_handoff_actions, 0);
        assert_eq!(summary.repair_actions, 0);
        assert_eq!(summary.lineage_gap_actions, 0);
        assert_eq!(summary.release_execution_actions, 0);
        assert_eq!(summary.review_required_actions, 0);
        assert_eq!(summary.lineage_complete_actions, 0);
        assert_eq!(summary.protocol_mentions, 0);
        assert_eq!(summary.primitive_mentions, 0);
        assert_eq!(summary.first_action_key, None);
        assert_eq!(summary.first_required_action_key, None);
        assert_eq!(summary.first_operator_handoff_action_key, None);
        assert_eq!(summary.first_repair_action_key, None);
        assert_eq!(summary.first_lineage_gap_action_key, None);
        assert_eq!(summary.first_release_execution_action_key, None);
        assert_eq!(summary.first_disposition_key, None);
        assert_eq!(summary.first_review_key, None);
        assert_eq!(summary.first_evidence_key, None);
        assert_eq!(summary.first_work_order_key, None);
        assert_eq!(summary.first_ticket_key, None);
        assert_eq!(summary.first_stage, None);
        assert_eq!(summary.first_action_kind, None);
        assert!(summary.execution_evidence_review_disposition_actions_ready);
        assert!(summary.is_empty());
        assert!(!summary.has_actions());
        assert!(!summary.has_required_actions());
        assert!(!summary.has_blockers());
        assert!(!summary.needs_operator());
        assert!(!summary.needs_repair());
        assert!(!summary.has_lineage_gaps());
        assert!(!summary.has_release_execution_actions());
        assert!(!summary.needs_review());
        assert!(summary.has_complete_lineage());
    }

    #[test]
    fn mesh_protocol_substrate_preflight_repair_slot_execution_evidence_review_disposition_action_slots_sequence_actions(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let slots =
            mesh_protocol_substrate_preflight_repair_slot_execution_evidence_review_disposition_action_slots(
                &available_primitives,
            );
        let summary =
            IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionActionSlotSummary::from_slots(
                slots.iter(),
            );

        assert_eq!(slots.len(), 3);
        assert_eq!(summary.total_slots, 3);
        assert_eq!(summary.required_slots, 3);
        assert_eq!(summary.blocking_slots, 3);
        assert_eq!(summary.operator_handoff_slots, 2);
        assert_eq!(summary.repair_slots, 1);
        assert_eq!(summary.lineage_gap_slots, 0);
        assert_eq!(summary.release_execution_slots, 0);
        assert_eq!(summary.review_required_slots, 3);
        assert_eq!(summary.lineage_complete_slots, 3);
        assert_eq!(
            summary.first_slot_key,
            Some("disposition-action-slot-01-radio-provision_radio".to_string())
        );
        assert_eq!(
            summary.first_required_slot_key,
            Some("disposition-action-slot-01-radio-provision_radio".to_string())
        );
        assert_eq!(
            summary.first_operator_handoff_slot_key,
            Some("disposition-action-slot-01-radio-provision_radio".to_string())
        );
        assert_eq!(
            summary.first_repair_slot_key,
            Some("disposition-action-slot-02-discovery-enable_discovery".to_string())
        );
        assert_eq!(summary.first_lineage_gap_slot_key, None);
        assert_eq!(summary.first_release_execution_slot_key, None);
        assert_eq!(
            summary.first_action_key,
            Some("disposition-action-01-radio-provision_radio".to_string())
        );
        assert_eq!(
            summary.first_disposition_key,
            Some("disposition-01-radio-provision_radio".to_string())
        );
        assert_eq!(
            summary.first_work_order_key,
            Some("work-01-radio-provision_radio".to_string())
        );
        assert_eq!(
            summary.first_ticket_key,
            Some("slot-01-radio-provision_radio".to_string())
        );
        assert_eq!(
            summary.first_stage,
            Some(IntegrationMeshProtocolSubstrateStage::Radio)
        );
        assert_eq!(
            summary.first_action_kind,
            Some(IntegrationMeshProtocolSubstratePreflightActionKind::ProvisionRadio)
        );
        assert!(!summary.execution_evidence_review_disposition_action_slots_ready);
        assert!(summary.has_slots());
        assert!(summary.has_required_slots());
        assert!(summary.has_blockers());
        assert!(summary.needs_operator());
        assert!(summary.needs_repair());
        assert!(!summary.has_lineage_gaps());
        assert!(!summary.has_release_execution_slots());
        assert!(summary.needs_review());
        assert!(summary.has_complete_lineage());

        let first = &slots[0];
        assert_eq!(first.sequence, 1);
        assert_eq!(
            first.slot_key,
            "disposition-action-slot-01-radio-provision_radio"
        );
        assert_eq!(first.action_sequence, 1);
        assert_eq!(
            first.action_key,
            "disposition-action-01-radio-provision_radio"
        );
        assert_eq!(
            first.disposition_key,
            "disposition-01-radio-provision_radio"
        );
        assert_eq!(first.work_order_key, "work-01-radio-provision_radio");
        assert_eq!(first.ticket_key, "slot-01-radio-provision_radio");
        assert_eq!(
            first.disposition_action_kind,
            IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionEvidenceReviewDispositionActionKind::CoordinateOperatorHandoff
        );
        assert!(first.requires_action());
        assert!(first.blocks_preflight());
        assert!(first.requires_operator());
        assert!(first.needs_review());
        assert!(first.has_lineage());
        assert!(first.is_operator_handoff_slot());
        assert!(!first.is_repair_slot());
        assert!(!first.is_lineage_slot());
        assert!(!first.is_release_execution_slot());
    }

    #[test]
    fn mesh_protocol_substrate_preflight_repair_slot_execution_evidence_review_disposition_action_slots_empty_when_ready(
    ) {
        let slots =
            mesh_protocol_substrate_preflight_repair_slot_execution_evidence_review_disposition_action_slots(
                all_primitive_families(),
            );
        let summary =
            mesh_protocol_substrate_preflight_repair_slot_execution_evidence_review_disposition_action_slot_summary(
                all_primitive_families(),
            );

        assert!(slots.is_empty());
        assert_eq!(summary.total_slots, 0);
        assert_eq!(summary.required_slots, 0);
        assert_eq!(summary.blocking_slots, 0);
        assert_eq!(summary.operator_handoff_slots, 0);
        assert_eq!(summary.repair_slots, 0);
        assert_eq!(summary.lineage_gap_slots, 0);
        assert_eq!(summary.release_execution_slots, 0);
        assert_eq!(summary.review_required_slots, 0);
        assert_eq!(summary.lineage_complete_slots, 0);
        assert_eq!(summary.first_slot_key, None);
        assert_eq!(summary.first_required_slot_key, None);
        assert_eq!(summary.first_operator_handoff_slot_key, None);
        assert_eq!(summary.first_repair_slot_key, None);
        assert_eq!(summary.first_lineage_gap_slot_key, None);
        assert_eq!(summary.first_release_execution_slot_key, None);
        assert_eq!(summary.first_action_key, None);
        assert_eq!(summary.first_disposition_key, None);
        assert_eq!(summary.first_work_order_key, None);
        assert_eq!(summary.first_ticket_key, None);
        assert_eq!(summary.first_stage, None);
        assert_eq!(summary.first_action_kind, None);
        assert!(summary.execution_evidence_review_disposition_action_slots_ready);
        assert!(summary.is_empty());
        assert!(!summary.has_slots());
        assert!(!summary.has_required_slots());
        assert!(!summary.has_blockers());
        assert!(!summary.needs_operator());
        assert!(!summary.needs_repair());
        assert!(!summary.has_lineage_gaps());
        assert!(!summary.has_release_execution_slots());
        assert!(!summary.needs_review());
        assert!(summary.has_complete_lineage());
    }

    #[test]
    fn mesh_protocol_substrate_preflight_repair_slot_execution_work_order_guardrails_classify_orders(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let guardrails =
            mesh_protocol_substrate_preflight_repair_slot_execution_work_order_guardrails(
                &available_primitives,
            );
        let summary =
            IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionWorkOrderGuardrailSummary::from_guardrails(
                guardrails.iter(),
            );

        assert_eq!(guardrails.len(), 3);
        assert_eq!(summary.total_guardrails, 3);
        assert_eq!(summary.release_blocker_guardrails, 1);
        assert_eq!(summary.operator_handoff_guardrails, 2);
        assert_eq!(summary.ready_to_execute_guardrails, 0);
        assert_eq!(summary.scheduled_actions, 5);
        assert_eq!(summary.protocol_mentions, 5);
        assert_eq!(summary.primitive_mentions, 3);
        assert_eq!(
            summary.first_work_order_key,
            Some("work-01-radio-provision_radio".to_string())
        );
        assert_eq!(
            summary.first_operator_handoff_work_order_key,
            Some("work-01-radio-provision_radio".to_string())
        );
        assert_eq!(
            summary.first_guardrail_kind,
            Some(
                IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionWorkOrderGuardrailKind::OperatorHandoff
            )
        );
        assert_eq!(
            summary.first_stage,
            Some(IntegrationMeshProtocolSubstrateStage::Radio)
        );
        assert_eq!(
            summary.first_action_kind,
            Some(IntegrationMeshProtocolSubstratePreflightActionKind::ProvisionRadio)
        );
        assert!(!summary.execution_work_order_guardrails_ready);
        assert!(summary.has_guardrails());
        assert!(summary.has_release_blockers());
        assert!(summary.needs_operator());
        assert!(!summary.has_ready_to_execute_work());

        let first = &guardrails[0];
        assert_eq!(first.sequence, 1);
        assert_eq!(first.work_order_sequence, 1);
        assert_eq!(first.work_order_key, "work-01-radio-provision_radio");
        assert_eq!(first.ticket_key, "slot-01-radio-provision_radio");
        assert_eq!(
            first.guardrail_kind,
            IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionWorkOrderGuardrailKind::OperatorHandoff
        );
        assert!(first.blocks_release());
        assert!(first.requires_operator());
        assert!(!first.ready_to_execute());

        let release_blocker = guardrails
            .iter()
            .find(|guardrail| {
                guardrail.guardrail_kind
                    == IntegrationMeshProtocolSubstratePreflightRepairSlotExecutionWorkOrderGuardrailKind::ReleaseBlocker
            })
            .unwrap();
        assert_eq!(
            summary.first_release_blocker_work_order_key,
            Some(release_blocker.work_order_key.clone())
        );
        assert_eq!(
            release_blocker.stage,
            IntegrationMeshProtocolSubstrateStage::Discovery
        );
        assert_eq!(
            release_blocker.action_kind,
            IntegrationMeshProtocolSubstratePreflightActionKind::EnableDiscovery
        );
        assert!(release_blocker.blocks_release());
        assert!(!release_blocker.requires_operator());
        assert!(!release_blocker.ready_to_execute());
    }

    #[test]
    fn mesh_protocol_substrate_preflight_repair_slot_execution_work_order_guardrails_empty_when_ready(
    ) {
        let guardrails =
            mesh_protocol_substrate_preflight_repair_slot_execution_work_order_guardrails(
                all_primitive_families(),
            );
        let summary =
            mesh_protocol_substrate_preflight_repair_slot_execution_work_order_guardrail_summary(
                all_primitive_families(),
            );

        assert!(guardrails.is_empty());
        assert_eq!(summary.total_guardrails, 0);
        assert_eq!(summary.release_blocker_guardrails, 0);
        assert_eq!(summary.operator_handoff_guardrails, 0);
        assert_eq!(summary.ready_to_execute_guardrails, 0);
        assert_eq!(summary.scheduled_actions, 0);
        assert_eq!(summary.protocol_mentions, 0);
        assert_eq!(summary.primitive_mentions, 0);
        assert_eq!(summary.first_work_order_key, None);
        assert_eq!(summary.first_release_blocker_work_order_key, None);
        assert_eq!(summary.first_operator_handoff_work_order_key, None);
        assert_eq!(summary.first_ready_to_execute_work_order_key, None);
        assert_eq!(summary.first_guardrail_kind, None);
        assert_eq!(summary.first_stage, None);
        assert_eq!(summary.first_action_kind, None);
        assert!(summary.execution_work_order_guardrails_ready);
        assert!(summary.is_empty());
        assert!(!summary.has_guardrails());
        assert!(!summary.has_release_blockers());
        assert!(!summary.needs_operator());
        assert!(!summary.has_ready_to_execute_work());
    }

    #[test]
    fn mesh_readiness_package_summary_combines_substrate_and_remediation() {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let summary =
            mesh_readiness_package_summary(&available_primitives, &allowed_capabilities, &[]);

        assert_eq!(summary.total_protocols, 3);
        assert_eq!(summary.ready_protocols, 0);
        assert_eq!(summary.blocked_protocols, 3);
        assert_eq!(summary.missing_primitive_count, 3);
        assert_eq!(summary.radio_network_key_blockers, 3);
        assert_eq!(summary.supervision_blockers, 0);
        assert_eq!(summary.first_blocked_protocol, Some(ProtocolFamily::Zigbee));
        assert!(!summary.primitive_substrate_ready);
        assert!(!summary.release_ready);
        assert!(summary.has_blockers());
        assert!(summary.has_remediations());
        assert!(summary.needs_radio_network_key());
        assert!(!summary.needs_supervision());
        assert!(summary.remediation_item_count > 0);
        assert!(summary.blocked_integration_count > 0);
        assert!(summary.readiness_remediation_items > 0);
        assert!(summary.next_remediation_kind.is_some());
    }

    #[test]
    fn mesh_readiness_package_summary_marks_full_substrate_ready() {
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.command.lock"),
            CapabilityId::trusted("smart_home.manage_network"),
        ];
        let summary =
            mesh_readiness_package_summary(all_primitive_families(), &allowed_capabilities, &[]);

        assert_eq!(summary.total_protocols, 3);
        assert_eq!(summary.ready_protocols, 3);
        assert_eq!(summary.blocked_protocols, 0);
        assert_eq!(summary.missing_primitive_count, 0);
        assert_eq!(summary.radio_network_key_blockers, 0);
        assert_eq!(summary.supervision_blockers, 0);
        assert_eq!(summary.first_blocked_protocol, None);
        assert!(summary.primitive_substrate_ready);
        assert!(!summary.needs_radio_network_key());
        assert!(!summary.needs_supervision());
        assert_eq!(summary.mesh_primitive_summary.missing_primitive_count, 0);
    }

    #[test]
    fn mesh_stage_release_summary_combines_stage_and_package_blockers() {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let summary = mesh_stage_release_summary(&available_primitives, &allowed_capabilities, &[]);

        assert_eq!(summary.total_protocols, 3);
        assert_eq!(summary.stage_blocker_count, 5);
        assert_eq!(summary.primitive_blocker_count, 3);
        assert!(summary.remediation_item_count > 0);
        assert_eq!(summary.controller_blockers, 0);
        assert_eq!(summary.radio_blockers, 1);
        assert_eq!(summary.discovery_blockers, 1);
        assert_eq!(summary.network_security_blockers, 3);
        assert_eq!(summary.supervision_blockers, 0);
        assert_eq!(summary.first_blocked_protocol, Some(ProtocolFamily::Zigbee));
        assert_eq!(
            summary.first_blocked_stage,
            Some(IntegrationMeshProtocolSubstrateStage::NetworkSecurity)
        );
        assert!(!summary.substrate_ready);
        assert!(!summary.package_ready);
        assert!(!summary.release_ready);
        assert!(summary.has_blockers());
        assert!(summary.has_stage_blockers());
        assert!(summary.has_remediations());
        assert_eq!(summary.substrate_stage_summary.blocked_rows, 5);
        assert_eq!(summary.package_summary.missing_primitive_count, 3);
    }

    #[test]
    fn mesh_stage_release_summary_marks_full_substrate_ready() {
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.command.lock"),
            CapabilityId::trusted("smart_home.manage_network"),
        ];
        let summary =
            mesh_stage_release_summary(all_primitive_families(), &allowed_capabilities, &[]);

        assert_eq!(summary.total_protocols, 3);
        assert_eq!(summary.stage_blocker_count, 0);
        assert_eq!(summary.primitive_blocker_count, 0);
        assert_eq!(summary.controller_blockers, 0);
        assert_eq!(summary.radio_blockers, 0);
        assert_eq!(summary.discovery_blockers, 0);
        assert_eq!(summary.network_security_blockers, 0);
        assert_eq!(summary.supervision_blockers, 0);
        assert_eq!(summary.first_blocked_protocol, None);
        assert_eq!(summary.first_blocked_stage, None);
        assert!(summary.substrate_ready);
        assert!(summary.substrate_stage_summary.all_ready());
        assert!(summary.package_summary.primitive_substrate_ready);
        assert!(!summary.has_stage_blockers());
    }

    #[test]
    fn mesh_action_readiness_summary_surfaces_next_substrate_action() {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let summary =
            mesh_action_readiness_summary(&available_primitives, &allowed_capabilities, &[]);

        assert_eq!(summary.total_protocols, 3);
        assert_eq!(summary.queued_substrate_actions, 5);
        assert_eq!(summary.queued_stage_count, 3);
        assert_eq!(summary.queued_primitive_count, 3);
        assert!(summary.remediation_item_count > 0);
        assert_eq!(summary.first_action_protocol, Some(ProtocolFamily::ZWave));
        assert_eq!(
            summary.first_action_stage,
            Some(IntegrationMeshProtocolSubstrateStage::Radio)
        );
        assert_eq!(
            summary.first_action_primitive,
            Some(PrimitiveFamily::ZWaveSerialApi)
        );
        assert!(!summary.substrate_actions_ready);
        assert!(!summary.release_ready);
        assert!(summary.has_substrate_actions());
        assert!(summary.has_remediations());
        assert_eq!(summary.action_summary.total_actions, 5);
        assert_eq!(summary.release_summary.stage_blocker_count, 5);
    }

    #[test]
    fn mesh_action_readiness_summary_marks_clear_substrate_actions() {
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.command.lock"),
            CapabilityId::trusted("smart_home.manage_network"),
        ];
        let summary =
            mesh_action_readiness_summary(all_primitive_families(), &allowed_capabilities, &[]);

        assert_eq!(summary.total_protocols, 3);
        assert_eq!(summary.queued_substrate_actions, 0);
        assert_eq!(summary.queued_stage_count, 0);
        assert_eq!(summary.queued_primitive_count, 0);
        assert_eq!(summary.first_action_protocol, None);
        assert_eq!(summary.first_action_stage, None);
        assert_eq!(summary.first_action_primitive, None);
        assert!(summary.substrate_actions_ready);
        assert!(!summary.has_substrate_actions());
        assert_eq!(summary.action_summary.total_actions, 0);
        assert_eq!(summary.release_summary.stage_blocker_count, 0);
    }

    #[test]
    fn mesh_release_readiness_summary_surfaces_package_blockers() {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let summary =
            mesh_release_readiness_summary(&available_primitives, &allowed_capabilities, &[]);

        assert_eq!(summary.total_protocols, 3);
        assert_eq!(summary.queued_substrate_actions, 5);
        assert_eq!(summary.stage_blocker_count, 5);
        assert!(summary.release_blocker_count >= summary.queued_substrate_actions);
        assert_eq!(summary.first_action_protocol, Some(ProtocolFamily::ZWave));
        assert_eq!(
            summary.first_action_stage,
            Some(IntegrationMeshProtocolSubstrateStage::Radio)
        );
        assert!(!summary.package_ready);
        assert!(!summary.substrate_ready);
        assert!(!summary.substrate_actions_ready);
        assert!(!summary.release_ready);
        assert!(summary.has_blockers());
        assert!(summary.has_queued_actions());
        assert!(summary.has_remediations());
    }

    #[test]
    fn mesh_release_readiness_summary_marks_clear_substrate_actions() {
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.command.lock"),
            CapabilityId::trusted("smart_home.manage_network"),
        ];
        let summary =
            mesh_release_readiness_summary(all_primitive_families(), &allowed_capabilities, &[]);

        assert_eq!(summary.total_protocols, 3);
        assert_eq!(summary.queued_substrate_actions, 0);
        assert_eq!(summary.stage_blocker_count, 0);
        assert_eq!(summary.primitive_blocker_count, 0);
        assert_eq!(summary.first_action_protocol, None);
        assert_eq!(summary.first_action_stage, None);
        assert!(summary.substrate_ready);
        assert!(summary.substrate_actions_ready);
        assert!(!summary.has_queued_actions());
        assert_eq!(
            summary
                .action_readiness_summary
                .action_summary
                .total_actions,
            0
        );
    }

    #[test]
    fn mesh_preflight_readiness_summary_combines_preflight_and_release_blockers() {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let summary =
            mesh_preflight_readiness_summary(&available_primitives, &allowed_capabilities, &[]);

        assert_eq!(summary.total_protocols, 3);
        assert_eq!(summary.total_preflight_checks, 16);
        assert_eq!(summary.preflight_blocker_count, 5);
        assert_eq!(summary.operator_required_checks, 4);
        assert_eq!(summary.first_failed_protocol, Some(ProtocolFamily::ZWave));
        assert_eq!(
            summary.first_failed_stage,
            Some(IntegrationMeshProtocolSubstrateStage::Radio)
        );
        assert_eq!(
            summary.first_failed_primitive,
            Some(PrimitiveFamily::ZWaveSerialApi)
        );
        assert!(summary.release_blocker_count >= 5);
        assert_eq!(
            summary.total_blocker_count,
            summary.release_blocker_count + summary.preflight_blocker_count
        );
        assert!(!summary.preflight_ready);
        assert!(!summary.ready_for_release);
        assert!(summary.has_blockers());
        assert!(summary.needs_operator());
        assert_eq!(summary.preflight_summary.failed_checks, 5);
        assert_eq!(
            summary.release_readiness_summary.queued_substrate_actions,
            5
        );
    }

    #[test]
    fn mesh_preflight_readiness_summary_marks_preflight_clear_state() {
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.command.lock"),
            CapabilityId::trusted("smart_home.manage_network"),
        ];
        let summary =
            mesh_preflight_readiness_summary(all_primitive_families(), &allowed_capabilities, &[]);

        assert_eq!(summary.total_protocols, 3);
        assert_eq!(summary.total_preflight_checks, 16);
        assert_eq!(summary.preflight_blocker_count, 0);
        assert_eq!(summary.operator_required_checks, 0);
        assert_eq!(summary.first_failed_protocol, None);
        assert_eq!(summary.first_failed_stage, None);
        assert_eq!(summary.first_failed_primitive, None);
        assert_eq!(summary.preflight_summary.passed_checks, 16);
        assert!(summary.preflight_ready);
        assert_eq!(summary.total_blocker_count, summary.release_blocker_count);
        assert_eq!(summary.ready_for_release, summary.release_ready);
        assert!(!summary.needs_operator());
    }

    #[test]
    fn mesh_preflight_repair_readiness_summary_surfaces_repair_queue() {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let summary = mesh_preflight_repair_readiness_summary(
            &available_primitives,
            &allowed_capabilities,
            &[],
        );

        assert_eq!(summary.total_protocols, 3);
        assert_eq!(summary.total_preflight_checks, 16);
        assert_eq!(summary.preflight_blocker_count, 5);
        assert_eq!(summary.queued_preflight_actions, 5);
        assert_eq!(summary.blocking_preflight_actions, 5);
        assert_eq!(summary.operator_required_actions, 4);
        assert_eq!(summary.queued_stage_count, 3);
        assert_eq!(summary.queued_primitive_count, 3);
        assert_eq!(summary.first_repair_protocol, Some(ProtocolFamily::ZWave));
        assert_eq!(
            summary.first_repair_stage,
            Some(IntegrationMeshProtocolSubstrateStage::Radio)
        );
        assert_eq!(
            summary.first_repair_primitive,
            Some(PrimitiveFamily::ZWaveSerialApi)
        );
        assert_eq!(
            summary.first_repair_action_kind,
            Some(IntegrationMeshProtocolSubstratePreflightActionKind::ProvisionRadio)
        );
        assert!(!summary.preflight_ready);
        assert!(!summary.preflight_actions_ready);
        assert!(!summary.ready_for_release);
        assert!(summary.has_repair_actions());
        assert!(summary.has_blockers());
        assert!(summary.needs_operator());
        assert_eq!(summary.preflight_action_summary.network_security_actions, 3);
    }

    #[test]
    fn mesh_preflight_repair_readiness_summary_marks_ready_release() {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let summary = mesh_preflight_repair_readiness_summary_for_catalog(
            &catalog,
            all_primitive_families(),
            &allowed_capabilities,
            &[],
        );

        assert_eq!(summary.total_protocols, 3);
        assert_eq!(summary.preflight_blocker_count, 0);
        assert_eq!(summary.release_blocker_count, 0);
        assert_eq!(summary.queued_preflight_actions, 0);
        assert_eq!(summary.operator_required_actions, 0);
        assert_eq!(summary.queued_stage_count, 0);
        assert_eq!(summary.queued_primitive_count, 0);
        assert_eq!(summary.first_repair_protocol, None);
        assert_eq!(summary.first_repair_stage, None);
        assert_eq!(summary.first_repair_primitive, None);
        assert_eq!(summary.first_repair_action_kind, None);
        assert!(summary.preflight_ready);
        assert!(summary.preflight_actions_ready);
        assert!(summary.release_ready);
        assert!(summary.ready_for_release);
        assert!(!summary.has_repair_actions());
        assert!(!summary.has_blockers());
        assert!(!summary.needs_operator());
    }

    #[test]
    fn mesh_preflight_batch_readiness_summary_surfaces_batched_repairs() {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let summary = mesh_preflight_batch_readiness_summary(
            &available_primitives,
            &allowed_capabilities,
            &[],
        );

        assert_eq!(summary.total_protocols, 3);
        assert_eq!(summary.total_preflight_checks, 16);
        assert_eq!(summary.preflight_blocker_count, 5);
        assert_eq!(summary.queued_preflight_actions, 5);
        assert_eq!(summary.repair_batch_count, 3);
        assert_eq!(summary.batched_preflight_actions, 5);
        assert_eq!(summary.blocking_repair_batches, 3);
        assert_eq!(summary.operator_required_batches, 2);
        assert_eq!(
            summary.first_batch_stage,
            Some(IntegrationMeshProtocolSubstrateStage::Radio)
        );
        assert_eq!(
            summary.first_batch_action_kind,
            Some(IntegrationMeshProtocolSubstratePreflightActionKind::ProvisionRadio)
        );
        assert_eq!(summary.first_batch_protocol, Some(ProtocolFamily::ZWave));
        assert_eq!(
            summary.first_batch_primitive,
            Some(PrimitiveFamily::ZWaveSerialApi)
        );
        assert!(!summary.preflight_ready);
        assert!(!summary.repair_actions_ready);
        assert!(!summary.repair_batches_ready);
        assert!(!summary.ready_for_release);
        assert!(summary.has_repair_batches());
        assert!(summary.has_blockers());
        assert!(summary.needs_operator());
        assert_eq!(summary.repair_batch_summary.network_security_batches, 1);
    }

    #[test]
    fn mesh_preflight_batch_readiness_summary_marks_ready_release() {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let summary = mesh_preflight_batch_readiness_summary_for_catalog(
            &catalog,
            all_primitive_families(),
            &allowed_capabilities,
            &[],
        );

        assert_eq!(summary.total_protocols, 3);
        assert_eq!(summary.preflight_blocker_count, 0);
        assert_eq!(summary.release_blocker_count, 0);
        assert_eq!(summary.queued_preflight_actions, 0);
        assert_eq!(summary.repair_batch_count, 0);
        assert_eq!(summary.batched_preflight_actions, 0);
        assert_eq!(summary.blocking_repair_batches, 0);
        assert_eq!(summary.operator_required_batches, 0);
        assert_eq!(summary.first_batch_stage, None);
        assert_eq!(summary.first_batch_action_kind, None);
        assert_eq!(summary.first_batch_protocol, None);
        assert_eq!(summary.first_batch_primitive, None);
        assert!(summary.preflight_ready);
        assert!(summary.repair_actions_ready);
        assert!(summary.repair_batches_ready);
        assert!(summary.release_ready);
        assert!(summary.ready_for_release);
        assert!(!summary.has_repair_batches());
        assert!(!summary.has_blockers());
        assert!(!summary.needs_operator());
    }

    #[test]
    fn mesh_preflight_schedule_readiness_summary_surfaces_scheduled_repairs() {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let summary = mesh_preflight_schedule_readiness_summary(
            &available_primitives,
            &allowed_capabilities,
            &[],
        );

        assert_eq!(summary.total_protocols, 3);
        assert_eq!(summary.total_preflight_checks, 16);
        assert_eq!(summary.preflight_blocker_count, 5);
        assert_eq!(summary.queued_preflight_actions, 5);
        assert_eq!(summary.repair_batch_count, 3);
        assert_eq!(summary.repair_slot_count, 3);
        assert_eq!(summary.scheduled_preflight_actions, 5);
        assert_eq!(summary.blocking_repair_slots, 3);
        assert_eq!(summary.operator_required_slots, 2);
        assert_eq!(
            summary.first_schedule_stage,
            Some(IntegrationMeshProtocolSubstrateStage::Radio)
        );
        assert_eq!(
            summary.first_schedule_action_kind,
            Some(IntegrationMeshProtocolSubstratePreflightActionKind::ProvisionRadio)
        );
        assert_eq!(summary.first_schedule_protocol, Some(ProtocolFamily::ZWave));
        assert_eq!(
            summary.first_schedule_primitive,
            Some(PrimitiveFamily::ZWaveSerialApi)
        );
        assert!(!summary.preflight_ready);
        assert!(!summary.repair_actions_ready);
        assert!(!summary.repair_batches_ready);
        assert!(!summary.repair_schedule_ready);
        assert!(!summary.ready_for_release);
        assert!(summary.has_repair_schedule());
        assert!(summary.has_blockers());
        assert!(summary.needs_operator());
        assert_eq!(summary.repair_schedule_summary.network_security_slots, 1);
    }

    #[test]
    fn mesh_preflight_schedule_readiness_summary_marks_ready_release() {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let summary = mesh_preflight_schedule_readiness_summary_for_catalog(
            &catalog,
            all_primitive_families(),
            &allowed_capabilities,
            &[],
        );

        assert_eq!(summary.total_protocols, 3);
        assert_eq!(summary.preflight_blocker_count, 0);
        assert_eq!(summary.release_blocker_count, 0);
        assert_eq!(summary.queued_preflight_actions, 0);
        assert_eq!(summary.repair_batch_count, 0);
        assert_eq!(summary.repair_slot_count, 0);
        assert_eq!(summary.scheduled_preflight_actions, 0);
        assert_eq!(summary.blocking_repair_slots, 0);
        assert_eq!(summary.operator_required_slots, 0);
        assert_eq!(summary.first_schedule_stage, None);
        assert_eq!(summary.first_schedule_action_kind, None);
        assert_eq!(summary.first_schedule_protocol, None);
        assert_eq!(summary.first_schedule_primitive, None);
        assert!(summary.preflight_ready);
        assert!(summary.repair_actions_ready);
        assert!(summary.repair_batches_ready);
        assert!(summary.repair_schedule_ready);
        assert!(summary.release_ready);
        assert!(summary.ready_for_release);
        assert!(!summary.has_repair_schedule());
        assert!(!summary.has_blockers());
        assert!(!summary.needs_operator());
    }

    #[test]
    fn mesh_preflight_slot_readiness_summary_surfaces_slot_audit() {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let summary = mesh_preflight_slot_readiness_summary(
            &available_primitives,
            &allowed_capabilities,
            &[],
        );

        assert_eq!(summary.total_protocols, 3);
        assert_eq!(summary.total_preflight_checks, 16);
        assert_eq!(summary.preflight_blocker_count, 5);
        assert_eq!(summary.queued_preflight_actions, 5);
        assert_eq!(summary.repair_slot_count, 3);
        assert_eq!(summary.scheduled_preflight_actions, 5);
        assert_eq!(summary.slot_audit_rows, 3);
        assert_eq!(summary.audited_preflight_actions, 5);
        assert_eq!(summary.blocking_slot_audit_rows, 3);
        assert_eq!(summary.operator_required_slot_audit_rows, 2);
        assert_eq!(summary.first_blocking_slot_sequence, Some(1));
        assert_eq!(summary.first_operator_slot_sequence, Some(1));
        assert_eq!(
            summary.first_slot_stage,
            Some(IntegrationMeshProtocolSubstrateStage::Radio)
        );
        assert_eq!(
            summary.first_slot_action_kind,
            Some(IntegrationMeshProtocolSubstratePreflightActionKind::ProvisionRadio)
        );
        assert!(!summary.preflight_ready);
        assert!(!summary.repair_actions_ready);
        assert!(!summary.repair_batches_ready);
        assert!(!summary.repair_schedule_ready);
        assert!(!summary.repair_slot_audit_ready);
        assert!(!summary.ready_for_release);
        assert!(summary.has_slot_audit_rows());
        assert!(summary.has_blockers());
        assert!(summary.needs_operator());
        assert_eq!(summary.slot_audit_summary.protocol_mentions, 5);
    }

    #[test]
    fn mesh_preflight_slot_readiness_summary_marks_ready_release() {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let summary = mesh_preflight_slot_readiness_summary_for_catalog(
            &catalog,
            all_primitive_families(),
            &allowed_capabilities,
            &[],
        );

        assert_eq!(summary.total_protocols, 3);
        assert_eq!(summary.preflight_blocker_count, 0);
        assert_eq!(summary.release_blocker_count, 0);
        assert_eq!(summary.queued_preflight_actions, 0);
        assert_eq!(summary.repair_slot_count, 0);
        assert_eq!(summary.scheduled_preflight_actions, 0);
        assert_eq!(summary.slot_audit_rows, 0);
        assert_eq!(summary.audited_preflight_actions, 0);
        assert_eq!(summary.blocking_slot_audit_rows, 0);
        assert_eq!(summary.operator_required_slot_audit_rows, 0);
        assert_eq!(summary.first_blocking_slot_sequence, None);
        assert_eq!(summary.first_operator_slot_sequence, None);
        assert_eq!(summary.first_slot_stage, None);
        assert_eq!(summary.first_slot_action_kind, None);
        assert!(summary.preflight_ready);
        assert!(summary.repair_actions_ready);
        assert!(summary.repair_batches_ready);
        assert!(summary.repair_schedule_ready);
        assert!(summary.repair_slot_audit_ready);
        assert!(summary.release_ready);
        assert!(summary.ready_for_release);
        assert!(!summary.has_slot_audit_rows());
        assert!(!summary.has_blockers());
        assert!(!summary.needs_operator());
    }

    #[test]
    fn mesh_preflight_execution_readiness_summary_surfaces_execution_tickets() {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let summary = mesh_preflight_execution_readiness_summary(
            &available_primitives,
            &allowed_capabilities,
            &[],
        );

        assert_eq!(summary.total_protocols, 3);
        assert_eq!(summary.total_preflight_checks, 16);
        assert_eq!(summary.preflight_blocker_count, 5);
        assert_eq!(summary.queued_preflight_actions, 5);
        assert_eq!(summary.repair_slot_count, 3);
        assert_eq!(summary.scheduled_preflight_actions, 5);
        assert_eq!(summary.slot_audit_rows, 3);
        assert_eq!(summary.execution_ticket_count, 3);
        assert_eq!(summary.ticketed_preflight_actions, 5);
        assert_eq!(summary.blocking_execution_tickets, 3);
        assert_eq!(summary.operator_required_execution_tickets, 2);
        assert_eq!(
            summary.first_execution_ticket_key,
            Some("slot-01-radio-provision_radio".to_string())
        );
        assert_eq!(
            summary.first_blocking_execution_ticket_key,
            Some("slot-01-radio-provision_radio".to_string())
        );
        assert_eq!(
            summary.first_operator_execution_ticket_key,
            Some("slot-01-radio-provision_radio".to_string())
        );
        assert_eq!(
            summary.first_execution_ticket_stage,
            Some(IntegrationMeshProtocolSubstrateStage::Radio)
        );
        assert_eq!(
            summary.first_execution_ticket_action_kind,
            Some(IntegrationMeshProtocolSubstratePreflightActionKind::ProvisionRadio)
        );
        assert!(!summary.preflight_ready);
        assert!(!summary.repair_actions_ready);
        assert!(!summary.repair_batches_ready);
        assert!(!summary.repair_schedule_ready);
        assert!(!summary.repair_slot_audit_ready);
        assert!(!summary.execution_tickets_ready);
        assert!(!summary.ready_for_release);
        assert!(summary.has_execution_tickets());
        assert!(summary.has_blockers());
        assert!(summary.needs_operator());
        assert_eq!(
            summary.execution_ticket_summary.first_ticket_key,
            summary.first_execution_ticket_key
        );
    }

    #[test]
    fn mesh_preflight_execution_readiness_summary_marks_ready_release() {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let summary = mesh_preflight_execution_readiness_summary_for_catalog(
            &catalog,
            all_primitive_families(),
            &allowed_capabilities,
            &[],
        );

        assert_eq!(summary.total_protocols, 3);
        assert_eq!(summary.preflight_blocker_count, 0);
        assert_eq!(summary.release_blocker_count, 0);
        assert_eq!(summary.queued_preflight_actions, 0);
        assert_eq!(summary.repair_slot_count, 0);
        assert_eq!(summary.scheduled_preflight_actions, 0);
        assert_eq!(summary.slot_audit_rows, 0);
        assert_eq!(summary.execution_ticket_count, 0);
        assert_eq!(summary.ticketed_preflight_actions, 0);
        assert_eq!(summary.blocking_execution_tickets, 0);
        assert_eq!(summary.operator_required_execution_tickets, 0);
        assert_eq!(summary.first_execution_ticket_key, None);
        assert_eq!(summary.first_blocking_execution_ticket_key, None);
        assert_eq!(summary.first_operator_execution_ticket_key, None);
        assert_eq!(summary.first_execution_ticket_stage, None);
        assert_eq!(summary.first_execution_ticket_action_kind, None);
        assert!(summary.preflight_ready);
        assert!(summary.repair_actions_ready);
        assert!(summary.repair_batches_ready);
        assert!(summary.repair_schedule_ready);
        assert!(summary.repair_slot_audit_ready);
        assert!(summary.execution_tickets_ready);
        assert!(summary.release_ready);
        assert!(summary.ready_for_release);
        assert!(!summary.has_execution_tickets());
        assert!(!summary.has_blockers());
        assert!(!summary.needs_operator());
    }

    #[test]
    fn mesh_preflight_work_order_readiness_summary_surfaces_execution_work_orders() {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let summary = mesh_preflight_work_order_readiness_summary(
            &available_primitives,
            &allowed_capabilities,
            &[],
        );

        assert_eq!(summary.total_protocols, 3);
        assert_eq!(summary.total_preflight_checks, 16);
        assert_eq!(summary.preflight_blocker_count, 5);
        assert_eq!(summary.queued_preflight_actions, 5);
        assert_eq!(summary.repair_slot_count, 3);
        assert_eq!(summary.scheduled_preflight_actions, 5);
        assert_eq!(summary.execution_ticket_count, 3);
        assert_eq!(summary.execution_work_order_count, 3);
        assert_eq!(summary.ordered_preflight_actions, 5);
        assert_eq!(summary.blocking_execution_work_orders, 3);
        assert_eq!(summary.operator_required_execution_work_orders, 2);
        assert_eq!(
            summary.first_execution_work_order_key,
            Some("work-01-radio-provision_radio".to_string())
        );
        assert_eq!(
            summary.first_blocking_execution_work_order_key,
            Some("work-01-radio-provision_radio".to_string())
        );
        assert_eq!(
            summary.first_operator_execution_work_order_key,
            Some("work-01-radio-provision_radio".to_string())
        );
        assert_eq!(
            summary.first_execution_ticket_key,
            Some("slot-01-radio-provision_radio".to_string())
        );
        assert_eq!(
            summary.first_execution_work_order_stage,
            Some(IntegrationMeshProtocolSubstrateStage::Radio)
        );
        assert_eq!(
            summary.first_execution_work_order_action_kind,
            Some(IntegrationMeshProtocolSubstratePreflightActionKind::ProvisionRadio)
        );
        assert!(!summary.preflight_ready);
        assert!(!summary.repair_actions_ready);
        assert!(!summary.repair_batches_ready);
        assert!(!summary.repair_schedule_ready);
        assert!(!summary.repair_slot_audit_ready);
        assert!(!summary.execution_tickets_ready);
        assert!(!summary.execution_work_orders_ready);
        assert!(!summary.ready_for_release);
        assert!(summary.has_execution_work_orders());
        assert!(summary.has_blockers());
        assert!(summary.needs_operator());
        assert_eq!(
            summary.execution_work_order_summary.first_ticket_key,
            summary.first_execution_ticket_key
        );
    }

    #[test]
    fn mesh_preflight_work_order_readiness_summary_marks_ready_release() {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let summary = mesh_preflight_work_order_readiness_summary_for_catalog(
            &catalog,
            all_primitive_families(),
            &allowed_capabilities,
            &[],
        );

        assert_eq!(summary.total_protocols, 3);
        assert_eq!(summary.preflight_blocker_count, 0);
        assert_eq!(summary.release_blocker_count, 0);
        assert_eq!(summary.queued_preflight_actions, 0);
        assert_eq!(summary.repair_slot_count, 0);
        assert_eq!(summary.scheduled_preflight_actions, 0);
        assert_eq!(summary.execution_ticket_count, 0);
        assert_eq!(summary.execution_work_order_count, 0);
        assert_eq!(summary.ordered_preflight_actions, 0);
        assert_eq!(summary.blocking_execution_work_orders, 0);
        assert_eq!(summary.operator_required_execution_work_orders, 0);
        assert_eq!(summary.first_execution_work_order_key, None);
        assert_eq!(summary.first_blocking_execution_work_order_key, None);
        assert_eq!(summary.first_operator_execution_work_order_key, None);
        assert_eq!(summary.first_execution_ticket_key, None);
        assert_eq!(summary.first_execution_work_order_stage, None);
        assert_eq!(summary.first_execution_work_order_action_kind, None);
        assert!(summary.preflight_ready);
        assert!(summary.repair_actions_ready);
        assert!(summary.repair_batches_ready);
        assert!(summary.repair_schedule_ready);
        assert!(summary.repair_slot_audit_ready);
        assert!(summary.execution_tickets_ready);
        assert!(summary.execution_work_orders_ready);
        assert!(summary.release_ready);
        assert!(summary.ready_for_release);
        assert!(!summary.has_execution_work_orders());
        assert!(!summary.has_blockers());
        assert!(!summary.needs_operator());
    }

    #[test]
    fn mesh_preflight_guardrail_readiness_summary_surfaces_guardrails_and_dispositions() {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let summary = mesh_preflight_guardrail_readiness_summary(
            &available_primitives,
            &allowed_capabilities,
            &[],
        );

        assert_eq!(summary.total_protocols, 3);
        assert_eq!(summary.total_preflight_checks, 16);
        assert_eq!(summary.preflight_blocker_count, 5);
        assert_eq!(summary.queued_preflight_actions, 5);
        assert_eq!(summary.repair_slot_count, 3);
        assert_eq!(summary.scheduled_preflight_actions, 5);
        assert_eq!(summary.execution_ticket_count, 3);
        assert_eq!(summary.execution_work_order_count, 3);
        assert_eq!(summary.work_order_guardrail_count, 3);
        assert_eq!(summary.release_blocker_guardrails, 1);
        assert_eq!(summary.operator_handoff_guardrails, 2);
        assert_eq!(summary.ready_to_execute_guardrails, 0);
        assert_eq!(summary.evidence_review_dispositions, 3);
        assert_eq!(summary.blocking_dispositions, 3);
        assert_eq!(summary.operator_handoff_dispositions, 2);
        assert_eq!(summary.repair_required_dispositions, 1);
        assert_eq!(summary.lineage_gap_dispositions, 0);
        assert_eq!(summary.review_required_dispositions, 3);
        assert_eq!(
            summary.first_guardrail_work_order_key,
            Some("work-01-radio-provision_radio".to_string())
        );
        assert_eq!(
            summary.first_release_blocker_work_order_key,
            Some("work-02-discovery-enable_discovery".to_string())
        );
        assert_eq!(
            summary.first_operator_handoff_work_order_key,
            Some("work-01-radio-provision_radio".to_string())
        );
        assert_eq!(
            summary.first_disposition_key,
            Some("disposition-01-radio-provision_radio".to_string())
        );
        assert_eq!(
            summary.first_operator_handoff_disposition_key,
            Some("disposition-01-radio-provision_radio".to_string())
        );
        assert_eq!(
            summary.first_repair_required_disposition_key,
            Some("disposition-02-discovery-enable_discovery".to_string())
        );
        assert_eq!(summary.first_lineage_gap_disposition_key, None);
        assert_eq!(
            summary.first_guardrail_stage,
            Some(IntegrationMeshProtocolSubstrateStage::Radio)
        );
        assert_eq!(
            summary.first_guardrail_action_kind,
            Some(IntegrationMeshProtocolSubstratePreflightActionKind::ProvisionRadio)
        );
        assert!(!summary.preflight_ready);
        assert!(!summary.repair_actions_ready);
        assert!(!summary.repair_batches_ready);
        assert!(!summary.repair_schedule_ready);
        assert!(!summary.repair_slot_audit_ready);
        assert!(!summary.execution_tickets_ready);
        assert!(!summary.execution_work_orders_ready);
        assert!(!summary.execution_work_order_guardrails_ready);
        assert!(!summary.execution_evidence_review_dispositions_ready);
        assert!(!summary.ready_for_release);
        assert!(summary.has_guardrails());
        assert!(summary.has_dispositions());
        assert!(summary.has_blockers());
        assert!(summary.needs_operator());
        assert!(summary.needs_review());
        assert!(summary.needs_repair());
        assert!(!summary.has_lineage_gaps());
    }

    #[test]
    fn mesh_preflight_guardrail_readiness_summary_marks_ready_release() {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let summary = mesh_preflight_guardrail_readiness_summary_for_catalog(
            &catalog,
            all_primitive_families(),
            &allowed_capabilities,
            &[],
        );

        assert_eq!(summary.total_protocols, 3);
        assert_eq!(summary.preflight_blocker_count, 0);
        assert_eq!(summary.release_blocker_count, 0);
        assert_eq!(summary.queued_preflight_actions, 0);
        assert_eq!(summary.repair_slot_count, 0);
        assert_eq!(summary.scheduled_preflight_actions, 0);
        assert_eq!(summary.execution_ticket_count, 0);
        assert_eq!(summary.execution_work_order_count, 0);
        assert_eq!(summary.work_order_guardrail_count, 0);
        assert_eq!(summary.release_blocker_guardrails, 0);
        assert_eq!(summary.operator_handoff_guardrails, 0);
        assert_eq!(summary.ready_to_execute_guardrails, 0);
        assert_eq!(summary.evidence_review_dispositions, 0);
        assert_eq!(summary.blocking_dispositions, 0);
        assert_eq!(summary.operator_handoff_dispositions, 0);
        assert_eq!(summary.repair_required_dispositions, 0);
        assert_eq!(summary.lineage_gap_dispositions, 0);
        assert_eq!(summary.review_required_dispositions, 0);
        assert_eq!(summary.first_guardrail_work_order_key, None);
        assert_eq!(summary.first_release_blocker_work_order_key, None);
        assert_eq!(summary.first_operator_handoff_work_order_key, None);
        assert_eq!(summary.first_disposition_key, None);
        assert_eq!(summary.first_operator_handoff_disposition_key, None);
        assert_eq!(summary.first_repair_required_disposition_key, None);
        assert_eq!(summary.first_lineage_gap_disposition_key, None);
        assert_eq!(summary.first_guardrail_stage, None);
        assert_eq!(summary.first_guardrail_action_kind, None);
        assert!(summary.preflight_ready);
        assert!(summary.repair_actions_ready);
        assert!(summary.repair_batches_ready);
        assert!(summary.repair_schedule_ready);
        assert!(summary.repair_slot_audit_ready);
        assert!(summary.execution_tickets_ready);
        assert!(summary.execution_work_orders_ready);
        assert!(summary.execution_work_order_guardrails_ready);
        assert!(summary.execution_evidence_review_dispositions_ready);
        assert!(summary.release_ready);
        assert!(summary.ready_for_release);
        assert!(!summary.has_guardrails());
        assert!(!summary.has_dispositions());
        assert!(!summary.has_blockers());
        assert!(!summary.needs_operator());
        assert!(!summary.needs_review());
        assert!(!summary.needs_repair());
        assert!(!summary.has_lineage_gaps());
    }

    #[test]
    fn mesh_preflight_disposition_action_readiness_summary_surfaces_action_queue() {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let summary = mesh_preflight_disposition_action_readiness_summary(
            &available_primitives,
            &allowed_capabilities,
            &[],
        );

        assert_eq!(summary.total_protocols, 3);
        assert_eq!(summary.total_preflight_checks, 16);
        assert_eq!(summary.preflight_blocker_count, 5);
        assert_eq!(summary.queued_preflight_actions, 5);
        assert_eq!(summary.repair_slot_count, 3);
        assert_eq!(summary.execution_ticket_count, 3);
        assert_eq!(summary.execution_work_order_count, 3);
        assert_eq!(summary.work_order_guardrail_count, 3);
        assert_eq!(summary.evidence_review_dispositions, 3);
        assert_eq!(summary.disposition_action_count, 3);
        assert_eq!(summary.required_disposition_actions, 3);
        assert_eq!(summary.blocking_disposition_actions, 3);
        assert_eq!(summary.operator_handoff_actions, 2);
        assert_eq!(summary.repair_actions, 1);
        assert_eq!(summary.lineage_gap_actions, 0);
        assert_eq!(summary.release_execution_actions, 0);
        assert_eq!(summary.review_required_actions, 3);
        assert_eq!(summary.lineage_complete_actions, 3);
        assert_eq!(
            summary.first_disposition_action_key,
            Some("disposition-action-01-radio-provision_radio".to_string())
        );
        assert_eq!(
            summary.first_required_disposition_action_key,
            Some("disposition-action-01-radio-provision_radio".to_string())
        );
        assert_eq!(
            summary.first_operator_handoff_action_key,
            Some("disposition-action-01-radio-provision_radio".to_string())
        );
        assert_eq!(
            summary.first_repair_action_key,
            Some("disposition-action-02-discovery-enable_discovery".to_string())
        );
        assert_eq!(summary.first_lineage_gap_action_key, None);
        assert_eq!(summary.first_release_execution_action_key, None);
        assert_eq!(
            summary.first_disposition_key,
            Some("disposition-01-radio-provision_radio".to_string())
        );
        assert_eq!(
            summary.first_work_order_key,
            Some("work-01-radio-provision_radio".to_string())
        );
        assert_eq!(
            summary.first_ticket_key,
            Some("slot-01-radio-provision_radio".to_string())
        );
        assert_eq!(
            summary.first_action_stage,
            Some(IntegrationMeshProtocolSubstrateStage::Radio)
        );
        assert_eq!(
            summary.first_action_kind,
            Some(IntegrationMeshProtocolSubstratePreflightActionKind::ProvisionRadio)
        );
        assert!(!summary.preflight_ready);
        assert!(!summary.repair_actions_ready);
        assert!(!summary.repair_batches_ready);
        assert!(!summary.repair_schedule_ready);
        assert!(!summary.repair_slot_audit_ready);
        assert!(!summary.execution_tickets_ready);
        assert!(!summary.execution_work_orders_ready);
        assert!(!summary.execution_work_order_guardrails_ready);
        assert!(!summary.execution_evidence_review_dispositions_ready);
        assert!(!summary.execution_evidence_review_disposition_actions_ready);
        assert!(!summary.ready_for_release);
        assert!(summary.has_disposition_actions());
        assert!(summary.has_required_actions());
        assert!(summary.has_blockers());
        assert!(summary.needs_operator());
        assert!(summary.needs_review());
        assert!(summary.needs_repair());
        assert!(!summary.has_lineage_gaps());
    }

    #[test]
    fn mesh_preflight_disposition_action_readiness_summary_marks_ready_release() {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let summary = mesh_preflight_disposition_action_readiness_summary_for_catalog(
            &catalog,
            all_primitive_families(),
            &allowed_capabilities,
            &[],
        );

        assert_eq!(summary.total_protocols, 3);
        assert_eq!(summary.preflight_blocker_count, 0);
        assert_eq!(summary.release_blocker_count, 0);
        assert_eq!(summary.queued_preflight_actions, 0);
        assert_eq!(summary.repair_slot_count, 0);
        assert_eq!(summary.execution_ticket_count, 0);
        assert_eq!(summary.execution_work_order_count, 0);
        assert_eq!(summary.work_order_guardrail_count, 0);
        assert_eq!(summary.evidence_review_dispositions, 0);
        assert_eq!(summary.disposition_action_count, 0);
        assert_eq!(summary.required_disposition_actions, 0);
        assert_eq!(summary.blocking_disposition_actions, 0);
        assert_eq!(summary.operator_handoff_actions, 0);
        assert_eq!(summary.repair_actions, 0);
        assert_eq!(summary.lineage_gap_actions, 0);
        assert_eq!(summary.release_execution_actions, 0);
        assert_eq!(summary.review_required_actions, 0);
        assert_eq!(summary.lineage_complete_actions, 0);
        assert_eq!(summary.first_disposition_action_key, None);
        assert_eq!(summary.first_required_disposition_action_key, None);
        assert_eq!(summary.first_operator_handoff_action_key, None);
        assert_eq!(summary.first_repair_action_key, None);
        assert_eq!(summary.first_lineage_gap_action_key, None);
        assert_eq!(summary.first_release_execution_action_key, None);
        assert_eq!(summary.first_disposition_key, None);
        assert_eq!(summary.first_work_order_key, None);
        assert_eq!(summary.first_ticket_key, None);
        assert_eq!(summary.first_action_stage, None);
        assert_eq!(summary.first_action_kind, None);
        assert!(summary.preflight_ready);
        assert!(summary.repair_actions_ready);
        assert!(summary.repair_batches_ready);
        assert!(summary.repair_schedule_ready);
        assert!(summary.repair_slot_audit_ready);
        assert!(summary.execution_tickets_ready);
        assert!(summary.execution_work_orders_ready);
        assert!(summary.execution_work_order_guardrails_ready);
        assert!(summary.execution_evidence_review_dispositions_ready);
        assert!(summary.execution_evidence_review_disposition_actions_ready);
        assert!(summary.release_ready);
        assert!(summary.ready_for_release);
        assert!(!summary.has_disposition_actions());
        assert!(!summary.has_required_actions());
        assert!(!summary.has_blockers());
        assert!(!summary.needs_operator());
        assert!(!summary.needs_review());
        assert!(!summary.needs_repair());
        assert!(!summary.has_lineage_gaps());
    }

    #[test]
    fn mesh_readiness_handoff_packages_bundle_actions_and_remediation() {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let packages =
            mesh_readiness_handoff_packages(&available_primitives, &allowed_capabilities, &[]);
        let summary =
            mesh_readiness_handoff_summary(&available_primitives, &allowed_capabilities, &[]);

        assert_eq!(packages.len(), 6);
        assert_eq!(summary.total_packages, 6);
        assert_eq!(summary.action_packages, 5);
        assert_eq!(summary.remediation_packages, 1);
        assert_eq!(summary.ready_packages, 0);
        assert_eq!(summary.blocked_packages, 5);
        assert_eq!(summary.review_required_packages, 1);
        assert_eq!(summary.queued_substrate_actions, 5);
        assert_eq!(summary.queued_stage_count, 3);
        assert_eq!(summary.queued_primitive_count, 3);
        assert!(summary.remediation_item_count > 0);
        assert_eq!(summary.stage_blocker_count, 5);
        assert_eq!(summary.primitive_blocker_count, 3);
        assert_eq!(
            summary.next_handoff_status,
            Some(IntegrationMeshReadinessHandoffStatus::Blocked)
        );
        assert_eq!(
            summary.next_package_kind,
            Some(IntegrationMeshReadinessHandoffKind::SubstrateAction)
        );
        assert_eq!(summary.first_action_sequence, Some(1));
        assert_eq!(summary.first_remediation_sequence, Some(6));
        assert_eq!(summary.first_blocked_sequence, Some(1));
        assert_eq!(summary.first_action_protocol, Some(ProtocolFamily::ZWave));
        assert_eq!(
            summary.first_action_stage,
            Some(IntegrationMeshProtocolSubstrateStage::Radio)
        );
        assert_eq!(
            summary.first_action_primitive,
            Some(PrimitiveFamily::ZWaveSerialApi)
        );
        assert!(!summary.substrate_actions_ready);
        assert!(!summary.release_ready);
        assert!(!summary.ready_for_handoff());
        assert!(summary.has_blockers());
        assert!(summary.requires_attention());

        let first = &packages[0];
        assert_eq!(first.sequence, 1);
        assert_eq!(
            first.package_kind,
            IntegrationMeshReadinessHandoffKind::SubstrateAction
        );
        assert_eq!(
            first.handoff_status,
            IntegrationMeshReadinessHandoffStatus::Blocked
        );
        assert_eq!(first.action_sequence, Some(1));
        assert_eq!(
            first.stage,
            Some(IntegrationMeshProtocolSubstrateStage::Radio)
        );
        assert_eq!(first.primitive, Some(PrimitiveFamily::ZWaveSerialApi));
        assert!(first.blocked());
        assert!(first.operator_required());

        let remediation = packages.last().unwrap();
        assert_eq!(
            remediation.package_kind,
            IntegrationMeshReadinessHandoffKind::EvidenceRemediation
        );
        assert!(remediation.remediation_kind.is_some());
        assert!(remediation.requires_attention());
    }

    #[test]
    fn mesh_readiness_handoff_packages_mark_release_ready() {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let packages = mesh_readiness_handoff_packages_for_catalog(
            &catalog,
            all_primitive_families(),
            &allowed_capabilities,
            &[],
        );
        let summary = mesh_readiness_handoff_summary_for_catalog(
            &catalog,
            all_primitive_families(),
            &allowed_capabilities,
            &[],
        );

        assert_eq!(packages.len(), 1);
        assert_eq!(summary.total_packages, 1);
        assert_eq!(summary.action_packages, 0);
        assert_eq!(summary.remediation_packages, 0);
        assert_eq!(summary.ready_packages, 1);
        assert_eq!(summary.blocked_packages, 0);
        assert_eq!(summary.queued_substrate_actions, 0);
        assert_eq!(summary.stage_blocker_count, 0);
        assert!(summary.substrate_actions_ready);
        assert!(summary.release_ready);
        assert!(summary.ready_for_handoff());
        assert!(!summary.has_blockers());
        assert!(!summary.requires_attention());

        let package = &packages[0];
        assert_eq!(
            package.package_kind,
            IntegrationMeshReadinessHandoffKind::ReleaseReady
        );
        assert_eq!(
            package.handoff_status,
            IntegrationMeshReadinessHandoffStatus::Ready
        );
        assert!(package.ready_for_handoff());
        assert!(!package.operator_required());
    }

    #[test]
    fn mesh_release_readiness_checks_surface_blocked_release_gates() {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let checks =
            mesh_release_readiness_checks(&available_primitives, &allowed_capabilities, &[]);
        let summary =
            mesh_release_readiness_check_summary(&available_primitives, &allowed_capabilities, &[]);

        assert_eq!(checks.len(), 5);
        assert_eq!(summary.total_checks, 5);
        assert_eq!(summary.ready_checks, 0);
        assert_eq!(summary.blocked_checks, 3);
        assert_eq!(summary.review_required_checks, 2);
        assert_eq!(summary.operator_required_checks, 4);
        assert_eq!(summary.checks_requiring_attention, 5);
        assert_eq!(summary.next_check_sequence, Some(1));
        assert_eq!(
            summary.next_check_kind,
            Some(IntegrationMeshReleaseReadinessCheckKind::SubstrateActions)
        );
        assert_eq!(
            summary.next_check_status,
            Some(IntegrationMeshReleaseReadinessStatus::Blocked)
        );
        assert_eq!(summary.first_blocked_check_sequence, Some(1));
        assert_eq!(summary.first_review_check_sequence, Some(2));
        assert_eq!(summary.first_operator_check_sequence, Some(1));
        assert_eq!(
            summary.next_package_kind,
            Some(IntegrationMeshReadinessHandoffKind::SubstrateAction)
        );
        assert_eq!(
            summary.next_handoff_status,
            Some(IntegrationMeshReadinessHandoffStatus::Blocked)
        );
        assert_eq!(summary.queued_substrate_actions, 5);
        assert_eq!(summary.remediation_item_count, 2);
        assert!(!summary.substrate_actions_ready);
        assert!(!summary.release_ready);
        assert!(!summary.ready_for_handoff);
        assert!(!summary.release_packet_ready);
        assert!(summary.has_blockers());
        assert!(summary.has_review_work());
        assert!(summary.requires_attention());

        let substrate = &checks[0];
        assert_eq!(
            substrate.check_kind,
            IntegrationMeshReleaseReadinessCheckKind::SubstrateActions
        );
        assert_eq!(
            substrate.status,
            IntegrationMeshReleaseReadinessStatus::Blocked
        );
        assert_eq!(
            substrate.package_kind,
            Some(IntegrationMeshReadinessHandoffKind::SubstrateAction)
        );
        assert_eq!(
            substrate.handoff_status,
            Some(IntegrationMeshReadinessHandoffStatus::Blocked)
        );
        assert!(substrate.blocked());
        assert!(substrate.operator_required());
        assert!(substrate.requires_attention());

        let evidence = &checks[1];
        assert_eq!(
            evidence.check_kind,
            IntegrationMeshReleaseReadinessCheckKind::EvidenceRemediation
        );
        assert_eq!(
            evidence.status,
            IntegrationMeshReleaseReadinessStatus::NeedsReview
        );
        assert!(evidence.review_required());
        assert!(evidence.operator_required());

        let release_packet = &checks[4];
        assert_eq!(
            release_packet.check_kind,
            IntegrationMeshReleaseReadinessCheckKind::ReleasePacket
        );
        assert_eq!(
            release_packet.status,
            IntegrationMeshReleaseReadinessStatus::Blocked
        );
        assert_eq!(
            release_packet.package_kind,
            Some(IntegrationMeshReadinessHandoffKind::ReleaseReady)
        );
    }

    #[test]
    fn mesh_release_readiness_checks_mark_release_packet_ready() {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let checks = mesh_release_readiness_checks_for_catalog(
            &catalog,
            all_primitive_families(),
            &allowed_capabilities,
            &[],
        );
        let summary = mesh_release_readiness_check_summary_for_catalog(
            &catalog,
            all_primitive_families(),
            &allowed_capabilities,
            &[],
        );

        assert_eq!(checks.len(), 5);
        assert_eq!(summary.total_checks, 5);
        assert_eq!(summary.ready_checks, 5);
        assert_eq!(summary.blocked_checks, 0);
        assert_eq!(summary.review_required_checks, 0);
        assert_eq!(summary.operator_required_checks, 0);
        assert_eq!(summary.checks_requiring_attention, 0);
        assert_eq!(summary.next_check_kind, None);
        assert_eq!(summary.first_blocked_check_sequence, None);
        assert_eq!(
            summary.next_package_kind,
            Some(IntegrationMeshReadinessHandoffKind::ReleaseReady)
        );
        assert_eq!(
            summary.next_handoff_status,
            Some(IntegrationMeshReadinessHandoffStatus::Ready)
        );
        assert_eq!(summary.queued_substrate_actions, 0);
        assert_eq!(summary.remediation_item_count, 0);
        assert!(summary.substrate_actions_ready);
        assert!(summary.release_ready);
        assert!(summary.ready_for_handoff);
        assert!(summary.release_packet_ready);
        assert!(!summary.has_blockers());
        assert!(!summary.has_review_work());
        assert!(!summary.requires_attention());
        assert!(checks
            .iter()
            .all(IntegrationMeshReleaseReadinessCheck::ready));
        assert_eq!(
            checks[4].check_kind,
            IntegrationMeshReleaseReadinessCheckKind::ReleasePacket
        );
        assert_eq!(
            checks[4].status,
            IntegrationMeshReleaseReadinessStatus::Ready
        );
    }

    #[test]
    fn mesh_release_readiness_check_slots_sequence_release_gates() {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let slots =
            mesh_release_readiness_check_slots(&available_primitives, &allowed_capabilities, &[]);
        let summary = mesh_release_readiness_check_slot_summary(
            &available_primitives,
            &allowed_capabilities,
            &[],
        );

        assert_eq!(slots.len(), 5);
        assert_eq!(summary.total_slots, 5);
        assert_eq!(summary.ready_slots, 0);
        assert_eq!(summary.blocked_slots, 3);
        assert_eq!(summary.review_required_slots, 2);
        assert_eq!(summary.operator_required_slots, 4);
        assert_eq!(summary.slots_requiring_attention, 5);
        assert_eq!(summary.queued_substrate_actions, 5);
        assert_eq!(summary.remediation_item_count, 2);
        assert_eq!(summary.review_required_packages, 1);
        assert_eq!(summary.operator_required_packages, 6);
        assert_eq!(summary.blocked_packages, 5);
        assert_eq!(
            summary.first_slot_key,
            Some("release-check-slot-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.first_blocked_slot_key,
            Some("release-check-slot-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.first_review_slot_key,
            Some("release-check-slot-02-evidence_remediation".to_string())
        );
        assert_eq!(
            summary.first_operator_slot_key,
            Some("release-check-slot-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_slot_key,
            Some("release-check-slot-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_check_kind,
            Some(IntegrationMeshReleaseReadinessCheckKind::SubstrateActions)
        );
        assert_eq!(
            summary.next_check_status,
            Some(IntegrationMeshReleaseReadinessStatus::Blocked)
        );
        assert_eq!(
            summary.next_package_kind,
            Some(IntegrationMeshReadinessHandoffKind::SubstrateAction)
        );
        assert_eq!(
            summary.next_handoff_status,
            Some(IntegrationMeshReadinessHandoffStatus::Blocked)
        );
        assert!(!summary.release_check_slots_ready);
        assert!(summary.has_slots());
        assert!(summary.has_blockers());
        assert!(summary.has_review_work());
        assert!(summary.needs_operator());
        assert!(summary.requires_attention());

        let first = &slots[0];
        assert_eq!(
            first.check_kind,
            IntegrationMeshReleaseReadinessCheckKind::SubstrateActions
        );
        assert_eq!(first.status, IntegrationMeshReleaseReadinessStatus::Blocked);
        assert_eq!(
            first.package_kind,
            Some(IntegrationMeshReadinessHandoffKind::SubstrateAction)
        );
        assert!(first.blocked());
        assert!(first.operator_required());
        assert!(first.requires_attention());
    }

    #[test]
    fn mesh_release_readiness_check_slots_mark_release_ready() {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let slots = mesh_release_readiness_check_slots_for_catalog(
            &catalog,
            all_primitive_families(),
            &allowed_capabilities,
            &[],
        );
        let summary = mesh_release_readiness_check_slot_summary_for_catalog(
            &catalog,
            all_primitive_families(),
            &allowed_capabilities,
            &[],
        );

        assert_eq!(slots.len(), 5);
        assert_eq!(summary.total_slots, 5);
        assert_eq!(summary.ready_slots, 5);
        assert_eq!(summary.blocked_slots, 0);
        assert_eq!(summary.review_required_slots, 0);
        assert_eq!(summary.operator_required_slots, 0);
        assert_eq!(summary.slots_requiring_attention, 0);
        assert_eq!(summary.next_slot_key, None);
        assert_eq!(summary.next_check_kind, None);
        assert_eq!(summary.next_check_status, None);
        assert_eq!(summary.next_package_kind, None);
        assert_eq!(summary.next_handoff_status, None);
        assert!(summary.release_check_slots_ready);
        assert!(summary.has_slots());
        assert!(!summary.has_blockers());
        assert!(!summary.has_review_work());
        assert!(!summary.needs_operator());
        assert!(!summary.requires_attention());
        assert!(slots
            .iter()
            .all(IntegrationMeshReleaseReadinessCheckSlot::ready));
    }

    #[test]
    fn mesh_release_packet_readiness_summary_surfaces_first_blocked_gate() {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let summary = mesh_release_packet_readiness_summary(
            &available_primitives,
            &allowed_capabilities,
            &[],
        );

        assert_eq!(summary.total_checks, 5);
        assert_eq!(summary.ready_checks, 0);
        assert_eq!(summary.blocked_checks, 3);
        assert_eq!(summary.review_required_checks, 2);
        assert_eq!(summary.operator_required_checks, 4);
        assert_eq!(summary.checks_requiring_attention, 5);
        assert_eq!(summary.queued_substrate_actions, 5);
        assert_eq!(summary.remediation_item_count, 2);
        assert_eq!(summary.review_required_packages, 1);
        assert_eq!(summary.operator_required_packages, 6);
        assert_eq!(summary.blocked_packages, 5);
        assert_eq!(summary.first_blocked_check_sequence, Some(1));
        assert_eq!(summary.first_review_check_sequence, Some(2));
        assert_eq!(summary.first_operator_check_sequence, Some(1));
        assert_eq!(summary.next_check_sequence, Some(1));
        assert_eq!(
            summary.next_check_kind,
            Some(IntegrationMeshReleaseReadinessCheckKind::SubstrateActions)
        );
        assert_eq!(
            summary.next_check_status,
            Some(IntegrationMeshReleaseReadinessStatus::Blocked)
        );
        assert_eq!(
            summary.next_package_kind,
            Some(IntegrationMeshReadinessHandoffKind::SubstrateAction)
        );
        assert_eq!(
            summary.next_handoff_status,
            Some(IntegrationMeshReadinessHandoffStatus::Blocked)
        );
        assert_eq!(
            summary.packet_status,
            IntegrationMeshReleaseReadinessStatus::Blocked
        );
        assert!(!summary.substrate_actions_ready);
        assert!(!summary.release_ready);
        assert!(!summary.ready_for_handoff);
        assert!(!summary.release_packet_ready);
        assert!(!summary.ready_for_release());
        assert!(summary.has_blockers());
        assert!(summary.needs_review());
        assert!(summary.needs_operator());
        assert!(summary.requires_attention());
    }

    #[test]
    fn mesh_release_packet_readiness_summary_marks_release_ready() {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let summary = mesh_release_packet_readiness_summary_for_catalog(
            &catalog,
            all_primitive_families(),
            &allowed_capabilities,
            &[],
        );

        assert_eq!(summary.total_checks, 5);
        assert_eq!(summary.ready_checks, 5);
        assert_eq!(summary.blocked_checks, 0);
        assert_eq!(summary.review_required_checks, 0);
        assert_eq!(summary.operator_required_checks, 0);
        assert_eq!(summary.checks_requiring_attention, 0);
        assert_eq!(summary.next_check_kind, None);
        assert_eq!(summary.first_blocked_check_sequence, None);
        assert_eq!(summary.first_review_check_sequence, None);
        assert_eq!(summary.first_operator_check_sequence, None);
        assert_eq!(
            summary.next_package_kind,
            Some(IntegrationMeshReadinessHandoffKind::ReleaseReady)
        );
        assert_eq!(
            summary.next_handoff_status,
            Some(IntegrationMeshReadinessHandoffStatus::Ready)
        );
        assert_eq!(
            summary.packet_status,
            IntegrationMeshReleaseReadinessStatus::Ready
        );
        assert!(summary.substrate_actions_ready);
        assert!(summary.release_ready);
        assert!(summary.ready_for_handoff);
        assert!(summary.release_packet_ready);
        assert!(summary.ready_for_release());
        assert!(!summary.has_blockers());
        assert!(!summary.needs_review());
        assert!(!summary.needs_operator());
        assert!(!summary.requires_attention());
    }

    #[test]
    fn mesh_release_execution_readiness_summary_surfaces_slot_blockers() {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let summary = mesh_release_execution_readiness_summary(
            &available_primitives,
            &allowed_capabilities,
            &[],
        );

        assert_eq!(summary.total_checks, 5);
        assert_eq!(summary.ready_checks, 0);
        assert_eq!(summary.blocked_checks, 3);
        assert_eq!(summary.review_required_checks, 2);
        assert_eq!(summary.operator_required_checks, 4);
        assert_eq!(summary.checks_requiring_attention, 5);
        assert_eq!(summary.total_slots, 5);
        assert_eq!(summary.ready_slots, 0);
        assert_eq!(summary.blocked_slots, 3);
        assert_eq!(summary.review_required_slots, 2);
        assert_eq!(summary.operator_required_slots, 4);
        assert_eq!(summary.slots_requiring_attention, 5);
        assert_eq!(summary.queued_substrate_actions, 5);
        assert_eq!(summary.remediation_item_count, 2);
        assert_eq!(summary.review_required_packages, 1);
        assert_eq!(summary.operator_required_packages, 6);
        assert_eq!(summary.blocked_packages, 5);
        assert_eq!(summary.next_check_sequence, Some(1));
        assert_eq!(
            summary.next_check_kind,
            Some(IntegrationMeshReleaseReadinessCheckKind::SubstrateActions)
        );
        assert_eq!(
            summary.next_check_status,
            Some(IntegrationMeshReleaseReadinessStatus::Blocked)
        );
        assert_eq!(
            summary.next_package_kind,
            Some(IntegrationMeshReadinessHandoffKind::SubstrateAction)
        );
        assert_eq!(
            summary.next_handoff_status,
            Some(IntegrationMeshReadinessHandoffStatus::Blocked)
        );
        assert_eq!(
            summary.next_slot_key,
            Some("release-check-slot-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.first_blocked_slot_key,
            Some("release-check-slot-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.first_review_slot_key,
            Some("release-check-slot-02-evidence_remediation".to_string())
        );
        assert_eq!(
            summary.first_operator_slot_key,
            Some("release-check-slot-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.packet_status,
            IntegrationMeshReleaseReadinessStatus::Blocked
        );
        assert_eq!(
            summary.execution_status,
            IntegrationMeshReleaseReadinessStatus::Blocked
        );
        assert!(!summary.release_packet_ready);
        assert!(!summary.release_check_slots_ready);
        assert!(!summary.release_execution_ready);
        assert!(!summary.ready_for_execution());
        assert!(summary.has_release_slots());
        assert!(summary.has_blockers());
        assert!(summary.needs_review());
        assert!(summary.needs_operator());
        assert!(summary.requires_attention());
    }

    #[test]
    fn mesh_release_execution_readiness_summary_marks_execution_ready() {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let summary = mesh_release_execution_readiness_summary_for_catalog(
            &catalog,
            all_primitive_families(),
            &allowed_capabilities,
            &[],
        );

        assert_eq!(summary.total_checks, 5);
        assert_eq!(summary.ready_checks, 5);
        assert_eq!(summary.blocked_checks, 0);
        assert_eq!(summary.review_required_checks, 0);
        assert_eq!(summary.operator_required_checks, 0);
        assert_eq!(summary.checks_requiring_attention, 0);
        assert_eq!(summary.total_slots, 5);
        assert_eq!(summary.ready_slots, 5);
        assert_eq!(summary.blocked_slots, 0);
        assert_eq!(summary.review_required_slots, 0);
        assert_eq!(summary.operator_required_slots, 0);
        assert_eq!(summary.slots_requiring_attention, 0);
        assert_eq!(summary.next_slot_key, None);
        assert_eq!(summary.next_check_kind, None);
        assert_eq!(summary.next_check_status, None);
        assert_eq!(
            summary.next_package_kind,
            Some(IntegrationMeshReadinessHandoffKind::ReleaseReady)
        );
        assert_eq!(
            summary.next_handoff_status,
            Some(IntegrationMeshReadinessHandoffStatus::Ready)
        );
        assert_eq!(
            summary.packet_status,
            IntegrationMeshReleaseReadinessStatus::Ready
        );
        assert_eq!(
            summary.execution_status,
            IntegrationMeshReleaseReadinessStatus::Ready
        );
        assert!(summary.release_packet_ready);
        assert!(summary.release_check_slots_ready);
        assert!(summary.release_execution_ready);
        assert!(summary.ready_for_execution());
        assert!(summary.has_release_slots());
        assert!(!summary.has_blockers());
        assert!(!summary.needs_review());
        assert!(!summary.needs_operator());
        assert!(!summary.requires_attention());
    }

    #[test]
    fn mesh_release_execution_tasks_surface_next_blocked_task() {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let tasks = mesh_release_execution_tasks(&available_primitives, &allowed_capabilities, &[]);
        let summary =
            mesh_release_execution_task_summary(&available_primitives, &allowed_capabilities, &[]);

        assert_eq!(tasks.len(), 5);
        assert_eq!(summary.total_tasks, 5);
        assert_eq!(summary.ready_tasks, 0);
        assert_eq!(summary.blocked_tasks, 3);
        assert_eq!(summary.review_required_tasks, 2);
        assert_eq!(summary.operator_required_tasks, 4);
        assert_eq!(summary.tasks_requiring_attention, 5);
        assert_eq!(summary.queued_substrate_actions, 5);
        assert_eq!(summary.remediation_item_count, 2);
        assert_eq!(summary.review_required_packages, 1);
        assert_eq!(summary.operator_required_packages, 6);
        assert_eq!(summary.blocked_packages, 5);
        assert_eq!(
            summary.first_task_key,
            Some("release-execution-task-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_task_key,
            Some("release-execution-task-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_slot_key,
            Some("release-check-slot-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_check_kind,
            Some(IntegrationMeshReleaseReadinessCheckKind::SubstrateActions)
        );
        assert_eq!(
            summary.next_check_status,
            Some(IntegrationMeshReleaseReadinessStatus::Blocked)
        );
        assert_eq!(
            summary.next_package_kind,
            Some(IntegrationMeshReadinessHandoffKind::SubstrateAction)
        );
        assert_eq!(
            summary.next_handoff_status,
            Some(IntegrationMeshReadinessHandoffStatus::Blocked)
        );
        assert_eq!(
            summary.packet_status,
            IntegrationMeshReleaseReadinessStatus::Blocked
        );
        assert_eq!(
            summary.execution_status,
            IntegrationMeshReleaseReadinessStatus::Blocked
        );
        assert!(!summary.release_execution_ready);
        assert!(!summary.ready_for_execution());
        assert!(summary.has_tasks());
        assert!(summary.has_blockers());
        assert!(summary.has_review_work());
        assert!(summary.needs_operator());
        assert!(summary.requires_attention());

        let first = &tasks[0];
        assert_eq!(
            first.task_key,
            "release-execution-task-01-substrate_actions"
        );
        assert_eq!(first.slot_key, "release-check-slot-01-substrate_actions");
        assert_eq!(
            first.check_kind,
            IntegrationMeshReleaseReadinessCheckKind::SubstrateActions
        );
        assert_eq!(first.status, IntegrationMeshReleaseReadinessStatus::Blocked);
        assert!(first.blocked());
        assert!(first.operator_required());
        assert!(first.requires_attention());
    }

    #[test]
    fn mesh_release_execution_tasks_mark_all_tasks_ready() {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let tasks = mesh_release_execution_tasks_for_catalog(
            &catalog,
            all_primitive_families(),
            &allowed_capabilities,
            &[],
        );
        let summary = mesh_release_execution_task_summary_for_catalog(
            &catalog,
            all_primitive_families(),
            &allowed_capabilities,
            &[],
        );

        assert_eq!(tasks.len(), 5);
        assert_eq!(summary.total_tasks, 5);
        assert_eq!(summary.ready_tasks, 5);
        assert_eq!(summary.blocked_tasks, 0);
        assert_eq!(summary.review_required_tasks, 0);
        assert_eq!(summary.operator_required_tasks, 0);
        assert_eq!(summary.tasks_requiring_attention, 0);
        assert_eq!(
            summary.first_task_key,
            Some("release-execution-task-01-substrate_actions".to_string())
        );
        assert_eq!(summary.next_task_key, None);
        assert_eq!(summary.next_slot_key, None);
        assert_eq!(summary.next_check_kind, None);
        assert_eq!(summary.next_check_status, None);
        assert_eq!(
            summary.packet_status,
            IntegrationMeshReleaseReadinessStatus::Ready
        );
        assert_eq!(
            summary.execution_status,
            IntegrationMeshReleaseReadinessStatus::Ready
        );
        assert!(summary.release_execution_ready);
        assert!(summary.ready_for_execution());
        assert!(summary.has_tasks());
        assert!(!summary.has_blockers());
        assert!(!summary.has_review_work());
        assert!(!summary.needs_operator());
        assert!(!summary.requires_attention());
        assert!(tasks.iter().all(IntegrationMeshReleaseExecutionTask::ready));
    }

    #[test]
    fn mesh_release_task_readiness_summary_surfaces_next_task_blocker() {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let summary =
            mesh_release_task_readiness_summary(&available_primitives, &allowed_capabilities, &[]);

        assert_eq!(summary.total_checks, 5);
        assert_eq!(summary.ready_checks, 0);
        assert_eq!(summary.blocked_checks, 3);
        assert_eq!(summary.review_required_checks, 2);
        assert_eq!(summary.operator_required_checks, 4);
        assert_eq!(summary.total_slots, 5);
        assert_eq!(summary.ready_slots, 0);
        assert_eq!(summary.blocked_slots, 3);
        assert_eq!(summary.review_required_slots, 2);
        assert_eq!(summary.operator_required_slots, 4);
        assert_eq!(summary.total_tasks, 5);
        assert_eq!(summary.ready_tasks, 0);
        assert_eq!(summary.blocked_tasks, 3);
        assert_eq!(summary.review_required_tasks, 2);
        assert_eq!(summary.operator_required_tasks, 4);
        assert_eq!(summary.tasks_requiring_attention, 5);
        assert_eq!(summary.queued_substrate_actions, 5);
        assert_eq!(summary.remediation_item_count, 2);
        assert_eq!(summary.review_required_packages, 1);
        assert_eq!(summary.operator_required_packages, 6);
        assert_eq!(summary.blocked_packages, 5);
        assert_eq!(
            summary.first_task_key,
            Some("release-execution-task-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.first_blocked_task_key,
            Some("release-execution-task-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.first_review_task_key,
            Some("release-execution-task-02-evidence_remediation".to_string())
        );
        assert_eq!(
            summary.first_operator_task_key,
            Some("release-execution-task-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_task_key,
            Some("release-execution-task-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_slot_key,
            Some("release-check-slot-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_check_kind,
            Some(IntegrationMeshReleaseReadinessCheckKind::SubstrateActions)
        );
        assert_eq!(
            summary.next_check_status,
            Some(IntegrationMeshReleaseReadinessStatus::Blocked)
        );
        assert_eq!(
            summary.next_package_kind,
            Some(IntegrationMeshReadinessHandoffKind::SubstrateAction)
        );
        assert_eq!(
            summary.next_handoff_status,
            Some(IntegrationMeshReadinessHandoffStatus::Blocked)
        );
        assert_eq!(
            summary.packet_status,
            IntegrationMeshReleaseReadinessStatus::Blocked
        );
        assert_eq!(
            summary.execution_status,
            IntegrationMeshReleaseReadinessStatus::Blocked
        );
        assert!(!summary.release_packet_ready);
        assert!(!summary.release_check_slots_ready);
        assert!(!summary.release_execution_ready);
        assert!(!summary.release_tasks_ready);
        assert!(!summary.ready_for_release_execution());
        assert!(summary.has_release_tasks());
        assert!(summary.has_blockers());
        assert!(summary.has_review_work());
        assert!(summary.needs_operator());
        assert!(summary.requires_attention());
    }

    #[test]
    fn mesh_release_task_readiness_summary_marks_release_execution_ready() {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let summary = mesh_release_task_readiness_summary_for_catalog(
            &catalog,
            all_primitive_families(),
            &allowed_capabilities,
            &[],
        );

        assert_eq!(summary.total_checks, 5);
        assert_eq!(summary.ready_checks, 5);
        assert_eq!(summary.blocked_checks, 0);
        assert_eq!(summary.review_required_checks, 0);
        assert_eq!(summary.operator_required_checks, 0);
        assert_eq!(summary.total_slots, 5);
        assert_eq!(summary.ready_slots, 5);
        assert_eq!(summary.blocked_slots, 0);
        assert_eq!(summary.review_required_slots, 0);
        assert_eq!(summary.operator_required_slots, 0);
        assert_eq!(summary.total_tasks, 5);
        assert_eq!(summary.ready_tasks, 5);
        assert_eq!(summary.blocked_tasks, 0);
        assert_eq!(summary.review_required_tasks, 0);
        assert_eq!(summary.operator_required_tasks, 0);
        assert_eq!(summary.tasks_requiring_attention, 0);
        assert_eq!(
            summary.first_task_key,
            Some("release-execution-task-01-substrate_actions".to_string())
        );
        assert_eq!(summary.next_task_key, None);
        assert_eq!(summary.next_slot_key, None);
        assert_eq!(summary.next_check_kind, None);
        assert_eq!(summary.next_check_status, None);
        assert_eq!(
            summary.next_package_kind,
            Some(IntegrationMeshReadinessHandoffKind::ReleaseReady)
        );
        assert_eq!(
            summary.next_handoff_status,
            Some(IntegrationMeshReadinessHandoffStatus::Ready)
        );
        assert_eq!(
            summary.packet_status,
            IntegrationMeshReleaseReadinessStatus::Ready
        );
        assert_eq!(
            summary.execution_status,
            IntegrationMeshReleaseReadinessStatus::Ready
        );
        assert!(summary.release_packet_ready);
        assert!(summary.release_check_slots_ready);
        assert!(summary.release_execution_ready);
        assert!(summary.release_tasks_ready);
        assert!(summary.ready_for_release_execution());
        assert!(summary.has_release_tasks());
        assert!(!summary.has_blockers());
        assert!(!summary.has_review_work());
        assert!(!summary.needs_operator());
        assert!(!summary.requires_attention());
    }

    #[test]
    fn mesh_release_execution_task_dispatch_slots_surface_blocked_dispatch() {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let dispatch_slots = mesh_release_execution_task_dispatch_slots(
            &available_primitives,
            &allowed_capabilities,
            &[],
        );
        let summary = mesh_release_execution_task_dispatch_summary(
            &available_primitives,
            &allowed_capabilities,
            &[],
        );

        assert_eq!(dispatch_slots.len(), 5);
        assert_eq!(summary.total_dispatch_slots, 5);
        assert_eq!(summary.ready_dispatch_slots, 0);
        assert_eq!(summary.blocked_dispatch_slots, 3);
        assert_eq!(summary.review_required_dispatch_slots, 2);
        assert_eq!(summary.operator_required_dispatch_slots, 4);
        assert_eq!(summary.dispatch_slots_requiring_attention, 5);
        assert_eq!(summary.dispatch_required_slots, 5);
        assert_eq!(
            summary.first_dispatch_key,
            Some("release-task-dispatch-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.first_blocked_dispatch_key,
            Some("release-task-dispatch-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.first_review_dispatch_key,
            Some("release-task-dispatch-02-evidence_remediation".to_string())
        );
        assert_eq!(
            summary.first_operator_dispatch_key,
            Some("release-task-dispatch-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_dispatch_key,
            Some("release-task-dispatch-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_task_key,
            Some("release-execution-task-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_slot_key,
            Some("release-check-slot-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_check_kind,
            Some(IntegrationMeshReleaseReadinessCheckKind::SubstrateActions)
        );
        assert_eq!(
            summary.next_check_status,
            Some(IntegrationMeshReleaseReadinessStatus::Blocked)
        );
        assert_eq!(
            summary.next_package_kind,
            Some(IntegrationMeshReadinessHandoffKind::SubstrateAction)
        );
        assert_eq!(
            summary.next_handoff_status,
            Some(IntegrationMeshReadinessHandoffStatus::Blocked)
        );
        assert_eq!(
            summary.packet_status,
            IntegrationMeshReleaseReadinessStatus::Blocked
        );
        assert_eq!(
            summary.execution_status,
            IntegrationMeshReleaseReadinessStatus::Blocked
        );
        assert!(!summary.release_execution_ready);
        assert!(!summary.release_tasks_ready);
        assert!(!summary.ready_for_dispatch());
        assert!(summary.has_dispatch_slots());
        assert!(summary.has_blockers());
        assert!(summary.has_review_work());
        assert!(summary.needs_operator());
        assert!(summary.requires_attention());

        let first = &dispatch_slots[0];
        assert_eq!(
            first.dispatch_key,
            "release-task-dispatch-01-substrate_actions"
        );
        assert_eq!(
            first.task_key,
            "release-execution-task-01-substrate_actions"
        );
        assert_eq!(first.slot_key, "release-check-slot-01-substrate_actions");
        assert_eq!(
            first.check_kind,
            IntegrationMeshReleaseReadinessCheckKind::SubstrateActions
        );
        assert!(first.blocked());
        assert!(first.operator_required());
        assert!(first.dispatch_required());
    }

    #[test]
    fn mesh_release_execution_task_dispatch_slots_mark_ready_dispatch() {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let dispatch_slots = mesh_release_execution_task_dispatch_slots_for_catalog(
            &catalog,
            all_primitive_families(),
            &allowed_capabilities,
            &[],
        );
        let summary = mesh_release_execution_task_dispatch_summary_for_catalog(
            &catalog,
            all_primitive_families(),
            &allowed_capabilities,
            &[],
        );

        assert_eq!(dispatch_slots.len(), 5);
        assert_eq!(summary.total_dispatch_slots, 5);
        assert_eq!(summary.ready_dispatch_slots, 5);
        assert_eq!(summary.blocked_dispatch_slots, 0);
        assert_eq!(summary.review_required_dispatch_slots, 0);
        assert_eq!(summary.operator_required_dispatch_slots, 0);
        assert_eq!(summary.dispatch_slots_requiring_attention, 0);
        assert_eq!(summary.dispatch_required_slots, 0);
        assert_eq!(
            summary.first_dispatch_key,
            Some("release-task-dispatch-01-substrate_actions".to_string())
        );
        assert_eq!(summary.next_dispatch_key, None);
        assert_eq!(summary.next_task_key, None);
        assert_eq!(summary.next_slot_key, None);
        assert_eq!(summary.next_check_kind, None);
        assert_eq!(summary.next_check_status, None);
        assert_eq!(
            summary.packet_status,
            IntegrationMeshReleaseReadinessStatus::Ready
        );
        assert_eq!(
            summary.execution_status,
            IntegrationMeshReleaseReadinessStatus::Ready
        );
        assert!(summary.release_execution_ready);
        assert!(summary.release_tasks_ready);
        assert!(summary.ready_for_dispatch());
        assert!(summary.has_dispatch_slots());
        assert!(!summary.has_blockers());
        assert!(!summary.has_review_work());
        assert!(!summary.needs_operator());
        assert!(!summary.requires_attention());
        assert!(dispatch_slots
            .iter()
            .all(IntegrationMeshReleaseExecutionTaskDispatchSlot::ready));
        assert!(dispatch_slots.iter().all(|slot| !slot.dispatch_required()));
    }

    #[test]
    fn mesh_release_dispatch_readiness_summary_surfaces_dispatch_blocker() {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let summary = mesh_release_dispatch_readiness_summary(
            &available_primitives,
            &allowed_capabilities,
            &[],
        );

        assert_eq!(summary.total_checks, 5);
        assert_eq!(summary.ready_checks, 0);
        assert_eq!(summary.blocked_checks, 3);
        assert_eq!(summary.review_required_checks, 2);
        assert_eq!(summary.operator_required_checks, 4);
        assert_eq!(summary.total_tasks, 5);
        assert_eq!(summary.ready_tasks, 0);
        assert_eq!(summary.blocked_tasks, 3);
        assert_eq!(summary.review_required_tasks, 2);
        assert_eq!(summary.operator_required_tasks, 4);
        assert_eq!(summary.total_dispatch_slots, 5);
        assert_eq!(summary.ready_dispatch_slots, 0);
        assert_eq!(summary.blocked_dispatch_slots, 3);
        assert_eq!(summary.review_required_dispatch_slots, 2);
        assert_eq!(summary.operator_required_dispatch_slots, 4);
        assert_eq!(summary.dispatch_slots_requiring_attention, 5);
        assert_eq!(summary.dispatch_required_slots, 5);
        assert_eq!(summary.queued_substrate_actions, 5);
        assert_eq!(summary.remediation_item_count, 2);
        assert_eq!(summary.review_required_packages, 1);
        assert_eq!(summary.operator_required_packages, 6);
        assert_eq!(summary.blocked_packages, 5);
        assert_eq!(
            summary.first_dispatch_key,
            Some("release-task-dispatch-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_dispatch_key,
            Some("release-task-dispatch-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_task_key,
            Some("release-execution-task-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_slot_key,
            Some("release-check-slot-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_check_kind,
            Some(IntegrationMeshReleaseReadinessCheckKind::SubstrateActions)
        );
        assert_eq!(
            summary.next_check_status,
            Some(IntegrationMeshReleaseReadinessStatus::Blocked)
        );
        assert_eq!(
            summary.next_package_kind,
            Some(IntegrationMeshReadinessHandoffKind::SubstrateAction)
        );
        assert_eq!(
            summary.next_handoff_status,
            Some(IntegrationMeshReadinessHandoffStatus::Blocked)
        );
        assert_eq!(
            summary.packet_status,
            IntegrationMeshReleaseReadinessStatus::Blocked
        );
        assert_eq!(
            summary.execution_status,
            IntegrationMeshReleaseReadinessStatus::Blocked
        );
        assert!(!summary.release_packet_ready);
        assert!(!summary.release_execution_ready);
        assert!(!summary.release_tasks_ready);
        assert!(!summary.release_dispatch_ready);
        assert!(!summary.ready_for_release_dispatch());
        assert!(summary.has_dispatch_slots());
        assert!(summary.has_blockers());
        assert!(summary.has_review_work());
        assert!(summary.needs_operator());
        assert!(summary.requires_attention());
    }

    #[test]
    fn mesh_release_dispatch_readiness_summary_marks_release_dispatch_ready() {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let summary = mesh_release_dispatch_readiness_summary_for_catalog(
            &catalog,
            all_primitive_families(),
            &allowed_capabilities,
            &[],
        );

        assert_eq!(summary.total_checks, 5);
        assert_eq!(summary.ready_checks, 5);
        assert_eq!(summary.blocked_checks, 0);
        assert_eq!(summary.review_required_checks, 0);
        assert_eq!(summary.operator_required_checks, 0);
        assert_eq!(summary.total_tasks, 5);
        assert_eq!(summary.ready_tasks, 5);
        assert_eq!(summary.blocked_tasks, 0);
        assert_eq!(summary.review_required_tasks, 0);
        assert_eq!(summary.operator_required_tasks, 0);
        assert_eq!(summary.total_dispatch_slots, 5);
        assert_eq!(summary.ready_dispatch_slots, 5);
        assert_eq!(summary.blocked_dispatch_slots, 0);
        assert_eq!(summary.review_required_dispatch_slots, 0);
        assert_eq!(summary.operator_required_dispatch_slots, 0);
        assert_eq!(summary.dispatch_slots_requiring_attention, 0);
        assert_eq!(summary.dispatch_required_slots, 0);
        assert_eq!(
            summary.first_dispatch_key,
            Some("release-task-dispatch-01-substrate_actions".to_string())
        );
        assert_eq!(summary.next_dispatch_key, None);
        assert_eq!(summary.next_task_key, None);
        assert_eq!(summary.next_slot_key, None);
        assert_eq!(summary.next_check_kind, None);
        assert_eq!(summary.next_check_status, None);
        assert_eq!(
            summary.next_package_kind,
            Some(IntegrationMeshReadinessHandoffKind::ReleaseReady)
        );
        assert_eq!(
            summary.next_handoff_status,
            Some(IntegrationMeshReadinessHandoffStatus::Ready)
        );
        assert_eq!(
            summary.packet_status,
            IntegrationMeshReleaseReadinessStatus::Ready
        );
        assert_eq!(
            summary.execution_status,
            IntegrationMeshReleaseReadinessStatus::Ready
        );
        assert!(summary.release_packet_ready);
        assert!(summary.release_execution_ready);
        assert!(summary.release_tasks_ready);
        assert!(summary.release_dispatch_ready);
        assert!(summary.ready_for_release_dispatch());
        assert!(summary.has_dispatch_slots());
        assert!(!summary.has_blockers());
        assert!(!summary.has_review_work());
        assert!(!summary.needs_operator());
        assert!(!summary.requires_attention());
    }

    #[test]
    fn mesh_release_dispatch_tickets_surface_next_blocked_ticket() {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let tickets =
            mesh_release_dispatch_tickets(&available_primitives, &allowed_capabilities, &[]);
        let summary =
            mesh_release_dispatch_ticket_summary(&available_primitives, &allowed_capabilities, &[]);

        assert_eq!(tickets.len(), 5);
        assert_eq!(summary.total_tickets, 5);
        assert_eq!(summary.ready_tickets, 0);
        assert_eq!(summary.blocked_tickets, 3);
        assert_eq!(summary.review_required_tickets, 2);
        assert_eq!(summary.operator_required_tickets, 4);
        assert_eq!(summary.dispatch_required_tickets, 5);
        assert_eq!(
            summary.first_ticket_key,
            Some("release-dispatch-ticket-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.first_blocked_ticket_key,
            Some("release-dispatch-ticket-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.first_review_ticket_key,
            Some("release-dispatch-ticket-02-evidence_remediation".to_string())
        );
        assert_eq!(
            summary.first_operator_ticket_key,
            Some("release-dispatch-ticket-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_ticket_key,
            Some("release-dispatch-ticket-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_dispatch_key,
            Some("release-task-dispatch-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_task_key,
            Some("release-execution-task-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_slot_key,
            Some("release-check-slot-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_check_kind,
            Some(IntegrationMeshReleaseReadinessCheckKind::SubstrateActions)
        );
        assert_eq!(
            summary.next_check_status,
            Some(IntegrationMeshReleaseReadinessStatus::Blocked)
        );
        assert_eq!(
            summary.next_package_kind,
            Some(IntegrationMeshReadinessHandoffKind::SubstrateAction)
        );
        assert_eq!(
            summary.next_handoff_status,
            Some(IntegrationMeshReadinessHandoffStatus::Blocked)
        );
        assert_eq!(
            summary.packet_status,
            IntegrationMeshReleaseReadinessStatus::Blocked
        );
        assert_eq!(
            summary.execution_status,
            IntegrationMeshReleaseReadinessStatus::Blocked
        );
        assert!(!summary.release_dispatch_ready);
        assert!(!summary.ready_for_ticket_dispatch());
        assert!(summary.has_tickets());
        assert!(summary.has_blockers());
        assert!(summary.has_review_work());
        assert!(summary.needs_operator());
        assert!(summary.requires_attention());

        let first = &tickets[0];
        assert_eq!(
            first.ticket_key,
            "release-dispatch-ticket-01-substrate_actions"
        );
        assert_eq!(
            first.dispatch_key,
            "release-task-dispatch-01-substrate_actions"
        );
        assert_eq!(
            first.task_key,
            "release-execution-task-01-substrate_actions"
        );
        assert_eq!(first.slot_key, "release-check-slot-01-substrate_actions");
        assert_eq!(first.status, IntegrationMeshReleaseReadinessStatus::Blocked);
        assert!(first.blocked());
        assert!(first.operator_required());
        assert!(first.dispatch_required());
    }

    #[test]
    fn mesh_release_dispatch_tickets_mark_ready_ticket_dispatch() {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let tickets = mesh_release_dispatch_tickets_for_catalog(
            &catalog,
            all_primitive_families(),
            &allowed_capabilities,
            &[],
        );
        let summary = mesh_release_dispatch_ticket_summary_for_catalog(
            &catalog,
            all_primitive_families(),
            &allowed_capabilities,
            &[],
        );

        assert_eq!(tickets.len(), 5);
        assert_eq!(summary.total_tickets, 5);
        assert_eq!(summary.ready_tickets, 5);
        assert_eq!(summary.blocked_tickets, 0);
        assert_eq!(summary.review_required_tickets, 0);
        assert_eq!(summary.operator_required_tickets, 0);
        assert_eq!(summary.dispatch_required_tickets, 0);
        assert_eq!(
            summary.first_ticket_key,
            Some("release-dispatch-ticket-01-substrate_actions".to_string())
        );
        assert_eq!(summary.next_ticket_key, None);
        assert_eq!(summary.next_dispatch_key, None);
        assert_eq!(summary.next_task_key, None);
        assert_eq!(summary.next_slot_key, None);
        assert_eq!(summary.next_check_kind, None);
        assert_eq!(summary.next_check_status, None);
        assert_eq!(
            summary.packet_status,
            IntegrationMeshReleaseReadinessStatus::Ready
        );
        assert_eq!(
            summary.execution_status,
            IntegrationMeshReleaseReadinessStatus::Ready
        );
        assert!(summary.release_dispatch_ready);
        assert!(summary.ready_for_ticket_dispatch());
        assert!(summary.has_tickets());
        assert!(!summary.has_blockers());
        assert!(!summary.has_review_work());
        assert!(!summary.needs_operator());
        assert!(!summary.requires_attention());
        assert!(tickets
            .iter()
            .all(IntegrationMeshReleaseDispatchTicket::ready));
        assert!(tickets.iter().all(|ticket| !ticket.dispatch_required()));
    }

    #[test]
    fn mesh_release_dispatch_ticket_handoff_packets_classify_attention_lanes() {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let packets = mesh_release_dispatch_ticket_handoff_packets(
            &available_primitives,
            &allowed_capabilities,
            &[],
        );
        let summary = mesh_release_dispatch_ticket_handoff_summary(
            &available_primitives,
            &allowed_capabilities,
            &[],
        );

        assert_eq!(packets.len(), 5);
        assert_eq!(summary.total_packets, 5);
        assert_eq!(summary.release_lane_packets, 0);
        assert_eq!(summary.operator_lane_packets, 0);
        assert_eq!(summary.repair_lane_packets, 3);
        assert_eq!(summary.review_lane_packets, 2);
        assert_eq!(summary.dispatch_required_packets, 5);
        assert_eq!(
            summary.first_packet_key,
            Some("release-dispatch-handoff-packet-01-substrate_actions".to_string())
        );
        assert_eq!(summary.first_release_packet_key, None);
        assert_eq!(summary.first_operator_packet_key, None);
        assert_eq!(
            summary.first_repair_packet_key,
            Some("release-dispatch-handoff-packet-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.first_review_packet_key,
            Some("release-dispatch-handoff-packet-02-evidence_remediation".to_string())
        );
        assert_eq!(
            summary.next_packet_key,
            Some("release-dispatch-handoff-packet-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_ticket_key,
            Some("release-dispatch-ticket-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_dispatch_key,
            Some("release-task-dispatch-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_task_key,
            Some("release-execution-task-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_slot_key,
            Some("release-check-slot-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_check_kind,
            Some(IntegrationMeshReleaseReadinessCheckKind::SubstrateActions)
        );
        assert_eq!(
            summary.next_check_status,
            Some(IntegrationMeshReleaseReadinessStatus::Blocked)
        );
        assert_eq!(
            summary.next_package_kind,
            Some(IntegrationMeshReadinessHandoffKind::SubstrateAction)
        );
        assert_eq!(
            summary.next_handoff_status,
            Some(IntegrationMeshReadinessHandoffStatus::Blocked)
        );
        assert_eq!(
            summary.next_handoff_lane,
            Some(IntegrationMeshReleaseDispatchTicketHandoffLane::Repair)
        );
        assert_eq!(
            summary.packet_status,
            IntegrationMeshReleaseReadinessStatus::Blocked
        );
        assert_eq!(
            summary.execution_status,
            IntegrationMeshReleaseReadinessStatus::Blocked
        );
        assert!(!summary.release_dispatch_ready);
        assert!(!summary.release_ticket_ready);
        assert!(!summary.release_handoff_ready);
        assert!(!summary.ready_for_release_handoff());
        assert!(summary.has_packets());
        assert!(summary.has_repair_work());
        assert!(summary.has_review_work());
        assert!(summary.needs_operator());
        assert!(summary.requires_attention());

        let first = &packets[0];
        assert_eq!(
            first.packet_key,
            "release-dispatch-handoff-packet-01-substrate_actions"
        );
        assert_eq!(
            first.ticket_key,
            "release-dispatch-ticket-01-substrate_actions"
        );
        assert_eq!(
            first.handoff_lane,
            IntegrationMeshReleaseDispatchTicketHandoffLane::Repair
        );
        assert_eq!(first.handoff_lane.as_str(), "repair");
        assert!(first.is_repair_lane());
        assert!(first.blocked());
        assert!(first.operator_required());
        assert!(first.dispatch_required());
    }

    #[test]
    fn mesh_release_dispatch_ticket_handoff_packets_mark_release_lane_ready() {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let packets = mesh_release_dispatch_ticket_handoff_packets_for_catalog(
            &catalog,
            all_primitive_families(),
            &allowed_capabilities,
            &[],
        );
        let summary = mesh_release_dispatch_ticket_handoff_summary_for_catalog(
            &catalog,
            all_primitive_families(),
            &allowed_capabilities,
            &[],
        );

        assert_eq!(packets.len(), 5);
        assert_eq!(summary.total_packets, 5);
        assert_eq!(summary.release_lane_packets, 5);
        assert_eq!(summary.operator_lane_packets, 0);
        assert_eq!(summary.repair_lane_packets, 0);
        assert_eq!(summary.review_lane_packets, 0);
        assert_eq!(summary.dispatch_required_packets, 0);
        assert_eq!(
            summary.first_packet_key,
            Some("release-dispatch-handoff-packet-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.first_release_packet_key,
            Some("release-dispatch-handoff-packet-01-substrate_actions".to_string())
        );
        assert_eq!(summary.first_operator_packet_key, None);
        assert_eq!(summary.first_repair_packet_key, None);
        assert_eq!(summary.first_review_packet_key, None);
        assert_eq!(summary.next_packet_key, None);
        assert_eq!(summary.next_ticket_key, None);
        assert_eq!(summary.next_dispatch_key, None);
        assert_eq!(summary.next_task_key, None);
        assert_eq!(summary.next_slot_key, None);
        assert_eq!(summary.next_check_kind, None);
        assert_eq!(summary.next_check_status, None);
        assert_eq!(summary.next_package_kind, None);
        assert_eq!(summary.next_handoff_status, None);
        assert_eq!(summary.next_handoff_lane, None);
        assert_eq!(
            summary.packet_status,
            IntegrationMeshReleaseReadinessStatus::Ready
        );
        assert_eq!(
            summary.execution_status,
            IntegrationMeshReleaseReadinessStatus::Ready
        );
        assert!(summary.release_dispatch_ready);
        assert!(summary.release_ticket_ready);
        assert!(summary.release_handoff_ready);
        assert!(summary.ready_for_release_handoff());
        assert!(summary.has_packets());
        assert!(!summary.has_repair_work());
        assert!(!summary.has_review_work());
        assert!(!summary.needs_operator());
        assert!(!summary.requires_attention());
        assert!(packets
            .iter()
            .all(IntegrationMeshReleaseDispatchTicketHandoffPacket::ready));
        assert!(packets.iter().all(|packet| packet.is_release_lane()));
    }

    #[test]
    fn mesh_release_ticket_readiness_summary_surfaces_ticket_attention() {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let summary = mesh_release_ticket_readiness_summary(
            &available_primitives,
            &allowed_capabilities,
            &[],
        );

        assert_eq!(summary.total_checks, 5);
        assert_eq!(summary.ready_checks, 0);
        assert_eq!(summary.blocked_checks, 3);
        assert_eq!(summary.review_required_checks, 2);
        assert_eq!(summary.operator_required_checks, 4);
        assert_eq!(summary.total_tasks, 5);
        assert_eq!(summary.ready_tasks, 0);
        assert_eq!(summary.blocked_tasks, 3);
        assert_eq!(summary.review_required_tasks, 2);
        assert_eq!(summary.operator_required_tasks, 4);
        assert_eq!(summary.total_dispatch_slots, 5);
        assert_eq!(summary.ready_dispatch_slots, 0);
        assert_eq!(summary.blocked_dispatch_slots, 3);
        assert_eq!(summary.review_required_dispatch_slots, 2);
        assert_eq!(summary.operator_required_dispatch_slots, 4);
        assert_eq!(summary.dispatch_slots_requiring_attention, 5);
        assert_eq!(summary.dispatch_required_slots, 5);
        assert_eq!(summary.total_tickets, 5);
        assert_eq!(summary.ready_tickets, 0);
        assert_eq!(summary.blocked_tickets, 3);
        assert_eq!(summary.review_required_tickets, 2);
        assert_eq!(summary.operator_required_tickets, 4);
        assert_eq!(summary.dispatch_required_tickets, 5);
        assert_eq!(summary.queued_substrate_actions, 5);
        assert_eq!(summary.remediation_item_count, 2);
        assert_eq!(summary.review_required_packages, 1);
        assert_eq!(summary.operator_required_packages, 6);
        assert_eq!(summary.blocked_packages, 5);
        assert_eq!(
            summary.first_ticket_key,
            Some("release-dispatch-ticket-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.first_blocked_ticket_key,
            Some("release-dispatch-ticket-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.first_review_ticket_key,
            Some("release-dispatch-ticket-02-evidence_remediation".to_string())
        );
        assert_eq!(
            summary.first_operator_ticket_key,
            Some("release-dispatch-ticket-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_ticket_key,
            Some("release-dispatch-ticket-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_dispatch_key,
            Some("release-task-dispatch-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_task_key,
            Some("release-execution-task-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_slot_key,
            Some("release-check-slot-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_check_kind,
            Some(IntegrationMeshReleaseReadinessCheckKind::SubstrateActions)
        );
        assert_eq!(
            summary.next_check_status,
            Some(IntegrationMeshReleaseReadinessStatus::Blocked)
        );
        assert_eq!(
            summary.next_package_kind,
            Some(IntegrationMeshReadinessHandoffKind::SubstrateAction)
        );
        assert_eq!(
            summary.next_handoff_status,
            Some(IntegrationMeshReadinessHandoffStatus::Blocked)
        );
        assert_eq!(
            summary.packet_status,
            IntegrationMeshReleaseReadinessStatus::Blocked
        );
        assert_eq!(
            summary.execution_status,
            IntegrationMeshReleaseReadinessStatus::Blocked
        );
        assert!(!summary.release_packet_ready);
        assert!(!summary.release_execution_ready);
        assert!(!summary.release_tasks_ready);
        assert!(!summary.release_dispatch_ready);
        assert!(!summary.release_ticket_ready);
        assert!(!summary.ready_for_release_ticket_dispatch());
        assert!(summary.has_tickets());
        assert!(summary.has_blockers());
        assert!(summary.has_review_work());
        assert!(summary.needs_operator());
        assert!(summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_readiness_summary_marks_release_ticket_ready() {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let summary = mesh_release_ticket_readiness_summary_for_catalog(
            &catalog,
            all_primitive_families(),
            &allowed_capabilities,
            &[],
        );

        assert_eq!(summary.total_checks, 5);
        assert_eq!(summary.ready_checks, 5);
        assert_eq!(summary.blocked_checks, 0);
        assert_eq!(summary.review_required_checks, 0);
        assert_eq!(summary.operator_required_checks, 0);
        assert_eq!(summary.total_tasks, 5);
        assert_eq!(summary.ready_tasks, 5);
        assert_eq!(summary.blocked_tasks, 0);
        assert_eq!(summary.review_required_tasks, 0);
        assert_eq!(summary.operator_required_tasks, 0);
        assert_eq!(summary.total_dispatch_slots, 5);
        assert_eq!(summary.ready_dispatch_slots, 5);
        assert_eq!(summary.blocked_dispatch_slots, 0);
        assert_eq!(summary.review_required_dispatch_slots, 0);
        assert_eq!(summary.operator_required_dispatch_slots, 0);
        assert_eq!(summary.dispatch_slots_requiring_attention, 0);
        assert_eq!(summary.dispatch_required_slots, 0);
        assert_eq!(summary.total_tickets, 5);
        assert_eq!(summary.ready_tickets, 5);
        assert_eq!(summary.blocked_tickets, 0);
        assert_eq!(summary.review_required_tickets, 0);
        assert_eq!(summary.operator_required_tickets, 0);
        assert_eq!(summary.dispatch_required_tickets, 0);
        assert_eq!(
            summary.first_ticket_key,
            Some("release-dispatch-ticket-01-substrate_actions".to_string())
        );
        assert_eq!(summary.next_ticket_key, None);
        assert_eq!(summary.next_dispatch_key, None);
        assert_eq!(summary.next_task_key, None);
        assert_eq!(summary.next_slot_key, None);
        assert_eq!(summary.next_check_kind, None);
        assert_eq!(summary.next_check_status, None);
        assert_eq!(
            summary.next_package_kind,
            Some(IntegrationMeshReadinessHandoffKind::ReleaseReady)
        );
        assert_eq!(
            summary.next_handoff_status,
            Some(IntegrationMeshReadinessHandoffStatus::Ready)
        );
        assert_eq!(
            summary.packet_status,
            IntegrationMeshReleaseReadinessStatus::Ready
        );
        assert_eq!(
            summary.execution_status,
            IntegrationMeshReleaseReadinessStatus::Ready
        );
        assert!(summary.release_packet_ready);
        assert!(summary.release_execution_ready);
        assert!(summary.release_tasks_ready);
        assert!(summary.release_dispatch_ready);
        assert!(summary.release_ticket_ready);
        assert!(summary.ready_for_release_ticket_dispatch());
        assert!(summary.has_tickets());
        assert!(!summary.has_blockers());
        assert!(!summary.has_review_work());
        assert!(!summary.needs_operator());
        assert!(!summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_summary_surfaces_lane_attention() {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let summary = mesh_release_ticket_handoff_readiness_summary(
            &available_primitives,
            &allowed_capabilities,
            &[],
        );

        assert_eq!(summary.total_tickets, 5);
        assert_eq!(summary.ready_tickets, 0);
        assert_eq!(summary.blocked_tickets, 3);
        assert_eq!(summary.review_required_tickets, 2);
        assert_eq!(summary.operator_required_tickets, 4);
        assert_eq!(summary.dispatch_required_tickets, 5);
        assert_eq!(summary.total_handoff_packets, 5);
        assert_eq!(summary.release_lane_packets, 0);
        assert_eq!(summary.operator_lane_packets, 0);
        assert_eq!(summary.repair_lane_packets, 3);
        assert_eq!(summary.review_lane_packets, 2);
        assert_eq!(summary.dispatch_required_packets, 5);
        assert_eq!(summary.queued_substrate_actions, 5);
        assert_eq!(summary.remediation_item_count, 2);
        assert_eq!(summary.review_required_packages, 1);
        assert_eq!(summary.operator_required_packages, 6);
        assert_eq!(summary.blocked_packages, 5);
        assert_eq!(
            summary.first_ticket_key,
            Some("release-dispatch-ticket-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.first_packet_key,
            Some("release-dispatch-handoff-packet-01-substrate_actions".to_string())
        );
        assert_eq!(summary.first_release_packet_key, None);
        assert_eq!(summary.first_operator_packet_key, None);
        assert_eq!(
            summary.first_repair_packet_key,
            Some("release-dispatch-handoff-packet-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.first_review_packet_key,
            Some("release-dispatch-handoff-packet-02-evidence_remediation".to_string())
        );
        assert_eq!(
            summary.next_packet_key,
            Some("release-dispatch-handoff-packet-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_ticket_key,
            Some("release-dispatch-ticket-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_dispatch_key,
            Some("release-task-dispatch-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_task_key,
            Some("release-execution-task-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_slot_key,
            Some("release-check-slot-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_check_kind,
            Some(IntegrationMeshReleaseReadinessCheckKind::SubstrateActions)
        );
        assert_eq!(
            summary.next_check_status,
            Some(IntegrationMeshReleaseReadinessStatus::Blocked)
        );
        assert_eq!(
            summary.next_package_kind,
            Some(IntegrationMeshReadinessHandoffKind::SubstrateAction)
        );
        assert_eq!(
            summary.next_handoff_status,
            Some(IntegrationMeshReadinessHandoffStatus::Blocked)
        );
        assert_eq!(
            summary.next_handoff_lane,
            Some(IntegrationMeshReleaseDispatchTicketHandoffLane::Repair)
        );
        assert_eq!(
            summary.packet_status,
            IntegrationMeshReleaseReadinessStatus::Blocked
        );
        assert_eq!(
            summary.execution_status,
            IntegrationMeshReleaseReadinessStatus::Blocked
        );
        assert!(!summary.release_packet_ready);
        assert!(!summary.release_execution_ready);
        assert!(!summary.release_tasks_ready);
        assert!(!summary.release_dispatch_ready);
        assert!(!summary.release_ticket_ready);
        assert!(!summary.release_handoff_ready);
        assert!(!summary.ready_for_release_handoff());
        assert!(summary.has_handoff_packets());
        assert!(summary.has_repair_work());
        assert!(summary.has_blockers());
        assert!(summary.has_review_work());
        assert!(summary.needs_operator());
        assert!(summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_summary_marks_release_handoff_ready() {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let summary = mesh_release_ticket_handoff_readiness_summary_for_catalog(
            &catalog,
            all_primitive_families(),
            &allowed_capabilities,
            &[],
        );

        assert_eq!(summary.total_tickets, 5);
        assert_eq!(summary.ready_tickets, 5);
        assert_eq!(summary.blocked_tickets, 0);
        assert_eq!(summary.review_required_tickets, 0);
        assert_eq!(summary.operator_required_tickets, 0);
        assert_eq!(summary.dispatch_required_tickets, 0);
        assert_eq!(summary.total_handoff_packets, 5);
        assert_eq!(summary.release_lane_packets, 5);
        assert_eq!(summary.operator_lane_packets, 0);
        assert_eq!(summary.repair_lane_packets, 0);
        assert_eq!(summary.review_lane_packets, 0);
        assert_eq!(summary.dispatch_required_packets, 0);
        assert_eq!(summary.queued_substrate_actions, 0);
        assert_eq!(summary.remediation_item_count, 0);
        assert_eq!(summary.review_required_packages, 0);
        assert_eq!(summary.operator_required_packages, 0);
        assert_eq!(summary.blocked_packages, 0);
        assert_eq!(
            summary.first_ticket_key,
            Some("release-dispatch-ticket-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.first_packet_key,
            Some("release-dispatch-handoff-packet-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.first_release_packet_key,
            Some("release-dispatch-handoff-packet-01-substrate_actions".to_string())
        );
        assert_eq!(summary.first_operator_packet_key, None);
        assert_eq!(summary.first_repair_packet_key, None);
        assert_eq!(summary.first_review_packet_key, None);
        assert_eq!(summary.next_packet_key, None);
        assert_eq!(summary.next_ticket_key, None);
        assert_eq!(summary.next_dispatch_key, None);
        assert_eq!(summary.next_task_key, None);
        assert_eq!(summary.next_slot_key, None);
        assert_eq!(summary.next_check_kind, None);
        assert_eq!(summary.next_check_status, None);
        assert_eq!(
            summary.next_package_kind,
            Some(IntegrationMeshReadinessHandoffKind::ReleaseReady)
        );
        assert_eq!(
            summary.next_handoff_status,
            Some(IntegrationMeshReadinessHandoffStatus::Ready)
        );
        assert_eq!(summary.next_handoff_lane, None);
        assert_eq!(
            summary.packet_status,
            IntegrationMeshReleaseReadinessStatus::Ready
        );
        assert_eq!(
            summary.execution_status,
            IntegrationMeshReleaseReadinessStatus::Ready
        );
        assert!(summary.release_packet_ready);
        assert!(summary.release_execution_ready);
        assert!(summary.release_tasks_ready);
        assert!(summary.release_dispatch_ready);
        assert!(summary.release_ticket_ready);
        assert!(summary.release_handoff_ready);
        assert!(summary.ready_for_release_handoff());
        assert!(summary.has_handoff_packets());
        assert!(!summary.has_repair_work());
        assert!(!summary.has_blockers());
        assert!(!summary.has_review_work());
        assert!(!summary.needs_operator());
        assert!(!summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_execution_slots_sequence_lane_work() {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let execution_slots = mesh_release_ticket_handoff_execution_slots(
            &available_primitives,
            &allowed_capabilities,
            &[],
        );
        let summary = mesh_release_ticket_handoff_execution_slot_summary(
            &available_primitives,
            &allowed_capabilities,
            &[],
        );

        assert_eq!(execution_slots.len(), 5);
        assert_eq!(summary.total_execution_slots, 5);
        assert_eq!(summary.ready_execution_slots, 0);
        assert_eq!(summary.blocked_execution_slots, 3);
        assert_eq!(summary.review_required_execution_slots, 2);
        assert_eq!(summary.operator_required_execution_slots, 4);
        assert_eq!(summary.dispatch_required_execution_slots, 5);
        assert_eq!(summary.release_lane_execution_slots, 0);
        assert_eq!(summary.operator_lane_execution_slots, 0);
        assert_eq!(summary.repair_lane_execution_slots, 3);
        assert_eq!(summary.review_lane_execution_slots, 2);
        assert_eq!(summary.execution_required_slots, 5);
        assert_eq!(
            summary.first_execution_slot_key,
            Some("release-ticket-handoff-execution-slot-01-repair-substrate_actions".to_string())
        );
        assert_eq!(
            summary.first_repair_execution_slot_key,
            Some("release-ticket-handoff-execution-slot-01-repair-substrate_actions".to_string())
        );
        assert_eq!(
            summary.first_review_execution_slot_key,
            Some(
                "release-ticket-handoff-execution-slot-02-review-evidence_remediation".to_string()
            )
        );
        assert_eq!(
            summary.next_execution_slot_key,
            Some("release-ticket-handoff-execution-slot-01-repair-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_packet_key,
            Some("release-dispatch-handoff-packet-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_ticket_key,
            Some("release-dispatch-ticket-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_handoff_lane,
            Some(IntegrationMeshReleaseDispatchTicketHandoffLane::Repair)
        );
        assert!(!summary.release_handoff_execution_ready);
        assert!(!summary.ready_for_release_handoff_execution());
        assert!(summary.has_execution_slots());
        assert!(summary.has_repair_work());
        assert!(summary.has_blockers());
        assert!(summary.has_review_work());
        assert!(summary.needs_operator());
        assert!(summary.requires_attention());

        let first = &execution_slots[0];
        assert_eq!(
            first.execution_slot_key,
            "release-ticket-handoff-execution-slot-01-repair-substrate_actions"
        );
        assert_eq!(
            first.packet_key,
            "release-dispatch-handoff-packet-01-substrate_actions"
        );
        assert_eq!(
            first.handoff_lane,
            IntegrationMeshReleaseDispatchTicketHandoffLane::Repair
        );
        assert!(first.blocked());
        assert!(first.operator_required());
        assert!(first.dispatch_required());
        assert!(first.execution_required());
    }

    #[test]
    fn mesh_release_ticket_handoff_execution_slots_mark_release_ready() {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let execution_slots = mesh_release_ticket_handoff_execution_slots_for_catalog(
            &catalog,
            all_primitive_families(),
            &allowed_capabilities,
            &[],
        );
        let summary = mesh_release_ticket_handoff_execution_slot_summary_for_catalog(
            &catalog,
            all_primitive_families(),
            &allowed_capabilities,
            &[],
        );

        assert_eq!(execution_slots.len(), 5);
        assert_eq!(summary.total_execution_slots, 5);
        assert_eq!(summary.ready_execution_slots, 5);
        assert_eq!(summary.blocked_execution_slots, 0);
        assert_eq!(summary.review_required_execution_slots, 0);
        assert_eq!(summary.operator_required_execution_slots, 0);
        assert_eq!(summary.dispatch_required_execution_slots, 0);
        assert_eq!(summary.release_lane_execution_slots, 5);
        assert_eq!(summary.operator_lane_execution_slots, 0);
        assert_eq!(summary.repair_lane_execution_slots, 0);
        assert_eq!(summary.review_lane_execution_slots, 0);
        assert_eq!(summary.execution_required_slots, 0);
        assert_eq!(
            summary.first_execution_slot_key,
            Some("release-ticket-handoff-execution-slot-01-release-substrate_actions".to_string())
        );
        assert_eq!(
            summary.first_release_execution_slot_key,
            Some("release-ticket-handoff-execution-slot-01-release-substrate_actions".to_string())
        );
        assert_eq!(summary.first_operator_execution_slot_key, None);
        assert_eq!(summary.first_repair_execution_slot_key, None);
        assert_eq!(summary.first_review_execution_slot_key, None);
        assert_eq!(summary.next_execution_slot_key, None);
        assert_eq!(summary.next_packet_key, None);
        assert_eq!(summary.next_ticket_key, None);
        assert_eq!(summary.next_handoff_lane, None);
        assert!(summary.release_handoff_execution_ready);
        assert!(summary.ready_for_release_handoff_execution());
        assert!(summary.has_execution_slots());
        assert!(!summary.has_repair_work());
        assert!(!summary.has_blockers());
        assert!(!summary.has_review_work());
        assert!(!summary.needs_operator());
        assert!(!summary.requires_attention());
        assert!(execution_slots
            .iter()
            .all(IntegrationMeshReleaseTicketHandoffExecutionSlot::ready));
        assert!(execution_slots
            .iter()
            .all(|slot| slot.is_release_lane() && !slot.execution_required()));
    }

    #[test]
    fn mesh_release_ticket_handoff_execution_readiness_summary_surfaces_execution_attention() {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let summary = mesh_release_ticket_handoff_execution_readiness_summary(
            &available_primitives,
            &allowed_capabilities,
            &[],
        );

        assert_eq!(summary.total_tickets, 5);
        assert_eq!(summary.ready_tickets, 0);
        assert_eq!(summary.blocked_tickets, 3);
        assert_eq!(summary.review_required_tickets, 2);
        assert_eq!(summary.operator_required_tickets, 4);
        assert_eq!(summary.dispatch_required_tickets, 5);
        assert_eq!(summary.total_handoff_packets, 5);
        assert_eq!(summary.repair_lane_packets, 3);
        assert_eq!(summary.review_lane_packets, 2);
        assert_eq!(summary.dispatch_required_packets, 5);
        assert_eq!(summary.total_execution_slots, 5);
        assert_eq!(summary.ready_execution_slots, 0);
        assert_eq!(summary.blocked_execution_slots, 3);
        assert_eq!(summary.review_required_execution_slots, 2);
        assert_eq!(summary.operator_required_execution_slots, 4);
        assert_eq!(summary.dispatch_required_execution_slots, 5);
        assert_eq!(summary.repair_lane_execution_slots, 3);
        assert_eq!(summary.review_lane_execution_slots, 2);
        assert_eq!(summary.execution_required_slots, 5);
        assert_eq!(summary.queued_substrate_actions, 5);
        assert_eq!(summary.remediation_item_count, 2);
        assert_eq!(summary.review_required_packages, 1);
        assert_eq!(summary.operator_required_packages, 6);
        assert_eq!(summary.blocked_packages, 5);
        assert_eq!(
            summary.first_execution_slot_key,
            Some("release-ticket-handoff-execution-slot-01-repair-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_execution_slot_key,
            Some("release-ticket-handoff-execution-slot-01-repair-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_packet_key,
            Some("release-dispatch-handoff-packet-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_ticket_key,
            Some("release-dispatch-ticket-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_dispatch_key,
            Some("release-task-dispatch-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_task_key,
            Some("release-execution-task-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_slot_key,
            Some("release-check-slot-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_check_kind,
            Some(IntegrationMeshReleaseReadinessCheckKind::SubstrateActions)
        );
        assert_eq!(
            summary.next_check_status,
            Some(IntegrationMeshReleaseReadinessStatus::Blocked)
        );
        assert_eq!(
            summary.next_package_kind,
            Some(IntegrationMeshReadinessHandoffKind::SubstrateAction)
        );
        assert_eq!(
            summary.next_handoff_status,
            Some(IntegrationMeshReadinessHandoffStatus::Blocked)
        );
        assert_eq!(
            summary.next_handoff_lane,
            Some(IntegrationMeshReleaseDispatchTicketHandoffLane::Repair)
        );
        assert_eq!(
            summary.packet_status,
            IntegrationMeshReleaseReadinessStatus::Blocked
        );
        assert_eq!(
            summary.execution_status,
            IntegrationMeshReleaseReadinessStatus::Blocked
        );
        assert!(!summary.release_packet_ready);
        assert!(!summary.release_execution_ready);
        assert!(!summary.release_tasks_ready);
        assert!(!summary.release_dispatch_ready);
        assert!(!summary.release_ticket_ready);
        assert!(!summary.release_handoff_ready);
        assert!(!summary.release_handoff_execution_ready);
        assert!(!summary.release_ticket_handoff_execution_ready);
        assert!(!summary.ready_for_release_ticket_handoff_execution());
        assert!(summary.has_execution_slots());
        assert!(summary.has_repair_work());
        assert!(summary.has_blockers());
        assert!(summary.has_review_work());
        assert!(summary.needs_operator());
        assert!(summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_execution_readiness_summary_marks_release_ready() {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let summary = mesh_release_ticket_handoff_execution_readiness_summary_for_catalog(
            &catalog,
            all_primitive_families(),
            &allowed_capabilities,
            &[],
        );

        assert_eq!(summary.total_tickets, 5);
        assert_eq!(summary.ready_tickets, 5);
        assert_eq!(summary.blocked_tickets, 0);
        assert_eq!(summary.review_required_tickets, 0);
        assert_eq!(summary.operator_required_tickets, 0);
        assert_eq!(summary.dispatch_required_tickets, 0);
        assert_eq!(summary.total_handoff_packets, 5);
        assert_eq!(summary.release_lane_packets, 5);
        assert_eq!(summary.dispatch_required_packets, 0);
        assert_eq!(summary.total_execution_slots, 5);
        assert_eq!(summary.ready_execution_slots, 5);
        assert_eq!(summary.blocked_execution_slots, 0);
        assert_eq!(summary.review_required_execution_slots, 0);
        assert_eq!(summary.operator_required_execution_slots, 0);
        assert_eq!(summary.dispatch_required_execution_slots, 0);
        assert_eq!(summary.release_lane_execution_slots, 5);
        assert_eq!(summary.execution_required_slots, 0);
        assert_eq!(summary.queued_substrate_actions, 0);
        assert_eq!(summary.remediation_item_count, 0);
        assert_eq!(summary.review_required_packages, 0);
        assert_eq!(summary.operator_required_packages, 0);
        assert_eq!(summary.blocked_packages, 0);
        assert_eq!(
            summary.first_execution_slot_key,
            Some("release-ticket-handoff-execution-slot-01-release-substrate_actions".to_string())
        );
        assert_eq!(summary.next_execution_slot_key, None);
        assert_eq!(summary.next_packet_key, None);
        assert_eq!(summary.next_ticket_key, None);
        assert_eq!(summary.next_dispatch_key, None);
        assert_eq!(summary.next_task_key, None);
        assert_eq!(summary.next_slot_key, None);
        assert_eq!(summary.next_check_kind, None);
        assert_eq!(summary.next_check_status, None);
        assert_eq!(
            summary.next_package_kind,
            Some(IntegrationMeshReadinessHandoffKind::ReleaseReady)
        );
        assert_eq!(
            summary.next_handoff_status,
            Some(IntegrationMeshReadinessHandoffStatus::Ready)
        );
        assert_eq!(summary.next_handoff_lane, None);
        assert_eq!(
            summary.packet_status,
            IntegrationMeshReleaseReadinessStatus::Ready
        );
        assert_eq!(
            summary.execution_status,
            IntegrationMeshReleaseReadinessStatus::Ready
        );
        assert!(summary.release_packet_ready);
        assert!(summary.release_execution_ready);
        assert!(summary.release_tasks_ready);
        assert!(summary.release_dispatch_ready);
        assert!(summary.release_ticket_ready);
        assert!(summary.release_handoff_ready);
        assert!(summary.release_handoff_execution_ready);
        assert!(summary.release_ticket_handoff_execution_ready);
        assert!(summary.ready_for_release_ticket_handoff_execution());
        assert!(summary.has_execution_slots());
        assert!(!summary.has_repair_work());
        assert!(!summary.has_blockers());
        assert!(!summary.has_review_work());
        assert!(!summary.needs_operator());
        assert!(!summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_execution_work_orders_sequence_lane_work() {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let work_orders = mesh_release_ticket_handoff_execution_work_orders(
            &available_primitives,
            &allowed_capabilities,
            &[],
        );
        let summary = mesh_release_ticket_handoff_execution_work_order_summary(
            &available_primitives,
            &allowed_capabilities,
            &[],
        );

        assert_eq!(work_orders.len(), 5);
        assert_eq!(summary.total_work_orders, 5);
        assert_eq!(summary.ready_work_orders, 0);
        assert_eq!(summary.blocked_work_orders, 3);
        assert_eq!(summary.review_required_work_orders, 2);
        assert_eq!(summary.operator_required_work_orders, 4);
        assert_eq!(summary.dispatch_required_work_orders, 5);
        assert_eq!(summary.release_lane_work_orders, 0);
        assert_eq!(summary.operator_lane_work_orders, 0);
        assert_eq!(summary.repair_lane_work_orders, 3);
        assert_eq!(summary.review_lane_work_orders, 2);
        assert_eq!(summary.execution_required_work_orders, 5);
        assert_eq!(
            summary.first_work_order_key,
            Some(
                "release-ticket-handoff-execution-work-order-01-repair-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_repair_work_order_key,
            Some(
                "release-ticket-handoff-execution-work-order-01-repair-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_review_work_order_key,
            Some(
                "release-ticket-handoff-execution-work-order-02-review-evidence_remediation"
                    .to_string()
            )
        );
        assert_eq!(
            summary.next_work_order_key,
            Some(
                "release-ticket-handoff-execution-work-order-01-repair-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.next_execution_slot_key,
            Some("release-ticket-handoff-execution-slot-01-repair-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_packet_key,
            Some("release-dispatch-handoff-packet-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_ticket_key,
            Some("release-dispatch-ticket-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_handoff_lane,
            Some(IntegrationMeshReleaseDispatchTicketHandoffLane::Repair)
        );
        assert!(!summary.release_handoff_execution_work_orders_ready);
        assert!(!summary.ready_for_release_handoff_execution_work_orders());
        assert!(summary.has_work_orders());
        assert!(summary.has_repair_work());
        assert!(summary.has_blockers());
        assert!(summary.has_review_work());
        assert!(summary.needs_operator());
        assert!(summary.requires_attention());

        let first = &work_orders[0];
        assert_eq!(
            first.work_order_key,
            "release-ticket-handoff-execution-work-order-01-repair-substrate_actions"
        );
        assert_eq!(
            first.execution_slot_key,
            "release-ticket-handoff-execution-slot-01-repair-substrate_actions"
        );
        assert_eq!(
            first.handoff_lane,
            IntegrationMeshReleaseDispatchTicketHandoffLane::Repair
        );
        assert!(first.blocked());
        assert!(first.operator_required());
        assert!(first.dispatch_required());
        assert!(first.execution_required());
    }

    #[test]
    fn mesh_release_ticket_handoff_execution_work_orders_mark_release_ready() {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let work_orders = mesh_release_ticket_handoff_execution_work_orders_for_catalog(
            &catalog,
            all_primitive_families(),
            &allowed_capabilities,
            &[],
        );
        let summary = mesh_release_ticket_handoff_execution_work_order_summary_for_catalog(
            &catalog,
            all_primitive_families(),
            &allowed_capabilities,
            &[],
        );

        assert_eq!(work_orders.len(), 5);
        assert_eq!(summary.total_work_orders, 5);
        assert_eq!(summary.ready_work_orders, 5);
        assert_eq!(summary.blocked_work_orders, 0);
        assert_eq!(summary.review_required_work_orders, 0);
        assert_eq!(summary.operator_required_work_orders, 0);
        assert_eq!(summary.dispatch_required_work_orders, 0);
        assert_eq!(summary.release_lane_work_orders, 5);
        assert_eq!(summary.operator_lane_work_orders, 0);
        assert_eq!(summary.repair_lane_work_orders, 0);
        assert_eq!(summary.review_lane_work_orders, 0);
        assert_eq!(summary.execution_required_work_orders, 0);
        assert_eq!(
            summary.first_work_order_key,
            Some(
                "release-ticket-handoff-execution-work-order-01-release-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_release_work_order_key,
            Some(
                "release-ticket-handoff-execution-work-order-01-release-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(summary.first_operator_work_order_key, None);
        assert_eq!(summary.first_repair_work_order_key, None);
        assert_eq!(summary.first_review_work_order_key, None);
        assert_eq!(summary.next_work_order_key, None);
        assert_eq!(summary.next_execution_slot_key, None);
        assert_eq!(summary.next_packet_key, None);
        assert_eq!(summary.next_ticket_key, None);
        assert_eq!(summary.next_handoff_lane, None);
        assert!(summary.release_handoff_execution_work_orders_ready);
        assert!(summary.ready_for_release_handoff_execution_work_orders());
        assert!(summary.has_work_orders());
        assert!(!summary.has_repair_work());
        assert!(!summary.has_blockers());
        assert!(!summary.has_review_work());
        assert!(!summary.needs_operator());
        assert!(!summary.requires_attention());
        assert!(work_orders
            .iter()
            .all(IntegrationMeshReleaseTicketHandoffExecutionWorkOrder::ready));
        assert!(work_orders
            .iter()
            .all(|work_order| work_order.is_release_lane() && !work_order.execution_required()));
    }

    #[test]
    fn mesh_release_ticket_handoff_execution_work_order_guardrails_classify_lane_work() {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let guardrails = mesh_release_ticket_handoff_execution_work_order_guardrails(
            &available_primitives,
            &allowed_capabilities,
            &[],
        );
        let summary = mesh_release_ticket_handoff_execution_work_order_guardrail_summary(
            &available_primitives,
            &allowed_capabilities,
            &[],
        );

        assert_eq!(guardrails.len(), 5);
        assert_eq!(summary.total_guardrails, 5);
        assert_eq!(summary.release_blocker_guardrails, 3);
        assert_eq!(summary.operator_handoff_guardrails, 0);
        assert_eq!(summary.review_gate_guardrails, 2);
        assert_eq!(summary.ready_to_execute_guardrails, 0);
        assert_eq!(summary.release_blocking_guardrails, 3);
        assert_eq!(summary.operator_required_guardrails, 4);
        assert_eq!(summary.review_required_guardrails, 2);
        assert_eq!(summary.dispatch_required_guardrails, 5);
        assert_eq!(summary.execution_required_guardrails, 5);
        assert_eq!(
            summary.first_guardrail_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-01-release_blocker-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_work_order_key,
            Some(
                "release-ticket-handoff-execution-work-order-01-repair-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_release_blocker_work_order_key,
            Some(
                "release-ticket-handoff-execution-work-order-01-repair-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(summary.first_operator_handoff_work_order_key, None);
        assert_eq!(
            summary.first_review_gate_work_order_key,
            Some(
                "release-ticket-handoff-execution-work-order-02-review-evidence_remediation"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_guardrail_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailKind::ReleaseBlocker
            )
        );
        assert_eq!(
            summary.first_handoff_lane,
            Some(IntegrationMeshReleaseDispatchTicketHandoffLane::Repair)
        );
        assert_eq!(
            summary.first_check_kind,
            Some(IntegrationMeshReleaseReadinessCheckKind::SubstrateActions)
        );
        assert_eq!(
            summary.first_check_status,
            Some(IntegrationMeshReleaseReadinessStatus::Blocked)
        );
        assert_eq!(
            summary.next_execution_slot_key,
            Some("release-ticket-handoff-execution-slot-01-repair-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_packet_key,
            Some("release-dispatch-handoff-packet-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_ticket_key,
            Some("release-dispatch-ticket-01-substrate_actions".to_string())
        );
        assert!(!summary.release_handoff_execution_work_order_guardrails_ready);
        assert!(summary.has_guardrails());
        assert!(summary.has_release_blockers());
        assert!(summary.needs_operator());
        assert!(summary.has_review_gates());
        assert!(!summary.has_ready_to_execute_work());
        assert!(summary.requires_attention());

        let first = &guardrails[0];
        assert_eq!(
            first.guardrail_key,
            "release-ticket-handoff-execution-work-order-guardrail-01-release_blocker-substrate_actions"
        );
        assert_eq!(
            first.work_order_key,
            "release-ticket-handoff-execution-work-order-01-repair-substrate_actions"
        );
        assert_eq!(
            first.guardrail_kind,
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailKind::ReleaseBlocker
        );
        assert!(first.blocks_release());
        assert!(first.requires_operator());
        assert!(!first.requires_review());
        assert!(!first.ready_to_execute());
    }

    #[test]
    fn mesh_release_ticket_handoff_execution_work_order_guardrails_mark_release_ready() {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let guardrails = mesh_release_ticket_handoff_execution_work_order_guardrails_for_catalog(
            &catalog,
            all_primitive_families(),
            &allowed_capabilities,
            &[],
        );
        let summary =
            mesh_release_ticket_handoff_execution_work_order_guardrail_summary_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(guardrails.len(), 5);
        assert_eq!(summary.total_guardrails, 5);
        assert_eq!(summary.release_blocker_guardrails, 0);
        assert_eq!(summary.operator_handoff_guardrails, 0);
        assert_eq!(summary.review_gate_guardrails, 0);
        assert_eq!(summary.ready_to_execute_guardrails, 5);
        assert_eq!(summary.release_blocking_guardrails, 0);
        assert_eq!(summary.operator_required_guardrails, 0);
        assert_eq!(summary.review_required_guardrails, 0);
        assert_eq!(summary.dispatch_required_guardrails, 0);
        assert_eq!(summary.execution_required_guardrails, 0);
        assert_eq!(
            summary.first_guardrail_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-01-ready_to_execute-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_work_order_key,
            Some(
                "release-ticket-handoff-execution-work-order-01-release-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(summary.first_release_blocker_work_order_key, None);
        assert_eq!(summary.first_operator_handoff_work_order_key, None);
        assert_eq!(summary.first_review_gate_work_order_key, None);
        assert_eq!(
            summary.first_ready_to_execute_work_order_key,
            Some(
                "release-ticket-handoff-execution-work-order-01-release-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_guardrail_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailKind::ReadyToExecute
            )
        );
        assert_eq!(
            summary.first_handoff_lane,
            Some(IntegrationMeshReleaseDispatchTicketHandoffLane::Release)
        );
        assert_eq!(
            summary.first_check_kind,
            Some(IntegrationMeshReleaseReadinessCheckKind::SubstrateActions)
        );
        assert_eq!(
            summary.first_check_status,
            Some(IntegrationMeshReleaseReadinessStatus::Ready)
        );
        assert_eq!(summary.next_execution_slot_key, None);
        assert_eq!(summary.next_packet_key, None);
        assert_eq!(summary.next_ticket_key, None);
        assert!(summary.release_handoff_execution_work_order_guardrails_ready);
        assert!(summary.has_guardrails());
        assert!(!summary.has_release_blockers());
        assert!(!summary.needs_operator());
        assert!(!summary.has_review_gates());
        assert!(summary.has_ready_to_execute_work());
        assert!(!summary.requires_attention());
        assert!(guardrails
            .iter()
            .all(IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrail::ready_to_execute));
    }

    #[test]
    fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_rows_classify_lane_work() {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let audit_rows = mesh_release_ticket_handoff_execution_work_order_guardrail_audit_rows(
            &available_primitives,
            &allowed_capabilities,
            &[],
        );
        let summary = mesh_release_ticket_handoff_execution_work_order_guardrail_audit_summary(
            &available_primitives,
            &allowed_capabilities,
            &[],
        );

        assert_eq!(audit_rows.len(), 5);
        assert_eq!(summary.total_audit_rows, 5);
        assert_eq!(summary.release_blocker_rows, 3);
        assert_eq!(summary.operator_handoff_rows, 0);
        assert_eq!(summary.review_gate_rows, 2);
        assert_eq!(summary.ready_to_execute_rows, 0);
        assert_eq!(summary.release_blocking_rows, 3);
        assert_eq!(summary.operator_required_rows, 4);
        assert_eq!(summary.review_required_rows, 2);
        assert_eq!(summary.dispatch_required_rows, 5);
        assert_eq!(summary.execution_required_rows, 5);
        assert_eq!(
            summary.first_audit_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-01-release_blocker-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_guardrail_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-01-release_blocker-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_work_order_key,
            Some(
                "release-ticket-handoff-execution-work-order-01-repair-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_release_blocker_work_order_key,
            Some(
                "release-ticket-handoff-execution-work-order-01-repair-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(summary.first_operator_handoff_work_order_key, None);
        assert_eq!(
            summary.first_review_gate_work_order_key,
            Some(
                "release-ticket-handoff-execution-work-order-02-review-evidence_remediation"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_severity,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditSeverity::ReleaseBlocker
            )
        );
        assert_eq!(
            summary.first_handoff_lane,
            Some(IntegrationMeshReleaseDispatchTicketHandoffLane::Repair)
        );
        assert_eq!(
            summary.first_check_kind,
            Some(IntegrationMeshReleaseReadinessCheckKind::SubstrateActions)
        );
        assert_eq!(
            summary.first_check_status,
            Some(IntegrationMeshReleaseReadinessStatus::Blocked)
        );
        assert_eq!(
            summary.next_execution_slot_key,
            Some("release-ticket-handoff-execution-slot-01-repair-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_packet_key,
            Some("release-dispatch-handoff-packet-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_ticket_key,
            Some("release-dispatch-ticket-01-substrate_actions".to_string())
        );
        assert!(!summary.release_handoff_execution_work_order_guardrail_audit_ready);
        assert!(summary.has_audit_rows());
        assert!(summary.has_release_blockers());
        assert!(summary.needs_operator());
        assert!(summary.has_review_gates());
        assert!(!summary.has_ready_to_execute_work());
        assert!(summary.requires_attention());

        let first = &audit_rows[0];
        assert_eq!(
            first.audit_key,
            "release-ticket-handoff-execution-work-order-guardrail-audit-01-release_blocker-substrate_actions"
        );
        assert_eq!(
            first.guardrail_key,
            "release-ticket-handoff-execution-work-order-guardrail-01-release_blocker-substrate_actions"
        );
        assert_eq!(
            first.work_order_key,
            "release-ticket-handoff-execution-work-order-01-repair-substrate_actions"
        );
        assert_eq!(
            first.severity,
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditSeverity::ReleaseBlocker
        );
        assert!(first.blocks_release());
        assert!(first.needs_operator());
        assert!(!first.needs_review());
        assert!(!first.ready_to_execute());
    }

    #[test]
    fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_rows_mark_release_ready() {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let audit_rows =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_rows_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );
        let summary =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_summary_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(audit_rows.len(), 5);
        assert_eq!(summary.total_audit_rows, 5);
        assert_eq!(summary.release_blocker_rows, 0);
        assert_eq!(summary.operator_handoff_rows, 0);
        assert_eq!(summary.review_gate_rows, 0);
        assert_eq!(summary.ready_to_execute_rows, 5);
        assert_eq!(summary.release_blocking_rows, 0);
        assert_eq!(summary.operator_required_rows, 0);
        assert_eq!(summary.review_required_rows, 0);
        assert_eq!(summary.dispatch_required_rows, 0);
        assert_eq!(summary.execution_required_rows, 0);
        assert_eq!(
            summary.first_audit_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-01-ready_to_execute-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_guardrail_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-01-ready_to_execute-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_work_order_key,
            Some(
                "release-ticket-handoff-execution-work-order-01-release-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(summary.first_release_blocker_work_order_key, None);
        assert_eq!(summary.first_operator_handoff_work_order_key, None);
        assert_eq!(summary.first_review_gate_work_order_key, None);
        assert_eq!(
            summary.first_ready_to_execute_work_order_key,
            Some(
                "release-ticket-handoff-execution-work-order-01-release-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_severity,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditSeverity::ReadyToExecute
            )
        );
        assert_eq!(
            summary.first_handoff_lane,
            Some(IntegrationMeshReleaseDispatchTicketHandoffLane::Release)
        );
        assert_eq!(
            summary.first_check_kind,
            Some(IntegrationMeshReleaseReadinessCheckKind::SubstrateActions)
        );
        assert_eq!(
            summary.first_check_status,
            Some(IntegrationMeshReleaseReadinessStatus::Ready)
        );
        assert_eq!(summary.next_execution_slot_key, None);
        assert_eq!(summary.next_packet_key, None);
        assert_eq!(summary.next_ticket_key, None);
        assert!(summary.release_handoff_execution_work_order_guardrail_audit_ready);
        assert!(summary.has_audit_rows());
        assert!(!summary.has_release_blockers());
        assert!(!summary.needs_operator());
        assert!(!summary.has_review_gates());
        assert!(summary.has_ready_to_execute_work());
        assert!(!summary.requires_attention());
        assert!(audit_rows
            .iter()
            .all(IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditRow::ready_to_execute));
    }

    #[test]
    fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_rows_classify_audit_work(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let clearance_rows =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_rows(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );
        let summary =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_summary(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );

        assert_eq!(clearance_rows.len(), 5);
        assert_eq!(summary.total_clearance_rows, 5);
        assert_eq!(summary.release_blocked_clearances, 3);
        assert_eq!(summary.operator_handoff_clearances, 0);
        assert_eq!(summary.review_gate_clearances, 2);
        assert_eq!(summary.cleared_clearances, 0);
        assert_eq!(summary.release_blocking_clearances, 3);
        assert_eq!(summary.operator_required_clearances, 4);
        assert_eq!(summary.review_required_clearances, 2);
        assert_eq!(summary.dispatch_required_clearances, 5);
        assert_eq!(summary.execution_required_clearances, 5);
        assert_eq!(
            summary.first_clearance_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-01-release_blocked-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_audit_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-01-release_blocker-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_guardrail_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-01-release_blocker-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_work_order_key,
            Some(
                "release-ticket-handoff-execution-work-order-01-repair-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_release_blocked_work_order_key,
            Some(
                "release-ticket-handoff-execution-work-order-01-repair-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(summary.first_operator_handoff_work_order_key, None);
        assert_eq!(
            summary.first_review_required_work_order_key,
            Some(
                "release-ticket-handoff-execution-work-order-02-review-evidence_remediation"
                    .to_string()
            )
        );
        assert_eq!(summary.first_cleared_work_order_key, None);
        assert_eq!(
            summary.first_clearance_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind::ReleaseBlocked
            )
        );
        assert_eq!(
            summary.first_audit_severity,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditSeverity::ReleaseBlocker
            )
        );
        assert_eq!(
            summary.first_handoff_lane,
            Some(IntegrationMeshReleaseDispatchTicketHandoffLane::Repair)
        );
        assert_eq!(
            summary.first_check_kind,
            Some(IntegrationMeshReleaseReadinessCheckKind::SubstrateActions)
        );
        assert_eq!(
            summary.first_check_status,
            Some(IntegrationMeshReleaseReadinessStatus::Blocked)
        );
        assert_eq!(
            summary.next_work_order_key,
            Some(
                "release-ticket-handoff-execution-work-order-01-repair-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.next_execution_slot_key,
            Some("release-ticket-handoff-execution-slot-01-repair-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_packet_key,
            Some("release-dispatch-handoff-packet-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_ticket_key,
            Some("release-dispatch-ticket-01-substrate_actions".to_string())
        );
        assert!(!summary.release_handoff_execution_work_order_guardrail_audit_clearance_ready);
        assert!(summary.has_clearance_rows());
        assert!(summary.has_release_blockers());
        assert!(summary.needs_operator());
        assert!(summary.needs_review());
        assert!(!summary.has_ready_clearances());
        assert!(summary.requires_attention());

        let first = &clearance_rows[0];
        assert_eq!(
            first.clearance_key,
            "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-01-release_blocked-substrate_actions"
        );
        assert_eq!(
            first.clearance_kind,
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind::ReleaseBlocked
        );
        assert_eq!(
            first.audit_severity,
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditSeverity::ReleaseBlocker
        );
        assert!(first.blocks_release());
        assert!(first.needs_operator());
        assert!(!first.needs_review());
        assert!(!first.ready_to_clear());
    }

    #[test]
    fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_rows_mark_release_clear(
    ) {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let clearance_rows =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_rows_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );
        let summary =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_summary_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(clearance_rows.len(), 5);
        assert_eq!(summary.total_clearance_rows, 5);
        assert_eq!(summary.release_blocked_clearances, 0);
        assert_eq!(summary.operator_handoff_clearances, 0);
        assert_eq!(summary.review_gate_clearances, 0);
        assert_eq!(summary.cleared_clearances, 5);
        assert_eq!(summary.release_blocking_clearances, 0);
        assert_eq!(summary.operator_required_clearances, 0);
        assert_eq!(summary.review_required_clearances, 0);
        assert_eq!(summary.dispatch_required_clearances, 0);
        assert_eq!(summary.execution_required_clearances, 0);
        assert_eq!(
            summary.first_clearance_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-01-cleared-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_audit_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-01-ready_to_execute-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_work_order_key,
            Some(
                "release-ticket-handoff-execution-work-order-01-release-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(summary.first_release_blocked_work_order_key, None);
        assert_eq!(summary.first_operator_handoff_work_order_key, None);
        assert_eq!(summary.first_review_required_work_order_key, None);
        assert_eq!(
            summary.first_cleared_work_order_key,
            Some(
                "release-ticket-handoff-execution-work-order-01-release-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_clearance_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind::Cleared
            )
        );
        assert_eq!(
            summary.first_audit_severity,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditSeverity::ReadyToExecute
            )
        );
        assert_eq!(
            summary.first_handoff_lane,
            Some(IntegrationMeshReleaseDispatchTicketHandoffLane::Release)
        );
        assert_eq!(summary.next_work_order_key, None);
        assert_eq!(summary.next_execution_slot_key, None);
        assert_eq!(summary.next_packet_key, None);
        assert_eq!(summary.next_ticket_key, None);
        assert!(summary.release_handoff_execution_work_order_guardrail_audit_clearance_ready);
        assert!(summary.has_clearance_rows());
        assert!(!summary.has_release_blockers());
        assert!(!summary.needs_operator());
        assert!(!summary.needs_review());
        assert!(summary.has_ready_clearances());
        assert!(!summary.requires_attention());
        assert!(clearance_rows.iter().all(
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceRow::ready_to_clear
        ));
    }

    #[test]
    fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_actions_route_clearance_work(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let actions =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_actions(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );
        let summary =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_summary(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );

        assert_eq!(actions.len(), 5);
        assert_eq!(summary.total_actions, 5);
        assert_eq!(summary.required_actions, 5);
        assert_eq!(summary.repair_release_blocker_actions, 3);
        assert_eq!(summary.operator_handoff_actions, 0);
        assert_eq!(summary.review_gate_actions, 2);
        assert_eq!(summary.release_clearance_actions, 0);
        assert_eq!(summary.release_blocking_actions, 3);
        assert_eq!(summary.operator_required_actions, 4);
        assert_eq!(summary.review_required_actions, 2);
        assert_eq!(summary.dispatch_required_actions, 5);
        assert_eq!(summary.execution_required_actions, 5);
        assert_eq!(
            summary.first_action_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-01-repair_release_blocker-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(summary.first_required_action_key, summary.first_action_key);
        assert_eq!(
            summary.first_repair_action_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-01-repair_release_blocker-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_review_gate_action_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-02-route_review_gate-evidence_remediation"
                    .to_string()
            )
        );
        assert_eq!(summary.first_release_clearance_action_key, None);
        assert_eq!(
            summary.first_clearance_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-01-release_blocked-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_work_order_key,
            Some(
                "release-ticket-handoff-execution-work-order-01-repair-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_action_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionKind::RepairReleaseBlocker
            )
        );
        assert_eq!(
            summary.first_clearance_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind::ReleaseBlocked
            )
        );
        assert_eq!(
            summary.first_handoff_lane,
            Some(IntegrationMeshReleaseDispatchTicketHandoffLane::Repair)
        );
        assert_eq!(
            summary.first_check_kind,
            Some(IntegrationMeshReleaseReadinessCheckKind::SubstrateActions)
        );
        assert_eq!(
            summary.first_check_status,
            Some(IntegrationMeshReleaseReadinessStatus::Blocked)
        );
        assert!(
            !summary.release_handoff_execution_work_order_guardrail_audit_clearance_actions_ready
        );
        assert!(summary.has_actions());
        assert!(summary.has_required_actions());
        assert!(summary.has_blockers());
        assert!(summary.needs_operator());
        assert!(summary.needs_review());
        assert!(!summary.has_release_clearance_actions());
        assert!(summary.requires_attention());

        let first = &actions[0];
        assert_eq!(
            first.action_kind,
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionKind::RepairReleaseBlocker
        );
        assert!(first.requires_action());
        assert!(first.blocks_release());
        assert!(first.needs_operator());
        assert!(!first.needs_review());
        assert!(!first.release_clearance_ready());
    }

    #[test]
    fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_actions_mark_release_clear(
    ) {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let actions =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_actions_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );
        let summary =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_summary_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(actions.len(), 5);
        assert_eq!(summary.total_actions, 5);
        assert_eq!(summary.required_actions, 0);
        assert_eq!(summary.repair_release_blocker_actions, 0);
        assert_eq!(summary.operator_handoff_actions, 0);
        assert_eq!(summary.review_gate_actions, 0);
        assert_eq!(summary.release_clearance_actions, 5);
        assert_eq!(summary.release_blocking_actions, 0);
        assert_eq!(summary.operator_required_actions, 0);
        assert_eq!(summary.review_required_actions, 0);
        assert_eq!(summary.dispatch_required_actions, 0);
        assert_eq!(summary.execution_required_actions, 0);
        assert_eq!(
            summary.first_action_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-01-release_clearance-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(summary.first_required_action_key, None);
        assert_eq!(summary.first_repair_action_key, None);
        assert_eq!(summary.first_operator_handoff_action_key, None);
        assert_eq!(summary.first_review_gate_action_key, None);
        assert_eq!(
            summary.first_release_clearance_action_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-01-release_clearance-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_clearance_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-01-cleared-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_work_order_key,
            Some(
                "release-ticket-handoff-execution-work-order-01-release-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_action_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionKind::ReleaseClearance
            )
        );
        assert_eq!(
            summary.first_clearance_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind::Cleared
            )
        );
        assert_eq!(
            summary.first_handoff_lane,
            Some(IntegrationMeshReleaseDispatchTicketHandoffLane::Release)
        );
        assert!(
            summary.release_handoff_execution_work_order_guardrail_audit_clearance_actions_ready
        );
        assert!(summary.has_actions());
        assert!(!summary.has_required_actions());
        assert!(!summary.has_blockers());
        assert!(!summary.needs_operator());
        assert!(!summary.needs_review());
        assert!(summary.has_release_clearance_actions());
        assert!(!summary.requires_attention());
        assert!(actions.iter().all(
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceAction::release_clearance_ready
        ));
    }

    #[test]
    fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_surfaces_lineage(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let evidence =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );
        let summary =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_summary(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );

        assert_eq!(evidence.len(), 5);
        assert_eq!(summary.total_evidence, 5);
        assert_eq!(summary.blocking_evidence, 3);
        assert_eq!(summary.operator_required_evidence, 4);
        assert_eq!(summary.review_required_evidence, 2);
        assert_eq!(summary.dispatch_required_evidence, 5);
        assert_eq!(summary.execution_required_evidence, 5);
        assert_eq!(summary.release_evidence, 0);
        assert_eq!(summary.lineage_complete_evidence, 5);
        assert_eq!(
            summary.first_evidence_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-evidence-01-blocker_evidence-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_blocking_evidence_key,
            summary.first_evidence_key
        );
        assert_eq!(
            summary.first_operator_evidence_key,
            summary.first_evidence_key
        );
        assert_eq!(
            summary.first_review_evidence_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-evidence-02-review_evidence-evidence_remediation"
                    .to_string()
            )
        );
        assert_eq!(summary.first_release_evidence_key, None);
        assert_eq!(
            summary.first_action_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-01-repair_release_blocker-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_clearance_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-01-release_blocked-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_work_order_key,
            Some(
                "release-ticket-handoff-execution-work-order-01-repair-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_ticket_key,
            Some("release-dispatch-ticket-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.first_evidence_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceKind::BlockerEvidence
            )
        );
        assert_eq!(
            summary.first_action_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionKind::RepairReleaseBlocker
            )
        );
        assert_eq!(
            summary.first_clearance_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind::ReleaseBlocked
            )
        );
        assert_eq!(
            summary.first_handoff_lane,
            Some(IntegrationMeshReleaseDispatchTicketHandoffLane::Repair)
        );
        assert_eq!(
            summary.first_check_kind,
            Some(IntegrationMeshReleaseReadinessCheckKind::SubstrateActions)
        );
        assert_eq!(
            summary.first_check_status,
            Some(IntegrationMeshReleaseReadinessStatus::Blocked)
        );
        assert!(
            !summary.release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_ready
        );
        assert!(summary.has_evidence());
        assert!(summary.has_blockers());
        assert!(summary.needs_operator());
        assert!(summary.needs_review());
        assert!(!summary.has_release_evidence());
        assert!(summary.has_complete_lineage());
        assert!(summary.requires_attention());

        let first = &evidence[0];
        assert_eq!(first.sequence, 1);
        assert_eq!(
            first.evidence_key,
            "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-evidence-01-blocker_evidence-substrate_actions"
        );
        assert_eq!(first.action_sequence, 1);
        assert_eq!(first.clearance_sequence, 1);
        assert_eq!(first.check_sequence, 1);
        assert!(first.blocks_release());
        assert!(first.needs_operator());
        assert!(!first.needs_review());
        assert!(!first.is_release_evidence());
        assert!(first.has_lineage());
    }

    #[test]
    fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_marks_release_ready(
    ) {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let evidence =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );
        let summary =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_summary_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(evidence.len(), 5);
        assert_eq!(summary.total_evidence, 5);
        assert_eq!(summary.blocking_evidence, 0);
        assert_eq!(summary.operator_required_evidence, 0);
        assert_eq!(summary.review_required_evidence, 0);
        assert_eq!(summary.dispatch_required_evidence, 0);
        assert_eq!(summary.execution_required_evidence, 0);
        assert_eq!(summary.release_evidence, 5);
        assert_eq!(summary.lineage_complete_evidence, 5);
        assert_eq!(
            summary.first_evidence_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-evidence-01-release_evidence-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(summary.first_blocking_evidence_key, None);
        assert_eq!(summary.first_operator_evidence_key, None);
        assert_eq!(summary.first_review_evidence_key, None);
        assert_eq!(
            summary.first_release_evidence_key,
            summary.first_evidence_key
        );
        assert_eq!(
            summary.first_action_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-01-release_clearance-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_evidence_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceKind::ReleaseEvidence
            )
        );
        assert_eq!(
            summary.first_action_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionKind::ReleaseClearance
            )
        );
        assert_eq!(
            summary.first_clearance_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind::Cleared
            )
        );
        assert!(
            summary.release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_ready
        );
        assert!(summary.has_evidence());
        assert!(!summary.has_blockers());
        assert!(!summary.needs_operator());
        assert!(!summary.needs_review());
        assert!(summary.has_release_evidence());
        assert!(summary.has_complete_lineage());
        assert!(!summary.requires_attention());
        assert!(evidence.iter().all(
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidence::is_release_evidence
        ));
    }

    #[test]
    fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_reviews_surface_attention(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let reviews =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_reviews(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );
        let summary =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_summary(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );

        assert_eq!(reviews.len(), 5);
        assert_eq!(summary.total_reviews, 5);
        assert_eq!(summary.blocking_reviews, 3);
        assert_eq!(summary.operator_required_reviews, 4);
        assert_eq!(summary.review_required_reviews, 2);
        assert_eq!(summary.dispatch_required_reviews, 5);
        assert_eq!(summary.execution_required_reviews, 5);
        assert_eq!(summary.lineage_gap_reviews, 0);
        assert_eq!(summary.release_ready_reviews, 0);
        assert_eq!(summary.lineage_complete_reviews, 5);
        assert_eq!(
            summary.first_review_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-evidence-review-01-blocker_review-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(summary.first_blocking_review_key, summary.first_review_key);
        assert_eq!(summary.first_operator_review_key, summary.first_review_key);
        assert_eq!(
            summary.first_review_gate_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-evidence-review-02-review_gate-evidence_remediation"
                    .to_string()
            )
        );
        assert_eq!(summary.first_lineage_gap_review_key, None);
        assert_eq!(summary.first_release_ready_review_key, None);
        assert_eq!(
            summary.first_evidence_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-evidence-01-blocker_evidence-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_action_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-01-repair_release_blocker-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_clearance_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-01-release_blocked-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_work_order_key,
            Some(
                "release-ticket-handoff-execution-work-order-01-repair-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_ticket_key,
            Some("release-dispatch-ticket-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.first_review_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::BlockerReview
            )
        );
        assert_eq!(
            summary.first_evidence_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceKind::BlockerEvidence
            )
        );
        assert_eq!(
            summary.first_action_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionKind::RepairReleaseBlocker
            )
        );
        assert_eq!(
            summary.first_clearance_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind::ReleaseBlocked
            )
        );
        assert_eq!(
            summary.first_handoff_lane,
            Some(IntegrationMeshReleaseDispatchTicketHandoffLane::Repair)
        );
        assert_eq!(
            summary.first_check_kind,
            Some(IntegrationMeshReleaseReadinessCheckKind::SubstrateActions)
        );
        assert_eq!(
            summary.first_check_status,
            Some(IntegrationMeshReleaseReadinessStatus::Blocked)
        );
        assert!(
            !summary.release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_reviews_ready
        );
        assert!(summary.has_reviews());
        assert!(summary.has_blockers());
        assert!(summary.needs_operator());
        assert!(summary.needs_review());
        assert!(!summary.has_lineage_gaps());
        assert!(!summary.has_release_ready_reviews());
        assert!(summary.has_complete_lineage());
        assert!(summary.requires_attention());

        let first = &reviews[0];
        assert_eq!(first.sequence, 1);
        assert_eq!(
            first.review_key,
            "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-evidence-review-01-blocker_review-substrate_actions"
        );
        assert_eq!(first.evidence_sequence, 1);
        assert_eq!(first.action_sequence, 1);
        assert_eq!(first.clearance_sequence, 1);
        assert_eq!(first.check_sequence, 1);
        assert_eq!(
            first.review_kind,
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::BlockerReview
        );
        assert!(first.blocks_release());
        assert!(first.needs_operator());
        assert!(!first.needs_review());
        assert!(first.has_lineage());
        assert!(!first.is_release_ready());
        assert!(first.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_reviews_mark_release_ready(
    ) {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let reviews =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_reviews_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );
        let summary =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_summary_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(reviews.len(), 5);
        assert_eq!(summary.total_reviews, 5);
        assert_eq!(summary.blocking_reviews, 0);
        assert_eq!(summary.operator_required_reviews, 0);
        assert_eq!(summary.review_required_reviews, 0);
        assert_eq!(summary.dispatch_required_reviews, 0);
        assert_eq!(summary.execution_required_reviews, 0);
        assert_eq!(summary.lineage_gap_reviews, 0);
        assert_eq!(summary.release_ready_reviews, 5);
        assert_eq!(summary.lineage_complete_reviews, 5);
        assert_eq!(
            summary.first_review_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-evidence-review-01-release_ready-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(summary.first_blocking_review_key, None);
        assert_eq!(summary.first_operator_review_key, None);
        assert_eq!(summary.first_review_gate_key, None);
        assert_eq!(summary.first_lineage_gap_review_key, None);
        assert_eq!(
            summary.first_release_ready_review_key,
            summary.first_review_key
        );
        assert_eq!(
            summary.first_evidence_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-evidence-01-release_evidence-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_review_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::ReleaseReady
            )
        );
        assert_eq!(
            summary.first_evidence_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceKind::ReleaseEvidence
            )
        );
        assert_eq!(
            summary.first_action_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionKind::ReleaseClearance
            )
        );
        assert_eq!(
            summary.first_clearance_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind::Cleared
            )
        );
        assert!(
            summary.release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_reviews_ready
        );
        assert!(summary.has_reviews());
        assert!(!summary.has_blockers());
        assert!(!summary.needs_operator());
        assert!(!summary.needs_review());
        assert!(!summary.has_lineage_gaps());
        assert!(summary.has_release_ready_reviews());
        assert!(summary.has_complete_lineage());
        assert!(!summary.requires_attention());
        assert!(reviews.iter().all(
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReview::is_release_ready
        ));
    }

    #[test]
    fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_dispositions_classify_attention(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let dispositions =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_dispositions(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );
        let summary =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_summary(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );

        assert_eq!(dispositions.len(), 5);
        assert_eq!(summary.total_dispositions, 5);
        assert_eq!(summary.release_blocking_dispositions, 3);
        assert_eq!(summary.operator_required_dispositions, 4);
        assert_eq!(summary.review_required_dispositions, 2);
        assert_eq!(summary.dispatch_required_dispositions, 5);
        assert_eq!(summary.execution_required_dispositions, 5);
        assert_eq!(summary.repair_required_dispositions, 3);
        assert_eq!(summary.operator_handoff_dispositions, 0);
        assert_eq!(summary.review_gate_dispositions, 2);
        assert_eq!(summary.lineage_gap_dispositions, 0);
        assert_eq!(summary.release_ready_dispositions, 0);
        assert_eq!(summary.lineage_complete_dispositions, 5);
        assert_eq!(
            summary.first_disposition_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-evidence-review-disposition-01-repair_release_blocker-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_repair_required_disposition_key,
            summary.first_disposition_key
        );
        assert_eq!(summary.first_operator_handoff_disposition_key, None);
        assert_eq!(
            summary.first_review_gate_disposition_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-evidence-review-disposition-02-review_required-evidence_remediation"
                    .to_string()
            )
        );
        assert_eq!(summary.first_lineage_gap_disposition_key, None);
        assert_eq!(summary.first_release_ready_disposition_key, None);
        assert_eq!(
            summary.first_review_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-evidence-review-01-blocker_review-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_evidence_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-evidence-01-blocker_evidence-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_action_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-01-repair_release_blocker-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_clearance_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-01-release_blocked-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_work_order_key,
            Some(
                "release-ticket-handoff-execution-work-order-01-repair-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_ticket_key,
            Some("release-dispatch-ticket-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.first_disposition_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind::RepairReleaseBlocker
            )
        );
        assert_eq!(
            summary.first_review_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::BlockerReview
            )
        );
        assert_eq!(
            summary.first_evidence_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceKind::BlockerEvidence
            )
        );
        assert_eq!(
            summary.first_action_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionKind::RepairReleaseBlocker
            )
        );
        assert_eq!(
            summary.first_clearance_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind::ReleaseBlocked
            )
        );
        assert_eq!(
            summary.first_handoff_lane,
            Some(IntegrationMeshReleaseDispatchTicketHandoffLane::Repair)
        );
        assert_eq!(
            summary.first_check_kind,
            Some(IntegrationMeshReleaseReadinessCheckKind::SubstrateActions)
        );
        assert_eq!(
            summary.first_check_status,
            Some(IntegrationMeshReleaseReadinessStatus::Blocked)
        );
        assert!(
            !summary
                .release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_dispositions_ready
        );
        assert!(summary.has_dispositions());
        assert!(summary.has_blockers());
        assert!(summary.needs_operator());
        assert!(summary.needs_review());
        assert!(!summary.has_lineage_gaps());
        assert!(!summary.has_release_ready_dispositions());
        assert!(summary.has_complete_lineage());
        assert!(summary.requires_attention());

        let first = &dispositions[0];
        assert_eq!(first.sequence, 1);
        assert_eq!(
            first.disposition_key,
            "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-evidence-review-disposition-01-repair_release_blocker-substrate_actions"
        );
        assert_eq!(first.review_sequence, 1);
        assert_eq!(
            first.review_key,
            "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-evidence-review-01-blocker_review-substrate_actions"
        );
        assert_eq!(first.evidence_sequence, 1);
        assert_eq!(
            first.evidence_key,
            "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-evidence-01-blocker_evidence-substrate_actions"
        );
        assert_eq!(
            first.disposition_kind,
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind::RepairReleaseBlocker
        );
        assert_eq!(first.disposition_kind.as_str(), "repair_release_blocker");
        assert!(first.blocks_release());
        assert!(first.needs_operator());
        assert!(!first.needs_review());
        assert!(first.has_lineage());
        assert!(!first.is_release_ready());
        assert!(first.is_repair_required());
        assert!(!first.is_operator_handoff());
        assert!(!first.is_review_required());
        assert!(!first.is_lineage_gap());
        assert!(first.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_dispositions_mark_release_ready(
    ) {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let dispositions =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_dispositions_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );
        let summary =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_summary_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(dispositions.len(), 5);
        assert_eq!(summary.total_dispositions, 5);
        assert_eq!(summary.release_blocking_dispositions, 0);
        assert_eq!(summary.operator_required_dispositions, 0);
        assert_eq!(summary.review_required_dispositions, 0);
        assert_eq!(summary.dispatch_required_dispositions, 0);
        assert_eq!(summary.execution_required_dispositions, 0);
        assert_eq!(summary.repair_required_dispositions, 0);
        assert_eq!(summary.operator_handoff_dispositions, 0);
        assert_eq!(summary.review_gate_dispositions, 0);
        assert_eq!(summary.lineage_gap_dispositions, 0);
        assert_eq!(summary.release_ready_dispositions, 5);
        assert_eq!(summary.lineage_complete_dispositions, 5);
        assert_eq!(
            summary.first_disposition_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-evidence-review-disposition-01-release_ready-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(summary.first_repair_required_disposition_key, None);
        assert_eq!(summary.first_operator_handoff_disposition_key, None);
        assert_eq!(summary.first_review_gate_disposition_key, None);
        assert_eq!(summary.first_lineage_gap_disposition_key, None);
        assert_eq!(
            summary.first_release_ready_disposition_key,
            summary.first_disposition_key
        );
        assert_eq!(
            summary.first_review_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-evidence-review-01-release_ready-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_evidence_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-evidence-01-release_evidence-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_disposition_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind::ReleaseReady
            )
        );
        assert_eq!(
            summary.first_review_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::ReleaseReady
            )
        );
        assert_eq!(
            summary.first_evidence_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceKind::ReleaseEvidence
            )
        );
        assert_eq!(
            summary.first_action_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionKind::ReleaseClearance
            )
        );
        assert_eq!(
            summary.first_clearance_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind::Cleared
            )
        );
        assert!(
            summary
                .release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_dispositions_ready
        );
        assert!(summary.has_dispositions());
        assert!(!summary.has_blockers());
        assert!(!summary.needs_operator());
        assert!(!summary.needs_review());
        assert!(!summary.has_lineage_gaps());
        assert!(summary.has_release_ready_dispositions());
        assert!(summary.has_complete_lineage());
        assert!(!summary.requires_attention());
        assert!(dispositions.iter().all(
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDisposition::is_release_ready
        ));
    }

    #[test]
    fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_actions_route_work(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let actions =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_actions(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );
        let summary =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );

        assert_eq!(actions.len(), 5);
        assert_eq!(summary.total_actions, 5);
        assert_eq!(summary.required_actions, 5);
        assert_eq!(summary.release_blocking_actions, 3);
        assert_eq!(summary.operator_required_actions, 4);
        assert_eq!(summary.review_required_actions, 2);
        assert_eq!(summary.dispatch_required_actions, 5);
        assert_eq!(summary.execution_required_actions, 5);
        assert_eq!(summary.repair_actions, 3);
        assert_eq!(summary.operator_handoff_actions, 0);
        assert_eq!(summary.review_gate_actions, 2);
        assert_eq!(summary.lineage_gap_actions, 0);
        assert_eq!(summary.release_handoff_actions, 0);
        assert_eq!(summary.lineage_complete_actions, 5);
        assert_eq!(
            summary.first_action_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-evidence-review-disposition-action-01-schedule_release_blocker_repair-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(summary.first_required_action_key, summary.first_action_key);
        assert_eq!(summary.first_repair_action_key, summary.first_action_key);
        assert_eq!(summary.first_operator_handoff_action_key, None);
        assert_eq!(
            summary.first_review_gate_action_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-evidence-review-disposition-action-02-complete_evidence_review-evidence_remediation"
                    .to_string()
            )
        );
        assert_eq!(summary.first_lineage_gap_action_key, None);
        assert_eq!(summary.first_release_handoff_action_key, None);
        assert_eq!(
            summary.first_disposition_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-evidence-review-disposition-01-repair_release_blocker-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_review_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-evidence-review-01-blocker_review-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_evidence_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-evidence-01-blocker_evidence-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_clearance_action_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-01-repair_release_blocker-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_clearance_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-01-release_blocked-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_work_order_key,
            Some(
                "release-ticket-handoff-execution-work-order-01-repair-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_ticket_key,
            Some("release-dispatch-ticket-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.first_disposition_action_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionKind::ScheduleReleaseBlockerRepair
            )
        );
        assert_eq!(
            summary.first_disposition_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind::RepairReleaseBlocker
            )
        );
        assert_eq!(
            summary.first_review_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::BlockerReview
            )
        );
        assert_eq!(
            summary.first_evidence_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceKind::BlockerEvidence
            )
        );
        assert_eq!(
            summary.first_clearance_action_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionKind::RepairReleaseBlocker
            )
        );
        assert_eq!(
            summary.first_clearance_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind::ReleaseBlocked
            )
        );
        assert_eq!(
            summary.first_handoff_lane,
            Some(IntegrationMeshReleaseDispatchTicketHandoffLane::Repair)
        );
        assert_eq!(
            summary.first_check_kind,
            Some(IntegrationMeshReleaseReadinessCheckKind::SubstrateActions)
        );
        assert_eq!(
            summary.first_check_status,
            Some(IntegrationMeshReleaseReadinessStatus::Blocked)
        );
        assert!(
            !summary
                .release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_actions_ready
        );
        assert!(summary.has_actions());
        assert!(summary.has_required_actions());
        assert!(summary.has_blockers());
        assert!(summary.needs_operator());
        assert!(summary.needs_review());
        assert!(!summary.has_lineage_gaps());
        assert!(!summary.has_release_handoff_actions());
        assert!(summary.has_complete_lineage());
        assert!(summary.requires_attention());

        let first = &actions[0];
        assert_eq!(first.sequence, 1);
        assert_eq!(
            first.disposition_action_key,
            "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-evidence-review-disposition-action-01-schedule_release_blocker_repair-substrate_actions"
        );
        assert_eq!(first.disposition_sequence, 1);
        assert_eq!(
            first.disposition_key,
            "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-evidence-review-disposition-01-repair_release_blocker-substrate_actions"
        );
        assert_eq!(
            first.disposition_action_kind,
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionKind::ScheduleReleaseBlockerRepair
        );
        assert_eq!(
            first.disposition_action_kind.as_str(),
            "schedule_release_blocker_repair"
        );
        assert!(first.requires_action());
        assert!(first.blocks_release());
        assert!(first.needs_operator());
        assert!(!first.needs_review());
        assert!(first.has_lineage());
        assert!(!first.is_release_ready());
        assert!(first.is_repair_action());
        assert!(!first.is_operator_handoff_action());
        assert!(!first.is_review_gate_action());
        assert!(!first.is_lineage_action());
        assert!(!first.is_release_handoff_action());
        assert!(first.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_actions_mark_release_handoff(
    ) {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let actions =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_actions_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );
        let summary =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_summary_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(actions.len(), 5);
        assert_eq!(summary.total_actions, 5);
        assert_eq!(summary.required_actions, 0);
        assert_eq!(summary.release_blocking_actions, 0);
        assert_eq!(summary.operator_required_actions, 0);
        assert_eq!(summary.review_required_actions, 0);
        assert_eq!(summary.dispatch_required_actions, 0);
        assert_eq!(summary.execution_required_actions, 0);
        assert_eq!(summary.repair_actions, 0);
        assert_eq!(summary.operator_handoff_actions, 0);
        assert_eq!(summary.review_gate_actions, 0);
        assert_eq!(summary.lineage_gap_actions, 0);
        assert_eq!(summary.release_handoff_actions, 5);
        assert_eq!(summary.lineage_complete_actions, 5);
        assert_eq!(
            summary.first_action_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-evidence-review-disposition-action-01-release_ticket_handoff-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(summary.first_required_action_key, None);
        assert_eq!(summary.first_repair_action_key, None);
        assert_eq!(summary.first_operator_handoff_action_key, None);
        assert_eq!(summary.first_review_gate_action_key, None);
        assert_eq!(summary.first_lineage_gap_action_key, None);
        assert_eq!(
            summary.first_release_handoff_action_key,
            summary.first_action_key
        );
        assert_eq!(
            summary.first_disposition_action_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionKind::ReleaseTicketHandoff
            )
        );
        assert_eq!(
            summary.first_disposition_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind::ReleaseReady
            )
        );
        assert_eq!(
            summary.first_review_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::ReleaseReady
            )
        );
        assert_eq!(
            summary.first_evidence_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceKind::ReleaseEvidence
            )
        );
        assert_eq!(
            summary.first_clearance_action_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionKind::ReleaseClearance
            )
        );
        assert_eq!(
            summary.first_clearance_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind::Cleared
            )
        );
        assert!(
            summary
                .release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_actions_ready
        );
        assert!(summary.has_actions());
        assert!(!summary.has_required_actions());
        assert!(!summary.has_blockers());
        assert!(!summary.needs_operator());
        assert!(!summary.needs_review());
        assert!(!summary.has_lineage_gaps());
        assert!(summary.has_release_handoff_actions());
        assert!(summary.has_complete_lineage());
        assert!(!summary.requires_attention());
        assert!(actions.iter().all(
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionAction::is_release_ready
        ));
        assert!(actions.iter().all(
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionAction::is_release_handoff_action
        ));
    }

    #[test]
    fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slots_sequence_work(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let slots =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slots(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );
        let summary =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_summary(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );

        assert_eq!(slots.len(), 5);
        assert_eq!(summary.total_slots, 5);
        assert_eq!(summary.required_slots, 5);
        assert_eq!(summary.release_blocking_slots, 3);
        assert_eq!(summary.operator_required_slots, 4);
        assert_eq!(summary.review_required_slots, 2);
        assert_eq!(summary.dispatch_required_slots, 5);
        assert_eq!(summary.execution_required_slots, 5);
        assert_eq!(summary.repair_slots, 3);
        assert_eq!(summary.operator_handoff_slots, 0);
        assert_eq!(summary.review_gate_slots, 2);
        assert_eq!(summary.lineage_gap_slots, 0);
        assert_eq!(summary.release_handoff_slots, 0);
        assert_eq!(summary.lineage_complete_slots, 5);
        assert_eq!(
            summary.first_slot_key,
            Some(
                "release-ticket-handoff-disposition-action-slot-01-schedule_release_blocker_repair-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(summary.first_required_slot_key, summary.first_slot_key);
        assert_eq!(summary.first_repair_slot_key, summary.first_slot_key);
        assert_eq!(summary.first_operator_handoff_slot_key, None);
        assert_eq!(
            summary.first_review_gate_slot_key,
            Some(
                "release-ticket-handoff-disposition-action-slot-02-complete_evidence_review-evidence_remediation"
                    .to_string()
            )
        );
        assert_eq!(summary.first_lineage_gap_slot_key, None);
        assert_eq!(summary.first_release_handoff_slot_key, None);
        assert_eq!(
            summary.first_disposition_action_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-evidence-review-disposition-action-01-schedule_release_blocker_repair-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_disposition_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-evidence-review-disposition-01-repair_release_blocker-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_disposition_action_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionKind::ScheduleReleaseBlockerRepair
            )
        );
        assert_eq!(
            summary.first_disposition_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind::RepairReleaseBlocker
            )
        );
        assert_eq!(
            summary.first_handoff_lane,
            Some(IntegrationMeshReleaseDispatchTicketHandoffLane::Repair)
        );
        assert_eq!(
            summary.first_check_kind,
            Some(IntegrationMeshReleaseReadinessCheckKind::SubstrateActions)
        );
        assert_eq!(
            summary.first_check_status,
            Some(IntegrationMeshReleaseReadinessStatus::Blocked)
        );
        assert!(
            !summary
                .release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slots_ready
        );
        assert!(summary.has_slots());
        assert!(summary.has_required_slots());
        assert!(summary.has_blockers());
        assert!(summary.needs_operator());
        assert!(summary.needs_review());
        assert!(summary.needs_repair());
        assert!(!summary.has_lineage_gaps());
        assert!(!summary.has_release_handoff_slots());
        assert!(summary.has_complete_lineage());
        assert!(summary.requires_attention());

        let first = &slots[0];
        assert_eq!(first.sequence, 1);
        assert_eq!(
            first.slot_key,
            "release-ticket-handoff-disposition-action-slot-01-schedule_release_blocker_repair-substrate_actions"
        );
        assert_eq!(first.disposition_action_sequence, 1);
        assert_eq!(
            first.disposition_action_key,
            "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-evidence-review-disposition-action-01-schedule_release_blocker_repair-substrate_actions"
        );
        assert_eq!(
            first.disposition_action_kind,
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionKind::ScheduleReleaseBlockerRepair
        );
        assert!(first.requires_action());
        assert!(first.blocks_release());
        assert!(first.needs_operator());
        assert!(!first.needs_review());
        assert!(first.has_lineage());
        assert!(!first.is_ready_for_handoff());
        assert!(first.is_repair_slot());
        assert!(!first.is_operator_handoff_slot());
        assert!(!first.is_review_gate_slot());
        assert!(!first.is_lineage_slot());
        assert!(!first.is_release_handoff_slot());
        assert!(first.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slots_mark_release_handoff(
    ) {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let slots =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slots_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );
        let summary =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_summary_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(slots.len(), 5);
        assert_eq!(summary.total_slots, 5);
        assert_eq!(summary.required_slots, 0);
        assert_eq!(summary.release_blocking_slots, 0);
        assert_eq!(summary.operator_required_slots, 0);
        assert_eq!(summary.review_required_slots, 0);
        assert_eq!(summary.dispatch_required_slots, 0);
        assert_eq!(summary.execution_required_slots, 0);
        assert_eq!(summary.repair_slots, 0);
        assert_eq!(summary.operator_handoff_slots, 0);
        assert_eq!(summary.review_gate_slots, 0);
        assert_eq!(summary.lineage_gap_slots, 0);
        assert_eq!(summary.release_handoff_slots, 5);
        assert_eq!(summary.lineage_complete_slots, 5);
        assert_eq!(
            summary.first_slot_key,
            Some(
                "release-ticket-handoff-disposition-action-slot-01-release_ticket_handoff-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(summary.first_required_slot_key, None);
        assert_eq!(summary.first_repair_slot_key, None);
        assert_eq!(summary.first_operator_handoff_slot_key, None);
        assert_eq!(summary.first_review_gate_slot_key, None);
        assert_eq!(summary.first_lineage_gap_slot_key, None);
        assert_eq!(
            summary.first_release_handoff_slot_key,
            summary.first_slot_key
        );
        assert_eq!(
            summary.first_disposition_action_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionKind::ReleaseTicketHandoff
            )
        );
        assert_eq!(
            summary.first_disposition_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind::ReleaseReady
            )
        );
        assert!(
            summary
                .release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slots_ready
        );
        assert!(summary.has_slots());
        assert!(!summary.has_required_slots());
        assert!(!summary.has_blockers());
        assert!(!summary.needs_operator());
        assert!(!summary.needs_review());
        assert!(!summary.needs_repair());
        assert!(!summary.has_lineage_gaps());
        assert!(summary.has_release_handoff_slots());
        assert!(summary.has_complete_lineage());
        assert!(!summary.requires_attention());
        assert!(slots.iter().all(
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlot::is_ready_for_handoff
        ));
        assert!(slots.iter().all(
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlot::is_release_handoff_slot
        ));
    }

    #[test]
    fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_rows_classify_work(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let clearance_rows =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_rows(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );
        let summary =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_summary(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );

        assert_eq!(clearance_rows.len(), 5);
        assert_eq!(summary.total_slot_clearance_rows, 5);
        assert_eq!(summary.repair_required_clearances, 3);
        assert_eq!(summary.operator_handoff_clearances, 0);
        assert_eq!(summary.review_required_clearances, 2);
        assert_eq!(summary.lineage_gap_clearances, 0);
        assert_eq!(summary.release_handoff_clearances, 0);
        assert_eq!(summary.required_clearances, 5);
        assert_eq!(summary.release_blocking_clearances, 3);
        assert_eq!(summary.operator_required_clearances, 4);
        assert_eq!(summary.review_required_clearances_count, 2);
        assert_eq!(summary.dispatch_required_clearances, 5);
        assert_eq!(summary.execution_required_clearances, 5);
        assert_eq!(summary.lineage_complete_clearances, 5);
        assert_eq!(
            summary.first_slot_clearance_key,
            Some(
                "release-ticket-handoff-disposition-action-slot-clearance-01-repair_required-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_required_clearance_key,
            summary.first_slot_clearance_key
        );
        assert_eq!(
            summary.first_repair_clearance_key,
            summary.first_slot_clearance_key
        );
        assert_eq!(summary.first_operator_handoff_clearance_key, None);
        assert_eq!(
            summary.first_review_clearance_key,
            Some(
                "release-ticket-handoff-disposition-action-slot-clearance-02-review_required-evidence_remediation"
                    .to_string()
            )
        );
        assert_eq!(summary.first_lineage_gap_clearance_key, None);
        assert_eq!(summary.first_release_handoff_clearance_key, None);
        assert_eq!(
            summary.first_slot_key,
            Some(
                "release-ticket-handoff-disposition-action-slot-01-schedule_release_blocker_repair-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_slot_clearance_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceKind::RepairRequired
            )
        );
        assert_eq!(
            summary.first_disposition_action_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionKind::ScheduleReleaseBlockerRepair
            )
        );
        assert_eq!(
            summary.first_disposition_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind::RepairReleaseBlocker
            )
        );
        assert_eq!(
            summary.first_handoff_lane,
            Some(IntegrationMeshReleaseDispatchTicketHandoffLane::Repair)
        );
        assert_eq!(
            summary.first_check_kind,
            Some(IntegrationMeshReleaseReadinessCheckKind::SubstrateActions)
        );
        assert_eq!(
            summary.first_check_status,
            Some(IntegrationMeshReleaseReadinessStatus::Blocked)
        );
        assert!(
            !summary
                .release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearances_ready
        );
        assert!(summary.has_slot_clearance_rows());
        assert!(summary.has_required_clearances());
        assert!(summary.has_blockers());
        assert!(summary.needs_operator());
        assert!(summary.needs_review());
        assert!(!summary.has_lineage_gaps());
        assert!(!summary.has_release_handoff_clearances());
        assert!(summary.has_complete_lineage());
        assert!(summary.requires_attention());

        let first = &clearance_rows[0];
        assert_eq!(first.sequence, 1);
        assert_eq!(
            first.slot_clearance_kind,
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceKind::RepairRequired
        );
        assert!(first.is_repair_clearance());
        assert!(first.requires_action());
        assert!(first.blocks_release());
        assert!(first.needs_operator());
        assert!(!first.needs_review());
        assert!(first.has_lineage());
        assert!(!first.ready_to_clear());
        assert!(first.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_rows_mark_release_handoff(
    ) {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let clearance_rows =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_rows_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );
        let summary =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_summary_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(clearance_rows.len(), 5);
        assert_eq!(summary.total_slot_clearance_rows, 5);
        assert_eq!(summary.repair_required_clearances, 0);
        assert_eq!(summary.operator_handoff_clearances, 0);
        assert_eq!(summary.review_required_clearances, 0);
        assert_eq!(summary.lineage_gap_clearances, 0);
        assert_eq!(summary.release_handoff_clearances, 5);
        assert_eq!(summary.required_clearances, 0);
        assert_eq!(summary.release_blocking_clearances, 0);
        assert_eq!(summary.operator_required_clearances, 0);
        assert_eq!(summary.review_required_clearances_count, 0);
        assert_eq!(summary.dispatch_required_clearances, 0);
        assert_eq!(summary.execution_required_clearances, 0);
        assert_eq!(summary.lineage_complete_clearances, 5);
        assert_eq!(
            summary.first_slot_clearance_key,
            Some(
                "release-ticket-handoff-disposition-action-slot-clearance-01-release_handoff-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(summary.first_required_clearance_key, None);
        assert_eq!(summary.first_repair_clearance_key, None);
        assert_eq!(summary.first_operator_handoff_clearance_key, None);
        assert_eq!(summary.first_review_clearance_key, None);
        assert_eq!(summary.first_lineage_gap_clearance_key, None);
        assert_eq!(
            summary.first_release_handoff_clearance_key,
            summary.first_slot_clearance_key
        );
        assert_eq!(
            summary.first_slot_clearance_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceKind::ReleaseHandoff
            )
        );
        assert!(
            summary
                .release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearances_ready
        );
        assert!(summary.has_slot_clearance_rows());
        assert!(!summary.has_required_clearances());
        assert!(!summary.has_blockers());
        assert!(!summary.needs_operator());
        assert!(!summary.needs_review());
        assert!(!summary.has_lineage_gaps());
        assert!(summary.has_release_handoff_clearances());
        assert!(summary.has_complete_lineage());
        assert!(!summary.requires_attention());
        assert!(clearance_rows.iter().all(
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceRow::ready_to_clear
        ));
        assert!(clearance_rows.iter().all(
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceRow::is_release_handoff_clearance
        ));
    }

    #[test]
    fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_rows_classify_work(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let action_rows =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_rows(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );
        let summary =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );

        assert_eq!(action_rows.len(), 5);
        assert_eq!(summary.total_slot_clearance_action_rows, 5);
        assert_eq!(summary.repair_actions, 3);
        assert_eq!(summary.operator_handoff_actions, 0);
        assert_eq!(summary.review_actions, 2);
        assert_eq!(summary.lineage_actions, 0);
        assert_eq!(summary.release_handoff_actions, 0);
        assert_eq!(summary.required_actions, 5);
        assert_eq!(summary.release_blocking_actions, 3);
        assert_eq!(summary.operator_required_actions, 4);
        assert_eq!(summary.review_required_actions, 2);
        assert_eq!(summary.dispatch_required_actions, 5);
        assert_eq!(summary.execution_required_actions, 5);
        assert_eq!(summary.lineage_complete_actions, 5);
        assert_eq!(summary.ready_actions, 0);
        assert_eq!(
            summary.first_slot_clearance_action_key,
            Some(
                "release-ticket-handoff-disposition-action-slot-clearance-action-01-schedule_repair-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_required_action_key,
            summary.first_slot_clearance_action_key
        );
        assert_eq!(
            summary.first_repair_action_key,
            summary.first_slot_clearance_action_key
        );
        assert_eq!(summary.first_operator_handoff_action_key, None);
        assert_eq!(
            summary.first_review_action_key,
            Some(
                "release-ticket-handoff-disposition-action-slot-clearance-action-02-complete_review-evidence_remediation"
                    .to_string()
            )
        );
        assert_eq!(summary.first_lineage_action_key, None);
        assert_eq!(summary.first_release_handoff_action_key, None);
        assert_eq!(
            summary.first_slot_clearance_key,
            Some(
                "release-ticket-handoff-disposition-action-slot-clearance-01-repair_required-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_slot_key,
            Some(
                "release-ticket-handoff-disposition-action-slot-01-schedule_release_blocker_repair-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_slot_clearance_action_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionKind::ScheduleRepair
            )
        );
        assert_eq!(
            summary.first_slot_clearance_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceKind::RepairRequired
            )
        );
        assert_eq!(
            summary.first_disposition_action_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionKind::ScheduleReleaseBlockerRepair
            )
        );
        assert_eq!(
            summary.first_disposition_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind::RepairReleaseBlocker
            )
        );
        assert_eq!(
            summary.first_handoff_lane,
            Some(IntegrationMeshReleaseDispatchTicketHandoffLane::Repair)
        );
        assert_eq!(
            summary.first_check_kind,
            Some(IntegrationMeshReleaseReadinessCheckKind::SubstrateActions)
        );
        assert_eq!(
            summary.first_check_status,
            Some(IntegrationMeshReleaseReadinessStatus::Blocked)
        );
        assert!(
            !summary
                .release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_actions_ready
        );
        assert!(summary.has_slot_clearance_action_rows());
        assert!(summary.has_required_actions());
        assert!(summary.has_blockers());
        assert!(summary.needs_operator());
        assert!(summary.needs_review());
        assert!(!summary.has_lineage_gaps());
        assert!(!summary.has_release_handoff_actions());
        assert!(summary.has_complete_lineage());
        assert!(summary.requires_attention());

        let first = &action_rows[0];
        assert_eq!(first.sequence, 1);
        assert_eq!(
            first.slot_clearance_action_kind,
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionKind::ScheduleRepair
        );
        assert!(first.is_repair_action());
        assert!(first.requires_action());
        assert!(first.blocks_release());
        assert!(first.needs_operator());
        assert!(!first.needs_review());
        assert!(first.has_lineage());
        assert!(!first.ready_to_release_handoff());
        assert!(first.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_rows_mark_release_handoff(
    ) {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let action_rows =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_rows_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );
        let summary =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_summary_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(action_rows.len(), 5);
        assert_eq!(summary.total_slot_clearance_action_rows, 5);
        assert_eq!(summary.repair_actions, 0);
        assert_eq!(summary.operator_handoff_actions, 0);
        assert_eq!(summary.review_actions, 0);
        assert_eq!(summary.lineage_actions, 0);
        assert_eq!(summary.release_handoff_actions, 5);
        assert_eq!(summary.required_actions, 0);
        assert_eq!(summary.release_blocking_actions, 0);
        assert_eq!(summary.operator_required_actions, 0);
        assert_eq!(summary.review_required_actions, 0);
        assert_eq!(summary.dispatch_required_actions, 0);
        assert_eq!(summary.execution_required_actions, 0);
        assert_eq!(summary.lineage_complete_actions, 5);
        assert_eq!(summary.ready_actions, 5);
        assert_eq!(
            summary.first_slot_clearance_action_key,
            Some(
                "release-ticket-handoff-disposition-action-slot-clearance-action-01-release_handoff-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(summary.first_required_action_key, None);
        assert_eq!(summary.first_repair_action_key, None);
        assert_eq!(summary.first_operator_handoff_action_key, None);
        assert_eq!(summary.first_review_action_key, None);
        assert_eq!(summary.first_lineage_action_key, None);
        assert_eq!(
            summary.first_release_handoff_action_key,
            summary.first_slot_clearance_action_key
        );
        assert_eq!(
            summary.first_slot_clearance_key,
            Some(
                "release-ticket-handoff-disposition-action-slot-clearance-01-release_handoff-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_slot_clearance_action_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionKind::ReleaseHandoff
            )
        );
        assert_eq!(
            summary.first_slot_clearance_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceKind::ReleaseHandoff
            )
        );
        assert!(
            summary
                .release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_actions_ready
        );
        assert!(summary.has_slot_clearance_action_rows());
        assert!(!summary.has_required_actions());
        assert!(!summary.has_blockers());
        assert!(!summary.needs_operator());
        assert!(!summary.needs_review());
        assert!(!summary.has_lineage_gaps());
        assert!(summary.has_release_handoff_actions());
        assert!(summary.has_complete_lineage());
        assert!(!summary.requires_attention());
        assert!(action_rows.iter().all(
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionRow::ready_to_release_handoff
        ));
        assert!(action_rows.iter().all(
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionRow::is_release_handoff_action
        ));
    }

    #[test]
    fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_readiness_surfaces_evidence_attention(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let summary =
            mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_readiness_summary(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );

        assert_eq!(summary.total_slot_clearance_action_rows, 5);
        assert_eq!(summary.total_slot_clearance_action_evidence_rows, 5);
        assert_eq!(summary.required_action_evidence, 5);
        assert_eq!(summary.repair_action_evidence, 3);
        assert_eq!(summary.review_action_evidence, 2);
        assert_eq!(summary.release_handoff_action_evidence, 0);
        assert_eq!(summary.ready_action_evidence, 0);
        assert_eq!(
            summary.first_slot_clearance_action_evidence_key,
            Some(
                "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-01-repair_evidence-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_required_action_evidence_key,
            summary.first_slot_clearance_action_evidence_key
        );
        assert_eq!(
            summary.first_slot_clearance_action_key,
            Some(
                "release-ticket-handoff-disposition-action-slot-clearance-action-01-schedule_repair-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_slot_clearance_action_evidence_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceKind::RepairEvidence
            )
        );
        assert_eq!(
            summary.next_slot_clearance_action_evidence_key,
            summary.first_slot_clearance_action_evidence_key
        );
        assert_eq!(
            summary.next_slot_clearance_action_key,
            summary.first_slot_clearance_action_key
        );
        assert_eq!(
            summary.next_work_order_key,
            Some(
                "release-ticket-handoff-execution-work-order-01-repair-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.next_ticket_key,
            Some("release-dispatch-ticket-01-substrate_actions".to_string())
        );
        assert!(
            !summary
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_ready
        );
        assert!(!summary
            .ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence());
        assert!(summary.has_slot_clearance_action_evidence_rows());
        assert!(summary.has_required_action_evidence());
        assert!(summary.has_blockers());
        assert!(summary.has_review_work());
        assert!(summary.needs_operator());
        assert!(!summary.has_release_handoff_action_evidence());
        assert!(summary.has_complete_lineage());
        assert!(summary.requires_execution());
        assert!(summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_readiness_marks_release_ready(
    ) {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let summary =
            mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_readiness_summary_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(summary.total_slot_clearance_action_rows, 5);
        assert_eq!(summary.total_slot_clearance_action_evidence_rows, 5);
        assert_eq!(summary.required_action_evidence, 0);
        assert_eq!(summary.repair_action_evidence, 0);
        assert_eq!(summary.review_action_evidence, 0);
        assert_eq!(summary.release_handoff_action_evidence, 5);
        assert_eq!(summary.ready_action_evidence, 5);
        assert_eq!(
            summary.first_slot_clearance_action_evidence_key,
            Some(
                "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-01-release_handoff_evidence-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(summary.first_required_action_evidence_key, None);
        assert_eq!(
            summary.first_release_handoff_action_evidence_key,
            summary.first_slot_clearance_action_evidence_key
        );
        assert_eq!(
            summary.first_slot_clearance_action_evidence_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceKind::ReleaseHandoffEvidence
            )
        );
        assert_eq!(summary.next_slot_clearance_action_evidence_key, None);
        assert_eq!(summary.next_slot_clearance_action_key, None);
        assert_eq!(summary.next_slot_clearance_key, None);
        assert_eq!(summary.next_work_order_key, None);
        assert_eq!(summary.next_ticket_key, None);
        assert!(
            summary
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_ready
        );
        assert!(summary
            .ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence());
        assert!(summary.has_slot_clearance_action_evidence_rows());
        assert!(!summary.has_required_action_evidence());
        assert!(!summary.has_blockers());
        assert!(!summary.has_review_work());
        assert!(!summary.needs_operator());
        assert!(summary.has_release_handoff_action_evidence());
        assert!(summary.has_complete_lineage());
        assert!(!summary.requires_execution());
        assert!(!summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_readiness_surfaces_review_attention(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let summary =
            mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_readiness_summary(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );

        assert_eq!(summary.total_slot_clearance_action_evidence_rows, 5);
        assert_eq!(summary.total_slot_clearance_action_evidence_reviews, 5);
        assert_eq!(summary.required_action_evidence_reviews, 5);
        assert_eq!(summary.blocking_action_evidence_reviews, 3);
        assert_eq!(summary.operator_required_action_evidence_reviews, 4);
        assert_eq!(summary.review_required_action_evidence_reviews, 2);
        assert_eq!(summary.dispatch_required_action_evidence_reviews, 5);
        assert_eq!(summary.execution_required_action_evidence_reviews, 5);
        assert_eq!(summary.lineage_gap_action_evidence_reviews, 0);
        assert_eq!(summary.release_ready_action_evidence_reviews, 0);
        assert_eq!(summary.release_handoff_action_evidence_reviews, 0);
        assert_eq!(summary.lineage_complete_action_evidence_reviews, 5);
        assert_eq!(
            summary.first_slot_clearance_action_evidence_review_key,
            Some(
                "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-review-01-blocker_review-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_required_action_evidence_review_key,
            summary.first_slot_clearance_action_evidence_review_key
        );
        assert_eq!(
            summary.first_blocking_action_evidence_review_key,
            summary.first_slot_clearance_action_evidence_review_key
        );
        assert_eq!(
            summary.first_slot_clearance_action_evidence_key,
            Some(
                "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-01-repair_evidence-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_action_evidence_review_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::BlockerReview
            )
        );
        assert_eq!(
            summary.next_slot_clearance_action_evidence_review_key,
            summary.first_slot_clearance_action_evidence_review_key
        );
        assert_eq!(
            summary.next_slot_clearance_action_evidence_key,
            summary.first_slot_clearance_action_evidence_key
        );
        assert_eq!(
            summary.next_slot_clearance_action_key,
            summary.first_slot_clearance_action_key
        );
        assert_eq!(
            summary.next_work_order_key,
            Some(
                "release-ticket-handoff-execution-work-order-01-repair-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.next_ticket_key,
            Some("release-dispatch-ticket-01-substrate_actions".to_string())
        );
        assert!(
            !summary
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_reviews_ready
        );
        assert!(!summary
            .ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_reviews());
        assert!(summary.has_slot_clearance_action_evidence_reviews());
        assert!(summary.has_required_action_evidence_reviews());
        assert!(summary.has_blockers());
        assert!(summary.has_review_work());
        assert!(summary.needs_operator());
        assert!(!summary.has_lineage_gaps());
        assert!(!summary.has_release_ready_action_evidence_reviews());
        assert!(summary.has_complete_lineage());
        assert!(summary.requires_execution());
        assert!(summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_readiness_marks_release_ready(
    ) {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let summary =
            mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_readiness_summary_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(summary.total_slot_clearance_action_evidence_rows, 5);
        assert_eq!(summary.total_slot_clearance_action_evidence_reviews, 5);
        assert_eq!(summary.required_action_evidence_reviews, 0);
        assert_eq!(summary.blocking_action_evidence_reviews, 0);
        assert_eq!(summary.operator_required_action_evidence_reviews, 0);
        assert_eq!(summary.review_required_action_evidence_reviews, 0);
        assert_eq!(summary.dispatch_required_action_evidence_reviews, 0);
        assert_eq!(summary.execution_required_action_evidence_reviews, 0);
        assert_eq!(summary.lineage_gap_action_evidence_reviews, 0);
        assert_eq!(summary.release_ready_action_evidence_reviews, 5);
        assert_eq!(summary.release_handoff_action_evidence_reviews, 5);
        assert_eq!(summary.lineage_complete_action_evidence_reviews, 5);
        assert_eq!(
            summary.first_slot_clearance_action_evidence_review_key,
            Some(
                "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-review-01-release_ready-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(summary.first_required_action_evidence_review_key, None);
        assert_eq!(summary.first_blocking_action_evidence_review_key, None);
        assert_eq!(
            summary.first_release_ready_action_evidence_review_key,
            summary.first_slot_clearance_action_evidence_review_key
        );
        assert_eq!(
            summary.first_action_evidence_review_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::ReleaseReady
            )
        );
        assert_eq!(summary.next_slot_clearance_action_evidence_review_key, None);
        assert_eq!(summary.next_slot_clearance_action_evidence_key, None);
        assert_eq!(summary.next_slot_clearance_action_key, None);
        assert_eq!(summary.next_slot_clearance_key, None);
        assert_eq!(summary.next_work_order_key, None);
        assert_eq!(summary.next_ticket_key, None);
        assert!(
            summary
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_reviews_ready
        );
        assert!(summary
            .ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_reviews());
        assert!(summary.has_slot_clearance_action_evidence_reviews());
        assert!(!summary.has_required_action_evidence_reviews());
        assert!(!summary.has_blockers());
        assert!(!summary.has_review_work());
        assert!(!summary.needs_operator());
        assert!(!summary.has_lineage_gaps());
        assert!(summary.has_release_ready_action_evidence_reviews());
        assert!(summary.has_complete_lineage());
        assert!(!summary.requires_execution());
        assert!(!summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_rows_surface_review_attention(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let clearance_rows =
            mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_rows(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );

        assert_eq!(clearance_rows.len(), 5);
        assert_eq!(
            clearance_rows[0].slot_clearance_action_evidence_review_clearance_key,
            "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-review-clearance-01-repair_required-substrate_actions"
        );
        assert_eq!(
            clearance_rows[0].slot_clearance_action_evidence_review_key,
            "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-review-01-blocker_review-substrate_actions"
        );
        assert_eq!(
            clearance_rows[0].clearance_kind,
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceKind::RepairRequired
        );
        assert_eq!(
            clearance_rows
                .iter()
                .filter(|row| {
                    row.clearance_kind
                        == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceKind::RepairRequired
                })
                .count(),
            3
        );
        assert_eq!(
            clearance_rows
                .iter()
                .filter(|row| {
                    row.clearance_kind
                        == IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceKind::ReviewRequired
                })
                .count(),
            2
        );
        assert!(clearance_rows.iter().all(
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceRow::requires_attention
        ));
        assert!(clearance_rows
            .iter()
            .all(|row| !row.evidence_review_readiness_ready));
        assert!(clearance_rows.iter().all(
            |row| !IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceRow::ready_to_release_handoff(row)
        ));
    }

    #[test]
    fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_rows_mark_release_handoff(
    ) {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let clearance_rows =
            mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_rows_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(clearance_rows.len(), 5);
        assert_eq!(
            clearance_rows[0].slot_clearance_action_evidence_review_clearance_key,
            "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-review-clearance-01-release_handoff-substrate_actions"
        );
        assert_eq!(
            clearance_rows[0].slot_clearance_action_evidence_review_key,
            "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-review-01-release_ready-substrate_actions"
        );
        assert_eq!(
            clearance_rows[0].clearance_kind,
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceKind::ReleaseHandoff
        );
        assert!(clearance_rows
            .iter()
            .all(|row| row.evidence_review_readiness_ready));
        assert!(clearance_rows.iter().all(
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceRow::is_release_handoff_clearance
        ));
        assert!(clearance_rows.iter().all(
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceRow::ready_to_release_handoff
        ));
        assert!(clearance_rows.iter().all(
            |row| !IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceRow::requires_attention(row)
        ));
    }

    #[test]
    fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_readiness_surfaces_clearance_attention(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let summary =
            mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_readiness_summary(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );

        assert_eq!(summary.total_slot_clearance_action_evidence_reviews, 5);
        assert_eq!(
            summary.total_slot_clearance_action_evidence_review_clearance_rows,
            5
        );
        assert_eq!(summary.required_clearances, 5);
        assert_eq!(summary.release_blocking_clearances, 3);
        assert_eq!(summary.operator_required_clearances, 4);
        assert_eq!(summary.review_required_clearances, 2);
        assert_eq!(summary.dispatch_required_clearances, 5);
        assert_eq!(summary.execution_required_clearances, 5);
        assert_eq!(summary.repair_required_clearances, 3);
        assert_eq!(summary.review_gate_clearances, 2);
        assert_eq!(summary.release_handoff_clearances, 0);
        assert_eq!(summary.release_ready_clearances, 0);
        assert_eq!(summary.cleared_for_handoff_clearances, 0);
        assert_eq!(summary.lineage_complete_clearances, 5);
        assert_eq!(
            summary.first_slot_clearance_action_evidence_review_clearance_key,
            Some(
                "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-review-clearance-01-repair_required-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_required_clearance_key,
            summary.first_slot_clearance_action_evidence_review_clearance_key
        );
        assert_eq!(
            summary.first_repair_clearance_key,
            summary.first_slot_clearance_action_evidence_review_clearance_key
        );
        assert_eq!(
            summary.first_review_clearance_key,
            Some(
                "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-review-clearance-02-review_required-evidence_remediation"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_slot_clearance_action_evidence_review_key,
            Some(
                "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-review-01-blocker_review-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_clearance_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceKind::RepairRequired
            )
        );
        assert_eq!(
            summary.first_action_evidence_review_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::BlockerReview
            )
        );
        assert_eq!(
            summary.next_slot_clearance_action_evidence_review_clearance_key,
            summary.first_slot_clearance_action_evidence_review_clearance_key
        );
        assert_eq!(
            summary.next_slot_clearance_action_evidence_review_key,
            summary.first_slot_clearance_action_evidence_review_key
        );
        assert_eq!(
            summary.next_work_order_key,
            Some(
                "release-ticket-handoff-execution-work-order-01-repair-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.next_ticket_key,
            Some("release-dispatch-ticket-01-substrate_actions".to_string())
        );
        assert!(
            !summary
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_reviews_ready
        );
        assert!(
            !summary
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearances_ready
        );
        assert!(!summary
            .ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearances());
        assert!(summary.has_clearance_rows());
        assert!(summary.has_required_clearances());
        assert!(summary.has_blockers());
        assert!(summary.has_review_work());
        assert!(summary.needs_operator());
        assert!(!summary.has_lineage_gaps());
        assert!(!summary.has_release_handoff_clearances());
        assert!(summary.has_complete_lineage());
        assert!(summary.requires_execution());
        assert!(summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_readiness_marks_release_ready(
    ) {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let summary =
            mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_readiness_summary_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(summary.total_slot_clearance_action_evidence_reviews, 5);
        assert_eq!(
            summary.total_slot_clearance_action_evidence_review_clearance_rows,
            5
        );
        assert_eq!(summary.required_clearances, 0);
        assert_eq!(summary.release_blocking_clearances, 0);
        assert_eq!(summary.operator_required_clearances, 0);
        assert_eq!(summary.review_required_clearances, 0);
        assert_eq!(summary.dispatch_required_clearances, 0);
        assert_eq!(summary.execution_required_clearances, 0);
        assert_eq!(summary.repair_required_clearances, 0);
        assert_eq!(summary.operator_handoff_clearances, 0);
        assert_eq!(summary.review_gate_clearances, 0);
        assert_eq!(summary.lineage_gap_clearances, 0);
        assert_eq!(summary.release_handoff_clearances, 5);
        assert_eq!(summary.release_ready_clearances, 5);
        assert_eq!(summary.cleared_for_handoff_clearances, 5);
        assert_eq!(summary.lineage_complete_clearances, 5);
        assert_eq!(
            summary.first_slot_clearance_action_evidence_review_clearance_key,
            Some(
                "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-review-clearance-01-release_handoff-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(summary.first_required_clearance_key, None);
        assert_eq!(summary.first_repair_clearance_key, None);
        assert_eq!(summary.first_review_clearance_key, None);
        assert_eq!(summary.first_lineage_gap_clearance_key, None);
        assert_eq!(
            summary.first_release_handoff_clearance_key,
            summary.first_slot_clearance_action_evidence_review_clearance_key
        );
        assert_eq!(
            summary.first_slot_clearance_action_evidence_review_key,
            Some(
                "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-review-01-release_ready-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_clearance_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceKind::ReleaseHandoff
            )
        );
        assert_eq!(
            summary.first_action_evidence_review_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::ReleaseReady
            )
        );
        assert_eq!(
            summary.next_slot_clearance_action_evidence_review_clearance_key,
            None
        );
        assert_eq!(summary.next_slot_clearance_action_evidence_review_key, None);
        assert_eq!(summary.next_slot_clearance_action_evidence_key, None);
        assert_eq!(summary.next_slot_clearance_action_key, None);
        assert_eq!(summary.next_slot_clearance_key, None);
        assert_eq!(summary.next_work_order_key, None);
        assert_eq!(summary.next_ticket_key, None);
        assert_eq!(summary.next_check_kind, None);
        assert_eq!(summary.next_check_status, None);
        assert_eq!(summary.next_handoff_lane, None);
        assert!(
            summary
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_reviews_ready
        );
        assert!(
            summary
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearances_ready
        );
        assert!(summary
            .ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearances());
        assert!(summary.has_clearance_rows());
        assert!(!summary.has_required_clearances());
        assert!(!summary.has_blockers());
        assert!(!summary.has_review_work());
        assert!(!summary.needs_operator());
        assert!(!summary.has_lineage_gaps());
        assert!(summary.has_release_handoff_clearances());
        assert!(summary.has_complete_lineage());
        assert!(!summary.requires_execution());
        assert!(!summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_rows_route_attention(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let action_rows =
            mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_rows(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );

        assert_eq!(action_rows.len(), 5);
        assert_eq!(
            action_rows[0].slot_clearance_action_evidence_review_clearance_action_key,
            "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-review-clearance-action-01-schedule_repair-substrate_actions"
        );
        assert_eq!(
            action_rows[0].slot_clearance_action_evidence_review_clearance_key,
            "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-review-clearance-01-repair_required-substrate_actions"
        );
        assert_eq!(
            action_rows[0].clearance_action_kind,
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionKind::ScheduleRepair
        );
        assert_eq!(
            action_rows
                .iter()
                .filter(|row| row.is_repair_action())
                .count(),
            3
        );
        assert_eq!(
            action_rows
                .iter()
                .filter(|row| row.is_review_action())
                .count(),
            2
        );
        assert!(action_rows.iter().all(
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionRow::requires_attention
        ));
        assert!(action_rows
            .iter()
            .all(|row| !row.evidence_review_clearance_readiness_ready));
        assert!(action_rows.iter().all(
            |row| !IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionRow::ready_to_release_handoff(row)
        ));
    }

    #[test]
    fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_rows_mark_release_handoff(
    ) {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let action_rows =
            mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_rows_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(action_rows.len(), 5);
        assert_eq!(
            action_rows[0].slot_clearance_action_evidence_review_clearance_action_key,
            "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-review-clearance-action-01-release_handoff-substrate_actions"
        );
        assert_eq!(
            action_rows[0].slot_clearance_action_evidence_review_clearance_key,
            "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-review-clearance-01-release_handoff-substrate_actions"
        );
        assert_eq!(
            action_rows[0].clearance_action_kind,
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionKind::ReleaseHandoff
        );
        assert!(action_rows
            .iter()
            .all(|row| row.evidence_review_clearance_readiness_ready));
        assert!(action_rows.iter().all(
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionRow::is_release_handoff_action
        ));
        assert!(action_rows.iter().all(
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionRow::ready_to_release_handoff
        ));
        assert!(action_rows.iter().all(
            |row| !IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionRow::requires_attention(row)
        ));
    }

    #[test]
    fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_summary_surfaces_next_action(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let summary =
            mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_summary(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );

        assert_eq!(
            summary.total_slot_clearance_action_evidence_review_clearance_action_rows,
            5
        );
        assert_eq!(summary.required_actions, 5);
        assert_eq!(summary.repair_actions, 3);
        assert_eq!(summary.review_actions, 2);
        assert_eq!(summary.release_handoff_actions, 0);
        assert_eq!(summary.ready_actions, 0);
        assert_eq!(
            summary.first_slot_clearance_action_evidence_review_clearance_action_key,
            Some(
                "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-review-clearance-action-01-schedule_repair-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_required_action_key,
            summary.first_slot_clearance_action_evidence_review_clearance_action_key
        );
        assert_eq!(
            summary.first_repair_action_key,
            summary.first_slot_clearance_action_evidence_review_clearance_action_key
        );
        assert_eq!(
            summary.first_clearance_action_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionKind::ScheduleRepair
            )
        );
        assert_eq!(
            summary.next_slot_clearance_action_evidence_review_clearance_action_key,
            summary.first_required_action_key
        );
        assert_eq!(
            summary.next_check_kind,
            Some(IntegrationMeshReleaseReadinessCheckKind::SubstrateActions)
        );
        assert!(summary.has_clearance_action_rows());
        assert!(summary.has_required_actions());
        assert!(summary.has_blockers());
        assert!(summary.has_review_work());
        assert!(summary.needs_operator());
        assert!(!summary.has_lineage_gaps());
        assert!(!summary.has_release_handoff_actions());
        assert!(summary.has_complete_lineage());
        assert!(summary.requires_execution());
        assert!(summary.requires_attention());
        assert!(
            !summary
                .ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_actions()
        );
    }

    #[test]
    fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_summary_marks_release_handoff_ready(
    ) {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let summary =
            mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_summary_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(
            summary.total_slot_clearance_action_evidence_review_clearance_action_rows,
            5
        );
        assert_eq!(summary.required_actions, 0);
        assert_eq!(summary.repair_actions, 0);
        assert_eq!(summary.review_actions, 0);
        assert_eq!(summary.release_handoff_actions, 5);
        assert_eq!(summary.ready_actions, 5);
        assert_eq!(
            summary.first_slot_clearance_action_evidence_review_clearance_action_key,
            Some(
                "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-review-clearance-action-01-release_handoff-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_release_handoff_action_key,
            summary.first_slot_clearance_action_evidence_review_clearance_action_key
        );
        assert_eq!(
            summary.first_clearance_action_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionKind::ReleaseHandoff
            )
        );
        assert_eq!(
            summary.next_slot_clearance_action_evidence_review_clearance_action_key,
            None
        );
        assert!(
            summary
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_actions_ready
        );
        assert!(summary.has_clearance_action_rows());
        assert!(!summary.has_required_actions());
        assert!(!summary.has_blockers());
        assert!(!summary.has_review_work());
        assert!(!summary.needs_operator());
        assert!(!summary.has_lineage_gaps());
        assert!(summary.has_release_handoff_actions());
        assert!(summary.has_complete_lineage());
        assert!(!summary.requires_execution());
        assert!(!summary.requires_attention());
        assert!(summary
            .ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_actions());
    }

    #[test]
    fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_rows_surface_action_readiness_lineage(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let action_rows =
            mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_rows(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );
        let evidence_rows =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_rows(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );

        assert_eq!(evidence_rows.len(), 5);
        assert_eq!(
            evidence_rows
                .iter()
                .filter(|row| row.is_repair_evidence())
                .count(),
            3
        );
        assert_eq!(
            evidence_rows
                .iter()
                .filter(|row| row.is_review_evidence())
                .count(),
            2
        );
        assert_eq!(
            evidence_rows
                .iter()
                .filter(|row| row.is_release_handoff_evidence())
                .count(),
            0
        );
        assert_eq!(
            evidence_rows
                .iter()
                .filter(|row| row.ready_to_release_handoff())
                .count(),
            0
        );
        assert!(evidence_rows
            .iter()
            .all(|row| !row.clearance_action_readiness_ready));
        assert!(evidence_rows
            .iter()
            .all(IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceRow::requires_attention));

        let first = &evidence_rows[0];
        assert_eq!(first.sequence, 1);
        assert_eq!(
            first.slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key,
            "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-review-clearance-action-readiness-evidence-01-repair_evidence-substrate_actions"
        );
        assert_eq!(
            first.slot_clearance_action_evidence_review_clearance_action_sequence,
            action_rows[0].sequence
        );
        assert_eq!(
            first.slot_clearance_action_evidence_review_clearance_action_key,
            action_rows[0].slot_clearance_action_evidence_review_clearance_action_key
        );
        assert_eq!(
            first.clearance_action_readiness_evidence_kind,
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceKind::RepairEvidence
        );
        assert!(first.is_repair_evidence());
        assert!(first.requires_action());
        assert!(first.blocks_release());
        assert!(first.needs_operator());
        assert!(!first.needs_review());
        assert!(first.requires_dispatch());
        assert!(first.requires_execution());
        assert!(first.has_lineage());
        assert!(!first.ready_to_release_handoff());
        assert!(first.requires_attention());

        let mut lineage_action = action_rows[0].clone();
        lineage_action.clearance_action_kind =
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionKind::RestoreLineage;
        lineage_action.slot_key.clear();
        lineage_action.lineage_complete = false;
        lineage_action.ready_for_release_handoff = false;
        let lineage_evidence =
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceRow::from_clearance_action_row(
                1,
                &lineage_action,
                false,
            );

        assert_eq!(
            lineage_evidence.slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key,
            "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-review-clearance-action-readiness-evidence-01-lineage_evidence-substrate_actions"
        );
        assert!(lineage_evidence.is_lineage_evidence());
        assert!(!lineage_evidence.has_lineage());
        assert!(!lineage_evidence.ready_to_release_handoff());
        assert!(lineage_evidence.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_rows_mark_release_handoff_ready(
    ) {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let evidence_rows =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_rows_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(evidence_rows.len(), 5);
        assert!(evidence_rows.iter().all(
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceRow::is_release_handoff_evidence
        ));
        assert!(evidence_rows.iter().all(
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceRow::ready_to_release_handoff
        ));
        assert!(evidence_rows.iter().all(
            |row| !IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceRow::requires_attention(row)
        ));
        assert!(evidence_rows
            .iter()
            .all(|row| row.clearance_action_readiness_ready));
        assert_eq!(
            evidence_rows[0].slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key,
            "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-review-clearance-action-readiness-evidence-01-release_handoff_evidence-substrate_actions"
        );
        assert_eq!(
            evidence_rows[0].clearance_action_readiness_evidence_kind,
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceKind::ReleaseHandoffEvidence
        );
        assert!(evidence_rows[0].release_ready_review);
        assert!(evidence_rows[0].has_lineage());
    }

    #[test]
    fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_summary_surfaces_next_evidence(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let evidence_rows =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_rows(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );
        let summary =
            mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_summary(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );

        assert_eq!(
            summary.total_slot_clearance_action_evidence_review_clearance_action_rows,
            5
        );
        assert_eq!(
            summary
                .total_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_rows,
            evidence_rows.len()
        );
        assert_eq!(summary.required_evidence, 5);
        assert_eq!(summary.repair_evidence, 3);
        assert_eq!(summary.review_evidence, 2);
        assert_eq!(summary.release_handoff_evidence, 0);
        assert_eq!(summary.ready_evidence, 0);
        assert!(summary.has_evidence_rows());
        assert!(summary.has_required_evidence());
        assert!(summary.has_blockers());
        assert!(summary.has_review_work());
        assert!(summary.needs_operator());
        assert!(summary.requires_dispatch());
        assert!(summary.requires_execution());
        assert!(!summary.has_release_handoff_evidence());
        assert!(summary.requires_attention());
        assert!(!summary.ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence());
        assert_eq!(
            summary
                .first_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key,
            Some(evidence_rows[0].slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key.clone())
        );
        assert_eq!(
            summary
                .next_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key,
            Some(
                evidence_rows[0]
                    .slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key
                    .clone()
            )
        );
        assert_eq!(
            summary.first_repair_evidence_key,
            Some(
                evidence_rows[0]
                    .slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key
                    .clone()
            )
        );
        assert_eq!(
            summary.first_clearance_action_readiness_evidence_kind,
            Some(IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceKind::RepairEvidence)
        );
        assert_eq!(
            summary.next_work_order_key,
            Some(evidence_rows[0].work_order_key.clone())
        );
    }

    #[test]
    fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_summary_marks_release_handoff_ready(
    ) {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let evidence_rows =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_rows_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );
        let summary =
            mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_summary_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(
            summary
                .total_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_rows,
            5
        );
        assert_eq!(summary.required_evidence, 0);
        assert_eq!(summary.release_handoff_evidence, evidence_rows.len());
        assert_eq!(summary.ready_evidence, evidence_rows.len());
        assert_eq!(summary.lineage_complete_evidence, evidence_rows.len());
        assert!(summary.has_release_handoff_evidence());
        assert!(summary.has_complete_lineage());
        assert!(!summary.has_blockers());
        assert!(!summary.has_review_work());
        assert!(!summary.needs_operator());
        assert!(!summary.requires_dispatch());
        assert!(!summary.requires_execution());
        assert!(!summary.requires_attention());
        assert!(summary.ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence());
        assert_eq!(
            summary.first_release_handoff_evidence_key,
            Some(
                evidence_rows[0]
                    .slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key
                    .clone()
            )
        );
        assert_eq!(
            summary
                .next_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key,
            None
        );
        assert_eq!(
            summary.first_clearance_action_readiness_evidence_kind,
            Some(IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceKind::ReleaseHandoffEvidence)
        );
    }

    #[test]
    fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_rows_surface_review_outcomes(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let evidence_rows =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_rows(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );
        let review_rows =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_rows(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );

        assert_eq!(review_rows.len(), evidence_rows.len());
        assert_eq!(
            review_rows
                .iter()
                .filter(|row| row.is_blocker_review())
                .count(),
            3
        );
        assert_eq!(
            review_rows
                .iter()
                .filter(|row| row.is_review_gate())
                .count(),
            2
        );
        assert_eq!(
            review_rows
                .iter()
                .filter(|row| row.is_release_ready_review())
                .count(),
            0
        );
        assert!(review_rows.iter().all(|row| !row.readiness_evidence_ready));
        assert!(review_rows.iter().all(
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewRow::requires_attention
        ));

        let first = &review_rows[0];
        assert_eq!(first.sequence, 1);
        assert_eq!(
            first.slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_key,
            "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-review-clearance-action-readiness-evidence-review-01-blocker_review-substrate_actions"
        );
        assert_eq!(
            first
                .slot_clearance_action_evidence_review_clearance_action_readiness_evidence_sequence,
            evidence_rows[0].sequence
        );
        assert_eq!(
            first.slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key,
            evidence_rows[0]
                .slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key
        );
        assert_eq!(
            first.readiness_evidence_review_kind,
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::BlockerReview
        );
        assert!(first.is_blocker_review());
        assert!(first.requires_evidence_review());
        assert!(first.blocks_release());
        assert!(first.needs_operator());
        assert!(!first.needs_review());
        assert!(first.has_lineage());
        assert!(!first.is_release_ready_review());
        assert!(!first.ready_to_release_handoff());

        let mut lineage_evidence = evidence_rows[0].clone();
        lineage_evidence.slot_key.clear();
        lineage_evidence.lineage_complete = false;
        lineage_evidence.ready_for_release_handoff = false;
        let lineage_review =
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewRow::from_readiness_evidence_row(
                1,
                &lineage_evidence,
                false,
            );

        assert_eq!(
            lineage_review.slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_key,
            "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-review-clearance-action-readiness-evidence-review-01-lineage_gap-substrate_actions"
        );
        assert!(lineage_review.is_lineage_gap());
        assert!(!lineage_review.has_lineage());
        assert!(lineage_review.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_rows_mark_release_ready(
    ) {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let review_rows =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_rows_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(review_rows.len(), 5);
        assert!(review_rows.iter().all(
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewRow::is_release_handoff_evidence
        ));
        assert!(review_rows.iter().all(
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewRow::is_release_ready_review
        ));
        assert!(review_rows.iter().all(
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewRow::ready_to_release_handoff
        ));
        assert!(review_rows.iter().all(|row| row.readiness_evidence_ready));
        assert!(review_rows.iter().all(|row| !row.requires_attention()));
        assert_eq!(
            review_rows[0].slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_key,
            "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-review-clearance-action-readiness-evidence-review-01-release_ready-substrate_actions"
        );
        assert_eq!(
            review_rows[0].readiness_evidence_review_kind,
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::ReleaseReady
        );
        assert!(review_rows[0].has_lineage());
    }

    #[test]
    fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_summary_surfaces_review_attention(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let summary =
            mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_summary(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );

        assert_eq!(
            summary
                .total_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_rows,
            5
        );
        assert_eq!(
            summary
                .total_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_rows,
            5
        );
        assert_eq!(summary.required_reviews, 5);
        assert_eq!(summary.blocker_reviews, 3);
        assert_eq!(summary.operator_reviews, 0);
        assert_eq!(summary.review_gate_reviews, 2);
        assert_eq!(summary.lineage_gap_reviews, 0);
        assert_eq!(summary.release_ready_reviews, 0);
        assert_eq!(summary.release_handoff_evidence_reviews, 0);
        assert_eq!(summary.lineage_complete_reviews, 5);
        assert_eq!(summary.ready_reviews, 0);
        assert_eq!(
            summary.first_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_key,
            Some(
                "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-review-clearance-action-readiness-evidence-review-01-blocker_review-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_required_review_key,
            summary
                .first_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_key
        );
        assert_eq!(
            summary.first_blocker_review_key,
            summary
                .first_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_key
        );
        assert_eq!(
            summary.first_review_gate_key,
            Some(
                "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-review-clearance-action-readiness-evidence-review-02-review_gate-evidence_remediation"
                    .to_string()
            )
        );
        assert_eq!(summary.first_operator_review_key, None);
        assert_eq!(summary.first_lineage_gap_review_key, None);
        assert_eq!(summary.first_release_ready_review_key, None);
        assert_eq!(
            summary.first_readiness_evidence_review_kind,
            Some(IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::BlockerReview)
        );
        assert_eq!(
            summary.first_check_kind,
            Some(IntegrationMeshReleaseReadinessCheckKind::SubstrateActions)
        );
        assert_eq!(
            summary.first_check_status,
            Some(IntegrationMeshReleaseReadinessStatus::Blocked)
        );
        assert_eq!(
            summary.next_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_key,
            summary
                .first_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_key
        );
        assert_eq!(
            summary.next_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key,
            summary
                .first_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key
        );
        assert_eq!(summary.next_work_order_key, summary.first_work_order_key);
        assert_eq!(summary.next_ticket_key, summary.first_ticket_key);
        assert_eq!(summary.next_check_kind, summary.first_check_kind);
        assert_eq!(summary.next_check_status, summary.first_check_status);
        assert_eq!(summary.next_handoff_lane, summary.first_handoff_lane);
        assert!(!summary
            .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_ready);
        assert!(!summary
            .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_reviews_ready);
        assert!(!summary
            .ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_reviews());
        assert!(summary.has_review_rows());
        assert!(summary.has_required_reviews());
        assert!(summary.has_blockers());
        assert!(summary.has_review_work());
        assert!(summary.needs_operator());
        assert!(!summary.has_lineage_gaps());
        assert!(!summary.has_release_ready_reviews());
        assert!(!summary.has_release_handoff_evidence());
        assert!(summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_summary_marks_release_ready(
    ) {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let summary =
            mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_summary_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(
            summary
                .total_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_rows,
            5
        );
        assert_eq!(
            summary
                .total_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_rows,
            5
        );
        assert_eq!(summary.required_reviews, 0);
        assert_eq!(summary.blocker_reviews, 0);
        assert_eq!(summary.operator_reviews, 0);
        assert_eq!(summary.review_gate_reviews, 0);
        assert_eq!(summary.lineage_gap_reviews, 0);
        assert_eq!(summary.release_ready_reviews, 5);
        assert_eq!(summary.release_handoff_evidence_reviews, 5);
        assert_eq!(summary.lineage_complete_reviews, 5);
        assert_eq!(summary.ready_reviews, 5);
        assert_eq!(
            summary.first_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_key,
            Some(
                "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-review-clearance-action-readiness-evidence-review-01-release_ready-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(summary.first_required_review_key, None);
        assert_eq!(summary.first_blocker_review_key, None);
        assert_eq!(summary.first_operator_review_key, None);
        assert_eq!(summary.first_review_gate_key, None);
        assert_eq!(summary.first_lineage_gap_review_key, None);
        assert_eq!(
            summary.first_release_ready_review_key,
            summary
                .first_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_key
        );
        assert_eq!(
            summary.first_readiness_evidence_review_kind,
            Some(IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::ReleaseReady)
        );
        assert_eq!(
            summary.next_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_key,
            None
        );
        assert_eq!(
            summary
                .next_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_key,
            None
        );
        assert_eq!(summary.next_work_order_key, None);
        assert_eq!(summary.next_ticket_key, None);
        assert_eq!(summary.next_check_kind, None);
        assert_eq!(summary.next_check_status, None);
        assert_eq!(summary.next_handoff_lane, None);
        assert!(summary
            .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_ready);
        assert!(summary
            .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_reviews_ready);
        assert!(summary
            .ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_reviews());
        assert!(summary.has_review_rows());
        assert!(!summary.has_required_reviews());
        assert!(!summary.has_blockers());
        assert!(!summary.has_review_work());
        assert!(!summary.needs_operator());
        assert!(!summary.has_lineage_gaps());
        assert!(summary.has_release_ready_reviews());
        assert!(summary.has_release_handoff_evidence());
        assert!(!summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_rows_surface_disposition_outcomes(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let review_rows =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_rows(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );
        let disposition_rows =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_rows(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );

        assert_eq!(disposition_rows.len(), review_rows.len());
        assert_eq!(
            disposition_rows
                .iter()
                .filter(|row| row.is_repair_required())
                .count(),
            3
        );
        assert_eq!(
            disposition_rows
                .iter()
                .filter(|row| row.is_review_required())
                .count(),
            2
        );
        assert_eq!(
            disposition_rows
                .iter()
                .filter(|row| row.is_release_ready())
                .count(),
            0
        );
        assert!(disposition_rows.iter().all(
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionRow::requires_attention
        ));

        let first = &disposition_rows[0];
        assert_eq!(first.sequence, 1);
        assert_eq!(
            first.slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_key,
            "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-review-clearance-action-readiness-evidence-review-disposition-01-repair_release_blocker-substrate_actions"
        );
        assert_eq!(
            first.slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_sequence,
            review_rows[0].sequence
        );
        assert_eq!(
            first.slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_key,
            review_rows[0]
                .slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_key
        );
        assert_eq!(
            first.readiness_evidence_review_kind,
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::BlockerReview
        );
        assert_eq!(
            first.readiness_evidence_review_disposition_kind,
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind::RepairReleaseBlocker
        );
        assert!(first.is_repair_required());
        assert!(first.blocks_release());
        assert!(first.needs_operator());
        assert!(!first.needs_review());
        assert!(first.has_lineage());
        assert!(!first.is_release_ready());

        let mut lineage_review = review_rows[0].clone();
        lineage_review.slot_key.clear();
        lineage_review.lineage_complete = false;
        lineage_review.ready_for_release_handoff = false;
        let lineage_disposition =
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionRow::from_readiness_evidence_review_row(
                1,
                &lineage_review,
            );

        assert_eq!(
            lineage_disposition.slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_key,
            "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-review-clearance-action-readiness-evidence-review-disposition-01-restore_evidence_lineage-substrate_actions"
        );
        assert!(lineage_disposition.is_lineage_gap());
        assert!(!lineage_disposition.has_lineage());
        assert!(lineage_disposition.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_rows_mark_release_ready(
    ) {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let disposition_rows =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_rows_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(disposition_rows.len(), 5);
        assert!(disposition_rows.iter().all(
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionRow::is_release_handoff_evidence
        ));
        assert!(disposition_rows.iter().all(
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionRow::is_release_ready
        ));
        assert!(disposition_rows.iter().all(|row| !row.requires_attention()));
        assert_eq!(
            disposition_rows[0].slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_key,
            "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-review-clearance-action-readiness-evidence-review-disposition-01-release_ready-substrate_actions"
        );
        assert_eq!(
            disposition_rows[0].readiness_evidence_review_kind,
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::ReleaseReady
        );
        assert_eq!(
            disposition_rows[0].readiness_evidence_review_disposition_kind,
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind::ReleaseReady
        );
        assert!(disposition_rows[0].has_lineage());
    }

    #[test]
    fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_summary_surfaces_disposition_attention(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let summary =
            mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_summary(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );

        assert_eq!(
            summary
                .total_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_rows,
            5
        );
        assert_eq!(
            summary
                .total_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_rows,
            5
        );
        assert_eq!(summary.required_dispositions, 5);
        assert_eq!(summary.release_blocking_dispositions, 3);
        assert_eq!(summary.repair_required_dispositions, 3);
        assert_eq!(summary.operator_handoff_dispositions, 0);
        assert_eq!(summary.review_required_dispositions, 2);
        assert_eq!(summary.lineage_gap_dispositions, 0);
        assert_eq!(summary.lineage_complete_dispositions, 5);
        assert_eq!(summary.release_ready_dispositions, 0);
        assert_eq!(summary.release_handoff_evidence_dispositions, 0);
        assert_eq!(
            summary.first_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_key,
            Some(
                "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-review-clearance-action-readiness-evidence-review-disposition-01-repair_release_blocker-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_required_disposition_key,
            summary
                .first_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_key
        );
        assert_eq!(
            summary.first_release_blocking_disposition_key,
            summary
                .first_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_key
        );
        assert_eq!(
            summary.first_repair_required_disposition_key,
            summary
                .first_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_key
        );
        assert_eq!(summary.first_operator_handoff_disposition_key, None);
        assert_eq!(
            summary.first_review_required_disposition_key,
            Some(
                "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-review-clearance-action-readiness-evidence-review-disposition-02-review_required-evidence_remediation"
                    .to_string()
            )
        );
        assert_eq!(summary.first_lineage_gap_disposition_key, None);
        assert_eq!(summary.first_release_ready_disposition_key, None);
        assert_eq!(
            summary.first_readiness_evidence_review_kind,
            Some(IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::BlockerReview)
        );
        assert_eq!(
            summary.first_readiness_evidence_review_disposition_kind,
            Some(IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind::RepairReleaseBlocker)
        );
        assert_eq!(
            summary.next_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_key,
            summary
                .first_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_key
        );
        assert_eq!(
            summary.next_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_key,
            summary
                .first_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_key
        );
        assert_eq!(summary.next_work_order_key, summary.first_work_order_key);
        assert_eq!(summary.next_ticket_key, summary.first_ticket_key);
        assert_eq!(summary.next_check_kind, summary.first_check_kind);
        assert_eq!(summary.next_check_status, summary.first_check_status);
        assert_eq!(summary.next_handoff_lane, summary.first_handoff_lane);
        assert!(!summary
            .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_reviews_ready);
        assert!(!summary
            .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_dispositions_ready);
        assert!(!summary
            .ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_dispositions());
        assert!(summary.has_disposition_rows());
        assert!(summary.has_required_dispositions());
        assert!(summary.has_blockers());
        assert!(summary.has_review_work());
        assert!(summary.needs_operator());
        assert!(summary.requires_dispatch());
        assert!(summary.requires_execution());
        assert!(!summary.has_lineage_gaps());
        assert!(!summary.has_release_ready_dispositions());
        assert!(!summary.has_release_handoff_evidence());
        assert!(summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_summary_marks_release_ready(
    ) {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let summary =
            mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_summary_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(
            summary
                .total_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_rows,
            5
        );
        assert_eq!(
            summary
                .total_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_rows,
            5
        );
        assert_eq!(summary.required_dispositions, 0);
        assert_eq!(summary.release_blocking_dispositions, 0);
        assert_eq!(summary.repair_required_dispositions, 0);
        assert_eq!(summary.operator_handoff_dispositions, 0);
        assert_eq!(summary.review_required_dispositions, 0);
        assert_eq!(summary.lineage_gap_dispositions, 0);
        assert_eq!(summary.lineage_complete_dispositions, 5);
        assert_eq!(summary.release_ready_dispositions, 5);
        assert_eq!(summary.release_handoff_evidence_dispositions, 5);
        assert_eq!(
            summary.first_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_key,
            Some(
                "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-review-clearance-action-readiness-evidence-review-disposition-01-release_ready-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(summary.first_required_disposition_key, None);
        assert_eq!(summary.first_release_blocking_disposition_key, None);
        assert_eq!(summary.first_repair_required_disposition_key, None);
        assert_eq!(summary.first_operator_handoff_disposition_key, None);
        assert_eq!(summary.first_review_required_disposition_key, None);
        assert_eq!(summary.first_lineage_gap_disposition_key, None);
        assert_eq!(
            summary.first_release_ready_disposition_key,
            summary
                .first_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_key
        );
        assert_eq!(
            summary.first_readiness_evidence_review_disposition_kind,
            Some(IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind::ReleaseReady)
        );
        assert_eq!(
            summary.next_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_key,
            None
        );
        assert_eq!(
            summary.next_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_key,
            None
        );
        assert_eq!(summary.next_work_order_key, None);
        assert_eq!(summary.next_ticket_key, None);
        assert_eq!(summary.next_check_kind, None);
        assert_eq!(summary.next_check_status, None);
        assert_eq!(summary.next_handoff_lane, None);
        assert!(summary
            .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_reviews_ready);
        assert!(summary
            .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_dispositions_ready);
        assert!(summary
            .ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_dispositions());
        assert!(summary.has_disposition_rows());
        assert!(!summary.has_required_dispositions());
        assert!(!summary.has_blockers());
        assert!(!summary.has_review_work());
        assert!(!summary.needs_operator());
        assert!(!summary.requires_dispatch());
        assert!(!summary.requires_execution());
        assert!(!summary.has_lineage_gaps());
        assert!(summary.has_release_ready_dispositions());
        assert!(summary.has_release_handoff_evidence());
        assert!(!summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_rows_surface_action_outcomes(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let disposition_rows =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_rows(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );
        let action_rows =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_rows(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );

        assert_eq!(action_rows.len(), disposition_rows.len());
        assert_eq!(
            action_rows
                .iter()
                .filter(|row| row.is_repair_action())
                .count(),
            3
        );
        assert_eq!(
            action_rows
                .iter()
                .filter(|row| row.is_review_action())
                .count(),
            2
        );
        assert_eq!(
            action_rows
                .iter()
                .filter(|row| row.is_release_handoff_action())
                .count(),
            0
        );
        assert!(action_rows.iter().all(
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionRow::requires_attention
        ));

        let first = &action_rows[0];
        assert_eq!(first.sequence, 1);
        assert_eq!(
            first.slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_key,
            "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-review-clearance-action-readiness-evidence-review-disposition-action-01-schedule_release_blocker_repair-substrate_actions"
        );
        assert_eq!(
            first.slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_sequence,
            disposition_rows[0].sequence
        );
        assert_eq!(
            first.slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_key,
            disposition_rows[0]
                .slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_key
        );
        assert_eq!(
            first.disposition_action_kind,
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::ScheduleReleaseBlockerRepair
        );
        assert_eq!(
            first.readiness_evidence_review_disposition_kind,
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind::RepairReleaseBlocker
        );
        assert!(first.is_repair_action());
        assert!(first.blocks_release());
        assert!(first.needs_operator());
        assert!(!first.needs_review());
        assert!(first.requires_dispatch());
        assert!(first.requires_execution());
        assert!(first.has_lineage());
        assert!(!first.is_release_ready());
    }

    #[test]
    fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_rows_mark_release_ready(
    ) {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let action_rows =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_rows_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(action_rows.len(), 5);
        assert!(action_rows.iter().all(
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionRow::is_release_handoff_action
        ));
        assert!(action_rows.iter().all(
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionRow::is_release_handoff_evidence
        ));
        assert!(action_rows.iter().all(
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionRow::is_release_ready
        ));
        assert!(action_rows.iter().all(|row| !row.requires_attention()));
        assert_eq!(
            action_rows[0].slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_key,
            "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-review-clearance-action-readiness-evidence-review-disposition-action-01-release_handoff-substrate_actions"
        );
        assert_eq!(
            action_rows[0].disposition_action_kind,
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::ReleaseHandoff
        );
    }

    #[test]
    fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary_surfaces_action_attention(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let summary =
            mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );

        assert_eq!(
            summary
                .total_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_rows,
            5
        );
        assert_eq!(
            summary
                .total_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_rows,
            5
        );
        assert_eq!(summary.required_actions, 5);
        assert_eq!(summary.release_blocking_actions, 3);
        assert_eq!(summary.repair_actions, 3);
        assert_eq!(summary.operator_handoff_actions, 0);
        assert_eq!(summary.review_actions, 2);
        assert_eq!(summary.release_handoff_actions, 0);
        assert_eq!(summary.release_handoff_evidence_actions, 0);
        assert_eq!(
            summary.first_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_key,
            Some(
                "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-review-clearance-action-readiness-evidence-review-disposition-action-01-schedule_release_blocker_repair-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_required_action_key,
            summary
                .first_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_key
        );
        assert_eq!(
            summary.first_repair_action_key,
            summary
                .first_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_key
        );
        assert_eq!(
            summary.first_review_action_key,
            Some(
                "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-review-clearance-action-readiness-evidence-review-disposition-action-02-complete_evidence_review-evidence_remediation"
                    .to_string()
            )
        );
        assert_eq!(summary.first_release_handoff_action_key, None);
        assert_eq!(
            summary.first_disposition_action_kind,
            Some(IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::ScheduleReleaseBlockerRepair)
        );
        assert_eq!(
            summary.next_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_key,
            summary
                .first_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_key
        );
        assert_eq!(summary.next_work_order_key, summary.first_work_order_key);
        assert_eq!(summary.next_ticket_key, summary.first_ticket_key);
        assert!(!summary
            .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_dispositions_ready);
        assert!(!summary
            .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_actions_ready);
        assert!(!summary
            .ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_actions());
        assert!(summary.has_action_rows());
        assert!(summary.has_required_actions());
        assert!(summary.has_blockers());
        assert!(summary.has_review_work());
        assert!(summary.needs_operator());
        assert!(summary.requires_dispatch());
        assert!(summary.requires_execution());
        assert!(!summary.has_lineage_gaps());
        assert!(!summary.has_release_handoff_actions());
        assert!(summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary_marks_release_ready(
    ) {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let summary =
            mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_summary_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(
            summary
                .total_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_rows,
            5
        );
        assert_eq!(summary.required_actions, 0);
        assert_eq!(summary.release_blocking_actions, 0);
        assert_eq!(summary.repair_actions, 0);
        assert_eq!(summary.review_actions, 0);
        assert_eq!(summary.release_handoff_actions, 5);
        assert_eq!(summary.release_handoff_evidence_actions, 5);
        assert_eq!(
            summary.first_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_key,
            Some(
                "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-review-clearance-action-readiness-evidence-review-disposition-action-01-release_handoff-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(summary.first_required_action_key, None);
        assert_eq!(summary.first_repair_action_key, None);
        assert_eq!(
            summary.first_release_handoff_action_key,
            summary
                .first_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_key
        );
        assert_eq!(
            summary.first_disposition_action_kind,
            Some(IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::ReleaseHandoff)
        );
        assert_eq!(
            summary.next_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_key,
            None
        );
        assert_eq!(summary.next_work_order_key, None);
        assert_eq!(summary.next_ticket_key, None);
        assert!(summary
            .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_dispositions_ready);
        assert!(summary
            .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_actions_ready);
        assert!(summary
            .ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_actions());
        assert!(summary.has_action_rows());
        assert!(!summary.has_required_actions());
        assert!(!summary.has_blockers());
        assert!(!summary.has_review_work());
        assert!(!summary.needs_operator());
        assert!(!summary.requires_dispatch());
        assert!(!summary.requires_execution());
        assert!(!summary.has_lineage_gaps());
        assert!(summary.has_release_handoff_actions());
        assert!(summary.has_release_handoff_evidence());
        assert!(!summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_readiness_evidence_review_disposition_action_readiness_surfaces_attention(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let summary =
            mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_readiness_summary(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );

        assert_eq!(summary.total_readiness_evidence_review_disposition_rows, 5);
        assert_eq!(
            summary.total_readiness_evidence_review_disposition_actions,
            5
        );
        assert_eq!(
            summary.required_readiness_evidence_review_disposition_actions,
            5
        );
        assert_eq!(
            summary.release_blocking_readiness_evidence_review_disposition_actions,
            3
        );
        assert_eq!(
            summary.repair_readiness_evidence_review_disposition_actions,
            3
        );
        assert_eq!(
            summary.review_readiness_evidence_review_disposition_actions,
            2
        );
        assert_eq!(
            summary.release_handoff_readiness_evidence_review_disposition_actions,
            0
        );
        assert_eq!(
            summary.release_handoff_evidence_readiness_evidence_review_disposition_actions,
            0
        );
        assert_eq!(
            summary.lineage_complete_readiness_evidence_review_disposition_actions,
            5
        );
        assert_eq!(
            summary.first_readiness_evidence_review_disposition_action_key,
            Some(
                "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-review-clearance-action-readiness-evidence-review-disposition-action-01-schedule_release_blocker_repair-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_required_readiness_evidence_review_disposition_action_key,
            summary.first_readiness_evidence_review_disposition_action_key
        );
        assert_eq!(
            summary.first_repair_readiness_evidence_review_disposition_action_key,
            summary.first_readiness_evidence_review_disposition_action_key
        );
        assert_eq!(
            summary.first_review_readiness_evidence_review_disposition_action_key,
            Some(
                "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-review-clearance-action-readiness-evidence-review-disposition-action-02-complete_evidence_review-evidence_remediation"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_release_handoff_readiness_evidence_review_disposition_action_key,
            None
        );
        assert_eq!(
            summary.first_disposition_action_kind,
            Some(IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::ScheduleReleaseBlockerRepair)
        );
        assert_eq!(
            summary.next_readiness_evidence_review_disposition_action_key,
            summary.first_readiness_evidence_review_disposition_action_key
        );
        assert_eq!(summary.next_work_order_key, summary.first_work_order_key);
        assert_eq!(summary.next_ticket_key, summary.first_ticket_key);
        assert!(!summary
            .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_dispositions_ready);
        assert!(!summary
            .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_actions_ready);
        assert!(!summary
            .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_readiness_ready);
        assert!(!summary
            .ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_readiness());
        assert!(summary.has_readiness_evidence_review_disposition_actions());
        assert!(summary.has_required_actions());
        assert!(summary.has_blockers());
        assert!(summary.has_review_work());
        assert!(summary.needs_operator());
        assert!(summary.requires_dispatch());
        assert!(summary.requires_execution());
        assert!(!summary.has_lineage_gaps());
        assert!(!summary.has_release_handoff_actions());
        assert!(summary.has_complete_lineage());
        assert!(summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_readiness_evidence_review_disposition_action_readiness_marks_release_ready(
    ) {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let summary =
            mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_readiness_summary_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(
            summary.total_readiness_evidence_review_disposition_actions,
            5
        );
        assert_eq!(
            summary.required_readiness_evidence_review_disposition_actions,
            0
        );
        assert_eq!(
            summary.release_blocking_readiness_evidence_review_disposition_actions,
            0
        );
        assert_eq!(
            summary.repair_readiness_evidence_review_disposition_actions,
            0
        );
        assert_eq!(
            summary.review_readiness_evidence_review_disposition_actions,
            0
        );
        assert_eq!(
            summary.release_handoff_readiness_evidence_review_disposition_actions,
            5
        );
        assert_eq!(
            summary.release_handoff_evidence_readiness_evidence_review_disposition_actions,
            5
        );
        assert_eq!(
            summary.first_readiness_evidence_review_disposition_action_key,
            Some(
                "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-review-clearance-action-readiness-evidence-review-disposition-action-01-release_handoff-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_required_readiness_evidence_review_disposition_action_key,
            None
        );
        assert_eq!(
            summary.first_repair_readiness_evidence_review_disposition_action_key,
            None
        );
        assert_eq!(
            summary.first_release_handoff_readiness_evidence_review_disposition_action_key,
            summary.first_readiness_evidence_review_disposition_action_key
        );
        assert_eq!(
            summary.first_disposition_action_kind,
            Some(IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::ReleaseHandoff)
        );
        assert_eq!(
            summary.next_readiness_evidence_review_disposition_action_key,
            None
        );
        assert_eq!(summary.next_work_order_key, None);
        assert_eq!(summary.next_ticket_key, None);
        assert!(summary
            .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_dispositions_ready);
        assert!(summary
            .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_actions_ready);
        assert!(summary
            .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_readiness_ready);
        assert!(summary
            .ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_readiness());
        assert!(summary.has_readiness_evidence_review_disposition_actions());
        assert!(!summary.has_required_actions());
        assert!(!summary.has_blockers());
        assert!(!summary.has_review_work());
        assert!(!summary.needs_operator());
        assert!(!summary.requires_dispatch());
        assert!(!summary.requires_execution());
        assert!(!summary.has_lineage_gaps());
        assert!(summary.has_release_handoff_actions());
        assert!(summary.has_release_handoff_evidence());
        assert!(summary.has_complete_lineage());
        assert!(!summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slots_surface_attention(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let slots =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_readiness_execution_slots(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );

        assert_eq!(slots.len(), 5);
        assert_eq!(
            slots[0].readiness_evidence_review_disposition_action_readiness_execution_slot_key,
            "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-slot-01-schedule_release_blocker_repair-substrate_actions"
        );
        assert_eq!(
            slots[0].readiness_evidence_review_disposition_action_key,
            "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-review-clearance-action-readiness-evidence-review-disposition-action-01-schedule_release_blocker_repair-substrate_actions"
        );
        assert_eq!(
            slots[0].readiness_evidence_review_disposition_action_sequence,
            1
        );
        assert_eq!(
            slots[0].handoff_lane,
            IntegrationMeshReleaseDispatchTicketHandoffLane::Repair
        );
        assert_eq!(
            slots[0].check_kind,
            IntegrationMeshReleaseReadinessCheckKind::SubstrateActions
        );
        assert_eq!(slots.iter().filter(|slot| slot.blocks_release()).count(), 3);
        assert_eq!(slots.iter().filter(|slot| slot.is_repair_slot()).count(), 3);
        assert_eq!(slots.iter().filter(|slot| slot.is_review_slot()).count(), 2);
        assert_eq!(
            slots
                .iter()
                .filter(|slot| slot.requires_execution())
                .count(),
            5
        );
        assert_eq!(
            slots
                .iter()
                .filter(|slot| slot.requires_attention())
                .count(),
            5
        );
        assert_eq!(
            slots
                .iter()
                .filter(|slot| slot.is_release_handoff_slot())
                .count(),
            0
        );
        assert!(slots.iter().all(|slot| slot.has_lineage()));
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slots_mark_release_ready(
    ) {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let slots =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_readiness_execution_slots_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(slots.len(), 5);
        assert_eq!(
            slots[0].readiness_evidence_review_disposition_action_readiness_execution_slot_key,
            "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-slot-01-release_handoff-substrate_actions"
        );
        assert_eq!(
            slots[0].readiness_evidence_review_disposition_action_key,
            "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-review-clearance-action-readiness-evidence-review-disposition-action-01-release_handoff-substrate_actions"
        );
        assert_eq!(
            slots[0].handoff_lane,
            IntegrationMeshReleaseDispatchTicketHandoffLane::Release
        );
        assert_eq!(
            slots.iter().filter(|slot| slot.is_release_ready()).count(),
            5
        );
        assert_eq!(
            slots
                .iter()
                .filter(|slot| slot.is_release_handoff_slot())
                .count(),
            5
        );
        assert_eq!(slots.iter().filter(|slot| slot.blocks_release()).count(), 0);
        assert_eq!(slots.iter().filter(|slot| slot.is_repair_slot()).count(), 0);
        assert_eq!(slots.iter().filter(|slot| slot.is_review_slot()).count(), 0);
        assert_eq!(
            slots
                .iter()
                .filter(|slot| slot.requires_attention())
                .count(),
            0
        );
        assert!(slots.iter().all(|slot| slot.has_lineage()));
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slot_summary_surfaces_attention(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let summary =
            mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_readiness_execution_slot_summary(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );

        assert_eq!(
            summary.total_readiness_evidence_review_disposition_actions,
            5
        );
        assert_eq!(
            summary.total_readiness_evidence_review_disposition_action_readiness_execution_slots,
            5
        );
        assert_eq!(
            summary.required_readiness_evidence_review_disposition_action_readiness_execution_slots,
            5
        );
        assert_eq!(
            summary.release_blocking_readiness_evidence_review_disposition_action_readiness_execution_slots,
            3
        );
        assert_eq!(
            summary.repair_readiness_evidence_review_disposition_action_readiness_execution_slots,
            3
        );
        assert_eq!(
            summary.review_readiness_evidence_review_disposition_action_readiness_execution_slots,
            2
        );
        assert_eq!(
            summary.execution_required_readiness_evidence_review_disposition_action_readiness_execution_slots,
            5
        );
        assert_eq!(
            summary.release_handoff_readiness_evidence_review_disposition_action_readiness_execution_slots,
            0
        );
        assert_eq!(
            summary.first_readiness_evidence_review_disposition_action_readiness_execution_slot_key,
            Some(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-slot-01-schedule_release_blocker_repair-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_required_readiness_evidence_review_disposition_action_readiness_execution_slot_key,
            summary
                .first_readiness_evidence_review_disposition_action_readiness_execution_slot_key
        );
        assert_eq!(
            summary.first_repair_readiness_evidence_review_disposition_action_readiness_execution_slot_key,
            summary
                .first_readiness_evidence_review_disposition_action_readiness_execution_slot_key
        );
        assert_eq!(
            summary.first_review_readiness_evidence_review_disposition_action_readiness_execution_slot_key,
            Some(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-slot-02-complete_evidence_review-evidence_remediation"
                    .to_string()
            )
        );
        assert_eq!(
            summary.next_readiness_evidence_review_disposition_action_readiness_execution_slot_key,
            summary.first_readiness_evidence_review_disposition_action_readiness_execution_slot_key
        );
        assert_eq!(
            summary.next_readiness_evidence_review_disposition_action_key,
            summary.first_readiness_evidence_review_disposition_action_key
        );
        assert_eq!(summary.next_work_order_key, summary.first_work_order_key);
        assert_eq!(summary.next_ticket_key, summary.first_ticket_key);
        assert_eq!(
            summary.next_handoff_lane,
            Some(IntegrationMeshReleaseDispatchTicketHandoffLane::Repair)
        );
        assert!(!summary
            .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_readiness_ready);
        assert!(!summary
            .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slots_ready);
        assert!(!summary
            .ready_for_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slots());
        assert!(summary.has_execution_slots());
        assert!(summary.has_required_slots());
        assert!(summary.has_blockers());
        assert!(summary.has_review_work());
        assert!(summary.needs_operator());
        assert!(summary.requires_dispatch());
        assert!(summary.requires_execution());
        assert!(!summary.has_lineage_gaps());
        assert!(!summary.has_release_handoff_slots());
        assert!(summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slot_summary_marks_release_ready(
    ) {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let summary =
            mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_readiness_execution_slot_summary_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(
            summary.total_readiness_evidence_review_disposition_action_readiness_execution_slots,
            5
        );
        assert_eq!(
            summary.required_readiness_evidence_review_disposition_action_readiness_execution_slots,
            0
        );
        assert_eq!(
            summary.release_blocking_readiness_evidence_review_disposition_action_readiness_execution_slots,
            0
        );
        assert_eq!(
            summary.repair_readiness_evidence_review_disposition_action_readiness_execution_slots,
            0
        );
        assert_eq!(
            summary.review_readiness_evidence_review_disposition_action_readiness_execution_slots,
            0
        );
        assert_eq!(
            summary.release_handoff_readiness_evidence_review_disposition_action_readiness_execution_slots,
            5
        );
        assert_eq!(
            summary.release_ready_readiness_evidence_review_disposition_action_readiness_execution_slots,
            5
        );
        assert_eq!(
            summary.first_readiness_evidence_review_disposition_action_readiness_execution_slot_key,
            Some(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-slot-01-release_handoff-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_required_readiness_evidence_review_disposition_action_readiness_execution_slot_key,
            None
        );
        assert_eq!(
            summary.first_repair_readiness_evidence_review_disposition_action_readiness_execution_slot_key,
            None
        );
        assert_eq!(
            summary.first_release_handoff_readiness_evidence_review_disposition_action_readiness_execution_slot_key,
            summary
                .first_readiness_evidence_review_disposition_action_readiness_execution_slot_key
        );
        assert_eq!(
            summary.next_readiness_evidence_review_disposition_action_readiness_execution_slot_key,
            None
        );
        assert_eq!(summary.next_work_order_key, None);
        assert_eq!(summary.next_ticket_key, None);
        assert!(summary
            .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_clearance_action_readiness_evidence_review_disposition_action_readiness_ready);
        assert!(summary
            .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slots_ready);
        assert!(summary
            .ready_for_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slots());
        assert!(summary.has_execution_slots());
        assert!(!summary.has_required_slots());
        assert!(!summary.has_blockers());
        assert!(!summary.has_review_work());
        assert!(!summary.needs_operator());
        assert!(!summary.requires_dispatch());
        assert!(!summary.requires_execution());
        assert!(!summary.has_lineage_gaps());
        assert!(summary.has_release_handoff_slots());
        assert!(!summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows_surface_attention(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let rows =
            mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );

        assert_eq!(rows.len(), 5);
        assert_eq!(
            rows[0].handoff_row_key,
            "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-01-repair-substrate_actions"
        );
        assert_eq!(
            rows[0].readiness_evidence_review_disposition_action_readiness_execution_slot_key,
            "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-slot-01-schedule_release_blocker_repair-substrate_actions"
        );
        assert_eq!(
            rows[0].handoff_lane,
            IntegrationMeshReleaseDispatchTicketHandoffLane::Repair
        );
        assert_eq!(
            rows[0].check_kind,
            IntegrationMeshReleaseReadinessCheckKind::SubstrateActions
        );
        assert_eq!(rows.iter().filter(|row| row.blocks_release()).count(), 3);
        assert_eq!(rows.iter().filter(|row| row.is_repair_handoff()).count(), 3);
        assert_eq!(rows.iter().filter(|row| row.is_review_handoff()).count(), 2);
        assert_eq!(
            rows.iter().filter(|row| row.is_release_handoff()).count(),
            0
        );
        assert_eq!(
            rows.iter().filter(|row| row.requires_execution()).count(),
            5
        );
        assert_eq!(
            rows.iter().filter(|row| row.requires_attention()).count(),
            5
        );
        assert_eq!(rows.iter().filter(|row| row.ready_for_handoff()).count(), 0);
        assert!(rows.iter().all(|row| row.has_lineage()));
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows_mark_release_ready(
    ) {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let rows =
            mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(rows.len(), 5);
        assert_eq!(
            rows[0].handoff_row_key,
            "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-01-release-substrate_actions"
        );
        assert_eq!(
            rows[0].readiness_evidence_review_disposition_action_readiness_execution_slot_key,
            "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-slot-01-release_handoff-substrate_actions"
        );
        assert_eq!(
            rows[0].handoff_lane,
            IntegrationMeshReleaseDispatchTicketHandoffLane::Release
        );
        assert_eq!(
            rows.iter().filter(|row| row.is_release_handoff()).count(),
            5
        );
        assert_eq!(rows.iter().filter(|row| row.ready_for_handoff()).count(), 5);
        assert_eq!(rows.iter().filter(|row| row.blocks_release()).count(), 0);
        assert_eq!(rows.iter().filter(|row| row.is_repair_handoff()).count(), 0);
        assert_eq!(rows.iter().filter(|row| row.is_review_handoff()).count(), 0);
        assert_eq!(
            rows.iter().filter(|row| row.requires_attention()).count(),
            0
        );
        assert!(rows.iter().all(|row| row.has_lineage()));
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_summary_surfaces_attention(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let summary =
            mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_summary(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );

        assert_eq!(
            summary.total_readiness_evidence_review_disposition_action_readiness_execution_slots,
            5
        );
        assert_eq!(
            summary
                .total_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows,
            5
        );
        assert_eq!(
            summary
                .required_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows,
            5
        );
        assert_eq!(
            summary
                .release_blocking_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows,
            3
        );
        assert_eq!(
            summary
                .repair_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows,
            3
        );
        assert_eq!(
            summary
                .review_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows,
            2
        );
        assert_eq!(
            summary
                .execution_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows,
            5
        );
        assert_eq!(
            summary
                .release_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows,
            0
        );
        assert_eq!(
            summary.first_readiness_evidence_review_disposition_action_readiness_execution_handoff_row_key,
            Some(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-01-repair-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_row_key,
            summary
                .first_readiness_evidence_review_disposition_action_readiness_execution_handoff_row_key
        );
        assert_eq!(
            summary.first_repair_readiness_evidence_review_disposition_action_readiness_execution_handoff_row_key,
            summary
                .first_readiness_evidence_review_disposition_action_readiness_execution_handoff_row_key
        );
        assert_eq!(
            summary.first_review_readiness_evidence_review_disposition_action_readiness_execution_handoff_row_key,
            Some(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-02-review-evidence_remediation"
                    .to_string()
            )
        );
        assert_eq!(
            summary.next_readiness_evidence_review_disposition_action_readiness_execution_handoff_row_key,
            summary.first_readiness_evidence_review_disposition_action_readiness_execution_handoff_row_key
        );
        assert_eq!(
            summary.next_readiness_evidence_review_disposition_action_readiness_execution_slot_key,
            summary.first_readiness_evidence_review_disposition_action_readiness_execution_slot_key
        );
        assert_eq!(summary.next_work_order_key, summary.first_work_order_key);
        assert_eq!(summary.next_ticket_key, summary.first_ticket_key);
        assert_eq!(
            summary.next_handoff_lane,
            Some(IntegrationMeshReleaseDispatchTicketHandoffLane::Repair)
        );
        assert!(!summary
            .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slots_ready);
        assert!(!summary
            .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows_ready);
        assert!(!summary
            .ready_for_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoffs());
        assert!(summary.has_handoff_rows());
        assert!(summary.has_required_handoffs());
        assert!(summary.has_blockers());
        assert!(summary.has_review_work());
        assert!(summary.needs_operator());
        assert!(summary.requires_dispatch());
        assert!(summary.requires_execution());
        assert!(!summary.has_lineage_gaps());
        assert!(!summary.has_release_handoffs());
        assert!(summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_summary_marks_release_ready(
    ) {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let summary =
            mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_summary_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(
            summary
                .total_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows,
            5
        );
        assert_eq!(
            summary
                .required_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows,
            0
        );
        assert_eq!(
            summary
                .release_blocking_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows,
            0
        );
        assert_eq!(
            summary
                .repair_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows,
            0
        );
        assert_eq!(
            summary
                .review_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows,
            0
        );
        assert_eq!(
            summary
                .release_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows,
            5
        );
        assert_eq!(
            summary
                .release_ready_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows,
            5
        );
        assert_eq!(
            summary.first_readiness_evidence_review_disposition_action_readiness_execution_handoff_row_key,
            Some(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-01-release-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_row_key,
            None
        );
        assert_eq!(
            summary.first_repair_readiness_evidence_review_disposition_action_readiness_execution_handoff_row_key,
            None
        );
        assert_eq!(
            summary.first_release_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_row_key,
            summary
                .first_readiness_evidence_review_disposition_action_readiness_execution_handoff_row_key
        );
        assert_eq!(
            summary.next_readiness_evidence_review_disposition_action_readiness_execution_handoff_row_key,
            None
        );
        assert_eq!(summary.next_work_order_key, None);
        assert_eq!(summary.next_ticket_key, None);
        assert!(summary
            .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slots_ready);
        assert!(summary
            .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows_ready);
        assert!(summary
            .ready_for_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoffs());
        assert!(summary.has_handoff_rows());
        assert!(!summary.has_required_handoffs());
        assert!(!summary.has_blockers());
        assert!(!summary.has_review_work());
        assert!(!summary.needs_operator());
        assert!(!summary.requires_dispatch());
        assert!(!summary.requires_execution());
        assert!(!summary.has_lineage_gaps());
        assert!(summary.has_release_handoffs());
        assert!(!summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows_surface_attention(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let action_rows =
            mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );

        assert_eq!(action_rows.len(), 5);
        assert_eq!(
            action_rows[0].handoff_action_row_key,
            "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-01-repair_handoff-substrate_actions"
        );
        assert_eq!(
            action_rows[0].handoff_row_key,
            "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-01-repair-substrate_actions"
        );
        assert_eq!(
            action_rows[0].handoff_action_kind,
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind::RepairHandoff
        );
        assert_eq!(
            action_rows[0].handoff_lane,
            IntegrationMeshReleaseDispatchTicketHandoffLane::Repair
        );
        assert_eq!(
            action_rows[0].check_kind,
            IntegrationMeshReleaseReadinessCheckKind::SubstrateActions
        );
        assert_eq!(
            action_rows
                .iter()
                .filter(|row| row.is_repair_action())
                .count(),
            3
        );
        assert_eq!(
            action_rows
                .iter()
                .filter(|row| row.is_review_action())
                .count(),
            2
        );
        assert_eq!(
            action_rows
                .iter()
                .filter(|row| row.is_release_handoff_action())
                .count(),
            0
        );
        assert_eq!(
            action_rows
                .iter()
                .filter(|row| row.requires_execution())
                .count(),
            5
        );
        assert_eq!(
            action_rows
                .iter()
                .filter(|row| row.requires_attention())
                .count(),
            5
        );
        assert_eq!(
            action_rows
                .iter()
                .filter(|row| row.ready_for_release_handoff())
                .count(),
            0
        );
        assert!(action_rows.iter().all(|row| row.has_lineage()));
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows_mark_release_ready(
    ) {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let action_rows =
            mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(action_rows.len(), 5);
        assert_eq!(
            action_rows[0].handoff_action_row_key,
            "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-01-release_handoff-substrate_actions"
        );
        assert_eq!(
            action_rows[0].handoff_row_key,
            "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-01-release-substrate_actions"
        );
        assert_eq!(
            action_rows[0].handoff_action_kind,
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind::ReleaseHandoff
        );
        assert_eq!(
            action_rows[0].handoff_lane,
            IntegrationMeshReleaseDispatchTicketHandoffLane::Release
        );
        assert_eq!(
            action_rows
                .iter()
                .filter(|row| row.is_release_handoff_action())
                .count(),
            5
        );
        assert_eq!(
            action_rows
                .iter()
                .filter(|row| row.ready_for_release_handoff())
                .count(),
            5
        );
        assert_eq!(
            action_rows
                .iter()
                .filter(|row| row.is_repair_action())
                .count(),
            0
        );
        assert_eq!(
            action_rows
                .iter()
                .filter(|row| row.is_review_action())
                .count(),
            0
        );
        assert_eq!(
            action_rows
                .iter()
                .filter(|row| row.requires_attention())
                .count(),
            0
        );
        assert!(action_rows.iter().all(|row| row.has_lineage()));
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows_surface_action_lineage(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let evidence_rows =
            mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );

        assert_eq!(evidence_rows.len(), 5);
        assert_eq!(
            evidence_rows[0].handoff_action_evidence_row_key,
            "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-evidence-01-repair_handoff-substrate_actions"
        );
        assert_eq!(
            evidence_rows[0].handoff_action_row_key,
            "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-01-repair_handoff-substrate_actions"
        );
        assert_eq!(
            evidence_rows[0].handoff_row_key,
            "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-01-repair-substrate_actions"
        );
        assert_eq!(
            evidence_rows[0].handoff_action_kind,
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind::RepairHandoff
        );
        assert_eq!(
            evidence_rows
                .iter()
                .filter(|row| row.is_repair_action())
                .count(),
            3
        );
        assert_eq!(
            evidence_rows
                .iter()
                .filter(|row| row.is_review_action())
                .count(),
            2
        );
        assert_eq!(
            evidence_rows
                .iter()
                .filter(|row| row.is_release_handoff_action())
                .count(),
            0
        );
        assert_eq!(
            evidence_rows
                .iter()
                .filter(|row| row.requires_execution())
                .count(),
            5
        );
        assert_eq!(
            evidence_rows
                .iter()
                .filter(|row| row.requires_attention())
                .count(),
            5
        );
        assert_eq!(
            evidence_rows
                .iter()
                .filter(|row| row.ready_for_release_handoff())
                .count(),
            0
        );
        assert!(evidence_rows.iter().all(|row| row.has_lineage()));
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows_mark_release_ready(
    ) {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let evidence_rows =
            mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(evidence_rows.len(), 5);
        assert_eq!(
            evidence_rows[0].handoff_action_evidence_row_key,
            "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-evidence-01-release_handoff-substrate_actions"
        );
        assert_eq!(
            evidence_rows[0].handoff_action_row_key,
            "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-01-release_handoff-substrate_actions"
        );
        assert_eq!(
            evidence_rows[0].handoff_action_kind,
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind::ReleaseHandoff
        );
        assert_eq!(
            evidence_rows
                .iter()
                .filter(|row| row.is_release_handoff_action())
                .count(),
            5
        );
        assert_eq!(
            evidence_rows
                .iter()
                .filter(|row| row.ready_for_release_handoff())
                .count(),
            5
        );
        assert_eq!(
            evidence_rows
                .iter()
                .filter(|row| row.requires_attention())
                .count(),
            0
        );
        assert!(evidence_rows.iter().all(|row| row.has_lineage()));
        assert!(evidence_rows
            .iter()
            .all(|row| row.has_release_handoff_evidence()));
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_rows_surface_review_outcomes(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let evidence_rows =
            mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );
        let review_rows =
            mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_rows(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );

        assert_eq!(review_rows.len(), evidence_rows.len());
        assert_eq!(
            review_rows
                .iter()
                .filter(|row| row.is_blocker_review())
                .count(),
            3
        );
        assert_eq!(
            review_rows
                .iter()
                .filter(|row| row.is_review_gate())
                .count(),
            2
        );
        assert_eq!(
            review_rows
                .iter()
                .filter(|row| row.is_release_ready_review())
                .count(),
            0
        );
        assert!(review_rows
            .iter()
            .all(|row| !row.handoff_action_evidence_ready));
        assert!(review_rows.iter().all(
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewRow::requires_attention
        ));

        let first = &review_rows[0];
        assert_eq!(first.sequence, 1);
        assert_eq!(
            first.handoff_action_evidence_review_row_key,
            "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-evidence-review-01-blocker_review-substrate_actions"
        );
        assert_eq!(
            first.handoff_action_evidence_row_sequence,
            evidence_rows[0].sequence
        );
        assert_eq!(
            first.handoff_action_evidence_row_key,
            evidence_rows[0].handoff_action_evidence_row_key
        );
        assert_eq!(
            first.handoff_action_evidence_review_kind,
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::BlockerReview
        );
        assert!(first.is_blocker_review());
        assert!(first.requires_evidence_review());
        assert!(first.blocks_release());
        assert!(first.needs_operator());
        assert!(!first.needs_review());
        assert!(first.has_lineage());
        assert!(!first.is_release_ready_review());
        assert!(!first.ready_to_release_handoff());

        let mut lineage_evidence = evidence_rows[0].clone();
        lineage_evidence.handoff_row_key.clear();
        lineage_evidence.lineage_complete = false;
        lineage_evidence.handoff_action_ready = false;
        let lineage_review =
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewRow::from_evidence_row(
                1,
                &lineage_evidence,
                false,
            );

        assert_eq!(
            lineage_review.handoff_action_evidence_review_row_key,
            "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-evidence-review-01-lineage_gap-substrate_actions"
        );
        assert!(lineage_review.is_lineage_gap());
        assert!(!lineage_review.has_lineage());
        assert!(lineage_review.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_rows_mark_release_ready(
    ) {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let review_rows =
            mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_rows_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(review_rows.len(), 5);
        assert!(review_rows.iter().all(
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewRow::is_release_handoff_evidence
        ));
        assert!(review_rows.iter().all(
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewRow::is_release_ready_review
        ));
        assert!(review_rows.iter().all(
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewRow::ready_to_release_handoff
        ));
        assert!(review_rows
            .iter()
            .all(|row| row.handoff_action_evidence_ready));
        assert!(review_rows.iter().all(|row| !row.requires_attention()));
        assert_eq!(
            review_rows[0].handoff_action_evidence_review_row_key,
            "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-evidence-review-01-release_ready-substrate_actions"
        );
        assert_eq!(
            review_rows[0].handoff_action_evidence_review_kind,
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::ReleaseReady
        );
        assert_eq!(review_rows[0].handoff_action_evidence_row_sequence, 1);
        assert_eq!(
            review_rows[0].handoff_action_kind,
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind::ReleaseHandoff
        );
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_rows_surface_review_dispositions(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let review_rows =
            mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_rows(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );
        let disposition_rows =
            mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_rows(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );

        assert_eq!(disposition_rows.len(), review_rows.len());
        assert_eq!(
            disposition_rows
                .iter()
                .filter(|row| row.is_repair_disposition())
                .count(),
            3
        );
        assert_eq!(
            disposition_rows
                .iter()
                .filter(|row| row.is_review_disposition())
                .count(),
            2
        );
        assert_eq!(
            disposition_rows
                .iter()
                .filter(|row| row.is_release_handoff_disposition())
                .count(),
            0
        );
        assert_eq!(
            disposition_rows
                .iter()
                .filter(|row| row.is_release_ready_disposition())
                .count(),
            0
        );
        assert!(disposition_rows
            .iter()
            .all(|row| !row.handoff_action_evidence_reviews_ready));
        assert!(disposition_rows.iter().all(
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionRow::requires_attention
        ));

        let first = &disposition_rows[0];
        assert_eq!(first.sequence, 1);
        assert_eq!(
            first.handoff_action_evidence_review_disposition_row_key,
            "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-evidence-review-disposition-01-schedule_release_blocker_repair-substrate_actions"
        );
        assert_eq!(
            first.handoff_action_evidence_review_row_sequence,
            review_rows[0].sequence
        );
        assert_eq!(
            first.handoff_action_evidence_review_row_key,
            review_rows[0].handoff_action_evidence_review_row_key
        );
        assert_eq!(
            first.handoff_action_evidence_review_disposition_action_kind,
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::ScheduleReleaseBlockerRepair
        );
        assert!(first.is_repair_disposition());
        assert!(first.blocks_release());
        assert!(first.needs_operator());
        assert!(!first.needs_review());
        assert!(first.has_lineage());
        assert!(!first.is_release_handoff_evidence());
        assert!(!first.is_release_ready_disposition());
        assert!(!first.ready_to_release_handoff());

        let second = &disposition_rows[1];
        assert_eq!(
            second.handoff_action_evidence_review_disposition_row_key,
            "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-evidence-review-disposition-02-complete_evidence_review-evidence_remediation"
        );
        assert!(second.is_review_disposition());
        assert!(second.needs_review());

        let mut lineage_review = review_rows[0].clone();
        lineage_review.handoff_row_key.clear();
        lineage_review.lineage_complete = false;
        lineage_review.ready_for_release_handoff = false;
        let lineage_disposition =
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionRow::from_review_row(
                1,
                &lineage_review,
                false,
            );

        assert_eq!(
            lineage_disposition.handoff_action_evidence_review_disposition_row_key,
            "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-evidence-review-disposition-01-restore_evidence_lineage-substrate_actions"
        );
        assert!(lineage_disposition.is_lineage_repair_disposition());
        assert!(!lineage_disposition.has_lineage());
        assert!(lineage_disposition.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_rows_mark_release_ready(
    ) {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let disposition_rows =
            mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_rows_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(disposition_rows.len(), 5);
        assert!(disposition_rows.iter().all(
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionRow::is_release_handoff_disposition
        ));
        assert!(disposition_rows.iter().all(
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionRow::is_release_handoff_evidence
        ));
        assert!(disposition_rows.iter().all(
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionRow::is_release_ready_review
        ));
        assert!(disposition_rows.iter().all(
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionRow::is_release_ready_disposition
        ));
        assert!(disposition_rows.iter().all(
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionRow::ready_to_release_handoff
        ));
        assert!(disposition_rows
            .iter()
            .all(|row| row.handoff_action_evidence_reviews_ready));
        assert!(disposition_rows.iter().all(|row| !row.requires_attention()));
        assert_eq!(
            disposition_rows[0].handoff_action_evidence_review_disposition_row_key,
            "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-evidence-review-disposition-01-release_handoff-substrate_actions"
        );
        assert_eq!(
            disposition_rows[0].handoff_action_evidence_review_disposition_action_kind,
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::ReleaseHandoff
        );
        assert_eq!(
            disposition_rows[0].handoff_action_evidence_review_row_sequence,
            1
        );
        assert_eq!(
            disposition_rows[0].handoff_action_kind,
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind::ReleaseHandoff
        );
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows_route_review_dispositions(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let disposition_rows =
            mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_rows(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );
        let action_rows =
            mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );

        assert_eq!(action_rows.len(), disposition_rows.len());
        assert_eq!(
            action_rows
                .iter()
                .filter(|row| row.is_repair_action())
                .count(),
            3
        );
        assert_eq!(
            action_rows
                .iter()
                .filter(|row| row.is_review_action())
                .count(),
            2
        );
        assert_eq!(
            action_rows
                .iter()
                .filter(|row| row.is_release_handoff_action())
                .count(),
            0
        );
        assert!(action_rows.iter().all(
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionRow::requires_attention
        ));

        let first = &action_rows[0];
        assert_eq!(first.sequence, 1);
        assert_eq!(
            first.handoff_action_evidence_review_disposition_action_row_key,
            "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-evidence-review-disposition-action-01-schedule_release_blocker_repair-substrate_actions"
        );
        assert_eq!(
            first.handoff_action_evidence_review_disposition_row_sequence,
            disposition_rows[0].sequence
        );
        assert_eq!(
            first.handoff_action_evidence_review_disposition_row_key,
            disposition_rows[0].handoff_action_evidence_review_disposition_row_key
        );
        assert_eq!(
            first.handoff_action_evidence_review_disposition_action_kind,
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::ScheduleReleaseBlockerRepair
        );
        assert!(first.is_repair_action());
        assert!(first.blocks_release());
        assert!(first.needs_operator());
        assert!(!first.needs_review());
        assert!(first.has_lineage());
        assert!(!first.is_release_handoff_evidence());
        assert!(!first.is_release_ready_disposition());
        assert!(!first.is_release_ready_disposition_action());
        assert!(!first.ready_to_release_handoff());

        let second = &action_rows[1];
        assert_eq!(
            second.handoff_action_evidence_review_disposition_action_row_key,
            "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-evidence-review-disposition-action-02-complete_evidence_review-evidence_remediation"
        );
        assert!(second.is_review_action());
        assert!(second.needs_review());
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows_mark_release_ready(
    ) {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let action_rows =
            mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(action_rows.len(), 5);
        assert!(action_rows.iter().all(
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionRow::is_release_handoff_action
        ));
        assert!(action_rows.iter().all(
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionRow::is_release_handoff_evidence
        ));
        assert!(action_rows.iter().all(
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionRow::is_release_ready_review
        ));
        assert!(action_rows.iter().all(
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionRow::is_release_ready_disposition
        ));
        assert!(action_rows.iter().all(
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionRow::is_release_ready_disposition_action
        ));
        assert!(action_rows.iter().all(
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionRow::ready_to_release_handoff
        ));
        assert!(action_rows
            .iter()
            .all(|row| row.handoff_action_evidence_reviews_ready));
        assert!(action_rows.iter().all(|row| !row.requires_attention()));
        assert_eq!(
            action_rows[0].handoff_action_evidence_review_disposition_action_row_key,
            "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-evidence-review-disposition-action-01-release_handoff-substrate_actions"
        );
        assert_eq!(
            action_rows[0].handoff_action_evidence_review_disposition_action_kind,
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::ReleaseHandoff
        );
        assert_eq!(
            action_rows[0].handoff_action_evidence_review_disposition_row_sequence,
            1
        );
        assert_eq!(
            action_rows[0].handoff_action_kind,
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind::ReleaseHandoff
        );
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_summary_surfaces_action_attention(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let summary =
            mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_summary(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );

        assert_eq!(
            summary
                .total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_rows,
            5
        );
        assert_eq!(
            summary
                .total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows,
            5
        );
        assert_eq!(
            summary
                .required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows,
            5
        );
        assert_eq!(
            summary
                .repair_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows,
            3
        );
        assert_eq!(
            summary
                .review_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows,
            2
        );
        assert_eq!(
            summary
                .operator_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows,
            0
        );
        assert_eq!(
            summary
                .operator_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows,
            4
        );
        assert_eq!(
            summary
                .lineage_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows,
            0
        );
        assert_eq!(
            summary
                .lineage_complete_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows,
            5
        );
        assert_eq!(
            summary
                .release_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows,
            0
        );
        assert_eq!(
            summary
                .release_ready_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows,
            0
        );
        assert_eq!(
            summary.first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_row_key,
            Some(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-evidence-review-disposition-action-01-schedule_release_blocker_repair-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_row_key,
            summary
                .first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_row_key
        );
        assert_eq!(
            summary.first_repair_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_row_key,
            summary
                .first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_row_key
        );
        assert_eq!(
            summary.first_review_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_row_key,
            Some(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-evidence-review-disposition-action-02-complete_evidence_review-evidence_remediation"
                    .to_string()
            )
        );
        assert_eq!(
            summary.next_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_row_key,
            summary
                .first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_row_key
        );
        assert_eq!(
            summary.next_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_row_key,
            summary
                .first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_row_key
        );
        assert_eq!(summary.next_work_order_key, summary.first_work_order_key);
        assert_eq!(summary.next_ticket_key, summary.first_ticket_key);
        assert_eq!(
            summary.next_handoff_lane,
            Some(IntegrationMeshReleaseDispatchTicketHandoffLane::Repair)
        );
        assert!(!summary
            .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_dispositions_ready);
        assert!(!summary
            .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_actions_ready);
        assert!(!summary.ready_for_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_actions());
        assert!(summary.has_handoff_action_evidence_review_disposition_action_rows());
        assert!(summary.has_required_actions());
        assert!(summary.has_blockers());
        assert!(summary.needs_operator());
        assert!(summary.has_review_work());
        assert!(!summary.has_lineage_actions());
        assert!(!summary.has_release_handoff_actions());
        assert!(!summary.has_release_ready_actions());
        assert!(summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_summary_marks_release_ready(
    ) {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let summary =
            mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_summary_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(
            summary
                .total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows,
            5
        );
        assert_eq!(
            summary
                .required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows,
            0
        );
        assert_eq!(
            summary
                .repair_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows,
            0
        );
        assert_eq!(
            summary
                .operator_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows,
            0
        );
        assert_eq!(
            summary
                .operator_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows,
            0
        );
        assert_eq!(
            summary
                .review_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows,
            0
        );
        assert_eq!(
            summary
                .lineage_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows,
            0
        );
        assert_eq!(
            summary
                .lineage_complete_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows,
            5
        );
        assert_eq!(
            summary
                .release_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows,
            5
        );
        assert_eq!(
            summary
                .release_handoff_evidence_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows,
            5
        );
        assert_eq!(
            summary
                .release_ready_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows,
            5
        );
        assert_eq!(
            summary.first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_row_key,
            Some(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-evidence-review-disposition-action-01-release_handoff-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_row_key,
            None
        );
        assert_eq!(
            summary.first_release_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_row_key,
            summary
                .first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_row_key
        );
        assert_eq!(
            summary.first_release_ready_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_row_key,
            summary
                .first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_row_key
        );
        assert_eq!(
            summary.next_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_row_key,
            None
        );
        assert_eq!(summary.next_work_order_key, None);
        assert_eq!(summary.next_ticket_key, None);
        assert!(summary
            .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_dispositions_ready);
        assert!(summary
            .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_actions_ready);
        assert!(summary.ready_for_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_actions());
        assert!(summary.has_handoff_action_evidence_review_disposition_action_rows());
        assert!(!summary.has_required_actions());
        assert!(!summary.has_blockers());
        assert!(!summary.needs_operator());
        assert!(!summary.has_review_work());
        assert!(!summary.has_lineage_actions());
        assert!(summary.has_release_handoff_actions());
        assert!(summary.has_release_handoff_evidence());
        assert!(summary.has_release_ready_actions());
        assert!(!summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows_surface_action_readiness(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let action_rows =
            mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );
        let readiness_rows =
            mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );

        assert_eq!(readiness_rows.len(), action_rows.len());
        assert_eq!(
            readiness_rows
                .iter()
                .filter(|row| row.is_repair_readiness())
                .count(),
            3
        );
        assert_eq!(
            readiness_rows
                .iter()
                .filter(|row| row.is_review_readiness())
                .count(),
            2
        );
        assert_eq!(
            readiness_rows
                .iter()
                .filter(|row| row.is_release_handoff_readiness())
                .count(),
            0
        );
        assert!(readiness_rows
            .iter()
            .all(|row| !row.handoff_action_evidence_review_disposition_actions_ready));
        assert!(readiness_rows.iter().all(
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessRow::requires_attention
        ));

        let first = &readiness_rows[0];
        assert_eq!(first.sequence, 1);
        assert_eq!(
            first.handoff_action_evidence_review_disposition_action_readiness_row_key,
            "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-evidence-review-disposition-action-readiness-01-schedule_release_blocker_repair-substrate_actions"
        );
        assert_eq!(
            first.handoff_action_evidence_review_disposition_action_row_sequence,
            action_rows[0].sequence
        );
        assert_eq!(
            first.handoff_action_evidence_review_disposition_action_row_key,
            action_rows[0].handoff_action_evidence_review_disposition_action_row_key
        );
        assert_eq!(
            first.handoff_action_evidence_review_disposition_action_kind,
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::ScheduleReleaseBlockerRepair
        );
        assert!(first.is_repair_readiness());
        assert!(first.blocks_release());
        assert!(first.needs_operator());
        assert!(!first.needs_review());
        assert!(first.has_lineage());
        assert!(!first.is_release_handoff_evidence());
        assert!(!first.is_release_ready_disposition_action());
        assert!(!first.ready_to_release_handoff());

        let second = &readiness_rows[1];
        assert_eq!(
            second.handoff_action_evidence_review_disposition_action_readiness_row_key,
            "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-evidence-review-disposition-action-readiness-02-complete_evidence_review-evidence_remediation"
        );
        assert!(second.is_review_readiness());
        assert!(second.needs_review());
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows_mark_release_ready(
    ) {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let readiness_rows =
            mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(readiness_rows.len(), 5);
        assert!(readiness_rows.iter().all(
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessRow::is_release_handoff_readiness
        ));
        assert!(readiness_rows.iter().all(
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessRow::is_release_handoff_evidence
        ));
        assert!(readiness_rows.iter().all(
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessRow::is_release_ready_review
        ));
        assert!(readiness_rows.iter().all(
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessRow::is_release_ready_disposition
        ));
        assert!(readiness_rows.iter().all(
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessRow::is_release_ready_disposition_action
        ));
        assert!(readiness_rows.iter().all(
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessRow::ready_to_release_handoff
        ));
        assert!(readiness_rows
            .iter()
            .all(|row| row.handoff_action_evidence_review_disposition_actions_ready));
        assert!(readiness_rows.iter().all(|row| !row.requires_attention()));
        assert_eq!(
            readiness_rows[0].handoff_action_evidence_review_disposition_action_readiness_row_key,
            "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-evidence-review-disposition-action-readiness-01-release_handoff-substrate_actions"
        );
        assert_eq!(
            readiness_rows[0].handoff_action_evidence_review_disposition_action_row_sequence,
            1
        );
        assert_eq!(
            readiness_rows[0].handoff_action_kind,
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind::ReleaseHandoff
        );
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_summary_surfaces_readiness_attention(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let summary =
            mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_summary(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );

        assert_eq!(
            summary
                .total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_rows,
            5
        );
        assert_eq!(
            summary
                .total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows,
            5
        );
        assert_eq!(
            summary
                .required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows,
            5
        );
        assert_eq!(
            summary
                .repair_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows,
            3
        );
        assert_eq!(
            summary
                .review_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows,
            2
        );
        assert_eq!(
            summary
                .operator_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows,
            0
        );
        assert_eq!(
            summary
                .operator_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows,
            4
        );
        assert_eq!(
            summary
                .dispatch_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows,
            5
        );
        assert_eq!(
            summary
                .execution_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows,
            5
        );
        assert_eq!(
            summary
                .lineage_complete_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows,
            5
        );
        assert_eq!(
            summary
                .release_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows,
            0
        );
        assert_eq!(
            summary
                .release_ready_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows,
            0
        );
        assert_eq!(
            summary.first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_row_key,
            Some(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-evidence-review-disposition-action-readiness-01-schedule_release_blocker_repair-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_row_key,
            summary
                .first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_row_key
        );
        assert_eq!(
            summary.first_repair_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_row_key,
            summary
                .first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_row_key
        );
        assert_eq!(
            summary.first_review_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_row_key,
            Some(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-evidence-review-disposition-action-readiness-02-complete_evidence_review-evidence_remediation"
                    .to_string()
            )
        );
        assert_eq!(
            summary.next_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_row_key,
            summary
                .first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_row_key
        );
        assert_eq!(
            summary.next_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_row_key,
            summary
                .first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_row_key
        );
        assert_eq!(summary.next_work_order_key, summary.first_work_order_key);
        assert_eq!(summary.next_ticket_key, summary.first_ticket_key);
        assert_eq!(
            summary.next_handoff_lane,
            Some(IntegrationMeshReleaseDispatchTicketHandoffLane::Repair)
        );
        assert!(!summary
            .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_actions_ready);
        assert!(!summary
            .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_ready);
        assert!(!summary.ready_for_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness());
        assert!(summary.has_handoff_action_evidence_review_disposition_action_readiness_rows());
        assert!(summary.has_required_readiness());
        assert!(summary.has_blockers());
        assert!(summary.needs_operator());
        assert!(summary.has_review_work());
        assert!(!summary.has_lineage_readiness());
        assert!(!summary.has_release_handoff_readiness());
        assert!(!summary.has_release_ready_readiness());
        assert!(summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_summary_marks_release_ready(
    ) {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let summary =
            mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_summary_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(
            summary
                .total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows,
            5
        );
        assert_eq!(
            summary
                .required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows,
            0
        );
        assert_eq!(
            summary
                .repair_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows,
            0
        );
        assert_eq!(
            summary
                .operator_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows,
            0
        );
        assert_eq!(
            summary
                .review_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows,
            0
        );
        assert_eq!(
            summary
                .release_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows,
            5
        );
        assert_eq!(
            summary
                .release_handoff_evidence_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows,
            5
        );
        assert_eq!(
            summary
                .release_ready_review_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows,
            5
        );
        assert_eq!(
            summary
                .release_ready_disposition_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows,
            5
        );
        assert_eq!(
            summary
                .release_ready_disposition_action_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows,
            5
        );
        assert_eq!(
            summary
                .release_ready_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_rows,
            5
        );
        assert_eq!(
            summary.first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_row_key,
            Some(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-evidence-review-disposition-action-readiness-01-release_handoff-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_row_key,
            None
        );
        assert_eq!(
            summary.first_release_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_row_key,
            summary
                .first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_row_key
        );
        assert_eq!(
            summary.first_release_ready_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_row_key,
            summary
                .first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_row_key
        );
        assert_eq!(
            summary.next_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_row_key,
            None
        );
        assert_eq!(summary.next_work_order_key, None);
        assert_eq!(summary.next_ticket_key, None);
        assert!(summary
            .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_actions_ready);
        assert!(summary
            .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_ready);
        assert!(summary.ready_for_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness());
        assert!(summary.has_handoff_action_evidence_review_disposition_action_readiness_rows());
        assert!(!summary.has_required_readiness());
        assert!(!summary.has_blockers());
        assert!(!summary.needs_operator());
        assert!(!summary.has_review_work());
        assert!(!summary.has_lineage_readiness());
        assert!(summary.has_release_handoff_readiness());
        assert!(summary.has_release_handoff_evidence());
        assert!(summary.has_release_ready_readiness());
        assert!(!summary.requires_attention());
    }

    fn readiness_execution_slot_source_row(
        action_kind: IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind,
        release_ready: bool,
    ) -> IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessRow{
        IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessRow {
            sequence: 7,
            handoff_action_evidence_review_disposition_action_readiness_row_key:
                "source-readiness-row".to_string(),
            handoff_action_evidence_review_disposition_action_row_sequence: 5,
            handoff_action_evidence_review_disposition_action_row_key: "source-action-row"
                .to_string(),
            handoff_action_evidence_review_disposition_row_key: "source-disposition-row"
                .to_string(),
            handoff_action_evidence_review_row_key: "source-review-row".to_string(),
            handoff_action_evidence_row_key: "source-evidence-row".to_string(),
            handoff_action_row_key: "source-handoff-action-row".to_string(),
            handoff_row_key: "source-handoff-row".to_string(),
            readiness_evidence_review_disposition_action_readiness_execution_slot_key:
                "source-readiness-execution-slot".to_string(),
            readiness_evidence_review_disposition_action_key: "source-readiness-action"
                .to_string(),
            work_order_key: "source-work-order".to_string(),
            ticket_key: "source-ticket".to_string(),
            check_sequence: 3,
            check_kind: IntegrationMeshReleaseReadinessCheckKind::SubstrateActions,
            status: if release_ready {
                IntegrationMeshReleaseReadinessStatus::Ready
            } else {
                IntegrationMeshReleaseReadinessStatus::Blocked
            },
            handoff_lane: if release_ready {
                IntegrationMeshReleaseDispatchTicketHandoffLane::Release
            } else {
                IntegrationMeshReleaseDispatchTicketHandoffLane::Repair
            },
            handoff_action_kind: if release_ready {
                IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind::ReleaseHandoff
            } else {
                IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind::RepairHandoff
            },
            disposition_action_kind: action_kind,
            handoff_action_evidence_review_kind:
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::ReleaseReady,
            handoff_action_evidence_review_disposition_action_kind: action_kind,
            release_blocking: !release_ready,
            operator_required: !release_ready,
            review_required: false,
            dispatch_required: !release_ready,
            execution_required: !release_ready,
            lineage_complete: true,
            release_handoff_evidence: release_ready,
            release_ready_review: release_ready,
            handoff_action_evidence_reviews_ready: release_ready,
            release_ready_disposition: release_ready,
            release_ready_disposition_action: release_ready,
            handoff_action_evidence_review_disposition_actions_ready: release_ready,
            ready_for_release_handoff: release_ready,
        }
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_execution_slot_surfaces_readiness_work(
    ) {
        let row = readiness_execution_slot_source_row(
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::ScheduleReleaseBlockerRepair,
            false,
        );
        let slot = IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionSlot::from_readiness_row(1, &row, false);

        assert_eq!(slot.sequence, 1);
        assert_eq!(
            slot.handoff_action_evidence_review_disposition_action_readiness_execution_slot_key,
            "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-execution-slot-01-schedule_release_blocker_repair-substrate_actions"
        );
        assert_eq!(
            slot.handoff_action_evidence_review_disposition_action_readiness_row_sequence,
            row.sequence
        );
        assert_eq!(
            slot.handoff_action_evidence_review_disposition_action_readiness_row_key,
            row.handoff_action_evidence_review_disposition_action_readiness_row_key
        );
        assert!(slot.is_repair_slot());
        assert!(slot.blocks_release());
        assert!(slot.needs_operator());
        assert!(slot.requires_dispatch());
        assert!(slot.requires_execution());
        assert!(slot.has_lineage());
        assert!(!slot.release_handoff_evidence);
        assert!(!slot.release_ready_disposition_action);
        assert!(!slot.handoff_action_evidence_review_disposition_action_readiness_ready);
        assert!(!slot.ready_for_execution_handoff());
        assert!(slot.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_execution_slot_marks_release_ready(
    ) {
        let row = readiness_execution_slot_source_row(
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::ReleaseHandoff,
            true,
        );
        let slot = IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionSlot::from_readiness_row(1, &row, true);

        assert_eq!(
            slot.handoff_action_evidence_review_disposition_action_readiness_execution_slot_key,
            "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-execution-slot-01-release_handoff-substrate_actions"
        );
        assert!(slot.is_release_handoff_slot());
        assert!(slot.release_handoff_evidence);
        assert!(slot.release_ready_review);
        assert!(slot.release_ready_disposition);
        assert!(slot.release_ready_disposition_action);
        assert!(slot.handoff_action_evidence_review_disposition_action_readiness_ready);
        assert!(slot.ready_for_execution_handoff());
        assert!(!slot.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_execution_slot_summary_surfaces_attention(
    ) {
        let repair_row = readiness_execution_slot_source_row(
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::ScheduleReleaseBlockerRepair,
            false,
        );
        let review_row = readiness_execution_slot_source_row(
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::CompleteEvidenceReview,
            false,
        );
        let slots = [IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionSlot::from_readiness_row(1, &repair_row, false),
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionSlot::from_readiness_row(2, &review_row, false)];
        let summary =
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionSlotSummary::from_slots(
                2,
                false,
                slots.iter(),
            );

        assert_eq!(
            summary
                .total_handoff_action_evidence_review_disposition_action_readiness_execution_slots,
            2
        );
        assert_eq!(
            summary
                .required_handoff_action_evidence_review_disposition_action_readiness_execution_slots,
            2
        );
        assert_eq!(
            summary
                .repair_handoff_action_evidence_review_disposition_action_readiness_execution_slots,
            1
        );
        assert_eq!(
            summary
                .review_handoff_action_evidence_review_disposition_action_readiness_execution_slots,
            1
        );
        assert_eq!(
            summary
                .operator_required_handoff_action_evidence_review_disposition_action_readiness_execution_slots,
            2
        );
        assert_eq!(
            summary.first_handoff_action_evidence_review_disposition_action_readiness_execution_slot_key,
            Some(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-execution-slot-01-schedule_release_blocker_repair-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.next_handoff_action_evidence_review_disposition_action_readiness_execution_slot_key,
            summary
                .first_handoff_action_evidence_review_disposition_action_readiness_execution_slot_key
        );
        assert_eq!(
            summary.next_handoff_lane,
            Some(IntegrationMeshReleaseDispatchTicketHandoffLane::Repair)
        );
        assert!(!summary
            .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_ready);
        assert!(!summary
            .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slots_ready);
        assert!(summary.has_execution_slots());
        assert!(summary.has_required_slots());
        assert!(summary.has_blockers());
        assert!(summary.needs_operator());
        assert!(summary.has_review_work());
        assert!(summary.requires_dispatch());
        assert!(summary.requires_execution());
        assert!(!summary.has_release_ready_slots());
        assert!(summary.requires_attention());
        assert!(!summary.ready_for_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slots());
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_execution_slot_summary_marks_release_ready(
    ) {
        let row = readiness_execution_slot_source_row(
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::ReleaseHandoff,
            true,
        );
        let slots = [IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionSlot::from_readiness_row(1, &row, true)];
        let summary =
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionSlotSummary::from_slots(
                1,
                true,
                slots.iter(),
            );

        assert_eq!(
            summary
                .total_handoff_action_evidence_review_disposition_action_readiness_execution_slots,
            1
        );
        assert_eq!(
            summary
                .required_handoff_action_evidence_review_disposition_action_readiness_execution_slots,
            0
        );
        assert_eq!(
            summary
                .release_handoff_action_evidence_review_disposition_action_readiness_execution_slots,
            1
        );
        assert_eq!(
            summary
                .release_ready_handoff_action_evidence_review_disposition_action_readiness_execution_slots,
            1
        );
        assert_eq!(
            summary.first_release_ready_handoff_action_evidence_review_disposition_action_readiness_execution_slot_key,
            summary
                .first_handoff_action_evidence_review_disposition_action_readiness_execution_slot_key
        );
        assert_eq!(
            summary.next_handoff_action_evidence_review_disposition_action_readiness_execution_slot_key,
            None
        );
        assert!(summary
            .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_ready);
        assert!(summary
            .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slots_ready);
        assert!(summary.has_execution_slots());
        assert!(!summary.has_required_slots());
        assert!(!summary.has_blockers());
        assert!(!summary.needs_operator());
        assert!(!summary.has_review_work());
        assert!(!summary.requires_dispatch());
        assert!(!summary.requires_execution());
        assert!(summary.has_release_handoff_slots());
        assert!(summary.has_release_ready_slots());
        assert!(!summary.requires_attention());
        assert!(summary.ready_for_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slots());
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_row_surfaces_repair_work(
    ) {
        let row = readiness_execution_slot_source_row(
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::ScheduleReleaseBlockerRepair,
            false,
        );
        let slot = IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionSlot::from_readiness_row(1, &row, false);
        let handoff_row =
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionHandoffRow::from_readiness_execution_slot(
                1,
                &slot,
                false,
            );

        assert_eq!(handoff_row.sequence, 1);
        assert_eq!(
            handoff_row.handoff_action_evidence_review_disposition_action_readiness_execution_handoff_row_key,
            "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-execution-handoff-01-schedule_release_blocker_repair-substrate_actions"
        );
        assert_eq!(
            handoff_row
                .handoff_action_evidence_review_disposition_action_readiness_execution_slot_sequence,
            slot.sequence
        );
        assert_eq!(
            handoff_row
                .handoff_action_evidence_review_disposition_action_readiness_execution_slot_key,
            slot.handoff_action_evidence_review_disposition_action_readiness_execution_slot_key
        );
        assert!(handoff_row.is_repair_handoff());
        assert!(handoff_row.blocks_release());
        assert!(handoff_row.needs_operator());
        assert!(handoff_row.requires_dispatch());
        assert!(handoff_row.requires_execution());
        assert!(handoff_row.has_lineage());
        assert!(!handoff_row.release_handoff_evidence);
        assert!(!handoff_row.release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slots_ready);
        assert!(!handoff_row.ready_for_handoff());
        assert!(handoff_row.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_row_marks_release_ready(
    ) {
        let row = readiness_execution_slot_source_row(
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::ReleaseHandoff,
            true,
        );
        let slot = IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionSlot::from_readiness_row(1, &row, true);
        let handoff_row =
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionHandoffRow::from_readiness_execution_slot(
                1,
                &slot,
                true,
            );

        assert_eq!(
            handoff_row.handoff_action_evidence_review_disposition_action_readiness_execution_handoff_row_key,
            "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-execution-handoff-01-release_handoff-substrate_actions"
        );
        assert!(handoff_row.is_release_handoff());
        assert!(handoff_row.release_handoff_evidence);
        assert!(handoff_row.release_ready_review);
        assert!(handoff_row.release_ready_disposition);
        assert!(handoff_row.release_ready_disposition_action);
        assert!(handoff_row.release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slots_ready);
        assert!(handoff_row.ready_for_handoff());
        assert!(!handoff_row.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_summary_surfaces_attention(
    ) {
        let repair_row = readiness_execution_slot_source_row(
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::ScheduleReleaseBlockerRepair,
            false,
        );
        let review_row = readiness_execution_slot_source_row(
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::CompleteEvidenceReview,
            false,
        );
        let slots = [IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionSlot::from_readiness_row(1, &repair_row, false),
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionSlot::from_readiness_row(2, &review_row, false)];
        let slot_summary =
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionSlotSummary::from_slots(
                2,
                false,
                slots.iter(),
            );
        let rows = slots
            .iter()
            .enumerate()
            .map(|(index, slot)| {
                IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionHandoffRow::from_readiness_execution_slot(
                    index + 1,
                    slot,
                    false,
                )
            })
            .collect::<Vec<_>>();
        let summary =
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionHandoffSummary::from_summaries(
                slot_summary,
                rows.iter(),
            );

        assert_eq!(
            summary
                .total_handoff_action_evidence_review_disposition_action_readiness_execution_slots,
            2
        );
        assert_eq!(
            summary.total_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_rows,
            2
        );
        assert_eq!(
            summary.required_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_rows,
            2
        );
        assert_eq!(
            summary.repair_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_rows,
            1
        );
        assert_eq!(
            summary.review_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_rows,
            1
        );
        assert_eq!(
            summary.operator_required_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_rows,
            2
        );
        assert_eq!(
            summary.first_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_row_key,
            Some(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-execution-handoff-01-schedule_release_blocker_repair-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.next_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_row_key,
            summary
                .first_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_row_key
        );
        assert_eq!(
            summary.next_handoff_lane,
            Some(IntegrationMeshReleaseDispatchTicketHandoffLane::Repair)
        );
        assert!(!summary
            .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slots_ready);
        assert!(!summary
            .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows_ready);
        assert!(summary.has_handoff_rows());
        assert!(summary.has_required_handoffs());
        assert!(summary.has_blockers());
        assert!(summary.needs_operator());
        assert!(summary.has_review_work());
        assert!(summary.requires_dispatch());
        assert!(summary.requires_execution());
        assert!(!summary.has_release_ready_handoffs());
        assert!(summary.requires_attention());
        assert!(!summary.ready_for_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoffs());
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_summary_marks_release_ready(
    ) {
        let row = readiness_execution_slot_source_row(
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::ReleaseHandoff,
            true,
        );
        let slots = [IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionSlot::from_readiness_row(1, &row, true)];
        let slot_summary =
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionSlotSummary::from_slots(
                1,
                true,
                slots.iter(),
            );
        let rows = slots
            .iter()
            .enumerate()
            .map(|(index, slot)| {
                IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionHandoffRow::from_readiness_execution_slot(
                    index + 1,
                    slot,
                    true,
                )
            })
            .collect::<Vec<_>>();
        let summary =
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionHandoffSummary::from_summaries(
                slot_summary,
                rows.iter(),
            );

        assert_eq!(
            summary.total_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_rows,
            1
        );
        assert_eq!(
            summary.required_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_rows,
            0
        );
        assert_eq!(
            summary.release_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_rows,
            1
        );
        assert_eq!(
            summary.release_ready_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_rows,
            1
        );
        assert_eq!(
            summary.first_release_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_row_key,
            summary
                .first_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_row_key
        );
        assert_eq!(
            summary.next_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_row_key,
            None
        );
        assert!(summary
            .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_slots_ready);
        assert!(summary
            .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows_ready);
        assert!(summary.has_handoff_rows());
        assert!(!summary.has_required_handoffs());
        assert!(!summary.has_blockers());
        assert!(!summary.needs_operator());
        assert!(!summary.has_review_work());
        assert!(!summary.requires_dispatch());
        assert!(!summary.requires_execution());
        assert!(summary.has_release_handoffs());
        assert!(summary.has_release_ready_handoffs());
        assert!(!summary.requires_attention());
        assert!(summary.ready_for_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoffs());
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_row_surfaces_repair_work(
    ) {
        let row = readiness_execution_slot_source_row(
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::ScheduleReleaseBlockerRepair,
            false,
        );
        let slot = IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionSlot::from_readiness_row(1, &row, false);
        let handoff_row =
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionHandoffRow::from_readiness_execution_slot(
                1,
                &slot,
                false,
            );
        let action_row =
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionHandoffActionRow::from_handoff_row(
                1,
                &handoff_row,
                false,
            );

        assert_eq!(action_row.sequence, 1);
        assert_eq!(
            action_row.handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_row_key,
            "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-execution-handoff-action-01-repair_handoff-substrate_actions"
        );
        assert_eq!(
            action_row.handoff_action_evidence_review_disposition_action_readiness_execution_handoff_row_sequence,
            handoff_row.sequence
        );
        assert_eq!(
            action_row.handoff_action_evidence_review_disposition_action_readiness_execution_handoff_row_key,
            handoff_row.handoff_action_evidence_review_disposition_action_readiness_execution_handoff_row_key
        );
        assert!(action_row.is_repair_action());
        assert!(action_row.blocks_release());
        assert!(action_row.needs_operator());
        assert!(action_row.requires_dispatch());
        assert!(action_row.requires_execution());
        assert!(action_row.has_lineage());
        assert!(!action_row.release_handoff_evidence);
        assert!(!action_row
            .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoffs_ready);
        assert!(!action_row.ready_for_release_handoff());
        assert!(action_row.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_row_marks_release_ready(
    ) {
        let row = readiness_execution_slot_source_row(
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::ReleaseHandoff,
            true,
        );
        let slot = IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionSlot::from_readiness_row(1, &row, true);
        let handoff_row =
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionHandoffRow::from_readiness_execution_slot(
                1,
                &slot,
                true,
            );
        let action_row =
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionHandoffActionRow::from_handoff_row(
                1,
                &handoff_row,
                true,
            );

        assert_eq!(
            action_row.handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_row_key,
            "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-execution-handoff-action-01-release_handoff-substrate_actions"
        );
        assert!(action_row.is_release_handoff_action());
        assert!(action_row.release_handoff_evidence);
        assert!(action_row.release_ready_review);
        assert!(action_row.release_ready_disposition);
        assert!(action_row.release_ready_disposition_action);
        assert!(action_row
            .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoffs_ready);
        assert!(action_row.ready_for_release_handoff());
        assert!(!action_row.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_summary_surfaces_attention(
    ) {
        let repair_row = readiness_execution_slot_source_row(
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::ScheduleReleaseBlockerRepair,
            false,
        );
        let review_row = readiness_execution_slot_source_row(
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::CompleteEvidenceReview,
            false,
        );
        let slots = [IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionSlot::from_readiness_row(1, &repair_row, false),
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionSlot::from_readiness_row(2, &review_row, false)];
        let slot_summary =
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionSlotSummary::from_slots(
                2,
                false,
                slots.iter(),
            );
        let handoff_rows = slots
            .iter()
            .enumerate()
            .map(|(index, slot)| {
                IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionHandoffRow::from_readiness_execution_slot(
                    index + 1,
                    slot,
                    false,
                )
            })
            .collect::<Vec<_>>();
        let handoff_summary =
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionHandoffSummary::from_summaries(
                slot_summary,
                handoff_rows.iter(),
            );
        let action_rows = handoff_rows
            .iter()
            .enumerate()
            .map(|(index, row)| {
                IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionHandoffActionRow::from_handoff_row(
                    index + 1,
                    row,
                    false,
                )
            })
            .collect::<Vec<_>>();
        let summary =
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionHandoffActionSummary::from_summaries(
                handoff_summary,
                action_rows.iter(),
            );

        assert_eq!(
            summary.total_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_rows,
            2
        );
        assert_eq!(
            summary.total_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_rows,
            2
        );
        assert_eq!(
            summary.required_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_rows,
            2
        );
        assert_eq!(
            summary.repair_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_rows,
            2
        );
        assert_eq!(
            summary.review_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_rows,
            0
        );
        assert_eq!(
            summary.operator_required_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_rows,
            2
        );
        assert_eq!(
            summary.first_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_row_key,
            Some(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-execution-handoff-action-01-repair_handoff-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_required_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_row_key,
            summary
                .first_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_row_key
        );
        assert_eq!(
            summary.first_repair_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_row_key,
            summary
                .first_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_row_key
        );
        assert_eq!(
            summary.first_review_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_row_key,
            None
        );
        assert_eq!(
            summary.next_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_row_key,
            summary
                .first_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_row_key
        );
        assert_eq!(
            summary.next_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_row_key,
            summary
                .first_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_row_key
        );
        assert_eq!(
            summary.next_handoff_lane,
            Some(IntegrationMeshReleaseDispatchTicketHandoffLane::Repair)
        );
        assert!(!summary
            .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoffs_ready);
        assert!(!summary
            .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_actions_ready);
        assert!(summary.has_handoff_action_rows());
        assert!(summary.has_required_actions());
        assert!(summary.has_blockers());
        assert!(summary.needs_operator());
        assert!(summary.has_review_work());
        assert!(summary.requires_dispatch());
        assert!(summary.requires_execution());
        assert!(!summary.has_release_ready_handoff_actions());
        assert!(summary.requires_attention());
        assert!(!summary.ready_for_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_actions());
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_summary_marks_release_ready(
    ) {
        let row = readiness_execution_slot_source_row(
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReviewClearanceActionReadinessEvidenceReviewDispositionActionKind::ReleaseHandoff,
            true,
        );
        let slots = [IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionSlot::from_readiness_row(1, &row, true)];
        let slot_summary =
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionSlotSummary::from_slots(
                1,
                true,
                slots.iter(),
            );
        let handoff_rows = slots
            .iter()
            .enumerate()
            .map(|(index, slot)| {
                IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionHandoffRow::from_readiness_execution_slot(
                    index + 1,
                    slot,
                    true,
                )
            })
            .collect::<Vec<_>>();
        let handoff_summary =
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionHandoffSummary::from_summaries(
                slot_summary,
                handoff_rows.iter(),
            );
        let action_rows = handoff_rows
            .iter()
            .enumerate()
            .map(|(index, row)| {
                IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionHandoffActionRow::from_handoff_row(
                    index + 1,
                    row,
                    true,
                )
            })
            .collect::<Vec<_>>();
        let summary =
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionEvidenceReviewDispositionActionReadinessExecutionHandoffActionSummary::from_summaries(
                handoff_summary,
                action_rows.iter(),
            );

        assert_eq!(
            summary.total_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_rows,
            1
        );
        assert_eq!(
            summary.required_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_rows,
            0
        );
        assert_eq!(
            summary.release_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_rows,
            1
        );
        assert_eq!(
            summary.release_ready_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_rows,
            1
        );
        assert_eq!(
            summary.first_release_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_row_key,
            summary
                .first_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_row_key
        );
        assert_eq!(
            summary.first_release_ready_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_row_key,
            summary
                .first_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_row_key
        );
        assert_eq!(
            summary.next_handoff_action_evidence_review_disposition_action_readiness_execution_handoff_action_row_key,
            None
        );
        assert!(summary
            .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoffs_ready);
        assert!(summary
            .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_actions_ready);
        assert!(summary.has_handoff_action_rows());
        assert!(!summary.has_required_actions());
        assert!(!summary.has_blockers());
        assert!(!summary.needs_operator());
        assert!(!summary.has_review_work());
        assert!(!summary.requires_dispatch());
        assert!(!summary.requires_execution());
        assert!(summary.has_release_handoff_actions());
        assert!(summary.has_release_ready_handoff_actions());
        assert!(!summary.requires_attention());
        assert!(summary.ready_for_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_actions());
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_summary_surfaces_attention(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let summary =
            mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_summary(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );

        assert_eq!(
            summary
                .total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_rows,
            5
        );
        assert_eq!(
            summary
                .total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_rows,
            5
        );
        assert_eq!(
            summary
                .required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_rows,
            5
        );
        assert_eq!(
            summary
                .repair_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_rows,
            3
        );
        assert_eq!(
            summary
                .review_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_rows,
            2
        );
        assert_eq!(
            summary
                .operator_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_rows,
            0
        );
        assert_eq!(
            summary
                .lineage_repair_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_rows,
            0
        );
        assert_eq!(
            summary
                .release_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_rows,
            0
        );
        assert_eq!(
            summary
                .release_ready_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_rows,
            0
        );
        assert_eq!(
            summary.first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_row_key,
            Some(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-evidence-review-disposition-01-schedule_release_blocker_repair-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_row_key,
            summary
                .first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_row_key
        );
        assert_eq!(
            summary.first_repair_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_row_key,
            summary
                .first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_row_key
        );
        assert_eq!(
            summary.first_review_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_row_key,
            Some(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-evidence-review-disposition-02-complete_evidence_review-evidence_remediation"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_operator_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_row_key,
            None
        );
        assert_eq!(
            summary.next_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_row_key,
            summary
                .first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_row_key
        );
        assert_eq!(
            summary.next_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_row_key,
            summary
                .first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_row_key
        );
        assert_eq!(summary.next_work_order_key, summary.first_work_order_key);
        assert_eq!(summary.next_ticket_key, summary.first_ticket_key);
        assert_eq!(
            summary.next_handoff_lane,
            Some(IntegrationMeshReleaseDispatchTicketHandoffLane::Repair)
        );
        assert!(!summary
            .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_reviews_ready);
        assert!(!summary
            .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_dispositions_ready);
        assert!(!summary.ready_for_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_dispositions());
        assert!(summary.has_handoff_action_evidence_review_disposition_rows());
        assert!(summary.has_required_dispositions());
        assert!(summary.has_blockers());
        assert!(summary.has_review_work());
        assert!(summary.needs_operator());
        assert!(!summary.has_lineage_repairs());
        assert!(!summary.has_release_handoff_dispositions());
        assert!(!summary.has_release_ready_dispositions());
        assert!(summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_summary_marks_release_ready(
    ) {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let summary =
            mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_summary_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(
            summary
                .total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_rows,
            5
        );
        assert_eq!(
            summary
                .required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_rows,
            0
        );
        assert_eq!(
            summary
                .repair_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_rows,
            0
        );
        assert_eq!(
            summary
                .review_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_rows,
            0
        );
        assert_eq!(
            summary
                .lineage_repair_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_rows,
            0
        );
        assert_eq!(
            summary
                .release_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_rows,
            5
        );
        assert_eq!(
            summary
                .release_ready_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_rows,
            5
        );
        assert_eq!(
            summary.first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_row_key,
            Some(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-evidence-review-disposition-01-release_handoff-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_row_key,
            None
        );
        assert_eq!(
            summary.first_release_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_row_key,
            summary
                .first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_row_key
        );
        assert_eq!(
            summary.first_release_ready_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_row_key,
            summary
                .first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_row_key
        );
        assert_eq!(
            summary.next_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_disposition_row_key,
            None
        );
        assert_eq!(summary.next_work_order_key, None);
        assert_eq!(summary.next_ticket_key, None);
        assert!(summary
            .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_reviews_ready);
        assert!(summary
            .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_dispositions_ready);
        assert!(summary.ready_for_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_dispositions());
        assert!(summary.has_handoff_action_evidence_review_disposition_rows());
        assert!(!summary.has_required_dispositions());
        assert!(!summary.has_blockers());
        assert!(!summary.needs_operator());
        assert!(!summary.has_review_work());
        assert!(!summary.has_lineage_repairs());
        assert!(summary.has_release_handoff_dispositions());
        assert!(summary.has_release_ready_dispositions());
        assert!(!summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_summary_surfaces_attention(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let summary =
            mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_summary(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );

        assert_eq!(
            summary
                .total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows,
            5
        );
        assert_eq!(
            summary
                .total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_rows,
            5
        );
        assert_eq!(
            summary
                .required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_rows,
            5
        );
        assert_eq!(
            summary
                .blocker_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_rows,
            3
        );
        assert_eq!(
            summary
                .operator_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_rows,
            0
        );
        assert_eq!(
            summary
                .review_gate_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_rows,
            2
        );
        assert_eq!(
            summary
                .lineage_gap_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_rows,
            0
        );
        assert_eq!(
            summary
                .release_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_rows,
            0
        );
        assert_eq!(
            summary
                .release_ready_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_rows,
            0
        );
        assert_eq!(
            summary.first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_row_key,
            Some(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-evidence-review-01-blocker_review-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_row_key,
            summary
                .first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_row_key
        );
        assert_eq!(
            summary.first_blocker_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_row_key,
            summary
                .first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_row_key
        );
        assert_eq!(
            summary.first_review_gate_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_row_key,
            Some(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-evidence-review-02-review_gate-evidence_remediation"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_operator_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_row_key,
            None
        );
        assert_eq!(
            summary.next_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_row_key,
            summary
                .first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_row_key
        );
        assert_eq!(
            summary.next_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_row_key,
            summary
                .first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_row_key
        );
        assert_eq!(summary.next_work_order_key, summary.first_work_order_key);
        assert_eq!(summary.next_ticket_key, summary.first_ticket_key);
        assert_eq!(
            summary.next_handoff_lane,
            Some(IntegrationMeshReleaseDispatchTicketHandoffLane::Repair)
        );
        assert!(!summary
            .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_ready);
        assert!(!summary
            .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_reviews_ready);
        assert!(!summary
            .ready_for_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_reviews());
        assert!(summary.has_handoff_action_evidence_review_rows());
        assert!(summary.has_required_reviews());
        assert!(summary.has_blockers());
        assert!(summary.has_review_work());
        assert!(summary.needs_operator());
        assert!(!summary.has_lineage_gaps());
        assert!(!summary.has_release_handoff_reviews());
        assert!(!summary.has_release_ready_reviews());
        assert!(summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_summary_marks_release_ready(
    ) {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let summary =
            mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_summary_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(
            summary
                .total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_rows,
            5
        );
        assert_eq!(
            summary
                .required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_rows,
            0
        );
        assert_eq!(
            summary
                .blocker_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_rows,
            0
        );
        assert_eq!(
            summary
                .operator_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_rows,
            0
        );
        assert_eq!(
            summary
                .review_gate_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_rows,
            0
        );
        assert_eq!(
            summary
                .release_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_rows,
            5
        );
        assert_eq!(
            summary
                .release_ready_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_rows,
            5
        );
        assert_eq!(
            summary.first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_row_key,
            Some(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-evidence-review-01-release_ready-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_row_key,
            None
        );
        assert_eq!(
            summary.first_release_ready_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_row_key,
            summary
                .first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_row_key
        );
        assert_eq!(
            summary.next_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_review_row_key,
            None
        );
        assert_eq!(summary.next_work_order_key, None);
        assert_eq!(summary.next_ticket_key, None);
        assert!(summary
            .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_ready);
        assert!(summary
            .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_reviews_ready);
        assert!(summary
            .ready_for_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_reviews());
        assert!(summary.has_handoff_action_evidence_review_rows());
        assert!(!summary.has_required_reviews());
        assert!(!summary.has_blockers());
        assert!(!summary.needs_operator());
        assert!(!summary.has_review_work());
        assert!(!summary.has_lineage_gaps());
        assert!(summary.has_release_handoff_reviews());
        assert!(summary.has_release_ready_reviews());
        assert!(!summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_summary_surfaces_attention(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let summary =
            mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_summary(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );

        assert_eq!(
            summary
                .total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows,
            5
        );
        assert_eq!(
            summary
                .total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows,
            5
        );
        assert_eq!(
            summary
                .required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows,
            5
        );
        assert_eq!(
            summary
                .release_blocking_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows,
            3
        );
        assert_eq!(
            summary
                .repair_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows,
            3
        );
        assert_eq!(
            summary
                .review_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows,
            2
        );
        assert_eq!(
            summary
                .execution_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows,
            5
        );
        assert_eq!(
            summary
                .release_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows,
            0
        );
        assert_eq!(
            summary.first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_row_key,
            Some(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-evidence-01-repair_handoff-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_row_key,
            summary
                .first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_row_key
        );
        assert_eq!(
            summary.first_repair_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_row_key,
            summary
                .first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_row_key
        );
        assert_eq!(
            summary.first_review_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_row_key,
            Some(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-evidence-02-review_handoff-evidence_remediation"
                    .to_string()
            )
        );
        assert_eq!(
            summary.next_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_row_key,
            summary
                .first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_row_key
        );
        assert_eq!(
            summary.next_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_row_key,
            summary
                .first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_row_key
        );
        assert_eq!(
            summary.next_readiness_evidence_review_disposition_action_readiness_execution_handoff_row_key,
            summary
                .first_readiness_evidence_review_disposition_action_readiness_execution_handoff_row_key
        );
        assert_eq!(summary.next_work_order_key, summary.first_work_order_key);
        assert_eq!(summary.next_ticket_key, summary.first_ticket_key);
        assert_eq!(
            summary.next_handoff_lane,
            Some(IntegrationMeshReleaseDispatchTicketHandoffLane::Repair)
        );
        assert!(!summary
            .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_actions_ready);
        assert!(!summary
            .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_ready);
        assert!(!summary
            .ready_for_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence());
        assert!(summary.has_handoff_action_evidence_rows());
        assert!(summary.has_required_evidence());
        assert!(summary.has_blockers());
        assert!(summary.has_review_work());
        assert!(summary.needs_operator());
        assert!(summary.requires_dispatch());
        assert!(summary.requires_execution());
        assert!(!summary.has_lineage_gaps());
        assert!(!summary.has_release_handoff_evidence());
        assert!(summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_summary_marks_release_ready(
    ) {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let summary =
            mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_summary_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(
            summary
                .total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows,
            5
        );
        assert_eq!(
            summary
                .required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows,
            0
        );
        assert_eq!(
            summary
                .release_blocking_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows,
            0
        );
        assert_eq!(
            summary
                .repair_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows,
            0
        );
        assert_eq!(
            summary
                .review_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows,
            0
        );
        assert_eq!(
            summary
                .release_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows,
            5
        );
        assert_eq!(
            summary
                .release_ready_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_rows,
            5
        );
        assert_eq!(
            summary.first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_row_key,
            Some(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-evidence-01-release_handoff-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_row_key,
            None
        );
        assert_eq!(
            summary.first_repair_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_row_key,
            None
        );
        assert_eq!(
            summary.first_release_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_row_key,
            summary
                .first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_row_key
        );
        assert_eq!(
            summary.next_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_row_key,
            None
        );
        assert_eq!(summary.next_work_order_key, None);
        assert_eq!(summary.next_ticket_key, None);
        assert!(summary
            .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_actions_ready);
        assert!(summary
            .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence_ready);
        assert!(summary
            .ready_for_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_evidence());
        assert!(summary.has_handoff_action_evidence_rows());
        assert!(!summary.has_required_evidence());
        assert!(!summary.has_blockers());
        assert!(!summary.has_review_work());
        assert!(!summary.needs_operator());
        assert!(!summary.requires_dispatch());
        assert!(!summary.requires_execution());
        assert!(!summary.has_lineage_gaps());
        assert!(summary.has_release_handoff_evidence());
        assert!(!summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_packets_surface_attention(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let rows =
            mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_packet_rows(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );
        let summary =
            mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_packet_summary(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );

        assert_eq!(rows.len(), 3);
        assert_eq!(rows[0].protocol, ProtocolFamily::Zigbee);
        assert_eq!(rows[0].total_substrate_rows, 5);
        assert_eq!(rows[0].blocked_substrate_rows, 1);
        assert_eq!(
            rows[0].first_blocked_substrate_stage,
            Some(IntegrationMeshProtocolSubstrateStage::NetworkSecurity)
        );
        assert_eq!(rows[1].protocol, ProtocolFamily::ZWave);
        assert_eq!(rows[1].blocked_substrate_rows, 2);
        assert_eq!(
            rows[1].first_blocked_substrate_stage,
            Some(IntegrationMeshProtocolSubstrateStage::Radio)
        );
        assert_eq!(rows[2].protocol, ProtocolFamily::Thread);
        assert_eq!(rows[2].total_substrate_rows, 6);
        assert_eq!(rows[2].blocked_substrate_rows, 2);
        assert_eq!(
            rows[2].first_blocked_substrate_stage,
            Some(IntegrationMeshProtocolSubstrateStage::Discovery)
        );
        assert_eq!(rows[0].total_handoff_action_evidence_rows, 5);
        assert_eq!(rows[0].required_handoff_action_evidence_rows, 5);
        assert_eq!(rows[0].release_blocking_handoff_action_evidence_rows, 3);
        assert_eq!(rows[0].operator_required_handoff_action_evidence_rows, 4);
        assert_eq!(rows[0].review_required_handoff_action_evidence_rows, 2);
        assert_eq!(rows[0].dispatch_required_handoff_action_evidence_rows, 5);
        assert_eq!(rows[0].execution_required_handoff_action_evidence_rows, 5);
        assert_eq!(rows[0].release_handoff_evidence_rows, 0);
        assert_eq!(
            rows[0].first_handoff_action_evidence_row_key,
            Some(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-evidence-01-repair_handoff-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            rows[0].first_required_handoff_action_evidence_row_key,
            rows[0].first_handoff_action_evidence_row_key
        );
        assert!(!rows[0].handoff_action_evidence_ready);
        assert!(!rows[0].protocol_substrate_ready);
        assert!(!rows[0].ready_for_protocol_evidence_packet());
        assert!(rows[0].requires_attention());

        assert_eq!(summary.total_protocol_evidence_packets, 3);
        assert_eq!(summary.ready_protocol_evidence_packets, 0);
        assert_eq!(summary.blocked_protocol_evidence_packets, 3);
        assert_eq!(summary.protocol_substrate_blockers, 5);
        assert_eq!(summary.required_handoff_action_evidence_rows, 5);
        assert_eq!(summary.release_blocking_handoff_action_evidence_rows, 3);
        assert_eq!(summary.operator_required_handoff_action_evidence_rows, 4);
        assert_eq!(summary.review_required_handoff_action_evidence_rows, 2);
        assert_eq!(summary.dispatch_required_handoff_action_evidence_rows, 5);
        assert_eq!(summary.execution_required_handoff_action_evidence_rows, 5);
        assert_eq!(summary.release_handoff_evidence_rows, 0);
        assert_eq!(summary.first_protocol, Some(ProtocolFamily::Zigbee));
        assert_eq!(summary.first_blocked_protocol, Some(ProtocolFamily::Zigbee));
        assert_eq!(
            summary.first_blocked_substrate_stage,
            Some(IntegrationMeshProtocolSubstrateStage::NetworkSecurity)
        );
        assert_eq!(
            summary.first_required_handoff_action_evidence_row_key,
            rows[0].first_handoff_action_evidence_row_key
        );
        assert!(!summary.handoff_action_evidence_ready);
        assert!(!summary.protocol_evidence_packets_ready);
        assert!(!summary.ready_for_release_ticket_handoff_protocol_evidence_packets());
        assert!(summary.has_blockers());
        assert!(summary.has_required_evidence());
        assert!(summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_packets_mark_release_ready(
    ) {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let rows =
            mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_packet_rows_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );
        let summary =
            mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_packet_summary_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(rows.len(), 3);
        assert_eq!(rows[0].protocol, ProtocolFamily::Zigbee);
        assert_eq!(rows[0].blocked_substrate_rows, 0);
        assert_eq!(rows[0].total_handoff_action_evidence_rows, 5);
        assert_eq!(rows[0].required_handoff_action_evidence_rows, 0);
        assert_eq!(rows[0].release_handoff_evidence_rows, 5);
        assert_eq!(rows[0].release_ready_handoff_action_evidence_rows, 5);
        assert_eq!(rows[0].first_required_handoff_action_evidence_row_key, None);
        assert!(rows[0].handoff_action_evidence_ready);
        assert!(rows[0].protocol_substrate_ready);
        assert!(rows[0].ready_for_protocol_evidence_packet());
        assert!(!rows[0].requires_attention());

        assert_eq!(summary.total_protocol_evidence_packets, 3);
        assert_eq!(summary.ready_protocol_evidence_packets, 3);
        assert_eq!(summary.blocked_protocol_evidence_packets, 0);
        assert_eq!(summary.protocol_substrate_blockers, 0);
        assert_eq!(summary.required_handoff_action_evidence_rows, 0);
        assert_eq!(summary.release_blocking_handoff_action_evidence_rows, 0);
        assert_eq!(summary.release_handoff_evidence_rows, 5);
        assert_eq!(summary.release_ready_handoff_action_evidence_rows, 5);
        assert_eq!(summary.first_blocked_protocol, None);
        assert_eq!(summary.first_blocked_substrate_stage, None);
        assert_eq!(summary.first_required_handoff_action_evidence_row_key, None);
        assert!(summary.handoff_action_evidence_ready);
        assert!(summary.protocol_evidence_packets_ready);
        assert!(summary.ready_for_release_ticket_handoff_protocol_evidence_packets());
        assert!(!summary.has_blockers());
        assert!(!summary.has_required_evidence());
        assert!(!summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_readiness_summary_surfaces_attention(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let summary =
            mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_readiness_summary(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );

        assert_eq!(
            summary.protocol_evidence_package_key,
            "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-protocol-evidence-package"
        );
        assert_eq!(summary.total_protocol_evidence_packets, 3);
        assert_eq!(summary.ready_protocol_evidence_packets, 0);
        assert_eq!(summary.blocked_protocol_evidence_packets, 3);
        assert_eq!(summary.protocol_substrate_blockers, 5);
        assert_eq!(summary.required_handoff_action_evidence_rows, 5);
        assert_eq!(summary.release_blocking_handoff_action_evidence_rows, 3);
        assert_eq!(summary.operator_required_handoff_action_evidence_rows, 4);
        assert_eq!(summary.review_required_handoff_action_evidence_rows, 2);
        assert_eq!(summary.dispatch_required_handoff_action_evidence_rows, 5);
        assert_eq!(summary.execution_required_handoff_action_evidence_rows, 5);
        assert_eq!(summary.release_handoff_evidence_rows, 0);
        assert_eq!(summary.release_ready_handoff_action_evidence_rows, 0);
        assert_eq!(
            summary.first_protocol_evidence_packet_key,
            Some(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-protocol-evidence-packet-01-Zigbee"
                    .to_string()
            )
        );
        assert_eq!(
            summary.next_protocol_evidence_packet_key,
            summary.first_protocol_evidence_packet_key
        );
        assert_eq!(
            summary.next_handoff_action_evidence_row_key,
            Some(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-evidence-01-repair_handoff-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(summary.next_protocol, Some(ProtocolFamily::Zigbee));
        assert_eq!(
            summary.next_blocked_substrate_stage,
            Some(IntegrationMeshProtocolSubstrateStage::NetworkSecurity)
        );
        assert!(!summary.handoff_action_evidence_ready);
        assert!(!summary.protocol_evidence_packets_ready);
        assert!(!summary.ready_for_release_ticket_handoff_protocol_evidence_package());
        assert!(summary.has_blockers());
        assert!(summary.has_required_evidence());
        assert!(summary.needs_operator());
        assert!(summary.has_review_work());
        assert!(summary.requires_dispatch());
        assert!(summary.requires_execution());
        assert!(summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_readiness_summary_marks_release_ready(
    ) {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let summary =
            mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_readiness_summary_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(summary.total_protocol_evidence_packets, 3);
        assert_eq!(summary.ready_protocol_evidence_packets, 3);
        assert_eq!(summary.blocked_protocol_evidence_packets, 0);
        assert_eq!(summary.protocol_substrate_blockers, 0);
        assert_eq!(summary.required_handoff_action_evidence_rows, 0);
        assert_eq!(summary.release_blocking_handoff_action_evidence_rows, 0);
        assert_eq!(summary.operator_required_handoff_action_evidence_rows, 0);
        assert_eq!(summary.review_required_handoff_action_evidence_rows, 0);
        assert_eq!(summary.dispatch_required_handoff_action_evidence_rows, 0);
        assert_eq!(summary.execution_required_handoff_action_evidence_rows, 0);
        assert_eq!(summary.release_handoff_evidence_rows, 5);
        assert_eq!(summary.release_ready_handoff_action_evidence_rows, 5);
        assert_eq!(
            summary.first_protocol_evidence_packet_key,
            Some(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-protocol-evidence-packet-01-Zigbee"
                    .to_string()
            )
        );
        assert_eq!(summary.next_protocol_evidence_packet_key, None);
        assert_eq!(summary.next_handoff_action_evidence_row_key, None);
        assert_eq!(summary.next_protocol, None);
        assert_eq!(summary.next_blocked_substrate_stage, None);
        assert!(summary.handoff_action_evidence_ready);
        assert!(summary.protocol_evidence_packets_ready);
        assert!(summary.release_protocol_evidence_package_ready);
        assert!(summary.ready_for_release_ticket_handoff_protocol_evidence_package());
        assert!(!summary.has_blockers());
        assert!(!summary.has_required_evidence());
        assert!(!summary.needs_operator());
        assert!(!summary.has_review_work());
        assert!(!summary.requires_dispatch());
        assert!(!summary.requires_execution());
        assert!(!summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_rows_surface_attention(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let rows =
            mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_rows(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );

        assert_eq!(rows.len(), 3);
        assert_eq!(rows[0].protocol, ProtocolFamily::Zigbee);
        assert_eq!(
            rows[0].protocol_evidence_package_handoff_row_key,
            "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-protocol-evidence-package-handoff-row-01-Zigbee"
        );
        assert_eq!(
            rows[0].protocol_evidence_package_key,
            "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-protocol-evidence-package"
        );
        assert_eq!(
            rows[0].protocol_evidence_packet_key,
            "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-protocol-evidence-packet-01-Zigbee"
        );
        assert_eq!(rows[0].total_substrate_rows, 5);
        assert_eq!(rows[0].ready_substrate_rows, 4);
        assert_eq!(rows[0].blocked_substrate_rows, 1);
        assert_eq!(rows[0].required_handoff_action_evidence_rows, 5);
        assert_eq!(rows[0].release_blocking_handoff_action_evidence_rows, 3);
        assert_eq!(rows[0].operator_required_handoff_action_evidence_rows, 4);
        assert_eq!(rows[0].review_required_handoff_action_evidence_rows, 2);
        assert_eq!(rows[0].dispatch_required_handoff_action_evidence_rows, 5);
        assert_eq!(rows[0].execution_required_handoff_action_evidence_rows, 5);
        assert_eq!(rows[0].release_handoff_evidence_rows, 0);
        assert_eq!(rows[0].release_ready_handoff_action_evidence_rows, 0);
        assert_eq!(
            rows[0].first_required_handoff_action_evidence_row_key,
            Some(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-evidence-01-repair_handoff-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            rows[0].first_blocked_substrate_stage,
            Some(IntegrationMeshProtocolSubstrateStage::NetworkSecurity)
        );
        assert!(!rows[0].handoff_action_evidence_ready);
        assert!(!rows[0].protocol_substrate_ready);
        assert!(!rows[0].protocol_evidence_packet_ready);
        assert!(!rows[0].protocol_evidence_package_ready);
        assert!(!rows[0].package_handoff_ready);
        assert!(!rows[0].ready_for_package_handoff());
        assert!(rows[0].has_blockers());
        assert!(rows[0].has_required_evidence());
        assert!(rows[0].needs_operator());
        assert!(rows[0].has_review_work());
        assert!(rows[0].requires_dispatch());
        assert!(rows[0].requires_execution());
        assert!(rows[0].requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_rows_mark_release_ready(
    ) {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let rows =
            mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_rows_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(rows.len(), 3);
        assert_eq!(rows[0].protocol, ProtocolFamily::Zigbee);
        assert_eq!(rows[0].blocked_substrate_rows, 0);
        assert_eq!(rows[0].required_handoff_action_evidence_rows, 0);
        assert_eq!(rows[0].release_blocking_handoff_action_evidence_rows, 0);
        assert_eq!(rows[0].operator_required_handoff_action_evidence_rows, 0);
        assert_eq!(rows[0].review_required_handoff_action_evidence_rows, 0);
        assert_eq!(rows[0].dispatch_required_handoff_action_evidence_rows, 0);
        assert_eq!(rows[0].execution_required_handoff_action_evidence_rows, 0);
        assert_eq!(rows[0].release_handoff_evidence_rows, 5);
        assert_eq!(rows[0].release_ready_handoff_action_evidence_rows, 5);
        assert_eq!(rows[0].first_required_handoff_action_evidence_row_key, None);
        assert_eq!(rows[0].first_blocked_substrate_stage, None);
        assert!(rows[0].handoff_action_evidence_ready);
        assert!(rows[0].protocol_substrate_ready);
        assert!(rows[0].protocol_evidence_packet_ready);
        assert!(rows[0].protocol_evidence_package_ready);
        assert!(rows[0].package_handoff_ready);
        assert!(rows[0].ready_for_package_handoff());
        assert!(!rows[0].has_blockers());
        assert!(!rows[0].has_required_evidence());
        assert!(!rows[0].needs_operator());
        assert!(!rows[0].has_review_work());
        assert!(!rows[0].requires_dispatch());
        assert!(!rows[0].requires_execution());
        assert!(!rows[0].requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_summary_surfaces_attention(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let summary =
            mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_summary(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );

        assert_eq!(summary.total_protocol_evidence_package_handoff_rows, 3);
        assert_eq!(summary.ready_protocol_evidence_package_handoff_rows, 0);
        assert_eq!(summary.blocked_protocol_evidence_package_handoff_rows, 3);
        assert_eq!(summary.protocol_substrate_blockers, 5);
        assert_eq!(summary.required_handoff_action_evidence_rows, 5);
        assert_eq!(summary.release_blocking_handoff_action_evidence_rows, 3);
        assert_eq!(summary.operator_required_handoff_action_evidence_rows, 4);
        assert_eq!(summary.review_required_handoff_action_evidence_rows, 2);
        assert_eq!(summary.dispatch_required_handoff_action_evidence_rows, 5);
        assert_eq!(summary.execution_required_handoff_action_evidence_rows, 5);
        assert_eq!(summary.release_handoff_evidence_rows, 0);
        assert_eq!(summary.release_ready_handoff_action_evidence_rows, 0);
        assert_eq!(
            summary.first_protocol_evidence_package_handoff_row_key,
            Some(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-protocol-evidence-package-handoff-row-01-Zigbee"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_required_protocol_evidence_package_handoff_row_key,
            summary.first_protocol_evidence_package_handoff_row_key
        );
        assert_eq!(
            summary.first_ready_protocol_evidence_package_handoff_row_key,
            None
        );
        assert_eq!(
            summary.first_protocol_evidence_packet_key,
            Some(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-protocol-evidence-packet-01-Zigbee"
                    .to_string()
            )
        );
        assert_eq!(
            summary.next_protocol_evidence_package_handoff_row_key,
            summary.first_protocol_evidence_package_handoff_row_key
        );
        assert_eq!(
            summary.next_protocol_evidence_packet_key,
            summary.first_protocol_evidence_packet_key
        );
        assert_eq!(
            summary.next_handoff_action_evidence_row_key,
            Some(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-evidence-01-repair_handoff-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(summary.next_protocol, Some(ProtocolFamily::Zigbee));
        assert_eq!(
            summary.next_blocked_substrate_stage,
            Some(IntegrationMeshProtocolSubstrateStage::NetworkSecurity)
        );
        assert!(!summary.handoff_action_evidence_ready);
        assert!(!summary.protocol_evidence_package_ready);
        assert!(!summary.protocol_evidence_package_handoff_rows_ready);
        assert!(!summary.ready_for_release_ticket_handoff_protocol_evidence_package_handoff());
        assert!(summary.has_blockers());
        assert!(summary.has_required_evidence());
        assert!(summary.needs_operator());
        assert!(summary.has_review_work());
        assert!(summary.requires_dispatch());
        assert!(summary.requires_execution());
        assert!(summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_summary_marks_release_ready(
    ) {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let summary =
            mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_summary_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(summary.total_protocol_evidence_package_handoff_rows, 3);
        assert_eq!(summary.ready_protocol_evidence_package_handoff_rows, 3);
        assert_eq!(summary.blocked_protocol_evidence_package_handoff_rows, 0);
        assert_eq!(summary.protocol_substrate_blockers, 0);
        assert_eq!(summary.required_handoff_action_evidence_rows, 0);
        assert_eq!(summary.release_blocking_handoff_action_evidence_rows, 0);
        assert_eq!(summary.operator_required_handoff_action_evidence_rows, 0);
        assert_eq!(summary.review_required_handoff_action_evidence_rows, 0);
        assert_eq!(summary.dispatch_required_handoff_action_evidence_rows, 0);
        assert_eq!(summary.execution_required_handoff_action_evidence_rows, 0);
        assert_eq!(summary.release_handoff_evidence_rows, 5);
        assert_eq!(summary.release_ready_handoff_action_evidence_rows, 5);
        assert_eq!(
            summary.first_protocol_evidence_package_handoff_row_key,
            Some(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-protocol-evidence-package-handoff-row-01-Zigbee"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_required_protocol_evidence_package_handoff_row_key,
            None
        );
        assert_eq!(
            summary.first_ready_protocol_evidence_package_handoff_row_key,
            summary.first_protocol_evidence_package_handoff_row_key
        );
        assert_eq!(summary.next_protocol_evidence_package_handoff_row_key, None);
        assert_eq!(summary.next_protocol_evidence_packet_key, None);
        assert_eq!(summary.next_handoff_action_evidence_row_key, None);
        assert_eq!(summary.next_protocol, None);
        assert_eq!(summary.next_blocked_substrate_stage, None);
        assert!(summary.handoff_action_evidence_ready);
        assert!(summary.protocol_evidence_package_ready);
        assert!(summary.protocol_evidence_package_handoff_rows_ready);
        assert!(summary.ready_for_release_ticket_handoff_protocol_evidence_package_handoff());
        assert!(!summary.has_blockers());
        assert!(!summary.has_required_evidence());
        assert!(!summary.needs_operator());
        assert!(!summary.has_review_work());
        assert!(!summary.requires_dispatch());
        assert!(!summary.requires_execution());
        assert!(!summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_slots_surface_attention(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let slots =
            mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_slots(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );

        assert_eq!(slots.len(), 3);
        assert_eq!(slots[0].protocol, ProtocolFamily::Zigbee);
        assert_eq!(
            slots[0].protocol_evidence_package_handoff_execution_slot_key,
            "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-protocol-evidence-package-handoff-execution-slot-01-Zigbee"
        );
        assert_eq!(slots[0].protocol_evidence_package_handoff_row_sequence, 1);
        assert_eq!(
            slots[0].protocol_evidence_package_handoff_row_key,
            "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-protocol-evidence-package-handoff-row-01-Zigbee"
        );
        assert_eq!(
            slots[0].protocol_evidence_packet_key,
            "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-protocol-evidence-packet-01-Zigbee"
        );
        assert_eq!(
            slots[0].handoff_lane,
            IntegrationMeshReleaseDispatchTicketHandoffLane::Review
        );
        assert_eq!(slots[0].total_substrate_rows, 5);
        assert_eq!(slots[0].ready_substrate_rows, 4);
        assert_eq!(slots[0].blocked_substrate_rows, 1);
        assert_eq!(slots[0].required_handoff_action_evidence_rows, 5);
        assert_eq!(slots[0].release_blocking_handoff_action_evidence_rows, 3);
        assert_eq!(slots[0].operator_required_handoff_action_evidence_rows, 4);
        assert_eq!(slots[0].review_required_handoff_action_evidence_rows, 2);
        assert_eq!(slots[0].dispatch_required_handoff_action_evidence_rows, 5);
        assert_eq!(slots[0].execution_required_handoff_action_evidence_rows, 5);
        assert_eq!(
            slots[0].first_required_handoff_action_evidence_row_key,
            Some(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-evidence-01-repair_handoff-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            slots[0].first_blocked_substrate_stage,
            Some(IntegrationMeshProtocolSubstrateStage::NetworkSecurity)
        );
        assert!(!slots[0].handoff_action_evidence_ready);
        assert!(!slots[0].protocol_substrate_ready);
        assert!(!slots[0].protocol_evidence_packet_ready);
        assert!(!slots[0].protocol_evidence_package_ready);
        assert!(!slots[0].package_handoff_ready);
        assert!(!slots[0].protocol_evidence_package_handoff_ready);
        assert!(!slots[0].ready_for_package_handoff_execution());
        assert!(!slots[0].is_release_slot());
        assert!(slots[0].is_review_slot());
        assert!(slots[0].has_blockers());
        assert!(slots[0].has_required_evidence());
        assert!(slots[0].needs_operator());
        assert!(slots[0].has_review_work());
        assert!(slots[0].requires_dispatch());
        assert!(slots[0].requires_execution());
        assert!(slots[0].requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_slots_mark_release_ready(
    ) {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let slots =
            mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_slots_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(slots.len(), 3);
        assert_eq!(slots[0].protocol, ProtocolFamily::Zigbee);
        assert_eq!(
            slots[0].handoff_lane,
            IntegrationMeshReleaseDispatchTicketHandoffLane::Release
        );
        assert_eq!(slots[0].blocked_substrate_rows, 0);
        assert_eq!(slots[0].required_handoff_action_evidence_rows, 0);
        assert_eq!(slots[0].release_blocking_handoff_action_evidence_rows, 0);
        assert_eq!(slots[0].operator_required_handoff_action_evidence_rows, 0);
        assert_eq!(slots[0].review_required_handoff_action_evidence_rows, 0);
        assert_eq!(slots[0].dispatch_required_handoff_action_evidence_rows, 0);
        assert_eq!(slots[0].execution_required_handoff_action_evidence_rows, 0);
        assert_eq!(slots[0].release_handoff_evidence_rows, 5);
        assert_eq!(slots[0].release_ready_handoff_action_evidence_rows, 5);
        assert_eq!(
            slots[0].first_required_handoff_action_evidence_row_key,
            None
        );
        assert_eq!(slots[0].first_blocked_substrate_stage, None);
        assert!(slots[0].handoff_action_evidence_ready);
        assert!(slots[0].protocol_substrate_ready);
        assert!(slots[0].protocol_evidence_packet_ready);
        assert!(slots[0].protocol_evidence_package_ready);
        assert!(slots[0].package_handoff_ready);
        assert!(slots[0].protocol_evidence_package_handoff_ready);
        assert!(slots[0].ready_for_package_handoff_execution());
        assert!(slots[0].is_release_slot());
        assert!(!slots[0].is_repair_slot());
        assert!(!slots[0].has_blockers());
        assert!(!slots[0].has_required_evidence());
        assert!(!slots[0].needs_operator());
        assert!(!slots[0].has_review_work());
        assert!(!slots[0].requires_dispatch());
        assert!(!slots[0].requires_execution());
        assert!(!slots[0].requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_slot_summary_surfaces_attention(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let summary =
            mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_slot_summary(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );

        assert_eq!(summary.total_protocol_evidence_package_handoff_rows, 3);
        assert_eq!(
            summary.total_protocol_evidence_package_handoff_execution_slots,
            3
        );
        assert_eq!(
            summary.ready_protocol_evidence_package_handoff_execution_slots,
            0
        );
        assert_eq!(
            summary.blocked_protocol_evidence_package_handoff_execution_slots,
            3
        );
        assert_eq!(
            summary.repair_protocol_evidence_package_handoff_execution_slots,
            0
        );
        assert_eq!(
            summary.operator_protocol_evidence_package_handoff_execution_slots,
            0
        );
        assert_eq!(
            summary.review_protocol_evidence_package_handoff_execution_slots,
            3
        );
        assert_eq!(
            summary.release_protocol_evidence_package_handoff_execution_slots,
            0
        );
        assert_eq!(summary.protocol_substrate_blockers, 5);
        assert_eq!(summary.required_handoff_action_evidence_rows, 5);
        assert_eq!(summary.release_blocking_handoff_action_evidence_rows, 3);
        assert_eq!(summary.operator_required_handoff_action_evidence_rows, 4);
        assert_eq!(summary.review_required_handoff_action_evidence_rows, 2);
        assert_eq!(summary.dispatch_required_handoff_action_evidence_rows, 5);
        assert_eq!(summary.execution_required_handoff_action_evidence_rows, 5);
        assert_eq!(
            summary.first_protocol_evidence_package_handoff_execution_slot_key,
            Some(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-protocol-evidence-package-handoff-execution-slot-01-Zigbee"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_required_protocol_evidence_package_handoff_execution_slot_key,
            summary.first_protocol_evidence_package_handoff_execution_slot_key
        );
        assert_eq!(
            summary.first_ready_protocol_evidence_package_handoff_execution_slot_key,
            None
        );
        assert_eq!(
            summary.first_release_protocol_evidence_package_handoff_execution_slot_key,
            None
        );
        assert_eq!(
            summary.next_protocol_evidence_package_handoff_execution_slot_key,
            summary.first_protocol_evidence_package_handoff_execution_slot_key
        );
        assert_eq!(
            summary.next_handoff_action_evidence_row_key,
            Some(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-evidence-01-repair_handoff-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(summary.next_protocol, Some(ProtocolFamily::Zigbee));
        assert_eq!(
            summary.next_blocked_substrate_stage,
            Some(IntegrationMeshProtocolSubstrateStage::NetworkSecurity)
        );
        assert_eq!(
            summary.next_handoff_lane,
            Some(IntegrationMeshReleaseDispatchTicketHandoffLane::Review)
        );
        assert!(!summary.protocol_evidence_package_handoff_ready);
        assert!(!summary.protocol_evidence_package_handoff_execution_slots_ready);
        assert!(
            !summary.ready_for_release_ticket_handoff_protocol_evidence_package_handoff_execution()
        );
        assert!(summary.has_blockers());
        assert!(summary.has_required_evidence());
        assert!(summary.needs_operator());
        assert!(summary.has_review_work());
        assert!(summary.requires_dispatch());
        assert!(summary.requires_execution());
        assert!(summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_slot_summary_marks_release_ready(
    ) {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let summary =
            mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_slot_summary_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(summary.total_protocol_evidence_package_handoff_rows, 3);
        assert_eq!(
            summary.total_protocol_evidence_package_handoff_execution_slots,
            3
        );
        assert_eq!(
            summary.ready_protocol_evidence_package_handoff_execution_slots,
            3
        );
        assert_eq!(
            summary.blocked_protocol_evidence_package_handoff_execution_slots,
            0
        );
        assert_eq!(
            summary.release_protocol_evidence_package_handoff_execution_slots,
            3
        );
        assert_eq!(summary.protocol_substrate_blockers, 0);
        assert_eq!(summary.required_handoff_action_evidence_rows, 0);
        assert_eq!(summary.release_blocking_handoff_action_evidence_rows, 0);
        assert_eq!(summary.operator_required_handoff_action_evidence_rows, 0);
        assert_eq!(summary.review_required_handoff_action_evidence_rows, 0);
        assert_eq!(summary.dispatch_required_handoff_action_evidence_rows, 0);
        assert_eq!(summary.execution_required_handoff_action_evidence_rows, 0);
        assert_eq!(summary.release_handoff_evidence_rows, 5);
        assert_eq!(summary.release_ready_handoff_action_evidence_rows, 5);
        assert_eq!(
            summary.first_ready_protocol_evidence_package_handoff_execution_slot_key,
            summary.first_protocol_evidence_package_handoff_execution_slot_key
        );
        assert_eq!(
            summary.first_release_protocol_evidence_package_handoff_execution_slot_key,
            summary.first_protocol_evidence_package_handoff_execution_slot_key
        );
        assert_eq!(
            summary.next_protocol_evidence_package_handoff_execution_slot_key,
            None
        );
        assert_eq!(summary.next_protocol_evidence_packet_key, None);
        assert_eq!(summary.next_handoff_action_evidence_row_key, None);
        assert_eq!(summary.next_protocol, None);
        assert_eq!(summary.next_blocked_substrate_stage, None);
        assert_eq!(summary.next_handoff_lane, None);
        assert!(summary.protocol_evidence_package_handoff_ready);
        assert!(summary.protocol_evidence_package_handoff_execution_slots_ready);
        assert!(
            summary.ready_for_release_ticket_handoff_protocol_evidence_package_handoff_execution()
        );
        assert!(!summary.has_blockers());
        assert!(!summary.has_required_evidence());
        assert!(!summary.needs_operator());
        assert!(!summary.has_review_work());
        assert!(!summary.requires_dispatch());
        assert!(!summary.requires_execution());
        assert!(!summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_action_rows_surface_attention(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let action_rows =
            mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_action_rows(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );

        assert_eq!(action_rows.len(), 3);
        assert_eq!(action_rows[0].protocol, ProtocolFamily::Zigbee);
        assert_eq!(
            action_rows[0].protocol_evidence_package_handoff_execution_action_row_key,
            "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-protocol-evidence-package-handoff-execution-action-01-repair_handoff-Zigbee"
        );
        assert_eq!(
            action_rows[0].protocol_evidence_package_handoff_execution_slot_sequence,
            1
        );
        assert_eq!(
            action_rows[0].protocol_evidence_package_handoff_execution_slot_key,
            "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-protocol-evidence-package-handoff-execution-slot-01-Zigbee"
        );
        assert_eq!(
            action_rows[0].protocol_evidence_package_handoff_row_key,
            "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-protocol-evidence-package-handoff-row-01-Zigbee"
        );
        assert_eq!(
            action_rows[0].handoff_lane,
            IntegrationMeshReleaseDispatchTicketHandoffLane::Review
        );
        assert!(action_rows[0].is_repair_action());
        assert!(!action_rows[0].is_release_handoff_action());
        assert_eq!(action_rows[0].blocked_substrate_rows, 1);
        assert_eq!(action_rows[0].required_handoff_action_evidence_rows, 5);
        assert_eq!(
            action_rows[0].release_blocking_handoff_action_evidence_rows,
            3
        );
        assert_eq!(
            action_rows[0].operator_required_handoff_action_evidence_rows,
            4
        );
        assert_eq!(
            action_rows[0].review_required_handoff_action_evidence_rows,
            2
        );
        assert_eq!(
            action_rows[0].dispatch_required_handoff_action_evidence_rows,
            5
        );
        assert_eq!(
            action_rows[0].execution_required_handoff_action_evidence_rows,
            5
        );
        assert_eq!(
            action_rows[0].next_handoff_action_evidence_row_key,
            Some(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-evidence-01-repair_handoff-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            action_rows[0].next_blocked_substrate_stage,
            Some(IntegrationMeshProtocolSubstrateStage::NetworkSecurity)
        );
        assert!(!action_rows[0].protocol_evidence_package_handoff_execution_slots_ready);
        assert!(!action_rows[0].package_handoff_execution_ready);
        assert!(action_rows[0].blocks_release());
        assert!(action_rows[0].needs_operator());
        assert!(action_rows[0].has_review_work());
        assert!(action_rows[0].requires_dispatch());
        assert!(action_rows[0].requires_execution());
        assert!(action_rows[0].requires_attention());
        assert!(!action_rows[0].ready_for_package_handoff_execution_action());
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_action_rows_mark_release_ready(
    ) {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let action_rows =
            mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_action_rows_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(action_rows.len(), 3);
        assert_eq!(action_rows[0].protocol, ProtocolFamily::Zigbee);
        assert_eq!(
            action_rows[0].protocol_evidence_package_handoff_execution_action_row_key,
            "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-protocol-evidence-package-handoff-execution-action-01-release_handoff-Zigbee"
        );
        assert_eq!(
            action_rows[0].handoff_lane,
            IntegrationMeshReleaseDispatchTicketHandoffLane::Release
        );
        assert!(action_rows[0].is_release_handoff_action());
        assert!(!action_rows[0].is_repair_action());
        assert!(!action_rows[0].is_review_action());
        assert_eq!(action_rows[0].blocked_substrate_rows, 0);
        assert_eq!(action_rows[0].required_handoff_action_evidence_rows, 0);
        assert_eq!(
            action_rows[0].release_blocking_handoff_action_evidence_rows,
            0
        );
        assert_eq!(
            action_rows[0].operator_required_handoff_action_evidence_rows,
            0
        );
        assert_eq!(
            action_rows[0].review_required_handoff_action_evidence_rows,
            0
        );
        assert_eq!(
            action_rows[0].dispatch_required_handoff_action_evidence_rows,
            0
        );
        assert_eq!(
            action_rows[0].execution_required_handoff_action_evidence_rows,
            0
        );
        assert_eq!(action_rows[0].release_handoff_evidence_rows, 5);
        assert_eq!(action_rows[0].release_ready_handoff_action_evidence_rows, 5);
        assert_eq!(action_rows[0].next_handoff_action_evidence_row_key, None);
        assert_eq!(action_rows[0].next_blocked_substrate_stage, None);
        assert!(action_rows[0].handoff_action_evidence_ready);
        assert!(action_rows[0].protocol_substrate_ready);
        assert!(action_rows[0].protocol_evidence_packet_ready);
        assert!(action_rows[0].protocol_evidence_package_ready);
        assert!(action_rows[0].package_handoff_ready);
        assert!(action_rows[0].protocol_evidence_package_handoff_ready);
        assert!(action_rows[0].protocol_evidence_package_handoff_execution_slots_ready);
        assert!(action_rows[0].package_handoff_execution_ready);
        assert!(!action_rows[0].blocks_release());
        assert!(!action_rows[0].needs_operator());
        assert!(!action_rows[0].has_review_work());
        assert!(!action_rows[0].requires_dispatch());
        assert!(!action_rows[0].requires_execution());
        assert!(!action_rows[0].requires_attention());
        assert!(action_rows[0].ready_for_package_handoff_execution_action());
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_action_summary_surfaces_attention(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let summary =
            mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_action_summary(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );

        assert_eq!(
            summary.total_protocol_evidence_package_handoff_execution_slots,
            3
        );
        assert_eq!(
            summary.total_protocol_evidence_package_handoff_execution_action_rows,
            3
        );
        assert_eq!(
            summary.required_protocol_evidence_package_handoff_execution_action_rows,
            3
        );
        assert_eq!(
            summary.release_blocking_protocol_evidence_package_handoff_execution_action_rows,
            3
        );
        assert_eq!(
            summary.repair_protocol_evidence_package_handoff_execution_action_rows,
            3
        );
        assert_eq!(
            summary.review_protocol_evidence_package_handoff_execution_action_rows,
            0
        );
        assert_eq!(
            summary.operator_protocol_evidence_package_handoff_execution_action_rows,
            3
        );
        assert_eq!(
            summary.dispatch_protocol_evidence_package_handoff_execution_action_rows,
            3
        );
        assert_eq!(
            summary.execution_protocol_evidence_package_handoff_execution_action_rows,
            3
        );
        assert_eq!(
            summary.release_handoff_protocol_evidence_package_handoff_execution_action_rows,
            0
        );
        assert_eq!(
            summary.ready_protocol_evidence_package_handoff_execution_action_rows,
            0
        );
        assert_eq!(
            summary.first_protocol_evidence_package_handoff_execution_action_row_key,
            Some(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-protocol-evidence-package-handoff-execution-action-01-repair_handoff-Zigbee"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_required_protocol_evidence_package_handoff_execution_action_row_key,
            summary.first_protocol_evidence_package_handoff_execution_action_row_key
        );
        assert_eq!(
            summary.first_release_protocol_evidence_package_handoff_execution_action_row_key,
            None
        );
        assert_eq!(
            summary.first_ready_protocol_evidence_package_handoff_execution_action_row_key,
            None
        );
        assert_eq!(
            summary.next_protocol_evidence_package_handoff_execution_action_row_key,
            summary.first_protocol_evidence_package_handoff_execution_action_row_key
        );
        assert_eq!(
            summary.next_protocol_evidence_package_handoff_execution_slot_key,
            Some(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-protocol-evidence-package-handoff-execution-slot-01-Zigbee"
                    .to_string()
            )
        );
        assert_eq!(
            summary.next_handoff_action_evidence_row_key,
            Some(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-evidence-01-repair_handoff-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(summary.next_protocol, Some(ProtocolFamily::Zigbee));
        assert_eq!(
            summary.next_blocked_substrate_stage,
            Some(IntegrationMeshProtocolSubstrateStage::NetworkSecurity)
        );
        assert_eq!(
            summary.next_handoff_lane,
            Some(IntegrationMeshReleaseDispatchTicketHandoffLane::Review)
        );
        assert!(!summary.protocol_evidence_package_handoff_execution_slots_ready);
        assert!(!summary.protocol_evidence_package_handoff_execution_actions_ready);
        assert!(summary.has_required_actions());
        assert!(summary.has_blockers());
        assert!(summary.needs_operator());
        assert!(summary.requires_dispatch());
        assert!(summary.requires_execution());
        assert!(!summary.has_release_handoff_actions());
        assert!(!summary.has_ready_handoff_actions());
        assert!(summary.requires_attention());
        assert!(!summary
            .ready_for_release_ticket_handoff_protocol_evidence_package_handoff_execution_actions(
            ));
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_action_summary_marks_release_ready(
    ) {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let summary =
            mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_action_summary_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(
            summary.total_protocol_evidence_package_handoff_execution_slots,
            3
        );
        assert_eq!(
            summary.total_protocol_evidence_package_handoff_execution_action_rows,
            3
        );
        assert_eq!(
            summary.required_protocol_evidence_package_handoff_execution_action_rows,
            0
        );
        assert_eq!(
            summary.release_blocking_protocol_evidence_package_handoff_execution_action_rows,
            0
        );
        assert_eq!(
            summary.repair_protocol_evidence_package_handoff_execution_action_rows,
            0
        );
        assert_eq!(
            summary.release_handoff_protocol_evidence_package_handoff_execution_action_rows,
            3
        );
        assert_eq!(
            summary.ready_protocol_evidence_package_handoff_execution_action_rows,
            3
        );
        assert_eq!(
            summary.first_release_protocol_evidence_package_handoff_execution_action_row_key,
            summary.first_protocol_evidence_package_handoff_execution_action_row_key
        );
        assert_eq!(
            summary.first_ready_protocol_evidence_package_handoff_execution_action_row_key,
            summary.first_protocol_evidence_package_handoff_execution_action_row_key
        );
        assert_eq!(
            summary.next_protocol_evidence_package_handoff_execution_action_row_key,
            None
        );
        assert_eq!(
            summary.next_protocol_evidence_package_handoff_execution_slot_key,
            None
        );
        assert_eq!(summary.next_protocol_evidence_package_handoff_row_key, None);
        assert_eq!(summary.next_handoff_action_evidence_row_key, None);
        assert_eq!(summary.next_protocol, None);
        assert_eq!(summary.next_blocked_substrate_stage, None);
        assert_eq!(summary.next_handoff_lane, None);
        assert!(summary.protocol_evidence_package_handoff_execution_slots_ready);
        assert!(summary.protocol_evidence_package_handoff_execution_actions_ready);
        assert!(!summary.has_required_actions());
        assert!(!summary.has_blockers());
        assert!(!summary.needs_operator());
        assert!(!summary.has_review_work());
        assert!(!summary.requires_dispatch());
        assert!(!summary.requires_execution());
        assert!(summary.has_release_handoff_actions());
        assert!(summary.has_ready_handoff_actions());
        assert!(!summary.requires_attention());
        assert!(summary
            .ready_for_release_ticket_handoff_protocol_evidence_package_handoff_execution_actions(
            ));
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_action_evidence_rows_surface_action_lineage(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let evidence_rows =
            mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_action_evidence_rows(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );

        assert_eq!(evidence_rows.len(), 3);
        assert_eq!(evidence_rows[0].protocol, ProtocolFamily::Zigbee);
        assert_eq!(
            evidence_rows[0].protocol_evidence_package_handoff_execution_action_evidence_row_key,
            "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-protocol-evidence-package-handoff-execution-action-evidence-01-repair_handoff-Zigbee"
        );
        assert_eq!(
            evidence_rows[0].protocol_evidence_package_handoff_execution_action_row_key,
            "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-protocol-evidence-package-handoff-execution-action-01-repair_handoff-Zigbee"
        );
        assert_eq!(
            evidence_rows[0].protocol_evidence_package_handoff_execution_slot_key,
            "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-protocol-evidence-package-handoff-execution-slot-01-Zigbee"
        );
        assert_eq!(
            evidence_rows[0].protocol_evidence_package_handoff_row_key,
            "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-protocol-evidence-package-handoff-row-01-Zigbee"
        );
        assert_eq!(
            evidence_rows[0].handoff_action_kind,
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind::RepairHandoff
        );
        assert_eq!(
            evidence_rows
                .iter()
                .filter(|row| row.is_repair_action())
                .count(),
            3
        );
        assert_eq!(
            evidence_rows
                .iter()
                .filter(|row| row.blocks_release())
                .count(),
            3
        );
        assert_eq!(
            evidence_rows
                .iter()
                .filter(|row| row.needs_operator())
                .count(),
            3
        );
        assert_eq!(
            evidence_rows
                .iter()
                .filter(|row| row.requires_dispatch())
                .count(),
            3
        );
        assert_eq!(
            evidence_rows
                .iter()
                .filter(|row| row.requires_execution())
                .count(),
            3
        );
        assert_eq!(
            evidence_rows[0].next_handoff_action_evidence_row_key,
            Some(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-evidence-01-repair_handoff-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            evidence_rows[0].next_blocked_substrate_stage,
            Some(IntegrationMeshProtocolSubstrateStage::NetworkSecurity)
        );
        assert!(evidence_rows.iter().all(|row| row.has_lineage()));
        assert_eq!(
            evidence_rows
                .iter()
                .filter(|row| row.has_release_handoff_evidence())
                .count(),
            0
        );
        assert_eq!(
            evidence_rows
                .iter()
                .filter(|row| row.requires_attention())
                .count(),
            3
        );
        assert_eq!(
            evidence_rows
                .iter()
                .filter(|row| row.ready_for_package_handoff_execution_action_evidence())
                .count(),
            0
        );
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_action_evidence_rows_mark_release_ready(
    ) {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let evidence_rows =
            mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_action_evidence_rows_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(evidence_rows.len(), 3);
        assert_eq!(evidence_rows[0].protocol, ProtocolFamily::Zigbee);
        assert_eq!(
            evidence_rows[0].protocol_evidence_package_handoff_execution_action_evidence_row_key,
            "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-protocol-evidence-package-handoff-execution-action-evidence-01-release_handoff-Zigbee"
        );
        assert_eq!(
            evidence_rows[0].protocol_evidence_package_handoff_execution_action_row_key,
            "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-protocol-evidence-package-handoff-execution-action-01-release_handoff-Zigbee"
        );
        assert_eq!(
            evidence_rows[0].handoff_lane,
            IntegrationMeshReleaseDispatchTicketHandoffLane::Release
        );
        assert_eq!(
            evidence_rows[0].handoff_action_kind,
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind::ReleaseHandoff
        );
        assert_eq!(
            evidence_rows
                .iter()
                .filter(|row| row.is_release_handoff_action())
                .count(),
            3
        );
        assert_eq!(
            evidence_rows
                .iter()
                .filter(|row| row.has_release_handoff_evidence())
                .count(),
            3
        );
        assert_eq!(
            evidence_rows
                .iter()
                .filter(|row| row.ready_for_package_handoff_execution_action_evidence())
                .count(),
            3
        );
        assert_eq!(
            evidence_rows
                .iter()
                .filter(|row| row.requires_attention())
                .count(),
            0
        );
        assert!(evidence_rows.iter().all(|row| row.has_lineage()));
        assert!(evidence_rows
            .iter()
            .all(|row| row.protocol_evidence_package_handoff_execution_slots_ready));
        assert!(evidence_rows
            .iter()
            .all(|row| row.package_handoff_execution_ready));
        assert!(evidence_rows
            .iter()
            .all(|row| row.handoff_action_evidence_ready));
    }

    fn protocol_action_evidence_row_for_review_test(
        handoff_action_kind: IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind,
    ) -> IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionActionEvidenceRow{
        let release_ready =
            handoff_action_kind == IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind::ReleaseHandoff;

        IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionActionEvidenceRow {
            sequence: 1,
            protocol_evidence_package_handoff_execution_action_evidence_row_key:
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-protocol-evidence-package-handoff-execution-action-evidence-01-Zigbee".to_string(),
            protocol_evidence_package_handoff_execution_action_row_sequence: 1,
            protocol_evidence_package_handoff_execution_action_row_key:
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-protocol-evidence-package-handoff-execution-action-01-Zigbee".to_string(),
            protocol_evidence_package_handoff_execution_slot_sequence: 1,
            protocol_evidence_package_handoff_execution_slot_key:
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-protocol-evidence-package-handoff-execution-slot-01-Zigbee".to_string(),
            protocol_evidence_package_handoff_row_sequence: 1,
            protocol_evidence_package_handoff_row_key:
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-protocol-evidence-package-handoff-row-01-Zigbee".to_string(),
            protocol_evidence_package_key: "protocol-evidence-package-Zigbee".to_string(),
            protocol_evidence_packet_key: "protocol-evidence-packet-Zigbee".to_string(),
            protocol: ProtocolFamily::Zigbee,
            handoff_lane: if release_ready {
                IntegrationMeshReleaseDispatchTicketHandoffLane::Release
            } else {
                IntegrationMeshReleaseDispatchTicketHandoffLane::Review
            },
            handoff_action_kind,
            total_substrate_rows: 3,
            ready_substrate_rows: if release_ready { 3 } else { 1 },
            blocked_substrate_rows: if release_ready { 0 } else { 2 },
            total_handoff_action_evidence_rows: 5,
            required_handoff_action_evidence_rows: if release_ready { 0 } else { 3 },
            release_blocking_handoff_action_evidence_rows: if release_ready { 0 } else { 3 },
            operator_required_handoff_action_evidence_rows: if release_ready { 0 } else { 1 },
            review_required_handoff_action_evidence_rows: 0,
            dispatch_required_handoff_action_evidence_rows: if release_ready { 0 } else { 1 },
            execution_required_handoff_action_evidence_rows: if release_ready { 0 } else { 1 },
            release_handoff_evidence_rows: if release_ready { 5 } else { 0 },
            release_ready_handoff_action_evidence_rows: if release_ready { 5 } else { 0 },
            next_handoff_action_evidence_row_key: if release_ready {
                None
            } else {
                Some("release-ticket-handoff-action-evidence-01".to_string())
            },
            next_blocked_substrate_stage: if release_ready {
                None
            } else {
                Some(IntegrationMeshProtocolSubstrateStage::NetworkSecurity)
            },
            release_blocking: !release_ready,
            operator_required: !release_ready,
            review_required: false,
            dispatch_required: !release_ready,
            execution_required: !release_ready,
            lineage_complete: true,
            release_handoff_evidence: release_ready,
            protocol_substrate_ready: release_ready,
            protocol_evidence_packet_ready: release_ready,
            protocol_evidence_package_ready: release_ready,
            package_handoff_ready: release_ready,
            protocol_evidence_package_handoff_ready: release_ready,
            protocol_evidence_package_handoff_execution_slots_ready: release_ready,
            package_handoff_execution_ready: release_ready,
            handoff_action_evidence_ready: release_ready,
            protocol_evidence_package_handoff_execution_action_evidence_ready: release_ready,
        }
    }

    fn protocol_action_evidence_summary_for_review_test(
        evidence_rows: &[IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionActionEvidenceRow],
    ) -> IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionActionEvidenceSummary
    {
        let first_row = evidence_rows.first();
        let total_rows = evidence_rows.len();
        let ready_rows = evidence_rows
            .iter()
            .filter(|row| row.ready_for_package_handoff_execution_action_evidence())
            .count();
        let blocked_rows = total_rows.saturating_sub(ready_rows);
        let release_ready = total_rows > 0 && ready_rows == total_rows;
        let required_evidence_rows = first_row
            .map(|row| row.required_handoff_action_evidence_rows)
            .unwrap_or(0);
        let release_blocking_rows = first_row
            .map(|row| row.release_blocking_handoff_action_evidence_rows)
            .unwrap_or(0);
        let operator_rows = first_row
            .map(|row| row.operator_required_handoff_action_evidence_rows)
            .unwrap_or(0);
        let review_rows = first_row
            .map(|row| row.review_required_handoff_action_evidence_rows)
            .unwrap_or(0);
        let dispatch_rows = first_row
            .map(|row| row.dispatch_required_handoff_action_evidence_rows)
            .unwrap_or(0);
        let execution_rows = first_row
            .map(|row| row.execution_required_handoff_action_evidence_rows)
            .unwrap_or(0);
        let release_handoff_rows = first_row
            .map(|row| row.release_handoff_evidence_rows)
            .unwrap_or(0);
        let release_ready_rows = first_row
            .map(|row| row.release_ready_handoff_action_evidence_rows)
            .unwrap_or(0);

        let packet_summary =
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePacketSummary {
                total_protocol_evidence_packets: total_rows,
                ready_protocol_evidence_packets: ready_rows,
                blocked_protocol_evidence_packets: blocked_rows,
                protocol_substrate_blockers: first_row.map(|row| row.blocked_substrate_rows).unwrap_or(0),
                required_handoff_action_evidence_rows: required_evidence_rows,
                release_blocking_handoff_action_evidence_rows: release_blocking_rows,
                operator_required_handoff_action_evidence_rows: operator_rows,
                review_required_handoff_action_evidence_rows: review_rows,
                dispatch_required_handoff_action_evidence_rows: dispatch_rows,
                execution_required_handoff_action_evidence_rows: execution_rows,
                release_handoff_evidence_rows: release_handoff_rows,
                release_ready_handoff_action_evidence_rows: release_ready_rows,
                first_protocol_evidence_packet_key: first_row.map(|row| row.protocol_evidence_packet_key.clone()),
                first_required_protocol_evidence_packet_key: first_row
                    .filter(|row| row.requires_attention())
                    .map(|row| row.protocol_evidence_packet_key.clone()),
                first_protocol: first_row.map(|row| row.protocol.clone()),
                first_blocked_protocol: first_row
                    .filter(|row| row.blocked_substrate_rows > 0)
                    .map(|row| row.protocol.clone()),
                first_blocked_substrate_stage: first_row.and_then(|row| row.next_blocked_substrate_stage),
                first_handoff_action_evidence_row_key: first_row
                    .and_then(|row| row.next_handoff_action_evidence_row_key.clone()),
                first_required_handoff_action_evidence_row_key: first_row
                    .filter(|row| row.requires_attention())
                    .and_then(|row| row.next_handoff_action_evidence_row_key.clone()),
                handoff_action_evidence_ready: release_ready,
                protocol_evidence_packets_ready: release_ready,
            };
        let package_summary =
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageReadinessSummary {
                release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_packet_summary: Box::new(packet_summary),
                protocol_evidence_package_key: first_row
                    .map(|row| row.protocol_evidence_package_key.clone())
                    .unwrap_or_else(|| "protocol-evidence-package-test".to_string()),
                total_protocol_evidence_packets: total_rows,
                ready_protocol_evidence_packets: ready_rows,
                blocked_protocol_evidence_packets: blocked_rows,
                protocol_substrate_blockers: first_row.map(|row| row.blocked_substrate_rows).unwrap_or(0),
                required_handoff_action_evidence_rows: required_evidence_rows,
                release_blocking_handoff_action_evidence_rows: release_blocking_rows,
                operator_required_handoff_action_evidence_rows: operator_rows,
                review_required_handoff_action_evidence_rows: review_rows,
                dispatch_required_handoff_action_evidence_rows: dispatch_rows,
                execution_required_handoff_action_evidence_rows: execution_rows,
                release_handoff_evidence_rows: release_handoff_rows,
                release_ready_handoff_action_evidence_rows: release_ready_rows,
                first_protocol_evidence_packet_key: first_row.map(|row| row.protocol_evidence_packet_key.clone()),
                next_protocol_evidence_packet_key: first_row
                    .filter(|row| row.requires_attention())
                    .map(|row| row.protocol_evidence_packet_key.clone()),
                next_handoff_action_evidence_row_key: first_row
                    .filter(|row| row.requires_attention())
                    .and_then(|row| row.next_handoff_action_evidence_row_key.clone()),
                next_protocol: first_row
                    .filter(|row| row.requires_attention())
                    .map(|row| row.protocol.clone()),
                next_blocked_substrate_stage: first_row.and_then(|row| row.next_blocked_substrate_stage),
                handoff_action_evidence_ready: release_ready,
                protocol_evidence_packets_ready: release_ready,
                release_protocol_evidence_package_ready: release_ready,
            };
        let handoff_summary =
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffSummary {
                release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_readiness_summary: Box::new(package_summary),
                total_protocol_evidence_package_handoff_rows: total_rows,
                ready_protocol_evidence_package_handoff_rows: ready_rows,
                blocked_protocol_evidence_package_handoff_rows: blocked_rows,
                protocol_substrate_blockers: first_row.map(|row| row.blocked_substrate_rows).unwrap_or(0),
                required_handoff_action_evidence_rows: required_evidence_rows,
                release_blocking_handoff_action_evidence_rows: release_blocking_rows,
                operator_required_handoff_action_evidence_rows: operator_rows,
                review_required_handoff_action_evidence_rows: review_rows,
                dispatch_required_handoff_action_evidence_rows: dispatch_rows,
                execution_required_handoff_action_evidence_rows: execution_rows,
                release_handoff_evidence_rows: release_handoff_rows,
                release_ready_handoff_action_evidence_rows: release_ready_rows,
                first_protocol_evidence_package_handoff_row_key: first_row
                    .map(|row| row.protocol_evidence_package_handoff_row_key.clone()),
                first_required_protocol_evidence_package_handoff_row_key: first_row
                    .filter(|row| row.requires_attention())
                    .map(|row| row.protocol_evidence_package_handoff_row_key.clone()),
                first_ready_protocol_evidence_package_handoff_row_key: first_row
                    .filter(|row| row.ready_for_package_handoff_execution_action_evidence())
                    .map(|row| row.protocol_evidence_package_handoff_row_key.clone()),
                first_protocol_evidence_packet_key: first_row.map(|row| row.protocol_evidence_packet_key.clone()),
                next_protocol_evidence_package_handoff_row_key: first_row
                    .filter(|row| row.requires_attention())
                    .map(|row| row.protocol_evidence_package_handoff_row_key.clone()),
                next_protocol_evidence_packet_key: first_row
                    .filter(|row| row.requires_attention())
                    .map(|row| row.protocol_evidence_packet_key.clone()),
                next_handoff_action_evidence_row_key: first_row
                    .filter(|row| row.requires_attention())
                    .and_then(|row| row.next_handoff_action_evidence_row_key.clone()),
                next_protocol: first_row
                    .filter(|row| row.requires_attention())
                    .map(|row| row.protocol.clone()),
                next_blocked_substrate_stage: first_row.and_then(|row| row.next_blocked_substrate_stage),
                handoff_action_evidence_ready: release_ready,
                protocol_evidence_package_ready: release_ready,
                protocol_evidence_package_handoff_rows_ready: release_ready,
            };
        let slot_summary =
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionSlotSummary {
                release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_summary: Box::new(handoff_summary),
                total_protocol_evidence_package_handoff_rows: total_rows,
                total_protocol_evidence_package_handoff_execution_slots: total_rows,
                ready_protocol_evidence_package_handoff_execution_slots: ready_rows,
                blocked_protocol_evidence_package_handoff_execution_slots: blocked_rows,
                repair_protocol_evidence_package_handoff_execution_slots: first_row
                    .filter(|row| row.is_repair_action())
                    .map(|_| 1)
                    .unwrap_or(0),
                operator_protocol_evidence_package_handoff_execution_slots: first_row
                    .filter(|row| row.needs_operator())
                    .map(|_| 1)
                    .unwrap_or(0),
                review_protocol_evidence_package_handoff_execution_slots: first_row
                    .filter(|row| row.has_review_work())
                    .map(|_| 1)
                    .unwrap_or(0),
                release_protocol_evidence_package_handoff_execution_slots: first_row
                    .filter(|row| row.is_release_handoff_action())
                    .map(|_| 1)
                    .unwrap_or(0),
                protocol_substrate_blockers: first_row.map(|row| row.blocked_substrate_rows).unwrap_or(0),
                required_handoff_action_evidence_rows: required_evidence_rows,
                release_blocking_handoff_action_evidence_rows: release_blocking_rows,
                operator_required_handoff_action_evidence_rows: operator_rows,
                review_required_handoff_action_evidence_rows: review_rows,
                dispatch_required_handoff_action_evidence_rows: dispatch_rows,
                execution_required_handoff_action_evidence_rows: execution_rows,
                release_handoff_evidence_rows: release_handoff_rows,
                release_ready_handoff_action_evidence_rows: release_ready_rows,
                first_protocol_evidence_package_handoff_execution_slot_key: first_row
                    .map(|row| row.protocol_evidence_package_handoff_execution_slot_key.clone()),
                first_required_protocol_evidence_package_handoff_execution_slot_key: first_row
                    .filter(|row| row.requires_attention())
                    .map(|row| row.protocol_evidence_package_handoff_execution_slot_key.clone()),
                first_ready_protocol_evidence_package_handoff_execution_slot_key: first_row
                    .filter(|row| row.ready_for_package_handoff_execution_action_evidence())
                    .map(|row| row.protocol_evidence_package_handoff_execution_slot_key.clone()),
                first_release_protocol_evidence_package_handoff_execution_slot_key: first_row
                    .filter(|row| row.is_release_handoff_action())
                    .map(|row| row.protocol_evidence_package_handoff_execution_slot_key.clone()),
                first_protocol_evidence_package_handoff_row_key: first_row
                    .map(|row| row.protocol_evidence_package_handoff_row_key.clone()),
                first_protocol_evidence_packet_key: first_row.map(|row| row.protocol_evidence_packet_key.clone()),
                next_protocol_evidence_package_handoff_execution_slot_key: first_row
                    .filter(|row| row.requires_attention())
                    .map(|row| row.protocol_evidence_package_handoff_execution_slot_key.clone()),
                next_protocol_evidence_package_handoff_row_key: first_row
                    .filter(|row| row.requires_attention())
                    .map(|row| row.protocol_evidence_package_handoff_row_key.clone()),
                next_protocol_evidence_packet_key: first_row
                    .filter(|row| row.requires_attention())
                    .map(|row| row.protocol_evidence_packet_key.clone()),
                next_handoff_action_evidence_row_key: first_row
                    .filter(|row| row.requires_attention())
                    .and_then(|row| row.next_handoff_action_evidence_row_key.clone()),
                next_protocol: first_row
                    .filter(|row| row.requires_attention())
                    .map(|row| row.protocol.clone()),
                next_blocked_substrate_stage: first_row.and_then(|row| row.next_blocked_substrate_stage),
                next_handoff_lane: first_row
                    .filter(|row| row.requires_attention())
                    .map(|row| row.handoff_lane),
                protocol_evidence_package_handoff_ready: release_ready,
                protocol_evidence_package_handoff_execution_slots_ready: release_ready,
            };
        let action_summary =
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionActionSummary {
                release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_slot_summary: Box::new(slot_summary),
                total_protocol_evidence_package_handoff_execution_slots: total_rows,
                total_protocol_evidence_package_handoff_execution_action_rows: total_rows,
                required_protocol_evidence_package_handoff_execution_action_rows: evidence_rows
                    .iter()
                    .filter(|row| row.requires_attention())
                    .count(),
                release_blocking_protocol_evidence_package_handoff_execution_action_rows: evidence_rows
                    .iter()
                    .filter(|row| row.blocks_release())
                    .count(),
                repair_protocol_evidence_package_handoff_execution_action_rows: evidence_rows
                    .iter()
                    .filter(|row| row.is_repair_action())
                    .count(),
                review_protocol_evidence_package_handoff_execution_action_rows: evidence_rows
                    .iter()
                    .filter(|row| row.is_review_action())
                    .count(),
                operator_protocol_evidence_package_handoff_execution_action_rows: evidence_rows
                    .iter()
                    .filter(|row| row.needs_operator())
                    .count(),
                dispatch_protocol_evidence_package_handoff_execution_action_rows: evidence_rows
                    .iter()
                    .filter(|row| row.requires_dispatch())
                    .count(),
                execution_protocol_evidence_package_handoff_execution_action_rows: evidence_rows
                    .iter()
                    .filter(|row| row.requires_execution())
                    .count(),
                release_handoff_protocol_evidence_package_handoff_execution_action_rows: evidence_rows
                    .iter()
                    .filter(|row| row.is_release_handoff_action())
                    .count(),
                ready_protocol_evidence_package_handoff_execution_action_rows: ready_rows,
                first_protocol_evidence_package_handoff_execution_action_row_key: first_row
                    .map(|row| row.protocol_evidence_package_handoff_execution_action_row_key.clone()),
                first_required_protocol_evidence_package_handoff_execution_action_row_key: first_row
                    .filter(|row| row.requires_attention())
                    .map(|row| row.protocol_evidence_package_handoff_execution_action_row_key.clone()),
                first_release_protocol_evidence_package_handoff_execution_action_row_key: first_row
                    .filter(|row| row.is_release_handoff_action())
                    .map(|row| row.protocol_evidence_package_handoff_execution_action_row_key.clone()),
                first_ready_protocol_evidence_package_handoff_execution_action_row_key: first_row
                    .filter(|row| row.ready_for_package_handoff_execution_action_evidence())
                    .map(|row| row.protocol_evidence_package_handoff_execution_action_row_key.clone()),
                first_protocol_evidence_package_handoff_execution_slot_key: first_row
                    .map(|row| row.protocol_evidence_package_handoff_execution_slot_key.clone()),
                first_protocol_evidence_package_handoff_row_key: first_row
                    .map(|row| row.protocol_evidence_package_handoff_row_key.clone()),
                next_protocol_evidence_package_handoff_execution_action_row_key: first_row
                    .filter(|row| row.requires_attention())
                    .map(|row| row.protocol_evidence_package_handoff_execution_action_row_key.clone()),
                next_protocol_evidence_package_handoff_execution_slot_key: first_row
                    .filter(|row| row.requires_attention())
                    .map(|row| row.protocol_evidence_package_handoff_execution_slot_key.clone()),
                next_protocol_evidence_package_handoff_row_key: first_row
                    .filter(|row| row.requires_attention())
                    .map(|row| row.protocol_evidence_package_handoff_row_key.clone()),
                next_handoff_action_evidence_row_key: first_row
                    .filter(|row| row.requires_attention())
                    .and_then(|row| row.next_handoff_action_evidence_row_key.clone()),
                next_protocol: first_row
                    .filter(|row| row.requires_attention())
                    .map(|row| row.protocol.clone()),
                next_blocked_substrate_stage: first_row.and_then(|row| row.next_blocked_substrate_stage),
                next_handoff_lane: first_row
                    .filter(|row| row.requires_attention())
                    .map(|row| row.handoff_lane),
                protocol_evidence_package_handoff_execution_slots_ready: release_ready,
                protocol_evidence_package_handoff_execution_actions_ready: release_ready,
            };

        IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionActionEvidenceSummary::from_summaries(
            action_summary,
            evidence_rows.iter(),
        )
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_action_evidence_review_rows_surface_review_outcomes(
    ) {
        let evidence_row = protocol_action_evidence_row_for_review_test(
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind::RepairHandoff,
        );
        let review_row =
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionActionEvidenceReviewRow::from_action_evidence_row(
                1,
                &evidence_row,
                false,
            );

        assert_eq!(review_row.sequence, 1);
        assert_eq!(
            review_row.protocol_evidence_package_handoff_execution_action_evidence_review_row_key,
            "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-protocol-evidence-package-handoff-execution-action-evidence-review-01-blocker_review-Zigbee"
        );
        assert_eq!(
            review_row.protocol_evidence_package_handoff_execution_action_evidence_row_sequence,
            evidence_row.sequence
        );
        assert_eq!(
            review_row.protocol_evidence_package_handoff_execution_action_evidence_row_key,
            evidence_row.protocol_evidence_package_handoff_execution_action_evidence_row_key
        );
        assert_eq!(review_row.protocol, ProtocolFamily::Zigbee);
        assert_eq!(
            review_row.protocol_evidence_package_handoff_execution_action_evidence_review_kind,
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::BlockerReview
        );
        assert!(review_row.is_blocker_review());
        assert!(review_row.requires_evidence_review());
        assert!(review_row.blocks_release());
        assert!(review_row.needs_operator());
        assert!(!review_row.has_review_work());
        assert!(review_row.requires_dispatch());
        assert!(review_row.requires_execution());
        assert!(review_row.has_lineage());
        assert!(!review_row.has_release_handoff_evidence());
        assert!(!review_row.ready_to_release_handoff());
        assert!(review_row.requires_attention());

        let mut lineage_evidence = evidence_row.clone();
        lineage_evidence
            .protocol_evidence_package_handoff_execution_action_row_key
            .clear();
        lineage_evidence.lineage_complete = false;
        lineage_evidence.protocol_evidence_package_handoff_execution_action_evidence_ready = false;
        let lineage_review =
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionActionEvidenceReviewRow::from_action_evidence_row(
                1,
                &lineage_evidence,
                false,
            );

        assert_eq!(
            lineage_review.protocol_evidence_package_handoff_execution_action_evidence_review_row_key,
            "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-protocol-evidence-package-handoff-execution-action-evidence-review-01-lineage_gap-Zigbee"
        );
        assert!(lineage_review.is_lineage_gap());
        assert!(!lineage_review.has_lineage());
        assert!(lineage_review.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_action_evidence_review_rows_mark_release_ready(
    ) {
        let evidence_row = protocol_action_evidence_row_for_review_test(
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind::ReleaseHandoff,
        );
        let review_row =
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionActionEvidenceReviewRow::from_action_evidence_row(
                1,
                &evidence_row,
                true,
            );

        assert!(review_row.has_release_handoff_evidence());
        assert!(review_row.is_release_ready_review());
        assert!(review_row.ready_to_release_handoff());
        assert!(
            review_row.protocol_evidence_package_handoff_execution_action_evidence_reviews_ready
        );
        assert!(!review_row.requires_attention());
        assert_eq!(
            review_row.protocol_evidence_package_handoff_execution_action_evidence_review_row_key,
            "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-protocol-evidence-package-handoff-execution-action-evidence-review-01-release_ready-Zigbee"
        );
        assert_eq!(
            review_row.protocol_evidence_package_handoff_execution_action_evidence_review_kind,
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::ReleaseReady
        );
        assert_eq!(
            review_row.protocol_evidence_package_handoff_execution_action_evidence_row_sequence,
            1
        );
        assert_eq!(review_row.protocol, ProtocolFamily::Zigbee);
        assert_eq!(
            review_row.handoff_action_kind,
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind::ReleaseHandoff
        );
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_action_evidence_review_summary_surfaces_attention(
    ) {
        let evidence_row = protocol_action_evidence_row_for_review_test(
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind::RepairHandoff,
        );
        let evidence_rows = vec![evidence_row];
        let evidence_summary = protocol_action_evidence_summary_for_review_test(&evidence_rows);
        let review_rows = [IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionActionEvidenceReviewRow::from_action_evidence_row(
                1,
                &evidence_rows[0],
                false,
            )];
        let summary =
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionActionEvidenceReviewSummary::from_summaries(
                evidence_summary,
                review_rows.iter(),
            );

        assert_eq!(
            summary.total_protocol_evidence_package_handoff_execution_action_evidence_rows,
            1
        );
        assert_eq!(
            summary.total_protocol_evidence_package_handoff_execution_action_evidence_review_rows,
            1
        );
        assert_eq!(
            summary
                .required_protocol_evidence_package_handoff_execution_action_evidence_review_rows,
            1
        );
        assert_eq!(
            summary.blocker_protocol_evidence_package_handoff_execution_action_evidence_review_rows,
            1
        );
        assert_eq!(
            summary
                .operator_protocol_evidence_package_handoff_execution_action_evidence_review_rows,
            0
        );
        assert_eq!(
            summary.review_gate_protocol_evidence_package_handoff_execution_action_evidence_review_rows,
            0
        );
        assert_eq!(
            summary.lineage_gap_protocol_evidence_package_handoff_execution_action_evidence_review_rows,
            0
        );
        assert_eq!(
            summary.release_handoff_protocol_evidence_package_handoff_execution_action_evidence_review_rows,
            0
        );
        assert_eq!(
            summary.release_ready_protocol_evidence_package_handoff_execution_action_evidence_review_rows,
            0
        );
        assert_eq!(
            summary.first_protocol_evidence_package_handoff_execution_action_evidence_review_row_key,
            Some(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-protocol-evidence-package-handoff-execution-action-evidence-review-01-blocker_review-Zigbee"
                    .to_string()
            )
        );
        assert_eq!(
            summary
                .first_required_protocol_evidence_package_handoff_execution_action_evidence_review_row_key,
            summary
                .first_protocol_evidence_package_handoff_execution_action_evidence_review_row_key
        );
        assert_eq!(
            summary
                .first_blocker_protocol_evidence_package_handoff_execution_action_evidence_review_row_key,
            summary
                .first_protocol_evidence_package_handoff_execution_action_evidence_review_row_key
        );
        assert_eq!(
            summary
                .first_release_ready_protocol_evidence_package_handoff_execution_action_evidence_review_row_key,
            None
        );
        assert_eq!(
            summary
                .first_protocol_evidence_package_handoff_execution_action_evidence_row_key,
            Some(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-protocol-evidence-package-handoff-execution-action-evidence-01-Zigbee"
                    .to_string()
            )
        );
        assert_eq!(summary.first_protocol, Some(ProtocolFamily::Zigbee));
        assert_eq!(
            summary.first_handoff_lane,
            Some(IntegrationMeshReleaseDispatchTicketHandoffLane::Review)
        );
        assert_eq!(
            summary.next_protocol_evidence_package_handoff_execution_action_evidence_review_row_key,
            summary
                .first_protocol_evidence_package_handoff_execution_action_evidence_review_row_key
        );
        assert_eq!(
            summary.next_protocol_evidence_package_handoff_execution_action_evidence_row_key,
            summary.first_protocol_evidence_package_handoff_execution_action_evidence_row_key
        );
        assert_eq!(summary.next_protocol, Some(ProtocolFamily::Zigbee));
        assert_eq!(
            summary.next_handoff_lane,
            Some(IntegrationMeshReleaseDispatchTicketHandoffLane::Review)
        );
        assert!(!summary.protocol_evidence_package_handoff_execution_action_evidence_ready);
        assert!(!summary.protocol_evidence_package_handoff_execution_action_evidence_reviews_ready);
        assert!(
            summary.has_protocol_evidence_package_handoff_execution_action_evidence_review_rows()
        );
        assert!(summary.has_required_reviews());
        assert!(summary.has_blockers());
        assert!(summary.needs_operator());
        assert!(!summary.has_review_work());
        assert!(summary.requires_dispatch());
        assert!(summary.requires_execution());
        assert!(!summary.has_lineage_gaps());
        assert!(!summary.has_release_handoff_reviews());
        assert!(!summary.has_release_ready_reviews());
        assert!(summary.requires_attention());
        assert!(!summary
            .ready_for_release_ticket_handoff_protocol_evidence_package_handoff_execution_action_evidence_reviews());
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_action_evidence_review_summary_marks_release_ready(
    ) {
        let evidence_row = protocol_action_evidence_row_for_review_test(
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionKind::ReleaseHandoff,
        );
        let evidence_rows = vec![evidence_row];
        let evidence_summary = protocol_action_evidence_summary_for_review_test(&evidence_rows);
        let review_rows = [IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionActionEvidenceReviewRow::from_action_evidence_row(
                1,
                &evidence_rows[0],
                true,
            )];
        let summary =
            IntegrationMeshReleaseTicketHandoffReadinessEvidenceReviewDispositionActionReadinessExecutionHandoffActionProtocolEvidencePackageHandoffExecutionActionEvidenceReviewSummary::from_summaries(
                evidence_summary,
                review_rows.iter(),
            );

        assert_eq!(
            summary.total_protocol_evidence_package_handoff_execution_action_evidence_rows,
            1
        );
        assert_eq!(
            summary.total_protocol_evidence_package_handoff_execution_action_evidence_review_rows,
            1
        );
        assert_eq!(
            summary
                .required_protocol_evidence_package_handoff_execution_action_evidence_review_rows,
            0
        );
        assert_eq!(
            summary.blocker_protocol_evidence_package_handoff_execution_action_evidence_review_rows,
            0
        );
        assert_eq!(
            summary.release_handoff_protocol_evidence_package_handoff_execution_action_evidence_review_rows,
            1
        );
        assert_eq!(
            summary.release_ready_protocol_evidence_package_handoff_execution_action_evidence_review_rows,
            1
        );
        assert_eq!(
            summary
                .first_release_ready_protocol_evidence_package_handoff_execution_action_evidence_review_row_key,
            summary
                .first_protocol_evidence_package_handoff_execution_action_evidence_review_row_key
        );
        assert_eq!(
            summary
                .first_required_protocol_evidence_package_handoff_execution_action_evidence_review_row_key,
            None
        );
        assert_eq!(
            summary.next_protocol_evidence_package_handoff_execution_action_evidence_review_row_key,
            None
        );
        assert_eq!(
            summary.next_protocol_evidence_package_handoff_execution_action_evidence_row_key,
            None
        );
        assert!(summary.protocol_evidence_package_handoff_execution_action_evidence_ready);
        assert!(summary.protocol_evidence_package_handoff_execution_action_evidence_reviews_ready);
        assert!(summary.has_release_handoff_reviews());
        assert!(summary.has_release_ready_reviews());
        assert!(!summary.requires_attention());
        assert!(summary
            .ready_for_release_ticket_handoff_protocol_evidence_package_handoff_execution_action_evidence_reviews());
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_action_evidence_summary_surfaces_attention(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let summary =
            mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_action_evidence_summary(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );

        assert_eq!(
            summary.total_protocol_evidence_package_handoff_execution_action_rows,
            3
        );
        assert_eq!(
            summary.total_protocol_evidence_package_handoff_execution_action_evidence_rows,
            3
        );
        assert_eq!(
            summary.required_protocol_evidence_package_handoff_execution_action_evidence_rows,
            3
        );
        assert_eq!(
            summary
                .release_blocking_protocol_evidence_package_handoff_execution_action_evidence_rows,
            3
        );
        assert_eq!(
            summary.repair_protocol_evidence_package_handoff_execution_action_evidence_rows,
            3
        );
        assert_eq!(
            summary.review_protocol_evidence_package_handoff_execution_action_evidence_rows,
            0
        );
        assert_eq!(
            summary.operator_protocol_evidence_package_handoff_execution_action_evidence_rows,
            3
        );
        assert_eq!(
            summary.dispatch_protocol_evidence_package_handoff_execution_action_evidence_rows,
            3
        );
        assert_eq!(
            summary.execution_protocol_evidence_package_handoff_execution_action_evidence_rows,
            3
        );
        assert_eq!(
            summary
                .lineage_complete_protocol_evidence_package_handoff_execution_action_evidence_rows,
            3
        );
        assert_eq!(
            summary
                .release_handoff_protocol_evidence_package_handoff_execution_action_evidence_rows,
            0
        );
        assert_eq!(
            summary.release_ready_protocol_evidence_package_handoff_execution_action_evidence_rows,
            0
        );
        assert_eq!(
            summary.first_protocol_evidence_package_handoff_execution_action_evidence_row_key,
            Some(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-protocol-evidence-package-handoff-execution-action-evidence-01-repair_handoff-Zigbee"
                    .to_string()
            )
        );
        assert_eq!(
            summary
                .first_required_protocol_evidence_package_handoff_execution_action_evidence_row_key,
            summary.first_protocol_evidence_package_handoff_execution_action_evidence_row_key
        );
        assert_eq!(
            summary
                .first_repair_protocol_evidence_package_handoff_execution_action_evidence_row_key,
            summary.first_protocol_evidence_package_handoff_execution_action_evidence_row_key
        );
        assert_eq!(
            summary
                .first_release_protocol_evidence_package_handoff_execution_action_evidence_row_key,
            None
        );
        assert_eq!(
            summary.next_protocol_evidence_package_handoff_execution_action_evidence_row_key,
            summary.first_protocol_evidence_package_handoff_execution_action_evidence_row_key
        );
        assert_eq!(
            summary.next_protocol_evidence_package_handoff_execution_action_row_key,
            Some(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-protocol-evidence-package-handoff-execution-action-01-repair_handoff-Zigbee"
                    .to_string()
            )
        );
        assert_eq!(
            summary.next_protocol_evidence_package_handoff_execution_slot_key,
            Some(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-protocol-evidence-package-handoff-execution-slot-01-Zigbee"
                    .to_string()
            )
        );
        assert_eq!(
            summary.next_handoff_action_evidence_row_key,
            Some(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-evidence-01-repair_handoff-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(summary.next_protocol, Some(ProtocolFamily::Zigbee));
        assert_eq!(
            summary.next_blocked_substrate_stage,
            Some(IntegrationMeshProtocolSubstrateStage::NetworkSecurity)
        );
        assert_eq!(
            summary.next_handoff_lane,
            Some(IntegrationMeshReleaseDispatchTicketHandoffLane::Review)
        );
        assert!(!summary.protocol_evidence_package_handoff_execution_actions_ready);
        assert!(!summary.protocol_evidence_package_handoff_execution_action_evidence_ready);
        assert!(summary.has_action_evidence_rows());
        assert!(summary.has_required_evidence());
        assert!(summary.has_blockers());
        assert!(summary.needs_operator());
        assert!(summary.requires_dispatch());
        assert!(summary.requires_execution());
        assert!(!summary.has_lineage_gaps());
        assert!(!summary.has_release_handoff_evidence());
        assert!(!summary.has_ready_evidence());
        assert!(summary.requires_attention());
        assert!(!summary
            .ready_for_release_ticket_handoff_protocol_evidence_package_handoff_execution_action_evidence());
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_action_evidence_summary_marks_release_ready(
    ) {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let summary =
            mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_protocol_evidence_package_handoff_execution_action_evidence_summary_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(
            summary.total_protocol_evidence_package_handoff_execution_action_rows,
            3
        );
        assert_eq!(
            summary.total_protocol_evidence_package_handoff_execution_action_evidence_rows,
            3
        );
        assert_eq!(
            summary.required_protocol_evidence_package_handoff_execution_action_evidence_rows,
            0
        );
        assert_eq!(
            summary
                .release_blocking_protocol_evidence_package_handoff_execution_action_evidence_rows,
            0
        );
        assert_eq!(
            summary.repair_protocol_evidence_package_handoff_execution_action_evidence_rows,
            0
        );
        assert_eq!(
            summary
                .release_handoff_protocol_evidence_package_handoff_execution_action_evidence_rows,
            3
        );
        assert_eq!(
            summary.release_ready_protocol_evidence_package_handoff_execution_action_evidence_rows,
            3
        );
        assert_eq!(
            summary
                .first_release_protocol_evidence_package_handoff_execution_action_evidence_row_key,
            summary.first_protocol_evidence_package_handoff_execution_action_evidence_row_key
        );
        assert_eq!(
            summary
                .first_required_protocol_evidence_package_handoff_execution_action_evidence_row_key,
            None
        );
        assert_eq!(
            summary.next_protocol_evidence_package_handoff_execution_action_evidence_row_key,
            None
        );
        assert_eq!(
            summary.next_protocol_evidence_package_handoff_execution_action_row_key,
            None
        );
        assert_eq!(summary.next_protocol_evidence_packet_key, None);
        assert_eq!(summary.next_handoff_action_evidence_row_key, None);
        assert_eq!(summary.next_protocol, None);
        assert_eq!(summary.next_blocked_substrate_stage, None);
        assert_eq!(summary.next_handoff_lane, None);
        assert!(summary.protocol_evidence_package_handoff_execution_actions_ready);
        assert!(summary.protocol_evidence_package_handoff_execution_action_evidence_ready);
        assert!(summary.has_action_evidence_rows());
        assert!(!summary.has_required_evidence());
        assert!(!summary.has_blockers());
        assert!(!summary.needs_operator());
        assert!(!summary.has_review_work());
        assert!(!summary.requires_dispatch());
        assert!(!summary.requires_execution());
        assert!(!summary.has_lineage_gaps());
        assert!(summary.has_release_handoff_evidence());
        assert!(summary.has_ready_evidence());
        assert!(!summary.requires_attention());
        assert!(summary
            .ready_for_release_ticket_handoff_protocol_evidence_package_handoff_execution_action_evidence());
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_summary_surfaces_attention(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let summary =
            mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_summary(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );

        assert_eq!(
            summary.total_readiness_evidence_review_disposition_action_readiness_execution_handoff_rows,
            5
        );
        assert_eq!(
            summary
                .total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows,
            5
        );
        assert_eq!(
            summary
                .required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows,
            5
        );
        assert_eq!(
            summary
                .release_blocking_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows,
            3
        );
        assert_eq!(
            summary
                .repair_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows,
            3
        );
        assert_eq!(
            summary
                .review_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows,
            2
        );
        assert_eq!(
            summary
                .execution_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows,
            5
        );
        assert_eq!(
            summary
                .release_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows,
            0
        );
        assert_eq!(
            summary.first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_row_key,
            Some(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-01-repair_handoff-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_row_key,
            summary
                .first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_row_key
        );
        assert_eq!(
            summary.first_repair_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_row_key,
            summary
                .first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_row_key
        );
        assert_eq!(
            summary.first_review_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_row_key,
            Some(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-02-review_handoff-evidence_remediation"
                    .to_string()
            )
        );
        assert_eq!(
            summary.next_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_row_key,
            summary
                .first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_row_key
        );
        assert_eq!(
            summary.next_readiness_evidence_review_disposition_action_readiness_execution_handoff_row_key,
            summary
                .first_readiness_evidence_review_disposition_action_readiness_execution_handoff_row_key
        );
        assert_eq!(summary.next_work_order_key, summary.first_work_order_key);
        assert_eq!(summary.next_ticket_key, summary.first_ticket_key);
        assert_eq!(
            summary.next_handoff_lane,
            Some(IntegrationMeshReleaseDispatchTicketHandoffLane::Repair)
        );
        assert!(!summary
            .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoffs_ready);
        assert!(!summary
            .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_actions_ready);
        assert!(!summary
            .ready_for_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_actions());
        assert!(summary.has_handoff_action_rows());
        assert!(summary.has_required_actions());
        assert!(summary.has_blockers());
        assert!(summary.has_review_work());
        assert!(summary.needs_operator());
        assert!(summary.requires_dispatch());
        assert!(summary.requires_execution());
        assert!(!summary.has_lineage_gaps());
        assert!(!summary.has_release_handoff_actions());
        assert!(summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_summary_marks_release_ready(
    ) {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let summary =
            mesh_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_summary_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(
            summary
                .total_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows,
            5
        );
        assert_eq!(
            summary
                .required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows,
            0
        );
        assert_eq!(
            summary
                .release_blocking_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows,
            0
        );
        assert_eq!(
            summary
                .repair_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows,
            0
        );
        assert_eq!(
            summary
                .review_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows,
            0
        );
        assert_eq!(
            summary
                .release_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows,
            5
        );
        assert_eq!(
            summary
                .release_ready_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_rows,
            5
        );
        assert_eq!(
            summary.first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_row_key,
            Some(
                "release-ticket-handoff-readiness-evidence-review-disposition-action-readiness-handoff-action-01-release_handoff-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_required_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_row_key,
            None
        );
        assert_eq!(
            summary.first_repair_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_row_key,
            None
        );
        assert_eq!(
            summary.first_release_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_row_key,
            summary
                .first_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_row_key
        );
        assert_eq!(
            summary.next_readiness_evidence_review_disposition_action_readiness_execution_handoff_action_row_key,
            None
        );
        assert_eq!(summary.next_work_order_key, None);
        assert_eq!(summary.next_ticket_key, None);
        assert!(summary
            .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoffs_ready);
        assert!(summary
            .release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_actions_ready);
        assert!(summary
            .ready_for_release_ticket_handoff_readiness_evidence_review_disposition_action_readiness_execution_handoff_actions());
        assert!(summary.has_handoff_action_rows());
        assert!(!summary.has_required_actions());
        assert!(!summary.has_blockers());
        assert!(!summary.has_review_work());
        assert!(!summary.needs_operator());
        assert!(!summary.requires_dispatch());
        assert!(!summary.requires_execution());
        assert!(!summary.has_lineage_gaps());
        assert!(summary.has_release_handoff_actions());
        assert!(!summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_rows_surface_action_lineage(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let action_rows =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_rows(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );
        let evidence_rows =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_rows(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );
        let summary =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );

        assert_eq!(evidence_rows.len(), 5);
        assert_eq!(summary.total_slot_clearance_action_evidence_rows, 5);
        assert_eq!(summary.repair_evidence, 3);
        assert_eq!(summary.operator_handoff_evidence, 0);
        assert_eq!(summary.review_evidence, 2);
        assert_eq!(summary.lineage_evidence, 0);
        assert_eq!(summary.release_handoff_evidence, 0);
        assert_eq!(summary.required_evidence, 5);
        assert_eq!(summary.release_blocking_evidence, 3);
        assert_eq!(summary.operator_required_evidence, 4);
        assert_eq!(summary.review_required_evidence, 2);
        assert_eq!(summary.dispatch_required_evidence, 5);
        assert_eq!(summary.execution_required_evidence, 5);
        assert_eq!(summary.lineage_complete_evidence, 5);
        assert_eq!(summary.ready_evidence, 0);
        assert_eq!(
            summary.first_slot_clearance_action_evidence_key,
            Some(
                "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-01-repair_evidence-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_required_evidence_key,
            summary.first_slot_clearance_action_evidence_key
        );
        assert_eq!(
            summary.first_repair_evidence_key,
            summary.first_slot_clearance_action_evidence_key
        );
        assert_eq!(summary.first_operator_handoff_evidence_key, None);
        assert_eq!(
            summary.first_review_evidence_key,
            Some(
                "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-02-review_evidence-evidence_remediation"
                    .to_string()
            )
        );
        assert_eq!(summary.first_lineage_evidence_key, None);
        assert_eq!(summary.first_release_handoff_evidence_key, None);
        assert_eq!(
            summary.first_slot_clearance_action_key,
            Some(
                "release-ticket-handoff-disposition-action-slot-clearance-action-01-schedule_repair-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_slot_clearance_action_evidence_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceKind::RepairEvidence
            )
        );
        assert_eq!(
            summary.first_slot_clearance_action_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionKind::ScheduleRepair
            )
        );
        assert!(
            !summary
                .release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_ready
        );
        assert!(summary.has_slot_clearance_action_evidence_rows());
        assert!(summary.has_required_evidence());
        assert!(summary.has_blockers());
        assert!(summary.needs_operator());
        assert!(summary.needs_review());
        assert!(!summary.has_lineage_gaps());
        assert!(!summary.has_release_handoff_evidence());
        assert!(summary.has_complete_lineage());
        assert!(summary.requires_attention());

        let first = &evidence_rows[0];
        assert_eq!(first.sequence, 1);
        assert_eq!(
            first.slot_clearance_action_sequence,
            action_rows[0].sequence
        );
        assert!(first.is_repair_evidence());
        assert!(first.requires_action());
        assert!(first.blocks_release());
        assert!(first.needs_operator());
        assert!(!first.needs_review());
        assert!(first.has_lineage());
        assert!(!first.ready_to_release_handoff());
        assert!(first.requires_attention());

        let mut lineage_action = action_rows[0].clone();
        lineage_action.slot_clearance_action_kind =
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionKind::RestoreLineage;
        lineage_action.slot_key.clear();
        lineage_action.lineage_complete = false;
        lineage_action.action_ready = false;
        let lineage_evidence =
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceRow::from_slot_clearance_action_row(
                1,
                &lineage_action,
            );

        assert_eq!(
            lineage_evidence.slot_clearance_action_evidence_key,
            "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-01-lineage_evidence-substrate_actions"
        );
        assert!(lineage_evidence.is_lineage_evidence());
        assert!(!lineage_evidence.has_lineage());
        assert!(!lineage_evidence.ready_to_release_handoff());
        assert!(lineage_evidence.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_rows_mark_release_handoff(
    ) {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let evidence_rows =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_rows_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );
        let summary =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_summary_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(evidence_rows.len(), 5);
        assert_eq!(summary.total_slot_clearance_action_evidence_rows, 5);
        assert_eq!(summary.repair_evidence, 0);
        assert_eq!(summary.operator_handoff_evidence, 0);
        assert_eq!(summary.review_evidence, 0);
        assert_eq!(summary.lineage_evidence, 0);
        assert_eq!(summary.release_handoff_evidence, 5);
        assert_eq!(summary.required_evidence, 0);
        assert_eq!(summary.release_blocking_evidence, 0);
        assert_eq!(summary.operator_required_evidence, 0);
        assert_eq!(summary.review_required_evidence, 0);
        assert_eq!(summary.dispatch_required_evidence, 0);
        assert_eq!(summary.execution_required_evidence, 0);
        assert_eq!(summary.lineage_complete_evidence, 5);
        assert_eq!(summary.ready_evidence, 5);
        assert_eq!(
            summary.first_slot_clearance_action_evidence_key,
            Some(
                "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-01-release_handoff_evidence-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(summary.first_required_evidence_key, None);
        assert_eq!(summary.first_repair_evidence_key, None);
        assert_eq!(summary.first_operator_handoff_evidence_key, None);
        assert_eq!(summary.first_review_evidence_key, None);
        assert_eq!(summary.first_lineage_evidence_key, None);
        assert_eq!(
            summary.first_release_handoff_evidence_key,
            summary.first_slot_clearance_action_evidence_key
        );
        assert_eq!(
            summary.first_slot_clearance_action_key,
            Some(
                "release-ticket-handoff-disposition-action-slot-clearance-action-01-release_handoff-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_slot_clearance_action_evidence_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceKind::ReleaseHandoffEvidence
            )
        );
        assert_eq!(
            summary.first_slot_clearance_action_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionKind::ReleaseHandoff
            )
        );
        assert!(
            summary
                .release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_ready
        );
        assert!(summary.has_slot_clearance_action_evidence_rows());
        assert!(!summary.has_required_evidence());
        assert!(!summary.has_blockers());
        assert!(!summary.needs_operator());
        assert!(!summary.needs_review());
        assert!(!summary.has_lineage_gaps());
        assert!(summary.has_release_handoff_evidence());
        assert!(summary.has_complete_lineage());
        assert!(!summary.requires_attention());
        assert!(evidence_rows.iter().all(
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceRow::ready_to_release_handoff
        ));
        assert!(evidence_rows.iter().all(
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceRow::is_release_handoff_evidence
        ));
    }

    #[test]
    fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_rows_surface_evidence_attention(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let evidence_rows =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_rows(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );
        let review_rows =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_rows(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );
        let summary =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );

        assert_eq!(review_rows.len(), 5);
        assert_eq!(summary.total_slot_clearance_action_evidence_reviews, 5);
        assert_eq!(summary.required_reviews, 5);
        assert_eq!(summary.blocking_reviews, 3);
        assert_eq!(summary.operator_required_reviews, 4);
        assert_eq!(summary.review_required_reviews, 2);
        assert_eq!(summary.dispatch_required_reviews, 5);
        assert_eq!(summary.execution_required_reviews, 5);
        assert_eq!(summary.lineage_gap_reviews, 0);
        assert_eq!(summary.release_ready_reviews, 0);
        assert_eq!(summary.lineage_complete_reviews, 5);
        assert_eq!(summary.release_handoff_evidence_reviews, 0);
        assert_eq!(
            summary.first_slot_clearance_action_evidence_review_key,
            Some(
                "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-review-01-blocker_review-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_required_review_key,
            summary.first_slot_clearance_action_evidence_review_key
        );
        assert_eq!(
            summary.first_blocking_review_key,
            summary.first_slot_clearance_action_evidence_review_key
        );
        assert_eq!(summary.first_operator_review_key, Some(
            "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-review-01-blocker_review-substrate_actions".to_string()
        ));
        assert_eq!(
            summary.first_review_gate_key,
            Some(
                "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-review-02-review_gate-evidence_remediation"
                    .to_string()
            )
        );
        assert_eq!(summary.first_lineage_gap_review_key, None);
        assert_eq!(summary.first_release_ready_review_key, None);
        assert_eq!(
            summary.first_slot_clearance_action_evidence_key,
            Some(
                "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-01-repair_evidence-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_review_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::BlockerReview
            )
        );
        assert_eq!(
            summary.first_slot_clearance_action_evidence_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceKind::RepairEvidence
            )
        );
        assert!(
            !summary
                .release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_reviews_ready
        );
        assert!(summary.has_slot_clearance_action_evidence_reviews());
        assert!(summary.has_required_reviews());
        assert!(summary.has_blockers());
        assert!(summary.needs_operator());
        assert!(summary.needs_review());
        assert!(!summary.has_lineage_gaps());
        assert!(!summary.has_release_ready_reviews());
        assert!(summary.has_complete_lineage());
        assert!(summary.requires_attention());

        let first = &review_rows[0];
        assert_eq!(first.sequence, 1);
        assert_eq!(
            first.slot_clearance_action_evidence_sequence,
            evidence_rows[0].sequence
        );
        assert_eq!(
            first.review_kind,
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::BlockerReview
        );
        assert!(first.requires_action());
        assert!(first.blocks_release());
        assert!(first.needs_operator());
        assert!(!first.needs_review());
        assert!(first.has_lineage());
        assert!(!first.is_release_ready());
        assert!(first.requires_attention());

        let mut lineage_evidence = evidence_rows[0].clone();
        lineage_evidence.slot_clearance_action_evidence_kind =
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceKind::LineageEvidence;
        lineage_evidence.slot_key.clear();
        lineage_evidence.lineage_complete = false;
        lineage_evidence.action_evidence_ready = false;
        let lineage_review =
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReview::from_slot_clearance_action_evidence_row(
                1,
                &lineage_evidence,
            );

        assert_eq!(
            lineage_review.slot_clearance_action_evidence_review_key,
            "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-review-01-lineage_gap-substrate_actions"
        );
        assert_eq!(
            lineage_review.review_kind,
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::LineageGap
        );
        assert!(!lineage_review.has_lineage());
        assert!(!lineage_review.is_release_ready());
        assert!(lineage_review.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_rows_mark_release_handoff(
    ) {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let review_rows =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_rows_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );
        let summary =
            mesh_release_ticket_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_review_summary_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(review_rows.len(), 5);
        assert_eq!(summary.total_slot_clearance_action_evidence_reviews, 5);
        assert_eq!(summary.required_reviews, 0);
        assert_eq!(summary.blocking_reviews, 0);
        assert_eq!(summary.operator_required_reviews, 0);
        assert_eq!(summary.review_required_reviews, 0);
        assert_eq!(summary.dispatch_required_reviews, 0);
        assert_eq!(summary.execution_required_reviews, 0);
        assert_eq!(summary.lineage_gap_reviews, 0);
        assert_eq!(summary.release_ready_reviews, 5);
        assert_eq!(summary.lineage_complete_reviews, 5);
        assert_eq!(summary.release_handoff_evidence_reviews, 5);
        assert_eq!(
            summary.first_slot_clearance_action_evidence_review_key,
            Some(
                "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-review-01-release_ready-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(summary.first_required_review_key, None);
        assert_eq!(summary.first_blocking_review_key, None);
        assert_eq!(summary.first_operator_review_key, None);
        assert_eq!(summary.first_review_gate_key, None);
        assert_eq!(summary.first_lineage_gap_review_key, None);
        assert_eq!(
            summary.first_release_ready_review_key,
            summary.first_slot_clearance_action_evidence_review_key
        );
        assert_eq!(
            summary.first_slot_clearance_action_evidence_key,
            Some(
                "release-ticket-handoff-disposition-action-slot-clearance-action-evidence-01-release_handoff_evidence-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_review_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::ReleaseReady
            )
        );
        assert_eq!(
            summary.first_slot_clearance_action_evidence_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceKind::ReleaseHandoffEvidence
            )
        );
        assert!(
            summary
                .release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_evidence_reviews_ready
        );
        assert!(summary.has_slot_clearance_action_evidence_reviews());
        assert!(!summary.has_required_reviews());
        assert!(!summary.has_blockers());
        assert!(!summary.needs_operator());
        assert!(!summary.needs_review());
        assert!(!summary.has_lineage_gaps());
        assert!(summary.has_release_ready_reviews());
        assert!(summary.has_complete_lineage());
        assert!(!summary.requires_attention());
        assert!(review_rows.iter().all(
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReview::is_release_ready
        ));
        assert!(review_rows.iter().all(
            IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionEvidenceReview::is_release_handoff_evidence
        ));
    }

    #[test]
    fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_readiness_surfaces_next_evidence(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let summary =
            mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_readiness_summary(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );

        assert_eq!(summary.total_clearance_actions, 5);
        assert_eq!(summary.total_action_evidence, 5);
        assert_eq!(summary.blocking_evidence, 3);
        assert_eq!(summary.operator_required_evidence, 4);
        assert_eq!(summary.review_required_evidence, 2);
        assert_eq!(summary.dispatch_required_evidence, 5);
        assert_eq!(summary.execution_required_evidence, 5);
        assert_eq!(summary.release_evidence, 0);
        assert_eq!(summary.lineage_complete_evidence, 5);
        assert_eq!(
            summary.first_action_evidence_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-evidence-01-blocker_evidence-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_clearance_action_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-01-repair_release_blocker-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_blocking_evidence_key,
            summary.first_action_evidence_key
        );
        assert_eq!(
            summary.next_action_evidence_key,
            summary.first_action_evidence_key
        );
        assert_eq!(
            summary.next_clearance_action_key,
            summary.first_clearance_action_key
        );
        assert_eq!(
            summary.next_work_order_key,
            Some(
                "release-ticket-handoff-execution-work-order-01-repair-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.next_ticket_key,
            Some("release-dispatch-ticket-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_check_kind,
            Some(IntegrationMeshReleaseReadinessCheckKind::SubstrateActions)
        );
        assert_eq!(
            summary.next_check_status,
            Some(IntegrationMeshReleaseReadinessStatus::Blocked)
        );
        assert_eq!(
            summary.next_handoff_lane,
            Some(IntegrationMeshReleaseDispatchTicketHandoffLane::Repair)
        );
        assert!(
            !summary
                .release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_ready
        );
        assert!(!summary.release_ticket_handoff_guardrail_audit_clearance_action_evidence_ready);
        assert!(
            !summary.ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence()
        );
        assert!(summary.has_action_evidence());
        assert!(summary.has_blockers());
        assert!(summary.has_review_work());
        assert!(summary.needs_operator());
        assert!(summary.requires_execution());
        assert!(summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_readiness_marks_release_ready(
    ) {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let summary =
            mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_readiness_summary_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(summary.total_clearance_actions, 5);
        assert_eq!(summary.total_action_evidence, 5);
        assert_eq!(summary.blocking_evidence, 0);
        assert_eq!(summary.operator_required_evidence, 0);
        assert_eq!(summary.review_required_evidence, 0);
        assert_eq!(summary.dispatch_required_evidence, 0);
        assert_eq!(summary.execution_required_evidence, 0);
        assert_eq!(summary.release_evidence, 5);
        assert_eq!(summary.lineage_complete_evidence, 5);
        assert_eq!(
            summary.first_action_evidence_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-evidence-01-release_evidence-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_release_evidence_key,
            summary.first_action_evidence_key
        );
        assert_eq!(summary.first_blocking_evidence_key, None);
        assert_eq!(summary.first_operator_evidence_key, None);
        assert_eq!(summary.first_review_evidence_key, None);
        assert_eq!(
            summary.first_evidence_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceKind::ReleaseEvidence
            )
        );
        assert_eq!(
            summary.first_clearance_action_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionKind::ReleaseClearance
            )
        );
        assert_eq!(
            summary.first_clearance_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind::Cleared
            )
        );
        assert_eq!(summary.next_action_evidence_key, None);
        assert_eq!(summary.next_clearance_action_key, None);
        assert_eq!(summary.next_work_order_key, None);
        assert_eq!(summary.next_ticket_key, None);
        assert!(
            summary.release_handoff_execution_work_order_guardrail_audit_clearance_actions_ready
        );
        assert!(summary.release_ticket_handoff_guardrail_audit_clearance_actions_ready);
        assert!(
            summary
                .release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_ready
        );
        assert!(summary.release_ticket_handoff_guardrail_audit_clearance_action_evidence_ready);
        assert!(
            summary.ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence()
        );
        assert!(summary.has_action_evidence());
        assert!(!summary.has_blockers());
        assert!(!summary.has_review_work());
        assert!(!summary.needs_operator());
        assert!(!summary.requires_execution());
        assert!(!summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_readiness_surfaces_review_attention(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let summary =
            mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_readiness_summary(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );

        assert_eq!(summary.total_clearance_actions, 5);
        assert_eq!(summary.total_action_evidence, 5);
        assert_eq!(summary.total_evidence_reviews, 5);
        assert_eq!(summary.blocking_reviews, 3);
        assert_eq!(summary.operator_required_reviews, 4);
        assert_eq!(summary.review_required_reviews, 2);
        assert_eq!(summary.dispatch_required_reviews, 5);
        assert_eq!(summary.execution_required_reviews, 5);
        assert_eq!(summary.lineage_gap_reviews, 0);
        assert_eq!(summary.release_ready_reviews, 0);
        assert_eq!(summary.lineage_complete_reviews, 5);
        assert_eq!(
            summary.first_evidence_review_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-evidence-review-01-blocker_review-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_blocking_review_key,
            summary.first_evidence_review_key
        );
        assert_eq!(
            summary.first_operator_review_key,
            summary.first_evidence_review_key
        );
        assert_eq!(
            summary.first_review_gate_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-evidence-review-02-review_gate-evidence_remediation"
                    .to_string()
            )
        );
        assert_eq!(summary.first_lineage_gap_review_key, None);
        assert_eq!(summary.first_release_ready_review_key, None);
        assert_eq!(
            summary.first_action_evidence_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-evidence-01-blocker_evidence-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_clearance_action_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-01-repair_release_blocker-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_clearance_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-01-release_blocked-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_work_order_key,
            Some(
                "release-ticket-handoff-execution-work-order-01-repair-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_ticket_key,
            Some("release-dispatch-ticket-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.first_review_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::BlockerReview
            )
        );
        assert_eq!(
            summary.first_evidence_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceKind::BlockerEvidence
            )
        );
        assert_eq!(
            summary.first_clearance_action_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionKind::RepairReleaseBlocker
            )
        );
        assert_eq!(
            summary.first_clearance_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind::ReleaseBlocked
            )
        );
        assert_eq!(
            summary.first_handoff_lane,
            Some(IntegrationMeshReleaseDispatchTicketHandoffLane::Repair)
        );
        assert_eq!(
            summary.first_check_kind,
            Some(IntegrationMeshReleaseReadinessCheckKind::SubstrateActions)
        );
        assert_eq!(
            summary.first_check_status,
            Some(IntegrationMeshReleaseReadinessStatus::Blocked)
        );
        assert_eq!(
            summary.next_evidence_review_key,
            summary.first_evidence_review_key
        );
        assert_eq!(
            summary.next_action_evidence_key,
            summary.first_action_evidence_key
        );
        assert_eq!(
            summary.next_clearance_action_key,
            summary.first_clearance_action_key
        );
        assert_eq!(summary.next_work_order_key, summary.first_work_order_key);
        assert_eq!(summary.next_ticket_key, summary.first_ticket_key);
        assert_eq!(summary.next_check_kind, summary.first_check_kind);
        assert_eq!(summary.next_check_status, summary.first_check_status);
        assert_eq!(summary.next_handoff_lane, summary.first_handoff_lane);
        assert!(!summary.release_ticket_handoff_guardrail_audit_clearance_action_evidence_ready);
        assert!(
            !summary
                .release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_reviews_ready
        );
        assert!(
            !summary.release_ticket_handoff_guardrail_audit_clearance_action_evidence_reviews_ready
        );
        assert!(!summary
            .ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_reviews());
        assert!(summary.has_evidence_reviews());
        assert!(summary.has_blockers());
        assert!(summary.has_review_work());
        assert!(summary.needs_operator());
        assert!(!summary.has_lineage_gaps());
        assert!(summary.requires_execution());
        assert!(summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_readiness_marks_release_ready(
    ) {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let summary =
            mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_readiness_summary_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(summary.total_clearance_actions, 5);
        assert_eq!(summary.total_action_evidence, 5);
        assert_eq!(summary.total_evidence_reviews, 5);
        assert_eq!(summary.blocking_reviews, 0);
        assert_eq!(summary.operator_required_reviews, 0);
        assert_eq!(summary.review_required_reviews, 0);
        assert_eq!(summary.dispatch_required_reviews, 0);
        assert_eq!(summary.execution_required_reviews, 0);
        assert_eq!(summary.lineage_gap_reviews, 0);
        assert_eq!(summary.release_ready_reviews, 5);
        assert_eq!(summary.lineage_complete_reviews, 5);
        assert_eq!(
            summary.first_evidence_review_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-evidence-review-01-release_ready-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(summary.first_blocking_review_key, None);
        assert_eq!(summary.first_operator_review_key, None);
        assert_eq!(summary.first_review_gate_key, None);
        assert_eq!(summary.first_lineage_gap_review_key, None);
        assert_eq!(
            summary.first_release_ready_review_key,
            summary.first_evidence_review_key
        );
        assert_eq!(
            summary.first_action_evidence_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-evidence-01-release_evidence-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_clearance_action_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-01-release_clearance-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_review_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::ReleaseReady
            )
        );
        assert_eq!(
            summary.first_evidence_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceKind::ReleaseEvidence
            )
        );
        assert_eq!(
            summary.first_clearance_action_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionKind::ReleaseClearance
            )
        );
        assert_eq!(
            summary.first_clearance_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind::Cleared
            )
        );
        assert_eq!(summary.next_evidence_review_key, None);
        assert_eq!(summary.next_action_evidence_key, None);
        assert_eq!(summary.next_clearance_action_key, None);
        assert_eq!(summary.next_work_order_key, None);
        assert_eq!(summary.next_ticket_key, None);
        assert_eq!(summary.next_check_kind, None);
        assert_eq!(summary.next_check_status, None);
        assert_eq!(summary.next_handoff_lane, None);
        assert!(summary.release_ticket_handoff_guardrail_audit_clearance_action_evidence_ready);
        assert!(
            summary
                .release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_reviews_ready
        );
        assert!(
            summary.release_ticket_handoff_guardrail_audit_clearance_action_evidence_reviews_ready
        );
        assert!(summary
            .ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_reviews());
        assert!(summary.has_evidence_reviews());
        assert!(!summary.has_blockers());
        assert!(!summary.has_review_work());
        assert!(!summary.needs_operator());
        assert!(!summary.has_lineage_gaps());
        assert!(!summary.requires_execution());
        assert!(!summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_readiness_surfaces_disposition_attention(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let summary =
            mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_readiness_summary(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );

        assert_eq!(summary.total_clearance_actions, 5);
        assert_eq!(summary.total_action_evidence, 5);
        assert_eq!(summary.total_evidence_reviews, 5);
        assert_eq!(summary.total_review_dispositions, 5);
        assert_eq!(summary.release_blocking_dispositions, 3);
        assert_eq!(summary.operator_required_dispositions, 4);
        assert_eq!(summary.review_required_dispositions, 2);
        assert_eq!(summary.dispatch_required_dispositions, 5);
        assert_eq!(summary.execution_required_dispositions, 5);
        assert_eq!(summary.repair_required_dispositions, 3);
        assert_eq!(summary.operator_handoff_dispositions, 0);
        assert_eq!(summary.review_gate_dispositions, 2);
        assert_eq!(summary.lineage_gap_dispositions, 0);
        assert_eq!(summary.release_ready_dispositions, 0);
        assert_eq!(summary.lineage_complete_dispositions, 5);
        assert_eq!(
            summary.first_review_disposition_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-evidence-review-disposition-01-repair_release_blocker-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_repair_required_disposition_key,
            summary.first_review_disposition_key
        );
        assert_eq!(summary.first_operator_handoff_disposition_key, None);
        assert_eq!(
            summary.first_review_gate_disposition_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-evidence-review-disposition-02-review_required-evidence_remediation"
                    .to_string()
            )
        );
        assert_eq!(summary.first_lineage_gap_disposition_key, None);
        assert_eq!(summary.first_release_ready_disposition_key, None);
        assert_eq!(
            summary.first_evidence_review_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-evidence-review-01-blocker_review-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_action_evidence_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-evidence-01-blocker_evidence-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_clearance_action_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-01-repair_release_blocker-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_clearance_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-01-release_blocked-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_work_order_key,
            Some(
                "release-ticket-handoff-execution-work-order-01-repair-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_ticket_key,
            Some("release-dispatch-ticket-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.first_disposition_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind::RepairReleaseBlocker
            )
        );
        assert_eq!(
            summary.first_review_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::BlockerReview
            )
        );
        assert_eq!(
            summary.first_evidence_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceKind::BlockerEvidence
            )
        );
        assert_eq!(
            summary.first_clearance_action_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionKind::RepairReleaseBlocker
            )
        );
        assert_eq!(
            summary.first_clearance_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind::ReleaseBlocked
            )
        );
        assert_eq!(
            summary.first_handoff_lane,
            Some(IntegrationMeshReleaseDispatchTicketHandoffLane::Repair)
        );
        assert_eq!(
            summary.first_check_kind,
            Some(IntegrationMeshReleaseReadinessCheckKind::SubstrateActions)
        );
        assert_eq!(
            summary.first_check_status,
            Some(IntegrationMeshReleaseReadinessStatus::Blocked)
        );
        assert_eq!(
            summary.next_review_disposition_key,
            summary.first_review_disposition_key
        );
        assert_eq!(
            summary.next_evidence_review_key,
            summary.first_evidence_review_key
        );
        assert_eq!(
            summary.next_action_evidence_key,
            summary.first_action_evidence_key
        );
        assert_eq!(
            summary.next_clearance_action_key,
            summary.first_clearance_action_key
        );
        assert_eq!(summary.next_work_order_key, summary.first_work_order_key);
        assert_eq!(summary.next_ticket_key, summary.first_ticket_key);
        assert_eq!(summary.next_check_kind, summary.first_check_kind);
        assert_eq!(summary.next_check_status, summary.first_check_status);
        assert_eq!(summary.next_handoff_lane, summary.first_handoff_lane);
        assert!(
            !summary.release_ticket_handoff_guardrail_audit_clearance_action_evidence_reviews_ready
        );
        assert!(
            !summary
                .release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_dispositions_ready
        );
        assert!(!summary
            .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_dispositions_ready);
        assert!(!summary
            .ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_dispositions());
        assert!(summary.has_review_dispositions());
        assert!(summary.has_blockers());
        assert!(summary.has_review_work());
        assert!(summary.needs_operator());
        assert!(!summary.has_lineage_gaps());
        assert!(summary.requires_execution());
        assert!(summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_readiness_marks_release_ready(
    ) {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let summary =
            mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_readiness_summary_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(summary.total_clearance_actions, 5);
        assert_eq!(summary.total_action_evidence, 5);
        assert_eq!(summary.total_evidence_reviews, 5);
        assert_eq!(summary.total_review_dispositions, 5);
        assert_eq!(summary.release_blocking_dispositions, 0);
        assert_eq!(summary.operator_required_dispositions, 0);
        assert_eq!(summary.review_required_dispositions, 0);
        assert_eq!(summary.dispatch_required_dispositions, 0);
        assert_eq!(summary.execution_required_dispositions, 0);
        assert_eq!(summary.repair_required_dispositions, 0);
        assert_eq!(summary.operator_handoff_dispositions, 0);
        assert_eq!(summary.review_gate_dispositions, 0);
        assert_eq!(summary.lineage_gap_dispositions, 0);
        assert_eq!(summary.release_ready_dispositions, 5);
        assert_eq!(summary.lineage_complete_dispositions, 5);
        assert_eq!(
            summary.first_review_disposition_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-evidence-review-disposition-01-release_ready-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(summary.first_repair_required_disposition_key, None);
        assert_eq!(summary.first_operator_handoff_disposition_key, None);
        assert_eq!(summary.first_review_gate_disposition_key, None);
        assert_eq!(summary.first_lineage_gap_disposition_key, None);
        assert_eq!(
            summary.first_release_ready_disposition_key,
            summary.first_review_disposition_key
        );
        assert_eq!(
            summary.first_evidence_review_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-evidence-review-01-release_ready-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_action_evidence_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-evidence-01-release_evidence-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_clearance_action_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-01-release_clearance-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_disposition_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind::ReleaseReady
            )
        );
        assert_eq!(
            summary.first_review_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::ReleaseReady
            )
        );
        assert_eq!(
            summary.first_evidence_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceKind::ReleaseEvidence
            )
        );
        assert_eq!(
            summary.first_clearance_action_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionKind::ReleaseClearance
            )
        );
        assert_eq!(
            summary.first_clearance_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind::Cleared
            )
        );
        assert_eq!(summary.next_review_disposition_key, None);
        assert_eq!(summary.next_evidence_review_key, None);
        assert_eq!(summary.next_action_evidence_key, None);
        assert_eq!(summary.next_clearance_action_key, None);
        assert_eq!(summary.next_work_order_key, None);
        assert_eq!(summary.next_ticket_key, None);
        assert_eq!(summary.next_check_kind, None);
        assert_eq!(summary.next_check_status, None);
        assert_eq!(summary.next_handoff_lane, None);
        assert!(
            summary.release_ticket_handoff_guardrail_audit_clearance_action_evidence_reviews_ready
        );
        assert!(
            summary
                .release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_dispositions_ready
        );
        assert!(
            summary
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_dispositions_ready
        );
        assert!(summary
            .ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_dispositions());
        assert!(summary.has_review_dispositions());
        assert!(!summary.has_blockers());
        assert!(!summary.has_review_work());
        assert!(!summary.needs_operator());
        assert!(!summary.has_lineage_gaps());
        assert!(!summary.requires_execution());
        assert!(!summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_readiness_surfaces_action_attention(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let summary =
            mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_readiness_summary(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );

        assert_eq!(summary.total_clearance_actions, 5);
        assert_eq!(summary.total_action_evidence, 5);
        assert_eq!(summary.total_evidence_reviews, 5);
        assert_eq!(summary.total_review_dispositions, 5);
        assert_eq!(summary.total_review_disposition_actions, 5);
        assert_eq!(summary.required_disposition_actions, 5);
        assert_eq!(summary.release_blocking_disposition_actions, 3);
        assert_eq!(summary.operator_required_disposition_actions, 4);
        assert_eq!(summary.review_required_disposition_actions, 2);
        assert_eq!(summary.dispatch_required_disposition_actions, 5);
        assert_eq!(summary.execution_required_disposition_actions, 5);
        assert_eq!(summary.repair_disposition_actions, 3);
        assert_eq!(summary.operator_handoff_disposition_actions, 0);
        assert_eq!(summary.review_gate_disposition_actions, 2);
        assert_eq!(summary.lineage_gap_disposition_actions, 0);
        assert_eq!(summary.release_handoff_disposition_actions, 0);
        assert_eq!(summary.lineage_complete_disposition_actions, 5);
        assert_eq!(
            summary.first_review_disposition_action_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-evidence-review-disposition-action-01-schedule_release_blocker_repair-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_required_disposition_action_key,
            summary.first_review_disposition_action_key
        );
        assert_eq!(
            summary.first_repair_disposition_action_key,
            summary.first_review_disposition_action_key
        );
        assert_eq!(summary.first_operator_handoff_disposition_action_key, None);
        assert_eq!(
            summary.first_review_gate_disposition_action_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-evidence-review-disposition-action-02-complete_evidence_review-evidence_remediation"
                    .to_string()
            )
        );
        assert_eq!(summary.first_lineage_gap_disposition_action_key, None);
        assert_eq!(summary.first_release_handoff_disposition_action_key, None);
        assert_eq!(
            summary.first_review_disposition_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-evidence-review-disposition-01-repair_release_blocker-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_disposition_action_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionKind::ScheduleReleaseBlockerRepair
            )
        );
        assert_eq!(
            summary.first_disposition_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind::RepairReleaseBlocker
            )
        );
        assert_eq!(
            summary.first_review_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewKind::BlockerReview
            )
        );
        assert_eq!(
            summary.first_evidence_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceKind::BlockerEvidence
            )
        );
        assert_eq!(
            summary.first_clearance_action_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionKind::RepairReleaseBlocker
            )
        );
        assert_eq!(
            summary.first_clearance_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind::ReleaseBlocked
            )
        );
        assert_eq!(
            summary.first_handoff_lane,
            Some(IntegrationMeshReleaseDispatchTicketHandoffLane::Repair)
        );
        assert_eq!(
            summary.first_check_kind,
            Some(IntegrationMeshReleaseReadinessCheckKind::SubstrateActions)
        );
        assert_eq!(
            summary.first_check_status,
            Some(IntegrationMeshReleaseReadinessStatus::Blocked)
        );
        assert_eq!(
            summary.next_review_disposition_action_key,
            summary.first_review_disposition_action_key
        );
        assert_eq!(
            summary.next_review_disposition_key,
            summary.first_review_disposition_key
        );
        assert_eq!(
            summary.next_evidence_review_key,
            summary.first_evidence_review_key
        );
        assert_eq!(
            summary.next_action_evidence_key,
            summary.first_action_evidence_key
        );
        assert_eq!(
            summary.next_clearance_action_key,
            summary.first_clearance_action_key
        );
        assert_eq!(summary.next_work_order_key, summary.first_work_order_key);
        assert_eq!(summary.next_ticket_key, summary.first_ticket_key);
        assert_eq!(summary.next_check_kind, summary.first_check_kind);
        assert_eq!(summary.next_check_status, summary.first_check_status);
        assert_eq!(summary.next_handoff_lane, summary.first_handoff_lane);
        assert!(
            !summary
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_dispositions_ready
        );
        assert!(
            !summary
                .release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_actions_ready
        );
        assert!(!summary
            .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_actions_ready);
        assert!(!summary
            .ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_actions());
        assert!(summary.has_review_disposition_actions());
        assert!(summary.has_required_actions());
        assert!(summary.has_blockers());
        assert!(summary.has_review_work());
        assert!(summary.needs_operator());
        assert!(!summary.has_lineage_gaps());
        assert!(!summary.has_release_handoff_actions());
        assert!(summary.has_complete_lineage());
        assert!(summary.requires_execution());
        assert!(summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_readiness_marks_release_ready(
    ) {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let summary =
            mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_readiness_summary_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(summary.total_clearance_actions, 5);
        assert_eq!(summary.total_action_evidence, 5);
        assert_eq!(summary.total_evidence_reviews, 5);
        assert_eq!(summary.total_review_dispositions, 5);
        assert_eq!(summary.total_review_disposition_actions, 5);
        assert_eq!(summary.required_disposition_actions, 0);
        assert_eq!(summary.release_blocking_disposition_actions, 0);
        assert_eq!(summary.operator_required_disposition_actions, 0);
        assert_eq!(summary.review_required_disposition_actions, 0);
        assert_eq!(summary.dispatch_required_disposition_actions, 0);
        assert_eq!(summary.execution_required_disposition_actions, 0);
        assert_eq!(summary.repair_disposition_actions, 0);
        assert_eq!(summary.operator_handoff_disposition_actions, 0);
        assert_eq!(summary.review_gate_disposition_actions, 0);
        assert_eq!(summary.lineage_gap_disposition_actions, 0);
        assert_eq!(summary.release_handoff_disposition_actions, 5);
        assert_eq!(summary.lineage_complete_disposition_actions, 5);
        assert_eq!(
            summary.first_review_disposition_action_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-evidence-review-disposition-action-01-release_ticket_handoff-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(summary.first_required_disposition_action_key, None);
        assert_eq!(summary.first_repair_disposition_action_key, None);
        assert_eq!(summary.first_operator_handoff_disposition_action_key, None);
        assert_eq!(summary.first_review_gate_disposition_action_key, None);
        assert_eq!(summary.first_lineage_gap_disposition_action_key, None);
        assert_eq!(
            summary.first_release_handoff_disposition_action_key,
            summary.first_review_disposition_action_key
        );
        assert_eq!(
            summary.first_disposition_action_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionKind::ReleaseTicketHandoff
            )
        );
        assert_eq!(
            summary.first_disposition_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind::ReleaseReady
            )
        );
        assert_eq!(summary.next_review_disposition_action_key, None);
        assert_eq!(summary.next_review_disposition_key, None);
        assert_eq!(summary.next_evidence_review_key, None);
        assert_eq!(summary.next_action_evidence_key, None);
        assert_eq!(summary.next_clearance_action_key, None);
        assert_eq!(summary.next_work_order_key, None);
        assert_eq!(summary.next_ticket_key, None);
        assert_eq!(summary.next_check_kind, None);
        assert_eq!(summary.next_check_status, None);
        assert_eq!(summary.next_handoff_lane, None);
        assert!(
            summary
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_dispositions_ready
        );
        assert!(
            summary
                .release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_actions_ready
        );
        assert!(summary
            .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_actions_ready);
        assert!(summary
            .ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_actions());
        assert!(summary.has_review_disposition_actions());
        assert!(!summary.has_required_actions());
        assert!(!summary.has_blockers());
        assert!(!summary.has_review_work());
        assert!(!summary.needs_operator());
        assert!(!summary.has_lineage_gaps());
        assert!(summary.has_release_handoff_actions());
        assert!(summary.has_complete_lineage());
        assert!(!summary.requires_execution());
        assert!(!summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_readiness_surfaces_slot_attention(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let summary =
            mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_readiness_summary(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );

        assert_eq!(summary.total_review_disposition_actions, 5);
        assert_eq!(summary.total_review_disposition_action_slots, 5);
        assert_eq!(summary.required_slots, 5);
        assert_eq!(summary.release_blocking_slots, 3);
        assert_eq!(summary.operator_required_slots, 4);
        assert_eq!(summary.review_required_slots, 2);
        assert_eq!(summary.dispatch_required_slots, 5);
        assert_eq!(summary.execution_required_slots, 5);
        assert_eq!(summary.repair_slots, 3);
        assert_eq!(summary.operator_handoff_slots, 0);
        assert_eq!(summary.review_gate_slots, 2);
        assert_eq!(summary.lineage_gap_slots, 0);
        assert_eq!(summary.release_handoff_slots, 0);
        assert_eq!(summary.lineage_complete_slots, 5);
        assert_eq!(
            summary.first_review_disposition_action_slot_key,
            Some(
                "release-ticket-handoff-disposition-action-slot-01-schedule_release_blocker_repair-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_required_slot_key,
            summary.first_review_disposition_action_slot_key
        );
        assert_eq!(
            summary.first_repair_slot_key,
            summary.first_review_disposition_action_slot_key
        );
        assert_eq!(summary.first_operator_handoff_slot_key, None);
        assert_eq!(
            summary.first_review_gate_slot_key,
            Some(
                "release-ticket-handoff-disposition-action-slot-02-complete_evidence_review-evidence_remediation"
                    .to_string()
            )
        );
        assert_eq!(summary.first_lineage_gap_slot_key, None);
        assert_eq!(summary.first_release_handoff_slot_key, None);
        assert_eq!(
            summary.first_review_disposition_action_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-evidence-review-disposition-action-01-schedule_release_blocker_repair-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_review_disposition_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-evidence-review-disposition-01-repair_release_blocker-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_disposition_action_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionKind::ScheduleReleaseBlockerRepair
            )
        );
        assert_eq!(
            summary.first_disposition_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind::RepairReleaseBlocker
            )
        );
        assert_eq!(
            summary.first_handoff_lane,
            Some(IntegrationMeshReleaseDispatchTicketHandoffLane::Repair)
        );
        assert_eq!(
            summary.first_check_kind,
            Some(IntegrationMeshReleaseReadinessCheckKind::SubstrateActions)
        );
        assert_eq!(
            summary.first_check_status,
            Some(IntegrationMeshReleaseReadinessStatus::Blocked)
        );
        assert_eq!(
            summary.next_review_disposition_action_slot_key,
            summary.first_review_disposition_action_slot_key
        );
        assert_eq!(
            summary.next_review_disposition_action_key,
            summary.first_review_disposition_action_key
        );
        assert_eq!(
            summary.next_review_disposition_key,
            summary.first_review_disposition_key
        );
        assert_eq!(
            summary.next_work_order_key,
            Some(
                "release-ticket-handoff-execution-work-order-01-repair-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.next_ticket_key,
            Some("release-dispatch-ticket-01-substrate_actions".to_string())
        );
        assert_eq!(summary.next_check_kind, summary.first_check_kind);
        assert_eq!(summary.next_check_status, summary.first_check_status);
        assert_eq!(summary.next_handoff_lane, summary.first_handoff_lane);
        assert!(
            !summary
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_actions_ready
        );
        assert!(
            !summary
                .release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slots_ready
        );
        assert!(!summary
            .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slots_ready);
        assert!(!summary
            .ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slots());
        assert!(summary.has_review_disposition_action_slots());
        assert!(summary.has_required_slots());
        assert!(summary.has_blockers());
        assert!(summary.has_review_work());
        assert!(summary.needs_operator());
        assert!(!summary.has_lineage_gaps());
        assert!(!summary.has_release_handoff_slots());
        assert!(summary.has_complete_lineage());
        assert!(summary.requires_execution());
        assert!(summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_readiness_marks_release_ready(
    ) {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let summary =
            mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_readiness_summary_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(summary.total_review_disposition_actions, 5);
        assert_eq!(summary.total_review_disposition_action_slots, 5);
        assert_eq!(summary.required_slots, 0);
        assert_eq!(summary.release_blocking_slots, 0);
        assert_eq!(summary.operator_required_slots, 0);
        assert_eq!(summary.review_required_slots, 0);
        assert_eq!(summary.dispatch_required_slots, 0);
        assert_eq!(summary.execution_required_slots, 0);
        assert_eq!(summary.repair_slots, 0);
        assert_eq!(summary.operator_handoff_slots, 0);
        assert_eq!(summary.review_gate_slots, 0);
        assert_eq!(summary.lineage_gap_slots, 0);
        assert_eq!(summary.release_handoff_slots, 5);
        assert_eq!(summary.lineage_complete_slots, 5);
        assert_eq!(
            summary.first_review_disposition_action_slot_key,
            Some(
                "release-ticket-handoff-disposition-action-slot-01-release_ticket_handoff-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(summary.first_required_slot_key, None);
        assert_eq!(summary.first_repair_slot_key, None);
        assert_eq!(summary.first_operator_handoff_slot_key, None);
        assert_eq!(summary.first_review_gate_slot_key, None);
        assert_eq!(summary.first_lineage_gap_slot_key, None);
        assert_eq!(
            summary.first_release_handoff_slot_key,
            summary.first_review_disposition_action_slot_key
        );
        assert_eq!(
            summary.first_disposition_action_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionKind::ReleaseTicketHandoff
            )
        );
        assert_eq!(
            summary.first_disposition_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind::ReleaseReady
            )
        );
        assert_eq!(summary.next_review_disposition_action_slot_key, None);
        assert_eq!(summary.next_review_disposition_action_key, None);
        assert_eq!(summary.next_review_disposition_key, None);
        assert_eq!(summary.next_work_order_key, None);
        assert_eq!(summary.next_ticket_key, None);
        assert_eq!(summary.next_check_kind, None);
        assert_eq!(summary.next_check_status, None);
        assert_eq!(summary.next_handoff_lane, None);
        assert!(
            summary
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_actions_ready
        );
        assert!(
            summary
                .release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slots_ready
        );
        assert!(summary
            .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slots_ready);
        assert!(summary
            .ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slots());
        assert!(summary.has_review_disposition_action_slots());
        assert!(!summary.has_required_slots());
        assert!(!summary.has_blockers());
        assert!(!summary.has_review_work());
        assert!(!summary.needs_operator());
        assert!(!summary.has_lineage_gaps());
        assert!(summary.has_release_handoff_slots());
        assert!(summary.has_complete_lineage());
        assert!(!summary.requires_execution());
        assert!(!summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_readiness_surfaces_clearance_attention(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let summary =
            mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_readiness_summary(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );

        assert_eq!(summary.total_review_disposition_action_slots, 5);
        assert_eq!(summary.total_slot_clearance_rows, 5);
        assert_eq!(summary.required_clearances, 5);
        assert_eq!(summary.release_blocking_clearances, 3);
        assert_eq!(summary.operator_required_clearances, 4);
        assert_eq!(summary.review_required_clearances, 2);
        assert_eq!(summary.dispatch_required_clearances, 5);
        assert_eq!(summary.execution_required_clearances, 5);
        assert_eq!(summary.repair_required_clearances, 3);
        assert_eq!(summary.operator_handoff_clearances, 0);
        assert_eq!(summary.review_gate_clearances, 2);
        assert_eq!(summary.lineage_gap_clearances, 0);
        assert_eq!(summary.release_handoff_clearances, 0);
        assert_eq!(summary.lineage_complete_clearances, 5);
        assert_eq!(
            summary.first_slot_clearance_key,
            Some(
                "release-ticket-handoff-disposition-action-slot-clearance-01-repair_required-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_required_clearance_key,
            summary.first_slot_clearance_key
        );
        assert_eq!(
            summary.first_repair_clearance_key,
            summary.first_slot_clearance_key
        );
        assert_eq!(summary.first_operator_handoff_clearance_key, None);
        assert_eq!(
            summary.first_review_clearance_key,
            Some(
                "release-ticket-handoff-disposition-action-slot-clearance-02-review_required-evidence_remediation"
                    .to_string()
            )
        );
        assert_eq!(summary.first_lineage_gap_clearance_key, None);
        assert_eq!(summary.first_release_handoff_clearance_key, None);
        assert_eq!(
            summary.first_review_disposition_action_slot_key,
            Some(
                "release-ticket-handoff-disposition-action-slot-01-schedule_release_blocker_repair-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_slot_clearance_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceKind::RepairRequired
            )
        );
        assert_eq!(
            summary.first_disposition_action_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionKind::ScheduleReleaseBlockerRepair
            )
        );
        assert_eq!(
            summary.first_disposition_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind::RepairReleaseBlocker
            )
        );
        assert_eq!(
            summary.first_handoff_lane,
            Some(IntegrationMeshReleaseDispatchTicketHandoffLane::Repair)
        );
        assert_eq!(
            summary.next_slot_clearance_key,
            summary.first_slot_clearance_key
        );
        assert_eq!(
            summary.next_review_disposition_action_slot_key,
            summary.first_review_disposition_action_slot_key
        );
        assert_eq!(
            summary.next_work_order_key,
            Some(
                "release-ticket-handoff-execution-work-order-01-repair-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.next_ticket_key,
            Some("release-dispatch-ticket-01-substrate_actions".to_string())
        );
        assert_eq!(summary.next_check_kind, summary.first_check_kind);
        assert_eq!(summary.next_check_status, summary.first_check_status);
        assert_eq!(summary.next_handoff_lane, summary.first_handoff_lane);
        assert!(
            !summary
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slots_ready
        );
        assert!(
            !summary
                .release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearances_ready
        );
        assert!(!summary
            .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearances_ready);
        assert!(!summary
            .ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearances());
        assert!(summary.has_slot_clearance_rows());
        assert!(summary.has_required_clearances());
        assert!(summary.has_blockers());
        assert!(summary.has_review_work());
        assert!(summary.needs_operator());
        assert!(!summary.has_lineage_gaps());
        assert!(!summary.has_release_handoff_clearances());
        assert!(summary.has_complete_lineage());
        assert!(summary.requires_execution());
        assert!(summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_readiness_marks_release_ready(
    ) {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let summary =
            mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_readiness_summary_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(summary.total_review_disposition_action_slots, 5);
        assert_eq!(summary.total_slot_clearance_rows, 5);
        assert_eq!(summary.required_clearances, 0);
        assert_eq!(summary.release_blocking_clearances, 0);
        assert_eq!(summary.operator_required_clearances, 0);
        assert_eq!(summary.review_required_clearances, 0);
        assert_eq!(summary.dispatch_required_clearances, 0);
        assert_eq!(summary.execution_required_clearances, 0);
        assert_eq!(summary.repair_required_clearances, 0);
        assert_eq!(summary.operator_handoff_clearances, 0);
        assert_eq!(summary.review_gate_clearances, 0);
        assert_eq!(summary.lineage_gap_clearances, 0);
        assert_eq!(summary.release_handoff_clearances, 5);
        assert_eq!(summary.lineage_complete_clearances, 5);
        assert_eq!(
            summary.first_slot_clearance_key,
            Some(
                "release-ticket-handoff-disposition-action-slot-clearance-01-release_handoff-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(summary.first_required_clearance_key, None);
        assert_eq!(summary.first_repair_clearance_key, None);
        assert_eq!(summary.first_operator_handoff_clearance_key, None);
        assert_eq!(summary.first_review_clearance_key, None);
        assert_eq!(summary.first_lineage_gap_clearance_key, None);
        assert_eq!(
            summary.first_release_handoff_clearance_key,
            summary.first_slot_clearance_key
        );
        assert_eq!(
            summary.first_slot_clearance_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceKind::ReleaseHandoff
            )
        );
        assert_eq!(
            summary.first_disposition_action_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionKind::ReleaseTicketHandoff
            )
        );
        assert_eq!(
            summary.first_disposition_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionKind::ReleaseReady
            )
        );
        assert_eq!(summary.next_slot_clearance_key, None);
        assert_eq!(summary.next_review_disposition_action_slot_key, None);
        assert_eq!(summary.next_review_disposition_action_key, None);
        assert_eq!(summary.next_review_disposition_key, None);
        assert_eq!(summary.next_work_order_key, None);
        assert_eq!(summary.next_ticket_key, None);
        assert_eq!(summary.next_check_kind, None);
        assert_eq!(summary.next_check_status, None);
        assert_eq!(summary.next_handoff_lane, None);
        assert!(
            summary
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slots_ready
        );
        assert!(
            summary
                .release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearances_ready
        );
        assert!(summary
            .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearances_ready);
        assert!(summary
            .ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearances());
        assert!(summary.has_slot_clearance_rows());
        assert!(!summary.has_required_clearances());
        assert!(!summary.has_blockers());
        assert!(!summary.has_review_work());
        assert!(!summary.needs_operator());
        assert!(!summary.has_lineage_gaps());
        assert!(summary.has_release_handoff_clearances());
        assert!(summary.has_complete_lineage());
        assert!(!summary.requires_execution());
        assert!(!summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_readiness_surfaces_action_attention(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let summary =
            mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_readiness_summary(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );

        assert_eq!(summary.total_slot_clearance_rows, 5);
        assert_eq!(summary.total_slot_clearance_action_rows, 5);
        assert_eq!(summary.required_actions, 5);
        assert_eq!(summary.release_blocking_actions, 3);
        assert_eq!(summary.operator_required_actions, 4);
        assert_eq!(summary.review_required_actions, 2);
        assert_eq!(summary.dispatch_required_actions, 5);
        assert_eq!(summary.execution_required_actions, 5);
        assert_eq!(summary.repair_actions, 3);
        assert_eq!(summary.operator_handoff_actions, 0);
        assert_eq!(summary.review_actions, 2);
        assert_eq!(summary.lineage_actions, 0);
        assert_eq!(summary.release_handoff_actions, 0);
        assert_eq!(summary.lineage_complete_actions, 5);
        assert_eq!(summary.ready_actions, 0);
        assert_eq!(
            summary.first_slot_clearance_action_key,
            Some(
                "release-ticket-handoff-disposition-action-slot-clearance-action-01-schedule_repair-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_required_action_key,
            summary.first_slot_clearance_action_key
        );
        assert_eq!(
            summary.first_repair_action_key,
            summary.first_slot_clearance_action_key
        );
        assert_eq!(summary.first_operator_handoff_action_key, None);
        assert_eq!(
            summary.first_review_action_key,
            Some(
                "release-ticket-handoff-disposition-action-slot-clearance-action-02-complete_review-evidence_remediation"
                    .to_string()
            )
        );
        assert_eq!(summary.first_lineage_action_key, None);
        assert_eq!(summary.first_release_handoff_action_key, None);
        assert_eq!(
            summary.first_slot_clearance_key,
            Some(
                "release-ticket-handoff-disposition-action-slot-clearance-01-repair_required-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_review_disposition_action_slot_key,
            Some(
                "release-ticket-handoff-disposition-action-slot-01-schedule_release_blocker_repair-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_slot_clearance_action_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionKind::ScheduleRepair
            )
        );
        assert_eq!(
            summary.first_slot_clearance_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceKind::RepairRequired
            )
        );
        assert_eq!(
            summary.first_handoff_lane,
            Some(IntegrationMeshReleaseDispatchTicketHandoffLane::Repair)
        );
        assert_eq!(
            summary.next_slot_clearance_action_key,
            summary.first_slot_clearance_action_key
        );
        assert_eq!(
            summary.next_slot_clearance_key,
            summary.first_slot_clearance_key
        );
        assert_eq!(
            summary.next_review_disposition_action_slot_key,
            summary.first_review_disposition_action_slot_key
        );
        assert_eq!(summary.next_work_order_key, summary.first_work_order_key);
        assert_eq!(summary.next_ticket_key, summary.first_ticket_key);
        assert_eq!(summary.next_check_kind, summary.first_check_kind);
        assert_eq!(summary.next_check_status, summary.first_check_status);
        assert_eq!(summary.next_handoff_lane, summary.first_handoff_lane);
        assert!(
            !summary
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearances_ready
        );
        assert!(
            !summary
                .release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_actions_ready
        );
        assert!(!summary
            .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_actions_ready);
        assert!(!summary
            .ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_actions());
        assert!(summary.has_slot_clearance_action_rows());
        assert!(summary.has_required_actions());
        assert!(summary.has_blockers());
        assert!(summary.has_review_work());
        assert!(summary.needs_operator());
        assert!(!summary.has_lineage_gaps());
        assert!(!summary.has_release_handoff_actions());
        assert!(summary.has_complete_lineage());
        assert!(summary.requires_execution());
        assert!(summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_readiness_marks_release_ready(
    ) {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let summary =
            mesh_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_action_readiness_summary_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(summary.total_slot_clearance_rows, 5);
        assert_eq!(summary.total_slot_clearance_action_rows, 5);
        assert_eq!(summary.required_actions, 0);
        assert_eq!(summary.release_blocking_actions, 0);
        assert_eq!(summary.operator_required_actions, 0);
        assert_eq!(summary.review_required_actions, 0);
        assert_eq!(summary.dispatch_required_actions, 0);
        assert_eq!(summary.execution_required_actions, 0);
        assert_eq!(summary.repair_actions, 0);
        assert_eq!(summary.operator_handoff_actions, 0);
        assert_eq!(summary.review_actions, 0);
        assert_eq!(summary.lineage_actions, 0);
        assert_eq!(summary.release_handoff_actions, 5);
        assert_eq!(summary.lineage_complete_actions, 5);
        assert_eq!(summary.ready_actions, 5);
        assert_eq!(
            summary.first_slot_clearance_action_key,
            Some(
                "release-ticket-handoff-disposition-action-slot-clearance-action-01-release_handoff-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(summary.first_required_action_key, None);
        assert_eq!(summary.first_repair_action_key, None);
        assert_eq!(summary.first_operator_handoff_action_key, None);
        assert_eq!(summary.first_review_action_key, None);
        assert_eq!(summary.first_lineage_action_key, None);
        assert_eq!(
            summary.first_release_handoff_action_key,
            summary.first_slot_clearance_action_key
        );
        assert_eq!(
            summary.first_slot_clearance_action_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceActionKind::ReleaseHandoff
            )
        );
        assert_eq!(
            summary.first_slot_clearance_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionEvidenceReviewDispositionActionSlotClearanceKind::ReleaseHandoff
            )
        );
        assert_eq!(summary.next_slot_clearance_action_key, None);
        assert_eq!(summary.next_slot_clearance_key, None);
        assert_eq!(summary.next_review_disposition_action_slot_key, None);
        assert_eq!(summary.next_review_disposition_action_key, None);
        assert_eq!(summary.next_review_disposition_key, None);
        assert_eq!(summary.next_work_order_key, None);
        assert_eq!(summary.next_ticket_key, None);
        assert_eq!(summary.next_check_kind, None);
        assert_eq!(summary.next_check_status, None);
        assert_eq!(summary.next_handoff_lane, None);
        assert!(
            summary
                .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearances_ready
        );
        assert!(
            summary
                .release_handoff_execution_work_order_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_actions_ready
        );
        assert!(summary
            .release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_actions_ready);
        assert!(summary
            .ready_for_release_ticket_handoff_guardrail_audit_clearance_action_evidence_review_disposition_action_slot_clearance_actions());
        assert!(summary.has_slot_clearance_action_rows());
        assert!(!summary.has_required_actions());
        assert!(!summary.has_blockers());
        assert!(!summary.has_review_work());
        assert!(!summary.needs_operator());
        assert!(!summary.has_lineage_gaps());
        assert!(summary.has_release_handoff_actions());
        assert!(summary.has_complete_lineage());
        assert!(!summary.requires_execution());
        assert!(!summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_work_order_readiness_summary_surfaces_work_order_attention() {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let summary = mesh_release_ticket_handoff_work_order_readiness_summary(
            &available_primitives,
            &allowed_capabilities,
            &[],
        );

        assert_eq!(summary.total_tickets, 5);
        assert_eq!(summary.ready_tickets, 0);
        assert_eq!(summary.blocked_tickets, 3);
        assert_eq!(summary.review_required_tickets, 2);
        assert_eq!(summary.operator_required_tickets, 4);
        assert_eq!(summary.dispatch_required_tickets, 5);
        assert_eq!(summary.total_handoff_packets, 5);
        assert_eq!(summary.total_execution_slots, 5);
        assert_eq!(summary.total_work_orders, 5);
        assert_eq!(summary.ready_work_orders, 0);
        assert_eq!(summary.blocked_work_orders, 3);
        assert_eq!(summary.review_required_work_orders, 2);
        assert_eq!(summary.operator_required_work_orders, 4);
        assert_eq!(summary.dispatch_required_work_orders, 5);
        assert_eq!(summary.release_lane_work_orders, 0);
        assert_eq!(summary.repair_lane_work_orders, 3);
        assert_eq!(summary.review_lane_work_orders, 2);
        assert_eq!(summary.execution_required_work_orders, 5);
        assert_eq!(summary.queued_substrate_actions, 5);
        assert_eq!(summary.remediation_item_count, 2);
        assert_eq!(summary.review_required_packages, 1);
        assert_eq!(summary.operator_required_packages, 6);
        assert_eq!(summary.blocked_packages, 5);
        assert_eq!(
            summary.first_execution_slot_key,
            Some("release-ticket-handoff-execution-slot-01-repair-substrate_actions".to_string())
        );
        assert_eq!(
            summary.first_work_order_key,
            Some(
                "release-ticket-handoff-execution-work-order-01-repair-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.next_work_order_key,
            Some(
                "release-ticket-handoff-execution-work-order-01-repair-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.next_execution_slot_key,
            Some("release-ticket-handoff-execution-slot-01-repair-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_packet_key,
            Some("release-dispatch-handoff-packet-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_ticket_key,
            Some("release-dispatch-ticket-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_dispatch_key,
            Some("release-task-dispatch-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_task_key,
            Some("release-execution-task-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_slot_key,
            Some("release-check-slot-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_check_kind,
            Some(IntegrationMeshReleaseReadinessCheckKind::SubstrateActions)
        );
        assert_eq!(
            summary.next_check_status,
            Some(IntegrationMeshReleaseReadinessStatus::Blocked)
        );
        assert_eq!(
            summary.next_package_kind,
            Some(IntegrationMeshReadinessHandoffKind::SubstrateAction)
        );
        assert_eq!(
            summary.next_handoff_status,
            Some(IntegrationMeshReadinessHandoffStatus::Blocked)
        );
        assert_eq!(
            summary.next_handoff_lane,
            Some(IntegrationMeshReleaseDispatchTicketHandoffLane::Repair)
        );
        assert_eq!(
            summary.packet_status,
            IntegrationMeshReleaseReadinessStatus::Blocked
        );
        assert_eq!(
            summary.execution_status,
            IntegrationMeshReleaseReadinessStatus::Blocked
        );
        assert!(!summary.release_packet_ready);
        assert!(!summary.release_execution_ready);
        assert!(!summary.release_tasks_ready);
        assert!(!summary.release_dispatch_ready);
        assert!(!summary.release_ticket_ready);
        assert!(!summary.release_handoff_ready);
        assert!(!summary.release_handoff_execution_ready);
        assert!(!summary.release_ticket_handoff_execution_ready);
        assert!(!summary.release_handoff_execution_work_orders_ready);
        assert!(!summary.release_ticket_handoff_work_orders_ready);
        assert!(!summary.ready_for_release_ticket_handoff_work_orders());
        assert!(summary.has_work_orders());
        assert!(summary.has_repair_work());
        assert!(summary.has_blockers());
        assert!(summary.has_review_work());
        assert!(summary.needs_operator());
        assert!(summary.requires_execution());
        assert!(summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_work_order_readiness_summary_marks_release_ready() {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let summary = mesh_release_ticket_handoff_work_order_readiness_summary_for_catalog(
            &catalog,
            all_primitive_families(),
            &allowed_capabilities,
            &[],
        );

        assert_eq!(summary.total_tickets, 5);
        assert_eq!(summary.ready_tickets, 5);
        assert_eq!(summary.blocked_tickets, 0);
        assert_eq!(summary.review_required_tickets, 0);
        assert_eq!(summary.operator_required_tickets, 0);
        assert_eq!(summary.dispatch_required_tickets, 0);
        assert_eq!(summary.total_handoff_packets, 5);
        assert_eq!(summary.total_execution_slots, 5);
        assert_eq!(summary.total_work_orders, 5);
        assert_eq!(summary.ready_work_orders, 5);
        assert_eq!(summary.blocked_work_orders, 0);
        assert_eq!(summary.review_required_work_orders, 0);
        assert_eq!(summary.operator_required_work_orders, 0);
        assert_eq!(summary.dispatch_required_work_orders, 0);
        assert_eq!(summary.release_lane_work_orders, 5);
        assert_eq!(summary.operator_lane_work_orders, 0);
        assert_eq!(summary.repair_lane_work_orders, 0);
        assert_eq!(summary.review_lane_work_orders, 0);
        assert_eq!(summary.execution_required_work_orders, 0);
        assert_eq!(summary.queued_substrate_actions, 0);
        assert_eq!(summary.remediation_item_count, 0);
        assert_eq!(summary.review_required_packages, 0);
        assert_eq!(summary.operator_required_packages, 0);
        assert_eq!(summary.blocked_packages, 0);
        assert_eq!(
            summary.first_execution_slot_key,
            Some("release-ticket-handoff-execution-slot-01-release-substrate_actions".to_string())
        );
        assert_eq!(
            summary.first_work_order_key,
            Some(
                "release-ticket-handoff-execution-work-order-01-release-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_release_work_order_key,
            Some(
                "release-ticket-handoff-execution-work-order-01-release-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(summary.next_work_order_key, None);
        assert_eq!(summary.next_execution_slot_key, None);
        assert_eq!(summary.next_packet_key, None);
        assert_eq!(summary.next_ticket_key, None);
        assert_eq!(summary.next_dispatch_key, None);
        assert_eq!(summary.next_task_key, None);
        assert_eq!(summary.next_slot_key, None);
        assert_eq!(summary.next_check_kind, None);
        assert_eq!(summary.next_check_status, None);
        assert_eq!(
            summary.next_package_kind,
            Some(IntegrationMeshReadinessHandoffKind::ReleaseReady)
        );
        assert_eq!(
            summary.next_handoff_status,
            Some(IntegrationMeshReadinessHandoffStatus::Ready)
        );
        assert_eq!(summary.next_handoff_lane, None);
        assert_eq!(
            summary.packet_status,
            IntegrationMeshReleaseReadinessStatus::Ready
        );
        assert_eq!(
            summary.execution_status,
            IntegrationMeshReleaseReadinessStatus::Ready
        );
        assert!(summary.release_packet_ready);
        assert!(summary.release_execution_ready);
        assert!(summary.release_tasks_ready);
        assert!(summary.release_dispatch_ready);
        assert!(summary.release_ticket_ready);
        assert!(summary.release_handoff_ready);
        assert!(summary.release_handoff_execution_ready);
        assert!(summary.release_ticket_handoff_execution_ready);
        assert!(summary.release_handoff_execution_work_orders_ready);
        assert!(summary.release_ticket_handoff_work_orders_ready);
        assert!(summary.ready_for_release_ticket_handoff_work_orders());
        assert!(summary.has_work_orders());
        assert!(!summary.has_repair_work());
        assert!(!summary.has_blockers());
        assert!(!summary.has_review_work());
        assert!(!summary.needs_operator());
        assert!(!summary.requires_execution());
        assert!(!summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_work_order_guardrail_readiness_summary_surfaces_guardrail_attention(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let summary = mesh_release_ticket_handoff_work_order_guardrail_readiness_summary(
            &available_primitives,
            &allowed_capabilities,
            &[],
        );

        assert_eq!(summary.total_tickets, 5);
        assert_eq!(summary.blocked_tickets, 3);
        assert_eq!(summary.review_required_tickets, 2);
        assert_eq!(summary.operator_required_tickets, 4);
        assert_eq!(summary.dispatch_required_tickets, 5);
        assert_eq!(summary.total_work_orders, 5);
        assert_eq!(summary.ready_work_orders, 0);
        assert_eq!(summary.blocked_work_orders, 3);
        assert_eq!(summary.review_required_work_orders, 2);
        assert_eq!(summary.operator_required_work_orders, 4);
        assert_eq!(summary.dispatch_required_work_orders, 5);
        assert_eq!(summary.release_lane_work_orders, 0);
        assert_eq!(summary.repair_lane_work_orders, 3);
        assert_eq!(summary.review_lane_work_orders, 2);
        assert_eq!(summary.execution_required_work_orders, 5);
        assert_eq!(summary.total_guardrails, 5);
        assert_eq!(summary.release_blocker_guardrails, 3);
        assert_eq!(summary.operator_handoff_guardrails, 0);
        assert_eq!(summary.review_gate_guardrails, 2);
        assert_eq!(summary.ready_to_execute_guardrails, 0);
        assert_eq!(summary.release_blocking_guardrails, 3);
        assert_eq!(summary.operator_required_guardrails, 4);
        assert_eq!(summary.review_required_guardrails, 2);
        assert_eq!(summary.dispatch_required_guardrails, 5);
        assert_eq!(summary.execution_required_guardrails, 5);
        assert_eq!(summary.queued_substrate_actions, 5);
        assert_eq!(summary.remediation_item_count, 2);
        assert_eq!(summary.review_required_packages, 1);
        assert_eq!(summary.operator_required_packages, 6);
        assert_eq!(summary.blocked_packages, 5);
        assert_eq!(
            summary.first_guardrail_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-01-release_blocker-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_guardrail_work_order_key,
            Some(
                "release-ticket-handoff-execution-work-order-01-repair-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_release_blocker_work_order_key,
            Some(
                "release-ticket-handoff-execution-work-order-01-repair-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_review_gate_work_order_key,
            Some(
                "release-ticket-handoff-execution-work-order-02-review-evidence_remediation"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_guardrail_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailKind::ReleaseBlocker
            )
        );
        assert_eq!(
            summary.first_handoff_lane,
            Some(IntegrationMeshReleaseDispatchTicketHandoffLane::Repair)
        );
        assert_eq!(
            summary.next_work_order_key,
            Some(
                "release-ticket-handoff-execution-work-order-01-repair-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.next_execution_slot_key,
            Some("release-ticket-handoff-execution-slot-01-repair-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_packet_key,
            Some("release-dispatch-handoff-packet-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_ticket_key,
            Some("release-dispatch-ticket-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_check_kind,
            Some(IntegrationMeshReleaseReadinessCheckKind::SubstrateActions)
        );
        assert_eq!(
            summary.next_check_status,
            Some(IntegrationMeshReleaseReadinessStatus::Blocked)
        );
        assert_eq!(
            summary.next_handoff_lane,
            Some(IntegrationMeshReleaseDispatchTicketHandoffLane::Repair)
        );
        assert!(!summary.release_handoff_execution_work_order_guardrails_ready);
        assert!(!summary.release_ticket_handoff_work_order_guardrails_ready);
        assert!(!summary.ready_for_release_ticket_handoff_work_order_guardrails());
        assert!(summary.has_guardrails());
        assert!(summary.has_blockers());
        assert!(summary.has_review_work());
        assert!(summary.needs_operator());
        assert!(summary.requires_execution());
        assert!(summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_work_order_guardrail_readiness_summary_marks_release_ready() {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let summary =
            mesh_release_ticket_handoff_work_order_guardrail_readiness_summary_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(summary.total_tickets, 5);
        assert_eq!(summary.ready_tickets, 5);
        assert_eq!(summary.blocked_tickets, 0);
        assert_eq!(summary.total_work_orders, 5);
        assert_eq!(summary.ready_work_orders, 5);
        assert_eq!(summary.blocked_work_orders, 0);
        assert_eq!(summary.release_lane_work_orders, 5);
        assert_eq!(summary.execution_required_work_orders, 0);
        assert_eq!(summary.total_guardrails, 5);
        assert_eq!(summary.release_blocker_guardrails, 0);
        assert_eq!(summary.operator_handoff_guardrails, 0);
        assert_eq!(summary.review_gate_guardrails, 0);
        assert_eq!(summary.ready_to_execute_guardrails, 5);
        assert_eq!(summary.release_blocking_guardrails, 0);
        assert_eq!(summary.operator_required_guardrails, 0);
        assert_eq!(summary.review_required_guardrails, 0);
        assert_eq!(summary.dispatch_required_guardrails, 0);
        assert_eq!(summary.execution_required_guardrails, 0);
        assert_eq!(
            summary.first_guardrail_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-01-ready_to_execute-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_guardrail_work_order_key,
            Some(
                "release-ticket-handoff-execution-work-order-01-release-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(summary.first_release_blocker_work_order_key, None);
        assert_eq!(summary.first_operator_handoff_work_order_key, None);
        assert_eq!(summary.first_review_gate_work_order_key, None);
        assert_eq!(
            summary.first_ready_to_execute_work_order_key,
            Some(
                "release-ticket-handoff-execution-work-order-01-release-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_guardrail_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailKind::ReadyToExecute
            )
        );
        assert_eq!(
            summary.first_handoff_lane,
            Some(IntegrationMeshReleaseDispatchTicketHandoffLane::Release)
        );
        assert_eq!(summary.next_work_order_key, None);
        assert_eq!(summary.next_execution_slot_key, None);
        assert_eq!(summary.next_packet_key, None);
        assert_eq!(summary.next_ticket_key, None);
        assert_eq!(summary.next_check_kind, None);
        assert_eq!(summary.next_check_status, None);
        assert_eq!(
            summary.next_package_kind,
            Some(IntegrationMeshReadinessHandoffKind::ReleaseReady)
        );
        assert_eq!(
            summary.next_handoff_status,
            Some(IntegrationMeshReadinessHandoffStatus::Ready)
        );
        assert_eq!(summary.next_handoff_lane, None);
        assert!(summary.release_ticket_handoff_work_orders_ready);
        assert!(summary.release_handoff_execution_work_order_guardrails_ready);
        assert!(summary.release_ticket_handoff_work_order_guardrails_ready);
        assert!(summary.ready_for_release_ticket_handoff_work_order_guardrails());
        assert!(summary.has_guardrails());
        assert!(!summary.has_blockers());
        assert!(!summary.has_review_work());
        assert!(!summary.needs_operator());
        assert!(!summary.requires_execution());
        assert!(!summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_guardrail_audit_readiness_summary_surfaces_audit_attention() {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let summary = mesh_release_ticket_handoff_guardrail_audit_readiness_summary(
            &available_primitives,
            &allowed_capabilities,
            &[],
        );

        assert_eq!(summary.total_tickets, 5);
        assert_eq!(summary.ready_tickets, 0);
        assert_eq!(summary.blocked_tickets, 3);
        assert_eq!(summary.total_work_orders, 5);
        assert_eq!(summary.ready_work_orders, 0);
        assert_eq!(summary.blocked_work_orders, 3);
        assert_eq!(summary.review_required_work_orders, 2);
        assert_eq!(summary.operator_required_work_orders, 4);
        assert_eq!(summary.dispatch_required_work_orders, 5);
        assert_eq!(summary.execution_required_work_orders, 5);
        assert_eq!(summary.total_guardrails, 5);
        assert_eq!(summary.release_blocker_guardrails, 3);
        assert_eq!(summary.review_gate_guardrails, 2);
        assert_eq!(summary.ready_to_execute_guardrails, 0);
        assert_eq!(summary.total_audit_rows, 5);
        assert_eq!(summary.release_blocker_audit_rows, 3);
        assert_eq!(summary.operator_handoff_audit_rows, 0);
        assert_eq!(summary.review_gate_audit_rows, 2);
        assert_eq!(summary.ready_to_execute_audit_rows, 0);
        assert_eq!(summary.release_blocking_audit_rows, 3);
        assert_eq!(summary.operator_required_audit_rows, 4);
        assert_eq!(summary.review_required_audit_rows, 2);
        assert_eq!(summary.dispatch_required_audit_rows, 5);
        assert_eq!(summary.execution_required_audit_rows, 5);
        assert_eq!(summary.queued_substrate_actions, 5);
        assert_eq!(summary.remediation_item_count, 2);
        assert_eq!(summary.review_required_packages, 1);
        assert_eq!(summary.operator_required_packages, 6);
        assert_eq!(summary.blocked_packages, 5);
        assert_eq!(
            summary.first_audit_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-01-release_blocker-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_audit_guardrail_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-01-release_blocker-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_audit_work_order_key,
            Some(
                "release-ticket-handoff-execution-work-order-01-repair-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_release_blocker_audit_work_order_key,
            Some(
                "release-ticket-handoff-execution-work-order-01-repair-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_review_gate_audit_work_order_key,
            Some(
                "release-ticket-handoff-execution-work-order-02-review-evidence_remediation"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_audit_severity,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditSeverity::ReleaseBlocker
            )
        );
        assert_eq!(
            summary.first_handoff_lane,
            Some(IntegrationMeshReleaseDispatchTicketHandoffLane::Repair)
        );
        assert_eq!(
            summary.first_check_kind,
            Some(IntegrationMeshReleaseReadinessCheckKind::SubstrateActions)
        );
        assert_eq!(
            summary.first_check_status,
            Some(IntegrationMeshReleaseReadinessStatus::Blocked)
        );
        assert_eq!(
            summary.next_work_order_key,
            Some(
                "release-ticket-handoff-execution-work-order-01-repair-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.next_execution_slot_key,
            Some("release-ticket-handoff-execution-slot-01-repair-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_packet_key,
            Some("release-dispatch-handoff-packet-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_ticket_key,
            Some("release-dispatch-ticket-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_check_kind,
            Some(IntegrationMeshReleaseReadinessCheckKind::SubstrateActions)
        );
        assert_eq!(
            summary.next_check_status,
            Some(IntegrationMeshReleaseReadinessStatus::Blocked)
        );
        assert_eq!(
            summary.next_handoff_lane,
            Some(IntegrationMeshReleaseDispatchTicketHandoffLane::Repair)
        );
        assert!(!summary.release_handoff_execution_work_order_guardrail_audit_ready);
        assert!(!summary.release_ticket_handoff_guardrail_audit_ready);
        assert!(!summary.ready_for_release_ticket_handoff_guardrail_audit());
        assert!(summary.has_audit_rows());
        assert!(summary.has_blockers());
        assert!(summary.has_review_work());
        assert!(summary.needs_operator());
        assert!(summary.requires_execution());
        assert!(summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_guardrail_audit_readiness_summary_marks_release_ready() {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let summary = mesh_release_ticket_handoff_guardrail_audit_readiness_summary_for_catalog(
            &catalog,
            all_primitive_families(),
            &allowed_capabilities,
            &[],
        );

        assert_eq!(summary.total_tickets, 5);
        assert_eq!(summary.ready_tickets, 5);
        assert_eq!(summary.blocked_tickets, 0);
        assert_eq!(summary.total_work_orders, 5);
        assert_eq!(summary.ready_work_orders, 5);
        assert_eq!(summary.blocked_work_orders, 0);
        assert_eq!(summary.execution_required_work_orders, 0);
        assert_eq!(summary.total_guardrails, 5);
        assert_eq!(summary.release_blocker_guardrails, 0);
        assert_eq!(summary.review_gate_guardrails, 0);
        assert_eq!(summary.ready_to_execute_guardrails, 5);
        assert_eq!(summary.total_audit_rows, 5);
        assert_eq!(summary.release_blocker_audit_rows, 0);
        assert_eq!(summary.operator_handoff_audit_rows, 0);
        assert_eq!(summary.review_gate_audit_rows, 0);
        assert_eq!(summary.ready_to_execute_audit_rows, 5);
        assert_eq!(summary.release_blocking_audit_rows, 0);
        assert_eq!(summary.operator_required_audit_rows, 0);
        assert_eq!(summary.review_required_audit_rows, 0);
        assert_eq!(summary.dispatch_required_audit_rows, 0);
        assert_eq!(summary.execution_required_audit_rows, 0);
        assert_eq!(
            summary.first_audit_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-01-ready_to_execute-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_audit_guardrail_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-01-ready_to_execute-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_audit_work_order_key,
            Some(
                "release-ticket-handoff-execution-work-order-01-release-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(summary.first_release_blocker_audit_work_order_key, None);
        assert_eq!(summary.first_operator_handoff_audit_work_order_key, None);
        assert_eq!(summary.first_review_gate_audit_work_order_key, None);
        assert_eq!(
            summary.first_ready_to_execute_audit_work_order_key,
            Some(
                "release-ticket-handoff-execution-work-order-01-release-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_audit_severity,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditSeverity::ReadyToExecute
            )
        );
        assert_eq!(
            summary.first_handoff_lane,
            Some(IntegrationMeshReleaseDispatchTicketHandoffLane::Release)
        );
        assert_eq!(summary.next_work_order_key, None);
        assert_eq!(summary.next_execution_slot_key, None);
        assert_eq!(summary.next_packet_key, None);
        assert_eq!(summary.next_ticket_key, None);
        assert_eq!(summary.next_check_kind, None);
        assert_eq!(summary.next_check_status, None);
        assert_eq!(
            summary.next_package_kind,
            Some(IntegrationMeshReadinessHandoffKind::ReleaseReady)
        );
        assert_eq!(
            summary.next_handoff_status,
            Some(IntegrationMeshReadinessHandoffStatus::Ready)
        );
        assert_eq!(summary.next_handoff_lane, None);
        assert!(summary.release_ticket_handoff_work_order_guardrails_ready);
        assert!(summary.release_handoff_execution_work_order_guardrail_audit_ready);
        assert!(summary.release_ticket_handoff_guardrail_audit_ready);
        assert!(summary.ready_for_release_ticket_handoff_guardrail_audit());
        assert!(summary.has_audit_rows());
        assert!(!summary.has_blockers());
        assert!(!summary.has_review_work());
        assert!(!summary.needs_operator());
        assert!(!summary.requires_execution());
        assert!(!summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_guardrail_audit_clearance_readiness_summary_surfaces_clearance_attention(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let summary = mesh_release_ticket_handoff_guardrail_audit_clearance_readiness_summary(
            &available_primitives,
            &allowed_capabilities,
            &[],
        );

        assert_eq!(summary.total_tickets, 5);
        assert_eq!(summary.blocked_tickets, 3);
        assert_eq!(summary.total_work_orders, 5);
        assert_eq!(summary.blocked_work_orders, 3);
        assert_eq!(summary.review_required_work_orders, 2);
        assert_eq!(summary.operator_required_work_orders, 4);
        assert_eq!(summary.dispatch_required_work_orders, 5);
        assert_eq!(summary.execution_required_work_orders, 5);
        assert_eq!(summary.total_guardrails, 5);
        assert_eq!(summary.release_blocker_guardrails, 3);
        assert_eq!(summary.review_gate_guardrails, 2);
        assert_eq!(summary.total_audit_rows, 5);
        assert_eq!(summary.release_blocker_audit_rows, 3);
        assert_eq!(summary.review_gate_audit_rows, 2);
        assert_eq!(summary.total_clearance_rows, 5);
        assert_eq!(summary.release_blocked_clearances, 3);
        assert_eq!(summary.operator_handoff_clearances, 0);
        assert_eq!(summary.review_gate_clearances, 2);
        assert_eq!(summary.cleared_clearances, 0);
        assert_eq!(summary.release_blocking_clearances, 3);
        assert_eq!(summary.operator_required_clearances, 4);
        assert_eq!(summary.review_required_clearances, 2);
        assert_eq!(summary.dispatch_required_clearances, 5);
        assert_eq!(summary.execution_required_clearances, 5);
        assert_eq!(summary.queued_substrate_actions, 5);
        assert_eq!(summary.remediation_item_count, 2);
        assert_eq!(summary.review_required_packages, 1);
        assert_eq!(summary.operator_required_packages, 6);
        assert_eq!(summary.blocked_packages, 5);
        assert_eq!(
            summary.first_clearance_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-01-release_blocked-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_clearance_audit_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-01-release_blocker-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_clearance_work_order_key,
            Some(
                "release-ticket-handoff-execution-work-order-01-repair-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_release_blocked_clearance_work_order_key,
            Some(
                "release-ticket-handoff-execution-work-order-01-repair-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_review_required_clearance_work_order_key,
            Some(
                "release-ticket-handoff-execution-work-order-02-review-evidence_remediation"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_clearance_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind::ReleaseBlocked
            )
        );
        assert_eq!(
            summary.first_audit_severity,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditSeverity::ReleaseBlocker
            )
        );
        assert_eq!(
            summary.first_handoff_lane,
            Some(IntegrationMeshReleaseDispatchTicketHandoffLane::Repair)
        );
        assert_eq!(
            summary.first_check_kind,
            Some(IntegrationMeshReleaseReadinessCheckKind::SubstrateActions)
        );
        assert_eq!(
            summary.first_check_status,
            Some(IntegrationMeshReleaseReadinessStatus::Blocked)
        );
        assert_eq!(
            summary.next_work_order_key,
            Some(
                "release-ticket-handoff-execution-work-order-01-repair-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.next_execution_slot_key,
            Some("release-ticket-handoff-execution-slot-01-repair-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_packet_key,
            Some("release-dispatch-handoff-packet-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_ticket_key,
            Some("release-dispatch-ticket-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_check_kind,
            Some(IntegrationMeshReleaseReadinessCheckKind::SubstrateActions)
        );
        assert_eq!(
            summary.next_check_status,
            Some(IntegrationMeshReleaseReadinessStatus::Blocked)
        );
        assert_eq!(
            summary.next_handoff_lane,
            Some(IntegrationMeshReleaseDispatchTicketHandoffLane::Repair)
        );
        assert!(!summary.release_ticket_handoff_guardrail_audit_ready);
        assert!(!summary.release_handoff_execution_work_order_guardrail_audit_clearance_ready);
        assert!(!summary.release_ticket_handoff_guardrail_audit_clearance_ready);
        assert!(!summary.ready_for_release_ticket_handoff_guardrail_audit_clearance());
        assert!(summary.has_clearance_rows());
        assert!(summary.has_blockers());
        assert!(summary.has_review_work());
        assert!(summary.needs_operator());
        assert!(summary.requires_execution());
        assert!(summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_guardrail_audit_clearance_readiness_summary_marks_release_ready()
    {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let summary =
            mesh_release_ticket_handoff_guardrail_audit_clearance_readiness_summary_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(summary.total_tickets, 5);
        assert_eq!(summary.ready_tickets, 5);
        assert_eq!(summary.blocked_tickets, 0);
        assert_eq!(summary.total_work_orders, 5);
        assert_eq!(summary.ready_work_orders, 5);
        assert_eq!(summary.blocked_work_orders, 0);
        assert_eq!(summary.total_guardrails, 5);
        assert_eq!(summary.release_blocker_guardrails, 0);
        assert_eq!(summary.ready_to_execute_guardrails, 5);
        assert_eq!(summary.total_audit_rows, 5);
        assert_eq!(summary.release_blocker_audit_rows, 0);
        assert_eq!(summary.ready_to_execute_audit_rows, 5);
        assert_eq!(summary.total_clearance_rows, 5);
        assert_eq!(summary.release_blocked_clearances, 0);
        assert_eq!(summary.operator_handoff_clearances, 0);
        assert_eq!(summary.review_gate_clearances, 0);
        assert_eq!(summary.cleared_clearances, 5);
        assert_eq!(summary.release_blocking_clearances, 0);
        assert_eq!(summary.operator_required_clearances, 0);
        assert_eq!(summary.review_required_clearances, 0);
        assert_eq!(summary.dispatch_required_clearances, 0);
        assert_eq!(summary.execution_required_clearances, 0);
        assert_eq!(
            summary.first_clearance_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-01-cleared-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_clearance_audit_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-01-ready_to_execute-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_clearance_work_order_key,
            Some(
                "release-ticket-handoff-execution-work-order-01-release-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(summary.first_release_blocked_clearance_work_order_key, None);
        assert_eq!(
            summary.first_operator_handoff_clearance_work_order_key,
            None
        );
        assert_eq!(summary.first_review_required_clearance_work_order_key, None);
        assert_eq!(
            summary.first_cleared_work_order_key,
            Some(
                "release-ticket-handoff-execution-work-order-01-release-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_clearance_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind::Cleared
            )
        );
        assert_eq!(
            summary.first_audit_severity,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditSeverity::ReadyToExecute
            )
        );
        assert_eq!(
            summary.first_handoff_lane,
            Some(IntegrationMeshReleaseDispatchTicketHandoffLane::Release)
        );
        assert_eq!(summary.next_work_order_key, None);
        assert_eq!(summary.next_execution_slot_key, None);
        assert_eq!(summary.next_packet_key, None);
        assert_eq!(summary.next_ticket_key, None);
        assert_eq!(
            summary.next_package_kind,
            Some(IntegrationMeshReadinessHandoffKind::ReleaseReady)
        );
        assert_eq!(
            summary.next_handoff_status,
            Some(IntegrationMeshReadinessHandoffStatus::Ready)
        );
        assert_eq!(summary.next_handoff_lane, None);
        assert!(summary.release_ticket_handoff_guardrail_audit_ready);
        assert!(summary.release_handoff_execution_work_order_guardrail_audit_clearance_ready);
        assert!(summary.release_ticket_handoff_guardrail_audit_clearance_ready);
        assert!(summary.ready_for_release_ticket_handoff_guardrail_audit_clearance());
        assert!(summary.has_clearance_rows());
        assert!(!summary.has_blockers());
        assert!(!summary.has_review_work());
        assert!(!summary.needs_operator());
        assert!(!summary.requires_execution());
        assert!(!summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_readiness_summary_surfaces_clearance_action_attention(
    ) {
        let available_primitives = vec![
            PrimitiveFamily::Usb,
            PrimitiveFamily::SerialController,
            PrimitiveFamily::Radio802154,
            PrimitiveFamily::Supervision,
        ];
        let allowed_capabilities = vec![CapabilityId::trusted("smart_home.read")];
        let summary =
            mesh_release_ticket_handoff_guardrail_audit_clearance_action_readiness_summary(
                &available_primitives,
                &allowed_capabilities,
                &[],
            );

        assert_eq!(summary.total_tickets, 5);
        assert_eq!(summary.blocked_tickets, 3);
        assert_eq!(summary.total_clearance_rows, 5);
        assert_eq!(summary.release_blocked_clearances, 3);
        assert_eq!(summary.review_gate_clearances, 2);
        assert_eq!(summary.cleared_clearances, 0);
        assert_eq!(summary.total_clearance_actions, 5);
        assert_eq!(summary.required_clearance_actions, 5);
        assert_eq!(summary.repair_release_blocker_actions, 3);
        assert_eq!(summary.operator_handoff_actions, 0);
        assert_eq!(summary.review_gate_actions, 2);
        assert_eq!(summary.release_clearance_actions, 0);
        assert_eq!(summary.release_blocking_actions, 3);
        assert_eq!(summary.operator_required_actions, 4);
        assert_eq!(summary.review_required_actions, 2);
        assert_eq!(summary.dispatch_required_actions, 5);
        assert_eq!(summary.execution_required_actions, 5);
        assert_eq!(
            summary.first_clearance_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-01-release_blocked-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_clearance_action_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-01-repair_release_blocker-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_required_clearance_action_key,
            summary.first_clearance_action_key
        );
        assert_eq!(
            summary.first_repair_clearance_action_key,
            summary.first_clearance_action_key
        );
        assert_eq!(
            summary.first_review_gate_clearance_action_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-02-route_review_gate-evidence_remediation"
                    .to_string()
            )
        );
        assert_eq!(summary.first_release_clearance_action_key, None);
        assert_eq!(
            summary.first_clearance_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind::ReleaseBlocked
            )
        );
        assert_eq!(
            summary.first_clearance_action_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionKind::RepairReleaseBlocker
            )
        );
        assert_eq!(
            summary.next_work_order_key,
            Some(
                "release-ticket-handoff-execution-work-order-01-repair-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.next_execution_slot_key,
            Some("release-ticket-handoff-execution-slot-01-repair-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_packet_key,
            Some("release-dispatch-handoff-packet-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_ticket_key,
            Some("release-dispatch-ticket-01-substrate_actions".to_string())
        );
        assert_eq!(
            summary.next_check_kind,
            Some(IntegrationMeshReleaseReadinessCheckKind::SubstrateActions)
        );
        assert_eq!(
            summary.next_check_status,
            Some(IntegrationMeshReleaseReadinessStatus::Blocked)
        );
        assert_eq!(
            summary.next_handoff_lane,
            Some(IntegrationMeshReleaseDispatchTicketHandoffLane::Repair)
        );
        assert!(!summary.release_ticket_handoff_guardrail_audit_clearance_ready);
        assert!(
            !summary.release_handoff_execution_work_order_guardrail_audit_clearance_actions_ready
        );
        assert!(!summary.release_ticket_handoff_guardrail_audit_clearance_actions_ready);
        assert!(!summary.ready_for_release_ticket_handoff_guardrail_audit_clearance_actions());
        assert!(summary.has_clearance_actions());
        assert!(summary.has_blockers());
        assert!(summary.has_review_work());
        assert!(summary.needs_operator());
        assert!(summary.requires_execution());
        assert!(summary.requires_attention());
    }

    #[test]
    fn mesh_release_ticket_handoff_guardrail_audit_clearance_action_readiness_summary_marks_release_ready(
    ) {
        let catalog = vec![hue_entry()];
        let allowed_capabilities = vec![
            CapabilityId::trusted("smart_home.read"),
            CapabilityId::trusted("smart_home.command.light"),
            CapabilityId::trusted("smart_home.pair"),
        ];
        let summary =
            mesh_release_ticket_handoff_guardrail_audit_clearance_action_readiness_summary_for_catalog(
                &catalog,
                all_primitive_families(),
                &allowed_capabilities,
                &[],
            );

        assert_eq!(summary.total_tickets, 5);
        assert_eq!(summary.ready_tickets, 5);
        assert_eq!(summary.blocked_tickets, 0);
        assert_eq!(summary.total_clearance_rows, 5);
        assert_eq!(summary.release_blocked_clearances, 0);
        assert_eq!(summary.review_gate_clearances, 0);
        assert_eq!(summary.cleared_clearances, 5);
        assert_eq!(summary.total_clearance_actions, 5);
        assert_eq!(summary.required_clearance_actions, 0);
        assert_eq!(summary.repair_release_blocker_actions, 0);
        assert_eq!(summary.operator_handoff_actions, 0);
        assert_eq!(summary.review_gate_actions, 0);
        assert_eq!(summary.release_clearance_actions, 5);
        assert_eq!(summary.release_blocking_actions, 0);
        assert_eq!(summary.operator_required_actions, 0);
        assert_eq!(summary.review_required_actions, 0);
        assert_eq!(summary.dispatch_required_actions, 0);
        assert_eq!(summary.execution_required_actions, 0);
        assert_eq!(
            summary.first_clearance_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-01-cleared-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(
            summary.first_clearance_action_key,
            Some(
                "release-ticket-handoff-execution-work-order-guardrail-audit-clearance-action-01-release_clearance-substrate_actions"
                    .to_string()
            )
        );
        assert_eq!(summary.first_required_clearance_action_key, None);
        assert_eq!(summary.first_repair_clearance_action_key, None);
        assert_eq!(summary.first_review_gate_clearance_action_key, None);
        assert_eq!(
            summary.first_release_clearance_action_key,
            summary.first_clearance_action_key
        );
        assert_eq!(
            summary.first_clearance_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceKind::Cleared
            )
        );
        assert_eq!(
            summary.first_clearance_action_kind,
            Some(
                IntegrationMeshReleaseTicketHandoffExecutionWorkOrderGuardrailAuditClearanceActionKind::ReleaseClearance
            )
        );
        assert_eq!(summary.next_work_order_key, None);
        assert_eq!(summary.next_execution_slot_key, None);
        assert_eq!(summary.next_packet_key, None);
        assert_eq!(summary.next_ticket_key, None);
        assert_eq!(
            summary.next_package_kind,
            Some(IntegrationMeshReadinessHandoffKind::ReleaseReady)
        );
        assert_eq!(
            summary.next_handoff_status,
            Some(IntegrationMeshReadinessHandoffStatus::Ready)
        );
        assert_eq!(summary.next_handoff_lane, None);
        assert!(summary.release_ticket_handoff_guardrail_audit_clearance_ready);
        assert!(
            summary.release_handoff_execution_work_order_guardrail_audit_clearance_actions_ready
        );
        assert!(summary.release_ticket_handoff_guardrail_audit_clearance_actions_ready);
        assert!(summary.ready_for_release_ticket_handoff_guardrail_audit_clearance_actions());
        assert!(summary.has_clearance_actions());
        assert!(!summary.has_blockers());
        assert!(!summary.has_review_work());
        assert!(!summary.needs_operator());
        assert!(!summary.requires_execution());
        assert!(!summary.requires_attention());
    }

    #[test]
    fn primitive_queries_group_delegated_mqtt_device_families() {
        let catalog = first_party_catalog();
        let mqtt_entries = entries_requiring_primitive(&catalog, PrimitiveFamily::Mqtt);

        assert!(mqtt_entries
            .iter()
            .any(|entry| entry.integration_id == IntegrationId::trusted("mqtt")));
        assert!(mqtt_entries
            .iter()
            .any(|entry| entry.integration_id == IntegrationId::trusted("tasmota")));
    }

    #[test]
    fn camera_entries_are_marked_as_privacy_sensitive_primitives() {
        let catalog = first_party_catalog();
        let camera_entries = entries_requiring_primitive(&catalog, PrimitiveFamily::CameraMedia);

        assert!(camera_entries
            .iter()
            .any(|entry| entry.integration_id == IntegrationId::trusted("onvif")));
        assert!(camera_entries.iter().all(|entry| entry
            .required_primitives
            .contains(&PrimitiveFamily::CapabilityPolicy)));
    }

    #[test]
    fn onvif_entry_exposes_executable_camera_runtime_primitives() {
        let catalog = first_party_catalog();
        let onvif = find_entry(&catalog, &IntegrationId::trusted("onvif")).unwrap();

        assert_eq!(onvif.implementation_status, ImplementationStatus::FirstPartyRuntime);
        assert_eq!(onvif.supported_protocols, vec![ProtocolFamily::Onvif]);
        assert!(onvif.target_entity_kinds.contains(&EntityKind::Camera));
        assert!(onvif
            .discovery_mechanisms
            .contains(&DiscoveryMechanism::WsDiscovery));
        assert!(onvif
            .required_primitives
            .contains(&PrimitiveFamily::CameraMedia));
    }

    #[test]
    fn reolink_entry_exposes_authenticated_cgi_runtime_primitives() {
        let catalog = first_party_catalog();
        let reolink = find_entry(&catalog, &IntegrationId::trusted("reolink")).unwrap();

        assert_eq!(
            reolink.implementation_status,
            ImplementationStatus::FirstPartyRuntime
        );
        assert_eq!(reolink.connectivity, ConnectivityClass::LocalPolling);
        assert_eq!(
            reolink.supported_protocols,
            vec![ProtocolFamily::Vendor("reolink_cgi".to_string())]
        );
        assert_eq!(reolink.auth_modes, vec![AuthMode::UsernamePassword]);
        assert!(reolink.target_entity_kinds.contains(&EntityKind::Camera));
        assert!(reolink.target_entity_kinds.contains(&EntityKind::Sensor));
        assert!(reolink
            .required_primitives
            .contains(&PrimitiveFamily::LocalHttp));
        assert!(reolink
            .required_primitives
            .contains(&PrimitiveFamily::VaultLease));
        assert!(!reolink
            .required_primitives
            .contains(&PrimitiveFamily::CameraMedia));
    }

    #[test]
    fn roku_entry_exposes_ssdp_and_read_only_ecp_runtime_primitives() {
        let catalog = first_party_catalog();
        let roku = find_entry(&catalog, &IntegrationId::trusted("roku")).unwrap();

        assert_eq!(
            roku.implementation_status,
            ImplementationStatus::FirstPartyRuntime
        );
        assert_eq!(roku.connectivity, ConnectivityClass::LocalPolling);
        assert_eq!(
            roku.supported_protocols,
            vec![ProtocolFamily::Vendor("roku_ecp".to_string())]
        );
        assert_eq!(roku.auth_modes, vec![AuthMode::None]);
        assert!(roku
            .discovery_mechanisms
            .contains(&DiscoveryMechanism::Ssdp));
        assert!(roku.required_primitives.contains(&PrimitiveFamily::Udp));
        assert!(roku
            .required_primitives
            .contains(&PrimitiveFamily::LocalHttp));
        assert!(!roku
            .required_capabilities
            .contains(&CapabilityId::trusted("smart_home.command.media")));
    }

    #[test]
    fn sonos_entry_exposes_ssdp_and_read_only_upnp_runtime_primitives() {
        let catalog = first_party_catalog();
        let sonos = find_entry(&catalog, &IntegrationId::trusted("sonos")).unwrap();

        assert_eq!(
            sonos.implementation_status,
            ImplementationStatus::FirstPartyRuntime
        );
        assert_eq!(sonos.connectivity, ConnectivityClass::LocalPolling);
        assert_eq!(
            sonos.supported_protocols,
            vec![ProtocolFamily::Vendor("sonos_upnp".to_string())]
        );
        assert_eq!(sonos.auth_modes, vec![AuthMode::None]);
        assert!(sonos
            .discovery_mechanisms
            .contains(&DiscoveryMechanism::Ssdp));
        assert!(sonos
            .required_primitives
            .contains(&PrimitiveFamily::LocalHttp));
        assert!(!sonos
            .required_capabilities
            .contains(&CapabilityId::trusted("smart_home.command.media")));
    }

    #[test]
    fn wemo_entry_exposes_ssdp_upnp_and_bounded_light_control() {
        let catalog = first_party_catalog();
        let wemo = find_entry(&catalog, &IntegrationId::trusted("wemo")).unwrap();

        assert_eq!(
            wemo.implementation_status,
            ImplementationStatus::FirstPartyRuntime
        );
        assert_eq!(wemo.connectivity, ConnectivityClass::LocalPolling);
        assert_eq!(
            wemo.supported_protocols,
            vec![ProtocolFamily::Vendor("wemo_upnp".to_string())]
        );
        assert_eq!(wemo.auth_modes, vec![AuthMode::None]);
        assert!(wemo
            .discovery_mechanisms
            .contains(&DiscoveryMechanism::Ssdp));
        assert!(wemo.required_primitives.contains(&PrimitiveFamily::Udp));
        assert!(wemo
            .required_primitives
            .contains(&PrimitiveFamily::CommandMapping));
        assert!(wemo
            .required_capabilities
            .contains(&CapabilityId::trusted("smart_home.command.light")));
    }

    #[test]
    fn shelly_entry_exposes_executable_local_runtime_primitives() {
        let catalog = first_party_catalog();
        let shelly = find_entry(&catalog, &IntegrationId::trusted("shelly")).unwrap();

        assert_eq!(
            shelly.implementation_status,
            ImplementationStatus::FirstPartyRuntime
        );
        assert_eq!(
            shelly.supported_protocols,
            vec![ProtocolFamily::Vendor("shelly".to_string())]
        );
        assert!(shelly
            .discovery_mechanisms
            .contains(&DiscoveryMechanism::Mdns));
        assert!(shelly
            .required_primitives
            .contains(&PrimitiveFamily::LocalHttp));
        assert!(shelly
            .required_primitives
            .contains(&PrimitiveFamily::CommandMapping));
    }

    #[test]
    fn wled_entry_exposes_executable_local_runtime_primitives() {
        let catalog = first_party_catalog();
        let wled = find_entry(&catalog, &IntegrationId::trusted("wled")).unwrap();

        assert_eq!(
            wled.implementation_status,
            ImplementationStatus::FirstPartyRuntime
        );
        assert_eq!(
            wled.supported_protocols,
            vec![ProtocolFamily::Vendor("wled".to_string())]
        );
        assert_eq!(wled.connectivity, ConnectivityClass::LocalPolling);
        assert!(wled
            .discovery_mechanisms
            .contains(&DiscoveryMechanism::Mdns));
        assert!(wled
            .required_primitives
            .contains(&PrimitiveFamily::LocalHttp));
        assert!(wled
            .required_primitives
            .contains(&PrimitiveFamily::CommandMapping));
    }

    #[test]
    fn nanoleaf_entry_exposes_pairing_token_and_verified_command_primitives() {
        let catalog = first_party_catalog();
        let nanoleaf = find_entry(&catalog, &IntegrationId::trusted("nanoleaf")).unwrap();

        assert_eq!(
            nanoleaf.implementation_status,
            ImplementationStatus::FirstPartyRuntime
        );
        assert_eq!(
            nanoleaf.supported_protocols,
            vec![ProtocolFamily::Vendor("nanoleaf_local".to_string())]
        );
        assert_eq!(nanoleaf.connectivity, ConnectivityClass::LocalPolling);
        assert!(nanoleaf
            .auth_modes
            .contains(&AuthMode::LocalPairing));
        assert!(nanoleaf
            .auth_modes
            .contains(&AuthMode::LocalToken));
        for primitive in [
            PrimitiveFamily::Mdns,
            PrimitiveFamily::LocalHttp,
            PrimitiveFamily::LocalPairing,
            PrimitiveFamily::LocalToken,
            PrimitiveFamily::VaultLease,
            PrimitiveFamily::CommandMapping,
        ] {
            assert!(nanoleaf.required_primitives.contains(&primitive));
        }
    }

    #[test]
    fn tasmota_entry_exposes_mqtt_and_native_local_http_runtime_paths() {
        let catalog = first_party_catalog();
        let tasmota = find_entry(&catalog, &IntegrationId::trusted("tasmota")).unwrap();

        assert_eq!(
            tasmota.implementation_status,
            ImplementationStatus::FirstPartyRuntime
        );
        assert!(tasmota.supported_protocols.contains(&ProtocolFamily::Mqtt));
        assert!(tasmota
            .supported_protocols
            .contains(&ProtocolFamily::Vendor("tasmota_http".to_string())));
        assert!(tasmota
            .discovery_mechanisms
            .contains(&DiscoveryMechanism::Mdns));
        assert!(tasmota.auth_modes.contains(&AuthMode::UsernamePassword));
        for primitive in [
            PrimitiveFamily::Mqtt,
            PrimitiveFamily::Mdns,
            PrimitiveFamily::LocalHttp,
            PrimitiveFamily::VaultLease,
            PrimitiveFamily::CommandMapping,
        ] {
            assert!(tasmota.required_primitives.contains(&primitive));
        }
    }

    #[test]
    fn fronius_entry_exposes_read_only_local_energy_telemetry_runtime() {
        let catalog = first_party_catalog();
        let fronius = find_entry(&catalog, &IntegrationId::trusted("fronius")).unwrap();

        assert_eq!(
            fronius.implementation_status,
            ImplementationStatus::FirstPartyRuntime
        );
        assert_eq!(fronius.connectivity, ConnectivityClass::LocalPolling);
        assert_eq!(fronius.auth_modes, vec![AuthMode::None]);
        assert_eq!(
            fronius.supported_protocols,
            vec![ProtocolFamily::Vendor(
                "fronius_solar_api_v1".to_string()
            )]
        );
        assert_eq!(
            fronius.required_capabilities,
            vec![CapabilityId::trusted("smart_home.read")]
        );
        for primitive in [
            PrimitiveFamily::Mdns,
            PrimitiveFamily::LocalHttp,
            PrimitiveFamily::EnergyTelemetry,
            PrimitiveFamily::CapabilityPolicy,
            PrimitiveFamily::TestSimulator,
        ] {
            assert!(fronius.required_primitives.contains(&primitive));
        }
    }

    #[test]
    fn homewizard_entry_exposes_read_only_local_utility_telemetry_runtime() {
        let catalog = first_party_catalog();
        let homewizard = find_entry(
            &catalog,
            &IntegrationId::trusted("homewizard_energy"),
        )
        .unwrap();

        assert_eq!(
            homewizard.implementation_status,
            ImplementationStatus::FirstPartyRuntime
        );
        assert_eq!(homewizard.connectivity, ConnectivityClass::LocalPolling);
        assert_eq!(homewizard.auth_modes, vec![AuthMode::None]);
        assert_eq!(
            homewizard.supported_protocols,
            vec![ProtocolFamily::Vendor(
                "homewizard_energy_api_v1".to_string()
            )]
        );
        assert_eq!(
            homewizard.required_capabilities,
            vec![CapabilityId::trusted("smart_home.read")]
        );
        for primitive in [
            PrimitiveFamily::Mdns,
            PrimitiveFamily::LocalHttp,
            PrimitiveFamily::EnergyTelemetry,
            PrimitiveFamily::CapabilityPolicy,
            PrimitiveFamily::TestSimulator,
        ] {
            assert!(homewizard.required_primitives.contains(&primitive));
        }
    }

    #[test]
    fn govee_entry_exposes_executable_udp_runtime_primitives() {
        let catalog = first_party_catalog();
        let govee = find_entry(
            &catalog,
            &IntegrationId::trusted("govee_light_local"),
        )
        .unwrap();

        assert_eq!(
            govee.implementation_status,
            ImplementationStatus::FirstPartyRuntime
        );
        assert_eq!(govee.connectivity, ConnectivityClass::LocalPolling);
        assert_eq!(
            govee.supported_protocols,
            vec![ProtocolFamily::Vendor("govee_lan".to_string())]
        );
        assert!(govee
            .discovery_mechanisms
            .contains(&DiscoveryMechanism::UdpMulticast));
        assert!(govee.required_primitives.contains(&PrimitiveFamily::Udp));
        assert!(!govee
            .required_primitives
            .contains(&PrimitiveFamily::LocalHttp));
        assert!(!govee
            .required_primitives
            .contains(&PrimitiveFamily::WebSocket));
        assert!(!govee
            .required_primitives
            .contains(&PrimitiveFamily::VaultLease));
    }

    #[test]
    fn lifx_entry_exposes_executable_udp_runtime_primitives() {
        let catalog = first_party_catalog();
        let lifx = find_entry(&catalog, &IntegrationId::trusted("lifx")).unwrap();

        assert_eq!(
            lifx.implementation_status,
            ImplementationStatus::FirstPartyRuntime
        );
        assert_eq!(lifx.connectivity, ConnectivityClass::LocalPolling);
        assert_eq!(
            lifx.supported_protocols,
            vec![ProtocolFamily::Vendor("lifx_lan".to_string())]
        );
        assert!(lifx
            .discovery_mechanisms
            .contains(&DiscoveryMechanism::UdpBroadcast));
        assert!(lifx.required_primitives.contains(&PrimitiveFamily::Udp));
        assert!(!lifx
            .required_primitives
            .contains(&PrimitiveFamily::LocalToken));
    }

    #[test]
    fn tplink_entry_exposes_executable_kasa_legacy_runtime_primitives() {
        let catalog = first_party_catalog();
        let tplink = find_entry(&catalog, &IntegrationId::trusted("tplink")).unwrap();

        assert_eq!(
            tplink.implementation_status,
            ImplementationStatus::FirstPartyRuntime
        );
        assert_eq!(tplink.connectivity, ConnectivityClass::LocalPolling);
        assert_eq!(
            tplink.supported_protocols,
            vec![ProtocolFamily::Vendor("kasa_legacy_lan".to_string())]
        );
        assert!(tplink
            .discovery_mechanisms
            .contains(&DiscoveryMechanism::UdpBroadcast));
        assert!(tplink.required_primitives.contains(&PrimitiveFamily::Udp));
        assert_eq!(tplink.auth_modes, vec![AuthMode::None]);
        assert!(!tplink.target_entity_kinds.contains(&EntityKind::Camera));
    }

    #[test]
    fn policy_surfaces_capture_privacy_and_entry_access() {
        let catalog = first_party_catalog();
        let onvif = find_entry(&catalog, &IntegrationId::trusted("onvif")).unwrap();
        let zwave = find_entry(&catalog, &IntegrationId::trusted("zwave")).unwrap();

        assert!(onvif.has_policy_surface(IntegrationPolicySurface::CameraMedia));
        assert_eq!(onvif.highest_policy_tier(), PrivilegeTier::HumanApproval);

        assert!(zwave.has_policy_surface(IntegrationPolicySurface::EntryAccess));
        assert!(zwave.has_policy_surface(IntegrationPolicySurface::RadioNetworkManagement));
        assert_eq!(zwave.highest_policy_tier(), PrivilegeTier::HighRisk);
    }

    #[test]
    fn policy_surfaces_expose_cloud_and_credential_boundaries() {
        let catalog = first_party_catalog();
        let ring = find_entry(&catalog, &IntegrationId::trusted("ring")).unwrap();
        let hue = find_entry(&catalog, &IntegrationId::trusted("hue")).unwrap();

        assert!(ring.has_policy_surface(IntegrationPolicySurface::CredentialedCloud));
        assert!(ring.has_policy_surface(IntegrationPolicySurface::CredentialLease));
        assert!(hue.has_policy_surface(IntegrationPolicySurface::CredentialLease));
        assert!(!hue.has_policy_surface(IntegrationPolicySurface::CredentialedCloud));
    }

    #[test]
    fn policy_surface_queries_group_d21_review_targets() {
        let catalog = first_party_catalog();
        let cameras = entries_with_policy_surface(&catalog, IntegrationPolicySurface::CameraMedia);
        let local_actuators =
            entries_with_policy_surface(&catalog, IntegrationPolicySurface::LocalActuation);

        assert!(cameras
            .iter()
            .any(|entry| entry.integration_id == IntegrationId::trusted("reolink")));
        assert!(local_actuators
            .iter()
            .any(|entry| entry.integration_id == IntegrationId::trusted("hue")));
        assert!(local_actuators
            .iter()
            .any(|entry| entry.integration_id == IntegrationId::trusted("mqtt")));
    }

    #[test]
    fn policy_surface_inventory_rolls_up_review_boundaries() {
        let catalog = first_party_catalog();
        let inventory = policy_surface_inventory(&catalog);
        let entry_access = inventory
            .iter()
            .find(|item| item.surface == IntegrationPolicySurface::EntryAccess)
            .unwrap();
        let credentialed_cloud = inventory
            .iter()
            .find(|item| item.surface == IntegrationPolicySurface::CredentialedCloud)
            .unwrap();
        let summary = IntegrationPolicySurfaceSummary::from_inventory(&inventory);

        assert_eq!(entry_access.required_tier, PrivilegeTier::HighRisk);
        assert!(entry_access.includes_integration(&IntegrationId::trusted("zwave")));
        assert!(entry_access.human_review_entry_count >= 1);
        assert_eq!(
            credentialed_cloud.required_tier,
            PrivilegeTier::HumanApproval
        );
        assert!(credentialed_cloud.cloud_entry_count >= 1);
        assert!(summary.total_surfaces >= 4);
        assert!(summary.unique_integrations >= 4);
        assert_eq!(summary.highest_policy_tier, PrivilegeTier::HighRisk);
        assert!(summary.has_review_work());
        assert!(summary.has_high_risk_surface());
        assert_eq!(summary.first_review_priority, Some(0));
    }

    #[test]
    fn catalog_query_composes_local_priority_and_primitive_filters() {
        let catalog = first_party_catalog();
        let query = IntegrationCatalogQuery::new()
            .include_virtual_aliases(false)
            .local_only(true)
            .at_or_before_priority(1)
            .requiring_primitive(PrimitiveFamily::Mqtt)
            .sorted_by(IntegrationCatalogSort::PriorityThenName);
        let results = query_integrations(&catalog, &query);

        assert!(results
            .iter()
            .any(|entry| entry.integration_id == IntegrationId::trusted("mqtt")));
        assert!(results
            .iter()
            .any(|entry| entry.integration_id == IntegrationId::trusted("tasmota")));
        assert!(!results
            .iter()
            .any(|entry| entry.integration_id == IntegrationId::trusted("hue")));
        assert!(results.iter().all(|entry| {
            !entry.is_virtual()
                && entry_local_only(entry)
                && entry.priority <= 1
                && entry.requires_primitive(PrimitiveFamily::Mqtt)
        }));
    }

    #[test]
    fn catalog_query_can_bound_cloud_policy_results() {
        let catalog = first_party_catalog();
        let query = IntegrationCatalogQuery::new()
            .cloud_required(true)
            .with_policy_surface(IntegrationPolicySurface::CredentialedCloud)
            .sorted_by(IntegrationCatalogSort::Name)
            .limited_to(2);
        let results = query_integrations(&catalog, &query);

        assert_eq!(results.len(), 2);
        assert!(results
            .windows(2)
            .all(|window| window[0].display_name <= window[1].display_name));
        assert!(results.iter().all(|entry| {
            entry_cloud_required(entry)
                && entry.has_policy_surface(IntegrationPolicySurface::CredentialedCloud)
        }));
    }

    #[test]
    fn catalog_query_protocol_filters_include_standard_aliases() {
        let catalog = first_party_catalog();
        let query = IntegrationCatalogQuery::new()
            .with_category(IntegrationCategory::VirtualAlias)
            .with_protocol_family(ProtocolFamily::ZWave);
        let results = query_integrations(&catalog, &query);

        assert!(results.iter().all(|entry| entry.is_virtual()));
        assert!(results
            .iter()
            .any(|entry| entry.integration_id == IntegrationId::trusted("ultraloq")));
    }
}
