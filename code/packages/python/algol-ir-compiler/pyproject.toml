[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "coding-adventures-algol-ir-compiler"
version = "0.1.0"
description = "Lower the Python ALGOL 60 semantic lane to compiler IR"
requires-python = ">=3.12"
license = "MIT"
authors = [{ name = "Adhithya Rajasekaran" }]
readme = "README.md"
dependencies = [
    "coding-adventures-graph",
    "coding-adventures-directed-graph",
    "coding-adventures-state-machine",
    "coding-adventures-grammar-tools",
    "coding-adventures-lexer",
    "coding-adventures-parser",
    "coding-adventures-algol-lexer",
    "coding-adventures-algol-parser",
    "coding-adventures-algol-type-checker",
    "coding-adventures-compiler-ir",
]

[project.optional-dependencies]
dev = ["pytest>=8.0", "pytest-cov>=5.0", "ruff>=0.4", "mypy>=1.10"]

[tool.hatch.build.targets.wheel]
packages = ["src/algol_ir_compiler"]

[tool.ruff]
target-version = "py312"
line-length = 88

[tool.ruff.lint]
select = ["E", "W", "F", "I", "UP", "B", "SIM", "ANN"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--cov=algol_ir_compiler --cov-report=term-missing --cov-fail-under=80"

[tool.coverage.run]
source = ["src/algol_ir_compiler"]

[tool.coverage.report]
fail_under = 80
show_missing = true
