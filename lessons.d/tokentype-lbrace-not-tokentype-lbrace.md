---
category: Compiler / VM / language pipeline
---

# `TokenType::LBrace`, not `TokenType::Lbrace`

The enum variant for `{` uses camelCase `LBrace` with capital B. Same applies to `LParen`, `RParen`, `RBrace` — always match the actual Rust declaration.
