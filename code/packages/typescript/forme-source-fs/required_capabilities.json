{
  "$schema": "https://raw.githubusercontent.com/adhithyan15/coding-adventures/main/code/specs/schemas/required_capabilities.schema.json",
  "version": 1,
  "package": "typescript/forme-source-fs",
  "capabilities": ["storage:read"],
  "justification": "Source stages ARE the storage adapter — they read directly via node:fs to populate ContentSource values for the rest of the pipeline.  The capability declaration here is for the manifest/audit layer; the runtime path bypasses ctx.storage (which would be a denied wrapper for a stage that didn't declare storage:read in the first place — circular).  This pragmatic exception is documented in the spec and the README."
}
