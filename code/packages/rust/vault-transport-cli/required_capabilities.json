{
  "$schema": "https://raw.githubusercontent.com/adhithyan15/coding-adventures/main/code/specs/schemas/required_capabilities.schema.json",
  "version": 1,
  "package": "rust/vault-transport-cli",
  "capabilities": [],
  "justification": "Pure parsing + dispatch + formatting library. No filesystem, network, process, environment, csprng, or wallclock access. Stdin reading and stdout writing are the host binary's responsibilities — this crate only produces typed `CliCommand`s and `CommandOutput`s and renders the latter as strings."
}
