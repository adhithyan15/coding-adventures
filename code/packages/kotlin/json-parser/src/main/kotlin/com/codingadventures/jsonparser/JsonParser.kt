package com.codingadventures.jsonparser

/**
 * JsonParser — JSON parser - parses JSON source text using the grammar-driven parser infrastructure
 *
 */
class JsonParser {
    fun ping(): String = "json-parser"
}
