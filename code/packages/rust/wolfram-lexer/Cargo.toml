[package]
name = "coding-adventures-wolfram-lexer"
version = "0.2.0"
edition = "2021"
description = "Tokenizer for the Wolfram Language (Mathematica) M-expression frontend"
license = "MIT"

[dependencies]
grammar-tools = { path = "../grammar-tools" }
lexer = { path = "../lexer" }
