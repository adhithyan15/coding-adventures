[package]
name = "mosaic-pkg-compose-host-navigation-split-fixture"
version = "0.1.0"
description = "Native-complete Compose HostNavigationSplit compilation fixture"
license = "MIT OR Apache-2.0"

[components]
exports = ["Shell"]

[dependencies]

[kernel]
version = "1"
