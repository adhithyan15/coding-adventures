# CMake build outputs
build-cmake/
*.user
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
Makefile

# qmake build outputs (the GUI app + the headless test)
gui-build/
test/qmake-build/
*.o
moc_*.cpp
moc_predefs.h
*.moc
*.app/
.qmake.stash
tst_model
tst_window

# Vendored engine — the C ABI static library + header, built by
# scripts/build.sh from the spreadsheet-capi crate (like the SwiftUI demo).
Vendor/

# Auto-generated by mosaic-compile via scripts/build.sh.
# (Note: build/qmldir IS checked in — the module manifest is
# author-written. Only the auto-generated .qml under build/ is
# regenerated.)
build/FormulaBar.qml
