[package]
name = "venture-browser-qt"
version = "0.1.0"
edition = "2021"
description = "Thin Qt adapter facade for Venture's backend-neutral Cairo content bridge."

[lib]
crate-type = ["rlib"]

[dependencies]
venture-browser-cairo = { path = "../venture-browser-cairo" }

[dev-dependencies]
mosaic-package-artifact-builder = { path = "../mosaic-package-artifact-builder" }
