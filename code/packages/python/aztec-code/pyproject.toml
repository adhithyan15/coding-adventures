[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "coding-adventures-aztec-code"
version = "0.1.0"
description = "Aztec Code encoder — ISO/IEC 24778:2008 compliant, byte-mode, compact + full symbols"
requires-python = ">=3.12"
license = "MIT"
authors = [{ name = "Adhithya Rajasekaran" }]
readme = "README.md"
dependencies = [
    "coding-adventures-barcode-2d",
    "coding-adventures-paint-instructions",
]

[project.optional-dependencies]
dev = ["pytest>=8.0", "pytest-cov>=5.0", "ruff>=0.4", "mypy>=1.10"]

[tool.hatch.build.targets.wheel]
packages = ["src/aztec_code"]

[tool.uv.sources]
coding-adventures-barcode-2d         = { path = "../barcode-2d", editable = true }
coding-adventures-paint-instructions = { path = "../paint-instructions", editable = true }

[tool.ruff]
target-version = "py312"
line-length = 88

[tool.ruff.lint]
select = ["E", "W", "F", "I", "UP", "B", "SIM", "ANN"]
ignore = [
    "ANN401",  # Any allowed in some places
]

[tool.ruff.lint.per-file-ignores]
# Tests don't need return-type annotations on methods.
"tests/**" = ["ANN201", "ANN001", "ANN202"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--cov=aztec_code --cov-report=term-missing --cov-fail-under=90"

[tool.coverage.run]
source = ["src/aztec_code"]

[tool.coverage.report]
fail_under = 90
show_missing = true
