if [ ! -d "../../../packages/swift/md5" ]; then echo "Swift MD5 package not yet available — skipping"; exit 0; fi
if command -v xcrun >/dev/null 2>&1; then xcrun swift run HashBreaker; else swift run HashBreaker; fi
