# coding-adventures-hash-set

Persistent hash set utilities for Python.

The package mirrors the Rust `hash-set` contract with immutable add/remove,
membership checks, set algebra, relation predicates, and compatibility option
fields for capacity, collision strategy, and hash algorithm.
