[package]
name = "semantic-ir-to-go"
version = "0.43.0"
edition = "2021"
description = "Semantic IR → self-contained Go source code (SIR15). Task #63: accept Feature::LoopControl (break/continue), with the bare-if-as-statement func-literal-IIFE fix that requires."

[dependencies]
semantic-ir = { path = "../semantic-ir" }

[dev-dependencies]
twig-to-semantic-ir = { path = "../twig-to-semantic-ir" }
