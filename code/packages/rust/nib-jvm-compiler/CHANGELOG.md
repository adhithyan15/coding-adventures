# Changelog

## Unreleased

- add the first Rust `nib-jvm-compiler` orchestrator
- expose `compile_source`, `pack_source`, and `write_class_file`
- preserve parse, type-check, IR, and generated class artifacts in the result
- add stage-labeled parse, type-check, IR compile, JVM lower, validation, and write errors
