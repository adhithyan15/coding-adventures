### OCaml representative package execution CI

- Add a closed OCAML07 contract, manifest, offline validator, and three-platform
  workflow for the four-package OCaml representative dependency chain.
- Require literal platform BUILD execution, numeric per-source coverage of at
  least 95 percent, strict opam lint, warning-clean odoc 3.0.0 documentation,
  tracked-only source archives, installed downstream consumers, and independent
  OCAML06 capability analysis.
- Preserve OCaml's emerging-lane boundary and the exact OCAML03 solver workflow
  while retaining complete per-target coverage, docs, archive, analyzer,
  install, downstream, and toolchain evidence.
