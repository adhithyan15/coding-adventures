[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "coding-adventures-logic-bytecode"
version = "0.1.0"
description = "Loader bytecode compiler and disassembler for standardized logic instructions"
requires-python = ">=3.12"
license = "MIT"
authors = [{ name = "Adhithya Rajasekaran" }]
readme = "README.md"
dependencies = [
    "coding-adventures-logic-engine>=0.3.0",
    "coding-adventures-logic-instructions>=0.1.0",
]

[project.urls]
Homepage = "https://github.com/adhithyan15/coding-adventures"
Repository = "https://github.com/adhithyan15/coding-adventures"
Documentation = "https://github.com/adhithyan15/coding-adventures/tree/main/code/specs/LP09-logic-bytecode.md"

[project.optional-dependencies]
dev = ["pytest>=8.0", "pytest-cov>=5.0", "ruff>=0.4", "mypy>=1.10"]

[tool.uv.sources]
coding-adventures-logic-engine = { path = "../logic-engine", editable = true }
coding-adventures-logic-instructions = { path = "../logic-instructions", editable = true }

[tool.hatch.build.targets.wheel]
packages = ["src/logic_bytecode"]

[tool.ruff]
target-version = "py312"
line-length = 88

[tool.ruff.lint]
select = ["E", "W", "F", "I", "UP", "B", "SIM", "ANN"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--cov=logic_bytecode --cov-report=term-missing --cov-fail-under=80"

[tool.coverage.run]
source = ["src/logic_bytecode"]

[tool.coverage.report]
fail_under = 80
show_missing = true
