[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "coding-adventures-image-point-ops"
version = "0.1.0"
description = "IMG03: Per-pixel point operations on PixelContainer (invert, gamma, LUTs, colour matrix, …)"
requires-python = ">=3.11"
license = "MIT"
authors = [{ name = "Adhithya Rajasekaran" }]
readme = "README.md"
dependencies = ["coding-adventures-pixel_container>=0.1.0"]

[project.optional-dependencies]
dev = ["pytest>=8.0", "pytest-cov>=5.0", "ruff>=0.4"]

[tool.hatch.build.targets.wheel]
packages = ["src/image_point_ops"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--cov=image_point_ops --cov-report=term-missing --cov-fail-under=90"

[tool.coverage.run]
source = ["src/image_point_ops"]

[tool.coverage.report]
fail_under = 90
show_missing = true

[tool.ruff]
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
