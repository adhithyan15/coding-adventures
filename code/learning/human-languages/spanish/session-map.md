# Session Map — Week 1

How Week 1's units compose into car-ride sessions, and the worked
spaced-repetition schedule behind it. Mechanics are defined in
[`HL00`](../../../specs/HL00-human-language-curriculum-framework.md) — this
file is the concrete worked example for Week 1.

Reminder: reviews are scheduled in **session-counts**, not calendar days. A
concept introduced in session *N* is due for review at sessions *N+1*,
*N+3*, *N+7*, *N+15*. Week 1 uses one session per day (sessions 1-7); from
Week 2 onward a day may hold two sessions (there-and-back commute) — the
counting rule doesn't change, only how fast session numbers tick up.

## Ramp-up exception (sessions 1-5)

The full three-part core block (due reviews → one new unit → a dedicated
practice-mix) only makes sense once there's enough vocabulary to recombine
meaningfully. For sessions 1-5, each new unit's own **Guided Practice**
section (part of every unit's anatomy per `HL00`) already provides that
local recombination. Dedicated `practice-mix` unit files start at session 6,
once five units' worth of vocabulary exists to draw on. This is intentional,
not a shortcut — see `HL00`'s Session Composition section.

## Session-by-session

| Session | Reviews due | New unit | Practice-mix |
|---|---|---|---|
| 1 | — (bootstrap: nothing exists yet) | `ES-P0-U01` Greetings | — |
| 2 | `ES-P0-U01` (N+1) → `ES-P0-R01` | `ES-P0-U02` Subject pronouns | — |
| 3 | `ES-P0-U02` (N+1) → `ES-P0-R02` | `ES-P0-U03` *Ser* | — |
| 4 | `ES-P0-U01` (N+3), `ES-P0-U03` (N+1) | `ES-P0-U04` Numbers 0-10 | — |
| 5 | `ES-P0-U02` (N+3), `ES-P0-U04` (N+1) | `ES-P0-U05` *Estar* | — |
| 6 | `ES-P0-U03` (N+3), `ES-P0-U05` (N+1) | `ES-P0-U07` Days of the week | `ES-P0-U06` *Ser* vs *estar* |
| 7 | `ES-P0-U04` (N+3), `ES-P0-U07` (N+1) | — | `ES-P0-U08` Introduce yourself (capstone) |

## On the review units themselves

`ES-P0-R01` and `ES-P0-R02` are fully authored as concrete examples of what
a "fresh combination, not verbatim repeat" review looks like in practice —
see `HL00`'s review-unit definition. The remaining due reviews in the table
above (sessions 4-7) follow the exact same pattern — take the concept's
vocabulary, place it in a situation the original unit didn't use — and are
generated the same way when a driving session reaches them, rather than each
being hand-authored ahead of time. This mirrors how the roadmap itself is a
skeleton beyond Week 1: content gets fully written as it's about to be
used, not exhaustively pre-built.

## Carrying into Week 2

By the end of session 7, every Week 1 concept still has reviews outstanding
beyond Week 1 (sorted by when they next come due):

- `ES-P0-U01` — next due at session 8 (N+7)
- `ES-P0-U05` — next due at session 8 (N+3)
- `ES-P0-U02` — next due at session 9 (N+7)
- `ES-P0-U07` — next due at session 9 (N+3)
- `ES-P0-U03` — next due at session 10 (N+7)
- `ES-P0-U04` — next due at session 11 (N+7)

Week 2's session map (not yet written) picks these up alongside Week 2's own
new units (numbers 11-100, telling time, months/seasons, question words,
survival phrases — see `roadmap.md`).
