[package]
name = "coding-adventures-source-preprocessor"
version = "0.2.0"
edition = "2021"
description = "PREP01 generic source-preprocessor engine — shared include resolution, conditional compilation, source mapping and resource bounds, with per-language dialect plug-ins."

[dependencies]
lexer = { path = "../lexer" }
