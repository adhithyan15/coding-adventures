---
category: BUILD files & dependency management
---

# TypeScript program path depth

Programs at `code/programs/typescript/<name>/` reach packages with `../../../packages/typescript/<pkg>` (three `..`, not two).
