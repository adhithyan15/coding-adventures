[package]
name = "image-point-ops"
version = "0.1.0"
edition = "2021"
description = "IMG03: Per-pixel point operations on PixelContainer (invert, gamma, LUTs, colour matrix, …)"

[dependencies]
pixel-container = { path = "../pixel-container" }
