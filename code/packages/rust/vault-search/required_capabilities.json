{
  "$schema": "https://raw.githubusercontent.com/adhithyan15/coding-adventures/main/code/specs/schemas/required_capabilities.schema.json",
  "version": 1,
  "package": "rust/vault-search",
  "capabilities": [],
  "justification": "Pure in-memory data structure: trigram index + BM25 ranker + binary serializer. No filesystem, network, process, environment, csprng, or wallclock access. Persistence is the host's responsibility — they wrap `to_bytes()` output in a VLT01 sealed-store record and write it through whatever storage backend they choose."
}
