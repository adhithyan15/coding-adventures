{
  "name": "vscode-lang-extension-generator",
  "version": "0.1.0",
  "description": "Generate VS Code extensions wrapping LSP and DAP servers for languages built on the LANG-VM pipeline",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@coding-adventures/cli-builder": "file:../../../packages/typescript/cli-builder"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@vitest/coverage-v8": "^3.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.0.0",
    "vitest": "^3.0.0"
  }
}
