# wasm-leb128

Native Kotlin implementation of the `wasm-leb128` package.

This package provides:

- `WasmLeb128.decodeUnsigned` and `decodeSigned`
- `WasmLeb128.encodeUnsigned` and `encodeSigned`
- `WasmLeb128.LEB128Error` for malformed encodings
