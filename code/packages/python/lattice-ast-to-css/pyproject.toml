[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "coding-adventures-lattice-ast-to-css"
version = "0.1.0"
description = "Lattice AST-to-CSS compiler — transforms Lattice AST into plain CSS"
requires-python = ">=3.12"
license = "MIT"
authors = [{ name = "Adhithya Rajasekaran" }]
readme = "README.md"
dependencies = [
    "coding-adventures-directed-graph",
    "coding-adventures-grammar-tools",
    "coding-adventures-lattice-lexer",
    "coding-adventures-lattice-parser",
    "coding-adventures-lexer",
    "coding-adventures-parser",
]

[project.optional-dependencies]
dev = ["pytest>=8.0", "pytest-cov>=5.0", "ruff>=0.4", "mypy>=1.10"]

[tool.hatch.build.targets.wheel]
packages = ["src/lattice_ast_to_css"]

[tool.ruff]
target-version = "py312"
line-length = 88

[tool.ruff.lint]
select = ["E", "W", "F", "I", "UP", "B", "SIM", "ANN"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--cov=lattice_ast_to_css --cov-report=term-missing --cov-fail-under=80"

[tool.coverage.run]
source = ["src/lattice_ast_to_css"]

[tool.coverage.report]
fail_under = 80
show_missing = true
