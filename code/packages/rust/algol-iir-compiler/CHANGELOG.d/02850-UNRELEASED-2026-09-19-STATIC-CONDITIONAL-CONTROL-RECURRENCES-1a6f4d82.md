## 0.284.0

- Let bounded integer and real `step`/`until` control recurrences follow statically decidable statement-condition branches while preserving the existing dynamic-selector, effect, overflow, finiteness, cycle, and iteration-cap guards.
