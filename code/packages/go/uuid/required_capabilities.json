{
  "$schema": "https://raw.githubusercontent.com/adhithyan15/coding-adventures/main/code/specs/schemas/required_capabilities.schema.json",
  "version": 1,
  "package": "go/uuid",
  "capabilities": [
    {
      "category": "time",
      "action": "read",
      "target": "*",
      "justification": "UUID v1 and v7 use time.Now() to embed timestamps in generated UUIDs."
    }
  ],
  "justification": "Uses system time for timestamp-based UUID generation (v1, v7). No filesystem or network access needed."
}
