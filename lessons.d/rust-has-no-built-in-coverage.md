---
category: Testing & coverage
---

# Rust has no built-in coverage

— install `cargo-tarpaulin`. `cargo tarpaulin -p <name> --out stdout`; sum the per-file lines for your package's `src/`. Always report a real number, never "n/a".
