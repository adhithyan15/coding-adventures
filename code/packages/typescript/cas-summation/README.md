# @coding-adventures/cas-summation

Pure TypeScript symbolic summation and product evaluation over `symbolic-ir`.

This package mirrors Python `cas-summation` and stays browser-safe by accepting
a host evaluation callback for arithmetic simplification.
