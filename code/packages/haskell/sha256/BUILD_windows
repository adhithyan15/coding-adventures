powershell -NoProfile -Command "if (Get-Command cabal -ErrorAction SilentlyContinue) { & cabal test all --enable-optimization=2; exit $LASTEXITCODE } else { Write-Output 'cabal not found -- skipping'; exit 0 }"
