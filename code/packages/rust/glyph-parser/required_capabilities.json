{
  "$schema": "https://raw.githubusercontent.com/adhithyan15/coding-adventures/main/code/specs/schemas/required_capabilities.schema.json",
  "version": 1,
  "package": "rust/glyph-parser",
  "capabilities": [],
  "justification": "Pure computation package. Decodes glyph outlines from a byte slice the caller already holds via font-parser. No filesystem, network, process, or environment access needed. The test suite reads font files and a fixture, but that is test-only, not library code."
}
