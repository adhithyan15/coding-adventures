[package]
name = "embeddable-http-server"
version = "0.1.0"
edition = "2021"
description = "Embeddable HTTP/1 server primitive built on tcp-runtime"
license = "MIT"

[dependencies]
http-core = { path = "../http-core" }
http1 = { path = "../http1" }
tcp-runtime = { path = "../tcp-runtime" }
transport-platform = { path = "../transport-platform" }
# WEB01b: the in-process mailbox job pool for deferred-response (parallel) serving.
embeddable-tcp-server = { path = "../embeddable-tcp-server" }
generic-job-protocol = { path = "../generic-job-protocol" }
