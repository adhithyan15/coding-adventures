[package]
name = "mosaic-pkg-review-actions"
version = "0.1.0"
description = "Reusable spaced-repetition review action controls built on the Mosaic UI pipeline"
license = "MIT OR Apache-2.0"

[components]
exports = ["ReviewActions"]

[dependencies]
# Built only from UI29 kernel primitives.

[kernel]
version = "1"
