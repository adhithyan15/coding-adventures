[package]
name = "grammar-tools"
version = "0.1.0"
edition = "2021"
description = "Parser and validator for .tokens and .grammar files — the bridge between human-readable grammar specifications and machine-usable parse tables"

[dependencies]
regex = "1"
serde_json = "1.0"
