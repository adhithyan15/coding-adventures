# build-tool: deps=c/iso-harness
# platform-harness: self-test the OS-dependent C/C++ multi-compiler harness.
# Reuses iso-harness's iso_test.h; needs C/C++ compilers + pthreads on this OS.
# (mac + linux use this file; Windows uses BUILD_windows.)
sh tools/selftest.sh
