[package]
name = "unix-tools"
version = "1.0.0"
edition = "2021"
description = "Unix command-line tools reimplemented in Rust — powered by CLI Builder"

[dependencies]
cli-builder = { path = "../../../packages/rust/cli-builder" }
serde_json = "1.0"
libc = "0.2"

[dev-dependencies]
tempfile = "3"
