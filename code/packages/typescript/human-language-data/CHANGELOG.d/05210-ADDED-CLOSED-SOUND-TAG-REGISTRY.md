### Added — closed, per-track sound-tag registry

- Add a reviewed `core/sound-tags.json` vocabulary for all 23 language tracks.
- Reject unknown lesson `sounds:` tags instead of silently creating singleton
  index entries, including on practice and writing lessons.
- Reserve Spanish `stress-antepenultimate` for the esdrújula distinction the
  curriculum already teaches.
