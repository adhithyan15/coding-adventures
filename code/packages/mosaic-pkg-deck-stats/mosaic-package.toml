[package]
name = "mosaic-pkg-deck-stats"
version = "0.1.0"
description = "Reusable spaced-repetition deck statistics panel built on the Mosaic UI pipeline"
license = "MIT OR Apache-2.0"

[components]
exports = ["DeckStatsPanel"]

[dependencies]
# Built only from UI29 kernel primitives.

[kernel]
version = "1"
