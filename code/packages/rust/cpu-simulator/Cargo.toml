[package]
name = "cpu-simulator"
version = "0.1.0"
edition = "2021"
description = "CPU simulator — registers, memory, and the fetch-decode-execute pipeline"

[dependencies]
arithmetic = { path = "../arithmetic" }
