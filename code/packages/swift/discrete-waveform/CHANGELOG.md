# Changelog

## [1.0.0] - 2026-04-06

### Added
- Standardized sampling generation from analog continuous functions (`AnalogWaveform`).
- Built discrete metadata extraction (`duration`, `samplePeriod`).
- Provided `zeroOrderHold(at t:)` handling proper discrete plateau constraints for DAC behavior.
