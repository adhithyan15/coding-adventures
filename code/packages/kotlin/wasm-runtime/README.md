# wasm-runtime

Native Kotlin WebAssembly runtime for the JVM package set.

This package currently provides:

- parse / validate / instantiate / call composition helpers
- a lightweight `WasmInstance` runtime container
- a minimal `WasiStub` for host wiring
