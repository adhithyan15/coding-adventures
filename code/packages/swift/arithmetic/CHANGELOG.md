# Changelog

## [1.0.0] - 2026-04-06

### Added
- Created `halfAdder`, `fullAdder`, and `rippleCarryAdder`.
- Built the `ALU` class utilizing Two's complement and standard bitwise logic rules to generate appropriate computation values alongside flags (`zero`, `carry`, `negative`, `overflow`).
- Fully tested using logic gate components.
