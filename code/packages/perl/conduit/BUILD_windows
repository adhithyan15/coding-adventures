if command -v mise >/dev/null 2>&1; then PERL_BIN=$(mise which perl 2>/dev/null || true); fi && if [ -z "${PERL_BIN:-}" ]; then PERL_BIN=$(command -v perl); fi && cargo build --release && dlext=$("$PERL_BIN" -MConfig -e 'print $Config{dlext}') && mkdir -p lib/auto/CodingAdventures/Conduit && cp target/release/Conduit.dll lib/auto/CodingAdventures/Conduit/Conduit.$dlext && prove -I"$PWD/lib" -v t/
