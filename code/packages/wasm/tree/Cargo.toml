[workspace]

[package]
name = "tree-wasm"
version = "0.1.0"
edition = "2021"
description = "WebAssembly bindings for the Rust tree crate via wasm-bindgen"
license = "MIT"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
tree = { path = "../../rust/tree" }
wasm-bindgen = "0.2"
