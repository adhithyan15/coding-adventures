if [ "$(uname -s)" = "Darwin" ] && [ "$(uname -m)" = "arm64" ]; then REPO_ROOT=$(git rev-parse --show-toplevel) && (cd "$REPO_ROOT/code/packages/rust/paint-vm-metal-c" && cargo build --release) && mkdir -p Sources/CPaintVmMetalNative/include && cp "$REPO_ROOT/code/packages/rust/paint-vm-metal-c/include/paint_vm_metal_c.h" Sources/CPaintVmMetalNative/include/paint_vm_metal_c.h && cp "$REPO_ROOT/code/packages/rust/target/release/libpaint_vm_metal_c.a" Sources/CPaintVmMetalNative/libpaint_vm_metal_c.a && swift test; else echo Skipping swift/PaintVmMetalNative on non-macOS-arm64; fi
