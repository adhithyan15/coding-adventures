# Changelog — codabar (Lua)

## 0.1.0 — 2026-04-13

Initial release.

- `normalize_codabar` — guard validation and normalization
- `encode_codabar` — full payload encoding with start/stop markers
- `expand_codabar_runs` — bar/space run expansion with quiet zones
- `draw_codabar` — PaintScene generation through `barcode-layout-1d`
