[package]
name = "perceptron"
version = "0.1.0"
edition = "2021"

[dependencies]
matrix = { path = "../matrix" }
loss-functions = { path = "../loss-functions" }
activation-functions = { path = "../activation-functions" }
