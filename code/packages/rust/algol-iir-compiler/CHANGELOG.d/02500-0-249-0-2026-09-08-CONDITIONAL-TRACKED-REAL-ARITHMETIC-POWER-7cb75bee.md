## 0.249.0 — 2026-09-08 — conditional tracked-real arithmetic powers

Pure conditional arithmetic over initialized tracked local real snapshots may
now provide a bounded power exponent when every runtime branch proves the same
exact integral value. Differing, uninitialized, effectful, nested-power, and
standard-free forms fail closed while accepted selectors remain emitted.

