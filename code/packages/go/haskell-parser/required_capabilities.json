{
  "$schema": "https://raw.githubusercontent.com/adhithyan15/coding-adventures/main/code/specs/schemas/required_capabilities.schema.json",
  "version": 1,
  "package": "go/haskell-parser",
  "capabilities": [],
  "justification": "Haskell parser grammars for every supported version are compiled into the package as native Go data structures (grammar_data.go). No grammar files are read at runtime, so the parser needs no filesystem, network, or process capabilities."
}
