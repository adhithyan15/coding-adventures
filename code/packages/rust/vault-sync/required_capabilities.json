{
  "$schema": "https://raw.githubusercontent.com/adhithyan15/coding-adventures/main/code/specs/schemas/required_capabilities.schema.json",
  "version": 1,
  "package": "rust/vault-sync",
  "capabilities": [],
  "justification": "Pure data-structure layer: version vectors, conflict resolver, OR-set CRDT, in-memory reference SyncServer. No filesystem, network, process, environment, csprng, or wallclock access — `last_writer_ms` is caller-supplied. Persistent / networked sync servers (Postgres, S3) are sibling crates that declare their own capabilities."
}
