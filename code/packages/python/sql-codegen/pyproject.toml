[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "coding-adventures-sql-codegen"
version = "1.8.0"
description = "Compiles LogicalPlan trees into flat IR bytecode for the sql-vm."
requires-python = ">=3.12"
license = "MIT"
authors = [{ name = "Adhithya Rajasekaran" }]
readme = "README.md"
keywords = ["sql", "codegen", "bytecode", "virtual-machine", "compiler", "education"]
dependencies = ["coding-adventures-sql-planner", "coding-adventures-sql-optimizer"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Education",
    "Topic :: Education",
    "Topic :: Database",
    "Programming Language :: Python :: 3.12",
    "License :: OSI Approved :: MIT License",
    "Typing :: Typed",
]

[project.urls]
Homepage = "https://github.com/adhithyan15/coding-adventures"
Repository = "https://github.com/adhithyan15/coding-adventures"

[project.optional-dependencies]
dev = ["pytest>=8.0", "pytest-cov>=5.0", "ruff>=0.4", "mypy>=1.10"]

[tool.hatch.build.targets.wheel]
packages = ["src/sql_codegen"]

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E", "W", "F", "I", "UP", "B", "SIM", "ANN"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--cov=sql_codegen --cov-report=term-missing --cov-fail-under=80"

[tool.coverage.run]
source = ["src/sql_codegen"]
omit = ["**/sql_codegen/* 2.py"]

[tool.coverage.report]
fail_under = 80
show_missing = true
