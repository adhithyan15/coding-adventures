# sql-execution-engine (Dart)

Small SELECT execution engine for mini-sqlite data sources.

The package exposes a `DataSource` abstraction, an in-memory implementation,
and query helpers for scans, filters, joins, grouping, aggregates, ordering,
distinct, limit, and offset.
