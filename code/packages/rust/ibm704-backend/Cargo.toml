[package]
name = "ibm704-backend"
version = "0.2.0"
edition = "2021"
description = "IBM 704 backend emitting canonical instructions and addressable sign-magnitude literal pools"

[lib]
name = "ibm704_backend"
path = "src/lib.rs"

[dependencies]
ibm704-encoder = { path = "../ibm704-encoder" }
jit-core       = { path = "../jit-core" }
vm-core        = { path = "../vm-core" }
