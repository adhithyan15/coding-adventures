# minify_else_body_flatten

CLOC14.37 byte-identity fixture (captured from the upstream
Closure JAR v20240317, WHITESPACE_ONLY).

- **Input:** `if(x)a();else{b()}`
- **Note:** gap-080: else-body single-statement flatten -> else b()
