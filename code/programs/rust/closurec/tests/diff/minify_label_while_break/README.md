# minify_label_while_break

CLOC14.58 byte-identity fixture (captured from the upstream
Closure JAR v20240317, WHITESPACE_ONLY).

- **Input:** `label:while(x)break label;`
- **Note:** labeled while-loop with labeled break
