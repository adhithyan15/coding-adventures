# build-tool: deps=c/iso-harness c/platform-harness c/os-platform
# event-loop (OS-agnostic generic event loop; composes os-platform thread +
# clock). run.sh compiles the loop and test with the os-platform thread + clock
# backends for this OS. Windows uses BUILD_windows (CRT + kernel32).
sh tools/run.sh
