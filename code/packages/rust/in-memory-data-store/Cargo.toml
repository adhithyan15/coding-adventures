[package]
name = "in-memory-data-store"
version = "0.1.0"
edition = "2021"
description = "Composable in-memory data store server"
license = "MIT"

[dependencies]
in-memory-data-store-engine = { path = "../in-memory-data-store-engine" }
in-memory-data-store-protocol = { path = "../in-memory-data-store-protocol" }
