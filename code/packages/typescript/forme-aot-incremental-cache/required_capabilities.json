{
  "$schema": "https://raw.githubusercontent.com/adhithyan15/coding-adventures/main/code/specs/schemas/required_capabilities.schema.json",
  "version": 1,
  "package": "typescript/forme-aot-incremental-cache",
  "capabilities": ["hash"],
  "justification": "Uses node:crypto.createHash('sha256') to compute order-stable cache keys from (canonicalStyleDocument(doc) + sorted usedRuleIds + sorted activeContexts).  Storage IO is injected via CacheIO — this package does NOT touch the filesystem directly; production callers wire CacheIO to fs in their own packages (which then declare fs capability themselves)."
}
