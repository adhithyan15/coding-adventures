# minify_case_neg_num

CLOC14.56 byte-identity fixture (captured from the upstream
Closure JAR v20240317, WHITESPACE_ONLY).

- **Input:** `switch(x){case-1:a()}`
- **Note:** gap-117: case + unary-operator operand needs a separating space
