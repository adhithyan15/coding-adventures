; Generated by grammar-tools from twig.grammar — DO NOT EDIT
;
; Twig CST parser: converts a token list into a Concrete Syntax Tree.
; Regenerate with:
;   grammar-tools compile-grammar-twig twig.grammar -o cst-parser.tw
;
; CST node format:
;   Non-terminal: (cons "rule-name" (list child ...))
;   Terminal:     Token record from compiler/token
;   Failure:      nil
;   Success:      (cons rest-tokens cst-node)

(module compiler/cst-parser
  (typed lenient)
  (export cst-parse-program cst-parse-expr)
  (import compiler/token))

; ─── Token-matching helpers ──────────────────────────────────────────────────────
;
; cst-match-<Kind>: match a token of given kind.
;   Returns (cons rest-tokens token) on match, nil on failure.
;
; cst-match-kw: match a TkIdentifier with a specific lexeme (keyword).

(define (cst-match-TkLParen tokens)
  ; Match TkLParen token.
  (if (null? tokens) nil
    (let* ((tok (car tokens)))
      (if (TkLParen? (token-kind tok))
        (cons (cdr tokens) tok)
        nil))))

(define (cst-match-TkRParen tokens)
  ; Match TkRParen token.
  (if (null? tokens) nil
    (let* ((tok (car tokens)))
      (if (TkRParen? (token-kind tok))
        (cons (cdr tokens) tok)
        nil))))

(define (cst-match-TkQuote tokens)
  ; Match TkQuote token.
  (if (null? tokens) nil
    (let* ((tok (car tokens)))
      (if (TkQuote? (token-kind tok))
        (cons (cdr tokens) tok)
        nil))))

(define (cst-match-TkColon tokens)
  ; Match TkColon token.
  (if (null? tokens) nil
    (let* ((tok (car tokens)))
      (if (TkColon? (token-kind tok))
        (cons (cdr tokens) tok)
        nil))))

(define (cst-match-TkIdentifier tokens)
  ; Match TkIdentifier token.
  (if (null? tokens) nil
    (let* ((tok (car tokens)))
      (if (TkIdentifier? (token-kind tok))
        (cons (cdr tokens) tok)
        nil))))

(define (cst-match-TkBoolean tokens)
  ; Match TkBoolean token.
  (if (null? tokens) nil
    (let* ((tok (car tokens)))
      (if (TkBoolean? (token-kind tok))
        (cons (cdr tokens) tok)
        nil))))

(define (cst-match-TkString tokens)
  ; Match TkString token.
  (if (null? tokens) nil
    (let* ((tok (car tokens)))
      (if (TkString? (token-kind tok))
        (cons (cdr tokens) tok)
        nil))))

(define (cst-match-TkInteger tokens)
  ; Match TkInteger token.
  (if (null? tokens) nil
    (let* ((tok (car tokens)))
      (if (TkInteger? (token-kind tok))
        (cons (cdr tokens) tok)
        nil))))

(define (cst-match-kw tokens kw)
  ; Match a TkIdentifier token whose lexeme equals kw.
  ; Used for keywords (define, lambda, if, …) and ARROW ("->").
  (if (null? tokens) nil
    (let* ((tok  (car tokens))
           (kind (token-kind tok))
           (lex  (token-lexeme tok)))
      (if (and (TkIdentifier? kind) (string=? lex kw))
        (cons (cdr tokens) tok)
        nil))))

; ─── Grammar rules (one function per rule, grammar-file order) ──────────────────

; Repetition helper for program rule
(define (cst-parse-program-rep3 tok-rep acc)
  (let* ((r-rep
    (cst-parse-form tok-rep)
  ))
  (if (null? r-rep)
    (cons tok-rep (reverse acc))
    (cst-parse-program-rep3 (car r-rep) (cons (cdr r-rep) acc)))))

; Optional element helper for program rule
(define (cst-parse-program-opt1 tok-opt)
  (let* ((r-opt
    (cst-parse-module-form tok-opt)
  ))
  (if (null? r-opt)
    (cons tok-opt nil)
    r-opt)))

; program
(define (cst-parse-program tokens)
  (let* ((r0 (cst-parse-program-opt1 tokens))
         (t0 (car r0)) (ch0 (cdr r0)))
  (let* ((r2 (cst-parse-program-rep3 t0 (list)))
         (t2 (car r2)) (ch2 (cdr r2)))
  (cons t2 (cons "program" (list ch0 ch2)))))
)

; Repetition helper for module-form rule
(define (cst-parse-module-form-rep4 tok-rep acc)
  (let* ((r-rep
    (cst-parse-module-clause tok-rep)
  ))
  (if (null? r-rep)
    (cons tok-rep (reverse acc))
    (cst-parse-module-form-rep4 (car r-rep) (cons (cdr r-rep) acc)))))

; module_form
(define (cst-parse-module-form tokens)
  (let* ((r0 (cst-match-TkLParen tokens)))
  (if (null? r0) nil
  (let* ((t0 (car r0)) (ch0 (cdr r0)))
  (let* ((r1 (cst-match-kw t0 "module")))
  (if (null? r1) nil
  (let* ((t1 (car r1)) (ch1 (cdr r1)))
  (let* ((r2 (cst-match-TkIdentifier t1)))
  (if (null? r2) nil
  (let* ((t2 (car r2)) (ch2 (cdr r2)))
  (let* ((r3 (cst-parse-module-form-rep4 t2 (list)))
         (t3 (car r3)) (ch3 (cdr r3)))
  (let* ((r5 (cst-match-TkRParen t3)))
  (if (null? r5) nil
  (let* ((t5 (car r5)) (ch5 (cdr r5)))
  (cons t5 (cons "module-form" (list ch0 ch1 ch2 ch3 ch5))))))))))))))))
)

; module_clause
(define (cst-parse-module-clause tokens)
  (let* ((r-alt1 (cst-parse-export-clause tokens)))
  (if (not (null? r-alt1)) r-alt1
  (let* ((r-alt0 (cst-parse-import-clause tokens)))
  (if (not (null? r-alt0)) r-alt0
  (cst-parse-typed-clause tokens)))))
)

; Repetition helper for export-clause rule
(define (cst-parse-export-clause-rep3 tok-rep acc)
  (let* ((r-rep
    (cst-match-TkIdentifier tok-rep)
  ))
  (if (null? r-rep)
    (cons tok-rep (reverse acc))
    (cst-parse-export-clause-rep3 (car r-rep) (cons (cdr r-rep) acc)))))

; export_clause
(define (cst-parse-export-clause tokens)
  (let* ((r0 (cst-match-TkLParen tokens)))
  (if (null? r0) nil
  (let* ((t0 (car r0)) (ch0 (cdr r0)))
  (let* ((r1 (cst-match-kw t0 "export")))
  (if (null? r1) nil
  (let* ((t1 (car r1)) (ch1 (cdr r1)))
  (let* ((r2 (cst-parse-export-clause-rep3 t1 (list)))
         (t2 (car r2)) (ch2 (cdr r2)))
  (let* ((r4 (cst-match-TkRParen t2)))
  (if (null? r4) nil
  (let* ((t4 (car r4)) (ch4 (cdr r4)))
  (cons t4 (cons "export-clause" (list ch0 ch1 ch2 ch4)))))))))))))
)

; Repetition helper for import-clause rule
(define (cst-parse-import-clause-rep3 tok-rep acc)
  (let* ((r-rep
    (cst-match-TkIdentifier tok-rep)
  ))
  (if (null? r-rep)
    (cons tok-rep (reverse acc))
    (cst-parse-import-clause-rep3 (car r-rep) (cons (cdr r-rep) acc)))))

; import_clause
(define (cst-parse-import-clause tokens)
  (let* ((r0 (cst-match-TkLParen tokens)))
  (if (null? r0) nil
  (let* ((t0 (car r0)) (ch0 (cdr r0)))
  (let* ((r1 (cst-match-kw t0 "import")))
  (if (null? r1) nil
  (let* ((t1 (car r1)) (ch1 (cdr r1)))
  (let* ((r2 (cst-parse-import-clause-rep3 t1 (list)))
         (t2 (car r2)) (ch2 (cdr r2)))
  (let* ((r4 (cst-match-TkRParen t2)))
  (if (null? r4) nil
  (let* ((t4 (car r4)) (ch4 (cdr r4)))
  (cons t4 (cons "import-clause" (list ch0 ch1 ch2 ch4)))))))))))))
)

; typed_clause
(define (cst-parse-typed-clause tokens)
  (let* ((r0 (cst-match-TkLParen tokens)))
  (if (null? r0) nil
  (let* ((t0 (car r0)) (ch0 (cdr r0)))
  (let* ((r1 (cst-match-kw t0 "typed")))
  (if (null? r1) nil
  (let* ((t1 (car r1)) (ch1 (cdr r1)))
  (let* ((r2 (cst-match-TkIdentifier t1)))
  (if (null? r2) nil
  (let* ((t2 (car r2)) (ch2 (cdr r2)))
  (let* ((r3 (cst-match-TkRParen t2)))
  (if (null? r3) nil
  (let* ((t3 (car r3)) (ch3 (cdr r3)))
  (cons t3 (cons "typed-clause" (list ch0 ch1 ch2 ch3)))))))))))))))
)

; form
(define (cst-parse-form tokens)
  (let* ((r-alt3 (cst-parse-define tokens)))
  (if (not (null? r-alt3)) r-alt3
  (let* ((r-alt2 (cst-parse-type-alias tokens)))
  (if (not (null? r-alt2)) r-alt2
  (let* ((r-alt1 (cst-parse-record-def tokens)))
  (if (not (null? r-alt1)) r-alt1
  (let* ((r-alt0 (cst-parse-union-def tokens)))
  (if (not (null? r-alt0)) r-alt0
  (cst-parse-expr tokens)))))))))
)

; type_alias
(define (cst-parse-type-alias tokens)
  (let* ((r0 (cst-match-TkLParen tokens)))
  (if (null? r0) nil
  (let* ((t0 (car r0)) (ch0 (cdr r0)))
  (let* ((r1 (cst-match-kw t0 "type")))
  (if (null? r1) nil
  (let* ((t1 (car r1)) (ch1 (cdr r1)))
  (let* ((r2 (cst-match-TkIdentifier t1)))
  (if (null? r2) nil
  (let* ((t2 (car r2)) (ch2 (cdr r2)))
  (let* ((r3 (cst-parse-type-annotation t2)))
  (if (null? r3) nil
  (let* ((t3 (car r3)) (ch3 (cdr r3)))
  (let* ((r4 (cst-match-TkRParen t3)))
  (if (null? r4) nil
  (let* ((t4 (car r4)) (ch4 (cdr r4)))
  (cons t4 (cons "type-alias" (list ch0 ch1 ch2 ch3 ch4))))))))))))))))))
)

; Repetition helper for record-def rule
(define (cst-parse-record-def-rep4 tok-rep acc)
  (let* ((r-rep
    (cst-parse-record-field tok-rep)
  ))
  (if (null? r-rep)
    (cons tok-rep (reverse acc))
    (cst-parse-record-def-rep4 (car r-rep) (cons (cdr r-rep) acc)))))

; record_def
(define (cst-parse-record-def tokens)
  (let* ((r0 (cst-match-TkLParen tokens)))
  (if (null? r0) nil
  (let* ((t0 (car r0)) (ch0 (cdr r0)))
  (let* ((r1 (cst-match-kw t0 "record")))
  (if (null? r1) nil
  (let* ((t1 (car r1)) (ch1 (cdr r1)))
  (let* ((r2 (cst-match-TkIdentifier t1)))
  (if (null? r2) nil
  (let* ((t2 (car r2)) (ch2 (cdr r2)))
  (let* ((r3 (cst-parse-record-def-rep4 t2 (list)))
         (t3 (car r3)) (ch3 (cdr r3)))
  (let* ((r5 (cst-match-TkRParen t3)))
  (if (null? r5) nil
  (let* ((t5 (car r5)) (ch5 (cdr r5)))
  (cons t5 (cons "record-def" (list ch0 ch1 ch2 ch3 ch5))))))))))))))))
)

; record_field
(define (cst-parse-record-field tokens)
  (let* ((r0 (cst-match-TkLParen tokens)))
  (if (null? r0) nil
  (let* ((t0 (car r0)) (ch0 (cdr r0)))
  (let* ((r1 (cst-match-TkIdentifier t0)))
  (if (null? r1) nil
  (let* ((t1 (car r1)) (ch1 (cdr r1)))
  (let* ((r2 (cst-match-TkColon t1)))
  (if (null? r2) nil
  (let* ((t2 (car r2)) (ch2 (cdr r2)))
  (let* ((r3 (cst-parse-type-annotation t2)))
  (if (null? r3) nil
  (let* ((t3 (car r3)) (ch3 (cdr r3)))
  (let* ((r4 (cst-match-TkRParen t3)))
  (if (null? r4) nil
  (let* ((t4 (car r4)) (ch4 (cdr r4)))
  (cons t4 (cons "record-field" (list ch0 ch1 ch2 ch3 ch4))))))))))))))))))
)

; Repetition helper for union-def rule
(define (cst-parse-union-def-rep4 tok-rep acc)
  (let* ((r-rep
    (cst-parse-union-variant tok-rep)
  ))
  (if (null? r-rep)
    (cons tok-rep (reverse acc))
    (cst-parse-union-def-rep4 (car r-rep) (cons (cdr r-rep) acc)))))

; union_def
(define (cst-parse-union-def tokens)
  (let* ((r0 (cst-match-TkLParen tokens)))
  (if (null? r0) nil
  (let* ((t0 (car r0)) (ch0 (cdr r0)))
  (let* ((r1 (cst-match-kw t0 "union")))
  (if (null? r1) nil
  (let* ((t1 (car r1)) (ch1 (cdr r1)))
  (let* ((r2 (cst-match-TkIdentifier t1)))
  (if (null? r2) nil
  (let* ((t2 (car r2)) (ch2 (cdr r2)))
  (let* ((r3 (cst-parse-union-def-rep4 t2 (list)))
         (t3 (car r3)) (ch3 (cdr r3)))
  (let* ((r5 (cst-match-TkRParen t3)))
  (if (null? r5) nil
  (let* ((t5 (car r5)) (ch5 (cdr r5)))
  (cons t5 (cons "union-def" (list ch0 ch1 ch2 ch3 ch5))))))))))))))))
)

; Repetition helper for union-variant rule
(define (cst-parse-union-variant-rep3 tok-rep acc)
  (let* ((r-rep
    (cst-parse-record-field tok-rep)
  ))
  (if (null? r-rep)
    (cons tok-rep (reverse acc))
    (cst-parse-union-variant-rep3 (car r-rep) (cons (cdr r-rep) acc)))))

; union_variant
(define (cst-parse-union-variant tokens)
  (let* ((r0 (cst-match-TkLParen tokens)))
  (if (null? r0) nil
  (let* ((t0 (car r0)) (ch0 (cdr r0)))
  (let* ((r1 (cst-match-TkIdentifier t0)))
  (if (null? r1) nil
  (let* ((t1 (car r1)) (ch1 (cdr r1)))
  (let* ((r2 (cst-parse-union-variant-rep3 t1 (list)))
         (t2 (car r2)) (ch2 (cdr r2)))
  (let* ((r4 (cst-match-TkRParen t2)))
  (if (null? r4) nil
  (let* ((t4 (car r4)) (ch4 (cdr r4)))
  (cons t4 (cons "union-variant" (list ch0 ch1 ch2 ch4)))))))))))))
)

; Repetition helper for define rule
(define (cst-parse-define-rep5 tok-rep acc)
  (let* ((r-rep
    (cst-parse-expr tok-rep)
  ))
  (if (null? r-rep)
    (cons tok-rep (reverse acc))
    (cst-parse-define-rep5 (car r-rep) (cons (cdr r-rep) acc)))))

; define
(define (cst-parse-define tokens)
  (let* ((r0 (cst-match-TkLParen tokens)))
  (if (null? r0) nil
  (let* ((t0 (car r0)) (ch0 (cdr r0)))
  (let* ((r1 (cst-match-kw t0 "define")))
  (if (null? r1) nil
  (let* ((t1 (car r1)) (ch1 (cdr r1)))
  (let* ((r2 (cst-parse-name-or-signature t1)))
  (if (null? r2) nil
  (let* ((t2 (car r2)) (ch2 (cdr r2)))
  (let* ((r3 (cst-parse-expr t2)))
  (if (null? r3) nil
  (let* ((t3 (car r3)) (ch3 (cdr r3)))
  (let* ((r4 (cst-parse-define-rep5 t3 (list)))
         (t4 (car r4)) (ch4 (cdr r4)))
  (let* ((r6 (cst-match-TkRParen t4)))
  (if (null? r6) nil
  (let* ((t6 (car r6)) (ch6 (cdr r6)))
  (cons t6 (cons "define" (list ch0 ch1 ch2 ch3 ch4 ch6)))))))))))))))))))
)

(define (cst-parse-name-or-signature-alt6 tokens)
  (let* ((r7 (cst-match-TkLParen tokens)))
  (if (null? r7) nil
  (let* ((t7 (car r7)) (ch7 (cdr r7)))
  (let* ((r8 (cst-match-TkIdentifier t7)))
  (if (null? r8) nil
  (let* ((t8 (car r8)) (ch8 (cdr r8)))
  (let* ((r9 (cst-parse-name-or-signature-rep10 t8 (list)))
         (t9 (car r9)) (ch9 (cdr r9)))
  (let* ((r11 (cst-parse-name-or-signature-opt12 t9))
         (t11 (car r11)) (ch11 (cdr r11)))
  (let* ((r15 (cst-match-TkRParen t11)))
  (if (null? r15) nil
  (let* ((t15 (car r15)) (ch15 (cdr r15)))
  (cons t15 (cons "name-or-signature-alt" (list ch7 ch8 ch9 ch11 ch15))))))))))))))
)

; Optional element helper for name-or-signature rule
(define (cst-parse-name-or-signature-opt12 tok-opt)
  (let* ((r-opt
    (let* ((r13 (cst-match-kw tok-opt "->")))
    (if (null? r13) nil
    (let* ((t13 (car r13)) (ch13 (cdr r13)))
    (let* ((r14 (cst-parse-type-annotation t13)))
    (if (null? r14) nil
    (let* ((t14 (car r14)) (ch14 (cdr r14)))
    (cons t14 (cons "name-or-signature-opt-item" (list ch13 ch14)))))))))
  ))
  (if (null? r-opt)
    (cons tok-opt nil)
    r-opt)))

; Repetition helper for name-or-signature rule
(define (cst-parse-name-or-signature-rep10 tok-rep acc)
  (let* ((r-rep
    (cst-parse-typed-param tok-rep)
  ))
  (if (null? r-rep)
    (cons tok-rep (reverse acc))
    (cst-parse-name-or-signature-rep10 (car r-rep) (cons (cdr r-rep) acc)))))

(define (cst-parse-name-or-signature-alt0 tokens)
  (let* ((r1 (cst-match-TkIdentifier tokens)))
  (if (null? r1) nil
  (let* ((t1 (car r1)) (ch1 (cdr r1)))
  (let* ((r2 (cst-parse-name-or-signature-opt3 t1))
         (t2 (car r2)) (ch2 (cdr r2)))
  (cons t2 (cons "name-or-signature-alt" (list ch1 ch2)))))))
)

; Optional element helper for name-or-signature rule
(define (cst-parse-name-or-signature-opt3 tok-opt)
  (let* ((r-opt
    (let* ((r4 (cst-match-TkColon tok-opt)))
    (if (null? r4) nil
    (let* ((t4 (car r4)) (ch4 (cdr r4)))
    (let* ((r5 (cst-parse-type-annotation t4)))
    (if (null? r5) nil
    (let* ((t5 (car r5)) (ch5 (cdr r5)))
    (cons t5 (cons "name-or-signature-opt-item" (list ch4 ch5)))))))))
  ))
  (if (null? r-opt)
    (cons tok-opt nil)
    r-opt)))

; name_or_signature
(define (cst-parse-name-or-signature tokens)
  (let* ((r-alt16 (cst-parse-name-or-signature-alt0 tokens)))
  (if (not (null? r-alt16)) r-alt16
  (cst-parse-name-or-signature-alt6 tokens)))
)

(define (cst-parse-typed-param-alt0 tokens)
  (let* ((r1 (cst-match-TkLParen tokens)))
  (if (null? r1) nil
  (let* ((t1 (car r1)) (ch1 (cdr r1)))
  (let* ((r2 (cst-match-TkIdentifier t1)))
  (if (null? r2) nil
  (let* ((t2 (car r2)) (ch2 (cdr r2)))
  (let* ((r3 (cst-match-TkColon t2)))
  (if (null? r3) nil
  (let* ((t3 (car r3)) (ch3 (cdr r3)))
  (let* ((r4 (cst-parse-type-annotation t3)))
  (if (null? r4) nil
  (let* ((t4 (car r4)) (ch4 (cdr r4)))
  (let* ((r5 (cst-match-TkRParen t4)))
  (if (null? r5) nil
  (let* ((t5 (car r5)) (ch5 (cdr r5)))
  (cons t5 (cons "typed-param-alt" (list ch1 ch2 ch3 ch4 ch5))))))))))))))))))
)

; typed_param
(define (cst-parse-typed-param tokens)
  (let* ((r-alt6 (cst-parse-typed-param-alt0 tokens)))
  (if (not (null? r-alt6)) r-alt6
  (cst-match-TkIdentifier tokens)))
)

(define (cst-parse-type-annotation-alt0 tokens)
  (let* ((r1 (cst-match-TkLParen tokens)))
  (if (null? r1) nil
  (let* ((t1 (car r1)) (ch1 (cdr r1)))
  (let* ((r2 (cst-parse-type-annotation-rep3 t1 (list)))
         (t2 (car r2)) (ch2 (cdr r2)))
  (let* ((r4 (cst-match-TkRParen t2)))
  (if (null? r4) nil
  (let* ((t4 (car r4)) (ch4 (cdr r4)))
  (cons t4 (cons "type-annotation-alt" (list ch1 ch2 ch4))))))))))
)

; Repetition helper for type-annotation rule
(define (cst-parse-type-annotation-rep3 tok-rep acc)
  (let* ((r-rep
    (cst-parse-type-annotation tok-rep)
  ))
  (if (null? r-rep)
    (cons tok-rep (reverse acc))
    (cst-parse-type-annotation-rep3 (car r-rep) (cons (cdr r-rep) acc)))))

; type_annotation
(define (cst-parse-type-annotation tokens)
  (let* ((r-alt6 (cst-parse-type-annotation-alt0 tokens)))
  (if (not (null? r-alt6)) r-alt6
  (let* ((r-alt5 (cst-match-TkIdentifier tokens)))
  (if (not (null? r-alt5)) r-alt5
  (cst-match-TkInteger tokens)))))
)

; expr
(define (cst-parse-expr tokens)
  (let* ((r-alt1 (cst-parse-atom tokens)))
  (if (not (null? r-alt1)) r-alt1
  (let* ((r-alt0 (cst-parse-quoted tokens)))
  (if (not (null? r-alt0)) r-alt0
  (cst-parse-compound tokens)))))
)

; atom
(define (cst-parse-atom tokens)
  (let* ((r-alt4 (cst-match-TkString tokens)))
  (if (not (null? r-alt4)) r-alt4
  (let* ((r-alt3 (cst-match-TkInteger tokens)))
  (if (not (null? r-alt3)) r-alt3
  (let* ((r-alt2 (let* ((tok-bt (if (null? tokens) nil (car tokens))))
    (if (or (null? tokens) (not (TkBoolean? (token-kind (car tokens))) ) (not (string=? (token-lexeme (car tokens)) "#t")))
      nil
      (cons (cdr tokens) (car tokens))))))
  (if (not (null? r-alt2)) r-alt2
  (let* ((r-alt1 (let* ((tok-bf (if (null? tokens) nil (car tokens))))
    (if (or (null? tokens) (not (TkBoolean? (token-kind (car tokens)))) (not (string=? (token-lexeme (car tokens)) "#f")))
      nil
      (cons (cdr tokens) (car tokens))))))
  (if (not (null? r-alt1)) r-alt1
  (let* ((r-alt0 (cst-match-kw tokens "nil")))
  (if (not (null? r-alt0)) r-alt0
  (cst-match-TkIdentifier tokens)))))))))))
)

; quoted
(define (cst-parse-quoted tokens)
  (let* ((r0 (cst-match-TkQuote tokens)))
  (if (null? r0) nil
  (let* ((t0 (car r0)) (ch0 (cdr r0)))
  (let* ((r1 (cst-match-TkIdentifier t0)))
  (if (null? r1) nil
  (let* ((t1 (car r1)) (ch1 (cdr r1)))
  (cons t1 (cons "quoted" (list ch0 ch1)))))))))
)

; compound
(define (cst-parse-compound tokens)
  (let* ((r-alt6 (cst-parse-if-form tokens)))
  (if (not (null? r-alt6)) r-alt6
  (let* ((r-alt5 (cst-parse-let-form tokens)))
  (if (not (null? r-alt5)) r-alt5
  (let* ((r-alt4 (cst-parse-let-star-form tokens)))
  (if (not (null? r-alt4)) r-alt4
  (let* ((r-alt3 (cst-parse-begin-form tokens)))
  (if (not (null? r-alt3)) r-alt3
  (let* ((r-alt2 (cst-parse-lambda-form tokens)))
  (if (not (null? r-alt2)) r-alt2
  (let* ((r-alt1 (cst-parse-quote-form tokens)))
  (if (not (null? r-alt1)) r-alt1
  (let* ((r-alt0 (cst-parse-match-form tokens)))
  (if (not (null? r-alt0)) r-alt0
  (cst-parse-apply tokens)))))))))))))))
)

; if_form
(define (cst-parse-if-form tokens)
  (let* ((r0 (cst-match-TkLParen tokens)))
  (if (null? r0) nil
  (let* ((t0 (car r0)) (ch0 (cdr r0)))
  (let* ((r1 (cst-match-kw t0 "if")))
  (if (null? r1) nil
  (let* ((t1 (car r1)) (ch1 (cdr r1)))
  (let* ((r2 (cst-parse-expr t1)))
  (if (null? r2) nil
  (let* ((t2 (car r2)) (ch2 (cdr r2)))
  (let* ((r3 (cst-parse-expr t2)))
  (if (null? r3) nil
  (let* ((t3 (car r3)) (ch3 (cdr r3)))
  (let* ((r4 (cst-parse-expr t3)))
  (if (null? r4) nil
  (let* ((t4 (car r4)) (ch4 (cdr r4)))
  (let* ((r5 (cst-match-TkRParen t4)))
  (if (null? r5) nil
  (let* ((t5 (car r5)) (ch5 (cdr r5)))
  (cons t5 (cons "if-form" (list ch0 ch1 ch2 ch3 ch4 ch5)))))))))))))))))))))
)

; Repetition helper for let-form rule
(define (cst-parse-let-form-rep8 tok-rep acc)
  (let* ((r-rep
    (cst-parse-expr tok-rep)
  ))
  (if (null? r-rep)
    (cons tok-rep (reverse acc))
    (cst-parse-let-form-rep8 (car r-rep) (cons (cdr r-rep) acc)))))

; Repetition helper for let-form rule
(define (cst-parse-let-form-rep4 tok-rep acc)
  (let* ((r-rep
    (cst-parse-binding tok-rep)
  ))
  (if (null? r-rep)
    (cons tok-rep (reverse acc))
    (cst-parse-let-form-rep4 (car r-rep) (cons (cdr r-rep) acc)))))

; let_form
(define (cst-parse-let-form tokens)
  (let* ((r0 (cst-match-TkLParen tokens)))
  (if (null? r0) nil
  (let* ((t0 (car r0)) (ch0 (cdr r0)))
  (let* ((r1 (cst-match-kw t0 "let")))
  (if (null? r1) nil
  (let* ((t1 (car r1)) (ch1 (cdr r1)))
  (let* ((r2 (cst-match-TkLParen t1)))
  (if (null? r2) nil
  (let* ((t2 (car r2)) (ch2 (cdr r2)))
  (let* ((r3 (cst-parse-let-form-rep4 t2 (list)))
         (t3 (car r3)) (ch3 (cdr r3)))
  (let* ((r5 (cst-match-TkRParen t3)))
  (if (null? r5) nil
  (let* ((t5 (car r5)) (ch5 (cdr r5)))
  (let* ((r6 (cst-parse-expr t5)))
  (if (null? r6) nil
  (let* ((t6 (car r6)) (ch6 (cdr r6)))
  (let* ((r7 (cst-parse-let-form-rep8 t6 (list)))
         (t7 (car r7)) (ch7 (cdr r7)))
  (let* ((r9 (cst-match-TkRParen t7)))
  (if (null? r9) nil
  (let* ((t9 (car r9)) (ch9 (cdr r9)))
  (cons t9 (cons "let-form" (list ch0 ch1 ch2 ch3 ch5 ch6 ch7 ch9)))))))))))))))))))))))
)

; binding
(define (cst-parse-binding tokens)
  (let* ((r0 (cst-match-TkLParen tokens)))
  (if (null? r0) nil
  (let* ((t0 (car r0)) (ch0 (cdr r0)))
  (let* ((r1 (cst-match-TkIdentifier t0)))
  (if (null? r1) nil
  (let* ((t1 (car r1)) (ch1 (cdr r1)))
  (let* ((r2 (cst-parse-expr t1)))
  (if (null? r2) nil
  (let* ((t2 (car r2)) (ch2 (cdr r2)))
  (let* ((r3 (cst-match-TkRParen t2)))
  (if (null? r3) nil
  (let* ((t3 (car r3)) (ch3 (cdr r3)))
  (cons t3 (cons "binding" (list ch0 ch1 ch2 ch3)))))))))))))))
)

; Repetition helper for let-star-form rule
(define (cst-parse-let-star-form-rep8 tok-rep acc)
  (let* ((r-rep
    (cst-parse-expr tok-rep)
  ))
  (if (null? r-rep)
    (cons tok-rep (reverse acc))
    (cst-parse-let-star-form-rep8 (car r-rep) (cons (cdr r-rep) acc)))))

; Repetition helper for let-star-form rule
(define (cst-parse-let-star-form-rep4 tok-rep acc)
  (let* ((r-rep
    (cst-parse-binding tok-rep)
  ))
  (if (null? r-rep)
    (cons tok-rep (reverse acc))
    (cst-parse-let-star-form-rep4 (car r-rep) (cons (cdr r-rep) acc)))))

; let_star_form
(define (cst-parse-let-star-form tokens)
  (let* ((r0 (cst-match-TkLParen tokens)))
  (if (null? r0) nil
  (let* ((t0 (car r0)) (ch0 (cdr r0)))
  (let* ((r1 (cst-match-kw t0 "let*")))
  (if (null? r1) nil
  (let* ((t1 (car r1)) (ch1 (cdr r1)))
  (let* ((r2 (cst-match-TkLParen t1)))
  (if (null? r2) nil
  (let* ((t2 (car r2)) (ch2 (cdr r2)))
  (let* ((r3 (cst-parse-let-star-form-rep4 t2 (list)))
         (t3 (car r3)) (ch3 (cdr r3)))
  (let* ((r5 (cst-match-TkRParen t3)))
  (if (null? r5) nil
  (let* ((t5 (car r5)) (ch5 (cdr r5)))
  (let* ((r6 (cst-parse-expr t5)))
  (if (null? r6) nil
  (let* ((t6 (car r6)) (ch6 (cdr r6)))
  (let* ((r7 (cst-parse-let-star-form-rep8 t6 (list)))
         (t7 (car r7)) (ch7 (cdr r7)))
  (let* ((r9 (cst-match-TkRParen t7)))
  (if (null? r9) nil
  (let* ((t9 (car r9)) (ch9 (cdr r9)))
  (cons t9 (cons "let-star-form" (list ch0 ch1 ch2 ch3 ch5 ch6 ch7 ch9)))))))))))))))))))))))
)

; Repetition helper for begin-form rule
(define (cst-parse-begin-form-rep4 tok-rep acc)
  (let* ((r-rep
    (cst-parse-expr tok-rep)
  ))
  (if (null? r-rep)
    (cons tok-rep (reverse acc))
    (cst-parse-begin-form-rep4 (car r-rep) (cons (cdr r-rep) acc)))))

; begin_form
(define (cst-parse-begin-form tokens)
  (let* ((r0 (cst-match-TkLParen tokens)))
  (if (null? r0) nil
  (let* ((t0 (car r0)) (ch0 (cdr r0)))
  (let* ((r1 (cst-match-kw t0 "begin")))
  (if (null? r1) nil
  (let* ((t1 (car r1)) (ch1 (cdr r1)))
  (let* ((r2 (cst-parse-expr t1)))
  (if (null? r2) nil
  (let* ((t2 (car r2)) (ch2 (cdr r2)))
  (let* ((r3 (cst-parse-begin-form-rep4 t2 (list)))
         (t3 (car r3)) (ch3 (cdr r3)))
  (let* ((r5 (cst-match-TkRParen t3)))
  (if (null? r5) nil
  (let* ((t5 (car r5)) (ch5 (cdr r5)))
  (cons t5 (cons "begin-form" (list ch0 ch1 ch2 ch3 ch5))))))))))))))))
)

; Repetition helper for lambda-form rule
(define (cst-parse-lambda-form-rep8 tok-rep acc)
  (let* ((r-rep
    (cst-parse-expr tok-rep)
  ))
  (if (null? r-rep)
    (cons tok-rep (reverse acc))
    (cst-parse-lambda-form-rep8 (car r-rep) (cons (cdr r-rep) acc)))))

; Repetition helper for lambda-form rule
(define (cst-parse-lambda-form-rep4 tok-rep acc)
  (let* ((r-rep
    (cst-match-TkIdentifier tok-rep)
  ))
  (if (null? r-rep)
    (cons tok-rep (reverse acc))
    (cst-parse-lambda-form-rep4 (car r-rep) (cons (cdr r-rep) acc)))))

; lambda_form
(define (cst-parse-lambda-form tokens)
  (let* ((r0 (cst-match-TkLParen tokens)))
  (if (null? r0) nil
  (let* ((t0 (car r0)) (ch0 (cdr r0)))
  (let* ((r1 (cst-match-kw t0 "lambda")))
  (if (null? r1) nil
  (let* ((t1 (car r1)) (ch1 (cdr r1)))
  (let* ((r2 (cst-match-TkLParen t1)))
  (if (null? r2) nil
  (let* ((t2 (car r2)) (ch2 (cdr r2)))
  (let* ((r3 (cst-parse-lambda-form-rep4 t2 (list)))
         (t3 (car r3)) (ch3 (cdr r3)))
  (let* ((r5 (cst-match-TkRParen t3)))
  (if (null? r5) nil
  (let* ((t5 (car r5)) (ch5 (cdr r5)))
  (let* ((r6 (cst-parse-expr t5)))
  (if (null? r6) nil
  (let* ((t6 (car r6)) (ch6 (cdr r6)))
  (let* ((r7 (cst-parse-lambda-form-rep8 t6 (list)))
         (t7 (car r7)) (ch7 (cdr r7)))
  (let* ((r9 (cst-match-TkRParen t7)))
  (if (null? r9) nil
  (let* ((t9 (car r9)) (ch9 (cdr r9)))
  (cons t9 (cons "lambda-form" (list ch0 ch1 ch2 ch3 ch5 ch6 ch7 ch9)))))))))))))))))))))))
)

; quote_form
(define (cst-parse-quote-form tokens)
  (let* ((r0 (cst-match-TkLParen tokens)))
  (if (null? r0) nil
  (let* ((t0 (car r0)) (ch0 (cdr r0)))
  (let* ((r1 (cst-match-kw t0 "quote")))
  (if (null? r1) nil
  (let* ((t1 (car r1)) (ch1 (cdr r1)))
  (let* ((r2 (cst-match-TkIdentifier t1)))
  (if (null? r2) nil
  (let* ((t2 (car r2)) (ch2 (cdr r2)))
  (let* ((r3 (cst-match-TkRParen t2)))
  (if (null? r3) nil
  (let* ((t3 (car r3)) (ch3 (cdr r3)))
  (cons t3 (cons "quote-form" (list ch0 ch1 ch2 ch3)))))))))))))))
)

; Repetition helper for match-form rule
(define (cst-parse-match-form-rep4 tok-rep acc)
  (let* ((r-rep
    (cst-parse-match-arm tok-rep)
  ))
  (if (null? r-rep)
    (cons tok-rep (reverse acc))
    (cst-parse-match-form-rep4 (car r-rep) (cons (cdr r-rep) acc)))))

; match_form
(define (cst-parse-match-form tokens)
  (let* ((r0 (cst-match-TkLParen tokens)))
  (if (null? r0) nil
  (let* ((t0 (car r0)) (ch0 (cdr r0)))
  (let* ((r1 (cst-match-kw t0 "match")))
  (if (null? r1) nil
  (let* ((t1 (car r1)) (ch1 (cdr r1)))
  (let* ((r2 (cst-parse-expr t1)))
  (if (null? r2) nil
  (let* ((t2 (car r2)) (ch2 (cdr r2)))
  (let* ((r3 (cst-parse-match-form-rep4 t2 (list)))
         (t3 (car r3)) (ch3 (cdr r3)))
  (let* ((r5 (cst-match-TkRParen t3)))
  (if (null? r5) nil
  (let* ((t5 (car r5)) (ch5 (cdr r5)))
  (cons t5 (cons "match-form" (list ch0 ch1 ch2 ch3 ch5))))))))))))))))
)

; Repetition helper for match-arm rule
(define (cst-parse-match-arm-rep4 tok-rep acc)
  (let* ((r-rep
    (cst-parse-expr tok-rep)
  ))
  (if (null? r-rep)
    (cons tok-rep (reverse acc))
    (cst-parse-match-arm-rep4 (car r-rep) (cons (cdr r-rep) acc)))))

; match_arm
(define (cst-parse-match-arm tokens)
  (let* ((r0 (cst-match-TkLParen tokens)))
  (if (null? r0) nil
  (let* ((t0 (car r0)) (ch0 (cdr r0)))
  (let* ((r1 (cst-parse-match-pat t0)))
  (if (null? r1) nil
  (let* ((t1 (car r1)) (ch1 (cdr r1)))
  (let* ((r2 (cst-parse-expr t1)))
  (if (null? r2) nil
  (let* ((t2 (car r2)) (ch2 (cdr r2)))
  (let* ((r3 (cst-parse-match-arm-rep4 t2 (list)))
         (t3 (car r3)) (ch3 (cdr r3)))
  (let* ((r5 (cst-match-TkRParen t3)))
  (if (null? r5) nil
  (let* ((t5 (car r5)) (ch5 (cdr r5)))
  (cons t5 (cons "match-arm" (list ch0 ch1 ch2 ch3 ch5))))))))))))))))
)

(define (cst-parse-match-pat-alt0 tokens)
  (let* ((r1 (cst-match-TkLParen tokens)))
  (if (null? r1) nil
  (let* ((t1 (car r1)) (ch1 (cdr r1)))
  (let* ((r2 (cst-match-TkIdentifier t1)))
  (if (null? r2) nil
  (let* ((t2 (car r2)) (ch2 (cdr r2)))
  (let* ((r3 (cst-parse-match-pat-rep4 t2 (list)))
         (t3 (car r3)) (ch3 (cdr r3)))
  (let* ((r5 (cst-match-TkRParen t3)))
  (if (null? r5) nil
  (let* ((t5 (car r5)) (ch5 (cdr r5)))
  (cons t5 (cons "match-pat-alt" (list ch1 ch2 ch3 ch5)))))))))))))
)

; Repetition helper for match-pat rule
(define (cst-parse-match-pat-rep4 tok-rep acc)
  (let* ((r-rep
    (cst-match-TkIdentifier tok-rep)
  ))
  (if (null? r-rep)
    (cons tok-rep (reverse acc))
    (cst-parse-match-pat-rep4 (car r-rep) (cons (cdr r-rep) acc)))))

; match_pat
(define (cst-parse-match-pat tokens)
  (let* ((r-alt6 (cst-parse-match-pat-alt0 tokens)))
  (if (not (null? r-alt6)) r-alt6
  (cst-match-TkIdentifier tokens)))
)

; Repetition helper for apply rule
(define (cst-parse-apply-rep3 tok-rep acc)
  (let* ((r-rep
    (cst-parse-expr tok-rep)
  ))
  (if (null? r-rep)
    (cons tok-rep (reverse acc))
    (cst-parse-apply-rep3 (car r-rep) (cons (cdr r-rep) acc)))))

; apply
(define (cst-parse-apply tokens)
  (let* ((r0 (cst-match-TkLParen tokens)))
  (if (null? r0) nil
  (let* ((t0 (car r0)) (ch0 (cdr r0)))
  (let* ((r1 (cst-parse-expr t0)))
  (if (null? r1) nil
  (let* ((t1 (car r1)) (ch1 (cdr r1)))
  (let* ((r2 (cst-parse-apply-rep3 t1 (list)))
         (t2 (car r2)) (ch2 (cdr r2)))
  (let* ((r4 (cst-match-TkRParen t2)))
  (if (null? r4) nil
  (let* ((t4 (car r4)) (ch4 (cdr r4)))
  (cons t4 (cons "apply" (list ch0 ch1 ch2 ch4)))))))))))))
)

