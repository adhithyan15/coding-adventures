{
  "$schema": "https://raw.githubusercontent.com/adhithyan15/coding-adventures/main/code/specs/schemas/required_capabilities.schema.json",
  "version": 1,
  "package": "rust/uuid",
  "capabilities": [
    {
      "category": "time",
      "action": "read",
      "target": "*",
      "justification": "Uses std::time::SystemTime::now() to generate time-based UUID v1 and v7 timestamps."
    }
  ],
  "justification": "UUID generator. Reads system time for time-based UUID variants (v1, v7)."
}
