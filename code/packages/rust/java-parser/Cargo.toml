[package]
name = "coding-adventures-java-parser"
version = "0.1.0"
edition = "2021"
description = "Java parser — parses Java source code into an AST using the grammar-driven parser and the java.grammar file"

[dependencies]
grammar-tools = { path = "../grammar-tools" }
lexer = { path = "../lexer" }
parser = { path = "../parser" }
coding-adventures-java-lexer = { path = "../java-lexer" }
