- Add a shared Copy Address chrome transaction that resolves the committed
  history URL, projects disabled/status state, and emits a typed clipboard
  effect without moving URL policy into platform hosts.
