<!-- Auto-generated by mosaic-emit-xaml. Do not edit. -->
<ContentDialog
    x:Class="Mosaic.Generated.HelloDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Mosaic.Generated" Title="{x:Bind DialogTitle, Mode=OneWay}" Closed="OnHostDialogClose_1">

    <!-- HostDialog #1 open-state: bind 'Open'; host code-behind watches this DP and calls ShowAsync()/Hide() accordingly. -->
    <!-- HostDialog #1 onClose: dispatches Close; handler wired in code-behind. -->
    <StackPanel Orientation="Vertical" Padding="16">
        <Border Padding="8">
            <TextBlock Text="{x:Bind Message}"/>
        </Border>
        <Border Padding="8">
            <Button x:Name="CloseBtn" Content="Close" Click="CloseBtn_Click"/>
        </Border>
    </StackPanel>

</ContentDialog>
