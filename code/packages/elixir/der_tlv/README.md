# der-tlv (Elixir)

`CodingAdventures.DerTlv` provides bounded canonical DER framing with
sub-binary views, explicit limits, stable payload-blind errors, and an
immutable iterative sibling cursor. It does not interpret ASN.1 values, parse
X.509, perform cryptography, or access the host.
