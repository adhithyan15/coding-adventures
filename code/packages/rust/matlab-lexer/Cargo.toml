[package]
name = "coding-adventures-matlab-lexer"
version = "0.1.0"
edition = "2021"
description = "Tokenizer for the MATLAB language (the array frontend on array-runtime)"

[dependencies]
grammar-tools = { path = "../grammar-tools" }
lexer = { path = "../lexer" }
