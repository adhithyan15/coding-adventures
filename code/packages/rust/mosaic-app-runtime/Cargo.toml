[package]
name = "mosaic-app-runtime"
version = "0.1.0"
edition = "2021"
description = "Shared Rust application contract and deterministic host protocol for Mosaic apps."
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
