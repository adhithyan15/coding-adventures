[package]
name = "in-memory-data-store-protocol"
version = "0.1.0"
edition = "2021"
description = "RESP-to-engine command frame conversion for the in-memory data store"
license = "MIT"

[dependencies]
