[package]
name = "draw-instructions-metal"
version = "0.1.0"
edition = "2021"
description = "Metal GPU renderer for draw-instructions scenes — renders DrawScene to PixelBuffer via Apple's Metal API"

[dependencies]
draw-instructions = { path = "../draw-instructions" }
draw-instructions-pixels = { path = "../draw-instructions-pixels" }
objc-bridge = { path = "../objc-bridge" }

[dev-dependencies]
draw-instructions-png = { path = "../draw-instructions-png" }
