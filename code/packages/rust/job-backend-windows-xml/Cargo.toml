[package]
name = "job-backend-windows-xml"
version = "0.1.0"
edition = "2021"
description = "Windows Task Scheduler XML backend for native job installation"

[dependencies]
job-core = { path = "../job-core" }
