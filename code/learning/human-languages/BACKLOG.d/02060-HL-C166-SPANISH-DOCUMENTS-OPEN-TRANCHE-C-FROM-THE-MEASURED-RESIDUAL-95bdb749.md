## HL-C166 — Spanish documents open tranche C from the measured residual

The required post-chapter-405 audit was re-run only after chapter 401's four
direction words were confirmed on A1 paths. Under the sitting's deliberately
lenient ownership rules, the corrected baseline answers **21/25 reading and
18/25 listening on mock 1**, and **20/25 reading and 15/25 listening on mock
2**. **26 objective items still fail**: 11 on mock 1 and 15 on mock 2. The
residual is therefore 35 distinct required lexemes, not the old 61-word queue.

One reproducibility debt remains explicit: the historical scratch harness was
not checked in. Reconstructing it from `sitting-2026-08-26.md` and the answer
keys reproduces the published post-qualities score to within one objective
item (44 failures versus the published 45), with the difference confined to
the wide citation-form/number alias index. Current measurements use the more
generous 44-item reconstruction, so they are an upper bound rather than a
manufactured failure. The next loop should turn this calibrated reconstruction
into a checked-in report before making a new aggregate pass claim.

Chapter **406** opens tranche C with **el cliente**, **el documento**, **el
dato**, **el impreso**, and **rellenar**. This is the strongest coherent
five-word bundle in the measured residual: `cliente` closes one item in each
mock, while `documento` and `dato` close one more item apiece. The chapter moves
the generous objective residual **26 → 22** and also prepares the four-word
foreign-package form item without pretending that `impreso` and `rellenar`
alone close it.

The next bounded tranche should be selected from the still-failing items, with
the same whole-item arithmetic. Do not rank one-occurrence words independently:
the education, music, registration, and foreign-package items each require a
bundle, while the remaining single blockers can buy a full point alone.
