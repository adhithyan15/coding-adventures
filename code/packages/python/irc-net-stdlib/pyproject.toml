[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "coding-adventures-irc-net-stdlib"
version = "0.1.0"
description = "Level 1 network implementation: stdlib sockets and threads"
requires-python = ">=3.12"
license = "MIT"
authors = [{ name = "Adhithya Rajasekaran" }]
readme = "README.md"
dependencies = [
    "coding-adventures-irc-framing",
    "coding-adventures-irc-proto",
    "coding-adventures-irc-server",
]

[project.optional-dependencies]
dev = ["pytest>=8.0", "pytest-cov>=5.0", "ruff>=0.4", "mypy>=1.10"]

[tool.hatch.build.targets.wheel]
packages = ["src/irc_net_stdlib"]

[tool.ruff]
target-version = "py312"
line-length = 88

[tool.ruff.lint]
select = ["E", "W", "F", "I", "UP", "B", "SIM", "ANN"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--cov=irc_net_stdlib --cov-report=term-missing --cov-fail-under=80"

[tool.coverage.run]
source = ["src/irc_net_stdlib"]

[tool.coverage.report]
fail_under = 80
show_missing = true
