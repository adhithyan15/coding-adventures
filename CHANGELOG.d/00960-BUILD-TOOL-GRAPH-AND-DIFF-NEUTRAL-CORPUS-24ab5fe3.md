### Build-tool graph and diff neutral corpus

- Closed graph as a bounded process-free domain with an independent iterative
  topology oracle and neutral isolated-node, chain, disconnected-component,
  and stable cycle-error fixtures.
- Added the missing fail-closed unknown-path diff-selection fixture and raised
  the checked neutral corpus total from 141 to 146 without expanding trusted
  execution authority.

