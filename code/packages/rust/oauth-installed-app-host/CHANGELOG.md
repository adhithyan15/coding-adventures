# Changelog

## Unreleased

- Added an audit-first installed-app loopback host with literal-loopback
  binding, injected external-user-agent opening, provider/trace/redirect
  binding, one-connection lifetime, strict bounded HTTP/1.1 callback parsing,
  zeroizing callback ownership, closed diagnostics, real socket tests, and
  durable attempt/result audit events around every external effect.
