{
  "$schema": "https://raw.githubusercontent.com/adhithyan15/coding-adventures/main/code/specs/schemas/required_capabilities.schema.json",
  "version": 1,
  "package": "rust/twig-hover",
  "capabilities": [],
  "justification": "Pure data → typed hover info: parses Twig source via twig-parser, looks up the symbol under the cursor, returns Option<Hover>.  No I/O, no FFI, no filesystem/network/process/env access.  Two deps (twig-parser, twig-document-symbols) — both empty."
}
