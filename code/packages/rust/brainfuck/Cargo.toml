[package]
name = "brainfuck"
version = "0.4.0"
edition = "2021"
build = "build.rs"

[dependencies]
virtual-machine = { path = "../virtual-machine" }
grammar-tools = { path = "../grammar-tools" }
lexer = { path = "../lexer" }
parser = { path = "../parser" }

[build-dependencies]
grammar-tools = { path = "../grammar-tools" }
