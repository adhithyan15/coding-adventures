[package]
name = "coding_adventures_correlation_vector"
version = "0.1.0"
edition = "2021"
description = "Correlation Vector — lightweight, append-only provenance tracking for any data pipeline"

[dependencies]
coding_adventures_sha256 = { path = "../sha256" }
coding-adventures-json-value = { path = "../json-value" }
coding-adventures-json-serializer = { path = "../json-serializer" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
