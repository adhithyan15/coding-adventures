"""Pipeline Visualizer — generates HTML reports from the computing stack.

Runs source code through the full pipeline (lexer → parser → compiler → VM/simulator)
and generates a self-contained HTML file showing every stage.

Usage:
    python generate_report.py

See specs/10-pipeline.md and specs/11-html-visualizer.md for design.
"""

# Implementation pending — see specs for design
