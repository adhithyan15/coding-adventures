[package]
name = "adjudication-coverage"
version = "0.1.0"
edition = "2021"
description = "Coverage checker for the adjudication IR (ADJ02). Verifies every meaningful input span belongs to some IR node."

[dependencies]
adjudication-ir = { path = "../adjudication-ir" }

[dev-dependencies]
logic-core = { path = "../logic-core" }
