### Added

- Added language-neutral exact-at-limit and over-limit fixtures for a fixed
  50,000,000-unit diff-selection glob-match preflight, including Unicode-scalar
  accounting, recursive BUILD-front exemptions, stable fail-closed diagnostics,
  and an independent oracle that no longer accepts arbitrary adapter errors.
