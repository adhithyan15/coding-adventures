# Changelog

## [0.1.0] - Unreleased

### Added

- Added a TypeScript mark-and-sweep garbage collector with heap objects, root scanning, and GC statistics.
- Added Lisp-oriented `ConsCell`, `Symbol`, `LispClosure`, and `SymbolTable` helpers for parity with the Python and Rust packages.
