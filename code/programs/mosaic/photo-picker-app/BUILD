# Compile-check the Mosaic package (.mil/.mll/.msl + manifest parse).
# A real native project is emitted via mosaic-compile, e.g.:
#   mosaic-compile pkg --backend xaml --output <dir> --emit-project \
#     --theme light --profile native-complete \
#     --runtime-library <path to the built photo-picker-mosaic-app cdylib> \
#     code/programs/mosaic/photo-picker-app
cargo test
