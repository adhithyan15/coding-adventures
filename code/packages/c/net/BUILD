# build-tool: deps=c/iso-harness c/platform-harness c/os-platform
# net (Unix backend: macOS + Linux). Compiles the POSIX BSD-sockets backend
# under every present C compiler via platform-harness. Windows uses BUILD_windows
# (the Winsock backend, linking ws2_32).
sh tools/run.sh
