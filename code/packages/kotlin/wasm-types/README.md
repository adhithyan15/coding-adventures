# wasm-types

Native Kotlin implementation of the `wasm-types` package.

This package provides:

- WebAssembly value, import, export, and memory types
- `WasmModule` as the in-memory module container
- helper constants and `makeFuncType`
