# Changelog

## Unreleased

New entries are committed as independently owned Markdown files in this
directory so parallel script work does not collide in `CHANGELOG.md`. The
existing monolith remains the historical record until it is migrated.
