# BUILD-EXEMPTIONS — crates that have a Cargo.toml but no BUILD file
#
# The build tool discovers work by scanning for BUILD files. A crate without
# one is therefore invisible to it: never built, its test targets never
# compiled, its assertions never run, and `cargo clippy --all-targets -- -D
# warnings` never applied. In a Cargo workspace that failure is silent, because
# every sibling listing the crate as a path dependency keeps it COMPILING while
# nothing tests or lints it. That is how 84 crates accumulated unnoticed, two of
# them carrying live clippy errors.
#
# `build-tool -validate-build-files` now fails on any crate directory that has a
# Cargo.toml, no BUILD, and no entry here. A brand-new crate is in neither state,
# so it fails the first time it is pushed. That is the point: writing the missing
# BUILD files fixes today's omissions, but only this gate makes the next one
# impossible.
#
# FORMAT
#
#   <KIND> <repo-relative path>  # <reason>
#
# KIND is one of:
#
#   EXCLUDED  This crate genuinely should never have a BUILD — a compile-only
#             bridge with nothing to run, or a crate built by a foreign
#             toolchain. The reason must say which, and where it IS covered.
#
#   PENDING   A real crate that ought to be built, tested and linted, but is not
#             yet. This is a backlog. It is meant to shrink.
#
# The reason is mandatory. An exemption with no reason is indistinguishable from
# an oversight, which is the thing this file exists to make impossible.
#
# STALE ENTRIES ARE ALSO AN ERROR. If a listed crate gains a BUILD file, or its
# directory disappears, validation fails until the line is deleted. So landing a
# BUILD for a PENDING crate forces the same change to remove its entry here, and
# this list cannot quietly outlive the problem it describes.
#
# ---------------------------------------------------------------------------
# EXCLUDED — never gets a BUILD
# ---------------------------------------------------------------------------

EXCLUDED code/packages/rust/conduit-jni  # compile-only JNI bridge; cdylib loaded by code/packages/java/conduit, which builds and tests it
EXCLUDED code/packages/rust/engram-core-wasm  # wasm32-only cdylib; built and tested via engram-wasm, which has its own BUILD
EXCLUDED code/packages/rust/irc-server-native-jni  # compile-only JNI bridge for code/packages/java/irc-server-native, which builds and tests it
EXCLUDED code/packages/rust/jit-loader-macos  # macOS-only mmap/mprotect JIT loader; no host-portable test target
EXCLUDED code/packages/rust/jni-bridge  # compile-only JNI helper crate; exercised through md5-native-jni / sha256-native-jni / silicon-rust-jni, which have their own BUILDs
EXCLUDED code/packages/rust/spreadsheet-android-jni  # compile-only JNI bridge; built by code/programs/kotlin/visicalc-android/scripts/build.sh against the Android NDK, not the host toolchain

# ---------------------------------------------------------------------------
# PENDING — known gaps, each needs a BUILD file
#
# Every crate below is a workspace member with a spec under code/specs/, so none
# of them is dead code. They are simply undiscovered. Ordered by area; the
# dependent count in each reason is the number of in-repo crates that already
# depend on it, which is the rough order to work through them in.
# ---------------------------------------------------------------------------

# Backends and encoders
PENDING code/packages/rust/aarch64-backend  # 2 in-repo dependents (jit-loader-macos, twig-aot); never built, tested or linted
PENDING code/packages/rust/x86_64-backend  # 2 in-repo dependents (twig-aot, x86-simulator); never built, tested or linted
PENDING code/packages/rust/iir-refinement-pass  # 1 in-repo dependent (twig-aot); never built, tested or linted

# Compiler frontends and language bridges
PENDING code/packages/rust/brainfuck-clr-compiler  # no in-repo dependents; never built, tested or linted
PENDING code/packages/rust/nib-clr-compiler  # no in-repo dependents; never built, tested or linted
PENDING code/packages/rust/dartmouth-basic-ge225-compiler  # no in-repo dependents; never built, tested or linted
PENDING code/packages/rust/dartmouth-basic-ir-compiler  # 1 in-repo dependent (dartmouth-basic-ge225-compiler); never built, tested or linted
PENDING code/packages/rust/nib-iir-compiler  # 3 in-repo dependents (jit-loader-macos, lang-aot, nib-dap); never built, tested or linted
PENDING code/packages/rust/oct-iir-compiler  # 2 in-repo dependents (lang-aot, oct-dap); never built, tested or linted
PENDING code/packages/rust/oct-type-checker  # 1 in-repo dependent (oct-iir-compiler); never built, tested or linted
PENDING code/packages/rust/twig-type-checker  # 2 in-repo dependents (twig-ir-compiler, twig-module-driver); never built, tested or linted
PENDING code/packages/rust/grammar-type-checker  # 1 in-repo dependent (twig-type-checker); never built, tested or linted
PENDING code/packages/rust/twig-dap  # no in-repo dependents; never built, tested or linted
PENDING code/packages/rust/twig-lsp-bridge  # no in-repo dependents; never built, tested or linted
PENDING code/packages/rust/twig-module-driver  # 7 deep-recursion self-compile tests reserve 768 MiB / 3 GiB thread stacks and fail on a Windows host; needs Linux verification before it can be watched
PENDING code/packages/rust/lang-refinement-checker  # 2 in-repo dependents (iir-refinement-pass, lang-refinement-protocol); never built, tested or linted
PENDING code/packages/rust/lang-refinement-protocol  # 1 in-repo dependent (twig-type-checker); never built, tested or linted
PENDING code/packages/rust/sir-bench  # no in-repo dependents; never built, tested or linted
PENDING code/packages/rust/sir-conformance  # no in-repo dependents; never built, tested or linted

# Spreadsheet / R function catalog
PENDING code/packages/rust/array-core  # no in-repo dependents; never built, tested or linted
PENDING code/packages/rust/data-frame  # 1 in-repo dependent (database-core); never built, tested or linted; also missing README.md + CHANGELOG.md
PENDING code/packages/rust/database-core  # no in-repo dependents; never built, tested or linted; also missing README.md + CHANGELOG.md
PENDING code/packages/rust/engineering-core  # no in-repo dependents; never built, tested or linted; also missing README.md + CHANGELOG.md
PENDING code/packages/rust/financial-core  # 1 in-repo dependent (spreadsheet-core); never built, tested or linted; also missing README.md + CHANGELOG.md
PENDING code/packages/rust/lookup-core  # 1 in-repo dependent (spreadsheet-core); never built, tested or linted
PENDING code/packages/rust/math-core  # 1 in-repo dependent (spreadsheet-core); never built, tested or linted; also missing README.md + CHANGELOG.md
PENDING code/packages/rust/number-format-core  # 1 in-repo dependent (spreadsheet-core); never built, tested or linted
PENDING code/packages/rust/statistics-core  # 2 in-repo dependents (s-runtime, spreadsheet-core); never built, tested or linted; also missing README.md + CHANGELOG.md
PENDING code/packages/rust/text-core  # 1 in-repo dependent (spreadsheet-core); never built, tested or linted; also missing README.md + CHANGELOG.md
PENDING code/packages/rust/wall-clock  # 3 in-repo dependents (datetime-core, financial-core, spreadsheet-core); never built, tested or linted; also missing README.md + CHANGELOG.md

# Paint VM backends (need native graphics libraries)
PENDING code/packages/rust/paint-vm-cairo  # 5 in-repo dependents (barcode-1d, barcode-2d, html-to-paint, venture-browser-cairo, …); never built, tested or linted
PENDING code/packages/rust/paint-vm-gpu-core  # 5 in-repo dependents (paint-vm-mesa, paint-vm-opencl, paint-vm-opengl, paint-vm-vulkan, …); never built, tested or linted
PENDING code/packages/rust/paint-vm-mesa  # no in-repo dependents; never built, tested or linted
PENDING code/packages/rust/paint-vm-opencl  # no in-repo dependents; never built, tested or linted
PENDING code/packages/rust/paint-vm-opengl  # no in-repo dependents; never built, tested or linted
PENDING code/packages/rust/paint-vm-runtime  # 11 in-repo dependents (barcode-1d, barcode-2d, paint-vm-cairo, paint-vm-direct2d, …); never built, tested or linted; also missing README.md + CHANGELOG.md
PENDING code/packages/rust/paint-vm-skia  # 2 in-repo dependents (barcode-1d, barcode-2d); never built, tested or linted
PENDING code/packages/rust/paint-vm-vulkan  # no in-repo dependents; never built, tested or linted
PENDING code/packages/rust/paint-vm-wgpu  # no in-repo dependents; never built, tested or linted

# Data structures
PENDING code/packages/rust/avl-tree  # 1 in-repo dependent (tree-set); never built, tested or linted; also missing README.md + CHANGELOG.md
PENDING code/packages/rust/binary-search-tree  # no in-repo dependents; never built, tested or linted; also missing README.md + CHANGELOG.md
PENDING code/packages/rust/binary-tree  # no in-repo dependents; never built, tested or linted; also missing README.md + CHANGELOG.md
PENDING code/packages/rust/red-black-tree  # 1 in-repo dependent (tree-set); never built, tested or linted; also missing README.md + CHANGELOG.md
PENDING code/packages/rust/rope  # no in-repo dependents; never built, tested or linted; also missing README.md + CHANGELOG.md
PENDING code/packages/rust/segment-tree  # no in-repo dependents; never built, tested or linted; also missing README.md + CHANGELOG.md
PENDING code/packages/rust/suffix-tree  # no in-repo dependents; never built, tested or linted; also missing README.md + CHANGELOG.md
PENDING code/packages/rust/treap  # no in-repo dependents; never built, tested or linted; also missing README.md + CHANGELOG.md

# Machine learning / numerics
PENDING code/packages/rust/activation-functions  # 3 in-repo dependents (mansion-classifier, perceptron, space-launch-predictor); never built, tested or linted; also missing README.md + CHANGELOG.md
PENDING code/packages/rust/perceptron  # 2 in-repo dependents (mansion-classifier, space-launch-predictor); never built, tested or linted; also missing README.md + CHANGELOG.md
PENDING code/packages/rust/matrix  # 4 in-repo dependents (house-price-predictor, mansion-classifier, perceptron, space-launch-predictor); never built, tested or linted

# Hardware / synthesis
PENDING code/packages/rust/synthesis  # no in-repo dependents; never built, tested or linted; also missing README.md + CHANGELOG.md
PENDING code/packages/rust/core  # no in-repo dependents; never built, tested or linted; also missing README.md + CHANGELOG.md

# DSP
PENDING code/packages/rust/dsp-conv  # no in-repo dependents; never built, tested or linted
PENDING code/packages/rust/dsp-dct  # 2 in-repo dependents (dsp-stft, image-codec-jpeg); never built, tested or linted
PENDING code/packages/rust/dsp-filters  # 2 in-repo dependents (dsp-conv, dsp-stft); never built, tested or linted
PENDING code/packages/rust/dsp-stft  # no in-repo dependents; never built, tested or linted
PENDING code/packages/rust/dsp-wavelets  # no in-repo dependents; never built, tested or linted

# Adjudication / engram / mosaic / constraint
PENDING code/packages/rust/adjudication-rulebook  # 1 in-repo dependent (adjudication-tsa-demo); never built, tested or linted
PENDING code/packages/rust/constraint-vm  # 1 in-repo dependent (lang-refinement-checker); never built, tested or linted
PENDING code/packages/rust/engram-anki-package  # 2 in-repo dependents (engram-capi, engram-core-wasm); never built, tested or linted
PENDING code/packages/rust/engram-core  # 3 in-repo dependents (engram-anki-package, engram-capi, engram-core-wasm); never built, tested or linted
PENDING code/packages/rust/mosaic-driver  # no in-repo dependents; never built, tested or linted
PENDING code/packages/rust/mosaic-emit-paint  # 1 in-repo dependent (mosaic-compile); never built, tested or linted
PENDING code/packages/rust/task-mosaic-app  # no in-repo dependents; never built, tested or linted; also missing CHANGELOG.md

# Programs — Rust ports of multi-language program families (Python/TypeScript/Go
# siblings of each already build); these are separate cargo projects, not members
# of the code/packages/rust workspace.
PENDING code/programs/rust/celsius-to-fahrenheit-predictor  # Rust port of a multi-language program family; never built, tested or linted
PENDING code/programs/rust/cowsay  # Rust port of a multi-language program family; never built, tested or linted
PENDING code/programs/rust/house-price-predictor  # Rust port of a multi-language program family; never built, tested or linted
PENDING code/programs/rust/mansion-classifier  # Rust port of a multi-language program family; never built, tested or linted
PENDING code/programs/rust/space-launch-predictor  # Rust port of a multi-language program family; never built, tested or linted
