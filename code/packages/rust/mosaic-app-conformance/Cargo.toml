[package]
name = "mosaic-app-conformance"
version = "0.1.0"
edition = "2021"
description = "Reusable real-engine fixture for Mosaic native host binding conformance."
license = "MIT"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
mosaic-app-capi = { path = "../mosaic-app-capi" }
mosaic-app-runtime = { path = "../mosaic-app-runtime" }
serde_json = "1"
