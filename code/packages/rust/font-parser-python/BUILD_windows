echo "font-parser-python: compile-only crate (requires Python dev headers to link)"
echo "  To build: cargo build -p font-parser-python --release"
echo "  To package for PyPI: maturin build --release"
