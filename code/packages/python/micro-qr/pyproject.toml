[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "coding-adventures-micro-qr"
version = "0.1.0"
description = "Micro QR Code encoder — ISO/IEC 18004:2015 Annex E compliant, all versions M1–M4"
requires-python = ">=3.12"
license = "MIT"
authors = [{ name = "Adhithya Rajasekaran" }]
readme = "README.md"
dependencies = [
    "coding-adventures-barcode-2d",
    "coding-adventures-gf256",
    "coding-adventures-paint-instructions",
]

[project.optional-dependencies]
dev = ["pytest>=8.0", "pytest-cov>=5.0", "ruff>=0.4", "mypy>=1.10"]

[tool.hatch.build.targets.wheel]
packages = ["src/micro_qr"]

[tool.uv.sources]
coding-adventures-barcode-2d         = { path = "../barcode-2d", editable = true }
coding-adventures-gf256              = { path = "../gf256", editable = true }
coding-adventures-paint-instructions = { path = "../paint-instructions", editable = true }

[tool.ruff]
target-version = "py312"
line-length = 88

[tool.ruff.lint]
select = ["E", "W", "F", "I", "UP", "B", "SIM", "ANN"]
ignore = [
    "ANN401",  # any type OK in some places
]

[tool.ruff.lint.per-file-ignores]
# Tests don't need return-type annotations on methods.
"tests/**" = ["ANN201", "ANN001", "ANN202"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--cov=micro_qr --cov-report=term-missing --cov-fail-under=90"

[tool.coverage.run]
source = ["src/micro_qr"]

[tool.coverage.report]
fail_under = 90
show_missing = true
