# layout-effects

Reusable visual-effect metadata for Layout IR. Producers describe affine
transforms, transform origins, opacity, filter chains, blend modes, isolation,
and shadows without importing a paint backend. Paint and hit-testing consumers
resolve the same contract against each positioned box.

Spec: [`UI47-layout-effects`](../../../specs/UI47-layout-effects.md).
