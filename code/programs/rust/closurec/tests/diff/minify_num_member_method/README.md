# minify_num_member_method

CLOC14.41 byte-identity fixture (captured from the upstream
Closure JAR v20240317, WHITESPACE_ONLY).

- **Input:** `a=1..toString();`
- **Note:** gap-093: integer number literal followed by .member needs paren-wrap -> (1).toString()
