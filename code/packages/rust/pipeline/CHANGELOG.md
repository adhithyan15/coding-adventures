# Changelog

## Unreleased

- Added the initial Rust `pipeline` package.
- Added a VM-path orchestrator that composes the Rust `lexer` and `parser`
  crates with local bytecode compiler and virtual-machine implementations.
- Added JSON export in the shared report shape used by the HTML renderer.
