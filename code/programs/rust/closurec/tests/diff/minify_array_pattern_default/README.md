# minify_array_pattern_default

CLOC14.48 byte-identity fixture (captured from the upstream
Closure JAR v20240317, WHITESPACE_ONLY).

- **Input:** `function f([a]=[]){}`
- **Note:** array-pattern default param round-trips (no stray ;)
