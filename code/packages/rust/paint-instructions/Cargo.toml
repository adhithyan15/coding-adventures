[package]
name = "paint-instructions"
version = "0.1.0"
edition = "2021"
description = "Universal 2D paint IR (P2D00): PaintScene and all instruction types"

[dependencies]
pixel-container = { path = "../pixel-container" }
