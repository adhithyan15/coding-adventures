[package]
name = "draw-instructions-svg"
version = "0.1.0"
edition = "2021"
description = "SVG renderer for backend-neutral draw instructions"

[dependencies]
draw-instructions = { path = "../draw-instructions" }
