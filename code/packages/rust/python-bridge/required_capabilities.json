{
  "$schema": "https://raw.githubusercontent.com/adhithyan15/coding-adventures/main/code/specs/schemas/required_capabilities.schema.json",
  "version": 1,
  "package": "rust/python-bridge",
  "capabilities": [
    {
      "category": "ffi",
      "action": "load",
      "target": "libpython3.*",
      "justification": "Loads the Python shared library via extern \"C\" declarations to call the CPython C API."
    },
    {
      "category": "ffi",
      "action": "call",
      "target": "CPython C API",
      "justification": "Calls Python C API functions (Py_Initialize, PyRun_SimpleString, etc.) through FFI."
    }
  ],
  "justification": "FFI bridge to CPython. Loads the Python shared library and calls C API functions through raw extern \"C\" declarations."
}
