{
  "$schema": "https://raw.githubusercontent.com/adhithyan15/coding-adventures/main/code/specs/schemas/required_capabilities.schema.json",
  "version": 1,
  "package": "lua/uuid",
  "capabilities": [],
  "justification": "Pure computation with pseudo-random number generation using Lua's math.random. Uses os.time() and os.clock() for seeding and timestamp-based UUID versions (v1, v7), but does not access the filesystem or network."
}
