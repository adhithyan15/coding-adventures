# build-tool: deps=c/iso-harness c/platform-harness c/os-platform c/net c/reactor
# tcp-runtime (OS-agnostic; rides net + reactor). run.sh compiles the runtime and
# test together with the net + reactor backends for this OS. Windows uses
# BUILD_windows (Winsock backends, linking ws2_32).
sh tools/run.sh
