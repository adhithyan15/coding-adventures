### Build-tool graph and diff contract coverage

- Defined canonical prerequisite-then-dependent graph edge ordering and added
  an unsorted-input fixture that requires native results to emit that order.
- Added positive package-prefix and forced-package closure fixtures so both
  diff source modes and forced changed-set seeding are shared-corpus evidence.
