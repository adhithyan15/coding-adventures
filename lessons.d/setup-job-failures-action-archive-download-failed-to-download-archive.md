---
category: CI & GitHub Actions
---

# Setup-job failures (action archive download, `Failed to download archive`)

are infrastructure flakiness, not code failures. Inspect the log before changing code; rerun. The same applies to `Prepare all required actions` failures before checkout.
