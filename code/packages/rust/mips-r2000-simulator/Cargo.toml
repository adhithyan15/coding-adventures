[package]
name = "mips-r2000-simulator"
version = "0.1.0"
edition = "2021"
description = "Complete Spec 07q MIPS R2000 functional simulator with exact 64 KiB state, typed atomic lifecycle, unaligned transfers, and Python full-state conformance"

[dependencies]
cpu-simulator = { path = "../cpu-simulator" }
