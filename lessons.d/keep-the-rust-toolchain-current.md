---
category: Workspace & package metadata
---

# Keep the Rust toolchain current

External deps adopting Edition 2024 require `rustup toolchain install stable` before declaring the workspace broken.
