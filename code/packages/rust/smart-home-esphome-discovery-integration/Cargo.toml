[package]
name = "smart-home-esphome-discovery-integration"
version = "0.1.0"
edition = "2021"
description = "Authorized bounded ESPHome native-API mDNS discovery for Smart Home"
license = "MIT"

[dependencies]
serde_json = "1"
smart-home-core = { path = "../smart-home-core" }
smart-home-discovery = { path = "../smart-home-discovery" }
smart-home-runtime = { path = "../smart-home-runtime" }

[[bin]]
name = "smart-home-esphome-discovery-integration"
path = "src/main.rs"
