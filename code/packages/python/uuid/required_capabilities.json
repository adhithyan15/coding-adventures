{
  "$schema": "https://raw.githubusercontent.com/adhithyan15/coding-adventures/main/code/specs/schemas/required_capabilities.schema.json",
  "version": 1,
  "package": "python/uuid",
  "capabilities": [
    {
      "category": "time",
      "action": "read",
      "target": "wall_clock",
      "justification": "Uses time.time_ns() for UUID v1 and v7 timestamp fields."
    }
  ],
  "justification": "UUID v1 and v7 generation requires reading the system wall clock. os.urandom is used for randomness but is not an OS-level capability in the same category."
}
