# minify_num_frac_milli

CLOC14.55 byte-identity fixture (captured from the upstream
Closure JAR v20240317, WHITESPACE_ONLY).

- **Input:** `x=.001;`
- **Note:** small fraction kept as decimal when not longer than scientific (.001 == 1E-3 in length)
