# coding-adventures-two-layer-network

Educational two-layer neural network primitives for hidden-layer examples.

The package models:

```text
inputs -> hidden weighted sum -> hidden activation -> output weighted sum -> output activation
```

It is intentionally small so the XOR example can show why hidden layers matter.
