{
  "$schema": "https://raw.githubusercontent.com/adhithyan15/coding-adventures/main/code/specs/schemas/required_capabilities.schema.json",
  "version": 1,
  "package": "rust/csprng",
  "capabilities": ["os_random"],
  "justification": "Reads bytes from the operating-system CSPRNG (getrandom(2) on Linux, getentropy(2) on macOS, BCryptGenRandom on Windows). No filesystem writes, no network, no process spawning."
}
