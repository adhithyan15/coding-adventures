# Changelog

## 0.1.0

- add the initial `format-doc-to-paint` TypeScript bridge
- convert `DocLayoutTree` values into `PaintScene` glyph runs
- provide a convenience `docToPaintScene()` helper over `layoutDoc()`
