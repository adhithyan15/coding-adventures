[package]
name = "coding-adventures-macsyma-parser"
version = "0.1.0"
edition = "2021"
description = "MACSYMA parser backed by statically compiled grammar data"
license = "MIT"

[dependencies]
grammar-tools = { path = "../grammar-tools" }
lexer = { path = "../lexer" }
parser = { path = "../parser" }
coding-adventures-macsyma-lexer = { path = "../macsyma-lexer" }
