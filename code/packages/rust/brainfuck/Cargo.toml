[package]
name = "brainfuck"
version = "0.1.0"
edition = "2021"

[dependencies]
virtual-machine = { path = "../virtual-machine" }
grammar-tools = { path = "../grammar-tools" }
lexer = { path = "../lexer" }
parser = { path = "../parser" }
