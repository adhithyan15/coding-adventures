[package]
name = "html-renderer"
version = "0.1.0"
edition = "2021"
description = "Language-agnostic HTML visualization generator for pipeline reports"

[lib]
name = "html_renderer"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
