# coding_adventures_twig

Ruby Twig frontend and runtime implemented on the LANG VM chain. Twig source
lowers to dynamically typed InterpreterIR and can run on the pure VM or emit
JVM, CLR, WASM, and pure VM backend artifacts through the shared registry.
