[package]
name = "mosaic-pkg-rating-controls"
version = "0.1.0"
description = "Reusable spaced-repetition rating controls built on the Mosaic UI pipeline"
license = "MIT OR Apache-2.0"

[components]
exports = ["RatingControls"]

[dependencies]
# Built only from UI29 kernel primitives.

[kernel]
version = "1"
