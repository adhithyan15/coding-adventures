# Changelog

## Unreleased

- add the first Go `compiler-ir` to JVM `.class` backend
- emit verifier-friendly class files with static register and memory helpers
- add a classpath-safe `WriteClassFile` helper and structural/runtime tests
