# build-tool: deps=c/iso-harness c/platform-harness c/os-platform
# reactor (Unix backend: macOS + Linux). Compiles the poll() backend under every
# present C compiler via platform-harness. Windows uses BUILD_windows (WSAPoll,
# linking ws2_32).
sh tools/run.sh
