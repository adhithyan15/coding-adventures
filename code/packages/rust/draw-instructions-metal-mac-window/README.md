# draw-instructions-metal-mac-window

Native macOS window display for draw-instructions scenes.

Opens a window and renders a `DrawScene` using Metal.  The window stays open
until the user closes it.  Used for visual inspection of rendered barcodes,
tables, and other draw-instructions output.

macOS only.
