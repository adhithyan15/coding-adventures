# BUILD — conduit-hello (Elixir Conduit demo)
#
# Depends on the conduit package (file: dep in mix.exs). The build tool
# orders builds via the dependency graph; this BUILD just installs deps,
# compiles, and runs tests.

mix deps.get --quiet && mix compile && mix test --no-color
