# wasm-validator

Native Kotlin wrapper for WebAssembly module validation.

This package currently provides:

- `validate(...)` to wrap a parsed `WasmModule`
- `ValidatedModule` as the validated handoff type for the runtime
