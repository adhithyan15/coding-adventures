// settings.gradle.kts — Project settings for the Java hello-world program.
//
// In Gradle, settings.gradle.kts defines the project structure. For a single
// project (no submodules), you only need to set the project name. For monorepo
// dependencies, you would add includeBuild("../sibling-project") here.
rootProject.name = "hello-world"
