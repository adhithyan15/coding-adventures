# wasm-opcodes

Native Java access to the WebAssembly 1.0 opcode metadata table.

This package provides:

- `WasmOpcodes.OPCODES` for byte-to-opcode lookups
- `WasmOpcodes.OPCODES_BY_NAME` for mnemonic-to-opcode lookups
- `WasmOpcodes.getOpcode(...)` convenience helpers
