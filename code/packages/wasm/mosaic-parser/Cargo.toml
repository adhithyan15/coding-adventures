[workspace]

[package]
name = "mosaic-parser-wasm"
version = "0.1.0"
edition = "2021"
description = "WebAssembly bindings for the Rust Mosaic parser"
license = "MIT"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
grammar_wasm_support = { package = "grammar-wasm-support", path = "../../rust/grammar-wasm-support" }
language_parser = { package = "mosaic-parser", path = "../../rust/mosaic-parser" }
wasm-bindgen = "0.2"
