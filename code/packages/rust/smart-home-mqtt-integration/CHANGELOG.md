# Changelog

## 0.1.0

- Add a synchronous MQTT broker host with bounded polling and reconnect state.
- Install Home Assistant MQTT discovery records into the normalized D23 runtime.
- Normalize state and availability publications and authorize outbound commands.
- Keep broker credentials ephemeral while durable bridge records retain only a vault reference.
