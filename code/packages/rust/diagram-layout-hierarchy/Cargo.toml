[package]
name = "diagram-layout-hierarchy"
version = "0.1.0"
edition = "2021"
description = "Deterministic backend-neutral layout for hierarchical diagrams"

[dependencies]
diagram-ir = { path = "../diagram-ir" }
