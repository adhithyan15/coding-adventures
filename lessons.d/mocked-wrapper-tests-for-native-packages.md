---
category: Python
---

# Mocked wrapper tests for native packages

— when native smoke tests skip on the wrong platform, the Python facade's wrapper logic still needs coverage from mocked tests, or coverage gates fail off-platform.
