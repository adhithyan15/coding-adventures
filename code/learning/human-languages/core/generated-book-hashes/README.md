# Language-owned book hash ledgers

These files are generated by `human-language-data`'s `generate:books` command.
Each language owns one JSON shard, so changing one book does not rewrite a
corpus-wide manifest or conflict with another language PR.

Do not edit the JSON by hand. CI's `check:books` gate regenerates every shard in
memory and compares it byte for byte.
