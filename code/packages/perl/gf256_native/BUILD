# Build the Perl GF256Native XS extension (cdylib loaded by DynaLoader).
# cargo sets CWD to the package directory; no cd needed.
cargo build --release 2>&1
echo "Built: target/release/libGF256Native.so (or .dylib)"
