# @coding-adventures/audio-device-sink

Backend-neutral PCM playback primitives for TypeScript.

This package validates signed 16-bit mono PCM buffers and exposes a no-op sink
for tests and higher-level packages.
