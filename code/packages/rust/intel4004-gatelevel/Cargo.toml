[package]
name = "intel4004-gatelevel"
version = "0.1.0"
edition = "2021"
description = "Gate-level Intel 4004 CPU simulator — all computation routes through real logic gates"

[dependencies]
logic-gates = { path = "../logic-gates" }
arithmetic = { path = "../arithmetic" }
