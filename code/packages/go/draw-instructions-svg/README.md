# draw-instructions-svg

SVG renderer for backend-neutral draw instructions.

This package consumes the generic scene model from `draw-instructions` and
returns SVG strings. It does not know what a barcode is.

## Development

```bash
bash BUILD
```
