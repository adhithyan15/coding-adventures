[package]
name = "arm-simulator"
version = "0.1.0"
edition = "2021"
description = "ARMv7 subset simulator — the architecture that powers your phone"

[dependencies]
cpu-simulator = { path = "../cpu-simulator" }
