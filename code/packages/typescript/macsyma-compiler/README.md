# @coding-adventures/macsyma-compiler

Compiles MACSYMA parser ASTs into `@coding-adventures/symbolic-ir` trees.

This package depends on the statically linked MACSYMA parser package and is
safe for browser use.
