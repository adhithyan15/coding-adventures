[package]
name = "mermaid-lexer"
version = "0.48.0"
edition = "2021"
description = "Grammar-driven lexers for Mermaid diagram families"

[dependencies]
grammar-tools = { path = "../grammar-tools" }
lexer = { path = "../lexer" }
