[workspace]

[package]
name = "tcp-client-wasm"
version = "0.1.0"
edition = "2021"
description = "WebAssembly stub for the tcp-client crate — TCP sockets unavailable in browser Wasm"
license = "MIT"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"
