### Changed — Letter-anchoring ratchets are owned per track

Changed the shared letter-anchoring ceiling map into one strict JSON owner per
non-Latin track. Independent script-track work can now lower its own cold,
builds-toward, and unwritten ceilings without editing a cross-track aggregate.
