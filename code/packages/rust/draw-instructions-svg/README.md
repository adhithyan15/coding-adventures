# draw-instructions-svg

SVG renderer for backend-neutral draw instructions.

This crate consumes the generic scene model from `draw-instructions` and emits
SVG without learning any barcode-specific rules.
