[package]
name = "paint-vm-wgpu"
version = "0.1.0"
edition = "2021"
description = "WGPU backend scaffold for the Paint VM runtime"

[dependencies]
paint-instructions = { path = "../paint-instructions" }
paint-vm-runtime = { path = "../paint-vm-runtime" }
