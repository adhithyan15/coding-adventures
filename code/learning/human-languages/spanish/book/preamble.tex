% Preamble for "Spanish, Step by Step, Root by Root" (HL00 book format).
% XeLaTeX only -- pdflatex will not work, this needs real Unicode/font
% support that later tracks (Arabic, Devanagari, Dravidian scripts) require.

\usepackage{fontspec}

% Latin Modern ships with every standard TeX distribution (MiKTeX, TeX
% Live) as OpenType fonts, so this compiles the same way on any machine --
% no dependency on whatever system fonts happen to be installed locally.
\setmainfont{Latin Modern Roman}[
  SmallCapsFont={Latin Modern Roman Caps}
]
\setsansfont{Latin Modern Sans}
\setmonofont{Latin Modern Mono}

\usepackage[table]{xcolor}
\usepackage{tcolorbox}
\tcbuselibrary{skins,breakable}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{array}
\usepackage{tabularx}
\newcolumntype{Y}{>{\raggedright\arraybackslash}X}
\usepackage[hidelinks]{hyperref}
\usepackage[raggedright]{titlesec}
\input{../../_shared/visual.tex}

% polyglossia loads bidi for Arabic, so color-related packages must come first.
\usepackage{polyglossia}
\setdefaultlanguage{english}
\setotherlanguage{spanish}
\setotherlanguage{arabic}

% Chapter 22 preserves the Arabic source of azul. Use the repository's static
% Naskh font so the source script is deterministic and never dropped in print.
\newfontfamily\arabicfont[
  Path=../../_fonts/,
  Script=Arabic,
  BoldFont=NotoNaskhArabic-Static.ttf,
  ItalicFont=NotoNaskhArabic-Static.ttf,
  BoldItalicFont=NotoNaskhArabic-Static.ttf
]{NotoNaskhArabic-Static.ttf}
\newcommand{\esar}[1]{\textarabic{#1}}

% Micro-lessons end at natural pause points instead of stretching their
% callout boxes to force every page to the same depth. The small reserve gives
% prose and legacy tables a cleaner line break before they exceed the measure.
\raggedbottom
\emergencystretch=2em

% Several legacy chapter and section titles are intentionally descriptive.
% Keep the running heads useful and stable without forcing those full titles
% into one unbreakable line at the top of every verso.
\renewcommand{\chaptermark}[1]{\markboth{\chaptername\ \thechapter}{}}
\renewcommand{\sectionmark}[1]{\markright{\thesection}}

% book.cls inserts a verso before each open-right chapter. Preserve that print
% layout while suppressing running heads and page numbers on the blank page.
\makeatletter
\renewcommand{\cleardoublepage}{%
  \clearpage
  \if@twoside
    \ifodd\c@page\else
      \hbox{}\thispagestyle{empty}\newpage
      \if@twocolumn\hbox{}\newpage\fi
    \fi
  \fi
}
\makeatother

\hypersetup{
  pdftitle={Spanish, Step by Step, Root by Root},
  pdfauthor={Adhithya Rajasekaran},
  pdfsubject={Etymology-driven Spanish curriculum},
  pdfkeywords={Spanish, etymology, Latin, language learning}
}

% Cousin Web callout: the root + the family of English cousins it lights up.
% This is the signature box of the book -- every word lesson has one.
\newtcolorbox{cousinweb}{
  breakable, skin=enhanced, colback=yellow!8, colframe=yellow!45!black,
  boxrule=0.5pt, arc=1mm, left=6pt, right=6pt, top=4pt, bottom=4pt,
  fonttitle=\bfseries, title={The word, taken apart --- and its English cousins}
}

% Culture callout: why a phrase is said this way (idiomatic / cultural).
\newtcolorbox{culture}{
  breakable, skin=enhanced, colback=red!5, colframe=red!40!black,
  boxrule=0.5pt, arc=1mm, left=6pt, right=6pt, top=4pt, bottom=4pt,
  fonttitle=\bfseries, title={Why it's said this way}
}

% Grammar Lens callout: plain-language grammar concept + English contrast.
\newtcolorbox{grammarlens}[1][]{
  breakable, skin=enhanced, colback=blue!5, colframe=blue!35!black,
  boxrule=0.5pt, arc=1mm, left=6pt, right=6pt, top=4pt, bottom=4pt,
  fonttitle=\bfseries, title={Grammar Lens}, #1
}

% Sounds callout: the pronunciation facts a single word needs.
\newtcolorbox{sounds}{
  breakable, skin=enhanced, colback=green!6, colframe=green!30!black,
  boxrule=0.5pt, arc=1mm, left=6pt, right=6pt, top=4pt, bottom=4pt,
  fonttitle=\bfseries, title={Sounds you'll need}
}

% legacy aliases (older chapters, pre-redesign) still reference these names.
\newtcolorbox{etymology}{
  breakable, skin=enhanced, colback=yellow!8, colframe=yellow!45!black,
  boxrule=0.5pt, arc=1mm, left=6pt, right=6pt, top=4pt, bottom=4pt,
  fonttitle=\bfseries, title={Etymology}
}
\newtcolorbox{morphologybox}{
  breakable, skin=enhanced, colback=green!6, colframe=green!30!black,
  boxrule=0.5pt, arc=1mm, left=6pt, right=6pt, top=4pt, bottom=4pt,
  fonttitle=\bfseries, title={Morphology Spotlight}
}

% New-vocabulary table: word, gender (if a noun), translation.
\newenvironment{vocabtable}{%
  \begin{longtable}{@{}p{0.28\textwidth}p{0.10\textwidth}p{0.46\textwidth}@{}}
  \toprule
  \textbf{Spanish} & \textbf{Gender} & \textbf{English} \\
  \midrule
  \endhead
}{%
  \bottomrule
  \end{longtable}
}

% Prefix-machine table: prefix | meaning | + root | = English word | literal.
\newenvironment{prefixtable}{%
  \begin{longtable}{@{}p{0.13\textwidth}p{0.18\textwidth}p{0.17\textwidth}p{0.18\textwidth}p{0.24\textwidth}@{}}
  \toprule
  \textbf{Prefix} & \textbf{Means} & \textbf{+ root} & \textbf{= English} & \textbf{= literally} \\
  \midrule
  \endhead
}{%
  \bottomrule
  \end{longtable}
}

\titleformat{\chapter}[display]
  {\normalfont\Large\bfseries}{Chapter \thechapter}{10pt}{\huge\raggedright}

% Chapter opening (HL09 §8): what the reader will be able to do by the end.
% Generated from the HL05 capability ledger, never hand-written.
\newenvironment{chapteropening}%
  {\begin{quote}\itshape\small\raggedright}%
  {\end{quote}\par\medskip}

% ---------------------------------------------------------------------------
% Table-of-contents number width (HL-C109).
%
% `book.cls` reserves 1.5em for a chapter number in the table of contents:
%
%     \newcommand*\l@chapter[2]{... \setlength\@tempdima{1.5em} ...}
%
% That is enough for two digits and not for three. At 10.95pt bold, "100" is
% 2.46pt wider than the box, so the number runs into its own title and TeX
% reports an overfull \hbox for every such line. A track notices nothing until
% it crosses a hundred chapters -- and then the bug arrives all at once, one
% line per chapter, which is exactly how it surfaced here.
%
% Below is `book.cls`'s own \l@chapter with a single number changed: 1.5em
% becomes 2.8em, which holds four bold digits with room to spare. A course that
% reached 100 chapters can reach 1000, and the fix should not need revisiting
% then. The section and subsection indents move by the same amount so the
% columns still line up under the widened chapter number.
%
% One further change to book.cls's original, added with HL-C198 below: the
% \rightskip and \parfillskip carry `plus 1fil' and `plus -1fil'. book.cls sets
% them to plain \@pnumwidth and -\@pnumwidth, which leaves a chapter title
% JUSTIFIED. A title short enough to sit on one line never notices; a title long
% enough to wrap has its first line stretched to the full measure, and a title
% like "Who Someone Is --- Neighbour, Friend, Teacher, Nurse, Cook" stretches
% far enough (badness 1038, just past the default \hbadness of 1000) for TeX to
% report an underfull \hbox. The two fil components make the title ragged right:
%
%   * non-final lines end with \rightskip = \@pnumwidth plus 1fil. Infinite
%     stretch means the line's badness is 0 no matter how short it falls, so a
%     wrapped title can never be reported underfull again -- by construction,
%     not by being narrowly under a threshold.
%   * the final line adds \parfillskip on top, and the two fil components are
%     equal and opposite, so they cancel exactly. The net glue there is 0, the
%     same as book.cls's, and the \hfil already in the body of the macro is what
%     pushes the page number flush right. The number does not move.
%
% Ragged right is what a wrapped contents entry wants anyway; justifying one is
% what produced the loose line in the first place.
\makeatletter
\renewcommand*\l@chapter[2]{%
  \ifnum \c@tocdepth >\m@ne
    \addpenalty{-\@highpenalty}%
    \vskip 1.0em \@plus\p@
    \setlength\@tempdima{2.8em}%
    \begingroup
      \parindent \z@ \rightskip \@pnumwidth plus 1fil
      \parfillskip -\@pnumwidth plus -1fil
      \leavevmode \bfseries
      \advance\leftskip\@tempdima
      \hskip -\leftskip
      #1\nobreak\hfil
      \nobreak\hb@xt@\@pnumwidth{\hss #2%
                                 \kern-\p@\kern\p@}\par
      \penalty\@highpenalty
    \endgroup
  \fi}
\renewcommand*\l@section{\@dottedtocline{1}{2.8em}{3.2em}}
\renewcommand*\l@subsection{\@dottedtocline{2}{6.0em}{4.1em}}
\makeatother

% ---------------------------------------------------------------------------
% Table-of-contents PAGE-number width (HL-C198).
%
% The block above widened the column that holds the chapter NUMBER on the left.
% There is a second, independent column in every contents line: the box on the
% right that holds the PAGE number. `book.cls` sizes that one too, and sizes it
% just as optimistically:
%
%     \newcommand*\@pnumwidth{1.55em}
%     \newcommand*\@tocrmarg{2.55em}
%
% Every contents line -- \l@chapter above, and \@dottedtocline for sections and
% subsections -- ends by packing the page number into a box of exactly
% \@pnumwidth:
%
%     \hb@xt@\@pnumwidth{\hss #2}
%
% \hb@xt@ means "an \hbox exactly this wide". Put something wider inside and TeX
% cannot shrink it, so it prints
%
%     Overfull \hbox (...pt too wide) detected at line N
%
% where N is a line of `book.toc`, not of any file a person wrote. That is why
% this one is confusing to chase: the warning points into a build artefact.
%
% Do the arithmetic, because it explains the number chosen below. Latin Modern's
% digits are tabular: every one of them is exactly 0.5em wide, so an n-digit
% page number needs 0.5n em.
%
%     column width      1.55em
%     "999"   3 digits  1.50em   fits, 0.05em to spare
%     "1000"  4 digits  2.00em   overfull by 0.45em
%
% At the contents' 10.95pt that overflow is 0.45 x 10.95 = 4.9275pt -- which is
% exactly the "4.92747pt too wide" this book started reporting, fourteen times,
% once for each contents line whose page number had reached four digits. The
% identical width on every one of them is the tell: one construct, repeated, not
% fourteen unrelated bad line breaks.
%
% This is the same shape of bug as HL-C109 and arrives the same way. A track
% notices nothing for years and then crosses a threshold -- there, 100 chapters;
% here, 1000 pages -- and the warning appears all at once, one line per crossing
% entry. Spanish crossed 1000 pages in the tranche that added chapters 289-295.
%
% 2.8em holds five digits (2.50em) with room to spare, so a book may grow past
% 10000 pages before this needs looking at again; it is also the number the
% chapter-number column already uses, which keeps the two edits reading as one
% decision. \@tocrmarg is the right-hand margin the dotted leaders stop at, and
% book.cls keeps it exactly 1em clear of \@pnumwidth so the dots never touch the
% number; it moves by the same amount to preserve that gap.
%
% Note what is deliberately NOT done here: \hfuzz and \hbadness stay at their
% defaults. Widening the box makes the number fit; raising the fuzz would only
% make TeX stop mentioning that it does not.
%
% This block is Spanish-only on purpose. Spanish is 1272 pages; the next longest
% book in the ramp is 355, and the shortest is 133. No other track is within
% three times of the threshold, so the other twenty-one preambles keep book.cls's
% defaults rather than take a contents-layout change they cannot yet benefit
% from. Whichever track crosses 1000 pages next wants this block copied across,
% together with the two fil components in \l@chapter above -- they are one fix in
% two halves, and the second half is what keeps the first from trading an
% overfull box for an underfull one.
\makeatletter
\renewcommand*\@pnumwidth{2.8em}
\renewcommand*\@tocrmarg{3.8em}
\makeatother

% ---------------------------------------------------------------------------
% Line-breaking tolerance (HL-C109).
%
% These books mix ordinary prose with tokens TeX cannot break: lesson ids
% (ES-C14-practice), slash-separated form lists (fui/fuiste/fue), and Latin
% etymons carrying macrons that a modern language's hyphenation patterns will
% not touch. Inside a narrow measure -- a quote block or a list item -- one such
% token forces the previous line to stretch, and TeX reports an underfull hbox.
% (The slash lists are handled directly further down; see HL-C109b.)
%
% \tolerance and \emergencystretch change how a paragraph is BROKEN, not how it
% is reported: TeX may accept a looser line early enough to find a break
% sequence that avoids a very loose one, and gets a final pass with extra
% stretchability instead of an overfull line. \hbadness and \hfuzz are
% deliberately left at their defaults -- raising those would silence the
% warnings rather than fix the paragraphs, and the CI log scanner would then be
% reading a muted log.
\tolerance=2000

% ---------------------------------------------------------------------------
% A slash is a place a line may break (HL-C109b).
%
% The note above has named slash-separated form lists -- fui/fuiste/fue, or
% qitt/cattus/gato/chat/Katze -- as an unbreakable token class since it was
% written, without ever doing anything about it. A run like that is several
% words wide and TeX treats it as one, so a line holding it has almost nowhere
% to break and must stretch the few interword spaces it has instead. That is an
% underfull hbox, and it is the same defect the callout rule above fixes, from a
% different direction.
%
% A slash is a natural break point: readers already parse `fui/fuiste/fue' as
% three items, and a line that ends after one of them reads correctly. So say so.
%
% XeTeX offers exactly the right hook. Every character belongs to an
% "interchar class", and tokens can be injected at a class BOUNDARY -- here,
% wherever a slash is followed by anything else. Nothing about the slash itself
% changes: its catcode stays 12, so \url, \includegraphics and file names are
% untouched, and the glyph, its kerning and its spacing are identical. All that
% is added is permission to break.
%
% Twenty-one of the twenty-two books paginate identically afterwards; tamil
% loses a page (249 -> 248), which is the change doing its job -- a slash run
% that had been forcing a loose line now breaks, the paragraph sets in fewer
% lines, and the saving reaches the end of the book. Overfull stays at 0 in
% every track and no glyph goes missing.
%
% \allowbreak is invalid in math mode, and $a/b$ is ordinary in these books, so
% the injection is guarded. Class 0 is "everything else"; class 255 is the
% boundary class TeX uses at the edges of a paragraph or box.
\XeTeXinterchartokenstate=1
\newXeTeXintercharclass\hlslashclass
\XeTeXcharclass`\/ \hlslashclass
\XeTeXinterchartoks \hlslashclass 0   = {\ifmmode\else\allowbreak\fi}
\XeTeXinterchartoks \hlslashclass 255 = {\ifmmode\else\allowbreak\fi}

% ---------------------------------------------------------------------------
% An opening delimiter must not cost a word its hyphenation (HL-C109c).
%
% TeX only hyphenates a word that follows glue. A word preceded by an opening
% delimiter -- ( [ or an opening quote -- is therefore never hyphenated at all,
% and becomes one unbreakable unit as wide as the whole phrase. In latin that
% was the phrase ("tomorrow") on the first line of a paragraph: nowhere to
% break, so the line stretched to badness 3088.
%
% That the cause really was hyphenation, and not merely the extra width, was
% settled by keeping the delimiters and writing to\-mor\-row by hand. The box
% went, at unchanged length.
%
% \nobreak\hskip0pt is zero-width glue behind an infinite penalty: it restarts
% TeX's word scan, so the following word can hyphenate, while forbidding a break
% after the delimiter itself -- a line must never end with a dangling `('.
%
% The \relax is load-bearing. A glue specification absorbs a following `plus' or
% `minus' keyword, and `(plus a few invented for Slavic sounds)' is ordinary
% English -- russian has exactly that sentence. Without \relax, \hskip0pt reads
% on into `plus a' and stops with "Missing number, treated as zero", failing the
% whole book. \fi does not help: it is expandable, so it is expanded away during
% the scan for the number rather than ending it.
%
% The two kinds of delimiter need two different mechanisms, which is worth
% knowing before editing this. Inter-character classes act on characters read
% from the SOURCE: ( and [ are exactly that, so a class reaches them. The quotes
% are not -- the book generator emits \textquotedblleft{}, a command -- so a
% class keyed on U+201C compiles cleanly and does nothing whatsoever. Those have
% to be wrapped at the command.
\newXeTeXintercharclass\hlopenclass
\XeTeXcharclass`\( \hlopenclass
\XeTeXcharclass`\[ \hlopenclass
\XeTeXinterchartoks \hlopenclass 0 = {\ifmmode\else\nobreak\hskip0pt\relax\fi}
\let\hlorigopenquote\textquotedblleft
\renewcommand{\textquotedblleft}{\hlorigopenquote\nobreak\hskip0pt\relax}
\let\hlorigopensingle\textquoteleft
\renewcommand{\textquoteleft}{\hlorigopensingle\nobreak\hskip0pt\relax}

% Callout boxes are set ragged right (HL-C88).
%
% Every callout runs at a narrower measure than the body, and its content is the
% densest part of the book: bold Spanish forms, Latin etymons carrying macrons,
% and arrows between them. None of those hyphenate under English patterns, so a
% justified line inside a callout has almost nowhere to break and must stretch
% the few interword spaces it has -- which is precisely an underfull hbox.
%
% Ragged right removes the stretch rather than hiding it: the line simply ends
% where the last word ended. Block callouts set ragged right is ordinary
% typography, and it is what \chapteropening already does above.
\tcbset{before upper=\raggedright}
