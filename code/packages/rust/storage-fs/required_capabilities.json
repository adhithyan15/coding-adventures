{
  "$schema": "https://raw.githubusercontent.com/adhithyan15/coding-adventures/main/code/specs/schemas/required_capabilities.schema.json",
  "version": 1,
  "package": "rust/storage-fs",
  "capabilities": ["filesystem", "clock"],
  "justification": "Filesystem-backed StorageBackend: creates / reads / writes / fsyncs files under a caller-supplied root directory; uses the system clock for record timestamps and lease expiry."
}
