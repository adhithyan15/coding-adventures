[project]
name = "coding-adventures-vendor-api-simulators"
version = "0.1.0"
description = "Six vendor GPU API simulators (CUDA, OpenCL, Metal, Vulkan, WebGPU, OpenGL) as thin wrappers over the compute runtime"
readme = "README.md"
requires-python = ">=3.12"
license = { text = "MIT" }
authors = [{ name = "Adhithya Rajasekaran" }]
keywords = [
    "gpu",
    "cuda",
    "opencl",
    "metal",
    "vulkan",
    "webgpu",
    "opengl",
    "compute",
    "simulator",
    "computer-science",
    "education",
]

dependencies = [
    "coding-adventures-compute-runtime",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-cov>=4.0",
    "ruff>=0.4.0",
    "mypy>=1.10",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/vendor_api_simulators"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v --tb=short"

[tool.ruff]
line-length = 88
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP", "B", "SIM", "TCH"]

[tool.mypy]
python_version = "3.12"
strict = true
