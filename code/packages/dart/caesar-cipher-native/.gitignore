# Generated by `dart pub get` during tools/run-tests.sh — not committed
# (matches dart/conduit, the sibling dart:ffi package).
pubspec.lock

# Cargo build output for the cdylib (also covered by the root .gitignore).
/target/
