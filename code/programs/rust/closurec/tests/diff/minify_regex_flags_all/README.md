# minify_regex_flags_all

CLOC14.41 byte-identity fixture (captured from the upstream
Closure JAR v20240317, WHITESPACE_ONLY).

- **Input:** `var r=/x/gimsuy;`
- **Note:** gap-096 (CORRECTNESS): u/y regex flags split into a separate token by the lexer
