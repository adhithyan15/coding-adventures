# neural-network

Graph-native neural network primitives for Rust.

This package mirrors the NN02 cross-language surface: graph/node/edge property
bags, weighted sums, activation nodes, output boundaries, and an explicit XOR
example with one hidden layer.
