[package]
name = "coding_adventures_argon2id"
version = "0.1.0"
edition = "2021"
description = "Argon2id (RFC 9106) — hybrid memory-hard password hashing, from scratch, no external deps except sibling blake2b"

[dependencies]
coding_adventures_blake2b = { path = "../blake2b" }
