{
  "$schema": "https://raw.githubusercontent.com/adhithyan15/coding-adventures/main/code/specs/schemas/required_capabilities.schema.json",
  "version": 1,
  "package": "typescript/forme-aot-page-emitter",
  "capabilities": ["fs"],
  "justification": "Writes per-page CSS (and optional HTML wrappers) to a caller-supplied distDir.  Reads nothing.  No network, no shell, no env reads.  The fs IO is also injectable (EmitIO) so tests run with in-memory writers; production wires to node:fs."
}
