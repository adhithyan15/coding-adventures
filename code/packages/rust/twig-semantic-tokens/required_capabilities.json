{
  "$schema": "https://raw.githubusercontent.com/adhithyan15/coding-adventures/main/code/specs/schemas/required_capabilities.schema.json",
  "version": 1,
  "package": "rust/twig-semantic-tokens",
  "capabilities": [],
  "justification": "Pure data → typed token list: parses Twig source via twig-parser, walks the AST, returns Vec<SemanticToken>.  No I/O, no FFI, no filesystem/network/process/env access.  Single dep on twig-parser (also empty)."
}
