[package]
name = "pipeline"
version = "0.1.0"
edition = "2021"
description = "Computing-stack orchestrator that chains the Rust lexer, parser, compiler, and VM into one traceable execution flow"

[dependencies]
lexer = { path = "../lexer" }
parser = { path = "../parser" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
