# The ABI logic is unit-tested natively (the exports are plain pointer + JSON logic).
# The actual wasm binary is produced by build-wasm.sh and smoke-tested with node.
cargo test -p task-wasm -- --nocapture
