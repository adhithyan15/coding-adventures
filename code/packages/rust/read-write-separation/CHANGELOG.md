# Changelog

## Unreleased

- Add the first manifest-level read/write separation classifier.
- Add v1 overlap checks for file, vault, and channel read/write pairs.
- Add `CapabilityManifestSummary` and `summarize_manifest()` for payload-free
  manifest shape, risk, overlap, and justification coverage.
