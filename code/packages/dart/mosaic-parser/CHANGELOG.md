# Changelog

## 0.1.0

- Initial Dart `mosaic-parser` package.
- Added grammar-driven parsing for imports, components, slots, properties,
  nested nodes, conditionals, and iteration blocks.
- Corrected the canonical Mosaic property-value ordering so dotted enum values
  are tried before their bare-name prefix.
