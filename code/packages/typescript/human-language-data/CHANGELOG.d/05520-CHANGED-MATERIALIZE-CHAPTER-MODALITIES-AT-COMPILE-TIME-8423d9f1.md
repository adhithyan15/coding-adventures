### Changed

- Retired the 23 tracked `chapter-modalities.tex` rollups. The all-books gate now
  derives the same fail-closed modality projections into an isolated temporary
  input tree immediately before XeLaTeX and cleans the tree on exit.
