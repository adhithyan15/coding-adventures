/**
 * TreeSet
 * =======
 *
 * Browser-safe ordered set with sorted iteration, rank / selection helpers,
 * range queries, and set algebra.
 */

export { TreeSet, fromValues } from "./tree-set.js";
export type { Comparator } from "./tree-set.js";

