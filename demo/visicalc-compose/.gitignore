.gradle/
.gradle-out/
build/
.idea/
*.iml
local.properties
out/

# Vendored engine — the C ABI dynamic library the Java FFM API loads, built
# by scripts/build.sh from the spreadsheet-capi crate (cdylib). A build
# artifact, like the SwiftUI/Qt demos' Vendor/.
native/

# Compose native-distribution staging (per-target engine dylib, git-ignored
# like native/ — scripts/build.sh repopulates it from the capi crate).
appResources/
