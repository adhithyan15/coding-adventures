[package]
name = "coding_adventures_vault_pm_domain"
version = "0.2.0"
edition = "2021"
description = "VLT-PM03 pure password-manager item, metadata merge, conflict, and redacted-view domain model"

[dependencies]
coding_adventures_vault_records = { path = "../vault-records" }
coding_adventures_zeroize = { path = "../zeroize" }
