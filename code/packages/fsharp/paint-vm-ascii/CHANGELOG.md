# Changelog

## 0.1.0

- add the initial pure F# `paint-vm-ascii` backend
- render rects, lines, glyph runs, groups, clips, and plain layers into a terminal grid
- expose reusable `createAsciiContext`, `createAsciiVM`, `createAsciiVMWith`, `renderToAscii`, and `renderToAsciiWith` entry points
