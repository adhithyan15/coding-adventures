# Changelog

## 0.1.0

- add the initial pure C# `paint-vm-ascii` backend
- render rects, lines, glyph runs, groups, clips, and plain layers into a terminal grid
- expose reusable `CreateAsciiContext()`, `CreateAsciiVm()`, and `RenderToAscii()` entry points
