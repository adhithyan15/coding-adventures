# code39

Dependency-free Code 39 encoder that emits backend-neutral paint scenes.

This package performs normalization, encoding, run expansion, and 1D layout,
then stops at `PaintScene`.

## Development

```bash
bash BUILD
```
