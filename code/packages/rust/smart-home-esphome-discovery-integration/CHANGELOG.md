# Changelog

## 0.1.0

- Add authorized, bounded `_esphomelib._tcp.local` discovery with strict
  identity and advertised Noise-security validation.
- Normalize verified ESPHome nodes into D23 without opening the native API or
  taking custody of encryption keys.
