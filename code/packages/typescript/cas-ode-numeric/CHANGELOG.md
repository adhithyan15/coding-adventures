# Changelog

## Unreleased

- Run POSIX dependency lock commands explicitly through Bash on Windows.

## 0.1.0

- Add pure TypeScript RK4 solver over symbolic IR expressions.
- Add numeric literal extraction and focused ODE regression tests.
