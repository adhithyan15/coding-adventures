---
category: Repo policy / workflow reminders
---

# Default to a fresh `git worktree`

from `origin/main` whenever the source checkout is shared, noisy, or has other agents active. Treat it as the default, not an exception.
