### Venture shared View Source panel

- Render the retained page title, committed address, and exact raw response in
  one shared closable Mosaic panel across every generated Venture host.
- Preserve the typed auxiliary-document presenter effect so richer platform
  integrations coexist with deterministic visible behavior in every shell.
