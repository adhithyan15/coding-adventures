[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "coding-adventures-twig-beam-compiler"
version = "0.1.0"
description = "Twig source → BEAM .beam file (real erl target) — BEAM01 Phase 4"
requires-python = ">=3.12"
license = "MIT"
authors = [{ name = "Adhithya Rajasekaran" }]
readme = "README.md"
keywords = ["twig", "lisp", "beam", "erlang", "compiler", "education"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Education",
    "Topic :: Education",
    "Topic :: Software Development :: Compilers",
    "Programming Language :: Python :: 3.12",
    "License :: OSI Approved :: MIT License",
    "Typing :: Typed",
]
dependencies = [
    "coding-adventures-twig",
    "coding-adventures-compiler-ir",
    "coding-adventures-ir-optimizer",
    "coding-adventures-ir-to-beam",
    "coding-adventures-beam-bytecode-encoder",
]

[project.urls]
Homepage = "https://github.com/adhithyan15/coding-adventures"
Repository = "https://github.com/adhithyan15/coding-adventures"

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-cov>=5.0",
    "ruff>=0.4",
    "mypy>=1.10",
]

[tool.hatch.build.targets.wheel]
packages = ["src/twig_beam_compiler"]

[tool.ruff]
target-version = "py312"
line-length = 88

[tool.ruff.lint]
select = ["E", "W", "F", "I", "UP", "B", "SIM"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--cov=twig_beam_compiler --cov-report=term-missing --cov-fail-under=80"

[tool.coverage.run]
source = ["src/twig_beam_compiler"]

[tool.coverage.report]
fail_under = 80
show_missing = true
