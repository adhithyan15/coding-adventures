# Java Directed Graph

Native Java implementation of the directed graph package.

It provides:

- `Graph` for standard directed graphs
- `LabeledDirectedGraph` for labeled edges
- cycle detection and topological sorting
- transitive reachability helpers
- shared traversal behavior via the Java `graph` package
