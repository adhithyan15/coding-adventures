# layout-backgrounds

Typed, producer-neutral background layer and corner geometry for Layout IR.
The component resolves border, padding, and content painting boxes plus nested
elliptical corner geometry without importing CSS parsers, HTML, paint
instructions, or host toolkits.

Spec: [`UI51-layout-backgrounds`](../../../specs/UI51-layout-backgrounds.md).
