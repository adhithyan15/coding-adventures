# When a shared discovery fixture gains a generated-artifact decoy, audit every consumer before treating the reference-engine repair as parity

Adding Dune's exact `_build` case fixed Go coverage but left Ruby's full registry consumer red and exposed missing behavior or fixture adoption across other build-tool lanes. Re-run each native fixture consumer, register every remaining engine before selecting new work, and preserve exact case plus near-name source directories in direct tests.
