{
  "$schema": "https://raw.githubusercontent.com/adhithyan15/coding-adventures/main/code/specs/schemas/required_capabilities.schema.json",
  "version": 1,
  "package": "swift/sha256-native",
  "capabilities": ["ffi:call"],
  "justification": "ffi:call — links the Rust sha256-c static library at compile time and calls its C ABI to compute SHA-256. No network, filesystem, or process access."
}
