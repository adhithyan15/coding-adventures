<!-- Auto-generated by mosaic-emit-xaml. Do not edit. -->
<UserControl
    x:Class="Mosaic.Generated.Alert"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Mosaic.Generated">
    <UserControl.Resources>
        <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>

    <Border Padding="12" CornerRadius="4" Background="#cff4fc" BorderThickness="1" BorderBrush="#b6effb">
        <Border.Resources>
            <Style TargetType="TextBlock">
                <Setter Property="Foreground" Value="#055160"/>
            </Style>
        </Border.Resources>
        <StackPanel Orientation="Horizontal" Padding="0">
            <TextBlock Text="{x:Bind Message}"/>
            <ContentControl Visibility="{x:Bind Dismissible, Converter={StaticResource BoolToVisibilityConverter}}">
                <Button x:Name="CloseBtn" Content="x" Click="CloseBtn_Click" Padding="4" Background="Transparent" BorderThickness="0" Foreground="#055160"/>
            </ContentControl>
        </StackPanel>
    </Border>

</UserControl>
