// Intentionally empty. This target exists only to expose spreadsheet.h as a
// Swift-importable module (`import CSpreadsheetEngine`). The sc_* functions are
// implemented in the Rust `spreadsheet-capi` static library, linked into the
// executable via the linker flags in Package.swift.
