# wasm-simulator

Native Kotlin educational WASM simulator.

This package currently provides:

- fixed-width encoders for a small stack-machine subset
- a bytecode decoder and step trace model
- a standalone simulator for simple WASM teaching programs
