[package]
name = "mosaic-pkg-card-browser"
version = "0.1.0"
description = "Reusable Anki-style card browser and search controls built on the Mosaic UI pipeline"
license = "MIT OR Apache-2.0"

[components]
exports = ["CardBrowser"]

[dependencies]
mosaic-pkg-toolkit = "0.11.0"

[kernel]
version = "1"
