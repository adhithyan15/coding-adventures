# Changelog

## 0.1.0

- Add local-IP-only configuration and authorized, bounded native basic device
  identification before register telemetry.
- Add explicit, read-only Modbus TCP register profiles with bounded polling.
- Normalize unsigned, signed, and floating-point registers into D23 sensors.
- Authorize reads before transport access and validate live MBAP responses.
