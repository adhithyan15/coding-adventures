[package]
name = "mosaic-app-wasm"
version = "0.1.0"
edition = "2021"
description = "Reusable linear-memory WebAssembly lifecycle bridge for Mosaic applications."
license = "MIT"

[dependencies]
mosaic-app-runtime = { path = "../mosaic-app-runtime" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
