# Changelog

## 0.1.0 — 2026-04-27

Initial release. Implements `ImaginaryUnit` constant, `i^n` power
normalization, rectangular-form arithmetic, and the
`Re`/`Im`/`Conjugate`/`Abs`/`Arg`/`RectForm`/`PolarForm` heads.
