{
  "$schema": "https://coding-adventures.example/schemas/required_capabilities/v1.json",
  "version": 1,
  "package": "dart/sha256-native",
  "capabilities": ["ffi:call"],
  "justification": "ffi:call — uses dart:ffi DynamicLibrary.open to call into the Rust sha256-native shared library (libsha256_native.so/.dylib), which wraps the pure coding_adventures_sha256 crate over a C ABI. No network, filesystem, or process capabilities."
}
