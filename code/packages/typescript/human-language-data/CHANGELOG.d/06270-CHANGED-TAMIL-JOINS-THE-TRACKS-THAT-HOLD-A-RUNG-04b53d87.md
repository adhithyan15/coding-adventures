### Changed — Tamil joins the tracks that hold a rung

`tests/level-gate.test.ts`: `tracksWithAnyLevel` 3 -> 4 and `tamil: "pre-A1"`
joins the pinned map. `curriculum-digests/tamil.json` moves for chapters 87-108.
