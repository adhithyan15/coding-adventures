# @coding-adventures/hash-map

Immutable hash map wrapper with chainable update helpers.

This package is intentionally small and dependency-free. It provides a thin
TypeScript-friendly abstraction over `Map` that mirrors the Rust-style
value-oriented API used elsewhere in the repo.
