[package]
name = "required-capabilities-compiler"
version = "0.1.0"
edition = "2021"
description = "Compile required_capabilities.json into Rust operation boundary modules"

[[bin]]
name = "required-capabilities-compiler"
path = "src/main.rs"

[dependencies]
capability-cage = { path = "../capability-cage" }
cli-builder = { path = "../cli-builder" }
coding-adventures-json-value = { path = "../json-value" }
