This directory vendors a slice of the official WebAssembly conformance
test suite, unmodified, for use by the `wasm-conformance` crate (see
`code/specs/W05-wasm-conformance-harness.md`).

Source:        https://github.com/WebAssembly/testsuite
Pinned commit: 28864811cf03bdbf880733786148feaba339582d
Fetched:       2026-08-13 (initial slice, W05/PR3), extended 2026-08-14 (WASM08),
               extended 2026-08-15 (WASM18 -- proposals/threads/atomic.wast,
               vendored flat as atomic.wast, same pinned commit, no re-pin),
               extended 2026-08-15 (task #93 -- linking.wast, real cross-
               module linking, same pinned commit, no re-pin),
               extended 2026-08-16 (task #96 -- table_get.wast/
               table_set.wast, real multi-table type-checking, same
               pinned commit, no re-pin),
               extended 2026-08-16 (task #94 -- memory_copy.wast/
               memory_fill.wast, first vendored bulk-memory-proposal
               files, same pinned commit, no re-pin),
               extended 2026-08-16 (task #95 -- memory_init.wast, real
               passive data segments + memory.init/data.drop, same
               pinned commit, no re-pin),
               extended 2026-08-16 (task #98 -- table_size.wast/
               table_fill.wast, table.size/table.grow/table.fill, same
               pinned commit, no re-pin),
               extended 2026-08-17 (task #92/#109-112 -- memory-multi.wast,
               real multi-memory memarg, same pinned commit, no re-pin),
               extended 2026-08-18 (task #113-117 -- simd_i32x4_arith.wast/
               simd_i32x4_cmp.wast, i32x4 SIMD opcode widening, same
               pinned commit, no re-pin),
               extended 2026-08-18 (task #118-120 -- simd_i32x4_arith2.wast,
               i32x4 abs/min/max widening, same pinned commit, no re-pin),
               extended 2026-08-18 (task #121-124 -- simd_i32x4_extadd_
               pairwise_i16x8.wast/simd_i32x4_dot_i16x8.wast/
               simd_i32x4_extmul_i16x8.wast, i32x4-from-i16x8 widening,
               same pinned commit, no re-pin),
               extended 2026-08-18 (task #125-128 -- simd_i8x16_arith.wast,
               i8x16 first slice, same pinned commit, no re-pin),
               extended 2026-08-18 (task #129-132 -- simd_i16x8_arith.wast,
               i16x8 first primary-lane-width slice, same pinned commit,
               no re-pin),
               extended 2026-08-18 (task #133-136 -- simd_i16x8_cmp.wast,
               i16x8 comparison family, same pinned commit, no re-pin),
               extended 2026-08-18 (task #137-140 -- simd_i8x16_cmp.wast,
               i8x16 comparison family, same pinned commit, no re-pin),
               extended 2026-08-18 (task #141-143 -- simd_i8x16_arith2.wast,
               i8x16 abs/popcnt/min/max/avgr_u family, same pinned
               commit, no re-pin),
               extended 2026-08-18 (task #144-146 -- simd_i16x8_arith2.wast,
               i16x8 abs/min/max/avgr_u family, same pinned commit, no
               re-pin),
               extended 2026-08-18 (task #147-149 --
               simd_i16x8_extadd_pairwise_i8x16.wast/
               simd_i16x8_extmul_i8x16.wast, i16x8-from-i8x16 widening,
               same pinned commit, no re-pin),
               extended 2026-08-18 (task #150-152 -- simd_bitwise.wast,
               v128.not/and/andnot/or/xor/bitselect bitwise family, same
               pinned commit, no re-pin),
               extended 2026-08-18 (task #153-155 -- simd_boolean.wast,
               v128.any_true + ixNxM.all_true/bitmask boolean-reduction
               family, same pinned commit, no re-pin),
               extended 2026-08-18 (task #156-158 -- simd_i64x2_arith.wast/
               simd_i64x2_arith2.wast/simd_i64x2_cmp.wast, i64x2's first
               real arithmetic family, same pinned commit, no re-pin),
               extended 2026-08-18 (task #159-161 -- simd_bit_shift.wast,
               ixNxM.shl/shr_s/shr_u across all 4 lane widths, the first
               mixed-type binary SIMD op family, same pinned commit, no
               re-pin),
               extended 2026-08-18 (task #162-164 -- simd_load.wast/
               simd_store.wast, v128.load/v128.store, the first SIMD ops
               touching real linear memory, same pinned commit, no
               re-pin),
               extended 2026-08-19 (task #165-167 -- simd_splat.wast,
               i8x16.splat/i16x8.splat/i64x2.splat, same pinned commit,
               no re-pin)
Fetch method:  code/packages/rust/wasm-conformance/tests/fixtures/fetch_testsuite.py

The pinned commit was resolved via:
    git ls-remote https://github.com/WebAssembly/testsuite.git refs/heads/main

`main` is a live, moving branch that interleaves post-MVP proposal files
(GC, exceptions, tail calls, SIMD, threads/atomics) with plain WASM 1.0
MVP-core ones, so a pinned SHA -- not `main` itself -- is vendored to keep
this repo's conformance baseline reproducible across re-fetches. Bumping
the pin is a deliberate, reviewed decision, not something a re-run of the
fetch script does casually.

Mostly WASM 1.0 MVP-core `.wast` files are vendored here; see
`code/specs/W05-wasm-conformance-harness.md` section 6 for the original
exclusion rationale (SIMD, threads, exceptions, tail calls, bulk memory
operations, GC/reference-types beyond this repo's existing narrow slice,
memory64, the component model, and files needing the `spectest`
host-import module) -- still the same list after the WASM08 slice
extension, just a larger set of files that satisfy it. WASM18 (`code/
specs/W09-wasm-atomics-plain.md`) is the first deliberate exception:
`atomic.wast` exercises PLAIN atomic memory ops (load/store/RMW/
cmpxchg/fence), which don't need real multi-threading to be meaningful
-- `memory.atomic.notify`/`wait32`/`wait64`, in that same file, remain
genuinely out of scope and are graded `NotYetSupported`. Every other
excluded category above still becomes its own future phase's vendored
slice.

`linking.wast` (task #93) is a second deliberate exception: real cross-
module linking (imports/exports/`register` across separate `WasmInstance`s)
has had genuine support since WASM05, so the file's own original W05
exclusion note ("heavier module-linking semantics") no longer applies. Of
its 71 modules, exactly 2 import from `spectest` (this crate still has no
`spectest` host, by design) -- those two modules and whatever directives
depend on them grade `NotYetSupported`/collateral-fail; every other
directive exercises real, already-supported linking machinery.

`table_get.wast`/`table_set.wast` (task #96) surfaced (and this repo now
fixes) a real bug: table declarations never actually read their own
`funcref`/`externref` reftype keyword, silently defaulting every table's
element_type to funcref regardless of the source. Deliberately excludes
their sibling `table.wast` (a hex-literal table limit + a real `spectest`
import, task #99) and `table_size.wast`/`table_grow.wast`/`table_fill.wast`
(need `table.size`/`table.grow`/`table.fill`, entirely unimplemented
opcodes, task #98).

`memory_copy.wast`/`memory_fill.wast` (task #94) are the first vendored
files from the bulk-memory proposal. `memory.copy` already had a real
interpreter handler (E4-dyn's runtime string concat needed it); this
vendoring pass added `memory.fill`'s handler and text-form parsing
support for BOTH instructions (neither had any `wast-parser` support
before -- see that crate's own CHANGELOG). Deliberately excludes their
sibling `bulk.wast`: it mixes memory.copy/memory.fill with
memory.init/data.drop (task #95) and table.init/elem.drop/table.copy
(task #97) in the same file, all still unimplemented.

`memory_init.wast` (task #95) needed real new interpreter state --
`memory.init`/`data.drop` (0xFC 0x08/0x09) were entirely unimplemented
before this pass (no opcode decoding, no interpreter handler, no
wast-parser text-form support, no validator type-check rule), and
neither can work without real PASSIVE data segments (`(data $d
"bytes")`, no offset expression): `wasm-module-parser`'s binary decoder
only ever handled segment-mode flag `0x00` (active, implicit memory 0)
before this, not the real 3-mode encoding the bulk-memory proposal
defines. 100% pass, every directive kind, on the first baseline regen
after implementation. Only uses single-memory numeric segment indices
(no `spectest` import, no table/elem instructions), so it's vendorable
standalone -- unlike its sibling `bulk.wast` (task #97 still pending)
and `memory-multi.wast` (task #92, explicit multi-memory indices, was
blocked on real multi-memory memarg support).

`memory-multi.wast` (task #92/#109-112) is that follow-up, now unblocked:
every memarg-carrying load/store opcode (`0x28`-`0x3E`, 23 total) plus
`memory.copy`/`memory.fill`/`memory.init` previously hardcoded memory
index 0 and either ignored or assumed a fixed byte width for any memidx
immediate -- a real, decoded-but-discarded gap, not a deferred feature
(`ModuleContext.has_memory` was a plain bool for exactly this reason, per
its own doc comment). This pass added the real binary flags-bit `0x40` +
memidx decode/encode (`wasm-execution`), the matching text-form leading
memidx token for `i32.load`/`memory.fill`/`memory.init`
(`wasm-wast-parser`), and the matching validation-time bounds-check
(`wasm-validator`, `ModuleContext.memory_count`) -- see `code/specs/
W18-wasm-multi-memory-memarg.md`. 100% pass, every directive kind
(2/2 modules, 4/4 assert_return), on the first baseline regen after
implementation.

`simd_i32x4_arith.wast`/`simd_i32x4_cmp.wast` (task #113-117) widen the
SIMD first slice's opcode coverage (`code/specs/
W13-wasm-simd-v128-first-slice.md`) within the SAME `i32x4` lane width
it already covers -- no new lane width, no new binary-encoding shape,
same "highly mechanical once the lane-arithmetic helpers exist" pattern
that spec's own W07 sizing doc predicted. Adds `i32x4.mul`/`neg`/`sub`
(joining the already-implemented `add`) and the full comparison family
`ne`/`lt_s`/`lt_u`/`gt_s`/`gt_u`/`le_s`/`le_u`/`ge_s`/`ge_u` (joining
`eq`) -- 12 new opcodes, each sub-opcode byte fetched live from the SIMD
proposal's own `BinarySIMD.md` and cross-checked against the already-
implemented `i32x4.eq`(`0x37`)/`i32x4.add`(`0xAE`) entries (both matched
exactly, same verification discipline as the original 5-opcode slice).
`simd_i32x4_arith.wast`: 100% pass on every gradeable directive (2/2
modules, 181/181 assert_return, 11/11 assert_invalid).
`simd_i32x4_cmp.wast`: 100% pass on every gradeable directive (420/420
assert_return, 30/30 assert_invalid); its one `NotYetSupported` module
and 13 `NotYetSupported` assert_return directives exercise
`i32x4.trunc_sat_f32x4_s`/`f32x4.const` float-to-int truncation boundary
values, genuinely out of scope for this i32x4-only widening pass.
Deliberately excludes their sibling `simd_splat.wast`: it references many
more unsupported opcode families across ALL 6 lane-width shapes
(f32x4/f64x2/i16x8/i8x16 arithmetic, `shr_s`, `all_true`, `swizzle`,
saturating add/sub, `trunc_sat`), not just more `i32x4` ops, so it stays
deferred to a future, larger widening pass.

`simd_i32x4_arith2.wast` (task #118-120) is the direct follow-up: the
upstream corpus itself splits `i32x4`'s arithmetic coverage across two
files, and this is the "second half" -- `i32x4.abs` (the first UNARY
kind besides `neg`) plus the `min_s`/`min_u`/`max_s`/`max_u` family
(same `v128,v128->v128` shape as `add`/`sub`/`mul`), each sub-opcode
fetched live from `BinarySIMD.md` and cross-checked against the
already-implemented `i32x4.eq`/`i32x4.add` entries, same discipline as
every widening pass above. 100% pass on every directive kind with zero
`NotYetSupported` at all (2/2 modules, 121/121 assert_return, 14/14
assert_invalid, 12/12 assert_malformed) -- the first SIMD file this repo
vendors with no partial-credit directives whatsoever.

`simd_i32x4_extadd_pairwise_i16x8.wast`/`simd_i32x4_dot_i16x8.wast`/
`simd_i32x4_extmul_i16x8.wast` (task #121-124) widen this arc once more:
`i32x4.extadd_pairwise_i16x8_s`/`_u`, `i32x4.dot_i16x8_s`, and
`i32x4.extmul_low`/`high_i16x8_s`/`_u` -- the first opcodes this repo
implements whose INPUT lane width (16-bit `i16x8`) differs from their
OUTPUT lane width (32-bit `i32x4`), each sub-opcode fetched live from
`BinarySIMD.md` and cross-checked against the already-implemented
`i32x4.eq`/`i32x4.add` entries, same discipline as every widening pass
above. 100% pass on every directive kind across all three files (3/3
modules, 148/148 assert_return, 19/19 assert_invalid).

`simd_i8x16_arith.wast` (task #125-128) is this arc's first pivot to a
BRAND-NEW lane width rather than a further widening of `i32x4`: `i32x4`
had run out of small increments (everything left needs a float lane
width). `i8x16.add`/`sub`/`neg` -- the first `i8x16` opcodes this repo
implements -- follow the exact same "first slice" pattern `i32x4` itself
started with (see the very first paragraph above), except even smaller:
no `i8x16.splat`/`extract_lane` were needed, since `v128.const i8x16
...` (already supported for all 6 shapes since PR1b) covers both this
file's operand construction and its result comparison on its own.
WASM SIMD defines no `i8x16.mul` (8-bit lanes are too narrow for a
useful lane-wise multiply), so this slice is deliberately just 3
opcodes. Each sub-opcode byte fetched live from `BinarySIMD.md` and
cross-checked against the already-implemented `i32x4.add`/`i32x4.abs`
entries, same discipline as every prior addition. 100% pass on every
directive kind (2/2 modules, 121/121 assert_return, 8/8 assert_invalid).

`simd_i16x8_arith.wast` (task #129-132) is the direct follow-up: `i8x16`
now has add/sub/neg but no `mul` (the spec defines none), so this is the
first file where `i16x8` is a PRIMARY lane width (produces `i16x8`
results) rather than merely an INPUT to an `i32x4`-producing widening op
(`extadd_pairwise`/`dot`/`extmul`, already implemented). `i16x8.add`/
`sub`/`mul`/`neg` -- unlike `i8x16`, WASM SIMD DOES define `i16x8.mul`,
so this slice includes all four ops the real upstream corpus bundles
together in one file. Same "no splat/extract_lane needed" pattern as
`i8x16.add`/`sub`/`neg`: `v128.const i16x8 ...` already covers this
file's own operand construction and result comparison. Each sub-opcode
byte fetched live from `BinarySIMD.md` and cross-checked against the
already-implemented `i32x4.add`/`i8x16.add` entries, same discipline as
every prior addition. 100% pass on every directive kind (2/2 modules,
181/181 assert_return, 11/11 assert_invalid).

`simd_i16x8_cmp.wast` (task #133-136) closes the gap left when
`i16x8.add`/`sub`/`mul`/`neg` landed without a comparison family --
unlike `i32x4`, which got arith+cmp together in one pass. `i16x8.eq`/
`ne`/`lt_s`/`lt_u`/`gt_s`/`gt_u`/`le_s`/`le_u`/`ge_s`/`ge_u` use the same
boolean-mask convention and signed/unsigned split as `i32x4`'s own
comparison family, just at `i16x8`'s narrower lane width. Each
sub-opcode byte fetched live from `BinarySIMD.md` and cross-checked
against the already-implemented `i16x8.add`/`i32x4.eq` entries, same
discipline as every prior addition. 100% pass on every GRADEABLE
directive (420/420 assert_return, 30/30 assert_invalid); the file's own
small "combination" tail references `v128.load` (not yet implemented),
so 1 module and 13 assert_return directives grade `NotYetSupported`,
the same lazy-grading discipline already established for
`simd_i32x4_cmp.wast`'s own `trunc_sat`-dependent tail.

`simd_i8x16_cmp.wast` (task #137-140) closes the same gap PR6 closed for
`i16x8`, but for `i8x16`: `i8x16.add`/`sub`/`neg` landed (PR4) without a
comparison family. `i8x16.eq`/`ne`/`lt_s`/`lt_u`/`gt_s`/`gt_u`/`le_s`/
`le_u`/`ge_s`/`ge_u` use the same boolean-mask convention and
signed/unsigned split as `i16x8`'s and `i32x4`'s own comparison
families, just at the narrowest lane width. Each sub-opcode byte
fetched live from `BinarySIMD.md` and cross-checked against the
already-implemented `i8x16.add`/`i16x8.eq` entries, same discipline as
every prior addition. 100% pass on every GRADEABLE directive (400/400
assert_return); the file's own small "combination" tail references
`v128.load` (not yet implemented), so 1 module and 13 assert_return
directives grade `NotYetSupported`, the same lazy-grading discipline
already established for `simd_i16x8_cmp.wast`'s own tail.

`simd_i8x16_arith2.wast` (task #141-143) is the direct follow-up: it
mirrors `i32x4`'s own `abs`/`min_s`/`min_u`/`max_s`/`max_u` widening
(PR2), plus two op SHAPES with no `i32x4`/`i16x8` precedent in this
interpreter -- `popcnt` (lane-wise Hamming weight) and `avgr_u`
(lane-wise unsigned rounding average, `(a + b + 1) >> 1`). WASM SIMD
only defines `popcnt`/`avgr_u` for `i8x16` (`avgr_u` is also defined
for `i16x8`, but not `i32x4`). Each sub-opcode byte fetched live from
`BinarySIMD.md` and cross-checked against the already-implemented
`i8x16.add`/`i8x16.neg`/`i8x16.sub` entries (all three matched
exactly), same discipline as every prior addition. 100% pass on EVERY
directive kind (2/2 modules, 184/184 assert_return, 19/19
assert_invalid, 6/6 assert_malformed) -- no `NotYetSupported` tail this
time, unlike `simd_i8x16_cmp.wast`'s own `v128.load`-dependent one.

`simd_i16x8_arith2.wast` (task #144-146) closes the same "arith2" gap
for `i16x8` that PR8 just closed for `i8x16` (no `i16x8.popcnt` --
WASM SIMD only defines `popcnt` for `i8x16`). `i16x8.abs`/`min_s`/
`min_u`/`max_s`/`max_u`/`avgr_u` use the same conventions as `i8x16`'s
own arith2 family, just at `i16x8`'s wider lane width. Each sub-opcode
byte fetched live from `BinarySIMD.md` and cross-checked against the
already-implemented `i16x8.neg`/`add`/`sub`/`mul` entries (all four
matched exactly), same discipline as every prior addition. 100% pass
on EVERY directive kind (2/2 modules, 151/151 assert_return, 17/17
assert_invalid, 2/2 assert_malformed).

`simd_i16x8_extadd_pairwise_i8x16.wast`/`simd_i16x8_extmul_i8x16.wast`
(task #147-149) mirror the already-implemented `i32x4`-from-`i16x8`
widening family one lane width down -- closing the last remaining gap
between `i16x8` and `i8x16`'s coverage. `extadd_pairwise_i8x16_s`/`_u`
pairwise-add adjacent `i8` lanes after sign-/zero-extending to `i16`;
`extmul_low`/`high_i8x16_s`/`_u` take only the low/high 8 `i8` lanes
of each operand, sign-/zero-extend to `i16`, and multiply lane-wise
(no summation). Unlike the `i32x4`-from-`i16x8` family, there is no
`i16x8.dot_i8x16_s` -- WASM SIMD does not define a dot-product for
this pair. Each sub-opcode byte fetched live from `BinarySIMD.md` and
cross-checked against the already-implemented `i8x16.add`/
`i16x8.mul`/`i16x8.avgr_u`/`i32x4.dot_i16x8_s`/`i8x16.popcnt`/
`i32x4.extadd_pairwise_i16x8_s` entries (all six matched exactly),
same discipline as every prior addition. 100% pass on EVERY directive
kind across both files (2/2 modules, 120/120 assert_return, 16/16
assert_invalid).

`simd_bitwise.wast` (task #150-152) covers `v128.not`/`and`/`andnot`/
`or`/`xor`/`bitselect` -- the lane-width-agnostic raw-byte bitwise
family, unlike every prior `simd_*` file which was scoped to one lane
width. Represents a strategic pivot from "widen the next narrow
per-lane-width family" (the pattern used in every prior SIMD PR) to
"close the highest-real-world-impact remaining gap", identified via a
broader prioritization survey now that `i8x16`/`i16x8`/`i32x4` all have
complete arith+cmp+arith2+widening coverage relative to each other.
`bitselect` is the first TERNARY SIMD op implemented in this
interpreter (pops three `v128`s, pushes one): `(a AND c) OR (b AND
(NOT c))`, i.e. select bits from `a` where the corresponding `c` bit is
1, else from `b`. Each sub-opcode byte fetched live from `BinarySIMD.md`
and cross-checked against the already-implemented `i8x16.add`/
`i32x4.add` entries, same discipline as every prior addition. 100%
pass on every gradeable directive kind (1/1 modules, 126/126
assert_return, 28/28 assert_invalid; 13 assert_return directives grade
`NotYetSupported` because they depend on `v128.load`, not a real
failure).

`simd_boolean.wast` (task #153-155) covers `v128.any_true` (one op,
exported under `i8x16.any_true`/`i16x8.any_true`/`i32x4.any_true` in
the corpus purely for test naming -- there is only one real
`v128.any_true` sub-opcode, lane-width-agnostic) plus `ixNxM.all_true`/
`bitmask` across all 4 lane widths (`i8x16`/`i16x8`/`i32x4`/`i64x2`).
The first `v128`-in/`i32`-out reduction shape besides `extract_lane`
(no lane-index immediate -- these reduce over ALL lanes), and the
first opcodes in this interpreter to read the operand as 8-byte
(`i64`) lanes. Chosen over shift-op and i64x2-arithmetic candidates in
a fresh prioritization survey: highest opcode count (9) behind a
single new operand shape and one 72KB corpus file, and unlocks real
use of the comparison families from earlier PRs (a `v128` mask result
is otherwise inert without a reduction op to consume it). Each
sub-opcode byte fetched live from `BinarySIMD.md` and cross-checked
against the already-implemented `v128.bitselect`/`i8x16.popcnt`/
`i16x8.abs`/`neg`/`i32x4.abs`/`neg` entries, same discipline as every
prior addition. 100% pass on every directive kind (2/2 modules,
259/259 assert_return, 12/12 assert_invalid, 4/4 assert_malformed).

`simd_i64x2_arith.wast`/`simd_i64x2_arith2.wast`/`simd_i64x2_cmp.wast`
(task #156-158) cover `i64x2.abs`/`neg`/`add`/`sub`/`mul`/`eq`/`ne`/
`lt_s`/`gt_s`/`le_s`/`ge_s` -- i64x2's first REAL ARITHMETIC family
(task #153-155 only added the `all_true`/`bitmask` reduction ops, no
computation). No `lt_u`/`gt_u`/`le_u`/`ge_u` -- the SIMD proposal never
defines unsigned `i64x2` comparisons, unlike every narrower lane
width. Reuses the existing `v128,v128->v128`/`v128->v128` shapes
already implemented for i8x16/i16x8/i32x4 -- this closes a lane-width
coverage gap, not a new operand shape. Each sub-opcode byte fetched
live from `BinarySIMD.md` and cross-checked against the
already-implemented `i64x2.all_true`/`bitmask` entries plus the
identical `abs`/`neg`/`[gap]`/`all_true`/`bitmask` cluster layout
already confirmed for i8x16/i16x8/i32x4. 100% pass on every directive
kind across all three files (5/5 modules, 310/310 assert_return, 23/23
assert_invalid). Implementing real i64x2 arithmetic also unblocked 22
previously-`NotYetSupported` `assert_return` directives in the
UNRELATED, already-vendored `simd_const.wast` (its i64x2 `v128.const`
round-trip cases needed a real i64x2 op to observe the result through)
-- a legitimate ripple effect, not a regression.

`simd_bit_shift.wast` (task #159-161) covers `ixNxM.shl`/`shr_s`/
`shr_u` across all 4 lane widths (`i8x16`/`i16x8`/`i32x4`/`i64x2`) --
the FIRST mixed-type binary SIMD op family in this interpreter: pops
a scalar `i32` shift amount (pushed last, so popped first) then a
`v128`, pushes one `v128`; every prior binary op popped two `v128`s or
one `v128`, never a mix of both types. Per the SIMD spec, the shift
amount is taken MODULO the lane's bit width before shifting (8/16/32/
64 respectively for `i8x16`/`i16x8`/`i32x4`/`i64x2`) -- both
spec-mandated and required for Rust safety, since shifting a
primitive by >= its bit width panics. Each sub-opcode byte fetched
live from `BinarySIMD.md` and cross-checked against the
already-implemented per-width `add` entries (every width's
`shl`/`shr_s`/`shr_u` triple sits immediately before that width's own
`add` sub-opcode -- e.g. `i8x16.shl`=`0x6B`/`shr_s`=`0x6C`/
`shr_u`=`0x6D`, then `i8x16.add`=`0x6E`). 100% pass on every gradeable
directive kind (1/1 modules, 187/187 assert_return, 24/24
assert_invalid, 15/15 assert_malformed; 24 assert_return directives
grade `NotYetSupported`).

`simd_load.wast`/`simd_store.wast` (task #162-164) cover `v128.load`/
`v128.store` -- the FIRST SIMD ops in this interpreter that touch real
linear memory (every prior SIMD op only reads/writes the per-instance
`v128` heap). Both single-byte sub-opcodes (`0x00`/`0x0B`); scoped to
memory-0-only execution for this first PR, since `DecodedOperand::
Simd{sub_opcode, aux}` (reused rather than inventing a new packing
scheme) only has room for one extra `u32`, so a full multi-memory
memarg decode is done for correct byte-consumption but only `offset`
is kept -- multi-memory `v128.load`/`v128.store` is deferred to a
later PR, same as WASM92 later widened the scalar load/store family.
Each sub-opcode byte fetched live from `BinarySIMD.md` and
cross-checked against the already-implemented `v128.const`/`i32.load`
entries. Landing this ALSO retroactively resolved 76 previously-
`NotYetSupported` `assert_return` directives (and 5 previously-
`NotYetSupported` modules, one per file) spread across FIVE
UNRELATED, already-vendored files whose own "combination"/round-trip
tails depended on a real `v128.load` to observe results through
memory: `simd_bitwise.wast` (task #150-152, +13), `simd_bit_shift.wast`
(task #159-161, +24), `simd_i16x8_cmp.wast` (task #133-136, +13),
`simd_i32x4_cmp.wast` (task #113-117, +13), and `simd_i8x16_cmp.wast`
(task #137-140, +13) -- same ripple-effect pattern as
`simd_i64x2_arith.wast`'s own fix to `simd_const.wast`, just wider.
100% pass on every GRADEABLE directive across both new files (9/9
modules, 27/27 assert_return, 11/11 assert_invalid, 6/6
assert_malformed); 7 modules and 7 assert_return directives in
`simd_load.wast` grade `NotYetSupported` because they exercise
float-lane ops (`f32x4.mul`/`abs`/`min`, `i32x4.trunc_sat_f32x4_s`,
`f32x4.convert_i32x4_u`) and `i8x16.swizzle`, none implemented yet --
genuinely out of scope for this memory-ops-only pass. Aggregate
`assert_return` went from 23937/23954 (pass/gradeable) to 24040/24057
-- +103 gradeable directives (+27 new passing directives from the two
new files themselves, out of 34 total new `assert_return` directives
with 7 grading `NotYetSupported`, plus +76 from the five-file ripple
above, which only moves directives between the `pass` and
`not_yet_supported` buckets of already-vendored files, not the total
directive count). `assert_invalid` rose by 11 and `assert_malformed`
rose by 6 (both still 100.0% of gradeable directives).

`simd_splat.wast` (task #165-167) covers `i8x16.splat`/`i16x8.splat`/
`i64x2.splat` (`0x0F`/`0x10`/`0x12`), widening lane-width coverage of
the already-implemented `i32x4.splat` (`0x11`) -- same "pop scalar,
push v128" shape; `i64x2.splat` is the first splat that pops a real
`i64` rather than an `i32`. Each sub-opcode byte fetched live from
`BinarySIMD.md` and cross-checked against the already-implemented
`i32x4.splat` entry (both matched exactly). Vendoring this file first
surfaced a genuine, unrelated bug in `wasm-wast-parser`'s numeric
literal parser: `nan:0x7f_ffff` (a `_` digit separator inside a NaN
payload) made the WHOLE script fail to parse, since `parse_f32_bits`/
`parse_f64_bits`'s `nan:0x<payload>` arm called `from_str_radix`
directly on the raw payload text instead of routing it through the
`strip_underscores` helper every other numeric literal path in that
file already uses -- fixed as part of this same PR, with dedicated
regression tests. Once the file parses, its own upstream structure
bundles all 6 lane widths' splat exports (including `f32x4.splat`/
`f64x2.splat`, neither implemented -- this crate has zero float-lane
SIMD support at all) into the SAME modules the new integer-lane
directives depend on, so every one of this file's `assert_return`
directives (158 total) grades `NotYetSupported` until float-lane
support lands in a future PR -- `assert_invalid` (22/22) and
`assert_malformed` (1/1) DO pass today, since those directives don't
require the shared modules to build. Vendored now anyway (rather than
deferred) since it's the real upstream file, the 3 new opcodes are
independently verified via dedicated unit tests in `wasm-execution`/
`wasm-wast-parser`/`wasm-validator`, and this file will automatically
flip to real credit with zero further changes once float-lane SIMD
support exists. Aggregate `assert_return` stays 24040/24057
(pass/gradeable, unchanged) but its `NotYetSupported` count rose from
552 to 710 (+158, exactly this file's own `assert_return` count);
`assert_invalid` rose from 1681/1681 to 1703/1703 (+22) and
`assert_malformed` from 274/274 to 275/275 (+1), both still 100.0% of
gradeable directives.

SIMD widen PR17 (task #168-170) implements `f32x4.splat`/`f64x2.splat`
-- the FIRST floating-point-typed SIMD ops in this crate. No new file
is vendored (no `fetch_testsuite.py` change); this is a baseline
regeneration against the already-vendored `simd_splat.wast` (task
#165-167 above). As predicted when that file was vendored, 3 of its 4
modules (module 1's 102 pure-splat directives, module 2's 5 splat-into-
store/load directives, and module 4's 8 splat-into-control-construct
directives -- 115 total) needed ONLY `f32x4.splat`/`f64x2.splat` to
build successfully; module 3's remaining 43 directives stay
`NotYetSupported`, since they additionally need `extract_lane`/
`replace_lane` for multiple lane widths and `i8x16.swizzle`, none
implemented yet. Aggregate `assert_return` rose from 24040/24057 to
24155/24172 (+115 pass, +115 gradeable, exactly matching the
predicted unblock count); `module` pass count rose from 1152 to 1155
(+3, one per fully-unblocked module) and its `NotYetSupported` count
fell from 78 to 75.

SIMD widen PR18 (task #171-173) implements `i8x16.swizzle`/
`extract_lane_s`/`extract_lane_u`/`replace_lane`. No new file is
vendored (no `fetch_testsuite.py` change); this is a baseline
regeneration against the already-vendored `simd_load.wast` (task
#162-164 above). That file has 17 `assert_return` directives across
14 modules, of which 7 directives (one module each) were stuck
`NotYetSupported`: 2 needed exactly the opcodes this PR adds
(`as-i8x16_extract_lane_s-value/0`, needing `i8x16.extract_lane_s`;
`as-i8x16.swizzle-operand`, needing `i8x16.swizzle`) and the other 5
need float-lane arithmetic/conversion ops this PR does not touch
(`f32x4.mul`/`f32x4.abs`/`f32x4.min`/`i32x4.trunc_sat_f32x4_s`/
`f32x4.convert_i32x4_u`, none implemented yet). Both of this PR's
target modules depend on nothing else unimplemented (each is just
`v128.load` plus the one new op), so both fully unblock. Aggregate
`assert_return` rose from 24155/24172 to 24157/24174 (+2 pass, +2
gradeable, exactly matching the predicted unblock count); `module`
pass count rose from 1155 to 1157 (+2, one per fully-unblocked
module) and its `NotYetSupported` count fell from 75 to 73.
`i8x16.replace_lane`/`extract_lane_u` are also new in this PR but
don't unblock anything in `simd_load.wast` -- no directive there
exercises either op; they're covered by dedicated unit tests in
`wasm-execution`/`wasm-wast-parser`/`wasm-validator` instead.

The vendored files are licensed under the Apache License, Version 2.0 --
see the accompanying LICENSE file, itself vendored verbatim from the same
pinned commit.
