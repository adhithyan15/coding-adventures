[package]
name = "os-kernel"
version = "0.1.0"
edition = "2021"
description = "Base real OS kernel crate that cross-compiles to UEFI targets"

[[bin]]
name = "os-kernel"
path = "src/main.rs"
test = false
bench = false

[dependencies]
uefi = { version = "0.37.0", features = ["panic_handler"] }
