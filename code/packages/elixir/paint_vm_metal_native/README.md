# paint_vm_metal_native

Elixir wrapper over the Rust `paint-metal` renderer.

This package takes a barcode-friendly `PaintScene`, executes it through Metal,
and returns a `CodingAdventures.PixelContainer`.
