## 0.294.0

- Allow bounded cross-assigned local scalar recurrence cycles to contain conditional expressions when every selector depends only on the exact loop control. Other conditionally selected cycles remain conservative.
