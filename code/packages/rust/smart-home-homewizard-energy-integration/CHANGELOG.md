# Changelog

## 0.1.0

- Add verified `_hwenergy._tcp.local` and manual HTTP discovery.
- Add bounded Energy API v1 device identity and measurement inspection.
- Normalize device and external-meter utility telemetry into D23 sensors.
- Add authorization-before-transport and two-request loopback HTTP coverage.
