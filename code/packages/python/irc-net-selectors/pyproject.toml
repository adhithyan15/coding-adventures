[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "coding-adventures-irc-net-selectors"
version = "0.1.0"
description = "Level 2 network implementation: single-threaded event loop using OS selectors"
requires-python = ">=3.12"
license = "MIT"
authors = [{ name = "Adhithya Rajasekaran" }]
readme = "README.md"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0", "pytest-cov>=5.0", "ruff>=0.4", "mypy>=1.10"]

[tool.hatch.build.targets.wheel]
packages = ["src/irc_net_selectors"]

[tool.ruff]
target-version = "py312"
line-length = 88

[tool.ruff.lint]
select = ["E", "W", "F", "I", "UP", "B", "SIM", "ANN"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--cov=irc_net_selectors --cov-report=term-missing --cov-fail-under=80"

[tool.coverage.run]
source = ["src/irc_net_selectors"]

[tool.coverage.report]
fail_under = 80
show_missing = true
