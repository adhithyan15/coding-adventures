{
  "$schema": "https://coding-adventures.example/schemas/required_capabilities/v1.json",
  "version": 1,
  "package": "dart/caesar-cipher-native",
  "capabilities": ["ffi:call"],
  "justification": "ffi:call — uses dart:ffi DynamicLibrary.open to call into the Rust caesar-cipher-native shared library (libcaesar_cipher_native.so/.dylib), which wraps the pure caesar-cipher crate over a C ABI. No network, filesystem, or process capabilities are used."
}
