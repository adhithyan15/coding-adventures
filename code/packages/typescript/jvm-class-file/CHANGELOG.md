# Changelog

## 0.1.0

- Add the first TypeScript `jvm-class-file` package with conservative parsing,
  minimal class-file building, and constant-pool resolution helpers.
- Remove accidentally committed transpiled JavaScript, declaration, and source
  map outputs so the package only tracks its TypeScript sources.
