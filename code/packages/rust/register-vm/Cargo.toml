[package]
name = "register-vm"
version = "0.1.0"
edition = "2021"
description = "Generic register-based virtual machine with accumulator model and feedback vectors — implements the V8 Ignition execution model"

[dependencies]
