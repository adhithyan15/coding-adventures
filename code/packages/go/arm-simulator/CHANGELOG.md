# Changelog

## [0.1.0] - Unreleased

### Added
- `ARMDecoder` capable of traversing ARM's immediate bit rotating syntax schemas.
- `ARMExecutor` for mapping CPU register behaviors (enforcing the 16 limit constraints).
- Implemented `ADD`, `MOV` and `SUB` operations specifically configured for Data Processing formatting variants.
- README explanations targeting design divergences against simpler ISAs like RISC-V.
