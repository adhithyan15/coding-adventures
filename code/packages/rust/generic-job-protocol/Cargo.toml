[package]
name = "generic-job-protocol"
version = "0.1.0"
edition = "2021"
description = "Generic cross-language job request and response protocol"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
