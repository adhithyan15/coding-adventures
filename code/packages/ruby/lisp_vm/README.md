# lisp-vm

GenericVM configured for McCarthy's Lisp — registers all Lisp opcodes and heap handlers

## Dependencies

- virtual-machine
- garbage-collector

## Development

```bash
# Run tests
bash BUILD
```
