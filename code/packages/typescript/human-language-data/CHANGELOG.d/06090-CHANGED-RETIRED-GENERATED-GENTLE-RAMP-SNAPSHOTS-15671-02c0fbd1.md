### Changed - retired generated gentle-ramp snapshots (#15671)

- Removed 851 exact-current-value metric and finding owners that duplicated the
  canonical curriculum and forced ordinary chapter work through language-wide
  generated files.
- Preserve the complete deterministic report and public loader API by deriving
  tracks on demand, while retaining hard invariants and authored budgets in
  their independent gates.
- Keep `check:gentle-snapshots` as a read-only derivation and resurrection guard;
  there is no generator or snapshot write capability.
