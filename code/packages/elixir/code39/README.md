# code39

  Dependency-free Code 39 encoder that emits backend-neutral paint scenes.

This package performs normalization, encoding, run expansion, and scene
generation, then leaves final output to a separate renderer package.
