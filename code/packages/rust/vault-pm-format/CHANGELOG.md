# Changelog

## 0.1.0

- Add strict canonical V1 codecs for bootstrap, device certificate, commit,
  and signed announcement values.
- Add the exact encrypted object-frame codec and domain-separated object and
  bootstrap IDs.
- Add signing preimages, defensive bounds, mutation tests, and shared golden
  vectors.
