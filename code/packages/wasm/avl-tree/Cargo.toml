[workspace]

[package]
name = "avl-tree-wasm"
version = "0.1.0"
edition = "2021"
description = "WebAssembly bindings for the Rust AVL tree crate via wasm-bindgen"
license = "MIT"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
avl-tree = { path = "../../rust/avl-tree" }
wasm-bindgen = "0.2"
