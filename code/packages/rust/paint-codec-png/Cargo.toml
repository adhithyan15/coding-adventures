[package]
name = "paint-codec-png"
version = "0.2.0"
edition = "2021"
description = "PNG image codec for paint-instructions: PixelContainer ↔ PNG bytes (full encode and decode)"

[dependencies]
paint-instructions = { path = "../paint-instructions" }
pixel-container = { path = "../pixel-container" }
png = { path = "../png" }
