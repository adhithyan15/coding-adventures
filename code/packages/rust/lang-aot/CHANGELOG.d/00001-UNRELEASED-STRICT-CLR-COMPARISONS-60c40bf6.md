## Unreleased — strict CLR comparisons and booleans (CLR08)

Execute all six strict scalar comparisons at both integer widths, signed and
wide intermediate values, exact inversion bytes and normalized bool metadata.
Cover false/true constants and bool move/call/return transport across forward
method references. Source routing remains unchanged.
