# Compiled Rust cdylib copied here by BUILD — rebuilt from source on every build.
*.node

# Node.js dependencies (regenerated by `npm ci`).
node_modules/

# TypeScript build output.
dist/

# Rust build artifacts.
ext/conduit_native_node/target/
