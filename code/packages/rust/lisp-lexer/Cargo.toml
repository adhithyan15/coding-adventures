[package]
name = "lisp-lexer"
version = "0.1.0"
edition = "2021"
description = "Tokenizes Lisp source code into a stream of tokens — parentheses, atoms (numbers, symbols, strings), quotes, and dots"

[dependencies]
