{
  "$schema": "https://raw.githubusercontent.com/adhithyan15/coding-adventures/main/code/specs/schemas/required_capabilities.schema.json",
  "version": 1,
  "package": "rust/llm-provider-ollama",
  "capabilities": ["network:loopback", "network:localnet"],
  "justification": "Connects to a local Ollama server over plain HTTP/1.1. Defaults to http://localhost:11434 (loopback); operators may override to a private-network host (e.g., http://my-ollama:11434). HTTPS is rejected at endpoint parse time. No public-internet egress, no auth tokens, no third-party HTTP crate."
}
