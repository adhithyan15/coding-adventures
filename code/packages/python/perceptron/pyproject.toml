[project]
name = "perceptron"
version = "0.1.0"
description = "Object-Oriented Single Layer Neural Node"
requires-python = ">=3.12"
dependencies = [
    "matrix", "loss-functions", "activation-functions"
]

[tool.uv.sources]
matrix = { path = "../matrix", editable = true }
loss-functions = { path = "../loss-functions", editable = true }
activation-functions = { path = "../activation-functions", editable = true }
