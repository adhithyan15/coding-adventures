[package]
name = "intel8051-simulator"
version = "0.1.0"
edition = "2021"
description = "Complete Intel 8051 functional simulator with full Harvard state, typed atomic lifecycle, exhaustive 256-opcode Python differentials, and consumer compatibility."

[lib]
name = "intel8051_simulator"
path = "src/lib.rs"

[dependencies]
