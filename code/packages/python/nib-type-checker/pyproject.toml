[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "coding-adventures-nib-type-checker"
version = "0.1.0"
description = "Type-checks Nib ASTs — enforces language-level invariants and annotates expression nodes with resolved NibTypes"
requires-python = ">=3.12"
license = "MIT"
authors = [{ name = "Adhithya Rajasekaran" }]
readme = "README.md"
keywords = ["nib", "intel4004", "type-checker", "compiler", "embedded", "education", "static-analysis"]
# Runtime dependency: coding-adventures-nib-parser (and its transitive deps)
# Declared via BUILD file's -e entries for local development and CI.
# When publishing to PyPI, restore full dependency list below.
dependencies = [
    "coding-adventures-directed-graph",
    "coding-adventures-grammar-tools",
    "coding-adventures-parser",
    "coding-adventures-lexer",
    "coding-adventures-nib-lexer",
    "coding-adventures-nib-parser",
    "coding-adventures-state-machine",
    "coding-adventures-type-checker-protocol",
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Education",
    "Topic :: Education",
    "Topic :: Scientific/Engineering",
    "Programming Language :: Python :: 3.12",
    "License :: OSI Approved :: MIT License",
    "Typing :: Typed",
]

[project.urls]
Homepage = "https://github.com/adhithyan15/coding-adventures"
Repository = "https://github.com/adhithyan15/coding-adventures"

[project.optional-dependencies]
dev = ["pytest>=8.0", "pytest-cov>=5.0", "ruff>=0.4", "mypy>=1.10"]

[tool.hatch.build.targets.wheel]
packages = ["src/nib_type_checker"]

[tool.ruff]
target-version = "py312"
line-length = 88

[tool.ruff.lint]
select = [
    "E",    # pycodestyle errors
    "W",    # pycodestyle warnings
    "F",    # pyflakes
    "I",    # isort (import sorting)
    "UP",   # pyupgrade (modern Python syntax)
    "B",    # bugbear (common bugs)
    "SIM",  # simplify
    "ANN",  # type annotation enforcement
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--cov=nib_type_checker --cov-report=term-missing --cov-fail-under=80"

[tool.coverage.run]
source = ["src/nib_type_checker"]

[tool.coverage.report]
fail_under = 80
show_missing = true
