[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "coding-adventures-brainfuck-wasm-compiler"
version = "0.1.0"
description = "End-to-end Brainfuck to WebAssembly compiler"
requires-python = ">=3.12"
license = "MIT"
authors = [{ name = "Adhithya Rajasekaran" }]
readme = "README.md"
dependencies = [
    "coding-adventures-brainfuck",
    "coding-adventures-brainfuck-ir-compiler",
    "coding-adventures-ir-optimizer",
    "coding-adventures-ir-to-wasm-validator",
    "coding-adventures-ir-to-wasm-assembly",
    "coding-adventures-wasm-assembler",
    "coding-adventures-wasm-validator",
]

[tool.uv.sources]
coding-adventures-brainfuck = { path = "../brainfuck", editable = true }
coding-adventures-brainfuck-ir-compiler = { path = "../brainfuck-ir-compiler", editable = true }
coding-adventures-ir-optimizer = { path = "../ir-optimizer", editable = true }
coding-adventures-ir-to-wasm-validator = { path = "../ir-to-wasm-validator", editable = true }
coding-adventures-ir-to-wasm-assembly = { path = "../ir-to-wasm-assembly", editable = true }
coding-adventures-wasm-assembler = { path = "../wasm-assembler", editable = true }
coding-adventures-wasm-validator = { path = "../wasm-validator", editable = true }
coding-adventures-wasm-runtime = { path = "../wasm-runtime", editable = true }

[project.optional-dependencies]
dev = [
    "coding-adventures-wasm-runtime",
    "pytest>=8.0",
    "pytest-cov>=5.0",
    "ruff>=0.4",
]

[tool.hatch.build.targets.wheel]
packages = ["src/brainfuck_wasm_compiler"]

[tool.ruff]
target-version = "py312"
line-length = 88

[tool.ruff.lint]
select = ["E", "W", "F", "I", "UP", "B", "SIM", "ANN"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--cov=brainfuck_wasm_compiler --cov-report=term-missing --cov-fail-under=80"

[tool.coverage.run]
source = ["src/brainfuck_wasm_compiler"]

[tool.coverage.report]
fail_under = 80
show_missing = true
