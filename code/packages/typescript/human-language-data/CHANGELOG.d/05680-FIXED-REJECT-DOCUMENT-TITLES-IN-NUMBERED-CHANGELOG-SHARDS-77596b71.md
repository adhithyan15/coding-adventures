### Fixed — reject document titles in numbered changelog shards

- Require strict numbered shards to start with their section heading so the standalone checker rejects copied document preambles. Preserve digest-locked legacy fragments.
