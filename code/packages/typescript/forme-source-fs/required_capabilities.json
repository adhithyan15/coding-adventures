{
  "$schema": "https://raw.githubusercontent.com/adhithyan15/coding-adventures/main/code/specs/schemas/required_capabilities.schema.json",
  "version": 1,
  "package": "typescript/forme-source-fs",
  "capabilities": ["storage:read", "storage:write"],
  "justification": "Source stages ARE the storage adapter: they read content via node:fs and atomically create a missing adjacent identity sidecar on first encounter, as required by FM01. Existing sidecars are never overwritten. The runtime path bypasses ctx.storage because source-fs is itself the filesystem storage adapter; this audited exception is documented in the source and README."
}
