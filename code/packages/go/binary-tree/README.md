# binary-tree

Generic binary tree utilities for Go.

This package mirrors the Rust `binary-tree` contract with level-order
construction, structural predicates, traversals, child lookup, array projection,
and compact ASCII rendering.
