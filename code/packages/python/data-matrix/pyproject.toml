[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "coding-adventures-data-matrix"
version = "0.1.0"
description = "Data Matrix ECC200 encoder — ISO/IEC 16022:2006 compliant"
requires-python = ">=3.12"
license = "MIT"
authors = [{ name = "Adhithya Rajasekaran" }]
readme = "README.md"
dependencies = [
    "coding-adventures-barcode-2d",
    "coding-adventures-gf256",
    "coding-adventures-paint-instructions",
]

[project.optional-dependencies]
dev = ["pytest>=8.0", "pytest-cov>=5.0", "ruff>=0.4", "mypy>=1.10"]

[tool.hatch.build.targets.wheel]
packages = ["src/coding_adventures"]

[tool.uv.sources]
coding-adventures-barcode-2d         = { path = "../barcode-2d", editable = true }
coding-adventures-gf256              = { path = "../gf256", editable = true }
coding-adventures-paint-instructions = { path = "../paint-instructions", editable = true }

[tool.ruff]
target-version = "py312"
line-length = 88

[tool.ruff.lint]
select = ["E", "W", "F", "I", "UP", "B", "SIM", "ANN"]
ignore = [
    "ANN401",
]

[tool.ruff.lint.per-file-ignores]
"tests/**" = ["ANN201", "ANN001", "ANN202"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--cov=coding_adventures.data_matrix --cov-report=term-missing --cov-fail-under=90"

[tool.coverage.run]
source = ["src/coding_adventures/data_matrix"]

[tool.coverage.report]
fail_under = 90
show_missing = true
