[package]
name = "cas-summation"
version = "2.12.0"
edition = "2021"
description = "Symbolic summation and product evaluation over symbolic IR"
license = "MIT"

[dependencies]
symbolic-ir = { path = "../symbolic-ir" }
