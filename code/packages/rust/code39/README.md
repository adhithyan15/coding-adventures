# code39

Dependency-free Code 39 encoder that emits backend-neutral draw scenes.

This crate performs normalization, encoding, run expansion, and scene
generation, then leaves final output to a separate renderer crate.
