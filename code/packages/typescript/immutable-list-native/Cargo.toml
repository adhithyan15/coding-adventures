[workspace]

[package]
name = "immutable-list-native-node"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
node-bridge = { path = "../../rust/node-bridge" }
immutable-list = { path = "../../rust/immutable-list" }
