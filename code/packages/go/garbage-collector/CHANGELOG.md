# Changelog

## [0.1.0] - Unreleased

### Added

- Added a Go mark-and-sweep garbage collector with heap object interfaces, recursive root scanning, and GC statistics.
- Added Lisp-oriented `ConsCell`, `Symbol`, `LispClosure`, and `SymbolTable` helpers for parity with the Python and Rust packages.
