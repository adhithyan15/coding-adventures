# Windows: Perl native extensions still need the Perl DLL import library and
# headers available locally. Windows CI runners here do not provide that full
# Perl development toolchain yet, so we skip the package there.
echo Skipping perl/gf256_native on Windows — Perl import library not available
