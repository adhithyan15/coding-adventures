[package]
name = "block-ram"
version = "0.1.0"
edition = "2021"
description = "Block RAM — SRAM cells, arrays, and configurable RAM modules for FPGA simulation"

[dependencies]
logic-gates = { path = "../logic-gates" }
