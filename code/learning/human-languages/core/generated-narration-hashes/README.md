# Language-owned narration hash ledgers

These files are generated by `human-language-data`'s `generate:narration`
command. Each language owns one JSON shard, keeping lesson and voice-script
changes independent across parallel language PRs.

Do not edit the JSON by hand. CI's `check:narration` gate regenerates every
shard in memory and compares it byte for byte.
