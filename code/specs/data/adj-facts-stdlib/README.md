# adj-facts-stdlib — the ADJ standard library of grounded facts (organized by subject)

A growing **standard library of recallable facts** that any ADJ program can `import` and
query. Together with [`adj-formula-stdlib`](../adj-formula-stdlib/) (grounded **formulas and
laws**) and the [medical recall domains](../mycin-2026/recall/), it forms the **ADJ standard
library**: the facts, formulas, and laws of the sciences — chemistry, physics, biology,
mathematics, and beyond — encoded once, provenanced, and reusable.

## Why a standard library of facts (and formulas, and laws)

The goal is that an AI agent working in a domain can **reason through this library** the way a
student reasons up from foundations — e.g. a medicine agent draws on chemistry, physics,
biology, and math the way a medical student builds on them from the start. Recall costs
**zero answer-time model calls**: the engine resolves a binding query against the grounded
rows and returns the answer **with its citation**, on the CPU. Every value is byte-provenanced
from a citable source (see [feedback: nothing human-authored]); nothing is asserted "from
memory," and the trust tier honestly reflects the source (`authoritative` for a primary/official
source — NIST, NASA, IUPAC, PubChem, a standards body; `consensus` for a secondary reference).

## Known overlapping tables (discovered 2026-08-11)

Because this library grows via many parallel, independently-scoped rotations, a handful of
tables ended up covering nearly the same ground under different names before anyone
cross-checked the whole tree. Discovered while scoping a new "science 13th slice" candidate:

- `earth-science/rock-types.adj` (`rock_formation(rock_type, forms_from)`, NPS source) and
  `geology/rock-type.adj` (`rock_type(rock, formation_process)`, USGS sources) both table the
  same three rock classes (igneous/sedimentary/metamorphic) and essentially the same "how it
  forms" fact, just with different atom labels and citations. Both are shipped; neither has
  been deprecated. Left as-is pending a maintainer decision on whether to merge/rename.
- `earth-science/water-cycle.adj` (`water_cycle_stage(stage, step_number)`, USGS, 5 rows) was
  discovered to already exist with the SAME predicate name as a candidate "water cycle stages"
  slice that was about to be implemented — that candidate was dropped before writing any code.
- `physics/simple-machines.adj` (`simple_machine_example(machine, example)`, NASA) was
  discovered to already cover the six simple machines' functions in its own header prose before
  a redundant `simple_machine(machine, description)` candidate (sourced from teachengineering.org)
  was implemented — that candidate was also dropped before writing any code.

**Lesson for future rotations**: before scoping a new table, grep the WHOLE
`adj-facts-stdlib/` tree for the candidate's predicate name and topic keywords (not just
`ls` the one subdirectory you plan to write to) — related content easily lands in a
different subject directory than you'd guess (e.g. a "cloud" or "rock" fact could be under
`meteorology/`, `geology/`, or `earth-science/`).

## Organized by subject, not by level

Files live under `code/specs/data/adj-facts-stdlib/<subject>/<name>.adj`. There is **no
grade/age categorization** — just subjects. Current subjects (grown one small, grounded library
per rotation, in parallel):

| subject | example library | source |
|---|---|---|
| `geometry/` | polygon → number of sides | Wolfram MathWorld |
| `geometry/` | triangle → the alternate word the same MathWorld source table also uses for it (`polygon_alt_name(shape, alt_name)`, triangle → trigon) — a sibling to `shapes.adj`, decoding the parenthetical half of a span already sitting unused in that table's own `source` field; honest abstention on the other seven polygons, whose own source-table rows name no alternate word | Wolfram MathWorld (authoritative; see `polygon-alt-name.adj`'s header — same source `shapes.adj` already cites) |
| `geometry/` | angle type → defining measure-condition (acute → between_0_and_90, right → equals_90, reflex → greater_than_180) | Mathematics LibreTexts (consensus) |
| `geometry/` | triangle (by sides) → defining side-condition (equilateral → three_equal_sides, isosceles → two_equal_sides, scalene → three_unequal_sides) | Wolfram MathWorld (authoritative) |
| `geometry/` | equilateral triangle → the alternate word the same MathWorld sentence also uses for it (`triangle_alt_name(triangle, alt_name)`, equilateral → regular) — a sibling to `triangle-types.adj`, decoding a clause already sitting unused in that table's equilateral row's own already-quoted sentence; honest abstention on isosceles and scalene, whose own cited spans name no alternate word | Wolfram MathWorld (authoritative; see `triangle-alt-name.adj`'s header — same source `triangle-types.adj` already cites) |
| `geometry/` | quadrilateral family → its one defining property (rectangle → four_right_angles, rhombus → all_sides_same_length, trapezoid → two_sides_parallel) | Wolfram MathWorld (authoritative) |
| `geometry/` | quadrilateral → a SECOND defining property the same MathWorld sentence states (`quadrilateral_secondary_property(shape, property)`, rectangle → opposite_sides_equal_length, parallelogram → opposite_angles_equal) — a sibling to `quadrilateral-types.adj`, decoding a second clause already sitting unused in that table's own provenance for two of its five quadrilaterals; honest abstention on square, rhombus, and trapezoid, whose own cited spans name only one property | Wolfram MathWorld (authoritative; see `quadrilateral-secondary-property.adj`'s header — same source `quadrilateral-types.adj` already cites) |
| `geometry/` | rhombus → the alternate word the same MathWorld sentence also uses for it (`quadrilateral_alt_name(shape, alt_name)`, rhombus → equilateral_parallelogram) — a sibling to `quadrilateral-types.adj`, decoding a clause already sitting unused in that table's rhombus row's own already-quoted sentence; honest abstention on square, rectangle, parallelogram, and trapezoid, whose own cited spans name no alternate word | Wolfram MathWorld (authoritative; see `quadrilateral-alt-name.adj`'s header — same source `quadrilateral-types.adj` already cites) |
| `geometry/` | circle part → its defining description (radius → center_to_perimeter, diameter → maximum_distance_across, chord → ends_on_circle) | Wolfram MathWorld (authoritative) |
| `geometry/` | shape → what "radius" measures for it (`radius_definition(shape, description)`, circle → center_to_perimeter, sphere → center_to_surface) — a sibling to `circle-parts.adj`, decoding the second clause of a sentence that table's own `source` field already quotes in full; honest abstention on any solid the cited sentence does not name | Wolfram MathWorld (authoritative; see `radius-definition.adj`'s header — same source `circle-parts.adj` already cites) |
| `geometry/` | angle-pair relationship → its defining condition (complementary → sum_to_90, supplementary → sum_to_180, vertical → equal, adjacent → share_side_and_vertex) | OpenStax / Mathematics LibreTexts (consensus) |
| `geometry/` | **DERIVED, not looked up** — how many triangles a named quadrilateral decomposes into (`triangle_decomposition_count(shape, 2)`), by a `rule` combining the `quadrilateral_property` table with the general definitions of triangulation and a polygon diagonal — no single source states the composite fact directly | Wolfram MathWorld (consensus; see `shape-composition.adj`'s header for why this earns `consensus` rather than `authoritative`) |
| `astronomy/` | planet → order from the Sun | NASA |
| `astronomy/` | stellar spectral class letter → the color NASA assigns it (o → blue, g → yellow, m → red) | NASA Science (authoritative) |
| `astronomy/` | stellar spectral class letter → its rank in the hottest-to-coolest sequence, per the same NASA sentence (`spectral_class_order(spectral_class, order)`, o → 1, b → 2, a → 3, f → 4, g → 5, k → 6, m → 7) — a sibling to `spectral-classes.adj`, decoding the order half of an already-cited quote; full 7/7 coverage, no abstention needed | NASA Science (authoritative; see `spectral-class-order.adj`'s header — same source `spectral-classes.adj` already cites) |
| `astronomy/` | galaxy type → defining shape NASA states (spiral → spiral_arms, elliptical → round_to_oval, irregular → unusual_shapes) | NASA Science (authoritative) |
| `astronomy/` | celestial-object type → defining property (star → gives_off_light, planet → revolves_around_star, moon → orbits_planet, comet → frozen_gases_and_dust, asteroid → rocky) | NASA StarChild / NASA Science (authoritative) |
| `astronomy/` | celestial-object type → an alternate name NASA gives it (`celestial_object_alt_name(object, alt_name)`, moon → planetary_satellites, asteroid → minor_planets) — a sibling to `celestial-objects.adj`, decoding spans already sitting unused in that table's own provenance block | NASA Science (authoritative; see `celestial-object-alt-name.adj`'s header) |
| `astronomy/` | solar eclipse type → what it actually is (`solar_eclipse_type(type, description)`, total_solar_eclipse/annular_solar_eclipse/partial_solar_eclipse → completely_blocking_the_face_of_the_sun/moon_at_or_near_its_farthest_point_from_earth/sun_moon_and_earth_not_perfectly_lined_up) — picked via the mandatory full-tree-grep-before-scoping check, confirming zero prior coverage; honest abstention on `hybrid_solar_eclipse` (a real type the source covers, but whose own explanation takes two sentences rather than one clean sentence, and not one of these three tabled here) | NASA "Types of Solar Eclipses" (authoritative; see `solar-eclipse-type.adj`'s header) |
| `astronomy/` | comet part → what it actually is (`comet_part(part, description)`, nucleus/coma/tail → solid_frozen_core_at_the_heart_of_the_comet/fuzzy_cloud_of_gas_and_dust_around_the_nucleus/streams_away_from_the_nucleus_pushed_by_sunlight_and_solar_particles) — picked via the mandatory full-tree-grep-before-scoping check, confirming zero prior coverage; honest abstention on `short_period_comet` (a real comet-related term the source covers, but one that classifies by orbital period rather than physical anatomy, and not one of these three tabled here) | NASA Space Place "What Is a Comet?" (authoritative; see `comet-part.adj`'s header) |
| `astronomy/` | which of a comet's two separate tails traces a named path (`comet_tail_type(tail, description)`, dust_tail/ion_tail → traces_a_broad_gently_curving_path_away_from_the_sun/always_points_directly_away_from_the_sun) — a sibling library to `comet-part.adj`, using the SAME already-cited page's own note that the single `tail` part further sub-divides into two; honest abstention on `coma`/`nucleus` (real comet parts, already tabled in the coarser sibling `comet-part.adj`, not tail sub-types) | NASA Space Place "What Is a Comet?" (authoritative; see `comet-tail-type.adj`'s header) |
| `astronomy/` | which stage a single rocky object is in, not three different kinds of object (`space_rock_stage(stage, description)`, meteoroid/meteor/meteorite → still_a_rock_in_space/called_a_fireball_or_shooting_star_when_it_burns_up_in_the_atmosphere/survives_the_atmosphere_and_hits_the_ground) — picked via the mandatory full-tree-grep-before-scoping check, confirming zero prior coverage; honest abstention on `asteroid` (a real object the source mentions in passing, but never defines in a sentence of its own on this page) | NASA Science "Meteors & Meteorites" (authoritative; see `space-rock-stage.adj`'s header) |
| `astronomy/` | the two everyday alternate names NASA gives a meteor, per the same sentence (`space_rock_alt_name(stage, alt_name)`, meteor → fireball, meteor → shooting_star) — a sibling to `space-rock-stage.adj`, decoding the two synonym terms a single already-cited compound clause names; honest abstention on meteoroid/meteorite (their cited spans name only the one primary term each) | NASA Science "Meteors & Meteorites" (authoritative; see `space-rock-alt-name.adj`'s header — same source `space-rock-stage.adj` already cites) |
| `language/` | preposition category → what it actually shows (`preposition_type(type, description)`, preposition_of_place/preposition_of_time/preposition_of_direction → shows_where_something_is_or_where_something_happened/shows_when_something_happened_or_will_happen/shows_how_something_is_moving_or_which_way_its_going) — picked via the mandatory full-tree-grep-before-scoping check, confirming zero prior coverage; honest abstention on `preposition_of_manner_cause_or_purpose` (a real category the source covers, but one that bundles three distinct functions rather than one clean single-concept category like the three tabled here) | Grammarly "Prepositions: Definition, Types, and Examples" (consensus; see `preposition-type.adj`'s header) |
| `language/` | conjunction category → what it actually does (`conjunction_type(type, description)`, coordinating_conjunction/correlative_conjunction/subordinating_conjunction → joins_words_phrases_and_clauses_of_equal_grammatical_rank/are_pairs_of_conjunctions_that_work_together/joins_dependent_clauses_to_independent_clauses) — picked via the mandatory full-tree-grep-before-scoping check, confirming zero prior coverage; honest abstention on `conjunctive_adverb` (a real category the source covers, but one that belongs to a different word class -- an adverb, not a conjunction -- rather than being a fourth conjunction type) | Grammarly "Conjunctions" (consensus; see `conjunction-type.adj`'s header) |
| `language/` | subordinating conjunction → each relationship kind it can signal between clauses (`subordinating_conjunction_relationship_type(relationship_type, conjunction_type)`, cause_and_effect/comparison/contrast/time → all subordinating_conjunction) — a sibling to `conjunction-type.adj`, decoding the four relationship kinds already sitting unused in that table's own already-quoted subordinating-conjunction sentence, excluding the sentence's own vague "or some other kind of relationship" trailing clause; honest abstention on `concession` (a real relationship subordinating conjunctions can express in general grammar, but not one this specific quoted sentence names) and on `coordinating_conjunction` (a real, already-tabled conjunction category, but not the one this table covers) | Grammarly "Conjunctions" (consensus; see `subordinating-conjunction-relationship-type.adj`'s header) |
| `astronomy/` | the three IAU requirements a body must meet to count as a full planet, not a dwarf planet (`planet_criterion(criterion, requirement)`, orbit/roundness/cleared_orbit → orbits_its_host_star/is_mostly_round/gravity_cleared_away_other_objects_of_similar_size_near_its_orbit) — picked via the mandatory full-tree-grep-before-scoping check, confirming zero prior coverage; honest abstention on `dwarf_planet` (a real classification the source covers, but one defined as satisfying the first two criteria while failing the third -- a compound category built from these criteria, not a fourth criterion itself) | NASA Science "Dwarf Planets" (authoritative; see `planet-criterion.adj`'s header) |
| `astronomy/` | for each of the three IAU planet criteria, whether a dwarf planet like Pluto meets it, per the same NASA sentence (`dwarf_planet_criterion_status(criterion, status)`, orbit → met, roundness → met, cleared_orbit → not_met) — a sibling to `planet-criterion.adj`, decoding the per-criterion status half of a sentence that table already quotes only to justify excluding `dwarf_planet` as a row; full 3/3 coverage, no abstention needed | NASA Science "Dwarf Planets" (authoritative; see `dwarf-planet-criterion-status.adj`'s header — same source `planet-criterion.adj` already cites) |
| `language/` | determiner category → what it actually does (`determiner_type(type, description)`, article/demonstrative_determiner/distributive_determiner → precedes_a_noun_and_identifies_it_as_specific_or_nonspecific/communicates_the_placement_of_a_noun_in_space_or_time/refers_to_a_group_or_individual_parts_within_a_group) — picked via the mandatory full-tree-grep-before-scoping check, confirming zero prior coverage; honest abstention on `possessive_determiner` (a real category the source covers, but one whose own defining sentence bundles two separate facts plus a full inline list of examples, rather than one clean single-fact sentence like the three tabled here) | Grammarly "What Are Determiners?" (consensus; see `determiner-type.adj`'s header) |
| `language/` | demonstrative determiner → its alternate name (`determiner_type_alias(type, alias)`, demonstrative_determiner → demonstrative_adjective) — a sibling to `determiner-type.adj`, decoding the alias clause already sitting unused in that table's own already-quoted demonstrative-determiner sentence; honest abstention on `article` (a real, already-tabled determiner type, but its own sentence states no alias) | Grammarly "What Are Determiners?" (consensus; see `determiner-type-alias.adj`'s header) |
| `geology/` | volcano type → what it actually is (`volcano_type(type, description)`, cinder_cone/shield_volcano/composite_volcano → is_the_simplest_type_of_volcano/built_almost_entirely_of_fluid_lava_flows/also_called_a_stratovolcano) — picked via the mandatory full-tree-grep-before-scoping check, confirming zero prior coverage; honest abstention on `lava_dome` (a real term the source names, but one the source itself explicitly disclaims as technically not a "volcano type" but rather an eruption phenomenon) | USGS "About Volcanoes" (authoritative; see `volcano-type.adj`'s header) |
| `language/` | end-of-sentence punctuation mark → what it actually does (`end_punctuation_mark(mark, description)`, period/question_mark/exclamation_point → ends_a_declarative_sentence/communicates_that_a_sentence_is_a_question/makes_sentences_exciting) — picked via the mandatory full-tree-grep-before-scoping check, confirming zero prior coverage; honest abstention on `comma` (a real mark the source covers, but one that belongs to a different category — a mid-sentence pause mark, not an end-of-sentence mark like the three tabled here) | Grammarly "Punctuation: The Best Guide to Using Punctuation Marks" (consensus; see `end-punctuation-mark.adj`'s header) |
| `geography/` | map type → what it actually shows (`map_type(type, description)`, political/physical/topographic → shows_boundaries_between_countries_states_counties_and_other_political_units/shows_the_natural_landscape_features_of_earth/shows_the_shape_of_earths_surface) — picked via the mandatory full-tree-grep-before-scoping check, confirming zero prior coverage; honest abstention on `weather` (a real map category the source covers, but one whose own section never states a single complete defining sentence the way the three tabled here do) | Geology.com "Types of Maps" (consensus; see `map-type.adj`'s header) |
| `language/` | figure of speech → what it actually does (`figurative_language_type(type, description)`, metaphor/personification/hyperbole → describes_something_in_a_way_thats_not_literally_true_to_make_a_comparison/gives_human_characteristics_to_nonhuman_or_abstract_things/a_great_exaggeration_used_to_add_emphasis) — picked via the mandatory full-tree-grep-before-scoping check, confirming zero prior coverage; honest abstention on `allusion` (a real device the source names with its own clean sentence, but one that works by referencing an external work, person, or event rather than by comparison, exaggeration, or personification — a different mechanism than the three tabled here); simile and idiom deliberately excluded too, already separately shipped as `simile-meaning.adj`/`idiom-meaning.adj` | Grammarly "Figurative Language Examples: 6 Common Types and Definitions" (consensus; see `figurative-language-type.adj`'s header) |
| `biology/` | biome → what actually defines it (`biome_type(biome, description)`, desert/forest/grassland/tundra → dry_areas_where_rainfall_is_less_than_50_centimeters_20_inches_per_year/dominated_by_trees_and_cover_about_one_third_of_the_earth/open_regions_dominated_by_grass_with_a_warm_dry_climate/has_extremely_inhospitable_conditions_with_the_lowest_measured_temperatures) — picked via the mandatory full-tree-grep-before-scoping check, confirming zero prior coverage (and that `animal-habitat.adj` only maps individual animals to habitat names, never tables biome-level defining sentences); honest abstention on `aquatic` (the source's fifth major biome, but one whose own section opens by deferring to its freshwater and marine sub-categories rather than stating a single, complete defining sentence the way the four tabled here do) | National Geographic Education "The Five Major Types of Biomes" (consensus; see `biome-type.adj`'s header) |
| `language/` | sound device → what it actually does (`sound_device_type(device, description)`, onomatopoeia/alliteration → is_when_a_word_imitates_the_natural_sound_of_a_thing/repeating_consonant_sounds_right_next_to_each_other) — picked via the mandatory full-tree-grep-before-scoping check, confirming zero prior coverage; deliberately only two rows, since only two of the source page's twenty figures of speech carry a genuinely standalone defining sentence — honest abstention applies to table size, not just individual queries; honest abstention on `simile` (a real figure of speech the same page also covers, but already separately shipped as `simile-meaning.adj`) | Grammarly "20 Types of Figures of Speech: Definitions and Examples" (consensus; see `sound-device-type.adj`'s header) |
| `environment/` | ecosystem factor type → what actually defines it (`ecosystem_factor_type(factor, description)`, biotic/abiotic → a_living_organism_that_shapes_its_environment/a_non_living_part_of_an_ecosystem_that_shapes_its_environment) — picked via the mandatory full-tree-grep-before-scoping check, confirming zero prior coverage; biotic and abiotic together exhaust the two-way classification, so there's no third excluded factor type — honest abstention on `producer` instead (a real ecology term, but one naming a food-chain role, not a factor type; see `food-chain-roles.adj`) | National Geographic Education "Biotic Factors" / "Abiotic Factors" (consensus; see `ecosystem-factor-type.adj`'s header) |
| `language/` | clause type → what makes it independent or dependent (`clause_type(type, description)`, independent_clause/dependent_clause → is_a_clause_that_alone_is_a_complete_sentence/is_a_clause_that_alone_is_not_a_complete_sentence) — picked via the mandatory full-tree-grep-before-scoping check, confirming zero prior coverage; independent and dependent together exhaust the two-way classification the source itself states ("every clause is either one or the other"), so there's no third excluded clause type — honest abstention on `noun_clause` instead (a real, well-documented clause category, but one naming a functional role a dependent clause can play, not a third structural type) | Grammarly "Independent and Dependent Clauses: Rules and Examples" (consensus; see `clause-type.adj`'s header) |
| `geology/` | fossil preservation structure → what it actually is (`fossil_preservation_type(type, description)`, mold/cast/trace_fossil → three_dimensional_impression_of_all_or_part_of_a_body_fossil_or_trace_fossil/replica_of_an_organism_or_a_trace_produced_by_the_infilling_of_a_natural_mold/consists_of_the_evidence_of_living_organisms_but_not_the_actual_organism_itself) — picked via the mandatory full-tree-grep-before-scoping check, confirming zero prior coverage; distinct from the sibling `fossil-formation-type.adj`, which names formation mechanisms from a different source and bundles mold+cast into one coarse row, while this table refines that pairing with a more detailed primary source; honest abstention on `steinkern` (a real term with its own clean definition on the same page, but one the source itself frames as a specific kind of cast, not a fourth preservation type) | National Park Service "Mold Casts and Steinkerns" (authoritative; see `fossil-preservation-type.adj`'s header) |
| `geology/` | which peer preservation structure a named sub-category belongs to (`fossil_preservation_subtype(subtype, parent_type, description)`, steinkern → cast) — a sibling to `fossil-preservation-type.adj`, decoding the `steinkern` definition already quoted in full in that table's own header but structurally excluded there as a classificatory/scope decision, not a correctness one; honest abstention on a peer type (`mold`) that is not itself a sub-category | National Park Service "Mold Casts and Steinkerns" (authoritative; see `fossil-preservation-subtype.adj`'s header) |
| `language/` | narrative point of view → what it actually is (`point_of_view(type, description)`, first_person/second_person/third_person → the_reader_accesses_the_story_through_one_person/uses_the_pronoun_you/the_narrator_has_the_ability_to_know_everything) — picked via the mandatory full-tree-grep-before-scoping check, confirming zero prior coverage; a literary device distinct from the sibling `pronoun-type.adj`, which tables grammatical pronoun categories rather than narrative perspective; honest abstention on `third_person_omniscient` (a real, well-documented term the same source defines with its own clean sentence, but one it frames as a subtype of third person, not a fourth peer point of view) | Grammarly "What Is Point of View in Writing, and How Does It Work?" (consensus; see `point-of-view.adj`'s header) |
| `astronomy/` | lunar eclipse type → what it actually is (`lunar_eclipse_type(type, description)`, total_lunar_eclipse/partial_lunar_eclipse/penumbral_eclipse → the_moon_moves_into_the_inner_part_of_earths_shadow_the_umbra/an_imperfect_alignment_of_sun_earth_and_moon_results_in_partial_umbra_passage/the_moon_travels_through_earths_penumbra_the_faint_outer_part_of_its_shadow) — picked via the mandatory full-tree-grep-before-scoping check, confirming zero prior coverage; honest abstention on `blood_moon` (a real term the same page discusses, but as a nickname for the color a total lunar eclipse produces, not a fourth peer eclipse type) | NASA "Eclipses and the Moon" (authoritative; see `lunar-eclipse-type.adj`'s header) |
| `language/` | comma rule → what it actually says to do (`comma_rule(rule, description)`, comma_in_a_series/comma_before_but/comma_with_direct_address → use_commas_to_separate_elements_in_a_list_of_more_than_two_elements/use_a_comma_before_but_when_it_is_joining_two_independent_clauses/set_off_the_name_with_commas_when_addressing_another_person_by_name) — picked via the mandatory full-tree-grep-before-scoping check, confirming zero prior coverage; the natural complement to `end-punctuation-mark.adj`, which already named comma as a mid-sentence mark it deliberately excludes; honest abstention on `oxford_comma` (a real, well-known term the same page discusses, but one whose own rule sentence bundles the placement rule with an optionality caveat rather than one clean fact) | Grammarly "Rules for Using Commas, With Examples" (consensus; see `comma-rule.adj`'s header) |
| `astronomy/` | layer of the Sun → what it actually is (`sun_layer(layer, description)`, photosphere/corona → the_visible_surface_of_the_sun/the_suns_outer_atmosphere) — picked via the mandatory full-tree-grep-before-scoping check, confirming zero prior coverage; deliberately only two rows, since only these two of the page's six named layers state one clean fact in their own sentence — honest abstention applies to table size, not just individual queries; honest abstention on `chromosphere` (a real solar layer the same page names, but one whose sentence bundles its position with a temperature range rather than one clean fact) | NASA "Layers of the Sun" (authoritative; see `sun-layer.adj`'s header) |
| `language/` | adverb type → what it actually describes (`adverb_type(type, description)`, manner/place/frequency/duration → describes_how_an_action_is_performed/describes_where_an_action_happens/describes_how_often_an_action_occurs/describes_how_long_an_action_lasts) — picked via the mandatory full-tree-grep-before-scoping check, confirming zero prior coverage; closes out adverbs as the last major part-of-speech family this stdlib had not yet named on its own; honest abstention on `time` (a real adverb type the same source table names, but one whose own defining sentence bundles when/how-long/how-often into a single sentence rather than one clean fact) | Grammarly "What Is an Adverb? Definition and Examples" (consensus; see `adverb-type.adj`'s header) |
| `earth-science/` | soil particle-size separate → the diameter range that actually defines it (`soil_texture_class(class, description)`, clay/silt/sand → less_than_two_thousandths_of_a_millimeter_in_diameter/between_two_thousandths_and_five_hundredths_of_a_millimeter/larger_than_five_hundredths_of_a_millimeter_in_diameter) — picked via the mandatory full-tree-grep-before-scoping check, confirming zero prior coverage; distinct from the sibling `soil-horizons.adj`, which tables a completely different axis (vertical layers a soil pit exposes, not the particle-size classes making up any one layer); honest abstention on `loam` (a real, common soil-texture term, but a composite mix of sand/silt/clay rather than one of the three particle-size separates itself) | Wikipedia "Soil texture" (consensus; see `soil-texture-class.adj`'s header) |
| `chemistry/` | element → atomic number | PubChem / NIH |
| `chemistry/` | common substance → approximate pH | LibreTexts (consensus) |
| `chemistry/` | element → periodic-table group family (`element_group_family(element, family)`, 28 elements across alkali_metal/alkaline_earth_metal/halogen/noble_gas/transition_metal, extended from the original 14 by pulling the remaining members each family's own already-cited Wikipedia sentence had always named) | Wikipedia (consensus) |
| `chemistry/` | common chemical → acid or base (`acid_or_base(substance, classification)`, 16 substances, extended from the original 12 by pulling the remaining 4 — chloric_acid, rubidium_hydroxide, caesium_hydroxide, strontium_hydroxide — that the table's own header had always named as grounded in the same already-cited source table but omitted only to keep the original set balanced) | LibreTexts (consensus) |
| `chemistry/` | subatomic particle → electric charge (proton → positive) | DOE "Explains…Nuclei" (authoritative) |
| `chemistry/` | chemical bond type → defining token (ionic → transfer) | LibreTexts (consensus) |
| `chemistry/` | chemical bond type → which family it belongs to (`bond_family(bond, family)`, ionic/covalent/metallic → primary, van_der_waals → secondary) — a sibling to the bond→token table above, discovered via a header-revisit of that table's own already-cited LibreTexts sentence | LibreTexts (consensus; see `chemical-bond-family.adj`'s header) |
| `chemistry/` | mixture kind → the everyday example the source names (colloid → milk) | LibreTexts (consensus) |
| `chemistry/` | lab equipment → its use (beaker → hold, bunsen_burner → heat) | LibreTexts (consensus) |
| `chemistry/` | measuring tool → the quantity it measures (`measuring_tool(tool, quantity)`, ruler → length, graduated_cylinder → volume, balance → mass, thermometer → temperature) — the "observation and measurement" gap, distinct from the sibling tool→use table above | Chemistry LibreTexts "Introducing Measurements in the Laboratory" (consensus) |
| `chemistry/` | Types of chemical reaction → the defining token each is described by (combination → two_or_more_combine, decomposition → breaks_down, combustion → reacts_with_oxygen). | Chemistry LibreTexts (consensus) |
| `chemistry/` | metal → flame-test color it gives (sodium → orange, potassium → violet, lithium → red) | University of Washington Department of Chemistry (authoritative) |
| `chemistry/` | mixture-separation method → property it separates by (filtration → by_particle_size, distillation → by_volatility, chromatography → by_different_rates) | Chemistry LibreTexts (consensus) |
| `chemistry/` | named gas law → the pair of quantities it relates (boyle → pressure_volume, charles → volume_temperature, avogadro → volume_moles) | Chemistry LibreTexts CK-12 (consensus) |
| `chemistry/` | element category → defining electrical property (metal → good_conductor, nonmetal → poor_conductor, metalloid → semiconductor) | Chemistry LibreTexts (consensus) |
| `metrology/` | SI prefix → power of ten | NIST |
| `metrology/` | time unit → the smaller unit it is composed of and the count, the SAME NIST span also states (`time_unit_composition(unit, sub_unit, count)`, hour → (minute, 60), day → (hour, 24)) — a sibling to `time-units.adj`, decoding a second fact already sitting unused in that table's own `source` field for two of its three units; honest abstention on minute, whose own cited span states only its seconds-length, no unit-to-unit relation | NIST Special Publication 811 (authoritative; see `time-unit-composition.adj`'s header — same source `time-units.adj` already cites) |
| `mathematics/` | Roman numeral → value | (consensus) |
| `calendar/` | day / month → number | ISO 8601 |
| `money/` | US coin → cents | US Mint |
| `money/` | US coin → the discontinuation status, year, and production span the SAME U.S. Mint span also states (`coin_status(coin, status, year, production_years)`, penny → (discontinued, 2025, 232)) — a sibling to `us-coins.adj`, decoding facts already sitting unused in that table's own header quote; deliberately a single row, since no other coin's own cited span states any circulation-status change | US Mint (authoritative; see `coin-penny-discontinued.adj`'s header — same source `us-coins.adj` already cites) |
| `money/` | US paper bill → who is on the front (`bill_portrait(dollars, portrait)`, 1 → washington, 20 → jackson, 100 → franklin) | U.S. Currency Education Program |
| `money/` | US paper bill → what's on the back, the SAME USCEP feature-sheet sentence also states (`bill_back_vignette(dollars, vignette)`, 1 → great_seal, 20 → white_house, 100 → independence_hall) — a sibling to `us-bills.adj`, decoding the back-vignette half of a quote already sitting unused in that table's own header; honest abstention on the $5 note, whose own cited span names no back vignette | U.S. Currency Education Program (authoritative; see `bill-back-vignette.adj`'s header — same source `us-bills.adj` already cites) |
| `earth-science/` | water-cycle stage → step number | USGS Water Science School |
| `earth-science/` | master soil horizon → what it is (o → organic_matter, c → parent_material, r → bedrock) | UNL passel Plant & Soil Sciences eLibrary (authoritative) |
| `earth-science/` | tectonic plate-boundary type → how plates move (divergent → rip_apart, convergent → subducts, transform → slide_past) | U.S. National Park Service (authoritative) |
| `earth-science/` | atmosphere layer → distinctive feature (troposphere → weather, stratosphere → ozone_layer, thermosphere → auroras) | NASA "Earth's Atmosphere" (authoritative) |
| `earth-science/` | cause of rock metamorphism → its shared effect (`metamorphism_cause(cause, effect)`, heat/pressure/hot_mineral_rich_fluids → denser_more_compact_rock) — a sibling library to `rock-types.adj`, but a finer-grained causal axis, confirmed distinct via the mandatory full-tree-grep-before-scoping check | USGS "What are metamorphic rocks?" (authoritative; see `metamorphism-cause.adj`'s header) |
| `nutrition/` | common food → MyPlate food group | USDA MyPlate |
| `nutrition/` | vegetable → the USDA MyPlate subgroup the SAME source span also states (`vegetable_subgroup(vegetable, subgroup)`, broccoli → dark_green, carrots → red_and_orange, corn → starchy) — a sibling to `food-groups.adj`, decoding a finer classification already sitting unused in that table's own header quote; covers the full five-vegetable domain with no abstention | USDA MyPlate (authoritative; see `vegetable-subgroup.adj`'s header — same source `food-groups.adj` already cites) |
| `nutrition/` | food group → the non-solid-food alternative form the SAME group definition also states (`food_group_alternative_form(food_group, alternative_form)`, fruits → hundred_percent_fruit_juice, dairy → lactose_free_milk / fortified_soy_milk / fortified_soy_yogurt) — a sibling to `food-groups.adj`, decoding an alternative form already sitting unused in three of that table's own per-group definitional sentences; honest abstention on grains and protein, whose own cited sentences name no comparable alternative form | USDA MyPlate (authoritative; see `food-group-alternative-form.adj`'s header — same source `food-groups.adj` already cites) |
| `agriculture/` | farm animal → product it gives | Iowa State University (CFSPH) |
| `agriculture/` | farm animal → the second product the SAME CFSPH span also states (`farm_animal_secondary_product(animal, product)`, chicken → meat, duck → meat, sheep → meat, sheep → milk) — a sibling to `farm-animals.adj`, decoding a second product already sitting unused in each row's own already-quoted provenance span; honest abstention on rabbit and goat, whose own cited spans state only a use or processing note for their already-recorded product, not a genuinely different second product | Iowa State University CFSPH (authoritative; see `farm-animal-secondary-product.adj`'s header — same source `farm-animals.adj` already cites) |
| `biology/` | common bone → body region | NIH / MedlinePlus |
| `biology/` | macronutrient → energy (kcal) per gram | NIH / MedlinePlus |
| `biology/` | basic tissue type → representative example | NCI SEER Training |
| `biology/` | kingdom of life → representative example organism (fungi → mushrooms) | Science Notes (consensus) |
| `biology/` | blood-vessel type → defining function (artery → away from heart) | NCI SEER Training |
| `biology/` | hormone → endocrine gland that secretes it | NCI SEER Training / NIH MedlinePlus |
| `biology/` | vitamin → deficiency disease it prevents | NIH Office of Dietary Supplements |
| `biology/` | vitamin → the symptom its deficiency causes (`vitamin_deficiency_symptom(vitamin, symptom)`, vitamin_d → soft_weak_deformed_painful_bones, vitamin_b12 → tired_and_weak) — a sibling to `vitamins.adj`, decoding the symptom half of five of the seven already-cited NIH ODS sentences that its disease-only schema had no room for; honestly narrower than its parent — abstains on vitamin_c and vitamin_b3, whose cited spans name the disease but state no symptom | NIH Office of Dietary Supplements (authoritative; see `vitamin-deficiency-symptom.adj`'s header) |
| `biology/` | amino acid → its three-letter code (`amino_acid_three_letter_code(amino_acid, code)`, glycine → gly) — a sibling to `amino-acids.adj`, decoding the other column of the same already-cited DDBJ page | DDBJ "Codes" (authoritative; see `amino-acid-three-letter-code.adj`'s header) |
| `biology/` | mRNA codon → whether it can initiate translation (`start_codon(codon, role)`, ttg/ctg/atg → start) — a sibling to `genetic-code.adj`, decoding the same already-quoted NCBI `Starts` line that table's own schema had no room for | NCBI "Genetic Codes" (authoritative; see `start-codon.adj`'s header) |
| `biology/` | leaf part → defining token / function (blade → flattened, stomata → gas_exchange) | Colorado State University Extension (authoritative) |
| `biology/` | diet category → food it eats (herbivore → plants, carnivore → animals, omnivore → anything) | U.S. National Park Service (authoritative) |
| `biology/` | flower part → function / role (petal → attract_pollinators, stamen → male, ovary → contains_ovules) | University of Illinois Extension (authoritative) |
| `biology/` | seed part → the role/function the source states (seed_coat → covering, cotyledon → food_storage, embryo → miniature_plant) | USDA Forest Service, Woody Plant Seed Manual, Ch.1 (authoritative) |
| `biology/` | fungus part → defining token / role (hypha → thread_like, mycelium → made_of_hyphae, gills → holds_spores) | UNLV "The Kingdom Fungi" biology faculty page (consensus) |
| `biology/` | vertebrate class → its one distinctive characteristic (bird → feathers, mammal → hair, reptile → dry_scaly_skin, fish → gills) | NPS "Vertebrate Grab Bag" (authoritative) |
| `biology/` | vertebrate class → whether it is ectothermic or endothermic (`vertebrate_thermoregulation(class, type)`, fish/amphibian/reptile → ectothermic, bird/mammal → endothermic) — a sibling to `vertebrate-groups.adj`, decoding a second cleanly binary fact already sitting unused inside that table's own per-class header quotes, alongside the body-covering feature it already captures; covers the full five-class domain with no abstention | NPS "Vertebrate Grab Bag" (authoritative; see `vertebrate-thermoregulation.adj`'s header) |
| `biology/` | muscle-tissue type → its one distinctive characteristic (skeletal → voluntary, smooth → involuntary, cardiac → intercalated_disks) | NCI SEER Training Modules (authoritative) |
| `biology/` | muscle-tissue type → whether it is striated (`muscle_striated(muscle, striated)`, skeletal/cardiac → yes, smooth → no) — a sibling to `muscle-types.adj`, decoding a second cleanly binary fact already sitting unused inside that table's own per-muscle header quotes, alongside the trait it already captures; covers the full three-type domain with no abstention | NCI SEER Training Modules (authoritative; see `muscle-striation.adj`'s header) |
| `biology/` | skeletal/cardiac muscle → its nuclei count (`muscle_nuclei_count(muscle, nuclei)`, skeletal → multinucleated, cardiac → single_nucleus) — a sibling to `tissue-types.adj`, decoding a fact already sitting unused inside that table's own muscle-row header quotes; a DIFFERENT leftover fact from the one `muscle-striation.adj` decoded off the different parent `muscle-types.adj` (both cite the same underlying SEER muscle page, but capture independent facts); honest abstention on smooth muscle, not part of this cited span | NCI SEER Training Modules (authoritative; see `muscle-nuclei-count.adj`'s header) |
| `biology/` | pancreatic hormone → the blood-glucose condition that triggers its release (`secretion_trigger(hormone, blood_glucose_level)`, insulin → high, glucagon → low) — a sibling to `hormone-glands.adj`, decoding a second fact already sitting unused inside two of that table's own per-hormone header quotes, alongside the gland it already captures; honestly narrow — abstains on every other hormone, which the source never ties to a blood-glucose trigger | NCI SEER Training Modules (authoritative; see `insulin-glucagon-trigger.adj`'s header) |
| `biology/` | plant tropism → the stimulus it responds to (phototropism → light, gravitropism → gravity, thigmotropism → touch) | Wikipedia "Tropism" (consensus) |
| `biology/` | insect body region → what it bears / its function (head → eyes_antennae_and_mouthparts, thorax → legs_and_wings, abdomen → digestion_and_reproduction) | UF/IFAS EDIS Entomology (authoritative) |
| `biology/` | mitosis phase → its defining event (prophase → chromatin_forms_chromosomes, metaphase → chromosomes_line_up, anaphase → chromatids_separate, telophase → nuclear_membrane_forms) | NCI SEER Training (authoritative) |
| `biology/` | mitosis phase → its position in the cycle as a NUMBER (`mitosis_phase_order`, prophase → 1 … telophase → 4), the same source's ordering restated as a bindable fact | NCI SEER Training (authoritative) |
| `biology/` | **DERIVED, not looked up, CROSS-DIRECTORY composition** — each mitotic phase's position in the cycle as an ORDINAL WORD, not just a number (`mitosis_phase_ordinal_position(phase, ordinal)`), by a `rule` bridging the new `mitosis_phase_order` table (`biology/`) to the already-shipped `ordinal_number` table (`mathematics/`) via a relative `../mathematics/ordinal-numbers.adj` import — grounds "anaphase is the THIRD phase of mitosis"; honest abstention on `interphase` (not one of the 4 ordered mitotic phases tabled); the FIRST biology-domain entry in this ordinal-bridge pattern | NCI SEER Training + standard English ordinal-number convention (authoritative/consensus; see `mitosis-phase-ordinal-position.adj`'s header) |
| `biology/` | cell-division process → how many daughter cells it produces (`cell_division_daughter_cells(process, count)`, mitosis → 2, meiosis → 4) — a numeric-cell table like `geology/mineral-hardness.adj`; a genuinely NEW library, distinct from the `mitosis-phases.adj` family (meiosis is a different process, not another phase of mitosis); honest abstention on `binary_fission` (the prokaryotic division process, not tabled here) | NIH National Human Genome Research Institute "Genetics Glossary" (authoritative; see `cell-division-daughter-cells.adj`'s header) |
| `biology/` | cell-division process → the genetic outcome of its daughter cells (`cell_division_genetic_outcome(process, outcome)`, mitosis → genetically_identical, meiosis → haploid) — a sibling to `cell-division-daughter-cells.adj`, decoding the OTHER clause of each defining sentence that table's header already quotes "for honesty" but its count-only schema doesn't capture; same abstention boundary on `binary_fission` | NIH National Human Genome Research Institute "Genetics Glossary" (authoritative; see `cell-division-genetic-outcome.adj`'s header) |
| `biology/` | monarch butterfly life-cycle stage → its position in the cycle as a NUMBER (`monarch_life_stage(stage, order)`, egg → 1, larva → 2, pupa → 3, adult → 4) — the same numbered-cycle shape `earth-science/water-cycle.adj` established, applied to a biological process; honest abstention on `nymph` (the incomplete-metamorphosis term) | USDA Forest Service "Monarch Butterfly Biology" (authoritative) |
| `biology/` | rainforest layer (top to bottom) → a one-fact description of it (`rainforest_layer(layer, description)`, emergent → tallest_trees_dominate_skyline, canopy → deep_treetop_vegetation_layer, understory → dark_humid_layer_below_canopy, forest_floor → darkest_layer_hard_for_plants_to_grow) — picked via the mandatory full-tree-grep-before-scoping check, confirming zero prior coverage | National Geographic Education "Rain Forest" (consensus; see `rainforest-layer.adj`'s header) |
| `biology/` | animal → the biome it lives in (`animal_habitat(animal, biome)`, polar_bear → arctic, bactrian_camel → desert, giraffe → grassland) — a sibling library to the already-shipped `animal-homes.adj` (built structures like hive/nest/burrow), but a genuinely different axis (biome/environment type), confirmed distinct via the mandatory full-tree-grep-before-scoping check | National Geographic (consensus; see `animal-habitat.adj`'s header) |
| `biology/` | familiar animal → its vertebrate CLASS (`animal_class(animal, class)`, 18 rows across mammal/bird/amphibian/reptile/fish — extended this cycle from 8 to 18 rows using material already quoted in the table's own header, e.g. fox/rabbit/bandicoot/quoll/koala for mammal, cassowary/hummingbird for bird, lizard/crocodile for reptile, ray for fish); honest abstention on `bat` (a real mammal, deliberately excluded as a beginner-surprising borderline case) | Australian Museum (authoritative; see `animal-classes.adj`'s header) |
| `biology/` | mammal → whether it is introduced or marsupial (`mammal_origin(animal, origin)`, cat/fox/rabbit → introduced, kangaroo/bandicoot/quoll/koala → marsupial) — a sibling to `animal-classes.adj`, decoding a second fact already sitting unused inside two of that table's own header quotes, alongside the vertebrate class it already captures; covers the full seven-mammal domain with no abstention | Australian Museum (authoritative; see `mammal-origin.adj`'s header) |
| `biology/` | classic plant tropism → the environmental stimulus it responds to (`tropism_stimulus(tropism, stimulus)`, 12 rows — extended this cycle from 5 to 12 using material already named in the article's own "Types of tropism" list, e.g. phototropism/gravitropism/thigmotropism/hydrotropism/chemotropism/aerotropism/electrotropism/heliotropism/magnetotropism/selenotropism/thermotropism/traumatotropism → light/gravity/touch/water/chemicals/wind/electric_field/sun_direction/magnetic_fields/moon_direction/temperature/wounding); honest abstention on `inotropism` (a real term on the same page, but a MUSCLE's drug response, not a plant tropism) | Wikipedia "Tropism" (consensus; see `plant-tropisms.adj`'s header) |
| `biology/` | photosynthesis input → the specific role it plays (`plant_need(need, role)`, sunlight → excites_chlorophyll_electrons, water → split_for_oxygen_and_electrons, carbon_dioxide → combined_to_make_glucose) — picked via the mandatory full-tree-grep-before-scoping check, confirming zero prior coverage | Washington State University "Ask Dr. Universe" (consensus; see `plant-need.adj`'s header) |
| `biology/` | frog life-cycle stage → its position in the cycle as a NUMBER (`frog_life_stage(stage, order)`, egg → 1, tadpole → 2, frog → 3) — a sibling library to the already-shipped `monarch-life-cycle.adj`, the same numbered-cycle shape applied to a different organism; honest abstention on `adult` (the source's own heading says "frog," not "adult") | National Geographic Kids UK "The Frog Life Cycle for Kids" (consensus; see `frog-life-cycle.adj`'s header) |
| `biology/` | animal → the one survival adaptation it is known for (`animal_adaptation(animal, adaptation)`, arctic_fox → camouflage, groundhog → hibernation, canada_goose → migration) — each row independently sourced from a different nationalgeographic.com animal-facts page, picked via the mandatory full-tree-grep-before-scoping check, confirming zero prior coverage | National Geographic (consensus; see `animal-adaptation.adj`'s header) |
| `biology/` | animal → the one survival adaptation it is known for (`animal_survival_adaptation(animal, adaptation)`, bats → echolocation, polar_bear → insulation, poison_dart_frog → warning_coloration) — a sibling library to `animal-adaptation.adj` with a DELIBERATELY different predicate name and a genuinely different set of animals/adaptations, each row independently sourced from a different nationalgeographic.com animal-facts page | National Geographic (consensus; see `animal-survival-adaptation.adj`'s header) |
| `biology/` | flowering-plant life-cycle stage → its position in the cycle as a NUMBER (`plant_life_stage(stage, order)`, seed → 1, germination → 2, seedling → 3) — a sibling library to `monarch-life-cycle.adj`/`frog-life-cycle.adj`, the same numbered-cycle shape applied to a different organism; the FIRST Ducksters citation in this stdlib; honest abstention on `flowering` (a real later stage the source describes, but not one of these three tabled here) | Ducksters "Flowering Plants" (Biology for Kids) (consensus; see `plant-life-cycle.adj`'s header) |
| `physics/` | simple machine → everyday example | NASA |
| `physics/` | phase change → its name (melting, freezing, …) | LibreTexts (consensus) |
| `physics/` | energy form → defining token (chemical → bonds, thermal → heat) | EIA (energy.gov) |
| `physics/` | common force → everyday example NASA uses (gravity → waterfall, tension → ropes) | NASA GSFC Swift (authoritative) |
| `physics/` | named wave → its family (sound → mechanical, radio → electromagnetic) | NASA Science (authoritative) |
| `physics/` | temperature reference point → the value NIST fixes it at (water_boils_celsius → 100, absolute_zero_kelvin → 0) | NIST (authoritative) |
| `physics/` | magnetic pole pairing → interaction (like_poles → repel, opposite_poles → attract) | NASA Heliophysics Education (authoritative) |
| `physics/` | Additive primary colors of light — red → white, green → white, blue → white (the three RGB primaries that combine to make white light) | HyperPhysics, Georgia State University (authoritative) |
| `physics/` | Basic parts of a simple electric circuit → the role each performs (e.g. `battery` → `provides_dc_power`, `wire` → `carries_current`, `switch` → `opens_or_closes`) | MIT K-12 Maker "Circuit Basics and Components" (consensus) |
| `physics/` | common energy source → whether renewable or nonrenewable (solar → renewable, coal → nonrenewable, nuclear → nonrenewable) | U.S. EIA Energy Explained (authoritative) |
| `physics/` | perceived sound property → physical wave quantity it corresponds to (pitch → frequency, loudness → amplitude, timbre → waveform) | Physics LibreTexts (consensus) |
| `physics/` | optical element → how it acts on parallel light (convex_lens → converges_light, concave_lens → diverges_light, convex_mirror → diverges_light) | OpenStax University Physics / College Physics via Physics LibreTexts (consensus) |
| `physics/` | mirror type → the sign of its focal length, per the same OpenStax sentence (`mirror_focal_length_sign(mirror, sign)`, concave_mirror → positive, convex_mirror → negative) — a sibling to `lens-types.adj`, decoding a fact already sitting unused in the mirror rows' own header quotes; honest abstention on convex_lens/concave_lens (no sign convention stated for lenses in the cited spans) | OpenStax College Physics via Physics LibreTexts (consensus; see `mirror-focal-length-sign.adj`'s header — same source `lens-types.adj` already cites) |
| `physics/` | optical element → where its focal point sits, per the same OpenStax sentences (`optic_focal_point_location(optic, location)`, convex_lens → opposite_side_of_the_lens, concave_mirror → same_side, convex_mirror → behind_the_mirror) — a second sibling to `lens-types.adj`, decoding the location half of three already-cited spans; distinguishes a real focal point (convex_lens, concave_mirror) from a virtual one (convex_mirror); honest abstention on concave_lens (no focal-point location stated in its cited span) | OpenStax University Physics / College Physics via Physics LibreTexts (consensus; see `optic-focal-point-location.adj`'s header — same source `lens-types.adj` already cites) |
| `physics/` | heat-transfer mode → how it moves heat (conduction → direct_contact, convection → motion_of_gasses_and_liquids, radiation → light_waves) | NASA Next Gen STEM (authoritative) |
| `physics/` | heat-transfer mode → the everyday example the same NASA sentence gives (`heat_transfer_example(mode, example)`, conduction → hot_chocolate_cup, convection → warm_air_rising, radiation → sunlight_on_skin) — a sibling to `heat-transfer.adj`, decoding the parenthetical `(e.g., ...)` half of the same already-cited sentence; covers the full three-mode domain with no abstention among the three | NASA Next Gen STEM "Thermal Energy Transfer" Educator Notes (authoritative; see `heat-transfer-example.adj`'s header — same source `heat-transfer.adj` already cites) |
| `physics/` | EM-spectrum band → everyday use NASA names (radio → radio_stations, infrared → night_vision, x_ray → teeth) | NASA Imagine the Universe! (authoritative) |
| `physics/` | EM-spectrum band → who/what emits it, per the same already-cited NASA sentences (`band_emitter(band, emitter)`, ultraviolet → sun, infrared → skin_and_heat_objects, visible → fireflies) — a sibling to `em-spectrum.adj`, decoding the emitter half of three already-cited spans; honest abstention on radio/microwave/x_ray/gamma_ray (no emitter stated for those bands) | NASA Imagine the Universe! (authoritative; see `band-emitter.adj`'s header — same source `em-spectrum.adj` already cites) |
| `physics/` | Newton's law number → short name NASA labels it (1 → inertia, 2 → force, 3 → action_reaction) | NASA Glenn Beginner's Guide (authoritative) |
| `physics/` | wave property → what it measures (wavelength → distance_between_identical_parts, frequency → waves_per_second, period → time_for_one_cycle) | OpenStax Physics (consensus) |
| `physics/` | friction type → context it acts in (static → at_rest, sliding → sliding_motion, rolling → spherical_object, fluid → fluid_layers) | Testbook "Types of Friction" (consensus) |
| `physics/` | light behavior → effect on light (reflection → bounces_off, refraction → changes_direction, scattering → variety_of_directions) | NASA Science (authoritative) |
| `anatomy/` | lung → number of lobes (right 3, left 2) | NIH / NCI SEER Training |
| `anatomy/` | individual lung lobe → which lung it belongs to (`lung_lobe_name(lobe, lung)`, right_upper/middle/lower_lobe → right_lung, left_upper/lower_lobe → left_lung) — a sibling to the lobe-COUNT table above, discovered via a header-revisit of that table's own already-cited StatPearls corroboration | NIH / NCBI Bookshelf StatPearls (authoritative; see `lung-lobe-names.adj`'s header) |
| `anatomy/` | brain part → every function the source states it controls (`brain_part_function(brain_part, function)`, 15 rows — extended this cycle from 6 to 15: `brainstem`'s own cited sentence always named ten autonomic functions, but only `breathing` had ever been turned into a row; added temperature_regulation/respiration/heart_rate/wake_sleep_cycles/coughing/sneezing/digestion/vomiting/swallowing as new rows sharing the same brainstem key) | NIH / NCI SEER Training + StatPearls |
| `anatomy/` | skeletal muscle → body region it is located in | Wikipedia (consensus) |
| `anatomy/` | digestive organ → primary function it performs | NIH NIDDK / NCI SEER Training |
| `anatomy/` | synovial joint type → representative example (hinge → elbow) | NIH / NLM StatPearls |
| `anatomy/` | skin layer → defining descriptor (epidermis → outermost, subcutaneous → fat) | NIH / NCI SEER Training |
| `anatomy/` | ear structure → ear region it sits in (cochlea → inner, malleus → middle, ear canal → outer) | NIH NIDCD (authoritative) |
| `anatomy/` | middle-ear ossicle → what it does to the sound signal (`ear_structure_function(structure, function)`, malleus/incus/stapes → all amplifies_sound) — a sibling to `ear-parts.adj`, decoding the action-verb clause already sitting unused in that table's own already-quoted NIDCD sentence; honest abstention on `ear_canal` (a real, already-tabled ear structure, but its own quote states no action-verb function) | NIH NIDCD "How Do We Hear?" (authoritative; see `ear-structure-function.adj`'s header) |
| `anatomy/` | hand-bone group → part of the hand it occupies (carpals → base_of_hand, metacarpals → middle_of_hand, phalanges → fingers) | InformedHealth.org / NIH NCBI Bookshelf (consensus) |
| `anatomy/` | body structure → how many the body has (`body_count(structure, count)`; EXTENDED with the hand-bone GROUP counts: carpals → 8, metacarpals → 5, phalanges → 14) — reuses the same already-cited NCBI-Bookshelf sentence `hand-bones.adj` already quotes in its own header (no new WebFetch); these three rows are honestly `consensus`-tier, matching `hand-bones.adj`'s own trust for the same source, one rung below the table's other six `authoritative` government-source rows | genome.gov (authoritative envelope) + InformedHealth.org / NIH NCBI Bookshelf (consensus; see `body-counts.adj`'s header) |
| `anatomy/` | foot-bone group → region it occupies (tarsals → heel_and_ankle, metatarsals → midfoot, phalanges → toes) | Wikipedia "Metatarsal bones" (consensus) |
| `anatomy/` | vertebral-column region → vertebra count (cervical → 7, thoracic → 12, lumbar → 5, sacral → 5, coccygeal → 4) | NCBI Bookshelf / StatPearls "Vertebral Column" (consensus) |
| `anatomy/` | eye part → function the source states (cornea → bends_light, retina → turns_light_into_signals, optic_nerve → carries_signals_to_brain) | NIH National Eye Institute (authoritative) |
| `anatomy/` | eye part → a descriptive property of it (`eye_part_property(part, property)`, cornea → dome_shaped, iris → colored_part_of_eye, retina → light_sensitive_layer) — a sibling to `eye-parts.adj`, decoding a parenthetical/descriptive clause already sitting unused in that table's own already-quoted NEI sentences; honest abstention on `lens` (a real, already-tabled eye part, but its own quote states only a function, no descriptive property) | NIH National Eye Institute (authoritative; see `eye-part-property.adj`'s header) |
| `anatomy/` | tooth part → its role/location (enamel → outer_surface, dentin → beneath_enamel, cementum → covers_roots) | MedlinePlus / NIH NCBI Bookshelf (authoritative) |
| `anatomy/` | respiratory part → function it performs (trachea → main_airway, alveoli → gas_exchange, diaphragm → contracts_inspiration) | NIH / NCI SEER Training (authoritative) |
| `anatomy/` | kidney / urinary part → what it is or does (renal_cortex → outer_region, renal_pelvis → collects_urine, ureter → carries_urine_to_bladder) | NIH NIDDK / NCI SEER Training (authoritative; medulla row Wikipedia consensus) |
| `anatomy/` | heart valve → the two chambers/vessels it separates (tricuspid → right_atrium_and_right_ventricle, aortic → left_ventricle_and_aorta) | NCI SEER Training (authoritative) |
| `anatomy/` | heart valve → its physiological kind (`valve_kind(valve, kind)`, tricuspid/mitral → atrioventricular, pulmonary/aortic → semilunar) — a sibling to `heart-valves.adj`, decoding the classifying adjective already sitting unused in that table's own already-quoted NCI SEER sentences; honest abstention on `eustachian` (a real cardiac valve name, but not one of the four valves this table covers) | NCI SEER Training (authoritative; see `valve-kind.adj`'s header) |
| `anatomy/` | heart chamber → the named vessel(s)/valve(s) it connects to (`heart_chamber_vessel(chamber, vessel)`, right_atrium → superior_vena_cava/inferior_vena_cava, left_ventricle → aortic_valve) — a sibling to `heart-chambers.adj`, decoding the named-vessel clause already sitting unused in that table's own already-quoted StatPearls sentences; a genuinely different axis from `heart-valves.adj`'s valve-keyed table (confirmed via a schema-duplication check); honest abstention on `septum` (a real cardiac structure, but not one of the four chambers) | NIH / NLM StatPearls (authoritative; see `heart-chamber-vessel.adj`'s header) |
| `anatomy/` | long-bone region → what it is / where it sits (diaphysis → shaft, epiphysis → tip_of_bone, metaphysis → between_diaphysis_and_epiphysis) | NIH NCBI StatPearls (authoritative) |
| `geology/` | Mohs reference mineral → whole-number hardness (talc → 1, quartz → 7, diamond → 10) | NPS "Mohs Hardness Scale" (authoritative) |
| `geology/` | Earth's internal layer → physical state, in the source's own words (crust → rigid, mantle → semi_solid, outer_core → liquid, inner_core → solid) | USGS "This Dynamic Earth" (authoritative) |
| `geology/` | Earth's internal layer → thickness in kilometers (`earth_layer_thickness(layer, thickness_km)`, mantle → 2900, outer_core → 2200, inner_core → 1250) — a sibling to `earth-layers.adj`, decoding figures already sitting unused in that table's own header and provenance block; honest abstention on `crust` (the cited span states only "very thin," no figure to decode) | USGS "This Dynamic Earth" (authoritative; see `earth-layer-thickness.adj`'s header) |
| `geology/` | basic rock-type class → how it forms (`rock_type(rock, formation_process)`, igneous → crystallized_molten_rock, sedimentary → deposited_weathered_material, metamorphic → heat_and_pressure_transformation) — a THREE-different-source-page table, unlike its single-source `earth-layers.adj` sibling | USGS "What are igneous/sedimentary/metamorphic rocks?" FAQ pages (authoritative; see `rock-type.adj`'s header) |
| `geology/` | for sedimentary and metamorphic rock, each individual material or agent the same USGS sentence lists (`rock_type_formation_component(rock, component)`, sedimentary → pre_existing_rocks / pieces_of_once_living_organisms, metamorphic → high_heat / high_pressure / hot_mineral_rich_fluids) — a sibling to `rock-type.adj`, decoding each "or"/comma-listed item as its own row instead of one compound `formation_process` atom; honest abstention on igneous, whose cited span names only one material | USGS "What are sedimentary/metamorphic rocks?" FAQ pages (authoritative; see `rock-type-formation-component.adj`'s header — same source `rock-type.adj` already cites) |
| `geology/` | fossil formation type → what it actually is (`fossil_formation_type(type, description)`, amber → preserved_in_hardened_tree_sap, cast_or_mold → impression_of_a_living_organism, permineralization → mineral_deposits_form_a_cast_of_the_organism) | Ducksters "Earth Science for Kids: Fossils" (consensus; see `fossil-formation-type.adj`'s header) |
| `meteorology/` | Beaufort wind force number → the name the source gives it (0 → calm, 6 → strong_breeze, 12 → hurricane) | NWS Beaufort Wind Scale (authoritative) |
| `meteorology/` | precipitation type → defining physical form (snow → ice_crystals, sleet → frozen_raindrops, hail → balls_of_ice) | NOAA National Weather Service (authoritative) |
| `meteorology/` | precipitation type → minimum particle diameter in mm (`precipitation_min_diameter(precip, min_diameter_mm)`, rain → 0.5, hail → 5) — a sibling to `precipitation-types.adj`, decoding spans already sitting unused in that table's own provenance block; honest abstention on the three types (snow/sleet/freezing_rain) whose cited spans give no diameter figure | NOAA NWS Glossary (authoritative; see `precipitation-minimum-diameter.adj`'s header) |
| `meteorology/` | for hail, the second descriptive term the same NWS Glossary sentence lists (`precipitation_alternate_form(precip, form)`, hail → irregular_pellets) — a sibling to `precipitation-types.adj`/`precipitation-minimum-diameter.adj`, decoding the "or"-listed second term as its own row instead of one compound `form` atom; honest abstention on rain/snow/sleet/freezing_rain, whose cited spans name only one descriptive term | NOAA NWS Glossary (authoritative; see `precipitation-alternate-form.adj`'s header — same source `precipitation-types.adj` already cites) |
| `meteorology/` | for hail, the originating cloud type the same NWS Glossary sentence names (`precipitation_source_cloud(precip, cloud)`, hail → cumulonimbus) — a sibling to `precipitation-types.adj`/`precipitation-minimum-diameter.adj`/`precipitation-alternate-form.adj`, decoding the sentence's cloud-source clause as its own row; honest abstention on rain/snow/sleet/freezing_rain, whose cited spans name no originating cloud | NOAA NWS Glossary (authoritative; see `precipitation-source-cloud.adj`'s header — same source `precipitation-types.adj` already cites) |
| `meteorology/` | for freezing rain, the numeric freeze threshold in °F an NWS sentence states (`precipitation_freeze_threshold_f(precip, temperature_f)`, freezing_rain → 32) — a sibling to `precipitation-types.adj`, decoding a numeric figure already sitting unused in that table's own header; honest abstention on rain/snow/sleet/hail, whose cited spans state no numeric freeze threshold | NOAA NWS (authoritative; see `precipitation-freeze-threshold.adj`'s header — same source `precipitation-types.adj` already cites) |
| `meteorology/` | Saffir-Simpson hurricane category → the damage descriptor NHC uses (1 → some_damage, 3 → devastating_damage, 5 → catastrophic_damage) | NOAA/NHC Saffir-Simpson Hurricane Wind Scale (authoritative) |
| `meteorology/` | Saffir-Simpson hurricane category → the specific well-built-home structural effect NHC describes (`hurricane_home_damage(category, home_damage_effect)`, 5 → total_roof_failure_and_wall_collapse) — a sibling to `hurricane-categories.adj`, decoding spans already sitting unused in that table's own provenance block; unlike the parent table's descriptor column, all five effects are distinct | NOAA/NHC Saffir-Simpson Hurricane Wind Scale (authoritative; see `hurricane-category-home-damage.adj`'s header) |
| `meteorology/` | weather instrument → the quantity it measures (`weather_instrument(instrument, quantity)`, anemometer → wind_speed, barometer → atmospheric_pressure, hygrometer → humidity) — a DIFFERENT "observation and measurement" axis from `chemistry/measuring-tools.adj` (lab tools, not weather instruments) | NOAA "Build Your Own Weather Station" (authoritative) |
| `meteorology/` | cloud type → the weather it indicates (`cloud_type(cloud, weather_indication)`, cirrus → approaching_warm_front, cumulonimbus → heavy_rain_thunderstorm, stratus → light_rain_drizzle_or_none) — NOT an instrument-measurement fact like its `weather-instruments.adj`/`ocean-observing-instruments.adj` siblings, but a cloud-appearance-to-weather-forecast fact | NWS Louisville "Cloud Classification" (authoritative; see `cloud-type.adj`'s header) |
| `meteorology/` | for cirrus and stratus clouds, each individual weather signal the same NWS sentences list (`cloud_signal(cloud, signal)`, cirrus → approaching_warm_front / upper_level_jet_streak, stratus → precipitation_free / light_precipitation / drizzle) — a sibling to `cloud-type.adj`, decoding each "or"-listed signal as its own row instead of one compound `weather_indication` atom; honest abstention on cumulonimbus, whose cited span names only one signal | NWS Louisville "Cloud Classification" (authoritative; see `cloud-signal.adj`'s header — same source `cloud-type.adj` already cites) |
| `environment/` | **RANGE table** — AQI value → EPA level of concern, keyed by band minimum (0 → good, 51 → moderate, 101 → unhealthy_for_sensitive_groups, 301 → hazardous, open top band) | EPA AirNow "AQI Basics" (authoritative) |
| `environment/` | AQI level of concern → the color the source's own truth table names for it (`aqi_category_color(category, color)`, good → green, unhealthy_for_sensitive_groups → orange, hazardous → maroon) — a sibling to `air-quality-index.adj`, decoding a color word already sitting unused in each row's own already-quoted per-row provenance; covers the full six-category domain with no abstention | EPA AirNow "AQI Basics" (authoritative; see `aqi-category-color.adj`'s header — same source `air-quality-index.adj` already cites) |
| `geography/` | common landform → defining descriptor (mountain → projects_above_surroundings, plateau → flat_elevated, canyon → deep_narrow) | USGS Feature Type Thesaurus (authoritative) |
| `geography/` | landform → the second, structural feature the SAME USGS thesaurus span also states (`landform_secondary_feature(landform, feature)`, valley → contains_stream_with_outlet, plateau → bounded_by_abrupt_descent, canyon → continuous_slope_at_bottom) — a sibling to `landforms.adj`, decoding a structural clause already sitting unused in each row's own already-quoted header span; honest abstention on mountain and plain, whose own cited spans state only the single descriptor already captured by the parent table | USGS Feature Type Thesaurus (authoritative; see `landform-secondary-feature.adj`'s header — same source `landforms.adj` already cites) |
| `geography/` | globe reference line → what it marks (equator → zero_degrees_latitude, prime_meridian → zero_degrees_longitude, tropic_of_cancer → northernmost_sun_overhead) | NOAA National Ocean Service / NESDIS (authoritative) |
| `geography/` | equator/prime meridian → the hemisphere pair each one divides the Earth into (`reference_line_hemisphere_split(line, hemispheres)`, equator → northern_southern, prime_meridian → eastern_western) — a sibling to `reference-lines.adj`, decoding a second fact already sitting unused in that table's own `source`/`cites` fields for two of its six lines; honest abstention on the two tropics and two polar circles, whose own cited spans state a latitude limit or polar ring, not a hemisphere split | NOAA National Ocean Service (authoritative; see `reference-line-hemisphere-split.adj`'s header — same source `reference-lines.adj` already cites) |
| `geography/` | ocean basin → the depth superlative the SAME NOAA sentence also states (`ocean_is_deepest(ocean, superlative)`, pacific → deepest) — a sibling to `oceans.adj`, decoding a second fact already sitting unused in that table's own `source` field's opening sentence; honest abstention on atlantic, indian, southern, and arctic, whose own cited span states only a size rank, never a depth claim | NOAA National Ocean Service (authoritative; see `ocean-deepest.adj`'s header — same source `oceans.adj` already cites) |
| `language/` | Greek letter → its 1-based position in the alphabet (alpha → 1, gamma → 3, pi → 16, omega → 24) | Wikipedia "Greek alphabet" (consensus) |
| `language/` | Latin letter → its International Morse code pattern as dot/dash word-atoms (s → dot_dot_dot, o → dash_dash_dash, e → dot) | ITU-R M.1677-1 via Wikipedia "Morse code" (consensus) |
| `language/` | **DERIVED, not looked up** — which pairs of words RHYME (`rhymes_with(word1, word2)`), by a `rule` combining a `word_family` table (the "-an" family's core CVC members: pan, fan, ran, man, tan, van) with the general word-family principle — no single source states "pan rhymes with fan" directly | Reading Rockets "Meet the Word Families" (consensus; see `word-families.adj`'s header) |
| `language/` | word → how many syllables it has (`syllable_count(word, count)`), as demonstrated by a classroom syllable-segmentation technique (peanut → 2, pencil → 2, sunset → 2, laptop → 2) — a genuinely different literacy sub-skill from rhyme-family derivation, grounding CCSS RF.K.2.b | Reading Rockets "Phonological and Phonemic Awareness: In Practice" (consensus; see `syllable-count.adj`'s header) |
| `language/` | word → the two syllables it breaks into (`syllable_segmentation(word, syllable_one, syllable_two)`, peanut → pea/nut, pencil → pen/cil, sunset → sun/set, laptop → lap/top) — a TWELFTH literacy sub-skill, a sibling to `syllable-count.adj` (same four words, this time carrying the actual parts instead of just a count) | Reading Rockets "Phonological and Phonemic Awareness: In Practice" (consensus; see `syllable-segmentation.adj`'s header) |
| `language/` | two syllables → the word they blend into (`syllable_blending(syllable_one, syllable_two, word)`, lap/top → laptop) — a THIRTEENTH literacy sub-skill, the exact OPPOSITE direction from `syllable-segmentation.adj` (composing syllables into a word rather than decomposing one) | Reading Rockets "Phonological and Phonemic Awareness: In Practice" (consensus; see `syllable-blending.adj`'s header) |
| `language/` | word → its beginning sound / phoneme (`initial_sound(word, sound)`, bell → b, bike → b, boy → b), the site's own canonical phoneme-identity example — a THIRD literacy sub-skill, grounding CCSS RF.K.2.d | Reading Rockets "Reading 101 for Parents: Phonological and Phonemic Awareness" (consensus; see `initial-sound.adj`'s header) |
| `language/` | word → its onset/rime split (`onset_rime(word, onset, rime)`, sleep → sl/eep, blast → bl/ast, map → m/ap, tape → t/ape) — a FOURTH literacy sub-skill, grounding CCSS RF.K.2.c (blend/segment onsets and rimes); extended from 2 to 4 rows via a fresh WebFetch of Reading Rockets' "In Practice" module | Reading Rockets "Tuning In to the Sounds in Words" + "In Practice" module (consensus; see `onset-rime.adj`'s header) |
| `language/` | changing one sound in a word to form a new word (`phoneme_substitution(original_word, original_sound, new_sound, new_word)`, make/m → bake/b) — the FIFTH literacy sub-skill, completing CCSS RF.K.2's five named parts, grounding RF.K.2.e | Reading Rockets "Phonological and Phonemic Awareness: In Practice" (consensus; see `phoneme-substitution.adj`'s header) |
| `language/` | removing one sound from a word to form a new word (`phoneme_deletion(original_word, removed_sound, new_word)`, bike/k → by) — a SIXTH literacy sub-skill, phoneme deletion, distinct from substitution | Reading Rockets "Phonological and Phonemic Awareness: In Practice" (consensus; see `phoneme-deletion.adj`'s header) |
| `language/` | substituting a whole SYLLABLE (not a single phoneme) to form a new word (`syllable_substitution(original_word, new_word, changed_position)`, suntan → sunset, second syllable) — a SEVENTH literacy sub-skill, distinct from the phoneme-level substitution/deletion tables | Reading Rockets "Phonological and Phonemic Awareness: In Practice" (consensus; see `syllable-substitution.adj`'s header) |
| `language/` | removing one whole SYLLABLE (not a single phoneme) from a word to form a new word (`syllable_deletion(original_word, removed_syllable, new_word)`, pencil/cil → pen) — an EIGHTH literacy sub-skill, the syllable-level analogue of `phoneme-deletion.adj` | Reading Rockets "Phonological and Phonemic Awareness: In Practice" (consensus; see `syllable-deletion.adj`'s header) |
| `language/` | blending three separate sounds together to say a word (`phoneme_blending(sound_one, sound_two, sound_three, word)`, s/o/p → soap) — a NINTH literacy sub-skill, the OPPOSITE direction from `phoneme-deletion.adj`/`phoneme-substitution.adj` (composing sounds into a word rather than decomposing one) | Reading Rockets "Phonological and Phonemic Awareness: In Practice" (consensus; see `phoneme-blending.adj`'s header) |
| `language/` | combining two separate sounds together to say a word (`phoneme_addition(sound_one, sound_two, word)`, i/s → ice) — a TENTH literacy sub-skill, the narrowest sibling of `phoneme-blending.adj` (same direction, distinct two-sound arity) | Reading Rockets "Phonological and Phonemic Awareness: In Practice" (consensus; see `phoneme-addition.adj`'s header) |
| `language/` | the three sounds a word breaks into (`phoneme_segmentation(word, sound_one, sound_two, sound_three)`, feet → f/ee/t) — an ELEVENTH literacy sub-skill, the exact OPPOSITE direction from `phoneme-blending.adj` (decomposing a word into sounds rather than composing sounds into a word) | Reading Rockets "Phonological and Phonemic Awareness: In Practice" (consensus; see `phoneme-segmentation.adj`'s header) |
| `language/` | compound word → why it makes a good beginner multisyllable-spelling example (`compound_word_spelling_example(word, teaching_use)`, catfish/hotdog/playground/yellowtail → beginner_multisyllable_spelling) — the FIRST literacy slice to move beyond CCSS RF.K.2 into a SPELLING pattern | Reading Rockets "How Spelling Supports Reading" (consensus; see `compound-word-spelling-example.adj`'s header) |
| `language/` | word → its syllable type (`silent_e_word(word, syllable_type)`, wake/whale/while/yoke/yore/rude/hare → vce_long_vowel) — ANOTHER spelling pattern beyond CCSS RF.K.2, the "silent e" / "magic e" (VCe) pattern | Reading Rockets "Six Syllable Types" (consensus; see `silent-e-word.adj`'s header) |
| `language/` | VCe syllable pattern → its alternate name (`syllable_type_alias(syllable_type, alias)`, vce_long_vowel → magic_e) — a sibling to `silent-e-word.adj`, decoding the alias clause already sitting unused in that table's own already-quoted source sentence; honest abstention on `closed_syllable` (a real syllable type the same "Six Syllable Types" source article covers, but not one silent-e-word.adj tables) | Reading Rockets "Six Syllable Types" (consensus; see `syllable-type-alias.adj`'s header) |
| `language/` | word → its r-controlled vowel digraph (`r_controlled_vowel_word(word, pattern)`, barn/corn/fern/bird/curl → ar/or/er/ir/ur) — a phonics pattern ("bossy r") beyond CCSS RF.K.2 | University of Florida Literacy Institute (authoritative; see `r-controlled-vowel-word.adj`'s header) |
| `language/` | fable → its own narrator-stated moral (`fable_moral(fable, moral)`, tortoise_and_the_hare/shepherds_boy_and_the_wolf/boy_and_the_filberts → their own stated lessons) — the FIRST literacy slice grounding a whole-text comprehension artifact rather than a word-level phonics/spelling fact | George Fyler Townsend's translation of Aesop's Fables via Project Gutenberg (authoritative; see `fable-moral.adj`'s header) |
| `language/` | vocabulary word → its meaning as revealed by a worked context-clue example sentence (`vocabulary_in_context(word, meaning)`, ornithology/frugivorous/inconspicuous → their own cited meanings) | Reading Rockets "Using Context Clues to Understand Word Meanings" (consensus; see `vocabulary-in-context.adj`'s header) |
| `language/` | regular past-tense verb → which of three sounds its -ed ending is pronounced with (`past_tense_ed_sound(word, sound)`, walked/lived/wanted → t_sound/d_sound/id_sound) — a phonics pattern beyond CCSS RF.K.2, distinct from the spelling-pattern (`silent-e-word.adj`, `r-controlled-vowel-word.adj`) and whole-text (`fable-moral.adj`) slices already shipped | 7ESL "Pronunciation of ED: Past Tense Pronunciation for Regular Verbs" (consensus; see `past-tense-ed-sound.adj`'s header) |
| `language/` | the /id/ past-tense -ed sound → the extra pronunciation effect it has (`past_tense_ed_sound_effect(sound, effect)`, id_sound → adds_a_whole_syllable) — a sibling to `past-tense-ed-sound.adj`, decoding the effect clause already sitting unused in that table's own already-quoted /id/ rule; honest abstention on `t_sound` (a real, already-tabled -ed sound, but its own rule states no comparable effect) | 7ESL "Pronunciation of ED: Past Tense Pronunciation for Regular Verbs" (consensus; see `past-tense-ed-sound-effect.adj`'s header) |
| `language/` | regular plural noun → which of three sounds its -s/-es ending is pronounced with (`plural_s_sound(word, sound)`, hats/dogs/boxes → s_sound/z_sound/iz_sound) — a sibling phonics pattern to `past-tense-ed-sound.adj` | Speakspeak "Pronunciation of 's' and 'es' plural endings" (consensus; see `plural-s-sound.adj`'s header) |
| `language/` | common idiom → what it actually means (`idiom_meaning(idiom, meaning)`, 23 rows — extended this cycle from 3 to 23, e.g. piece_of_cake/break_the_ice/under_the_weather/let_the_cat_out_of_the_bag/throw_in_the_towel → very_easy_to_do/start_a_conversation/feeling_slightly_ill/reveal_a_secret_by_mistake/give_up) — a figurative-language skill beyond CCSS RF.K.2, picked via the mandatory full-tree-grep-before-scoping check confirming zero prior coverage | Oxford International English "30 Useful English Idiomatic Expressions & Their Meanings" (consensus; see `idiom-meaning.adj`'s header) |
| `language/` | common word → every synonym its Wiktionary Synonyms line names (`synonym(word, synonym)`, 17 rows across 3 words — extended this cycle from 3 to 17 using material already quoted in this table's own header when it originally shipped, e.g. happy → cheerful/content/delighted/elated/exultant/glad/joyful/jubilant/merry) — a sibling library to the already-shipped `opposites.adj` (antonyms), same source family and trust tier; the first extension needing zero new WebFetch to DISCOVER extra values, mirroring `kingdoms.adj`'s single-to-many-valued-per-key shape | English Wiktionary "Synonyms" lines (consensus; see `synonyms.adj`'s header) |
| `language/` | common word → every sound-alike its Wiktionary Homophones line names (`homophone(word, sound_alike)`, 5 rows — extended this cycle from 3 to 5 using material already quoted in this table's own header when it originally shipped, there → their/theyre, to → too/two, flower → flour) — a sibling library to the already-shipped `opposites.adj`/`synonyms.adj`, same source family, trust tier, and header-only zero-new-WebFetch extend technique | English Wiktionary "Homophones" lines (consensus; see `homophones.adj`'s header) |
| `language/` | example sentence → which of the four grammatical sentence types it is (`sentence_type(example, type)`, bears_dont_eat_when_they_hibernate/what_is_your_next_class/sweep_the_floor_before_you_mop_it/myra_had_the_baby → declarative/interrogative/imperative/exclamatory) — picked via the mandatory full-tree-grep-before-scoping check, confirming zero prior coverage | Grammarly "4 Types of Sentences to Know, With Examples" (consensus; see `sentence-type.adj`'s header) |
| `language/` | example word → which part of speech it is, in a sentence that shows it doing that job (`part_of_speech(word, category)`, queen/run/brown/quietly/against → noun/verb/adjective/adverb/preposition) — picked via the mandatory full-tree-grep-before-scoping check, confirming zero prior coverage; extended from 3 to 5 rows using the same already-cited Grammarly page, which covers all 8 parts of speech; honest abstention on `pronoun`/`conjunction`/`interjection` (each real parts of speech the same page covers, but none has one clean standalone single-fact example sentence) | Grammarly "The 8 Parts of Speech" (consensus; see `part-of-speech.adj`'s header) |
| `language/` | contraction → the two-word phrase it stands for (`contraction(word, expansion)`, 16 rows — extended this cycle from 3 to 16 using the source's own "Negative Contractions" table, e.g. arent/couldnt/hasnt/shouldnt/wouldnt → are_not/could_not/has_not/should_not/would_not) — picked via the mandatory full-tree-grep-before-scoping check, confirming zero prior coverage; honest abstention on genuinely ambiguous contractions like `hes` (he has / he is) from the source's separate "Common Contractions" table | Grammarly "What Are Contractions in Writing?" (consensus; see `contraction.adj`'s header) |
| `language/` | example noun → which of the three possessive-noun categories its possessive form falls into (`possessive_noun(word, category)`, dog/bottles/geese → singular_possessive/plural_possessive/irregular_possessive) — picked via the mandatory full-tree-grep-before-scoping check, confirming zero prior coverage | Grammarly "Possessive Nouns: How to Use Them, With Examples" (consensus; see `possessive-noun.adj`'s header) |
| `language/` | common simile → what it actually means (`simile_meaning(simile, meaning)`, 15 rows — extended this cycle from 3 to 15, e.g. as_brave_as_a_lion/like_a_needle_in_a_haystack/as_free_as_a_bird/as_strong_as_an_ox/as_hungry_as_a_horse → extremely_courageous/very_difficult_to_find/free_or_unrestricted/very_strong/extremely_eager_to_eat) — a sibling figurative-language library to `idiom-meaning.adj`, picked via the mandatory full-tree-grep-before-scoping check, confirming zero prior coverage | Grammarly "Simile: Definition and Examples" (consensus; see `simile-meaning.adj`'s header) |
| `language/` | common word-morphology prefix → what it actually means (`prefix_meaning(prefix, meaning)`, un_/re_/dis_/non_/pre_/inter_ → negation_or_absence/doing_again/negation_or_reversal/not_or_negation/happening_before/among_between) — a genuinely new topic distinct from `metric-prefixes.adj` (metric UNIT prefixes), introduces a new trailing-underscore atom-label convention for prefix position; extended from 3 to 6 rows using the same already-cited source page, which defines roughly seventy prefixes in total | Grammarly "Prefixes: Definition and Examples" (consensus; see `prefix-meaning.adj`'s header) |
| `earth-science/` | cloud → which altitude deck it belongs to (`cloud_altitude(cloud, altitude)`, cirrus/cirrostratus/cirrocumulus → high, altostratus/altocumulus → middle, stratus/cumulus → low) — extended from 4 to 7 rows using this table's OWN already-quoted source sentence, which had already named all seven clouds; distinct from the sibling `meteorology/cloud-type.adj`, which answers a different question (what weather does a cloud indicate?) from the same NWS page's separate sentences | NWS Louisville "Cloud Classification" (authoritative; see `cloud-types.adj`'s header) |
| `biology/` | freshwater lake/pond zone → what it actually is (`pond_zone(zone, description)`, littoral_zone/limnetic_zone/profundal_zone → close_to_the_shore/open_and_well_lit_area_of_a_freestanding_body_of_fresh_water/deep_zone_located_below_the_range_of_effective_light_penetration) — picked via the mandatory full-tree-grep-before-scoping check, confirming zero prior coverage; distinct from the sibling `oceanography/ocean-zones.adj`, which names three OCEAN depth zones from a completely different body of water and organizing question; honest abstention on `benthic_zone` (a real freshwater-zone term, but not one of these three tabled here) | Wikipedia "Littoral zone" / "Limnetic zone" / "Profundal zone" (consensus; see `pond-zone.adj`'s header) |
| `biology/` | biological kingdom → every example organism the source lists for it (`kingdom_example(kingdom, example)`, 23 rows across 5 kingdoms — extended this cycle from 5 to 23 (one per kingdom) using the same already-cited page's own "Examples:" lines, which name several organisms per kingdom, e.g. animalia → humans/birds/crustaceans/sponges, protista → amoebas/diatoms/dinoflagellates/ciliates/slime_molds/single_celled_algae) — the first extension in this stdlib to go from single-valued to many-valued per key rather than adding new keys, the reverse shape of the many-to-one pattern `flame-colors.adj` already established; honest abstention on `virus` (not placed in any of these kingdoms) | Science Notes "Kingdoms of Life in Biology" (consensus; see `kingdoms.adj`'s header) |
| `biology/` | familiar animal → the name of its baby (`animal_baby(animal, baby)`, 24 rows — extended this cycle from 7 to 24 using the SAME already-cited Wikipedia "List of animal names" table, which names dozens more familiar animals, e.g. bear/cheetah/lion/tiger/wolf → cub, deer → fawn, elephant/whale → calf) — many-to-one on purpose (several animals genuinely share a baby word in the source), so a reverse recall on `cub` returns five animals at once; honest abstention on anything not a recorded animal | Wikipedia "List of animal names" (consensus; see `animal-babies.adj`'s header) |
| `biology/` | baby animal → its SEX-SPECIFIC name, where the source distinguishes one (`animal_baby_sex(animal, sex, baby)`, horse/male → colt, horse/female → filly) — a sibling to `animal-babies.adj`, decoding a span already sitting unused inside that table's own `source` field; honestly narrow (horse is the only animal in the shipped set with a clean sex split) | Wikipedia "List of animal names" (consensus; see `animal-baby-sex.adj`'s header) |
| `language/` | common word → every opposite (antonym) its Wiktionary Antonyms line names (`opposite(word, opposite)`, 21 rows across 7 words — extended this cycle from 7 to 21, four words' extra antonyms already sitting in this table's own header, e.g. happy → sad/blue/depressed/down/miserable/moody/morose/unhappy) — the sibling library to `synonyms.adj`, same source family, trust tier, and single-to-many-valued-per-key extension shape | English Wiktionary "Antonyms" lines (consensus; see `opposites.adj`'s header) |
| `physics/` | named wave → the wave FAMILY it belongs to, mechanical or electromagnetic (`wave_family(wave, family)`, 10 rows — extended this cycle from 7 to 10 using material already quoted in this table's own header, which always named seven electromagnetic waves in its cited sentence but had only turned four into rows: added ultraviolet/infrared/microwave) — a foundational sort a physics learner reasons UP from before wavelength/frequency/refraction; distinct from the sibling `earth-science/seismic-wave-arrival-order.adj`, which classifies seismic waves by arrival order, not this mechanical/electromagnetic family axis; honest abstention on a seismic wave (never classified by this source) | NASA Science "Electromagnetic Wave (Light Wave) vs. Mechanical Wave" (authoritative; see `wave-types.adj`'s header) |
| `physics/` | wave family → its underlying MECHANISM, per a third already-cited NASA sentence (`wave_family_mechanism(family, mechanism)`, electromagnetic → oscillations_of_electric_and_magnetic_fields, mechanical → oscillations_of_matter) — a sibling to `wave-types.adj`, keyed by FAMILY (2 rows) rather than by individual wave (10 rows), decoding a fact already sitting unused in that table's own header; honest abstention on any individual wave name (that belongs to the parent's own key set) | NASA Science "Electromagnetic Wave (Light Wave) vs. Mechanical Wave" (authoritative; see `wave-family-mechanism.adj`'s header — same source `wave-types.adj` already cites) |
| `biology/` | seed-dispersal mechanism → how it actually works (`seed_dispersal_mechanism(mechanism, description)`, barochory/ballochory/anemochory/epizoochory → uses_gravity_as_a_simple_means_of_seed_dispersal/seed_is_forcefully_ejected_by_explosive_dehiscence_of_the_fruit/seeds_float_on_the_breeze_or_flutter_to_the_ground/transported_on_the_outside_of_vertebrate_animals) — picked via the mandatory full-tree-grep-before-scoping check, confirming zero prior coverage, after exhausting the "extend an existing table" pattern across 12 not-yet-checked science tables and three non-Wikipedia sources (NPS 404, USDA Forest Service research papers, a kids'-science page) failing the clean-single-fact-sentence bar; honest abstention on `hydrochory` (water dispersal, no clean standalone sentence found) and `endozoochory` (its own defining sentence bundles in a separate tree-species-prevalence claim) | Wikipedia "Seed dispersal" (consensus; see `seed-dispersal-mechanism.adj`'s header) |
| `biology/` | named type of symbiotic relationship → what actually defines it (`symbiosis_type(type, description)`, mutualism/commensalism/parasitism → both_parties_benefit/one_organism_benefits_and_the_other_is_not_significantly_harmed_or_helped/the_parasite_benefits_while_the_host_is_harmed) — picked via the mandatory full-tree-grep-before-scoping check, confirming zero prior coverage, after exhausting the "extend an existing table" pattern across 9 not-yet-checked science tables under chemistry/physics/geology/geography/anatomy/meteorology; honest abstention on `amensalism` (a real interaction category the same article's opening paragraph also names, but its own defining sentence bundles it together with `competition` in one semicolon-joined compound sentence rather than stating one clean fact each) | Wikipedia "Symbiosis" (consensus; see `symbiosis-type.adj`'s header) |
| `language/` | classic reason an author writes something → what it actually is (`author_purpose(purpose, description)`, persuade/inform/entertain → convince_the_reader_of_the_merits_of_a_particular_point_of_view/enlighten_the_readership_about_a_real_world_topic/keep_things_as_interesting_as_possible) — picked via the mandatory full-tree-grep-before-scoping check, confirming zero prior coverage, after exhausting the "extend an existing table" pattern across 12 not-yet-checked literacy tables this window; honest abstention on `describe` (a real fourth purpose the same article also names, but its defining sentence uses a photograph comparison rather than the same parallel pattern the three tabled here share, and the classic PIE mnemonic this table grounds names only these three) | LiteracyIdeas "The Author's Purpose: Ultimate Guide for Teachers and Students" (consensus; see `author-purpose.adj`'s header) |
| `earth-science/` | named seismic body wave → which one an earthquake sends out first (`seismic_wave_arrival_order(wave, description)`, p_wave/s_wave → are_the_first_waves_to_arrive_after_an_earthquake/are_the_next_waves_to_arrive_after_p_waves) — picked via the mandatory full-tree-grep-before-scoping check, confirming zero prior coverage; distinct from the sibling `physics/wave-types.adj`, which classifies waves by mechanical/electromagnetic FAMILY (and explicitly abstains on seismic waves itself), not this arrival-order axis; honest abstention on `surface_wave` (a real third seismic wave commonly grouped with these two, but every source checked either bundles its definition with a second distinct fact or uses a different framing than the arrival-order pattern the two tabled here share) | Cal OES News "What Are P-Waves and S-Waves?" (authoritative; see `seismic-wave-arrival-order.adj`'s header) |
| `language/` | named nonfiction text-organization structure → what it actually is (`text_structure_type(type, description)`, cause_and_effect/compare_and_contrast/description → tells_why_something_happened_and_what_happened/examines_the_similarities_and_differences_between_two_or_more_things/describes_a_topic_to_give_the_reader_a_mental_picture) — picked via the mandatory full-tree-grep-before-scoping check, confirming zero prior coverage, after exhausting the "extend an existing table" pattern across 7 not-yet-checked literacy tables this window; honest abstention on `sequence` and `problem_and_solution` (both real text structures the same article also names, but each one's own defining sentence bundles a second distinct function or component rather than stating one clean fact) | Reading Rockets "Teaching Text Structure" (consensus; see `text-structure-type.adj`'s header) |
| `geology/` | broad type of igneous rock → what actually defines it (`igneous_rock_type(type, description)`, intrusive/extrusive → solidifies_within_earth/erupted_onto_the_surface_or_into_the_atmosphere) — picked via the mandatory full-tree-grep-before-scoping check, confirming zero prior coverage; distinct from the sibling `earth-science/rock-types.adj`, which names the three ROCK-CYCLE families by formation mechanism, not this within-igneous split by cooling location; a genuinely EXHAUSTIVE two-way split (the source's own opening line states there are exactly two broad types), so honest abstention is demonstrated with `hypabyssal` (a real geological term for shallow-depth cooling, but not named or pinned to a defining sentence by this source's own two-category framework) | NPS "Igneous Rocks" (authoritative; see `igneous-rock-type.adj`'s header) |
| `geology/` | for extrusive igneous rock, each individual location the same NPS sentence lists (`igneous_rock_type_eruption_location(type, location)`, extrusive → surface / atmosphere) — a sibling to `igneous-rock-type.adj`, decoding the extrusive row's "or"-listed locations as their own rows instead of one compound `description` atom; honest abstention on intrusive, whose cited span names only one location | NPS "Igneous Rocks" (authoritative; see `igneous-rock-type-eruption-location.adj`'s header — same source `igneous-rock-type.adj` already cites) |
| `language/` | common English capitalization rule → what it actually requires (`capitalization_rule(rule, description)`, first_word_of_sentence/pronoun_i/proper_noun → capitalize_first_letter/capitalized_anywhere_in_sentence/capitalized_regardless_of_position) — picked via the mandatory full-tree-grep-before-scoping check, confirming zero prior coverage; honest abstention on `quotation` (a real rule the source covers, but not one of these three tabled here) | Grammarly "Capitalization Rules and Examples" (consensus; see `capitalization-rule.adj`'s header) |
| `language/` | common English superlative-adjective formation rule → what it actually requires (`superlative_adjective_rule(rule, description)`, one_syllable_adjective/one_syllable_consonant_vowel_consonant/adjective_ending_in_y → add_est_suffix/double_final_consonant_before_est/change_y_to_i_before_est) — picked via the mandatory full-tree-grep-before-scoping check, confirming zero prior coverage; honest abstention on `three_or_more_syllable_adjective` (a real rule the source covers, but only via a bullet-list fragment rather than a clean sentence, and not one of these three tabled here) | Grammarly "What Are Superlative Adjectives?" (consensus; see `superlative-adjective-rule.adj`'s header) |
| `language/` | noun type → what it actually is (`noun_type(type, definition)`, common_noun/collective_noun/abstract_noun/concrete_noun/countable_noun/uncountable_noun → generic_name_of_an_item_in_a_class_or_group/denotes_a_group_or_collection_of_people_or_things/cannot_be_perceived_by_the_senses/perceived_by_the_senses_physical_or_tangible/can_be_counted/impossible_to_count) — picked via the mandatory full-tree-grep-before-scoping check, confirming zero prior coverage; extended from 3 to 6 rows using the same already-cited Grammarly page; honest abstention on `possessive_noun` (a real noun type the source mentions and describes functionally, but for which it gives no single formal definition sentence) and on `proper_noun`/`singular_noun`/`plural_noun`/`gerund` (each real terms the same page defines, but each one's sentence bundles two distinct facts rather than stating one clean fact) | Grammarly "Nouns: Definition and Examples" (consensus; see `noun-type.adj`'s header) |
| `language/` | verb type → what it actually is (`verb_type(type, description)`, action_verb/linking_verb/auxiliary_verb/stative_verb → physical_action_or_activity_that_can_be_seen_or_heard/connects_the_subject_to_other_words_in_the_sentence/changes_another_verbs_tense_voice_or_mood/describes_a_subjects_state_or_feeling) — picked via the mandatory full-tree-grep-before-scoping check, confirming zero prior coverage; extended from 3 to 4 rows using the same already-cited Grammarly page; honest abstention on `transitive_verb` (a real verb category the source covers, but only through worked examples rather than a single formal definition sentence) and on `modal_auxiliary_verb`/`phrasal_verb` (each real terms the same page defines, but each sentence bundles a second distinct fact rather than stating one clean fact) | Grammarly "Verbs: Definition and Examples" (consensus; see `verb-type.adj`'s header) |
| `language/` | pronoun type → what it actually is (`pronoun_type(type, description)`, 4 rows — extended this cycle from 3 to 4, personal_pronoun/indefinite_pronoun/interrogative_pronoun/distributive_pronoun → changes_form_based_on_grammatical_person/refers_generally_without_specific_identification/used_in_questions/refers_to_nouns_as_individual_elements_of_larger_groups) — picked via the mandatory full-tree-grep-before-scoping check, confirming zero prior coverage; honest abstention on `relative_pronoun` (whose own explanation takes three sentences), `intensive_pronoun` (whose sentence is a comparison that never states its function), and `reciprocal_pronoun` (whose sentence names a closed word-list rather than a function) | Grammarly "Pronouns: Definition and Examples" (consensus; see `pronoun-type.adj`'s header) |
| `language/` | Morse code system → the international standard that specifies it (`morse_code_standard(code_system, standard_id)`, international_morse_code → itu_r_m_1677_1) — a sibling to `morse-code.adj`, decoding the standard-ID fact already sitting unused in that table's own already-quoted source sentence; honest abstention on `american_morse_code` (a real, distinct historical Morse variant, but not named in the cited span) | Wikipedia "Morse code" (consensus; see `morse-code-standard.adj`'s header) |
| `language/` | Morse code system → who proposed the code it derives from, and when (`morse_code_origin(code_system, originator, year)`, international_morse_code → friedrich_gerke, 1848) — a sibling to `morse-code.adj`/`morse-code-standard.adj`, decoding the origin fact already sitting unused in that SAME already-quoted source sentence; honest abstention on `american_morse_code` (same abstention target as the sibling) | Wikipedia "Morse code" (consensus; see `morse-code-origin.adj`'s header) |
| `language/` | Greek alphabet variant → the historical period by which it became standard (`greek_alphabet_standardization(alphabet_name, standardized_by_period)`, euclidean_alphabet → fourth_century_bc) — a sibling to `greek-alphabet.adj`, decoding the standardization-period fact already sitting unused in that table's own already-quoted source sentence; honest abstention on `attic_alphabet` (a real, distinct Greek alphabet variant, but not named in the cited span) | Wikipedia "Greek alphabet" (consensus; see `greek-alphabet-standardization.adj`'s header) |
| `physics/` | **DERIVED, not looked up, CROSS-FILE composition** — which phase change heating or cooling CAUSES (`causes_phase_change(direction, name)`), by a `rule` combining a new `heat_direction` table with the ALREADY-SHIPPED sibling `phase_change_name` table — no single source states "heating causes melting" directly, but the general heat-direction principle and the specific change-name mapping are each independently citable | LibreTexts "9.3.1: Melting, Freezing, and Sublimation" (consensus; see `heat-causes-phase-change.adj`'s header — same source `states-of-matter.adj` already cites) |
| `physics/` | **DERIVED, not looked up, CROSS-FILE composition** — which named force CAUSES acceleration of its everyday example (`force_causes_acceleration(force, example)`), by a `rule` combining Newton's second law's own general statement with the ALREADY-SHIPPED sibling `force_example` table — reuses two already-verified NASA citations, zero new sourcing work | NASA Beginner's Guide to Aeronautics (authoritative; see `force-causes-acceleration.adj`'s header — same sources `newton-laws.adj`/`forces.adj` already cite) |
| `earth-science/` | **DERIVED, not looked up, CROSS-DIRECTORY composition** — which numbered month a meteorological season STARTS in (`season_start_month_number(season, number)`), by a `rule` bridging the already-shipped `season_start_month` table (`earth-science/`) to the already-shipped `month_number` table (`calendar/`) via a relative `../calendar/months.adj` import — the first cross-DIRECTORY (not just cross-file, same-directory) `rule` composition in this library; the companion query file lives at the package root so the CLI's import sandbox (rooted at the top-level program's own directory) resolves the `../` hop, mirroring `mathematics/word-problems.adj`'s established cross-directory pattern | NOAA NCEI + ISO 8601 month numbering (authoritative/consensus; see `season-start-month-number.adj`'s header) |
| `astronomy/` | **DERIVED, not looked up, CROSS-DIRECTORY composition** — each planet's position from the Sun as an ORDINAL WORD, not just a number (`planet_ordinal_position(planet, ordinal)`), by a `rule` bridging the already-shipped `planet_order` table (`astronomy/`) to the already-shipped `ordinal_number` table (`mathematics/`) via a relative `../mathematics/ordinal-numbers.adj` import — grounds "Earth is the THIRD planet"; honest abstention on Pluto (not one of the 8 major planets tabled); the companion query file lives at the package root so the CLI's import sandbox resolves the `../` hop, the same pattern `earth-science/season-start-month-number.adj` established | NASA Solar System Planets + standard English ordinal-number convention (authoritative/consensus; see `planet-ordinal-position.adj`'s header) |
| `astronomy/` | **DERIVED, not looked up, CROSS-DIRECTORY composition** — each Moon phase's place in the lunar cycle as an ORDINAL WORD, not just a number (`moon_phase_ordinal_position(phase, ordinal)`), by a `rule` bridging the already-shipped `moon_phase_order` table to the already-shipped `ordinal_number` table (`mathematics/`) — the SAME number-to-ordinal-word bridge pattern `planet-ordinal-position.adj` already established, applied to a second already-shipped `astronomy/` table; honest abstention on "eclipse" (not one of the 8 lunar phases tabled) | NASA Moon Phases + standard English ordinal-number convention (authoritative/consensus; see `moon-phase-ordinal-position.adj`'s header) |
| `music/` | movable-do solfège syllable → its 1-based major-scale degree (do → 1, sol → 5, ti → 7) | Wikipedia "Solfège" (consensus) |
| `music/` | solfège syllable → the alternate spelling or name the SAME source span also states (`solfege_alt_name(syllable, alt_name)`, do → doh, ti → si) — a sibling to `solfege.adj`, decoding an alt-name parenthetical already sitting unused in that table's own `source` field; honest abstention on the other five syllables, whose own cited span states no alternate | Wikipedia "Solfège" (consensus; see `solfege-alt-name.adj`'s header — same source `solfege.adj` already cites) |
| `oceanography/` | ocean-observing instrument → the quantity it measures or detects (`ocean_instrument(instrument, quantity)`, tide_gauge → sea_level, hydrophone → underwater_sound, sonar → distance_to_object) — a THIRD "observation and measurement" axis after `chemistry/measuring-tools.adj` (lab tools) and `meteorology/weather-instruments.adj` (weather instruments) | NOAA oceanservice.noaa.gov "facts" pages (authoritative; see `ocean-observing-instruments.adj`'s header) |
| `oceanography/` | ocean-observing instrument → a SECOND quantity it determines (`ocean_instrument_secondary_quantity(instrument, secondary_quantity)`, sonar → orientation_of_object) — a sibling to `ocean-observing-instruments.adj`, decoding the second quantity already sitting unused in that table's own already-quoted sonar sentence; honest abstention on `tide_gauge` (a real tabled instrument, but whose cited span names no second quantity) | NOAA "Sonar" oceanservice.noaa.gov/facts/sonar.html (authoritative; see `ocean-instrument-secondary-quantity.adj`'s header) |
| `oceanography/` | ocean depth zone → its position in the ordered-by-depth sequence as a NUMBER (`ocean_zone(zone, order)`, sunlight_zone → 1, twilight_zone → 2, midnight_zone → 3) — a sibling library to `biology/plant-life-cycle.adj`/`frog-life-cycle.adj`, the same numbered-ordered-sequence shape applied to a different domain; honest abstention on `abyssal_zone` (a real deeper zone the source names, but not one of these three tabled here) | Woods Hole Oceanographic Institution "Ocean Zones" (consensus; see `ocean-zones.adj`'s header) |
| `oceanography/` | ocean depth zone → the approximate depth in meters it reaches down to (`ocean_zone_depth(zone, max_depth_meters)`, sunlight_zone → 200, twilight_zone → 1000, midnight_zone → 4000) — a sibling to `ocean-zones.adj`, decoding depth figures already sitting unused in that table's own header truth table | Woods Hole Oceanographic Institution "Ocean Zones" (consensus; see `ocean-zone-depth.adj`'s header) |
| `oceanography/` | ocean depth zone → its alternate scientific name (`ocean_zone_scientific_name(zone, scientific_name)`, midnight_zone → bathypelagic) — a sibling to `ocean-zones.adj`/`ocean-zone-depth.adj`, decoding the alternate-name clause already sitting unused in the SAME already-quoted midnight-zone sentence; honest abstention on `sunlight_zone`/`twilight_zone` (real zones the source names, but whose cited spans give no alternate scientific name) | Woods Hole Oceanographic Institution "Ocean Zones" (consensus; see `ocean-zone-scientific-name.adj`'s header) |
| `oceanography/` | ocean-current category → the physical driver that creates it (`ocean_current_driver(current_type, driver)`, tidal_currents → tides, wind_driven_currents → wind, thermohaline_circulation → density_differences_from_temperature_and_salinity) — a sibling library to `ocean-zones.adj`, a different oceanography axis (what moves the water, not how deep light reaches); honest abstention on `gulf_stream` (a real, specific named current the source mentions, but not one of these three driver categories tabled here) | NOAA National Ocean Service "What is a current?" (authoritative; see `ocean-current-drivers.adj`'s header) |
| `oceanography/` | ocean-current category → where it occurs in the ocean (`ocean_current_surface_position(current_type, position)`, wind_driven_currents → at_or_near_the_oceans_surface) — a sibling to `ocean-current-drivers.adj`, decoding the positional fact already sitting unused in that table's own already-quoted wind-driven-currents sentence; honest abstention on `thermohaline_circulation` (a real tabled current type, but whose cited span names no position) | NOAA National Ocean Service "What is a current?" (authoritative; see `ocean-current-surface-position.adj`'s header) |
| `oceanography/` | ocean-current category → where it is strongest (`ocean_current_strongest_location(current_type, location)`, tidal_currents → near_the_shore_and_in_bays_and_estuaries_along_the_coast) — a sibling to `ocean-current-drivers.adj`, decoding the location fact already sitting unused in that table's own already-quoted tidal-currents sentence; honest abstention on `thermohaline_circulation` (a real tabled current type, but whose cited span names no location) | NOAA National Ocean Service "What is a current?" (authoritative; see `ocean-current-strongest-location.adj`'s header) |
| `physics/` | phase-change direction → the OLDER or ALTERNATE word the same source also uses for it (`phase_change_alt_name(change, alt_name)`, solid_to_liquid → fusion, liquid_to_solid → solidification, liquid_to_gas → boiling) — a sibling to `states-of-matter.adj`, decoding a second word already sitting unused in that table's own header and provenance for three of its six directions; honest abstention on `gas_to_liquid`/`solid_to_gas`/`gas_to_solid` (condensation/sublimation/deposition have no alternate name in the cited spans) | LibreTexts "9.3.1: Melting, Freezing, and Sublimation" (consensus; see `phase-change-alt-name.adj`'s header — same source `states-of-matter.adj` already cites) |
| `physics/` | simple machine → the SECOND everyday example the same NASA source lists for it (`simple_machine_alt_example(machine, alt_example)`, lever → scissors, inclined_plane → stairs, wedge → knife, wheel_and_axle → clock, pulley → water_well) — a sibling to `simple-machines.adj`, decoding a second example already sitting unused in that table's own header and provenance for five of its six machines; honest abstention on `screw` (its first-listed "example" is the word screw itself, and its second, bottle caps, is already `simple-machines.adj`'s primary row) | NASA Next Gen STEM "Simple Machines" classroom-connection educator notes (authoritative; see `simple-machine-alt-example.adj`'s header — same source `simple-machines.adj` already cites) |
| `physics/` | simple machine → the FUNCTION clause the same NASA source states before its example (`simple_machine_function(machine, function)`, lever → moves_object_over_fulcrum, screw → fastens_objects_together) — a sibling to `simple-machines.adj` and `simple-machine-alt-example.adj`, decoding the leading "uses X to do Y" clause already sitting unused in the SAME six sentences both tables already quote; covers the full six-machine domain with no abstention among the six (including screw, which the alt-example sibling must skip) | NASA Next Gen STEM "Simple Machines" classroom-connection educator notes (authoritative; see `simple-machine-function.adj`'s header — same source `simple-machines.adj` already cites) |
| `physics/` | EM band → a SECOND everyday use the same NASA source names for it (`band_secondary_use(band, application)`, microwave → astronomy, x_ray → airport_security) — a sibling to `em-spectrum.adj`, decoding a second use already sitting unused in that table's own provenance for two of its seven bands; honest abstention on the other five bands, whose own cited spans name only one use each | NASA Goddard Space Flight Center "Imagine the Universe!" (authoritative; see `band-secondary-use.adj`'s header — same source `em-spectrum.adj` already cites) |
| `physics/` | named energy form → which of the source's two families, potential or kinetic, it belongs to (`energy_form_family(form, family)`, chemical/mechanical/nuclear/gravitational → potential, radiant/thermal/motion/electrical → kinetic) — a sibling to `energy-forms.adj`, re-verified LIVE via WebFetch this cycle against the same already-cited EIA page's own "potential energy or kinetic energy" split and two-heading structure; honest abstention on `sound` (a real form the same EIA page also lists under Kinetic, but not one of `energy-forms.adj`'s eight tabled forms) | U.S. EIA "Forms of energy" (authoritative; see `energy-form-family.adj`'s header — same source `energy-forms.adj` already cites) |
| `transportation/` | steady traffic-signal color → what it means to a driver (`traffic_light_meaning(color, meaning)`, red → stop, yellow → warned, green → proceed) | FHWA Manual on Uniform Traffic Control Devices (MUTCD), Section 4D.04 (authoritative) |
| `transportation/` | steady CIRCULAR GREEN signal → each individual movement the SAME MUTCD sentence enumerates as permitted (`green_signal_permitted_movement(movement)`, straight_through, turn_right, turn_left, u_turn) — a sibling to `traffic-lights.adj`, decoding four distinct movements already sitting unused inside the single atomic value `proceed`; keyless table, since all four are simultaneously true for a green signal | FHWA MUTCD, Section 4D.04 (authoritative; see `green-signal-permitted-movement.adj`'s header — same source `traffic-lights.adj` already cites) |
| … | *geography, physical constants, …* | *(expanding)* |

Formulas and laws (Newton's `F = ma`, the ideal gas law `PV = nRT`, area/volume, …) are grown
in `adj-formula-stdlib/<subject>/` using the `formula` construct — simple ones first, growing
more complex — and are consumed the same way.

## Consuming a library

```adj
import "chemistry/elements.adj"
? atomic_number(oxygen, $Z)          % 8, cited to its source
```

Because a `table` row lowers to a relation whose value is a number ([`ADJ-TABLES`](../../ADJ-TABLES.md)),
a recalled fact **composes into a formula** — a looked-up atomic number, side count, or
conversion factor flows straight into arithmetic. That is the bridge from **recall** to
**compute**, and the reason facts and formulas belong in one standard library.

## When no single source states the fact directly: DERIVE it with a `rule`

Every library above is a `table`: one source states the answer, and recall is a plain lookup.
Not every curriculum fact has that shape — `geometry/shape-composition.adj` is the first library
here where no single source states the target conclusion as a citable sentence (checked several
MathWorld pages; a formal mathematics reference does not bother spelling out something this
elementary). Rather than block on a missing verbatim quote, it DERIVES the fact with a `rule`,
composing cited PRIMITIVES:

```adj
rule { head: triangle_decomposition_count($Shape, 2)
       when: quadrilateral_property($Shape, $Property)
       source "…the general definitions this rule's own reasoning is built on…"
       locator "https://mathworld.wolfram.com/…"
       trust consensus }
```

The bar is **human-auditability**, not a verbatim match: a query's `steps` trail names BOTH the
rule's own citation (the general definitions) and the underlying fact's citation (the specific
shape's defining property), so a human can independently check each link in the inference — and
if one is wrong, point at exactly which step and fix it, rather than the whole content family
being unrepresentable. `trust consensus` (not `authoritative`) is the honest tier here: the
CONCLUSION is this library's own derivation from quoted material, not a sentence any single
source states outright.
