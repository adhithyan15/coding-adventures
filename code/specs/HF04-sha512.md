# HF04 — SHA-512

## Overview

SHA-512 is the 64-bit sibling of SHA-256 in the SHA-2 family. It
produces a 512-bit (64-byte) digest using 8 × 64-bit state words and
80 rounds. On 64-bit platforms, SHA-512 is often faster than SHA-256
because it processes 128-byte blocks (vs 64-byte) and uses native
64-bit arithmetic.

SHA-512 also serves as the basis for SHA-384 (truncated) and SHA-512/256
(truncated with different IVs), though those variants are separate.

## Algorithm

Same structure as SHA-256 but with wider words:
- **State:** 8 × 64-bit words (vs 32-bit)
- **Block size:** 1024 bits / 128 bytes (vs 512 / 64)
- **Rounds:** 80 (vs 64)
- **Round constants:** 80 × 64-bit (cube roots of first 80 primes)
- **Length field:** 128-bit big-endian (vs 64-bit)
- **Rotation amounts differ** from SHA-256 (tuned for 64-bit words)

### Rotation/Shift amounts
- Σ0(x) = ROTR(28,x) XOR ROTR(34,x) XOR ROTR(39,x)
- Σ1(x) = ROTR(14,x) XOR ROTR(18,x) XOR ROTR(41,x)
- σ0(x) = ROTR(1,x) XOR ROTR(8,x) XOR SHR(7,x)
- σ1(x) = ROTR(19,x) XOR ROTR(61,x) XOR SHR(6,x)

## Interface Contract

Same API pattern as SHA-256:

| Function | Signature | Description |
|----------|-----------|-------------|
| `sha512` | `(data: bytes) -> bytes` | Returns 64-byte digest. |
| `sha512_hex` | `(data: bytes) -> string` | Returns 128-char lowercase hex. |
| `SHA512Hasher` | class/struct | Streaming hasher. |
| `.update` | `(data: bytes) -> self` | Feed bytes. |
| `.digest` | `() -> bytes` | Get 64-byte result. |
| `.hex_digest` | `() -> string` | Get 128-char hex. |
| `.copy` | `() -> SHA512Hasher` | Deep copy. |

## Test Vectors (FIPS 180-4)

```
sha512("") = cf83e1357eefb8bdf1542850d66d8007d620e4050b5715dc83f4a921d36ce9ce
             47d0d13c5d85f2b0ff8318d2877eec2f63b931bd47417a81a538327af927da3e
sha512("abc") = ddaf35a193617abacc417349ae20413112e6fa4e89a97ea20a9eeee64b55d39a
                2192992a274fc1a836ba3c23a3feebbd454d4423643ce80e2a9ac94fa54ca49f
```

## Language-Specific Notes

- **Lua/Perl:** Require 64-bit integer arithmetic. Lua 5.3+ has native
  64-bit integers. Perl has 64-bit on most platforms but needs care
  with bitwise operations.
- **Go/Rust/Swift:** Native 64-bit, straightforward.
- **TypeScript:** Uses BigInt or manual 64-bit arithmetic (two 32-bit words).

## Package Matrix

Same 9 languages as SHA-256, in `sha512/` directories.

**Dependencies:** None.
