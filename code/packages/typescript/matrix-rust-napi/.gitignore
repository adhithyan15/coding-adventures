# Per workspace convention (see affine2d, etc.), package-lock is
# regenerated by `npm install` and not committed.
package-lock.json

# The .node addon is built + copied here by BUILD on each run.
# Regenerated per build; never committed.
matrix_rust_napi.node

# Standard
node_modules/
dist/
coverage/
