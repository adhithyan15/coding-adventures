### Bounded CI-gate selection match work

- Added exact-at-limit and over-limit language-neutral fixtures for a fixed
  50,000,000-unit CI-gate path-match preflight, Unicode-scalar accounting,
  zero matcher calls on rejection, stable fail-closed diagnostics, and a
  memoized Go globstar engine that bounds repeated recursive states.
