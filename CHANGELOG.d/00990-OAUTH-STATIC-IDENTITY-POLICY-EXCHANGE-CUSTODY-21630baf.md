### OAuth static identity policy exchange custody

- Composed exact-schema static ID-token policy loading into the retained
  client-secret Authorization Code exchange, audited identity verification,
  and opaque credential-custody path. Registered authentication, endpoint,
  client, verification context, nonce, provider, and trace bindings now fail
  before policy, secret, transport, clock, verifier, or storage effects, and
  only the final opaque revision is released.

