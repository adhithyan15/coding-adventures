# JVM Bytecode Disassembler

Reusable JVM bytecode decoding and version metadata for the JVM toolchain.

This package owns opcode tables, exact-version method metadata, and the
disassembled instruction representation that the simulator executes.
