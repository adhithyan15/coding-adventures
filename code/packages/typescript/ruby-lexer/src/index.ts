/**
 * Ruby Lexer — tokenizes Ruby source code using the grammar-driven approach.
 *
 * Usage:
 *
 *     import { tokenizeRuby } from "@coding-adventures/ruby-lexer";
 *
 *     const tokens = tokenizeRuby("x = 1 + 2");
 */

export { tokenizeRuby } from "./tokenizer.js";
