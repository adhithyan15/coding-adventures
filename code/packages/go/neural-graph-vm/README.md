# neural-graph-vm

Reference Go compiler and scalar forward interpreter for NN00 neural graph
bytecode.

The compiler consumes neural-network models backed by the generic
`multi-directed-graph` substrate.
