if [ "$(uname -s)" = "Darwin" ] && [ "$(uname -m)" = "arm64" ]; then cd native/paint_vm_metal_native && cargo build --release && cd ../.. && mkdir -p priv && { cp native/paint_vm_metal_native/target/release/libpaint_vm_metal_native.dylib priv/paint_vm_metal_native.so 2>/dev/null || cp native/paint_vm_metal_native/target/release/libpaint_vm_metal_native.so priv/paint_vm_metal_native.so; } && mix deps.get --quiet && mix compile && mix test; else echo Skipping elixir/paint_vm_metal_native on non-macOS-arm64; fi
