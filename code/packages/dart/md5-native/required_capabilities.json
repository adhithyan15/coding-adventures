{
  "$schema": "https://coding-adventures.example/schemas/required_capabilities/v1.json",
  "version": 1,
  "package": "dart/md5-native",
  "capabilities": ["ffi:call"],
  "justification": "ffi:call — uses dart:ffi DynamicLibrary.open to call into the Rust md5-native shared library (libmd5_native.so/.dylib), which wraps the pure coding_adventures_md5 crate over a C ABI. No network, filesystem, or process capabilities."
}
