{
  "$schema": "https://raw.githubusercontent.com/adhithyan15/coding-adventures/main/code/specs/schemas/required_capabilities.schema.json",
  "version": 1,
  "package": "rust/perl-bridge",
  "capabilities": [
    {
      "category": "ffi",
      "action": "load",
      "target": "libperl.*",
      "justification": "Loads the Perl shared library via extern \"C\" declarations to call the Perl C API."
    },
    {
      "category": "ffi",
      "action": "call",
      "target": "Perl C API",
      "justification": "Calls Perl C API functions (newSViv, av_push, croak, etc.) through FFI."
    }
  ],
  "justification": "FFI bridge to Perl runtime. Loads the Perl shared library and calls C API functions through raw extern \"C\" declarations."
}
