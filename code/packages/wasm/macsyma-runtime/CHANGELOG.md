# Changelog

## 0.1.1

- Validate that the runtime compiles for `wasm32-unknown-unknown` in `BUILD`.
- Document the TypeScript browser bridge package.

## 0.1.0

- Add `wasm-bindgen` bindings for the Rust MACSYMA runtime JSON facade.
