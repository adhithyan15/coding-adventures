[package]
name = "image-codec-tiff"
version = "0.1.0"
edition = "2021"
description = "TIFF image codec — foundation for RAW camera formats (CR2, NEF, ARW, DNG)"
license = "MIT"
repository = "https://github.com/coding-adventures/coding-adventures"

[dependencies]
pixel-container = { path = "../pixel-container" }
paint-instructions = { path = "../paint-instructions" }
image-raw-pipeline = { path = "../image-raw-pipeline" }
