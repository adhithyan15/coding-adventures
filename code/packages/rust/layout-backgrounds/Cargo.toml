[package]
name = "layout-backgrounds"
version = "0.1.0"
edition = "2021"
description = "Reusable background-layer and corner-radius contracts over Layout IR."

[dependencies]
layout-ir = { path = "../layout-ir" }
