{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://coding-adventures.dev/schemas/der-asn1-v1-consumers.json",
  "title": "Target registry for established-lane consumers of the typed DER value v1 corpus",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "profile", "fixture", "case_count", "error_id_count", "established_lane_count", "consumers"],
  "properties": {
    "schema_version": { "const": 1 },
    "profile": { "const": "x690-der-asn1-values-v1-portable-consumers" },
    "fixture": { "const": "code/specs/fixtures/der-asn1-v1/cases.json" },
    "case_count": { "const": 122 },
    "error_id_count": { "const": 22 },
    "established_lane_count": { "const": 15 },
    "consumers": { "type": "array", "minItems": 15, "maxItems": 15, "items": { "$ref": "#/$defs/consumer" } }
  },
  "$defs": {
    "repo_path": { "type": "string", "pattern": "^code/packages/[a-z]+/der[-_]asn1/[A-Za-z0-9_.@+\\/-]+$" },
    "surface_name": { "type": "string", "minLength": 3, "maxLength": 100, "pattern": "^[A-Za-z_][A-Za-z0-9_.:]*$" },
    "consumer": {
      "type": "object",
      "additionalProperties": false,
      "required": ["language", "package_root", "api_source", "fixture_test", "build_files", "capability_manifest", "surface"],
      "properties": {
        "language": { "enum": ["csharp", "dart", "elixir", "fsharp", "go", "haskell", "java", "kotlin", "lua", "perl", "python", "ruby", "rust", "swift", "typescript"] },
        "package_root": { "type": "string", "pattern": "^code/packages/[a-z]+/der[-_]asn1$" },
        "api_source": { "$ref": "#/$defs/repo_path" },
        "fixture_test": { "$ref": "#/$defs/repo_path" },
        "build_files": { "type": "array", "minItems": 1, "maxItems": 2, "uniqueItems": true, "items": { "$ref": "#/$defs/repo_path" } },
        "capability_manifest": { "$ref": "#/$defs/repo_path" },
        "surface": {
          "type": "object",
          "additionalProperties": false,
          "required": ["decoder", "element", "cursor", "limits", "error_taxonomy", "typed_values"],
          "properties": {
            "decoder": { "$ref": "#/$defs/surface_name" },
            "element": { "$ref": "#/$defs/surface_name" },
            "cursor": { "$ref": "#/$defs/surface_name" },
            "limits": { "$ref": "#/$defs/surface_name" },
            "error_taxonomy": { "$ref": "#/$defs/surface_name" },
            "typed_values": { "$ref": "#/$defs/surface_name" }
          }
        }
      }
    }
  }
}
