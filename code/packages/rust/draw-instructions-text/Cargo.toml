[package]
name = "draw-instructions-text"
version = "0.1.0"
edition = "2021"
description = "ASCII/Unicode text renderer for backend-neutral draw instructions"

[dependencies]
draw-instructions = { path = "../draw-instructions" }
