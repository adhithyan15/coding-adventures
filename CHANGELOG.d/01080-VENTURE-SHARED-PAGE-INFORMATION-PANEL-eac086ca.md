### Venture shared page-information panel

- Render retained page title, requested and final addresses, HTTP status, and
  resource diagnostics in one shared closable Mosaic panel across every
  generated Venture host.
- Preserve the typed page-information presenter effect and repair the HTML
  navigation-split emitter's current compiled-style contract so cross-host
  generation remains buildable.
