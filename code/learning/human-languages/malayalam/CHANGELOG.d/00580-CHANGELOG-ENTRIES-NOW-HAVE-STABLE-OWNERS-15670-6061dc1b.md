## Changelog entries now have stable owners — #15670

The 57-entry, 150,974-byte history now lives under `CHANGELOG.d/`, one
append-only fragment per level-2 entry. New Malayalam chapters add a new ranked
fragment instead of rewriting this shared rendered view. `CHANGELOG.md` remains
available locally through the deterministic document-shard renderer, but is no
longer tracked source.

