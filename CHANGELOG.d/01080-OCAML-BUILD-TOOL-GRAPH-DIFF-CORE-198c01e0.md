### OCaml build-tool graph and diff-selection core

- Add the first native OCaml build-tool domain as a process-free typed library
  over caller-supplied graph and changed-path snapshots.
- Consume all eight graph and eleven diff-selection neutral fixtures with
  deterministic topology, exact closures, portable Unicode-scalar globs,
  guarded boundary-digest validation, bounded match work and fixture decoding,
  and stable empty failures.
- Keep the OCaml front door and adapter explicitly missing until the later
  resolver, planner, cache, reporter, execution, and current-contract owners
  are complete.
